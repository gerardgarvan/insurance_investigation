---
phase: 86-documentation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - R/00_config.R
  - R/88_smoke_test_comprehensive.R
  - tests/generate_fixtures.R
  - tests/run_local_test.R
  - .Renviron.example
  - .gitignore
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "Developer opens .planning/PROJECT.md and sees v2.2 milestone in Shipped section with environment detection and fixture design decisions documented"
    - "Developer attempts git add .Renviron and git blocks it due to .gitignore rules"
    - "Developer opens .Renviron.example and sees commented R_TESTING_ENV override pattern"
    - "All modified scripts have documentation headers with Purpose, Inputs, Outputs, Dependencies sections"
    - "All modified scripts have inline WHY comments explaining non-obvious decisions"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "v2.2 milestone documentation in Shipped section"
      contains: "v2.2 Local Testing Infrastructure (Shipped"
    - path: ".gitignore"
      provides: ".Renviron exclusion"
      contains: ".Renviron"
    - path: ".Renviron.example"
      provides: "Override pattern documentation"
      contains: "R_TESTING_ENV=local"
    - path: "R/00_config.R"
      provides: "Environment detection with documentation headers and WHY comments"
      contains: "SECTION 0: ENVIRONMENT DETECTION"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Smoke test with documentation header"
      contains: "88_smoke_test_comprehensive.R -- Comprehensive Structural Smoke Test"
    - path: "tests/generate_fixtures.R"
      provides: "Fixture generator with documentation header"
      contains: "Generate PCORnet CDM test fixture CSVs"
    - path: "tests/run_local_test.R"
      provides: "Integration test runner with documentation header"
      contains: "run_local_test.R -- End-to-end local pipeline integration test"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "v2.2 milestone"
      via: "Shipped milestone section"
      pattern: "v2.2 Local Testing Infrastructure.*Shipped"
---

<objective>
Finalize v2.2 milestone documentation and verify quality standards compliance for all scripts modified during phases 83-85.

Purpose: Close out the v2.2 Local Testing Infrastructure milestone by updating PROJECT.md with the shipped milestone, verifying .gitignore/.Renviron.example are correct (already created in Phase 83), and ensuring all modified R scripts meet v2.0 quality standards (documentation headers, WHY comments, styler formatting, lintr compliance).

Output: Updated PROJECT.md with v2.2 in Shipped section; all v2.2 scripts verified for quality compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-environment-detection-infrastructure/83-01-SUMMARY.md
@.planning/phases/83-environment-detection-infrastructure/83-02-SUMMARY.md
@.planning/phases/84-test-fixture-design-creation/84-01-SUMMARY.md
@.planning/phases/85-testing-integration-validation/85-01-SUMMARY.md
@R/00_config.R
@R/88_smoke_test_comprehensive.R
@tests/generate_fixtures.R
@tests/run_local_test.R
@.gitignore
@.Renviron.example
@.lintr
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md to ship v2.2 milestone</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current state -- must see existing structure)
    - .planning/ROADMAP.md (phase details and success criteria for v2.2)
    - .planning/phases/83-environment-detection-infrastructure/83-01-SUMMARY.md (key decisions)
    - .planning/phases/84-test-fixture-design-creation/84-01-SUMMARY.md (key decisions)
    - .planning/phases/85-testing-integration-validation/85-01-SUMMARY.md (key decisions)
  </read_first>
  <action>
Update .planning/PROJECT.md with the following specific changes:

1. **Move "Current Milestone" section content to "Previous Milestones"**: The existing "## Current Milestone: v2.2 Local Testing Infrastructure" section (lines 85-95) must be moved to become the first entry under "## Previous Milestones" (before v2.1). Change the heading to:
   ```
   ### v2.2 Local Testing Infrastructure (Shipped 2026-06-05)
   ```

2. **Replace the Current Milestone section** with a placeholder or remove it entirely (since v2.2 is the last planned milestone). If there are future phases (81, 82, 87-89 in the unassigned section of ROADMAP), note that. Otherwise, update to indicate no active milestone.

3. **Add v2.2 Shipped summary** in the Previous Milestones section with this content:
   ```
   ### v2.2 Local Testing Infrastructure (Shipped 2026-06-05)

   **Goal:** Add environment auto-detection to R/00_config.R and create targeted test fixtures with clinical edge cases so key pipeline logic can be verified locally on Windows before deploying to HiPerGator.

   **Shipped:**
   - Environment auto-detection: IS_LOCAL flag via Sys.info() with R_TESTING_ENV env var override, conditional paths for data/cache/DuckDB, 1-thread local / SLURM-allocated production (Phase 83)
   - Infrastructure files: .gitignore for .Renviron and .duckdb, .Renviron.example template, smoke test environment validation Section 15b (Phase 83)
   - Test fixture design: FIXTURE_DESIGN.md mapping 20 patients to 11 clinical edge cases, generate_fixtures.R with 15 tribble()-based table generators (Phase 84)
   - Fixture CSV materialization: 15 PCORnet CDM fixture CSVs (8.45 KB total), all edge cases verified, git-tracked (Phase 84)
   - DuckDB integration validation: R/88 Sections 32-33 for local DuckDB table/row verification and fixture edge case assertions (Phase 85)
   - End-to-end test runner: tests/run_local_test.R with 5-step pipeline validation and 2-minute performance target (Phase 85)
   ```

4. **Add v2.2 Key Decisions** to the Key Decisions table (after the last existing row "ICD-9 201.x in HL cohort confirmation"):
   ```
   | IS_LOCAL via OS detection with env var override | Windows-only local dev in project; env var enables Linux VM testing without OS misdetection | Phase 83 |
   | tempdir() for all local cache paths | Avoids gitignore conflicts, R session cleanup automatically removes cache | Phase 83 |
   | Hand-crafted 20-patient fixtures over synthetic generator | Targeted edge case coverage (11 cases) beats statistical realism for logic testing | Phase 84 |
   | Fully-qualified DBI::/dplyr:: calls in R/88 Sections 32-33 | Avoids namespace pollution; smoke test only loads glue at top | Phase 85 |
   ```

5. **Update "Current State" section** (line 100): Change "Phase 85 complete" references to indicate v2.2 complete. The Current State paragraph should end with something like: "v2.2 complete: Environment auto-detection (IS_LOCAL flag, R_TESTING_ENV override), 20-patient test fixtures with 11 edge cases, DuckDB integration validation, end-to-end local test runner (tests/run_local_test.R)."

6. **Move the QUAL-01 requirement** from Active to Validated:
   - In Active section, change `- [ ] All new/modified scripts follow v2.0 quality standards` to `- [x] All new/modified scripts follow v2.0 quality standards (styler, lintr, checkmate, headers, smoke test updates) -- v2.2 Phase 86`

7. **Update the "Last updated" line** at the bottom to: `*Last updated: 2026-06-05 after Phase 86 complete (Documentation and Cleanup -- v2.2 milestone shipped)*`
  </action>
  <verify>
    <automated>grep -c "v2.2 Local Testing Infrastructure (Shipped" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md contains "v2.2 Local Testing Infrastructure (Shipped 2026-06-05)"
    - .planning/PROJECT.md contains "IS_LOCAL via OS detection with env var override" in Key Decisions table
    - .planning/PROJECT.md contains "tempdir() for all local cache paths" in Key Decisions table
    - .planning/PROJECT.md contains "Hand-crafted 20-patient fixtures over synthetic generator" in Key Decisions table
    - .planning/PROJECT.md contains "Fully-qualified DBI::/dplyr:: calls" in Key Decisions table
    - .planning/PROJECT.md Active section shows "[x] All new/modified scripts follow v2.0 quality standards"
    - .planning/PROJECT.md Last updated line contains "2026-06-05" and "Phase 86"
    - .planning/PROJECT.md Current State paragraph mentions "v2.2 complete"
  </acceptance_criteria>
  <done>PROJECT.md has v2.2 in Shipped section with 4 key decisions, QUAL-01 marked validated, Current State reflects v2.2 completion</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix quality standards compliance for v2.2 scripts</name>
  <files>R/00_config.R, R/88_smoke_test_comprehensive.R, tests/generate_fixtures.R, tests/run_local_test.R</files>
  <read_first>
    - R/00_config.R (full file -- check headers, WHY comments, formatting)
    - R/88_smoke_test_comprehensive.R (full file -- check headers, WHY comments, formatting)
    - tests/generate_fixtures.R (full file -- check headers, WHY comments, formatting)
    - tests/run_local_test.R (full file -- check headers, WHY comments, formatting)
    - .lintr (linting rules -- line_length_linter(150L), pipe_consistency_linter("%>%"), excluded: object_usage, return, object_length, object_name)
  </read_first>
  <action>
For each of the 4 scripts modified during v2.2, verify and fix the following v2.0 quality standards:

**Standard 1: Documentation header** (lines 1-30 area)
Each script must have a header block with:
- `# ==============================================================================`
- `# {filename} -- {one-line description}`
- `# ==============================================================================`
- `# Purpose:` section
- `# Inputs:` section (or `# Usage:` for test scripts)
- `# Outputs:` section
- `# Dependencies:` section
- `# Requirements:` section (list v2.2 requirement IDs)

Check each file. If any header section is missing, add it. The 4 files to check:
- R/00_config.R -- Should already have full header (added in v2.0). Verify Requirements line mentions ENV-01 through ENV-06.
- R/88_smoke_test_comprehensive.R -- Should already have full header. Verify Requirements line mentions TEST-01 through TEST-05.
- tests/generate_fixtures.R -- Has header but may be missing Inputs/Outputs/Dependencies/Requirements sections. Add if missing:
  ```
  # Inputs:
  #   - R/00_config.R (PCORNET_TABLES, PCORNET_PATHS, ICD_CODES, TREATMENT_CODES)
  #
  # Outputs:
  #   - 15 CSV files in tests/fixtures/ (one per PCORnet CDM table)
  #
  # Dependencies:
  #   - tibble, dplyr, readr, glue, purrr
  #   - R/00_config.R (must be sourced first)
  #
  # Requirements: FIX-01, FIX-02, FIX-03, FIX-04
  ```
- tests/run_local_test.R -- Has header but may be missing Inputs/Outputs/Dependencies sections. Add if missing:
  ```
  # Inputs:
  #   - R/00_config.R, R/01_load_pcornet.R, R/03_duckdb_ingest.R, R/88_smoke_test_comprehensive.R
  #   - tests/fixtures/ directory with 15 PCORnet CDM CSVs
  #
  # Outputs:
  #   - Console output: per-step timing, validation results, PASS/FAIL summary
  #   - Exit code 0 (success) or 1 (failure) for Rscript compatibility
  #
  # Dependencies:
  #   - tidyverse, duckdb, DBI, vroom, glue
  #   - IS_LOCAL must be TRUE (aborts otherwise)
  ```

**Standard 2: Inline WHY comments**
Scan each file for any non-obvious logic that lacks a WHY comment. In particular:
- R/00_config.R: SECTION 0 already has WHY comments (verified). Check SECTION 1b directory creation has WHY comment.
- R/88 Sections 32-33: Verify DBI::dbDisconnect(con, shutdown = TRUE) has WHY comment about Windows file locking.
- tests/generate_fixtures.R: Verify walk2() usage has WHY comment, na = "" in write_csv has WHY comment.
- tests/run_local_test.R: Verify save/restore of timing variables around R/88 has WHY comment about rm(list=ls()).

Add WHY comments where missing. Format: `# WHY {thing}: {reason}` inline or as block comment above the line.

**Standard 3: styler formatting**
All R code should follow tidyverse style (2-space indentation, spaces around operators, no trailing whitespace). Since styler cannot be run in this environment, visually inspect for:
- Consistent 2-space indentation throughout
- Spaces around `<-`, `==`, `!=`, `>=`
- Spaces after commas in function arguments
- No lines exceeding 150 characters (per .lintr)
Fix any violations found.

**Standard 4: lintr compliance**
Per .lintr config, check for:
- Line length: no lines over 150 characters
- Pipe consistency: use `%>%` not `|>` (pipe_consistency_linter)
- Other defaults from linters_with_defaults() that are not disabled
Fix any violations found.

**Standard 5: .gitignore and .Renviron.example verification**
These were created in Phase 83-02. Verify they are still correct:
- .gitignore: Must contain `.Renviron` on its own line (line 68). Must contain `*.duckdb` and `*.duckdb.wal`.
- .Renviron.example: Must contain `# R_TESTING_ENV=local` (commented). Must warn about project-root placement.
If either has drifted, fix. Otherwise, confirm no changes needed.
  </action>
  <verify>
    <automated>grep -c "# Purpose:" R/00_config.R R/88_smoke_test_comprehensive.R tests/generate_fixtures.R tests/run_local_test.R</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains "# Purpose:" in documentation header
    - R/88_smoke_test_comprehensive.R contains "# Purpose:" in documentation header
    - tests/generate_fixtures.R contains "# Purpose:" in documentation header
    - tests/generate_fixtures.R contains "# Inputs:" section
    - tests/generate_fixtures.R contains "# Outputs:" section
    - tests/generate_fixtures.R contains "# Dependencies:" section
    - tests/run_local_test.R contains "# Purpose:" in documentation header
    - tests/run_local_test.R contains "# Inputs:" section if currently missing
    - tests/run_local_test.R contains "# Outputs:" section if currently missing
    - .gitignore contains "^.Renviron$" pattern (exact line matching .Renviron)
    - .Renviron.example contains "R_TESTING_ENV=local" (commented)
    - No R file among the 4 has lines exceeding 150 characters
    - All pipe operators in the 4 files use %>% (not |>)
  </acceptance_criteria>
  <done>All 4 v2.2-modified scripts have complete v2.0 documentation headers, inline WHY comments for non-obvious logic, and pass visual styler/lintr compliance checks. .gitignore and .Renviron.example verified correct.</done>
</task>

</tasks>

<verification>
Phase 86 verification checks:

1. PROJECT.md: `grep "v2.2 Local Testing Infrastructure (Shipped" .planning/PROJECT.md` returns 1 match
2. PROJECT.md decisions: `grep "IS_LOCAL via OS detection" .planning/PROJECT.md` returns 1 match
3. PROJECT.md QUAL-01: `grep "\[x\] All new/modified scripts follow v2.0 quality standards" .planning/PROJECT.md` returns 1 match
4. .gitignore: `grep "^\.Renviron$" .gitignore` returns 1 match
5. .Renviron.example: `grep "R_TESTING_ENV=local" .Renviron.example` returns 1 match
6. Documentation headers: All 4 scripts contain "# Purpose:" in first 35 lines
7. tests/generate_fixtures.R: Contains "# Inputs:", "# Outputs:", "# Dependencies:" sections
8. tests/run_local_test.R: Contains "# Inputs:", "# Outputs:", "# Dependencies:" sections
</verification>

<success_criteria>
1. .planning/PROJECT.md has v2.2 in "Previous Milestones" as Shipped with 4 key decisions documented
2. .gitignore blocks .Renviron (already done, verified)
3. .Renviron.example shows R_TESTING_ENV override (already done, verified)
4. All 4 v2.2-modified scripts have complete documentation headers (Purpose, Inputs, Outputs, Dependencies, Requirements)
5. All 4 scripts have inline WHY comments for non-obvious logic
6. No styler or lintr violations in the 4 scripts (visual inspection)
</success_criteria>

<output>
After completion, create `.planning/phases/86-documentation-cleanup/86-01-SUMMARY.md`
</output>
