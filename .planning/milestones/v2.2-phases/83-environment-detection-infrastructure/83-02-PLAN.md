---
phase: 83-environment-detection-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 83-01
files_modified:
  - .gitignore
  - .Renviron.example
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - INFRA-02
  - INFRA-04

must_haves:
  truths:
    - ".gitignore blocks .Renviron from being committed"
    - ".gitignore blocks .duckdb files from being committed"
    - ".Renviron.example documents the R_TESTING_ENV override pattern"
    - "Smoke test validates IS_LOCAL flag existence and type"
    - "Smoke test validates local mode paths when IS_LOCAL is TRUE"
    - "Smoke test validates production mode paths when IS_LOCAL is FALSE"
    - "Smoke test validates file.path() usage in PCORNET_PATHS (no hardcoded separators)"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion rules for .Renviron, .duckdb, local test artifacts"
      contains: ".Renviron"
    - path: ".Renviron.example"
      provides: "Template documenting R_TESTING_ENV override pattern"
      contains: "R_TESTING_ENV"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Environment detection validation section"
      contains: "Environment detection"
  key_links:
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/00_config.R"
      via: "source() then IS_LOCAL flag check"
      pattern: "IS_LOCAL"
    - from: ".Renviron.example"
      to: "R/00_config.R"
      via: "R_TESTING_ENV env var consumed by Sys.getenv"
      pattern: "R_TESTING_ENV"
---

<objective>
Create infrastructure support files (.gitignore updates, .Renviron.example) and add environment detection validation to the smoke test suite.

Purpose: Protect the repo from accidental commits of .Renviron and .duckdb files, document the override pattern for developers, and ensure the environment detection from Plan 01 is validated by the existing smoke test framework.

Output: Updated .gitignore, new .Renviron.example template, updated R/88_smoke_test_comprehensive.R with environment validation section.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation/.planning/workflows/execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation/.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-environment-detection-infrastructure/83-RESEARCH.md

<interfaces>
<!-- From Plan 01: IS_LOCAL flag and CONFIG conditional paths created in R/00_config.R -->

From R/00_config.R (after Plan 01 modifications):
```r
IS_LOCAL <- if (Sys.getenv("R_TESTING_ENV") != "") {
  Sys.getenv("R_TESTING_ENV") == "local"
} else {
  Sys.info()["sysname"] == "Windows"
}

THREAD_COUNT <- if (IS_LOCAL) { 1L } else { as.integer(Sys.getenv("SLURM_CPUS_PER_TASK", unset = "16")) }

CONFIG <- list(
  data_dir = if (IS_LOCAL) { file.path("tests", "fixtures") } else { "/orange/..." },
  performance = list(num_threads = THREAD_COUNT),
  cache = list(
    duckdb_path = if (IS_LOCAL) { file.path(tempdir(), "insurance_investigation_duckdb", "pcornet_test.duckdb") } else { "/blue/.../pcornet.duckdb" }
  )
)
```

From R/88_smoke_test_comprehensive.R (current structure):
```r
# SECTION 1: SETUP (lines 1-60) — check() function, passed/failed counters
# SECTION 2-3: Config loading, [2/27] and [3/27] checks
# Sections 4-15: Decade validation, [4/27] through [28/28]
# SECTION 16: SUMMARY (lines 1229-1271) — final pass/fail count and requirement list
```

Current smoke test uses [N/28] numbering (was 27, bumped to 28 in Phase 82).
The new environment section will become [29/29].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore for environment and DuckDB files</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current contents — 66 lines, must see what's already excluded before adding)
  </read_first>
  <action>
Append a new section to the END of the existing .gitignore file (after the last line `"/blue/erin.mobley-hl.bcu/clean/"`). Do NOT modify any existing lines.

Add this block:

```gitignore

# Environment override (per-developer, never committed) -- Phase 83
.Renviron

# DuckDB database files (binary, large, environment-specific) -- Phase 83
*.duckdb
*.duckdb.wal

# Local test output artifacts (tempdir cache, not repo content) -- Phase 83
tests/fixtures/*.rds
tests/fixtures/*.duckdb
```

Note: The .Renviron exclusion prevents accidental commit of developer-specific environment overrides (Pitfall 5 from research). The *.duckdb pattern prevents any DuckDB files from being committed (they are binary and environment-specific). The tests/fixtures/ exclusions prevent derived artifacts from polluting the fixture directory (only hand-crafted CSVs should be tracked there, added in Phase 84).
  </action>
  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && Rscript -e "lines <- readLines('.gitignore'); cat('Has .Renviron:', any(grepl('^.Renviron$', trimws(lines))), '\n'); cat('Has *.duckdb:', any(grepl('\\*.duckdb$', trimws(lines))), '\n'); stopifnot(any(grepl('.Renviron', lines))); stopifnot(any(grepl('duckdb', lines))); cat('OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains a line that is exactly ".Renviron" (excluding .Renviron.example)
    - .gitignore contains "*.duckdb"
    - .gitignore contains "*.duckdb.wal"
    - .gitignore contains "tests/fixtures/*.rds"
    - .gitignore still contains all previous entries (no lines removed)
    - .gitignore still contains ".Rhistory" (first original entry, proves no truncation)
    - .gitignore still contains "/blue/erin.mobley-hl.bcu/clean/" (last original entry)
  </acceptance_criteria>
  <done>
    .gitignore updated with exclusion rules for .Renviron, .duckdb files, and local test artifacts. All previous exclusions preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .Renviron.example documenting override pattern</name>
  <files>.Renviron.example</files>
  <read_first>
    - .planning/phases/83-environment-detection-infrastructure/83-RESEARCH.md (Pattern 4: .Renviron Override Configuration section for exact template)
  </read_first>
  <action>
Create a new file `.Renviron.example` in the project root with this exact content:

```
# ==============================================================================
# .Renviron -- R Environment Configuration (project-level)
# ==============================================================================
#
# Copy this file to .Renviron in the project root (it is gitignored).
# Restart your R session after changes (Session > Restart R in RStudio).
#
# This file is read by R on startup BEFORE any scripts run.
# Values set here override auto-detection in R/00_config.R.
#
# IMPORTANT: Create .Renviron in THIS directory (project root), NOT in ~/
# A user-level ~/.Renviron would affect ALL R projects on your machine.
# ==============================================================================

# Force local testing mode (overrides OS auto-detection in R/00_config.R)
# Default behavior: Windows = local testing, Linux = HiPerGator production
# Uncomment the line below to force local mode on any OS (e.g., Linux VM):
# R_TESTING_ENV=local
```

This file documents the R_TESTING_ENV override for new developers (per INFRA-04). The .Renviron file itself is gitignored (added in Task 1), but .Renviron.example is tracked so developers know the pattern.
  </action>
  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && Rscript -e "stopifnot(file.exists('.Renviron.example')); lines <- readLines('.Renviron.example'); cat('Has R_TESTING_ENV:', any(grepl('R_TESTING_ENV', lines)), '\n'); stopifnot(any(grepl('R_TESTING_ENV=local', lines))); cat('OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - .Renviron.example file exists in project root
    - .Renviron.example contains "R_TESTING_ENV=local" (commented out)
    - .Renviron.example contains "Copy this file to .Renviron"
    - .Renviron.example contains "project root" (warns against user-level placement)
    - .Renviron.example does NOT contain any uncommented R_TESTING_ENV line (default is auto-detect)
  </acceptance_criteria>
  <done>
    .Renviron.example exists with documented R_TESTING_ENV override pattern. Commented example shows how to force local mode. Warns against user-level .Renviron placement.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add environment detection validation section to R/88 smoke test</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (full file — need to see current section numbering, SECTION 16 SUMMARY location, and the check() function signature)
    - R/00_config.R (lines 1-50 to verify IS_LOCAL and CONFIG structure after Plan 01)
  </read_first>
  <action>
Add a new environment detection validation section to R/88_smoke_test_comprehensive.R. This requires three changes:

**Change 1: Update header numbering**

In the header comment block (around line 62-63), update the version label from "v2.1" to "v2.2":
```r
message("SMOKE TEST: Comprehensive Pipeline Validation (v2.2)")
```

**Change 2: Insert new section BEFORE SECTION 16: SUMMARY**

Find the line `# SECTION 16: SUMMARY ----` (currently around line 1229). Insert the following new section BEFORE it:

```r
# ==============================================================================
# SECTION 15b: ENVIRONMENT DETECTION (Phase 83: ENV-01 through ENV-06) ----
# ==============================================================================

message("\n[29/29] Environment detection validation...")

# ENV-01: IS_LOCAL flag defined and logical
check("IS_LOCAL flag is defined", exists("IS_LOCAL"))
check("IS_LOCAL is logical type", is.logical(IS_LOCAL))

# ENV-02: R_TESTING_ENV env var readable (does not crash)
check(
  "R_TESTING_ENV env var readable (empty string if unset)",
  is.character(Sys.getenv("R_TESTING_ENV"))
)

# ENV-06: THREAD_COUNT defined and valid
check("THREAD_COUNT is defined", exists("THREAD_COUNT"))
check("THREAD_COUNT is integer >= 1", is.integer(THREAD_COUNT) && THREAD_COUNT >= 1L)

# Conditional checks based on current environment mode
if (IS_LOCAL) {
  # ENV-03: Local mode path validation
  check(
    "Local mode: data_dir points to tests/fixtures",
    grepl("tests.*fixtures", CONFIG$data_dir, ignore.case = TRUE)
  )
  check(
    "Local mode: DuckDB path in tempdir()",
    grepl(normalizePath(tempdir(), winslash = "/", mustWork = FALSE),
          normalizePath(CONFIG$cache$duckdb_path, winslash = "/", mustWork = FALSE),
          fixed = TRUE)
  )
  check(
    "Local mode: cache_dir in tempdir()",
    grepl(normalizePath(tempdir(), winslash = "/", mustWork = FALSE),
          normalizePath(CONFIG$cache$cache_dir, winslash = "/", mustWork = FALSE),
          fixed = TRUE)
  )
  check(
    "Local mode: 1 thread configured",
    CONFIG$performance$num_threads == 1
  )
} else {
  # ENV-04: Production mode path validation (safe defaults)
  check(
    "Production mode: data_dir points to /orange/",
    grepl("^/orange/", CONFIG$data_dir)
  )
  check(
    "Production mode: DuckDB path in /blue/",
    grepl("^/blue/", CONFIG$cache$duckdb_path)
  )
  check(
    "Production mode: thread count >= 1",
    CONFIG$performance$num_threads >= 1
  )
}

# INFRA-01: Validate file.path() usage in PCORNET_PATHS (no hardcoded separators)
for (table_name in names(PCORNET_PATHS)) {
  path <- PCORNET_PATHS[[table_name]]
  # Paths should not contain double-separators (// or \\) which indicate paste0 misuse
  has_double_sep <- grepl("//", path) || grepl("\\\\\\\\", path)
  check(
    glue("PCORNET_PATHS${table_name}: no hardcoded double-separators"),
    !has_double_sep
  )
}

# INFRA-03: Required directories exist (created by SECTION 1b of 00_config.R)
check("output/ directory exists", dir.exists(CONFIG$output_dir))
check("output/figures/ directory exists", dir.exists(file.path(CONFIG$output_dir, "figures")))
check("output/tables/ directory exists", dir.exists(file.path(CONFIG$output_dir, "tables")))
check("cache directory exists", dir.exists(CONFIG$cache$cache_dir))
check("DuckDB directory exists", dir.exists(CONFIG$cache$duckdb_dir))

# INFRA-04: .Renviron.example exists
check(".Renviron.example template exists", file.exists(".Renviron.example"))
```

**Change 3: Update all [N/28] counters to [N/29]**

The smoke test currently has checks numbered [1/27] through [28/28] (the count in the denominator was bumped from 27 to 28 in Phase 82 but only for section 26+). Update ALL section message() calls to use /29 as the denominator:
- `[1/27]` becomes `[1/29]`
- `[2/27]` becomes `[2/29]`
- ... through ...
- `[28/28]` becomes `[28/29]`
- The new section is `[29/29]`

Use a systematic find-and-replace: replace `/27]` with `/29]` and `/28]` with `/29]`.

**Change 4: Update SECTION 16: SUMMARY validated requirements list**

In the summary section (after `message("\nValidated requirements:")`), append these lines before the final `if (failed > 0)` block:

```r
message("  * ENV-01: IS_LOCAL auto-detection via Sys.info()")
message("  * ENV-02: R_TESTING_ENV override readable")
message("  * ENV-03: Local mode paths (tests/fixtures, tempdir)")
message("  * ENV-04: Production mode safe defaults (/orange, /blue)")
message("  * ENV-05: Startup logging (validated by sourcing 00_config.R)")
message("  * ENV-06: THREAD_COUNT configuration")
message("  * INFRA-01: file.path() usage (no hardcoded separators)")
message("  * INFRA-03: Automatic directory creation")
message("  * INFRA-04: .Renviron.example template exists")
```
  </action>
  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && Rscript R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains "Environment detection validation"
    - R/88_smoke_test_comprehensive.R contains "[29/29]"
    - R/88_smoke_test_comprehensive.R contains 'check("IS_LOCAL flag is defined"'
    - R/88_smoke_test_comprehensive.R contains 'check("IS_LOCAL is logical type"'
    - R/88_smoke_test_comprehensive.R contains "THREAD_COUNT is defined"
    - R/88_smoke_test_comprehensive.R contains "Local mode: data_dir points to tests/fixtures"
    - R/88_smoke_test_comprehensive.R contains "Production mode: data_dir points to /orange/"
    - R/88_smoke_test_comprehensive.R contains "PCORNET_PATHS$"
    - R/88_smoke_test_comprehensive.R contains "no hardcoded double-separators"
    - R/88_smoke_test_comprehensive.R contains ".Renviron.example template exists"
    - R/88_smoke_test_comprehensive.R contains "ENV-01: IS_LOCAL auto-detection"
    - R/88_smoke_test_comprehensive.R contains "INFRA-01: file.path() usage"
    - R/88_smoke_test_comprehensive.R contains "v2.2" (version bumped from v2.1)
    - Rscript R/88_smoke_test_comprehensive.R exits 0 (all checks pass)
  </acceptance_criteria>
  <done>
    Smoke test has new Section 15b validating environment detection (IS_LOCAL flag, conditional paths, thread count, PCORNET_PATHS separators, directory creation, .Renviron.example). All checks pass. Section numbering updated to [N/29]. Summary lists all ENV and INFRA requirements validated.
  </done>
</task>

</tasks>

<verification>
1. `Rscript R/88_smoke_test_comprehensive.R` exits 0 with all checks passing including new environment section
2. `.gitignore` contains ".Renviron" and "*.duckdb" entries
3. `.Renviron.example` exists with documented R_TESTING_ENV=local pattern
4. Smoke test output includes "[29/29] Environment detection validation..." line
5. Smoke test summary lists ENV-01 through ENV-06, INFRA-01, INFRA-03, INFRA-04
</verification>

<success_criteria>
- .gitignore blocks .Renviron and .duckdb files
- .Renviron.example documents the R_TESTING_ENV override for new developers
- Smoke test validates environment detection, conditional paths, thread count, path construction, directory creation, and .Renviron.example existence
- Full smoke test passes (`Rscript R/88_smoke_test_comprehensive.R` exits 0)
- No regressions: all 28 existing smoke test sections still pass
</success_criteria>

<output>
After completion, create `.planning/phases/83-environment-detection-infrastructure/83-02-SUMMARY.md`
</output>
