---
phase: 87-fix-cancer-summary-pre-post-to-include-icd9
plan: 03
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - R/56_new_tables_from_groupings.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - ICD-07
  - ICD-08

must_haves:
  truths:
    - "R/56 uses the shared is_cancer_code() from R/utils/utils_cancer.R instead of a local copy"
    - "R/56 has no local is_cancer_code() function definition"
    - "R/50 has no cancer code references requiring changes (verified, no action needed)"
    - "R/88 smoke test validates ICD9_CANCER_SITE_MAP exists and has correct entry count"
    - "R/88 smoke test validates is_cancer_code() is defined in utils_cancer.R"
  artifacts:
    - path: "R/56_new_tables_from_groupings.R"
      provides: "Drug grouping tables using shared cancer code detection"
      contains: "source.*utils_cancer"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Smoke test with ICD-9 validation checks"
      contains: "ICD9_CANCER_SITE_MAP"
  key_links:
    - from: "R/56_new_tables_from_groupings.R"
      to: "R/utils/utils_cancer.R"
      via: "source() and is_cancer_code() call"
      pattern: "is_cancer_code"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/00_config.R"
      via: "ICD9_CANCER_SITE_MAP constant validation"
      pattern: "ICD9_CANCER_SITE_MAP"
---

<objective>
Replace R/56's local is_cancer_code() with the shared utility, verify R/50 needs no changes, and update R/88 smoke test to validate ICD-9 infrastructure.

Purpose: Per D-07, R/56 must use the shared is_cancer_code() instead of its local copy. Per D-08, R/50 needs verification only (no changes). Smoke test must validate the new ICD-9 config constants and shared utility.

Output: R/56 with shared utility linkage, R/88 with ICD-9 validation checks. R/50 confirmed no-action.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/87-fix-cancer-summary-pre-post-to-include-icd9-but-be-still-filtered-on-icd10-81-and-all-codes-resolved-next-tables-and-drug-grouping-tables-should-all-be-linked-in-the-codes-they-use/87-CONTEXT.md
@.planning/phases/87-fix-cancer-summary-pre-post-to-include-icd9-but-be-still-filtered-on-icd10-81-and-all-codes-resolved-next-tables-and-drug-grouping-tables-should-all-be-linked-in-the-codes-they-use/87-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 -->

From R/utils/utils_cancer.R (created in Plan 01):
```r
is_cancer_code <- function(dx)  # Vectorized, returns logical
# Map-based detection: CANCER_SITE_MAP (ICD-10) + ICD9_CANCER_SITE_MAP (ICD-9)
# Strips dots internally. 140-209 for ICD-9 (malignant only).

classify_codes <- function(codes)  # Returns character vector of category names
```

From R/00_config.R (created in Plan 01):
```r
ICD9_CANCER_SITE_MAP  # 78-entry named vector, keys "140"-"209" + "2010"-"2019"
CANCER_SITE_MAP       # 324-entry named vector (unchanged)
ICD9_NLPHL_CODES      # 10-entry character vector (unchanged)
```

From R/56_new_tables_from_groupings.R (current local code, lines 156-174):
```r
# Local variables to be removed:
cancer_prefixes_icd10 <- names(CANCER_SITE_MAP)
cancer_prefixes_icd9 <- as.character(140:239)

# Local function to be removed:
is_cancer_code <- function(dx) {
  dx_clean <- str_remove(dx, "\\.")
  icd10_pattern <- paste0("^(", paste(cancer_prefixes_icd10, collapse = "|"), ")")
  icd10_match <- str_detect(dx_clean, icd10_pattern)
  icd9_match <- substr(dx_clean, 1, 3) %in% cancer_prefixes_icd9
  icd10_match | icd9_match
}
```

From R/50_all_codes_resolved.R (verified -- no cancer code references):
```
# Grep confirmed: No references to is_cancer_code, classify_codes,
# DX_TYPE, cancer, C81, or 201 (other than radiation code descriptions).
# D-08 decision confirmed: no structural changes needed.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace R/56 local is_cancer_code() with shared utility (per D-07)</name>
  <files>R/56_new_tables_from_groupings.R</files>
  <read_first>
    - R/56_new_tables_from_groupings.R (lines 1-30 for source statements, lines 150-200 for local is_cancer_code() and its usage)
    - R/utils/utils_cancer.R (verify is_cancer_code() exists from Plan 01)
  </read_first>
  <action>
**Step 1: Add source statement for utils_cancer.R**

Check if R/56 already sources `R/utils/utils_cancer.R`. Look for existing `source("R/utils/utils_cancer.R")` in the setup section. If NOT present, add it after the existing `source("R/00_config.R")` line (or wherever other utility sources are grouped):

```r
source("R/utils/utils_cancer.R")  # is_cancer_code(), classify_codes()
```

**Step 2: Remove local cancer prefix variables** (lines 156-163)

Delete these lines entirely:
```r
# Build cancer code prefix set from CANCER_SITE_MAP (ICD-10: C00-C96, D00-D49)
# plus ICD-9 neoplasm range (140-239)
cancer_prefixes_icd10 <- names(CANCER_SITE_MAP)
# ICD-9 neoplasm prefixes (140.x-239.x)
cancer_prefixes_icd9 <- as.character(140:239)

message(glue("  Cancer prefixes: {length(cancer_prefixes_icd10)} ICD-10, {length(cancer_prefixes_icd9)} ICD-9"))
```

Replace with a log message confirming shared utility usage:
```r
# Cancer code detection uses shared is_cancer_code() from R/utils/utils_cancer.R (per D-07)
# Detection is map-based: CANCER_SITE_MAP (ICD-10) + ICD9_CANCER_SITE_MAP (ICD-9 malignant 140-209)
message(glue("  Using shared is_cancer_code() -- ICD-10: {length(names(CANCER_SITE_MAP))} prefixes, ICD-9: {length(names(ICD9_CANCER_SITE_MAP))} prefixes"))
```

**Step 3: Remove local is_cancer_code() function definition** (lines 164-174)

Delete the entire local function definition:
```r
# Helper: check if diagnosis codes are cancer/neoplasm codes (vectorized)
is_cancer_code <- function(dx) {
  dx_clean <- str_remove(dx, "\\.")  # Remove dots for prefix matching
  # ICD-10: single regex from all prefixes
  icd10_pattern <- paste0("^(", paste(cancer_prefixes_icd10, collapse = "|"), ")")
  icd10_match <- str_detect(dx_clean, icd10_pattern)
  # ICD-9: 3-char prefix lookup (already vectorized)
  icd9_match <- substr(dx_clean, 1, 3) %in% cancer_prefixes_icd9

  icd10_match | icd9_match
}
```

Do NOT add a replacement function -- the shared one from utils_cancer.R is now available via source().

**Step 4: Verify call site is unchanged**

The existing call at line ~200 `filter(is_cancer_code(DX))` should work without modification since the shared function has the same signature: takes character vector, returns logical vector.

**Step 5: Update the script header comment**

In the file header (lines 1-30), update Dependencies section to add:
```
#   - utils_cancer (is_cancer_code shared utility)
```

**Important behavioral note:** The shared is_cancer_code() uses **map-based detection** (only detects codes with prefixes in CANCER_SITE_MAP or ICD9_CANCER_SITE_MAP). The old local version used **range-based ICD-9 detection** (140-239 via `as.character(140:239)`). This means the shared version will NOT detect ICD-9 codes 210-239 (benign/uncertain). This is the CORRECT behavior per D-02 (exclude benign/uncertain), and R/56 should only process malignant neoplasm codes anyway.
  </action>
  <verify>
    <automated>Rscript -e "lines <- readLines('R/56_new_tables_from_groupings.R'); cat('Has source utils_cancer:', any(grepl('source.*utils_cancer', lines)), '\n'); cat('Local is_cancer_code removed:', !any(grepl('is_cancer_code <- function', lines)), '\n'); cat('Local cancer_prefixes_icd9 removed:', !any(grepl('cancer_prefixes_icd9 <-', lines)), '\n'); cat('Local cancer_prefixes_icd10 removed:', !any(grepl('cancer_prefixes_icd10 <-', lines)), '\n'); cat('Still calls is_cancer_code:', any(grepl('is_cancer_code\\(', lines)), '\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/56_new_tables_from_groupings.R contains `source("R/utils/utils_cancer.R")` (or equivalent path)
    - R/56_new_tables_from_groupings.R does NOT contain `is_cancer_code <- function` (local definition removed)
    - R/56_new_tables_from_groupings.R does NOT contain `cancer_prefixes_icd10 <-` (local variable removed)
    - R/56_new_tables_from_groupings.R does NOT contain `cancer_prefixes_icd9 <-` (local variable removed)
    - R/56_new_tables_from_groupings.R still contains `is_cancer_code(DX)` call (usage preserved)
    - R/56_new_tables_from_groupings.R header Dependencies includes "utils_cancer"
    - Log message references shared utility with prefix counts from both maps
  </acceptance_criteria>
  <done>R/56 local is_cancer_code() and its supporting variables removed. Shared utility from R/utils/utils_cancer.R sourced instead. No duplicate cancer code detection logic remains in the codebase (per D-07).</done>
</task>

<task type="auto">
  <name>Task 2: Update R/88 smoke test with ICD-9 infrastructure validation</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (full file -- understand existing check structure, section numbering, passed/failed counting pattern)
    - R/00_config.R (verify ICD9_CANCER_SITE_MAP exists)
    - R/utils/utils_cancer.R (verify is_cancer_code() exists)
  </read_first>
  <action>
Add a NEW smoke test section for Phase 87 ICD-9 infrastructure validation. Find the last numbered section in R/88 and add a new section after it. Follow the existing pattern for check structure (increment `passed` on success, `failed` on failure, use `message()` for output).

Add the following checks (adapt exact syntax to match existing R/88 patterns):

**Section N: Phase 87 -- ICD-9 Cancer Code Infrastructure**

```r
# ==============================================================================
# SECTION N: PHASE 87 -- ICD-9 CANCER CODE INFRASTRUCTURE ----
# ==============================================================================
# Validates ICD9_CANCER_SITE_MAP, shared is_cancer_code(), and updated
# classify_codes() for ICD-9/ICD-10 harmonization (Phase 87).

message("\n--- Section N: Phase 87 ICD-9 Cancer Code Infrastructure ---")
```

**Check N.1: ICD9_CANCER_SITE_MAP exists and has correct entry count**
```r
# Check N.1: ICD9_CANCER_SITE_MAP exists with expected entries
if (exists("ICD9_CANCER_SITE_MAP") && length(ICD9_CANCER_SITE_MAP) >= 70) {
  message(glue("  PASS: ICD9_CANCER_SITE_MAP exists ({length(ICD9_CANCER_SITE_MAP)} entries)"))
  passed <- passed + 1L
} else {
  message("  FAIL: ICD9_CANCER_SITE_MAP missing or insufficient entries")
  failed <- failed + 1L
}
```

**Check N.2: ICD9_CANCER_SITE_MAP covers full malignant range (140-209)**
```r
# Check N.2: All malignant ICD-9 prefixes (140-209) present
icd9_prefixes_expected <- as.character(140:209)
icd9_prefixes_present <- intersect(names(ICD9_CANCER_SITE_MAP), icd9_prefixes_expected)
if (length(icd9_prefixes_present) == 70) {
  message(glue("  PASS: All 70 malignant ICD-9 prefixes (140-209) mapped"))
  passed <- passed + 1L
} else {
  missing_pfx <- setdiff(icd9_prefixes_expected, names(ICD9_CANCER_SITE_MAP))
  message(glue("  FAIL: Missing {length(missing_pfx)} ICD-9 prefixes: {paste(head(missing_pfx, 10), collapse=', ')}"))
  failed <- failed + 1L
}
```

**Check N.3: No benign/uncertain ICD-9 prefixes (210-239) in map**
```r
# Check N.3: No benign/uncertain ICD-9 codes (210-239) in map (per D-02)
benign_in_map <- intersect(names(ICD9_CANCER_SITE_MAP), as.character(210:239))
if (length(benign_in_map) == 0) {
  message("  PASS: No benign/uncertain ICD-9 codes (210-239) in ICD9_CANCER_SITE_MAP")
  passed <- passed + 1L
} else {
  message(glue("  FAIL: Benign codes found in map: {paste(benign_in_map, collapse=', ')}"))
  failed <- failed + 1L
}
```

**Check N.4: ICD-9 HL subcategory discrimination (NLPHL vs classical)**
```r
# Check N.4: ICD-9 201.4x maps to NLPHL, other 201.x to classical HL
if (exists("ICD9_CANCER_SITE_MAP")) {
  nlphl_ok <- identical(unname(ICD9_CANCER_SITE_MAP["2014"]), "NLPHL")
  classical_ok <- identical(unname(ICD9_CANCER_SITE_MAP["201"]), "Hodgkin Lymphoma (non-NLPHL)")
  if (nlphl_ok && classical_ok) {
    message("  PASS: ICD-9 HL subcategory discrimination correct (2014=NLPHL, 201=classical)")
    passed <- passed + 1L
  } else {
    message(glue("  FAIL: ICD-9 HL mapping -- 2014={ICD9_CANCER_SITE_MAP['2014']}, 201={ICD9_CANCER_SITE_MAP['201']}"))
    failed <- failed + 1L
  }
}
```

**Check N.5: is_cancer_code() exists in utils_cancer.R**
```r
# Check N.5: Shared is_cancer_code() function available
utils_cancer_lines <- readLines("R/utils/utils_cancer.R")
if (any(grepl("is_cancer_code <- function", utils_cancer_lines))) {
  message("  PASS: is_cancer_code() defined in R/utils/utils_cancer.R")
  passed <- passed + 1L
} else {
  message("  FAIL: is_cancer_code() not found in R/utils/utils_cancer.R")
  failed <- failed + 1L
}
```

**Check N.6: No local is_cancer_code() in R/56**
```r
# Check N.6: R/56 uses shared utility (no local is_cancer_code definition)
r56_lines <- readLines("R/56_new_tables_from_groupings.R")
if (!any(grepl("is_cancer_code <- function", r56_lines))) {
  message("  PASS: R/56 uses shared is_cancer_code() (no local definition)")
  passed <- passed + 1L
} else {
  message("  FAIL: R/56 still has local is_cancer_code() definition")
  failed <- failed + 1L
}
```

**Check N.7: No DX_TYPE == "10" hard-filters in cancer summary pipeline**
```r
# Check N.7: DX_TYPE == "10" removed from cancer summary scripts (per D-01)
dx_type_scripts <- c("R/45_cancer_summary.R", "R/47_cancer_summary_refined.R",
                      "R/48_cancer_summary_post_hl.R", "R/49_cancer_summary_pre_post.R")
dx_type_violations <- character()
for (script in dx_type_scripts) {
  if (file.exists(script)) {
    script_lines <- readLines(script)
    if (any(grepl('filter.*DX_TYPE.*==.*"10"', script_lines))) {
      dx_type_violations <- c(dx_type_violations, script)
    }
  }
}
if (length(dx_type_violations) == 0) {
  message("  PASS: No DX_TYPE==\"10\" hard-filters in cancer summary pipeline")
  passed <- passed + 1L
} else {
  message(glue("  FAIL: DX_TYPE==\"10\" still in: {paste(dx_type_violations, collapse=', ')}"))
  failed <- failed + 1L
}
```

**Check N.8: Category string consistency between ICD-9 and ICD-10 maps**
```r
# Check N.8: ICD-9 categories are subset of ICD-10 categories (cross-system consistency)
icd9_categories <- unique(unname(ICD9_CANCER_SITE_MAP))
icd10_categories <- unique(unname(CANCER_SITE_MAP))
novel_icd9_categories <- setdiff(icd9_categories, icd10_categories)
if (length(novel_icd9_categories) == 0) {
  message(glue("  PASS: All {length(icd9_categories)} ICD-9 categories match ICD-10 category strings"))
  passed <- passed + 1L
} else {
  message(glue("  FAIL: ICD-9 categories not in ICD-10: {paste(novel_icd9_categories, collapse=', ')}"))
  failed <- failed + 1L
}
```

Replace `N` with the actual next section number. The section numbering should be sequential with existing sections.
  </action>
  <verify>
    <automated>Rscript R/88_smoke_test_comprehensive.R 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains "Phase 87" section header
    - R/88_smoke_test_comprehensive.R contains check for `ICD9_CANCER_SITE_MAP` existence and entry count
    - R/88_smoke_test_comprehensive.R contains check for malignant range completeness (140-209)
    - R/88_smoke_test_comprehensive.R contains check for no benign codes (210-239) in map
    - R/88_smoke_test_comprehensive.R contains check for HL subcategory discrimination (2014=NLPHL)
    - R/88_smoke_test_comprehensive.R contains check for shared is_cancer_code() in utils_cancer.R
    - R/88_smoke_test_comprehensive.R contains check for no local is_cancer_code() in R/56
    - R/88_smoke_test_comprehensive.R contains check for no DX_TYPE=="10" in R/45, R/47, R/48, R/49
    - R/88_smoke_test_comprehensive.R contains check for category string consistency between maps
    - Smoke test runs without errors and all Phase 87 checks pass
  </acceptance_criteria>
  <done>R/56 uses shared is_cancer_code() with no duplicate logic. R/88 validates Phase 87 ICD-9 infrastructure: map completeness, benign exclusion, HL discrimination, shared utility deployment, DX_TYPE filter removal, and cross-system category consistency. R/50 confirmed no-action needed per D-08.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "is_cancer_code <- function" R/56_new_tables_from_groupings.R` returns 0 (local removed)
2. `grep "source.*utils_cancer" R/56_new_tables_from_groupings.R` shows source statement present
3. `Rscript R/88_smoke_test_comprehensive.R 2>&1 | grep "Phase 87"` shows section executed
4. `Rscript R/88_smoke_test_comprehensive.R 2>&1 | grep FAIL` returns empty (all checks pass)
</verification>

<success_criteria>
- R/56 has NO local is_cancer_code() definition -- sources shared version from R/utils/utils_cancer.R
- R/50 confirmed no cancer code references needing updates (per D-08)
- R/88 smoke test has 8 new checks validating Phase 87 ICD-9 infrastructure
- All smoke test checks pass when run via Rscript
</success_criteria>

<output>
After completion, create `.planning/phases/87-fix-cancer-summary-pre-post-to-include-icd9-but-be-still-filtered-on-icd10-81-and-all-codes-resolved-next-tables-and-drug-grouping-tables-should-all-be-linked-in-the-codes-they-use/87-03-SUMMARY.md`
</output>
