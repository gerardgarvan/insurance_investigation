---
phase: 85-testing-integration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/88_smoke_test_comprehensive.R
  - tests/run_local_test.R
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "DuckDB ingest (R/03) succeeds against fixture CSVs in local mode without any code changes to R/01 or R/03"
    - "R/88 smoke test passes locally with new Section 3B (DuckDB integration) and Section 3C (fixture schema validation)"
    - "Fixture schema checks only run when IS_LOCAL is TRUE; production mode skips them"
    - "Full local pipeline (R/00 -> R/01 -> R/03 -> R/88) completes in under 2 minutes"
    - "Edge case patients (PT002 dual-eligible, PT003 NLPHL, PT004 SCT, PT012 ABVD) are queryable in DuckDB after ingest"
  artifacts:
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Sections 3B and 3C for DuckDB integration and fixture schema validation"
      contains: "Fixture schema validation"
    - path: "tests/run_local_test.R"
      provides: "End-to-end local pipeline runner with timing validation"
      contains: "run_local_test"
  key_links:
    - from: "tests/run_local_test.R"
      to: "R/00_config.R"
      via: "source() call"
      pattern: 'source\("R/00_config\.R"\)'
    - from: "tests/run_local_test.R"
      to: "R/01_load_pcornet.R"
      via: "source() call"
      pattern: 'source\("R/01_load_pcornet\.R"\)'
    - from: "tests/run_local_test.R"
      to: "R/03_duckdb_ingest.R"
      via: "source() call"
      pattern: 'source\("R/03_duckdb_ingest\.R"\)'
    - from: "R/88_smoke_test_comprehensive.R"
      to: "CONFIG$cache$duckdb_path"
      via: "DBI::dbConnect in Section 3B"
      pattern: "DBI::dbConnect.*duckdb_path"
---

<objective>
Add DuckDB integration validation and fixture schema checks to R/88 smoke test, and create a local integration test runner that validates the full pipeline (R/00 -> R/01 -> R/03 -> R/88) works end-to-end against fixture CSVs with timing under 2 minutes.

Purpose: Phase 83-84 created the environment detection and fixtures. This plan closes the loop by validating they work together through the actual pipeline scripts.
Output: Updated R/88 with Sections 3B/3C, new tests/run_local_test.R script
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-testing-integration-validation/85-RESEARCH.md

@.planning/phases/83-environment-detection-infrastructure/83-01-SUMMARY.md
@.planning/phases/83-environment-detection-infrastructure/83-02-SUMMARY.md
@.planning/phases/84-test-fixture-design-creation/84-01-SUMMARY.md
@.planning/phases/84-test-fixture-design-creation/84-02-SUMMARY.md

@R/88_smoke_test_comprehensive.R
@R/00_config.R
@R/01_load_pcornet.R
@R/03_duckdb_ingest.R
@tests/fixtures/FIXTURE_DESIGN.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From R/00_config.R:
- IS_LOCAL: logical flag (TRUE on Windows, overridable via R_TESTING_ENV env var)
- CONFIG$data_dir: "tests/fixtures" (local) or "/orange/..." (production)
- CONFIG$cache$raw_dir: tempdir()/insurance_investigation_cache/raw (local)
- CONFIG$cache$duckdb_path: tempdir()/insurance_investigation_duckdb/pcornet_test.duckdb (local)
- CONFIG$cache$duckdb_dir: tempdir()/insurance_investigation_duckdb (local)
- PCORNET_TABLES: character vector of 15 table names
- PCORNET_PATHS: named character vector of CSV file paths

From R/88_smoke_test_comprehensive.R:
- check(description, condition): helper function at lines 51-59, increments passed/failed counters
- Current section numbering: [1/29] through [29/29], then [30/31] and [31/31]
- Section 15b: Environment detection validation (lines 1226-1304)
- Section 30: Phase 87 ICD-9 cancer code infrastructure (lines 1306-1409)
- Section 31: Phase 88 instance-level drug grouping tables (lines 1411-1474)
- Section 16 (SUMMARY): lines 1476-1539

From R/01_load_pcornet.R:
- load_pcornet_table(): reads CSV via vroom, caches to RDS
- pcornet: named list of 15 tibbles (output)
- Uses CONFIG$cache$raw_dir for RDS caching

From R/03_duckdb_ingest.R:
- Reads from CONFIG$cache$raw_dir (RDS files, NOT CSV)
- TABLES_TO_INGEST = PCORNET_TABLES (15 tables)
- Writes to CONFIG$cache$duckdb_path
- Atomic write pattern: builds at .tmp, renames on success
- Pipeline flow: R/01 creates RDS cache -> R/03 reads RDS -> writes DuckDB

From tests/fixtures/:
- 15 CSV files matching PCORNET_PATHS naming convention (TABLE_Mailhot_V1.csv)
- 65 total data rows, 20 patients (PT001-PT020)
- Edge cases: PT002 (dual-eligible, payer "14"), PT003 (NLPHL, C81.00), PT004 (SCT, 38241),
  PT005 (multiple cancers), PT006 (death), PT007 (orphan dx, Z51.11),
  PT008 (same-day multi-payer), PT009 (1900 sentinel), PT010 (ICD-9/10 cross),
  PT011 (missing payer, NI), PT012 (ABVD regimen, 4 RXNORM_CUIs)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sections 3B and 3C to R/88 smoke test for DuckDB integration and fixture schema validation</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    R/88_smoke_test_comprehensive.R
    R/00_config.R
    tests/fixtures/FIXTURE_DESIGN.md
    .planning/phases/85-testing-integration-validation/85-RESEARCH.md
  </read_first>
  <action>
Add two new sections to R/88_smoke_test_comprehensive.R BETWEEN the existing Section 31 (Phase 88, ending at line 1474) and Section 16 (SUMMARY, starting at line 1476). These new sections are numbered 32 and 33. Also update the Section 30 and 31 headers from [30/31] and [31/31] to [30/33] and [31/33] respectively. Leave sections [1/29] through [29/29] unchanged (they use a different total counter from the later sections).

**Section 32: DuckDB Local Integration Validation (TEST-01, TEST-02)**

Insert after Section 31 ends (after line 1474). This section runs in BOTH local and production modes but with different checks.

```r
# ==============================================================================
# SECTION 32: DuckDB LOCAL INTEGRATION VALIDATION (TEST-01, TEST-02) ----
# ==============================================================================
# Validates that DuckDB ingest succeeds in current environment mode.
# Local mode: checks fixture-based DuckDB file in tempdir().
# Production mode: checks production DuckDB file on /blue/.

message("\n[32/33] DuckDB integration validation...")

if (file.exists(CONFIG$cache$duckdb_path)) {
  con <- tryCatch(
    DBI::dbConnect(duckdb::duckdb(), CONFIG$cache$duckdb_path, read_only = TRUE),
    error = function(e) NULL
  )

  if (!is.null(con)) {
    tables_found <- DBI::dbListTables(con)

    check(
      glue("DuckDB file accessible at CONFIG path ({basename(CONFIG$cache$duckdb_path)})"),
      TRUE
    )

    if (IS_LOCAL) {
      # TEST-01: Fixture-specific table count (15 tables from fixture CSVs)
      check(
        glue("DuckDB contains 15 tables from fixtures (found {length(tables_found)})"),
        length(tables_found) == 15
      )

      # Validate critical tables exist
      critical_tables <- c("ENROLLMENT", "DIAGNOSIS", "ENCOUNTER", "DEMOGRAPHIC",
                           "PROCEDURES", "PRESCRIBING", "DEATH")
      missing_critical <- setdiff(critical_tables, tables_found)
      check(
        glue("All 7 critical tables present (missing: {if (length(missing_critical) == 0) 'none' else paste(missing_critical, collapse=', ')})"),
        length(missing_critical) == 0
      )

      # TEST-05: Conditional fixture row count assertions
      enrollment_count <- DBI::dbGetQuery(con, "SELECT COUNT(*) AS n FROM ENROLLMENT")$n
      check(
        glue("ENROLLMENT has 20 fixture patients (found {enrollment_count})"),
        enrollment_count == 20
      )

      diagnosis_count <- DBI::dbGetQuery(con, "SELECT COUNT(*) AS n FROM DIAGNOSIS")$n
      check(
        glue("DIAGNOSIS has 18 fixture rows (found {diagnosis_count})"),
        diagnosis_count == 18
      )

      encounter_count <- DBI::dbGetQuery(con, "SELECT COUNT(*) AS n FROM ENCOUNTER")$n
      check(
        glue("ENCOUNTER has 19 fixture rows (found {encounter_count})"),
        encounter_count == 19
      )

      prescribing_count <- DBI::dbGetQuery(con, "SELECT COUNT(*) AS n FROM PRESCRIBING")$n
      check(
        glue("PRESCRIBING has 4 fixture rows (found {prescribing_count})"),
        prescribing_count == 4
      )

    } else {
      # Production: just check tables exist (counts are data-dependent)
      check(
        glue("DuckDB contains >= 13 tables (found {length(tables_found)})"),
        length(tables_found) >= 13
      )
    }

    DBI::dbDisconnect(con, shutdown = TRUE)
  } else {
    check("DuckDB file accessible (connection failed)", FALSE)
  }
} else {
  message("  SKIP: DuckDB file not found at CONFIG path (run R/01 + R/03 first)")
}
```

**Section 33: Fixture Schema and Edge Case Validation (TEST-03, TEST-05)**

Insert after Section 32. This section ONLY runs in local mode (`if (IS_LOCAL)`). It validates fixture-specific edge case data is present in the loaded pcornet list. This section requires that `pcornet` list exists in the global environment (loaded by R/01). If pcornet doesn't exist, it skips with an informative message.

```r
# ==============================================================================
# SECTION 33: FIXTURE SCHEMA & EDGE CASE VALIDATION (TEST-03, TEST-05) ----
# ==============================================================================
# Validates fixture-specific edge case data when running in local mode.
# Requires pcornet list from R/01_load_pcornet.R in global environment.
# Production mode: skipped entirely (fixture data not present).

if (IS_LOCAL) {
  message("\n[33/33] Fixture schema validation (local mode only)...")

  if (exists("pcornet", envir = .GlobalEnv) && is.list(pcornet) && length(pcornet) > 0) {

    # TEST-03: Fixture row counts match FIXTURE_DESIGN.md specifications
    check(
      glue("Fixture ENROLLMENT has 20 patients (found {nrow(pcornet$ENROLLMENT)})"),
      nrow(pcornet$ENROLLMENT) == 20
    )

    check(
      glue("Fixture DIAGNOSIS has 18 rows (found {nrow(pcornet$DIAGNOSIS)})"),
      nrow(pcornet$DIAGNOSIS) == 18
    )

    check(
      glue("Fixture DEMOGRAPHIC has 20 rows (found {nrow(pcornet$DEMOGRAPHIC)})"),
      nrow(pcornet$DEMOGRAPHIC) == 20
    )

    check(
      glue("Fixture ENCOUNTER has 19 rows (found {nrow(pcornet$ENCOUNTER)})"),
      nrow(pcornet$ENCOUNTER) == 19
    )

    check(
      glue("Fixture PRESCRIBING has 4 rows (found {nrow(pcornet$PRESCRIBING)})"),
      nrow(pcornet$PRESCRIBING) == 4
    )

    check(
      glue("Fixture PROCEDURES has 1 row (found {nrow(pcornet$PROCEDURES)})"),
      nrow(pcornet$PROCEDURES) == 1
    )

    check(
      glue("Fixture DEATH has 1 row (found {nrow(pcornet$DEATH)})"),
      nrow(pcornet$DEATH) == 1
    )

    # TEST-05: Edge case patient validation
    # Edge case 1: PT002 dual-eligible (payer code "14")
    pt002_dual <- pcornet$ENCOUNTER %>%
      dplyr::filter(ID == "PT002")
    check(
      "PT002 (dual-eligible) exists in ENCOUNTER",
      nrow(pt002_dual) > 0
    )

    # Edge case 2: PT003 NLPHL (C81.00)
    pt003_nlphl <- pcornet$DIAGNOSIS %>%
      dplyr::filter(ID == "PT003", DX == "C81.00")
    check(
      "PT003 has NLPHL diagnosis (C81.00)",
      nrow(pt003_nlphl) > 0
    )

    # Edge case 3: PT004 SCT (CPT 38241)
    pt004_sct <- pcornet$PROCEDURES %>%
      dplyr::filter(ID == "PT004", PX == "38241")
    check(
      "PT004 has SCT procedure (CPT 38241)",
      nrow(pt004_sct) > 0
    )

    # Edge case 4: PT007 orphan dx (Z51.11)
    pt007_orphan <- pcornet$DIAGNOSIS %>%
      dplyr::filter(ID == "PT007", DX == "Z51.11")
    check(
      "PT007 has orphan dx code (Z51.11)",
      nrow(pt007_orphan) > 0
    )

    # Edge case 5: PT009 sentinel date (1900-01-01)
    pt009_sentinel <- pcornet$ENROLLMENT %>%
      dplyr::filter(ID == "PT009")
    check(
      "PT009 exists in ENROLLMENT (1900 sentinel date patient)",
      nrow(pt009_sentinel) > 0
    )

    # Edge case 6: PT010 ICD-9/ICD-10 cross-system
    pt010_dx <- pcornet$DIAGNOSIS %>%
      dplyr::filter(ID == "PT010")
    check(
      glue("PT010 has 2 diagnoses for cross-system HL (found {nrow(pt010_dx)})"),
      nrow(pt010_dx) == 2
    )

    # Edge case 7: PT012 ABVD regimen (4 RXNORM_CUIs)
    pt012_abvd <- pcornet$PRESCRIBING %>%
      dplyr::filter(ID == "PT012")
    expected_cuis <- c("3639", "11213", "67228", "3946")
    found_cuis <- pt012_abvd$RXNORM_CUI
    check(
      glue("PT012 has 4 ABVD drugs (found {length(found_cuis)}: {paste(found_cuis, collapse=', ')})"),
      length(found_cuis) == 4 && all(expected_cuis %in% found_cuis)
    )

    # Edge case 8: PT006 death record
    pt006_death <- pcornet$DEATH %>%
      dplyr::filter(ID == "PT006")
    check(
      "PT006 has death record",
      nrow(pt006_death) == 1
    )

  } else {
    message("  SKIP: pcornet list not loaded (run R/01_load_pcornet.R first)")
    message("  To run full validation: source R/01 before R/88")
  }

} else {
  message("\n[33/33] Fixture schema validation -- SKIPPED (production mode)")
}
```

**Also update the SUMMARY section (Section 16):**

After the existing requirement messages (around line 1535), add the following new requirement messages before the `if (failed > 0)` block:

```r
message("  * TEST-01: DuckDB ingest works with fixture CSVs (Section 32)")
message("  * TEST-02: R/88 smoke test passes locally against fixtures (Section 32)")
message("  * TEST-03: Fixture schema validation in local mode (Section 33)")
message("  * TEST-05: Conditional fixture count assertions (Sections 32+33)")
```

**Update the version string** in the banner message (line 62) from:
```r
message("SMOKE TEST: Comprehensive Pipeline Validation (v2.2 + Phase 87)")
```
to:
```r
message("SMOKE TEST: Comprehensive Pipeline Validation (v2.2 + Phase 87-89)")
```

**Critical implementation details:**
- The DBI and duckdb libraries are NOT loaded at the top of R/88. In Section 32, use the fully qualified `DBI::dbConnect()`, `DBI::dbListTables()`, `DBI::dbGetQuery()`, `DBI::dbDisconnect()` and `duckdb::duckdb()` calls. The smoke test only uses `library(glue)` at the top. Do NOT add library() calls for DBI/duckdb.
- The `dplyr::filter()` calls in Section 33 use the `dplyr::` prefix because R/88 does not load dplyr (only glue). Do NOT add library(dplyr) to R/88.
- Section 33 uses the `pcornet` list (from R/01). If R/88 is run standalone (without R/01 first), the section gracefully skips.
- Use `normalizePath(..., winslash = "/", mustWork = FALSE)` for any path comparisons (existing pattern from line 1254).
- DBI::dbDisconnect MUST include `shutdown = TRUE` to avoid DuckDB file locking on Windows (Pitfall 1 from research).
  </action>
  <verify>
    <automated>grep -c "Fixture schema validation" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains "SECTION 32: DuckDB LOCAL INTEGRATION VALIDATION"
    - R/88_smoke_test_comprehensive.R contains "SECTION 33: FIXTURE SCHEMA"
    - R/88_smoke_test_comprehensive.R contains "[32/33]"
    - R/88_smoke_test_comprehensive.R contains "[33/33]"
    - R/88_smoke_test_comprehensive.R contains "DBI::dbConnect(duckdb::duckdb()" (fully qualified, no library call)
    - R/88_smoke_test_comprehensive.R contains "dplyr::filter" (fully qualified, no library call)
    - R/88_smoke_test_comprehensive.R contains "shutdown = TRUE" in dbDisconnect call
    - R/88_smoke_test_comprehensive.R contains "if (IS_LOCAL)" wrapping Section 33
    - R/88_smoke_test_comprehensive.R contains "PT003.*NLPHL.*C81.00"
    - R/88_smoke_test_comprehensive.R contains "PT012.*ABVD"
    - R/88_smoke_test_comprehensive.R contains "TEST-01" in summary section
    - R/88_smoke_test_comprehensive.R contains "TEST-03" in summary section
    - R/88_smoke_test_comprehensive.R contains "TEST-05" in summary section
    - R/88_smoke_test_comprehensive.R Section 30 header shows [30/33] not [30/31]
    - R/88_smoke_test_comprehensive.R Section 31 header shows [31/33] not [31/31]
    - R/88_smoke_test_comprehensive.R version string contains "Phase 87-89"
  </acceptance_criteria>
  <done>R/88 smoke test has Sections 32 (DuckDB integration) and 33 (fixture schema), with proper conditional execution (IS_LOCAL for Section 33), fully-qualified DBI/dplyr calls, shutdown=TRUE on disconnect, and updated version string and section counts</done>
</task>

<task type="auto">
  <name>Task 2: Create local integration test runner script (tests/run_local_test.R)</name>
  <files>tests/run_local_test.R</files>
  <read_first>
    R/00_config.R
    R/01_load_pcornet.R
    R/03_duckdb_ingest.R
    R/88_smoke_test_comprehensive.R
    tests/fixtures/FIXTURE_DESIGN.md
  </read_first>
  <action>
Create `tests/run_local_test.R` -- a standalone script that runs the full local pipeline end-to-end with timing. This script is the TEST-04 deliverable: full pipeline end-to-end runnable locally.

The script does the following in order:
1. Records start time
2. Sources R/00_config.R and asserts IS_LOCAL == TRUE (stops with clear error if not)
3. Sources R/01_load_pcornet.R (loads fixture CSVs into pcornet list, caches as RDS in tempdir)
4. Sources R/03_duckdb_ingest.R (ingests RDS cache into DuckDB in tempdir)
5. Validates DuckDB contains expected tables and edge case data by connecting and querying
6. Sources R/88_smoke_test_comprehensive.R (runs full smoke test including new Sections 32+33)
7. Records end time, calculates duration, validates under 120 seconds (2 min target)
8. Prints summary report

Write the following content:

```r
# ==============================================================================
# run_local_test.R -- End-to-end local pipeline integration test
# ==============================================================================
#
# Purpose:
#   Runs the full PCORnet pipeline locally against test fixtures to validate
#   environment detection, CSV loading, DuckDB ingest, and smoke test pass.
#   Measures total execution time against 2-minute target (TEST-04).
#
# Usage:
#   From project root in RStudio:
#     source("tests/run_local_test.R")
#
#   Or from command line:
#     Rscript tests/run_local_test.R
#
# Prerequisites:
#   - R with tidyverse, duckdb, DBI, vroom, glue packages installed
#   - tests/fixtures/ directory populated with 15 PCORnet CDM CSVs (Phase 84)
#   - Must be run from the project root directory (where R/ and tests/ are)
#
# Requirements: TEST-01, TEST-02, TEST-03, TEST-04, TEST-05
#
# ==============================================================================

cat(strrep("=", 70), "\n")
cat("LOCAL INTEGRATION TEST: Full Pipeline Validation\n")
cat(strrep("=", 70), "\n\n")

# Track overall timing
pipeline_start <- Sys.time()

# --------------------------------------------------------------------------
# STEP 1: Environment detection (R/00_config.R)
# --------------------------------------------------------------------------
cat("[Step 1/5] Sourcing R/00_config.R...\n")
step1_start <- Sys.time()

# Force clean environment for pcornet reload
if (exists("pcornet", envir = .GlobalEnv)) rm(pcornet, envir = .GlobalEnv)

source("R/00_config.R")

if (!IS_LOCAL) {
  stop(paste0(
    "ABORT: IS_LOCAL is FALSE. This test must run in local mode.\n",
    "  Current OS: ", Sys.info()["sysname"], "\n",
    "  Fix: Set R_TESTING_ENV=local in project-root .Renviron\n",
    "  Or: Run on a Windows machine (auto-detected as local)"
  ))
}

step1_time <- as.numeric(difftime(Sys.time(), step1_start, units = "secs"))
cat(sprintf("  Done (%.1fs) -- IS_LOCAL=%s, data_dir=%s\n\n",
            step1_time, IS_LOCAL, CONFIG$data_dir))

# --------------------------------------------------------------------------
# STEP 2: Load fixture CSVs (R/01_load_pcornet.R)
# --------------------------------------------------------------------------
cat("[Step 2/5] Sourcing R/01_load_pcornet.R (fixture CSV loading)...\n")
step2_start <- Sys.time()

source("R/01_load_pcornet.R")

step2_time <- as.numeric(difftime(Sys.time(), step2_start, units = "secs"))
tables_loaded <- sum(!sapply(pcornet, is.null))
total_rows <- sum(sapply(pcornet, function(x) if (!is.null(x)) nrow(x) else 0L))
cat(sprintf("  Done (%.1fs) -- %d tables loaded, %d total rows\n\n",
            step2_time, tables_loaded, total_rows))

# --------------------------------------------------------------------------
# STEP 3: DuckDB ingest (R/03_duckdb_ingest.R)
# --------------------------------------------------------------------------
cat("[Step 3/5] Sourcing R/03_duckdb_ingest.R (RDS -> DuckDB ingest)...\n")
step3_start <- Sys.time()

source("R/03_duckdb_ingest.R")

step3_time <- as.numeric(difftime(Sys.time(), step3_start, units = "secs"))
cat(sprintf("  Done (%.1fs) -- DuckDB at %s\n\n",
            step3_time, CONFIG$cache$duckdb_path))

# --------------------------------------------------------------------------
# STEP 4: DuckDB content validation
# --------------------------------------------------------------------------
cat("[Step 4/5] Validating DuckDB content...\n")
step4_start <- Sys.time()

val_errors <- character()

if (file.exists(CONFIG$cache$duckdb_path)) {
  con <- DBI::dbConnect(duckdb::duckdb(), CONFIG$cache$duckdb_path, read_only = TRUE)

  # Check table count
  db_tables <- DBI::dbListTables(con)
  if (length(db_tables) != 15) {
    val_errors <- c(val_errors,
      sprintf("Expected 15 DuckDB tables, found %d", length(db_tables)))
  }

  # Check fixture row counts
  expected_counts <- list(
    ENROLLMENT = 20L, DIAGNOSIS = 18L, ENCOUNTER = 19L,
    DEMOGRAPHIC = 20L, PRESCRIBING = 4L, PROCEDURES = 1L,
    DEATH = 1L, PROVIDER = 2L, CONDITION = 1L
  )

  for (tbl in names(expected_counts)) {
    if (tbl %in% db_tables) {
      actual <- DBI::dbGetQuery(con, sprintf("SELECT COUNT(*) AS n FROM %s", tbl))$n
      if (actual != expected_counts[[tbl]]) {
        val_errors <- c(val_errors,
          sprintf("%s: expected %d rows, found %d", tbl, expected_counts[[tbl]], actual))
      }
    }
  }

  # Check edge case patients in DuckDB
  pt003_count <- DBI::dbGetQuery(con,
    "SELECT COUNT(*) AS n FROM DIAGNOSIS WHERE ID = 'PT003' AND DX = 'C81.00'")$n
  if (pt003_count == 0) {
    val_errors <- c(val_errors, "PT003 NLPHL (C81.00) not found in DuckDB DIAGNOSIS")
  }

  pt012_count <- DBI::dbGetQuery(con,
    "SELECT COUNT(*) AS n FROM PRESCRIBING WHERE ID = 'PT012'")$n
  if (pt012_count != 4) {
    val_errors <- c(val_errors,
      sprintf("PT012 ABVD: expected 4 prescribing rows, found %d", pt012_count))
  }

  DBI::dbDisconnect(con, shutdown = TRUE)
} else {
  val_errors <- c(val_errors, "DuckDB file not found after ingest")
}

step4_time <- as.numeric(difftime(Sys.time(), step4_start, units = "secs"))
if (length(val_errors) == 0) {
  cat(sprintf("  Done (%.1fs) -- All DuckDB validations passed\n\n", step4_time))
} else {
  cat(sprintf("  Done (%.1fs) -- %d validation errors:\n", step4_time, length(val_errors)))
  for (err in val_errors) cat(sprintf("    ERROR: %s\n", err))
  cat("\n")
}

# --------------------------------------------------------------------------
# STEP 5: Smoke test (R/88_smoke_test_comprehensive.R)
# --------------------------------------------------------------------------
cat("[Step 5/5] Sourcing R/88_smoke_test_comprehensive.R (full smoke test)...\n")
step5_start <- Sys.time()

# R/88 clears workspace with rm(list = ls()) at top, so save what we need
saved_pipeline_start <- pipeline_start
saved_step_times <- c(step1_time, step2_time, step3_time, step4_time)
saved_val_errors <- val_errors

source("R/88_smoke_test_comprehensive.R")

step5_time <- as.numeric(difftime(Sys.time(), step5_start, units = "secs"))

# Restore saved values (R/88 cleared the environment)
pipeline_start <- saved_pipeline_start
step_times <- saved_step_times
val_errors <- saved_val_errors

# --------------------------------------------------------------------------
# SUMMARY
# --------------------------------------------------------------------------
pipeline_end <- Sys.time()
total_seconds <- as.numeric(difftime(pipeline_end, pipeline_start, units = "secs"))

cat("\n", strrep("=", 70), "\n")
cat("LOCAL INTEGRATION TEST: SUMMARY\n")
cat(strrep("=", 70), "\n\n")

cat(sprintf("  Step 1 (config):     %6.1fs\n", step_times[1]))
cat(sprintf("  Step 2 (CSV load):   %6.1fs\n", step_times[2]))
cat(sprintf("  Step 3 (DuckDB):     %6.1fs\n", step_times[3]))
cat(sprintf("  Step 4 (validate):   %6.1fs\n", step_times[4]))
cat(sprintf("  Step 5 (smoke test): %6.1fs\n", step5_time))
cat(sprintf("  %-20s %6.1fs\n", "TOTAL:", total_seconds))
cat("\n")

# TEST-04: 2-minute performance target
if (total_seconds <= 120) {
  cat(sprintf("  PASS: Pipeline completed in %.1fs (under 2-minute target)\n", total_seconds))
} else {
  cat(sprintf("  WARNING: Pipeline took %.1fs (exceeded 2-minute target of 120s)\n", total_seconds))
}

# DuckDB validation results
if (length(val_errors) == 0) {
  cat("  PASS: All DuckDB content validations passed\n")
} else {
  cat(sprintf("  FAIL: %d DuckDB validation errors\n", length(val_errors)))
}

# Smoke test results (passed/failed from R/88 are in global env)
if (exists("passed") && exists("failed")) {
  cat(sprintf("  Smoke test: %d passed, %d failed\n", passed, failed))
}

cat("\n", strrep("=", 70), "\n")

# Exit with error if any failures
if (length(val_errors) > 0 || (exists("failed") && failed > 0)) {
  cat("RESULT: FAILED\n")
  if (!interactive()) quit(status = 1)
} else {
  cat("RESULT: ALL TESTS PASSED\n")
}
```

**Key implementation details:**
- The script saves timing variables before sourcing R/88 because R/88 does `rm(list = ls())` at its top (line 44). The saved values are restored after R/88 completes.
- Uses `cat()` instead of `message()` for its own output to distinguish from R/01/R/03/R/88 output which uses `message()`.
- Always uses `shutdown = TRUE` with DBI::dbDisconnect to prevent DuckDB file locking on Windows.
- The `if (!interactive()) quit(status = 1)` pattern allows the script to work both in RStudio (interactive, no quit) and via Rscript (non-interactive, exits with code 1 on failure).
- Forces clean pcornet reload by removing it from global env before sourcing R/01.
  </action>
  <verify>
    <automated>grep -c "LOCAL INTEGRATION TEST" tests/run_local_test.R</automated>
  </verify>
  <acceptance_criteria>
    - tests/run_local_test.R exists
    - tests/run_local_test.R contains 'source("R/00_config.R")'
    - tests/run_local_test.R contains 'source("R/01_load_pcornet.R")'
    - tests/run_local_test.R contains 'source("R/03_duckdb_ingest.R")'
    - tests/run_local_test.R contains 'source("R/88_smoke_test_comprehensive.R")'
    - tests/run_local_test.R contains "if (!IS_LOCAL)" stop guard
    - tests/run_local_test.R contains "shutdown = TRUE" in DBI::dbDisconnect
    - tests/run_local_test.R contains "120" (2-minute performance target)
    - tests/run_local_test.R contains "TEST-01" and "TEST-04" in header comments
    - tests/run_local_test.R contains expected_counts list with ENROLLMENT = 20
    - tests/run_local_test.R contains PT003 and PT012 edge case validations
    - tests/run_local_test.R contains saved_pipeline_start pattern (R/88 rm(list=ls()) workaround)
    - tests/run_local_test.R contains 'if (!interactive()) quit(status = 1)' for Rscript compat
  </acceptance_criteria>
  <done>tests/run_local_test.R exists as a standalone end-to-end local pipeline runner that sources R/00, R/01, R/03, R/88 in sequence, validates DuckDB content against fixture expectations, measures total time against 2-minute target, and exits with status 1 on any failure</done>
</task>

</tasks>

<verification>
After both tasks are complete, verify:

1. R/88_smoke_test_comprehensive.R has Sections 32 and 33 between Section 31 and the SUMMARY section
2. Section 32 (DuckDB integration) runs in both modes but has IS_LOCAL-specific checks
3. Section 33 (fixture schema) only runs when IS_LOCAL == TRUE
4. tests/run_local_test.R exists and contains the full pipeline test sequence
5. No library() calls added to R/88 (DBI/dplyr use fully-qualified :: notation)
6. All DBI::dbDisconnect calls include shutdown = TRUE
7. Section counts updated: [30/33], [31/33], [32/33], [33/33]

```bash
# Verify Section 32 exists
grep "SECTION 32.*DuckDB LOCAL INTEGRATION" R/88_smoke_test_comprehensive.R

# Verify Section 33 exists
grep "SECTION 33.*FIXTURE SCHEMA" R/88_smoke_test_comprehensive.R

# Verify no library(DBI) or library(duckdb) added
grep -c "^library(DBI)" R/88_smoke_test_comprehensive.R  # should be 0
grep -c "^library(duckdb)" R/88_smoke_test_comprehensive.R  # should be 0

# Verify run_local_test.R exists with key content
grep "source.*R/00_config.R" tests/run_local_test.R
grep "source.*R/01_load_pcornet.R" tests/run_local_test.R
grep "source.*R/03_duckdb_ingest.R" tests/run_local_test.R
grep "source.*R/88_smoke_test" tests/run_local_test.R
```
</verification>

<success_criteria>
- R/88 smoke test has 2 new sections (32: DuckDB integration, 33: fixture schema)
- Section 33 is local-only (wrapped in `if (IS_LOCAL)`)
- Section 32 works in both modes with conditional assertions
- tests/run_local_test.R runs full pipeline end-to-end with timing
- All DuckDB connections use shutdown = TRUE
- No new library() calls added to R/88
- Section numbering updated to /33 total
- All 5 requirements (TEST-01 through TEST-05) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/85-testing-integration-validation/85-01-SUMMARY.md`
</output>
