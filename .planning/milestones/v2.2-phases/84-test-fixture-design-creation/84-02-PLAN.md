---
phase: 84-test-fixture-design-creation
plan: 02
type: execute
wave: 2
depends_on: [84-01]
files_modified:
  - tests/fixtures/ENROLLMENT_Mailhot_V1.csv
  - tests/fixtures/DIAGNOSIS_Mailhot_V1.csv
  - tests/fixtures/CONDITION_Mailhot_V1.csv
  - tests/fixtures/PROCEDURES_Mailhot_V1.csv
  - tests/fixtures/PRESCRIBING_Mailhot_V1.csv
  - tests/fixtures/ENCOUNTER_Mailhot_V1.csv
  - tests/fixtures/DEMOGRAPHIC_Mailhot_V1.csv
  - tests/fixtures/TUMOR_REGISTRY1_Mailhot_V1.csv
  - tests/fixtures/TUMOR_REGISTRY2_Mailhot_V1.csv
  - tests/fixtures/TUMOR_REGISTRY3_Mailhot_V1.csv
  - tests/fixtures/DISPENSING_Mailhot_V1.csv
  - tests/fixtures/MED_ADMIN_Mailhot_V1.csv
  - tests/fixtures/LAB_RESULT_Mailhot_V1.csv
  - tests/fixtures/PROVIDER_Mailhot_V1.csv
  - tests/fixtures/DEATH_Mailhot_V1.csv
autonomous: true
requirements: [FIX-01, FIX-02, FIX-05]

must_haves:
  truths:
    - "15 CSV files exist in tests/fixtures/ matching PCORNET_PATHS naming convention"
    - "LAB_RESULT file is named LAB_RESULT_Mailhot_V1.csv (not LAB_RESULT_CM_Mailhot_V1.csv)"
    - "Every CSV has correct column headers matching production specs"
    - "All CSVs combined are under 1MB for reasonable git performance"
    - "All 15 CSVs are git-tracked (not in .gitignore)"
    - "Edge case data is present: dual-eligible (PT002), NLPHL (PT003), SCT (PT004), ABVD (PT012)"
  artifacts:
    - path: "tests/fixtures/ENROLLMENT_Mailhot_V1.csv"
      provides: "20 enrollment records for synthetic patients"
      contains: "PT001"
    - path: "tests/fixtures/DIAGNOSIS_Mailhot_V1.csv"
      provides: "Diagnosis records including edge case ICD codes"
      contains: "C81.00"
    - path: "tests/fixtures/ENCOUNTER_Mailhot_V1.csv"
      provides: "Encounter records with payer codes"
      contains: "PAYER_TYPE_PRIMARY"
    - path: "tests/fixtures/PRESCRIBING_Mailhot_V1.csv"
      provides: "ABVD drug records for PT012"
      contains: "3639"
    - path: "tests/fixtures/DEATH_Mailhot_V1.csv"
      provides: "Death record for PT006"
      contains: "PT006"
    - path: "tests/fixtures/LAB_RESULT_Mailhot_V1.csv"
      provides: "Empty lab result table with correct headers"
      contains: "LAB_RESULTID"
  key_links:
    - from: "tests/fixtures/*.csv"
      to: "R/03_duckdb_ingest.R"
      via: "PCORNET_PATHS file naming"
      pattern: "_Mailhot_V1\\.csv"
    - from: "tests/fixtures/*.csv"
      to: "R/01_load_pcornet.R"
      via: "Column headers match TABLE_SPEC definitions"
      pattern: "DIAGNOSISID|ENCOUNTERID|PRESCRIBINGID"
---

<objective>
Write all 15 PCORnet CDM fixture CSV files to tests/fixtures/ based on the data defined in tests/generate_fixtures.R (from Plan 01). Verify correct file naming, column headers, edge case presence, git trackability, and total size constraint.

Purpose: Materialize the fixture data as committed CSV files that R/03_duckdb_ingest.R can consume without any code changes when CONFIG$data_dir points to tests/fixtures/. The CSVs are committed for convenience (per D-05) so developers don't need to run the generator before testing.

Output: 15 CSV files in tests/fixtures/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-test-fixture-design-creation/84-CONTEXT.md
@.planning/phases/84-test-fixture-design-creation/84-01-SUMMARY.md

<interfaces>
<!-- CSV file naming convention from R/00_config.R -->
File naming pattern: {TABLE_NAME}_Mailhot_V1.csv
CRITICAL exception: LAB_RESULT_CM table uses LAB_RESULT_Mailhot_V1.csv (no _CM suffix)

Expected files (15 total):
  ENROLLMENT_Mailhot_V1.csv
  DIAGNOSIS_Mailhot_V1.csv
  CONDITION_Mailhot_V1.csv
  PROCEDURES_Mailhot_V1.csv
  PRESCRIBING_Mailhot_V1.csv
  ENCOUNTER_Mailhot_V1.csv
  DEMOGRAPHIC_Mailhot_V1.csv
  TUMOR_REGISTRY1_Mailhot_V1.csv
  TUMOR_REGISTRY2_Mailhot_V1.csv
  TUMOR_REGISTRY3_Mailhot_V1.csv
  DISPENSING_Mailhot_V1.csv
  MED_ADMIN_Mailhot_V1.csv
  LAB_RESULT_Mailhot_V1.csv         <-- NOTE: no _CM
  PROVIDER_Mailhot_V1.csv
  DEATH_Mailhot_V1.csv

Column headers per table (from R/01_load_pcornet.R specs):
  ENROLLMENT:      ID,ENR_START_DATE,ENR_END_DATE,CHART,ENR_BASIS,SOURCE
  DIAGNOSIS:       DIAGNOSISID,ID,ENCOUNTERID,ENC_TYPE,ADMIT_DATE,PROVIDERID,DX,DX_TYPE,DX_DATE,DX_SOURCE,DX_ORIGIN,PDX,DX_POA,SOURCE
  CONDITION:       CONDITIONID,ID,ENCOUNTERID,CONDITION,CONDITION_TYPE,CONDITION_SOURCE,CONDITION_STATUS,ONSET_DATE,REPORT_DATE,RESOLVE_DATE,RAW_CONDITION_TYPE,RAW_CONDITION_SOURCE
  PROCEDURES:      PROCEDURESID,ID,ENCOUNTERID,ENC_TYPE,ADMIT_DATE,PROVIDERID,PX_DATE,PX,PX_TYPE,PX_SOURCE,PPX,SOURCE
  PRESCRIBING:     PRESCRIBINGID,ID,ENCOUNTERID,RX_PROVIDERID,RX_ORDER_DATE,RX_ORDER_TIME,RX_START_DATE,RX_END_DATE,RX_DOSE_ORDERED,RX_DOSE_ORDERED_UNIT,RX_QUANTITY,RX_DOSE_FORM,RX_REFILLS,RX_DAYS_SUPPLY,RX_FREQUENCY,RX_PRN_FLAG,RX_ROUTE,RX_BASIS,RXNORM_CUI,RX_SOURCE,RX_DISPENSE_AS_WRITTEN,RAW_RX_MED_NAME,RAW_RXNORM_CUI,SOURCE
  ENCOUNTER:       ENCOUNTERID,ID,ADMIT_DATE,ADMIT_TIME,DISCHARGE_DATE,DISCHARGE_TIME,PROVIDERID,FACILITY_LOCATION,ENC_TYPE,FACILITYID,DISCHARGE_DISPOSITION,DISCHARGE_STATUS,DRG,DRG_TYPE,ADMITTING_SOURCE,PAYER_TYPE_PRIMARY,PAYER_TYPE_SECONDARY,FACILITY_TYPE,SOURCE
  DEMOGRAPHIC:     ID,BIRTH_DATE,BIRTH_TIME,SEX,SEXUAL_ORIENTATION,GENDER_IDENTITY,HISPANIC,RACE,BIOBANK_FLAG,PAT_PREF_LANGUAGE_SPOKEN,ZIP_CODE,SOURCE
  DEATH:           ID,DEATH_DATE,DEATH_DATE_IMPUTE,DEATH_SOURCE,DEATH_MATCH_CONFIDENCE,SOURCE
  TUMOR_REGISTRY1: ID,SOURCE,SITE_CODE,HISTOLOGICAL_TYPE,HISTOLOGICAL_TYPE_DESCRIPTION,BEHAVIOR_CODE,GRADE,AGE_AT_DIAGNOSIS,TUMOR_SIZE_SUMMARY,DX_DATE_TR (pipeline-used subset)
  TUMOR_REGISTRY2: ID,SOURCE,SITE,MORPH,DXAGE,DX_DATE_TR (pipeline-used subset)
  TUMOR_REGISTRY3: ID,SOURCE,SITE,MORPH,DXAGE,DX_DATE_TR (pipeline-used subset)
  DISPENSING:      DISPENSINGID,PRESCRIBINGID,ID,DISPENSE_DATE,NDC,DISPENSE_SUP,DISPENSE_AMT,DISPENSE_DOSE_DISP,DISPENSE_DOSE_DISP_UNIT,DISPENSE_ROUTE,RAW_NDC,RXNORM_CUI,DISPENSE_SOURCE,RAW_DISPENSE_MED_NAME,SOURCE
  MED_ADMIN:       MEDADMINID,ID,ENCOUNTERID,PRESCRIBINGID,MEDADMIN_CODE,MEDADMIN_TYPE,MEDADMIN_START_DATE,MEDADMIN_STOP_DATE,MEDADMIN_ROUTE,RXNORM_CUI,RAW_MEDADMIN_MED_NAME,SOURCE
  LAB_RESULT_CM:   LAB_RESULTID,ID,ENCOUNTERID,LAB_ORDER_DATE,RESULT_DATE,SPECIMEN_DATE,LAB_LOINC,LAB_LOINC_SOURCE,LAB_PX,LAB_PX_TYPE,RESULT_NUM,RESULT_UNIT,RESULT_QUAL,RESULT_MODIFIER,SPECIMEN_SOURCE,ABN_IND,NORM_RANGE_HIGH,NORM_RANGE_LOW,RAW_LAB_CODE,RAW_LAB_NAME,RAW_RESULT,SOURCE
  PROVIDER:        PROVIDERID,PROVIDER_SEX,PROVIDER_SPECIALTY_PRIMARY,SOURCE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write all 15 fixture CSV files to tests/fixtures/</name>
  <files>
    tests/fixtures/ENROLLMENT_Mailhot_V1.csv
    tests/fixtures/DIAGNOSIS_Mailhot_V1.csv
    tests/fixtures/CONDITION_Mailhot_V1.csv
    tests/fixtures/PROCEDURES_Mailhot_V1.csv
    tests/fixtures/PRESCRIBING_Mailhot_V1.csv
    tests/fixtures/ENCOUNTER_Mailhot_V1.csv
    tests/fixtures/DEMOGRAPHIC_Mailhot_V1.csv
    tests/fixtures/TUMOR_REGISTRY1_Mailhot_V1.csv
    tests/fixtures/TUMOR_REGISTRY2_Mailhot_V1.csv
    tests/fixtures/TUMOR_REGISTRY3_Mailhot_V1.csv
    tests/fixtures/DISPENSING_Mailhot_V1.csv
    tests/fixtures/MED_ADMIN_Mailhot_V1.csv
    tests/fixtures/LAB_RESULT_Mailhot_V1.csv
    tests/fixtures/PROVIDER_Mailhot_V1.csv
    tests/fixtures/DEATH_Mailhot_V1.csv
  </files>
  <read_first>
    - tests/generate_fixtures.R (created in Plan 01 -- THE source of truth for all fixture data; read every tribble() definition and transcribe exactly to CSV)
    - tests/fixtures/FIXTURE_DESIGN.md (created in Plan 01 -- cross-reference patient assignments)
    - R/00_config.R (lines 225-251 for PCORNET_TABLES and PCORNET_PATHS, especially line 251 LAB_RESULT_CM override)
    - R/01_load_pcornet.R (lines 56-401 for column specs -- verify CSV headers match)
  </read_first>
  <action>
Write each of the 15 CSV files using the Write tool. The data for each CSV comes DIRECTLY from the tribble() definitions in tests/generate_fixtures.R (created in Plan 01). Transcribe the data exactly -- same values, same column order, same NA handling.

**CSV format rules:**
- First row is header (comma-separated column names, no spaces after commas)
- NA values are empty strings (matching write_csv(na="") behavior)
- Character values are NOT quoted unless they contain commas (standard CSV)
- Numeric values are plain numbers (50, not 50.0 unless the spec uses col_double)
- Integer values are plain integers (0, 1, not 0L)
- No trailing newline after last data row

**File-by-file instructions:**

1. **ENROLLMENT_Mailhot_V1.csv**: Header: ID,ENR_START_DATE,ENR_END_DATE,CHART,ENR_BASIS,SOURCE. 20 data rows (one per PT001-PT020). PT009 has ENR_START_DATE=1900-01-01. Copy enrollment data exactly from generate_enrollment() tribble.

2. **DIAGNOSIS_Mailhot_V1.csv**: Header: DIAGNOSISID,ID,ENCOUNTERID,ENC_TYPE,ADMIT_DATE,PROVIDERID,DX,DX_TYPE,DX_DATE,DX_SOURCE,DX_ORIGIN,PDX,DX_POA,SOURCE. ~18 data rows. Multi-row patients: PT005 (DX005A+DX005B), PT007 (DX007A+DX007B), PT010 (DX010A+DX010B). Copy from generate_diagnosis() tribble.

3. **CONDITION_Mailhot_V1.csv**: Header: CONDITIONID,ID,ENCOUNTERID,CONDITION,CONDITION_TYPE,CONDITION_SOURCE,CONDITION_STATUS,ONSET_DATE,REPORT_DATE,RESOLVE_DATE,RAW_CONDITION_TYPE,RAW_CONDITION_SOURCE. 1 data row. Copy from generate_condition() tribble.

4. **PROCEDURES_Mailhot_V1.csv**: Header: PROCEDURESID,ID,ENCOUNTERID,ENC_TYPE,ADMIT_DATE,PROVIDERID,PX_DATE,PX,PX_TYPE,PX_SOURCE,PPX,SOURCE. 1 data row (PT004 SCT). Copy from generate_procedures() tribble.

5. **PRESCRIBING_Mailhot_V1.csv**: Header: all 24 PRESCRIBING_SPEC columns. 4 data rows (PT012 ABVD). Copy from generate_prescribing() tribble. Note: RX_DOSE_ORDERED is numeric (50, 10, 6, 375). RX_REFILLS and RX_DAYS_SUPPLY are integers (0, 1).

6. **ENCOUNTER_Mailhot_V1.csv**: Header: all 19 ENCOUNTER_SPEC columns. ~20 data rows. PT008 has 2 rows same ADMIT_DATE. AV encounters have empty DISCHARGE_DATE, DISCHARGE_DISPOSITION, etc. Copy from generate_encounter() tribble.

7. **DEMOGRAPHIC_Mailhot_V1.csv**: Header: all 12 DEMOGRAPHIC_SPEC columns. 20 data rows. Copy from generate_demographic() tribble. SEXUAL_ORIENTATION and GENDER_IDENTITY and BIOBANK_FLAG are empty (NA).

8. **TUMOR_REGISTRY1_Mailhot_V1.csv**: Header only (pipeline-used columns): ID,SOURCE,SITE_CODE,HISTOLOGICAL_TYPE,HISTOLOGICAL_TYPE_DESCRIPTION,BEHAVIOR_CODE,GRADE,AGE_AT_DIAGNOSIS,TUMOR_SIZE_SUMMARY,DX_DATE_TR. Zero data rows.

9. **TUMOR_REGISTRY2_Mailhot_V1.csv**: Header only: ID,SOURCE,SITE,MORPH,DXAGE,DX_DATE_TR. Zero data rows.

10. **TUMOR_REGISTRY3_Mailhot_V1.csv**: Header only: ID,SOURCE,SITE,MORPH,DXAGE,DX_DATE_TR. Zero data rows.

11. **DISPENSING_Mailhot_V1.csv**: Header only: all 15 DISPENSING_SPEC columns. Zero data rows.

12. **MED_ADMIN_Mailhot_V1.csv**: Header only: all 12 MED_ADMIN_SPEC columns. Zero data rows.

13. **LAB_RESULT_Mailhot_V1.csv**: CRITICAL -- filename is LAB_RESULT, NOT LAB_RESULT_CM. Header only: all LAB_RESULT_CM_SPEC columns. Zero data rows.

14. **PROVIDER_Mailhot_V1.csv**: Header: PROVIDERID,PROVIDER_SEX,PROVIDER_SPECIALTY_PRIMARY,SOURCE. 1-2 data rows (PROV001, PROV002).

15. **DEATH_Mailhot_V1.csv**: Header: ID,DEATH_DATE,DEATH_DATE_IMPUTE,DEATH_SOURCE,DEATH_MATCH_CONFIDENCE,SOURCE. 1 data row (PT006).

Write each file using the Write tool. Do NOT use Bash heredocs or echo commands for file creation.
  </action>
  <verify>
    <automated>MISSING -- verify via file existence checks and content grep. Run: ls tests/fixtures/*.csv | wc -l (should output 15). Verify LAB_RESULT_Mailhot_V1.csv exists (not LAB_RESULT_CM_).</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 15 CSV files exist in tests/fixtures/
    - File named LAB_RESULT_Mailhot_V1.csv exists (NOT LAB_RESULT_CM_Mailhot_V1.csv)
    - ENROLLMENT CSV contains 20 data rows (+ 1 header = 21 lines)
    - ENROLLMENT CSV header is: ID,ENR_START_DATE,ENR_END_DATE,CHART,ENR_BASIS,SOURCE
    - ENROLLMENT CSV contains "1900-01-01" (PT009 sentinel date)
    - DIAGNOSIS CSV contains "C81.00" (PT003 NLPHL)
    - DIAGNOSIS CSV contains "201.90" (PT010 ICD-9)
    - DIAGNOSIS CSV contains "Z51.11" (PT007 orphan dx)
    - ENCOUNTER CSV contains payer code "14" (PT002 dual-eligible)
    - ENCOUNTER CSV contains payer code "NI" (PT011 missing payer)
    - ENCOUNTER CSV has 2 rows with ADMIT_DATE "2013-07-10" for PT008
    - PRESCRIBING CSV contains RXNORM_CUI values "3639","11213","67228","3946"
    - PRESCRIBING CSV has exactly 4 data rows
    - PROCEDURES CSV contains PX value "38241" (PT004 SCT)
    - DEATH CSV contains "PT006" and DEATH_DATE "2014-06-15"
    - DEMOGRAPHIC CSV has 20 data rows
    - TUMOR_REGISTRY1, TUMOR_REGISTRY2, TUMOR_REGISTRY3 CSVs have header-only (1 line each)
    - DISPENSING, MED_ADMIN, LAB_RESULT CSVs have header-only (1 line each)
    - No CSV file contains "LAB_RESULT_CM_Mailhot" as a filename
    - PROVIDER CSV has 1-2 data rows
    - CONDITION CSV has 1 data row
  </acceptance_criteria>
  <done>All 15 PCORnet CDM fixture CSV files exist in tests/fixtures/ with correct file naming (including LAB_RESULT override), correct column headers matching production specs, and data faithfully transcribed from generate_fixtures.R tribble() definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Verify fixture git tracking and total size</name>
  <files>tests/fixtures/.gitkeep</files>
  <read_first>
    - .gitignore (verify CSVs in tests/fixtures/ are NOT excluded -- .gitignore excludes tests/fixtures/*.rds and tests/fixtures/*.duckdb but NOT *.csv)
    - tests/fixtures/FIXTURE_DESIGN.md (verify it exists)
    - tests/generate_fixtures.R (verify it exists)
  </read_first>
  <action>
Verify that all fixture artifacts are properly set up for git tracking (per FIX-05):

1. **Check .gitignore does NOT exclude fixture CSVs.** The .gitignore should have rules for `tests/fixtures/*.rds` and `tests/fixtures/*.duckdb` (added in Phase 83) but NOT `tests/fixtures/*.csv`. Read .gitignore and confirm.

2. **Count fixture files.** Run `ls tests/fixtures/*.csv` and confirm exactly 15 CSV files are listed.

3. **Verify LAB_RESULT filename.** Confirm `tests/fixtures/LAB_RESULT_Mailhot_V1.csv` exists and `tests/fixtures/LAB_RESULT_CM_Mailhot_V1.csv` does NOT exist.

4. **Check total fixture size.** All CSVs combined should be well under 1MB. Run a size check and report total.

5. **Spot-check edge case data.** Run quick greps to confirm:
   - PT002 dual-eligible: grep "14" in ENCOUNTER CSV
   - PT003 NLPHL: grep "C81.00" in DIAGNOSIS CSV
   - PT009 sentinel: grep "1900-01-01" in ENROLLMENT CSV
   - PT010 cross-system: grep "201.90" in DIAGNOSIS CSV
   - PT012 ABVD: grep "3639" in PRESCRIBING CSV

6. **Remove .gitkeep** if it's still present (no longer needed since directory has real files). Or keep it -- either is fine.

7. **Stage all fixture files** for git visibility: run `git add tests/fixtures/*.csv tests/fixtures/FIXTURE_DESIGN.md tests/generate_fixtures.R` to confirm they are trackable (not blocked by .gitignore).

If any check fails, fix the issue before proceeding.
  </action>
  <verify>
    <automated>git status tests/fixtures/ tests/generate_fixtures.R -- should show 15 CSV files, 1 FIXTURE_DESIGN.md, and 1 generate_fixtures.R as new/staged files (not ignored)</automated>
  </verify>
  <acceptance_criteria>
    - `git status` shows tests/fixtures/*.csv files as trackable (new file or staged), NOT ignored
    - `git status` shows tests/generate_fixtures.R as trackable
    - `git status` shows tests/fixtures/FIXTURE_DESIGN.md as trackable
    - Total CSV file count in tests/fixtures/ is exactly 15
    - LAB_RESULT_Mailhot_V1.csv exists, LAB_RESULT_CM_Mailhot_V1.csv does NOT exist
    - Combined size of all CSVs is under 1MB
    - .gitignore does NOT contain a rule that would exclude tests/fixtures/*.csv
  </acceptance_criteria>
  <done>All 15 fixture CSVs, FIXTURE_DESIGN.md, and generate_fixtures.R are git-tracked. Total fixture size is under 1MB. No .gitignore rule blocks CSV files. LAB_RESULT filename override is correct.</done>
</task>

</tasks>

<verification>
1. 15 CSV files exist in tests/fixtures/ with correct _Mailhot_V1.csv naming
2. LAB_RESULT uses correct filename (no _CM suffix)
3. All column headers match R/01_load_pcornet.R specs
4. Edge case data is present: dual-eligible (14), NLPHL (C81.00), SCT (38241), ABVD (3639/11213/67228/3946), sentinel (1900-01-01), cross-system (201.90), missing payer (NI), orphan (Z51.11)
5. All files are git-tracked (not excluded by .gitignore)
6. Total size under 1MB
</verification>

<success_criteria>
- All 15 PCORnet CDM fixture CSVs exist in tests/fixtures/ with production-matching filenames and column headers
- CSV data faithfully represents the tribble() definitions from generate_fixtures.R
- All files are git-trackable and combined size is under 1MB
- Edge case data is verifiable via simple grep commands
</success_criteria>

<output>
After completion, create `.planning/phases/84-test-fixture-design-creation/84-02-SUMMARY.md`
</output>
