---
phase: 62-first-line-therapy-and-death-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/62_first_line_and_death_analysis.R
autonomous: false
requirements:
  - FLT-01
  - FLT-02
  - DEATH-01
  - DEATH-02
  - DEATH-03

must_haves:
  truths:
    - "Chemotherapy episodes with regimen labels for adults 21+ have is_first_line column in treatment_episodes.rds"
    - "Only the FIRST qualifying episode per patient has is_first_line=TRUE"
    - "60-day clean period checks ALL individual chemo dates, not just episode boundaries"
    - "Death analysis summary table shows count of patients with validated death dates"
    - "Death analysis shows count where death is last encounter (no ENCOUNTER after DEATH_DATE)"
    - "Death analysis shows post-death encounter count stratified by ENC_TYPE"
  artifacts:
    - path: "R/62_first_line_and_death_analysis.R"
      provides: "First-line therapy flagging and death analysis tables"
      min_lines: 250
    - path: "output/death_analysis.xlsx"
      provides: "Multi-sheet death analysis report (generated on execution)"
    - path: "output/death_analysis.csv"
      provides: "Flat death analysis export (generated on execution)"
  key_links:
    - from: "R/62_first_line_and_death_analysis.R"
      to: "treatment_episodes.rds"
      via: "readRDS + saveRDS in-place enrichment with is_first_line column"
      pattern: "readRDS.*treatment_episodes\\.rds"
    - from: "R/62_first_line_and_death_analysis.R"
      to: "treatment_episode_detail.rds"
      via: "readRDS for all individual chemo dates (60-day lookback)"
      pattern: "readRDS.*treatment_episode_detail\\.rds"
    - from: "R/62_first_line_and_death_analysis.R"
      to: "validated_death_dates.rds"
      via: "readRDS for validated deaths (Phase 59 artifact)"
      pattern: "readRDS.*validated_death_dates\\.rds"
    - from: "R/62_first_line_and_death_analysis.R"
      to: "DuckDB ENCOUNTER table"
      via: "get_pcornet_table for max ADMIT_DATE and ENC_TYPE stratification"
      pattern: 'get_pcornet_table\("ENCOUNTER"\)'
    - from: "R/62_first_line_and_death_analysis.R"
      to: "DuckDB DEMOGRAPHIC table"
      via: "get_pcornet_table for BIRTH_DATE (age 21+ calculation)"
      pattern: 'get_pcornet_table\("DEMOGRAPHIC"\)'
---

<objective>
Create R/62_first_line_and_death_analysis.R — a combined script that (1) identifies first-line chemotherapy for adult HL patients aged 21+ using a 60-day clean period with no prior chemotherapy, adds is_first_line boolean to treatment_episodes.rds, and (2) produces death date data quality summary tables showing patient counts with death dates, death-as-last-encounter counts, and post-death encounter counts stratified by PCORnet ENC_TYPE.

Purpose: First-line therapy identification is the key clinical classification needed for Phase 63 Gantt v2 export. Death analysis tables provide data quality metrics for the PCORnet extract's death date reliability.

Output: R/62_first_line_and_death_analysis.R script, modified treatment_episodes.rds (+ is_first_line column), death_analysis.xlsx (3 sheets), death_analysis.csv
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-first-line-therapy-and-death-analysis/62-CONTEXT.md
@.planning/phases/62-first-line-therapy-and-death-analysis/62-RESEARCH.md
@.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-03-SUMMARY.md
@.planning/phases/59-death-date-validation-and-treatment-timeline-cleanup/59-01-SUMMARY.md

<interfaces>
<!-- Key schemas and contracts the executor needs. Extracted from codebase. -->

From R/44a_treatment_episodes.R — treatment_episodes.rds schema (per-episode):
```
patient_id, treatment_type, episode_number, episode_start, episode_stop,
episode_length_days, distinct_dates_in_episode, historical_flag,
triggering_codes, encounter_ids, drug_names
```

From R/44a_treatment_episodes.R — treatment_episode_detail.rds schema (per-date):
```
patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name,
episode_number, episode_start, episode_stop, historical_flag
```

From R/59_death_date_validation.R — validated_death_dates.rds schema:
```
ID, DEATH_DATE, DEATH_SOURCE, death_valid, post_death_activity
```
Note: death_valid=FALSE for impossible deaths (before earliest treatment).
      post_death_activity=TRUE/FALSE for valid deaths with/without post-death encounters.

From R/00_config.R — path configuration:
```r
CONFIG$cache$outputs_dir  # RDS files: treatment_episodes.rds, validated_death_dates.rds
CONFIG$output_dir         # Output files: xlsx, csv
```

From R/utils_duckdb.R — query dispatcher:
```r
get_pcornet_table("DEMOGRAPHIC")  # Returns lazy DuckDB table
get_pcornet_table("ENCOUNTER")    # Returns lazy DuckDB table
```

From R/utils_dates.R — date parsing:
```r
parse_pcornet_date(date_column)  # Parses PCORnet date strings to R Date
```

From R/59_death_date_validation.R — age calculation pattern (lines 308-309):
```r
age_at_death = as.numeric(difftime(DEATH_DATE, BIRTH_DATE, units = "days")) / 365.25
```

From R/59_death_date_validation.R — openxlsx2 styling pattern:
```r
wb$add_fill(sheet = "...", dims = "A1:B1", color = wb_color("FF374151"))
wb$add_font(sheet = "...", dims = "A1:B1", name = "Calibri", size = 11, bold = TRUE, color = wb_color("FFFFFFFF"))
wb$freeze_pane(sheet = "...", firstActiveRow = 2)
```

Phase 61 adds `regimen_label` column to treatment_episodes.rds. This column is the gatekeeper for first-line eligibility (D-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/62_first_line_and_death_analysis.R with first-line flagging and death analysis</name>
  <files>R/62_first_line_and_death_analysis.R</files>

  <read_first>
    R/44a_treatment_episodes.R
    R/59_death_date_validation.R
    R/00_config.R
    R/utils_duckdb.R
    R/utils_dates.R
    .planning/phases/62-first-line-therapy-and-death-analysis/62-CONTEXT.md
    .planning/phases/62-first-line-therapy-and-death-analysis/62-RESEARCH.md
  </read_first>

  <action>
Create R/62_first_line_and_death_analysis.R with the following structure. Per D-11, this is a single combined script handling both first-line therapy flagging and death analysis.

**HEADER (lines 1-30):**
```r
# R/62_first_line_and_death_analysis.R
# Phase 62: First-Line Therapy Identification & Death Analysis
#
# Part 1: Identify first-line chemotherapy for adults 21+ at treatment date
#   - Only episodes with regimen_label (ABVD, BV+AVD, Nivo+AVD) qualify (D-01)
#   - Age 21+ calculated at episode_start using DEMOGRAPHIC.BIRTH_DATE (D-02)
#   - 60-day clean period: no chemotherapy dates in 60 days before episode_start (D-03)
#   - Only FIRST qualifying episode per patient gets is_first_line=TRUE (D-04)
#   - is_first_line column added to treatment_episodes.rds in-place (D-09)
#
# Part 2: Death date data quality analysis
#   - Uses validated_death_dates.rds (D-05, D-12) — does NOT re-query DEATH table
#   - DEATH-01: Count of patients with validated death dates
#   - DEATH-02: Count where death is last encounter (DEATH_DATE >= max(ADMIT_DATE)) (D-06)
#   - DEATH-03: Count with post-death encounters, stratified by ENC_TYPE (D-07, D-08)
#
# Outputs: modified treatment_episodes.rds, death_analysis.xlsx, death_analysis.csv
```

**SECTION 1 — Setup (lines ~31-55):**
- `source("R/00_config.R")`
- `source("R/utils_duckdb.R")`
- `source("R/utils_dates.R")`
- `library(dplyr)`, `library(glue)`, `library(openxlsx2)`, `library(lubridate)`
- Define output paths:
  - `OUTPUT_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")` (overwrite in-place)
  - `DETAIL_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail.rds")` (read-only)
  - `DEATH_RDS <- file.path(CONFIG$cache$outputs_dir, "validated_death_dates.rds")` (read-only)
  - `OUTPUT_XLSX <- file.path(CONFIG$output_dir, "death_analysis.xlsx")`
  - `OUTPUT_CSV <- file.path(CONFIG$output_dir, "death_analysis.csv")`
- Banner message with glue: `"=== Phase 62: First-Line Therapy & Death Analysis ==="`
- Log all input/output paths

**SECTION 2 — Load Data (lines ~56-95):**
- Load treatment_episodes.rds into `episodes`
- Load treatment_episode_detail.rds into `episode_detail`
- Load validated_death_dates.rds into `validated_deaths`
- Query DEMOGRAPHIC table: `get_pcornet_table("DEMOGRAPHIC") %>% select(ID, BIRTH_DATE) %>% collect() %>% mutate(BIRTH_DATE = parse_pcornet_date(BIRTH_DATE))`
- Guard for missing regimen_label column: `if (!"regimen_label" %in% names(episodes)) { episodes <- episodes %>% mutate(regimen_label = NA_character_); warning("regimen_label column not found in treatment_episodes.rds — Phase 61 not yet run. First-line detection will produce 0 results.") }`
- Log counts: total episodes, total detail rows, total validated deaths, total demographics

**SECTION 3 — First-Line Therapy Identification (lines ~96-200):**

Step 3a: Filter to eligible episodes (per D-01: must have regimen_label)
```r
eligible_episodes <- episodes %>%
  filter(treatment_type == "Chemotherapy") %>%
  filter(!is.na(regimen_label))
```
Log: `"  Episodes with regimen labels: {nrow(eligible_episodes)} from {n_distinct(eligible_episodes$patient_id)} patients"`

Step 3b: Join demographics and filter to adults 21+ (per D-02)
```r
eligible_episodes <- eligible_episodes %>%
  left_join(demographics, by = c("patient_id" = "ID")) %>%
  mutate(age_at_treatment = as.numeric(difftime(episode_start, BIRTH_DATE, units = "days")) / 365.25) %>%
  filter(age_at_treatment >= 21)
```
Log: `"  Adults 21+ at treatment: {nrow(eligible_episodes)} episodes from {n_distinct(eligible_episodes$patient_id)} patients"`

Step 3c: Check 60-day clean period using ALL individual chemo dates (per D-03)
- Extract ALL chemo dates from episode_detail (NOT episode boundaries):
```r
all_chemo_dates <- episode_detail %>%
  filter(treatment_type == "Chemotherapy") %>%
  select(patient_id, treatment_date) %>%
  distinct()
```
- For each eligible episode, check if ANY prior chemo date falls within 60 days BEFORE episode_start:
```r
episodes_with_lookback <- eligible_episodes %>%
  select(patient_id, episode_number, episode_start) %>%
  left_join(all_chemo_dates, by = "patient_id", relationship = "many-to-many") %>%
  mutate(
    days_before = as.numeric(difftime(episode_start, treatment_date, units = "days")),
    is_prior_chemo_in_window = (days_before > 0 & days_before <= 60)
  ) %>%
  group_by(patient_id, episode_number) %>%
  summarise(
    has_prior_chemo_within_60d = any(is_prior_chemo_in_window, na.rm = TRUE),
    .groups = "drop"
  )
```
- Filter to episodes with clean 60-day period:
```r
clean_episodes <- eligible_episodes %>%
  left_join(episodes_with_lookback, by = c("patient_id", "episode_number")) %>%
  filter(!has_prior_chemo_within_60d | is.na(has_prior_chemo_within_60d))
```
Log: `"  Episodes with 60-day clean period: {nrow(clean_episodes)} from {n_distinct(clean_episodes$patient_id)} patients"`

Step 3d: Flag ONLY first qualifying episode per patient (per D-04)
```r
first_line_flags <- clean_episodes %>%
  group_by(patient_id) %>%
  arrange(episode_start) %>%
  mutate(rank = row_number()) %>%
  ungroup() %>%
  mutate(is_first_line = (rank == 1)) %>%
  select(patient_id, episode_number, is_first_line)
```
- Keep only is_first_line=TRUE rows (the first qualifying per patient):
```r
first_line_ids <- first_line_flags %>%
  filter(is_first_line) %>%
  select(patient_id, episode_number)
```
Log: `"  First-line episodes identified: {nrow(first_line_ids)} patients"`

Step 3e: Enrich treatment_episodes.rds in-place (per D-09)
```r
episodes <- episodes %>%
  left_join(
    first_line_ids %>% mutate(is_first_line = TRUE),
    by = c("patient_id", "episode_number")
  ) %>%
  mutate(is_first_line = if_else(is.na(is_first_line), FALSE, is_first_line))
```
- Save modified RDS: `saveRDS(episodes, OUTPUT_RDS)`
Log: `"  Saved treatment_episodes.rds with is_first_line column ({sum(episodes$is_first_line)} TRUE, {sum(!episodes$is_first_line)} FALSE)"`

Step 3f: Build first-line summary for xlsx (Claude's discretion — add as Sheet 3)
```r
first_line_summary <- episodes %>%
  filter(is_first_line) %>%
  select(patient_id, treatment_type, episode_number, episode_start,
         episode_stop, regimen_label, drug_names, is_first_line)
```
- Also compute a regimen distribution summary:
```r
regimen_dist <- first_line_summary %>%
  count(regimen_label, name = "n_patients") %>%
  arrange(desc(n_patients))
```
Log: `"  First-line regimen distribution:"` followed by printing regimen_dist

**SECTION 4 — Death Analysis (lines ~201-310):**

Step 4a: Filter to validated deaths only (per D-05)
```r
valid_deaths <- validated_deaths %>%
  filter(death_valid == TRUE)
```
- DEATH-01 count: `n_with_death <- nrow(valid_deaths)`
Log: `"  DEATH-01: Patients with validated death dates: {n_with_death}"`

Step 4b: Death as last encounter (per D-06)
- Query ENCOUNTER table for max ADMIT_DATE per patient:
```r
last_encounters <- get_pcornet_table("ENCOUNTER") %>%
  select(ID, ADMIT_DATE) %>%
  collect() %>%
  mutate(ADMIT_DATE = parse_pcornet_date(ADMIT_DATE)) %>%
  filter(!is.na(ADMIT_DATE)) %>%
  group_by(ID) %>%
  summarise(last_encounter_date = max(ADMIT_DATE), .groups = "drop")
```
- Join validated deaths to last encounter and compare:
```r
death_vs_encounter <- valid_deaths %>%
  left_join(last_encounters, by = "ID") %>%
  mutate(
    has_encounters = !is.na(last_encounter_date),
    death_is_last = case_when(
      is.na(last_encounter_date) ~ TRUE,
      DEATH_DATE >= last_encounter_date ~ TRUE,
      TRUE ~ FALSE
    )
  )
```
- DEATH-02 count: `n_death_is_last <- sum(death_vs_encounter$death_is_last, na.rm = TRUE)`
- Also track no-encounter edge case: `n_no_encounters <- sum(!death_vs_encounter$has_encounters)`
Log: `"  DEATH-02: Death is last encounter: {n_death_is_last} (includes {n_no_encounters} with no encounter records)"`

Step 4c: Post-death encounters stratified by ENC_TYPE (per D-07, D-08)
- Total count using Phase 59's flag (per D-08): `n_post_death <- sum(valid_deaths$post_death_activity, na.rm = TRUE)`
Log: `"  DEATH-03: Patients with post-death activity: {n_post_death} (from Phase 59 flag)"`
- Query ENCOUNTER for NEW ENC_TYPE stratification (per D-07):
```r
post_death_by_enc_type <- get_pcornet_table("ENCOUNTER") %>%
  select(ID, ADMIT_DATE, ENC_TYPE) %>%
  collect() %>%
  mutate(ADMIT_DATE = parse_pcornet_date(ADMIT_DATE)) %>%
  filter(!is.na(ADMIT_DATE)) %>%
  inner_join(valid_deaths %>% select(ID, DEATH_DATE), by = "ID") %>%
  filter(ADMIT_DATE > DEATH_DATE)
```
- Patient-level count by ENC_TYPE (unique patients per type, not encounter count):
```r
enc_type_patient_counts <- post_death_by_enc_type %>%
  distinct(ID, ENC_TYPE) %>%
  count(ENC_TYPE, name = "n_patients") %>%
  arrange(desc(n_patients))
```
- Encounter-level count by ENC_TYPE:
```r
enc_type_encounter_counts <- post_death_by_enc_type %>%
  count(ENC_TYPE, name = "n_encounters") %>%
  arrange(desc(n_encounters))
```
- Combine into detail table:
```r
enc_type_detail <- enc_type_encounter_counts %>%
  left_join(enc_type_patient_counts, by = "ENC_TYPE") %>%
  arrange(desc(n_encounters))
```
Log the ENC_TYPE breakdown

**SECTION 5 — Output: xlsx (lines ~311-420):**

Per D-10: Styled multi-sheet xlsx with openxlsx2.

Sheet 1: "Death Analysis Summary"
- Create summary tibble with these exact rows:
```r
summary_stats <- tibble(
  Metric = c(
    "Total patients with validated death dates (DEATH-01)",
    "Death is last encounter (DEATH-02)",
    "  - With encounter records",
    "  - Without encounter records (death is last by default)",
    "Patients with post-death clinical activity (DEATH-03)",
    "",
    "First-Line Therapy Summary",
    "Total first-line episodes identified",
    "Unique regimens among first-line"
  ),
  Count = c(
    n_with_death,
    n_death_is_last,
    n_death_is_last - n_no_encounters,
    n_no_encounters,
    n_post_death,
    NA_integer_,
    NA_integer_,
    nrow(first_line_summary),
    n_distinct(first_line_summary$regimen_label)
  )
)
```
- Style: dark gray header (#374151) with white text, Calibri 11pt bold, freeze pane row 2, column widths 60 and 15
- Follow exact styling from R/59_death_date_validation.R (lines 373-445)

Sheet 2: "Post-Death Encounters by Type"
- Write enc_type_detail tibble (ENC_TYPE, n_encounters, n_patients)
- Same header styling as Sheet 1
- Column widths: 15, 20, 20

Sheet 3: "First-Line Patient Detail" (Claude's discretion — QA review sheet)
- Write first_line_summary tibble (patient_id, treatment_type, episode_number, episode_start, episode_stop, regimen_label, drug_names, is_first_line)
- Same header styling
- Freeze pane row 2

Save workbook: `wb_save(wb, OUTPUT_XLSX, overwrite = TRUE)`
Log: `"Saved death_analysis.xlsx: {OUTPUT_XLSX}"`

**SECTION 6 — Output: CSV (lines ~421-440):**

Per D-10: Flat CSV export.
- Build CSV content from death analysis:
```r
death_csv <- death_vs_encounter %>%
  select(ID, DEATH_DATE, DEATH_SOURCE, death_valid, post_death_activity,
         has_encounters, last_encounter_date, death_is_last)
write.csv(death_csv, OUTPUT_CSV, row.names = FALSE)
```
Log: `"Saved death_analysis.csv: {OUTPUT_CSV}"`

**SECTION 7 — Final Summary (lines ~441-460):**
- Print completion banner
- Log all output file paths
- Log key metrics: first-line count, death analysis counts
- Print regimen distribution table

**IMPORTANT implementation notes:**
- Do NOT re-query the DEATH table (per D-12). Use validated_death_dates.rds exclusively.
- Do NOT flag unlabeled chemotherapy episodes as first-line (per D-01). The `filter(!is.na(regimen_label))` is the gatekeeper.
- Do NOT check radiation/SCT/immunotherapy in the 60-day lookback (per D-03). Only chemotherapy dates.
- Do NOT flag multiple episodes per patient as first-line (per D-04). Only `row_number() == 1` after `arrange(episode_start)`.
- The `regimen_label` column is added by Phase 61. If it does not yet exist in treatment_episodes.rds, guard with a check: `if (!"regimen_label" %in% names(episodes)) { ... add regimen_label = NA_character_ ... }`. Log a warning if regimen_label is missing and skip first-line detection (produce 0 first-line flags).
- Use `relationship = "many-to-many"` in the left_join for 60-day lookback to explicitly handle multiple chemo dates per episode.
- The `days_before > 0` condition excludes same-day chemo (days_before == 0 means treatment_date == episode_start, which is part of THIS episode, not prior chemo).
- The `days_before <= 60` condition: a treatment 60 days before episode_start IS within the window and DOES disqualify.
  </action>

  <verify>
    <automated>grep -c "is_first_line" R/62_first_line_and_death_analysis.R && grep -c "regimen_label" R/62_first_line_and_death_analysis.R && grep -c "death_valid" R/62_first_line_and_death_analysis.R && grep -c "get_pcornet_table" R/62_first_line_and_death_analysis.R && grep -c "ENC_TYPE" R/62_first_line_and_death_analysis.R && grep -c "wb_save" R/62_first_line_and_death_analysis.R && grep -c "saveRDS" R/62_first_line_and_death_analysis.R && grep -c "write.csv" R/62_first_line_and_death_analysis.R</automated>
  </verify>

  <acceptance_criteria>
    - R/62_first_line_and_death_analysis.R exists and has >= 250 lines
    - File contains `source("R/00_config.R")` and `source("R/utils_duckdb.R")` and `source("R/utils_dates.R")`
    - File contains `readRDS` calls for treatment_episodes.rds, treatment_episode_detail.rds, and validated_death_dates.rds
    - File contains `filter(!is.na(regimen_label))` for D-01 gating
    - File contains `age_at_treatment` calculation using `difftime(episode_start, BIRTH_DATE, units = "days")) / 365.25`
    - File contains `filter(age_at_treatment >= 21)` for D-02
    - File contains `filter(treatment_type == "Chemotherapy")` for extracting all chemo dates from episode_detail (D-03)
    - File contains `days_before > 0 & days_before <= 60` for 60-day lookback window
    - File contains `row_number() == 1` pattern for selecting only first qualifying episode per patient (D-04)
    - File contains `filter(death_valid == TRUE)` for using validated deaths only (D-05)
    - File contains `get_pcornet_table("ENCOUNTER")` for DEATH-02 and DEATH-03 queries (D-06, D-07)
    - File contains `get_pcornet_table("DEMOGRAPHIC")` for age calculation
    - File contains `DEATH_DATE >= last_encounter_date` for death-is-last logic (D-06)
    - File contains `filter(ADMIT_DATE > DEATH_DATE)` for post-death encounters (D-07)
    - File contains `count(ENC_TYPE` for ENC_TYPE stratification (D-07)
    - File contains `post_death_activity` reference from validated_deaths for DEATH-03 total (D-08)
    - File contains `saveRDS(` call for modified treatment_episodes.rds (D-09)
    - File contains `wb_workbook()` and `wb_save(` for xlsx output (D-10)
    - File contains `add_worksheet` calls for at least 2 sheets (D-10)
    - File contains `write.csv(` for CSV output (D-10)
    - File does NOT contain `get_pcornet_table("DEATH")` (D-12: must not re-query DEATH table)
    - File contains `relationship = "many-to-many"` in the lookback join
    - File contains guard for missing regimen_label column: `"regimen_label" %in% names(`
  </acceptance_criteria>

  <done>
    R/62_first_line_and_death_analysis.R is complete with:
    (1) First-line therapy identification: regimen-labeled chemo episodes for adults 21+ with 60-day clean period, only first qualifying episode per patient flagged, is_first_line column saved to treatment_episodes.rds in-place
    (2) Death analysis tables: DEATH-01 (validated death count), DEATH-02 (death as last encounter count), DEATH-03 (post-death encounter count with ENC_TYPE stratification)
    (3) Multi-sheet death_analysis.xlsx with summary counts + ENC_TYPE detail + first-line patient detail
    (4) Flat death_analysis.csv export
    Script runs on HiPerGator via source("R/62_first_line_and_death_analysis.R")
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify script on HiPerGator</name>
  <files>R/62_first_line_and_death_analysis.R</files>
  <action>
    Human verifies the script output on HiPerGator after running source("R/62_first_line_and_death_analysis.R"). No Claude action required — this is a verification checkpoint.
  </action>
  <what-built>
    R/62_first_line_and_death_analysis.R — combined first-line therapy identification and death analysis script.
    The script:
    1. Loads treatment_episodes.rds, treatment_episode_detail.rds, validated_death_dates.rds, and queries DEMOGRAPHIC/ENCOUNTER via DuckDB
    2. Identifies first-line chemotherapy episodes (regimen-labeled, adults 21+, 60-day clean period, first per patient)
    3. Adds is_first_line boolean column to treatment_episodes.rds in-place
    4. Produces death_analysis.xlsx (3 sheets: summary, ENC_TYPE detail, first-line patient detail) and death_analysis.csv
  </what-built>
  <how-to-verify>
    1. On HiPerGator RStudio, run: source("R/62_first_line_and_death_analysis.R")
    2. Check console output for:
       - Episode counts at each filter step (regimen-labeled, adults 21+, 60-day clean period, first-line)
       - DEATH-01, DEATH-02, DEATH-03 counts
       - Regimen distribution table
    3. Open output/death_analysis.xlsx:
       - Sheet 1 "Death Analysis Summary": verify DEATH-01/02/03 counts match console
       - Sheet 2 "Post-Death Encounters by Type": verify ENC_TYPE breakdown
       - Sheet 3 "First-Line Patient Detail": verify patient list with regimen labels
    4. Verify treatment_episodes.rds has is_first_line column:
       eps <- readRDS(file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds"))
       "is_first_line" %in% names(eps)  # Should be TRUE
       table(eps$is_first_line)          # Should show TRUE/FALSE counts
    5. Verify only one is_first_line=TRUE per patient:
       eps %>% filter(is_first_line) %>% count(patient_id) %>% filter(n > 1)  # Should be 0 rows
    6. Verify output/death_analysis.csv exists and has expected columns
  </how-to-verify>
  <verify>User confirms script runs without errors and outputs are correct</verify>
  <done>Script executes successfully on HiPerGator, all outputs verified</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 62 verification checklist:
1. R/62_first_line_and_death_analysis.R exists with >= 250 lines
2. treatment_episodes.rds contains is_first_line boolean column after execution
3. Only one is_first_line=TRUE per patient_id
4. is_first_line=TRUE only on episodes with non-NA regimen_label
5. No patient under 21 at episode_start has is_first_line=TRUE
6. death_analysis.xlsx exists with 3 sheets (summary, ENC_TYPE detail, first-line detail)
7. death_analysis.csv exists with death analysis columns
8. DEATH-01 count matches nrow(validated_deaths[death_valid==TRUE])
9. DEATH-02 count <= DEATH-01 count
10. Script does NOT re-query DEATH table (uses validated_death_dates.rds only)
</verification>

<success_criteria>
- FLT-01: is_first_line column exists in treatment_episodes.rds for adults 21+ chemo episodes
- FLT-02: 60-day clean period correctly evaluated against ALL individual chemo dates
- DEATH-01: Summary table shows total count of patients with validated death dates
- DEATH-02: Summary table shows count where death date >= max encounter date
- DEATH-03: Summary table shows post-death encounter count with ENC_TYPE stratification detail
- All outputs generated without errors on HiPerGator
</success_criteria>

<output>
After completion, create `.planning/phases/62-first-line-therapy-and-death-analysis/62-01-SUMMARY.md`
</output>
