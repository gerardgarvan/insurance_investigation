---
phase: 60-foundation-encounterid-propagation-and-drug-name-resolution
plan: 03
type: execute
wave: 2
depends_on: [60-01, 60-02]
files_modified:
  - R/44a_treatment_episodes.R
  - R/49_gantt_data_export.R
autonomous: true
requirements: [TREAT-04]
must_haves:
  truths:
    - "Each chemotherapy episode detail row has a drug_name column resolved from drug_name_lookup.rds"
    - "Each treatment episode row has a drug_names column (comma-separated unique drug names from that episode)"
    - "gantt_episodes.csv includes encounter_ids and drug_names columns"
    - "gantt_detail.csv includes ENCOUNTERID and drug_name columns"
    - "Phase 60 audit xlsx contains ENCOUNTERID population rates and SCT source audit sheets"
  artifacts:
    - path: "R/44a_treatment_episodes.R"
      provides: "Drug name join from drug_name_lookup.rds to episode detail and episode summary"
      contains: "drug_name_lookup.rds"
    - path: "R/49_gantt_data_export.R"
      provides: "Gantt CSV export with encounter_ids and drug_names columns"
      contains: "encounter_ids"
    - path: "output/phase_60_audit.xlsx"
      provides: "Phase 60 audit workbook with ENCOUNTERID rates and SCT audit"
  key_links:
    - from: "R/44a_treatment_episodes.R"
      to: "drug_name_lookup.rds"
      via: "left_join on triggering_code = code"
      pattern: "left_join.*drug_lookup"
    - from: "R/49_gantt_data_export.R"
      to: "treatment_episodes.rds"
      via: "readRDS with encounter_ids and drug_names columns"
      pattern: "encounter_ids|drug_names"
    - from: "R/49_gantt_data_export.R"
      to: "treatment_episode_detail.rds"
      via: "readRDS with ENCOUNTERID and drug_name columns"
      pattern: "ENCOUNTERID|drug_name"
---

<objective>
Join drug names from drug_name_lookup.rds into R/44a episode detail output, aggregate drug_names per episode, propagate new columns (encounter_ids, drug_names, ENCOUNTERID, drug_name) to Gantt CSV exports, and produce the Phase 60 audit xlsx.

Purpose: Completes the data pipeline by carrying drug names through to Gantt output (TREAT-04), making chemotherapy episodes queryable by specific drug for Phase 61 regimen identification.

Output: Modified R/44a (drug name join), modified R/49 (new columns in CSVs), phase_60_audit.xlsx (ENCOUNTERID rates + SCT audit).
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-CONTEXT.md
@.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-RESEARCH.md
@.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-01-SUMMARY.md
@.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-02-SUMMARY.md

<interfaces>
<!-- Post Plan 01 and 02, these artifacts exist -->

From Plan 01 (R/44a modified):
- treatment_episodes.rds schema: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, encounter_ids
- treatment_episode_detail.rds schema: patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, episode_number, episode_start, episode_stop, historical_flag
- sct_audit_result.rds: tibble(metric, value) with SCT pre/post comparison
- encounterid_profile.rds: tibble(table, total_rows, encounterid_populated, population_rate)

From Plan 02 (R/60 created):
- drug_name_lookup.rds schema: tibble(code, code_type, drug_name, lookup_status, source_tables)
- drug_name_lookup.csv: same schema as CSV

From R/49_gantt_data_export.R (current state):
- episodes_export columns: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, triggering_code_descriptions, cancer_category, is_hodgkin
- detail_export columns: patient_id, treatment_type, treatment_date, triggering_code, episode_number, episode_start, episode_stop, historical_flag, triggering_code_description, cancer_category, is_hodgkin
- Death pseudo-treatment rows built inline (must include new columns)
- HL Diagnosis pseudo-treatment rows built inline (must include new columns)
- Column validation in Section 3 checks expected columns against RDS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drug name join to R/44a and produce Phase 60 audit xlsx</name>
  <read_first>
    - R/44a_treatment_episodes.R (as modified by Plan 01 -- current state with ENCOUNTERID)
    - R/60_drug_name_resolution.R (as created by Plan 02 -- understand drug_name_lookup schema)
    - R/00_config.R (as modified by Plan 01 -- confirm sct_dx_icd10 removed)
  </read_first>
  <files>R/44a_treatment_episodes.R</files>
  <action>
**PART A: Load drug_name_lookup.rds and join to episode detail (per D-12)**

Add a new section in R/44a AFTER the main execution loop (Section 5, after `all_detail <- bind_rows(detail_list)`) and BEFORE the CSV/xlsx output sections. This section:

1. Loads `drug_name_lookup.rds`:
```r
message("\n--- Joining drug names to episode detail ---")
DRUG_LOOKUP_RDS <- file.path(CONFIG$cache$outputs_dir, "drug_name_lookup.rds")

if (file.exists(DRUG_LOOKUP_RDS)) {
  drug_lookup <- readRDS(DRUG_LOOKUP_RDS)
  message(glue("  Loaded {nrow(drug_lookup)} drug name lookups"))
} else {
  warning("drug_name_lookup.rds not found. Run R/60_drug_name_resolution.R first. Drug names will be NA.")
  drug_lookup <- tibble(code = character(0), drug_name = character(0))
}
```

2. Joins drug names to `all_detail` via `triggering_code`:
```r
# Join drug name to detail level (one row per patient/date/code)
# Per D-12: left_join on triggering_code = code
all_detail <- all_detail %>%
  left_join(
    drug_lookup %>% select(code, drug_name),
    by = c("triggering_code" = "code")
  )

n_with_names <- sum(!is.na(all_detail$drug_name))
n_chemo_rows <- sum(all_detail$treatment_type == "Chemotherapy")
message(glue("  Drug names joined: {n_with_names} detail rows have drug names ({n_chemo_rows} chemotherapy rows total)"))
```

3. Aggregates drug_names per episode in `all_episodes`:
```r
# Aggregate drug_names per episode from detail level
# Per D-12: comma-separated unique drug names per episode
drug_names_per_episode <- all_detail %>%
  filter(!is.na(drug_name)) %>%
  group_by(patient_id, treatment_type, episode_number) %>%
  summarise(
    drug_names = paste(sort(unique(drug_name)), collapse = ","),
    .groups = "drop"
  )

# Join to episodes
all_episodes <- all_episodes %>%
  left_join(drug_names_per_episode, by = c("patient_id", "treatment_type", "episode_number")) %>%
  mutate(drug_names = ifelse(is.na(drug_names), "", drug_names))

n_with_drugs <- sum(nchar(all_episodes$drug_names) > 0)
message(glue("  Episodes with drug names: {n_with_drugs} / {nrow(all_episodes)}"))
```

4. Update the `all_episodes` select to include `drug_names`:
```r
all_episodes <- all_episodes %>%
  select(patient_id, treatment_type, episode_number, episode_start, episode_stop,
         episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes,
         encounter_ids, drug_names)
```

5. Update `all_detail` select to include `drug_name`:
```r
all_detail <- all_detail %>%
  select(patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name,
         episode_number, episode_start, episode_stop, historical_flag)
```

6. Save both updated RDS artifacts (the existing saveRDS calls should remain, they just save the now-wider tibbles).

7. Update per-type CSV output select blocks to include `drug_names` for episode CSVs and `drug_name` for detail CSVs.

8. Update xlsx headers: episode sheets get "Drug Names" as the last column, detail sheets get "Drug Name" after "ENCOUNTERID". Update all xlsx dimension strings accordingly.

**PART B: Produce Phase 60 audit xlsx (per D-14)**

Add a new section at the END of R/44a (after all CSV/xlsx output) that creates `phase_60_audit.xlsx`:

```r
message("\n--- Creating Phase 60 audit report ---")

AUDIT_XLSX <- file.path(CONFIG$output_dir, "phase_60_audit.xlsx")
wb_audit <- wb_workbook()
```

Sheet 1: ENCOUNTERID Population Rates
```r
wb_audit$add_worksheet("ENCOUNTERID Rates")
# Title row
wb_audit$add_data(sheet = "ENCOUNTERID Rates", x = "ENCOUNTERID Population Rates by Source Table",
                  start_row = 1, start_col = 1)
wb_audit$add_font(sheet = "ENCOUNTERID Rates", dims = "A1",
                  name = "Calibri", size = 16, bold = TRUE, color = wb_color("FF1F2937"))
wb_audit$merge_cells(sheet = "ENCOUNTERID Rates", dims = "A1:D1")

# Load profile from Plan 01
PROFILE_RDS <- file.path(CONFIG$cache$outputs_dir, "encounterid_profile.rds")
if (file.exists(PROFILE_RDS)) {
  encounterid_profile <- readRDS(PROFILE_RDS)

  # Headers
  profile_headers <- c("Table", "Total Rows", "ENCOUNTERID Populated", "Population Rate (%)")
  for (i in seq_along(profile_headers)) {
    wb_audit$add_data(sheet = "ENCOUNTERID Rates", x = profile_headers[i], start_row = 3, start_col = i)
  }
  wb_audit$add_fill(sheet = "ENCOUNTERID Rates", dims = "A3:D3", color = wb_color("FF374151"))
  wb_audit$add_font(sheet = "ENCOUNTERID Rates", dims = "A3:D3",
                    name = "Calibri", size = 11, bold = TRUE, color = wb_color("FFFFFFFF"))

  wb_audit$add_data(sheet = "ENCOUNTERID Rates", x = encounterid_profile, start_row = 4, col_names = FALSE)
  wb_audit$add_numfmt(sheet = "ENCOUNTERID Rates",
                      dims = glue("B4:C{3 + nrow(encounterid_profile)}"), numfmt = "#,##0")
  wb_audit$add_numfmt(sheet = "ENCOUNTERID Rates",
                      dims = glue("D4:D{3 + nrow(encounterid_profile)}"), numfmt = "#,##0.0")
  wb_audit$set_col_widths(sheet = "ENCOUNTERID Rates", cols = 1:4, widths = c(20, 15, 25, 20))
} else {
  wb_audit$add_data(sheet = "ENCOUNTERID Rates", x = "encounterid_profile.rds not found", start_row = 3, start_col = 1)
}
```

Sheet 2: SCT Source Audit
```r
wb_audit$add_worksheet("SCT Source Audit")
wb_audit$add_data(sheet = "SCT Source Audit", x = "SCT Detection: Pre/Post ICD DX Code Removal",
                  start_row = 1, start_col = 1)
wb_audit$add_font(sheet = "SCT Source Audit", dims = "A1",
                  name = "Calibri", size = 16, bold = TRUE, color = wb_color("FF1F2937"))
wb_audit$merge_cells(sheet = "SCT Source Audit", dims = "A1:B1")

AUDIT_RDS <- file.path(CONFIG$cache$outputs_dir, "sct_audit_result.rds")
if (file.exists(AUDIT_RDS)) {
  sct_audit <- readRDS(AUDIT_RDS)

  audit_headers <- c("Metric", "Value")
  for (i in seq_along(audit_headers)) {
    wb_audit$add_data(sheet = "SCT Source Audit", x = audit_headers[i], start_row = 3, start_col = i)
  }
  wb_audit$add_fill(sheet = "SCT Source Audit", dims = "A3:B3", color = wb_color("FF374151"))
  wb_audit$add_font(sheet = "SCT Source Audit", dims = "A3:B3",
                    name = "Calibri", size = 11, bold = TRUE, color = wb_color("FFFFFFFF"))

  wb_audit$add_data(sheet = "SCT Source Audit", x = sct_audit, start_row = 4, col_names = FALSE)
  wb_audit$set_col_widths(sheet = "SCT Source Audit", cols = 1:2, widths = c(45, 20))
} else {
  wb_audit$add_data(sheet = "SCT Source Audit", x = "sct_audit_result.rds not found", start_row = 3, start_col = 1)
}
```

Sheet 3: Drug Name Resolution Summary
```r
wb_audit$add_worksheet("Drug Name Resolution")
wb_audit$add_data(sheet = "Drug Name Resolution", x = "Drug Name Resolution via RxNorm API",
                  start_row = 1, start_col = 1)
wb_audit$add_font(sheet = "Drug Name Resolution", dims = "A1",
                  name = "Calibri", size = 16, bold = TRUE, color = wb_color("FF1F2937"))
wb_audit$merge_cells(sheet = "Drug Name Resolution", dims = "A1:D1")

if (file.exists(DRUG_LOOKUP_RDS)) {
  # Summary stats
  n_success <- sum(drug_lookup$lookup_status == "success", na.rm = TRUE)
  n_not_found <- sum(grepl("not_found", drug_lookup$lookup_status), na.rm = TRUE)
  n_error <- sum(grepl("error", drug_lookup$lookup_status), na.rm = TRUE)

  drug_summary <- tibble(
    metric = c("Total codes resolved", "Successful lookups", "Not found", "Errors", "Unique drug names"),
    value = c(nrow(drug_lookup), n_success, n_not_found, n_error,
              n_distinct(drug_lookup$drug_name[!is.na(drug_lookup$drug_name)]))
  )

  summary_headers <- c("Metric", "Value")
  for (i in seq_along(summary_headers)) {
    wb_audit$add_data(sheet = "Drug Name Resolution", x = summary_headers[i], start_row = 3, start_col = i)
  }
  wb_audit$add_fill(sheet = "Drug Name Resolution", dims = "A3:B3", color = wb_color("FF374151"))
  wb_audit$add_font(sheet = "Drug Name Resolution", dims = "A3:B3",
                    name = "Calibri", size = 11, bold = TRUE, color = wb_color("FFFFFFFF"))
  wb_audit$add_data(sheet = "Drug Name Resolution", x = drug_summary, start_row = 4, col_names = FALSE)

  # Full lookup table below summary
  wb_audit$add_data(sheet = "Drug Name Resolution", x = "Full Lookup Table:",
                    start_row = 10, start_col = 1)
  lookup_headers <- c("Code", "Code Type", "Drug Name", "Status", "Source Tables")
  for (i in seq_along(lookup_headers)) {
    wb_audit$add_data(sheet = "Drug Name Resolution", x = lookup_headers[i], start_row = 11, start_col = i)
  }
  wb_audit$add_fill(sheet = "Drug Name Resolution", dims = "A11:E11", color = wb_color("FF374151"))
  wb_audit$add_font(sheet = "Drug Name Resolution", dims = "A11:E11",
                    name = "Calibri", size = 11, bold = TRUE, color = wb_color("FFFFFFFF"))
  wb_audit$add_data(sheet = "Drug Name Resolution", x = drug_lookup, start_row = 12, col_names = FALSE)
  wb_audit$set_col_widths(sheet = "Drug Name Resolution", cols = 1:5, widths = c(15, 12, 50, 15, 25))
} else {
  wb_audit$add_data(sheet = "Drug Name Resolution", x = "drug_name_lookup.rds not found", start_row = 3, start_col = 1)
}

wb_audit$save(AUDIT_XLSX)
message(glue("Phase 60 audit saved: {AUDIT_XLSX}"))
```
  </action>
  <verify>
    <automated>grep -c "drug_name" R/44a_treatment_episodes.R</automated>
    <automated>grep -c "drug_name_lookup" R/44a_treatment_episodes.R</automated>
    <automated>grep -c "phase_60_audit" R/44a_treatment_episodes.R</automated>
  </verify>
  <acceptance_criteria>
    - R/44a_treatment_episodes.R contains "drug_name_lookup.rds" (load path for lookup table)
    - R/44a_treatment_episodes.R contains 'left_join' with 'drug_lookup' (drug name join)
    - R/44a_treatment_episodes.R contains "drug_names = paste(sort(unique" (per-episode aggregation)
    - R/44a_treatment_episodes.R all_episodes select includes "drug_names"
    - R/44a_treatment_episodes.R all_detail select includes "drug_name"
    - R/44a_treatment_episodes.R contains "phase_60_audit.xlsx" (audit workbook output)
    - R/44a_treatment_episodes.R contains "ENCOUNTERID Rates" (sheet 1 of audit)
    - R/44a_treatment_episodes.R contains "SCT Source Audit" (sheet 2 of audit)
    - R/44a_treatment_episodes.R contains "Drug Name Resolution" (sheet 3 of audit)
    - R/44a_treatment_episodes.R episode CSV select includes "drug_names"
    - R/44a_treatment_episodes.R detail CSV select includes "drug_name"
  </acceptance_criteria>
  <done>R/44a joins drug names from drug_name_lookup.rds to episode detail, aggregates drug_names per episode. Both RDS artifacts include drug name columns. Phase 60 audit xlsx contains 3 sheets: ENCOUNTERID population rates, SCT source audit, and drug name resolution summary.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate encounter_ids, drug_names, ENCOUNTERID, drug_name to Gantt CSVs</name>
  <read_first>
    - R/49_gantt_data_export.R (full file -- modification target for Gantt CSV export)
    - R/44a_treatment_episodes.R (as modified by Plan 01 Task 2 and this plan Task 1 -- to know new column names in RDS)
  </read_first>
  <files>R/49_gantt_data_export.R</files>
  <action>
**PART A: Update column validation (Section 3)**

Update `expected_episode_cols` to include new columns:
```r
expected_episode_cols <- c(
  "patient_id", "treatment_type", "episode_number",
  "episode_start", "episode_stop", "episode_length_days",
  "distinct_dates_in_episode", "historical_flag", "triggering_codes",
  "encounter_ids", "drug_names"
)
```

Update `expected_detail_cols` to include new columns:
```r
expected_detail_cols <- c(
  "patient_id", "treatment_type", "treatment_date", "triggering_code",
  "ENCOUNTERID", "drug_name",
  "episode_number", "episode_start", "episode_stop", "historical_flag"
)
```

**PART B: Update episode export (Section 4)**

In `episodes_export`, the `select()` block must include `encounter_ids` and `drug_names` from the RDS data. Add them after `triggering_codes`:

```r
episodes_export <- episodes %>%
  select(
    patient_id, treatment_type, episode_number,
    episode_start, episode_stop, episode_length_days,
    distinct_dates_in_episode, historical_flag, triggering_codes,
    encounter_ids, drug_names
  ) %>%
  mutate(
    triggering_code_descriptions = sapply(triggering_codes, map_codes_to_descriptions, USE.NAMES = FALSE)
  ) %>%
  left_join(cancer_categories_per_patient, by = c("patient_id" = "ID")) %>%
  mutate(
    cancer_category = ifelse(is.na(cancer_category), "", cancer_category),
    is_hodgkin = ifelse(is.na(is_hodgkin), FALSE, is_hodgkin)
  )
```

**PART C: Update detail export (Section 4)**

In `detail_export`, include `ENCOUNTERID` and `drug_name`:

```r
detail_export <- detail %>%
  select(
    patient_id, treatment_type, treatment_date, triggering_code,
    ENCOUNTERID, drug_name,
    episode_number, episode_start, episode_stop, historical_flag
  ) %>%
  mutate(
    triggering_code_description = sapply(triggering_code, lookup_description, USE.NAMES = FALSE)
  ) %>%
  left_join(cancer_categories_per_patient, by = c("patient_id" = "ID")) %>%
  mutate(
    cancer_category = ifelse(is.na(cancer_category), "", cancer_category),
    is_hodgkin = ifelse(is.na(is_hodgkin), FALSE, is_hodgkin)
  )
```

**PART D: Update Death pseudo-treatment rows (Section 4B)**

Add the new columns to death_episodes and death_detail with empty/NA values:

In `death_episodes` mutate block, add:
```r
encounter_ids = "",
drug_names = "",
```

In `death_detail` mutate block, add:
```r
ENCOUNTERID = NA_character_,
drug_name = NA_character_,
```

Update both death select() blocks to include the new columns in the same order as the main export.

**PART E: Update HL Diagnosis pseudo-treatment rows (Section 4C)**

Add the new columns to hl_dx_episodes and hl_dx_detail with empty/NA values:

In `hl_dx_episodes` mutate block, add:
```r
encounter_ids = "",
drug_names = "",
```

In `hl_dx_detail` mutate block, add:
```r
ENCOUNTERID = NA_character_,
drug_name = NA_character_,
```

Update both HL Diagnosis select() blocks to include the new columns in the same order as the main export.

**PART F: Update final summary stats (Section 6)**

Add drug name coverage stats:
```r
detail_has_drug <- sum(!is.na(detail_export$drug_name) & detail_export$drug_name != "", na.rm = TRUE)
message(glue("  Detail rows with drug names: {format(detail_has_drug, big.mark = ',')}"))

episodes_has_drugs <- sum(episodes_export$drug_names != "", na.rm = TRUE)
message(glue("  Episodes with drug_names: {format(episodes_has_drugs, big.mark = ',')}"))

episodes_has_encounters <- sum(episodes_export$encounter_ids != "", na.rm = TRUE)
message(glue("  Episodes with encounter_ids: {format(episodes_has_encounters, big.mark = ',')}"))
```

**PART G: Update script header comment**

Add new decision traceability lines to the header:
```r
#   D-01 (Phase 60): encounter_ids column (comma-separated) from R/44a treatment_episodes.rds
#   D-04 (Phase 60): drug_names column (comma-separated) from R/44a treatment_episodes.rds
#   D-11 (Phase 60): ENCOUNTERID and drug_name columns from treatment_episode_detail.rds
```

Update the Inputs section to note new columns in existing RDS files.
  </action>
  <verify>
    <automated>grep -c "encounter_ids" R/49_gantt_data_export.R</automated>
    <automated>grep -c "drug_names" R/49_gantt_data_export.R</automated>
    <automated>grep -c "drug_name" R/49_gantt_data_export.R</automated>
    <automated>grep "ENCOUNTERID" R/49_gantt_data_export.R | head -5</automated>
  </verify>
  <acceptance_criteria>
    - R/49_gantt_data_export.R expected_episode_cols includes "encounter_ids" and "drug_names"
    - R/49_gantt_data_export.R expected_detail_cols includes "ENCOUNTERID" and "drug_name"
    - R/49_gantt_data_export.R episodes_export select includes "encounter_ids" and "drug_names"
    - R/49_gantt_data_export.R detail_export select includes "ENCOUNTERID" and "drug_name"
    - R/49_gantt_data_export.R death_episodes mutate includes "encounter_ids" and "drug_names"
    - R/49_gantt_data_export.R death_detail mutate includes "ENCOUNTERID" and "drug_name"
    - R/49_gantt_data_export.R hl_dx_episodes mutate includes "encounter_ids" and "drug_names"
    - R/49_gantt_data_export.R hl_dx_detail mutate includes "ENCOUNTERID" and "drug_name"
    - R/49_gantt_data_export.R contains "Detail rows with drug names:" (summary stat)
    - R/49_gantt_data_export.R contains "Episodes with drug_names:" (summary stat)
    - R/49_gantt_data_export.R contains "Episodes with encounter_ids:" (summary stat)
    - R/49_gantt_data_export.R header contains "Phase 60" decision traceability comments
  </acceptance_criteria>
  <done>gantt_episodes.csv includes encounter_ids and drug_names columns. gantt_detail.csv includes ENCOUNTERID and drug_name columns. Death and HL Diagnosis pseudo-treatment rows include new columns with empty/NA values. Column validation updated. Summary stats report drug name and encounter ID coverage.</done>
</task>

</tasks>

<verification>
After both tasks:
1. R/44a produces treatment_episodes.rds with columns: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, encounter_ids, drug_names
2. R/44a produces treatment_episode_detail.rds with columns: patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name, episode_number, episode_start, episode_stop, historical_flag
3. R/49 produces gantt_episodes.csv with encounter_ids and drug_names columns
4. R/49 produces gantt_detail.csv with ENCOUNTERID and drug_name columns
5. phase_60_audit.xlsx exists with 3 sheets

Verify no column alignment issues:
```bash
grep "encounter_ids\|drug_names\|ENCOUNTERID\|drug_name" R/44a_treatment_episodes.R R/49_gantt_data_export.R | wc -l
```
Should return 30+ matches total.
</verification>

<success_criteria>
- Drug names from drug_name_lookup.rds are joined to episode detail via triggering_code
- Per-episode drug_names aggregated as comma-separated unique drug names
- treatment_episodes.rds has encounter_ids and drug_names columns
- treatment_episode_detail.rds has ENCOUNTERID and drug_name columns
- gantt_episodes.csv has encounter_ids and drug_names columns (TREAT-04)
- gantt_detail.csv has ENCOUNTERID and drug_name columns (TREAT-04)
- Death and HL Diagnosis pseudo-treatment rows include all new columns
- Column validation in R/49 updated to expect new columns
- Phase 60 audit xlsx produced with ENCOUNTERID rates, SCT audit, and drug resolution summary
</success_criteria>

<output>
After completion, create `.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-03-SUMMARY.md`
</output>
