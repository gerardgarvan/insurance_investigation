---
phase: 60-foundation-encounterid-propagation-and-drug-name-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/60_drug_name_resolution.R
autonomous: true
requirements: [TREAT-02, TREAT-03]
must_haves:
  truths:
    - "Every unique RXNORM_CUI and NDC code from chemotherapy patient data is resolved to a generic drug name via RxNorm API"
    - "Drug name lookup table exists as both RDS and CSV reference artifacts"
    - "Cached results are reused on re-run, only new codes trigger API calls"
    - "API calls use 0.1s rate limiting and httr2 retry logic for transient errors"
  artifacts:
    - path: "R/60_drug_name_resolution.R"
      provides: "Standalone drug name resolution script"
      contains: "lookup_rxcui_name"
    - path: "cache/outputs/drug_name_lookup.rds"
      provides: "Cached drug name lookup table"
    - path: "output/drug_name_lookup.csv"
      provides: "Human-readable drug name reference"
  key_links:
    - from: "R/60_drug_name_resolution.R"
      to: "drug_name_lookup.rds"
      via: "saveRDS after API lookups"
      pattern: "saveRDS.*drug_name_lookup"
    - from: "R/60_drug_name_resolution.R"
      to: "RxNorm API"
      via: "httr2 request to rxnav.nlm.nih.gov"
      pattern: "rxnav\\.nlm\\.nih\\.gov"
---

<objective>
Create a standalone drug name resolution script that queries the RxNorm API to resolve RXNORM_CUI and NDC codes from chemotherapy patient data to generic drug names. Produces a cached lookup table (RDS + CSV) for downstream use in R/44a episode detail.

Purpose: Enables specific drug name labeling on chemotherapy episodes, which Phase 61 needs for regimen identification (ABVD requires knowing doxorubicin, bleomycin, vinblastine, dacarbazine).

Output: R/60_drug_name_resolution.R script, drug_name_lookup.rds (cache), drug_name_lookup.csv (reference).
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-CONTEXT.md
@.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-RESEARCH.md

<interfaces>
<!-- Reusable API lookup functions from R/40 -->

From R/40_investigate_unmatched_ndc.R (lines 227-372):
```r
lookup_rxcui_name <- function(rxcui, sleep_sec = 0.1)
  # Returns: tibble(code, drug_name, lookup_status)
  # API: https://rxnav.nlm.nih.gov/REST/rxcui/{rxcui}/properties.json
  # Uses: httr2 req_retry() with transient detection for 429, 503, 504

lookup_ndc_to_name <- function(ndc, sleep_sec = 0.1)
  # Returns: tibble(code, drug_name, lookup_status)
  # 2-step: NDC -> RxCUI via /REST/rxcui.json?idtype=NDC&id={ndc}
  #         RxCUI -> Name via lookup_rxcui_name()

lookup_drug_codes_batch <- function(codes_df)
  # Input: tibble with code and code_type columns
  # Returns: tibble(code, drug_name, lookup_status)
  # Dispatches RXNORM to lookup_rxcui_name(), NDC to lookup_ndc_to_name()
```

From R/00_config.R:
```r
TREATMENT_CODES$chemo_rxnorm  # Vector of RXNORM_CUI strings for chemotherapy
# Used in PRESCRIBING, DISPENSING, MED_ADMIN queries
```

DuckDB query pattern:
```r
open_pcornet_con()
get_pcornet_table("PRESCRIBING") %>% filter(RXNORM_CUI %in% ...) %>% collect()
get_pcornet_table("DISPENSING") %>% filter(RXNORM_CUI %in% ...) %>% collect()
get_pcornet_table("MED_ADMIN") %>% filter(RXNORM_CUI %in% ...) %>% collect()
close_pcornet_con()
```

Output directory pattern:
```r
CONFIG$cache$outputs_dir  # e.g., "cache/outputs/"
CONFIG$output_dir         # e.g., "output/"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/60_drug_name_resolution.R</name>
  <read_first>
    - R/40_investigate_unmatched_ndc.R (lines 220-380 -- lookup_rxcui_name, lookup_ndc_to_name, lookup_drug_codes_batch functions to reuse)
    - R/00_config.R (lines 392-945 -- chemo_rxnorm vector to understand what codes exist in config)
    - R/01_load_pcornet.R (column specs for PRESCRIBING, DISPENSING, MED_ADMIN)
    - R/utils_duckdb.R (get_pcornet_table, open/close_pcornet_con)
  </read_first>
  <files>R/60_drug_name_resolution.R</files>
  <action>
Create new script `R/60_drug_name_resolution.R` with the following structure:

**SECTION 1: Header and Setup**

```r
# =============================================================================
# Phase 60: Drug Name Resolution via RxNorm API
# =============================================================================
# Resolves RXNORM_CUI and NDC codes from chemotherapy patient data to generic
# drug names using the NLM RxNorm REST API. Produces a cached lookup table
# for downstream use in R/44a treatment episode detail.
#
# Decision traceability:
#   D-06: Drug name resolution covers chemotherapy only
#   D-07: Both RXNORM_CUI and NDC codes resolved via R/40 functions
#   D-08: Only codes from patient data (not all config codes) are resolved
#   D-09: Results cached in drug_name_lookup.rds; re-runs only query new codes
#   D-10: Standalone script separate from episode extraction
#
# Outputs:
#   - cache/outputs/drug_name_lookup.rds (cached lookup table)
#   - output/drug_name_lookup.csv (human-readable reference)
# =============================================================================

suppressPackageStartupMessages({
  library(dplyr)
  library(glue)
  library(stringr)
  library(httr2)
  library(purrr)
})

source("R/00_config.R")
source("R/utils_duckdb.R")
```

**SECTION 2: Copy API lookup functions from R/40**

Copy the three functions directly from R/40_investigate_unmatched_ndc.R into R/60:
- `lookup_rxcui_name(rxcui, sleep_sec = 0.1)` (lines 227-265)
- `lookup_ndc_to_name(ndc, sleep_sec = 0.1)` (lines 275-319)
- `lookup_drug_codes_batch(codes_df)` (lines 329-372)

Copy them verbatim. Add a comment: `# Copied from R/40_investigate_unmatched_ndc.R for script independence (per Claude's discretion)`.

Do NOT source R/40 directly -- R/40 has side effects (it runs analysis on load). Copying is the correct pattern.

**SECTION 3: Extract unique chemotherapy drug codes from patient data (per D-08)**

Open DuckDB connection and query ONLY codes that appear in patient data:

```r
message("=== Phase 60: Drug Name Resolution ===\n")
message("--- Extracting unique chemotherapy drug codes from patient data ---")

open_pcornet_con()

# RXNORM_CUI codes from PRESCRIBING
rx_codes_prescribing <- NULL
if (!is.null(get_pcornet_table("PRESCRIBING"))) {
  rx_codes_prescribing <- get_pcornet_table("PRESCRIBING") %>%
    filter(RXNORM_CUI %in% TREATMENT_CODES$chemo_rxnorm) %>%
    filter(!is.na(RXNORM_CUI)) %>%
    distinct(RXNORM_CUI) %>%
    collect() %>%
    mutate(code = RXNORM_CUI, code_type = "RXNORM", source_table = "PRESCRIBING") %>%
    select(code, code_type, source_table)
}

# RXNORM_CUI codes from DISPENSING
rx_codes_dispensing <- NULL
if (!is.null(get_pcornet_table("DISPENSING")) &&
    "RXNORM_CUI" %in% colnames(get_pcornet_table("DISPENSING"))) {
  rx_codes_dispensing <- get_pcornet_table("DISPENSING") %>%
    filter(RXNORM_CUI %in% TREATMENT_CODES$chemo_rxnorm) %>%
    filter(!is.na(RXNORM_CUI)) %>%
    distinct(RXNORM_CUI) %>%
    collect() %>%
    mutate(code = RXNORM_CUI, code_type = "RXNORM", source_table = "DISPENSING") %>%
    select(code, code_type, source_table)
}

# RXNORM_CUI codes from MED_ADMIN
rx_codes_medadmin <- NULL
if (!is.null(get_pcornet_table("MED_ADMIN")) &&
    "RXNORM_CUI" %in% colnames(get_pcornet_table("MED_ADMIN"))) {
  rx_codes_medadmin <- get_pcornet_table("MED_ADMIN") %>%
    filter(RXNORM_CUI %in% TREATMENT_CODES$chemo_rxnorm) %>%
    filter(!is.na(RXNORM_CUI)) %>%
    distinct(RXNORM_CUI) %>%
    collect() %>%
    mutate(code = RXNORM_CUI, code_type = "RXNORM", source_table = "MED_ADMIN") %>%
    select(code, code_type, source_table)
}

# NDC codes from DISPENSING (if NDC column exists)
ndc_codes <- NULL
if (!is.null(get_pcornet_table("DISPENSING")) &&
    "NDC" %in% colnames(get_pcornet_table("DISPENSING"))) {
  # Get NDC codes that appear alongside chemo RXNORM_CUI codes
  # These are NDC codes for prescriptions that matched chemo_rxnorm
  ndc_codes <- get_pcornet_table("DISPENSING") %>%
    filter(RXNORM_CUI %in% TREATMENT_CODES$chemo_rxnorm) %>%
    filter(!is.na(NDC)) %>%
    distinct(NDC) %>%
    collect() %>%
    mutate(code = NDC, code_type = "NDC", source_table = "DISPENSING") %>%
    select(code, code_type, source_table)
}

close_pcornet_con()
```

Combine and deduplicate:
```r
all_codes <- bind_rows(
  rx_codes_prescribing, rx_codes_dispensing, rx_codes_medadmin, ndc_codes
) %>%
  filter(!is.na(code) & code != "")

# Deduplicate by code (keep first source_table for reference)
unique_codes <- all_codes %>%
  group_by(code, code_type) %>%
  summarise(source_tables = paste(unique(source_table), collapse = ","), .groups = "drop")

message(glue("  Found {nrow(unique_codes)} unique drug codes in patient data"))
message(glue("    RXNORM: {sum(unique_codes$code_type == 'RXNORM')}"))
message(glue("    NDC: {sum(unique_codes$code_type == 'NDC')}"))
```

**SECTION 4: Load cache and determine codes to query (per D-09)**

```r
CACHE_FILE <- file.path(CONFIG$cache$outputs_dir, "drug_name_lookup.rds")
OUTPUT_CSV <- file.path(CONFIG$output_dir, "drug_name_lookup.csv")

if (file.exists(CACHE_FILE)) {
  cached_lookups <- readRDS(CACHE_FILE)
  message(glue("  Loaded {nrow(cached_lookups)} cached drug name lookups"))
} else {
  cached_lookups <- tibble(
    code = character(0),
    code_type = character(0),
    drug_name = character(0),
    lookup_status = character(0),
    source_tables = character(0)
  )
  message("  No cache found -- all codes will be queried")
}

# Only query codes not already in cache
codes_to_query <- unique_codes %>%
  anti_join(cached_lookups, by = "code")

message(glue("  {nrow(codes_to_query)} new codes to resolve via RxNorm API"))
```

**SECTION 5: Query RxNorm API for uncached codes**

```r
if (nrow(codes_to_query) > 0) {
  message("\n--- Querying RxNorm API ---")

  new_lookups <- lookup_drug_codes_batch(codes_to_query)

  # Add code_type and source_tables from codes_to_query
  new_lookups <- new_lookups %>%
    left_join(codes_to_query %>% select(code, code_type, source_tables), by = "code")

  # Combine with cache
  all_lookups <- bind_rows(cached_lookups, new_lookups)
} else {
  message("\n--- All codes already cached, skipping API calls ---")
  all_lookups <- cached_lookups
}
```

**SECTION 6: Save outputs**

```r
# Save RDS cache (per D-09)
saveRDS(all_lookups, CACHE_FILE)
message(glue("\nRDS cache saved: {CACHE_FILE} ({nrow(all_lookups)} entries)"))

# Save CSV reference (per D-10, TREAT-03)
write.csv(all_lookups, OUTPUT_CSV, row.names = FALSE)
message(glue("CSV reference saved: {OUTPUT_CSV}"))
```

**SECTION 7: Summary statistics**

```r
message("\n=== Drug Name Resolution Complete ===")
message(glue("  Total codes resolved: {nrow(all_lookups)}"))
message(glue("  Successful lookups: {sum(all_lookups$lookup_status == 'success', na.rm = TRUE)}"))
message(glue("  Not found: {sum(grepl('not_found', all_lookups$lookup_status), na.rm = TRUE)}"))
message(glue("  Errors: {sum(grepl('error', all_lookups$lookup_status), na.rm = TRUE)}"))

# Show unique drug names found
drug_names_found <- all_lookups %>%
  filter(lookup_status == "success") %>%
  distinct(drug_name) %>%
  arrange(drug_name)

message(glue("  Unique drug names: {nrow(drug_names_found)}"))
if (nrow(drug_names_found) > 0) {
  message("  Drug names:")
  for (name in drug_names_found$drug_name) {
    message(glue("    - {name}"))
  }
}

message(glue("\nOutputs:"))
message(glue("  RDS: {CACHE_FILE}"))
message(glue("  CSV: {OUTPUT_CSV}"))
```

The `all_lookups` tibble must have columns: code (character), code_type (character), drug_name (character), lookup_status (character), source_tables (character).
  </action>
  <verify>
    <automated>grep -c "lookup_rxcui_name" R/60_drug_name_resolution.R</automated>
    <automated>grep -c "drug_name_lookup" R/60_drug_name_resolution.R</automated>
    <automated>grep -c "TREATMENT_CODES\\$chemo_rxnorm" R/60_drug_name_resolution.R</automated>
    <automated>grep "rxnav.nlm.nih.gov" R/60_drug_name_resolution.R</automated>
  </verify>
  <acceptance_criteria>
    - R/60_drug_name_resolution.R file exists
    - R/60_drug_name_resolution.R contains "lookup_rxcui_name" (function definition copied from R/40)
    - R/60_drug_name_resolution.R contains "lookup_ndc_to_name" (function definition copied from R/40)
    - R/60_drug_name_resolution.R contains "lookup_drug_codes_batch" (function definition copied from R/40)
    - R/60_drug_name_resolution.R contains "TREATMENT_CODES$chemo_rxnorm" (filtering to chemo codes per D-06)
    - R/60_drug_name_resolution.R contains "drug_name_lookup.rds" (cache save path)
    - R/60_drug_name_resolution.R contains "drug_name_lookup.csv" (reference output path)
    - R/60_drug_name_resolution.R contains "rxnav.nlm.nih.gov" (API endpoint)
    - R/60_drug_name_resolution.R contains "anti_join(cached_lookups" (cache-aware query logic per D-09)
    - R/60_drug_name_resolution.R contains "library(httr2)" (API request library)
    - R/60_drug_name_resolution.R does NOT source R/40 (no "source.*40")
    - R/60_drug_name_resolution.R sources R/00_config.R and R/utils_duckdb.R
  </acceptance_criteria>
  <done>R/60_drug_name_resolution.R exists as a standalone script. It extracts unique RXNORM_CUI and NDC codes from PRESCRIBING/DISPENSING/MED_ADMIN patient data (chemo only per D-06), resolves them via RxNorm API with httr2 retry logic, caches results in drug_name_lookup.rds, and writes drug_name_lookup.csv as a human-readable reference.</done>
</task>

</tasks>

<verification>
- R/60_drug_name_resolution.R is a standalone script that can run independently
- Script sources R/00_config.R and R/utils_duckdb.R (not R/43a or R/44a)
- Script does NOT source R/40 (functions are copied for independence)
- API calls use httr2 with req_retry() for transient errors (429, 503, 504)
- 0.1s sleep between requests for rate limiting
- Cache file drug_name_lookup.rds in CONFIG$cache$outputs_dir
- CSV file drug_name_lookup.csv in CONFIG$output_dir
- Only codes from patient data are queried (per D-08), not all config codes
</verification>

<success_criteria>
- R/60_drug_name_resolution.R file exists with complete implementation
- Script extracts codes from PRESCRIBING, DISPENSING, MED_ADMIN tables
- Only chemo codes are resolved (TREATMENT_CODES$chemo_rxnorm filter per D-06)
- Both RXNORM_CUI and NDC codes are resolved (per D-07)
- Cache logic: loads existing RDS, queries only new codes, saves updated cache (per D-09)
- Produces drug_name_lookup.rds (TREAT-03) and drug_name_lookup.csv (TREAT-03)
- Console logs show lookup summary (success/not_found/error counts)
</success_criteria>

<output>
After completion, create `.planning/phases/60-foundation-encounterid-propagation-and-drug-name-resolution/60-02-SUMMARY.md`
</output>
