---
phase: 136-confirm-loose-ends
plan: "02"
type: execute
wave: 2
depends_on:
  - "136-01"
files_modified:
  - R/00_config.R
  - R/01_load_pcornet.R
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CONFIRM-02
  - CONFIRM-01

must_haves:
  truths:
    - "CONFIG$analysis$date_range_max is either Sys.Date() (if data was being dropped) or unchanged with an explanatory comment added (if no drop found)"
    - "R/01_load_pcornet.R date validation block carries a comment explaining the intent of date_range_max"
    - "CONFIRM-01 and CONFIRM-02 are marked resolved in REQUIREMENTS.md with one-line finding notes"
  artifacts:
    - path: "R/00_config.R"
      provides: "Updated date_range_max definition with explanatory comment"
    - path: "R/01_load_pcornet.R"
      provides: "Explanatory comment added alongside date validation block (lines 579-597)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONFIRM-01 and CONFIRM-02 entries updated from [ ] to [x] with finding notes"
  key_links:
    - from: "R/00_config.R"
      to: "R/01_load_pcornet.R"
      via: "CONFIG$analysis$date_range_max read at line 582"
      pattern: "CONFIG\\$analysis\\$date_range_max"
---

<objective>
Investigate whether `CONFIG$analysis$date_range_max = as.Date("2025-03-31")` is silently
dropping valid encounter or death records with dates in Apr–Sep 2025, then apply the
appropriate fix (D-04: change to Sys.Date()) or documentation (D-06: leave unchanged,
record finding). Also mark both CONFIRM-01 and CONFIRM-02 resolved in REQUIREMENTS.md.

Purpose: Close the code review's open question about the date upper bound. The extract
cutoff is 2025-09-15; if the config bound of 2025-03-31 flags real records as invalid,
those records may be silently excluded from analysis. Resolves CONFIRM-02.

Output: Updated R/00_config.R and R/01_load_pcornet.R; REQUIREMENTS.md with both
CONFIRM entries resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/136-confirm-loose-ends/136-01-SUMMARY.md

<interfaces>
<!-- Current state of the two files this plan edits -->

<!-- R/00_config.R lines 2813-2817 (current) -->
```r
  # Catches SAS epoch (1899-12-30) and Excel epoch (1900-01-01) sentinels as lower bound
  # Upper bound is end of data collection period
  date_range_min = as.Date("1901-01-01"),
  date_range_max = as.Date("2025-03-31")
)
```

<!-- R/01_load_pcornet.R lines 579-597 (current) -->
```r
  # --- Date range validation: data collection period (bounds from CONFIG$analysis) ---
  # Flags dates outside the study period and SAS epoch sentinels (1899-12-30)
  date_range_min <- CONFIG$analysis$date_range_min
  date_range_max <- CONFIG$analysis$date_range_max
  for (dcol in date_cols) {
    if (dcol %in% names(df) && inherits(df[[dcol]], "Date")) {
      valid_col_name <- paste0(dcol, "_VALID")
      df[[valid_col_name]] <- case_when(
        is.na(df[[dcol]]) ~ NA,
        df[[dcol]] < date_range_min ~ FALSE, # Pre-1901 (SAS/Excel epoch sentinels)
        df[[dcol]] > date_range_max ~ FALSE, # Extreme future dates
        TRUE ~ TRUE
      )
      n_invalid <- sum(!df[[valid_col_name]], na.rm = TRUE)
      if (n_invalid > 0) {
        message(glue("  Validation: {n_invalid} invalid {dcol} values flagged (out of {date_range_min} to {date_range_max} range)"))
      }
    }
  }
```

<!-- Key determination for Task 1 investigation -->
<!-- The validation logic sets a _VALID flag to FALSE for dates > date_range_max.   -->
<!-- If downstream code FILTERS on _VALID == TRUE (i.e. drops FALSE rows), then    -->
<!-- Apr-Sep 2025 records ARE being dropped. If the flag is only used for logging   -->
<!-- (n_invalid message) and never used as a filter predicate, no data is dropped.  -->
<!-- The executor MUST grep for _VALID usage across the R/ directory to determine   -->
<!-- which case applies before deciding which branch (D-04 vs D-06) to execute.    -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate _VALID flag usage — determine if records are being dropped</name>
  <files>R/01_load_pcornet.R</files>
  <action>
Perform the investigation required by D-04 / D-06 to determine whether the
`date_range_max = as.Date("2025-03-31")` bound is causing real Apr–Sep 2025 data to be
silently dropped.

**Step 1 — Trace _VALID flag propagation:**
Search all R/ files for any use of columns matching `_VALID` (the flag columns created by
the date validation loop in R/01, e.g. `admit_date_VALID`, `death_date_VALID`):

```bash
grep -rn "_VALID" R/ --include="*.R" | grep -v "^R/01_load_pcornet.R"
```

Record every file and line that references a `_VALID` column. For each hit, determine:
- Is the `_VALID` column used in a `filter()`, `subset()`, or logical predicate that
  would DROP rows where `_VALID == FALSE`?
- Or is it used only for counting / logging (e.g., `sum(!col_VALID, na.rm=TRUE)`)?

**Step 2 — Determine outcome branch:**

Branch A — Data IS being dropped (filter on _VALID == FALSE removes rows):
- Apply D-04: change `date_range_max` from `as.Date("2025-03-31")` to `Sys.Date()`
  in R/00_config.R line 2816.
- Update the comment on the line above (currently "Upper bound is end of data collection
  period") to: "Upper bound catches future-year sentinel dates; data-source cutoff is
  enforced by the extract itself, not this config value."
- Proceed to Task 2.

Branch B — Data is NOT being dropped (_VALID is only used for message() / logging, never
as a filter predicate):
- Apply D-06: leave `date_range_max` value unchanged.
- Update the comment on line 2814 to reflect the correct intent:
  "Upper bound catches future-year sentinel dates only; does not filter records
  (validation flag is informational). Data-source cutoff enforced by the extract."
- Proceed to Task 2 (REQUIREMENTS.md update still required, finding note differs).

**Document the investigation result** in your task completion note for the SUMMARY:
record which branch was taken, which files referenced _VALID, and whether any of them
filtered on it.
  </action>
  <verify>
```bash
# Confirm grep ran and produced output (non-empty = _VALID referenced somewhere)
grep -rn "_VALID" R/ --include="*.R" | wc -l
# Should be > 0 (at minimum R/01 itself defines them)

# Confirm which branch was taken by checking config:
grep "date_range_max" R/00_config.R
# Branch A result:  date_range_max = Sys.Date()
# Branch B result:  date_range_max = as.Date("2025-03-31")  [plus updated comment]
```
  </verify>
  <done>
Investigation is complete; either `date_range_max` has been changed to `Sys.Date()` (Branch A)
or the config value is unchanged with an updated comment (Branch B). The branch decision is
documented with evidence (which files filter on _VALID).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explanatory comment to R/01 validation block and resolve CONFIRM entries in REQUIREMENTS.md</name>
  <files>R/01_load_pcornet.R, .planning/REQUIREMENTS.md</files>
  <action>
Two targeted edits. No functional code changes in R/01 beyond adding a comment.

**Edit 1 — R/01_load_pcornet.R (per D-05):**

Locate the date validation block (lines ~579-597). Add a clarifying comment immediately
after the two existing comment lines (`# --- Date range validation…` and `# Flags dates…`)
and before `date_range_min <- …`. The comment must make clear:

```r
  # --- Date range validation: data collection period (bounds from CONFIG$analysis) ---
  # Flags dates outside the study period and SAS epoch sentinels (1899-12-30)
  # Intent: date_range_min guards against SAS/Excel epoch sentinels (pre-1901).
  #         date_range_max guards against far-future sentinel dates only.
  #         The data-source cutoff (extract date) is enforced by the extract itself,
  #         not by this config bound. See R/00_config.R CONFIG$analysis$date_range_max.
  #         CONFIRM-02: bound reviewed 2026-07-25; see REQUIREMENTS.md for finding.
```

Do not alter any executable lines in the block (lines assigning date_range_min,
date_range_max, the for-loop, case_when, or the message()).

**Edit 2 — .planning/REQUIREMENTS.md (per D-03, D-07):**

Locate the two CONFIRM entries under `### Confirm Loose Ends (CONFIRM)`:

```
- [ ] **CONFIRM-01**: Locate where suppress_small()...
- [ ] **CONFIRM-02**: Reconcile CONFIG$analysis$date_range_max...
```

Change both to `[x]` and append a one-line finding note after each description.

CONFIRM-01 finding note (always the same regardless of branch):
```
  *Finding: clean_multi_value() and union_field() extracted to R/utils/utils_format.R;
  R/52, R/101, R/104 now source from there. suppress_small() remains inline in R/106
  (single-file use). Resolved Phase 136 plan 01.*
```

CONFIRM-02 finding note (branch-dependent — insert the applicable one):

Branch A (data was being dropped):
```
  *Finding: date_range_max = 2025-03-31 caused Apr-Sep 2025 records to be flagged
  _VALID=FALSE and filtered by [script(s) identified in Task 1]. Changed to Sys.Date()
  per D-04. Resolved Phase 136 plan 02.*
```

Branch B (no data was being dropped):
```
  *Finding: _VALID flag is informational only (used for message() counts, never as a
  filter predicate). No records were being dropped. date_range_max left unchanged;
  comment updated to clarify intent per D-06. Resolved Phase 136 plan 02.*
```

Also update the tracking table at the bottom of REQUIREMENTS.md:
- Change `CONFIRM-01 | Phase 136 | Pending` to `CONFIRM-01 | Phase 136 | Complete`
- Change `CONFIRM-02 | Phase 136 | Pending` to `CONFIRM-02 | Phase 136 | Complete`
  </action>
  <verify>
```r
# In R console from project root:
req_lines <- readLines(here::here(".planning/REQUIREMENTS.md"))
stopifnot(any(grepl("\\[x\\].*CONFIRM-01", req_lines)))
stopifnot(any(grepl("\\[x\\].*CONFIRM-02", req_lines)))
stopifnot(any(grepl("CONFIRM-01.*Complete", req_lines)))
stopifnot(any(grepl("CONFIRM-02.*Complete", req_lines)))

r01_lines <- readLines(here::here("R/01_load_pcornet.R"))
stopifnot(any(grepl("CONFIRM-02", r01_lines)))
stopifnot(any(grepl("data-source cutoff.*enforced by the extract", r01_lines)))
message("REQUIREMENTS.md and R/01 comment verification PASSED")
```
  </verify>
  <done>
REQUIREMENTS.md: CONFIRM-01 and CONFIRM-02 both show `[x]` with finding notes, and the
tracking table shows both as Complete. R/01_load_pcornet.R: the date validation block
contains the explanatory comment about `date_range_max` intent and CONFIRM-02 reference.
  </done>
</task>

</tasks>

<verification>
After both tasks:

```r
library(here); library(tidyverse)

# 1. Config bound is either Sys.Date() or unchanged (both acceptable)
cfg_line <- grep("date_range_max", readLines(here("R/00_config.R")), value = TRUE)
message("date_range_max config: ", cfg_line)

# 2. R/01 has the explanatory comment
r01 <- readLines(here("R/01_load_pcornet.R"))
stopifnot(any(grepl("CONFIRM-02", r01)))
stopifnot(any(grepl("data-source cutoff", r01)))

# 3. REQUIREMENTS.md has both CONFIRM entries resolved
req <- readLines(here(".planning/REQUIREMENTS.md"))
stopifnot(any(grepl("\\[x\\].*CONFIRM-01", req)))
stopifnot(any(grepl("\\[x\\].*CONFIRM-02", req)))
stopifnot(any(grepl("CONFIRM-01.*Complete", req)))
stopifnot(any(grepl("CONFIRM-02.*Complete", req)))

# 4. utils_format.R still loads (regression check from plan 01)
source(here("R/utils/utils_format.R"))
stopifnot(is.function(clean_multi_value))

message("Phase 136 plan 02 — all verification checks passed")
```
</verification>

<success_criteria>
- Investigation of `_VALID` flag usage is documented (which scripts use it, whether as filter or log)
- `date_range_max` is either `Sys.Date()` (Branch A — data was dropped) or unchanged with updated comment (Branch B — no drop)
- R/01_load_pcornet.R date validation block has an explanatory comment citing CONFIRM-02
- REQUIREMENTS.md: CONFIRM-01 and CONFIRM-02 show `[x]` with one-line finding notes and tracking table shows Complete
- Phase 136 is fully closed: no open CONFIRM items remain
</success_criteria>

<output>
After completion, create `.planning/phases/136-confirm-loose-ends/136-02-SUMMARY.md`
covering: investigation result (Branch A or B), evidence found (which files referenced
_VALID and how), config change made or not, and REQUIREMENTS.md update applied.
</output>
