---
phase: 29-duckdb-ingest-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/25_duckdb_ingest.R
autonomous: true
requirements:
  - DBING-01
  - DBING-02

must_haves:
  truths:
    - "User can run R/25_duckdb_ingest.R and see a DuckDB file created at CONFIG$cache$duckdb_path"
    - "User can see 13 table names when connecting to the DuckDB file and running dbListTables()"
    - "User can see per-table ingest log CSV at output/logs/duckdb_ingest_2025-09-15.csv with 13 rows"
    - "Interrupted run leaves canonical DuckDB file untouched (atomic write via .tmp swap)"
  artifacts:
    - path: "R/00_config.R"
      provides: "EXTRACT_DATE, CONFIG$cache$duckdb_dir, CONFIG$cache$duckdb_path constants"
      contains: "EXTRACT_DATE"
    - path: "R/25_duckdb_ingest.R"
      provides: "DuckDB ingest script with atomic write and per-table logging"
      min_lines: 80
    - path: "output/logs/duckdb_ingest_2025-09-15.csv"
      provides: "Per-table ingest metrics (table_name, row_count, col_count, duration_sec)"
  key_links:
    - from: "R/25_duckdb_ingest.R"
      to: "R/00_config.R"
      via: "source() and CONFIG$cache$raw_dir, CONFIG$cache$duckdb_path, PCORNET_TABLES"
      pattern: "source.*00_config|CONFIG\\$cache\\$raw_dir|CONFIG\\$cache\\$duckdb_path|PCORNET_TABLES"
    - from: "R/25_duckdb_ingest.R"
      to: "RDS cache files"
      via: "readRDS() from CONFIG$cache$raw_dir/{table}.rds"
      pattern: "readRDS.*CONFIG\\$cache\\$raw_dir"
---

<objective>
Create the DuckDB ingest script and config additions that read all 13 PCORnet tables from the existing RDS cache and write them into a single DuckDB file with atomic write guarantees and per-table ingest logging.

Purpose: This is the foundation of the v1.3 DuckDB backend migration. The ingest script produces the DuckDB file that all downstream phases (30-32) depend on. Atomic write ensures the canonical file is never partially written.

Output: `R/25_duckdb_ingest.R` (new standalone script), config additions in `R/00_config.R`, and ingest log CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-duckdb-ingest-infrastructure/29-CONTEXT.md
@.planning/phases/29-duckdb-ingest-infrastructure/29-RESEARCH.md

@R/00_config.R
@R/01_load_pcornet.R

<interfaces>
<!-- Key constants and patterns the executor needs from existing code. -->

From R/00_config.R:
```r
# CONFIG structure (add duckdb settings alongside existing cache block):
CONFIG$cache = list(
  cache_dir    = "/blue/erin.mobley-hl.bcu/clean/rds",
  force_reload = FALSE,
  raw_dir      = "/blue/erin.mobley-hl.bcu/clean/rds/raw",
  cohort_dir   = "/blue/erin.mobley-hl.bcu/clean/rds/cohort",
  outputs_dir  = "/blue/erin.mobley-hl.bcu/clean/rds/outputs"
)

# PCORNET_TABLES (13 tables):
PCORNET_TABLES <- c(
  "ENROLLMENT", "DIAGNOSIS", "PROCEDURES", "PRESCRIBING", "ENCOUNTER",
  "DEMOGRAPHIC", "TUMOR_REGISTRY1", "TUMOR_REGISTRY2", "TUMOR_REGISTRY3",
  "DISPENSING", "MED_ADMIN", "LAB_RESULT_CM", "PROVIDER"
)
```

From R/01_load_pcornet.R:
```r
# RDS files live at: CONFIG$cache$raw_dir/{TABLE_NAME}.rds
# e.g., /blue/erin.mobley-hl.bcu/clean/rds/raw/ENROLLMENT.rds
# readRDS() returns tibble with parsed dates and _VALID columns
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DuckDB config constants to R/00_config.R</name>
  <files>R/00_config.R</files>
  <read_first>
    - R/00_config.R (current config structure, especially CONFIG$cache block at lines 51-66)
  </read_first>
  <action>
Add two items to R/00_config.R:

1. Add `EXTRACT_DATE` constant ABOVE the `CONFIG <- list(...)` block (around line 24, before CONFIG definition). This is a top-level constant, not inside CONFIG:
```r
# Extract date for current PCORnet CDM data pull (Mailhot_V1_20250915)
# Update this when a new extract arrives. Used for ingest log filenames.
EXTRACT_DATE <- "2025-09-15"
```

2. Add DuckDB settings inside the existing `CONFIG$cache` list (after `outputs_dir` line, around line 66). Add these two entries before the closing `)` of the cache list:
```r
    # Phase 29: DuckDB file storage (gitignored via /blue/erin.mobley-hl.bcu/clean/)
    duckdb_dir   = "/blue/erin.mobley-hl.bcu/clean/duckdb",
    duckdb_path  = "/blue/erin.mobley-hl.bcu/clean/duckdb/pcornet.duckdb"
```

Per D-01: DuckDB path is under `/blue/erin.mobley-hl.bcu/clean/duckdb/` which is already gitignored.
Per D-05: EXTRACT_DATE is hardcoded to "2025-09-15" matching the Mailhot_V1_20250915 extract.
  </action>
  <verify>
    <automated>grep -n "EXTRACT_DATE" R/00_config.R && grep -n "duckdb_dir" R/00_config.R && grep -n "duckdb_path" R/00_config.R</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains `EXTRACT_DATE <- "2025-09-15"` as a top-level constant
    - R/00_config.R contains `duckdb_dir   = "/blue/erin.mobley-hl.bcu/clean/duckdb"` inside CONFIG$cache
    - R/00_config.R contains `duckdb_path  = "/blue/erin.mobley-hl.bcu/clean/duckdb/pcornet.duckdb"` inside CONFIG$cache
    - Both duckdb entries are inside the `cache = list(...)` block (indented at same level as raw_dir, cohort_dir, outputs_dir)
    - EXTRACT_DATE is defined BEFORE or OUTSIDE the CONFIG list (it is a standalone constant)
    - No existing CONFIG entries are modified or removed
  </acceptance_criteria>
  <done>EXTRACT_DATE, CONFIG$cache$duckdb_dir, and CONFIG$cache$duckdb_path are defined in R/00_config.R</done>
</task>

<task type="auto">
  <name>Task 2: Create R/25_duckdb_ingest.R with atomic write and ingest logging</name>
  <files>R/25_duckdb_ingest.R</files>
  <read_first>
    - R/00_config.R (after Task 1 modifications -- CONFIG$cache$duckdb_path, CONFIG$cache$raw_dir, PCORNET_TABLES, EXTRACT_DATE)
    - R/01_load_pcornet.R (RDS cache read pattern -- readRDS from CONFIG$cache$raw_dir)
    - .planning/phases/29-duckdb-ingest-infrastructure/29-RESEARCH.md (architecture patterns, code examples, anti-patterns)
  </read_first>
  <action>
Create `R/25_duckdb_ingest.R` as a standalone script. The script:

**Header block:**
```r
# ==============================================================================
# 25_duckdb_ingest.R -- Ingest PCORnet CDM tables from RDS cache into DuckDB
# ==============================================================================
#
# Reads all 13 PCORnet tables from RDS cache (Phase 15) and writes them
# into a single DuckDB file with atomic write guarantee.
#
# Usage:
#   source("R/00_config.R")
#   source("R/25_duckdb_ingest.R")
#
# Output:
#   - DuckDB file at CONFIG$cache$duckdb_path (per D-01)
#   - Ingest log CSV at output/logs/duckdb_ingest_<EXTRACT_DATE>.csv
#
# Behavior:
#   - Always rebuilds from scratch (per D-02: no cache-check)
#   - Aborts on any table failure (per D-03: .tmp not swapped)
#   - Sequential table ingestion with gc() between tables
#   - Note: TUMOR_REGISTRY_ALL is NOT ingested -- it is a derived table (TR1+TR2+TR3)
#
# Requirements: DBING-01, DBING-02
# ==============================================================================
```

**Library loading:**
```r
source("R/00_config.R")

library(duckdb)
library(DBI)
library(dplyr)
library(readr)
library(glue)
```

**Constants (top of script, after library loading):**
```r
# Tables to ingest (same as PCORNET_TABLES from 00_config.R, 13 tables)
# Note: TUMOR_REGISTRY_ALL is NOT ingested -- it's a derived bind_rows(TR1+TR2+TR3)
TABLES_TO_INGEST <- PCORNET_TABLES

DUCKDB_PATH <- CONFIG$cache$duckdb_path
DUCKDB_DIR  <- CONFIG$cache$duckdb_dir
TMP_PATH    <- paste0(DUCKDB_PATH, ".tmp")
```

**Main ingest logic:**

1. Create output directories if they don't exist:
```r
if (!dir.exists(DUCKDB_DIR)) {
  dir.create(DUCKDB_DIR, recursive = TRUE, showWarnings = FALSE)
  message(glue("Created DuckDB directory: {DUCKDB_DIR}"))
}
if (!dir.exists("output/logs")) {
  dir.create("output/logs", recursive = TRUE, showWarnings = FALSE)
}
```

2. Clean up stale `.tmp` from prior interrupted run:
```r
if (file.exists(TMP_PATH)) {
  message(glue("Removing stale .tmp file from prior interrupted run: {TMP_PATH}"))
  file.remove(TMP_PATH)
}
```

3. Open DuckDB connection to `.tmp` path with `on.exit()` cleanup:
```r
message(strrep("=", 60))
message(glue("DuckDB Ingest: {length(TABLES_TO_INGEST)} tables from RDS cache"))
message(glue("Target: {DUCKDB_PATH}"))
message(glue("Building at: {TMP_PATH}"))
message(strrep("=", 60))

build_start <- Sys.time()

con <- DBI::dbConnect(duckdb::duckdb(), dbdir = TMP_PATH)
on.exit({
  tryCatch(DBI::dbDisconnect(con, shutdown = TRUE), error = function(e) NULL)
  # Clean up .tmp on error (per D-03: don't swap partial builds)
  if (file.exists(TMP_PATH)) {
    message(glue("Cleaning up .tmp file after error: {TMP_PATH}"))
    file.remove(TMP_PATH)
  }
}, add = TRUE)
```

4. Initialize ingest log data frame:
```r
ingest_log <- tibble(
  table_name   = character(),
  row_count    = integer(),
  col_count    = integer(),
  duration_sec = numeric()
)
```

5. Sequential table ingestion loop:
```r
for (tbl_name in TABLES_TO_INGEST) {
  rds_path <- file.path(CONFIG$cache$raw_dir, paste0(tbl_name, ".rds"))

  if (!file.exists(rds_path)) {
    stop(glue("RDS file not found for {tbl_name}: {rds_path}"))
  }

  message(glue("\n[{which(TABLES_TO_INGEST == tbl_name)}/{length(TABLES_TO_INGEST)}] Ingesting {tbl_name}..."))
  tbl_start <- Sys.time()

  # Load from RDS cache
  df <- readRDS(rds_path)
  message(glue("  Loaded RDS: {format(nrow(df), big.mark=',')} rows x {ncol(df)} cols"))

  # Write to DuckDB (per D-02: overwrite = TRUE for clean rebuild)
  DBI::dbWriteTable(con, tbl_name, df, overwrite = TRUE)

  # Record metrics
  duration <- as.numeric(difftime(Sys.time(), tbl_start, units = "secs"))
  ingest_log <- bind_rows(ingest_log, tibble(
    table_name   = tbl_name,
    row_count    = nrow(df),
    col_count    = ncol(df),
    duration_sec = round(duration, 2)
  ))

  message(glue("  Written to DuckDB in {round(duration, 1)}s"))

  # Free memory before next table
  rm(df)
  gc(verbose = FALSE)
}
```

6. After successful loop, disconnect cleanly and do atomic swap:
```r
# Disconnect cleanly before file operations
DBI::dbDisconnect(con, shutdown = TRUE)
on.exit()  # Clear the on.exit hook (build succeeded, don't clean up .tmp)

# Atomic swap: remove old canonical, rename .tmp to canonical
if (file.exists(DUCKDB_PATH)) {
  file.remove(DUCKDB_PATH)
  message(glue("Removed previous DuckDB file: {DUCKDB_PATH}"))
}
file.rename(TMP_PATH, DUCKDB_PATH)

build_duration <- as.numeric(difftime(Sys.time(), build_start, units = "secs"))
file_size_mb <- round(file.info(DUCKDB_PATH)$size / 1024^2, 1)
```

7. Write ingest log CSV:
```r
log_path <- file.path("output", "logs", glue("duckdb_ingest_{EXTRACT_DATE}.csv"))
readr::write_csv(ingest_log, log_path)
```

8. Print summary:
```r
message(strrep("=", 60))
message(glue("DuckDB Ingest COMPLETE"))
message(glue("  File: {DUCKDB_PATH}"))
message(glue("  Size: {file_size_mb} MB"))
message(glue("  Tables: {nrow(ingest_log)}"))
message(glue("  Total rows: {format(sum(ingest_log$row_count), big.mark=',')}"))
message(glue("  Build time: {round(build_duration, 1)}s ({round(build_duration/60, 1)} min)"))
message(glue("  Ingest log: {log_path}"))
message(strrep("=", 60))
```

Per D-02: Always rebuild from scratch -- no timestamp or cache-check logic.
Per D-03: Any table failure (missing RDS, dbWriteTable error) stops the loop. `on.exit()` cleans up `.tmp` and disconnects. The canonical file is untouched.
Per D-05: Ingest log filename uses EXTRACT_DATE ("2025-09-15").
  </action>
  <verify>
    <automated>grep -c "dbWriteTable" R/25_duckdb_ingest.R && grep -c "file.rename" R/25_duckdb_ingest.R && grep -c "on.exit" R/25_duckdb_ingest.R && grep "TABLES_TO_INGEST" R/25_duckdb_ingest.R | head -3</automated>
  </verify>
  <acceptance_criteria>
    - R/25_duckdb_ingest.R exists with 80+ lines
    - File contains `source("R/00_config.R")` near the top
    - File contains `library(duckdb)` and `library(DBI)`
    - File contains `TABLES_TO_INGEST <- PCORNET_TABLES` (uses the authoritative 13-table list)
    - File contains `TMP_PATH <- paste0(DUCKDB_PATH, ".tmp")` (atomic write pattern)
    - File contains `on.exit(` with both `dbDisconnect` and `.tmp` cleanup
    - File contains `DBI::dbWriteTable(con, tbl_name, df, overwrite = TRUE)` inside a for loop
    - File contains `file.rename(TMP_PATH, DUCKDB_PATH)` after loop completes
    - File contains `rm(df)` and `gc()` inside the loop (memory management)
    - File contains `readr::write_csv(ingest_log,` to produce the log CSV
    - File contains `duckdb_ingest_{EXTRACT_DATE}.csv` in the log path
    - File contains a comment stating TUMOR_REGISTRY_ALL is NOT ingested
    - File does NOT contain any cache-check or timestamp comparison logic (per D-02)
    - File calls `stop()` or allows error propagation on table failure (per D-03)
  </acceptance_criteria>
  <done>R/25_duckdb_ingest.R exists as a standalone script that ingests 13 PCORnet tables from RDS cache into a DuckDB file with atomic write and per-table logging</done>
</task>

</tasks>

<verification>
1. `R/00_config.R` contains EXTRACT_DATE, CONFIG$cache$duckdb_dir, CONFIG$cache$duckdb_path
2. `R/25_duckdb_ingest.R` exists with complete ingest loop, atomic write, and log output
3. Script sources `R/00_config.R` and uses PCORNET_TABLES as the table list
4. Atomic write pattern: build at .tmp, disconnect, rename to canonical path
5. Error handling: on.exit() cleans up .tmp and disconnects on failure
6. Memory management: rm(df) + gc() between tables
7. Ingest log CSV written to output/logs/duckdb_ingest_2025-09-15.csv
</verification>

<success_criteria>
- R/00_config.R has EXTRACT_DATE and DuckDB path constants added without breaking existing config
- R/25_duckdb_ingest.R is a complete, runnable standalone script
- All 5 locked decisions (D-01 through D-05) are honored in the implementation
- Script can be run on HiPerGator with `source("R/25_duckdb_ingest.R")`
</success_criteria>

<output>
After completion, create `.planning/phases/29-duckdb-ingest-infrastructure/29-01-SUMMARY.md`
</output>
