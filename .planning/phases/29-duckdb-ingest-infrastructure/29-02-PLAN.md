---
phase: 29-duckdb-ingest-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - R/25_duckdb_ingest.R
  - R/utils_duckdb.R
autonomous: true
requirements:
  - DBING-03

must_haves:
  truths:
    - "User can see PATID indexes on all 13 tables after ingest completes"
    - "User can see ENCOUNTERID indexes on the 6 tables that have ENCOUNTERID columns"
    - "User can verify round-trip dimension and column name verification passes for all 13 tables"
    - "A failed index logs a warning but does not abort the build"
  artifacts:
    - path: "R/25_duckdb_ingest.R"
      provides: "Index creation loop added after table writes, before atomic swap"
      contains: "CREATE INDEX"
    - path: "R/utils_duckdb.R"
      provides: "verify_duckdb_roundtrip() function for dimension/column verification"
      exports: ["verify_duckdb_roundtrip"]
      min_lines: 30
  key_links:
    - from: "R/25_duckdb_ingest.R"
      to: "R/utils_duckdb.R"
      via: "source() call to load verify_duckdb_roundtrip()"
      pattern: "source.*utils_duckdb|verify_duckdb_roundtrip"
    - from: "R/25_duckdb_ingest.R"
      to: "DuckDB .tmp file"
      via: "CREATE INDEX statements via DBI::dbExecute()"
      pattern: "CREATE INDEX"
---

<objective>
Add index creation and round-trip verification to the DuckDB ingest script, ensuring all tables have PATID indexes and tables with ENCOUNTERID columns get secondary indexes, with a reusable verification helper in utils_duckdb.R.

Purpose: Indexes on PATID and ENCOUNTERID dramatically speed up the join-heavy queries used throughout the cohort pipeline (Phase 30-31). Round-trip verification catches schema drift before the DuckDB file is promoted to canonical.

Output: Extended `R/25_duckdb_ingest.R` with index creation and verification steps; new `R/utils_duckdb.R` with `verify_duckdb_roundtrip()` function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-duckdb-ingest-infrastructure/29-CONTEXT.md
@.planning/phases/29-duckdb-ingest-infrastructure/29-RESEARCH.md
@.planning/phases/29-duckdb-ingest-infrastructure/29-01-SUMMARY.md

@R/00_config.R
@R/01_load_pcornet.R

<interfaces>
<!-- From Plan 29-01: the ingest script structure that this plan extends -->

From R/25_duckdb_ingest.R (created by Plan 29-01):
```r
# Script structure (insertion points for Plan 29-02):
#
# 1. Libraries + source("R/00_config.R")
# 2. Constants: TABLES_TO_INGEST, DUCKDB_PATH, TMP_PATH
# 3. Directory creation + .tmp cleanup
# 4. con <- dbConnect(..., dbdir = TMP_PATH)
# 5. on.exit({ disconnect + .tmp cleanup })
# 6. for (tbl_name in TABLES_TO_INGEST) { ... dbWriteTable ... rm(df); gc() }
#    <<<< INSERT INDEX CREATION HERE (after loop, before disconnect) >>>>
#    <<<< INSERT ROUND-TRIP VERIFICATION HERE (after indexes) >>>>
# 7. dbDisconnect(con, shutdown = TRUE)
# 8. on.exit()  # clear hook
# 9. file.rename(TMP_PATH, DUCKDB_PATH)  # atomic swap
# 10. write_csv(ingest_log, ...)
# 11. Summary message
```

From R/00_config.R (relevant constants):
```r
PCORNET_TABLES <- c(
  "ENROLLMENT", "DIAGNOSIS", "PROCEDURES", "PRESCRIBING", "ENCOUNTER",
  "DEMOGRAPHIC", "TUMOR_REGISTRY1", "TUMOR_REGISTRY2", "TUMOR_REGISTRY3",
  "DISPENSING", "MED_ADMIN", "LAB_RESULT_CM", "PROVIDER"
)
CONFIG$cache$raw_dir = "/blue/erin.mobley-hl.bcu/clean/rds/raw"
```

From R/01_load_pcornet.R column specs (verified ENCOUNTERID presence):
```r
# Tables WITH ENCOUNTERID column: DIAGNOSIS, PROCEDURES, PRESCRIBING,
#   ENCOUNTER, MED_ADMIN, LAB_RESULT_CM (6 tables)
# Tables WITHOUT ENCOUNTERID: ENROLLMENT, DEMOGRAPHIC,
#   TUMOR_REGISTRY1/2/3, DISPENSING, PROVIDER (7 tables)
```

Note: The RESEARCH.md lists 8 tables with ENCOUNTERID (including DISPENSING and PROVIDER),
but actual column specs in 01_load_pcornet.R show only 6 tables have it.
The PCORnet CDM v7.0 DISPENSING table does not have ENCOUNTERID;
PROVIDER has PROVIDERID as its primary key (not linked by ENCOUNTERID).
Use the actual column specs as source of truth.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/utils_duckdb.R with verify_duckdb_roundtrip()</name>
  <files>R/utils_duckdb.R</files>
  <read_first>
    - R/00_config.R (CONFIG$cache$raw_dir path for RDS source lookup)
    - R/utils_snapshot.R (pattern reference for utility function style)
    - .planning/phases/29-duckdb-ingest-infrastructure/29-RESEARCH.md (Pattern 4: Round-Trip Verification code example)
  </read_first>
  <action>
Create `R/utils_duckdb.R` with the `verify_duckdb_roundtrip()` function. This file will be extended in Phase 30 with additional DuckDB utilities (connection management, dispatcher, etc.), so keep it clean and well-structured as a foundation.

```r
# ==============================================================================
# utils_duckdb.R -- DuckDB utility functions
# ==============================================================================
#
# Phase 29: verify_duckdb_roundtrip() for ingest validation
# Phase 30: Will add get_pcornet_table(), open_pcornet_con(), close_pcornet_con(),
#           materialize(), and USE_DUCKDB dispatcher logic
#
# ==============================================================================

library(DBI)
library(glue)

# ------------------------------------------------------------------------------
# verify_duckdb_roundtrip()
# ------------------------------------------------------------------------------
#' Verify round-trip integrity of a single table in DuckDB
#'
#' Compares dimension (nrow x ncol) and column names between the RDS source
#' and the DuckDB copy. Does NOT compare values (that's Phase 31 parity testing).
#'
#' @param table_name Character. Name of the table (e.g., "ENROLLMENT")
#' @param con DBI connection to DuckDB file
#' @param raw_dir Character. Path to RDS cache directory (default: CONFIG$cache$raw_dir)
#'
#' @return List with elements:
#'   - ok: logical, TRUE if dimensions and column names match
#'   - table_name: character
#'   - rds_nrow, rds_ncol: integer, dimensions from RDS source
#'   - ddb_nrow, ddb_ncol: integer, dimensions from DuckDB table
#'   - dim_match: logical, TRUE if nrow and ncol match
#'   - col_match: logical, TRUE if column names match (order-sensitive)
#'   - mismatched_cols: character vector of column names that differ (empty if match)
#'
verify_duckdb_roundtrip <- function(table_name, con, raw_dir = CONFIG$cache$raw_dir) {
  # Read RDS source dimensions (without loading full data)
  rds_path <- file.path(raw_dir, paste0(table_name, ".rds"))
  if (!file.exists(rds_path)) {
    warning(glue("RDS file not found for verification: {rds_path}"))
    return(list(
      ok = FALSE, table_name = table_name,
      rds_nrow = NA, rds_ncol = NA,
      ddb_nrow = NA, ddb_ncol = NA,
      dim_match = FALSE, col_match = FALSE,
      mismatched_cols = "RDS file not found"
    ))
  }

  rds_df <- readRDS(rds_path)
  rds_dims <- dim(rds_df)
  rds_cols <- colnames(rds_df)
  rm(rds_df)
  gc(verbose = FALSE)

  # Read DuckDB table dimensions via SQL (avoids materializing full table)
  ddb_nrow <- DBI::dbGetQuery(con, glue("SELECT COUNT(*) AS n FROM {table_name}"))$n
  ddb_cols <- DBI::dbListFields(con, table_name)
  ddb_ncol <- length(ddb_cols)

  # Compare dimensions
  dim_match <- (rds_dims[1] == ddb_nrow) && (rds_dims[2] == ddb_ncol)

  # Compare column names (order-sensitive)
  col_match <- identical(rds_cols, ddb_cols)
  mismatched <- character()
  if (!col_match) {
    in_rds_not_ddb <- setdiff(rds_cols, ddb_cols)
    in_ddb_not_rds <- setdiff(ddb_cols, rds_cols)
    if (length(in_rds_not_ddb) > 0) {
      mismatched <- c(mismatched, paste0("in RDS only: ", paste(in_rds_not_ddb, collapse = ", ")))
    }
    if (length(in_ddb_not_rds) > 0) {
      mismatched <- c(mismatched, paste0("in DuckDB only: ", paste(in_ddb_not_rds, collapse = ", ")))
    }
    if (length(mismatched) == 0 && !identical(rds_cols, ddb_cols)) {
      mismatched <- "column order differs"
    }
  }

  ok <- dim_match && col_match

  list(
    ok = ok,
    table_name = table_name,
    rds_nrow = rds_dims[1],
    rds_ncol = rds_dims[2],
    ddb_nrow = ddb_nrow,
    ddb_ncol = ddb_ncol,
    dim_match = dim_match,
    col_match = col_match,
    mismatched_cols = if (length(mismatched) > 0) mismatched else character()
  )
}
```

Key design choices:
- Uses `DBI::dbGetQuery(con, "SELECT COUNT(*) ...")` + `DBI::dbListFields()` to get DuckDB metadata WITHOUT materializing the full table (memory-efficient for large tables like DIAGNOSIS).
- Loads RDS only to get `dim()` and `colnames()`, then immediately `rm()` + `gc()`.
- Returns a detailed result list so the caller can report specific failures.
- Default `raw_dir = CONFIG$cache$raw_dir` assumes 00_config.R is already sourced.
  </action>
  <verify>
    <automated>grep -c "verify_duckdb_roundtrip" R/utils_duckdb.R && grep "function" R/utils_duckdb.R | head -3</automated>
  </verify>
  <acceptance_criteria>
    - R/utils_duckdb.R exists with 30+ lines
    - File defines `verify_duckdb_roundtrip <- function(table_name, con, raw_dir = CONFIG$cache$raw_dir)`
    - Function uses `DBI::dbGetQuery(con, glue("SELECT COUNT(*) AS n FROM {table_name}"))` for row count (not dbReadTable)
    - Function uses `DBI::dbListFields(con, table_name)` for column names
    - Function returns a list with at minimum: `ok`, `table_name`, `dim_match`, `col_match`
    - Function calls `readRDS()` to load RDS source, then `rm()` + `gc()` after extracting dims/cols
    - File contains `library(DBI)` and `library(glue)`
    - File header comment mentions Phase 30 expansion plans
  </acceptance_criteria>
  <done>R/utils_duckdb.R exists with verify_duckdb_roundtrip() function that compares RDS dimensions and column names against DuckDB table metadata</done>
</task>

<task type="auto">
  <name>Task 2: Add index creation and round-trip verification to R/25_duckdb_ingest.R</name>
  <files>R/25_duckdb_ingest.R</files>
  <read_first>
    - R/25_duckdb_ingest.R (current state from Plan 29-01 -- find insertion point between table loop and dbDisconnect)
    - R/utils_duckdb.R (verify_duckdb_roundtrip function signature from Task 1)
    - R/01_load_pcornet.R (confirm which tables have ENCOUNTERID: DIAGNOSIS, PROCEDURES, PRESCRIBING, ENCOUNTER, MED_ADMIN, LAB_RESULT_CM)
    - .planning/phases/29-duckdb-ingest-infrastructure/29-RESEARCH.md (Pattern 3: Index Creation After Data Load)
  </read_first>
  <action>
Extend R/25_duckdb_ingest.R with three additions:

**Addition 1: Source utils_duckdb.R near the top (after source("R/00_config.R")):**
```r
source("R/utils_duckdb.R")
```

**Addition 2: Add TABLES_WITH_ENCOUNTERID constant (after TABLES_TO_INGEST):**
```r
# Tables that have an ENCOUNTERID column (verified from 01_load_pcornet.R col_type specs)
# Used for secondary index creation. DISPENSING and PROVIDER do NOT have ENCOUNTERID.
TABLES_WITH_ENCOUNTERID <- c(
  "DIAGNOSIS", "PROCEDURES", "PRESCRIBING", "ENCOUNTER",
  "MED_ADMIN", "LAB_RESULT_CM"
)
```

**Addition 3: Insert index creation + round-trip verification BETWEEN the table write loop and the `dbDisconnect()` call.** Find the line after the `for (tbl_name in TABLES_TO_INGEST) { ... }` loop closes and BEFORE the `DBI::dbDisconnect(con, shutdown = TRUE)` call. Insert:

```r
# ==============================================================================
# INDEX CREATION (Phase 29 Plan 02 -- DBING-03)
# ==============================================================================
# Build indexes AFTER all data is loaded (faster than during insert).
# Per D-04: tryCatch() per index -- failed index = warning, not error.
# An unindexed table is still queryable, just slower.

message(glue("\n{strrep('=', 60)}"))
message("Creating indexes...")
message(strrep("=", 60))

index_results <- tibble(
  table_name = character(),
  index_name = character(),
  column     = character(),
  status     = character()
)

# PATID indexes on all 13 tables (universal join key)
# Note: PCORnet CDM uses "ID" as the patient ID column name, not "PATID"
for (tbl_name in TABLES_TO_INGEST) {
  idx_name <- paste0("idx_", tolower(tbl_name), "_patid")
  tryCatch({
    DBI::dbExecute(con, glue("CREATE INDEX {idx_name} ON {tbl_name} (ID)"))
    message(glue("  Created: {idx_name}"))
    index_results <- bind_rows(index_results, tibble(
      table_name = tbl_name, index_name = idx_name,
      column = "ID", status = "created"
    ))
  }, error = function(e) {
    warning(glue("  FAILED: {idx_name} -- {e$message}"))
    index_results <<- bind_rows(index_results, tibble(
      table_name = tbl_name, index_name = idx_name,
      column = "ID", status = paste0("failed: ", e$message)
    ))
  })
}

# ENCOUNTERID indexes on tables that have it (6 tables)
for (tbl_name in TABLES_WITH_ENCOUNTERID) {
  idx_name <- paste0("idx_", tolower(tbl_name), "_encounterid")
  tryCatch({
    DBI::dbExecute(con, glue("CREATE INDEX {idx_name} ON {tbl_name} (ENCOUNTERID)"))
    message(glue("  Created: {idx_name}"))
    index_results <- bind_rows(index_results, tibble(
      table_name = tbl_name, index_name = idx_name,
      column = "ENCOUNTERID", status = "created"
    ))
  }, error = function(e) {
    warning(glue("  FAILED: {idx_name} -- {e$message}"))
    index_results <<- bind_rows(index_results, tibble(
      table_name = tbl_name, index_name = idx_name,
      column = "ENCOUNTERID", status = paste0("failed: ", e$message)
    ))
  })
}

n_created <- sum(index_results$status == "created")
n_failed  <- sum(index_results$status != "created")
message(glue("\nIndexes: {n_created} created, {n_failed} failed (of {nrow(index_results)} total)"))

# ==============================================================================
# ROUND-TRIP VERIFICATION (Phase 29 Plan 02 -- DBING-03)
# ==============================================================================
# Verify dimensions and column names match between RDS source and DuckDB copy.
# Full value parity testing is deferred to Phase 31.

message(glue("\n{strrep('=', 60)}"))
message("Round-trip verification...")
message(strrep("=", 60))

all_ok <- TRUE
for (tbl_name in TABLES_TO_INGEST) {
  result <- verify_duckdb_roundtrip(tbl_name, con)
  if (result$ok) {
    message(glue("  PASS: {tbl_name} ({format(result$rds_nrow, big.mark=',')} rows x {result$rds_ncol} cols)"))
  } else {
    message(glue("  FAIL: {tbl_name} -- dim_match={result$dim_match}, col_match={result$col_match}"))
    if (length(result$mismatched_cols) > 0) {
      message(glue("         {paste(result$mismatched_cols, collapse = '; ')}"))
    }
    all_ok <- FALSE
  }
}

if (!all_ok) {
  stop("Round-trip verification FAILED for one or more tables. DuckDB file NOT promoted.")
}

message(glue("\nAll {length(TABLES_TO_INGEST)} tables passed round-trip verification."))
```

**CRITICAL: The column name for PATID indexes is `ID`, not `PATID`.** The PCORnet CDM data uses "ID" as the patient identifier column across all 13 tables (see R/00_config.R comment: "NOTE: Patient ID column is 'ID' (not 'PATID') across all tables"). The index names use `_patid` as a descriptive suffix but the actual SQL column is `ID`.

**Addition 4: Update the summary message at the end of the script** (after atomic swap) to include index and verification info:
```r
# Add after the existing summary lines:
message(glue("  Indexes: {n_created} created ({n_failed} failed)"))
message(glue("  Verification: {length(TABLES_TO_INGEST)}/{length(TABLES_TO_INGEST)} tables passed"))
```
  </action>
  <verify>
    <automated>grep -c "CREATE INDEX" R/25_duckdb_ingest.R && grep -c "tryCatch" R/25_duckdb_ingest.R && grep -c "verify_duckdb_roundtrip" R/25_duckdb_ingest.R && grep "TABLES_WITH_ENCOUNTERID" R/25_duckdb_ingest.R | head -2</automated>
  </verify>
  <acceptance_criteria>
    - R/25_duckdb_ingest.R contains `source("R/utils_duckdb.R")`
    - File contains `TABLES_WITH_ENCOUNTERID <- c("DIAGNOSIS", "PROCEDURES", "PRESCRIBING", "ENCOUNTER", "MED_ADMIN", "LAB_RESULT_CM")` (exactly 6 tables)
    - File contains `CREATE INDEX {idx_name} ON {tbl_name} (ID)` for PATID indexes (column is "ID" not "PATID")
    - File contains `CREATE INDEX {idx_name} ON {tbl_name} (ENCOUNTERID)` for ENCOUNTERID indexes
    - Every CREATE INDEX is wrapped in `tryCatch()` with error handler that calls `warning()` (per D-04)
    - File contains `verify_duckdb_roundtrip(tbl_name, con)` called in a loop over all 13 tables
    - File contains `stop("Round-trip verification FAILED` if any table fails verification
    - Index creation and verification code appears BEFORE `dbDisconnect()` and BEFORE atomic swap
    - The on.exit() hook still cleans up .tmp on error (index/verification failures trigger stop() which fires on.exit)
    - Summary message includes index count and verification status
  </acceptance_criteria>
  <done>R/25_duckdb_ingest.R has index creation (13 PATID + 6 ENCOUNTERID = 19 indexes) and round-trip verification for all 13 tables, with tryCatch per index and stop on verification failure</done>
</task>

</tasks>

<verification>
1. `R/utils_duckdb.R` exists with `verify_duckdb_roundtrip()` function
2. `R/25_duckdb_ingest.R` sources `R/utils_duckdb.R`
3. Index creation loop builds 13 PATID indexes (on column `ID`) + 6 ENCOUNTERID indexes = 19 total
4. Each CREATE INDEX wrapped in tryCatch (per D-04: warnings, not errors)
5. Round-trip verification runs on all 13 tables after index creation
6. Verification failure calls stop() which triggers on.exit() cleanup (no partial DuckDB promoted)
7. Summary message reports index and verification status
</verification>

<success_criteria>
- R/utils_duckdb.R is a clean foundation file ready for Phase 30 expansion
- R/25_duckdb_ingest.R is a complete end-to-end script: ingest + index + verify + atomic swap
- All D-04 requirements honored: tryCatch per index, warnings not errors
- DBING-03 fully addressed: PATID indexes on 13 tables, ENCOUNTERID indexes on 6 tables, round-trip verification
</success_criteria>

<output>
After completion, create `.planning/phases/29-duckdb-ingest-infrastructure/29-02-SUMMARY.md`
</output>
