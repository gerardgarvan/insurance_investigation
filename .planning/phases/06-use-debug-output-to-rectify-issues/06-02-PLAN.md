---
phase: 06-use-debug-output-to-rectify-issues
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - R/utils_dates.R
  - R/01_load_pcornet.R
  - R/00_config.R
autonomous: false
requirements:
  - RECT-03
  - RECT-04

must_haves:
  truths:
    - "User can share diagnostic CSVs from HiPerGator and Claude writes targeted fixes based on actual findings"
    - "Date parsing failures are reduced by adding new format handlers discovered in date_parsing_failures.csv"
    - "Date column detection regex catches all date columns identified by date_column_regex_audit.csv"
    - "TUMOR_REGISTRY col_types specs are updated for columns flagged by tr_type_audit.csv"
    - "Numeric range validation columns (_VALID suffix) are added for age, tumor size, and date columns"
    - "R vs Python payer mapping differences are documented side-by-side in 00_config.R comments"
  artifacts:
    - path: "R/utils_dates.R"
      provides: "Expanded parse_pcornet_date() with new format handlers"
      contains: "parse_pcornet_date"
    - path: "R/01_load_pcornet.R"
      provides: "Updated date_regex, col_types specs, and validation columns"
      contains: "date_regex"
    - path: "R/00_config.R"
      provides: "Payer mapping with R vs Python comparison comments"
      contains: "PAYER_MAPPING"
  key_links:
    - from: "R/utils_dates.R"
      to: "R/01_load_pcornet.R"
      via: "parse_pcornet_date() called in load_pcornet_table() date column loop"
      pattern: "parse_pcornet_date"
    - from: "R/01_load_pcornet.R"
      to: "R/01_load_pcornet.R"
      via: "date_regex drives which columns get date-parsed in load_pcornet_table()"
      pattern: "date_regex"
    - from: "R/01_load_pcornet.R"
      to: "R/01_load_pcornet.R"
      via: "TABLE_SPECS used by load_pcornet_table() for col_types argument"
      pattern: "TABLE_SPECS"
---

<objective>
Apply data-driven fixes to the pipeline based on actual diagnostic output from 07_diagnostics.R run on HiPerGator.

Purpose: Per D-11, fixes are data-driven -- the user runs diagnostics first, shares CSV files and sample raw data rows, then Claude writes targeted fixes. This plan covers all code-level fixes: expanding the date parser (D-03), updating date column regex (D-07), correcting col_types specs (D-05), adding numeric validation columns (D-08), and documenting payer mapping differences (D-04). Fixes go directly into existing scripts per D-10, grouped by issue type per D-13.

Output: Updated R/utils_dates.R, R/01_load_pcornet.R, R/00_config.R with all data-driven fixes applied
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-use-debug-output-to-rectify-issues/06-CONTEXT.md
@.planning/phases/06-use-debug-output-to-rectify-issues/06-RESEARCH.md

@R/utils_dates.R
@R/01_load_pcornet.R
@R/00_config.R
@csv_columns.txt

<interfaces>
<!-- Key contracts the executor needs -->

From R/utils_dates.R (current parse_pcornet_date, 4 format handlers):
```r
parse_pcornet_date <- function(date_char) {
  # Attempt 1: YYYY-MM-DD (ISO) via lubridate::ymd()
  # Attempt 2: Excel serial numbers (numeric 1-100000) via janitor::excel_numeric_to_date()
  # Attempt 3: SAS DATE9 (DDMMMYYYY) via lubridate::parse_date_time(orders="dby")
  # Attempt 4: YYYYMMDD (compact, 8-char strings) via lubridate::ymd()
  # Logs unparsed count via message()
}
```

From R/01_load_pcornet.R line 231 (current date_regex):
```r
date_regex <- "(?i)(DATE|^DT_|^BDATE$|^DOD$|^DT_FU$|DXDATE|_DT$|RECUR_DT|COMBINED_LAST_CONTACT|ADDRESS_PERIOD_START|ADDRESS_PERIOD_END)"
```

From R/01_load_pcornet.R lines 158-180 (current TUMOR_REGISTRY specs):
```r
TUMOR_REGISTRY1_SPEC <- cols(
  .default = col_character(),
  AGE_AT_DIAGNOSIS = col_integer(),
  TUMOR_SIZE_SUMMARY = col_double(),
  TUMOR_SIZE_CLINICAL = col_double(),
  TUMOR_SIZE_PATHOLOGIC = col_double()
)
TUMOR_REGISTRY2_SPEC <- cols(.default = col_character(), DXAGE = col_integer())
TUMOR_REGISTRY3_SPEC <- cols(.default = col_character(), DXAGE = col_integer())
```

From R/00_config.R lines 181-210 (current PAYER_MAPPING):
```r
PAYER_MAPPING <- list(
  medicare_prefix = "1",
  medicaid_prefix = "2",
  private_prefix = c("5", "6"),
  other_gov_prefix = c("3", "4"),
  no_payment_prefix = "8",
  other_prefix = c("7", "9"),
  unavailable_codes = c("99", "9999"),
  unknown_codes = c("NI", "UN", "OT", "UNKNOWN"),
  dual_eligible_codes = c("14", "141", "142"),
  sentinel_values = c("NI", "UN", "OT"),
  categories = c("Medicare", "Medicaid", "Dual eligible", "Private",
                  "Other government", "No payment / Self-pay", "Other",
                  "Unavailable", "Unknown")
)
```

Diagnostic CSV files (user shares from output/diagnostics/ on HiPerGator):
- date_parsing_failures.csv: table, column, type_detected, n_total, n_na, na_percent, sample_raw_values
- date_column_regex_audit.csv: table, column, regex_match (TRUE/FALSE), potentially_missed flag
- tr_type_audit.csv: table, column, current_type, pct_numeric, pct_datelike, recommendation
- numeric_range_issues.csv: table, column, issue_type, n_affected, sample_values
- payer_mapping_audit.csv: PAYER_CATEGORY_PRIMARY, n_patients, pct
- missing_values_audit.csv: table, column, n_total, n_missing, pct_missing
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User shares diagnostic output from HiPerGator</name>
  <files>output/diagnostics/date_parsing_failures.csv, output/diagnostics/tr_type_audit.csv, output/diagnostics/payer_mapping_audit.csv</files>
  <action>
Phase 5 created 07_diagnostics.R (817 lines, 6 sections) and Phase 6 Plan 01 updated has_hodgkin_diagnosis() with HL_SOURCE tracking. The user needs to run the full pipeline and diagnostics on HiPerGator to produce the diagnostic CSVs that drive all fixes in this plan.

On HiPerGator (interactive RStudio session):

1. Pull latest code changes (Phase 5 + Phase 6 Plan 01 commits)

2. Run the diagnostic script:
   ```r
   source("R/07_diagnostics.R")
   ```

3. Share the following diagnostic CSV files with Claude:
   - output/diagnostics/date_parsing_failures.csv (Section 1 -- shows which date columns have parse failures and sample raw values)
   - output/diagnostics/date_column_regex_audit.csv (Section 2 -- shows which columns the date regex missed)
   - output/diagnostics/tr_type_audit.csv (Section 3 -- shows TUMOR_REGISTRY columns that should be numeric/date but are character)
   - output/diagnostics/numeric_range_issues.csv (Section 6 -- shows implausible ages, dates, tumor sizes)
   - output/diagnostics/payer_mapping_audit.csv (Section 5 -- shows payer category distribution for R vs Python comparison)
   - output/diagnostics/missing_values_audit.csv (Section 3 -- shows columns with >10% missing for D-09 investigation)
   - output/diagnostics/hl_identification_venn.csv (Section 4 -- shows HL source breakdown including "Neither" count)

4. Also share:
   - Console output from 07_diagnostics.R (copy/paste the full console output)
   - For any date_parsing_failures with high NA rates (>5%): share 5-10 sample raw values from the problematic column so Claude can identify the date format pattern

5. If the cohort rebuild ran (via source("R/04_build_cohort.R")):
   - Share the attrition log console output
   - Confirm excluded_no_hl_evidence.csv was written (if any "Neither" patients exist)
  </action>
  <verify>User confirms diagnostic CSV files and console output have been shared</verify>
  <done>User has shared all diagnostic CSV files from HiPerGator. Claude has the data needed to write targeted fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply all data-driven fixes to pipeline scripts</name>
  <files>R/utils_dates.R, R/01_load_pcornet.R, R/00_config.R</files>
  <read_first>
    - R/utils_dates.R (full file -- current parse_pcornet_date with 4 format handlers)
    - R/01_load_pcornet.R (full file -- current date_regex at line 231, TABLE_SPECS at lines 33-196, load_pcornet_table at lines 216-246)
    - R/00_config.R (full file -- current PAYER_MAPPING at lines 181-210)
    - csv_columns.txt (full column listing for all 22 PCORnet tables -- reference for regex audit)
    - The diagnostic CSV files and console output shared by the user in Task 1
  </read_first>
  <action>
Apply fixes grouped by issue type (per D-13). ALL fixes go directly into existing scripts (per D-10). The specific changes depend on what the diagnostic output reveals, but the structure is:

**Batch 1: Date parser expansion (D-03) -- R/utils_dates.R**

Analyze date_parsing_failures.csv sample_raw_values column. For each new date format pattern found:

Add new Attempt blocks AFTER Attempt 4 in parse_pcornet_date(), following the existing pattern:
```r
  # Attempt N: [FORMAT DESCRIPTION] (discovered in diagnostics)
  remaining <- is.na(result) & !is.na(date_char)
  if (any(remaining)) {
    parsed_new <- suppressWarnings([lubridate_parser](date_char[remaining], quiet = TRUE))
    if (any(!is.na(parsed_new))) {
      idx_in_remaining <- which(remaining)[!is.na(parsed_new)]
      result[idx_in_remaining] <- [parsed_value][!is.na(parsed_new)]
    }
  }
```

Common PCORnet date formats to check for in diagnostics output:
- MM/DD/YYYY (US format) -> add `mdy()` attempt
- DD/MM/YYYY (European) -> add `dmy()` attempt
- YYYY-MM-DD HH:MM:SS (datetime strings) -> add `ymd_hms()` with as.Date() conversion
- SAS numeric (days since 1960-01-01) -> add `as.Date("1960-01-01") + numeric_val` attempt

CRITICAL: Always use `suppressWarnings()` around parse attempts. Always validate `idx_in_remaining` indexing. Always check `any(remaining)` before attempting new format. Follow the exact index-arithmetic pattern from existing Attempts 1-4.

**Batch 2: Date column detection regex expansion (D-07) -- R/01_load_pcornet.R line 231**

Analyze date_column_regex_audit.csv for columns with `potentially_missed = TRUE` or `regex_match = FALSE` that are genuinely date columns. Update the date_regex string to include new patterns.

CAUTION per research pitfall 4: After expanding regex, verify ALL new matches are actually date columns. Do NOT add "TIME" blindly if it catches TIME_TO_EVENT (numeric duration). Use specific column names or anchored patterns if needed.

Update the regex at line 231 AND the matching comment at lines 228-230.

**Batch 3: TUMOR_REGISTRY col_types updates (D-05) -- R/01_load_pcornet.R lines 158-180**

Analyze tr_type_audit.csv for columns with pct_numeric > 80% or pct_datelike > 80%. For each flagged column:
- If pct_numeric > 80%: change to `col_integer()` (for count/code columns) or `col_double()` (for measurement columns)
- If pct_datelike > 80%: keep as `col_character()` (date columns always stay character for parse_pcornet_date() -- per research pitfall 2)

Update TUMOR_REGISTRY1_SPEC, TUMOR_REGISTRY2_SPEC, and TUMOR_REGISTRY3_SPEC as needed. Add inline comments noting "per tr_type_audit.csv" for each added column.

**Batch 4: Numeric range validation columns (D-08) -- R/01_load_pcornet.R load_pcornet_table()**

Add validation column logic AFTER the date parsing loop in load_pcornet_table() (after line 236). For each numeric range issue type found in numeric_range_issues.csv:

```r
  # Numeric range validation (D-08): preserve raw values, add _VALID flags
  if (table_name == "TUMOR_REGISTRY1" && "AGE_AT_DIAGNOSIS" %in% names(df)) {
    df <- df %>%
      mutate(
        AGE_AT_DIAGNOSIS_VALID = case_when(
          is.na(AGE_AT_DIAGNOSIS) ~ NA,
          AGE_AT_DIAGNOSIS < 0 ~ FALSE,
          AGE_AT_DIAGNOSIS > 120 ~ FALSE,
          TRUE ~ TRUE
        )
      )
    n_invalid <- sum(!df$AGE_AT_DIAGNOSIS_VALID, na.rm = TRUE)
    if (n_invalid > 0) {
      message(glue("  Validation: {n_invalid} invalid AGE_AT_DIAGNOSIS values flagged"))
    }
  }
```

Apply same pattern for:
- DXAGE in TUMOR_REGISTRY2/3 (range 0-120)
- TUMOR_SIZE_SUMMARY, TUMOR_SIZE_CLINICAL, TUMOR_SIZE_PATHOLOGIC in TUMOR_REGISTRY1 (range 0-999)
- All parsed date columns (range 1900-01-01 to Sys.Date())

Add `library(dplyr)` to R/01_load_pcornet.R if not already imported (needed for mutate/case_when).

**Batch 5: Payer mapping documentation (D-04) -- R/00_config.R**

Analyze payer_mapping_audit.csv for category distribution. Add a comment block above the PAYER_MAPPING list documenting the R vs Python comparison:

```r
# ------------------------------------------------------------------------------
# R vs Python Payer Mapping Comparison (D-04)
# ------------------------------------------------------------------------------
# Category          | R pipeline | Python pipeline | Notes
# Medicare          |   XX.X%    |     XX.X%       |
# Medicaid          |   XX.X%    |     XX.X%       |
# Dual eligible     |   XX.X%    |     XX.X%       | Detection via encounter-level overlap
# Private           |   XX.X%    |     XX.X%       |
# ...
# NOTE: Exact parity not required (D-04). R pipeline is exploratory.
# Differences expected from: different dual-eligible detection thresholds,
# encounter vs enrollment-level aggregation, sentinel value handling.
# ------------------------------------------------------------------------------
```

Fill in actual percentages from payer_mapping_audit.csv (R pipeline) and Python pipeline reference (user provides or note "Python pipeline: TBD").

**Batch 6: Missing value investigation (D-09)**

For columns with >50% missing in missing_values_audit.csv:
- Check if column is optional per PCORnet CDM spec -> document as "accepted" (genuinely absent)
- Check if column has data in Python pipeline but not R -> likely parsing issue, fix in appropriate batch
- Add inline comments in TABLE_SPECS for any columns documented as intentionally optional
  </action>
  <verify>
    <automated>grep -c "Attempt" R/utils_dates.R</automated>
  </verify>
  <acceptance_criteria>
    - R/utils_dates.R contains more Attempt blocks than the original 4 (unless diagnostics show no new formats needed -- in that case, add a comment noting "Diagnostics confirmed 4 formats are sufficient")
    - R/utils_dates.R uses `suppressWarnings()` for all new parse attempts
    - R/01_load_pcornet.R date_regex string is updated if date_column_regex_audit.csv showed missed columns
    - R/01_load_pcornet.R TUMOR_REGISTRY specs updated if tr_type_audit.csv showed columns needing type changes
    - R/01_load_pcornet.R load_pcornet_table() contains `_VALID` validation column logic for age and tumor size ranges
    - R/01_load_pcornet.R contains `case_when(` for at least one _VALID validation column
    - R/00_config.R PAYER_MAPPING section contains "R vs Python" comparison comment block
    - All changes are in existing scripts (no new files created per D-10)
  </acceptance_criteria>
  <done>All data-driven fixes applied: date parser expanded, regex updated, col_types corrected, validation columns added, payer mapping documented</done>
</task>

</tasks>

<verification>
1. `grep -c "Attempt" R/utils_dates.R` shows >= 4 attempt blocks (original or expanded)
2. `grep "date_regex" R/01_load_pcornet.R` shows updated regex pattern
3. `grep "_VALID" R/01_load_pcornet.R` shows validation column definitions
4. `grep "R vs Python" R/00_config.R` confirms payer comparison documentation
5. `grep "case_when" R/01_load_pcornet.R` confirms validation logic
</verification>

<success_criteria>
- Date parsing failures reduced (user re-runs diagnostics to confirm)
- All date columns detected by regex (no "potentially missed" columns remain)
- TUMOR_REGISTRY col_types accurate per audit findings
- Numeric range validation columns added for age, tumor size, dates
- R vs Python payer mapping differences documented
- All fixes in existing scripts (D-10), grouped by issue type (D-13)
</success_criteria>

<output>
After completion, create `.planning/phases/06-use-debug-output-to-rectify-issues/06-02-SUMMARY.md`
</output>
