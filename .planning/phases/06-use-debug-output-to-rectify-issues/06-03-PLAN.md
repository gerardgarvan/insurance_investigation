---
phase: 06-use-debug-output-to-rectify-issues
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - R/07_diagnostics.R
  - R/08_data_quality_summary.R
autonomous: false
requirements:
  - RECT-05

must_haves:
  truths:
    - "User can rebuild the full pipeline end-to-end (00_config through 06_sankey) and all outputs are regenerated"
    - "07_diagnostics.R reflects any structural changes from Plan 01 and Plan 02 fixes (new columns, updated logic)"
    - "data_quality_summary.csv exists with columns: issue_type, count_before, count_after, status, notes"
    - "Every issue in data_quality_summary.csv has status = fixed, accepted, or documented (no unexplained anomalies per D-16)"
    - "User confirms final diagnostic re-run shows all issues resolved or explained"
  artifacts:
    - path: "R/07_diagnostics.R"
      provides: "Updated diagnostic script reflecting pipeline changes"
      contains: "HL_SOURCE"
    - path: "R/08_data_quality_summary.R"
      provides: "Script generating data_quality_summary.csv from diagnostic before/after counts"
      contains: "data_quality_summary"
    - path: "output/diagnostics/data_quality_summary.csv"
      provides: "Formal resolution record for all diagnostic findings"
  key_links:
    - from: "R/07_diagnostics.R"
      to: "R/03_cohort_predicates.R"
      via: "References HL_SOURCE column now added by has_hodgkin_diagnosis()"
      pattern: "HL_SOURCE"
    - from: "R/08_data_quality_summary.R"
      to: "output/diagnostics/data_quality_summary.csv"
      via: "write_csv to output directory"
      pattern: "data_quality_summary"
---

<objective>
Update 07_diagnostics.R to reflect pipeline changes, create a data quality summary script, rebuild the full pipeline end-to-end, and verify all issues are resolved or explained.

Purpose: Per D-18, a full end-to-end pipeline rebuild is required after all fixes. Per D-19, 07_diagnostics.R must be updated if fixes changed column names or detection logic. Per D-17, a formal data_quality_summary.csv tracks the resolution status of every diagnostic finding. Per D-16, "clean enough" means all remaining issues have an explanation. This plan is the final validation and documentation step.

Output: Updated R/07_diagnostics.R, new R/08_data_quality_summary.R, regenerated output/ files (cohort CSV, figures, diagnostics), output/diagnostics/data_quality_summary.csv
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-use-debug-output-to-rectify-issues/06-CONTEXT.md
@.planning/phases/06-use-debug-output-to-rectify-issues/06-01-SUMMARY.md
@.planning/phases/06-use-debug-output-to-rectify-issues/06-02-SUMMARY.md

@R/07_diagnostics.R
@R/04_build_cohort.R
@R/03_cohort_predicates.R
@R/01_load_pcornet.R

<interfaces>
<!-- Key contracts from upstream plans -->

From Plan 01 changes to R/03_cohort_predicates.R:
```r
has_hodgkin_diagnosis <- function(patient_df) {
  # NOW returns tibble with HL_SOURCE column added via inner_join
  # HL_SOURCE values: "DIAGNOSIS only", "TR only", "Both"
  # "Neither" patients excluded and written to excluded_no_hl_evidence.csv
}
```

From Plan 01 changes to R/04_build_cohort.R:
```r
hl_cohort <- cohort %>%
  select(
    ID, SOURCE, HL_SOURCE, SEX, RACE, HISPANIC,  # HL_SOURCE is new
    ...
  )
```

From Plan 02 changes to R/01_load_pcornet.R:
```r
# Potential new patterns in load_pcornet_table():
# - _VALID validation columns (AGE_AT_DIAGNOSIS_VALID, DXAGE_VALID, etc.)
# - Updated date_regex
# - Updated TUMOR_REGISTRY*_SPEC col_types
```

From R/07_diagnostics.R Section 4 (current HL identification audit):
```r
# Currently does its own HL source venn analysis
# May need updating if HL_SOURCE is now part of cohort output
# Also needs to account for _VALID columns in Section 6
```

data_quality_summary.csv target schema (D-17):
```
issue_type, count_before, count_after, status, notes
```
status values: "fixed", "accepted", "documented"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 07_diagnostics.R and create 08_data_quality_summary.R</name>
  <files>R/07_diagnostics.R, R/08_data_quality_summary.R</files>
  <read_first>
    - R/07_diagnostics.R (full file -- understand all 6 sections, identify what needs updating after Plan 01 and Plan 02 changes)
    - R/03_cohort_predicates.R (verify HL_SOURCE column is now added by has_hodgkin_diagnosis from Plan 01)
    - R/01_load_pcornet.R (verify _VALID columns and updated date_regex from Plan 02)
    - R/04_build_cohort.R (verify HL_SOURCE in final select from Plan 01)
    - .planning/phases/06-use-debug-output-to-rectify-issues/06-01-SUMMARY.md (what changed in Plan 01)
    - .planning/phases/06-use-debug-output-to-rectify-issues/06-02-SUMMARY.md (what changed in Plan 02)
  </read_first>
  <action>
**Part A: Update 07_diagnostics.R (D-19)**

Review each of the 6 sections and update as needed:

**Section 1 (Date Parsing Failures):** If Plan 02 changed the date_regex, update the local copy at line 50 to match the new regex in 01_load_pcornet.R line 231. The diagnostic script duplicates the regex string (it must match the load script exactly).

**Section 3 (Column Type and Missing Value Audit):** If Plan 02 added _VALID validation columns to load_pcornet_table(), Section 3's column discrepancy check may flag these as "extra columns not in spec." Add a filter to exclude columns ending in `_VALID` from the discrepancy check:
```r
# Exclude _VALID validation columns from discrepancy check
extra_cols <- setdiff(actual_cols, spec_cols)
extra_cols <- extra_cols[!str_detect(extra_cols, "_VALID$")]
```

**Section 4 (HL Identification Source Comparison):** Add a check for the excluded patients file (D-02):
```r
# Check for excluded patients file (D-02)
excl_path <- file.path(CONFIG$output_dir, "cohort", "excluded_no_hl_evidence.csv")
if (file.exists(excl_path)) {
  excl_df <- read_csv(excl_path, show_col_types = FALSE)
  message(glue("  Excluded patients (Neither): {nrow(excl_df)} (written to excluded_no_hl_evidence.csv)"))
} else {
  message("  No excluded patients file found (no 'Neither' patients)")
}
```

**Section 6 (Numeric Range Checks):** If Plan 02 added _VALID columns, add a summary of validation results:
```r
# Summarize _VALID column results
for (tbl_name in names(pcornet)) {
  if (is.null(pcornet[[tbl_name]])) next
  valid_cols <- names(pcornet[[tbl_name]])[str_detect(names(pcornet[[tbl_name]]), "_VALID$")]
  for (vcol in valid_cols) {
    n_invalid <- sum(!pcornet[[tbl_name]][[vcol]], na.rm = TRUE)
    if (n_invalid > 0) {
      message(glue("  {tbl_name}.{vcol}: {n_invalid} invalid values"))
    }
  }
}
```

**Part B: Create R/08_data_quality_summary.R (D-17)**

Create a new script that generates the data quality summary CSV. This script:
1. Sources R/01_load_pcornet.R (loads data)
2. Re-runs key diagnostic checks to get current (post-fix) counts
3. Builds the data_quality_summary tribble with before/after counts
4. Writes to output/diagnostics/data_quality_summary.csv

The script must follow the project convention: numbered script pattern (08_), message() + glue() for console output, readr::write_csv() for CSV output, dir.create() for output directory.

Structure:
```r
# ==============================================================================
# 08_data_quality_summary.R -- Data quality resolution tracker
# ==============================================================================
# Generates output/diagnostics/data_quality_summary.csv (D-17)
# Run AFTER all fixes are applied and full pipeline is rebuilt.
# Sources 01_load_pcornet.R to get current data state.
#
# Columns: issue_type, count_before, count_after, status, notes
# Status values: fixed, accepted, documented
# ==============================================================================

source("R/01_load_pcornet.R")

library(tibble)
library(readr)
library(dplyr)
library(stringr)
library(glue)

message(strrep("=", 60))
message("Data Quality Summary Generation")
message(strrep("=", 60))

# --- Collect current (post-fix) counts ---
# [Count date parsing NAs, character date columns, _VALID=FALSE values, etc.]

# --- Build summary tribble ---
# NOTE: count_before values come from diagnostic CSV files shared by user in Plan 02.
# count_after values are computed from current data state.
data_quality_summary <- tribble(
  ~issue_type, ~count_before, ~count_after, ~status, ~notes
  # [Rows populated from actual diagnostic findings -- see Plan 02 Task 2 output]
)

# --- Write output ---
dir.create(file.path(CONFIG$output_dir, "diagnostics"), showWarnings = FALSE, recursive = TRUE)
write_csv(data_quality_summary, file.path(CONFIG$output_dir, "diagnostics", "data_quality_summary.csv"))

message(glue("\nData quality summary:"))
message(glue("  Total issues: {nrow(data_quality_summary)}"))
message(glue("  Fixed: {sum(data_quality_summary$status == 'fixed')}"))
message(glue("  Accepted: {sum(data_quality_summary$status == 'accepted')}"))
message(glue("  Documented: {sum(data_quality_summary$status == 'documented')}"))
```

Include rows for ALL issue categories from the 6 diagnostic sections:
- Date parsing failures (count before, count after fix)
- Character date columns (count before regex expansion, count after)
- TR column type mismatches (count before, count after spec update)
- Encoding issues (count, status = documented per D-06)
- Negative/extreme ages (count, status = documented with _VALID)
- Future/pre-1900 dates (count, status = documented with _VALID)
- Missing values >50% (count, status = accepted if optional field, or fixed if parsing issue)
- HL identification: Neither (count before, 0 after exclusion, status = fixed)
- HL identification: source breakdown (Both/DX-only/TR-only counts, status = documented)
- Dual-eligible rate comparison (R vs Python, status = documented)
- Unmapped payer codes (count, status = accepted or documented)
  </action>
  <verify>
    <automated>grep -c "HL_SOURCE\|_VALID\|data_quality_summary" R/07_diagnostics.R</automated>
  </verify>
  <acceptance_criteria>
    - R/07_diagnostics.R contains reference to HL_SOURCE (Section 4 update)
    - R/07_diagnostics.R contains reference to _VALID columns (Section 6 update)
    - R/07_diagnostics.R date_regex at line ~50 matches the regex in R/01_load_pcornet.R line 231
    - R/08_data_quality_summary.R exists and contains `tribble(` for data_quality_summary
    - R/08_data_quality_summary.R contains `write_csv(` to output/diagnostics/data_quality_summary.csv
    - R/08_data_quality_summary.R contains columns: issue_type, count_before, count_after, status, notes
    - R/08_data_quality_summary.R sources R/01_load_pcornet.R (needs loaded data for current counts)
  </acceptance_criteria>
  <done>07_diagnostics.R updated to reflect Plan 01/02 changes. 08_data_quality_summary.R created with data quality resolution tracker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full pipeline rebuild and final diagnostic verification on HiPerGator</name>
  <files>output/cohort/hl_cohort.csv, output/figures/waterfall_attrition.png, output/figures/sankey_patient_flow.png, output/diagnostics/data_quality_summary.csv</files>
  <action>
All code fixes from Plans 01-03 are complete. The user needs to rebuild the full pipeline on HiPerGator and verify everything works end-to-end (D-18).

On HiPerGator (interactive RStudio session), perform the full end-to-end pipeline rebuild:

1. Pull latest code changes (all Phase 6 commits)

2. Run the full pipeline in order:
   ```r
   source("R/04_build_cohort.R")     # Loads 00-03 upstream, builds cohort
   source("R/05_visualize_waterfall.R")  # Regenerates waterfall PNG
   source("R/06_visualize_sankey.R")     # Regenerates sankey PNG
   ```

3. Verify rebuilt cohort:
   - hl_cohort.csv has HL_SOURCE column
   - Attrition log shows "Has HL diagnosis (ICD or histology, excludes Neither)"
   - excluded_no_hl_evidence.csv written (if any Neither patients)
   - Waterfall and Sankey PNGs regenerated in output/figures/

4. Run final diagnostics:
   ```r
   source("R/07_diagnostics.R")
   ```
   - All 6 sections run without errors
   - Review console output for any new or unexpected issues

5. Generate data quality summary:
   ```r
   source("R/08_data_quality_summary.R")
   ```
   - output/diagnostics/data_quality_summary.csv is written
   - All issues have status = fixed, accepted, or documented
   - No unexplained anomalies (D-16)

6. Share with Claude:
   - Final console output from all 3 scripts above
   - data_quality_summary.csv contents
   - Confirm: Are all remaining issues explained? (D-16)

7. If any issues remain unexplained:
   - Share the specific diagnostic CSV showing the issue
   - Claude will write an additional fix (iterating per D-12)
  </action>
  <verify>User confirms full pipeline runs end-to-end and data_quality_summary.csv shows all issues resolved or explained</verify>
  <done>User has verified: (a) full pipeline runs 00 through 06 without errors, (b) all outputs regenerated (cohort CSV, waterfall PNG, sankey PNG), (c) data_quality_summary.csv shows all issues as fixed/accepted/documented, (d) no unexplained anomalies remain (D-16 clean enough criterion met)</done>
</task>

</tasks>

<verification>
1. `ls R/08_data_quality_summary.R` confirms script exists
2. `grep "data_quality_summary" R/08_data_quality_summary.R` confirms summary generation
3. `grep "HL_SOURCE" R/07_diagnostics.R` confirms diagnostics updated for new column
4. User confirms full pipeline runs end-to-end without errors on HiPerGator
5. User confirms data_quality_summary.csv has no unexplained anomalies
</verification>

<success_criteria>
- Full pipeline (00 through 06) runs end-to-end without errors on HiPerGator
- All outputs regenerated: hl_cohort.csv (with HL_SOURCE), waterfall PNG, sankey PNG
- 07_diagnostics.R runs cleanly against rebuilt data
- data_quality_summary.csv exists with all issues classified as fixed/accepted/documented
- No unexplained anomalies remain (D-16 clean enough criterion met)
- Phase 6 remediation cycle complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-use-debug-output-to-rectify-issues/06-03-SUMMARY.md`
</output>
