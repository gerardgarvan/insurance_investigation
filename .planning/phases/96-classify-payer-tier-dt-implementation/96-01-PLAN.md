---
phase: 96-classify-payer-tier-dt-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/utils/utils_payer.R
  - R/96_validate_payer_dt.R
autonomous: false
requirements:
  - PAYER-01
  - PAYER-02
user_setup: []

must_haves:
  truths:
    - "User can call classify_payer_tier_dt() on encounter data and receive tibble with payer_category column matching classify_payer_tier() output"
    - "User can run R/96_validate_payer_dt.R and see parity assertion pass between classify_payer_tier() and classify_payer_tier_dt()"
    - "User can inspect classify_payer_tier_dt function header and see copy() usage documented for reference semantics defense"
    - "User can call classify_payer_tier_dt() with factor-column input and see explicit as.character() coercion without NA matches"
    - "Existing classify_payer_tier() callers (R/60, R/61, R/62) are unmodified and continue working"
  artifacts:
    - path: "R/utils/utils_payer.R"
      provides: "classify_payer_tier_dt() function alongside existing classify_payer_tier()"
      contains: "classify_payer_tier_dt"
      min_lines: 250
    - path: "R/96_validate_payer_dt.R"
      provides: "Standalone validation script comparing dplyr and data.table payer classification"
      contains: "classify_payer_tier_dt"
      min_lines: 100
  key_links:
    - from: "R/utils/utils_payer.R"
      to: "R/utils/utils_dt.R"
      via: "ensure_dt(), to_tibble_safe(), get_lookup_dt() calls"
      pattern: "ensure_dt\\|to_tibble_safe\\|get_lookup_dt"
    - from: "R/utils/utils_payer.R"
      to: "R/00_config.R"
      via: "PAYER_MAPPING and LOOKUP_TABLES_DT references"
      pattern: "PAYER_MAPPING\\|LOOKUP_TABLES_DT"
    - from: "R/96_validate_payer_dt.R"
      to: "R/utils/utils_payer.R"
      via: "calls both classify_payer_tier() and classify_payer_tier_dt()"
      pattern: "classify_payer_tier_dt\\|classify_payer_tier"
---

<objective>
Implement classify_payer_tier_dt() -- a data.table variant of the most-called payer utility function -- and validate it produces identical output to the existing dplyr version.

Purpose: This is the core function needed for Phase 97-98 hot-path migrations. The data.table variant uses keyed joins and fcase() internally for significant performance gains on large datasets while maintaining API and output compatibility with the existing dplyr pipeline.

Output:
- classify_payer_tier_dt() function added to R/utils/utils_payer.R (per D-01)
- R/96_validate_payer_dt.R standalone validation script (per D-03)
- Human-verified parity on user's environment
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-classify-payer-tier-dt-implementation/96-CONTEXT.md
@.planning/phases/96-classify-payer-tier-dt-implementation/96-RESEARCH.md
@.planning/phases/95-infrastructure-setup/95-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From R/utils/utils_payer.R (existing dplyr version to replicate):
```r
# Lines 93-177: classify_payer_tier(df, include_dual = TRUE, flm_override = FALSE)
# Returns: data frame with added columns: effective_payer, payer_category, tier, tier_rank, (optionally dual_eligible)
# Logic chain:
#   1. as.character() coercion on PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY, SOURCE
#   2. effective_payer resolution: primary if valid, else secondary, else NA
#      (valid = not NA, not empty/whitespace, not in PAYER_MAPPING$sentinel_values)
#   3. payer_category: AMC_PAYER_LOOKUP[effective_payer], fallback to prefix (startsWith), fallback to "Missing"
#   4. tier = CODE_TO_TIER(payer_category) -- 1:1 mapping, same strings
#   5. tier override: coalesce(case_when(PRIMARY/SECONDARY %in% c("93","14") ~ "Medicaid"), tier)
#   6. if flm_override: tier = "Medicaid" when SOURCE == "FLM"
#   7. tier safety net: NA tier -> "Missing"
#   8. tier_rank = unlist(TIER_MAPPING[tier]), NA -> 8L
#   9. if include_dual: dual_eligible flag (0L/1L based on dual codes + cross-payer detection)

# Lines 45-58: CODE_TO_TIER(payer_category) -- 1:1 mapping via case_when
# Lines 32-36: is_missing_payer(payer_value) -- NA/empty/sentinel detection
```

From R/utils/utils_dt.R (Phase 95 conversion helpers):
```r
ensure_dt(df, name = "input", script_name = "unknown")  # -> data.table
to_tibble_safe(dt, name = "output", script_name = "unknown")  # -> tibble
get_lookup_dt(table_name, lookup_list = LOOKUP_TABLES_DT)  # -> keyed data.table
```

From R/00_config.R (lookup tables and constants):
```r
# PAYER_MAPPING list:
PAYER_MAPPING$sentinel_values  # c("NI", "UN", "OT")
PAYER_MAPPING$dual_eligible_codes  # c("14", "141", "142")
PAYER_MAPPING$prefix_fallback  # named list: "1"="Medicare", "2"="Medicaid", etc.

# LOOKUP_TABLES_DT keyed data.tables:
# AMC_PAYER_LOOKUP: columns (code, payer_category), keyed on code, 219 rows
# TIER_MAPPING: columns (payer_category, tier), keyed on payer_category, 8 rows
#   where tier is INTEGER (Medicaid=1, Medicare=2, Private=3, Other govt=4, Other=5, Self-pay=6, Uninsured=7, Missing=8)

# Original named vectors (still exist for backward compat):
# AMC_PAYER_LOOKUP: named character vector, code -> payer_category
# TIER_MAPPING: named list, payer_category -> integer rank
```

From R/95_validate_dt_infrastructure.R (validation pattern to follow):
```r
# Pattern: source("R/00_config.R"), check() helper, numbered sections, pass/fail summary
check <- function(description, condition) {
  if (isTRUE(condition)) { message(sprintf("[PASS] %s", description)); pass_count <<- pass_count + 1L }
  else { message(sprintf("[FAIL] %s", description)); fail_count <<- fail_count + 1L }
}
```

From callers (reference only, NOT modified in Phase 96 per D-02):
```r
# R/60 line 90: classify_payer_tier(include_dual=TRUE, flm_override=FALSE)
# R/61 line 79: classify_payer_tier(include_dual=TRUE, flm_override=TRUE)
# R/62 line 122: classify_payer_tier(include_dual=FALSE, flm_override=TRUE)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement classify_payer_tier_dt() in R/utils/utils_payer.R</name>
  <files>R/utils/utils_payer.R</files>

  <read_first>
    R/utils/utils_payer.R
    R/utils/utils_dt.R
    R/00_config.R
    .planning/phases/96-classify-payer-tier-dt-implementation/96-RESEARCH.md
  </read_first>

  <action>
  Append classify_payer_tier_dt() function to the end of R/utils/utils_payer.R (after line 177, after the existing classify_payer_tier function). Per D-01, both versions live in the same file for side-by-side comparison.

  The function signature MUST match the existing dplyr version exactly:
  ```r
  classify_payer_tier_dt <- function(df, include_dual = TRUE, flm_override = FALSE)
  ```

  Implementation steps (each must be a clearly commented section in the function body):

  **Step 1 -- Defensive copy (reference semantics defense):**
  ```r
  dt <- copy(ensure_dt(df, name = "input", script_name = "classify_payer_tier_dt"))
  ```
  copy() wraps ensure_dt() to prevent reference mutation of caller's input.

  **Step 2 -- Explicit character coercion (factor input defense):**
  ```r
  dt[, `:=`(
    PAYER_TYPE_PRIMARY = as.character(PAYER_TYPE_PRIMARY),
    PAYER_TYPE_SECONDARY = as.character(PAYER_TYPE_SECONDARY),
    SOURCE = as.character(SOURCE)
  )]
  ```

  **Step 3 -- Effective payer resolution via fcase():**
  ```r
  sentinels <- PAYER_MAPPING$sentinel_values
  dt[, effective_payer := fcase(
    !is.na(PAYER_TYPE_PRIMARY) & nchar(trimws(PAYER_TYPE_PRIMARY)) > 0 &
      !PAYER_TYPE_PRIMARY %in% sentinels,
    PAYER_TYPE_PRIMARY,
    !is.na(PAYER_TYPE_SECONDARY) & nchar(trimws(PAYER_TYPE_SECONDARY)) > 0 &
      !PAYER_TYPE_SECONDARY %in% sentinels,
    PAYER_TYPE_SECONDARY,
    default = NA_character_
  )]
  ```

  **Step 4 -- AMC 8-category mapping via keyed join:**
  ```r
  amc_lookup <- get_lookup_dt("AMC_PAYER_LOOKUP")
  dt[amc_lookup, on = .(effective_payer = code), payer_category := i.payer_category]
  ```
  IMPORTANT: This is an update-join syntax (X[Y, on=, col := i.col]). It assigns payer_category from the right table to matching rows in dt. Rows where effective_payer is not found in amc_lookup$code get NA for payer_category (left join semantics). This is NOT the same as dt[amc_lookup, on=, nomatch=NA] which would be a right join returning a new table.

  **Step 5 -- Prefix fallback for unmapped codes:**
  ```r
  dt[is.na(payer_category) & !is.na(effective_payer), payer_category := fcase(
    startsWith(effective_payer, "1"), "Medicare",
    startsWith(effective_payer, "2"), "Medicaid",
    startsWith(effective_payer, "5") | startsWith(effective_payer, "6"), "Private",
    startsWith(effective_payer, "3") | startsWith(effective_payer, "4"), "Other govt",
    startsWith(effective_payer, "7"), "Private",
    startsWith(effective_payer, "8"), "Uninsured",
    startsWith(effective_payer, "9"), "Other",
    default = "Other"
  )]
  ```
  This must match the exact prefix-to-category mapping in the existing dplyr version (lines 113-124 of utils_payer.R).

  **Step 6 -- Missing fallback:**
  ```r
  dt[is.na(effective_payer), payer_category := "Missing"]
  ```

  **Step 7 -- Tier assignment via fcase (equivalent to CODE_TO_TIER):**
  ```r
  dt[, tier := fcase(
    payer_category == "Medicaid", "Medicaid",
    payer_category == "Medicare", "Medicare",
    payer_category == "Private", "Private",
    payer_category == "Other govt", "Other govt",
    payer_category == "Other", "Other",
    payer_category == "Self-pay", "Self-pay",
    payer_category == "Uninsured", "Uninsured",
    payer_category == "Missing", "Missing",
    default = "Missing"
  )]
  ```

  **Step 8 -- Special code override (93/14 -> Medicaid):**
  ```r
  dt[, tier := fcase(
    PAYER_TYPE_PRIMARY %in% c("93", "14"), "Medicaid",
    PAYER_TYPE_SECONDARY %in% c("93", "14"), "Medicaid",
    default = tier
  )]
  ```
  This replicates the coalesce(case_when(93/14 check), tier) pattern from the dplyr version.

  **Step 9 -- Conditional FLM source override (if flm_override == TRUE):**
  ```r
  if (flm_override) {
    dt[SOURCE == "FLM" & !is.na(SOURCE), tier := "Medicaid"]
  }
  ```

  **Step 10 -- Tier safety net (NA -> "Missing"):**
  ```r
  dt[is.na(tier), tier := "Missing"]
  ```

  **Step 11 -- Tier rank assignment via keyed join on TIER_MAPPING:**
  ```r
  tier_lookup <- get_lookup_dt("TIER_MAPPING")
  dt[tier_lookup, on = .(tier = payer_category), tier_rank := i.tier]
  dt[is.na(tier_rank), tier_rank := 8L]
  ```
  IMPORTANT: TIER_MAPPING data.table has columns (payer_category, tier) where payer_category is the character key ("Medicaid", "Medicare", etc.) and tier is the integer rank (1L, 2L, etc.). The join matches dt$tier (character like "Medicaid") to tier_lookup$payer_category (character like "Medicaid"), and assigns tier_lookup$tier (integer rank) to dt$tier_rank.

  **Step 12 -- Conditional dual_eligible flag (if include_dual == TRUE):**
  ```r
  if (include_dual) {
    dual_codes <- PAYER_MAPPING$dual_eligible_codes
    dt[, dual_eligible := {
      sec_missing <- is.na(PAYER_TYPE_SECONDARY) |
                     nchar(trimws(PAYER_TYPE_SECONDARY)) == 0
      has_dual <- PAYER_TYPE_PRIMARY %in% dual_codes |
                  PAYER_TYPE_SECONDARY %in% dual_codes
      cross_payer <- (startsWith(PAYER_TYPE_PRIMARY, "1") &
                      startsWith(PAYER_TYPE_SECONDARY, "2")) |
                     (startsWith(PAYER_TYPE_PRIMARY, "2") &
                      startsWith(PAYER_TYPE_SECONDARY, "1"))
      fcase(
        sec_missing, 0L,
        has_dual, 1L,
        cross_payer, 1L,
        default = 0L
      )
    }]
  }
  ```
  IMPORTANT: dual_eligible uses startsWith() on PAYER_TYPE_SECONDARY which may be NA. The sec_missing branch must come first in fcase() to short-circuit NA inputs before startsWith() is evaluated.

  **Step 13 -- Return tibble (per D-04 discretion: return tibble for dplyr pipeline compatibility):**
  ```r
  to_tibble_safe(dt, name = "result", script_name = "classify_payer_tier_dt")
  ```

  **Roxygen header:** Include roxygen-style documentation block before the function matching the style of classify_payer_tier() (lines 74-92 of utils_payer.R). Document:
  - Purpose: data.table variant of classify_payer_tier() using keyed joins and fcase()
  - All three parameters with same descriptions as dplyr version
  - Return type: tibble (same as dplyr version)
  - Note about reference semantics defense (copy() at entry)
  - Note about Phase 96 v3.0 origin
  - Note: "Callers switch in Phase 97/98; this phase only creates and validates."

  **DO NOT modify any existing code in utils_payer.R.** Only append the new function after the existing classify_payer_tier() function.
  </action>

  <verify>
    <automated>
    In R console: source("R/00_config.R") succeeds (utils_payer.R auto-sourced), and exists("classify_payer_tier_dt") returns TRUE, and is.function(classify_payer_tier_dt) returns TRUE. Both classify_payer_tier and classify_payer_tier_dt exist simultaneously.
    </automated>
  </verify>

  <acceptance_criteria>
  - R/utils/utils_payer.R contains `classify_payer_tier_dt <- function(df, include_dual = TRUE, flm_override = FALSE)`
  - R/utils/utils_payer.R contains `copy(ensure_dt(` (reference semantics defense)
  - R/utils/utils_payer.R contains `as.character(PAYER_TYPE_PRIMARY)` (factor coercion)
  - R/utils/utils_payer.R contains `get_lookup_dt("AMC_PAYER_LOOKUP")` (keyed join for payer category)
  - R/utils/utils_payer.R contains `get_lookup_dt("TIER_MAPPING")` (keyed join for tier rank)
  - R/utils/utils_payer.R contains `fcase(` (at least 4 occurrences: effective_payer, prefix fallback, tier assignment, special code override)
  - R/utils/utils_payer.R contains `to_tibble_safe(` (tibble return)
  - R/utils/utils_payer.R contains `PAYER_MAPPING$sentinel_values` (sentinel check in effective_payer resolution)
  - R/utils/utils_payer.R contains `PAYER_MAPPING$dual_eligible_codes` (dual_eligible computation)
  - R/utils/utils_payer.R contains `startsWith(effective_payer, "1")` (prefix fallback)
  - Original classify_payer_tier function (lines 93-177) is unchanged (no modifications)
  - File contains both `classify_payer_tier <-` and `classify_payer_tier_dt <-` function definitions
  </acceptance_criteria>

  <done>
  classify_payer_tier_dt() function exists in R/utils/utils_payer.R alongside the original dplyr version. Function uses copy() + ensure_dt() at entry, fcase() for all multi-branch conditionals, keyed joins via get_lookup_dt() for AMC_PAYER_LOOKUP and TIER_MAPPING, returns tibble via to_tibble_safe(). API signature matches existing function exactly (df, include_dual, flm_override).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create R/96_validate_payer_dt.R parity validation script</name>
  <files>R/96_validate_payer_dt.R</files>

  <read_first>
    R/utils/utils_payer.R
    R/95_validate_dt_infrastructure.R
    R/00_config.R
    .planning/phases/96-classify-payer-tier-dt-implementation/96-CONTEXT.md
  </read_first>

  <action>
  Create R/96_validate_payer_dt.R following the Phase 95 validation pattern (R/95_validate_dt_infrastructure.R). Per D-03, this is a standalone validation script that runs both classify_payer_tier() and classify_payer_tier_dt() on test data, compares all output columns row-by-row, and logs pass/fail.

  **Script structure:**

  **Header block** (lines 1-25): Standard documentation block matching R/95 pattern:
  ```r
  # ==============================================================================
  # 96_validate_payer_dt.R -- Phase 96 payer classification parity validation
  # ==============================================================================
  #
  # Purpose:
  #   Validates that classify_payer_tier_dt() produces identical output to
  #   classify_payer_tier() across all parameter combinations and edge cases.
  #   Runs both functions on test data and compares output columns row-by-row.
  #
  # Usage:
  #   source("R/96_validate_payer_dt.R")
  #
  # Expected output:
  #   Series of [PASS] / [FAIL] messages. All must show [PASS].
  #
  # Requirements:
  #   - PAYER-01: classify_payer_tier_dt() function exists
  #   - PAYER-02: Output parity with classify_payer_tier()
  #
  # ==============================================================================
  ```

  **Setup** (source config, define check() helper):
  ```r
  source("R/00_config.R")

  pass_count <- 0L
  fail_count <- 0L

  check <- function(description, condition) {
    if (isTRUE(condition)) {
      message(sprintf("[PASS] %s", description))
      pass_count <<- pass_count + 1L
    } else {
      message(sprintf("[FAIL] %s", description))
      fail_count <<- fail_count + 1L
    }
  }
  ```

  **Section 1: Function existence checks (PAYER-01)**
  - classify_payer_tier_dt exists and is a function
  - classify_payer_tier_dt has 3 parameters (df, include_dual, flm_override)
  - classify_payer_tier_dt uses default values matching dplyr version (include_dual=TRUE, flm_override=FALSE)

  **Section 2: Fixture data construction**
  Build a comprehensive test tibble covering all edge cases. This fixture must exercise every branch in the function. Create it as:
  ```r
  fixture <- tibble::tibble(
    PAYER_TYPE_PRIMARY = c(
      "219",    # Direct AMC lookup: Medicaid code
      "119",    # Direct AMC lookup: Medicare code
      "523",    # Direct AMC lookup: Private code
      "93",     # Special code override -> Medicaid
      "14",     # Special code override -> Medicaid (dual_eligible_codes member)
      "NI",     # Sentinel value -> fall to secondary
      "UN",     # Sentinel value -> fall to secondary
      "",       # Empty string -> fall to secondary
      NA_character_,  # NA primary -> fall to secondary
      "1XX",    # Prefix fallback: starts with "1" -> Medicare
      "2YY",    # Prefix fallback: starts with "2" -> Medicaid
      "5ZZ",    # Prefix fallback: starts with "5" -> Private
      "3AA",    # Prefix fallback: starts with "3" -> Other govt
      "8BB",    # Prefix fallback: starts with "8" -> Uninsured
      "9CC",    # Prefix fallback: starts with "9" -> Other
      NA_character_,  # Both NA -> Missing
      "141",    # Dual-eligible code (in PAYER_MAPPING$dual_eligible_codes)
      "119",    # Cross-payer: Medicare primary + Medicaid secondary
      "219"     # Cross-payer: Medicaid primary + Medicare secondary
    ),
    PAYER_TYPE_SECONDARY = c(
      NA_character_,  # No secondary
      "219",    # Secondary present but primary valid (ignored)
      NA_character_,  # No secondary
      NA_character_,  # No secondary (93 override)
      NA_character_,  # No secondary (14 override)
      "219",    # Sentinel primary -> use secondary: Medicaid
      "523",    # Sentinel primary -> use secondary: Private
      "119",    # Empty primary -> use secondary: Medicare
      "219",    # NA primary -> use secondary: Medicaid
      NA_character_,  # No secondary (prefix fallback)
      NA_character_,  # No secondary (prefix fallback)
      NA_character_,  # No secondary (prefix fallback)
      NA_character_,  # No secondary (prefix fallback)
      NA_character_,  # No secondary (prefix fallback)
      NA_character_,  # No secondary (prefix fallback)
      NA_character_,  # Both missing -> Missing
      "219",    # Secondary for dual-eligible test
      "219",    # Secondary: Medicaid (cross-payer with Medicare primary)
      "119"     # Secondary: Medicare (cross-payer with Medicaid primary)
    ),
    SOURCE = c(
      "ENC", "ENC", "ENC", "ENC", "ENC",
      "ENC", "ENC", "ENC", "ENC", "ENC",
      "ENC", "ENC", "ENC", "ENC", "ENC",
      "ENC", "ENC", "ENC", "FLM"   # Last row: FLM source for override test
    )
  )
  ```
  NOTE: The exact codes (219, 119, 523, etc.) MUST come from the AMC_PAYER_LOOKUP named vector in R/00_config.R. Before writing the fixture, read R/00_config.R to confirm these codes map to the expected categories. If 219 does not map to "Medicaid" in AMC_PAYER_LOOKUP, adjust the fixture accordingly. The existing Phase 95 validation already confirmed AMC_PAYER_LOOKUP["219"] == "Medicaid".

  **Section 3: Parity test -- default parameters (include_dual=TRUE, flm_override=FALSE)**
  ```r
  result_dplyr <- classify_payer_tier(fixture, include_dual = TRUE, flm_override = FALSE)
  result_dt <- classify_payer_tier_dt(fixture, include_dual = TRUE, flm_override = FALSE)
  ```
  Compare:
  - check() that nrow matches
  - check() that ncol matches
  - check() that column names are identical (using all(names(result_dplyr) == names(result_dt)))
  - For each output column (effective_payer, payer_category, tier, tier_rank, dual_eligible):
    - Row-by-row comparison handling NAs: `all(result_dplyr$col == result_dt$col, na.rm = FALSE)` OR use `identical(result_dplyr$col, result_dt$col)`
    - If identical() fails, log which rows differ for debugging

  **Section 4: Parity test -- include_dual=TRUE, flm_override=TRUE**
  Run both functions with flm_override=TRUE, compare all columns row-by-row. The FLM row (row 19) should have tier="Medicaid" due to FLM override.

  **Section 5: Parity test -- include_dual=FALSE, flm_override=TRUE**
  Run both functions with include_dual=FALSE, flm_override=TRUE. Confirm:
  - dual_eligible column does NOT exist in output
  - All other columns match row-by-row

  **Section 6: Reference semantics safety test**
  ```r
  fixture_copy <- tibble::tibble(
    PAYER_TYPE_PRIMARY = c("219", "119"),
    PAYER_TYPE_SECONDARY = c(NA_character_, NA_character_),
    SOURCE = c("ENC", "ENC")
  )
  original_ncol <- ncol(fixture_copy)
  result <- classify_payer_tier_dt(fixture_copy)
  check("Input not mutated (ncol unchanged)", ncol(fixture_copy) == original_ncol)
  check("Input not mutated (no new columns)", !("effective_payer" %in% names(fixture_copy)))
  ```

  **Section 7: Factor input defense test**
  ```r
  fixture_factor <- fixture
  fixture_factor$PAYER_TYPE_PRIMARY <- as.factor(fixture_factor$PAYER_TYPE_PRIMARY)
  fixture_factor$PAYER_TYPE_SECONDARY <- as.factor(fixture_factor$PAYER_TYPE_SECONDARY)
  result_factor <- classify_payer_tier_dt(fixture_factor, include_dual = TRUE, flm_override = FALSE)
  result_char <- classify_payer_tier_dt(fixture, include_dual = TRUE, flm_override = FALSE)
  check("Factor input produces same payer_category as character input",
        identical(result_factor$payer_category, result_char$payer_category))
  check("Factor input produces same tier as character input",
        identical(result_factor$tier, result_char$tier))
  ```

  **Section 8: Return type verification**
  ```r
  result <- classify_payer_tier_dt(fixture)
  check("Return type is tibble", tibble::is_tibble(result))
  check("Return type is NOT data.table", !data.table::is.data.table(result))
  ```

  **Section 9: Summary**
  ```r
  message("")
  message(sprintf("========================================"))
  message(sprintf("Phase 96 Validation: %d PASS, %d FAIL", pass_count, fail_count))
  if (fail_count == 0) {
    message("All checks passed -- classify_payer_tier_dt() validated for Phase 97")
  } else {
    message(sprintf("WARNING: %d check(s) failed -- review output above", fail_count))
  }
  message(sprintf("========================================"))
  ```

  **IMPORTANT:** Use the SAME check() pattern as R/95_validate_dt_infrastructure.R (pass_count/fail_count tracking, [PASS]/[FAIL] prefixed messages, summary at end).
  </action>

  <verify>
    <automated>
    File R/96_validate_payer_dt.R exists, contains "classify_payer_tier_dt", contains "classify_payer_tier(fixture", contains "check(" pattern, and contains "Phase 96 Validation" summary line.
    </automated>
  </verify>

  <acceptance_criteria>
  - R/96_validate_payer_dt.R exists with header documentation block referencing PAYER-01 and PAYER-02
  - File contains `source("R/00_config.R")` on first executable line
  - File contains `check <- function(description, condition)` helper pattern
  - File contains fixture tibble with at least 15 rows covering: direct lookup codes, sentinel values, empty/NA primary, prefix fallback codes, special codes 93/14, dual-eligible codes, cross-payer codes, FLM source
  - File contains parity tests for all 3 parameter combinations: (TRUE,FALSE), (TRUE,TRUE), (FALSE,TRUE)
  - File contains row-by-row column comparison for: effective_payer, payer_category, tier, tier_rank
  - File contains reference semantics safety test (ncol unchanged after function call)
  - File contains factor input defense test (factor columns produce same output as character)
  - File contains return type verification (tibble check)
  - File contains summary section with pass_count/fail_count report
  </acceptance_criteria>

  <done>
  R/96_validate_payer_dt.R is a standalone validation script that exercises classify_payer_tier_dt() across all parameter combinations and edge cases, compares every output column row-by-row against classify_payer_tier(), tests reference semantics safety, factor input defense, and return type. Script follows the Phase 95 check() pattern with pass/fail summary.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify parity validation on user environment</name>
  <files>R/96_validate_payer_dt.R</files>

  <read_first>
    R/96_validate_payer_dt.R
  </read_first>

  <action>
  Present the human checkpoint for environment verification. The user must run the validation script in their R environment (RStudio / HiPerGator) to confirm classify_payer_tier_dt() produces identical output to classify_payer_tier(). This follows the Phase 95 established pattern where automation builds the artifact and the user verifies in their environment.

  Steps for the user:
  1. Open RStudio on their environment
  2. Run: source("R/96_validate_payer_dt.R")
  3. Confirm ALL checks show [PASS] (expect 20+ checks)
  4. Confirm summary line shows "0 FAIL"
  5. (Optional) Quick sanity check in console with production encounter data

  Wait for user to type "approved" or describe any failures.
  </action>

  <verify>
  User confirms all validation checks pass with 0 FAIL in their environment.
  </verify>

  <acceptance_criteria>
  - User has run source("R/96_validate_payer_dt.R") in their environment
  - User confirms all checks show [PASS]
  - User confirms summary shows 0 FAIL
  - User types "approved" to proceed
  </acceptance_criteria>

  <done>
  User has verified parity between classify_payer_tier() and classify_payer_tier_dt() in their R environment. All validation checks passed. Phase 96 requirements PAYER-01 and PAYER-02 are satisfied.
  </done>
</task>

</tasks>

<verification>
Phase 96 verification (run after all tasks complete):
1. Both classify_payer_tier() and classify_payer_tier_dt() exist in R/utils/utils_payer.R
2. R/96_validate_payer_dt.R runs with all [PASS] results
3. No existing callers (R/60, R/61, R/62) are modified
4. classify_payer_tier_dt() returns tibble (not data.table)
5. classify_payer_tier_dt() does not mutate input (reference safety)
6. Factor inputs produce same output as character inputs
</verification>

<success_criteria>
1. classify_payer_tier_dt() produces byte-identical output columns (effective_payer, payer_category, tier, tier_rank, dual_eligible) to classify_payer_tier() for all 3 parameter combinations
2. R/96_validate_payer_dt.R passes all checks (0 FAIL)
3. No existing code modified (only additions to utils_payer.R, new validation script)
4. Function uses data.table internals (fcase, keyed joins, := syntax) -- not dplyr
5. Function uses copy() at entry for reference semantics defense
</success_criteria>

<output>
After completion, create `.planning/phases/96-classify-payer-tier-dt-implementation/96-01-SUMMARY.md`
</output>
