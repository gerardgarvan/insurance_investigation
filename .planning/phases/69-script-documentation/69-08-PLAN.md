---
phase: 69-script-documentation
plan: 08
type: execute
wave: 2
depends_on:
  - 69-01
  - 69-02
  - 69-03
  - 69-04
  - 69-05
  - 69-06
  - 69-07
files_modified:
  - R/SCRIPT_INDEX.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "All 67 numbered scripts have the 5-field header block (Purpose, Inputs, Outputs, Dependencies, Requirements)"
    - "All 67 numbered scripts have at least 2 section headers ending with 4+ dashes"
    - "All 8 utility scripts have the 5-field header block"
    - "No scripts are missing required header fields"
    - "Validation report documents coverage counts and any gaps"
  artifacts:
    - path: "R/SCRIPT_INDEX.md"
      provides: "Updated with documentation status note"
      contains: "Documentation standardized"
  key_links:
    - from: "validation scan"
      to: "all 75 scripts"
      via: "grep-based automated verification"
      pattern: "# Purpose:"
---

<objective>
Validate documentation coverage across all 75 scripts (67 numbered + 8 utils) and fix any gaps.

Purpose: After 7 parallel documentation plans, this validation plan confirms 100% coverage of DOC-01 (headers), DOC-02 (section headers), and DOC-03 (WHY comments). Any scripts that were missed or incompletely documented get fixed here. Also updates SCRIPT_INDEX.md with a documentation status note.

Output: Verified 100% documentation coverage across all scripts. SCRIPT_INDEX.md updated.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate DOC-01/DOC-02 coverage across all 75 scripts and fix any gaps</name>
  <files>R/SCRIPT_INDEX.md</files>
  <read_first>R/SCRIPT_INDEX.md</read_first>
  <action>
Run a systematic validation scan across all 75 scripts (67 numbered + 8 utils) to verify DOC-01 and DOC-02 compliance.

**DOC-01 Validation (Header blocks):**

For each of the 67 numbered R scripts in R/*.R, check that the file contains all 5 required header fields within the first 60 lines:
1. `# Purpose:` -- present?
2. `# Inputs:` -- present?
3. `# Outputs:` -- present?
4. `# Dependencies:` -- present?
5. `# Requirements:` -- present? (or "N/A")

For each of the 8 R/utils/*.R files, check the same 5 fields.

Use grep to scan:
```bash
# Check each field across all R scripts
grep -rL "# Purpose:" R/*.R R/utils/*.R    # Lists files MISSING Purpose
grep -rL "# Inputs:" R/*.R R/utils/*.R      # Lists files MISSING Inputs
grep -rL "# Outputs:" R/*.R R/utils/*.R     # Lists files MISSING Outputs
grep -rL "# Dependencies:" R/*.R R/utils/*.R # Lists files MISSING Dependencies
```

**DOC-02 Validation (Section headers):**

For each of the 67 numbered R scripts (NOT utils -- per D-11 they don't get sections), check that the file contains at least 2 lines matching `SECTION.*----`:
```bash
# Count section headers per file
grep -c "SECTION.*----" R/*.R
```

Any file with 0 or 1 matching lines needs section headers added.

**Fix any gaps found:**

For any script missing a header field:
- Read the script
- Add the missing field in the appropriate location within the existing header block
- Use information from SCRIPT_INDEX.md to populate Purpose, Inputs, Outputs

For any script missing section headers:
- Read the script
- Add `# SECTION 1: SETUP ----` at the top (after header block)
- Add at least one more domain-appropriate section header
- Add `# SECTION N: OUTPUT ----` near bottom if script produces output

**Update SCRIPT_INDEX.md:**

Add a documentation status line at the top of the file, below the existing introduction:
```markdown
**Documentation:** All 67 numbered scripts and 8 utility scripts have standardized header blocks
(Purpose, Inputs, Outputs, Dependencies, Requirements) and RStudio-compatible section headers
(Phase 69, DOC-01/DOC-02/DOC-03).
```

Record the validation results as a brief summary: total scripts checked, fields verified, any gaps fixed.
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && echo "=== Files missing Purpose ===" && grep -rL "# Purpose:" R/*.R R/utils/*.R 2>/dev/null && echo "=== Files missing Inputs ===" && grep -rL "# Inputs:" R/*.R R/utils/*.R 2>/dev/null && echo "=== Section header counts ===" && grep -c "SECTION.*----" R/*.R 2>/dev/null | grep ":0$" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -rL "# Purpose:" R/*.R R/utils/*.R returns no results (all files have Purpose)
    - grep -rL "# Inputs:" R/*.R R/utils/*.R returns no results (all files have Inputs)
    - grep -rL "# Outputs:" R/*.R R/utils/*.R returns no results (all files have Outputs)
    - grep -rL "# Dependencies:" R/*.R R/utils/*.R returns no results (all files have Dependencies)
    - No numbered R script (R/*.R) has 0 matches for "SECTION.*----" (grep -c returns at least 2 per file)
    - R/SCRIPT_INDEX.md contains "Documentation" and "Phase 69" and "DOC-01"
  </acceptance_criteria>
  <done>100% of scripts (67 numbered + 8 utils) have all 5 required header fields. All 67 numbered scripts have 2+ section headers with 4+ trailing dashes. SCRIPT_INDEX.md updated with documentation status. Zero gaps remaining.</done>
</task>

</tasks>

<verification>
- Automated grep scan confirms 0 files missing any of the 5 header fields
- Automated grep scan confirms 0 numbered scripts with fewer than 2 section headers
- SCRIPT_INDEX.md updated with Phase 69 documentation status
- No functional code changes in any script
</verification>

<success_criteria>
- 75/75 scripts pass DOC-01 (5-field header block present)
- 67/67 numbered scripts pass DOC-02 (2+ section headers with 4+ dashes)
- SCRIPT_INDEX.md documents the standardization
- Any gaps found during validation are fixed before plan completion
</success_criteria>

<output>
After completion, create `.planning/phases/69-script-documentation/69-08-SUMMARY.md`
</output>
