---
phase: 69-script-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/01_load_pcornet.R
  - R/02_harmonize_payer.R
  - R/03_duckdb_ingest.R
  - R/utils/utils_attrition.R
  - R/utils/utils_dates.R
  - R/utils/utils_duckdb.R
  - R/utils/utils_icd.R
  - R/utils/utils_payer.R
  - R/utils/utils_pptx.R
  - R/utils/utils_snapshot.R
  - R/utils/utils_treatment.R
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "Every foundation script (00-03) has a 5-field header block with Purpose, Inputs, Outputs, Dependencies, Requirements"
    - "Every foundation script has numbered section headers ending with 4+ dashes"
    - "Every utils script has a standardized header block matching the 5-field template"
    - "Non-obvious logic in foundation scripts has WHY comments explaining clinical/business rationale"
  artifacts:
    - path: "R/00_config.R"
      provides: "Standardized header + section headers for project config"
      contains: "# Purpose:"
    - path: "R/01_load_pcornet.R"
      provides: "Standardized header + section headers for data loading"
      contains: "# Inputs:"
    - path: "R/02_harmonize_payer.R"
      provides: "Standardized header + section headers for payer harmonization"
      contains: "# Outputs:"
    - path: "R/03_duckdb_ingest.R"
      provides: "Standardized header + section headers for DuckDB ingest"
      contains: "# Dependencies:"
  key_links:
    - from: "R/00_config.R"
      to: "R/utils/*.R"
      via: "auto-source documented in Dependencies field"
      pattern: "# Dependencies:"
---

<objective>
Document foundation scripts (00-03) and standardize utility script headers (R/utils/ 8 files).

Purpose: Foundation scripts are the most-sourced files in the entire pipeline (00_config alone is sourced by 50+ scripts). Clear headers and section navigation make onboarding fast. Utils already have good roxygen2 function docs (per D-11) so they only need header standardization.

Output: 4 fully documented foundation scripts + 8 header-standardized utility scripts.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document foundation scripts (00-03) with full 5-field headers, section headers, and WHY comments</name>
  <files>R/00_config.R, R/01_load_pcornet.R, R/02_harmonize_payer.R, R/03_duckdb_ingest.R</files>
  <read_first>R/00_config.R, R/01_load_pcornet.R, R/02_harmonize_payer.R, R/03_duckdb_ingest.R, R/SCRIPT_INDEX.md</read_first>
  <action>
For each of the 4 foundation scripts, apply the documentation standard per decisions D-01 through D-09:

**Header block standardization (D-01, D-02, D-03):**

Replace or standardize existing header to this exact 5-field format with box-style equals borders:

```r
# ==============================================================================
# {NN}_{name}.R -- {One-line purpose from SCRIPT_INDEX.md}
# ==============================================================================
#
# Purpose:
#   {2-3 sentences explaining what the script does and its role in the pipeline}
#
# Inputs:
#   - {path}: {description}
#   (or "None (configuration only)" for 00_config.R)
#
# Outputs:
#   - {path}: {description}
#   (or "None (defines configuration objects in memory)" for 00_config.R)
#
# Dependencies:
#   - source("R/{dep}.R"): {why}
#   - {package}: {what features used}
#   (or "None (root configuration)" for 00_config.R)
#
# Requirements: {REQ-IDs if applicable, or "N/A"}
#
# ==============================================================================
```

Preserve any existing content that adds value (e.g., decision traceability D-xx references, detailed field listings in 00_config.R). Add missing fields but don't delete useful existing commentary.

**Section headers (D-04, D-05, D-06):**

Convert ALL existing section-like comments to the standard format:
- `# --- SECTION N: TITLE ---` becomes `# SECTION N: TITLE ----`
- `# === TITLE ===` becomes `# SECTION N: TITLE ----`
- `# -------------- N. TITLE` becomes `# SECTION N: TITLE ----`
- Wrap major sections with box-style equals borders for visual consistency:
```r
# ==============================================================================
# SECTION N: TITLE ----
# ==============================================================================
```

Every script MUST have at minimum:
- `# SECTION 1: SETUP ----` (or SETUP AND CONFIGURATION) at top
- `# SECTION N: OUTPUT ----` near bottom (if script produces output)
- Middle sections are domain-appropriate

For 00_config.R specifically, sections should follow existing structure:
- SECTION 1: DATA PATHS
- SECTION 2: PCORNET TABLE PATHS
- SECTION 3: ICD CODE LISTS
- SECTION 4: PAYER MAPPING RULES
- SECTION 5: TREATMENT CODES
- SECTION 6: ANALYSIS PARAMETERS
- SECTION 7: UTILITY AUTO-SOURCING

**WHY comments (D-07, D-08, D-09):**

Add WHY comments for:
- 00_config.R: Explain WHY specific ICD code ranges are included, WHY payer mapping uses the specific hierarchy (Medicaid > Medicare > Private), WHY treatment code sets include specific CPT ranges
- 01_load_pcornet.R: Explain WHY specific column types are forced (e.g., character for IDs to prevent integer overflow), WHY certain tables use specific delimiters
- 02_harmonize_payer.R: Explain WHY the AMC 8-category system is used, WHY dual-eligible detection logic works the way it does, WHY the hierarchy ordering matters
- 03_duckdb_ingest.R: Explain WHY atomic write pattern is used (prevent partial ingests), WHY RDS-to-DuckDB instead of CSV-to-DuckDB

Do NOT comment obvious dplyr operations (per D-08). Preserve existing D-xx references (per D-09).
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/00_config.R R/01_load_pcornet.R R/02_harmonize_payer.R R/03_duckdb_ingest.R && grep -c "SECTION.*----" R/00_config.R R/01_load_pcornet.R R/02_harmonize_payer.R R/03_duckdb_ingest.R</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains "# Purpose:" within first 20 lines
    - R/00_config.R contains "# Inputs:" within first 20 lines
    - R/00_config.R contains "# Outputs:" within first 20 lines
    - R/00_config.R contains "# Dependencies:" within first 20 lines
    - R/01_load_pcornet.R contains "# Purpose:" within first 20 lines
    - R/02_harmonize_payer.R contains "# Purpose:" within first 20 lines
    - R/03_duckdb_ingest.R contains "# Purpose:" within first 20 lines
    - All 4 scripts contain at least 2 lines matching "SECTION.*----"
    - R/00_config.R contains "# ==============================================================================\n" as first line
    - R/02_harmonize_payer.R contains a WHY comment about payer hierarchy (grep for "hierarchy" or "Medicaid.*Medicare")
  </acceptance_criteria>
  <done>All 4 foundation scripts have standardized 5-field headers, numbered section headers with 4+ trailing dashes visible in RStudio outline, and WHY comments on clinical/business logic</done>
</task>

<task type="auto">
  <name>Task 2: Standardize header blocks on all 8 utility scripts (header-only per D-11)</name>
  <files>R/utils/utils_attrition.R, R/utils/utils_dates.R, R/utils/utils_duckdb.R, R/utils/utils_icd.R, R/utils/utils_payer.R, R/utils/utils_pptx.R, R/utils/utils_snapshot.R, R/utils/utils_treatment.R</files>
  <read_first>R/utils/utils_attrition.R, R/utils/utils_dates.R, R/utils/utils_duckdb.R, R/utils/utils_icd.R, R/utils/utils_payer.R, R/utils/utils_pptx.R, R/utils/utils_snapshot.R, R/utils/utils_treatment.R</read_first>
  <action>
For each of the 8 utility scripts, standardize the header block to the 5-field format per D-01/D-02/D-03. Per D-11, these scripts already have good roxygen2 function documentation -- do NOT modify existing function-level `#'` documentation.

**Header block template for utils:**

```r
# ==============================================================================
# utils/{name}.R -- {One-line purpose from SCRIPT_INDEX.md}
# ==============================================================================
#
# Purpose:
#   {What this utility module provides and when it's used}
#
# Inputs:
#   - None (utility function library, not a standalone script)
#
# Outputs:
#   - None (defines functions loaded into calling scripts' environment)
#
# Dependencies:
#   - {packages used}: {what for}
#
# Requirements: N/A (utility module)
#
# ==============================================================================
```

Specific per-file details to include in Purpose field:
- utils_attrition.R: "Attrition logging for cohort construction. Provides init_attrition_log() and log_attrition() for tracking patient counts through filter steps."
- utils_dates.R: "Multi-format date parsing for PCORnet CDM data. Provides parse_pcornet_date() handling YYYY-MM-DD, MM/DD/YYYY, and epoch formats."
- utils_duckdb.R: "DuckDB utility functions for backend-agnostic data access. Provides get_pcornet_table(), connection management, and lazy-to-eager materialization."
- utils_icd.R: "ICD code normalization and HL diagnosis matching. Provides normalize_icd() and is_hl_diagnosis() for dotted/undotted format handling."
- utils_payer.R: "Shared payer classification and comparison helpers. Provides is_missing_payer() and payer category validation functions."
- utils_pptx.R: "PowerPoint styling and slide generation helpers using officer package. Provides UF brand colors, table styling, and slide layout functions."
- utils_snapshot.R: "Snapshot helper for consistent RDS output creation. Provides save_output_data() with timestamped filenames and metadata."
- utils_treatment.R: "Shared treatment analysis helpers. Provides safe_table(), get_hl_patient_ids(), and empty_result() for treatment pipeline scripts."

Do NOT add section headers to utils files (they are function libraries, not multi-section scripts). Do NOT modify roxygen2 `#'` blocks. Only standardize the file-level header.
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/utils/utils_attrition.R R/utils/utils_dates.R R/utils/utils_duckdb.R R/utils/utils_icd.R R/utils/utils_payer.R R/utils/utils_pptx.R R/utils/utils_snapshot.R R/utils/utils_treatment.R</automated>
  </verify>
  <acceptance_criteria>
    - All 8 utils files contain "# Purpose:" within first 20 lines
    - All 8 utils files contain "# Inputs:" within first 20 lines
    - All 8 utils files contain "# Dependencies:" within first 20 lines
    - All 8 utils files start with "# ==============================================================================" (box-style equals border)
    - Existing roxygen2 blocks (#' lines) are preserved unchanged in all 8 files
    - No "SECTION.*----" lines added to utils files (they are function libraries, not sectioned scripts)
  </acceptance_criteria>
  <done>All 8 utility scripts have standardized 5-field header blocks matching the project template. Existing roxygen2 function documentation is untouched.</done>
</task>

</tasks>

<verification>
- grep "# Purpose:" R/00_config.R R/01_load_pcornet.R R/02_harmonize_payer.R R/03_duckdb_ingest.R confirms all 4 foundation scripts have Purpose field
- grep "SECTION.*----" R/00_config.R R/01_load_pcornet.R R/02_harmonize_payer.R R/03_duckdb_ingest.R confirms all 4 have section headers
- grep "# Purpose:" R/utils/*.R confirms all 8 utils have Purpose field
- No functional code changes -- only comments and whitespace modified
</verification>

<success_criteria>
- All 4 foundation scripts have 5-field headers (Purpose, Inputs, Outputs, Dependencies, Requirements)
- All 4 foundation scripts have numbered section headers with 4+ trailing dashes
- All 8 utility scripts have standardized 5-field header blocks
- WHY comments present on payer hierarchy, ICD code logic, and atomic write patterns
- Zero functional code changes (documentation only)
</success_criteria>

<output>
After completion, create `.planning/phases/69-script-documentation/69-01-SUMMARY.md`
</output>
