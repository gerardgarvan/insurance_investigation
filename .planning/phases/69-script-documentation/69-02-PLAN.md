---
phase: 69-script-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/10_cohort_predicates.R
  - R/11_treatment_payer.R
  - R/12_surveillance.R
  - R/13_survivorship_encounters.R
  - R/14_build_cohort.R
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "Every cohort script (10-14) has a 5-field header block with Purpose, Inputs, Outputs, Dependencies, Requirements"
    - "Every cohort script has numbered section headers ending with 4+ dashes"
    - "Clinical logic in cohort predicates and survivorship classification has WHY comments"
    - "Treatment-anchored payer logic has WHY comments explaining the +/-30 day window and hierarchy"
  artifacts:
    - path: "R/10_cohort_predicates.R"
      provides: "Documented named filter predicates with clinical rationale"
      contains: "# Purpose:"
    - path: "R/14_build_cohort.R"
      provides: "Documented cohort assembly pipeline with attrition logic"
      contains: "# SECTION"
  key_links:
    - from: "R/14_build_cohort.R"
      to: "R/10_cohort_predicates.R"
      via: "Dependencies field documents source() chain"
      pattern: "# Dependencies:"
---

<objective>
Document cohort building scripts (10-14) with full headers, section navigation, and clinical WHY comments.

Purpose: Cohort scripts contain the densest clinical logic in the entire pipeline -- named predicates encoding HL diagnosis rules, enrollment criteria, treatment flags, surveillance modality detection, and survivorship encounter classification. These are the scripts most likely to confuse a new team member, so WHY comments are critical here.

Output: 5 fully documented cohort scripts with clinical rationale explained.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document cohort predicate and helper scripts (10-13) with headers, sections, and clinical WHY comments</name>
  <files>R/10_cohort_predicates.R, R/11_treatment_payer.R, R/12_surveillance.R, R/13_survivorship_encounters.R</files>
  <read_first>R/10_cohort_predicates.R, R/11_treatment_payer.R, R/12_surveillance.R, R/13_survivorship_encounters.R, R/SCRIPT_INDEX.md</read_first>
  <action>
For each of the 4 cohort helper scripts, apply the full documentation standard (D-01 through D-09).

**Header block standardization (D-01, D-02, D-03):**

Apply the 5-field header template with box-style equals borders. For scripts that already have good headers (like 10_cohort_predicates.R which has a detailed header), preserve existing valuable content and add any missing fields.

Per-script header details:
- **10_cohort_predicates.R**: Purpose = "Named filter predicates (has_*, with_*, exclude_*) for HL cohort building. Each function accepts a patient-level tibble and returns a filtered subset." Inputs = "PCORnet tables via get_pcornet_table() (DIAGNOSIS, ENROLLMENT, DEMOGRAPHIC)". Outputs = "None (defines functions loaded by 14_build_cohort.R)". Dependencies = "00_config.R (auto-sources utils), get_pcornet_table() from utils_duckdb, is_hl_diagnosis() from utils_icd". Requirements = "CHRT-01, CHRT-02, CHRT-03".
- **11_treatment_payer.R**: Purpose = "Treatment-anchored payer mode: assigns primary payer within +/-30 day window around first treatment date". Inputs = "hl_cohort environment from 14_build_cohort". Outputs = "treatment_payer tibble added to environment". Dependencies = "Sourced by 14_build_cohort.R, requires payer_summary and hl_cohort in environment".
- **12_surveillance.R**: Purpose = "Surveillance modality detection: identifies 9 procedure-based and 10 lab-based post-diagnosis events for each patient". Inputs = "PCORnet PROCEDURES and LAB_RESULT_CM tables". Outputs = "surveillance_flags tibble added to environment". Dependencies = "Sourced by 14_build_cohort.R".
- **13_survivorship_encounters.R**: Purpose = "4-level survivorship encounter classification: non-acute, cancer-related, cancer provider, survivorship provider". Inputs = "PCORnet ENCOUNTER and PROVIDER tables". Outputs = "survivorship_encounters tibble added to environment". Dependencies = "Sourced by 14_build_cohort.R".

**Section headers (D-04, D-05, D-06):**

Convert all existing section-like comments to `# SECTION N: TITLE ----` format with 4+ trailing dashes.

For 10_cohort_predicates.R (function library structure):
- SECTION 1: SETUP AND DEPENDENCIES ----
- SECTION 2: DIAGNOSIS PREDICATES ----
- SECTION 3: ENROLLMENT PREDICATES ----
- SECTION 4: TREATMENT FLAG FUNCTIONS ----
- SECTION 5: UTILITY PREDICATES ----

For 11_treatment_payer.R:
- SECTION 1: SETUP ----
- SECTION 2: TREATMENT DATE EXTRACTION ----
- SECTION 3: PAYER WINDOW MATCHING ----
- SECTION 4: MODAL PAYER ASSIGNMENT ----

For 12_surveillance.R:
- SECTION 1: SETUP ----
- SECTION 2: PROCEDURE-BASED SURVEILLANCE ----
- SECTION 3: LAB-BASED SURVEILLANCE ----
- SECTION 4: SURVEILLANCE FLAG ASSEMBLY ----

For 13_survivorship_encounters.R:
- SECTION 1: SETUP ----
- SECTION 2: ENCOUNTER CLASSIFICATION ----
- SECTION 3: PROVIDER CLASSIFICATION ----
- SECTION 4: SURVIVORSHIP LEVEL ASSIGNMENT ----

Adjust section names and counts based on actual script structure -- these are guidelines. Use box-style equals borders for major sections.

**WHY comments (D-07, D-08, D-09):**

These scripts are the most clinical-logic-heavy in the pipeline. Add WHY comments for:

- **10_cohort_predicates.R**: WHY ICD codes are matched in both dotted and undotted formats (PCORnet data quality varies by site). WHY semi_join is used instead of filter() for set-based predicate matching. WHY the "Translation gap workaround" exists (preserve existing comment). WHY enrollment period overlap is checked.
- **11_treatment_payer.R**: WHY the +/-30 day window around treatment date (clinically relevant payer capture window). WHY modal payer (most frequent) is used when multiple payers exist in the window. WHY the hierarchy is Medicaid > Medicare > Private for tie-breaking.
- **12_surveillance.R**: WHY 9 specific procedure codes indicate surveillance (cite clinical relevance if available). WHY 10 specific lab codes indicate surveillance. WHY post-diagnosis temporal filter is applied.
- **13_survivorship_encounters.R**: WHY the 4 levels are structured hierarchically (non-acute is broadest, survivorship provider is most specific). WHY specific provider specialties indicate survivorship care.

Do NOT comment obvious dplyr operations (per D-08). Preserve existing D-xx references and REQ-xx references (per D-09).
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/10_cohort_predicates.R R/11_treatment_payer.R R/12_surveillance.R R/13_survivorship_encounters.R && grep -c "SECTION.*----" R/10_cohort_predicates.R R/11_treatment_payer.R R/12_surveillance.R R/13_survivorship_encounters.R</automated>
  </verify>
  <acceptance_criteria>
    - R/10_cohort_predicates.R contains "# Purpose:" within first 25 lines
    - R/11_treatment_payer.R contains "# Purpose:" within first 20 lines
    - R/12_surveillance.R contains "# Purpose:" within first 20 lines
    - R/13_survivorship_encounters.R contains "# Purpose:" within first 20 lines
    - All 4 scripts contain "# Inputs:" and "# Outputs:" and "# Dependencies:" in header block
    - All 4 scripts contain at least 3 lines matching "SECTION.*----"
    - R/10_cohort_predicates.R preserves "Translation gap workaround" comment
    - R/11_treatment_payer.R contains a WHY comment about the 30-day window (grep for "30.*day" or "window")
  </acceptance_criteria>
  <done>All 4 cohort helper scripts have standardized 5-field headers, numbered section headers with 4+ trailing dashes, and WHY comments on clinical logic (ICD matching, payer windows, surveillance criteria, survivorship levels)</done>
</task>

<task type="auto">
  <name>Task 2: Document the cohort assembly script (14_build_cohort.R) with full documentation</name>
  <files>R/14_build_cohort.R</files>
  <read_first>R/14_build_cohort.R, R/10_cohort_predicates.R, R/SCRIPT_INDEX.md</read_first>
  <action>
Apply full documentation standard to the main cohort assembly script (D-01 through D-09).

**Header block (D-01, D-02, D-03):**

```
Purpose: Compose named predicates into sequential filter chain, add treatment flags,
  calculate patient ages, and assemble the final HL cohort. This is the main pipeline
  entry point that sources all cohort helpers and produces the cohort used by all
  downstream analysis scripts.

Inputs:
  - PCORnet CDM tables via 01_load_pcornet.R (DEMOGRAPHIC, DIAGNOSIS, ENROLLMENT, etc.)
  - Payer summary via 02_harmonize_payer.R (patient-level AMC 8-category payer assignments)

Outputs:
  - hl_cohort (in-memory tibble): Final filtered cohort with treatment flags and demographics
  - attrition_log (in-memory tibble): Step-by-step patient count through filter chain
  - output/hl_cohort.rds: Cached cohort for downstream scripts

Dependencies:
  - source("R/02_harmonize_payer.R"): Loads full upstream chain (config, data, payer)
  - source("R/10_cohort_predicates.R"): Named filter functions (has_*, with_*, exclude_*)
  - source("R/11_treatment_payer.R"): Treatment-anchored payer assignment
  - source("R/12_surveillance.R"): Surveillance modality detection
  - source("R/13_survivorship_encounters.R"): Survivorship encounter classification

Requirements: CHRT-01, CHRT-02
```

**Section headers (D-04, D-05, D-06):**

- SECTION 1: SETUP AND DEPENDENCY LOADING ----
- SECTION 2: SEQUENTIAL FILTER CHAIN ----
- SECTION 3: TREATMENT FLAGS AND AGE CALCULATION ----
- SECTION 4: COHORT ASSEMBLY AND CACHE ----
- SECTION 5: ATTRITION SUMMARY ----

Adjust section names and counts based on actual script structure.

**WHY comments (D-07, D-08, D-09):**

- WHY predicates are applied in this specific order (enrollment before diagnosis to reduce join size, etc.)
- WHY treatment flags are added after filtering (to avoid computing flags for excluded patients)
- WHY age is calculated as current year minus birth year (no precise DOB in PCORnet CDM)
- WHY attrition log tracks unique patient IDs not row counts (per D-17 from earlier phase)

Preserve all existing decision traceability references.
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/14_build_cohort.R && grep -c "SECTION.*----" R/14_build_cohort.R && grep "Dependencies" R/14_build_cohort.R</automated>
  </verify>
  <acceptance_criteria>
    - R/14_build_cohort.R contains "# Purpose:" within first 25 lines
    - R/14_build_cohort.R contains "# Inputs:", "# Outputs:", "# Dependencies:", "# Requirements:" in header
    - R/14_build_cohort.R contains at least 4 lines matching "SECTION.*----"
    - R/14_build_cohort.R header lists all 5 source() dependencies (02, 10, 11, 12, 13)
    - R/14_build_cohort.R contains a WHY comment about filter ordering or attrition tracking
  </acceptance_criteria>
  <done>14_build_cohort.R has standardized 5-field header documenting all 5 source() dependencies, numbered section headers, and WHY comments on filter ordering and attrition tracking</done>
</task>

</tasks>

<verification>
- grep "# Purpose:" R/1[0-4]_*.R confirms all 5 cohort scripts have Purpose field
- grep "SECTION.*----" R/1[0-4]_*.R confirms all 5 have section headers
- Clinical WHY comments present for: ICD matching, 30-day payer window, surveillance criteria, filter ordering
- No functional code changes -- only comments and whitespace modified
</verification>

<success_criteria>
- All 5 cohort scripts have 5-field headers (Purpose, Inputs, Outputs, Dependencies, Requirements)
- All 5 cohort scripts have numbered section headers with 4+ trailing dashes
- Clinical logic has WHY comments (ICD format matching, payer windows, surveillance, survivorship levels)
- Existing decision traceability (D-xx, REQ-xx) preserved
- Zero functional code changes
</success_criteria>

<output>
After completion, create `.planning/phases/69-script-documentation/69-02-SUMMARY.md`
</output>
