---
phase: 69-script-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - R/20_treatment_inventory.R
  - R/21_investigate_unmatched.R
  - R/22_investigate_unmatched_ndc.R
  - R/23_combine_reports.R
  - R/24_treatment_codes_resolved.R
  - R/25_treatment_durations.R
  - R/26_treatment_episodes.R
  - R/27_drug_name_resolution.R
  - R/28_episode_classification.R
  - R/29_first_line_and_death_analysis.R
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "Every treatment script (20-29) has a 5-field header block with Purpose, Inputs, Outputs, Dependencies, Requirements"
    - "Every treatment script has numbered section headers ending with 4+ dashes"
    - "Treatment duration logic (90-day gap threshold, episode splitting) has WHY comments"
    - "Drug resolution and regimen detection logic has WHY comments explaining clinical rationale"
  artifacts:
    - path: "R/25_treatment_durations.R"
      provides: "Documented treatment duration analysis with episode logic explained"
      contains: "# Purpose:"
    - path: "R/28_episode_classification.R"
      provides: "Documented episode-level cancer linkage with regimen detection explained"
      contains: "# Purpose:"
    - path: "R/29_first_line_and_death_analysis.R"
      provides: "Documented first-line therapy identification with 60-day clean period explained"
      contains: "# Purpose:"
  key_links:
    - from: "R/26_treatment_episodes.R"
      to: "R/25_treatment_durations.R"
      via: "Dependencies field documents source() chain"
      pattern: "# Dependencies:"
---

<objective>
Document treatment analysis scripts (20-29) with full headers, section navigation, and clinical WHY comments.

Purpose: Treatment scripts implement complex clinical logic -- 90-day episode gaps, drug name resolution via RxNorm API, ABVD/BV+AVD/Nivo+AVD regimen detection, and 60-day clean period for first-line therapy. These are the second most clinical-logic-heavy scripts after cohort and need thorough WHY documentation.

Output: 10 fully documented treatment scripts.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document treatment inventory and code resolution scripts (20-24)</name>
  <files>R/20_treatment_inventory.R, R/21_investigate_unmatched.R, R/22_investigate_unmatched_ndc.R, R/23_combine_reports.R, R/24_treatment_codes_resolved.R</files>
  <read_first>R/20_treatment_inventory.R, R/21_investigate_unmatched.R, R/22_investigate_unmatched_ndc.R, R/23_combine_reports.R, R/24_treatment_codes_resolved.R, R/SCRIPT_INDEX.md</read_first>
  <action>
For each of scripts 20-24, apply the full documentation standard (D-01 through D-09).

**Header block standardization (D-01, D-02, D-03):**

Apply 5-field header template with box-style equals borders. Per-script details:

- **20_treatment_inventory.R**: Purpose = "Treatment inventory by source table: counts code frequencies across 7 PCORnet tables for Chemotherapy, Radiation, SCT, and Immunotherapy". Inputs = "PCORnet CDM tables (PROCEDURES, PRESCRIBING, MED_ADMIN, DIAGNOSIS, LAB_RESULT_CM, CONDITION, OBS_CLIN)". Outputs = "output/treatment_inventory.xlsx".
- **21_investigate_unmatched.R**: Purpose = "Investigate unmatched CPT/HCPCS codes via NLM HCPCS API to classify them as relevant/irrelevant treatment codes". Inputs = "Treatment inventory from R/20, NLM HCPCS API (external)". Outputs = "cache/outputs/unmatched_codes_classified.rds, output/unmatched_codes_report.xlsx".
- **22_investigate_unmatched_ndc.R**: Purpose = "Investigate unmatched NDC/RXNORM drug codes via RxNorm API to classify as chemotherapy/non-chemo". Inputs = "Treatment inventory, RxNorm API (external)". Outputs = "cache/outputs/unmatched_ndc_classified.rds".
- **23_combine_reports.R**: Purpose = "Merge CPT/HCPCS (Phase 39) and NDC/RXNORM (Phase 40) unmatched code reports into consolidated xlsx". Inputs = "cache/outputs/unmatched_codes_classified.rds, cache/outputs/unmatched_ndc_classified.rds". Outputs = "output/combined_unmatched_report.xlsx".
- **24_treatment_codes_resolved.R**: Purpose = "Create per-treatment-type resolved xlsx files with human-readable descriptions for Radiation, SCT, Immunotherapy, Supportive Care". Inputs = "TREATMENT_CODES from 00_config.R, code_descriptions.rds". Outputs = "output/treatment_codes_resolved_*.xlsx (4 files)".

**Section headers (D-04, D-05, D-06):**

Convert all existing section-like comments to `# SECTION N: TITLE ----` format. Each script should have at minimum SETUP and OUTPUT sections. Adjust middle sections to match each script's actual structure (API calls, code matching, report assembly, etc.).

**WHY comments (D-07, D-08, D-09):**

- **20**: WHY 7 PCORnet tables are searched (treatment evidence scattered across procedure, prescribing, med admin, diagnosis, lab, condition, obs_clin). WHY codes are matched at the prefix level vs exact match.
- **21**: WHY NLM HCPCS API is used (official CMS code descriptions, free access). WHY auto-classification logic categorizes codes (reduce manual review burden).
- **22**: WHY RxNorm API is used for NDC resolution (NDC codes are manufacturer-specific, RxNorm normalizes to ingredient level). WHY drug classification distinguishes chemotherapy from supportive care.
- **23**: WHY reports are combined (single deliverable for clinical review vs two separate reports).
- **24**: WHY treatment types are separated into individual xlsx files (different clinical reviewers per type).

Preserve existing D-xx and REQ-xx references (per D-09).
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/20_treatment_inventory.R R/21_investigate_unmatched.R R/22_investigate_unmatched_ndc.R R/23_combine_reports.R R/24_treatment_codes_resolved.R && grep -c "SECTION.*----" R/20_treatment_inventory.R R/21_investigate_unmatched.R R/22_investigate_unmatched_ndc.R R/23_combine_reports.R R/24_treatment_codes_resolved.R</automated>
  </verify>
  <acceptance_criteria>
    - All 5 scripts (20-24) contain "# Purpose:" within first 25 lines
    - All 5 scripts contain "# Inputs:" and "# Outputs:" and "# Dependencies:" in header
    - All 5 scripts contain at least 2 lines matching "SECTION.*----"
    - R/21_investigate_unmatched.R contains a WHY comment about NLM API (grep for "NLM" or "HCPCS API")
    - R/22_investigate_unmatched_ndc.R contains a WHY comment about RxNorm (grep for "RxNorm" or "NDC.*normalize")
  </acceptance_criteria>
  <done>Scripts 20-24 have standardized 5-field headers, numbered section headers, and WHY comments on API usage, code matching logic, and report structure</done>
</task>

<task type="auto">
  <name>Task 2: Document treatment duration, episode, and first-line therapy scripts (25-29)</name>
  <files>R/25_treatment_durations.R, R/26_treatment_episodes.R, R/27_drug_name_resolution.R, R/28_episode_classification.R, R/29_first_line_and_death_analysis.R</files>
  <read_first>R/25_treatment_durations.R, R/26_treatment_episodes.R, R/27_drug_name_resolution.R, R/28_episode_classification.R, R/29_first_line_and_death_analysis.R, R/SCRIPT_INDEX.md</read_first>
  <action>
For each of scripts 25-29, apply the full documentation standard (D-01 through D-09).

**Header block standardization (D-01, D-02, D-03):**

25_treatment_durations.R already has an excellent header with decision traceability (D-01 through D-13). Standardize it to the 5-field format while PRESERVING all existing D-xx references. Add missing fields (Inputs, Dependencies) if not present.

Per-script details:
- **25_treatment_durations.R**: Purpose preserved from existing header. Add Inputs = "PCORnet CDM tables (7 tables via extract_all_dates)". Dependencies = "00_config, 01_load_pcornet". Requirements = "N/A (standalone analysis)".
- **26_treatment_episodes.R**: Purpose = "Per-episode start/stop dates with episode length, historical date flagging, and triggering codes". Inputs = "PCORnet treatment tables + treatment_durations.rds from R/25". Outputs = "cache/outputs/treatment_episodes.rds, output/treatment_episodes.xlsx".
- **27_drug_name_resolution.R**: Purpose = "Drug name resolution for chemotherapy via RxNorm API -- maps NDC/RXNORM codes to human-readable drug names". Inputs = "PCORnet PRESCRIBING and MED_ADMIN tables". Outputs = "cache/outputs/drug_names_resolved.rds".
- **28_episode_classification.R**: Purpose = "Episode-level cancer linkage using ENCOUNTERID + 30-day temporal fallback, with regimen detection (ABVD, BV+AVD, Nivo+AVD)". Inputs = "treatment_episodes.rds, PCORnet tables". Outputs = "cache/outputs/episode_classification.rds, output/episode_classification.xlsx".
- **29_first_line_and_death_analysis.R**: Purpose = "First-line therapy flagging (60-day clean period before first treatment) and death date validation cross-referenced against treatment timeline". Inputs = "treatment_episodes.rds, PCORnet DEATH table". Outputs = "cache/outputs/first_line_therapy.rds".

**Section headers (D-04, D-05, D-06):**

25_treatment_durations.R already has `# --- SECTION N: TITLE ---` format. Convert to `# SECTION N: TITLE ----` (4+ trailing dashes, remove leading dashes). Apply same conversion to all 5 scripts.

**WHY comments (D-07, D-08, D-09):**

These are the most clinical-logic-heavy treatment scripts. Focus WHY comments on:

- **25**: WHY 90-day gap threshold for episode splitting (clinical standard for oncology treatment cycles). WHY window-based episode splitting instead of previous-date-based (prevents episodes > threshold). WHY all 7 PCORnet tables are searched (treatment evidence distributed across multiple tables).
- **26**: WHY historical date flagging exists (dates before 2000 may indicate data quality issues). WHY triggering codes are captured (identifies which specific code initiated the episode).
- **27**: WHY RxNorm API is needed for drug resolution (NDC codes are manufacturer-specific, not clinically meaningful). WHY only chemotherapy drugs are resolved (other types have simple code-to-name mappings).
- **28**: WHY ENCOUNTERID linkage is tried first before temporal fallback (most reliable connection between treatment and cancer diagnosis). WHY 30-day temporal window for fallback (clinical proximity). WHY specific drug combinations define ABVD/BV+AVD/Nivo+AVD regimens.
- **29**: WHY 60-day clean period defines first-line therapy (clinical definition: no prior treatment in 60 days means this is the first course). WHY death date validation cross-references treatment dates (impossible deaths = dates before last treatment).

Preserve all existing D-01 through D-13 references in 25_treatment_durations.R.
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/25_treatment_durations.R R/26_treatment_episodes.R R/27_drug_name_resolution.R R/28_episode_classification.R R/29_first_line_and_death_analysis.R && grep -c "SECTION.*----" R/25_treatment_durations.R R/26_treatment_episodes.R R/27_drug_name_resolution.R R/28_episode_classification.R R/29_first_line_and_death_analysis.R</automated>
  </verify>
  <acceptance_criteria>
    - All 5 scripts (25-29) contain "# Purpose:" within first 30 lines
    - All 5 scripts contain "# Inputs:" and "# Outputs:" and "# Dependencies:" in header
    - All 5 scripts contain at least 3 lines matching "SECTION.*----"
    - R/25_treatment_durations.R still contains "D-01" through "D-05" decision references (preserved per D-09)
    - R/25_treatment_durations.R section headers converted from "# ---" format to "# SECTION N: TITLE ----" format
    - R/28_episode_classification.R contains WHY comment about regimen detection (grep for "ABVD" or "regimen")
    - R/29_first_line_and_death_analysis.R contains WHY comment about 60-day clean period (grep for "60.*day" or "clean period")
  </acceptance_criteria>
  <done>Scripts 25-29 have standardized 5-field headers, numbered section headers with 4+ trailing dashes, and WHY comments on 90-day episode gaps, regimen detection, 60-day clean period, and death date validation</done>
</task>

</tasks>

<verification>
- grep "# Purpose:" R/2[0-9]_*.R confirms all 10 treatment scripts have Purpose field
- grep "SECTION.*----" R/2[0-9]_*.R confirms all 10 have section headers
- Clinical WHY comments present for: 90-day gap, episode splitting, RxNorm usage, regimen detection, 60-day clean period
- Decision traceability references (D-01 through D-13) preserved in R/25
- No functional code changes
</verification>

<success_criteria>
- All 10 treatment scripts have 5-field headers
- All 10 treatment scripts have numbered section headers with 4+ trailing dashes
- Clinical logic documented with WHY comments
- Existing decision traceability preserved
- Zero functional code changes
</success_criteria>

<output>
After completion, create `.planning/phases/69-script-documentation/69-03-SUMMARY.md`
</output>
