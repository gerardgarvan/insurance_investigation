---
phase: 69-script-documentation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - R/70_visualize_waterfall.R
  - R/71_visualize_sankey.R
  - R/72_generate_pptx.R
  - R/73_generate_phase19_20_pptx.R
  - R/74_generate_documentation.R
  - R/75_encounter_analysis.R
  - R/80_smoke_test_backends.R
  - R/81_parity_test_cohort.R
  - R/82_benchmark_cohort.R
  - R/83_generate_speedup_report.R
  - R/84_test_durations.R
  - R/85_test_episodes.R
  - R/86_smoke_test_foundation.R
  - R/87_smoke_test_full_pipeline.R
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "Every output script (70-75) has a 5-field header block with Purpose, Inputs, Outputs, Dependencies, Requirements"
    - "Every test script (80-87) has a 5-field header block with Purpose, Inputs, Outputs, Dependencies, Requirements"
    - "All 14 scripts have numbered section headers ending with 4+ dashes"
    - "HIPAA suppression logic and test validation logic have WHY comments"
  artifacts:
    - path: "R/70_visualize_waterfall.R"
      provides: "Documented waterfall visualization with HIPAA notes"
      contains: "# Purpose:"
    - path: "R/72_generate_pptx.R"
      provides: "Documented PowerPoint generation with slide inventory"
      contains: "# Purpose:"
    - path: "R/87_smoke_test_full_pipeline.R"
      provides: "Documented full pipeline smoke test"
      contains: "# Purpose:"
  key_links:
    - from: "R/72_generate_pptx.R"
      to: "R/utils/utils_pptx.R"
      via: "Dependencies field documents utils_pptx usage"
      pattern: "# Dependencies:"
---

<objective>
Document output/visualization scripts (70-75) and test scripts (80-87) with full headers, section navigation, and WHY comments.

Purpose: Output scripts produce the project's deliverables (waterfall charts, Sankey diagrams, PowerPoint decks, documentation). Test scripts validate pipeline integrity after reorganization. Both need clear documentation for maintainability -- output scripts so clinical reviewers understand what's produced, test scripts so future reorganization work knows what's validated.

Output: 6 documented output scripts + 8 documented test scripts = 14 total.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document output and visualization scripts (70-75)</name>
  <files>R/70_visualize_waterfall.R, R/71_visualize_sankey.R, R/72_generate_pptx.R, R/73_generate_phase19_20_pptx.R, R/74_generate_documentation.R, R/75_encounter_analysis.R</files>
  <read_first>R/70_visualize_waterfall.R, R/71_visualize_sankey.R, R/72_generate_pptx.R, R/73_generate_phase19_20_pptx.R, R/74_generate_documentation.R, R/75_encounter_analysis.R, R/SCRIPT_INDEX.md</read_first>
  <action>
For each of scripts 70-75, apply the full documentation standard (D-01 through D-09).

**Header block standardization (D-01, D-02, D-03):**

R/72_generate_pptx.R already has an extensive header listing all 52 slides. Preserve that slide inventory -- it's extremely valuable. Standardize to 5-field format while keeping the slide list as additional content within the Purpose section.

Per-script details:
- **70_visualize_waterfall.R**: Purpose = "Attrition waterfall chart showing cohort reduction through sequential filter steps (VIZ-01). Produces a bar chart where each bar represents a filter step, showing patients remaining and excluded." Inputs = "attrition_log from 14_build_cohort.R (in-memory tibble)". Outputs = "output/figures/waterfall_attrition.png". Requirements = "VIZ-01".
- **71_visualize_sankey.R**: Purpose = "Payer-stratified Sankey/alluvial diagram showing patient flow from Payer Category to Treatment Type (VIZ-02). Uses ggalluvial for alluvial plot." Inputs = "hl_cohort from 14_build_cohort.R". Outputs = "output/figures/sankey_payer_treatment.png". Requirements = "VIZ-02".
- **72_generate_pptx.R**: Purpose = "Generate insurance tables PowerPoint deck using officer package. [Keep existing 52-slide listing]". Inputs = "encounter_analysis from 75_encounter_analysis.R, hl_cohort from 14_build_cohort.R". Outputs = "output/insurance_tables_YYYY-MM-DD.pptx". Dependencies = "utils_pptx.R, 75_encounter_analysis.R".
- **73_generate_phase19_20_pptx.R**: Purpose = "Standalone PowerPoint deck for Phase 19 (UF missingness) and Phase 20 (FLM duplicates) analysis results". Inputs = "FLM duplicate data from 94_flm_duplicate_dates.R". Outputs = "output/phase19_20_deck.pptx". Dependencies = "utils_pptx.R, 94_flm_duplicate_dates.R".
- **74_generate_documentation.R**: Purpose = "Auto-generate variable documentation (.md + .docx) from config code lists -- creates human-readable documentation of all lookup tables and code mappings". Inputs = "00_config.R code lists and lookup tables". Outputs = "output/docs/variable_documentation.md, output/docs/variable_documentation.docx".
- **75_encounter_analysis.R**: Purpose = "Encounter analysis by payer category, diagnosis year, and age group -- produces histograms and summary tables consumed by PowerPoint generator". Inputs = "hl_cohort from 14_build_cohort.R". Outputs = "encounter_analysis objects in memory (consumed by R/72)".

**Section headers (D-04, D-05, D-06):**

Convert all existing section-like comments to `# SECTION N: TITLE ----` format. For R/72 which is very long (52 slides), use major section groupings rather than per-slide sections.

**WHY comments (D-07, D-08, D-09):**

- **70**: WHY waterfall (not table) for attrition (visual impact shows where most patients are lost). WHY bars show both remaining and excluded counts (clinical reviewers need both absolute numbers and proportions).
- **71**: WHY Sankey/alluvial (shows flow relationships between categorical variables that tables cannot). WHY ggalluvial over alternatives (established standard, ggplot2-compatible).
- **72**: WHY officer package for PowerPoint (programmatic slide generation ensures reproducibility; manual PowerPoint creation is error-prone with 52 slides). WHY specific slide ordering (matches Python pipeline output for comparison).
- **73**: WHY standalone deck (Phase 19/20 results needed as separate deliverable for site-specific review, not bundled in main deck).
- **74**: WHY auto-generation from config (ensures documentation stays in sync with actual code lists; manual documentation drifts).
- **75**: WHY age groups and diagnosis year stratification (standard epidemiological analysis dimensions for cancer cohorts). WHY this is a separate script from R/72 (separation of data computation from slide generation).
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/70_visualize_waterfall.R R/71_visualize_sankey.R R/72_generate_pptx.R R/73_generate_phase19_20_pptx.R R/74_generate_documentation.R R/75_encounter_analysis.R && grep -c "SECTION.*----" R/70_visualize_waterfall.R R/71_visualize_sankey.R R/72_generate_pptx.R R/73_generate_phase19_20_pptx.R R/74_generate_documentation.R R/75_encounter_analysis.R</automated>
  </verify>
  <acceptance_criteria>
    - All 6 scripts (70-75) contain "# Purpose:" within first 60 lines (R/72 has lengthy slide listing)
    - All 6 scripts contain "# Inputs:" and "# Outputs:" and "# Dependencies:" in header
    - All 6 scripts contain at least 2 lines matching "SECTION.*----"
    - R/72_generate_pptx.R preserves the slide inventory listing (grep for "Slide" or "slide")
    - R/70_visualize_waterfall.R contains "VIZ-01" requirement reference
    - R/71_visualize_sankey.R contains "VIZ-02" requirement reference
  </acceptance_criteria>
  <done>Scripts 70-75 have standardized 5-field headers, numbered section headers, and WHY comments on visualization choices, PowerPoint generation rationale, and analysis stratification</done>
</task>

<task type="auto">
  <name>Task 2: Document test and smoke test scripts (80-87)</name>
  <files>R/80_smoke_test_backends.R, R/81_parity_test_cohort.R, R/82_benchmark_cohort.R, R/83_generate_speedup_report.R, R/84_test_durations.R, R/85_test_episodes.R, R/86_smoke_test_foundation.R, R/87_smoke_test_full_pipeline.R</files>
  <read_first>R/80_smoke_test_backends.R, R/81_parity_test_cohort.R, R/82_benchmark_cohort.R, R/83_generate_speedup_report.R, R/84_test_durations.R, R/85_test_episodes.R, R/86_smoke_test_foundation.R, R/87_smoke_test_full_pipeline.R, R/SCRIPT_INDEX.md</read_first>
  <action>
For each of scripts 80-87, apply the full documentation standard (D-01 through D-09).

**Header block standardization (D-01, D-02, D-03):**

- **80_smoke_test_backends.R**: Purpose = "Backend parity smoke test: runs 6 named predicates on both RDS and DuckDB backends with 100-patient sample, comparing results via waldo::compare()". Inputs = "PCORnet tables via both backends". Outputs = "Console output (PASS/FAIL per predicate)". Dependencies = "00_config, 01_load_pcornet, 02_harmonize_payer, 10_cohort_predicates".
- **81_parity_test_cohort.R**: Purpose = "Full cohort build parity verification: builds complete HL cohort using RDS backend and DuckDB backend, compares results via waldo::compare()". Inputs = "Full PCORnet CDM data". Outputs = "Console output (PASS/FAIL with diff details)". Dependencies = "00_config, 01_load_pcornet, 14_build_cohort".
- **82_benchmark_cohort.R**: Purpose = "RDS vs DuckDB cohort build benchmark: 3 runs per backend, reports median execution time for performance comparison". Inputs = "Full PCORnet CDM data". Outputs = "output/benchmark_results.csv". Dependencies = "00_config, 01_load_pcornet, 14_build_cohort".
- **83_generate_speedup_report.R**: Purpose = "Generate human-readable DuckDB vs RDS speedup report from benchmark CSV". Inputs = "output/benchmark_results.csv from R/82". Outputs = "output/speedup_report.txt".
- **84_test_durations.R**: Purpose = "Verification script: clinical plausibility checks, structural validation, and anomaly detection for treatment_durations.rds output". Inputs = "cache/outputs/treatment_durations.rds from R/25". Outputs = "Console output (PASS/FAIL per check)". Dependencies = "00_config, 25_treatment_durations".
- **85_test_episodes.R**: Purpose = "Verification script: structural, data quality, historical flag, and clinical plausibility checks for treatment_episodes.rds output". Inputs = "cache/outputs/treatment_episodes.rds from R/26". Outputs = "Console output (PASS/FAIL per check)". Dependencies = "00_config, 26_treatment_episodes".
- **86_smoke_test_foundation.R**: Purpose = "Validates Phase 65 foundation reorganization: utils subfolder structure, script renumbering, source() reference resolution". Inputs = "R/ directory filesystem structure". Outputs = "Console output (PASS/FAIL per validation)".
- **87_smoke_test_full_pipeline.R**: Purpose = "Validates Phase 66-67 complete reorganization: all decade counts, source() resolution, archive structure, smoke test placement in test decade". Inputs = "R/ directory filesystem structure". Outputs = "Console output (PASS/FAIL per decade)".

**Section headers (D-04, D-05, D-06):**

Convert all existing section-like comments to `# SECTION N: TITLE ----` format.

**WHY comments (D-07, D-08, D-09):**

- **80**: WHY 100-patient sample (full cohort takes too long for quick smoke test; 100 patients sufficient to catch most parity issues). WHY 6 specific predicates (covers the core filter chain -- enrollment, diagnosis, payer, treatment flags).
- **81**: WHY waldo::compare over identical() (waldo shows detailed diffs, not just TRUE/FALSE -- critical for debugging parity failures).
- **82**: WHY 3 runs per backend (reduces variance from system load; median is more robust than mean for timing).
- **83**: WHY separate report generator from benchmark (benchmark produces raw data, report is human-readable interpretation).
- **84**: WHY clinical plausibility checks (treatment durations > 10 years or < 0 days indicate data issues, not clinical reality). WHY anomaly detection (outliers may indicate data quality problems or legitimate edge cases).
- **85**: WHY historical flag validation (dates before 2000 are flagged as potentially erroneous -- PCORnet data starts around 2010 for most sites).
- **86**: WHY filesystem structure validation (after reorganization, source() calls must resolve to actual files). WHY utils subfolder check (Phase 65 moved utils to R/utils/, verifying they were all moved).
- **87**: WHY per-decade validation (ensures reorganization didn't miss scripts or create gaps in numbering).
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/80_smoke_test_backends.R R/81_parity_test_cohort.R R/82_benchmark_cohort.R R/83_generate_speedup_report.R R/84_test_durations.R R/85_test_episodes.R R/86_smoke_test_foundation.R R/87_smoke_test_full_pipeline.R && grep -c "SECTION.*----" R/80_smoke_test_backends.R R/81_parity_test_cohort.R R/82_benchmark_cohort.R R/83_generate_speedup_report.R R/84_test_durations.R R/85_test_episodes.R R/86_smoke_test_foundation.R R/87_smoke_test_full_pipeline.R</automated>
  </verify>
  <acceptance_criteria>
    - All 8 scripts (80-87) contain "# Purpose:" within first 25 lines
    - All 8 scripts contain "# Inputs:" and "# Outputs:" and "# Dependencies:" in header
    - All 8 scripts contain at least 2 lines matching "SECTION.*----"
    - R/80_smoke_test_backends.R contains a WHY comment about 100-patient sample (grep for "100.*patient" or "sample.*size")
    - R/84_test_durations.R contains a WHY comment about clinical plausibility (grep for "plausibility" or "clinical.*check")
  </acceptance_criteria>
  <done>Scripts 80-87 have standardized 5-field headers, numbered section headers, and WHY comments on test sample sizes, parity comparison methods, plausibility thresholds, and reorganization validation logic</done>
</task>

</tasks>

<verification>
- grep "# Purpose:" R/7[0-5]_*.R R/8[0-7]_*.R confirms all 14 scripts have Purpose field
- grep "SECTION.*----" R/7[0-5]_*.R R/8[0-7]_*.R confirms all 14 have section headers
- R/72 slide inventory preserved
- No functional code changes
</verification>

<success_criteria>
- All 6 output scripts and 8 test scripts have 5-field headers
- All 14 scripts have numbered section headers with 4+ trailing dashes
- WHY comments on visualization choices, test methodologies, and validation logic
- Existing content (slide inventories, D-xx references) preserved
- Zero functional code changes
</success_criteria>

<output>
After completion, create `.planning/phases/69-script-documentation/69-06-SUMMARY.md`
</output>
