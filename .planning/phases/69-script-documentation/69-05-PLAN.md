---
phase: 69-script-documentation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - R/60_tiered_same_day_payer.R
  - R/61_tiered_encounter_level.R
  - R/62_tiered_date_level.R
  - R/63_value_audit.R
  - R/64_all_source_missingness.R
  - R/65_uf_insurance_missingness.R
  - R/66_all_site_duplicate_dates.R
  - R/67_multi_source_overlap_detection.R
  - R/68_overlap_classification.R
  - R/69_per_patient_source_detection.R
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "Every payer/QA script (60-69) has a 5-field header block with Purpose, Inputs, Outputs, Dependencies, Requirements"
    - "Every payer/QA script has numbered section headers ending with 4+ dashes"
    - "Payer tiering hierarchy (Medicaid > Medicare > Private) has WHY comments"
    - "Multi-source overlap detection and classification logic has WHY comments"
  artifacts:
    - path: "R/60_tiered_same_day_payer.R"
      provides: "Documented tiered payer with hierarchy rationale"
      contains: "# Purpose:"
    - path: "R/67_multi_source_overlap_detection.R"
      provides: "Documented overlap detection with temporal matching logic"
      contains: "# Purpose:"
    - path: "R/68_overlap_classification.R"
      provides: "Documented overlap classification (Identical/Partial/Distinct)"
      contains: "# Purpose:"
  key_links:
    - from: "R/68_overlap_classification.R"
      to: "R/67_multi_source_overlap_detection.R"
      via: "Dependencies field documents overlap data flow"
      pattern: "# Dependencies:"
---

<objective>
Document payer and QA scripts (60-69) with full headers, section navigation, and WHY comments on payer hierarchy and data quality investigation logic.

Purpose: Payer scripts implement the AMC 8-category payer tiering system with complex hierarchy decisions. QA scripts investigate data quality issues (missingness, duplicates, multi-source overlaps). The hierarchy rationale and overlap classification logic need clear WHY documentation for clinical reviewers.

Output: 10 fully documented payer/QA scripts.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document payer tiering and audit scripts (60-64)</name>
  <files>R/60_tiered_same_day_payer.R, R/61_tiered_encounter_level.R, R/62_tiered_date_level.R, R/63_value_audit.R, R/64_all_source_missingness.R</files>
  <read_first>R/60_tiered_same_day_payer.R, R/61_tiered_encounter_level.R, R/62_tiered_date_level.R, R/63_value_audit.R, R/64_all_source_missingness.R, R/SCRIPT_INDEX.md</read_first>
  <action>
For each of scripts 60-64, apply the full documentation standard (D-01 through D-09).

**Header block standardization (D-01, D-02, D-03):**

60_tiered_same_day_payer.R already has a detailed header with output file listings. Standardize to 5-field format while preserving the output listing. Per-script details:

- **60_tiered_same_day_payer.R**: Purpose = "Tiered same-day payer categorization with AMC 8-category hierarchy for both all-encounter and AV+TH scopes. Produces 12 CSV deliverables (6 frequency + 6 resolution)." Keep existing output file listing in header. Add Inputs, Dependencies fields if missing.
- **61_tiered_encounter_level.R**: Purpose = "Assign AMC 8-category payer tiers to every individual encounter without same-day collapsing -- preserves encounter-level granularity for downstream analysis". Inputs = "PCORnet ENCOUNTER table, AMC_PAYER_LOOKUP from 00_config.R". Outputs = "output/tables/encounter_level_payer.csv".
- **62_tiered_date_level.R**: Purpose = "Expand treatment episodes to daily rows and assign payer tier per patient+date using 3-tier fill cascade (encounter match, date match, modal fill)". Inputs = "treatment_episodes.rds, encounter_level_payer from R/61". Outputs = "output/tables/date_level_payer.csv".
- **63_value_audit.R**: Purpose = "Comprehensive value audit: every distinct value for every column in every PCORnet table, with frequencies and sample values for data quality review". Inputs = "All 13 PCORnet tables via 01_load_pcornet.R". Outputs = "output/value_audit.xlsx".
- **64_all_source_missingness.R**: Purpose = "All-source payer missingness analysis: extends original UFH-only analysis to all 5 OneFlorida+ partner sites". Inputs = "payer_summary from 02_harmonize_payer.R". Outputs = "output/all_source_missingness.xlsx".

**Section headers (D-04, D-05, D-06):**

Convert all existing section-like comments to `# SECTION N: TITLE ----` format. For scripts with existing output file listings (like R/60), keep those as content within the header block -- they are valuable documentation.

**WHY comments (D-07, D-08, D-09):**

- **60**: WHY the hierarchy is Medicaid > Medicare > Private > Other govt > Other > Self-pay > Uninsured > Missing (Medicaid has most restrictive eligibility, indicating strongest coverage signal; Medicare indicates age/disability; hierarchy resolves same-day conflicts in favor of most-informative payer). WHY both all-encounter and AV+TH scopes (AV=ambulatory + TH=hospital are the clinically relevant encounter types for treatment analysis). WHY same-day collapsing is needed (patients may have multiple encounters on same day with different payer codes).
- **61**: WHY encounter-level (no collapsing) differs from same-day (R/60) approach (preserves granularity for analysis that needs individual encounter payer assignments, not just daily summaries).
- **62**: WHY 3-tier fill cascade (direct encounter match is most reliable, same-date match is next, modal fill handles gaps using most common payer for that patient). WHY daily expansion (treatment episodes span multiple days; payer may change during episode).
- **63**: WHY comprehensive value audit (PCORnet CDM data from 5 partner sites has inconsistent coding; audit reveals site-specific patterns and data quality issues).
- **64**: WHY all-site extension (original UFH-only analysis revealed high missingness; need to know if this is UFH-specific or system-wide).
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/60_tiered_same_day_payer.R R/61_tiered_encounter_level.R R/62_tiered_date_level.R R/63_value_audit.R R/64_all_source_missingness.R && grep -c "SECTION.*----" R/60_tiered_same_day_payer.R R/61_tiered_encounter_level.R R/62_tiered_date_level.R R/63_value_audit.R R/64_all_source_missingness.R</automated>
  </verify>
  <acceptance_criteria>
    - All 5 scripts (60-64) contain "# Purpose:" within first 40 lines (R/60 has lengthy existing header)
    - All 5 scripts contain "# Inputs:" and "# Outputs:" and "# Dependencies:" in header
    - All 5 scripts contain at least 2 lines matching "SECTION.*----"
    - R/60_tiered_same_day_payer.R contains a WHY comment about hierarchy ordering (grep for "Medicaid.*restrictive" or "hierarchy" or "eligibility")
    - R/62_tiered_date_level.R contains a WHY comment about the fill cascade (grep for "cascade" or "3-tier" or "modal fill")
  </acceptance_criteria>
  <done>Scripts 60-64 have standardized 5-field headers, numbered section headers, and WHY comments on payer hierarchy, encounter-level vs same-day distinction, 3-tier fill cascade, and audit rationale</done>
</task>

<task type="auto">
  <name>Task 2: Document payer missingness, duplicate, and overlap detection scripts (65-69)</name>
  <files>R/65_uf_insurance_missingness.R, R/66_all_site_duplicate_dates.R, R/67_multi_source_overlap_detection.R, R/68_overlap_classification.R, R/69_per_patient_source_detection.R</files>
  <read_first>R/65_uf_insurance_missingness.R, R/66_all_site_duplicate_dates.R, R/67_multi_source_overlap_detection.R, R/68_overlap_classification.R, R/69_per_patient_source_detection.R, R/SCRIPT_INDEX.md</read_first>
  <action>
For each of scripts 65-69, apply the full documentation standard (D-01 through D-09).

**Header block standardization (D-01, D-02, D-03):**

- **65_uf_insurance_missingness.R**: Purpose = "UFH-specific payer data missingness diagnostic by year and encounter type -- identifies patterns in UF Health's payer data completeness". Inputs = "payer_summary from 02_harmonize_payer.R". Outputs = "output/uf_insurance_missingness.xlsx".
- **66_all_site_duplicate_dates.R**: Purpose = "All-site duplicate date investigation: extends original FLM-only analysis to all 5 sites, identifying same-date duplicate encounters". Inputs = "PCORnet ENCOUNTER table via 00_config". Outputs = "output/all_site_duplicate_dates.xlsx".
- **67_multi_source_overlap_detection.R**: Purpose = "Detect same-date and same-week encounter pairs from different ENCOUNTER.SOURCE values across all encounter types". Inputs = "PCORnet ENCOUNTER table". Outputs = "output/multi_source_overlap.xlsx".
- **68_overlap_classification.R**: Purpose = "Classify multi-source encounter groups (same-date/same-week) as Identical, Partial, or Distinct based on shared column values, with per-site deduplication recommendations". Inputs = "multi_source_overlap.rds from R/67". Outputs = "output/overlap_classification.xlsx".
- **69_per_patient_source_detection.R**: Purpose = "Per-patient source detection by date: identifies which ENCOUNTER.SOURCE values are present for each patient on each date". Inputs = "PCORnet ENCOUNTER table". Outputs = "output/per_patient_source_detection.csv".

**Section headers (D-04, D-05, D-06):**

Convert all existing section-like comments to `# SECTION N: TITLE ----` format.

**WHY comments (D-07, D-08, D-09):**

- **65**: WHY UFH-specific (UF Health was the primary partner site with known missingness issues; targeted analysis before broadening to all sites in R/64). WHY by year and encounter type (missingness patterns may vary temporally and by care setting).
- **66**: WHY duplicate dates matter (same-date encounters from different sources may represent the same clinical event recorded twice, inflating counts). WHY FLM was original focus (Florida Medicaid had the highest duplicate rate in initial analysis).
- **67**: WHY same-date AND same-week (same-date catches exact duplicates; same-week catches encounters that may be the same clinical event reported on slightly different dates). WHY all encounter types not just AV+TH (duplicates occur across all types, not just ambulatory/hospital).
- **68**: WHY Identical/Partial/Distinct classification (Identical overlaps should be deduplicated, Partial overlaps need clinical review, Distinct overlaps are legitimate separate encounters). WHY per-site recommendations (different sites have different data quality patterns).
- **69**: WHY per-patient-per-date granularity (enables downstream analysis to identify patients seen at multiple facilities on the same date, relevant for payer assignment when sources disagree).
  </action>
  <verify>
    <automated>cd /d "C:\Users\Owner\Documents\insurance_investigation" && grep -c "# Purpose:" R/65_uf_insurance_missingness.R R/66_all_site_duplicate_dates.R R/67_multi_source_overlap_detection.R R/68_overlap_classification.R R/69_per_patient_source_detection.R && grep -c "SECTION.*----" R/65_uf_insurance_missingness.R R/66_all_site_duplicate_dates.R R/67_multi_source_overlap_detection.R R/68_overlap_classification.R R/69_per_patient_source_detection.R</automated>
  </verify>
  <acceptance_criteria>
    - All 5 scripts (65-69) contain "# Purpose:" within first 25 lines
    - All 5 scripts contain "# Inputs:" and "# Outputs:" and "# Dependencies:" in header
    - All 5 scripts contain at least 2 lines matching "SECTION.*----"
    - R/68_overlap_classification.R contains a WHY comment about the 3 categories (grep for "Identical" or "Partial" or "Distinct")
    - R/67_multi_source_overlap_detection.R contains a WHY comment about same-week detection (grep for "same.*week" or "slight.*different.*date")
  </acceptance_criteria>
  <done>Scripts 65-69 have standardized 5-field headers, numbered section headers, and WHY comments on missingness patterns, duplicate detection logic, overlap classification categories, and per-patient source analysis</done>
</task>

</tasks>

<verification>
- grep "# Purpose:" R/6[0-9]_*.R confirms all 10 payer/QA scripts have Purpose field
- grep "SECTION.*----" R/6[0-9]_*.R confirms all 10 have section headers
- WHY comments present for: hierarchy ordering, fill cascade, overlap classification, duplicate detection
- No functional code changes
</verification>

<success_criteria>
- All 10 payer/QA scripts have 5-field headers
- All 10 payer/QA scripts have numbered section headers with 4+ trailing dashes
- Payer hierarchy and data quality logic documented with WHY comments
- Zero functional code changes
</success_criteria>

<output>
After completion, create `.planning/phases/69-script-documentation/69-05-SUMMARY.md`
</output>
