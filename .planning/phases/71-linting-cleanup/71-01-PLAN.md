---
phase: 71-linting-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .lintr
autonomous: true
requirements:
  - SAFE-05
must_haves:
  truths:
    - "lintr .lintr config declares magrittr pipe as project standard"
    - "object_usage_linter is disabled (no false positives from dplyr NSE)"
    - "line_length_linter threshold is 150 characters"
    - "return_linter and object_length_linter are disabled"
    - "Re-running lintr after config changes shows ~461 violations (down from 6,187)"
  artifacts:
    - path: ".lintr"
      provides: "Updated lintr configuration with 5 rule changes per D-01 through D-08"
      contains: "pipe_consistency_linter"
  key_links:
    - from: ".lintr"
      to: "lintr::lint_dir()"
      via: "lintr reads .lintr config on every run"
      pattern: "linters_with_defaults"
---

<objective>
Update .lintr configuration to eliminate ~5,726 false-positive and project-convention violations.

Purpose: Configuration changes alone remove 92% of the 6,187 baseline violations without touching any R code. This must happen first (per D-09) so that re-running lintr after Wave 2 code fixes shows the true remaining count.

Output: Updated .lintr file with 5 rule changes (D-01, D-02, D-06, D-07, D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-linting-cleanup/71-CONTEXT.md
@.planning/phases/71-linting-cleanup/71-RESEARCH.md
@.planning/phases/70-automated-formatting/70-LINT-BASELINE.md

<interfaces>
<!-- Current .lintr file that will be modified -->
From .lintr (current state):
```r
linters: linters_with_defaults(
    line_length_linter(120),
    object_name_linter = NULL
  )
exclusions: list(
    "R/archive" = list()
  )
```

<!-- Target .lintr file after modification -->
Target .lintr (after this plan):
```r
linters: linters_with_defaults(
    pipe_consistency_linter = pipe_consistency_linter("%>%"),
    object_usage_linter = NULL,
    line_length_linter = line_length_linter(150L),
    return_linter = NULL,
    object_length_linter = NULL,
    object_name_linter = NULL
  )
exclusions: list(
    "R/archive" = list()
  )
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .lintr configuration with 5 rule changes</name>
  <files>.lintr</files>
  <read_first>
    - .lintr (current config to modify)
    - .planning/phases/71-linting-cleanup/71-CONTEXT.md (locked decisions D-01 through D-08)
    - .planning/phases/71-linting-cleanup/71-RESEARCH.md (exact .lintr syntax from "Code Examples" section)
    - .planning/phases/70-automated-formatting/70-LINT-BASELINE.md (baseline violation counts per rule)
  </read_first>
  <action>
Modify `.lintr` to contain exactly this content (per D-01, D-02, D-06, D-07, D-08):

```r
linters: linters_with_defaults(
    pipe_consistency_linter = pipe_consistency_linter("%>%"),
    object_usage_linter = NULL,
    line_length_linter = line_length_linter(150L),
    return_linter = NULL,
    object_length_linter = NULL,
    object_name_linter = NULL
  )
exclusions: list(
    "R/archive" = list()
  )
```

Changes from current .lintr:
1. ADD `pipe_consistency_linter = pipe_consistency_linter("%>%")` -- per D-01, declares magrittr pipe as standard, eliminates 3,622 violations
2. ADD `object_usage_linter = NULL` -- per D-02, disables linter causing 2,104 false positives from dplyr NSE
3. CHANGE `line_length_linter(120)` to `line_length_linter = line_length_linter(150L)` -- per D-06, bumps threshold from 120 to 150 chars
4. ADD `return_linter = NULL` -- per D-07, disables return_linter (18 violations, explicit return() is project style)
5. ADD `object_length_linter = NULL` -- per D-08, disables object_length_linter (7 violations, PCORnet names are long)
6. KEEP `object_name_linter = NULL` -- already present from Phase 70

Note: The `exclusions` section stays unchanged (R/archive already excluded).

Important syntax note: Use `linter_name = linter_function()` assignment style inside `linters_with_defaults()` for customized linters, and `linter_name = NULL` for disabled linters. The `line_length_linter` uses `150L` (integer literal) per lintr convention.
  </action>
  <verify>
    <automated>grep -c "pipe_consistency_linter" .lintr && grep -c "object_usage_linter = NULL" .lintr && grep -c "150L" .lintr && grep -c "return_linter = NULL" .lintr && grep -c "object_length_linter = NULL" .lintr && grep -c "object_name_linter = NULL" .lintr</automated>
  </verify>
  <acceptance_criteria>
    - .lintr contains the string `pipe_consistency_linter = pipe_consistency_linter("%>%")`
    - .lintr contains the string `object_usage_linter = NULL`
    - .lintr contains the string `line_length_linter = line_length_linter(150L)`
    - .lintr contains the string `return_linter = NULL`
    - .lintr contains the string `object_length_linter = NULL`
    - .lintr contains the string `object_name_linter = NULL` (preserved from Phase 70)
    - .lintr contains the string `"R/archive" = list()` (exclusions preserved)
    - .lintr does NOT contain `line_length_linter(120)` (old threshold replaced)
  </acceptance_criteria>
  <done>
.lintr has all 6 linter customizations (5 new + 1 preserved) inside linters_with_defaults(), with R/archive exclusion intact. Expected impact: ~5,726 violations eliminated by config alone (pipe_consistency 3,622 + object_usage 2,104 + return 18 + object_length 7 = 5,751 disabled; line_length reduction TBD after threshold bump).
  </done>
</task>

</tasks>

<verification>
- .lintr file has correct syntax (parseable by lintr)
- All 6 linter rules present in linters_with_defaults() call
- No leftover `line_length_linter(120)` reference
- exclusions section preserved with R/archive
</verification>

<success_criteria>
.lintr configuration updated with all 5 decision-driven rule changes (D-01, D-02, D-06, D-07, D-08) plus the preserved object_name_linter = NULL from Phase 70. The file is syntactically valid lintr configuration. When lintr is next run on HiPerGator, it should report approximately 461 violations instead of 6,187.
</success_criteria>

<output>
After completion, create `.planning/phases/71-linting-cleanup/71-01-SUMMARY.md`
</output>
