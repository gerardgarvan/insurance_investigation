---
phase: 97-r-60-hot-path-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/60_tiered_same_day_payer.R
  - R/97_validate_r60_migration.R
autonomous: false
requirements: [PERF-01, PERF-02, VALID-02]

must_haves:
  truths:
    - "User can run R/60_tiered_same_day_payer.R and get 12 CSV outputs identical to pre-migration baseline"
    - "User can run R/97_validate_r60_migration.R and see benchmark with old vs new elapsed times and speedup factor"
    - "User can inspect R/60 and see data.table [, by=] syntax replacing group_by+summarise in Section 4"
    - "User can see classify_payer_tier_dt() used instead of classify_payer_tier() in R/60 Section 2"
  artifacts:
    - path: "R/60_tiered_same_day_payer.R"
      provides: "Data.table migrated same-day payer resolution (Sections 2-4)"
      contains: "classify_payer_tier_dt"
    - path: "R/97_validate_r60_migration.R"
      provides: "Combined benchmark + 12-CSV parity validation"
      min_lines: 150
      contains: "system.time"
  key_links:
    - from: "R/60_tiered_same_day_payer.R"
      to: "R/utils/utils_payer.R"
      via: "classify_payer_tier_dt() function call"
      pattern: "classify_payer_tier_dt\\("
    - from: "R/60_tiered_same_day_payer.R"
      to: "R/utils/utils_dt.R"
      via: "ensure_dt(), to_tibble_safe(), get_lookup_dt() helpers"
      pattern: "ensure_dt\\(|to_tibble_safe\\(|get_lookup_dt\\("
    - from: "R/97_validate_r60_migration.R"
      to: "R/60_tiered_same_day_payer.R"
      via: "Runs new data.table path and compares CSVs"
      pattern: "identical\\("
---

<objective>
Migrate R/60_tiered_same_day_payer.R from dplyr to data.table across all 3 operational sections and create R/97 combined benchmark + validation script proving output parity and speedup.

Purpose: R/60 is the hot-path script for same-day payer resolution. Migrating group_by+summarise to data.table [, by=] with setkey() targets 5-20x speedup on production data (100K+ encounter rows). This is the primary performance payoff of the v3.0 data.table adoption.

Output: Modified R/60 using data.table internally (12 CSV outputs unchanged), R/97 validation/benchmark script proving parity and measuring speedup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-r-60-hot-path-migration/97-CONTEXT.md
@.planning/phases/97-r-60-hot-path-migration/97-RESEARCH.md
@.planning/phases/96-classify-payer-tier-dt-implementation/96-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/utils/utils_dt.R:
```r
ensure_dt <- function(df, name = "input", script_name = "unknown")
# Returns: data.table (always; stops on NULL, warns on empty)

to_tibble_safe <- function(dt, name = "output", script_name = "unknown")
# Returns: tibble (always; stops on NULL, warns on empty)

get_lookup_dt <- function(table_name, lookup_list = LOOKUP_TABLES_DT)
# Returns: keyed data.table from LOOKUP_TABLES_DT
```

From R/utils/utils_payer.R:
```r
classify_payer_tier_dt <- function(df, include_dual = TRUE, flm_override = FALSE)
# Returns: tibble with columns: effective_payer, payer_category, tier, tier_rank, dual_eligible
# NOTE: Returns tibble (not data.table) for dplyr pipeline compatibility

classify_payer_tier <- function(df, include_dual = TRUE, flm_override = FALSE)
# Returns: data.frame with same columns as above (dplyr version, kept for benchmark comparison)
```

From R/00_config.R:
```r
AMC_PAYER_LOOKUP  # Named character vector: code -> payer_category (234 entries)
TIER_MAPPING      # Named list: payer_category -> tier_rank integer (8 entries)
LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP  # Keyed data.table: code (key), payer_category
LOOKUP_TABLES_DT$TIER_MAPPING      # Keyed data.table: payer_category (key), tier
```

From R/96_validate_payer_dt.R (validation pattern):
```r
check <- function(description, condition) {
  if (isTRUE(condition)) {
    message(sprintf("[PASS] %s", description))
    pass_count <<- pass_count + 1L
  } else {
    message(sprintf("[FAIL] %s", description))
    fail_count <<- fail_count + 1L
  }
}
```

R/60 CSV output files (12 total):
- Frequency (6): payer_primary_code_freq_all.csv, payer_secondary_code_freq_all.csv, payer_category_summary_all.csv, payer_primary_code_freq_av_th_v2.csv, payer_secondary_code_freq_av_th_v2.csv, payer_category_summary_av_th_v2.csv
- Resolution (6): payer_resolved_detail_all.csv, payer_resolved_detail_av_th.csv, payer_resolved_patient_summary_all.csv, payer_resolved_patient_summary_av_th.csv, payer_resolved_impact_all.csv, payer_resolved_impact_av_th.csv
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate R/60 to data.table (all 3 sections per D-01)</name>
  <files>R/60_tiered_same_day_payer.R</files>
  <read_first>
    - R/60_tiered_same_day_payer.R (current dplyr implementation to migrate)
    - R/utils/utils_payer.R (classify_payer_tier_dt() signature and patterns)
    - R/utils/utils_dt.R (ensure_dt, to_tibble_safe, get_lookup_dt signatures)
    - R/00_config.R lines 3430-3530 (LOOKUP_TABLES_DT structure, AMC_PAYER_LOOKUP keyed table)
    - .planning/phases/97-r-60-hot-path-migration/97-RESEARCH.md (migration patterns and anti-patterns)
  </read_first>
  <action>
Migrate R/60_tiered_same_day_payer.R from dplyr to data.table. Preserve ALL existing message() calls, output file names, column names, column orders, and CSV write locations. Add `library(data.table)` to Section 1 imports.

**Section 2 changes (lines 89-91, trivial swap per D-01):**
Replace `classify_payer_tier()` with `classify_payer_tier_dt()`:
```r
enc <- enc_raw %>%
  classify_payer_tier_dt(include_dual = TRUE, flm_override = FALSE) %>%
  mutate(admit_date_parsed = as.Date(ADMIT_DATE, format = "%Y-%m-%d"))
```
NOTE: classify_payer_tier_dt() returns tibble, so the `mutate()` and downstream `filter()` calls still work. Keep the n_distinct() calls in the Section 2 summary messages using dplyr (these are one-off counts, not hot path).

**Section 3 changes (lines 108-204, rewrite build_frequency_tables per D-01):**
Keep `build_frequency_tables()` as a function (called twice for both scopes, keeps code DRY). Rewrite internals to data.table:

```r
build_frequency_tables <- function(enc_scope, suffix, output_dir) {
  total_enc <- nrow(enc_scope)

  # --- PRIMARY frequency table ---
  enc_dt <- copy(ensure_dt(enc_scope, name = "enc_scope", script_name = "R/60"))
  enc_dt[, code := fcase(
    is.na(PAYER_TYPE_PRIMARY), "<NA>",
    PAYER_TYPE_PRIMARY == "", "<EMPTY>",
    default = PAYER_TYPE_PRIMARY
  )]
  primary_freq_dt <- enc_dt[, .(n = .N), by = .(code)]
  # Keyed join for AMC category (replaces AMC_PAYER_LOOKUP[code] named-vector lookup)
  amc_lookup <- get_lookup_dt("AMC_PAYER_LOOKUP")
  primary_freq_dt[amc_lookup, on = .(code), amc_category := i.payer_category]
  # Missing codes
  primary_freq_dt[code %in% c("<NA>", "<EMPTY>"), amc_category := "Missing"]
  # Prefix fallback for unmapped codes (where amc_category is still NA after join)
  primary_freq_dt[is.na(amc_category), amc_category := fcase(
    startsWith(code, "1"), "Medicare",
    startsWith(code, "2"), "Medicaid",
    startsWith(code, "5") | startsWith(code, "6") | startsWith(code, "7"), "Private",
    startsWith(code, "3") | startsWith(code, "4"), "Other govt",
    startsWith(code, "8"), "Uninsured",
    startsWith(code, "9"), "Other",
    default = "Other"
  )]
  primary_freq_dt[, pct := round(100 * n / total_enc, 2)]
  setorder(primary_freq_dt, -n)
  primary_freq_dt <- primary_freq_dt[, .(code, amc_category, n, pct)]
  primary_freq <- to_tibble_safe(primary_freq_dt, name = "primary_freq", script_name = "R/60")

  # --- SECONDARY frequency table ---
  # (same pattern as primary but uses PAYER_TYPE_SECONDARY)
  enc_dt2 <- copy(ensure_dt(enc_scope, name = "enc_scope", script_name = "R/60"))
  enc_dt2[, code := fcase(
    is.na(PAYER_TYPE_SECONDARY), "<NA>",
    PAYER_TYPE_SECONDARY == "", "<EMPTY>",
    default = PAYER_TYPE_SECONDARY
  )]
  secondary_freq_dt <- enc_dt2[, .(n = .N), by = .(code)]
  secondary_freq_dt[amc_lookup, on = .(code), amc_category := i.payer_category]
  secondary_freq_dt[code %in% c("<NA>", "<EMPTY>"), amc_category := "Missing"]
  secondary_freq_dt[is.na(amc_category), amc_category := fcase(
    startsWith(code, "1"), "Medicare",
    startsWith(code, "2"), "Medicaid",
    startsWith(code, "5") | startsWith(code, "6") | startsWith(code, "7"), "Private",
    startsWith(code, "3") | startsWith(code, "4"), "Other govt",
    startsWith(code, "8"), "Uninsured",
    startsWith(code, "9"), "Other",
    default = "Other"
  )]
  secondary_freq_dt[, pct := round(100 * n / total_enc, 2)]
  setorder(secondary_freq_dt, -n)
  secondary_freq_dt <- secondary_freq_dt[, .(code, amc_category, n, pct)]
  secondary_freq <- to_tibble_safe(secondary_freq_dt, name = "secondary_freq", script_name = "R/60")

  # --- Category-level summary ---
  primary_cat_dt <- primary_freq_dt[, .(n = sum(n)), by = .(amc_category)]
  primary_cat_dt[, `:=`(field = "PRIMARY", pct = round(100 * n / total_enc, 2))]
  primary_cat_dt <- primary_cat_dt[, .(field, amc_category, n, pct)]
  setorder(primary_cat_dt, -n)

  secondary_cat_dt <- secondary_freq_dt[, .(n = sum(n)), by = .(amc_category)]
  secondary_cat_dt[, `:=`(field = "SECONDARY", pct = round(100 * n / total_enc, 2))]
  secondary_cat_dt <- secondary_cat_dt[, .(field, amc_category, n, pct)]
  setorder(secondary_cat_dt, -n)

  category_summary <- to_tibble_safe(
    rbindlist(list(primary_cat_dt, secondary_cat_dt)),
    name = "category_summary", script_name = "R/60"
  )

  # Write CSVs (same filenames, same output_dir)
  write_csv(primary_freq, file.path(output_dir, paste0("payer_primary_code_freq", suffix, ".csv")))
  write_csv(secondary_freq, file.path(output_dir, paste0("payer_secondary_code_freq", suffix, ".csv")))
  write_csv(category_summary, file.path(output_dir, paste0("payer_category_summary", suffix, ".csv")))

  # Preserve ALL existing message() calls exactly as-is
  message(glue("  Written: payer_primary_code_freq{suffix}.csv ({nrow(primary_freq)} rows)"))
  message(glue("  Written: payer_secondary_code_freq{suffix}.csv ({nrow(secondary_freq)} rows)"))
  message(glue("  Written: payer_category_summary{suffix}.csv ({nrow(category_summary)} rows)"))

  # Fallback diagnostics -- use original named vector for this diagnostic count
  # (not on hot path; exact backward compatibility of message output matters more)
  n_fallback_primary <- sum(is.na(AMC_PAYER_LOOKUP[primary_freq$code]) &
    !primary_freq$code %in% c("<NA>", "<EMPTY>"), na.rm = TRUE)
  n_fallback_secondary <- sum(is.na(AMC_PAYER_LOOKUP[secondary_freq$code]) &
    !secondary_freq$code %in% c("<NA>", "<EMPTY>"), na.rm = TRUE)

  message(glue("  Distinct PRIMARY codes: {nrow(primary_freq)} ({n_fallback_primary} via prefix fallback)"))
  message(glue("  Distinct SECONDARY codes: {nrow(secondary_freq)} ({n_fallback_secondary} via prefix fallback)"))
}
```

IMPORTANT: The fallback count diagnostic at the end uses the original AMC_PAYER_LOOKUP named vector (still available per INFRA-04 backward compatibility). This is acceptable because the diagnostic is not on the hot path and exact backward compatibility of the message output matters more than optimizing a one-time count. If you want to avoid the named vector entirely, track a `was_fallback` flag column on primary_freq_dt before applying prefix fallback.

**Section 4 changes (lines 230-319, rewrite resolve_same_day_payer per D-01):**
Replace group_by(ID, admit_date_parsed) %>% summarise(...) with data.table [, by=] aggregation. Use setkey() before aggregation for performance.

```r
resolve_same_day_payer <- function(enc_scope, suffix, output_dir) {
  # Early exit guard (unchanged)
  if (sum(!is.na(enc_scope$admit_date_parsed)) == 0) {
    message(glue("  WARNING: No valid ADMIT_DATE values in {suffix} scope -- skipping resolution"))
    return(invisible(NULL))
  }

  # --- Resolved detail (data.table aggregation) ---
  enc_dt <- copy(ensure_dt(enc_scope, name = "enc_scope", script_name = "R/60"))
  enc_dt <- enc_dt[!is.na(admit_date_parsed)]
  setkey(enc_dt, ID, admit_date_parsed)  # KEY OPTIMIZATION: setkey before [, by=]

  resolved_detail_dt <- enc_dt[, .(
    n_encounters = .N,
    n_distinct_tiers = length(unique(tier)),
    has_flm = any(SOURCE == "FLM", na.rm = TRUE),
    has_special_code = any(PAYER_TYPE_PRIMARY %in% c("93", "14") |
      PAYER_TYPE_SECONDARY %in% c("93", "14"), na.rm = TRUE),
    original_tiers = paste(sort(unique(tier)), collapse = "+"),
    original_codes_primary = paste(sort(unique(na.omit(PAYER_TYPE_PRIMARY))), collapse = ","),
    resolved_payer = fcase(
      any(SOURCE == "FLM", na.rm = TRUE), "Medicaid",
      any(PAYER_TYPE_PRIMARY %in% c("93", "14") |
        PAYER_TYPE_SECONDARY %in% c("93", "14"), na.rm = TRUE), "Medicaid",
      default = tier[which.min(tier_rank)]
    ),
    resolution_reason = {
      n_enc <- .N
      n_tiers <- length(unique(tier))
      fcase(
        n_enc == 1L, "single encounter",
        any(SOURCE == "FLM", na.rm = TRUE), "FLM source override",
        any(PAYER_TYPE_PRIMARY %in% c("93", "14") |
          PAYER_TYPE_SECONDARY %in% c("93", "14"), na.rm = TRUE), "special code override (93/14)",
        n_tiers == 1L, "all encounters same tier",
        default = paste0("tier hierarchy (", n_tiers, " tiers)")
      )
    }
  ), by = .(ID, admit_date_parsed)]

  setnames(resolved_detail_dt, "admit_date_parsed", "ADMIT_DATE")
  setorder(resolved_detail_dt, ID, ADMIT_DATE)
  resolved_detail <- to_tibble_safe(resolved_detail_dt, name = "resolved_detail", script_name = "R/60")

  # CSV A: Per-patient-per-date detail
  write_csv(resolved_detail, file.path(output_dir, paste0("payer_resolved_detail", suffix, ".csv")))
  message(glue("  Written: payer_resolved_detail{suffix}.csv ({nrow(resolved_detail)} rows)"))

  # --- CSV B: Patient-level modal summary ---
  rd_dt <- copy(ensure_dt(resolved_detail, name = "resolved_detail", script_name = "R/60"))
  patient_total_dates <- rd_dt[, .(total_dates = .N), by = .(ID)]

  patient_summary_dt <- rd_dt[, .(n_dates_with_payer = .N), by = .(ID, resolved_payer)]
  setorder(patient_summary_dt, ID, -n_dates_with_payer, resolved_payer)
  patient_summary_dt <- patient_summary_dt[, .SD[1], by = .(ID)]
  setnames(patient_summary_dt, "resolved_payer", "modal_resolved_payer")
  patient_summary_dt[patient_total_dates, on = .(ID), total_dates := i.total_dates]
  patient_summary <- to_tibble_safe(patient_summary_dt, name = "patient_summary", script_name = "R/60")

  write_csv(patient_summary, file.path(output_dir, paste0("payer_resolved_patient_summary", suffix, ".csv")))
  message(glue("  Written: payer_resolved_patient_summary{suffix}.csv ({nrow(patient_summary)} rows)"))

  # --- CSV C: Before vs after impact ---
  enc_dt_impact <- copy(ensure_dt(enc_scope, name = "enc_scope", script_name = "R/60"))
  enc_dt_impact <- enc_dt_impact[!is.na(admit_date_parsed)]

  tier_order <- names(TIER_MAPPING)
  before_dt <- enc_dt_impact[, .(n_encounters_before = .N), by = .(tier)]
  before_dt[, tier_ord := match(tier, tier_order)]
  setorder(before_dt, tier_ord)
  before_dt[, tier_ord := NULL]

  rd_dt2 <- copy(ensure_dt(resolved_detail, name = "resolved_detail", script_name = "R/60"))
  after_dt <- rd_dt2[, .(n_patient_dates_after = .N), by = .(resolved_payer)]
  after_dt[, payer_ord := match(resolved_payer, tier_order)]
  setorder(after_dt, payer_ord)
  after_dt[, payer_ord := NULL]

  # Full join equivalent in data.table: merge with all=TRUE
  impact_dt <- merge(before_dt, after_dt, by.x = "tier", by.y = "resolved_payer", all = TRUE)
  impact_dt[is.na(n_encounters_before), n_encounters_before := 0L]
  impact_dt[is.na(n_patient_dates_after), n_patient_dates_after := 0L]
  total_before <- sum(impact_dt$n_encounters_before)
  total_after <- sum(impact_dt$n_patient_dates_after)
  impact_dt[, `:=`(
    pct_encounters_before = round(100 * n_encounters_before / total_before, 2),
    pct_patient_dates_after = round(100 * n_patient_dates_after / total_after, 2)
  )]
  setnames(impact_dt, "tier", "category")
  impact <- to_tibble_safe(impact_dt, name = "impact", script_name = "R/60")

  write_csv(impact, file.path(output_dir, paste0("payer_resolved_impact", suffix, ".csv")))
  message(glue("  Written: payer_resolved_impact{suffix}.csv ({nrow(impact)} rows)"))

  # Log summary (unchanged logic, use resolved_detail tibble)
  message(glue("  Total patient-dates: {format(nrow(resolved_detail), big.mark=',')}"))
  single_enc <- sum(resolved_detail$n_encounters == 1)
  multi_enc <- sum(resolved_detail$n_encounters > 1)
  message(glue("  Single-encounter dates: {format(single_enc, big.mark=',')} ({round(100*single_enc/nrow(resolved_detail), 1)}%)"))
  message(glue("  Multi-encounter dates: {format(multi_enc, big.mark=',')} ({round(100*multi_enc/nrow(resolved_detail), 1)}%)"))
}
```

**Section 5 (lines 330-368):** Leave completely unchanged. Console summary messages are static strings.

**Anti-patterns to avoid:**
- Do NOT use setDT() -- use copy(ensure_dt()) per Phase 96 pattern
- Do NOT use dplyr::n_distinct() inside data.table [, j, by] -- use length(unique())
- Do NOT forget to convert back to tibble before write_csv() -- use to_tibble_safe()
- Do NOT remove or change ANY message() call text -- messages must be identical for log comparison
- Do NOT change ANY CSV filenames or column orders
- The `n_distinct()` calls in Section 2 (lines 99-100) and Section 5 (lines 343-344) are OUTSIDE data.table context (operating on tibbles) -- these are fine to keep using dplyr
  </action>
  <verify>
    <automated>grep -c "classify_payer_tier_dt" R/60_tiered_same_day_payer.R</automated>
  </verify>
  <acceptance_criteria>
    - R/60_tiered_same_day_payer.R contains "classify_payer_tier_dt(" (Section 2 swap)
    - R/60_tiered_same_day_payer.R contains "library(data.table)" in Section 1
    - R/60_tiered_same_day_payer.R contains "ensure_dt(" at least 3 times (Section 3 + Section 4)
    - R/60_tiered_same_day_payer.R contains "to_tibble_safe(" at least 3 times
    - R/60_tiered_same_day_payer.R contains "get_lookup_dt(" at least 1 time
    - R/60_tiered_same_day_payer.R contains "setkey(" at least 1 time (Section 4 hot path)
    - R/60_tiered_same_day_payer.R contains ", by = .(" at least 2 times (Section 3 + Section 4)
    - R/60_tiered_same_day_payer.R contains "fcase(" at least 2 times
    - R/60_tiered_same_day_payer.R contains ".N" at least 2 times (replaces n() and count())
    - R/60_tiered_same_day_payer.R does NOT contain "setDT(" (anti-pattern)
    - R/60_tiered_same_day_payer.R preserves all 12 CSV filenames (payer_primary_code_freq, payer_secondary_code_freq, payer_category_summary, payer_resolved_detail, payer_resolved_patient_summary, payer_resolved_impact with _all and _av_th suffixes)
    - R/60_tiered_same_day_payer.R preserves Section 5 console summary block unchanged
    - R/60_tiered_same_day_payer.R still contains "build_frequency_tables" as a function definition
    - R/60_tiered_same_day_payer.R still contains "resolve_same_day_payer" as a function definition
  </acceptance_criteria>
  <done>R/60 fully migrated to data.table across all 3 sections: Section 2 uses classify_payer_tier_dt(), Section 3 uses .N/keyed joins/fcase/setorder, Section 4 uses setkey+[,by=] aggregation with fcase resolution. All message() calls preserved. All 12 CSV filenames and column orders preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create R/97 combined benchmark + validation script (per D-02, D-03)</name>
  <files>R/97_validate_r60_migration.R</files>
  <read_first>
    - R/60_tiered_same_day_payer.R (newly migrated script -- read AFTER Task 1)
    - R/96_validate_payer_dt.R (check() helper pattern and validation script structure)
    - R/95_validate_dt_infrastructure.R (validation script structure reference -- first 40 lines for check() pattern)
    - R/utils/utils_payer.R (classify_payer_tier and classify_payer_tier_dt signatures)
    - .planning/phases/97-r-60-hot-path-migration/97-RESEARCH.md (benchmark and CSV comparison patterns)
  </read_first>
  <action>
Create R/97_validate_r60_migration.R following the Phase 95-96 validation script pattern. This script both benchmarks old vs new paths AND validates 12 CSV outputs are identical (per D-02, D-03).

**Script structure:**

```r
# ==============================================================================
# 97_validate_r60_migration.R -- Phase 97 benchmark + parity validation
# ==============================================================================
#
# Purpose:
#   Benchmarks R/60 old (dplyr) vs new (data.table) code paths side-by-side,
#   then diffs all 12 CSV outputs to prove parity. One-time script per D-02.
#
# Usage:
#   source("R/97_validate_r60_migration.R")
#
# Expected output:
#   - Timing comparison (old vs new elapsed seconds)
#   - Speedup factor
#   - [PASS]/[FAIL] for each of 12 CSV files
#
# Requirements:
#   - PERF-01: data.table by= aggregation in R/60
#   - PERF-02: CSV outputs identical pre/post optimization
#   - VALID-02: Runtime benchmark logged
#
# ==============================================================================

source("R/00_config.R")
library(dplyr)
library(data.table)
library(readr)
library(glue)
library(lubridate)
```

**Section 1: Setup**
- check() helper function (same as R/96 pattern: pass_count/fail_count counters)
- Create two temp directories: `dir_old <- tempfile("r60_old_")` and `dir_new <- tempfile("r60_new_")`
- `dir.create(dir_old, recursive = TRUE)` and `dir.create(dir_new, recursive = TRUE)`
- Load encounter data once: `enc_raw <- get_pcornet_table("ENCOUNTER") %>% materialize()`
- Run assert_df_valid() on enc_raw (same as R/60 Section 1b)

**Section 2: OLD path benchmark (dplyr)**
- `message("\n=== BENCHMARK: Old path (dplyr) ===")`
- `time_old <- system.time({ ... })` wrapping:
  - Run classify_payer_tier() (dplyr version) on enc_raw
  - mutate admit_date_parsed
  - Create enc_all and enc_av_th scopes
  - Call the ORIGINAL dplyr build_frequency_tables() logic (inline the function body from pre-migration R/60, writing to dir_old)
  - Call the ORIGINAL dplyr resolve_same_day_payer() logic (inline the function body from pre-migration R/60, writing to dir_old)
- `message(sprintf("Old path runtime: %.2f seconds", time_old["elapsed"]))`
- IMPORTANT: The OLD path must reproduce the EXACT original dplyr logic from R/60 BEFORE migration. Copy the original function bodies from the git history or from the 97-RESEARCH.md code examples. The key functions are:
  - build_frequency_tables() using count(), case_when(), AMC_PAYER_LOOKUP[code] named vector, group_by+summarise
  - resolve_same_day_payer() using group_by(ID, admit_date_parsed) %>% summarise(..., .groups="drop"), count(), left_join

**Section 3: NEW path benchmark (data.table)**
- `message("\n=== BENCHMARK: New path (data.table) ===")`
- `time_new <- system.time({ ... })` wrapping:
  - Run classify_payer_tier_dt() on enc_raw
  - mutate admit_date_parsed
  - Create enc_all and enc_av_th scopes
  - Call the NEW data.table build_frequency_tables() (from migrated R/60, writing to dir_new)
  - Call the NEW data.table resolve_same_day_payer() (from migrated R/60, writing to dir_new)
- `message(sprintf("New path runtime: %.2f seconds", time_new["elapsed"]))`
- NOTE: For the new path, you can source() the migrated R/60's functions if they are self-contained, or inline them. The simplest approach: define both old and new function variants in the validation script itself (with _old and _dt suffixes) to avoid sourcing R/60 which would execute it.

**Section 4: CSV parity validation**
- Define the 12 CSV filenames:
```r
csv_files <- c(
  "payer_primary_code_freq_all.csv",
  "payer_secondary_code_freq_all.csv",
  "payer_category_summary_all.csv",
  "payer_primary_code_freq_av_th_v2.csv",
  "payer_secondary_code_freq_av_th_v2.csv",
  "payer_category_summary_av_th_v2.csv",
  "payer_resolved_detail_all.csv",
  "payer_resolved_detail_av_th.csv",
  "payer_resolved_patient_summary_all.csv",
  "payer_resolved_patient_summary_av_th.csv",
  "payer_resolved_impact_all.csv",
  "payer_resolved_impact_av_th.csv"
)
```
- For each file:
  - Read old and new CSVs with `vroom::vroom(path, show_col_types = FALSE)`
  - Check column names match: `check(sprintf("Columns match: %s", file), identical(names(old_df), names(new_df)))`
  - Check row count match: `check(sprintf("Row count match: %s", file), nrow(old_df) == nrow(new_df))`
  - Check values match using all.equal with tolerance for numeric columns: For each column, if numeric use `all.equal(old_df[[col]], new_df[[col]], tolerance = 1e-8)`, if character/logical use `identical(old_df[[col]], new_df[[col]])`
  - Provide a summary check: `check(sprintf("CSV match: %s", file), all_cols_match)`
- IMPORTANT per Research Pitfall 5: Do NOT use bare identical() on the whole data frame for CSVs with percentage columns. Use column-by-column comparison with all.equal(tolerance=1e-8) for numeric columns (pct, n_encounters_before, pct_encounters_before, etc.)

**Section 5: Benchmark summary**
```r
message(sprintf("\n=== BENCHMARK SUMMARY ==="))
message(sprintf("Old path (dplyr):      %.2f seconds", time_old["elapsed"]))
message(sprintf("New path (data.table): %.2f seconds", time_new["elapsed"]))
speedup <- time_old["elapsed"] / time_new["elapsed"]
message(sprintf("Speedup: %.1fx", speedup))
if (speedup > 1) {
  message(sprintf("data.table is %.1fx faster", speedup))
} else {
  message("WARNING: data.table path was NOT faster (may need investigation)")
}
```

**Section 6: Final summary**
- Print pass_count/fail_count
- Print "All checks passed" or "FAILURES detected" message
- Clean up temp directories: `unlink(dir_old, recursive = TRUE)` and `unlink(dir_new, recursive = TRUE)`

**Anti-patterns:**
- Do NOT use identical() on whole data frames with float columns (Pitfall 5)
- Do NOT source R/60 directly inside this script (it would execute the full script including data loading)
- Do NOT use microbenchmark (system.time() is sufficient per Research recommendation)
  </action>
  <verify>
    <automated>grep -c "system.time" R/97_validate_r60_migration.R</automated>
  </verify>
  <acceptance_criteria>
    - R/97_validate_r60_migration.R exists with at least 150 lines
    - R/97_validate_r60_migration.R contains "system.time" at least 2 times (old + new path benchmarks)
    - R/97_validate_r60_migration.R contains all 12 CSV filenames: payer_primary_code_freq_all, payer_secondary_code_freq_all, payer_category_summary_all, payer_primary_code_freq_av_th_v2, payer_secondary_code_freq_av_th_v2, payer_category_summary_av_th_v2, payer_resolved_detail_all, payer_resolved_detail_av_th, payer_resolved_patient_summary_all, payer_resolved_patient_summary_av_th, payer_resolved_impact_all, payer_resolved_impact_av_th
    - R/97_validate_r60_migration.R contains "check(" function calls for pass/fail tracking
    - R/97_validate_r60_migration.R contains "pass_count" and "fail_count" variables
    - R/97_validate_r60_migration.R contains "Speedup" in benchmark summary output
    - R/97_validate_r60_migration.R contains "all.equal" for numeric column comparison (floating-point tolerance)
    - R/97_validate_r60_migration.R contains "classify_payer_tier(" (old path) and "classify_payer_tier_dt(" (new path)
    - R/97_validate_r60_migration.R contains "tempfile(" for temporary output directories
    - R/97_validate_r60_migration.R contains "source(" only for R/00_config.R (does NOT source R/60)
    - R/97_validate_r60_migration.R contains "unlink(" for temp directory cleanup
  </acceptance_criteria>
  <done>R/97 validation script created with: (1) side-by-side benchmark of old dplyr vs new data.table paths using system.time(), (2) column-by-column comparison of all 12 CSV outputs with floating-point tolerance, (3) speedup factor logged, (4) check() pattern with pass/fail summary matching Phase 95-96 convention.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify R/60 migration and R/97 benchmark + parity on production data</name>
  <files>R/97_validate_r60_migration.R, R/60_tiered_same_day_payer.R</files>
  <action>
User runs R/97_validate_r60_migration.R on HiPerGator production data to verify:
1. All 12 CSV parity checks pass (data.table output identical to dplyr output)
2. Benchmark shows data.table path is faster (speedup > 1.0x)
3. No [FAIL] messages in validation output

This follows the Phase 95-96 checkpoint pattern: automation builds the artifact (Tasks 1-2), user verifies in their environment.
  </action>
  <what-built>
    Migrated R/60_tiered_same_day_payer.R from dplyr to data.table across all 3 operational sections.
    Created R/97_validate_r60_migration.R as combined benchmark + parity validation script.
  </what-built>
  <how-to-verify>
    1. In RStudio on HiPerGator, run: `source("R/97_validate_r60_migration.R")`
    2. Verify ALL check() calls show [PASS] (12 CSV parity checks + column/row count checks)
    3. Verify benchmark summary shows speedup > 1.0x (data.table faster than dplyr)
    4. Note the actual speedup factor reported (e.g., "Speedup: 5.2x")
    5. Optionally: run `source("R/60_tiered_same_day_payer.R")` directly and verify 12 CSVs are written to output/tables/
    6. Optionally: compare a few CSV files manually with the baseline saved before migration

    Expected outcome:
    - All 12 CSV parity checks pass
    - Speedup is 2-20x (exact value depends on data size and HiPerGator load)
    - No [FAIL] messages in output
  </how-to-verify>
  <verify>User reports all checks [PASS] and speedup factor</verify>
  <done>User confirms all R/97 validation checks pass and reports observed speedup factor. Phase 97 requirements PERF-01, PERF-02, VALID-02 are satisfied.</done>
  <resume-signal>Type "approved" with the observed speedup factor, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
Phase 97 verification (run after all tasks):

1. R/60 uses data.table: `grep -c "ensure_dt\|to_tibble_safe\|setkey\|get_lookup_dt" R/60_tiered_same_day_payer.R` returns >= 6
2. R/60 uses classify_payer_tier_dt: `grep -c "classify_payer_tier_dt" R/60_tiered_same_day_payer.R` returns >= 1
3. R/60 does NOT use setDT: `grep -c "setDT" R/60_tiered_same_day_payer.R` returns 0
4. R/97 exists with benchmark: `grep -c "system.time" R/97_validate_r60_migration.R` returns >= 2
5. R/97 checks all 12 CSVs: `grep -c "payer_.*\.csv" R/97_validate_r60_migration.R` returns >= 12
6. User confirms all R/97 checks pass and speedup > 1.0x
</verification>

<success_criteria>
- PERF-01: R/60 Section 4 uses data.table [, by=.(ID, admit_date_parsed)] with setkey() before aggregation
- PERF-02: R/97 validates all 12 CSV outputs are identical (column-by-column with float tolerance) between old dplyr and new data.table paths
- VALID-02: R/97 logs system.time() for both paths and prints speedup factor
- All 12 CSV output filenames and column orders preserved in R/60
- No regressions in R/60 message() output
</success_criteria>

<output>
After completion, create `.planning/phases/97-r-60-hot-path-migration/97-01-SUMMARY.md`
</output>
