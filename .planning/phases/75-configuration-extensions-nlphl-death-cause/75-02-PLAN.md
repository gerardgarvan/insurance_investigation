---
phase: 75-configuration-extensions-nlphl-death-cause
plan: 02
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - CANCER-01
  - DEATH-01
  - DEATH-02
  - QUAL-01

must_haves:
  truths:
    - "Smoke test validates NLPHL ICD-10 codes (C810, C8100, C8109) classify as 'NLPHL'"
    - "Smoke test validates classical HL codes (C811, C812, C819) classify as 'Hodgkin Lymphoma (non-NLPHL)'"
    - "Smoke test validates ICD-9 NLPHL codes (201.4, 201.40, 201.48) classify as 'NLPHL'"
    - "Smoke test validates mutual exclusivity: NLPHL count + classical count = total HL codes tested"
    - "Smoke test validates CANCER_SITE_MAP contains both C810 and C81 entries with correct values"
    - "Smoke test validates DEATH_CAUSE_MAP exists with 30+ entries and UNK fallback"
    - "Smoke test counter updated from [N/17] to [N/18] across all sections"
  artifacts:
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "NLPHL mutual exclusivity + DEATH_CAUSE_MAP validation as Section 13 test group"
      contains: "NLPHL classification"
  key_links:
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/00_config.R"
      via: "source('R/00_config.R') at test start, then classify_codes() calls and CANCER_SITE_MAP/DEATH_CAUSE_MAP lookups"
      pattern: "classify_codes.*NLPHL"
---

<objective>
Add NLPHL classification mutual exclusivity tests and DEATH_CAUSE_MAP validation to the comprehensive smoke test, verifying that Phase 75 configuration changes produce correct classification results.

Purpose: Ensures regression safety for the NLPHL breakout -- any future change to CANCER_SITE_MAP or classify_codes() that breaks mutual exclusivity will be caught immediately. Also validates DEATH_CAUSE_MAP structural integrity before Phase 78 consumption.

Output: Updated R/88_smoke_test_comprehensive.R with a new test section (Section 13: NLPHL & Death Cause Validation) containing 8-10 checks, and all section counters updated from /17 to /18.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-configuration-extensions-nlphl-death-cause/75-CONTEXT.md
@.planning/phases/75-configuration-extensions-nlphl-death-cause/75-RESEARCH.md
@.planning/phases/75-configuration-extensions-nlphl-death-cause/75-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan validates against -->

From R/00_config.R (after Plan 01 changes):
```r
ICD9_NLPHL_CODES <- c("201.4", "201.40", "201.41", ..., "201.48")  # 10 codes
CANCER_SITE_MAP <- c(
  ...,
  "C810" = "NLPHL",                          # 4-char key for NLPHL
  "C81" = "Hodgkin Lymphoma (non-NLPHL)",     # 3-char key for classical HL
  ...
)
DEATH_CAUSE_MAP <- c(
  ...,  # 30+ categories
  "UNK" = "Unknown or Unspecified"
)
```

From R/utils/utils_cancer.R (after Plan 01 changes):
```r
classify_codes <- function(codes) {
  prefix4 <- substr(codes, 1, 4)
  prefix3 <- substr(codes, 1, 3)
  match4 <- CANCER_SITE_MAP[prefix4]
  match3 <- CANCER_SITE_MAP[prefix3]
  categories <- ifelse(!is.na(match4), match4, match3)
  is_icd9_nlphl <- codes %in% ICD9_NLPHL_CODES
  categories[is_icd9_nlphl] <- "NLPHL"
  unname(categories)
}
```

From R/88_smoke_test_comprehensive.R (current structure):
- Uses check(description, condition) function with global passed/failed counters
- 17 test groups numbered [1/17] through [17/17]
- Sections numbered SECTION 2 through SECTION 13
- SECTION 12 = Cross-platform data availability = last functional section before SECTION 13 = Summary
- Config is loaded via source("R/00_config.R") at [2/17]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NLPHL and DEATH_CAUSE_MAP validation section to smoke test</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (full file -- understand section numbering, check() pattern, current [N/17] counters, Section 12 ending, Section 13 Summary position)
    - R/00_config.R (verify ICD9_NLPHL_CODES, CANCER_SITE_MAP C810/C81 entries, DEATH_CAUSE_MAP exist after Plan 01)
    - R/utils/utils_cancer.R (verify classify_codes() has prefix4 logic after Plan 01)
    - .planning/phases/75-configuration-extensions-nlphl-death-cause/75-RESEARCH.md (Pattern 3: Smoke Test Validation example at lines 237-303, Mutual Exclusivity Unit Test at lines 597-661)
  </read_first>
  <action>
Make two changes to R/88_smoke_test_comprehensive.R:

**Change 1: Update all section counters from /17 to /18**

Find and replace all occurrences of `/17]` with `/18]` in message() calls. There are exactly 17 occurrences:
- `[1/17]` becomes `[1/18]`
- `[2/17]` becomes `[2/18]`
- ... through ...
- `[17/17]` becomes `[17/18]`

Also update the banner from `"SMOKE TEST: Comprehensive Pipeline Validation (v2.0)"` to `"SMOKE TEST: Comprehensive Pipeline Validation (v2.1)"`.

**Change 2: Insert new SECTION 13 between the current Section 12 (Cross-platform data availability) and Section 13 (Summary)**

The current SECTION 12 ends around line 594 with closing braces. The current SECTION 13 (SUMMARY) starts with `# SECTION 13: SUMMARY ----`. Renumber current SECTION 13 to SECTION 14.

Insert new section BETWEEN them:

```r
# ==============================================================================
# SECTION 13: NLPHL CLASSIFICATION & DEATH CAUSE VALIDATION ----
# ==============================================================================
# Phase 75 (CANCER-01): Validates NLPHL breakout mutual exclusivity.
# Phase 75 (DEATH-01/DEATH-02): Validates DEATH_CAUSE_MAP structure.

message("\n[18/18] NLPHL classification & death cause validation...")

# --- NLPHL ICD-10 mutual exclusivity ---

# Test data: codes that MUST classify as NLPHL
nlphl_icd10 <- c("C810", "C8100", "C8105", "C8109")
nlphl_icd9 <- c("201.4", "201.40", "201.45", "201.48")

# Test data: codes that MUST classify as classical HL (non-NLPHL)
classical_icd10 <- c("C811", "C8110", "C812", "C8120", "C819", "C8190")
classical_icd9 <- c("201.0", "201.00", "201.5", "201.50", "201.9", "201.90")

# Classify all test codes
nlphl_10_results <- classify_codes(nlphl_icd10)
nlphl_9_results <- classify_codes(nlphl_icd9)
classical_10_results <- classify_codes(classical_icd10)
classical_9_results <- classify_codes(classical_icd9)

# Check 1: NLPHL ICD-10 codes → "NLPHL"
check(
  "ICD-10 C81.0x codes classify as 'NLPHL'",
  all(nlphl_10_results == "NLPHL", na.rm = TRUE)
)

# Check 2: NLPHL ICD-9 codes → "NLPHL"
check(
  "ICD-9 201.4x codes classify as 'NLPHL'",
  all(nlphl_9_results == "NLPHL", na.rm = TRUE)
)

# Check 3: Classical HL ICD-10 codes → "Hodgkin Lymphoma (non-NLPHL)"
check(
  "ICD-10 C81.1-C81.9 codes classify as 'Hodgkin Lymphoma (non-NLPHL)'",
  all(classical_10_results == "Hodgkin Lymphoma (non-NLPHL)", na.rm = TRUE)
)

# Check 4: Classical HL ICD-9 codes → "Hodgkin Lymphoma (non-NLPHL)"
check(
  "ICD-9 201.0-201.9 (except 201.4x) classify as 'Hodgkin Lymphoma (non-NLPHL)'",
  all(classical_9_results == "Hodgkin Lymphoma (non-NLPHL)", na.rm = TRUE)
)

# Check 5: Mutual exclusivity — NLPHL count + classical count = total
all_hl_codes <- c(nlphl_icd10, nlphl_icd9, classical_icd10, classical_icd9)
all_hl_results <- classify_codes(all_hl_codes)
nlphl_count <- sum(all_hl_results == "NLPHL", na.rm = TRUE)
classical_count <- sum(all_hl_results == "Hodgkin Lymphoma (non-NLPHL)", na.rm = TRUE)
total_hl <- length(all_hl_codes)

check(
  glue("Mutual exclusivity: NLPHL ({nlphl_count}) + classical ({classical_count}) = total ({total_hl})"),
  (nlphl_count + classical_count) == total_hl
)

# Check 6: CANCER_SITE_MAP contains C810 entry
check(
  "CANCER_SITE_MAP has 'C810' = 'NLPHL'",
  "C810" %in% names(CANCER_SITE_MAP) && CANCER_SITE_MAP["C810"] == "NLPHL"
)

# Check 7: CANCER_SITE_MAP contains C81 entry with updated name
check(
  "CANCER_SITE_MAP has 'C81' = 'Hodgkin Lymphoma (non-NLPHL)'",
  "C81" %in% names(CANCER_SITE_MAP) && CANCER_SITE_MAP["C81"] == "Hodgkin Lymphoma (non-NLPHL)"
)

# Check 8: ICD9_NLPHL_CODES has expected count
check(
  glue("ICD9_NLPHL_CODES contains 10 codes (found {length(ICD9_NLPHL_CODES)})"),
  length(ICD9_NLPHL_CODES) == 10
)

# --- DEATH_CAUSE_MAP validation ---

# Check 9: DEATH_CAUSE_MAP exists and has sufficient coverage
check(
  glue("DEATH_CAUSE_MAP has >= 30 entries (found {length(DEATH_CAUSE_MAP)})"),
  exists("DEATH_CAUSE_MAP") && length(DEATH_CAUSE_MAP) >= 30
)

# Check 10: DEATH_CAUSE_MAP has UNK fallback
check(
  "DEATH_CAUSE_MAP has 'UNK' = 'Unknown or Unspecified'",
  "UNK" %in% names(DEATH_CAUSE_MAP) &&
    DEATH_CAUSE_MAP["UNK"] == "Unknown or Unspecified"
)

# Check 11: DEATH_CAUSE_MAP covers major ICD-10 chapters
major_chapters <- c("C81", "I25", "J44", "E11", "G30")
chapter_hits <- sum(major_chapters %in% names(DEATH_CAUSE_MAP))
check(
  glue("DEATH_CAUSE_MAP covers major ICD-10 chapters ({chapter_hits}/5 present)"),
  chapter_hits == 5
)
```

Then rename current `# SECTION 13: SUMMARY ----` to `# SECTION 14: SUMMARY ----`.

Also update the validated requirements list at the end of the summary section to include:
```r
message("  * CANCER-01: NLPHL mutual exclusivity (classify_codes)")
message("  * DEATH-01/02: DEATH_CAUSE_MAP structure and coverage")
```
  </action>
  <verify>
    <automated>Rscript R/88_smoke_test_comprehensive.R 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains `[18/18]` (updated counter)
    - R/88_smoke_test_comprehensive.R contains `[1/18]` (first counter updated)
    - R/88_smoke_test_comprehensive.R does NOT contain `/17]` in any message() call
    - R/88_smoke_test_comprehensive.R contains `SECTION 13: NLPHL CLASSIFICATION`
    - R/88_smoke_test_comprehensive.R contains `SECTION 14: SUMMARY`
    - R/88_smoke_test_comprehensive.R contains `classify_codes(nlphl_icd10)` (NLPHL test)
    - R/88_smoke_test_comprehensive.R contains `DEATH_CAUSE_MAP` validation check
    - R/88_smoke_test_comprehensive.R contains `"UNK" %in% names(DEATH_CAUSE_MAP)`
    - R/88_smoke_test_comprehensive.R contains 11 new check() calls in Section 13
    - `Rscript R/88_smoke_test_comprehensive.R` exits with code 0 (all checks pass)
    - Smoke test output contains "PASS: ICD-10 C81.0x codes classify as 'NLPHL'"
    - Smoke test output contains "PASS: Mutual exclusivity"
    - Smoke test output contains "PASS: DEATH_CAUSE_MAP has >= 30 entries"
  </acceptance_criteria>
  <done>Smoke test R/88_smoke_test_comprehensive.R updated from 17 to 18 test groups. Section 13 validates NLPHL mutual exclusivity (8 checks: ICD-10 NLPHL, ICD-9 NLPHL, ICD-10 classical, ICD-9 classical, mutual exclusivity sum, CANCER_SITE_MAP C810 entry, CANCER_SITE_MAP C81 entry, ICD9_NLPHL_CODES count) and DEATH_CAUSE_MAP structure (3 checks: entry count >= 30, UNK fallback, major chapter coverage). All checks pass.</done>
</task>

</tasks>

<verification>
1. `Rscript R/88_smoke_test_comprehensive.R` exits 0 (all checks pass including new Section 13)
2. Smoke test output contains "PASS" for all 11 new checks (no FAIL lines)
3. No `/17]` counters remain in message() calls
4. SECTION 14: SUMMARY correctly prints validated requirements including CANCER-01 and DEATH-01/02
</verification>

<success_criteria>
- Smoke test passes with 0 failures, including 11 new NLPHL and DEATH_CAUSE_MAP checks
- All section counters updated from /17 to /18
- Section 13 validates NLPHL mutual exclusivity at CODE level (not patient level)
- Section 13 validates DEATH_CAUSE_MAP exists with adequate coverage
- Validated requirements list includes CANCER-01 and DEATH-01/02
</success_criteria>

<output>
After completion, create `.planning/phases/75-configuration-extensions-nlphl-death-cause/75-02-SUMMARY.md`
</output>
