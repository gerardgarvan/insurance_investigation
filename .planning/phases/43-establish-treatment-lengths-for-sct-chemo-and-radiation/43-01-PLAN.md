---
phase: 43-establish-treatment-lengths-for-sct-chemo-and-radiation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/43_treatment_durations.R
autonomous: false
requirements:
  - PHASE-43-GOAL
must_haves:
  truths:
    - "Per-patient treatment duration metrics exist for all 4 types (Chemotherapy, Radiation, SCT, Immunotherapy)"
    - "Each patient-type row shows first date, last date, span in days, distinct date count, and episode count"
    - "Single-date patients appear with span=0 and count=1 (not filtered out)"
    - "Episodes are split at 90-day gaps between consecutive treatment dates"
    - "Console output shows median, IQR, and range per treatment type during execution"
    - "Styled xlsx report contains per-type sheets with TREATMENT_TYPE_COLORS styling"
    - "Distribution PNG shows treatment duration spread by type"
    - "RDS artifact saved with one row per patient per treatment type"
  artifacts:
    - path: "R/43_treatment_durations.R"
      provides: "Treatment duration extraction, computation, and output script"
      min_lines: 350
    - path: "output/treatment_durations.xlsx"
      provides: "Styled xlsx report with per-type duration sheets"
    - path: "output/treatment_duration_distributions.png"
      provides: "Histogram + boxplot of duration distributions by type"
  key_links:
    - from: "R/43_treatment_durations.R"
      to: "R/00_config.R"
      via: "source() for TREATMENT_CODES and CONFIG"
      pattern: 'source\("R/00_config\.R"\)'
    - from: "R/43_treatment_durations.R"
      to: "R/01_load_pcornet.R"
      via: "source() for get_pcornet_table()"
      pattern: 'source\("R/01_load_pcornet\.R"\)'
    - from: "R/43_treatment_durations.R"
      to: "7 PCORnet tables"
      via: "get_pcornet_table() calls"
      pattern: 'get_pcornet_table\("PROCEDURES"\)'
---

<objective>
Create R/43_treatment_durations.R that extracts ALL treatment dates from 7 PCORnet tables for Chemotherapy, Radiation, SCT, and Immunotherapy, calculates per-patient duration metrics (first-to-last span, distinct date count, episode count with 90-day gap threshold), and produces three outputs: RDS artifact, styled xlsx report, and distribution visualization PNG.

Purpose: Establish treatment length estimates that enable downstream analysis of treatment patterns, payer-stratified duration comparisons, and clinical validation of treatment episode boundaries.

Output: R/43_treatment_durations.R script, treatment_durations.xlsx, treatment_duration_distributions.png, treatment_durations.rds
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-establish-treatment-lengths-for-sct-chemo-and-radiation/43-CONTEXT.md
@.planning/phases/43-establish-treatment-lengths-for-sct-chemo-and-radiation/43-RESEARCH.md

@R/00_config.R
@R/01_load_pcornet.R
@R/10_treatment_payer.R
@R/41_combine_reports.R
@R/42_treatment_codes_resolved.R

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/00_config.R (TREATMENT_CODES vectors by type):
- Chemo: chemo_hcpcs, chemo_rxnorm, chemo_icd9, chemo_icd10pcs_prefixes, chemo_dx_icd10, chemo_dx_icd9, chemo_drg, chemo_revenue
- Radiation: radiation_cpt, radiation_icd9, radiation_icd10pcs_prefixes, radiation_dx_icd10, radiation_dx_icd9, radiation_drg, radiation_revenue
- SCT: sct_cpt, sct_hcpcs, sct_icd9, sct_icd10pcs, sct_dx_icd10, sct_drg, sct_revenue
- Immunotherapy: cart_icd10pcs_prefixes (NOTE: no dedicated immuno HCPCS/DRG/DX vectors exist -- see action notes)

From R/00_config.R (CONFIG paths):
- CONFIG$output_dir = "output"
- CONFIG$cache$outputs_dir = "/blue/erin.mobley-hl.bcu/clean/rds/outputs"

From R/01_load_pcornet.R:
- get_pcornet_table(table_name) returns tibble or NULL

From R/10_treatment_payer.R (extraction pattern):
- compute_payer_at_chemo(): 7-source extraction with min() aggregation (lines 96-246)
- compute_payer_at_radiation(): 4-source extraction (lines 259-376)
- compute_payer_at_sct(): 4-source extraction (lines 389-501)
- nrow_or_0() helper function
- Pattern: NULL guard -> filter -> group_by(ID) -> summarise(min()) -> compact() -> bind_rows() -> stacked -> min per patient

From R/42_treatment_codes_resolved.R (openxlsx2 styling):
- TREATMENT_TYPE_COLORS list with $fill and $font per type
- write_resolved_xlsx() pattern: wb_workbook() -> add_worksheet() -> title row -> headers -> data -> styling -> save()

From R/41_combine_reports.R (workbook structure):
- Summary sheet pattern with sections, total rows
- Per-category sheets with title, subtitle, headers, bulk data write
- Range-based styling (not cell-by-cell)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/43_treatment_durations.R -- full treatment duration analysis script</name>
  <files>R/43_treatment_durations.R</files>
  <read_first>
    R/00_config.R (lines 29-75 for CONFIG paths, lines 412-659 for TREATMENT_CODES vectors)
    R/10_treatment_payer.R (lines 96-246 for chemo extraction pattern, lines 259-376 for radiation, lines 389-501 for SCT -- CRITICAL: this is the source-of-truth for multi-source date extraction)
    R/01_load_pcornet.R (for get_pcornet_table() signature and backend dispatching)
    R/41_combine_reports.R (lines 1-40 for TREATMENT_TYPE_COLORS, lines 107-367 for workbook creation pattern)
    R/42_treatment_codes_resolved.R (lines 52-134 for write_resolved_xlsx pattern -- simpler version)
  </read_first>
  <action>
Create R/43_treatment_durations.R with 6 sections. The script sources R/00_config.R and R/01_load_pcornet.R at the top.

**SECTION 1: Setup and Configuration**

```r
suppressPackageStartupMessages({
  library(dplyr)
  library(stringr)
  library(glue)
  library(purrr)
  library(ggplot2)
  library(scales)
  library(openxlsx2)
})
source("R/00_config.R")
source("R/01_load_pcornet.R")
```

Define constants:
- `GAP_THRESHOLD <- 90` (per D-05: 90-day gap defines new episode)
- `TREATMENT_TYPES <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy")` (per D-12)
- `OUTPUT_XLSX <- file.path(CONFIG$output_dir, "treatment_durations.xlsx")`
- `OUTPUT_PNG <- file.path(CONFIG$output_dir, "treatment_duration_distributions.png")`
- `OUTPUT_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_durations.rds")`
- Copy TREATMENT_TYPE_COLORS from R/42_treatment_codes_resolved.R (lines 40-47) verbatim
- `nrow_or_0 <- function(df) if (is.null(df)) 0L else nrow(df)`

**SECTION 2: Multi-Source Date Extraction Functions**

Create `extract_all_dates(type)` function that returns a tibble with columns `ID` and `treatment_date` containing ALL distinct treatment dates (not min/max).

For each type, replicate the exact source queries from R/10_treatment_payer.R but REMOVE the `group_by(ID) %>% summarise(... = min(...))` aggregation step. Instead, just `select(ID, treatment_date = <DATE_COL>)` from each source.

**Chemotherapy extraction** (extend compute_payer_at_chemo, lines 96-246):
- PROCEDURES: PX_TYPE=="CH" & PX %in% chemo_hcpcs, PX_TYPE=="09" & PX %in% chemo_icd9, PX_TYPE=="10" & str_detect(PX, chemo_icd10pcs_rx), PX_TYPE=="RE" & PX %in% chemo_revenue -> select(ID, treatment_date = PX_DATE)
- PRESCRIBING: RXNORM_CUI %in% chemo_rxnorm -> mutate(treatment_date = coalesce(RX_ORDER_DATE, RX_START_DATE)) -> select(ID, treatment_date)
- DIAGNOSIS: (DX_TYPE=="10" & DX %in% chemo_dx_icd10) | (DX_TYPE=="09" & DX %in% chemo_dx_icd9) -> select(ID, treatment_date = DX_DATE)
- ENCOUNTER: DRG %in% chemo_drg -> select(ID, treatment_date = ADMIT_DATE)
- DISPENSING: RXNORM_CUI %in% chemo_rxnorm -> select(ID, treatment_date = DISPENSE_DATE)
- MED_ADMIN: RXNORM_CUI %in% chemo_rxnorm -> select(ID, treatment_date = MEDADMIN_START_DATE)
- TUMOR_REGISTRY_ALL: cols CHEMO_START_DATE_SUMMARY / DT_CHEMO -> pivot to long -> select(ID, treatment_date)

CRITICAL: After all chemo extraction, EXCLUDE immunotherapy-specific codes that are mixed into chemo vectors. Specifically:
- Z51.12 and V58.12 are under chemo_dx_icd10/icd9 but are immunotherapy encounter codes. For chemo extraction, still include them (they are encounter codes for chemo visits that may also include immunotherapy -- the distinction is ambiguous in PCORnet). Per D-13, all chemo codes pooled together.
- J9042 (brentuximab) and J9299 (nivolumab) are under chemo_hcpcs. These ARE checkpoint inhibitors/ADCs but are classified as chemo HCPCS in the config. Per D-13, keep them in chemo. They will NOT be double-counted in immunotherapy (immunotherapy only uses cart_icd10pcs_prefixes for PROCEDURES extraction).

**Radiation extraction** (extend compute_payer_at_radiation, lines 259-376):
- PROCEDURES: radiation_cpt (PX_TYPE=="CH"), radiation_icd9 (PX_TYPE=="09"), radiation_icd10pcs_prefixes (PX_TYPE=="10", str_detect), radiation_revenue (PX_TYPE=="RE") -> select(ID, treatment_date = PX_DATE)
- DIAGNOSIS: radiation_dx_icd10 (DX_TYPE=="10"), radiation_dx_icd9 (DX_TYPE=="09") -> select(ID, treatment_date = DX_DATE)
- ENCOUNTER: radiation_drg -> select(ID, treatment_date = ADMIT_DATE)
- TUMOR_REGISTRY_ALL: cols RAD_START_DATE_SUMMARY / DT_RAD -> select(ID, treatment_date)

**SCT extraction** (extend compute_payer_at_sct, lines 389-501):
- PROCEDURES: sct_cpt + sct_hcpcs (PX_TYPE=="CH"), sct_icd9 (PX_TYPE=="09"), sct_icd10pcs (PX_TYPE=="10", exact match not str_detect), sct_revenue (PX_TYPE=="RE") -> select(ID, treatment_date = PX_DATE)
- DIAGNOSIS: sct_dx_icd10 (DX_TYPE=="10" only) -> select(ID, treatment_date = DX_DATE)
- ENCOUNTER: sct_drg -> select(ID, treatment_date = ADMIT_DATE)
- TUMOR_REGISTRY_ALL: cols DT_HTE / DT_SCT / SCT_DATE / BMT_DATE / TRANSPLANT_DATE / HCT_DATE / DT_TRANSPLANT -> select(ID, treatment_date)

**Immunotherapy extraction** (NEW -- no existing function in R/10):
- PROCEDURES: PX_TYPE=="10" & str_detect(PX, cart_icd10pcs_rx) where cart_icd10pcs_rx is built from TREATMENT_CODES$cart_icd10pcs_prefixes with paste0("^(", paste(..., collapse="|"), ")") -> select(ID, treatment_date = PX_DATE)
- ENCOUNTER: DRG %in% c("018") (DRG 018 = Chimeric antigen receptor T-cell immunotherapy) -> select(ID, treatment_date = ADMIT_DATE). Note: DRG 018 is NOT currently in TREATMENT_CODES -- hardcode it with a comment explaining why (CAR T-cell DRG mentioned in config comments but not in a vector).
- No PRESCRIBING/DISPENSING/MED_ADMIN/DIAGNOSIS/TUMOR_REGISTRY sources for immunotherapy (the DX codes Z51.12/V58.12 are too generic -- they cover both chemo and immunotherapy encounters, and are already captured under chemo per D-13).

For each type, after stacking all sources with `compact() %>% bind_rows()`:
1. Filter out `is.na(treatment_date)` rows
2. Apply `distinct(ID, treatment_date)` to deduplicate same-day records across sources (CRITICAL per Pitfall 2)
3. `arrange(ID, treatment_date)`
4. Log source-level counts: `message(glue("  {type} date sources: PX={nrow_or_0(px_dates)}, ..."))`
5. Log total: `message(glue("  {type}: {n_distinct(result$ID)} patients, {nrow(result)} distinct patient-dates"))`

**SECTION 3: Duration and Episode Calculation**

Create `calculate_durations_and_episodes(dates_df, gap_threshold = GAP_THRESHOLD)` function.

Implementation per D-01, D-02, D-03, D-04, D-05:
```r
calculate_durations_and_episodes <- function(dates_df, gap_threshold = GAP_THRESHOLD) {
  dates_df %>%
    group_by(ID) %>%
    arrange(treatment_date, .by_group = TRUE) %>%
    mutate(
      days_since_prev = as.numeric(treatment_date - lag(treatment_date)),
      new_episode = is.na(days_since_prev) | days_since_prev >= gap_threshold,
      episode_id = cumsum(new_episode)
    ) %>%
    # Per-episode summary first
    group_by(ID, episode_id) %>%
    summarise(
      episode_first_date = min(treatment_date),
      episode_last_date = max(treatment_date),
      episode_span_days = as.numeric(max(treatment_date) - min(treatment_date)),
      episode_distinct_dates = n(),
      .groups = "drop_last"
    ) %>%
    # Then per-patient summary (D-07 output shape)
    summarise(
      first_treatment_date = min(episode_first_date),
      last_treatment_date = max(episode_last_date),
      overall_span_days = as.numeric(max(episode_last_date) - min(episode_first_date)),
      distinct_treatment_dates = sum(episode_distinct_dates),
      episode_count = n(),
      .groups = "drop"
    )
}
```

Per D-03: Single-date patients naturally produce span=0, count=1, episodes=1. Do NOT filter them out.

**SECTION 4: Main Execution Loop + Console Stats + RDS Save**

```r
message("=== Phase 43: Treatment Duration Analysis ===\n")
```

Loop over TREATMENT_TYPES, calling extract_all_dates() then calculate_durations_and_episodes() for each type. Collect results into a list and a combined tibble with `treatment_type` column.

For each type, log summary stats per D-10:
```r
log_duration_stats <- function(durations_df, type_name) {
  stats <- durations_df %>%
    summarise(
      n = n(),
      median_span = median(overall_span_days, na.rm = TRUE),
      q1_span = quantile(overall_span_days, 0.25, na.rm = TRUE),
      q3_span = quantile(overall_span_days, 0.75, na.rm = TRUE),
      min_span = min(overall_span_days, na.rm = TRUE),
      max_span = max(overall_span_days, na.rm = TRUE),
      median_dates = median(distinct_treatment_dates, na.rm = TRUE),
      median_episodes = median(episode_count, na.rm = TRUE)
    )
  message(glue("\n  {type_name} Summary:"))
  message(glue("    Patients: {stats$n}"))
  message(glue("    Span (days): median={stats$median_span}, IQR=[{stats$q1_span}, {stats$q3_span}], range=[{stats$min_span}, {stats$max_span}]"))
  message(glue("    Distinct dates: median={stats$median_dates}"))
  message(glue("    Episodes: median={stats$median_episodes}"))
}
```

After all types processed, combine into `all_durations` tibble (bind_rows with treatment_type column) and save RDS:
```r
saveRDS(all_durations, OUTPUT_RDS)
message(glue("\nRDS saved: {OUTPUT_RDS} ({nrow(all_durations)} rows)"))
```

**SECTION 5: Styled XLSX Report (per D-08)**

Per D-06/D-11 (Claude's Discretion): Create multi-sheet workbook with:
- Sheet 1: "Summary" -- overall stats table (one row per treatment type showing N patients, median span, IQR, median dates, median episodes)
- Sheets 2-5: Per-type detail sheets ("Chemotherapy Durations", "Radiation Durations", "SCT Durations", "Immunotherapy Durations")

Summary sheet structure (following R/41_combine_reports.R pattern):
- Row 1: Title "Treatment Duration Analysis" (Calibri 16pt bold)
- Row 2: Subtitle with date
- Row 4: Headers: "Treatment Type", "Patients", "Median Span (days)", "IQR", "Median Distinct Dates", "Median Episodes"
- Row 5+: One row per type
- Header fill: "FF374151" with white font (same as R/41)

Per-type detail sheets (following R/42 write_resolved_xlsx pattern):
- Row 1: Title "{Type} Treatment Durations ({N} patients)" (Calibri 16pt bold, merged A1:F1)
- Row 2: Headers: "Patient ID", "First Date", "Last Date", "Span (days)", "Distinct Dates", "Episodes"
- Header styling: TREATMENT_TYPE_COLORS[type]$fill background, TREATMENT_TYPE_COLORS[type]$font color, bold
- Row 3+: Data (bulk write with wb$add_data, col_names = FALSE)
- Number formatting: "#,##0" on Span/Distinct Dates/Episodes columns
- Column widths: c(20, 15, 15, 15, 15, 12)

Save to OUTPUT_XLSX.

**SECTION 6: Distribution Visualization (per D-09)**

Per Claude's Discretion: Create BOTH histogram and boxplot as a 2-panel vertical layout using patchwork-style approach (or just two separate ggsave calls, or use cowplot::plot_grid if available, or simply facet_wrap).

Simpler approach -- single PNG with two plots stacked:
- Top panel: faceted histogram of overall_span_days by treatment_type (free x scales, bins=30, fill="steelblue")
- Bottom panel: boxplot of overall_span_days by treatment_type (fill by type using manual scale with colors from TREATMENT_TYPE_COLORS hex values)

Use ggplot2 only (no extra dependencies). Save with:
```r
ggsave(OUTPUT_PNG, width = 12, height = 10, dpi = 300)
```

If cowplot/patchwork are not available, create TWO separate PNGs:
- `treatment_duration_histogram.png`
- `treatment_duration_boxplot.png`

Or use a single PNG with the boxplot only (simpler, more informative for comparing types).

Recommended: Single boxplot PNG is cleanest. Add jittered points for small N types (SCT, Immunotherapy).

```r
p <- ggplot(all_durations, aes(x = treatment_type, y = overall_span_days, fill = treatment_type)) +
  geom_boxplot(outlier.alpha = 0.3) +
  geom_jitter(width = 0.2, alpha = 0.1, size = 0.5) +
  labs(
    title = "Treatment Duration Distribution by Type",
    subtitle = glue("N = {nrow(all_durations)} patients across {length(unique(all_durations$treatment_type))} treatment types"),
    x = "Treatment Type",
    y = "Duration (days, first to last treatment date)"
  ) +
  scale_fill_manual(values = c(
    "Chemotherapy" = "#DCEEFB", "Radiation" = "#DDF4E1",
    "SCT" = "#FFF4D6", "Immunotherapy" = "#E8DCF4"
  )) +
  theme_minimal(base_size = 12) +
  theme(legend.position = "none")
ggsave(OUTPUT_PNG, plot = p, width = 10, height = 6, dpi = 300)
```

Final message:
```r
message(glue("\n=== Phase 43 Complete ==="))
message(glue("Outputs:"))
message(glue("  RDS:  {OUTPUT_RDS}"))
message(glue("  XLSX: {OUTPUT_XLSX}"))
message(glue("  PNG:  {OUTPUT_PNG}"))
```

**IMPORTANT IMPLEMENTATION NOTES:**

1. For TUMOR_REGISTRY_ALL extraction, use the intersect() pattern from R/10_treatment_payer.R (lines 177-197) to handle columns that may not exist. For ALL dates, do NOT aggregate -- instead pivot each TUMOR_REGISTRY row's date columns to long format. If a row has both CHEMO_START_DATE_SUMMARY and DT_CHEMO, treat them as two separate date observations (they may represent different dates from different abstraction sources).

2. For immunotherapy, the only PROCEDURES-level codes are cart_icd10pcs_prefixes. There are no immunotherapy-specific RXNORM, DISPENSING, or MED_ADMIN vectors in TREATMENT_CODES. DRG 018 (Chimeric Antigen Receptor T-cell Immunotherapy) should be hardcoded as `immuno_drg <- c("018")` with a comment explaining it is from CMS MS-DRG classification.

3. All dates MUST be coerced to Date type via `as.Date()` before arithmetic. DuckDB returns dates as Date class already, but TUMOR_REGISTRY fields may be character or POSIXct depending on source.

4. The combined RDS artifact (per D-07) should have columns: ID, treatment_type, first_treatment_date, last_treatment_date, overall_span_days, distinct_treatment_dates, episode_count.
  </action>
  <verify>
    <automated>Rscript -e "f <- readLines('R/43_treatment_durations.R'); cat('Lines:', length(f), '\n'); stopifnot(length(f) >= 350); stopifnot(any(grepl('extract_all_dates|extract_all.*dates', f))); stopifnot(any(grepl('calculate_durations_and_episodes', f))); stopifnot(any(grepl('GAP_THRESHOLD.*90', f))); stopifnot(any(grepl('saveRDS', f))); stopifnot(any(grepl('wb_workbook', f))); stopifnot(any(grepl('ggsave', f))); stopifnot(any(grepl('distinct\\(ID.*treatment_date', f))); stopifnot(any(grepl('cumsum\\(new_episode', f))); cat('All structural checks passed\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/43_treatment_durations.R exists with 350+ lines
    - File contains `source("R/00_config.R")` and `source("R/01_load_pcornet.R")`
    - File contains `GAP_THRESHOLD <- 90`
    - File contains function `extract_all_dates` or equivalent that queries 7 PCORnet tables
    - File contains `distinct(ID, treatment_date)` deduplication after bind_rows
    - File contains `calculate_durations_and_episodes` function with `lag(treatment_date)` and `cumsum(new_episode)` pattern
    - File contains `log_duration_stats` function that prints median, IQR, range
    - File contains `saveRDS(` call saving to CONFIG$cache$outputs_dir path
    - File contains `wb_workbook()` call for xlsx output
    - File contains TREATMENT_TYPE_COLORS with Chemotherapy/Radiation/SCT/Immunotherapy entries
    - File contains `ggsave(` call for PNG output
    - File contains all 4 treatment types: "Chemotherapy", "Radiation", "SCT", "Immunotherapy"
    - File does NOT contain `filter(distinct_treatment_dates > 1)` or `filter(overall_span_days > 0)` (would violate D-03)
    - Immunotherapy extraction includes cart_icd10pcs_prefixes and DRG 018
  </acceptance_criteria>
  <done>R/43_treatment_durations.R is a complete, runnable script that extracts all treatment dates from 7 PCORnet tables for 4 types, computes duration/episode metrics with 90-day gap threshold, saves RDS artifact, writes styled xlsx with summary + per-type sheets, produces distribution boxplot PNG, and logs summary statistics to console.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify treatment duration outputs on HiPerGator</name>
  <files>output/treatment_durations.xlsx, output/treatment_duration_distributions.png</files>
  <read_first>
    R/43_treatment_durations.R (the script just created)
  </read_first>
  <action>
Run the complete script on HiPerGator and verify outputs. Execute: `source("R/43_treatment_durations.R")`.

What was built:
- R/43_treatment_durations.R: Complete treatment duration analysis script that extracts ALL treatment dates from 7 PCORnet tables for Chemotherapy, Radiation, SCT, and Immunotherapy, deduplicates same-day records across sources, calculates per-patient first-to-last span, distinct date count, and episode count (90-day gap threshold), saves RDS artifact with one row per patient per treatment type, writes styled xlsx report with summary + per-type detail sheets, produces distribution boxplot PNG, and prints summary statistics (median, IQR, range) per type to console.

How to verify:
1. In RStudio on HiPerGator, run: `source("R/43_treatment_durations.R")`
2. Check console output for:
   - Per-type source counts (e.g., "Chemo date sources: PX=..., RX=..., DX=..., DRG=..., DISP=..., MA=..., TR=...")
   - Deduplication log showing before/after counts
   - Summary stats per type (median span, IQR, range, median dates, median episodes)
3. Verify clinical plausibility:
   - Chemotherapy median span should be 100-200 days (4-6 ABVD cycles of 28 days each)
   - SCT median span should be 0-30 days (often single-day infusion)
   - Radiation median span should be 20-50 days (daily fractions over 3-5 weeks)
   - Most patients should have episode_count = 1 (single treatment course)
4. Open output/treatment_durations.xlsx:
   - Verify Summary sheet has one row per type with stats
   - Verify per-type sheets have colored headers and patient-level data
   - Verify single-date patients appear with span=0
5. Open output/treatment_duration_distributions.png:
   - Verify boxplot shows all 4 treatment types
   - Verify SCT has tight distribution near 0 (expected)
6. Verify RDS: `readRDS()` from outputs_dir path and check columns match (ID, treatment_type, first_treatment_date, last_treatment_date, overall_span_days, distinct_treatment_dates, episode_count)

Resume: Type "approved" or describe any issues with the outputs.
  </action>
  <verify>User confirms outputs are clinically plausible and correctly formatted</verify>
  <acceptance_criteria>
    - Script runs to completion without errors on HiPerGator
    - Console shows summary stats for all 4 treatment types
    - output/treatment_durations.xlsx exists with Summary + 4 per-type sheets
    - output/treatment_duration_distributions.png exists showing boxplot
    - RDS file exists at CONFIG$cache$outputs_dir/treatment_durations.rds
    - Duration values are clinically plausible (chemo ~100-200d, SCT ~0-30d, radiation ~20-50d)
  </acceptance_criteria>
  <done>User has verified that the script runs, outputs are present, xlsx is styled correctly, PNG shows expected distributions, and duration values are clinically plausible.</done>
</task>

</tasks>

<verification>
1. R/43_treatment_durations.R exists and is syntactically valid R
2. Script sources R/00_config.R and R/01_load_pcornet.R
3. All 4 treatment types (Chemotherapy, Radiation, SCT, Immunotherapy) are extracted
4. 7 PCORnet tables queried for applicable types (chemo uses all 7; radiation uses 4; SCT uses 4; immunotherapy uses 2)
5. Same-day deduplication via distinct(ID, treatment_date) present
6. 90-day gap threshold hardcoded per D-05
7. Single-date patients included (span=0, count=1) per D-03
8. Console summary stats printed per D-10
9. RDS artifact saved per D-07
10. Styled xlsx report with per-type sheets per D-08
11. Distribution PNG output per D-09
</verification>

<success_criteria>
- Script runs to completion on HiPerGator producing all 3 outputs (RDS, XLSX, PNG)
- RDS artifact has columns: ID, treatment_type, first_treatment_date, last_treatment_date, overall_span_days, distinct_treatment_dates, episode_count
- XLSX has Summary sheet + 4 per-type detail sheets with TREATMENT_TYPE_COLORS styling
- PNG shows treatment duration distributions for all 4 types
- Console output shows per-type median, IQR, range
- Duration values are clinically plausible (chemo ~100-200d median, SCT ~0-30d, radiation ~20-50d)
</success_criteria>

<output>
After completion, create `.planning/phases/43-establish-treatment-lengths-for-sct-chemo-and-radiation/43-01-SUMMARY.md`
</output>
