---
phase: 123-quantify-med-admin-dispensing-fix-impact
plan: 03
type: execute
wave: 3
depends_on: ["123-01", "123-02"]
files_modified:
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: false
requirements: [D-11, D-12]
must_haves:
  truths:
    - "R/SCRIPT_INDEX.md documents R/109 in the Post-Renumber Investigations (100+) table (count 9 -> 10)"
    - "R/88 has a new Section 15u with structural checks validating R/109 and a SMOKE-123-01 summary line"
    - "The full Rscript parse + HiPerGator DuckDB/RxNav/xlsx run of R/109 is confirmed by the user at a runtime checkpoint (mirrors Phase 122 R/108)"
  artifacts:
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15u: Phase 123 R/109 structural validation + SMOKE-123-01 summary line"
      contains: "Section 15u"
    - path: "R/SCRIPT_INDEX.md"
      provides: "R/109 row + updated 100+ count"
      contains: "R/109"
  key_links:
    - from: "R/88 Section 15u"
      to: "R/109_med_admin_dispensing_fix_impact_audit.R"
      via: "readLines + grepl structural checks"
      pattern: "109_med_admin_dispensing_fix_impact_audit"
---

<objective>
Register R/109 (SCRIPT_INDEX-only, mirroring R/107/R/108 — NOT wired into R/39 per D-12/D-02),
add an R/88 Section 15u structural smoke test with a SMOKE-123-01 summary line, and confirm the
full HiPerGator runtime via a checkpoint.

Purpose: Lock in structural validation on Windows and defer the data-dependent run to the
one environment that can execute it (HiPerGator), mirroring the Phase 122 R/108 precedent.
Output: updated R/88 + R/SCRIPT_INDEX.md, plus a user-confirmed runtime checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-quantify-how-much-the-med-admin-dispensing-chemo-detection-fix-changes-treatment-outputs-before-after-diff-and-investigate-whether-unmatched-ndcs-are-missing-real-chemo/123-CONTEXT.md
@.planning/phases/123-quantify-how-much-the-med-admin-dispensing-chemo-detection-fix-changes-treatment-outputs-before-after-diff-and-investigate-whether-unmatched-ndcs-are-missing-real-chemo/123-RESEARCH.md
@R/109_med_admin_dispensing_fix_impact_audit.R

<interfaces>
<!-- R/88 section-marker + check()/SMOKE-line conventions (verified) -->
<!-- Existing sequence: 15r=Phase 120, 15s=Phase 121, 15t=Phase 122 -> 15u=Phase 123 (next) -->
<!-- Section 15t block ends right before "# SECTION 15g: PROTON THERAPY..." — insert 15u AFTER 15t and BEFORE 15g -->

R/88 check() usage (each is one PASS/FAIL structural assertion):
```r
check("<description> (Phase 123)", <boolean condition, e.g. grepl(pattern, r109_text)>)
```
IS_LOCAL-gated runtime check pattern (Section 15t Check 14 precedent — copy structure):
```r
if (IS_LOCAL) {
  check("... runtime check -- SKIPPED (HiPerGator runtime) (Phase 123)", TRUE)
} else {
  check("... runtime check -- SKIPPED (HiPerGator runtime, not IS_LOCAL) (Phase 123)", TRUE)
}
```
SMOKE summary block location: after the SMOKE-122-01 message() line near the end of R/88, before `if (failed > 0) quit(status = 1)`. Existing line:
```r
message("  * SMOKE-122-01: R/88 validates Phase 122 MED_ADMIN/DISPENSING chemo-detection gap fix structural integrity (Section 15t, 14 checks)")
```

R/SCRIPT_INDEX.md Post-Renumber Investigations (100+) table: R/108 is the last row.
Current footer line: "Post-renumber investigations (100+): 9 (R/100 ... R/108 ...)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add R/88 Section 15u structural smoke test + SMOKE-123-01 summary line</name>
  <read_first>
    - R/88_smoke_test_comprehensive.R lines ~2585-2726 (Section 15t Phase 122 block — the exact template for readLines pattern, check() calls, IS_LOCAL gating; Section 15u goes immediately after 15t, before Section 15g at ~2727)
    - R/88_smoke_test_comprehensive.R lines ~4300-4306 (the SMOKE-*-01 summary message block + the final `if (failed > 0) quit`)
    - R/109_med_admin_dispensing_fix_impact_audit.R (the completed script — to write greps that match its ACTUAL content, per the Rule-3 lesson from Phase 120: adapt patterns to what R/109 really contains)
  </read_first>
  <action>
    Insert a new "SECTION 15u: MED_ADMIN/DISPENSING FIX BEFORE/AFTER DIFF + NDC AUDIT (Phase 123)"
    block in R/88 immediately AFTER the Section 15t block (after line ~2725) and BEFORE
    "# SECTION 15g: PROTON THERAPY...". Follow the 15t structure exactly: a message() header, read
    R/109 into r109_text via readLines guarded by file.exists, then 14 check() calls. Write each grep
    to match what R/109 ACTUALLY contains (verify against the file, do not assume):

    1. check R/109 file exists: file.exists("R/109_med_admin_dispensing_fix_impact_audit.R")
    2. R/109 uses get_chemo_hits for the after set: grepl("get_chemo_hits", r109_text)
    3. R/109 before set is PRESCRIBING-only: grepl('get_chemo_hits\\("PRESCRIBING"', r109_text)
    4. R/109 after set includes MED_ADMIN + DISPENSING: grepl('get_chemo_hits\\("MED_ADMIN"', r109_text) && grepl('get_chemo_hits\\("DISPENSING"', r109_text)
    5. R/109 parses dates after collect: grepl("parse_pcornet_date", r109_text)
    6. R/109 D-04 timing shift: grepl("first_after < first_before", r109_text)
    7. R/109 D-05 ingredient delta via MEDICATION_LOOKUP: grepl("MEDICATION_LOOKUP\\[triggering_code\\]", r109_text)
    8. R/109 D-06 guarded on episodes.rds + no re-run: grepl("file.exists\\(EPISODES_RDS\\)", r109_text) && !grepl('source\\("R/2[568]', r109_text)
    9. R/109 D-06 labeled UPPER BOUND: grepl("UPPER BOUND", r109_text)
    10. R/109 D-09 IS_LOCAL-gated + alternate endpoints: grepl("ndcproperties", r109_text) && grepl("ndcstatus", r109_text) && grepl("if \\(!IS_LOCAL\\)", r109_text)
    11. R/109 D-09 writes NEW requery file, not overwrite audit: grepl("ndc_rxnorm_crosswalk_requery.csv", r109_text) && length(gregexpr('write.csv\\([^)]*ndc_rxnorm_crosswalk_audit', r109_text)[[1]]) == 1 && attr(gregexpr(...)) handling — simpler: !grepl('write.csv\\(.*"ndc_rxnorm_crosswalk_audit', r109_text)  (use the form that reliably returns FALSE when no audit-write exists)
    12. R/109 D-10 flags gaps without modifying list: grepl("CANDIDATE_CHEMO_GAP", r109_text) && !grepl("TREATMENT_CODES\\$chemo_rxnorm <-", r109_text)
    13. R/109 D-11 single xlsx via openxlsx2: grepl("wb_workbook\\(\\)", r109_text) && grepl("wb\\$save\\(OUTPUT_XLSX\\)", r109_text)
    14. IS_LOCAL-gated runtime check (structural PASS both branches, mirroring 15t Check 14):
        if (IS_LOCAL) check("R/109 runtime (Rscript parse + DuckDB diff + xlsx) -- SKIPPED (HiPerGator runtime) (Phase 123)", TRUE)
        else check("R/109 runtime check -- SKIPPED (HiPerGator runtime, not IS_LOCAL) (Phase 123)", TRUE)

    Then add to the SMOKE summary block (after the SMOKE-122-01 line):
    message("  * SMOKE-123-01: R/88 validates Phase 123 R/109 before/after diff + unmatched-NDC audit structural integrity (Section 15u, 14 checks)")

    Preserve the existing `if (failed > 0) { quit(status = 1) }` at the end. Do NOT alter Section 15t
    or any other section.
  </action>
  <acceptance_criteria>
    - R/88 contains "SECTION 15u" and "Phase 123" section header
    - R/88 Section 15u contains exactly 14 `check(` calls tagged `(Phase 123)` (grep count == 14)
    - R/88 Section 15u contains `grepl("get_chemo_hits"` and `grepl('get_chemo_hits\\("PRESCRIBING"'` and `grepl("UPPER BOUND"` and `grepl("ndcproperties"` and `grepl("CANDIDATE_CHEMO_GAP"` and `grepl("wb_workbook`
    - R/88 Section 15u is positioned after the Section 15t block and before "# SECTION 15g: PROTON THERAPY" (grep line numbers confirm ordering)
    - R/88 contains the SMOKE-123-01 summary message line referencing "Section 15u, 14 checks"
    - R/88 still contains the Section 15t block unchanged (SMOKE-122-01 line still present) and the final `if (failed > 0)` quit
    - R/88 parenthesis/brace balance holds (Windows structural check; full Rscript parse = HiPerGator checkpoint)
  </acceptance_criteria>
  <done>
    R/88 has a 14-check Section 15u validating R/109 structure and a SMOKE-123-01 summary line;
    Section 15t and the quit-on-fail logic are intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register R/109 in R/SCRIPT_INDEX.md (SCRIPT_INDEX-only, not R/39)</name>
  <read_first>
    - R/SCRIPT_INDEX.md lines ~140-155 (Post-Renumber Investigations (100+) table — R/107 and R/108 rows are the format template)
    - R/SCRIPT_INDEX.md lines ~207 (the "Post-renumber investigations (100+): 9 (...)" footer count line)
  </read_first>
  <action>
    Add ONE new row to the Post-Renumber Investigations (100+) table in R/SCRIPT_INDEX.md, immediately
    after the R/108 row:

    | `R/109_med_admin_dispensing_fix_impact_audit.R` | Read-only Phase 123 quantification + audit (NOT wired into R/39, mirrors R/107/R/108). Computes the deterministic before/after chemo-detection diff from the Phase 122 fix: before = PRESCRIBING-only, after = PRESCRIBING + MED_ADMIN (RX + ND) + DISPENSING via the production `get_chemo_hits()` path. Reports patient & date counts by source (D-03), first-chemo timing shift distribution (D-04), per-ingredient delta (D-05), and an UPPER-BOUND regimen-label impact from a `treatment_episodes.rds` join (D-06, no R/25/26/28 re-run). Audits the ~7,739 unmatched + ~16,588 resolved NDCs from `ndc_rxnorm_crosswalk_audit.csv` four ways: MED_ADMIN-ND drug-name string match (D-07), top-50 frequency rank (D-08), IS_LOCAL-gated RxNav `ndcproperties`/`ndcstatus` re-query to a NEW `ndc_rxnorm_crosswalk_requery.csv` (D-09), and a resolved-non-chemo `chemo_rxnorm` gap flag (D-10). Delivers a single multi-sheet styled openxlsx2 workbook `output/med_admin_dispensing_fix_impact.xlsx` (D-11), HIPAA-suppressed. Quantification only — no downstream regeneration (D-12). | 123 |

    Update the footer count line (~line 207): change
    "Post-renumber investigations (100+): 9 (R/100 ... R/108 NDC->RxNorm crosswalk builder)"
    to "10" and append ", R/109 MED_ADMIN/DISPENSING fix before/after diff + unmatched-NDC audit"
    to the parenthetical list.
    Do NOT modify R/39_run_all_investigations.R (R/109 is SCRIPT_INDEX-only per D-12/D-02, mirroring
    R/107 and R/108).
  </action>
  <acceptance_criteria>
    - R/SCRIPT_INDEX.md contains a row with `R/109_med_admin_dispensing_fix_impact_audit.R` in the 100+ table with phase column `123`
    - The R/109 row text contains "get_chemo_hits", "D-03", "D-06", "UPPER-BOUND", "D-09", "D-11", and "Quantification only"
    - The footer count line reads "Post-renumber investigations (100+):** 10" and the parenthetical includes "R/109"
    - R/39_run_all_investigations.R is NOT modified (git diff shows no change to R/39)
    - R/SCRIPT_INDEX.md R/107 and R/108 rows are unchanged
  </acceptance_criteria>
  <done>
    R/SCRIPT_INDEX.md documents R/109 with the 100+ count bumped 9 -> 10; R/39 untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HiPerGator runtime confirmation of R/109</name>
  <what-built>
    R/109_med_admin_dispensing_fix_impact_audit.R — the complete before/after diff + four-method
    unmatched-NDC audit, producing output/med_admin_dispensing_fix_impact.xlsx. Windows verification
    was structural only (no Rscript). The full run requires HiPerGator (DuckDB PCORnet data, the
    treatment_episodes.rds cache, and — for D-09 — RxNav network access).
  </what-built>
  <how-to-verify>
    On HiPerGator (RStudio or `module load R/4.4.2`):
    1. Run the structural smoke test — expect Section 15u 14/14 PASS:
       `Rscript R/88_smoke_test_comprehensive.R`  (look for "SMOKE-123-01" and no Section 15u FAIL)
    2. Run the diagnostic:
       `Rscript R/109_med_admin_dispensing_fix_impact_audit.R`
       Expected:
         - Cohort scope logs the HL patient count (~9,282)
         - Before/after headline logs patients + chemo dates gained by source
           (sanity-check against Phase 122 VERIFICATION: ~+1,139 patients / ~+10,752 dates)
         - D-06 either runs (if cache/outputs/treatment_episodes.rds exists) or logs the skip message
         - D-09 runs the RxNav re-query (this can take ~20-30 min for ~7,739 NDCs x 2 endpoints)
           and writes output/ndc_rxnorm_crosswalk_requery.csv (NOT overwriting the audit CSV)
    3. Open output/med_admin_dispensing_fix_impact.xlsx — confirm all sheets present
       (Before-After Summary, Timing Shift, Per-Ingredient Delta, Regimen Impact, Unmatched NDC Top-N,
        NDC String Match, RxNav Requery, Resolved-Gap Findings), styled headers, HIPAA-suppressed counts.
    4. Confirm output/ndc_rxnorm_crosswalk_audit.csv is UNCHANGED (byte-identical) after the run.
  </how-to-verify>
  <resume-signal>Type "approved" (paste the headline before/after numbers), or describe any errors / mismatches for gap-closure planning.</resume-signal>
</task>

</tasks>

<verification>
Windows-executor structural verification (Tasks 1-2, no Rscript):
- R/88 Section 15u present with 14 Phase 123 checks + SMOKE-123-01 line; Section 15t intact
- R/SCRIPT_INDEX.md has the R/109 row; 100+ count = 10; R/39 unchanged
- Parenthesis/brace balance holds in R/88
HiPerGator runtime verification (Task 3, checkpoint):
- R/88 Section 15u 14/14 PASS; R/109 runs, writes the xlsx + requery CSV; audit CSV unchanged
</verification>

<success_criteria>
- R/SCRIPT_INDEX.md documents R/109 (100+ count 9 -> 10); R/39 untouched (D-02/D-12)
- R/88 Section 15u (14 checks) + SMOKE-123-01 validate R/109 structure
- User confirms the HiPerGator runtime produces the xlsx deliverable and the headline diff numbers
</success_criteria>

<output>
After completion, create `.planning/phases/123-quantify-how-much-the-med-admin-dispensing-chemo-detection-fix-changes-treatment-outputs-before-after-diff-and-investigate-whether-unmatched-ndcs-are-missing-real-chemo/123-03-SUMMARY.md`
</output>
