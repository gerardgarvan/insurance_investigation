---
phase: 09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - R/10_treatment_payer.R
autonomous: true
requirements:
  - TXEXP-06

must_haves:
  truths:
    - "compute_payer_at_chemo() extracts first treatment dates from DIAGNOSIS, ENCOUNTER DRG, DISPENSING, MED_ADMIN, and PROCEDURES revenue in addition to existing PROCEDURES/PRESCRIBING sources"
    - "compute_payer_at_radiation() extracts first treatment dates from DIAGNOSIS, ENCOUNTER DRG, and PROCEDURES revenue in addition to existing PROCEDURES source"
    - "compute_payer_at_sct() extracts first treatment dates from DIAGNOSIS, ENCOUNTER DRG, and PROCEDURES revenue in addition to existing PROCEDURES source"
    - "First treatment date per patient is the minimum across ALL sources"
  artifacts:
    - path: "R/10_treatment_payer.R"
      provides: "Expanded compute_payer_at_chemo/radiation/sct() with multi-source date extraction"
      contains: "TREATMENT_CODES$chemo_drg"
  key_links:
    - from: "R/10_treatment_payer.R"
      to: "R/00_config.R"
      via: "TREATMENT_CODES code lists for filtering treatment records"
      pattern: "TREATMENT_CODES\\$chemo_drg"
    - from: "R/10_treatment_payer.R"
      to: "R/01_load_pcornet.R"
      via: "pcornet$DISPENSING, pcornet$MED_ADMIN loaded tables"
      pattern: "pcornet\\$DISPENSING"
    - from: "R/10_treatment_payer.R"
      to: "compute_payer_mode_in_window()"
      via: "first_dates tibble passed to existing helper function"
      pattern: "compute_payer_mode_in_window"
---

<objective>
Expand compute_payer_at_chemo(), compute_payer_at_radiation(), and compute_payer_at_sct() in 10_treatment_payer.R to extract first treatment dates from all new data sources, ensuring the treatment-anchored payer window captures the earliest possible treatment date per D-03.

Purpose: More date sources = earlier first treatment date discovery = better payer match coverage within the +/-30 day window. Patients previously missing treatment dates (e.g., only had DRG evidence) now get payer assignment.

Output: Updated 10_treatment_payer.R with expanded date extraction from DIAGNOSIS, ENCOUNTER, DISPENSING, MED_ADMIN, and PROCEDURES revenue code sources.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-CONTEXT.md
@.planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-RESEARCH.md
@.planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-01-SUMMARY.md
@R/10_treatment_payer.R
@R/00_config.R

<interfaces>
<!-- Current function signatures and patterns in 10_treatment_payer.R -->

```r
# Helper function (unchanged):
compute_payer_mode_in_window <- function(first_dates, window_days, payer_col_name)
# Takes tibble(ID, <date_column>), returns tibble(ID, {payer_col_name})

# Current chemo: combines px_dates + rx_dates via full_join -> rowwise -> min()
compute_payer_at_chemo <- function()  # Returns tibble(ID, FIRST_CHEMO_DATE, PAYER_AT_CHEMO)
compute_payer_at_radiation <- function()  # Returns tibble(ID, FIRST_RADIATION_DATE, PAYER_AT_RADIATION)
compute_payer_at_sct <- function()  # Returns tibble(ID, FIRST_SCT_DATE, PAYER_AT_SCT)
```

<!-- Date extraction pattern (from current compute_payer_at_chemo): -->
```r
# Extract dates from a source, group by ID, take min per patient
px_dates <- pcornet$PROCEDURES %>%
  filter(<treatment code filter>) %>%
  filter(!is.na(PX_DATE)) %>%
  group_by(ID) %>%
  summarise(px_date = min(PX_DATE, na.rm = TRUE), .groups = "drop")

# Combine two date sources via full_join + rowwise min
first_dates <- full_join(px_dates, rx_dates, by = "ID") %>%
  rowwise() %>%
  mutate(FIRST_CHEMO_DATE = min(c(px_date, rx_date), na.rm = TRUE)) %>%
  ungroup() %>%
  select(ID, FIRST_CHEMO_DATE)
```

<!-- New code lists from Plan 01 (consumed for filtering): -->
```r
TREATMENT_CODES$chemo_dx_icd10    # c("Z51.11", "Z51.12")
TREATMENT_CODES$chemo_dx_icd9     # c("V58.11", "V58.12")
TREATMENT_CODES$radiation_dx_icd10 # c("Z51.0")
TREATMENT_CODES$radiation_dx_icd9  # c("V58.0")
TREATMENT_CODES$sct_dx_icd10      # c("Z94.84", "T86.5", "T86.09", "Z48.290", "T86.0")
TREATMENT_CODES$chemo_drg         # c("837", "838", "839", "846", "847", "848")
TREATMENT_CODES$radiation_drg     # c("849")
TREATMENT_CODES$sct_drg           # c("014", "016", "017")
TREATMENT_CODES$chemo_revenue     # c("0331", "0332", "0335")
TREATMENT_CODES$radiation_revenue  # c("0330", "0333")
TREATMENT_CODES$sct_revenue       # c("0362", "0815")
TREATMENT_CODES$chemo_rxnorm      # c("3639", "11213", "67228", "3946") -- reused for DISPENSING/MED_ADMIN
```

<!-- Date columns per table (for anchoring): -->
- DIAGNOSIS: DX_DATE
- ENCOUNTER: ADMIT_DATE
- DISPENSING: DISPENSE_DATE
- MED_ADMIN: MEDADMIN_START_DATE
- PROCEDURES (revenue): PX_DATE (same column as existing PROCEDURES queries)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand compute_payer_at_chemo() with 5 new date sources</name>
  <files>R/10_treatment_payer.R</files>
  <read_first>
    - R/10_treatment_payer.R (full file, focus on compute_payer_at_chemo() lines 79-145)
    - R/00_config.R (TREATMENT_CODES to confirm new vectors exist after Plan 01)
    - .planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-RESEARCH.md (Pattern 2: Multi-Source Date Extraction code example)
    - .planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-CONTEXT.md (D-03, D-09, D-10, D-11, D-12)
  </read_first>
  <action>
Modify compute_payer_at_chemo() in R/10_treatment_payer.R to add 5 new date extraction blocks after the existing PRESCRIBING block, then combine all date sources into a single first_dates tibble.

**Add after rx_dates extraction (around line 103) and before the existing date combination logic:**

```r
  # --- Phase 9: Expanded date extraction sources ---

  # DIAGNOSIS DX_DATE: Z51.11/Z51.12 (ICD-10), V58.11/V58.12 (ICD-9) per D-09
  dx_dates <- NULL
  if (!is.null(pcornet$DIAGNOSIS)) {
    dx_dates <- pcornet$DIAGNOSIS %>%
      filter(
        (DX_TYPE == "10" & DX %in% TREATMENT_CODES$chemo_dx_icd10) |
        (DX_TYPE == "09" & DX %in% TREATMENT_CODES$chemo_dx_icd9)
      ) %>%
      filter(!is.na(DX_DATE)) %>%
      group_by(ID) %>%
      summarise(dx_date = min(DX_DATE, na.rm = TRUE), .groups = "drop")
  }

  # ENCOUNTER ADMIT_DATE: DRGs 837-839, 846-848 per D-10
  drg_dates <- NULL
  if (!is.null(pcornet$ENCOUNTER)) {
    drg_dates <- pcornet$ENCOUNTER %>%
      filter(DRG %in% TREATMENT_CODES$chemo_drg) %>%
      filter(!is.na(ADMIT_DATE)) %>%
      group_by(ID) %>%
      summarise(drg_date = min(ADMIT_DATE, na.rm = TRUE), .groups = "drop")
  }

  # DISPENSING DISPENSE_DATE: RXNORM_CUI matching per D-12
  disp_dates <- NULL
  if (!is.null(pcornet$DISPENSING)) {
    disp_dates <- pcornet$DISPENSING %>%
      filter(RXNORM_CUI %in% TREATMENT_CODES$chemo_rxnorm) %>%
      filter(!is.na(DISPENSE_DATE)) %>%
      group_by(ID) %>%
      summarise(disp_date = min(DISPENSE_DATE, na.rm = TRUE), .groups = "drop")
  }

  # MED_ADMIN MEDADMIN_START_DATE: RXNORM_CUI matching per D-12
  ma_dates <- NULL
  if (!is.null(pcornet$MED_ADMIN)) {
    ma_dates <- pcornet$MED_ADMIN %>%
      filter(RXNORM_CUI %in% TREATMENT_CODES$chemo_rxnorm) %>%
      filter(!is.na(MEDADMIN_START_DATE)) %>%
      group_by(ID) %>%
      summarise(ma_date = min(MEDADMIN_START_DATE, na.rm = TRUE), .groups = "drop")
  }

  # PROCEDURES revenue codes PX_DATE: 0331/0332/0335 per D-11
  rev_dates <- NULL
  if (!is.null(pcornet$PROCEDURES)) {
    rev_dates <- pcornet$PROCEDURES %>%
      filter(PX_TYPE == "RE" & PX %in% TREATMENT_CODES$chemo_revenue) %>%
      filter(!is.na(PX_DATE)) %>%
      group_by(ID) %>%
      summarise(rev_date = min(PX_DATE, na.rm = TRUE), .groups = "drop")
  }
```

**Replace the existing date combination logic** (the if/else if/else chain around lines 106-123) with a generic combiner that handles all 7 date sources:

```r
  # Combine ALL date sources into single first-date-per-patient
  all_date_sources <- list(
    px_dates = px_dates, rx_dates = rx_dates, dx_dates = dx_dates,
    drg_dates = drg_dates, disp_dates = disp_dates, ma_dates = ma_dates,
    rev_dates = rev_dates
  )

  # Filter out NULLs and rename date columns to generic "src_date"
  non_null_sources <- compact(all_date_sources)

  if (length(non_null_sources) == 0) {
    message(glue("  Patients with chemo dates: 0"))
    message(glue("PAYER_AT_CHEMO: 0 matched, 0 no encounters in window (NA)"))
    return(tibble(ID = character(0), FIRST_CHEMO_DATE = as.Date(character(0)), PAYER_AT_CHEMO = character(0)))
  }

  # Stack all sources with generic date column, then take min per patient
  stacked <- bind_rows(
    lapply(non_null_sources, function(df) {
      date_col <- setdiff(names(df), "ID")
      df %>% rename(src_date = !!date_col[1]) %>% select(ID, src_date)
    })
  )

  first_dates <- stacked %>%
    group_by(ID) %>%
    summarise(FIRST_CHEMO_DATE = min(src_date, na.rm = TRUE), .groups = "drop") %>%
    filter(!is.infinite(FIRST_CHEMO_DATE))
```

**Add source-level date count logging** after the stacked computation:
```r
  message(glue("  Chemo date sources: PX={nrow_or_0(px_dates)}, RX={nrow_or_0(rx_dates)}, DX={nrow_or_0(dx_dates)}, DRG={nrow_or_0(drg_dates)}, DISP={nrow_or_0(disp_dates)}, MA={nrow_or_0(ma_dates)}, REV={nrow_or_0(rev_dates)}"))
```

**Add a helper function** at the top of 10_treatment_payer.R (after the library calls, before compute_payer_mode_in_window):
```r
# Helper: return nrow or 0 for NULL tibbles (for logging)
nrow_or_0 <- function(df) if (is.null(df)) 0L else nrow(df)
```

The remainder of the function (compute_payer_mode_in_window call, left_join, logging) stays the same.

**Update the function docstring** to list all 7 date sources.
  </action>
  <verify>
    <automated>grep -c "chemo_dx_icd10\|chemo_drg\|chemo_revenue\|DISPENSE_DATE\|MEDADMIN_START_DATE\|nrow_or_0" R/10_treatment_payer.R</automated>
  </verify>
  <acceptance_criteria>
    - R/10_treatment_payer.R contains `nrow_or_0 <- function(df)` helper
    - compute_payer_at_chemo() contains `DX %in% TREATMENT_CODES$chemo_dx_icd10`
    - compute_payer_at_chemo() contains `DRG %in% TREATMENT_CODES$chemo_drg`
    - compute_payer_at_chemo() contains `RXNORM_CUI %in% TREATMENT_CODES$chemo_rxnorm` for DISPENSING (filter on DISPENSE_DATE)
    - compute_payer_at_chemo() contains `RXNORM_CUI %in% TREATMENT_CODES$chemo_rxnorm` for MED_ADMIN (filter on MEDADMIN_START_DATE)
    - compute_payer_at_chemo() contains `PX_TYPE == "RE" & PX %in% TREATMENT_CODES$chemo_revenue`
    - compute_payer_at_chemo() uses bind_rows + group_by(ID) + min(src_date) to combine all sources
    - compute_payer_at_chemo() contains `Chemo date sources:` logging line
    - grep count returns >= 6 for the new references
  </acceptance_criteria>
  <done>compute_payer_at_chemo() extracts first treatment dates from 7 sources (PROCEDURES CPT/ICD + PRESCRIBING + DIAGNOSIS + ENCOUNTER DRG + DISPENSING + MED_ADMIN + PROCEDURES revenue), takes per-patient minimum, and passes to compute_payer_mode_in_window()</done>
</task>

<task type="auto">
  <name>Task 2: Expand compute_payer_at_radiation() and compute_payer_at_sct() with new date sources</name>
  <files>R/10_treatment_payer.R</files>
  <read_first>
    - R/10_treatment_payer.R (compute_payer_at_radiation() and compute_payer_at_sct() functions)
    - R/00_config.R (TREATMENT_CODES radiation/sct code vectors)
    - .planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-RESEARCH.md (Code Example 2: Extend compute_payer_at_radiation() with ENCOUNTER DRG dates)
  </read_first>
  <action>
Apply the same multi-source date extraction pattern to compute_payer_at_radiation() and compute_payer_at_sct().

**compute_payer_at_radiation() -- add 3 new date sources after existing PROCEDURES extraction:**

```r
  # --- Phase 9: Expanded date extraction sources ---

  # DIAGNOSIS DX_DATE: Z51.0 (ICD-10), V58.0 (ICD-9) per D-09
  dx_dates <- NULL
  if (!is.null(pcornet$DIAGNOSIS)) {
    dx_dates <- pcornet$DIAGNOSIS %>%
      filter(
        (DX_TYPE == "10" & DX %in% TREATMENT_CODES$radiation_dx_icd10) |
        (DX_TYPE == "09" & DX %in% TREATMENT_CODES$radiation_dx_icd9)
      ) %>%
      filter(!is.na(DX_DATE)) %>%
      group_by(ID) %>%
      summarise(dx_date = min(DX_DATE, na.rm = TRUE), .groups = "drop")
  }

  # ENCOUNTER ADMIT_DATE: DRG 849 per D-10
  drg_dates <- NULL
  if (!is.null(pcornet$ENCOUNTER)) {
    drg_dates <- pcornet$ENCOUNTER %>%
      filter(DRG %in% TREATMENT_CODES$radiation_drg) %>%
      filter(!is.na(ADMIT_DATE)) %>%
      group_by(ID) %>%
      summarise(drg_date = min(ADMIT_DATE, na.rm = TRUE), .groups = "drop")
  }

  # PROCEDURES revenue codes PX_DATE: 0330/0333 per D-11
  rev_dates <- NULL
  if (!is.null(pcornet$PROCEDURES)) {
    rev_dates <- pcornet$PROCEDURES %>%
      filter(PX_TYPE == "RE" & PX %in% TREATMENT_CODES$radiation_revenue) %>%
      filter(!is.na(PX_DATE)) %>%
      group_by(ID) %>%
      summarise(rev_date = min(PX_DATE, na.rm = TRUE), .groups = "drop")
  }
```

**Replace the existing date extraction and combination logic** in compute_payer_at_radiation() with the same stacked combiner pattern used in Task 1:

The current function extracts only px_dates from PROCEDURES and directly passes to compute_payer_mode_in_window. Replace with:

```r
  # Combine ALL date sources
  all_date_sources <- list(
    px_dates = px_dates, dx_dates = dx_dates,
    drg_dates = drg_dates, rev_dates = rev_dates
  )

  non_null_sources <- compact(all_date_sources)

  if (length(non_null_sources) == 0) {
    message(glue("  Patients with radiation dates: 0"))
    message(glue("PAYER_AT_RADIATION: 0 matched, 0 no encounters in window (NA)"))
    return(tibble(ID = character(0), FIRST_RADIATION_DATE = as.Date(character(0)), PAYER_AT_RADIATION = character(0)))
  }

  stacked <- bind_rows(
    lapply(non_null_sources, function(df) {
      date_col <- setdiff(names(df), "ID")
      df %>% rename(src_date = !!date_col[1]) %>% select(ID, src_date)
    })
  )

  first_dates <- stacked %>%
    group_by(ID) %>%
    summarise(FIRST_RADIATION_DATE = min(src_date, na.rm = TRUE), .groups = "drop") %>%
    filter(!is.infinite(FIRST_RADIATION_DATE))

  message(glue("  Radiation date sources: PX={nrow_or_0(px_dates)}, DX={nrow_or_0(dx_dates)}, DRG={nrow_or_0(drg_dates)}, REV={nrow_or_0(rev_dates)}"))
```

NOTE: Radiation does NOT use DISPENSING or MED_ADMIN date sources (same rationale as Plan 02: radiation is a procedure, no drug RXNORM matching).

Preserve the remainder of the function (compute_payer_mode_in_window call, left_join, logging).

---

**compute_payer_at_sct() -- add 3 new date sources after existing PROCEDURES extraction:**

```r
  # --- Phase 9: Expanded date extraction sources ---

  # DIAGNOSIS DX_DATE: Z94.84/T86.5/T86.09/Z48.290/T86.0 (ICD-10 only) per D-09
  dx_dates <- NULL
  if (!is.null(pcornet$DIAGNOSIS)) {
    dx_dates <- pcornet$DIAGNOSIS %>%
      filter(DX_TYPE == "10" & DX %in% TREATMENT_CODES$sct_dx_icd10) %>%
      filter(!is.na(DX_DATE)) %>%
      group_by(ID) %>%
      summarise(dx_date = min(DX_DATE, na.rm = TRUE), .groups = "drop")
  }

  # ENCOUNTER ADMIT_DATE: DRGs 014, 016, 017 per D-10
  drg_dates <- NULL
  if (!is.null(pcornet$ENCOUNTER)) {
    drg_dates <- pcornet$ENCOUNTER %>%
      filter(DRG %in% TREATMENT_CODES$sct_drg) %>%
      filter(!is.na(ADMIT_DATE)) %>%
      group_by(ID) %>%
      summarise(drg_date = min(ADMIT_DATE, na.rm = TRUE), .groups = "drop")
  }

  # PROCEDURES revenue codes PX_DATE: 0362/0815 per D-11
  rev_dates <- NULL
  if (!is.null(pcornet$PROCEDURES)) {
    rev_dates <- pcornet$PROCEDURES %>%
      filter(PX_TYPE == "RE" & PX %in% TREATMENT_CODES$sct_revenue) %>%
      filter(!is.na(PX_DATE)) %>%
      group_by(ID) %>%
      summarise(rev_date = min(PX_DATE, na.rm = TRUE), .groups = "drop")
  }
```

**Replace the existing date extraction and combination logic** in compute_payer_at_sct() with the same stacked combiner:

```r
  # Combine ALL date sources
  all_date_sources <- list(
    px_dates = px_dates, dx_dates = dx_dates,
    drg_dates = drg_dates, rev_dates = rev_dates
  )

  non_null_sources <- compact(all_date_sources)

  if (length(non_null_sources) == 0) {
    message(glue("  Patients with SCT dates: 0"))
    message(glue("PAYER_AT_SCT: 0 matched, 0 no encounters in window (NA)"))
    return(tibble(ID = character(0), FIRST_SCT_DATE = as.Date(character(0)), PAYER_AT_SCT = character(0)))
  }

  stacked <- bind_rows(
    lapply(non_null_sources, function(df) {
      date_col <- setdiff(names(df), "ID")
      df %>% rename(src_date = !!date_col[1]) %>% select(ID, src_date)
    })
  )

  first_dates <- stacked %>%
    group_by(ID) %>%
    summarise(FIRST_SCT_DATE = min(src_date, na.rm = TRUE), .groups = "drop") %>%
    filter(!is.infinite(FIRST_SCT_DATE))

  message(glue("  SCT date sources: PX={nrow_or_0(px_dates)}, DX={nrow_or_0(dx_dates)}, DRG={nrow_or_0(drg_dates)}, REV={nrow_or_0(rev_dates)}"))
```

Preserve the remainder (compute_payer_mode_in_window call, left_join, logging).

**Update function docstrings** for both functions to list all date sources.

**Update file header comment** (lines 1-32) to mention expanded date extraction from DIAGNOSIS, ENCOUNTER, DISPENSING, MED_ADMIN, and PROCEDURES revenue code sources.
  </action>
  <verify>
    <automated>grep -c "radiation_dx_icd10\|radiation_drg\|radiation_revenue\|sct_dx_icd10\|sct_drg\|sct_revenue\|Radiation date sources\|SCT date sources" R/10_treatment_payer.R</automated>
  </verify>
  <acceptance_criteria>
    - compute_payer_at_radiation() contains `DX %in% TREATMENT_CODES$radiation_dx_icd10`
    - compute_payer_at_radiation() contains `DRG %in% TREATMENT_CODES$radiation_drg`
    - compute_payer_at_radiation() contains `PX_TYPE == "RE" & PX %in% TREATMENT_CODES$radiation_revenue`
    - compute_payer_at_radiation() does NOT reference DISPENSING or MED_ADMIN
    - compute_payer_at_radiation() uses bind_rows + min(src_date) to combine sources
    - compute_payer_at_radiation() contains `Radiation date sources:` logging line
    - compute_payer_at_sct() contains `DX %in% TREATMENT_CODES$sct_dx_icd10`
    - compute_payer_at_sct() contains `DRG %in% TREATMENT_CODES$sct_drg`
    - compute_payer_at_sct() contains `PX_TYPE == "RE" & PX %in% TREATMENT_CODES$sct_revenue`
    - compute_payer_at_sct() does NOT reference DISPENSING or MED_ADMIN
    - compute_payer_at_sct() uses bind_rows + min(src_date) to combine sources
    - compute_payer_at_sct() contains `SCT date sources:` logging line
    - grep count returns >= 8 for the new references
  </acceptance_criteria>
  <done>compute_payer_at_radiation() extracts dates from 4 sources (PROCEDURES CPT/ICD + DIAGNOSIS + ENCOUNTER DRG + PROCEDURES revenue) and compute_payer_at_sct() extracts dates from 4 sources (PROCEDURES CPT/ICD + DIAGNOSIS + ENCOUNTER DRG + PROCEDURES revenue), both using stacked minimum-date pattern</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "TREATMENT_CODES\\$chemo_drg" R/10_treatment_payer.R` returns a match
2. `grep "TREATMENT_CODES\\$radiation_drg" R/10_treatment_payer.R` returns a match
3. `grep "TREATMENT_CODES\\$sct_drg" R/10_treatment_payer.R` returns a match
4. `grep "nrow_or_0" R/10_treatment_payer.R` returns 3+ matches (helper + usage)
5. `grep "date sources:" R/10_treatment_payer.R` returns 3 matches (one per function)
6. `grep "pcornet\\$DISPENSING" R/10_treatment_payer.R` returns 1 match (only in chemo)
</verification>

<success_criteria>
- compute_payer_at_chemo() extracts dates from 7 sources (PROCEDURES CPT/ICD + PRESCRIBING + DIAGNOSIS + ENCOUNTER DRG + DISPENSING + MED_ADMIN + PROCEDURES revenue)
- compute_payer_at_radiation() extracts dates from 4 sources (PROCEDURES CPT/ICD + DIAGNOSIS + ENCOUNTER DRG + PROCEDURES revenue)
- compute_payer_at_sct() extracts dates from 4 sources (PROCEDURES CPT/ICD + DIAGNOSIS + ENCOUNTER DRG + PROCEDURES revenue)
- All functions use the stacked bind_rows + group_by + min(src_date) pattern for combining dates
- All functions log per-source patient counts with date sources
- DISPENSING/MED_ADMIN only used for chemo (consistent with Plan 02)
- compute_payer_mode_in_window() is NOT modified (generic helper stays unchanged)
- FIRST_*_DATE output columns still have the same name and type (Date)
</success_criteria>

<output>
After completion, create `.planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-03-SUMMARY.md`
</output>
