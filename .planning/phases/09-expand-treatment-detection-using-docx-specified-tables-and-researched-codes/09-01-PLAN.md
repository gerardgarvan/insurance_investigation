---
phase: 09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/01_load_pcornet.R
autonomous: true
requirements:
  - TXEXP-01
  - TXEXP-02
  - TXEXP-03
  - TXEXP-04

must_haves:
  truths:
    - "TREATMENT_CODES contains new code list vectors for DRG, revenue, diagnosis, and ICD-9 additions"
    - "DISPENSING and MED_ADMIN tables are loadable via 01_load_pcornet.R with full col_types specifications"
    - "New tables use null-safe loading (warn and skip if CSV file missing)"
  artifacts:
    - path: "R/00_config.R"
      provides: "Expanded TREATMENT_CODES with 12+ new code list vectors"
      contains: "chemo_drg"
    - path: "R/01_load_pcornet.R"
      provides: "DISPENSING_SPEC and MED_ADMIN_SPEC col_types, updated PCORNET_TABLES and TABLE_SPECS"
      contains: "DISPENSING_SPEC"
  key_links:
    - from: "R/00_config.R"
      to: "R/03_cohort_predicates.R"
      via: "TREATMENT_CODES list consumed by has_*() functions"
      pattern: "TREATMENT_CODES\\$chemo_drg"
    - from: "R/01_load_pcornet.R"
      to: "R/03_cohort_predicates.R"
      via: "pcornet$DISPENSING and pcornet$MED_ADMIN tables"
      pattern: "pcornet\\$DISPENSING"
---

<objective>
Add all new treatment code lists to TREATMENT_CODES in 00_config.R and add DISPENSING/MED_ADMIN table loading to 01_load_pcornet.R.

Purpose: Provides the foundational code lists and data loading that Plans 02 and 03 depend on to expand treatment detection and treatment-anchored payer computation.

Output: Updated 00_config.R with 12+ new code list vectors; updated 01_load_pcornet.R with DISPENSING_SPEC, MED_ADMIN_SPEC, and expanded PCORNET_TABLES/TABLE_SPECS.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-CONTEXT.md
@.planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-RESEARCH.md
@R/00_config.R
@R/01_load_pcornet.R

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/00_config.R (current TREATMENT_CODES structure):
```r
TREATMENT_CODES <- list(
  chemo_hcpcs = c("J9000", "J9040", "J9360", "J9130", "J9042", "J9299"),
  chemo_rxnorm = c("3639", "11213", "67228", "3946"),
  radiation_cpt = c("77401", "77402", "77407", "77412", "77427"),
  sct_cpt = c("38230", "38232", "38240", "38241", "38242", "38243"),
  chemo_icd9 = c("99.25"),
  chemo_icd10pcs_prefixes = c("3E03305", "3E04305", "3E05305", "3E06305"),
  radiation_icd9 = c("92.20", ..., "92.41"),
  radiation_icd10pcs_prefixes = c("D70", "D71", "D72", "D7Y"),
  sct_icd9 = c("41.00", ..., "41.09"),
  sct_icd10pcs = c("30233G0", ..., "30243Y1")
)
```

From R/01_load_pcornet.R (table loading pattern):
```r
PCORNET_TABLES <- c("ENROLLMENT", "DIAGNOSIS", "PROCEDURES", "PRESCRIBING",
                     "ENCOUNTER", "DEMOGRAPHIC", "TUMOR_REGISTRY1",
                     "TUMOR_REGISTRY2", "TUMOR_REGISTRY3")
TABLE_SPECS <- list(ENROLLMENT = ENROLLMENT_SPEC, ..., TUMOR_REGISTRY3 = TUMOR_REGISTRY3_SPEC)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add expanded treatment code lists to TREATMENT_CODES in 00_config.R</name>
  <files>R/00_config.R</files>
  <read_first>
    - R/00_config.R (current TREATMENT_CODES structure, line ~295-412)
    - .planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-RESEARCH.md (verified code lists in "Standard Stack > Code Lists" section and "Code Examples > Common Operation 4")
    - .planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-CONTEXT.md (locked decisions D-04 through D-12)
  </read_first>
  <action>
Add the following new vectors to the TREATMENT_CODES list in R/00_config.R, after the existing sct_icd10pcs entry (around line 411). Add a comment section header "--- Phase 9: Expanded detection codes (D-09, D-10, D-11) ---" before the new entries.

**Per D-07: Expand existing ICD-9 lists:**
- Add "99.28" to chemo_icd9 (currently only "99.25"). Final: `chemo_icd9 = c("99.25", "99.28")`
- Add "V58.11", "V58.12" to chemo_icd9 -- WAIT: these are DIAGNOSIS codes (V codes), not procedure codes. V58.11/V58.12 go in chemo_dx_icd9, not chemo_icd9. Per D-07: "add V58.11, V58.12, 99.28 to chemo" -- 99.28 is a procedure code, V58.xx are diagnosis codes. Add 99.28 to chemo_icd9. V58.11/V58.12 go in chemo_dx_icd9 below. Similarly V58.0 goes in radiation_dx_icd9 below.

**Per D-09: Diagnosis-based treatment evidence codes (new vectors):**
```r
  # Diagnosis-based treatment evidence (ICD-10-CM Z/T codes)
  chemo_dx_icd10 = c(
    "Z51.11",   # Encounter for antineoplastic chemotherapy
    "Z51.12"    # Encounter for antineoplastic immunotherapy
  ),
  chemo_dx_icd9 = c(
    "V58.11",   # Encounter for antineoplastic chemotherapy
    "V58.12"    # Encounter for antineoplastic immunotherapy
  ),
  radiation_dx_icd10 = c(
    "Z51.0"     # Encounter for antineoplastic radiation therapy
  ),
  radiation_dx_icd9 = c(
    "V58.0"     # Encounter for radiotherapy
  ),
  sct_dx_icd10 = c(
    "Z94.84",   # Stem cells transplant status
    "T86.5",    # Complications of stem cell transplant
    "T86.09",   # Other complications of bone marrow transplant
    "Z48.290",  # Encounter for aftercare following bone marrow transplant
    "T86.0"     # Complications of bone marrow transplant (per D-09 in CONTEXT.md)
  ),
```

**Per D-10: DRG codes (new vectors):**
```r
  # MS-DRG treatment evidence (ENCOUNTER table DRG column)
  chemo_drg = c(
    "837",   # Chemo w/o acute leukemia as SDx w MCC
    "838",   # Chemo w/o acute leukemia as SDx w CC
    "839",   # Chemo w/o acute leukemia as SDx w/o CC/MCC
    "846",   # Chemo w hematologic malignancy as SDx w MCC
    "847",   # Chemo w hematologic malignancy as SDx w CC
    "848"    # Chemo w hematologic malignancy as SDx w/o CC/MCC
  ),
  radiation_drg = c(
    "849"    # Radiotherapy
  ),
  sct_drg = c(
    "014",   # Allogeneic bone marrow transplant
    "016",   # Autologous BMT w CC/MCC or T-cell immunotherapy
    "017"    # Autologous BMT w/o CC/MCC
    # NOTE: DRG 015 deleted FY2012, omitted per research pitfall 4
  ),
```

**Per D-11: Revenue codes (new vectors):**
```r
  # Revenue codes (PROCEDURES table PX_TYPE = "RE")
  chemo_revenue = c(
    "0331",   # Chemo - injected
    "0332",   # Chemo - oral
    "0335"    # Chemo - IV push
  ),
  radiation_revenue = c(
    "0330",   # General classification (therapeutic radiology)
    "0333"    # Radiation therapy
  ),
  sct_revenue = c(
    "0362",   # Organ transplant - other than kidney (includes SCT)
    "0815"    # Allogeneic stem cell acquisition/donor services
  )
```

Also update the section header comment for TREATMENT_CODES (around line 271) to mention expanded Phase 9 sources:
```r
# Used by 03_cohort_predicates.R to identify chemotherapy, radiation, and SCT
# evidence in PROCEDURES (CPT/HCPCS, ICD-9-CM, ICD-10-PCS, revenue codes),
# PRESCRIBING (RXNORM), DISPENSING (RXNORM), MED_ADMIN (RXNORM),
# DIAGNOSIS (Z/V codes), and ENCOUNTER (DRG codes) tables.
```
  </action>
  <verify>
    <automated>grep -c "chemo_drg\|radiation_drg\|sct_drg\|chemo_revenue\|radiation_revenue\|sct_revenue\|chemo_dx_icd10\|chemo_dx_icd9\|radiation_dx_icd10\|radiation_dx_icd9\|sct_dx_icd10" R/00_config.R</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains `chemo_drg = c("837", "838", "839", "846", "847", "848")`
    - R/00_config.R contains `radiation_drg = c("849")`
    - R/00_config.R contains `sct_drg = c("014", "016", "017")`
    - R/00_config.R contains `chemo_revenue = c("0331", "0332", "0335")`
    - R/00_config.R contains `radiation_revenue = c("0330", "0333")`
    - R/00_config.R contains `sct_revenue = c("0362", "0815")`
    - R/00_config.R contains `chemo_dx_icd10 = c("Z51.11", "Z51.12")`
    - R/00_config.R contains `chemo_dx_icd9 = c("V58.11", "V58.12")`
    - R/00_config.R contains `radiation_dx_icd10 = c("Z51.0")`
    - R/00_config.R contains `radiation_dx_icd9 = c("V58.0")`
    - R/00_config.R contains `sct_dx_icd10 = c("Z94.84", "T86.5", "T86.09", "Z48.290", "T86.0")`
    - R/00_config.R contains `chemo_icd9 = c("99.25", "99.28")` (expanded per D-07)
    - grep count for the 11 new vector names returns 11 (one definition per vector)
  </acceptance_criteria>
  <done>TREATMENT_CODES list contains all 11 new code list vectors plus the expanded chemo_icd9, with commented source references for every code</done>
</task>

<task type="auto">
  <name>Task 2: Add DISPENSING and MED_ADMIN table loading to 01_load_pcornet.R</name>
  <files>R/01_load_pcornet.R</files>
  <read_first>
    - R/01_load_pcornet.R (current table specs and PCORNET_TABLES vector, full file)
    - R/00_config.R (PCORNET_TABLES and PCORNET_PATHS usage around lines 55-72)
    - .planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-RESEARCH.md (DISPENSING_SPEC and MED_ADMIN_SPEC col_types in "Pattern 3" and "Code Examples > Common Operation 3")
  </read_first>
  <action>
Add DISPENSING and MED_ADMIN col_types specifications to R/01_load_pcornet.R, following the established pattern (numbered section headers, detailed comments).

**1. Add DISPENSING_SPEC after TUMOR_REGISTRY3_SPEC (after line 223):**
```r
# ------------------------------------------------------------------------------
# 10. DISPENSING (15 columns)
# ------------------------------------------------------------------------------
# Phase 9: Added for expanded treatment detection (D-08, D-15)
# RXNORM_CUI is the key matching column for chemo drugs (D-12: no NDC matching)
# DISPENSE_DATE used for treatment date anchoring
# Column list from PCORnet CDM v7.0 specification (Jan 2025)
DISPENSING_SPEC <- cols(
  DISPENSINGID = col_character(),
  PRESCRIBINGID = col_character(),
  ID = col_character(),
  DISPENSE_DATE = col_character(),       # Parsed by parse_pcornet_date()
  NDC = col_character(),
  DISPENSE_SUP = col_integer(),          # Days supply
  DISPENSE_AMT = col_double(),           # Quantity dispensed
  DISPENSE_DOSE_DISP = col_double(),
  DISPENSE_DOSE_DISP_UNIT = col_character(),
  DISPENSE_ROUTE = col_character(),
  RAW_NDC = col_character(),
  RXNORM_CUI = col_character(),          # KEY: chemo matching per D-12
  DISPENSE_SOURCE = col_character(),
  RAW_DISPENSE_MED_NAME = col_character(),
  SOURCE = col_character()
)
```

**2. Add MED_ADMIN_SPEC after DISPENSING_SPEC:**
```r
# ------------------------------------------------------------------------------
# 11. MED_ADMIN (12 columns)
# ------------------------------------------------------------------------------
# Phase 9: Added for expanded treatment detection (D-08, D-15)
# RXNORM_CUI is the key matching column for chemo drugs (D-12: no NDC matching)
# MEDADMIN_START_DATE used for treatment date anchoring
# Column list from PCORnet CDM v7.0 specification (Jan 2025)
MED_ADMIN_SPEC <- cols(
  MEDADMINID = col_character(),
  ID = col_character(),
  ENCOUNTERID = col_character(),
  PRESCRIBINGID = col_character(),
  MEDADMIN_CODE = col_character(),
  MEDADMIN_TYPE = col_character(),
  MEDADMIN_START_DATE = col_character(),  # Parsed by parse_pcornet_date()
  MEDADMIN_STOP_DATE = col_character(),   # Parsed by parse_pcornet_date()
  MEDADMIN_ROUTE = col_character(),
  RXNORM_CUI = col_character(),          # KEY: chemo matching per D-12
  RAW_MEDADMIN_MED_NAME = col_character(),
  SOURCE = col_character()
)
```

**3. Update PCORNET_TABLES vector (around line 55) to add the two new tables:**
```r
PCORNET_TABLES <- c(
  "ENROLLMENT",
  "DIAGNOSIS",
  "PROCEDURES",
  "PRESCRIBING",
  "ENCOUNTER",
  "DEMOGRAPHIC",
  "TUMOR_REGISTRY1",
  "TUMOR_REGISTRY2",
  "TUMOR_REGISTRY3",
  "DISPENSING",       # Phase 9: expanded treatment detection
  "MED_ADMIN"         # Phase 9: expanded treatment detection
)
```

**4. Update TABLE_SPECS list (around line 229) to add the two new entries:**
```r
TABLE_SPECS <- list(
  ENROLLMENT = ENROLLMENT_SPEC,
  DIAGNOSIS = DIAGNOSIS_SPEC,
  PROCEDURES = PROCEDURES_SPEC,
  PRESCRIBING = PRESCRIBING_SPEC,
  ENCOUNTER = ENCOUNTER_SPEC,
  DEMOGRAPHIC = DEMOGRAPHIC_SPEC,
  TUMOR_REGISTRY1 = TUMOR_REGISTRY1_SPEC,
  TUMOR_REGISTRY2 = TUMOR_REGISTRY2_SPEC,
  TUMOR_REGISTRY3 = TUMOR_REGISTRY3_SPEC,
  DISPENSING = DISPENSING_SPEC,          # Phase 9
  MED_ADMIN = MED_ADMIN_SPEC            # Phase 9
)
```

**5. Update file header comment (line 1-17) to reflect 11 tables instead of 9:**
- Change "Loads 9 primary tables" to "Loads 11 primary tables" in the file header comment
- Update "Primary load set: 9 tables" to "Primary load set: 11 tables"
- Add "DISPENSING" and "MED_ADMIN" to the table list in the header comment

NOTE: The existing load_pcornet_table() function and the main loading block (imap over PCORNET_PATHS) require no changes -- they dynamically iterate over PCORNET_TABLES and look up TABLE_SPECS. Adding to these vectors is sufficient. The existing null-safe file check (`if (!file.exists(file_path))`) will warn and skip if DISPENSING/MED_ADMIN CSV files are missing from the HiPerGator extract.
  </action>
  <verify>
    <automated>grep -c "DISPENSING_SPEC\|MED_ADMIN_SPEC\|DISPENSING\|MED_ADMIN" R/01_load_pcornet.R</automated>
  </verify>
  <acceptance_criteria>
    - R/01_load_pcornet.R contains `DISPENSING_SPEC <- cols(`
    - R/01_load_pcornet.R contains `MED_ADMIN_SPEC <- cols(`
    - R/01_load_pcornet.R DISPENSING_SPEC has `RXNORM_CUI = col_character()`
    - R/01_load_pcornet.R MED_ADMIN_SPEC has `RXNORM_CUI = col_character()`
    - R/01_load_pcornet.R MED_ADMIN_SPEC has `MEDADMIN_START_DATE = col_character()`
    - R/01_load_pcornet.R DISPENSING_SPEC has `DISPENSE_DATE = col_character()`
    - PCORNET_TABLES vector contains "DISPENSING" and "MED_ADMIN"
    - TABLE_SPECS list contains `DISPENSING = DISPENSING_SPEC` and `MED_ADMIN = MED_ADMIN_SPEC`
    - grep count for DISPENSING/MED_ADMIN references returns >= 8 (specs + tables + table_specs + comments)
  </acceptance_criteria>
  <done>01_load_pcornet.R loads 11 PCORnet CDM tables (9 existing + DISPENSING + MED_ADMIN) with full col_types specifications, null-safe file loading, and automatic date parsing</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "chemo_drg" R/00_config.R` returns a match
2. `grep "DISPENSING_SPEC" R/01_load_pcornet.R` returns a match
3. `grep "MED_ADMIN_SPEC" R/01_load_pcornet.R` returns a match
4. No R syntax errors: `Rscript -e "source('R/00_config.R')"` completes without error (if R is available locally)
</verification>

<success_criteria>
- TREATMENT_CODES in 00_config.R has 11 new code list vectors (DRG, revenue, diagnosis codes for all 3 treatment types) plus expanded chemo_icd9
- 01_load_pcornet.R has DISPENSING_SPEC (15 cols) and MED_ADMIN_SPEC (12 cols) with explicit col_types
- PCORNET_TABLES vector lists 11 tables
- TABLE_SPECS maps all 11 tables to their specs
- All code comments reference the decision IDs (D-07, D-08, D-09, D-10, D-11, D-12, D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/09-expand-treatment-detection-using-docx-specified-tables-and-researched-codes/09-01-SUMMARY.md`
</output>
