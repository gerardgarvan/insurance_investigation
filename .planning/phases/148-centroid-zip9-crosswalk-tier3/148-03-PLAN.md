---
phase: 148-centroid-zip9-crosswalk-tier3
plan: 03
type: execute
wave: 3
status: pending
depends_on: [148-02]
files_modified:
  - R/utils/utils_address.R
  - R/116_encounter_ses_index.R
autonomous: false
requirements: [148-D04, 148-D06]

must_haves:
  truths:
    - "zip5_representative added to .classify_zip9_source() in utils_address.R"
    - "R/116 joins zip5_adi_summary.csv and assigns zip9_source = zip5_representative for hits"
    - "R/88 passes (778 or more checks; no new failures beyond the 3 pre-existing ones)"
    - "Pre-148 encounter_ses_index output archived with _pre148 suffix before re-run"
    - "R/116 re-run produces encounter_ses_index with zip5_representative rows"
    - "D-04(b) and D-04(c) figures recorded"
  artifacts:
    - path: "R/utils/utils_address.R"
      provides: "zip5_representative zip9_source value in .classify_zip9_source()"
    - path: "R/116_encounter_ses_index.R"
      provides: "zip5_adi_summary.csv join + adi_natrank_zip5_median column"
  key_links:
    - from: "data/reference/zip5_adi_summary.csv"
      to: "R/116_encounter_ses_index.R"
      via: "left_join on ZIP5 after modal tier; produces adi_natrank_zip5_median"
    - from: ".classify_zip9_source()"
      to: "encounter_ses zip9_source column"
      via: "zip5_representative branch fires when adi_zip5 join hits"
---

<objective>
Add `zip5_representative` to utils_address.R, wire R/116 to join zip5_adi_summary.csv, archive
pre-148 outputs, re-run R/116 on HiPerGator, and record D-04(b) and D-04(c).

Purpose: The reference file from Wave 2 is inert until R/116 joins it and utils_address.R
knows the new zip9_source value. This wave makes the code change, validates it with R/88,
then triggers the HiPerGator re-run to measure actual encounter coverage.

Output: Updated utils_address.R and R/116; encounter_ses_index rebuilt with zip5_representative rows;
D-04(b) and D-04(c) recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-centroid-zip9-crosswalk-tier3/148-CONTEXT.md
@.planning/phases/148-centroid-zip9-crosswalk-tier3/148-02-SUMMARY.md
@R/utils/utils_address.R
@R/116_encounter_ses_index.R
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add zip5_representative to utils_address.R + wire R/116 join</name>
  <files>R/utils/utils_address.R, R/116_encounter_ses_index.R</files>
  <behavior>
    - After the change, approximate_zip9() assigns zip9_source = "zip5_representative"
      for rows where ZIP5 matched zip5_adi_summary.csv but had no ZIP9 via modal tier
    - zip9_source = "zip5_centroid" is UNCHANGED — its branch still fires if
      zip5_centroid_zip9_crosswalk.csv is staged (separate file, separate path)
    - R/116 loads zip5_adi_summary.csv when file exists at ADI_SUMMARY_PATH;
      adds adi_natrank_zip5_median column to encounter_ses (NA when no ZIP5 match)
    - adi_natrank_zip5_median is NEVER merged with adi_natrank (ZIP9-level column)
    - R/88 smoke test passes with no new failures
  </behavior>
  <action>
**utils_address.R changes:**

In `.classify_zip9_source()`, add a `zip5_representative` branch immediately after the
`zip5_modal` branch (and before `zip5_centroid`). The function currently uses a
`centroid_zip9` column from the crosswalk lookup; the new branch uses a boolean
`has_adi_zip5` column that R/116 will attach before calling `approximate_zip9()`.

Locate the case_when block in `.classify_zip9_source()`. It currently reads approximately:
```r
zip9_source = dplyr::case_when(
  !is.na(ZIP9)          ~ "zip9_observed",
  !is.na(modal_zip9)    ~ "zip5_modal",
  !is.na(centroid_zip9) ~ "zip5_centroid",
  !is.na(ZIP5)          ~ "zip5_no_zip9",
  ...
)
```

Add after the `zip5_modal` arm:
```r
  isTRUE(has_adi_zip5)  ~ "zip5_representative",
```

Where `has_adi_zip5` is a logical column that R/116 will set to `TRUE` for rows that have
a ZIP5 match in zip5_adi_summary.csv and would otherwise land in `zip5_no_zip9`.

IMPORTANT: This column must default to `FALSE` / `NA` in `.empty_zip5_lookup()` so the
branch does not fire when the file is absent. Add `has_adi_zip5 = logical()` to the empty
lookup tibble in `.empty_zip5_lookup()`.

Update the summary glue message at the end of `approximate_zip9()` to include:
```r
glue("  zip5_representative:   {get_count('zip5_representative')}"),
```

Do NOT alter the `zip5_centroid` branch — it fires only when
`zip5_centroid_zip9_crosswalk.csv` is staged (separate file).

**R/116 changes:**

1. Add constant near the other path constants:
```r
ADI_SUMMARY_PATH <- here::here("data", "reference", "zip5_adi_summary.csv")
```

2. Add probe in the reference-file probe section (near `has_adi`):
```r
has_adi_summary <- probe_reference(ADI_SUMMARY_PATH, "ADI_SUMMARY")
```

3. After the modal-tier join and before `approximate_zip9()` is called, add a conditional
   join that attaches `has_adi_zip5`:
```r
if (has_adi_summary) {
  adi_summary <- vroom::vroom(
    ADI_SUMMARY_PATH,
    col_types = vroom::cols(
      ZIP5               = vroom::col_character(),
      adi_natrank_median = vroom::col_double(),
      adi_natrank_p25    = vroom::col_double(),
      adi_natrank_p75    = vroom::col_double(),
      n_zip9_in_zip5     = vroom::col_integer(),
      n_zip9_with_adi    = vroom::col_integer(),
      vintage            = vroom::col_character(),
      method             = vroom::col_character(),
      source             = vroom::col_character()
    ),
    progress = FALSE
  ) |>
    dplyr::select(ZIP5, adi_natrank_zip5_median = adi_natrank_median) |>
    dplyr::mutate(has_adi_zip5 = TRUE)

  zip_resolved_raw <- zip_resolved_raw |>
    dplyr::left_join(adi_summary, by = "ZIP5") |>
    dplyr::mutate(has_adi_zip5 = dplyr::coalesce(has_adi_zip5, FALSE))

  message(glue::glue("  ADI summary staged: {nrow(adi_summary):,} ZIP5s"))
} else {
  zip_resolved_raw <- zip_resolved_raw |>
    dplyr::mutate(has_adi_zip5 = FALSE, adi_natrank_zip5_median = NA_real_)
  message("  ADI summary not staged — zip5_representative tier inactive")
}
```

4. In the final `encounter_ses` select() call, add `adi_natrank_zip5_median` as a column
   alongside `adi_natrank`. These are two distinct measurements — never coalesce them.

5. In the zip9_source count/coverage section, include `zip5_representative` in the tallies
   (already works if `.classify_zip9_source()` is updated correctly).

**Code style:** all dplyr, here(), vroom with col_types, named variables, no setwd().
</action>
  <verify>
    <automated>Rscript --vanilla --parse-only R/utils/utils_address.R && Rscript --vanilla --parse-only R/116_encounter_ses_index.R && grep -q "zip5_representative" R/utils/utils_address.R && grep -q "ADI_SUMMARY_PATH" R/116_encounter_ses_index.R && grep -q "adi_natrank_zip5_median" R/116_encounter_ses_index.R</automated>
  </verify>
  <done>
Both files parse without error; utils_address.R contains "zip5_representative"; R/116 contains
ADI_SUMMARY_PATH, has_adi_zip5 join, and adi_natrank_zip5_median column; adi_natrank_zip5_median
is never coalesced with adi_natrank.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run R/88 smoke test locally</name>
  <files></files>
  <action>
Run the smoke test against the updated source files (parse-only path on Windows; full run on
HiPerGator if available). The expected result is 778+ checks passing, with the same 3
pre-existing failures as recorded in 147-DISCOVERY.md §6 and no new failures:

Pre-existing failures (do not treat as regressions):
1. `[15ad] is_sentinel_zip5() defined in utils_address.R, exactly once`
2. `[15ae] utils_address.R guards against synthetic ZIP9s ending in '0000' (P0-01)`
3. `[Phase 114] DRUG_NAME_ALIASES has NO liposomal key`

If any new failure appears, fix it before proceeding to the HiPerGator checkpoint.

On Windows (parse-only, no data):
```
Rscript --vanilla --parse-only R/utils/utils_address.R
Rscript --vanilla --parse-only R/116_encounter_ses_index.R
```

On HiPerGator (full run):
```bash
module load R/4.4.2
Rscript R/88_smoke_test_comprehensive.R 2>&1 | tail -20
```

Record result in SUMMARY: "R/88: NNN/781 passed; N pre-existing failures; 0 new failures."
  </action>
  <verify>
    <automated>Rscript --vanilla --parse-only R/utils/utils_address.R && Rscript --vanilla --parse-only R/116_encounter_ses_index.R</automated>
  </verify>
  <done>
Parse checks pass on both files. R/88 full run (HiPerGator) shows no new failures.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
utils_address.R now has zip5_representative in .classify_zip9_source(). R/116 joins
zip5_adi_summary.csv (when staged) and adds adi_natrank_zip5_median. R/88 passes with no
new failures. Pre-148 encounter_ses_index output must be archived before re-run.
  </what-built>
  <how-to-verify>
On HiPerGator:

**Step 1 — Archive pre-148 output:**
```bash
cd /blue/erin.mobley-hl.bcu/insurance_investigation/output
ls encounter_ses_index_*.rds
# rename the most recent file:
mv encounter_ses_index_YYYYMMDD.rds encounter_ses_index_pre148.rds
```

**Step 2 — R/88 full run (if not yet done):**
```bash
module load R/4.4.2
Rscript R/88_smoke_test_comprehensive.R 2>&1 | tail -5
```
Expected: 778/781 PASS (or higher), 3 pre-existing failures, 0 new.

**Step 3 — Re-run R/116:**
```bash
Rscript R/116_encounter_ses_index.R 2>&1 | tee logs/148_r116_rerun.log
```

From the log, record:
- **D-04(b):** Line "ZIP5s needing Tier 3: NNN | covered by crosswalk: MMM"
  (or compute from the zip9_source table: count distinct ZIP5 where source = zip5_no_zip9
   and check how many appear in zip5_adi_summary.csv)
- **D-04(c):** Line "zip5_representative: NNN" from the approximate_zip9() summary

**Step 4 — Confirm adi_natrank_zip5_median column exists in output:**
```bash
Rscript -e "
  f <- tail(sort(list.files('output', pattern='encounter_ses_index.*\\.rds', full.names=TRUE)), 1)
  d <- readRDS(f)
  cat('columns:', paste(names(d), collapse=', '), '\n')
  cat('zip9_source table:\n')
  print(table(d\$zip9_source, useNA='ifany'))
  cat('adi_natrank_zip5_median non-NA:', sum(!is.na(d\$adi_natrank_zip5_median)), '\n')
"
```
  </how-to-verify>
  <resume-signal>
Paste:
1. R/88 result line (e.g. "778/781 PASS")
2. D-04(b): "ZIP5s needing Tier 3: NNN | covered: MMM"
3. D-04(c): "zip5_representative: NNN encounters"
  </resume-signal>
</task>

</tasks>

<verification>
- utils_address.R parses; contains "zip5_representative" and updated glue summary
- R/116 parses; contains ADI_SUMMARY_PATH, has_adi_zip5 join, adi_natrank_zip5_median
- R/88 passes with 0 new failures
- Pre-148 encounter_ses_index archived as _pre148.rds
- R/116 re-run produces file with zip5_representative rows
- D-04(b) and D-04(c) figures recorded in SUMMARY
</verification>

<success_criteria>
utils_address.R and R/116 updated and committed. R/88 passes. D-04(b) (cohort-relevant ZIP5
coverage — the lead figure) and D-04(c) (encounters resolved) are recorded with exact numbers
in 148-03-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/148-centroid-zip9-crosswalk-tier3/148-03-SUMMARY.md`
with D-04(b) and D-04(c) figures, R/88 result, and zip9_source table from the new R/116 output.
</output>
