---
phase: 148-centroid-zip9-crosswalk-tier3
plan: 01
type: execute
wave: 1
status: pending
depends_on: []
files_modified:
  - .planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md
  - R/118_build_centroid_crosswalk.R
autonomous: true
requirements: [148-D01, 148-D02, 148-D03, 148-D05]

must_haves:
  truths:
    - "148-DISCOVERY.md records zip5_no_zip9 = 59,818 and D-01 decision (build) with reasoning"
    - "Phase 146 D-05 is answered in 148-DISCOVERY.md (Neighborhood Atlas IS ZIP9-keyed, Route B viable)"
    - "D-02 is recorded with reasoning (Route B preferred over Route A for this cohort)"
    - "D-03 is answered: Route B chosen, no geocoding required"
    - "R/118_build_centroid_crosswalk.R is committed with all five stopifnot gates"
  artifacts:
    - path: ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md"
      provides: "D-01, D-02, D-03, D-05 decisions with evidence"
    - path: "R/118_build_centroid_crosswalk.R"
      provides: "Builder for zip5_adi_summary.csv with validation gates"
  key_links:
    - from: "147-DISCOVERY.md §4"
      to: "148-DISCOVERY.md D-01"
      via: "zip5_no_zip9 = 59,818 transcribed and decision recorded"
    - from: "data/reference/neighborhood_atlas_zip9_adi.csv"
      to: "148-DISCOVERY.md D-05"
      via: "column ZIP9 confirmed — file IS joinable on ZIP9"
---

<objective>
Record the D-01 gate decision, answer open questions D-02/D-03/D-05, and draft the build
script R/118 — all work that can run without HiPerGator.

Purpose: Gate and route are now established; this wave documents them with evidence so no
future executor has to re-derive them, and produces build code ready for Wave 2 HiPerGator
execution.

Output: 148-DISCOVERY.md (decisions record), R/118_build_centroid_crosswalk.R (build script).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-centroid-zip9-crosswalk-tier3/148-CONTEXT.md
@.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md
@R/117_build_svi_zcta.R
@R/utils/utils_address.R
@data/reference/README.md

<interfaces>
<!-- From 147-DISCOVERY.md §4 — gate values already measured -->
zip5_no_zip9  = 59,818 encounters   (Tier 3 population; material → D-01 = build)
zip5_modal    = 198,768
no_zip5       = 16,942
none          = 158,699
zip9_observed = 1,516,469

<!-- From data/reference/neighborhood_atlas_zip9_adi.csv (confirmed locally) -->
Columns: ZIP9 (character, 9-digit), ADI_NATRANK (character)
Rows: 37,029,488
Status: staged locally; gitignored (478 MB); must be scp'd to HiPerGator

<!-- From data/reference/README.md D-05 section -->
D-05 answer (recorded 2026-08-17): The Neighborhood Atlas portal provides per-state
9-digit ZIP files; column is BENE_ZIP_CD (= ZIP9). All 23-state collation complete.
File IS joinable on ZIP9 directly.

<!-- From R/utils/utils_address.R -->
CENTROID_FILENAME <- "zip5_centroid_zip9_crosswalk.csv"
# approximate_zip9() also probes for this path:
#   file.path(CONFIG$reference_dir, CENTROID_FILENAME)   -- if CONFIG exists
#   here::here("data", "reference", CENTROID_FILENAME)   -- here() preferred
#   file.path("data", "reference", CENTROID_FILENAME)    -- fallback

# The crosswalk loader validates on load:
stopifnot("centroid crosswalk missing centroid_zip9 column" =
            "centroid_zip9" %in% names(lkp))
bad <- lkp$centroid_zip9[grepl("0000$", lkp$centroid_zip9)]
# rows failing the 0000 guard are rejected before any join

# .classify_zip9_source() assigns zip9_source values:
#   "zip9_observed"   -- has ZIP9 from address record
#   "zip5_modal"      -- modal tier filled it
#   "zip5_centroid"   -- centroid crosswalk filled it
#   "zip5_no_zip9"    -- ZIP5 present, no ZIP9, no crosswalk hit
#   "no_zip5"         -- no usable ZIP5
#   "none"            -- nothing

<!-- From R/117_build_svi_zcta.R — conventions to follow -->
library(here); library(dplyr); library(vroom); library(readr)
# Named constants at top; here() for all paths; no setwd(); explicit col_types;
# vroom::cols(.default = "c") as default, then mutate to correct types
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 148-DISCOVERY.md — gate, D-05, D-02, D-03 decisions</name>
  <files>.planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md</files>
  <action>
Create `.planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md` with the
following sections. Use exact numbers from 147-DISCOVERY.md §4 — do not round or paraphrase.

**§0 — Gate (D-01)**
Record: `zip5_no_zip9 = 59,818 encounters` (source: 147-DISCOVERY.md §4, post-147-redo column).
Decision: **BUILD** — 59,818 is material per the D-01 decision table in 148-CONTEXT.md.
Reasoning: Must state explicitly that (a) this is not "no population" — an earlier artefact
showed 0 encounters, but that was from a missing coalesce arm now corrected; (b) 59,818 encounters
represent ZIP5s where no cohort member ever had an observed ZIP9, so Tier 2 (modal) could not
help; (c) at ~60K encounters the cost of building a reference file is warranted.

**§1 — Phase 146 D-05 (Neighborhood Atlas ZIP+4 file availability)**
Answer: **YES** — the Neighborhood Atlas does publish a ZIP9-keyed file.
Evidence: `data/reference/neighborhood_atlas_zip9_adi.csv` is already staged (per README.md
D-05 section, 2026-08-17). Columns: `ZIP9` (character, 9-digit), `ADI_NATRANK`. 37,029,488 rows.
23 state files collated. This file is joinable on ZIP9 directly. It IS a ZIP+4-keyed ADI file
in the sense required by §2 of the spec — each row is one delivery segment.

**§2 — Route decision (D-02): centroid crosswalk vs ZIP5-level ADI summary**
Decision: **Route B — ZIP5-level ADI summary** (`data/reference/zip5_adi_summary.csv`).
Reasoning:
- `neighborhood_atlas_zip9_adi.csv` already contains every ZIP9 with its ADI_NATRANK.
  A ZIP5-level summary requires only: group by `substr(ZIP9, 1, 5)`, compute
  median/p25/p75 of ADI_NATRANK, count n_zip9_in_zip5. No coordinates, no geocoding,
  no additional licence.
- Route A (centroid crosswalk) is an indirect path: pick one representative ZIP+4,
  then look up its ADI. Route B skips the geography and states the distribution (median + IQR),
  which is more defensible in a methods section.
- Route A also requires either USPS API bulk use (terms risk), a commercial licence
  (cost/procurement delay), or a non-centroid selection rule that would need its own
  `zip9_source` value. Route B has none of these obstacles.
- The R/116 column will be `adi_natrank_zip5_median` — clearly distinct from
  `adi_natrank` (ZIP9-level). Two measurements, two columns, no confusion.

**§3 — Build route (D-03)**
Decision: **D-03 not applicable** — Route B does not build a centroid crosswalk.
Record: `R/118_build_centroid_crosswalk.R` is repurposed to build `zip5_adi_summary.csv`.
The output file does not require D-03a/b/c routing because no centroid selection is performed.
Record that D-03a (USPS bulk API) and D-03b (commercial file) are ruled out for this phase.
D-03c (ADI ZIP+4 list as universe) is what Route B uses, but via grouping rather than selection —
there is no single "representative" ZIP+4, so the `zip9_source` value is NOT `zip5_centroid`.

**§4 — zip9_source for Route B**
Because Route B does not select one representative ZIP+4 (it summarises all of them), the
`zip9_source` value written into `encounter_ses` will be distinct from `zip5_centroid`.
The value `"zip5_representative"` must be added to `.classify_zip9_source()` in
`R/utils/utils_address.R`. This is tracked as a Wave 3 code change (see 148-03-PLAN.md).

**§5 — Figures to be recorded after Wave 2/3 (placeholders)**
D-04 figures — to be filled after HiPerGator runs:
- (a) ZIP5s resolved (rows in zip5_adi_summary.csv): `[TBD after Wave 2]`
- (b) of the ZIP5s in zip5_no_zip9 encounters, how many the summary covers: `[TBD after Wave 3]`
- (c) encounters gaining a ZIP9 via zip5_representative: `[TBD after Wave 3]`
</action>
  <verify>
    <automated>test -f ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md" && grep -q "59,818" ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md" && grep -q "Route B" ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md" && grep -q "D-05" ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md"</automated>
  </verify>
  <done>
148-DISCOVERY.md exists; contains 59,818; answers D-01 (build), D-05 (yes, ZIP9-keyed file exists),
D-02 (Route B chosen), D-03 (not applicable); explains why zip9_source cannot be "zip5_centroid".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write R/118_build_centroid_crosswalk.R — ZIP5 ADI summary builder</name>
  <files>R/118_build_centroid_crosswalk.R</files>
  <behavior>
    - Script reads `data/reference/neighborhood_atlas_zip9_adi.csv` with vroom, col_types = cols(.default = "c")
    - Derives ZIP5 = substr(ZIP9, 1, 5), filters to nchar(ZIP9) == 9 AND grepl("^[0-9]{9}$", ZIP9)
    - Converts ADI_NATRANK to numeric (suppression codes "GQ", "GQ-PH", "PH", "QDI" → NA_real_)
    - Groups by ZIP5, computes: adi_natrank_median, adi_natrank_p25, adi_natrank_p75, n_zip9_in_zip5
    - Uses na.rm = TRUE for all quantile computations
    - Runs all five §5 stopifnot gates (adapted for ADI summary):
        "ZIP5 must be 5 characters"     = all(nchar(out$ZIP5) == 5L)
        "ZIP5 must be digits only"      = all(grepl("^[0-9]{5}$", out$ZIP5))
        "no synthetic 0000 entries"     = !any(grepl("0000$", out$ZIP5))
        "ZIP5 must be unique"           = !any(duplicated(out$ZIP5))
        "n_zip9_in_zip5 must be positive" = all(out$n_zip9_in_zip5 > 0L)
    - Writes to `data/reference/zip5_adi_summary.csv` via readr::write_csv
    - Named constants: ADI_INPUT_PATH, ADI_OUTPUT_PATH, ADI_SUPPRESSION_CODES, VINTAGE
    - header comment explains D-02 choice (Route B) and references 148-DISCOVERY.md §2
  </behavior>
  <action>
Create `R/118_build_centroid_crosswalk.R`. Follow `R/117_build_svi_zcta.R` conventions:

**Header comment block:**
```
# R/118_build_centroid_crosswalk.R
# Build zip5_adi_summary.csv — ZIP5-level ADI summary (Route B, D-02).
#
# Phase 148 D-02: Route B chosen over Route A (centroid crosswalk) because
# neighborhood_atlas_zip9_adi.csv is already staged and joinable on ZIP9.
# Grouping by ZIP5 prefix gives a median + IQR without coordinates or geocoding.
# See 148-DISCOVERY.md §2 for full reasoning.
#
# Output: data/reference/zip5_adi_summary.csv
#   ZIP5                  character(5), zero-padded
#   adi_natrank_median    numeric, median ADI national rank across ZIP9s in ZIP5
#   adi_natrank_p25       numeric, 25th percentile
#   adi_natrank_p75       numeric, 75th percentile
#   n_zip9_in_zip5        integer, ZIP9 rows considered (before NA suppression)
#   n_zip9_with_adi       integer, ZIP9 rows with non-NA ADI_NATRANK
#   vintage               character, e.g. "Neighborhood Atlas 2024 v4.0.1"
#   method                character, "zip5_adi_summary_route_b"
#   source                character, citation
#
# Run on HiPerGator: Rscript R/118_build_centroid_crosswalk.R
# Prerequisites: tidyverse, vroom (already in renv.lock)
# ADI_INPUT_PATH is gitignored (478 MB); must be scp'd to HiPerGator first.
# ==============================================================================
```

**Constants section:**
```r
ADI_INPUT_PATH      <- here::here("data", "reference", "neighborhood_atlas_zip9_adi.csv")
ADI_OUTPUT_PATH     <- here::here("data", "reference", "zip5_adi_summary.csv")
ADI_SUPPRESSION_CODES <- c("GQ", "GQ-PH", "PH", "QDI")
VINTAGE             <- "Neighborhood Atlas 2024 v4.0.1"
METHOD              <- "zip5_adi_summary_route_b"
SOURCE              <- "University of Wisconsin Neighborhood Atlas (neighborhoodatlas.medicine.wisc.edu). Registration required. Redistribution: file gitignored per project policy."
```

**Step 1 — guard:**
```r
if (!file.exists(ADI_INPUT_PATH)) {
  stop(
    "ADI input file not found: ", ADI_INPUT_PATH, "\n",
    "Transfer to HiPerGator: scp data/reference/neighborhood_atlas_zip9_adi.csv ",
    "{user}@hpg.rc.ufl.edu:/blue/erin.mobley-hl.bcu/insurance_investigation/data/reference/"
  )
}
```

**Step 2 — load:**
```r
raw <- vroom::vroom(ADI_INPUT_PATH,
                    col_types = vroom::cols(.default = "c"),
                    progress  = FALSE)
message(glue::glue("Loaded {nrow(raw):,} rows from neighborhood_atlas_zip9_adi.csv"))
```

**Step 3 — clean and derive ZIP5:**
```r
clean <- raw |>
  dplyr::filter(nchar(ZIP9) == 9L, grepl("^[0-9]{9}$", ZIP9)) |>
  dplyr::mutate(
    ZIP5         = substr(ZIP9, 1, 5),
    adi_num      = dplyr::if_else(ADI_NATRANK %in% ADI_SUPPRESSION_CODES,
                                  NA_real_,
                                  suppressWarnings(as.numeric(ADI_NATRANK)))
  )
message(glue::glue("After format filter: {nrow(clean):,} rows; {dplyr::n_distinct(clean$ZIP5):,} unique ZIP5s"))
```

**Step 4 — summarise:**
```r
out <- clean |>
  dplyr::group_by(ZIP5) |>
  dplyr::summarise(
    adi_natrank_median = median(adi_num, na.rm = TRUE),
    adi_natrank_p25    = quantile(adi_num, 0.25, na.rm = TRUE),
    adi_natrank_p75    = quantile(adi_num, 0.75, na.rm = TRUE),
    n_zip9_in_zip5     = dplyr::n(),
    n_zip9_with_adi    = sum(!is.na(adi_num)),
    .groups = "drop"
  ) |>
  dplyr::mutate(
    vintage = VINTAGE,
    method  = METHOD,
    source  = SOURCE
  )
message(glue::glue("Summary: {nrow(out):,} ZIP5s; {sum(!is.na(out$adi_natrank_median)):,} with non-NA median ADI"))
```

**Step 5 — validation gates (all blocking):**
```r
stopifnot(
  "ZIP5 must be 5 characters"       = all(nchar(out$ZIP5) == 5L),
  "ZIP5 must be digits only"        = all(grepl("^[0-9]{5}$", out$ZIP5)),
  "no synthetic 0000 entries"       = !any(grepl("0000$", out$ZIP5)),
  "ZIP5 must be unique"             = !any(duplicated(out$ZIP5)),
  "n_zip9_in_zip5 must be positive" = all(out$n_zip9_in_zip5 > 0L)
)
message("All 5 validation gates passed.")
```

**Step 6 — write:**
```r
readr::write_csv(out, ADI_OUTPUT_PATH)
message(glue::glue("Written: {ADI_OUTPUT_PATH}"))
message(glue::glue("ZIP5s resolved: {nrow(out):,}"))
```

**No `setwd()`, no `read.csv()`, no `data.table`. Use `here::here()` for all paths.**
Script must be parseable by `Rscript --vanilla --parse-only R/118_build_centroid_crosswalk.R`
on Windows (no actual execution required locally).
</action>
  <verify>
    <automated>Rscript --vanilla --parse-only R/118_build_centroid_crosswalk.R && grep -q "ADI_SUPPRESSION_CODES" R/118_build_centroid_crosswalk.R && grep -q "stopifnot" R/118_build_centroid_crosswalk.R && grep -q "zip5_adi_summary_route_b" R/118_build_centroid_crosswalk.R</automated>
  </verify>
  <done>
R/118_build_centroid_crosswalk.R parses without error; contains all five stopifnot gates; uses
here(), vroom with explicit col_types, and the Route B method string; no setwd(); header
references D-02 and 148-DISCOVERY.md.
  </done>
</task>

</tasks>

<verification>
- `148-DISCOVERY.md` exists and contains: "59,818", "Route B", "D-05", "zip5_representative"
- `R/118_build_centroid_crosswalk.R` parses clean (`Rscript --vanilla --parse-only`)
- No use of `setwd()`, `read.csv()`, or `data.table` in R/118
- All five stopifnot gates present in R/118
- D-01, D-02, D-03, D-05 all answered with reasoning in DISCOVERY.md
</verification>

<success_criteria>
148-DISCOVERY.md documents the gate (59,818), the D-01 decision (build), D-05 (yes, ZIP9 file
exists), D-02 (Route B — ZIP5-level ADI summary), and explains why zip9_source will not be
"zip5_centroid". R/118 is committed, parseable, and ready to run on HiPerGator.
</success_criteria>

<output>
After completion, create `.planning/phases/148-centroid-zip9-crosswalk-tier3/148-01-SUMMARY.md`
</output>
