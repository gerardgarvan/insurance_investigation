---
phase: 53-make-dataset-that-produces-cancer-summary-template-xlsx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/53_cancer_summary.R
autonomous: false
requirements:
  - CSUM-01
  - CSUM-02
  - CSUM-03
  - CSUM-04

must_haves:
  truths:
    - "R/53_cancer_summary.R runs end-to-end producing cancer_summary.xlsx and cancer_summary.csv"
    - "Each row represents one patient + one unique cancer code from DIAGNOSIS (ICD-10, C/D prefix)"
    - "Binary confirmation columns use integer 1/0 encoding, not TRUE/FALSE"
    - "Patient-code combos with all-NA dates have 0 for all four metric columns"
    - "Description column shows cancer site category from PREFIX_MAP with code-level detail where available"
    - "All patients in DIAGNOSIS with neoplasm codes are included (not restricted to HL cohort)"
  artifacts:
    - path: "R/53_cancer_summary.R"
      provides: "Patient-code level cancer summary dataset generation"
      min_lines: 150
      contains: "cancer_summary"
    - path: "output/tables/cancer_summary.xlsx"
      provides: "Excel output with single Cancer Summary sheet"
    - path: "output/tables/cancer_summary.csv"
      provides: "CSV output with same data as xlsx"
  key_links:
    - from: "R/53_cancer_summary.R"
      to: "R/47_cancer_site_frequency.R"
      via: "Inline copy of PREFIX_MAP and classify_codes()"
      pattern: "PREFIX_MAP"
    - from: "R/53_cancer_summary.R"
      to: "R/01_load_pcornet.R"
      via: "get_pcornet_table('DIAGNOSIS')"
      pattern: "get_pcornet_table.*DIAGNOSIS"
    - from: "R/53_cancer_summary.R"
      to: "output/tables/cancer_summary.xlsx"
      via: "openxlsx2 wb$save()"
      pattern: "wb.*save.*cancer_summary"
---

<objective>
Create R/53_cancer_summary.R that produces a patient-code level dataset from the DIAGNOSIS table with date-based confirmation metrics, outputting cancer_summary.xlsx and cancer_summary.csv to output/tables/.

Purpose: Provides the underlying dataset behind the cancer_summary_template.xlsx specification -- each row is one patient + one unique neoplasm code with confirmation flags (2+ dates, 7-day gap) and date counts.

Output: R/53_cancer_summary.R script, cancer_summary.xlsx (single flat sheet), cancer_summary.csv
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-make-dataset-that-produces-cancer-summary-template-xlsx/06-CONTEXT.md
@.planning/phases/06-make-dataset-that-produces-cancer-summary-template-xlsx/06-RESEARCH.md

@R/47_cancer_site_frequency.R
@R/50_cancer_site_confirmation.R
@R/51_cancer_site_confirmation_7day.R
@R/52_all_codes_resolved.R
@R/00_config.R
@R/01_load_pcornet.R

<interfaces>
<!-- Key patterns and contracts the executor needs. Extracted from codebase. -->

From R/47_cancer_site_frequency.R (lines 49-382):
```r
# PREFIX_MAP: Named character vector mapping 3-char ICD-10 prefixes to cancer site categories
# 309 prefix mappings to 53 categories
PREFIX_MAP <- c(
  "C00" = "Lip, Oral Cavity, and Pharynx",
  "C01" = "Lip, Oral Cavity, and Pharynx",
  # ... 307 more entries ...
  "D49" = "Neoplasms of Unspecified Behavior"
)

# classify_codes(): Classifies normalized ICD-10 codes by 3-char prefix
classify_codes <- function(codes) {
  prefix3 <- substr(codes, 1, 3)
  categories <- unname(PREFIX_MAP[prefix3])
  categories
}
```

From R/50_cancer_site_confirmation.R (lines 377-432):
```r
# Data loading pattern (DIAGNOSIS, ICD-10 only, with DX_DATE):
dx_icd10 <- get_pcornet_table("DIAGNOSIS") %>%
  filter(DX_TYPE == "10") %>%
  select(ID, DX, DX_DATE) %>%
  collect()

# Code normalization:
dx_icd10 <- dx_icd10 %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\.")))

# Neoplasm filter:
dx_cancer <- dx_icd10 %>%
  filter(str_detect(DX_norm, "^[CD]"))

# 2-date confirmation (exact code level):
confirmed_exact <- dx_cancer %>%
  filter(!is.na(DX_DATE)) %>%
  distinct(ID, DX_norm, DX_DATE, category) %>%
  group_by(ID, DX_norm) %>%
  filter(n_distinct(DX_DATE) >= 2)
```

From R/51_cancer_site_confirmation_7day.R:
```r
# 7-day gap confirmation:
# filter(as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7)
```

From R/52_all_codes_resolved.R (lines 74-186):
```r
# Multi-source description cascade:
# Source 1: Phase 39-41 RDS artifacts (output/unmatched_codes_classified.rds, output/unmatched_ndc_classified.rds)
# Source 2: Phase 45 hardcoded radiation descriptions (named character vector)
# Source 3: R/00_config.R inline comments via extract_config_comments()
# Source 4: "No description available" fallback
```

From R/00_config.R:
```r
CONFIG <- list(
  output_dir = "output",
  # ... other config ...
)
```

From R/01_load_pcornet.R:
```r
# get_pcornet_table(table_name) - returns DuckDB lazy table
# close_pcornet_con() - closes DuckDB connection at script end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/53_cancer_summary.R patient-code level dataset script</name>
  <files>R/53_cancer_summary.R</files>

  <read_first>
    R/47_cancer_site_frequency.R
    R/50_cancer_site_confirmation.R
    R/51_cancer_site_confirmation_7day.R
    R/52_all_codes_resolved.R
    R/00_config.R
    R/01_load_pcornet.R
    .planning/phases/06-make-dataset-that-produces-cancer-summary-template-xlsx/06-CONTEXT.md
    .planning/phases/06-make-dataset-that-produces-cancer-summary-template-xlsx/06-RESEARCH.md
  </read_first>

  <action>
Create R/53_cancer_summary.R with the following sections. Follow the exact patterns from R/47, R/50-51, R/52.

**SECTION 0: HEADER COMMENT BLOCK**
Standard header comment block matching project convention (see R/47 line 1-20 pattern):
- Phase 6 description: patient-code level cancer summary dataset
- Inputs: DIAGNOSIS DuckDB table (ICD-10 codes)
- Outputs: output/tables/cancer_summary.xlsx, output/tables/cancer_summary.csv
- Usage: Rscript R/53_cancer_summary.R

**SECTION 1: SETUP**
```r
suppressPackageStartupMessages({
  library(dplyr)
  library(stringr)
  library(glue)
  library(openxlsx2)
})

source("R/00_config.R")
source("R/01_load_pcornet.R")

OUTPUT_XLSX <- file.path(CONFIG$output_dir, "tables", "cancer_summary.xlsx")
OUTPUT_CSV  <- file.path(CONFIG$output_dir, "tables", "cancer_summary.csv")
dir.create(dirname(OUTPUT_XLSX), showWarnings = FALSE, recursive = TRUE)

message("=== Phase 6: Cancer Summary Dataset ===")
message(glue("Output XLSX: {OUTPUT_XLSX}"))
message(glue("Output CSV:  {OUTPUT_CSV}"))
```

**SECTION 2: REUSE PREFIX_MAP AND classify_codes() FROM R/47**
Source R/47 to get PREFIX_MAP and classify_codes(). However, R/47 is a full script that runs queries when sourced. Instead, **copy PREFIX_MAP and classify_codes() definition inline** (same pattern R/50 and R/51 use -- they each define PREFIX_MAP locally). Copy the entire PREFIX_MAP named vector (all 309 prefix mappings) and the classify_codes() function from R/47 lines 49-382.

Add a comment: `# PREFIX_MAP and classify_codes() copied from R/47_cancer_site_frequency.R`
`# Keep in sync with R/47 if categories change.`

**SECTION 3: LOAD AND CLASSIFY DIAGNOSIS DATA** (per D-09, D-14, D-08)
Follow R/50 lines 377-409 exactly:
- `get_pcornet_table("DIAGNOSIS")` with `filter(DX_TYPE == "10")`, `select(ID, DX, DX_DATE)`, `collect()`
- Normalize: `mutate(DX_norm = toupper(str_remove_all(DX, "\\.")))`
- Filter neoplasm: `filter(str_detect(DX_norm, "^[CD]"))`
- Classify: `mutate(category = classify_codes(DX_norm))`
- Handle unclassified: label as "Unclassified" (same as R/47 line 417-418)
- Log row counts at each step with `message(glue(...))` and `format(..., big.mark=",")`

**SECTION 4: BUILD DESCRIPTION LOOKUP** (per D-10)
Build a code-level description lookup using the multi-source cascade from R/52 (lines 74-186):

1. Load Phase 39-41 RDS artifacts (optional, with file.exists() guard):
   - `output/unmatched_codes_classified.rds` (Phase 39)
   - `output/unmatched_ndc_classified.rds` (Phase 40)
   Extract `code` and `description` columns if present.

2. Define Phase 45 hardcoded radiation descriptions (copy the named vector from R/52 lines 115-151 -- all 31 entries from 77401 through G6016).

3. Extract R/00_config.R inline comments using the `extract_config_comments()` function pattern from R/52 lines 161-183.

4. Merge all description sources into a single lookup tibble `desc_lookup` with columns `code` and `code_description`. Prioritize: RDS > hardcoded > config comments. Deduplicate by code, keeping first occurrence.

5. Build the final description column by joining desc_lookup on `DX_norm == code`, then combining:
   - Format: `"{category} | {code_description}"` when code_description exists
   - Format: `"{category}"` when no code-level description available
   This gives both the cancer site category name AND code-level detail (per D-10).

**SECTION 5: PATIENT-CODE AGGREGATION** (per D-01, D-02, D-03, D-04, D-05, D-06, D-07)
Deduplicate first: `distinct(ID, DX_norm, DX_DATE, category)` (per Pitfall 1 from research).

Then group_by(ID, DX_norm, category) and summarise:

```r
cancer_summary <- dx_dedup %>%
  group_by(ID, DX_norm, category) %>%
  summarise(
    # D-05: Count of distinct non-NA dates
    unique_dates_total = as.integer(n_distinct(DX_DATE[!is.na(DX_DATE)])),

    # D-03: 1 if 2+ distinct non-NA dates, else 0
    two_or_more_unique_dates = as.integer(n_distinct(DX_DATE[!is.na(DX_DATE)]) >= 2),

    # D-04: 1 if 2+ distinct dates AND max-min >= 7 days, else 0
    two_or_more_unique_dates_gt_7 = as.integer({
      dates <- DX_DATE[!is.na(DX_DATE)]
      ud <- unique(dates)
      if (length(ud) >= 2) {
        as.numeric(max(ud) - min(ud)) >= 7
      } else {
        FALSE
      }
    }),

    # D-06: Count of dates that are >7 days from at least one other date
    # Interpretation (Claude's discretion): A date "contributes to spread evidence"
    # if it is >= 7 days away from at least one other date in this patient-code set.
    # If overall span < 7 days, returns 0. Otherwise, for each unique date, check
    # if it is >= 7 days from ANY other date.
    unique_dates_with_sep_gt_7 = {
      dates <- unique(sort(DX_DATE[!is.na(DX_DATE)]))
      if (length(dates) < 2) {
        0L
      } else {
        span <- as.numeric(max(dates) - min(dates))
        if (span < 7) {
          0L
        } else {
          # For each date, check if distance to any other date >= 7
          count <- 0L
          for (d in dates) {
            diffs <- abs(as.numeric(dates - d))
            # Exclude self-comparison (diff == 0)
            if (any(diffs[diffs > 0] >= 7)) {
              count <- count + 1L
            }
          }
          count
        }
      }
    },
    .groups = "drop"
  )
```

After summarise, apply D-07 safety net:
```r
cancer_summary <- cancer_summary %>%
  mutate(
    two_or_more_unique_dates = ifelse(unique_dates_total == 0L, 0L, two_or_more_unique_dates),
    two_or_more_unique_dates_gt_7 = ifelse(unique_dates_total == 0L, 0L, two_or_more_unique_dates_gt_7),
    unique_dates_with_sep_gt_7 = ifelse(unique_dates_total == 0L, 0L, unique_dates_with_sep_gt_7)
  )
```

**SECTION 6: ADD DESCRIPTION AND FINALIZE COLUMNS** (per D-02)
Join desc_lookup from Section 4. Construct description column. Rename DX_norm to cancer_code. Select final 7 columns in exact order:
`ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7`

Sort by ID, then cancer_code for deterministic output.

Log summary stats:
```r
message(glue("\n=== SUMMARY ==="))
message(glue("  Total rows: {format(nrow(cancer_summary), big.mark=',')}"))
message(glue("  Unique patients: {format(n_distinct(cancer_summary$ID), big.mark=',')}"))
message(glue("  Unique codes: {format(n_distinct(cancer_summary$cancer_code), big.mark=',')}"))
message(glue("  Rows with 2+ dates confirmed: {format(sum(cancer_summary$two_or_more_unique_dates), big.mark=',')}"))
message(glue("  Rows with 7-day gap confirmed: {format(sum(cancer_summary$two_or_more_unique_dates_gt_7), big.mark=',')}"))
```

**SECTION 7: WRITE XLSX** (per D-11, D-12, D-13, D-15, D-16, D-18)
Minimal styling -- no dark header fill (per D-18):

```r
wb <- wb_workbook()
wb$add_worksheet("Cancer Summary")

# Write data with headers (col_names = TRUE writes headers automatically)
wb$add_data(sheet = "Cancer Summary", x = as.data.frame(cancer_summary),
            start_row = 1, col_names = TRUE)

# Integer number format for columns 4-7 (the metric columns)
if (nrow(cancer_summary) > 0) {
  last_row <- 1 + nrow(cancer_summary)
  wb$add_numfmt(sheet = "Cancer Summary",
                dims = glue("D2:G{last_row}"), numfmt = "0")
}

# Auto column widths
wb$set_col_widths(sheet = "Cancer Summary", cols = 1:7, widths = "auto")

# Freeze top row
wb$freeze_pane(sheet = "Cancer Summary", first_row = TRUE)

wb$save(OUTPUT_XLSX)
message(glue("\nWrote {OUTPUT_XLSX} ({format(nrow(cancer_summary), big.mark=',')} data rows)"))
```

**SECTION 8: WRITE CSV** (per D-13, D-16)
```r
write.csv(cancer_summary, OUTPUT_CSV, row.names = FALSE)
message(glue("Wrote {OUTPUT_CSV}"))
```

**SECTION 9: CLEANUP**
```r
close_pcornet_con()
message("\n=== Phase 6 complete ===")
```
  </action>

  <verify>
    <automated>Rscript -e "lines <- readLines('R/53_cancer_summary.R'); cat('Lines:', length(lines), '\n'); stopifnot(length(lines) >= 150); stopifnot(any(grepl('cancer_summary', lines))); stopifnot(any(grepl('PREFIX_MAP', lines))); stopifnot(any(grepl('classify_codes', lines))); stopifnot(any(grepl('two_or_more_unique_dates', lines))); stopifnot(any(grepl('unique_dates_with_sep_gt_7', lines))); stopifnot(any(grepl('cancer_summary\\.xlsx', lines))); stopifnot(any(grepl('cancer_summary\\.csv', lines))); stopifnot(any(grepl('as\\.integer', lines))); stopifnot(any(grepl('get_pcornet_table', lines))); stopifnot(any(grepl('close_pcornet_con', lines))); cat('All checks passed\n')"</automated>
  </verify>

  <acceptance_criteria>
    - R/53_cancer_summary.R exists with >= 150 lines
    - File contains `PREFIX_MAP <- c(` (local copy of prefix map)
    - File contains `classify_codes <- function(codes)` (local copy of classifier)
    - File contains `get_pcornet_table("DIAGNOSIS")` (DuckDB data loading)
    - File contains `filter(DX_TYPE == "10")` (ICD-10 filter per D-09)
    - File contains `select(ID, DX, DX_DATE)` (three required columns)
    - File contains `filter(str_detect(DX_norm, "^[CD]"))` (neoplasm filter per D-08)
    - File contains `as.integer(` at least 3 times (1/0 encoding per D-03, D-04, D-06)
    - File contains `unique_dates_total` (per D-05)
    - File contains `two_or_more_unique_dates` (per D-03)
    - File contains `two_or_more_unique_dates_gt_7` (per D-04)
    - File contains `unique_dates_with_sep_gt_7` (per D-06)
    - File contains `ifelse(unique_dates_total == 0` (D-07 NA safety net)
    - File contains `cancer_summary.xlsx` and `cancer_summary.csv` output paths (per D-15, D-16)
    - File contains `wb_workbook()` (openxlsx2 per D-11)
    - File contains `write.csv(` (CSV output per D-13)
    - File contains `close_pcornet_con()` (cleanup)
    - File contains `desc_lookup` or equivalent description lookup mechanism (per D-10)
    - Final select contains exactly: ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7 (per D-02)
  </acceptance_criteria>

  <done>
    R/53_cancer_summary.R is a complete, self-contained script that loads DIAGNOSIS via DuckDB, filters to ICD-10 neoplasm codes (C/D prefix), classifies by PREFIX_MAP, computes 4 date-based confirmation metrics per patient-code combo using integer 1/0 encoding, adds a description column combining category name and code-level description, and writes both cancer_summary.xlsx (single flat sheet, minimal styling) and cancer_summary.csv to output/tables/. Script handles NA dates gracefully (0 for all metrics) and includes all patients in DIAGNOSIS (not just HL cohort).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run R/53_cancer_summary.R on HiPerGator and verify output</name>
  <files>output/tables/cancer_summary.xlsx, output/tables/cancer_summary.csv</files>

  <read_first>
    R/53_cancer_summary.R
  </read_first>

  <action>
This is a human-verify checkpoint. The user runs the script on HiPerGator and verifies the output.

What was built: R/53_cancer_summary.R -- patient-code level cancer summary dataset script that produces cancer_summary.xlsx and cancer_summary.csv from the DIAGNOSIS table via DuckDB.

How to verify:
1. On HiPerGator, navigate to the project directory
2. Run: `Rscript R/53_cancer_summary.R`
3. Verify script completes without errors and logs summary stats
4. Verify `output/tables/cancer_summary.xlsx` exists and is non-empty
5. Verify `output/tables/cancer_summary.csv` exists and is non-empty
6. Open cancer_summary.xlsx and confirm:
   - Single sheet named "Cancer Summary"
   - Column headers: ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7
   - Binary columns contain 1 and 0 (not TRUE/FALSE)
   - Description column shows category names (and code descriptions where available)
   - Data looks reasonable (spot check a few patient-code rows)
7. Quick sanity: `Rscript -e "d <- read.csv('output/tables/cancer_summary.csv'); cat('Rows:', nrow(d), 'Patients:', length(unique(d$ID)), 'Codes:', length(unique(d$cancer_code)), '\n'); cat('Col types:', paste(sapply(d[4:7], class), collapse=', '), '\n')"`
   - Expect: integer types for columns 4-7, reasonable row/patient/code counts

Resume signal: Type "approved" if output looks correct, or describe any issues.
  </action>

  <verify>
    <automated>MISSING -- Task 2 is a human-verify checkpoint; verification happens on HiPerGator by the user</automated>
  </verify>

  <acceptance_criteria>
    - output/tables/cancer_summary.xlsx exists and is non-empty
    - output/tables/cancer_summary.csv exists and is non-empty
    - xlsx has single sheet named "Cancer Summary"
    - Column headers match exactly: ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7
    - Columns 4-7 contain integer values (1/0 for binary, counts for totals)
    - Row count is plausible (one row per patient per unique cancer code)
    - User approves output
  </acceptance_criteria>

  <done>
    Script runs successfully on HiPerGator, cancer_summary.xlsx and cancer_summary.csv are generated with correct structure and data, user has approved the output.
  </done>
</task>

</tasks>

<verification>
Phase 6 verification checks:
1. R/53_cancer_summary.R exists with all required sections (setup, PREFIX_MAP, data loading, description lookup, aggregation, xlsx output, csv output, cleanup)
2. Script produces cancer_summary.xlsx with single "Cancer Summary" sheet
3. Script produces cancer_summary.csv with identical data
4. Output has exactly 7 columns in correct order: ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7
5. Binary columns use integer 1/0 encoding (not logical TRUE/FALSE)
6. All neoplasm codes (C/D prefix) from DIAGNOSIS included, all patients included (not restricted to HL cohort)
7. Patient-code combos with all-NA DX_DATEs have 0 for all metric columns
</verification>

<success_criteria>
- R/53_cancer_summary.R runs end-to-end on HiPerGator without errors
- cancer_summary.xlsx exists in output/tables/ with correct 7-column structure
- cancer_summary.csv exists in output/tables/ with identical data
- Binary flag columns are integer 1/0
- Description column contains meaningful cancer site category names
- Row count is plausible (one row per patient per unique cancer code)
</success_criteria>

<output>
After completion, create `.planning/phases/06-make-dataset-that-produces-cancer-summary-template-xlsx/06-01-SUMMARY.md`
</output>
