---
phase: 140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook
plan: 07
type: execute
wave: 6
depends_on:
  - 140-06
files_modified:
  - R/115_zip_stability_counts.R
  - R/utils/utils_address.R
  - tests/testthat/test-115-validation-curve.R
autonomous: true
requirements: [P-07a, P-07b, P-07c]
must_haves:
  truths:
    - "Part A's universe difference from Part B/C (addr_coal's ~8,675 patients vs. the 9,282-patient cohort; ~49 non-cohort patients included, ~656 cohort patients with unmeasurable stability omitted) is stated prominently on every Part A sheet and the KEY sheet, with a live cross-check against compute_c02()'s n_cohort_absent_from_addr rather than a second hardcoded figure"
    - "Sentinel ZIP nulling (Pitfall 2) is documented as reviewed-and-closed (negligible: 1 ZIP9, 32 ZIP5 in the 08/06 run) rather than left open as an unresolved QC flag"
    - "get_zip9_at_date() gains an optional addr_full = NULL injection seam (defaulting to the existing CONFIG$data_dir load-on-demand behavior when NULL) so unit tests can exercise its logic without file I/O -- normalize_zip9()/normalize_zip5()/normalize_zip5_raw() and the D-06 load-on-demand default behavior are unchanged"
  artifacts:
    - path: "R/115_zip_stability_counts.R"
      provides: "Universe-difference documentation on Part A sheet subtitles + KEY sheet; sentinel-nulling close-out KEY field"
      contains: "n_cohort_in_addr"
    - path: "R/utils/utils_address.R"
      provides: "get_zip9_at_date(ids, dates, addr_full = NULL) with the injection seam, additive and backward-compatible"
      contains: "addr_full = NULL"
  key_links:
    - from: "get_zip9_at_date(ids, dates, addr_full = NULL)"
      to: "tests/testthat/test-115-validation-curve.R"
      via: "A test calls get_zip9_at_date() with a synthetic addr_full tibble, proving interval/fallback/none logic without CONFIG$data_dir or file I/O"
      pattern: "addr_full"
---

<objective>
Close out 140-CONTEXT.md Section 8's smaller items: (P-07a) make Part A's universe difference from Part B/C explicit and self-checking rather than implicit; (P-07b) document sentinel-nulling as reviewed-and-closed; (P-07c, optional) add a test-injection seam to `get_zip9_at_date()` so it can be unit-tested without round-tripping through `CONFIG$data_dir`.

Purpose: Part A currently runs over `addr_coal` (n = 8,675 in the 08/06 run) while Part B/C run over the 9,282-patient cohort. Overlap is ~8,626, so Part A includes ~49 non-cohort patients and OMITS ~656 cohort patients whose stability is unmeasurable -- the stability figures on A_stability_patient/A_stability_summary/A_validation_curve describe the address file, not the cohort. 140-CONTEXT.md presents two options ("re-run Part A cohort-restricted, or state the universe difference prominently on each Part A sheet") -- this plan takes the lower-risk documentation-only path (re-scoping Part A would touch most of Plan 01's original A-01..A-06 code this late in the phase); if the team later wants the fully cohort-restricted variant, that is an explicit follow-up, not silently dropped.

This is the final wave of this phase -- once executed, the next step is a real HiPerGator run of the full amended R/115 to confirm C-02 (per 140-01's resolution), review the new backward-only/forward-inclusive waterfall split (140-04), the encounter-anchored validation curve (140-06), and the analytic gap_days_at_assignment covariate (140-05) against real data, before the workbook ships to Erin/Amy.

Output: Part A sheets and the KEY sheet carry an explicit, self-checking universe-difference note; sentinel-nulling closed out in the KEY sheet; get_zip9_at_date() gains an optional, additive addr_full parameter with test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-CONTEXT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-06-SUMMARY.md

<interfaces>
Current `get_zip9_at_date()` signature (R/utils/utils_address.R, ~line 87): `get_zip9_at_date <- function(ids, dates)`. The load-on-demand read (~line 95-101) is the ONLY place that touches `CONFIG$data_dir`/`addr_path` -- everything after that operates on the resulting `addr_raw` tibble. Out of Scope (140-CONTEXT.md Section 11) explicitly forbids changes to `normalize_zip9()`/`normalize_zip5()`/`normalize_zip5_raw()` and forbids CACHING `LDS_ADDRESS_HISTORY` -- an optional `addr_full = NULL` parameter that, when NULL, preserves the exact existing load-on-demand call is additive and does not cache anything (each call still loads fresh when `addr_full` is not supplied), so it does not violate either constraint.

`compute_c02()`'s `n_cohort_absent_from_addr` (already computed, SECTION 12) is the authoritative cohort-patients-missing-from-addr_coal figure -- reuse it for the P-07a cross-check rather than re-deriving a second count that could silently drift from it.

Sentinel-nulling counts (`n_zip9_sentinel_nulled`, `n_zip5_sentinel_nulled`) are already computed and on the QC sheet (SECTION 3/13) -- this task only adds a closing status note, no new computation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Part A universe-difference documentation + sentinel-nulling close-out (P-07a, P-07b)</name>
  <files>R/115_zip_stability_counts.R</files>
  <action>
In SECTION 14 (KEY sheet `key_tbl` assembly), compute the cross-check figures right before the table is built:

```r
n_cohort_in_addr    <- length(intersect(COHORT_IDS, unique(addr_coal$ID)))
n_addr_noncohort    <- n_patients_total - n_cohort_in_addr
n_cohort_not_in_addr <- length(COHORT_IDS) - n_cohort_in_addr

stopifnot(n_cohort_not_in_addr == c02_result$n_cohort_absent_from_addr)   # P-07a: cross-check against compute_c02()'s own figure -- catches silent drift
```

Add a new `key_tbl` field (SECTION 14), e.g. `"Part A vs Part B/C universe overlap (P-07a)"`, value:

```r
glue(
  "Part A's universe is addr_coal ({n_patients_total} patients, all patients in LDS_ADDRESS_HISTORY ",
  "surviving date/sentinel filters), NOT the {length(COHORT_IDS)}-patient study cohort Part B/C use. ",
  "Overlap: {n_cohort_in_addr}. {n_addr_noncohort} addr_coal patients are non-cohort; ",
  "{n_cohort_not_in_addr} cohort patients have zero addr_coal rows and are entirely excluded from ",
  "Part A (see QC sheet n_cohort_absent_from_addr / C-02 for the cohort-scoped figure). Part A's ",
  "stability figures describe the address file, not the cohort."
)
```

Append this same note (shortened) to each Part A sheet's existing subtitle in SECTION 15 (`A_stability_patient`, `A_stability_summary`, `A_validation_curve`, `A_validation_curve_encounter_anchored`) -- e.g. append: `" Universe note: this sheet's population is addr_coal ({n_patients_total} patients), NOT the {length(COHORT_IDS)}-patient cohort Part B/C use -- see KEY sheet."`

Add a sentinel-nulling close-out field to `key_tbl` (P-07b): `"Sentinel ZIP nulling (Pitfall 2) -- status"`, value: `glue("Reviewed and closed: negligible impact ({n_zip9_sentinel_nulled} ZIP9, {n_zip5_sentinel_nulled} ZIP5 nulled in the {format(Sys.Date(), '%Y-%m-%d')} run). See QC sheet for the underlying counts.")`
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('n_cohort_in_addr', lines)), any(grepl('n_cohort_not_in_addr', lines)), any(grepl('Sentinel ZIP nulling', lines))); cat('structural checks OK\n')"</automated>
  </verify>
  <done>KEY sheet carries the Part A universe-overlap note (with a live stopifnot cross-check against compute_c02()'s own n_cohort_absent_from_addr) and the sentinel-nulling close-out note; every Part A sheet's subtitle states its universe is addr_coal, not the cohort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_zip9_at_date() optional addr_full injection seam (P-07c)</name>
  <files>R/utils/utils_address.R, tests/testthat/test-115-validation-curve.R</files>
  <behavior>
    - `get_zip9_at_date(ids, dates, addr_full = NULL)`: when `addr_full` is NULL (default), behavior is byte-for-byte identical to today (load `addr_path` via `CONFIG$data_dir`, D-06 load-on-demand, no caching). When `addr_full` is supplied (a data.frame/tibble with the same raw columns `ID`/`ADDRESS_ZIP9`/`ADDRESS_PERIOD_START`/`ADDRESS_PERIOD_END`, character-coerced), skip the file read entirely and use it as `addr_raw` directly -- all downstream normalization/interval-matching logic is unchanged.
    - Test: call `get_zip9_at_date(ids, dates, addr_full = <synthetic tibble>)` with a small synthetic address history (one patient, one interval-covering record, one most-recent-before case, one no-match case) and assert the three `match_type` outcomes ("interval", "most_recent_before", "none") without touching `CONFIG$data_dir` or performing any file I/O.
  </behavior>
  <action>
Edit `get_zip9_at_date()`'s signature and load step (~line 87-101):

```r
get_zip9_at_date <- function(ids, dates, addr_full = NULL) {
  stopifnot(length(ids) == length(dates))
  queries <- tibble(ID = as.character(ids), query_date = as.Date(dates))

  if (is.null(addr_full)) {
    ADDR_FILENAME <- "LDS_ADDRESS_HISTORY_Mailhot_V1.csv"
    addr_path <- file.path(CONFIG$data_dir, ADDR_FILENAME)

    # D-06: load on demand -- NOT precomputed/cached
    addr_raw <- tryCatch(
      vroom::vroom(addr_path, col_types = vroom::cols(.default = "c"), progress = FALSE),
      error = function(e) {
        message(glue("[utils_address] vroom failed ({conditionMessage(e)}); falling back to read.csv"))
        read.csv(addr_path, colClasses = "character", na.strings = c("", "NA"))
      }
    )
  } else {
    # P-07c: test-injection seam -- character-coerce to match the file-load path's column types
    addr_raw <- as.data.frame(lapply(addr_full, as.character), stringsAsFactors = FALSE)
  }

  ...   # everything from "Validate ID column (Pitfall 3...)" onward is UNCHANGED
```

Do not touch `normalize_zip9()`/`normalize_zip5()`/`normalize_zip5_raw()` (Out of Scope, 140-CONTEXT.md Section 11) or anything past the `addr_raw` assignment.

Add a `test_that()` block to `tests/testthat/test-115-validation-curve.R` (append) exercising `get_zip9_at_date()` via `addr_full`, covering the three `match_type` cases from this task's `<behavior>` section. This test does NOT use the `.test_env`/`sys.source()` pattern (that pattern is for R/115's own SECTION 1B functions) -- `get_zip9_at_date()` is sourced normally via `source("R/00_config.R")` at the top of the test file.
  </action>
  <verify>
    <automated>Rscript -e "library(testthat); res <- test_file('tests/testthat/test-115-validation-curve.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0); cat('testthat: 0 failures\n')" && Rscript -e "invisible(parse(file = 'R/utils/utils_address.R')); lines <- readLines('R/utils/utils_address.R'); stopifnot(any(grepl('addr_full = NULL', lines)), any(grepl('normalize_zip9 <- function', lines))); cat('signature + normalize_zip9() untouched OK\n')"</automated>
  </verify>
  <done>get_zip9_at_date() accepts an optional addr_full parameter defaulting to NULL (existing load-on-demand behavior preserved exactly when omitted); a new test exercises the function's interval/fallback/none logic via injection, without file I/O; normalize_zip9()/normalize_zip5()/normalize_zip5_raw() are byte-for-byte unchanged.</done>
</task>

</tasks>

<verification>
1. Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R'))" -- no error.
2. Rscript -e "invisible(parse(file = 'R/utils/utils_address.R'))" -- no error.
3. Rscript -e "library(testthat); test_file('tests/testthat/test-115-validation-curve.R')" -- 0 failures across all fixtures added by this entire phase (140-01 through 140-07).
4. grep "n_cohort_in_addr" R/115_zip_stability_counts.R -- present.
5. grep "addr_full = NULL" R/utils/utils_address.R -- present; grep "normalize_zip9 <- function" R/utils/utils_address.R -- still present and unchanged (confirm via diff against the pre-plan version if uncertain).
</verification>

<success_criteria>
- Part A's universe difference from Part B/C is explicit, prominent, and self-checking (not a second hardcoded number that could silently drift from compute_c02()'s own figure).
- Sentinel-nulling is documented as closed, not left as an open QC flag.
- get_zip9_at_date() gains a test-injection seam additively -- default behavior, normalize_*() functions, and the D-06 no-caching contract are all unchanged.
- This is the final plan in Phase 140. All 24 CONTEXT.md task/decision IDs (P-01a..P-07c, D-1..D-4) are addressed across 140-01 through 140-07 -- either implemented, or explicitly recorded as a team decision / deferred follow-up.
</success_criteria>

<output>
After completion, create .planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-07-SUMMARY.md
</output>
