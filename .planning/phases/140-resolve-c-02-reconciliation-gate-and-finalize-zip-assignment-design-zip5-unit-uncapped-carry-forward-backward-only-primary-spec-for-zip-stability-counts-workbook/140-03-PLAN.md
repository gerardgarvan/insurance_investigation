---
phase: 140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook
plan: 03
type: execute
wave: 2b   # AMENDED 2026-08-06 by 140-09-PATCH: was Wave 2; renumbered to 2b so 140-09
           # (Wave 2a) rewires the C-02 gate before the Wave 2.5 HiPerGator run this
           # plan's Task 3 gates on -- the run must exercise the new invariant-based gate.
depends_on:
  - 140-01
  - 140-09   # AMENDED 2026-08-06 by 140-09-PATCH: this plan's Task 3 HiPerGator run must
             # confirm 140-09's rewired c02_reconciled invariants, not the retired
             # C02_EXPECTED/C02_TOLERANCE comparison.
files_modified:
  - R/115_zip_stability_counts.R
  - tests/testthat/test-115-c02.R
autonomous: false
requirements: [P-02a, P-02b, P-02c]
must_haves:
  truths:
    - "The 4,912 unparseable period_start_dt records are classified into failure-mode categories (blank/NULL-like, malformed-but-numeric, malformed-but-textual, other) by the script itself, not just described in a planning document"
    - "The top-20 most frequent raw date strings per failure-mode category are captured in the workbook (not just category counts) so P-02b's parser-extension decision can be made from a single HiPerGator run rather than requiring a second round-trip (140-08-PATCH FIX-13)"
    - "The 280-patient pre/post-filter gap (cohort patients who lose EVERY address row to the study-period/unparseable-date filters) is reported as its own explicitly-named QC row, not left implicit in the existing pre/post C-02 comparison"
    - "Whether any classified failure-mode category is recoverable via extending parse_pcornet_date() is explicitly flagged as a follow-up decision requiring review of the REAL classification breakdown from a HiPerGator run -- not silently resolved or silently dropped"
    - "A real HiPerGator run confirms (or flags divergence from) the assumed C-02 breakdown (656 vs 9) and date-classification figures (4,912 unparseable; 280-patient filter loss) before Wave 3 (140-04) proceeds -- recorded as an explicit blocking checkpoint (140-08-PATCH FIX-14), not silently skipped"
  artifacts:
    - path: "R/115_zip_stability_counts.R"
      provides: "classify_unparseable_dates_vec()/classify_unparseable_dates() pure functions (SECTION 1B); SECTION 3 wiring capturing and classifying the actual failing raw date strings; unparseable_date_examples top-20-per-category table; QC sheet 280-patient-loss row"
      contains: "classify_unparseable_dates_vec"
    - path: "tests/testthat/test-115-c02.R"
      provides: "Fixture asserting classify_unparseable_dates()'s category counts on known synthetic bad-date strings"
      contains: "classify_unparseable_dates"
  key_links:
    - from: "classify_unparseable_dates_vec()"
      to: "unparseable_date_examples (QC sheet extra_tbl)"
      via: "Applied to addr_coal$ADDRESS_PERIOD_START[is.na(addr_coal$period_start_dt)] before the unparseable-date filter drops those rows; top-20-most-frequent raw values per category retained"
      pattern: "unparseable_date_examples"
---

<objective>
Investigate the two coverage gaps flagged in 140-CONTEXT.md Section 3, independent of the C-02 gate: (1) 4,912 unparseable `period_start_dt` records dropped by SECTION 3's date-parsing filter, and (2) the 280-patient gap between the pre-filter (385) and post-filter (665) "no usable ZIP5 ever" counts -- cohort patients who lose EVERY address row to the study-period/unparseable-date filters.

Purpose: 140-CONTEXT.md Section 3: "Independent of the [C-02] gate, two coverage numbers need an explanation before the workbook carries a completeness claim." Building the classification tool now (rather than only describing the numbers in a planning document) means the next real HiPerGator run produces an actionable breakdown immediately, instead of requiring another investigation round-trip. Per 140-08-PATCH FIX-13, category counts alone cannot answer P-02b's "is this recoverable by extending parse_pcornet_date()" question -- the actual raw strings are needed, so this plan also captures the top-20 most frequent raw values per category (LDS_ADDRESS_HISTORY is a Limited Data Set, so raw date strings are permitted in the workbook).

If D-1 (140-01 Task 2) returned option-b, this plan still executes (its output is diagnostic), but no workbook produced by this phase ships until D-1 resolves.

This plan touches the same file as 140-01 (R/115_zip_stability_counts.R) and is therefore sequenced after it (Wave 2) to avoid concurrent-edit conflicts, even though the underlying investigation is logically independent of C-02's resolution -- 140-CONTEXT.md Section 10 itself describes P-02 as running "in parallel with P-01" as an investigation effort, not as a literal same-wave code-edit constraint.

Per 140-08-PATCH FIX-14, this plan is now the last purely-diagnostic plan before assignment-logic changes begin in Wave 3 (140-04). Because every real-data question this phase depends on (P-02a's actual breakdown, P-04b's composition-versus-artifact call, P-03b's block-group tier, D-1 itself) otherwise resolves only once, after 140-07, this plan adds a blocking checkpoint (Task 3) requiring a real HiPerGator run of 140-01+140-03's diagnostic capability before Wave 3 proceeds -- so 140-04 through 140-07's structural changes are not built on unconfirmed assumptions.

Output: `classify_unparseable_dates_vec()`/`classify_unparseable_dates()` (SECTION 1B) wired into SECTION 3's date-parsing step; a new QC-sheet breakdown (category counts, folded into qc_tbl) and top-20-raw-value-per-category examples table; an explicit named "280-patient filter loss" QC row; P-02b (extending `parse_pcornet_date()`) explicitly flagged as a follow-up contingent on reviewing the real classification breakdown; a recorded, real-data-confirmed Wave-2.5 checkpoint before 140-04 starts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-CONTEXT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-01-SUMMARY.md

<interfaces>
`parse_pcornet_date()` (R/utils/utils_dates.R) already normalizes several known-bad-input patterns to NA before attempting to parse: blank strings, `"NULL"`/`"null"`/`"."`, whitespace-only strings (line 58-60), then tries ISO/US/SAS-DATE9/compact/Excel-serial formats in sequence. A string that survives ALL of those and still returns NA is genuinely unparseable by the existing parser -- this plan's classifier should distinguish "recognized-as-blank" (already handled, not a parser gap) from "attempted-and-failed" (a genuine parser gap or genuinely bad data) so P-02b's eventual real-data review is not re-litigating already-solved cases.

R/115 SECTION 3 (~line 318-357): `addr_coal` is built from `coalesce_zip5(addr_raw)` plus a `mutate()` that parses `period_start_dt`/`period_end_dt` -- the ORIGINAL raw `ADDRESS_PERIOD_START` column is still present on `addr_coal` at this point (coalesce_zip5 does not drop source columns). `n_unparseable_dates <- sum(is.na(addr_coal$period_start_dt))` is computed at ~line 353, and the `filter(!is.na(period_start_dt))` that drops those rows happens at ~line 356-357 -- the raw strings must be captured BEFORE that filter executes.

R/115 SECTION 12 (~line 1150-1163): `n_patients_no_zip5_ever_prefilter` (via `compute_c02(COHORT_IDS, coalesce_zip5(addr_raw))`) and `n_patients_no_zip5_ever` (post-filter) are ALREADY both computed -- the 280-patient gap is simply their difference; no new computation is needed for the gap itself, only an explicit named row surfacing it (P-02c). This must run AFTER 140-01's Task 1 (n_present_no_usable_zip5) since both live in the same QC-assembly area.

`add_styled_sheet()`'s `extra_tbl`/`extra_label` parameters (SECTION 15, ~line 1297-1345) are the established pattern for a second table on the same sheet, but only support ONE extra table per sheet call. Per 140-08-PATCH FIX-13, the QC sheet's `extra_tbl` slot is used for the higher-value `unparseable_date_examples` table (top-20 raw strings per category -- what P-02b actually needs); the per-category COUNT breakdown (`unparseable_date_breakdown`) is instead folded into `qc_tbl`'s own rows (one named row per category), following the file's established "each QC concept gets its own named row" pattern (e.g. `n_present_no_usable_zip5`, both already in `qc_tbl` proper, not an extra_tbl).

Per 140-08-PATCH FIX-16, `tests/testthat/test-115-c02.R` (created by 140-01) is the shared home for both 140-01's and this plan's fixtures -- append to it, do not create a third file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classify_unparseable_dates_vec() / classify_unparseable_dates() (P-02a, refactored per 140-08-PATCH FIX-13)</name>
  <files>R/115_zip_stability_counts.R, tests/testthat/test-115-c02.R</files>
  <behavior>
    - `classify_unparseable_dates_vec(raw_values)` takes a character vector of raw date strings that FAILED `parse_pcornet_date()` and returns a character vector of the SAME LENGTH, one category per element:
      - `"blank_or_null"`: NA, `""`, `"NULL"`, `"null"`, `"."`, or whitespace-only (mirrors parse_pcornet_date()'s own blank-handling list, line 58-60 of utils_dates.R -- these are recognized-as-blank, not a parser gap)
      - `"numeric_looking_but_invalid"`: non-blank, all-digit-and-separator characters (e.g. impossible dates like "20259931", or digit strings of an unexpected length)
      - `"text_looking_but_invalid"`: non-blank, contains letters, does not match any known SAS/ISO pattern
      - `"other_unrecognized"`: anything not caught above
    - `classify_unparseable_dates(raw_values)` is a thin counting wrapper: calls `classify_unparseable_dates_vec()`, returns a tibble with columns `category` and `n` (counts per category, descending).
    - Test: `classify_unparseable_dates_vec(c(NA, "", "NULL", "20259931", "not-a-date", "13FOOO2020", "###"))` returns a length-7 character vector equal to `c("blank_or_null", "blank_or_null", "blank_or_null", "numeric_looking_but_invalid", "text_looking_but_invalid", "text_looking_but_invalid", "other_unrecognized")`. `classify_unparseable_dates()` on the same input returns `blank_or_null = 3`, `numeric_looking_but_invalid = 1`, `text_looking_but_invalid = 2`, `other_unrecognized = 1`.
  </behavior>
  <action>
Add BOTH functions to SECTION 1B, immediately after `compute_c02()` (the last existing SECTION 1B function). Per 140-08-PATCH FIX-13, `classify_unparseable_dates()` is refactored into a vectorised classifier plus a counting wrapper (not a single combined function), so the top-20-raw-value table added in Task 2 can reuse the per-element category assignment directly:

```r
classify_unparseable_dates_vec <- function(raw_values) {
  is_blank <- is.na(raw_values) |
    raw_values %in% c("", "NULL", "null", ".") |
    str_detect(coalesce(raw_values, "NA_PLACEHOLDER"), "^\\s*$")
  is_blank[is.na(raw_values)] <- TRUE

  case_when(
    is_blank ~ "blank_or_null",
    str_detect(raw_values, "^[0-9/\\-: ]+$") ~ "numeric_looking_but_invalid",
    str_detect(raw_values, "[A-Za-z]") ~ "text_looking_but_invalid",
    TRUE ~ "other_unrecognized"
  )
}

classify_unparseable_dates <- function(raw_values) {
  tibble(category = classify_unparseable_dates_vec(raw_values)) %>%
    count(category, name = "n") %>%
    arrange(desc(n))
}
```

Add the corresponding `test_that()` block to `tests/testthat/test-115-c02.R` (append, per 140-08-PATCH FIX-16 -- this file already exists, created by 140-01), using `.test_env$classify_unparseable_dates_vec()` and `.test_env$classify_unparseable_dates()` per the file's `sys.source()` pattern, asserting both functions' outputs from this task's `<behavior>` section.
  </action>
  <verify>
    <automated>Rscript -e "library(testthat); res <- test_file('tests/testthat/test-115-c02.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0); cat('testthat: 0 failures')"</automated>
  </verify>
  <done>classify_unparseable_dates_vec() and classify_unparseable_dates() both exist in SECTION 1B (vectorised classifier + thin counting wrapper); testthat fixture passes with both functions' outputs confirmed on synthetic input.</done>
</task>

<task type="auto">
  <name>Task 2: Wire classification into SECTION 3 + top-20 raw-value examples table + explicit 280-patient QC row (P-02b deferral note, P-02c, 140-08-PATCH FIX-13)</name>
  <files>R/115_zip_stability_counts.R</files>
  <action>
In SECTION 3, immediately after the existing `n_unparseable_dates <- sum(is.na(addr_coal$period_start_dt))` line and its `message()` (~line 353-354) but BEFORE the `filter(!is.na(period_start_dt))` that drops those rows (~line 356-357), capture and classify the raw strings, and build the top-20-per-category examples table:

```r
unparseable_raw_sample <- addr_coal$ADDRESS_PERIOD_START[is.na(addr_coal$period_start_dt)]
unparseable_date_breakdown <- classify_unparseable_dates(unparseable_raw_sample)

# 140-08-PATCH FIX-13: counts alone cannot answer P-02b -- carry the actual strings so
# the parser-extension decision can be made from one run, not two. LDS_ADDRESS_HISTORY
# is a Limited Data Set, so raw date strings are permitted in the workbook; cap at 20
# per category regardless.
unparseable_date_examples <- tibble(raw_value = unparseable_raw_sample) %>%
  mutate(category = classify_unparseable_dates_vec(raw_value)) %>%
  count(category, raw_value, name = "n") %>%
  group_by(category) %>%
  slice_max(n, n = 20, with_ties = FALSE) %>%
  ungroup() %>%
  arrange(category, desc(n))

message("--- Unparseable period_start_dt breakdown (P-02a) ---")
print(unparseable_date_breakdown)
message(glue(
  "[R/115] Whether any category above is RECOVERABLE via extending parse_pcornet_date() (P-02b) is a ",
  "follow-up decision requiring review of this REAL breakdown from a HiPerGator run -- not resolved by ",
  "this script. If 'numeric_looking_but_invalid' or 'text_looking_but_invalid' account for a material ",
  "share, inspect unparseable_date_examples (top-20 raw values per category, on the QC sheet) directly ",
  "and consider extending R/utils/utils_dates.R's parse_pcornet_date() (out of scope for this plan)."
))
```

In SECTION 12/13's QC assembly (after 140-01's `n_present_no_usable_zip5` addition), add the explicit 280-patient filter-loss figure -- this is arithmetic on two values ALREADY computed (`n_patients_no_zip5_ever_prefilter` and `n_patients_no_zip5_ever`, both present since 139-04), not a new computation:

```r
n_patients_lost_to_filters_c02 <- n_patients_no_zip5_ever - n_patients_no_zip5_ever_prefilter
message(glue(
  "[R/115] P-02c: {n_patients_lost_to_filters_c02} cohort patients lose EVERY usable-ZIP5 address row to ",
  "the study-period/unparseable-date filters (post-filter {n_patients_no_zip5_ever} minus pre-filter ",
  "{n_patients_no_zip5_ever_prefilter}). Reported explicitly here -- not left implicit in the pre/post ",
  "comparison alone."
))
```

Add a matching named row to `qc_tbl` (SECTION 13): `"n_patients_lost_to_filters_c02 (P-02c, cohort patients who lose every usable-ZIP5 row to filters)"` with `Value = as.character(n_patients_lost_to_filters_c02)`.

Also in SECTION 13, per 140-08-PATCH FIX-13's resolution of the add_styled_sheet() one-extra_tbl-per-sheet constraint (see this plan's `<interfaces>` block), fold `unparseable_date_breakdown`'s per-category COUNTS into `qc_tbl` itself as individual named rows (since the QC sheet's single `extra_tbl` slot is used for the more decision-relevant `unparseable_date_examples` table below):

```r
unparseable_breakdown_rows <- unparseable_date_breakdown %>%
  transmute(
    Metric = glue("n_unparseable_{category} (P-02a breakdown)"),
    Value  = as.character(n)
  )
qc_tbl <- bind_rows(qc_tbl, unparseable_breakdown_rows)
```

(Adjust column names to match `qc_tbl`'s actual existing column names if they differ from `Metric`/`Value` -- follow the file's own established `qc_tbl` row-naming convention exactly.)

In SECTION 15's QC sheet `add_styled_sheet()` call, set `extra_tbl = unparseable_date_examples, extra_label = "Unparseable period_start_dt raw-value examples (top 20 per category, P-02a/140-08-PATCH FIX-13) -- whether any category is recoverable via extending parse_pcornet_date() (P-02b) requires reviewing this table on a real HiPerGator run"`.
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('unparseable_date_examples', lines)), any(grepl('unparseable_date_breakdown', lines)), any(grepl('n_patients_lost_to_filters_c02', lines)), any(grepl('classify_unparseable_dates_vec', lines))); cat('structural checks OK')"</automated>
  </verify>
  <done>SECTION 3 prints the per-category unparseable-date breakdown and builds the top-20-raw-value-per-category examples table before the filter drops those rows; QC sheet carries the breakdown as named qc_tbl rows AND the raw-value examples table as extra_tbl; the explicitly-named 280-patient filter-loss row is present; console explicitly states that P-02b (extending parse_pcornet_date()) is a follow-up decision pending real-data review, not resolved here.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run R/115 on HiPerGator and confirm the real C-02 breakdown + date-classification results before Wave 3 (140-08-PATCH FIX-14, Wave 2.5 gate)</name>
  <action>This cannot be automated -- it requires running R/115_zip_stability_counts.R against the real LDS_ADDRESS_HISTORY / PCORnet DuckDB data on HiPerGator, which is outside this environment. Present the request below; do not let Wave 3 (140-04) proceed until the user confirms the real run's output.</action>
  <what-built>140-01 and this plan (140-03) add ONLY diagnostic capability -- compute_c02()'s present-vs-absent breakdown (140-01 Task 1), classify_unparseable_dates_vec()/classify_unparseable_dates() and the unparseable_date_examples top-20-per-category table (this plan's Tasks 1-2), and the explicit 280-patient filter-loss QC row (this plan's Task 2). No assignment logic changes yet -- those start in 140-04 (Wave 3) and are designed against ASSUMPTIONS about what the real breakdown will show (656 vs 9 for C-02; 4,912 unparseable dates; 280-patient filter loss, all from 140-CONTEXT.md's prior failed HiPerGator run). This checkpoint exists so a real run with this plan's diagnostic additions confirms or corrects those assumptions before 140-04 through 140-07 build on them.</what-built>
  <how-to-verify>
    1. On HiPerGator, run `Rscript R/115_zip_stability_counts.R` against the real data with this phase's 140-01, 140-02 (Task 1b), 140-09, and 140-03 changes applied (140-04 through 140-07 do not exist yet at this point in the wave sequence -- do not wait for them).
    1b. **(140-09-PATCH FIX-21)** In the same HiPerGator session, run `Rscript -e "library(testthat); test_dir('tests/testthat')"` and report the pass/fail count. Rscript is unavailable in the Windows planning environment, so every fixture this phase has added up to this point -- 140-01's compute_c02() breakdown fixture, 140-09's compute_c02_baseline()/monotonicity fixtures (including the synthetic violation case), and this plan's classify_unparseable_dates_vec() fixtures -- has been written but never executed until this step. A brace/paren balance check (the local structural-proxy check used during planning) does not catch a wrong join key, a `case_when` returning a mismatched type, `arrange(.by_group = TRUE)` without a preceding `group_by()`, or a factor-level mismatch -- the actual failure modes this phase's new joins and reductions are exposed to. Any failure here is a real defect in code that Waves 3-6 build on, and should be resolved before 140-04 starts. Zero marginal cost -- the session is already open for the R/115 run.
    2. Report back: the real C-02 breakdown console line (140-01 Task 1's message -- n_cohort_absent_from_addr vs n_present_no_usable_zip5, and the percentages), the rewired C-02 gate's three invariants (140-09's c02a_monotone/c02b_partition/c02c_coverage and the c02_reconciled result), and the unparseable-date classification breakdown (this plan's Task 1/2 console output and the QC sheet's unparseable_date_examples table).
    3. Confirm these real figures are consistent with 140-CONTEXT.md's assumed figures (656 vs 9 for C-02; 92.9% coverage; 4,912 unparseable dates; 280-patient filter loss) -- or report the actual figures if they differ materially, so Wave 3 (140-04's S1-fold-in / backward-only waterfall design, which assumes the ZIP5-coalescing pipeline is sound) can be re-evaluated before it is built on a stale assumption.
  </how-to-verify>
  <verify>
    <automated>echo "MANUAL: real HiPerGator run output recorded in plan output/summary before Wave 3 proceeds"</automated>
  </verify>
  <done>The real run's C-02 breakdown and date-classification breakdown are recorded in this plan's execution summary, and either confirm the assumed figures or flag a material discrepancy for review before 140-04 (Wave 3) starts.</done>
  <resume-signal>Reply with the real console output (or a summary of it) for the C-02 breakdown and the unparseable-date classification breakdown, or "deferred -- proceeding on assumed figures" if the team explicitly accepts the risk of building Wave 3 on unconfirmed numbers.</resume-signal>
</task>

</tasks>

<verification>
1. Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R'))" -- no error.
2. Rscript -e "library(testthat); test_file('tests/testthat/test-115-c02.R')" -- 0 failures.
3. grep "classify_unparseable_dates_vec" R/115_zip_stability_counts.R -- present in SECTION 1B and SECTION 3.
4. grep "unparseable_date_examples" R/115_zip_stability_counts.R -- present.
5. grep "n_patients_lost_to_filters_c02" R/115_zip_stability_counts.R -- present.
6. QC sheet's add_styled_sheet() call includes extra_tbl = unparseable_date_examples.
7. Task 3's real-HiPerGator-run confirmation (or explicit "deferred") is recorded before this plan is considered complete.
</verification>

<success_criteria>
- The 4,912 unparseable-date records are classified into failure-mode categories directly by the script, ready to be reviewed on the next real HiPerGator run.
- The top-20 most frequent raw date strings per category are captured in the workbook, so P-02b's parser-extension decision does not require a second HiPerGator round-trip.
- The 280-patient filter loss is an explicit, named, independently-labeled QC figure, not something a reader has to infer by subtracting two other rows themselves.
- P-02b's parser-extension decision is explicitly flagged as deferred pending real classification output -- not silently resolved and not silently dropped.
- A real HiPerGator run of 140-01+140-03's diagnostic capability is confirmed (or explicitly deferred with acknowledged risk) before Wave 3 (140-04) begins building structural assignment-logic changes on top of it.
</success_criteria>

<output>
After completion, create .planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-03-SUMMARY.md
</output>
