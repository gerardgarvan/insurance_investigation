---
phase: 140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook
plan: 06
type: execute
wave: 5
depends_on:
  - 140-05
files_modified:
  - R/115_zip_stability_counts.R
  - tests/testthat/test-115-validation-curve.R
autonomous: true
requirements: [P-05a, P-05b]
must_haves:
  truths:
    - "A second, encounter-anchored validation curve exists alongside A-06's record-anchored curve, sampling real ADMIT_DATEs and comparing against a same-day covering address record where one exists (P-05a)"
    - "A-06's existing (record-anchored) curve is explicitly labeled a lower bound everywhere it appears (KEY sheet, A_validation_curve subtitle) -- not presented as a production accuracy estimate (P-05b)"
  artifacts:
    - path: "R/115_zip_stability_counts.R"
      provides: "build_encounter_anchored_validation_cases() pure function (SECTION 1B); A_validation_curve_encounter_anchored sheet"
      contains: "build_encounter_anchored_validation_cases"
    - path: "tests/testthat/test-115-validation-curve.R"
      provides: "Fixture proving the encounter-anchored function samples real encounter dates and excludes has_direct_zip9 == FALSE encounters (no ground truth to compare against)"
      contains: "build_encounter_anchored_validation_cases"
  key_links:
    - from: "build_encounter_anchored_validation_cases(encounters, addr_coal)"
      to: "aggregate_validation_curve() (reused unchanged from 139-02)"
      via: "Returns the same shape as build_validation_cases() (gap_bin, exact_match, zip5_match) so the existing aggregation function applies without modification"
      pattern: "build_encounter_anchored_validation_cases"
---

<objective>
Implement 140-CONTEXT.md Section 6's recommendation: build a SECOND, encounter-anchored validation curve (P-05a), and explicitly label the existing record-anchored A-06 curve as a LOWER BOUND wherever it appears (P-05b).

Purpose: A-06 is record-anchored -- it holds out an `addr_coal` record and predicts from the prior one. Address records exist BECAUSE something changed, so non-same-day pairs are sampled at or near change boundaries; encounters are not distributed that way. Compounding this, 2,339 patients were excluded from A-06 for having a single usable record -- exactly the most STABLE patients in the file (64.8% of patients have zero ZIP9 transitions, median is 0), structurally absent from the current validation. The 37.1% overall figure therefore does not describe production behavior and should not be presented as if it does.

Building `build_encounter_anchored_validation_cases()` answers the actual question production cares about: for a REAL encounter date, how often does the carry-forward rule (the SAME "most-recent-before" rule `get_zip9_at_date()` implements) predict correctly? Sampling actual `ADMIT_DATE`s (instead of address-change boundaries) and comparing against a same-day covering record naturally includes stable patients in proportion to how often they actually generate encounters, rather than excluding them by construction.

Output: `build_encounter_anchored_validation_cases()` (SECTION 1B) reusing `aggregate_validation_curve()` unchanged; a new `A_validation_curve_encounter_anchored` sheet; lower-bound labeling added to A_validation_curve's subtitle and the KEY sheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-CONTEXT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-05-SUMMARY.md

<interfaces>
`aggregate_validation_curve(validation_cases)` (SECTION 1B, ~line 127-148) requires only that its input have `gap_bin`, `exact_match`, `zip5_match` columns -- it is deliberately reusable as-is for a second sampling frame; do not modify it.

`encounter_zip` (SECTION 11) already carries `has_direct_zip9` and `direct_zip9`/`direct_zip5`/`direct_zip_period_start` (the latter added by 140-05 Task 1) for encounters with a genuine same-day covering address record -- these are the "ground truth" rows for the encounter-anchored comparison. `zip9_seq` (SECTION 4) is the spell-collapsed sequence to predict FROM.

Bin scheme (SAME as `build_validation_cases()`, for direct comparability -- do not invent a different scheme): `"0 (same-day)"`, `"0-30"`, `"31-90"`, `"91-180"`, `"181-365"`, `"366+"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_encounter_anchored_validation_cases() -- sample real ADMIT_DATEs (P-05a)</name>
  <files>R/115_zip_stability_counts.R, tests/testthat/test-115-validation-curve.R</files>
  <behavior>
    - `build_encounter_anchored_validation_cases(encounter_zip_with_direct, zip9_seq)` takes encounters with `has_direct_zip9 == TRUE` (the ground truth: a genuine same-day covering address record) and, for each, predicts ZIP9 from the most-recent `zip9_seq` spell whose `period_start_dt` is STRICTLY BEFORE the covering record's own `direct_zip_period_start` (i.e., the record just prior to the one that actually covers the encounter -- excluding the covering record itself, mirroring the backward-only "most-recent-before" rule `get_zip9_at_date()` implements). Returns the same shape as `build_validation_cases()`: `gap_bin`, `exact_match` (predicted == direct_zip9), `zip5_match` (same ZIP5 prefix), `gap_days`.
    - Test: a synthetic patient with 3 zip9_seq spells (A at day 0, B at day 100, C at day 300) and an encounter with `has_direct_zip9 = TRUE`, `direct_zip9 = C`, `direct_zip_period_start = day 300`. Predicts from spell B (day 100, the record just prior to C), gap_days = 200, exact_match = (B == C) = FALSE, zip5_match computed on ZIP5 prefixes. A second test case: an encounter whose covering record is the patient's ONLY spell (no prior spell exists) -- assert this encounter contributes zero rows (mirrors build_validation_cases()'s single-record exclusion).
  </behavior>
  <action>
Add `build_encounter_anchored_validation_cases()` to SECTION 1B, immediately after `aggregate_validation_curve()` (grouping it with the other A-06-family functions, before `classify_encounter_zip()`):

```r
build_encounter_anchored_validation_cases <- function(encounter_zip_with_direct, zip9_seq) {
  ground_truth <- encounter_zip_with_direct %>%
    filter(has_direct_zip9, !is.na(direct_zip_period_start)) %>%
    select(ID, ENCOUNTERID, ADMIT_DATE, direct_zip9, direct_zip_period_start)

  candidates <- ground_truth %>%
    left_join(
      zip9_seq %>% select(ID, zip9_norm, period_start_dt),
      by = "ID", relationship = "many-to-many"
    ) %>%
    filter(period_start_dt < direct_zip_period_start)

  nearest_prior <- candidates %>%
    group_by(ID, ENCOUNTERID, ADMIT_DATE) %>%
    slice_max(period_start_dt, n = 1, with_ties = FALSE) %>%
    ungroup()

  nearest_prior %>%
    mutate(
      predicted_zip9 = zip9_norm,
      gap_days       = as.numeric(direct_zip_period_start - period_start_dt),
      exact_match    = predicted_zip9 == direct_zip9,
      zip5_match     = substr(predicted_zip9, 1, 5) == substr(direct_zip9, 1, 5),
      gap_bin = case_when(
        gap_days == 0   ~ "0 (same-day)",
        gap_days <= 30  ~ "0-30",
        gap_days <= 90  ~ "31-90",
        gap_days <= 180 ~ "91-180",
        gap_days <= 365 ~ "181-365",
        TRUE            ~ "366+"
      )
    ) %>%
    select(ID, ENCOUNTERID, ADMIT_DATE, gap_bin, gap_days, exact_match, zip5_match)
}
```

Add the corresponding `test_that()` block to `tests/testthat/test-115-validation-curve.R` (append), covering the two cases from this task's `<behavior>` section.
  </action>
  <verify>
    <automated>Rscript -e "library(testthat); res <- test_file('tests/testthat/test-115-validation-curve.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0); cat('testthat: 0 failures\n')"</automated>
  </verify>
  <done>build_encounter_anchored_validation_cases() exists in SECTION 1B, reuses the same gap-bin scheme as build_validation_cases(), and testthat confirms both the prediction logic and the no-prior-spell exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Wire encounter-anchored curve into a new sheet + lower-bound labeling on A_validation_curve/KEY (P-05b)</name>
  <files>R/115_zip_stability_counts.R</files>
  <action>
After Part B's `encounter_zip` is fully assembled (SECTION 11, after 140-05's gap_days_at_assignment addition), call:

```r
encounter_anchored_cases <- build_encounter_anchored_validation_cases(encounter_zip, zip9_seq)
validation_curve_encounter_anchored <- aggregate_validation_curve(encounter_anchored_cases)

message("\n=== P-05a: Encounter-Anchored Validation Curve (Real ADMIT_DATEs, Second Estimate) ===")
print(validation_curve_encounter_anchored)
message(glue(
  "P-05a: {nrow(encounter_anchored_cases)} encounter-anchored test cases (from {n_distinct(encounter_anchored_cases$ID)} patients), ",
  "vs. {nrow(validation_cases)} record-anchored cases (A-06). Read these two curves alongside each other, ",
  "not as a replacement -- see KEY sheet / A_validation_curve subtitle for the lower-bound framing (P-05b)."
))
```

Add a new styled sheet in SECTION 15, positioned immediately after `A_validation_curve`:

```r
add_styled_sheet(
  wb, "A_validation_curve_encounter_anchored",
  title_text    = "A_validation_curve_encounter_anchored -- Second Estimate, Real ADMIT_DATEs (P-05a)",
  subtitle_text = glue(
    "Samples real cohort-restricted ENCOUNTER ADMIT_DATEs (not address-change boundaries), predicts via ",
    "the same backward most-recent-before rule get_zip9_at_date() applies, compares against a same-day ",
    "covering address record where one exists. Unlike A_validation_curve (record-anchored, a LOWER BOUND -- ",
    "see its subtitle), this curve naturally includes stable patients in proportion to how often they ",
    "actually generate encounters, rather than excluding single-record patients by construction."
  ),
  data_tbl      = validation_curve_encounter_anchored
)
```

Update `A_validation_curve`'s existing subtitle (SECTION 15) to add explicit lower-bound framing (P-05b), e.g. append: `" LOWER BOUND: this record-anchored sampling over-represents recently-changed addresses (address records exist BECAUSE something changed) and structurally excludes {n_excluded_no_prior} single-record patients -- the most STABLE patients in the file (64.8% of patients have zero ZIP9 transitions in the 08/06 run). Read alongside A_validation_curve_encounter_anchored (P-05a) for a second, encounter-representative estimate."`

Update the KEY sheet's `"A-06 sampling frame (139-05-PATCH FIX-01)"` field (SECTION 14) with the same lower-bound caveat, and add a new field `"A-06 vs P-05a"` briefly stating both curves exist and why they can diverge.
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('validation_curve_encounter_anchored', lines)), any(grepl('LOWER BOUND', lines)), any(grepl('A_validation_curve_encounter_anchored', lines))); cat('structural checks OK\n')"</automated>
  </verify>
  <done>A new A_validation_curve_encounter_anchored sheet exists with its own console summary; A_validation_curve's subtitle and the KEY sheet explicitly state the record-anchored curve is a lower bound, not a production estimate.</done>
</task>

</tasks>

<verification>
1. Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R'))" -- no error.
2. Rscript -e "library(testthat); test_file('tests/testthat/test-115-validation-curve.R')" -- 0 failures.
3. grep "build_encounter_anchored_validation_cases" R/115_zip_stability_counts.R -- present.
4. grep "LOWER BOUND" R/115_zip_stability_counts.R -- present on A_validation_curve's subtitle and/or KEY sheet.
5. The workbook now has 9 sheets (KEY, A_stability_patient, A_stability_summary, A_validation_curve, A_validation_curve_encounter_anchored, B_scenario_counts, B_direction_split, C_completeness, QC).
</verification>

<success_criteria>
- A second, encounter-anchored validation curve exists, reusing aggregate_validation_curve() unchanged, giving the team a second, more production-representative accuracy estimate.
- A-06's original record-anchored curve is unambiguously labeled a lower bound everywhere it appears -- never presented as a standalone production accuracy figure.
</success_criteria>

<output>
After completion, create .planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-06-SUMMARY.md
</output>
