---
phase: 140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook
plan: 04
type: execute
wave: 3
depends_on:
  - 140-01
  - 140-02
  - 140-03
files_modified:
  - R/115_zip_stability_counts.R
  - tests/testthat/test-115-scenarios.R
autonomous: false
requirements: [P-06a, P-06b, P-06c, P-06d, D-2, D-4]
must_haves:
  truths:
    - "The S1 fold-in (P-06b) is explicitly recorded as conditional on D-2 = option-a (ZIP5 as analysis unit, decided in 140-02); if D-2 returned option-b, this plan's Task 1 is NOT executed and the prior 5-level ordered assignment is retained pending P-03b (140-08-PATCH FIX-03)"
    - "S1 is no longer a distinct ordered bucket -- S1-eligible encounters resolve through the same single 'S3' branch as every other has_direct_zip5_only encounter, via one universally-applied case_when (P-06b); unordered S1 backward/forward/either counts are still reported on B_direction_split (reporting-only, not removed)"
    - "A backward-only ordered waterfall is computed independently -- via its own case_when, not by arithmetic on the forward-inclusive waterfall -- and reported as parallel columns alongside the existing forward-inclusive waterfall on C_completeness (P-06a)"
    - "Whether forward lookup becomes the PRIMARY specification (vs. remaining a sensitivity comparison) is an explicit recorded team decision (D-4), never silently assumed"
    - "The ~86% backward-only coverage figure is never computed by summing B_direction_split rows -- it comes from the independently-computed backward-only ordered waterfall only"
    - "The ordered-vs-unordered S3 discrepancy inverts (not disappears) once S1 is folded in: ordered S3 now EXCEEDS B_direction_split's unordered 'S3-eligible' count by exactly the S1-backward count, and this is stated plainly on the KEY sheet rather than claimed to be unnecessary (140-08-PATCH FIX-04)"
    - "A coverage-neutrality assertion confirms the S1 fold-in only relabels buckets and does not change total resolved coverage (140-08-PATCH FIX-04)"
  artifacts:
    - path: "R/115_zip_stability_counts.R"
      provides: "assign_scenarios(encounter_zip_flagged, backward_only) pure function (SECTION 1B); scenario_assigned (forward-inclusive) and scenario_assigned_backward_only columns assigned via direct $ assignment (not mutate()-internal self-reference); parallel-column waterfall tables"
      contains: "assign_scenarios"
    - path: "tests/testthat/test-115-scenarios.R"
      provides: "Fixture proving S1-forward-only encounters resolve to S3 in both variants, and that a forward-only S2 encounter is 'S2' under forward-inclusive but 'unresolvable' under backward-only"
      contains: "assign_scenarios"
  key_links:
    - from: "assign_scenarios(encounter_zip, backward_only = FALSE/TRUE)"
      to: "waterfall_encounter / waterfall_patient (SECTION 12)"
      via: "Two independent calls, each producing its own independently-computed cumulative waterfall, assigned via encounter_zip$scenario_assigned <- ... / encounter_zip$scenario_assigned_backward_only <- ... (direct $ assignment, per 140-08-PATCH FIX-09)"
      pattern: "scenario_assigned_backward_only"
---

<objective>
Implement 140-CONTEXT.md Section 7's two findings: (P-06b) fold S1 into S3 in the ordered scenario assignment -- S1 buys only 1.7pp and is definitionally entangled with S3 (an encounter eligible only via S1-forward is assigned S1 by the old ordered rule while remaining S3-eligible), so the implementation/documentation cost exceeds the yield; and (P-06a) add an independently-computed backward-only ordered waterfall as a parallel column set on C_completeness, since 92% of S2 occurrences (and the old S1's forward share) are forward-only, and `get_zip9_at_date()` is backward-only by design -- adopting forward lookup as primary would be a methodological change to production, not a coverage patch.

Per 140-08-PATCH FIX-03: the S1 fold-in is coherent ONLY if D-2 (decided in 140-02) lands on ZIP5 as the analysis unit -- folding S1 into S3 discards ZIP9 (+4) recovery for 33,785 encounters and resolves them at ZIP5 precision instead, which is only defensible if ZIP5 is the analysis unit this deliverable is built around. This plan therefore now depends explicitly on 140-02 (not just for file-conflict avoidance, as with 140-01/140-03, but for the logical precondition D-2 supplies). If D-2 returned option-b (defer pending the block-group re-run), Task 1 of this plan is NOT executed -- the prior 5-level ordered assignment (already_has_zip9, S1, S2, S3, unresolvable) is retained pending P-03b, and the rest of this plan's tasks are re-scoped accordingly (Task 2's waterfall/KEY-sheet updates and Task 3's D-4 checkpoint do not apply to a fold that did not happen). `assign_scenarios()` (SECTION 1B) is the single site to revert if D-2 is later reconsidered -- the fold is a contained, reversible change, not a rewrite of the eligibility logic underneath it (s1_backward/s1_forward/s1_either/s2_backward/s2_forward/s2_either/s3_eligible all stay exactly as SECTION 11 already computes them).

Purpose: 140-CONTEXT.md's recommended specification is Primary = backward-only (already_has_zip9 + S2-backward + S3) at ~86% encounter coverage, Sensitivity = forward-inclusive (full ordered waterfall) at ~96.9% -- agreement between the two preempts the obvious reviewer objection to forward inference, and divergence is itself a reportable finding. Explicitly: "Do not compute the ~86% by summing B_direction_split rows" -- those counts are unordered and overlapping; the backward-only figure requires its own recomputed ordered waterfall (P-06a's whole point).

If D-1 (140-01 Task 2) returned option-b, this plan still executes (its output is diagnostic), but no workbook produced by this phase ships until D-1 resolves.

This plan touches the same SECTION 11/12 logic 140-01/140-03 already amended (SECTION 12/13 QC assembly), so it is sequenced after both (Wave 3) to avoid concurrent-edit conflicts.

Output: `assign_scenarios()` (SECTION 1B, parameterized `backward_only`) replaces the old inline 5-level case_when, called via direct `$` assignment (not from inside `mutate()`, per 140-08-PATCH FIX-09); `scenario_assigned` (forward-inclusive, now 4 levels) and `scenario_assigned_backward_only` (new) columns on `encounter_zip`; C_completeness carries both waterfalls as parallel columns; the KEY sheet states the inverted ordered-vs-unordered S3 discrepancy plainly, plus a coverage-neutrality assertion; a recorded D-4 decision on whether forward lookup becomes primary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-CONTEXT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-01-SUMMARY.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-02-SUMMARY.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-03-SUMMARY.md

<interfaces>
Current inline ordered assignment (R/115 SECTION 11, ~line 954-968) -- being replaced by a SECTION 1B function:

encounter_zip <- encounter_zip %>%
  mutate(
    scenario_assigned = case_when(
      has_direct_zip9 ~ "already_has_zip9",
      s1_either       ~ "S1",
      s2_either       ~ "S2",
      s3_eligible     ~ "S3",
      TRUE            ~ "unresolvable"
    ),
    scenario_assigned = factor(scenario_assigned,
      levels = c("already_has_zip9", "S1", "S2", "S3", "unresolvable"))
  )

`encounter_zip` already carries (from SECTION 11's earlier eligibility joins, unchanged by this plan): `has_direct_zip9`, `has_direct_zip5_only`, `s1_backward`/`s1_forward`/`s1_either`, `s2_backward`/`s2_forward`/`s2_either`, `s3_eligible`. All of these stay as-is and continue feeding B_direction_split's unordered reporting (SECTION 15) unchanged -- this plan only replaces the ORDERED assignment logic, not the unordered eligibility columns.

`s3_eligible` is already defined as `!s1_backward` within `has_direct_zip5_only` (SECTION 11, ~line 929-931) -- i.e. it is ALREADY "has_direct_zip5_only and not reachable via S1-backward." Since S1 is being folded in universally, the new collapsed "S3" branch tests `has_direct_zip5_only` directly (the full set), not the narrower `s3_eligible` -- this is what "folding S1 into S3" means: every `has_direct_zip5_only` encounter becomes S3 in the ordered assignment now, regardless of whether it would also have matched S1. Per 140-08-PATCH FIX-04, `s3_eligible`'s OWN definition (`!s1_backward`) is deliberately left UNCHANGED (it continues feeding B_direction_split's unordered "S3-eligible" reporting row) -- redefining it to match `has_direct_zip5_only` was considered and rejected because it would silently change B_direction_split's unordered eligibility semantics, which this plan does not otherwise touch. The resulting inverted discrepancy (ordered S3 now EXCEEDS unordered "S3-eligible" by exactly the S1-backward count) is documented explicitly instead -- see Task 2.

Current waterfall computation (SECTION 12, ~line 1096-1107) uses `scenario_priority <- c(already_has_zip9 = 1L, S1 = 2L, S2 = 3L, S3 = 4L, unresolvable = 5L)` and `slice_min(scenario_rank, ...)` for the patient-level bucket -- this 5-level priority vector must become 4 levels (`already_has_zip9`, `S2`, `S3`, `unresolvable`) to match the collapsed assignment.

`add_styled_sheet()`'s existing pattern (extra_tbl for a second table) is already used for the encounter/patient split on C_completeness -- for the backward-only parallel columns, prefer joining them directly ONTO `waterfall_encounter`/`waterfall_patient` (same table, added columns) rather than a third table, per P-06a's literal "Column present" acceptance wording.

Per 140-08-PATCH FIX-16, this plan creates `tests/testthat/test-115-scenarios.R` (new file, shared with 140-05, which appends its own block later) rather than appending to the shared `tests/testthat/test-115-validation-curve.R`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assign_scenarios() -- S1-collapsed, dual-mode ordered assignment (P-06b; conditional on D-2 = option-a, 140-08-PATCH FIX-03)</name>
  <files>R/115_zip_stability_counts.R, tests/testthat/test-115-scenarios.R</files>
  <behavior>
    - IF 140-02's D-2 decision is option-a (ZIP5 as analysis unit): `assign_scenarios(encounter_zip_flagged, backward_only = FALSE)` returns its input with a `scenario_assigned` factor column (levels `already_has_zip9`, `S2`, `S3`, `unresolvable`) added, using `s2_either` when `backward_only = FALSE` and `s2_backward` when `backward_only = TRUE` as the S2 test; the S3 test is `has_direct_zip5_only` in BOTH modes (S1 folded in universally, S3 is inherently backward-only already so it does not vary by mode).
    - IF D-2 is option-b: do NOT execute this task -- leave the existing 5-level inline case_when (already_has_zip9, S1, S2, S3, unresolvable) exactly as it stands in R/115 today, and record in this plan's SUMMARY that Task 1 was skipped pending D-2's resolution.
    - Test (D-2 = option-a path): a synthetic 4-row `encounter_zip`-shaped tibble with one row per case: (a) `has_direct_zip9 = TRUE` -> `"already_has_zip9"` in both modes; (b) `has_direct_zip9 = FALSE, has_direct_zip5_only = FALSE, s2_backward = FALSE, s2_either = TRUE` (forward-only S2) -> `"S2"` under `backward_only = FALSE` but `"unresolvable"` under `backward_only = TRUE`; (c) `has_direct_zip5_only = TRUE, s1_backward = FALSE, s1_forward = TRUE` (an old S1-forward-only case) -> `"S3"` in BOTH modes, proving the fold-in is universal, not conditional; (d) none of the above -> `"unresolvable"` in both modes.
  </behavior>
  <action>
First, confirm 140-02's D-2 decision from 140-02-SUMMARY.md. If D-2 is option-b, skip the rest of this task's action (do not add assign_scenarios(), do not touch SECTION 11's existing inline case_when) and record the skip in this plan's SUMMARY -- proceed directly to the rest of this plan's tasks understanding that Task 2's waterfall/KEY-sheet work and Task 3's D-4 checkpoint are scoped only to the case where Task 1 executed.

If D-2 is option-a, add `assign_scenarios()` to SECTION 1B, immediately after `classify_encounter_zip()` (before `compute_c02()`, preserving the file's documented population order: coalesce_zip5, build_validation_cases, aggregate_validation_curve, classify_encounter_zip, assign_scenarios, compute_c02):

```r
assign_scenarios <- function(encounter_zip_flagged, backward_only = FALSE) {
  encounter_zip_flagged %>%
    mutate(
      .s2_test = if (backward_only) s2_backward else s2_either,
      scenario_assigned = case_when(
        has_direct_zip9      ~ "already_has_zip9",
        .s2_test              ~ "S2",
        has_direct_zip5_only  ~ "S3",   # 140-04 P-06b: S1 folded into S3 universally, no separate ordered bucket
        TRUE                   ~ "unresolvable"
      ),
      scenario_assigned = factor(scenario_assigned,
        levels = c("already_has_zip9", "S2", "S3", "unresolvable"))
    ) %>%
    select(-.s2_test)
}
```

In SECTION 11, replace the old inline `mutate(scenario_assigned = case_when(...))` block (~line 954-968) with two independent calls using DIRECT `$` ASSIGNMENT -- per 140-08-PATCH FIX-09, NOT from inside `mutate()`, which would reference the outer (pre-assignment) `encounter_zip` from inside its own `mutate()` call, relying on row-order stability between the outer frame and the function's return, and evaluating the full tibble twice:

```r
# 140-08-PATCH FIX-09: direct assignment -- no outer-frame reference from inside mutate()
encounter_zip$scenario_assigned <-
  assign_scenarios(encounter_zip, backward_only = FALSE)$scenario_assigned
encounter_zip$scenario_assigned_backward_only <-
  assign_scenarios(encounter_zip, backward_only = TRUE)$scenario_assigned
```

Add the corresponding `test_that()` block to `tests/testthat/test-115-scenarios.R` (new file, per 140-08-PATCH FIX-16 -- create it by copying the sourcing header / `.test_env` / `sys.source()` setup verbatim from `tests/testthat/test-115-validation-curve.R` lines 1-46, updating only the file-purpose comment to reference scenario-assignment fixtures; 140-05 appends its own block to this same file later), using `.test_env$assign_scenarios()`, covering all 4 cases from this task's `<behavior>` section.

Update the SECTION 11 console NOTE (~line 975-981, the FIX-04d note about ordered-vs-unordered S3) -- it currently explains why the ordered "S3" column undercounts because S1-forward-eligible encounters were assigned "S1" instead. That reasoning no longer applies (S1 folded into S3 universally), and per 140-08-PATCH FIX-04, the relationship has INVERTED, not disappeared. Replace the NOTE with: "NOTE (140-04 P-06b, amended by 140-08-PATCH FIX-04): S1 is no longer a distinct ordered bucket -- every has_direct_zip5_only encounter is assigned 'S3' regardless of S1-eligibility. Unordered s1_backward/s1_forward/s1_either counts remain reported on B_direction_split for transparency (reporting-only), but do not correspond to any ordered scenario_assigned level any more. The ordered S3 count now EXCEEDS B_direction_split's unordered 'S3-eligible' count by exactly the S1-backward count (s3_eligible retains its complement-of-S1-backward definition) -- see the KEY sheet for the full explanation (Task 2)."
  </action>
  <verify>
    <automated>Rscript -e "library(testthat); res <- test_file('tests/testthat/test-115-scenarios.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0); cat('testthat: 0 failures')"</automated>
  </verify>
  <done>If D-2 = option-a: assign_scenarios() exists in SECTION 1B; encounter_zip carries both scenario_assigned (forward-inclusive, 4 levels) and scenario_assigned_backward_only (new), assigned via direct $ assignment; testthat proves the S1-forward-only case resolves to S3 in both modes and the forward-only-S2 case differs between modes as expected. If D-2 = option-b: Task 1 is explicitly recorded as skipped, and the prior 5-level inline case_when is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Backward-only waterfall as parallel columns on C_completeness (P-06a) + inverted-discrepancy KEY sheet text + coverage-neutrality assertion (140-08-PATCH FIX-04)</name>
  <files>R/115_zip_stability_counts.R</files>
  <action>
(This task applies only if Task 1 executed, i.e. D-2 = option-a. If Task 1 was skipped, skip this task too and record it in the SUMMARY.)

In SECTION 12, update `scenario_priority` to the collapsed 4-level scheme and compute the forward-inclusive waterfall exactly as before but against the now-4-level `scenario_assigned` (same `count()`/`arrange()`/`cumsum()` pattern as the existing code, just fewer factor levels).

Then compute an INDEPENDENT backward-only waterfall from `scenario_assigned_backward_only` (own `count()`/`arrange()`/`cumsum()` call, not derived from the forward-inclusive table by subtraction):

```r
waterfall_encounter_backward <- encounter_zip %>%
  count(scenario_assigned_backward_only) %>%
  mutate(scenario_assigned_backward_only = factor(scenario_assigned_backward_only,
           levels = c("already_has_zip9", "S2", "S3", "unresolvable"))) %>%
  arrange(scenario_assigned_backward_only) %>%
  mutate(
    cumulative_n_backward_only   = cumsum(n),
    cumulative_pct_backward_only = round(100 * cumulative_n_backward_only / sum(n), 1)
  ) %>%
  rename(n_backward_only = n)

waterfall_encounter <- waterfall_encounter %>%
  left_join(
    waterfall_encounter_backward %>% rename(scenario_assigned = scenario_assigned_backward_only),
    by = "scenario_assigned"
  )
```

Apply the identical pattern for `waterfall_patient` / `waterfall_patient_backward` (patient-level bucket recomputed independently via its own `slice_min(scenario_rank)` against `scenario_assigned_backward_only`, since a patient's furthest-resolved scenario can differ between the two orderings -- e.g. a patient resolved only via forward-only S2 reaches "S2" under forward-inclusive but "unresolvable" under backward-only).

Add a console block reproducing 140-CONTEXT.md Section 7's headline comparison (do not compute the ~86% by summing B_direction_split -- read it directly off `waterfall_encounter`'s `cumulative_pct_backward_only` at the "S3" row):

```r
message("\n=== Part C: Backward-Only (Primary) vs Forward-Inclusive (Sensitivity) Waterfall Comparison ===")
print(waterfall_encounter %>% select(scenario_assigned, n, cumulative_pct, n_backward_only, cumulative_pct_backward_only))
message("NOTE (P-06): the backward-only cumulative_pct_backward_only figure above is computed from its own independent ordered assignment (assign_scenarios(backward_only = TRUE)) -- NEVER derived by summing B_direction_split rows, which are unordered and overlapping.")
```

Immediately after both waterfalls are fully computed, add the coverage-neutrality assertion (140-08-PATCH FIX-04) -- the fold relabels buckets; it must not change total resolved coverage:

```r
# 140-08-PATCH FIX-04: the S1 fold-in is coverage-neutral by construction
# (old already_has_zip9 + S1 + S2 + S3 = new already_has_zip9 + S2 + S3, since S1's
# encounters are a subset of has_direct_zip5_only and therefore already counted within
# the new S3 bucket). Any drift here means assign_scenarios() changed scope, not just
# labels.
stopifnot(
  sum(encounter_zip$scenario_assigned != "unresolvable") ==
    sum(!encounter_zip$has_direct_zip9 &
        (encounter_zip$s2_either | encounter_zip$has_direct_zip5_only)) +
    sum(encounter_zip$has_direct_zip9)
)
```

Update the C_completeness sheet's subtitle (SECTION 15) to explain both column sets (forward-inclusive `n`/`cumulative_n`/`cumulative_pct` vs. backward-only `n_backward_only`/`cumulative_n_backward_only`/`cumulative_pct_backward_only`, each independently computed).

Update the KEY sheet's `"Ordered vs unordered S3 (139-05-PATCH FIX-04d)"` field (SECTION 14) with the INVERTED caveat, stated plainly (140-08-PATCH FIX-04 -- do NOT claim the caveat "becomes unnecessary"; it flips sign and remains necessary):

```
Ordered vs unordered S3 (140-04 P-06b, amended by 140-08-PATCH FIX-04): S1 is no longer
a distinct ordered bucket -- every has_direct_zip5_only encounter is assigned "S3".
The ordered S3 count therefore now EXCEEDS B_direction_split's unordered "S3-eligible"
count by exactly the S1-backward count, because s3_eligible retains its
complement-of-S1-backward definition. This is the inverse of the pre-fold relationship
described by 139-05-PATCH FIX-04d. Use the ordered count for the assignment; use
"S3-eligible" only for continuity with Phase 139 outputs.
```

Update `B_scenario_counts`'s subtitle similarly (it currently references the old 5-level ordered assignment with a separate S1 row) -- note the collapse to 4 levels and cross-reference the KEY sheet field above for the ordered-vs-unordered S3 relationship.
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('scenario_assigned_backward_only', lines)), any(grepl('cumulative_pct_backward_only', lines)), any(grepl('waterfall_encounter_backward', lines)), any(grepl('EXCEEDS', lines))); cat('structural checks OK')"</automated>
  </verify>
  <done>waterfall_encounter/waterfall_patient each carry both the forward-inclusive and independently-computed backward-only cumulative columns; console prints the headline comparison with the explicit "never sum B_direction_split" caveat; the coverage-neutrality stopifnot passes; C_completeness/B_scenario_counts/KEY sheet text updated to reflect the S1 fold-in AND the inverted (not eliminated) ordered-vs-unordered S3 discrepancy.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: D-4 -- accept or reject forward lookup as the primary specification (P-06c), scope P-06d</name>
  <action>(This task applies only if Task 1 executed, i.e. D-2 = option-a. If Task 1 was skipped, skip this task too and record it in the SUMMARY -- D-4 is not actionable without the dual-mode assignment Task 1 would have built.) Present the evidence and the two options below; record the team's D-4 decision. Tasks 1-2 of this plan already implement both specifications side by side (option-a requires no further changes); option-b requires a distinct follow-up plan for P-06d, out of this plan's scope.</action>
  <decision>Should the forward-inclusive waterfall become the PRIMARY specification reported to Erin/Amy (overriding 140-CONTEXT.md's own recommendation of backward-only-primary / forward-inclusive-sensitivity)?</decision>
  <context>
92% of S2 occurrences (201,118 of 217,570) and the majority of the old S1's occurrences are forward-only -- forward lookup infers a PAST address from a FUTURE one. `get_zip9_at_date()` is backward-only by design (Phase 137, Out of Scope for modification in this phase). Adopting forward lookup as primary would therefore be a methodological change to production, not merely a coverage patch.

140-CONTEXT.md Section 7's own recommendation: keep backward-only as PRIMARY (already_has_zip9 + S2-backward + S3, ~86% of encounters) and report forward-inclusive as SENSITIVITY (~96.9%) -- agreement between the two preempts the obvious reviewer objection to forward inference, and divergence is itself a reportable finding. Task 1/2 of this plan already implement BOTH specifications side by side with the recommended framing (backward-only columns alongside forward-inclusive columns) -- this decision only changes which one is labeled "primary" in the deliverable's language, not what is computed.

**P-06d scoping:** if forward lookup IS adopted as primary, 140-CONTEXT.md requires extending `R/utils/utils_address.R` with a forward-lookup variant (a shared production utility, e.g. a `direction` parameter on `get_zip9_at_date()` or a sibling function) "rather than keeping this logic local to R/115" -- i.e. production code, not just this investigation script, would need a forward-capable lookup. That is a materially different and larger scope than anything else in this plan (touching shared, already-in-production utility code used by other consumers) and is intentionally NOT built by this plan regardless of the answer -- see options below.
  </context>
  <options>
    <option id="option-a">
      <name>Keep backward-only as primary, forward-inclusive as sensitivity (recommended, matches 140-CONTEXT.md)</name>
      <pros>No further code changes needed -- Tasks 1-2 of this plan already implement this framing. utils_address.R (Out of Scope for this phase) stays untouched. P-06d is not triggered.</pros>
      <cons>None identified beyond the general caveat that ~14% of encounters (the forward-only share) are reported only in the sensitivity comparison, not the headline primary figure.</cons>
    </option>
    <option id="option-b">
      <name>Adopt forward-inclusive as primary -- triggers P-06d</name>
      <pros>Higher headline coverage (~96.9% vs ~86%) reported as the primary figure.</pros>
      <cons>Per P-06d, this requires extending R/utils/utils_address.R (shared production utility, out of this plan's scope) with a forward-lookup variant before it can be adopted in production rather than staying local to this one investigation script. This plan does NOT build that extension -- if option-b is selected, treat P-06d as a distinct follow-up plan requiring its own planning session (different risk profile: shared utility code with other consumers, not R/115-local).</cons>
    </option>
  </options>
  <verify>
    <automated>echo "MANUAL: D-4 answer recorded in plan output/summary"</automated>
  </verify>
  <done>The team's answer (option-a or option-b) is recorded in this plan's execution summary; if option-b, P-06d is explicitly logged as a required follow-up plan, not built here.</done>
  <resume-signal>Select option-a (recommended -- this plan's Tasks 1-2 already implement it) or option-b (requires a follow-up plan for P-06d before it can be adopted in production).</resume-signal>
</task>

</tasks>

<verification>
1. Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R'))" -- no error.
2. Rscript -e "library(testthat); test_file('tests/testthat/test-115-scenarios.R')" -- 0 failures.
3. If D-2 = option-a: grep "assign_scenarios" R/115_zip_stability_counts.R -- present in SECTION 1B and SECTION 11 (via direct $ assignment, not inside mutate()).
4. grep "scenario_assigned_backward_only" R/115_zip_stability_counts.R -- present (if D-2 = option-a).
5. grep "EXCEEDS" R/115_zip_stability_counts.R -- the inverted-discrepancy KEY sheet text is present, not the old "becomes unnecessary" claim (if D-2 = option-a).
6. Task 3's D-4 decision is recorded (option-a or option-b), or explicitly marked not-applicable if D-2 = option-b.
</verification>

<success_criteria>
- The S1 fold-in is explicitly conditional on D-2 = option-a and reversible via a single site (assign_scenarios()) -- not silently assumed regardless of D-2's answer.
- S1 is no longer a distinct ordered bucket anywhere in the ordered assignment (when D-2 = option-a); unordered S1 reporting on B_direction_split is unchanged.
- Backward-only and forward-inclusive waterfalls are both independently computed and reported as parallel columns on C_completeness -- never one derived from the other by subtraction.
- The ordered-vs-unordered S3 discrepancy is documented as INVERTED (ordered exceeds unordered), not eliminated -- with a coverage-neutrality assertion proving the fold only relabels buckets.
- D-4 (forward lookup as primary) is an explicit recorded decision; P-06d (production utils_address.R extension) is explicitly scoped as a follow-up, not silently built or silently skipped.
</success_criteria>

<output>
After completion, create .planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-04-SUMMARY.md
</output>
