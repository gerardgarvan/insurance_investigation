---
phase: 140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/115_zip_stability_counts.R
  - tests/testthat/test-115-c02.R
autonomous: false
requirements: [P-01a, P-01b, P-01c, D-1]
must_haves:
  truths:
    - "The 665-vs-26 C-02 gap is broken down by the script itself into cohort-patients-absent-from-addr_coal (656, a coverage question) vs cohort-patients-present-in-addr_coal-with-no-usable-ZIP5 (9, the only population a coalescing defect could actually produce) -- not just asserted in a planning document"
    - "The team's confirmation (or non-confirmation) of the original 26-patient control total's denominator is recorded as an explicit decision (D-1), never silently assumed"
    - "If confirmed, C02_EXPECTED changes with a documented basis stated in the KEY sheet; if not confirmed, C02_EXPECTED is left unchanged and the workbook's shippability stays explicitly blocked pending team follow-up"
    - "C02_TOLERANCE is never widened to absorb this gap, regardless of which branch is taken (139-05-PATCH FIX-03d's narrowed claim is preserved)"
  artifacts:
    - path: "R/115_zip_stability_counts.R"
      provides: "compute_c02() SECTION 1B addition exposing n_present_no_usable_zip5; SECTION 12/13 console + QC sheet breakdown; C02_EXPECTED/KEY-sheet text updated per the D-1 resolution"
      contains: "n_present_no_usable_zip5"
    - path: "tests/testthat/test-115-c02.R"
      provides: "Extended/new fixture asserting compute_c02()'s present-vs-absent breakdown on a synthetic cohort"
      contains: "n_present_no_usable_zip5"
  key_links:
    - from: "compute_c02() (SECTION 1B)"
      to: "qc_tbl (SECTION 13)"
      via: "n_present_no_usable_zip5 = n_patients_no_zip5_ever - n_cohort_absent_from_addr, added as its own named QC row"
      pattern: "n_present_no_usable_zip5"
---

<objective>
Resolve Phase 139's C-02 reconciliation gate failure (computed 665 vs. expected 26, tolerance +/-5) on the real HiPerGator run. Per 140-CONTEXT.md Section 2, 656 of the 665 (98.6%) are cohort patients with ZERO rows in `addr_coal` at all -- a coverage question the FIX-03b denominator fix deliberately made visible -- and only 9 are cohort patients present in `addr_coal` with no usable ZIP5, which is the only population a genuine ZIP5-coalescing defect could actually produce. The working hypothesis is that the team's "26" control total was derived over a narrower population than the full 9,282-patient cohort, making it stale relative to the now-correctly-cohort-scoped denominator -- but this cannot be confirmed from the codebase alone; it requires the team (Erin/Amy) to state what population the original 08/04 notes' "26" was actually computed over.

Purpose: this is 140-CONTEXT.md's own blocking item -- "Nothing ships before this" (Section 10, step 1). Per Section 2's explicit instruction, do NOT widen `C02_TOLERANCE` to absorb the gap; FIX-03d narrowed it to cohort-definition drift specifically, and using it as general slack would undo that fix.

Output: `compute_c02()` exposes the present-vs-absent breakdown directly (reproducible from the script, not just this planning document); a recorded D-1 decision; `C02_EXPECTED`/KEY sheet updated (or explicitly left unresolved) per that decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-CONTEXT.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-04-SUMMARY.md

<interfaces>
Current compute_c02() (R/115_zip_stability_counts.R SECTION 1B, ~line 187-207):

compute_c02 <- function(cohort_ids, addr_coal) {
  zip5_ever <- addr_coal %>%
    group_by(ID) %>%
    summarise(has_any_zip5 = any(!is.na(zip5_coalesced)), .groups = "drop")

  c02_tbl <- tibble(ID = cohort_ids) %>%
    left_join(zip5_ever, by = "ID") %>%
    mutate(has_any_zip5 = coalesce(has_any_zip5, FALSE))

  list(
    c02_tbl = c02_tbl,
    n_patients_no_zip5_ever = sum(!c02_tbl$has_any_zip5),
    n_cohort_absent_from_addr = sum(!(cohort_ids %in% addr_coal$ID))
  )
}

Called at R/115 SECTION 12 (~line 1121): c02_result <- compute_c02(COHORT_IDS, addr_coal). C02_EXPECTED <- 26L and C02_TOLERANCE <- 5L are defined immediately after (~line 1127-1132) -- C02_TOLERANCE must not change in this plan.

qc_tbl (SECTION 13, ~line 1176-1207) already carries n_cohort_absent_from_addr and n_patients_no_zip5_ever as separate named rows -- the new n_present_no_usable_zip5 row is arithmetic on two values already computed, but the FUNCTION itself (compute_c02()) should also expose it directly so a future reader of the function's output list does not have to re-derive it by hand from two separately-named fields.

tests/testthat/test-115-validation-curve.R currently has a compute_c02() fixture ("cohort patient absent from addr_coal counts toward n_patients_no_zip5_ever (FIX-03)", ~line 198) using a 3-row synthetic cohort. Per 140-08-PATCH FIX-16, C-02-focused fixtures now live in a new sibling file, tests/testthat/test-115-c02.R (owned by 140-01 and 140-03), so the single shared validation-curve file does not keep absorbing every plan's fixtures across all of Phase 140. Create tests/testthat/test-115-c02.R by copying the sourcing header / .test_env / sys.source() setup verbatim from tests/testthat/test-115-validation-curve.R lines 1-46 (updating only the file-purpose comment to describe C-02/date-classification fixtures), move the existing FIX-03 compute_c02() test_that() block (currently at test-115-validation-curve.R ~line 198-221) into the new file, and remove it from test-115-validation-curve.R. test-115-validation-curve.R keeps its build_validation_cases()/aggregate_validation_curve()/classify_encounter_zip() fixtures unchanged -- only the compute_c02() block moves.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose the present-vs-absent C-02 breakdown (n_present_no_usable_zip5)</name>
  <files>R/115_zip_stability_counts.R, tests/testthat/test-115-c02.R</files>
  <behavior>
    - compute_c02()'s returned list gains n_present_no_usable_zip5 = n_patients_no_zip5_ever - n_cohort_absent_from_addr (patients who DO have at least one addr_coal row but none carry a usable ZIP5 -- the only population a coalescing defect could produce).
    - Fixture: a 4-patient cohort where patient A is entirely absent from addr_coal, patient B has one addr_coal row with zip5_coalesced = NA, patient C has one addr_coal row with a real zip5_coalesced, patient D has two addr_coal rows both NA. Assert n_patients_no_zip5_ever == 3 (A, B, D), n_cohort_absent_from_addr == 1 (A), n_present_no_usable_zip5 == 2 (B, D).
  </behavior>
  <action>
Edit compute_c02() in SECTION 1B (~line 187-207) to add the new field to its returned list, computed from the two values already being returned (do not re-derive via a second group_by/summarise):

compute_c02 <- function(cohort_ids, addr_coal) {
  zip5_ever <- addr_coal %>%
    group_by(ID) %>%
    summarise(has_any_zip5 = any(!is.na(zip5_coalesced)), .groups = "drop")

  c02_tbl <- tibble(ID = cohort_ids) %>%
    left_join(zip5_ever, by = "ID") %>%
    mutate(has_any_zip5 = coalesce(has_any_zip5, FALSE))

  n_no_zip5_ever   <- sum(!c02_tbl$has_any_zip5)
  n_absent_from_addr <- sum(!(cohort_ids %in% addr_coal$ID))

  list(
    c02_tbl = c02_tbl,
    n_patients_no_zip5_ever   = n_no_zip5_ever,
    n_cohort_absent_from_addr = n_absent_from_addr,
    n_present_no_usable_zip5  = n_no_zip5_ever - n_absent_from_addr   # 140-01 P-01a: the only population a
                                                                        # genuine coalescing defect could produce
  )
}

In SECTION 12 (~line 1124-1148, right after c02_result <- compute_c02(COHORT_IDS, addr_coal) and the existing message() calls), add a console line reproducing 140-CONTEXT.md Section 2's breakdown table:

message(glue(
  "[R/115] C-02 breakdown: {c02_result$n_cohort_absent_from_addr} of {c02_result$n_patients_no_zip5_ever} ",
  "({round(100 * c02_result$n_cohort_absent_from_addr / c02_result$n_patients_no_zip5_ever, 1)}%) are cohort ",
  "patients with ZERO rows in addr_coal (a coverage question); {c02_result$n_present_no_usable_zip5} are cohort ",
  "patients present in addr_coal with no usable ZIP5 (the only population a coalescing defect could produce). ",
  "See 140-CONTEXT.md Section 2 for the working hypothesis on why C02_EXPECTED may be stale."
))

In SECTION 13's qc_tbl (~line 1176-1207), add a new named row "n_present_no_usable_zip5 (C-02, cohort patients IN addr_coal with no usable ZIP5)" with Value = as.character(c02_result$n_present_no_usable_zip5), inserted immediately after the existing "n_cohort_absent_from_addr ..." row so the two sit adjacent.

Per 140-08-PATCH FIX-16, create tests/testthat/test-115-c02.R (new file; copy the sourcing header / .test_env / sys.source() setup from tests/testthat/test-115-validation-curve.R lines 1-46, updating only the file-purpose comment) and move the existing FIX-03 compute_c02() test_that() block out of tests/testthat/test-115-validation-curve.R (~line 198-221) into it, deleting the block from the old file. Extend the moved block with the 4-patient fixture from this task's behavior section, asserting all three of n_patients_no_zip5_ever, n_cohort_absent_from_addr, and n_present_no_usable_zip5 -- add the new assertions to the moved block rather than duplicating the fixture setup in a new block, since the underlying scenario (cohort patients absent from addr_coal) is the same concern this test already exercises. tests/testthat/test-115-validation-curve.R keeps its build_validation_cases()/aggregate_validation_curve()/classify_encounter_zip() fixtures unchanged -- only the compute_c02() block moves out of it.
  </action>
  <verify>
    <automated>Rscript -e "library(testthat); res <- test_file('tests/testthat/test-115-c02.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0); cat('testthat: 0 failures\n')"</automated>
  </verify>
  <done>compute_c02()'s return list includes n_present_no_usable_zip5; qc_tbl carries it as its own row adjacent to n_cohort_absent_from_addr; console prints the percentage breakdown; tests/testthat/test-115-c02.R exists (compute_c02() fixture moved here from test-115-validation-curve.R) and testthat passes with the extended fixture's 3 new assertions green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: D-1 -- confirm the 26-patient control total's original denominator</name>
  <action>Present the working hypothesis from Task 1's breakdown and the two options below; record the team's answer verbatim for Task 3 to act on. Do not proceed to Task 3 without an explicit answer captured here.</action>
  <decision>Was the 08/04 team meeting notes' "only 26 pts with no 5-digit ZIP code at any single point" computed over a narrower population than the full 9,282-patient Hodgkin lymphoma study cohort (e.g., patients present in LDS_ADDRESS_HISTORY at all, rather than the whole cohort including patients with zero address-history rows)?</decision>
  <context>
Task 1's breakdown shows 656 of the 665 gap (98.6%) are cohort patients with ZERO rows in addr_coal -- entirely a coverage question, not a coalescing defect. Only 9 cohort patients who DO appear in addr_coal end up with no usable ZIP5, which is the only population a genuine ZIP5-coalescing bug could actually produce. This strongly suggests the pipeline's ZIP5-coalescing logic (coalesce_zip5(), FIX-03b's cohort-scoped compute_c02()) is working correctly, and the "26" figure itself is stale relative to the now-correctly-cohort-scoped denominator it is being compared against.

This cannot be resolved from the codebase alone -- it requires locating the ORIGINATING team notes (08/04 meeting notes referenced throughout 139-CONTEXT.md/140-CONTEXT.md) or asking Erin/Amy directly what population "26" was computed over. No such record of the exact denominator exists in this repository (confirmed by search across .planning/ during planning).

Constraint: regardless of the answer, C02_TOLERANCE stays at 5L. FIX-03d narrowed the tolerance to cohort-definition drift specifically -- using it as general slack to absorb this gap would undo that fix and is explicitly disallowed by 140-CONTEXT.md Section 2.
  </context>
  <options>
    <option id="option-a">
      <name>Confirmed: "26" was computed over a narrower population (e.g., patients present in LDS_ADDRESS_HISTORY)</name>
      <pros>The pipeline is correct; C02_EXPECTED can be reset to a properly re-derived, cohort-scoped control total with a documented basis, and the workbook can proceed toward release once it reconciles.</pros>
      <cons>Requires the team to state (and this plan to record) the exact original denominator and, ideally, a re-derived expected value computed the same way against the current cohort -- not just "raise the number until it passes."</cons>
    </option>
    <option id="option-b">
      <name>Cannot confirm / notes indicate a cohort-wide denominator matching current scope</name>
      <pros>Avoids resetting C02_EXPECTED on a guess.</pros>
      <cons>The coalescing-defect hypothesis is back in scope (P-01c) -- this phase's later plans (which assume the ZIP5-coalescing pipeline is sound) should be treated as provisional, and the workbook must not ship, until this is investigated further outside this plan's scope.</cons>
    </option>
  </options>
  <verify>
    <automated>echo "MANUAL: D-1 answer recorded in plan output/summary"</automated>
  </verify>
  <done>The team's answer (option-a with source citation, or option-b "cannot confirm") is recorded in this plan's execution summary.</done>
  <resume-signal>Reply with the confirmed denominator and its source citation (option-a), or "cannot confirm -- escalate" (option-b), so Task 3 can apply the correct branch.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the D-1 resolution to C02_EXPECTED and the KEY sheet</name>
  <files>R/115_zip_stability_counts.R</files>
  <action>
Branch on Task 2's recorded answer.

If option-a (confirmed narrower denominator): Update C02_EXPECTED (~line 1127) to the re-derived value, with an inline comment citing the confirmed source (e.g., "re-derived 2026-08-06 per Erin/Amy confirmation: notes counted patients present in LDS_ADDRESS_HISTORY, not the full cohort -- see 140-01-SUMMARY.md"). Do NOT touch C02_TOLERANCE. Update the KEY sheet's "C-02 tolerance (139-05-PATCH FIX-03d)" field (SECTION 14, ~line 1258-1265) to state the new basis in addition to the existing narrowed-tolerance-claim language -- append, do not replace, since the tolerance's scope statement is still accurate. Re-run the testthat suite to confirm nothing else assumed the old literal 26L.

If option-b (cannot confirm): Leave C02_EXPECTED <- 26L and C02_TOLERANCE <- 5L exactly as-is. Add a new KEY sheet field (SECTION 14 key_tbl, inserted after the existing "C-02 tolerance ..." row) named "C-02 status (140-01, unresolved)" with value: "UNRESOLVED as of {run date}. The team could not confirm the original 26-patient control total's denominator (D-1). c02_reconciled is expected to continue reading FAIL until this is investigated further. Per 139-CONTEXT.md/140-CONTEXT.md: do not ship this workbook to Erin/Amy while this is open." Add an equivalent message() NOTE immediately after the existing C-02 reconciliation console block (~line 1148).

In both branches, confirm via grep that C02_TOLERANCE <- 5L is unchanged from before this task's edit (see verify).
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('^C02_TOLERANCE <- 5L', lines))); cat('C02_TOLERANCE unchanged; parse OK\n')" && Rscript -e "library(testthat); res <- test_file('tests/testthat/test-115-c02.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0); cat('testthat: 0 failures\n')"</automated>
  </verify>
  <done>Either C02_EXPECTED carries a re-derived value with a documented KEY-sheet basis (option-a), or C02_EXPECTED/C02_TOLERANCE are unchanged and the KEY sheet + console explicitly record C-02 as unresolved pending team follow-up (option-b). C02_TOLERANCE is 5L in both cases. Script still parses; testthat still passes.</done>
</task>

</tasks>

<verification>
1. Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R'))" -- no error, file still parses after this plan's edits.
2. Rscript -e "library(testthat); test_file('tests/testthat/test-115-c02.R')" -- all tests pass, 0 failures.
3. Rscript -e "library(testthat); test_file('tests/testthat/test-115-validation-curve.R')" -- confirms the compute_c02() block was cleanly removed and the remaining fixtures (build_validation_cases, classify_encounter_zip) still pass, 0 failures.
4. grep "n_present_no_usable_zip5" R/115_zip_stability_counts.R -- present in compute_c02(), the console message, and qc_tbl.
5. grep "C02_TOLERANCE <- 5L" R/115_zip_stability_counts.R -- present, unchanged.
6. Confirm the KEY sheet carries either the option-a re-derivation basis or the option-b unresolved-status note (not neither).
</verification>

<success_criteria>
- compute_c02() exposes n_present_no_usable_zip5 directly, reproducing 140-CONTEXT.md's 656-vs-9 breakdown from the script itself.
- The D-1 decision (original 26-patient denominator) is explicitly recorded, not silently assumed -- Task 2's checkpoint captures the team's answer.
- C02_EXPECTED is either updated with a documented basis, or left unchanged with an explicit unresolved-status note -- never silently left ambiguous.
- C02_TOLERANCE remains 5L in every branch; it is never used to absorb this gap.
- Per 140-08-PATCH FIX-16, C-02 fixtures live in tests/testthat/test-115-c02.R, not the shared test-115-validation-curve.R file.
</success_criteria>

<output>
After completion, create .planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-01-SUMMARY.md
</output>
