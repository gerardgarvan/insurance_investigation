---
phase: 140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/reference/README.md
  - R/115_zip_stability_counts.R
autonomous: false
requirements: [P-03a, P-03b, P-03c, D-2]
must_haves:
  truths:
    - "The exact expected path, filename, and column-name contract for the Neighborhood Atlas ZIP9-to-block-group crosswalk is documented so staging it requires no FURTHER code changes to R/115 once this plan lands (the block-group tier already auto-probes, gracefully degrades, and -- after this plan -- also gates on match-rate coverage)"
    - "The required crosswalk vintage (release year) is explicitly documented against the LDS_ADDRESS_HISTORY study period, and the block-group tier's accuracy figures are stated as conditional on that vintage choice (140-08-PATCH FIX-11)"
    - "Obtaining the crosswalk file itself is recorded as a human action, not silently skipped or assumed unnecessary -- Claude has no CLI/API to download it"
    - "The block-group tier's activation is coverage-aware, not presence-only: a match rate below a threshold (default 90%, pending team confirmation) causes block_group_tier_status to read 'found but coverage insufficient' rather than publishing a percentage computed on a non-random subset of the observed ZIP9s -- P-03a's acceptance is has_block_group_crosswalk = TRUE AND match rate above threshold, not file-existence alone (140-08-PATCH FIX-12)"
    - "Whether ZIP5 is adopted as the analysis unit (D-2) is recorded as an explicit team decision, informed by the real accuracy-by-gap-bin evidence already computed on the failed HiPerGator run, not silently assumed"
    - "P-03b (re-running A-06 with the block-group tier populated) and full confirmation of P-03c are explicitly left as a follow-up once the crosswalk file is staged -- not claimed as complete by this plan"
  artifacts:
    - path: "data/reference/README.md"
      provides: "Documents the expected crosswalk filename/path/columns/vintage and the acquisition source (Neighborhood Atlas, University of Wisconsin)"
      contains: "neighborhood_atlas_block_group_crosswalk.csv"
    - path: "R/115_zip_stability_counts.R"
      provides: "SECTION 8 match-rate QC row (n_zip9_matched_to_block_group / n_zip9_distinct_observed) and a 90%-threshold (pending team confirmation) coverage gate on block_group_tier_status"
      contains: "n_zip9_matched_to_block_group"
  key_links:
    - from: "data/reference/neighborhood_atlas_block_group_crosswalk.csv (once staged)"
      to: "R/115_zip_stability_counts.R SECTION 8 (has_block_group_crosswalk probe + match-rate gate)"
      via: "file.exists(NEIGHBORHOOD_ATLAS_PATH) auto-detection, followed by the match-rate coverage gate this plan adds -- no further code change required to activate either"
      pattern: "neighborhood_atlas_block_group_crosswalk"
---

<objective>
Prepare and request staging of the ZIP9-to-Census-block-group crosswalk (140-CONTEXT.md P-03a), and record the team's decision on whether ZIP5 (not ZIP9) should be adopted as the primary analysis unit (D-2), informed by the real accuracy-by-gap-bin evidence already produced on the failed HiPerGator run.

Purpose: 140-CONTEXT.md Section 4 recommends adopting ZIP5 as the analysis unit but flags this as provisional pending the block-group crosswalk -- exact-ZIP9 match is a conservative proxy for geographic accuracy (a +4 change that stays inside the same block group scores as a miss in the current ZIP9/ZIP5 comparison but would be a hit for ADI), and the block-group tier currently reads `#N/A` (`has_block_group_crosswalk = FALSE`) because no crosswalk file exists anywhere in this repo.

Per 140-08-PATCH FIX-11 and FIX-12 (applied now, since the crosswalk acquisition is already in flight): a crosswalk file existing is not sufficient evidence the block-group tier is trustworthy -- it also needs (a) a documented vintage, since Neighborhood Atlas publishes ZIP+4-to-block-group files by year and an off-vintage file would silently misclassify pre-/post-boundary-change addresses, and (b) a match-rate floor, since a crosswalk matching only a minority of observed ZIP9s would populate `pct_block_group_match` on a non-random subset and D-2/P-03c would be decided against numbers that look complete but are not. This plan therefore does two things beyond the original file-acquisition-only scope: documents the required vintage in the README, and adds a match-rate QC row + coverage gate to R/115 SECTION 8 now (both are additive, activate only inside the existing `if (has_block_group_crosswalk)` block, and require no further code changes once the file is staged).

This plan can run independently of 140-01 (no file overlap in the parts that matter -- both touch R/115_zip_stability_counts.R, but in different, non-overlapping SECTIONs: this plan's SECTION 8 addition vs. 140-01's SECTION 1B/12/13 additions) -- 140-CONTEXT.md Section 10 places P-03a/b as step 3, after P-01/P-02, but the crosswalk acquisition itself has no dependency on the C-02 gate's resolution and staging it early does not risk anything, so it is scheduled in Wave 1 to start the (likely slow, human-driven) file-acquisition process as early as possible.

Output: `data/reference/README.md` documenting the exact staging contract including required vintage; R/115 SECTION 8 gains a match-rate QC row and coverage-threshold gate; a recorded D-2 decision; P-03b (the actual A-06 re-run with the crosswalk populated) and full P-03c confirmation explicitly deferred as a follow-up once the file exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-CONTEXT.md

<interfaces>
R/115's existing block-group probe/join logic (SECTION 8, ~line 665-741) -- the file-acquisition and column-naming parts need NO changes once the file is staged; this plan adds new code immediately after this block, activating only when has_block_group_crosswalk is TRUE and the ZIP9/block-group columns were identified:

NEIGHBORHOOD_ATLAS_PATH <- file.path("data", "reference", "neighborhood_atlas_block_group_crosswalk.csv")
has_block_group_crosswalk <- file.exists(NEIGHBORHOOD_ATLAS_PATH)
...
candidate_cols <- intersect(
  c("ZIP9", "zip9", "ADDRESS_ZIP9", "zip9_norm", "GEOID9", "ZIP_PLUS4"),
  names(crosswalk_raw)
)
...
candidate_bg_cols <- intersect(
  c("block_group_id", "block_group", "BLOCK_GROUP", "GEOID_BG", "bg_id"),
  names(crosswalk_raw)
)
...
crosswalk_lookup <- crosswalk_raw %>%
  select(all_of(c(crosswalk_zip9_col, crosswalk_bg_col))) %>%
  rename(zip9_norm = all_of(crosswalk_zip9_col), block_group_id = all_of(crosswalk_bg_col)) %>%
  distinct(zip9_norm, .keep_all = TRUE)
...
n_bg_matched <- sum(!is.na(validation_cases$block_group_match))
if (n_bg_matched == 0 || n_bg_matched / nrow(validation_cases) < 0.01) {
  ...
  block_group_tier_status <- "found but unusable (near-zero join match rate)"
  validation_cases$block_group_match <- NA
} else {
  block_group_tier_status <- "available"
  ...
}

The existing near-zero-join-match check (the `< 0.01` branch above, ~line 726-741) measures something DIFFERENT from this plan's new match-rate gate: it checks how many of the validation_cases' predicted/actual ZIP9 PAIRS joined successfully to a block group, whereas this plan's new gate (140-08-PATCH FIX-12) checks what fraction of the DISTINCT ZIP9s actually observed in `addr_coal` are covered by the crosswalk at all -- a broader, more direct "is this crosswalk usable" measure that P-03a's acceptance criterion is about. Both checks stay; this plan's new code sits after the existing block, immediately before SECTION 9.

If the staged file's actual column names are NOT in the two candidate lists above, R/115 SECTION 8 is the single place to extend them (`candidate_cols`/`candidate_bg_cols` vectors) -- note this in the README so a future session knows exactly where to look without re-reading the whole script.

140-CONTEXT.md Section 4's real accuracy-by-gap-bin table (from the actual failed HiPerGator run, not synthetic data) for the D-2 decision context:

| Gap (days) | n cases | ZIP9 exact | ZIP5 same |
|---|---:|---:|---:|
| 0 (same-day) | 1,111 | 97.0% | 97.7% |
| 0-30 | 964 | 62.6% | 77.4% |
| 31-90 | 1,145 | 41.7% | 65.0% |
| 91-180 | 1,308 | 34.7% | 57.0% |
| 181-365 | 2,008 | 29.1% | 52.0% |
| 366+ | 5,767 | 23.7% | 56.1% |
| Overall | 12,303 | 37.1% | 61.8% |

Plus: 39.3% of all ZIP9 transitions are +4-only (same ZIP5, different delivery point).

LDS_ADDRESS_HISTORY's study period (ZIP_STUDY_PERIOD_MIN/MAX, used elsewhere in this codebase e.g. R/106 SECTION 9) is 2012-01-01 to 2025-03-31 -- the reference range for the FIX-11 Vintage section below.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the crosswalk staging contract (with required vintage) + wire the match-rate coverage gate (140-08-PATCH FIX-11, FIX-12)</name>
  <files>data/reference/README.md, R/115_zip_stability_counts.R</files>
  <action>
Check whether `data/reference/` exists (it should -- it already holds `RUCA-codes-2020-zipcode.xlsx`, used by R/100). Create (or extend, if a README already exists there) `data/reference/README.md` with a new section:

## Neighborhood Atlas ZIP9-to-Block-Group Crosswalk (Phase 140, P-03a)

**Expected path:** `data/reference/neighborhood_atlas_block_group_crosswalk.csv`

**Purpose:** Enables R/115_zip_stability_counts.R's A-06 block-group accuracy tier
(SECTION 8), currently gracefully degraded to `NA_real_` / "not available" because this
file does not exist in this repo. No further code changes are required to activate the
tier -- R/115 auto-probes for this exact path, joins automatically, and (per this plan)
gates on match-rate coverage once the file is present.

**Source:** University of Wisconsin Neighborhood Atlas (https://www.neighborhoodatlas.medicine.wisc.edu/).
Requires portal access/download -- not obtainable via CLI or API. If the team has an
existing internal source for this crosswalk, use that instead of a fresh Neighborhood
Atlas download.

**Vintage (140-08-PATCH FIX-11):** Neighborhood Atlas publishes ZIP+4-to-block-group
files by release year (known releases include 2015, 2019, 2020, 2021). Applying an
off-vintage crosswalk to LDS_ADDRESS_HISTORY records (study period 2012-01-01 to
2025-03-31, per ZIP_STUDY_PERIOD_MIN/MAX) risks the block-group tier attributing
boundary-redefinition noise to carry-forward error, or vice versa. Required vintage for
this deliverable: the most recent Neighborhood Atlas release available at acquisition
time (as of this document, that is the 2021 release) -- recommended as the default
choice, PENDING TEAM CONFIRMATION (same pending-confirmation posture as the 90% match-rate
threshold below; do not treat either as final without Erin/Amy sign-off). Whichever
release year is actually obtained, record it in this README (below) and note it in the
KEY sheet -- the block-group tier's accuracy figures are conditional on that choice and
must not be reported as vintage-neutral. If multiple vintages end up staged
side-by-side, R/115 SECTION 8 must be extended to select one explicitly (by filename
convention or a config constant) rather than globbing for "a" crosswalk file -- it does
not do this today because only one vintage is expected to be staged at a time.

**Expected columns** (R/115 SECTION 8 probes for the first match in each list; if the
staged file uses different names, add them to R/115's `candidate_cols`/`candidate_bg_cols`
vectors -- do not rename the source file's columns):
- ZIP9 key: one of `ZIP9`, `zip9`, `ADDRESS_ZIP9`, `zip9_norm`, `GEOID9`, `ZIP_PLUS4`
- Block-group key: one of `block_group_id`, `block_group`, `BLOCK_GROUP`, `GEOID_BG`, `bg_id`

**Match-rate coverage gate (140-08-PATCH FIX-12):** File existence alone is not
sufficient for P-03a's acceptance. R/115 SECTION 8 also computes
`n_zip9_matched_to_block_group / n_zip9_distinct_observed` (the fraction of DISTINCT
ZIP9s actually observed in `addr_coal` that the crosswalk covers) and gates on a minimum
threshold, default 90% -- PENDING TEAM CONFIRMATION. Below that threshold,
`block_group_tier_status` reads `"found but coverage insufficient"` rather than
publishing `pct_block_group_match` figures computed on a non-random subset of the data.
P-03a's acceptance is therefore `has_block_group_crosswalk = TRUE` **and** match rate
above threshold, not file existence alone.

**Status as of 2026-08-06:** Not staged. R/115's block-group tier reads
`block_group_tier_status <- "not available (crosswalk file not found)"` and
`pct_block_group_match` is `NA_real_` across all gap bins on the last real run.

In R/115_zip_stability_counts.R SECTION 8, immediately after the existing
`crosswalk_lookup` join succeeds and `crosswalk_zip9_col`/`crosswalk_bg_col` are both
identified (i.e. inside the same `if (is.null(crosswalk_zip9_col) || is.null(crosswalk_bg_col)) { ... } else { ... }`
branch's `else` block, after `crosswalk_lookup` is built but before the existing
`validation_cases <- validation_cases %>% left_join(...)` block that computes
`block_group_match`), add the match-rate coverage gate:

```r
# 140-08-PATCH FIX-12: match-rate QC row -- how much of the DISTINCT ZIP9 universe
# actually observed in this run's addr_coal is covered by the crosswalk, not just the
# validation_cases join outcome computed below (a different, narrower measure). File
# existence alone is not sufficient evidence the crosswalk is usable.
BLOCK_GROUP_MATCH_THRESHOLD <- 0.90  # suggested; PENDING TEAM CONFIRMATION (data/reference/README.md)
n_zip9_distinct_observed        <- n_distinct(addr_coal$zip9_norm[!is.na(addr_coal$zip9_norm)])
n_zip9_matched_to_block_group   <- n_distinct(intersect(addr_coal$zip9_norm, crosswalk_lookup$zip9_norm))
pct_zip9_matched_to_block_group <- round(100 * n_zip9_matched_to_block_group / n_zip9_distinct_observed, 1)

if (n_zip9_matched_to_block_group / n_zip9_distinct_observed < BLOCK_GROUP_MATCH_THRESHOLD) {
  block_group_tier_status <- "found but coverage insufficient"
  message(glue(
    "A-06: Neighborhood Atlas crosswalk match rate ({pct_zip9_matched_to_block_group}%, ",
    "{n_zip9_matched_to_block_group} of {n_zip9_distinct_observed} distinct ZIP9s) is below the ",
    "{BLOCK_GROUP_MATCH_THRESHOLD * 100}% threshold (pending team confirmation, 140-08-PATCH FIX-12) -- ",
    "treating block-group tier as found-but-insufficient-coverage rather than publishing a percentage ",
    "computed on a non-random subset of the data."
  ))
} else {
  message(glue(
    "A-06: Neighborhood Atlas crosswalk match rate {pct_zip9_matched_to_block_group}% ",
    "({n_zip9_matched_to_block_group} of {n_zip9_distinct_observed} distinct ZIP9s) -- meets the ",
    "{BLOCK_GROUP_MATCH_THRESHOLD * 100}% threshold."
  ))
}
```

This runs BEFORE the existing near-zero-join-match check (`n_bg_matched / nrow(validation_cases) < 0.01`), which stays as-is -- that check can still downgrade `block_group_tier_status` further to `"found but unusable (near-zero join match rate)"` if the broader coverage check passes but the specific validation_cases join still fails for some other reason (e.g. a crosswalk covering most ZIP9s but not the specific ones that happen to appear in validation_cases). Add `n_zip9_matched_to_block_group` and `pct_zip9_matched_to_block_group` to `qc_tbl` (SECTION 13) as two new named rows so the coverage figure is visible on the QC sheet regardless of `block_group_tier_status`'s final value -- guard this addition with `if (has_block_group_crosswalk && exists("n_zip9_matched_to_block_group"))` since these variables only exist inside the crosswalk-found branch.
  </action>
  <verify>
    <automated>test -f "data/reference/README.md" && grep -q "neighborhood_atlas_block_group_crosswalk.csv" data/reference/README.md && grep -q "Vintage" data/reference/README.md && grep -q "90%" data/reference/README.md && echo "README documents crosswalk contract, vintage, and match-rate threshold" && Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('n_zip9_matched_to_block_group', lines)), any(grepl('BLOCK_GROUP_MATCH_THRESHOLD', lines))); cat('SECTION 8 match-rate gate present')"</automated>
  </verify>
  <done>data/reference/README.md documents the exact expected path, columns, required vintage, and match-rate threshold; R/115 SECTION 8 computes n_zip9_matched_to_block_group / n_zip9_distinct_observed and gates block_group_tier_status on the 90% (pending-confirmation) threshold, additive inside the existing has_block_group_crosswalk branch -- no change to default (crosswalk-absent) behavior.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Obtain and stage the Neighborhood Atlas block-group crosswalk (P-03a)</name>
  <action>Present the acquisition request below to the user; this cannot be automated (no CLI/API for this restricted geographic dataset). Record the outcome for Plans 140-04 onward, which do not block on this task's completion.</action>
  <what-built>Task 1's README documents exactly where the file goes, what columns it needs, and the required vintage. R/115's block-group tier (SECTION 8) is already fully implemented, probes automatically, degrades gracefully today, and -- after Task 1 -- also gates on match-rate coverage rather than mere presence. This task is purely the file acquisition, which has no CLI or API and requires a human to obtain.</what-built>
  <how-to-verify>
    1. Obtain the ZIP+4-to-Census-block-group crosswalk from the University of Wisconsin Neighborhood Atlas (or the team's existing internal source, if Erin/Amy know of one already in use) -- confirm the release year/vintage with the team per Task 1's README section, since this is currently a recommendation pending confirmation, not a settled choice.
    2. Place it at `data/reference/neighborhood_atlas_block_group_crosswalk.csv`.
    3. On the next HiPerGator run of R/115, confirm the console reports `A-06: Neighborhood Atlas block-group crosswalk found at data/reference/neighborhood_atlas_block_group_crosswalk.csv`, reports the match-rate line from Task 1's new gate, and `block_group_tier_status` reads `"available"` (not "found but unusable" or "found but coverage insufficient") -- if it reads "found but unusable," report the crosswalk's actual column names back so R/115 SECTION 8's candidate-column lists can be extended; if it reads "found but coverage insufficient," report the actual match-rate percentage back so the team can decide whether to accept a lower threshold, source a different vintage, or investigate the mismatch.
  </how-to-verify>
  <verify>
    <automated>echo "MANUAL: crosswalk staging outcome recorded in plan output/summary"</automated>
  </verify>
  <done>The crosswalk is staged at the documented path, or the team has explicitly deferred acquisition -- either way, the outcome (including the vintage actually obtained and the match-rate figure once staged) is recorded, not left ambiguous.</done>
  <resume-signal>Reply "staged" (with the vintage/release year and, once run, the match-rate figure) once the file exists at that path, or "not available -- defer" if the team decides not to pursue it now (in which case P-03b/P-03c stay deferred and the block-group tier remains gracefully degraded, which is not a regression from the current shipped state).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: D-2 -- accept ZIP5 as the analysis unit (pending P-03b)</name>
  <action>Present the evidence and the two options below; record the team's D-2 decision for Plans 140-04 through 140-07, which treat ZIP5 as the working assumption per 140-CONTEXT.md's own recommendation absent an explicit override.</action>
  <decision>Should ZIP5 (not ZIP9) be adopted as the primary analysis unit for the ZIP-assignment design used by this workbook and downstream linkage work (ADI, SVI, RUCA, ZCTA-level covariates)?</decision>
  <context>
140-CONTEXT.md Section 4's evidence, from the actual failed HiPerGator run (not synthetic data): 39.3% of all ZIP9 transitions are +4-only (same ZIP5, different delivery point). ZIP5 exact-match consistently outperforms ZIP9 exact-match at every gap horizon past same-day (see the table in this plan's `<interfaces>` block) -- e.g. at 181-365 days, ZIP5 matches 52.0% of the time vs. ZIP9's 29.1%; overall, 61.8% vs. 37.1%.

Countervailing consideration: exact-ZIP9 match is a CONSERVATIVE proxy for geographic accuracy -- a +4 change that stays inside the same Census block group scores as a "miss" in the table above but would be a HIT for ADI, which resolves at tract/block-group level. The block-group tier (which would directly measure this) currently returns `#N/A` because no crosswalk file exists (Task 2 of this plan). Adopting ZIP5 now, before the block-group tier is available, means the 23.7% overall ZIP9-exact figure may materially understate ZIP9's real utility at the geographic unit the team actually links on.

140-CONTEXT.md's own recommendation: adopt ZIP5 as the analysis unit now, but treat the block-group re-run (once the crosswalk is staged, Task 2, and now match-rate-gated per Task 1) as confirmatory evidence, not a gating precondition -- i.e., do not block downstream Phase 140 work (Plans 03-07) on Task 2's file acquisition, which may take an unknown amount of time and is outside Claude's control.

Note (140-04's dependency on this decision, per 140-08-PATCH FIX-03): 140-04's S1-fold-in design (folding S1 into S3 in the ordered scenario assignment) is coherent only if D-2 lands on ZIP5 as the analysis unit -- 140-04 now explicitly depends on this plan's outcome and records the fold as reversible/conditional on option-a.
  </context>
  <options>
    <option id="option-a">
      <name>Accept ZIP5 as the primary analysis unit now (recommended)</name>
      <pros>Unblocks the rest of this phase's ZIP5-dependent design work (Plans 04-07) immediately; the block-group re-run becomes confirmatory once available, and a materially different block-group finding is itself a reportable result rather than a blocker.</pros>
      <cons>If the eventual block-group re-run shows ZIP9's true (block-group-level) accuracy is much higher than the ZIP5-vs-ZIP9 proxy comparison suggests, this decision may need to be revisited later.</cons>
    </option>
    <option id="option-b">
      <name>Defer the ZIP5-vs-ZIP9 decision entirely until the crosswalk is staged and P-03b (A-06 re-run with the block-group tier) is complete</name>
      <pros>Avoids committing to ZIP5 before the most decision-relevant evidence (block-group accuracy) exists.</pros>
      <cons>Blocks this phase's remaining ZIP5-dependent plans (04-07) on an external file acquisition of unknown timeline (Task 2); specifically, 140-04's Task 1 (S1 fold-in) is not executed under this option per its own FIX-03 conditional, and the 5-level ordered assignment is retained pending P-03b.</cons>
    </option>
  </options>
  <verify>
    <automated>echo "MANUAL: D-2 answer recorded in plan output/summary"</automated>
  </verify>
  <done>The team's answer (option-a or option-b) is recorded in this plan's execution summary.</done>
  <resume-signal>Select option-a or option-b. If option-a (or unanswered after reasonable follow-up), ZIP5 becomes the working assumption for Plans 140-04 through 140-07 in this phase, per 140-CONTEXT.md's own recommendation.</resume-signal>
</task>

</tasks>

<verification>
1. data/reference/README.md exists and documents the crosswalk's expected path/columns/source/vintage/match-rate threshold.
2. Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R'))" -- no error after Task 1's SECTION 8 addition.
3. grep "n_zip9_matched_to_block_group" R/115_zip_stability_counts.R -- present.
4. Task 2's checkpoint resume-signal is recorded (staged, or explicitly deferred).
5. Task 3's D-2 decision is recorded (option-a or option-b), not silently assumed.
</verification>

<success_criteria>
- The crosswalk staging contract is fully documented, including the required vintage -- no code changes are needed to activate the block-group tier once the file exists.
- The block-group tier's activation is coverage-aware (a 90%-pending-confirmation match-rate gate), not presence-only -- P-03a's acceptance criterion is file existence AND adequate match rate.
- File acquisition is explicitly logged as a human action with its own resume signal, not silently skipped.
- D-2 (ZIP5-as-analysis-unit) is an explicit recorded decision, informed by the real gap-bin evidence, feeding into the remaining Phase 140 plans -- including 140-04's now-explicit dependency on this decision.
</success_criteria>

<output>
After completion, create .planning/phases/140-resolve-c-02-reconciliation-gate-and-finalize-zip-assignment-design-zip5-unit-uncapped-carry-forward-backward-only-primary-spec-for-zip-stability-counts-workbook/140-02-SUMMARY.md
</output>
