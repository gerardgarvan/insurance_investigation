---
phase: 56-temporal-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/56_cancer_summary_post_hl.R
autonomous: false
requirements: [CREF-04]

must_haves:
  truths:
    - "Baseline cancer summary outputs (cancer_summary.csv, cancer_summary_table.xlsx) are NOT modified by R/56"
    - "Post-HL cancer_summary_post_hl.csv exists with only cancer diagnosis rows where DX_DATE > first_hl_dx_date"
    - "Post-HL cancer_summary_table_post_hl.xlsx has three sheets: EXPLORATORY - Category Summary, EXPLORATORY - Code Summary, Comparison"
    - "Every data sheet in post-HL xlsx outputs includes an EXPLORATORY footnote about immortal time bias"
    - "Patients with NA first_hl_dx_date are excluded from all post-HL outputs"
    - "Comparison sheet shows baseline vs post-HL patient counts with delta per category"
  artifacts:
    - path: "R/56_cancer_summary_post_hl.R"
      provides: "Post-HL temporal filtering, re-aggregation, and three-sheet xlsx output"
      min_lines: 400
    - path: "output/tables/cancer_summary_post_hl.csv"
      provides: "Patient-code level post-HL cancer summary"
      contains: "cancer_code"
    - path: "output/tables/cancer_summary_post_hl.xlsx"
      provides: "Single-sheet post-HL patient-code level output"
    - path: "output/tables/cancer_summary_table_post_hl.xlsx"
      provides: "Three-sheet styled workbook with EXPLORATORY labeling and Comparison sheet"
  key_links:
    - from: "R/56_cancer_summary_post_hl.R"
      to: "output/confirmed_hl_cohort.rds"
      via: "readRDS() to get first_hl_dx_date per patient"
      pattern: "readRDS.*confirmed_hl_cohort"
    - from: "R/56_cancer_summary_post_hl.R"
      to: "DuckDB DIAGNOSIS table"
      via: "get_pcornet_table() query for raw DX_DATE rows"
      pattern: "get_pcornet_table.*DIAGNOSIS"
    - from: "R/56_cancer_summary_post_hl.R"
      to: "output/tables/cancer_summary_table.xlsx"
      via: "openxlsx2::read_xlsx() to extract baseline counts for Comparison sheet"
      pattern: "read_xlsx.*cancer_summary_table"
---

<objective>
Create R/56_cancer_summary_post_hl.R that produces post-HL cancer summary variants filtered to cancer diagnoses occurring AFTER each patient's first HL diagnosis date, with EXPLORATORY labeling and a side-by-side Comparison sheet.

Purpose: Enable exploratory comparison of cancer burden before vs after HL diagnosis (CREF-04). The post-HL variant flags immortal time bias and provides denominator differences for clinical interpretation.

Output: R/56_cancer_summary_post_hl.R script, cancer_summary_post_hl.csv, cancer_summary_post_hl.xlsx, cancer_summary_table_post_hl.xlsx (3-sheet workbook with Comparison)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-temporal-filtering/56-CONTEXT.md
@.planning/phases/56-temporal-filtering/56-RESEARCH.md
@.planning/phases/55-cancer-summary-refinement-foundation/55-01-SUMMARY.md

@R/55_cancer_summary_refined.R
@R/00_config.R
@R/01_load_pcornet.R

<interfaces>
<!-- Key contracts the executor needs. Extracted from R/55 and Phase 55 SUMMARY. -->

From output/confirmed_hl_cohort.rds (Phase 55 bridge artifact):
- Columns: ID (character), first_hl_dx_date (Date), first_hl_dx_source (character: "DIAGNOSIS", "TUMOR_REGISTRY", or "Both")
- Contains ONLY confirmed HL patients (2+ C81 codes, 7-day gap)
- Sentinel 1900 dates already nullified to NA

From output/tables/cancer_summary.csv (Phase 55 output, 7 columns):
- Columns: ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7
- Contains ONLY C-codes (D-codes already removed by R/55)
- Contains ONLY confirmed HL cohort patients
- DOES NOT contain DX_DATE or category columns (these are internal to R/55 only)

From cancer_summary_table.xlsx (Phase 55 output, 2-sheet workbook):
- Sheet "Category Summary": Row 1 = title, Row 2 = headers, Row 3+ = data, last data row + 1 = TOTAL
  Headers: Cancer Site Category | Total Patients | Confirmed (2+ Dates) | Rate (2+ Dates) | Confirmed (7-Day Gap) | Rate (7-Day Gap) | Mean Unique Dates | Median Unique Dates | Mean Dates (7-Day Sep) | Median Dates (7-Day Sep) | Total Records
- Sheet "Code Summary": Row 1 = title, Row 2 = headers, Row 3+ = data
  Headers: ICD-10 Code | Cancer Site Category | Total Patients | ... (same as above plus code column)

From R/55 styling constants (lines 653-656):
- DARK_HEADER_FILL = "FF374151"
- WHITE_FONT = "FFFFFFFF"
- TITLE_FONT_COLOR = "FF1F2937"
- TOTALS_FILL = "FFE5E7EB"

From R/55 classify_codes() function (lines 334-338):
- Takes character vector of normalized codes (uppercase, no dots)
- Returns character vector of category names using PREFIX_MAP lookup on 3-char prefix
- PREFIX_MAP has 309 entries covering C00-C96 and D00-D49

CRITICAL IMPLEMENTATION NOTE:
cancer_summary.csv does NOT contain DX_DATE. It contains pre-aggregated patient-code metrics.
R/56 MUST query DIAGNOSIS table in DuckDB to get raw (ID, DX_code, DX_DATE) rows,
join with confirmed_hl_cohort.rds, apply DX_DATE > first_hl_dx_date filter on raw rows,
then re-compute the patient-code aggregation metrics (unique_dates_total, etc.) from the
filtered date rows. This is the same aggregation pattern from R/53 Section 6 / R/55 Section 9-10
but applied to post-HL-filtered raw dates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/56_cancer_summary_post_hl.R script</name>
  <files>R/56_cancer_summary_post_hl.R</files>
  <read_first>
    R/55_cancer_summary_refined.R
    R/53_cancer_summary.R
    R/00_config.R
    R/01_load_pcornet.R
    .planning/phases/56-temporal-filtering/56-CONTEXT.md
  </read_first>
  <action>
Create R/56_cancer_summary_post_hl.R as a standalone script following R/55's single-script consolidation pattern (per D-01 and D-02). The script produces post-HL filtered cancer summary outputs. DO NOT modify any R/55 baseline outputs.

CRITICAL: cancer_summary.csv does NOT contain DX_DATE. R/56 must query DuckDB DIAGNOSIS table directly for raw date rows, apply the temporal filter on raw rows, then re-aggregate patient-code metrics. Do NOT attempt to filter cancer_summary.csv by DX_DATE -- that column is not in the CSV.

Script structure (12 sections, matching R/55's organization):

**SECTION 1: SETUP**
- Libraries: dplyr, stringr, glue, openxlsx2, lubridate (same as R/55)
- source("R/00_config.R") and source("R/01_load_pcornet.R")
- Define output paths:
  - INPUT_CSV = file.path(CONFIG$output_dir, "tables", "cancer_summary.csv")
  - INPUT_RDS = file.path(CONFIG$output_dir, "confirmed_hl_cohort.rds")
  - BASELINE_TABLE_XLSX = file.path(CONFIG$output_dir, "tables", "cancer_summary_table.xlsx")
  - OUTPUT_CSV = file.path(CONFIG$output_dir, "tables", "cancer_summary_post_hl.csv")
  - OUTPUT_XLSX = file.path(CONFIG$output_dir, "tables", "cancer_summary_post_hl.xlsx")
  - OUTPUT_TABLE_XLSX = file.path(CONFIG$output_dir, "tables", "cancer_summary_table_post_hl.xlsx")

**SECTION 2: PREFIX_MAP AND classify_codes()**
- Copy the FULL PREFIX_MAP from R/55 lines 69-329 for script independence (it won't matter since input data has no D-codes). Copy classify_codes() function identically from R/55 lines 334-338.

**SECTION 3: LOAD INPUTS**
- Load confirmed_hl_cohort.rds via readRDS(INPUT_RDS)
- Load cancer_summary.csv via read.csv(INPUT_CSV, stringsAsFactors = FALSE) -- used ONLY for description lookup, NOT for temporal filtering
- Log: number of confirmed patients, number of patients with non-NA first_hl_dx_date, number of cancer_summary rows
- Per D-06: Filter confirmed_hl_cohort to only rows where !is.na(first_hl_dx_date). Log how many patients excluded due to NA date.
- Store n_excluded_na_date for the Comparison sheet.

**SECTION 4: QUERY DIAGNOSIS FOR RAW DATE ROWS**
- Query DuckDB DIAGNOSIS table for ALL ICD-10 C-code diagnosis rows for patients in the confirmed cohort (after NA date exclusion):
  ```r
  cohort_ids <- confirmed_hl_cohort$ID  # after NA filter

  dx_raw <- get_pcornet_table("DIAGNOSIS") %>%
    filter(DX_TYPE == "10") %>%
    mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
    filter(str_detect(DX_norm, "^C")) %>%
    select(ID, DX_norm, DX_DATE) %>%
    collect() %>%
    filter(ID %in% cohort_ids)
  ```
- Log: total C-code diagnosis rows retrieved

**SECTION 5: APPLY TEMPORAL FILTER (DX_DATE > first_hl_dx_date)**
- Join dx_raw with confirmed_hl_cohort on ID to get first_hl_dx_date per row
- Filter out rows where is.na(DX_DATE) (cannot compare NA dates)
- Apply strict > filter: filter(DX_DATE > first_hl_dx_date)
  - Use strict > (not >=) per clinical temporal precedence -- same-day diagnoses are ambiguous
- Log attrition: total rows before filter, rows after filter, rows excluded, patients remaining
- Store as dx_post_hl

**SECTION 6: RE-AGGREGATE PATIENT-CODE METRICS**
- From dx_post_hl (raw date rows), re-compute the same patient-code level metrics that R/53 Section 6 computes. Group by ID and DX_norm (cancer_code):
  ```r
  cancer_summary_post_hl <- dx_post_hl %>%
    distinct(ID, DX_norm, DX_DATE) %>%
    group_by(ID, DX_norm) %>%
    summarise(
      unique_dates_total = as.integer(n_distinct(DX_DATE[!is.na(DX_DATE)])),
      two_or_more_unique_dates = as.integer(n_distinct(DX_DATE[!is.na(DX_DATE)]) >= 2),
      two_or_more_unique_dates_gt_7 = as.integer({
        dates <- DX_DATE[!is.na(DX_DATE)]
        ud <- unique(dates)
        if (length(ud) >= 2) {
          as.numeric(max(ud) - min(ud)) >= 7
        } else {
          FALSE
        }
      }),
      unique_dates_with_sep_gt_7 = {
        dates <- unique(sort(DX_DATE[!is.na(DX_DATE)]))
        if (length(dates) < 2) {
          0L
        } else {
          span <- as.numeric(max(dates) - min(dates))
          if (span < 7) {
            0L
          } else {
            n <- length(dates)
            has_7day_sep <- logical(n)
            for (k in seq_len(n)) {
              diffs <- abs(as.numeric(dates[k] - dates[-k]))
              has_7day_sep[k] <- any(diffs >= 7)
            }
            as.integer(sum(has_7day_sep))
          }
        }
      },
      .groups = "drop"
    ) %>%
    rename(cancer_code = DX_norm)
  ```

**SECTION 7: ADD DESCRIPTIONS AND CATEGORY**
- Add category column via classify_codes(cancer_summary_post_hl$cancer_code)
- Handle unclassified codes (set to "Unclassified")
- For descriptions: build description lookup from the baseline cancer_summary.csv (which has cancer_code and description columns). Left join cancer_summary_post_hl with baseline on cancer_code to get description. For any codes in post-HL not in baseline (unlikely but handle), set description to category name.
  ```r
  baseline_desc <- cancer_summary_baseline %>%
    distinct(cancer_code, description)

  cancer_summary_post_hl <- cancer_summary_post_hl %>%
    left_join(baseline_desc, by = "cancer_code") %>%
    mutate(
      category = classify_codes(cancer_code),
      description = if_else(is.na(description), category, description)
    )
  ```
- Log: patient count, code count, row count

**SECTION 8: QUERY RECORD COUNTS FROM DIAGNOSIS (DuckDB)**
- Scope record counts to post-HL rows only (not total DIAGNOSIS counts), since we want record_count to reflect post-HL diagnosis volume:
  ```r
  dx_post_hl_records <- dx_post_hl %>%
    group_by(DX_norm) %>%
    summarise(record_count = n(), .groups = "drop")

  cancer_summary_post_hl <- cancer_summary_post_hl %>%
    left_join(dx_post_hl_records, by = c("cancer_code" = "DX_norm")) %>%
    mutate(record_count = ifelse(is.na(record_count), 0L, as.integer(record_count)))
  ```

**SECTION 9: CATEGORY-LEVEL AGGREGATION**
- Identical aggregation logic to R/55 Section 9 (lines 525-540) but on cancer_summary_post_hl data:
  ```r
  category_summary_post_hl <- cancer_summary_post_hl %>%
    group_by(category) %>%
    summarise(
      total_patients        = n_distinct(ID),
      confirmed_2date       = n_distinct(ID[two_or_more_unique_dates == 1]),
      pct_confirmed_2date   = n_distinct(ID[two_or_more_unique_dates == 1]) / n_distinct(ID),
      confirmed_7day        = n_distinct(ID[two_or_more_unique_dates_gt_7 == 1]),
      pct_confirmed_7day    = n_distinct(ID[two_or_more_unique_dates_gt_7 == 1]) / n_distinct(ID),
      mean_unique_dates     = mean(unique_dates_total, na.rm = TRUE),
      median_unique_dates   = median(unique_dates_total, na.rm = TRUE),
      mean_dates_7day_sep   = mean(unique_dates_with_sep_gt_7, na.rm = TRUE),
      median_dates_7day_sep = median(unique_dates_with_sep_gt_7, na.rm = TRUE),
      total_records         = sum(record_count, na.rm = TRUE),
      .groups = "drop"
    ) %>%
    arrange(desc(total_patients))
  ```

**SECTION 10: CODE-LEVEL AGGREGATION**
- Identical to R/55 Section 10 (lines 550-565) but on cancer_summary_post_hl:
  ```r
  code_summary_post_hl <- cancer_summary_post_hl %>%
    group_by(cancer_code, category) %>%
    summarise(
      total_patients        = n_distinct(ID),
      confirmed_2date       = n_distinct(ID[two_or_more_unique_dates == 1]),
      pct_confirmed_2date   = n_distinct(ID[two_or_more_unique_dates == 1]) / n_distinct(ID),
      confirmed_7day        = n_distinct(ID[two_or_more_unique_dates_gt_7 == 1]),
      pct_confirmed_7day    = n_distinct(ID[two_or_more_unique_dates_gt_7 == 1]) / n_distinct(ID),
      mean_unique_dates     = mean(unique_dates_total, na.rm = TRUE),
      median_unique_dates   = median(unique_dates_total, na.rm = TRUE),
      mean_dates_7day_sep   = mean(unique_dates_with_sep_gt_7, na.rm = TRUE),
      median_dates_7day_sep = median(unique_dates_with_sep_gt_7, na.rm = TRUE),
      total_records         = sum(record_count, na.rm = TRUE),
      .groups = "drop"
    ) %>%
    arrange(desc(total_patients))
  ```
- Build totals rows (totals_category_post_hl and totals_code_post_hl) following R/55 lines 570-597 pattern.

**SECTION 11: WRITE OUTPUTS**
Three sub-sections:

**11a. Write cancer_summary_post_hl.csv (7-column format)**
- Same 7 columns as R/55: ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7
- write.csv(cancer_summary_post_hl_output, OUTPUT_CSV, row.names = FALSE)

**11b. Write cancer_summary_post_hl.xlsx (single flat sheet)**
- Sheet name: "EXPLORATORY - Cancer Summary" (per D-04)
- Same flat sheet pattern as R/55 Section 11b but with EXPLORATORY prefix on sheet name
- After data, add footnote row (per D-05):
  - Text: "Note: Post-HL filter introduces potential immortal time bias. Use for exploratory comparison only."
  - Style: Calibri 10pt, italic, gray color (wb_color("FF6B7280")), merged across columns A through G

**11c. Write cancer_summary_table_post_hl.xlsx (three-sheet styled workbook)**
- Styling constants: same as R/55 (DARK_HEADER_FILL = "FF374151", WHITE_FONT = "FFFFFFFF", TITLE_FONT_COLOR = "FF1F2937", TOTALS_FILL = "FFE5E7EB")

Sheet 1: "EXPLORATORY - Category Summary" (per D-04)
- Row 1: Title "Cancer Summary Table - By Category (POST-HL DIAGNOSES ONLY)" with R/55 title styling
- Row 2: Same 11 headers as R/55 Sheet 1 (Cancer Site Category, Total Patients, Confirmed (2+ Dates), Rate (2+ Dates), Confirmed (7-Day Gap), Rate (7-Day Gap), Mean Unique Dates, Median Unique Dates, Mean Dates (7-Day Sep), Median Dates (7-Day Sep), Total Records) with dark header fill
- Row 3+: category_summary_post_hl data with same number formatting as R/55 (B/C/E = "#,##0", D/F = "0.0%", G/H/I/J = "0.0", K = "#,##0")
- Totals row with TOTALS_FILL styling
- Footnote row (per D-05): 2 rows below totals, text "Note: Post-HL filter introduces potential immortal time bias. Use for exploratory comparison only.", Calibri 10pt italic gray (FF6B7280), merged A through K
- Freeze pane at row 3, col 1

Sheet 2: "EXPLORATORY - Code Summary" (per D-04)
- Row 1: Title "Cancer Summary Table - By ICD-10 Code (POST-HL DIAGNOSES ONLY)"
- Row 2: Same 12 headers as R/55 Sheet 2 (ICD-10 Code, Cancer Site Category, Total Patients, ..., Total Records) with dark header fill
- Row 3+: code_summary_post_hl data with same number formatting as R/55 (C/D/F = "#,##0", E/G = "0.0%", H/I/J/K = "0.0", L = "#,##0")
- Totals row with TOTALS_FILL styling
- Footnote row (per D-05): same text, style, merged A through L
- Freeze pane at row 3, col 1

Sheet 3: "Comparison" (per D-03)
- Row 1: Title "Baseline vs Post-HL Comparison - By Cancer Site Category" with title styling
- Row 2: Headers: "Cancer Site Category", "Baseline Patients", "Post-HL Patients", "Delta", "% Retained" with dark header fill
- Build comparison_df by reading baseline counts from cancer_summary_table.xlsx:
  ```r
  baseline_raw <- openxlsx2::read_xlsx(BASELINE_TABLE_XLSX, sheet = "Category Summary", start_row = 2)
  # start_row = 2 skips the title row, uses row 2 (headers) as column names
  baseline_category <- baseline_raw %>%
    filter(`Cancer Site Category` != "TOTAL" & !is.na(`Cancer Site Category`)) %>%
    select(category = `Cancer Site Category`, baseline_patients = `Total Patients`) %>%
    mutate(baseline_patients = as.integer(baseline_patients))
  ```
  Then full_join with post-HL category totals:
  ```r
  comparison_df <- category_summary_post_hl %>%
    select(category, post_hl_patients = total_patients) %>%
    full_join(baseline_category, by = "category") %>%
    replace_na(list(baseline_patients = 0L, post_hl_patients = 0L)) %>%
    mutate(
      delta = post_hl_patients - baseline_patients,
      pct_retained = if_else(baseline_patients > 0L,
                             as.numeric(post_hl_patients) / as.numeric(baseline_patients),
                             NA_real_)
    ) %>%
    arrange(desc(baseline_patients))
  ```
- Row 3+: comparison_df data
- Number formatting: B/C = "#,##0", D = "#,##0" (signed), E = "0.0%"
- Freeze pane at row 3
- After data, add info rows:
  - "Patients excluded (NA first_hl_dx_date): {n_excluded_na_date}" (per D-06)
  - "Temporal filter: DX_DATE > first_hl_dx_date (strict, excludes same-day)"
- Column widths: c(40, 18, 18, 10, 14)

**SECTION 12: CLEANUP**
- close_pcornet_con()
- Log completion message

IMPORTANT EDGE CASES:
1. The read_xlsx() call to read baseline cancer_summary_table.xlsx may need careful row skipping. R/55 writes Row 1 = title (merged cells), Row 2 = headers. Use start_row = 2 to skip the title row and use row 2 (headers) as column names.

2. Record counts should be scoped to post-HL rows only (not total DIAGNOSIS counts), since we want record_count to reflect post-HL diagnosis volume.

3. The description lookup from baseline cancer_summary.csv handles the fact that R/53's description cascade (Phase 39/40 RDS + R/45 hardcoded + config comments) is already baked into cancer_summary.csv descriptions. R/56 reuses those descriptions rather than re-running the cascade.
  </action>
  <verify>
    <automated>grep -c "SECTION" R/56_cancer_summary_post_hl.R</automated>
  </verify>
  <acceptance_criteria>
    - R/56_cancer_summary_post_hl.R exists and is >= 400 lines
    - File contains "source(\"R/00_config.R\")" and "source(\"R/01_load_pcornet.R\")"
    - File contains "readRDS" call for confirmed_hl_cohort.rds
    - File contains "get_pcornet_table(\"DIAGNOSIS\")" for raw date query
    - File contains "filter(DX_DATE > first_hl_dx_date)" (strict greater-than per clinical standard)
    - File contains "filter(!is.na(first_hl_dx_date))" (per D-06)
    - File contains three output path variables: OUTPUT_CSV, OUTPUT_XLSX, OUTPUT_TABLE_XLSX all with _post_hl suffix
    - File contains sheet names "EXPLORATORY - Category Summary" and "EXPLORATORY - Code Summary" (per D-04)
    - File contains "Comparison" sheet name (per D-03)
    - File contains footnote text "Note: Post-HL filter introduces potential immortal time bias. Use for exploratory comparison only." (per D-05)
    - File contains "read_xlsx" call reading BASELINE_TABLE_XLSX for comparison data
    - File contains classify_codes function and PREFIX_MAP
    - File contains category-level and code-level summarise() blocks with same 10 metrics as R/55 (total_patients, confirmed_2date, pct_confirmed_2date, confirmed_7day, pct_confirmed_7day, mean_unique_dates, median_unique_dates, mean_dates_7day_sep, median_dates_7day_sep, total_records)
    - File contains close_pcornet_con() at end
    - File does NOT write to cancer_summary.csv or cancer_summary_table.xlsx (baseline paths)
    - File contains n_excluded_na_date tracking for Comparison sheet info row
  </acceptance_criteria>
  <done>R/56_cancer_summary_post_hl.R is a complete, self-contained script that queries DuckDB DIAGNOSIS for raw date rows, applies DX_DATE > first_hl_dx_date temporal filter, re-aggregates patient-code metrics, and produces three post-HL output files with EXPLORATORY labeling, footnotes, and a Comparison sheet showing baseline vs post-HL denominators.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify R/56 execution on HiPerGator</name>
  <files>output/tables/cancer_summary_post_hl.csv, output/tables/cancer_summary_post_hl.xlsx, output/tables/cancer_summary_table_post_hl.xlsx</files>
  <action>
User runs R/56_cancer_summary_post_hl.R on HiPerGator and verifies outputs. This is a human-verify checkpoint -- Claude cannot run R scripts on the remote HiPerGator environment.
  </action>
  <what-built>R/56_cancer_summary_post_hl.R script that produces post-HL cancer summary variants with temporal filtering and EXPLORATORY labeling</what-built>
  <how-to-verify>
    1. On HiPerGator, run: Rscript R/56_cancer_summary_post_hl.R
    2. Verify console output shows:
       - Number of patients excluded due to NA first_hl_dx_date
       - Row counts before and after temporal filter (DX_DATE > first_hl_dx_date)
       - Post-HL patient count and row count
       - Category summary row count
       - Code summary row count
       - All three output files written successfully
    3. Verify output/tables/cancer_summary_post_hl.csv exists and has 7 columns (ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7)
    4. Verify cancer_summary_post_hl.csv has FEWER rows than baseline cancer_summary.csv (temporal filter removed pre-HL diagnoses)
    5. Open output/tables/cancer_summary_post_hl.xlsx:
       - Sheet name starts with "EXPLORATORY"
       - Footnote row at bottom reads "Note: Post-HL filter introduces potential immortal time bias..."
    6. Open output/tables/cancer_summary_table_post_hl.xlsx:
       - Sheet 1 named "EXPLORATORY - Category Summary" with "(POST-HL DIAGNOSES ONLY)" in title
       - Sheet 2 named "EXPLORATORY - Code Summary" with "(POST-HL DIAGNOSES ONLY)" in title
       - Both sheets have footnote rows about immortal time bias
       - Sheet 3 named "Comparison" showing Baseline Patients vs Post-HL Patients with Delta column
       - Comparison sheet shows the NA first_hl_dx_date exclusion count
    7. Verify baseline files are UNCHANGED:
       - output/tables/cancer_summary.csv has same file size/modification date as before running R/56
       - output/tables/cancer_summary_table.xlsx has same file size/modification date as before running R/56
  </how-to-verify>
  <verify>User confirms all 7 verification steps pass</verify>
  <done>R/56 executes without errors on HiPerGator, produces all three post-HL output files with correct EXPLORATORY labeling and Comparison sheet, and baseline files remain untouched.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 56 verification checks:
1. Baseline outputs untouched: cancer_summary.csv and cancer_summary_table.xlsx have identical content to pre-R/56 execution
2. Post-HL outputs exist: cancer_summary_post_hl.csv, cancer_summary_post_hl.xlsx, cancer_summary_table_post_hl.xlsx
3. Post-HL patient count <= baseline patient count (temporal filter reduces or equals)
4. Post-HL row count < baseline row count (some diagnoses are pre-HL)
5. All post-HL xlsx sheets have EXPLORATORY prefix in sheet names
6. All post-HL data sheets have immortal time bias footnote
7. Comparison sheet shows non-zero delta for at least some categories
8. Comparison sheet reports NA date exclusion count
</verification>

<success_criteria>
- CREF-04 satisfied: cancer summary exists in two versions (baseline unchanged, post-HL new) with side-by-side comparison
- Post-HL outputs clearly labeled EXPLORATORY with bias warnings
- Comparison sheet quantifies denominator differences between baseline and post-HL
- Script runs end-to-end on HiPerGator without errors (user verified)
</success_criteria>

<output>
After completion, create `.planning/phases/56-temporal-filtering/56-01-SUMMARY.md`
</output>
