---
phase: 36-all-encounter-payer-frequency-and-same-day-categorization-with-amc-8-category-coding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/36_tiered_same_day_payer.R
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "R/36 uses AMC_PAYER_LOOKUP from R/00_config.R as sole payer mapping source"
    - "R/36 has no PayerVariable.xlsx dependency (no readxl, no PAYER_XLSX_PATH, no xlsx loading)"
    - "R/36 has no local function copies (no compute_effective_payer_local, detect_dual_eligible_local, map_payer_category_local)"
    - "Frequency table CSVs have columns: code, amc_category, n, pct (no description column)"
    - "Category summary CSVs aggregate by 8 AMC categories"
    - "Same 12 CSV filenames are produced"
    - "Unmapped codes fall back to prefix-based category assignment"
    - "Same-day resolution logic and CODE_TO_TIER function are preserved"
  artifacts:
    - path: "R/36_tiered_same_day_payer.R"
      provides: "Refactored dual-scope payer frequency + same-day resolution script using AMC 8-category coding"
      contains: "AMC_PAYER_LOOKUP"
  key_links:
    - from: "R/36_tiered_same_day_payer.R"
      to: "R/00_config.R"
      via: "source() and AMC_PAYER_LOOKUP named vector"
      pattern: "AMC_PAYER_LOOKUP\\["
    - from: "R/36_tiered_same_day_payer.R"
      to: "output/tables/*.csv"
      via: "write_csv() for 12 output files"
      pattern: "write_csv"
---

<objective>
Refactor R/36_tiered_same_day_payer.R to replace PayerVariable.xlsx dependency with the centralized AMC 8-category payer mapping from R/00_config.R.

Purpose: Eliminate duplicated payer logic, remove xlsx dependency, consolidate all payer category assignment to the single AMC_PAYER_LOOKUP source of truth already in R/00_config.R. R/35 (Phase 34 baseline) stays untouched per D-02.

Output: Updated R/36_tiered_same_day_payer.R that produces the same 12 CSV files with AMC categories instead of PayerVariable.xlsx categories.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-all-encounter-payer-frequency-and-same-day-categorization-with-amc-8-category-coding/36-CONTEXT.md
@.planning/phases/36-all-encounter-payer-frequency-and-same-day-categorization-with-amc-8-category-coding/36-RESEARCH.md

<interfaces>
<!-- AMC_PAYER_LOOKUP from R/00_config.R (lines 265-329): -->
<!-- Named character vector mapping ~35 raw payer codes to 8 AMC categories -->
<!-- Example: AMC_PAYER_LOOKUP["219"] returns "Medicaid" -->
<!-- AMC_PAYER_LOOKUP[code] returns NA for unmapped codes -->

<!-- PAYER_MAPPING from R/00_config.R (lines 331-362): -->
<!-- PAYER_MAPPING$sentinel_values: c("NI", "UN", "OT") -->
<!-- PAYER_MAPPING$dual_eligible_codes: c("14", "141", "142") -->
<!-- PAYER_MAPPING$prefix_fallback: list("1"="Medicare", "2"="Medicaid", ...) -->
<!-- PAYER_MAPPING$categories: c("Medicaid","Medicare","Private","Other govt","Other","Self-pay","Uninsured","Missing") -->

<!-- R/02_harmonize_payer.R has side effects (sources R/01_load_pcornet.R, runs Section 2) -->
<!-- DO NOT source R/02_harmonize_payer.R from R/36 -->
<!-- Instead, inline the logic using AMC_PAYER_LOOKUP + prefix fallback directly -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor R/36 to use AMC_PAYER_LOOKUP and remove PayerVariable.xlsx dependency</name>
  <files>R/36_tiered_same_day_payer.R</files>

  <read_first>
    - R/36_tiered_same_day_payer.R (current 490-line script being refactored)
    - R/00_config.R lines 238-362 (AMC_PAYER_LOOKUP definition, PAYER_MAPPING)
    - R/02_harmonize_payer.R lines 47-129 (reference implementations of compute_effective_payer, detect_dual_eligible, map_payer_category -- DO NOT source this file, it has side effects)
    - .planning/phases/36-all-encounter-payer-frequency-and-same-day-categorization-with-amc-8-category-coding/36-CONTEXT.md (user decisions D-01 through D-10)
    - .planning/phases/36-all-encounter-payer-frequency-and-same-day-categorization-with-amc-8-category-coding/36-RESEARCH.md (code patterns and anti-patterns)
  </read_first>

  <action>
Refactor R/36_tiered_same_day_payer.R in-place (per D-01). Make these specific changes:

**SECTION 0 (Setup) -- lines 46-104:**

1. Remove `library(readxl)` from line 56 (per D-07).

2. Remove `PAYER_XLSX_PATH <- "PayerVariable.xlsx"` from line 61 (per D-07).

3. Update the script header comment block (lines 1-46):
   - Change title to "Phase 36: All-encounter payer frequency and same-day categorization (AMC 8-category)"
   - Remove "Requires: readxl package for reading PayerVariable.xlsx" from Dependencies
   - Replace "with PayerVariable.xlsx cross-reference" with "using AMC_PAYER_LOOKUP from R/00_config.R"
   - Update the frequency table description to say columns are `code, amc_category, n, pct`

4. Keep TIER_MAPPING and CODE_TO_TIER function exactly as-is (per D-08).

**DELETE LOCAL FUNCTIONS -- lines 106-164 (per D-06):**

5. Delete the entire `compute_effective_payer_local` function (lines 106-123).
6. Delete the entire `detect_dual_eligible_local` function (lines 125-142).
7. Delete the entire `map_payer_category_local` function (lines 144-164).

**DELETE SECTION 1 -- lines 166-189 (per D-07):**

8. Delete the entire Section 1 block ("Load PayerVariable.xlsx") including the message, read_excel call, column renaming, trimming, and console preview.

**SECTION 2 (Load ENCOUNTER) -- currently lines 191-241:**

9. In the `enc <- enc_raw %>% mutate(...)` pipeline (lines 202-231), replace the three `*_local()` function calls with inline logic using AMC_PAYER_LOOKUP directly:

   Replace lines 209-213:
   ```r
   # OLD:
   effective_payer = compute_effective_payer_local(PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY),
   dual_eligible = detect_dual_eligible_local(PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY),
   payer_category = map_payer_category_local(effective_payer),
   ```

   With inline equivalents (matching R/02_harmonize_payer.R logic exactly):
   ```r
   # Compute effective payer: primary if valid, else secondary, else NA
   effective_payer = case_when(
     !is.na(PAYER_TYPE_PRIMARY) & nchar(trimws(PAYER_TYPE_PRIMARY)) > 0 &
       !PAYER_TYPE_PRIMARY %in% PAYER_MAPPING$sentinel_values ~ PAYER_TYPE_PRIMARY,
     !is.na(PAYER_TYPE_SECONDARY) & nchar(trimws(PAYER_TYPE_SECONDARY)) > 0 &
       !PAYER_TYPE_SECONDARY %in% PAYER_MAPPING$sentinel_values ~ PAYER_TYPE_SECONDARY,
     TRUE ~ NA_character_
   ),
   # Dual-eligible flag (informational only, does not override category)
   dual_eligible = {
     dual_codes <- PAYER_MAPPING$dual_eligible_codes
     sec_missing <- is.na(PAYER_TYPE_SECONDARY) | nchar(trimws(PAYER_TYPE_SECONDARY)) == 0
     has_dual <- PAYER_TYPE_PRIMARY %in% dual_codes | PAYER_TYPE_SECONDARY %in% dual_codes
     cross_payer <- (str_starts(PAYER_TYPE_PRIMARY, "1") & str_starts(PAYER_TYPE_SECONDARY, "2")) |
                    (str_starts(PAYER_TYPE_PRIMARY, "2") & str_starts(PAYER_TYPE_SECONDARY, "1"))
     case_when(sec_missing ~ 0L, has_dual ~ 1L, cross_payer ~ 1L, TRUE ~ 0L)
   },
   # Map to AMC 8-category: direct lookup + prefix fallback
   payer_category = {
     looked_up <- AMC_PAYER_LOOKUP[effective_payer]
     prefix_cat <- case_when(
       str_starts(effective_payer, "1") ~ "Medicare",
       str_starts(effective_payer, "2") ~ "Medicaid",
       str_starts(effective_payer, "5") | str_starts(effective_payer, "6") ~ "Private",
       str_starts(effective_payer, "3") | str_starts(effective_payer, "4") ~ "Other govt",
       str_starts(effective_payer, "7") ~ "Private",
       str_starts(effective_payer, "8") ~ "Uninsured",
       str_starts(effective_payer, "9") ~ "Other",
       TRUE ~ "Other"
     )
     result <- if_else(!is.na(looked_up), looked_up, prefix_cat)
     if_else(is.na(effective_payer), "Missing", result)
   },
   ```

   NOTE: The `{ }` blocks inside mutate work in dplyr because they return a vector. This is the same logic as R/02_harmonize_payer.R functions but without needing to source that file (which has side effects on line 22: `source("R/01_load_pcornet.R")`).

**SECTION 3 (Frequency Tables) -- currently lines 242-343:**

10. Rewrite `build_frequency_tables()` function signature: remove the `payer_lookup` parameter.
    Old: `build_frequency_tables <- function(enc_scope, suffix, payer_lookup, output_dir)`
    New: `build_frequency_tables <- function(enc_scope, suffix, output_dir)`

11. Replace the PRIMARY frequency table logic. Remove `left_join(payer_lookup, by = "code")` and the `description`/`category` ifelse with NOT IN XLSX fallbacks. Replace with AMC_PAYER_LOOKUP vector lookup + prefix fallback:
    ```r
    primary_freq <- enc_scope %>%
      mutate(
        code = case_when(
          is.na(PAYER_TYPE_PRIMARY) ~ "<NA>",
          PAYER_TYPE_PRIMARY == "" ~ "<EMPTY>",
          TRUE ~ PAYER_TYPE_PRIMARY
        )
      ) %>%
      count(code, name = "n") %>%
      mutate(
        amc_category = case_when(
          code %in% c("<NA>", "<EMPTY>") ~ "Missing",
          !is.na(AMC_PAYER_LOOKUP[code]) ~ unname(AMC_PAYER_LOOKUP[code]),
          substr(code, 1, 1) == "1" ~ "Medicare",
          substr(code, 1, 1) == "2" ~ "Medicaid",
          substr(code, 1, 1) %in% c("5", "6", "7") ~ "Private",
          substr(code, 1, 1) %in% c("3", "4") ~ "Other govt",
          substr(code, 1, 1) == "8" ~ "Uninsured",
          substr(code, 1, 1) == "9" ~ "Other",
          TRUE ~ "Other"
        ),
        pct = round(100 * n / total_enc, 2)
      ) %>%
      select(code, amc_category, n, pct) %>%
      arrange(desc(n))
    ```

12. Apply the same pattern for the SECONDARY frequency table (replace left_join with AMC_PAYER_LOOKUP vector lookup, use PAYER_TYPE_SECONDARY, output columns `code, amc_category, n, pct`).

13. Update category summary aggregation: change `group_by(category)` to `group_by(amc_category)` and `select(field, category, n, pct)` to `select(field, amc_category, n, pct)` in both primary_cat and secondary_cat.

14. Update console messages at end of build_frequency_tables():
    - Replace "NOT IN XLSX" counting with prefix fallback counting:
    ```r
    n_fallback_primary <- sum(is.na(AMC_PAYER_LOOKUP[primary_freq$code]) &
                              !primary_freq$code %in% c("<NA>", "<EMPTY>"), na.rm = TRUE)
    n_fallback_secondary <- sum(is.na(AMC_PAYER_LOOKUP[secondary_freq$code]) &
                                !secondary_freq$code %in% c("<NA>", "<EMPTY>"), na.rm = TRUE)
    message(glue("  Distinct PRIMARY codes: {nrow(primary_freq)} ({n_fallback_primary} via prefix fallback)"))
    message(glue("  Distinct SECONDARY codes: {nrow(secondary_freq)} ({n_fallback_secondary} via prefix fallback)"))
    ```

15. Update the two calls to build_frequency_tables() (lines 339 and 343) to remove the `payer_lookup` argument:
    Old: `build_frequency_tables(enc_all, "_all", payer_lookup, output_dir)`
    New: `build_frequency_tables(enc_all, "_all", output_dir)`
    Old: `build_frequency_tables(enc_av_th, "_av_th_v2", payer_lookup, output_dir)`
    New: `build_frequency_tables(enc_av_th, "_av_th_v2", output_dir)`

**SECTION 4 (Same-Day Resolution) -- lines 345-448:**

16. No substantive changes needed (per D-10). The resolve_same_day_payer() function already uses `tier` and `tier_rank` columns computed in Section 2, which flow through correctly from the inline AMC_PAYER_LOOKUP logic. Verify that no references to `payer_lookup` or `description` column exist in Section 4.

**SECTION 5 (Console Summary) -- lines 450-489:**

17. Update the banner from "Phase 35" to "Phase 36" in the header message (line 72):
    Old: `message("Phase 35: Dual-scope frequency + hierarchical same-day resolution")`
    New: `message("Phase 36: AMC 8-category payer frequency + same-day resolution")`

18. No other changes needed in Section 5 -- it just lists filenames and counts.
  </action>

  <verify>
    <automated>
      grep -c "readxl" R/36_tiered_same_day_payer.R | grep "^0$" &&
      grep -c "PayerVariable" R/36_tiered_same_day_payer.R | grep "^0$" &&
      grep -c "PAYER_XLSX_PATH" R/36_tiered_same_day_payer.R | grep "^0$" &&
      grep -c "_local" R/36_tiered_same_day_payer.R | grep "^0$" &&
      grep -c "NOT IN XLSX" R/36_tiered_same_day_payer.R | grep "^0$" &&
      grep -c "left_join.*payer_lookup" R/36_tiered_same_day_payer.R | grep "^0$" &&
      grep -c "AMC_PAYER_LOOKUP" R/36_tiered_same_day_payer.R &&
      grep -c "amc_category" R/36_tiered_same_day_payer.R &&
      grep -c "CODE_TO_TIER" R/36_tiered_same_day_payer.R &&
      grep -c "prefix fallback" R/36_tiered_same_day_payer.R &&
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>

  <acceptance_criteria>
    - R/36_tiered_same_day_payer.R contains zero occurrences of "readxl" (grep -c returns 0)
    - R/36_tiered_same_day_payer.R contains zero occurrences of "PayerVariable" (grep -c returns 0)
    - R/36_tiered_same_day_payer.R contains zero occurrences of "PAYER_XLSX_PATH" (grep -c returns 0)
    - R/36_tiered_same_day_payer.R contains zero occurrences of "_local" function names (grep -c returns 0)
    - R/36_tiered_same_day_payer.R contains zero occurrences of "NOT IN XLSX" (grep -c returns 0)
    - R/36_tiered_same_day_payer.R contains zero occurrences of "left_join.*payer_lookup" (grep -c returns 0)
    - R/36_tiered_same_day_payer.R contains at least 3 occurrences of "AMC_PAYER_LOOKUP" (Section 2 inline, Section 3 primary/secondary freq tables)
    - R/36_tiered_same_day_payer.R contains "amc_category" in select() calls for frequency tables
    - R/36_tiered_same_day_payer.R still contains "CODE_TO_TIER" (resolution logic preserved per D-08)
    - R/36_tiered_same_day_payer.R contains "prefix fallback" in console messages
    - build_frequency_tables function signature has 3 parameters (enc_scope, suffix, output_dir) -- no payer_lookup parameter
    - R/36_tiered_same_day_payer.R contains "Phase 36" in the banner message
    - R/36_tiered_same_day_payer.R still produces 12 CSV write_csv calls (6 frequency + 6 resolution)
    - R/35_payer_code_frequency_av_th.R is NOT modified (per D-02)
  </acceptance_criteria>

  <done>
    R/36_tiered_same_day_payer.R refactored: PayerVariable.xlsx dependency removed, local function copies removed, frequency tables use AMC_PAYER_LOOKUP vector lookup with prefix fallback, output columns are code/amc_category/n/pct, same 12 CSV filenames preserved, same-day resolution logic unchanged, CODE_TO_TIER preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify refactored R/36 on HiPerGator</name>
  <files>R/36_tiered_same_day_payer.R</files>
  <action>Human verifies that the refactored R/36 script runs correctly on HiPerGator with real PCORnet data, producing all 12 CSV files with the correct AMC 8-category columns.</action>
  <what-built>
    Refactored R/36_tiered_same_day_payer.R to use AMC_PAYER_LOOKUP from R/00_config.R instead of PayerVariable.xlsx. Removed readxl dependency, removed local function copies, updated frequency table logic to use vector lookup with prefix fallback, changed output columns to code/amc_category/n/pct.
  </what-built>
  <how-to-verify>
    1. Upload updated R/36_tiered_same_day_payer.R to HiPerGator
    2. Run in RStudio: `source("R/36_tiered_same_day_payer.R")`
    3. Verify script completes without errors
    4. Check output/tables/ for all 12 CSV files:
       - 6 frequency: payer_primary_code_freq_all.csv, payer_secondary_code_freq_all.csv, payer_category_summary_all.csv, payer_primary_code_freq_av_th_v2.csv, payer_secondary_code_freq_av_th_v2.csv, payer_category_summary_av_th_v2.csv
       - 6 resolution: payer_resolved_detail_all.csv, payer_resolved_detail_av_th.csv, payer_resolved_patient_summary_all.csv, payer_resolved_patient_summary_av_th.csv, payer_resolved_impact_all.csv, payer_resolved_impact_av_th.csv
    5. Open a frequency CSV (e.g., payer_primary_code_freq_all.csv) and verify columns are: code, amc_category, n, pct
    6. Open a category summary CSV and verify the 8 AMC categories appear: Medicaid, Medicare, Private, Other govt, Other, Self-pay, Uninsured, Missing
    7. Verify console output shows "prefix fallback" counts instead of "NOT IN XLSX"
  </how-to-verify>
  <verify>Human confirms all 12 CSVs generated with correct AMC columns</verify>
  <done>R/36 runs on HiPerGator, all 12 CSVs generated with amc_category column and AMC 8-category values</done>
  <resume-signal>Type "approved" if all 12 CSVs generated with correct columns and categories, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- R/36_tiered_same_day_payer.R has no readxl, no PayerVariable.xlsx, no PAYER_XLSX_PATH, no _local functions, no "NOT IN XLSX"
- AMC_PAYER_LOOKUP is used for all payer category assignment
- Prefix fallback handles unmapped codes
- Output columns are code, amc_category, n, pct (per D-05)
- CODE_TO_TIER function preserved (per D-08)
- Same 12 CSV filenames (per D-09)
- R/35 untouched (per D-02)
</verification>

<success_criteria>
- R/36 runs to completion on HiPerGator
- All 12 CSVs generated with AMC 8-category values
- Frequency tables have columns: code, amc_category, n, pct
- No PayerVariable.xlsx dependency in R/36
- No duplicated payer logic (local function copies removed)
</success_criteria>

<output>
After completion, create `.planning/phases/36-all-encounter-payer-frequency-and-same-day-categorization-with-amc-8-category-coding/36-01-SUMMARY.md`
</output>
