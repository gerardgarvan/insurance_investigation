---
phase: 05-fix-parsing
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - R/04_build_cohort.R
autonomous: false
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "User can rebuild the HL cohort with expanded HL identification (DIAGNOSIS + TUMOR_REGISTRY)"
    - "User can verify the rebuilt cohort has more patients than the prior version (TUMOR_REGISTRY-only patients now included)"
    - "Updated attrition log reflects new has_hodgkin_diagnosis() behavior with both DIAGNOSIS and TR counts"
    - "Rebuilt hl_cohort.csv is written to output/cohort/"
  artifacts:
    - path: "R/04_build_cohort.R"
      provides: "Updated attrition step label reflecting expanded HL identification"
      contains: "Has HL diagnosis (ICD or histology)"
    - path: "output/cohort/hl_cohort.csv"
      provides: "Rebuilt cohort CSV with expanded HL identification"
  key_links:
    - from: "R/04_build_cohort.R"
      to: "R/03_cohort_predicates.R"
      via: "has_hodgkin_diagnosis() call (now checks DIAGNOSIS + TR)"
      pattern: "has_hodgkin_diagnosis"
    - from: "R/04_build_cohort.R"
      to: "output/cohort/hl_cohort.csv"
      via: "write_csv output"
      pattern: "write_csv.*hl_cohort"
---

<objective>
Update the cohort build script attrition label and rebuild the HL cohort using the expanded has_hodgkin_diagnosis() predicate.

Purpose: Per D-14, after fixing the pipeline scripts (Plan 01), the cohort must be rebuilt to include patients identified via TUMOR_REGISTRY histology codes. The 04_build_cohort.R script needs a minor label update to reflect the expanded HL identification, then the full pipeline is re-run to produce an updated hl_cohort.csv. A checkpoint verifies the user sees expected improvements.

Output: Updated R/04_build_cohort.R, rebuilt output/cohort/hl_cohort.csv
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-fix-parsing-of-dates-and-other-possible-parsing-errors-and-investigate-why-not-everyone-has-an-hl-diagnosis/05-CONTEXT.md

@R/04_build_cohort.R
@R/03_cohort_predicates.R

<interfaces>
<!-- Key contracts from upstream code -->

From R/03_cohort_predicates.R (updated in Plan 01):
```r
has_hodgkin_diagnosis <- function(patient_df) {
  # Now checks BOTH DIAGNOSIS (ICD-9/10) and TUMOR_REGISTRY (histology)
  # Logs: "[Predicate] has_hodgkin_diagnosis: N patients with HL evidence"
  #       "  DIAGNOSIS (ICD-9/10): N patients"
  #       "  TUMOR_REGISTRY (histology): N patients"
  patient_df %>% semi_join(hl_patients, by = "ID")
}
```

From R/04_build_cohort.R (current attrition step, line 52):
```r
attrition_log <- log_attrition(attrition_log, "Has HL diagnosis (ICD-9/10)", n_distinct(cohort$ID))
```
This label needs updating to reflect the expanded identification.

From R/utils_attrition.R:
```r
log_attrition(log, step_name, n)  # step_name is a free-text string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update attrition label and rebuild cohort</name>
  <files>R/04_build_cohort.R</files>
  <read_first>
    - R/04_build_cohort.R (full file, find the attrition_log step label on line ~52)
    - R/03_cohort_predicates.R (verify has_hodgkin_diagnosis now checks DIAGNOSIS + TR from Plan 01)
  </read_first>
  <action>
**In R/04_build_cohort.R (per D-14):**

Change line 52 from:
```r
attrition_log <- log_attrition(attrition_log, "Has HL diagnosis (ICD-9/10)", n_distinct(cohort$ID))
```

To:
```r
attrition_log <- log_attrition(attrition_log, "Has HL diagnosis (ICD or histology)", n_distinct(cohort$ID))
```

This is a one-line change. The actual behavior change comes from the updated has_hodgkin_diagnosis() in 03_cohort_predicates.R (Plan 01). The label update makes the attrition log accurately reflect that both DIAGNOSIS and TUMOR_REGISTRY sources are now checked.

No other changes needed to 04_build_cohort.R -- the script already:
- Sources 02_harmonize_payer.R (which sources 01_load_pcornet.R and 00_config.R)
- Sources 03_cohort_predicates.R
- Calls has_hodgkin_diagnosis() which now includes TR checks
- Writes hl_cohort.csv to output/cohort/

The cohort rebuild happens automatically when the script is sourced/run.
  </action>
  <verify>
    <automated>grep "Has HL diagnosis (ICD or histology)" R/04_build_cohort.R</automated>
  </verify>
  <acceptance_criteria>
    - R/04_build_cohort.R line 52 contains `"Has HL diagnosis (ICD or histology)"`
    - R/04_build_cohort.R does NOT contain the old label `"Has HL diagnosis (ICD-9/10)"`
    - R/04_build_cohort.R still sources `R/02_harmonize_payer.R` and `R/03_cohort_predicates.R`
    - R/04_build_cohort.R still calls `has_hodgkin_diagnosis()` on cohort
    - R/04_build_cohort.R still writes `output/cohort/hl_cohort.csv`
  </acceptance_criteria>
  <done>04_build_cohort.R attrition label updated from "ICD-9/10" to "ICD or histology"</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify rebuilt cohort and diagnostic outputs on HiPerGator</name>
  <files>output/cohort/hl_cohort.csv, output/diagnostics/hl_identification_venn.csv</files>
  <action>
Human verification checkpoint. All code changes from Plans 01, 02, and 03 Task 1 are complete. The user needs to run the pipeline on HiPerGator (where the actual data resides) and verify results.

What was built:
- Plan 01 fixed 4 pipeline scripts: added ICD-O-3 histology codes to config (00_config.R), added is_hl_histology() utility (utils_icd.R), expanded has_hodgkin_diagnosis() to check TUMOR_REGISTRY tables (03_cohort_predicates.R), and updated date column detection regex (01_load_pcornet.R).
- Plan 02 created 07_diagnostics.R -- a reusable diagnostic script with 6 sections covering date parsing, column detection, column types, HL identification Venn breakdown, payer audit, and numeric range checks.
- Plan 03 Task 1 updated the attrition label in 04_build_cohort.R.

Steps for user to verify on HiPerGator (interactive RStudio session):

1. Run the diagnostic script: source("R/07_diagnostics.R")
   - Console shows 6 diagnostic sections with summaries
   - CSVs appear in output/diagnostics/ (6 files)

2. Check HL Venn breakdown: open output/diagnostics/hl_identification_venn.csv
   - Verify 4 categories: Both, DIAGNOSIS only, TR only, Neither
   - Note "TR only" count (patients previously missed)
   - Note "Neither" count (data quality flags)

3. Rebuild cohort: source("R/04_build_cohort.R")
   - has_hodgkin_diagnosis() reports BOTH DIAGNOSIS and TUMOR_REGISTRY counts
   - Attrition log shows "Has HL diagnosis (ICD or histology)"
   - Final cohort count should be >= prior count

4. Verify rebuilt cohort: nrow(read.csv("output/cohort/hl_cohort.csv"))

5. Check date parsing: review date_parsing_failures.csv for improvements
  </action>
  <verify>User confirms diagnostic outputs and rebuilt cohort are correct on HiPerGator</verify>
  <done>User has verified: (a) 07_diagnostics.R runs successfully with 6 sections, (b) HL Venn breakdown shows expected categories, (c) rebuilt cohort includes TR-identified patients, (d) date parsing improvements confirmed</done>
</task>

</tasks>

<verification>
1. `grep "ICD or histology" R/04_build_cohort.R` confirms updated label
2. Rebuilt hl_cohort.csv exists in output/cohort/
3. Diagnostic CSVs exist in output/diagnostics/ (6 files minimum)
4. Console output from both 07_diagnostics.R and 04_build_cohort.R shows expected sections
</verification>

<success_criteria>
- 04_build_cohort.R has updated attrition label
- Rebuilt cohort includes patients identified via TUMOR_REGISTRY histology codes
- Attrition log shows "Has HL diagnosis (ICD or histology)" step
- User confirms diagnostic outputs and rebuilt cohort on HiPerGator
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-parsing-of-dates-and-other-possible-parsing-errors-and-investigate-why-not-everyone-has-an-hl-diagnosis/05-03-SUMMARY.md`
</output>
