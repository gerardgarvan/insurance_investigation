---
phase: 102-single-agent-co-administration-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/58_co_administration_analysis.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements: [COADMIN-01, COADMIN-02]

must_haves:
  truths:
    - "User can open co_administration_analysis.xlsx Sheet 1 and see each single-agent chemo encounter with co-administered drugs within 30 days"
    - "User can open co_administration_analysis.xlsx Sheet 2 and see most common drug pairings ranked descending by frequency"
    - "User can filter detail table by drug and trace all co-administered drugs across patient encounters"
    - "User can identify fragmented ABVD/BV+AVD patterns via co-administration temporal clustering"
    - "User can run R/88 smoke test and see R/58 structural validation passing"
  artifacts:
    - path: "R/58_co_administration_analysis.R"
      provides: "Self-contained investigation script producing co-administration detail and pattern summary"
      min_lines: 150
    - path: "output/co_administration_analysis.xlsx"
      provides: "Two-sheet xlsx: Co-Administration Detail + Pattern Summary"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Phase 102 validation section for R/58 structural checks"
  key_links:
    - from: "R/58_co_administration_analysis.R"
      to: "cache/outputs/treatment_episode_detail.rds"
      via: "readRDS() for encounter-level chemotherapy data"
      pattern: "readRDS.*treatment_episode_detail"
    - from: "R/58_co_administration_analysis.R"
      to: "cache/outputs/treatment_episodes.rds"
      via: "readRDS() for regimen_label filter (D-05 exclusion)"
      pattern: "readRDS.*treatment_episodes"
    - from: "R/58_co_administration_analysis.R"
      to: "output/co_administration_analysis.xlsx"
      via: "openxlsx2 wb_workbook() two-sheet output"
      pattern: "wb_workbook|add_worksheet.*Co-Administration"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/58_co_administration_analysis.R"
      via: "readLines() structural validation checks"
      pattern: "readLines.*58_co_administration"
---

<objective>
Create R/58_co_administration_analysis.R as a self-contained investigation script that detects fragmented regimen patterns by identifying single-agent chemotherapy encounters and finding all co-administered chemotherapies within a +/-30-day window. Produces a two-sheet xlsx: (1) Co-Administration Detail table (one row per single-agent encounter + co-admin drug pair with days_apart) and (2) Pattern Summary table (drug pairings ranked by frequency). Add R/88 smoke test validation for R/58 structural integrity.

Purpose: Surface fragmented billing patterns where multi-drug regimens (ABVD, BV+AVD) appear as separate single-agent encounters instead of being billed together. This directly answers team questions about single-agent chemo patterns.

Output: R/58_co_administration_analysis.R (new script), updated R/88_smoke_test_comprehensive.R (new validation section), output/co_administration_analysis.xlsx (runtime output)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-single-agent-co-administration-analysis/102-CONTEXT.md
@.planning/phases/102-single-agent-co-administration-analysis/102-RESEARCH.md
@.planning/phases/101-broadened-drug-grouping-output/101-01-SUMMARY.md

@R/57_drug_grouping_instances.R
@R/00_config.R
@R/utils/utils_assertions.R
@R/utils/utils_dt.R
@R/88_smoke_test_comprehensive.R

<interfaces>
<!-- Key patterns and contracts the executor needs from existing codebase -->

From R/00_config.R:
- CONFIG$cache$outputs_dir — path to RDS outputs (treatment_episode_detail.rds, treatment_episodes.rds)
- CONFIG$output_dir — path to "output/" directory for xlsx files
- CODE_SUBCATEGORY_MAP — named list mapping triggering_code -> sub-category name
- TREATMENT_CODES — lists of CPT/HCPCS/NDC codes by treatment type
- DRUG_GROUPINGS — named vector mapping code -> drug group
- IS_LOCAL — boolean, TRUE on Windows, FALSE on HiPerGator

From R/utils/utils_assertions.R:
- assert_rds_exists(path, script_name) — validates RDS file exists
- assert_df_valid(df, name, required_cols, script_name) — validates data frame structure

From R/utils/utils_dt.R:
- ensure_dt(df, name, script_name) — converts tibble to data.table safely
- to_tibble_safe(dt, name, script_name) — converts data.table back to tibble
- get_lookup_dt(name) — retrieves keyed data.table from LOOKUP_TABLES_DT

From R/57_drug_grouping_instances.R (patterns to reuse):
- Section 1: suppressPackageStartupMessages + source("R/00_config.R") + source("R/utils/utils_assertions.R")
- Section 2: assert_rds_exists + readRDS + assert_df_valid pattern
- Section 3: wb_load(REFERENCE_XLSX) -> wb_to_df(sheet, start_row=2) -> chemo_map named vector
- Section 7: wb_workbook() -> $add_worksheet() -> $add_data() -> $save() pattern
- Log handler pattern: .log_con + globalCallingHandlers(message = ...)

From treatment_episode_detail.rds (encounter-level grain):
- Columns: patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name, episode_number, episode_start, episode_stop, historical_flag

From treatment_episodes.rds (episode-level):
- Columns include: patient_id, episode_number, regimen_label (ABVD, BV+AVD, Nivo+AVD, or NA)

From R/88_smoke_test_comprehensive.R (validation patterns):
- Current last section: [33/33] Fixture schema validation
- Section counter: [N/M] format in message() calls
- Summary section: SECTION 16 at bottom with message() listing validated requirements
- Pattern: readLines("R/XX_script.R") -> grepl() checks for structural elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/58_co_administration_analysis.R</name>
  <files>R/58_co_administration_analysis.R</files>
  <read_first>
    R/57_drug_grouping_instances.R
    R/00_config.R
    R/utils/utils_assertions.R
    R/utils/utils_dt.R
    .planning/phases/102-single-agent-co-administration-analysis/102-CONTEXT.md
    .planning/phases/102-single-agent-co-administration-analysis/102-RESEARCH.md
  </read_first>
  <action>
Create a new standalone investigation script R/58_co_administration_analysis.R with the following 8 sections, implementing all locked decisions D-01 through D-10:

**HEADER BLOCK** (follow R/57 header pattern):
- Title: "58_co_administration_analysis.R -- Single-Agent Co-Administration Analysis"
- Purpose: Identify single-agent chemotherapy encounters and find all co-administered chemotherapies within +/-30 days. Detects fragmented regimen patterns where multi-drug regimens appear as separate single-agent billing events.
- Inputs: cache/outputs/treatment_episode_detail.rds, cache/outputs/treatment_episodes.rds, data/reference/all_codes_resolved_next_tables_v2.1.xlsx
- Outputs: output/co_administration_analysis.xlsx (2-sheet workbook)
- Decision traceability: D-01 through D-10 documented in header comments
- Dependencies: R/00_config.R, R/utils/utils_assertions.R, openxlsx2, data.table, dplyr, glue

**SECTION 1: SETUP AND CONFIGURATION**
- Clear stale log handler: `try(close(.log_con), silent = TRUE)` + `globalCallingHandlers(NULL)`
- suppressPackageStartupMessages for: dplyr, glue, stringr, openxlsx2, data.table, checkmate
- source("R/00_config.R"), source("R/utils/utils_assertions.R")
- Define file paths:
  - DETAIL_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail.rds")
  - EPISODES_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")
  - REFERENCE_XLSX <- "data/reference/all_codes_resolved_next_tables_v2.1.xlsx"
  - OUTPUT_XLSX <- file.path(CONFIG$output_dir, "co_administration_analysis.xlsx")
- Log console output to file: LOG_FILE <- file.path(CONFIG$output_dir, "58_co_administration_analysis.log")
- .log_con file handler with globalCallingHandlers(message = ...) pattern (identical to R/57)
- Opening message: "=== Phase 102: Single-Agent Co-Administration Analysis ==="

**SECTION 2: LOAD AND FILTER DATA**
- assert_rds_exists() for both RDS files
- Load treatment_episode_detail.rds -> detail
- assert_df_valid(detail, required_cols = c("patient_id", "treatment_type", "treatment_date", "triggering_code", "ENCOUNTERID", "episode_number", "drug_name"))
- Load treatment_episodes.rds -> episodes
- Filter detail to Chemotherapy treatment_type only (per D-04): `chemo_detail <- detail %>% filter(treatment_type == "Chemotherapy")`
- Anti-join to exclude regimen-classified encounters (per D-05):
  ```r
  regimen_encounters <- episodes %>%
    filter(!is.na(regimen_label)) %>%
    select(patient_id, episode_number)
  chemo_detail <- chemo_detail %>%
    anti_join(regimen_encounters, by = c("patient_id", "episode_number"))
  ```
- Log counts at each step with glue(): total detail rows, chemo-only rows, post-regimen-exclusion rows

**SECTION 3: BUILD SUB-CATEGORY MAPPINGS**
- Reuse R/57 Section 3 pattern: wb_load(REFERENCE_XLSX), wb_to_df for Chemotherapy sheet (start_row = 2)
- Build chemo_map: `setNames(as.character(chemo_sheet[[3]]), as.character(chemo_sheet[[1]]))` (column C = medication name, column A = code)
- Filter out NAs: `chemo_map <- chemo_map[!is.na(names(chemo_map)) & !is.na(chemo_map)]`
- Only need chemotherapy sub-categories for this analysis (per D-04: chemo-to-chemo only)
- Log: "Chemo sub-categories: {length(unique(chemo_map))} medications from {length(chemo_map)} codes"
- Build combined code_to_subcategory with CODE_SUBCATEGORY_MAP fallback:
  ```r
  resolve_drug_name <- function(code, drug_name_col) {
    # Tier 1: xlsx reference
    if (code %in% names(chemo_map)) return(chemo_map[[code]])
    # Tier 2: CODE_SUBCATEGORY_MAP
    if (code %in% names(CODE_SUBCATEGORY_MAP)) return(CODE_SUBCATEGORY_MAP[[code]])
    # Tier 3: drug_name from RDS if available
    if (!is.na(drug_name_col) && drug_name_col != "") return(drug_name_col)
    # Tier 4: code itself as fallback
    return(code)
  }
  ```

**SECTION 4: IDENTIFY SINGLE-AGENT ENCOUNTERS (D-01, D-02)**
- Group by (patient_id, treatment_date) to count distinct chemo codes per patient-date (per D-01):
  ```r
  single_agent_base <- chemo_detail %>%
    group_by(patient_id, treatment_date) %>%
    mutate(n_chemo_codes_on_date = n_distinct(triggering_code)) %>%
    ungroup() %>%
    filter(n_chemo_codes_on_date == 1)
  ```
- Per D-02: Include encounters with drug_name = NA (use triggering_code as identifier)
- Keep one representative row per (patient_id, treatment_date, triggering_code, ENCOUNTERID) for the single-agent base:
  ```r
  single_agent_encounters <- single_agent_base %>%
    distinct(patient_id, treatment_date, triggering_code, drug_name, ENCOUNTERID, episode_number)
  ```
- Log: "Single-agent encounters identified: {nrow(single_agent_encounters)}" and "Unique patients with single-agent encounters: {n_distinct(single_agent_encounters$patient_id)}"

**SECTION 5: TEMPORAL SELF-JOIN (+/-30-DAY WINDOW) (D-03)**
- Use data.table cartesian join on patient_id with date arithmetic filter (per RESEARCH.md recommendation):
  ```r
  single_dt <- as.data.table(single_agent_encounters)
  all_chemo_dt <- as.data.table(
    chemo_detail %>%
      distinct(patient_id, treatment_date, triggering_code, drug_name, ENCOUNTERID)
  )
  setkey(single_dt, patient_id)
  setkey(all_chemo_dt, patient_id)

  coadmin_dt <- single_dt[all_chemo_dt,
    on = .(patient_id),
    allow.cartesian = TRUE,
    nomatch = NULL
  ]

  # Filter to +/-30-day window, exclude self-matches (per D-03)
  coadmin_dt <- coadmin_dt[
    abs(as.numeric(difftime(i.treatment_date, treatment_date, units = "days"))) <= 30 &
    ENCOUNTERID != i.ENCOUNTERID
  ]

  # Signed days_apart (negative = co-admin occurred before index encounter)
  coadmin_dt[, days_apart := as.integer(as.numeric(difftime(i.treatment_date, treatment_date, units = "days")))]
  ```
- Convert back to tibble: `coadmin_pairs <- as_tibble(coadmin_dt)`
- Log: "Co-administration pairs found: {nrow(coadmin_pairs)}", "Unique patients with co-admin: {n_distinct(coadmin_pairs$patient_id)}"

**SECTION 6: BUILD DETAIL TABLE (COADMIN-01, D-07, D-08)**
- Resolve human-readable drug names for both index and co-admin drugs using resolve_drug_name() from Section 3
- Apply vectorized resolution:
  ```r
  coadmin_detail <- coadmin_pairs %>%
    rowwise() %>%
    mutate(
      index_drug_name = resolve_drug_name(triggering_code, drug_name),
      coadmin_drug_name = resolve_drug_name(i.triggering_code, i.drug_name)
    ) %>%
    ungroup()
  ```
- Select and rename columns for the detail table (per D-07, D-08):
  ```r
  detail_table <- coadmin_detail %>%
    transmute(
      patient_id,
      index_encounter_id = ENCOUNTERID,
      index_treatment_date = treatment_date,
      index_triggering_code = triggering_code,
      index_drug_name,
      coadmin_encounter_id = i.ENCOUNTERID,
      coadmin_treatment_date = i.treatment_date,
      coadmin_triggering_code = i.triggering_code,
      coadmin_drug_name,
      days_apart
    ) %>%
    arrange(patient_id, index_treatment_date, abs(days_apart))
  ```
- Log: "Detail table: {nrow(detail_table)} rows, {n_distinct(detail_table$patient_id)} patients"

**SECTION 7: BUILD PATTERN SUMMARY TABLE (COADMIN-02)**
- Create sorted drug pairs to avoid A+B / B+A duplication:
  ```r
  pattern_summary <- detail_table %>%
    mutate(
      drug_A = pmin(index_drug_name, coadmin_drug_name),
      drug_B = pmax(index_drug_name, coadmin_drug_name)
    ) %>%
    group_by(drug_A, drug_B) %>%
    summarise(
      n_instances = n(),
      n_patients = n_distinct(patient_id),
      mean_days_apart = round(mean(abs(days_apart)), 1),
      .groups = "drop"
    ) %>%
    arrange(desc(n_instances))
  ```
- Show ALL pairs (not top N) in the xlsx output, sorted descending by n_instances
- Log: "Unique drug pair patterns: {nrow(pattern_summary)}"
- Log top 10 patterns: for loop printing "  {drug_A} + {drug_B}: {n_instances} instances, {n_patients} patients"

**SECTION 8: WRITE XLSX OUTPUT (D-06)**
- Two-sheet xlsx via openxlsx2 (per D-06):
  ```r
  wb <- wb_workbook()
  wb$add_worksheet("Co-Administration Detail")
  wb$add_data("Co-Administration Detail", detail_table, start_row = 1, col_names = TRUE)
  wb$add_worksheet("Pattern Summary")
  wb$add_data("Pattern Summary", pattern_summary, start_row = 1, col_names = TRUE)
  wb$save(OUTPUT_XLSX)
  ```
- Log: "Saved output: {OUTPUT_XLSX}"

**SECTION 9: CONSOLE SUMMARY**
- Print final summary block:
  - Total chemo encounters loaded
  - Encounters excluded (regimen-classified)
  - Single-agent encounters identified
  - Encounters with co-administered drugs found
  - Total co-administration pairs in detail table
  - Unique drug pair patterns in summary table
  - Top 5 most common pairings with counts
- Close log handler: `close(.log_con)`
  </action>
  <verify>
    <automated>Rscript -e "lines <- readLines('R/58_co_administration_analysis.R'); cat('Lines:', length(lines), '\n'); stopifnot(length(lines) >= 150); stopifnot(any(grepl('SECTION 1.*SETUP', lines))); stopifnot(any(grepl('SECTION 2.*LOAD', lines))); stopifnot(any(grepl('SECTION 3.*SUB-CATEGORY', lines))); stopifnot(any(grepl('SECTION 4.*SINGLE-AGENT', lines))); stopifnot(any(grepl('SECTION 5.*TEMPORAL.*SELF-JOIN', lines))); stopifnot(any(grepl('SECTION 6.*DETAIL TABLE', lines))); stopifnot(any(grepl('SECTION 7.*PATTERN SUMMARY', lines))); stopifnot(any(grepl('SECTION 8.*WRITE.*XLSX', lines))); stopifnot(any(grepl('D-01', lines))); stopifnot(any(grepl('D-10', lines))); stopifnot(any(grepl('treatment_episode_detail', lines))); stopifnot(any(grepl('treatment_episodes', lines))); stopifnot(any(grepl('wb_workbook', lines))); stopifnot(any(grepl('Co-Administration Detail', lines))); stopifnot(any(grepl('Pattern Summary', lines))); stopifnot(any(grepl('allow.cartesian', lines))); stopifnot(any(grepl('anti_join.*regimen', lines, ignore.case=TRUE))); cat('ALL CHECKS PASSED\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/58_co_administration_analysis.R exists and has >= 150 lines
    - File contains all 9 sections: SECTION 1 through SECTION 9 (SETUP, LOAD, SUB-CATEGORY, SINGLE-AGENT, TEMPORAL SELF-JOIN, DETAIL TABLE, PATTERN SUMMARY, WRITE XLSX, CONSOLE SUMMARY)
    - Header contains decision traceability comments for D-01 through D-10
    - SECTION 2 contains `filter(treatment_type == "Chemotherapy")` for D-04
    - SECTION 2 contains `anti_join(regimen_encounters` for D-05
    - SECTION 4 contains `n_distinct(triggering_code)` grouping by patient_id and treatment_date for D-01
    - SECTION 5 contains `allow.cartesian = TRUE` and `ENCOUNTERID != i.ENCOUNTERID` for self-match exclusion
    - SECTION 5 contains `<= 30` for the 30-day window filter (D-03)
    - SECTION 5 contains `days_apart` calculation with signed value
    - SECTION 6 contains both `index_drug_name` and `coadmin_drug_name` columns (D-08)
    - SECTION 6 contains both `index_triggering_code` and `coadmin_triggering_code` columns (D-08)
    - SECTION 7 contains `pmin` and `pmax` for symmetric pair deduplication
    - SECTION 7 contains `n_instances` and `n_patients` in summarise()
    - SECTION 7 contains `arrange(desc(n_instances))` for frequency ranking
    - SECTION 8 contains `wb_workbook()`, `add_worksheet("Co-Administration Detail")`, `add_worksheet("Pattern Summary")`
    - File sources "R/00_config.R" and "R/utils/utils_assertions.R"
    - File uses assert_rds_exists() for both RDS inputs
    - File does NOT contain saveRDS() (per D-10: investigation script, read-only)
    - File contains `close(.log_con)` at end
  </acceptance_criteria>
  <done>R/58_co_administration_analysis.R is a complete, self-contained investigation script with all 9 sections implementing D-01 through D-10. It loads treatment_episode_detail.rds and treatment_episodes.rds, filters to chemo-only non-regimen encounters, identifies single-agent encounters by patient-date, performs temporal self-join with data.table, builds detail and pattern summary tables with human-readable drug names, and writes a two-sheet xlsx output.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 102 validation to R/88 smoke test</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    R/88_smoke_test_comprehensive.R
    R/58_co_administration_analysis.R
    .planning/phases/102-single-agent-co-administration-analysis/102-CONTEXT.md
  </read_first>
  <action>
Add a new validation section to R/88_smoke_test_comprehensive.R for Phase 102 co-administration analysis. Follow the established pattern from Section 31A (Phase 101 validation).

**1. Update section counter** — The current last numbered section is [33/33]. Insert a new section BEFORE Section 32 (DuckDB) by placing it after the Phase 101 section (Section 31A, around line 2090). Update all subsequent section counters:
- New section: message("\n[32/34] Phase 102: Co-administration analysis validation...")
- Section 32 (DuckDB) becomes [33/34]
- Section 33 (Fixture schema) becomes [34/34]

**2. Add SECTION 31B: PHASE 102 -- CO-ADMINISTRATION ANALYSIS (COADMIN-01, COADMIN-02)**

```r
# ==============================================================================
# SECTION 31B: PHASE 102 -- CO-ADMINISTRATION ANALYSIS (COADMIN-01, COADMIN-02) ----
# ==============================================================================
# Validates R/58 co-administration analysis structural integrity.

message("\n[32/34] Phase 102: Co-administration analysis validation...")

if (file.exists("R/58_co_administration_analysis.R")) {
  r58_lines <- readLines("R/58_co_administration_analysis.R", warn = FALSE)

  # D-04: Chemotherapy-only filter
  check("R/58 filters to Chemotherapy treatment_type (D-04)",
        any(grepl('treatment_type.*==.*"Chemotherapy"', r58_lines)))

  # D-05: Regimen exclusion via anti_join
  check("R/58 excludes regimen-classified encounters via anti_join (D-05)",
        any(grepl("anti_join.*regimen", r58_lines, ignore.case = TRUE)))

  # D-01: Single-agent identification by patient_id + treatment_date grouping
  check("R/58 groups by patient_id and treatment_date for single-agent ID (D-01)",
        any(grepl("group_by.*patient_id.*treatment_date", r58_lines)))

  # D-01: n_distinct(triggering_code) for single-agent definition
  check("R/58 uses n_distinct(triggering_code) for single-agent count (D-01)",
        any(grepl("n_distinct.*triggering_code", r58_lines)))

  # D-03: 30-day window
  check("R/58 uses 30-day window for co-administration (D-03)",
        any(grepl("<= 30", r58_lines)) || any(grepl("<=\\s*30", r58_lines)))

  # D-03: Self-match exclusion
  check("R/58 excludes self-matches (ENCOUNTERID != i.ENCOUNTERID)",
        any(grepl("ENCOUNTERID.*!=.*i\\.ENCOUNTERID", r58_lines)))

  # D-07: days_apart column in detail table
  check("R/58 calculates days_apart for temporal analysis (D-07)",
        any(grepl("days_apart", r58_lines)))

  # D-08: Both triggering_code and sub_category/drug_name shown
  check("R/58 includes index_drug_name and coadmin_drug_name (D-08)",
        any(grepl("index_drug_name", r58_lines)) && any(grepl("coadmin_drug_name", r58_lines)))

  check("R/58 includes index_triggering_code and coadmin_triggering_code (D-08)",
        any(grepl("index_triggering_code", r58_lines)) && any(grepl("coadmin_triggering_code", r58_lines)))

  # D-06: Two-sheet xlsx output
  check("R/58 creates 'Co-Administration Detail' sheet (D-06, COADMIN-01)",
        any(grepl("Co-Administration Detail", r58_lines)))

  check("R/58 creates 'Pattern Summary' sheet (D-06, COADMIN-02)",
        any(grepl("Pattern Summary", r58_lines)))

  # COADMIN-02: Symmetric pair deduplication (pmin/pmax)
  check("R/58 uses pmin/pmax for symmetric pair deduplication (COADMIN-02)",
        any(grepl("pmin", r58_lines)) && any(grepl("pmax", r58_lines)))

  # D-09: Script placement in drug grouping decade
  check("R/58 sources R/00_config.R (D-09)",
        any(grepl('source.*R/00_config', r58_lines)))

  # D-10: Investigation script (no saveRDS)
  check("R/58 does NOT contain saveRDS (D-10: investigation only)",
        !any(grepl("saveRDS", r58_lines)))

  # D-10: Uses assert_rds_exists for input validation
  check("R/58 uses assert_rds_exists for input validation",
        any(grepl("assert_rds_exists", r58_lines)))

  # data.table temporal join
  check("R/58 uses data.table cartesian join (allow.cartesian)",
        any(grepl("allow\\.cartesian", r58_lines)))

  # Decision traceability
  check("R/58 header contains decision traceability (D-01 through D-10)",
        any(grepl("D-01", r58_lines)) && any(grepl("D-10", r58_lines)))

  # Optional: output file validation (only if co_administration_analysis.xlsx exists)
  COADMIN_XLSX_PATH <- file.path(CONFIG$output_dir, "co_administration_analysis.xlsx")
  if (file.exists(COADMIN_XLSX_PATH)) {
    tryCatch({
      wb_ca <- openxlsx2::wb_load(COADMIN_XLSX_PATH)
      sheet_names_ca <- wb_ca$sheet_names

      check("co_administration_analysis.xlsx has 2 sheets",
            length(sheet_names_ca) == 2)

      check("Sheet 1 is 'Co-Administration Detail'",
            sheet_names_ca[1] == "Co-Administration Detail")

      check("Sheet 2 is 'Pattern Summary'",
            sheet_names_ca[2] == "Pattern Summary")

      # Check detail table columns
      detail_data <- openxlsx2::wb_to_df(wb_ca, sheet = "Co-Administration Detail", start_row = 1)
      check("Detail table contains days_apart column",
            "days_apart" %in% colnames(detail_data))

      check("Detail table contains index_drug_name column",
            "index_drug_name" %in% colnames(detail_data))

      check("Detail table contains coadmin_drug_name column",
            "coadmin_drug_name" %in% colnames(detail_data))

      # Check pattern summary columns
      summary_data <- openxlsx2::wb_to_df(wb_ca, sheet = "Pattern Summary", start_row = 1)
      check("Pattern Summary contains n_instances column",
            "n_instances" %in% colnames(summary_data))

      check("Pattern Summary contains n_patients column",
            "n_patients" %in% colnames(summary_data))

      check("Pattern Summary is sorted descending by n_instances",
            all(diff(summary_data$n_instances) <= 0))

    }, error = function(e) {
      message(glue("  SKIP: Could not read co_administration xlsx ({e$message})"))
    })
  } else {
    message("  SKIP: co_administration_analysis.xlsx not found (run R/58 first)")
  }

} else {
  message("  FAIL: R/58_co_administration_analysis.R not found")
  failed <- failed + 1L
}
```

**3. Update summary section** (SECTION 16 at bottom of file) — Add these requirement lines before the TEST-01 line:
```r
message("  * COADMIN-01: Co-administration detail table with +/-30-day window (R/58 Phase 102)")
message("  * COADMIN-02: Pattern summary with symmetric pair deduplication (R/58 Phase 102)")
```

**4. Update section counter references** — Change:
- `[32/32] DuckDB integration validation` to `[33/34] DuckDB integration validation`
- `[33/33] Fixture schema validation` to `[34/34] Fixture schema validation`
  </action>
  <verify>
    <automated>Rscript -e "lines <- readLines('R/88_smoke_test_comprehensive.R'); cat('Lines:', length(lines), '\n'); stopifnot(any(grepl('SECTION 31B.*PHASE 102', lines))); stopifnot(any(grepl('COADMIN-01', lines))); stopifnot(any(grepl('COADMIN-02', lines))); stopifnot(any(grepl('58_co_administration', lines))); stopifnot(any(grepl('32/34', lines))); stopifnot(any(grepl('33/34', lines))); stopifnot(any(grepl('34/34', lines))); cat('ALL CHECKS PASSED\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains "SECTION 31B: PHASE 102" comment header
    - R/88 contains checks for COADMIN-01 and COADMIN-02 requirement IDs
    - R/88 contains `readLines("R/58_co_administration_analysis.R"` pattern
    - R/88 validates D-04 (Chemotherapy filter), D-05 (anti_join regimen), D-01 (group_by patient_id treatment_date), D-03 (30-day window), D-06 (two sheets), D-07 (days_apart), D-08 (drug names + codes), D-10 (no saveRDS)
    - Section counter updated: [32/34] for Phase 102, [33/34] for DuckDB, [34/34] for Fixture
    - Summary section contains "COADMIN-01" and "COADMIN-02" requirement messages
    - Optional xlsx runtime validation block checks sheet names, column presence, and sort order
    - Existing sections (31A, 32, 33) are NOT deleted or broken — only counter numbers updated
  </acceptance_criteria>
  <done>R/88 smoke test has a new Section 31B validating Phase 102 structural changes with 18+ static checks covering all locked decisions (D-01 through D-10) and both requirements (COADMIN-01, COADMIN-02), plus optional runtime xlsx validation. Section counters updated to [N/34] format. Summary section lists COADMIN-01 and COADMIN-02 as validated requirements.</done>
</task>

</tasks>

<verification>
1. R/58_co_administration_analysis.R exists with 9 sections implementing all D-01 through D-10 decisions
2. R/88_smoke_test_comprehensive.R has Section 31B with Phase 102 structural validation
3. `Rscript R/88_smoke_test_comprehensive.R` passes (including new Phase 102 section)
4. No existing files modified except R/88 (R/58 is new)
5. No saveRDS() in R/58 (investigation-only script per D-10)
</verification>

<success_criteria>
- R/58 is a complete standalone investigation script that reads treatment_episode_detail.rds and treatment_episodes.rds, filters to chemo-only non-regimen encounters, identifies single-agent encounters, performs temporal self-join, and produces a two-sheet xlsx with Co-Administration Detail and Pattern Summary tables
- R/88 smoke test validates R/58 structural integrity with 18+ checks covering all locked decisions
- `Rscript R/88_smoke_test_comprehensive.R` exits 0 with all checks passing
- No upstream RDS files or existing outputs are modified (per D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/102-single-agent-co-administration-analysis/102-01-SUMMARY.md`
</output>
