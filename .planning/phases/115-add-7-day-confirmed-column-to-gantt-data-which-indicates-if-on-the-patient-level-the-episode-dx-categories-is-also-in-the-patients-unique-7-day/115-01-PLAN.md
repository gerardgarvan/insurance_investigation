---
phase: 115-add-7-day-confirmed-column-to-gantt-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/52_gantt_v2_export.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - GANTT7DAY-01
  - GANTT7DAY-02
  - GANTAGE-01
  - SMOKE-115-01

must_haves:
  truths:
    - "gantt_episodes.csv contains episode_dx_7day_confirmed column with comma-separated subset of episode_dx_categories that are 7-day confirmed at patient level"
    - "gantt_episodes.csv contains age_at_episode column with integer years (floor) between DEMOGRAPHIC BIRTH_DATE and episode_start"
    - "gantt_episodes.csv schema is 20 columns (18 existing + 2 new)"
    - "episode_dx_7day_confirmed values are alphabetically sorted and are a strict subset of episode_dx_categories"
    - "R/88 smoke test passes with Phase 115 structural checks"
  artifacts:
    - path: "R/52_gantt_v2_export.R"
      provides: "Two new Gantt export columns: episode_dx_7day_confirmed, age_at_episode"
      contains: "episode_dx_7day_confirmed"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Phase 115 structural validation"
      contains: "Phase 115"
  key_links:
    - from: "R/52_gantt_v2_export.R"
      to: "output/tables/cancer_summary.csv"
      via: "read.csv for 7-day confirmed patient-code lookup"
      pattern: "cancer_summary\\.csv"
    - from: "R/52_gantt_v2_export.R"
      to: "DuckDB DEMOGRAPHIC table"
      via: "get_pcornet_table for birth dates"
      pattern: "get_pcornet_table.*DEMOGRAPHIC"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/52_gantt_v2_export.R"
      via: "readLines structural checks"
      pattern: "r52_lines"
---

<objective>
Add two new columns to gantt_episodes.csv: (1) `episode_dx_7day_confirmed` showing which of each episode's dx categories are 7-day confirmed at the patient level, and (2) `age_at_episode` showing patient age in integer years at episode start. Update R/88 smoke test for structural validation.

Purpose: The 7-day confirmed column provides a clinical quality signal distinguishing robustly confirmed diagnoses from incidental single-code appearances. Age at episode enables age-stratified analysis without downstream demographic joins.

Output: Modified R/52 with 20-column EPISODES_SCHEMA (was 18), updated R/88 with Phase 115 checks.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@R/52_gantt_v2_export.R
@R/45_cancer_summary.R
@R/28_episode_classification.R
@R/utils/utils_cancer.R
@R/00_config.R
@R/88_smoke_test_comprehensive.R

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/52_gantt_v2_export.R (lines 140-149):
```r
EPISODES_SCHEMA <- c(
  "patient_id", "treatment_type", "episode_number",
  "episode_start", "episode_stop", "episode_length_days",
  "distinct_dates_in_episode",
  "triggering_codes", "drug_names", "triggering_code_descriptions",
  "cancer_category", "is_hodgkin",
  "drug_group",
  "code_type", "source_table", "sct_cross_use_flag",
  "episode_dx_codes", "episode_dx_categories"
)
```

From R/45_cancer_summary.R (lines 256-304):
- cancer_summary CSV has columns: ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7
- `two_or_more_unique_dates_gt_7` is the 7-day confirmation flag (1 = confirmed, 0 = not)
- Each row is a patient-code pair
- `classify_codes()` maps cancer_code to category name

From R/utils/utils_cancer.R:
```r
classify_codes <- function(codes) { ... }  # ICD code -> category name mapping
```

From R/29_first_line_and_death_analysis.R (lines 106-110) -- established DEMOGRAPHIC query pattern:
```r
demographics <- get_pcornet_table("DEMOGRAPHIC") %>%
  select(ID, BIRTH_DATE) %>%
  collect() %>%
  mutate(BIRTH_DATE = parse_pcornet_date(BIRTH_DATE)) %>%
  filter(!is.na(BIRTH_DATE))
```

From R/52 clean_multi_value (lines 598-612) -- established multi-value cleanup:
```r
clean_multi_value <- function(field_str, sep_in = ",", sep_out = ";") {
  # dedup, drop blanks, sort ascending, paste with sep_out
}
```

Death pseudo-treatment episode pattern (R/52 lines 365-397):
- All 20 columns must be present including both new columns
- New columns get NA_character_ and NA_integer_ respectively for pseudo-treatment rows

HL Diagnosis pseudo-treatment episode pattern (R/52 lines 487-519):
- Same as Death: all 20 columns, new columns get NA values for pseudo-treatment rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add episode_dx_7day_confirmed and age_at_episode columns to R/52 Gantt export</name>
  <files>R/52_gantt_v2_export.R</files>

  <read_first>
    R/52_gantt_v2_export.R
    R/45_cancer_summary.R
    R/29_first_line_and_death_analysis.R
    R/utils/utils_cancer.R
    R/00_config.R
  </read_first>

  <action>
Modify R/52_gantt_v2_export.R to add two new columns. All changes are additive -- no existing columns or logic are modified.

**Step 1: Update EPISODES_SCHEMA (line ~140)**
Add two new entries at the end of the EPISODES_SCHEMA vector:
```r
EPISODES_SCHEMA <- c(
  "patient_id", "treatment_type", "episode_number",
  "episode_start", "episode_stop", "episode_length_days",
  "distinct_dates_in_episode",
  "triggering_codes", "drug_names", "triggering_code_descriptions",
  "cancer_category", "is_hodgkin",
  "drug_group",
  "code_type", "source_table", "sct_cross_use_flag",
  "episode_dx_codes", "episode_dx_categories",
  "episode_dx_7day_confirmed", "age_at_episode"
)
```

**Step 2: Add source() for utils_cancer.R (after existing source lines ~126-127)**
Add: `source("R/utils/utils_cancer.R")` (needed for classify_codes() to map cancer_summary codes to category names).

**Step 3: Add cancer_summary.csv path constant (after COHORT_RDS ~133)**
```r
CANCER_SUMMARY_CSV <- build_output_path("tables", "cancer_summary.csv")
```

**Step 4: Build patient-level 7-day confirmed categories lookup (new section after SECTION 2 loads, before SECTION 3)**

Insert a new section "SECTION 2B: 7-DAY CONFIRMED CATEGORIES LOOKUP" after the existing guard clauses (after line ~228):

```r
# --- SECTION 2B: 7-DAY CONFIRMED CATEGORIES LOOKUP (Phase 115, D-01 through D-05) ---

message("\n--- Building patient-level 7-day confirmed categories lookup ---")

patient_7day_categories <- NULL
if (file.exists(CANCER_SUMMARY_CSV)) {
  cancer_summary_raw <- read.csv(CANCER_SUMMARY_CSV, stringsAsFactors = FALSE)
  message(glue("  Loaded cancer_summary.csv: {nrow(cancer_summary_raw)} patient-code rows"))

  # Filter to 7-day confirmed rows only (per D-04)
  confirmed_rows <- cancer_summary_raw %>%
    filter(two_or_more_unique_dates_gt_7 == 1)

  message(glue("  7-day confirmed rows: {nrow(confirmed_rows)}"))

  # Map cancer_code to category name using classify_codes() (per D-03, D-04)
  confirmed_rows <- confirmed_rows %>%
    mutate(category = classify_codes(cancer_code))

  # Aggregate to patient level: set of confirmed category names per patient
  patient_7day_categories <- confirmed_rows %>%
    filter(!is.na(category)) %>%
    group_by(ID) %>%
    summarise(
      confirmed_categories = list(sort(unique(category))),
      .groups = "drop"
    )

  message(glue("  Patients with 7-day confirmed categories: {nrow(patient_7day_categories)}"))
} else {
  message("  WARNING: cancer_summary.csv not found. episode_dx_7day_confirmed will be empty.")
}
```

**Step 5: Build DEMOGRAPHIC birth date lookup (in same section area)**

Add immediately after the 7-day confirmed lookup:

```r
# --- SECTION 2C: DEMOGRAPHIC BIRTH DATE LOOKUP (Phase 115, D-06, D-07) ---

message("\n--- Loading DEMOGRAPHIC birth dates for age_at_episode ---")

birth_dates <- NULL
tryCatch({
  open_pcornet_con()
  birth_dates <- get_pcornet_table("DEMOGRAPHIC") %>%
    select(ID, BIRTH_DATE) %>%
    collect() %>%
    mutate(BIRTH_DATE = parse_pcornet_date(BIRTH_DATE)) %>%
    filter(!is.na(BIRTH_DATE))
  close_pcornet_con()
  message(glue("  Loaded {nrow(birth_dates)} patients with valid birth dates"))
}, error = function(e) {
  message(glue("  WARNING: Could not load DEMOGRAPHIC birth dates: {conditionMessage(e)}"))
  try(close_pcornet_con(), silent = TRUE)
})
```

**Step 6: Compute episode_dx_7day_confirmed column (in SECTION 4 after episodes_export is built, before the left_join)**

After the initial `episodes_export <- episodes %>% select(...)` block (line ~272) and the left_join that adds cancer_category etc., add computation of the new column.

The cleanest approach: add both new columns AFTER the final `select()` in the column trimming step (SECTION 4D Step 6, line ~690). Insert BEFORE the final select() that trims columns:

```r
# --- Phase 115: Compute episode_dx_7day_confirmed (D-01 through D-05) ---
if (!is.null(patient_7day_categories)) {
  # Helper: intersect episode categories with patient's confirmed categories
  get_confirmed_subset <- function(patient_id_val, dx_categories_str) {
    if (is.na(dx_categories_str) || dx_categories_str == "") return("")

    # Parse episode categories (semicolon-separated after clean_multi_value)
    ep_cats <- str_split(dx_categories_str, ";")[[1]]
    ep_cats <- str_trim(ep_cats)
    ep_cats <- ep_cats[ep_cats != ""]

    if (length(ep_cats) == 0) return("")

    # Look up patient's confirmed categories
    patient_row <- patient_7day_categories %>% filter(ID == patient_id_val)
    if (nrow(patient_row) == 0) return("")

    confirmed <- patient_row$confirmed_categories[[1]]
    matched <- intersect(ep_cats, confirmed)

    if (length(matched) == 0) return("")
    paste(sort(matched), collapse = ";")
  }

  episodes_export <- episodes_export %>%
    mutate(
      episode_dx_7day_confirmed = mapply(
        get_confirmed_subset,
        patient_id, episode_dx_categories,
        USE.NAMES = FALSE
      )
    )
  n_with_confirmed <- sum(episodes_export$episode_dx_7day_confirmed != "", na.rm = TRUE)
  message(glue("  Phase 115: {n_with_confirmed} episodes with 7-day confirmed dx categories"))
} else {
  episodes_export <- episodes_export %>%
    mutate(episode_dx_7day_confirmed = "")
  message("  Phase 115: episode_dx_7day_confirmed defaulted to empty (no cancer_summary.csv)")
}
```

**Step 7: Compute age_at_episode column**

Immediately after the 7-day confirmed computation:

```r
# --- Phase 115: Compute age_at_episode (D-06, D-07) ---
if (!is.null(birth_dates)) {
  episodes_export <- episodes_export %>%
    left_join(birth_dates, by = c("patient_id" = "ID")) %>%
    mutate(
      age_at_episode = as.integer(floor(
        as.numeric(difftime(episode_start, BIRTH_DATE, units = "days")) / 365.25
      ))
    ) %>%
    select(-BIRTH_DATE)

  n_with_age <- sum(!is.na(episodes_export$age_at_episode))
  message(glue("  Phase 115: {n_with_age}/{nrow(episodes_export)} episodes with age_at_episode"))
} else {
  episodes_export <- episodes_export %>%
    mutate(age_at_episode = NA_integer_)
  message("  Phase 115: age_at_episode defaulted to NA (no DEMOGRAPHIC data)")
}
```

**Step 8: Update SECTION 4D Step 6 final select() (line ~690)**

Add the two new columns at the end of the final select():
```r
  select(
    # ... existing 18 columns ...
    episode_dx_codes, episode_dx_categories,
    # Phase 115: 7-day confirmed subset + age
    episode_dx_7day_confirmed, age_at_episode
  )
```

**Step 9: Update Death pseudo-treatment rows (SECTION 4B, line ~365)**

Add two new columns to the death_episodes mutate:
```r
episode_dx_7day_confirmed = "",
age_at_episode = NA_integer_
```
And add them to the death_episodes select() at the end.

**Step 10: Update HL Diagnosis pseudo-treatment rows (SECTION 4C, line ~487)**

Add two new columns to the hl_dx_episodes mutate:
```r
episode_dx_7day_confirmed = "",
age_at_episode = NA_integer_
```
And add them to the hl_dx_episodes select() at the end.

**Step 11: Update guard clauses (after line ~225)**

Add guard clauses for the two new columns (following the established pattern):
```r
if (!"episode_dx_7day_confirmed" %in% names(episodes)) {
  # Phase 115 column — computed at export time, not in RDS
}
if (!"age_at_episode" %in% names(episodes)) {
  # Phase 115 column — computed at export time, not in RDS
}
```
Note: These columns are NOT in the RDS -- they are computed at export time in R/52. No guard clauses needed for RDS source columns. But the episodes_export select() at Section 4 line ~272 should NOT try to select them from episodes -- they are added AFTER the select().

**Step 12: Update header comment schema documentation**

Update the header comments (lines ~39-57) to show 20 columns:
- Add column 19: `episode_dx_7day_confirmed (chr) - Semicolon-separated 7-day confirmed cancer categories (subset of episode_dx_categories)`
- Add column 20: `age_at_episode (int) - Patient age in integer years (floor) at episode_start`
- Update "18 columns" references to "20 columns" in header comments

**Step 13: Update the clean_multi_value applications**

In SECTION 4D Step 1 (line ~615), add episode_dx_7day_confirmed to the clean_multi_value applications:
```r
episode_dx_7day_confirmed = sapply(episode_dx_7day_confirmed, clean_multi_value, USE.NAMES = FALSE),
```

**IMPORTANT placement notes:**
- The 7-day confirmed computation and age computation must happen AFTER SECTION 4D Step 1 (clean_multi_value) runs on episode_dx_categories (since the get_confirmed_subset function reads the semicolon-separated format). OR, do the 7-day computation BEFORE clean_multi_value using comma separator. The cleanest approach: compute BOTH new columns in a dedicated block BETWEEN the initial episodes_export build (SECTION 4) and the clean_multi_value step (SECTION 4D Step 1), using comma as input separator since episode_dx_categories is still comma-separated at that point. Then let clean_multi_value handle the semicolon conversion for episode_dx_7day_confirmed.

Adjust get_confirmed_subset to use comma separator:
```r
ep_cats <- str_split(dx_categories_str, ",")[[1]]
```

This way the flow is: build episodes_export -> compute 7day_confirmed + age -> clean_multi_value converts all multi-value fields to semicolons -> NA cleanup -> column trimming -> schema verification -> write CSV.
  </action>

  <verify>
    <automated>Rscript -e "lines <- readLines('R/52_gantt_v2_export.R'); schema_line <- grep('EPISODES_SCHEMA', lines); cat('EPISODES_SCHEMA lines:', schema_line[1:3], '\n'); cat('Has episode_dx_7day_confirmed:', any(grepl('episode_dx_7day_confirmed', lines)), '\n'); cat('Has age_at_episode:', any(grepl('age_at_episode', lines)), '\n'); cat('Has classify_codes:', any(grepl('classify_codes|utils_cancer', lines)), '\n'); cat('Has DEMOGRAPHIC:', any(grepl('DEMOGRAPHIC', lines)), '\n'); cat('Has cancer_summary.csv:', any(grepl('cancer_summary', lines)), '\n'); cat('Schema length:', length(eval(parse(text=paste(lines[schema_line[1]:(schema_line[1]+10)], collapse='\n')))), '\n')"</automated>
  </verify>

  <acceptance_criteria>
    - R/52_gantt_v2_export.R EPISODES_SCHEMA vector contains "episode_dx_7day_confirmed" and "age_at_episode" (20 entries total)
    - R/52 contains `source("R/utils/utils_cancer.R")` line
    - R/52 contains `classify_codes(cancer_code)` for mapping confirmed codes to categories
    - R/52 contains `get_pcornet_table("DEMOGRAPHIC")` for birth date lookup
    - R/52 contains `cancer_summary.csv` reference for 7-day confirmed data
    - R/52 contains `two_or_more_unique_dates_gt_7 == 1` filter for confirmed rows
    - R/52 contains `as.integer(floor(` for age computation (integer years, floored)
    - R/52 death pseudo-treatment rows include `episode_dx_7day_confirmed = ""` and `age_at_episode = NA_integer_`
    - R/52 HL Diagnosis pseudo-treatment rows include `episode_dx_7day_confirmed = ""` and `age_at_episode = NA_integer_`
    - R/52 clean_multi_value is applied to episode_dx_7day_confirmed
    - R/52 header comments show 20 columns and document both new columns
    - DETAIL_SCHEMA is unchanged at 14 columns
  </acceptance_criteria>

  <done>
R/52 produces gantt_episodes.csv with 20 columns including episode_dx_7day_confirmed (comma-separated 7-day confirmed category subset, alphabetically sorted) and age_at_episode (integer years, NA for missing birth dates). Death and HL Diagnosis pseudo-treatment rows include both new columns with appropriate defaults. Schema verification passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 115 structural validation to R/88 smoke test</name>
  <files>R/88_smoke_test_comprehensive.R</files>

  <read_first>
    R/88_smoke_test_comprehensive.R
    R/52_gantt_v2_export.R
  </read_first>

  <action>
Add a new Phase 115 smoke test section to R/88_smoke_test_comprehensive.R. Insert AFTER the Phase 114 section (SECTION 15k, ending around line 3371) and BEFORE the final summary block (line ~3373).

**New section: SECTION 15l: GANTT 7-DAY CONFIRMED + AGE AT EPISODE (Phase 115)**

```r
# ==============================================================================
# SECTION 15l: GANTT 7-DAY CONFIRMED + AGE AT EPISODE (Phase 115) ----
# ==============================================================================

message("\n[Phase 115] Gantt 7-day confirmed + age at episode...")

# Check 1: R/52 EPISODES_SCHEMA includes episode_dx_7day_confirmed
check("R/52 EPISODES_SCHEMA includes episode_dx_7day_confirmed (Phase 115)",
      any(grepl("episode_dx_7day_confirmed", r52_lines)))

# Check 2: R/52 EPISODES_SCHEMA includes age_at_episode
check("R/52 EPISODES_SCHEMA includes age_at_episode (Phase 115)",
      any(grepl("age_at_episode", r52_lines)))

# Check 3: R/52 EPISODES_SCHEMA has 20 entries (was 18)
r52_schema_start <- grep("^EPISODES_SCHEMA\\s*<-", r52_lines)[1]
if (!is.na(r52_schema_start)) {
  r52_schema_end <- r52_schema_start
  while (r52_schema_end <= length(r52_lines) && !grepl("\\)$", r52_lines[r52_schema_end])) {
    r52_schema_end <- r52_schema_end + 1
  }
  schema_text <- paste(r52_lines[r52_schema_start:r52_schema_end], collapse = "\n")
  schema_entries <- lengths(regmatches(schema_text, gregexpr('"[a-z_]+"', schema_text)))
  check("R/52 EPISODES_SCHEMA has 20 entries (Phase 115: +2 from 18)",
        schema_entries == 20)
} else {
  check("R/52 EPISODES_SCHEMA has 20 entries (Phase 115: +2 from 18)", FALSE)
}

# Check 4: R/52 sources utils_cancer.R (needed for classify_codes)
check("R/52 sources utils_cancer.R for classify_codes (Phase 115)",
      any(grepl('source.*utils_cancer', r52_lines)))

# Check 5: R/52 reads cancer_summary.csv for 7-day confirmed data
check("R/52 references cancer_summary.csv (Phase 115)",
      any(grepl("cancer_summary\\.csv|cancer_summary", r52_lines)))

# Check 6: R/52 filters on two_or_more_unique_dates_gt_7 for confirmation
check("R/52 filters two_or_more_unique_dates_gt_7 == 1 for 7-day confirmed (Phase 115)",
      any(grepl("two_or_more_unique_dates_gt_7.*==.*1", r52_lines)))

# Check 7: R/52 queries DEMOGRAPHIC for birth dates
check("R/52 queries DEMOGRAPHIC table for birth dates (Phase 115)",
      any(grepl("DEMOGRAPHIC", r52_lines)))

# Check 8: R/52 computes age as integer floor
check("R/52 computes age_at_episode as integer floor (Phase 115)",
      any(grepl("as\\.integer.*floor", r52_text)))

# Check 9: R/52 applies clean_multi_value to episode_dx_7day_confirmed
check("R/52 applies clean_multi_value to episode_dx_7day_confirmed (Phase 115)",
      any(grepl("episode_dx_7day_confirmed.*clean_multi_value", r52_text)))

# Check 10: R/52 DETAIL_SCHEMA does NOT include episode_dx_7day_confirmed (episodes-only)
check("R/52 DETAIL_SCHEMA excludes episode_dx_7day_confirmed (Phase 115: episode-level only)",
      !any(grepl("DETAIL_SCHEMA.*episode_dx_7day_confirmed", r52_text)))

# Check 11: R/52 DETAIL_SCHEMA does NOT include age_at_episode (episodes-only)
check("R/52 DETAIL_SCHEMA excludes age_at_episode (Phase 115: episode-level only)",
      !any(grepl("DETAIL_SCHEMA.*age_at_episode", r52_text)))

# Check 12: Death pseudo-treatment rows include episode_dx_7day_confirmed
check("R/52 Death pseudo-treatment includes episode_dx_7day_confirmed (Phase 115)",
      any(grepl("episode_dx_7day_confirmed.*=.*\"\"", r52_text) &
          grepl("Death", r52_text)))

# Check 13: Death pseudo-treatment rows include age_at_episode
check("R/52 Death pseudo-treatment includes age_at_episode (Phase 115)",
      any(grepl("age_at_episode.*=.*NA_integer_", r52_text)))

# Check 14: R/52 uses classify_codes to map cancer codes to categories
check("R/52 uses classify_codes() for 7-day code-to-category mapping (Phase 115)",
      any(grepl("classify_codes", r52_lines)))
```

Also update the final summary message block (around line 3371) to add Phase 115 entries:

```r
message("  * GANTT7DAY-01: episode_dx_7day_confirmed column in gantt_episodes.csv (Phase 115)")
message("  * GANTAGE-01: age_at_episode column in gantt_episodes.csv (Phase 115)")
message("  * SMOKE-115-01: R/88 validates Phase 115 structural integrity (14 checks)")
```
  </action>

  <verify>
    <automated>Rscript -e "lines <- readLines('R/88_smoke_test_comprehensive.R'); cat('Has Phase 115 section:', any(grepl('Phase 115', lines)), '\n'); cat('Has GANTT7DAY-01:', any(grepl('GANTT7DAY-01', lines)), '\n'); cat('Has GANTAGE-01:', any(grepl('GANTAGE-01', lines)), '\n'); cat('Check count:', sum(grepl('check\\(\"R/52.*Phase 115', lines)), '\n')"</automated>
  </verify>

  <acceptance_criteria>
    - R/88 contains "SECTION 15l: GANTT 7-DAY CONFIRMED + AGE AT EPISODE (Phase 115)" section header
    - R/88 contains at least 14 check() calls referencing "Phase 115"
    - R/88 checks EPISODES_SCHEMA includes "episode_dx_7day_confirmed"
    - R/88 checks EPISODES_SCHEMA includes "age_at_episode"
    - R/88 checks EPISODES_SCHEMA has 20 entries
    - R/88 checks R/52 sources utils_cancer.R
    - R/88 checks R/52 references cancer_summary.csv
    - R/88 checks R/52 uses two_or_more_unique_dates_gt_7
    - R/88 checks R/52 queries DEMOGRAPHIC
    - R/88 checks age computed as integer floor
    - R/88 checks clean_multi_value applied to episode_dx_7day_confirmed
    - R/88 checks DETAIL_SCHEMA excludes both new columns
    - R/88 final summary includes "GANTT7DAY-01", "GANTAGE-01", "SMOKE-115-01" message lines
  </acceptance_criteria>

  <done>
R/88 smoke test includes Phase 115 section with 14 structural checks validating that R/52 correctly defines, computes, and exports both new columns (episode_dx_7day_confirmed, age_at_episode). Smoke test passes when run via `Rscript R/88_smoke_test_comprehensive.R`.
  </done>
</task>

</tasks>

<verification>
1. `Rscript R/88_smoke_test_comprehensive.R` exits 0 (all structural checks pass)
2. R/52 EPISODES_SCHEMA has exactly 20 entries
3. DETAIL_SCHEMA remains at 14 entries (unchanged)
4. grep confirms `episode_dx_7day_confirmed` and `age_at_episode` present in R/52
5. grep confirms no changes to DETAIL_SCHEMA (gantt_detail.csv unchanged per D-08)
</verification>

<success_criteria>
- gantt_episodes.csv schema expanded from 18 to 20 columns with episode_dx_7day_confirmed and age_at_episode
- 7-day confirmed column intersects episode dx categories with patient-level cancer_summary 7-day flags
- Age column computes integer years (floor) from DEMOGRAPHIC BIRTH_DATE to episode_start
- Both columns alphabetically sorted where multi-value (per Phase 112 universal sort rule)
- Death and HL Diagnosis pseudo-treatment rows include both new columns with appropriate defaults
- R/88 smoke test validates Phase 115 structural integrity
- gantt_detail.csv schema unchanged at 14 columns
</success_criteria>

<output>
After completion, create `.planning/phases/115-add-7-day-confirmed-column-to-gantt-data-which-indicates-if-on-the-patient-level-the-episode-dx-categories-is-also-in-the-patients-unique-7-day/115-01-SUMMARY.md`
</output>
