---
phase: 79-code-investigations-new-tables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/54_investigate_sct_0362.R
  - R/55_verify_replaced_by_codes.R
autonomous: true
requirements: [CODE-01, CODE-02, QUAL-01]

must_haves:
  truths:
    - "R/54 produces multi-sheet xlsx distinguishing true SCT from coding artifacts for 0362 patients"
    - "R/55 produces verification report with PASS/FAIL status for all replaced-by code pairs"
    - "R/55 detects cycles and long chains (>3 steps) in replacement mappings using igraph"
    - "Both scripts follow v2.0 standards with documentation headers, checkmate assertions, section structure"
  artifacts:
    - path: "R/54_investigate_sct_0362.R"
      provides: "SCT code 0362 encounter-level investigation with automated recommendation"
      contains: "sct_0362_investigation"
    - path: "R/55_verify_replaced_by_codes.R"
      provides: "Replaced-by code verification with cycle detection"
      contains: "is_dag"
  key_links:
    - from: "R/54_investigate_sct_0362.R"
      to: "DuckDB PROCEDURES table"
      via: "get_pcornet_table('PROCEDURES')"
      pattern: "get_pcornet_table.*PROCEDURES"
    - from: "R/54_investigate_sct_0362.R"
      to: "R/00_config.R"
      via: "TREATMENT_CODES$sct_cpt for standard SCT codes"
      pattern: "TREATMENT_CODES"
    - from: "R/55_verify_replaced_by_codes.R"
      to: "all_codes_resolved_next_tables_v2.1.xlsx"
      via: "openxlsx2 wb_load for replaced-by pairs"
      pattern: "wb_load|read_xlsx"
    - from: "R/55_verify_replaced_by_codes.R"
      to: "R/00_config.R"
      via: "DRUG_GROUPINGS and TREATMENT_CODES for category verification"
      pattern: "DRUG_GROUPINGS"
---

<objective>
Create two code investigation scripts: R/54 to investigate SCT code 0362 across 90 patients (encounter-level profiling with automated recommendation), and R/55 to verify replaced-by code mappings from the xlsx reference with pairwise validation and graph-based cycle detection.

Purpose: CODE-02 resolves open question about whether revenue code 0362 represents true SCT procedures or coding artifacts. CODE-01 validates replaced-by code mappings to prevent broken code resolution chains.
Output: Two multi-sheet xlsx investigation reports with console diagnostics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-code-investigations-new-tables/79-CONTEXT.md
@.planning/phases/79-code-investigations-new-tables/79-RESEARCH.md

@R/00_config.R
@R/35_death_cause_quality.R
@R/50_all_codes_resolved.R
@R/utils/utils_assertions.R
@R/utils/utils_duckdb.R

<interfaces>
<!-- Key types and contracts the executor needs. -->

From R/00_config.R (TREATMENT_CODES SCT section, lines ~1998-2446):
```r
TREATMENT_CODES$sct_cpt = c("38230", "38232", "38240", "38241", "38204", "38205", "38206", "38208", "38209", "38210", "38211", "38212", "38213", "38214", "38215")
TREATMENT_CODES$sct_hcpcs = c("S2140", "S2142", "S2150")
TREATMENT_CODES$sct_revenue = c("0362", "0815")
```

From R/00_config.R (DRUG_GROUPINGS, line 1153):
```r
DRUG_GROUPINGS <- c(
  # 454-entry named vector: code -> category
  # Categories: "Chemotherapy", "Radiation", "SCT", "Immunotherapy", "Supportive Care"
)
```

From R/utils/utils_duckdb.R:
```r
open_pcornet_con()          # Opens DuckDB connection
get_pcornet_table("TABLE")  # Returns lazy table reference
```

From R/utils/utils_assertions.R:
```r
assert_rds_exists(path, script_name)
assert_df_valid(df, name, required_cols, script_name, allow_empty = FALSE)
warn_row_count(df, name, expected_min, expected_max, script_name)
```

From R/35_death_cause_quality.R (pattern template):
- Section structure: SECTION 1: SETUP, SECTION 2: LOAD, SECTION 3+: ANALYSIS, SECTION N: OUTPUT
- Header format: # ==============================================================================
- suppressPackageStartupMessages({}) block
- source("R/00_config.R") then source utilities
- USE_DUCKDB <- TRUE / open_pcornet_con() pattern
- openxlsx2 wb_workbook() pipe API for multi-sheet output
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/54_investigate_sct_0362.R -- SCT Code 0362 Investigation</name>
  <files>R/54_investigate_sct_0362.R</files>
  <read_first>
    R/35_death_cause_quality.R (template for investigation script structure, multi-sheet xlsx output pattern)
    R/00_config.R (TREATMENT_CODES$sct_cpt, sct_hcpcs, sct_revenue for standard SCT codes; CONFIG paths)
    R/utils/utils_duckdb.R (open_pcornet_con, get_pcornet_table)
    R/utils/utils_assertions.R (assert_df_valid, warn_row_count)
    R/76_treatment_source_coverage.R (encounter-level profiling pattern)
  </read_first>
  <action>
Create R/54_investigate_sct_0362.R following R/35 script structure (per D-02, D-05 through D-07, QUAL-01):

**Documentation header** (per v2.0 standards from R/35):
- Purpose: Investigate SCT code 0362 encounter-level data quality for ~90 patients
- Inputs: DuckDB PROCEDURES table, DuckDB DIAGNOSIS table, R/00_config.R (TREATMENT_CODES)
- Outputs: output/sct_0362_investigation.xlsx (3-sheet workbook)
- Dependencies: R/00_config.R, R/utils/utils_duckdb.R, R/utils/utils_assertions.R, openxlsx2
- Requirements: CODE-02
- Decision traceability: D-05 through D-07

**SECTION 1: SETUP AND CONFIGURATION**
```r
suppressPackageStartupMessages({
  library(dplyr)
  library(glue)
  library(stringr)
  library(openxlsx2)
  library(checkmate)
})

source("R/00_config.R")
source("R/utils/utils_duckdb.R")

OUTPUT_XLSX <- file.path(CONFIG$output_dir, "sct_0362_investigation.xlsx")

message("=== Phase 79: SCT Code 0362 Investigation ===\n")
message(glue("Output: {OUTPUT_XLSX}\n"))
```

**SECTION 2: IDENTIFY 0362 ENCOUNTERS**
- USE_DUCKDB <- TRUE, open_pcornet_con()
- Query PROCEDURES table: filter(PX == "0362", PX_TYPE == "RE") to get distinct PATID (as ID), ENCOUNTERID pairs
- Log count: message(glue("  Found {n_distinct(encounters_0362$ID)} patients with {nrow(encounters_0362)} encounters"))

**SECTION 3: PULL FULL ENCOUNTER PROFILES (per D-05)**
- For all encounters found in Section 2, pull ALL procedures: get_pcornet_table("PROCEDURES") %>% semi_join(encounters_0362, by = c("ID", "ENCOUNTERID")) %>% collect()
- For all encounters found in Section 2, pull ALL diagnoses: get_pcornet_table("DIAGNOSIS") %>% semi_join(encounters_0362, by = c("ID", "ENCOUNTERID")) %>% collect()
- assert_df_valid() on both results

**SECTION 4: DETECT STANDARD SCT CODES**
- Define standard_sct_codes <- c(TREATMENT_CODES$sct_cpt, TREATMENT_CODES$sct_hcpcs, "0815") -- 38204-38241 plus S2140/S2142/S2150 plus revenue 0815
- For each 0362 encounter, check if ANY procedure in the same encounter matches standard_sct_codes (by PX column)
- Compute per-patient: has_other_sct = TRUE/FALSE, other_sct_codes = comma-separated list of standard SCT codes found
- Compute overlap_rate: n_patients_with_other_sct / n_total_patients * 100

**SECTION 5: BUILD OUTPUT TABLES (per D-06)**

Sheet 1 - "Patient Summary": One row per patient with columns:
- PATID (ID), encounter_count (how many encounters have 0362), has_other_sct (TRUE/FALSE), other_sct_codes_found (comma-separated), n_procedures_total, n_diagnoses_total

Sheet 2 - "Encounter Detail": One row per encounter with columns:
- PATID (ID), ENCOUNTERID, encounter_date (ADMIT_DATE or PX_DATE), all_procedures (comma-separated PX values), all_diagnoses (comma-separated DX values), has_standard_sct (TRUE/FALSE)

Sheet 3 - "Summary Statistics":
- total_patients, total_encounters, patients_with_other_sct, patients_without_other_sct, overlap_rate_pct
- Automated recommendation (per D-07):
  - If overlap_rate > 80: recommendation = "CONFIRMED SCT: >80% of 0362 patients have standard SCT codes"
  - If overlap_rate < 30: recommendation = "LIKELY CODING ARTIFACT: <30% have standard SCT codes"
  - Else: recommendation = "MANUAL REVIEW NEEDED: 30-80% overlap rate"

**SECTION 6: WRITE XLSX OUTPUT**
```r
wb <- wb_workbook()
wb$add_worksheet("Patient Summary")
wb$add_data("Patient Summary", patient_summary, start_row = 1, col_names = TRUE)
wb$add_worksheet("Encounter Detail")
wb$add_data("Encounter Detail", encounter_detail, start_row = 1, col_names = TRUE)
wb$add_worksheet("Summary Statistics")
wb$add_data("Summary Statistics", summary_stats, start_row = 1, col_names = TRUE)
wb$save(OUTPUT_XLSX)
```
- Console diagnostics: print overlap rate and recommendation to console via message()

**SECTION 7: CONSOLE SUMMARY**
- Print recommendation and key stats
- message(glue("Overlap rate: {overlap_rate}% ({n_with_sct}/{n_total} patients)"))
- message(glue("Recommendation: {recommendation}"))
  </action>
  <verify>
    <automated>Rscript -e "source('R/00_config.R'); stopifnot(file.exists('R/54_investigate_sct_0362.R')); cat('R/54 exists\n'); lines <- readLines('R/54_investigate_sct_0362.R'); stopifnot(any(grepl('SECTION 1:', lines))); stopifnot(any(grepl('get_pcornet_table.*PROCEDURES', lines))); stopifnot(any(grepl('0362', lines))); stopifnot(any(grepl('sct_cpt|standard_sct', lines))); stopifnot(any(grepl('wb_workbook', lines))); stopifnot(any(grepl('CODE-02', lines))); stopifnot(any(grepl('overlap_rate', lines))); cat('All structural checks passed\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/54_investigate_sct_0362.R exists with 7 sections (SETUP through CONSOLE SUMMARY)
    - File contains `# Requirements:` line with `CODE-02`
    - File contains `source("R/00_config.R")` and `source("R/utils/utils_duckdb.R")`
    - File contains `get_pcornet_table("PROCEDURES")` for encounter extraction
    - File contains `get_pcornet_table("DIAGNOSIS")` for diagnosis extraction
    - File contains reference to `TREATMENT_CODES$sct_cpt` or equivalent standard SCT code list including 38204-38241
    - File contains `wb_workbook()` and three `add_worksheet()` calls for Patient Summary, Encounter Detail, Summary Statistics
    - File contains overlap_rate calculation and 3-tier recommendation logic (>80%, <30%, 30-80%)
    - File contains `checkmate` or `assert_df_valid` assertion calls
    - File contains `suppressPackageStartupMessages` block
    - Documentation header includes Purpose, Inputs, Outputs, Dependencies, Requirements, Decision Traceability
  </acceptance_criteria>
  <done>R/54_investigate_sct_0362.R exists, follows v2.0 script standards, queries DuckDB for 0362 encounters, cross-references against standard SCT codes (38204-38241, 0815), produces 3-sheet xlsx with patient summary, encounter detail, and automated recommendation based on overlap rate</done>
</task>

<task type="auto">
  <name>Task 2: Create R/55_verify_replaced_by_codes.R -- Replaced-by Code Verification</name>
  <files>R/55_verify_replaced_by_codes.R</files>
  <read_first>
    R/35_death_cause_quality.R (template for investigation script structure)
    R/50_all_codes_resolved.R (openxlsx2 multi-sheet pattern, code_type_map reference)
    R/00_config.R (DRUG_GROUPINGS named vector at line 1153, TREATMENT_CODES structure)
    data/reference/all_codes_resolved_next_tables_v2.1.xlsx (actual file -- inspect sheet names and column names)
    R/utils/utils_assertions.R (assert_df_valid)
  </read_first>
  <action>
Create R/55_verify_replaced_by_codes.R following R/35 script structure (per D-08 through D-11, QUAL-01):

**Documentation header** (per v2.0 standards):
- Purpose: Verify replaced-by code mappings from all_codes_resolved_next_tables_v2.1.xlsx with pairwise validation and graph-based cycle detection
- Inputs: data/reference/all_codes_resolved_next_tables_v2.1.xlsx (replaced-by column), R/00_config.R (DRUG_GROUPINGS, TREATMENT_CODES)
- Outputs: output/replaced_by_verification.xlsx (3-sheet workbook)
- Dependencies: R/00_config.R, igraph, openxlsx2, checkmate
- Requirements: CODE-01
- Decision traceability: D-08 through D-11

**SECTION 1: SETUP AND CONFIGURATION**
```r
suppressPackageStartupMessages({
  library(dplyr)
  library(glue)
  library(stringr)
  library(openxlsx2)
  library(checkmate)
  library(igraph)
})

source("R/00_config.R")

XLSX_PATH <- "data/reference/all_codes_resolved_next_tables_v2.1.xlsx"
OUTPUT_XLSX <- file.path(CONFIG$output_dir, "replaced_by_verification.xlsx")

message("=== Phase 79: Replaced-by Code Verification ===\n")
message(glue("  Source: {XLSX_PATH}"))
message(glue("  Output: {OUTPUT_XLSX}\n"))
```

**SECTION 2: LOAD AND INSPECT XLSX**
- Use openxlsx2::wb_load(XLSX_PATH) to load workbook
- Get sheet names with wb_get_sheet_names()
- Log sheet names to console: message(glue("  Sheets found: {paste(sheet_names, collapse=', ')}"))
- Read the appropriate sheet containing replaced-by mappings (inspect for columns containing "replaced" or "old"/"new" code pairs)
- Use checkmate::assert_file_exists(XLSX_PATH) for input validation
- After reading, identify the old_code and new_code columns (may be named differently -- use the actual column names found in the file)
- Log: message(glue("  Found {nrow(replaced_by_pairs)} replaced-by mappings"))

**SECTION 3: PAIRWISE VERIFICATION (per D-09)**
Build all_known_codes vector by combining all names from TREATMENT_CODES lists AND names from DRUG_GROUPINGS:
```r
all_treatment_codes <- unlist(TREATMENT_CODES, use.names = FALSE)
all_known_codes <- unique(c(all_treatment_codes, names(DRUG_GROUPINGS)))
```

For each old->new pair, verify:
```r
verification <- replaced_by_pairs %>%
  mutate(
    old_in_codes = old_code %in% all_known_codes,
    new_in_codes = new_code %in% all_known_codes,
    old_group = DRUG_GROUPINGS[old_code],
    new_group = DRUG_GROUPINGS[new_code],
    category_match = case_when(
      is.na(old_group) | is.na(new_group) ~ NA,
      TRUE ~ old_group == new_group
    ),
    status = case_when(
      !old_in_codes & !new_in_codes ~ "MISSING: both codes not in code lists",
      !old_in_codes ~ "MISSING: old code not in code lists",
      !new_in_codes ~ "MISSING: new code not in code lists",
      !is.na(category_match) & !category_match ~ "FAIL: category mismatch",
      TRUE ~ "PASS"
    )
  )
```

Console summary: count of PASS, FAIL, MISSING statuses

**SECTION 4: CHAIN ANALYSIS WITH igraph (per D-10)**
Build directed graph from old->new edges:
```r
edge_list <- replaced_by_pairs %>%
  select(from = old_code, to = new_code) %>%
  filter(!is.na(from), !is.na(to))

g <- graph_from_data_frame(edge_list, directed = TRUE)
```

Cycle detection:
```r
dag_check <- is_dag(g)
if (!dag_check) {
  # Extract cycles for reporting
  cycle_info <- "Cycles detected - see Chain Analysis sheet"
  message("  WARNING: Replacement graph contains cycles!")
} else {
  cycle_info <- "No cycles detected (valid DAG)"
  message("  PASS: No cycles in replacement graph")
}
```

Long chain detection (>3 steps):
- Use distances(g, mode = "out") to compute shortest path lengths between all pairs
- Filter for distances > 3
- Build chain_results tibble with: start_code, end_code, chain_length, intermediate_codes (reconstruct path with shortest_paths())
- Log count of long chains

**SECTION 5: BUILD OUTPUT TABLES (per D-11)**

Sheet 1 - "Pairwise Verification": All rows from verification tibble with columns:
- old_code, new_code, old_in_codes, new_in_codes, old_group, new_group, category_match, status

Sheet 2 - "Chain Analysis":
- Long chains (>3 steps): start_code, end_code, chain_length, path
- Cycle information (if any cycles detected)
- If no long chains: single row stating "No chains >3 steps detected"

Sheet 3 - "Summary Statistics":
- total_pairs, n_pass, n_fail, n_missing, pass_rate_pct
- is_dag (TRUE/FALSE), n_long_chains (>3 steps), max_chain_length
- Overall verdict: "PASS" if is_dag=TRUE and n_fail=0, "NEEDS REVIEW" if fails exist

**SECTION 6: WRITE XLSX OUTPUT**
- Same openxlsx2 wb_workbook() pattern as R/35
- wb$save(OUTPUT_XLSX)

**SECTION 7: CONSOLE SUMMARY**
- Print pass/fail/missing counts
- Print DAG check result
- Print long chain count
- Print overall verdict
  </action>
  <verify>
    <automated>Rscript -e "source('R/00_config.R'); stopifnot(file.exists('R/55_verify_replaced_by_codes.R')); cat('R/55 exists\n'); lines <- readLines('R/55_verify_replaced_by_codes.R'); stopifnot(any(grepl('SECTION 1:', lines))); stopifnot(any(grepl('igraph', lines))); stopifnot(any(grepl('is_dag', lines))); stopifnot(any(grepl('DRUG_GROUPINGS', lines))); stopifnot(any(grepl('wb_workbook', lines))); stopifnot(any(grepl('CODE-01', lines))); stopifnot(any(grepl('PASS|FAIL', lines))); cat('All structural checks passed\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/55_verify_replaced_by_codes.R exists with 7 sections (SETUP through CONSOLE SUMMARY)
    - File contains `# Requirements:` line with `CODE-01`
    - File contains `library(igraph)` in the suppressPackageStartupMessages block
    - File contains `source("R/00_config.R")`
    - File references `data/reference/all_codes_resolved_next_tables_v2.1.xlsx` as input
    - File contains `DRUG_GROUPINGS` for category matching
    - File contains `graph_from_data_frame` and `is_dag` for cycle detection
    - File contains `distances` or equivalent for chain length analysis
    - File contains PASS/FAIL/MISSING status logic in case_when()
    - File contains `wb_workbook()` with three sheets: Pairwise Verification, Chain Analysis, Summary Statistics
    - File contains `checkmate` or `assert_file_exists` assertion
    - Documentation header includes Purpose, Inputs, Outputs, Dependencies, Requirements, Decision Traceability
  </acceptance_criteria>
  <done>R/55_verify_replaced_by_codes.R exists, follows v2.0 script standards, loads replaced-by mappings from xlsx, performs pairwise PASS/FAIL verification against DRUG_GROUPINGS, uses igraph for cycle detection and chain length analysis, produces 3-sheet xlsx verification report</done>
</task>

</tasks>

<verification>
1. Both R/54 and R/55 files exist in R/ directory
2. Both files have v2.0-standard documentation headers (Purpose, Inputs, Outputs, Dependencies, Requirements)
3. Both files use suppressPackageStartupMessages block
4. Both files use section headers with `# --- SECTION N: NAME ----` or `# SECTION N: NAME ----` format
5. R/54 references TREATMENT_CODES$sct_cpt and queries DuckDB PROCEDURES table
6. R/55 references DRUG_GROUPINGS and uses igraph for cycle detection
7. Both files produce multi-sheet xlsx output via openxlsx2
8. Both files include checkmate assertions for input validation
</verification>

<success_criteria>
- R/54_investigate_sct_0362.R: Structurally complete script that queries 0362 encounters, cross-references standard SCT codes, produces 3-sheet xlsx with automated overlap recommendation
- R/55_verify_replaced_by_codes.R: Structurally complete script that loads replaced-by pairs, validates pairwise against DRUG_GROUPINGS, detects cycles via igraph, produces 3-sheet xlsx with PASS/FAIL statuses
- Both scripts follow v2.0 standards (documentation headers, checkmate, section structure, openxlsx2 pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/79-code-investigations-new-tables/79-01-SUMMARY.md`
</output>
