---
phase: 99-fix-gantt-v2-vs-gantt-v1-disagreements-and-bugs-extraneous-columns-etc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/52_gantt_v2_export.R
  - R/51_gantt_data_export.R
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "R/52 exports gantt_episodes.csv and gantt_detail.csv (no _v2 suffix)"
    - "R/52 uses dynamic schema vectors for column verification instead of hardcoded counts"
    - "Episodes CSV has 22 columns (added is_hodgkin, removed encounter_ids/is_sct_conditioning_context/immuno_confidence)"
    - "Detail CSV has 20 columns (added is_hodgkin, removed ENCOUNTERID/is_sct_conditioning_context/immuno_confidence)"
    - "Death and HL Diagnosis pseudo-treatment rows have empty strings for character enrichment columns (not NA)"
    - "is_hodgkin derived from cancer_category via str_detect for all row types"
    - "R/51_gantt_data_export.R is deleted from the codebase"
  artifacts:
    - path: "R/52_gantt_v2_export.R"
      provides: "Consolidated canonical Gantt export with clean schema"
      contains: "EPISODES_SCHEMA"
  key_links:
    - from: "R/52_gantt_v2_export.R"
      to: "output/gantt_episodes.csv"
      via: "OUTPUT_EPISODES file path variable"
      pattern: "OUTPUT_EPISODES\\s*<-.*gantt_episodes\\.csv"
    - from: "R/52_gantt_v2_export.R"
      to: "output/gantt_detail.csv"
      via: "OUTPUT_DETAIL file path variable"
      pattern: "OUTPUT_DETAIL\\s*<-.*gantt_detail\\.csv"
---

<objective>
Consolidate R/52 Gantt export: add schema vectors, add is_hodgkin, remove extraneous columns, fix pseudo-treatment metadata, rename outputs, delete R/51.

Purpose: Eliminate v1/v2 parallel maintenance, clean up schema for Tableau consumption, fix metadata bugs causing misleading filter behavior.
Output: Modified R/52_gantt_v2_export.R with clean 22-column episodes / 20-column detail schema; R/51 deleted.
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-fix-gantt-v2-vs-gantt-v1-disagreements-and-bugs-extraneous-columns-etc/99-CONTEXT.md
@.planning/phases/99-fix-gantt-v2-vs-gantt-v1-disagreements-and-bugs-extraneous-columns-etc/99-RESEARCH.md

<interfaces>
<!-- Key schema definitions the executor needs. Extracted from R/52 current state. -->

Current R/52 episodes select() at line 894 (23 columns):
```r
select(
    patient_id, treatment_type, episode_number,
    episode_start, episode_stop, episode_length_days,
    distinct_dates_in_episode, historical_flag,
    triggering_codes, drug_names, triggering_code_descriptions,
    cancer_category, regimen_label, is_first_line,
    drug_group, cause_of_death,
    medication_name, code_type, source_table, treatment_line, sct_cross_use_flag,
    is_sct_conditioning_context, immuno_confidence  # <-- TO BE REMOVED (D-11)
)
```

Current R/52 detail select() at line 908 (21 columns):
```r
select(
    patient_id, treatment_type, treatment_date,
    triggering_code, drug_name, episode_number,
    episode_start, episode_stop, historical_flag,
    triggering_code_description, cancer_category,
    regimen_label, is_first_line,
    cause_of_death,
    medication_name, code_type, source_table, treatment_line, sct_cross_use_flag,
    is_sct_conditioning_context, immuno_confidence  # <-- TO BE REMOVED (D-11)
)
```

Current R/52 output paths at lines 140-141:
```r
OUTPUT_EPISODES_V2 <- file.path(CONFIG$output_dir, "gantt_episodes_v2.csv")
OUTPUT_DETAIL_V2 <- file.path(CONFIG$output_dir, "gantt_detail_v2.csv")
```

Current R/52 column count verification at lines 925-926:
```r
expected_ep_cols <- 23
expected_detail_cols <- 21
```

Death pseudo-treatment construction at lines 441-479 (key metadata values):
```r
regimen_label = NA_character_,
is_first_line = FALSE,
drug_group = NA_character_,
medication_name = NA_character_,
code_type = NA_character_,
source_table = NA_character_,
treatment_line = NA_character_,
sct_cross_use_flag = NA_character_,
is_sct_conditioning_context = NA,
immuno_confidence = NA_character_
```

HL Diagnosis pseudo-treatment construction at lines 579-606 (key metadata values):
```r
regimen_label = NA_character_,
is_first_line = FALSE,
drug_group = NA_character_,
cause_of_death = NA_character_,
medication_name = NA_character_,
code_type = NA_character_,
source_table = NA_character_,
treatment_line = NA_character_,
sct_cross_use_flag = NA_character_,
is_sct_conditioning_context = NA,
immuno_confidence = NA_character_
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema vectors, is_hodgkin, remove extraneous columns, fix pseudo-treatment metadata, rename outputs in R/52</name>
  <files>R/52_gantt_v2_export.R</files>
  <read_first>
    - R/52_gantt_v2_export.R (full file -- 1007 lines, the primary modification target)
    - R/51_gantt_data_export.R (lines 1-50, to confirm v1 output paths that will be superseded)
    - .planning/phases/99-fix-gantt-v2-vs-gantt-v1-disagreements-and-bugs-extraneous-columns-etc/99-CONTEXT.md (decisions D-01 through D-13)
  </read_first>
  <action>
Modify R/52_gantt_v2_export.R with these specific changes (execute in order):

**1. Update header comment block (lines 1-116)**

Replace the OUTPUTS section (lines 21-22 and 113-114) to reference new filenames:
```
#   - output/gantt_episodes.csv (22 columns, Phase 99 consolidated)
#   - output/gantt_detail.csv (20 columns, Phase 99 consolidated)
```

Update the v2 SCHEMA DOCUMENTATION block (lines 37-85) to reflect the new 22-column episodes schema and 20-column detail schema. The episodes schema adds is_hodgkin (position 13, after cancer_category) and removes encounter_ids, is_sct_conditioning_context, immuno_confidence. The detail schema adds is_hodgkin (position 12, after cancer_category) and removes ENCOUNTERID, is_sct_conditioning_context, immuno_confidence.

Add a note at top: "# Updated by Phase 99: v1 deprecated, schema consolidated (D-01 through D-15)"

**2. Add EPISODES_SCHEMA and DETAIL_SCHEMA vectors after SECTION 1: SETUP (after line 141, per D-13)**

Insert these exact schema definition vectors:
```r
# --- SCHEMA DEFINITIONS (Phase 99, D-13: dynamic verification) ---
EPISODES_SCHEMA <- c(
  "patient_id", "treatment_type", "episode_number",
  "episode_start", "episode_stop", "episode_length_days",
  "distinct_dates_in_episode", "historical_flag",
  "triggering_codes", "drug_names", "triggering_code_descriptions",
  "cancer_category", "is_hodgkin", "regimen_label", "is_first_line",
  "drug_group", "cause_of_death",
  "medication_name", "code_type", "source_table", "treatment_line", "sct_cross_use_flag"
)

DETAIL_SCHEMA <- c(
  "patient_id", "treatment_type", "treatment_date",
  "triggering_code", "drug_name", "episode_number",
  "episode_start", "episode_stop", "historical_flag",
  "triggering_code_description",
  "cancer_category", "is_hodgkin",
  "regimen_label", "is_first_line",
  "cause_of_death",
  "medication_name", "code_type", "source_table", "treatment_line", "sct_cross_use_flag"
)
```

**3. Rename output path variables (line 140-141, per D-05)**

Replace:
```r
OUTPUT_EPISODES_V2 <- file.path(CONFIG$output_dir, "gantt_episodes_v2.csv")
OUTPUT_DETAIL_V2 <- file.path(CONFIG$output_dir, "gantt_detail_v2.csv")
```
With:
```r
OUTPUT_EPISODES <- file.path(CONFIG$output_dir, "gantt_episodes.csv")
OUTPUT_DETAIL <- file.path(CONFIG$output_dir, "gantt_detail.csv")
```

Then find-and-replace all remaining occurrences of OUTPUT_EPISODES_V2 with OUTPUT_EPISODES and OUTPUT_DETAIL_V2 with OUTPUT_DETAIL throughout the file (lines 942-948 in the write section).

**4. Fix Death pseudo-treatment metadata (lines 441-479, per D-12)**

In the death_episodes mutate block (~line 441-468), change these values:
- `regimen_label = NA_character_` --> `regimen_label = ""`
- `is_first_line = FALSE` --> `is_first_line = NA`
- `drug_group = NA_character_` --> `drug_group = ""`
- `medication_name = NA_character_` --> `medication_name = ""`
- `code_type = NA_character_` --> `code_type = ""`
- `source_table = NA_character_` --> `source_table = ""`
- `treatment_line = NA_character_` --> `treatment_line = ""`
- `sct_cross_use_flag = NA_character_` --> `sct_cross_use_flag = ""`
- REMOVE the `is_sct_conditioning_context = NA` line (D-11)
- REMOVE the `immuno_confidence = NA_character_` line (D-11)
- Keep `is_hodgkin = FALSE` as-is (Death rows are not Hodgkin-specific)
- Keep `cause_of_death` as-is (already mapped from death_data)

In the death_episodes select() block (~line 470-479), update to match EPISODES_SCHEMA:
- Remove `encounter_ids` from select
- Remove `cancer_link_method` from select
- Remove `is_sct_conditioning_context` from select
- Remove `immuno_confidence` from select
- Ensure `is_hodgkin` is positioned after `cancer_category`
- The select should be: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, drug_names, triggering_code_descriptions, cancer_category, is_hodgkin, regimen_label, is_first_line, drug_group, cause_of_death, medication_name, code_type, source_table, treatment_line, sct_cross_use_flag

In the death_detail mutate block (~line 495-520), apply same metadata changes:
- `drug_name = NA_character_` --> `drug_name = ""`
- `regimen_label = NA_character_` --> `regimen_label = ""`
- `is_first_line = FALSE` --> `is_first_line = NA`
- `medication_name = NA_character_` --> `medication_name = ""`
- `code_type = NA_character_` --> `code_type = ""`
- `source_table = NA_character_` --> `source_table = ""`
- `treatment_line = NA_character_` --> `treatment_line = ""`
- `sct_cross_use_flag = NA_character_` --> `sct_cross_use_flag = ""`
- REMOVE `ENCOUNTERID = NA_character_` (D-06)
- REMOVE `is_sct_conditioning_context = NA` (D-11)
- REMOVE `immuno_confidence = NA_character_` (D-11)
- REMOVE `cancer_link_method` assignment
- Keep `is_hodgkin = FALSE`

In the death_detail select() block (~line 522-531), update to match DETAIL_SCHEMA:
- Remove ENCOUNTERID, is_hodgkin (wait -- keep is_hodgkin), cancer_link_method, is_sct_conditioning_context, immuno_confidence
- The select should be: patient_id, treatment_type, treatment_date, triggering_code, drug_name, episode_number, episode_start, episode_stop, historical_flag, triggering_code_description, cancer_category, is_hodgkin, regimen_label, is_first_line, cause_of_death, medication_name, code_type, source_table, treatment_line, sct_cross_use_flag

**5. Fix HL Diagnosis pseudo-treatment metadata (lines 579-669, per D-12)**

In the hl_dx_episodes mutate block (~line 579-606), change:
- `regimen_label = NA_character_` --> `regimen_label = ""`
- `is_first_line = FALSE` --> `is_first_line = NA`
- `drug_group = NA_character_` --> `drug_group = ""`
- `cause_of_death = NA_character_` --> `cause_of_death = ""`
- `medication_name = NA_character_` --> `medication_name = ""`
- `code_type = NA_character_` --> `code_type = ""`
- `source_table = NA_character_` --> `source_table = ""`
- `treatment_line = NA_character_` --> `treatment_line = ""`
- `sct_cross_use_flag = NA_character_` --> `sct_cross_use_flag = ""`
- REMOVE `is_sct_conditioning_context = NA` (D-11)
- REMOVE `immuno_confidence = NA_character_` (D-11)
- REMOVE `cancer_link_method = "none"` (dropped column)
- Keep `is_hodgkin = TRUE` (HL Diagnosis rows are Hodgkin by definition)
- REMOVE `encounter_ids = ""` (dropped column per D-06)

In the hl_dx_episodes select() block (~line 608-617), update to match EPISODES_SCHEMA (same 22 columns as death_episodes select).

In the hl_dx_detail mutate block (~line 633-658), apply same changes:
- `drug_name = NA_character_` --> `drug_name = ""`
- `regimen_label = NA_character_` --> `regimen_label = ""`
- `is_first_line = FALSE` --> `is_first_line = NA`
- `cause_of_death = NA_character_` --> `cause_of_death = ""`
- `medication_name = NA_character_` --> `medication_name = ""`
- `code_type = NA_character_` --> `code_type = ""`
- `source_table = NA_character_` --> `source_table = ""`
- `treatment_line = NA_character_` --> `treatment_line = ""`
- `sct_cross_use_flag = NA_character_` --> `sct_cross_use_flag = ""`
- REMOVE `ENCOUNTERID = NA_character_`
- REMOVE `is_sct_conditioning_context = NA`
- REMOVE `immuno_confidence = NA_character_`
- REMOVE `cancer_link_method = "none"`
- Keep `is_hodgkin = TRUE`

In the hl_dx_detail select() block (~line 660-668), update to match DETAIL_SCHEMA (same 20 columns as death_detail select).

**6. Add is_hodgkin derivation for treatment rows (before the final select at ~line 894, per D-07)**

Before the episodes_export select() at ~line 894, add a mutate to derive is_hodgkin:
```r
# Phase 99: Derive is_hodgkin from cancer_category (D-07)
episodes_export <- episodes_export %>%
  mutate(is_hodgkin = str_detect(cancer_category, "Hodgkin Lymphoma"))
```

Before the detail_export select() at ~line 908, add:
```r
detail_export <- detail_export %>%
  mutate(is_hodgkin = str_detect(cancer_category, "Hodgkin Lymphoma"))
```

NOTE: This must happen AFTER the cancer_category "Unlinked" fill (step 5 in the cleanup section, ~line 883-890) so that "Unlinked" rows get is_hodgkin=FALSE. The pseudo-treatment rows already have is_hodgkin set explicitly in their construction blocks (Death=FALSE, HL Diagnosis=TRUE), but the mutate will overwrite them. For HL Diagnosis rows, cancer_category is "Hodgkin Lymphoma" so str_detect will correctly produce TRUE. For Death rows, cancer_category is "" (empty) which gets filled to "Unlinked" by step 5, so str_detect will correctly produce FALSE.

**7. Update final select() statements (~lines 894 and 908, per D-06/D-07/D-11)**

Replace the episodes_export select() with (must match EPISODES_SCHEMA order exactly):
```r
episodes_export <- episodes_export %>%
  select(
    # Core identifiers
    patient_id, treatment_type, episode_number,
    # Episode boundaries
    episode_start, episode_stop, episode_length_days,
    distinct_dates_in_episode, historical_flag,
    # Code details (semicolon-separated per D-02)
    triggering_codes, drug_names, triggering_code_descriptions,
    # Cancer classification (D-07: is_hodgkin after cancer_category)
    cancer_category, is_hodgkin,
    # Clinical context (D-08)
    regimen_label, is_first_line,
    # Death/drug info (D-09)
    drug_group, cause_of_death,
    # Source metadata (D-10)
    medication_name, code_type, source_table, treatment_line, sct_cross_use_flag
  )
```

Replace the detail_export select() with (must match DETAIL_SCHEMA order exactly):
```r
detail_export <- detail_export %>%
  select(
    patient_id, treatment_type, treatment_date,
    triggering_code, drug_name, episode_number,
    episode_start, episode_stop, historical_flag,
    triggering_code_description,
    cancer_category, is_hodgkin,
    regimen_label, is_first_line,
    cause_of_death,
    medication_name, code_type, source_table, treatment_line, sct_cross_use_flag
  )
```

**8. Replace hardcoded column count verification (lines 925-933, per D-13)**

Replace the entire block at lines 924-935 with:
```r
# Step 7: Schema verification (Phase 99, D-13: dynamic schema validation)
if (!identical(colnames(episodes_export), EPISODES_SCHEMA)) {
  missing <- setdiff(EPISODES_SCHEMA, colnames(episodes_export))
  extra <- setdiff(colnames(episodes_export), EPISODES_SCHEMA)
  stop(glue("Episodes schema mismatch: missing=[{paste(missing, collapse=', ')}], extra=[{paste(extra, collapse=', ')}]"))
}
if (!identical(colnames(detail_export), DETAIL_SCHEMA)) {
  missing <- setdiff(DETAIL_SCHEMA, colnames(detail_export))
  extra <- setdiff(colnames(detail_export), DETAIL_SCHEMA)
  stop(glue("Detail schema mismatch: missing=[{paste(missing, collapse=', ')}], extra=[{paste(extra, collapse=', ')}]"))
}

message(glue("  Schema verification: PASSED ({length(EPISODES_SCHEMA)} episode cols, {length(DETAIL_SCHEMA)} detail cols)"))
```

**9. Update summary messages at end of file (lines 940-1007)**

- Replace "v2 Gantt Export" messages with "Gantt Export" (remove v2 references)
- Update the v1 vs v2 comparison block (~lines 986-989) to instead note "Phase 99 consolidated schema":
```r
message("\n  Phase 99 consolidated schema:")
message(glue("    Episodes: {length(EPISODES_SCHEMA)} columns (added is_hodgkin, removed encounter_ids/immunotherapy flags)"))
message(glue("    Detail: {length(DETAIL_SCHEMA)} columns (added is_hodgkin, removed ENCOUNTERID/immunotherapy flags)"))
message("    v1 export (R/51) deprecated — this script is the canonical Gantt export")
```

**10. Remove encounter_ids and ENCOUNTERID from treatment row construction earlier in the script**

The treatment rows also construct encounter_ids and ENCOUNTERID. These columns need to NOT be in the final select() (which we handled in step 7), but they can remain in intermediate data frames -- the select() will simply not include them. No changes needed to the main treatment row construction, only to the pseudo-treatment construction blocks (already handled in steps 4-5) and the final select() (handled in step 7).

Similarly, is_hodgkin and cancer_link_method exist in intermediate frames from earlier phases but are dropped by the new select(). The new is_hodgkin is derived fresh in step 6.

**11. Also remove the guard clause for is_sct_conditioning_context and immuno_confidence**

Search for any guard clauses that add is_sct_conditioning_context or immuno_confidence columns if they don't exist (these were added in Phase 93). These guard clauses can be removed since we no longer export these columns. They may look like:
```r
if (!"is_sct_conditioning_context" %in% names(episodes_export)) {
  episodes_export$is_sct_conditioning_context <- NA
}
```
Remove these guards entirely since the columns are no longer in the export schema.
  </action>
  <verify>
    <automated>
      powershell -Command "
        $f = Get-Content 'R/52_gantt_v2_export.R' -Raw;
        $checks = @(
          ($f -match 'EPISODES_SCHEMA\s*<-\s*c\('),
          ($f -match 'DETAIL_SCHEMA\s*<-\s*c\('),
          ($f -match 'OUTPUT_EPISODES\s*<-.*gantt_episodes\.csv'),
          ($f -match 'OUTPUT_DETAIL\s*<-.*gantt_detail\.csv'),
          ($f -notmatch 'OUTPUT_EPISODES_V2'),
          ($f -notmatch 'OUTPUT_DETAIL_V2'),
          ($f -match 'is_hodgkin = str_detect\(cancer_category'),
          ($f -match 'identical\(colnames\(episodes_export\), EPISODES_SCHEMA\)'),
          ($f -notmatch 'is_sct_conditioning_context' -or $f -match 'REMOVE'),
          ($f -match 'regimen_label = """"')
        );
        Write-Host ('EPISODES_SCHEMA defined: ' + $checks[0]);
        Write-Host ('DETAIL_SCHEMA defined: ' + $checks[1]);
        Write-Host ('OUTPUT_EPISODES renamed: ' + $checks[2]);
        Write-Host ('OUTPUT_DETAIL renamed: ' + $checks[3]);
        Write-Host ('No OUTPUT_EPISODES_V2: ' + $checks[4]);
        Write-Host ('No OUTPUT_DETAIL_V2: ' + $checks[5]);
        Write-Host ('is_hodgkin derivation: ' + $checks[6]);
        Write-Host ('Schema validation: ' + $checks[7]);
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - R/52_gantt_v2_export.R contains `EPISODES_SCHEMA <- c(` with exactly 22 column names
    - R/52_gantt_v2_export.R contains `DETAIL_SCHEMA <- c(` with exactly 20 column names
    - R/52_gantt_v2_export.R contains `OUTPUT_EPISODES <- file.path(CONFIG$output_dir, "gantt_episodes.csv")`
    - R/52_gantt_v2_export.R contains `OUTPUT_DETAIL <- file.path(CONFIG$output_dir, "gantt_detail.csv")`
    - R/52_gantt_v2_export.R does NOT contain `OUTPUT_EPISODES_V2` anywhere
    - R/52_gantt_v2_export.R does NOT contain `OUTPUT_DETAIL_V2` anywhere
    - R/52_gantt_v2_export.R contains `is_hodgkin = str_detect(cancer_category, "Hodgkin Lymphoma")` (derivation for treatment rows)
    - R/52_gantt_v2_export.R contains `identical(colnames(episodes_export), EPISODES_SCHEMA)` (dynamic verification)
    - R/52_gantt_v2_export.R does NOT contain `expected_ep_cols <- 23` (old hardcoded count removed)
    - R/52_gantt_v2_export.R does NOT contain `is_sct_conditioning_context` in any select() statement
    - R/52_gantt_v2_export.R does NOT contain `immuno_confidence` in any select() statement
    - Death pseudo-treatment block contains `regimen_label = ""` (not NA_character_)
    - Death pseudo-treatment block contains `drug_group = ""` (not NA_character_)
    - Death pseudo-treatment block contains `is_first_line = NA` (not FALSE)
    - HL Diagnosis pseudo-treatment block contains `regimen_label = ""` (not NA_character_)
    - HL Diagnosis pseudo-treatment block contains `is_hodgkin = TRUE`
    - Death pseudo-treatment block contains `is_hodgkin = FALSE`
    - EPISODES_SCHEMA contains "is_hodgkin" immediately after "cancer_category"
    - DETAIL_SCHEMA contains "is_hodgkin" immediately after "cancer_category"
  </acceptance_criteria>
  <done>R/52 produces 22-column episodes and 20-column detail CSVs with dynamic schema verification, is_hodgkin derivation, clean pseudo-treatment metadata, and gantt_*.csv output filenames (no _v2 suffix). No immunotherapy context columns in export.</done>
</task>

<task type="auto">
  <name>Task 2: Delete R/51 v1 Gantt export script</name>
  <files>R/51_gantt_data_export.R</files>
  <read_first>
    - R/51_gantt_data_export.R (confirm it is the v1 script before deletion)
  </read_first>
  <action>
Delete R/51_gantt_data_export.R from the codebase per D-01. Git history preserves the file if ever needed.

Use `git rm R/51_gantt_data_export.R` to both delete the file and stage the deletion.

Verify the file no longer exists with `ls R/51_gantt_data_export.R` (should fail / not found).
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path 'R/51_gantt_data_export.R') { Write-Host 'FAIL: R/51 still exists'; exit 1 } else { Write-Host 'PASS: R/51 deleted' }"</automated>
  </verify>
  <acceptance_criteria>
    - R/51_gantt_data_export.R does NOT exist in the working directory
    - `git status` shows R/51_gantt_data_export.R as deleted
  </acceptance_criteria>
  <done>R/51_gantt_data_export.R is removed from the codebase. v2 (R/52) is the sole canonical Gantt export.</done>
</task>

</tasks>

<verification>
- R/52_gantt_v2_export.R has EPISODES_SCHEMA with 22 entries and DETAIL_SCHEMA with 20 entries
- R/52 output paths use gantt_episodes.csv and gantt_detail.csv (no _v2)
- R/52 uses identical(colnames(), SCHEMA) instead of hardcoded column counts
- is_hodgkin derived via str_detect(cancer_category, "Hodgkin Lymphoma")
- Pseudo-treatment rows use "" for character enrichment columns, NA for logical is_first_line
- R/51_gantt_data_export.R no longer exists
- No references to is_sct_conditioning_context or immuno_confidence in any select() statement
</verification>

<success_criteria>
R/52 is the single canonical Gantt export with clean 22-column episodes / 20-column detail schema, dynamic schema verification, is_hodgkin column, and output files named gantt_episodes.csv / gantt_detail.csv. R/51 deleted. All pseudo-treatment metadata uses empty strings for character columns.
</success_criteria>

<output>
After completion, create `.planning/phases/99-fix-gantt-v2-vs-gantt-v1-disagreements-and-bugs-extraneous-columns-etc/99-01-SUMMARY.md`
</output>
