---
phase: 18-one-enrolled-person-does-not-have-an-hl-diagnosis-caught
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/utils_icd.R
  - R/09_dx_gap_analysis.R
autonomous: false
requirements:
  - INV-01
  - INV-02
  - INV-03

must_haves:
  truths:
    - "User can see the exact ICD codes for the one 'Neither' patient in gap analysis CSV output"
    - "Root cause is diagnosed as one of: (a) missing code in ICD_CODES, (b) DX_TYPE mismatch, (c) normalization bug, (d) histology outside range, (e) correctly excluded non-HL lymphoma"
    - "If fix applied: patient now appears in cohort with HL_VERIFIED=1 after pipeline rerun"
    - "If correctly excluded: exclusion is documented with specific code and rationale"
  artifacts:
    - path: "output/diagnostics/neither_lymphoma_codes.csv"
      provides: "Lymphoma/cancer ICD codes for the excluded Neither patient"
    - path: "output/diagnostics/neither_patient_summary.csv"
      provides: "Gap classification summary for the excluded Neither patient"
  key_links:
    - from: "R/00_config.R"
      to: "R/utils_icd.R"
      via: "ICD_CODES list consumed by is_hl_diagnosis()"
      pattern: "ICD_CODES\\$hl_icd10"
    - from: "R/utils_icd.R"
      to: "R/03_cohort_predicates.R"
      via: "is_hl_diagnosis() called in has_hodgkin_diagnosis()"
      pattern: "is_hl_diagnosis\\(DX, DX_TYPE\\)"
    - from: "R/03_cohort_predicates.R"
      to: "R/04_build_cohort.R"
      via: "HL_SOURCE map built inline using same logic"
      pattern: "HL_SOURCE.*Neither"
---

<objective>
Investigate and resolve why one enrolled patient is classified as "Neither" (no HL evidence) despite having lymphoma/cancer codes visible in gap analysis. Diagnose the root cause from the 5 possibilities (per D-03) and apply a targeted fix or document correct exclusion.

Purpose: Close the last remaining gap in HL identification so every enrolled patient with HL evidence is correctly captured in the cohort.
Output: Either updated ICD code list / normalization fix with validated pipeline rerun, or documented correct exclusion.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-one-enrolled-person-does-not-have-an-hl-diagnosis-caught/18-CONTEXT.md
@.planning/phases/18-one-enrolled-person-does-not-have-an-hl-diagnosis-caught/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/utils_icd.R:
```r
normalize_icd <- function(icd_code) {
  # Uppercase then remove dots
  toupper(str_remove_all(icd_code, "\\."))
}

is_hl_diagnosis <- function(icd_code, icd_type) {
  # Matches against ICD_CODES$hl_icd10 (when DX_TYPE=="10")
  # and ICD_CODES$hl_icd9 (when DX_TYPE=="09")
  # Both input and config codes normalized before comparison
}

is_hl_histology <- function(histology_code) {
  # Matches first 4 digits against ICD_CODES$hl_histology (9650-9667)
}
```

From R/00_config.R:
```r
ICD_CODES <- list(
  hl_icd10 = c(
    # 77 codes: C81.00-C81.99 (7 subtypes x 10 sites)
    # + 7 remission variants: C81.0A, C81.1A, C81.2A, C81.3A, C81.4A, C81.7A, C81.9A
  ),
  hl_icd9 = c(
    # 80 codes: 201.0-201.9 parent + 201.00-201.98 site-specific
  ),
  hl_histology = c(
    # 13 codes: 9650-9667
  )
)
```

From R/03_cohort_predicates.R:
```r
has_hodgkin_diagnosis <- function(patient_df) {
  # Source 1: DIAGNOSIS table via is_hl_diagnosis(DX, DX_TYPE)
  # Source 2: TUMOR_REGISTRY_ALL via is_hl_histology(HISTOLOGICAL_TYPE/MORPH)
  # Builds HL_SOURCE map: "Both", "DIAGNOSIS only", "TR only", "Neither"
  # Writes excluded "Neither" patients to excluded_no_hl_evidence.csv
  # Returns only patients with HL_SOURCE != "Neither"
}
```

From R/09_dx_gap_analysis.R:
```r
# Sections 1-7: Load excluded_no_hl_evidence.csv, explore DIAGNOSIS/ENROLLMENT/TR,
# classify gaps, output 3 CSVs to output/diagnostics/:
#   - neither_all_diagnoses.csv
#   - neither_lymphoma_codes.csv (key file for root cause diagnosis)
#   - neither_patient_summary.csv
```

From R/04_build_cohort.R:
```r
# Section 1 (Step 1): Builds hl_source_map inline (same logic as has_hodgkin_diagnosis)
# HL_VERIFIED = as.integer(HL_SOURCE != "Neither")
# Neither patients retained in cohort with HL_VERIFIED=0, NOT excluded
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run gap analysis on HiPerGator and share CSV output</name>
  <action>
    The user must execute the gap analysis pipeline on HiPerGator to generate the diagnostic CSVs for the "Neither" patient. Claude cannot access HiPerGator.

    Steps for the user:
    1. SSH into HiPerGator and open RStudio
    2. Run the full pipeline first (required input for gap analysis):
       ```r
       source("R/04_build_cohort.R")
       ```
    3. Verify the console output shows HL_VERIFIED=0 count (should be 1 "Neither" patient)
    4. Run the gap analysis script:
       ```r
       source("R/09_dx_gap_analysis.R")
       ```
    5. Check the console output for the gap classification summary
    6. Share the following files with Claude:
       - `output/diagnostics/neither_lymphoma_codes.csv` (most critical -- contains the patient's actual ICD codes)
       - `output/diagnostics/neither_patient_summary.csv` (gap classification)
       - The console output from step 4 (copy/paste the summary section)
  </action>
  <how-to-verify>
    User has shared neither_lymphoma_codes.csv content showing the patient's DX, DX_TYPE, and DX_normalized columns.
    User has shared neither_patient_summary.csv showing the gap_classification for the patient.
    User has shared the console output from 09_dx_gap_analysis.R.
  </how-to-verify>
  <resume-signal>Paste the CSV contents and console output, then type "ready for diagnosis"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Diagnose root cause and apply targeted fix</name>
  <read_first>
    - R/00_config.R (ICD_CODES$hl_icd10, ICD_CODES$hl_icd9, ICD_CODES$hl_histology -- lines 113-207)
    - R/utils_icd.R (normalize_icd() and is_hl_diagnosis() -- full file)
    - R/03_cohort_predicates.R (has_hodgkin_diagnosis() -- lines 56-143)
    - R/04_build_cohort.R (HL_SOURCE map -- lines 64-98)
    - R/09_dx_gap_analysis.R (lymphoma code filtering logic -- lines 86-123)
  </read_first>
  <files>R/00_config.R, R/utils_icd.R, R/09_dx_gap_analysis.R</files>
  <action>
    Examine the CSV data shared by the user in Task 1. Apply the root cause decision tree from D-03:

    **Decision Tree:**

    1. Extract the patient's DX code(s), DX_TYPE, and DX_normalized from neither_lymphoma_codes.csv
    2. For each code, determine:
       - Does the code start with C81 (ICD-10 HL) or 201 (ICD-9 HL)?
         - YES: Check if normalize_icd(code) appears in normalize_icd(ICD_CODES$hl_icd10) or normalize_icd(ICD_CODES$hl_icd9)
           - NOT IN LIST: **Root cause (a)** -- Missing code variant. Add the specific code to ICD_CODES$hl_icd10 or ICD_CODES$hl_icd9 in R/00_config.R. Add ONLY the specific missing code(s) per D-04. Add a comment: `# Phase 18: Added [code] found in gap analysis for 1 Neither patient`. Place after the existing C81.xA remission block (line 145) for ICD-10 or after the 201.9x block (line 183) for ICD-9.
           - IN LIST: Check DX_TYPE -- **Root cause (b)** DX_TYPE mismatch (e.g., ICD-10 code with DX_TYPE="09"). This would be a data quality issue, not a code fix. Document in 09_dx_gap_analysis.R.
         - NO (code is C82-C96 or 200/202-208): **Root cause (e)** -- Patient has non-HL lymphoma. Correctly excluded. Add a comment block at the end of 09_dx_gap_analysis.R (after Section 7, before the end comment) documenting:
           ```r
           # ==============================================================================
           # Phase 18 Investigation (2026-04-07)
           # ==============================================================================
           # Patient investigation: 1 enrolled patient classified as "Neither"
           # Finding: [describe specific codes found, e.g., "C83.30 (Diffuse large B-cell lymphoma)"]
           # Conclusion: [Correctly excluded -- non-HL lymphoma / OR code added to ICD_CODES]
           # Pipeline changes: [None needed / OR added C81.XX to ICD_CODES$hl_icd10 in 00_config.R]
           ```

    3. If no lymphoma codes at all in the CSV (patient has zero lymphoma/cancer diagnoses):
       - Check neither_patient_summary.csv for has_tr_record and tr_tables columns
       - If has_tr_record=TRUE: **Root cause (d)** -- TR histology code outside 9650-9667 range. Document the specific histology code found.
       - If has_tr_record=FALSE: Patient is a "phantom record" or "coding gap" -- correctly excluded, document in 09_dx_gap_analysis.R.

    4. If root cause is (a) -- missing code addition:
       - Edit R/00_config.R to add the specific code(s) to the appropriate ICD_CODES vector
       - Keep the existing code organization (grouped by subtype with comments)
       - Update the code count in the header comment if it changes (line 114: "149 Hodgkin Lymphoma diagnosis codes")
       - Do NOT add any codes beyond what appears in the gap analysis CSV per D-04

    5. If root cause is (c) -- normalization bug:
       - Edit R/utils_icd.R normalize_icd() function to handle the specific format issue
       - The function currently does: toupper(str_remove_all(icd_code, "\\."))
       - Possible additions: str_trim() for whitespace, handle lowercase, handle non-standard delimiters
       - Add a comment explaining what format was found and why the fix is needed

    6. Regardless of root cause, add the Phase 18 investigation documentation block to the end of R/09_dx_gap_analysis.R (before the final "End of" comment on line 377).
  </action>
  <verify>
    <automated>
    For root cause (a) -- code addition:
      grep "Phase 18" R/00_config.R confirms new code added with Phase 18 comment
      grep "Phase 18 Investigation" R/09_dx_gap_analysis.R confirms documentation block added

    For root cause (c) -- normalization fix:
      grep "Phase 18" R/utils_icd.R confirms fix with Phase 18 comment
      grep "Phase 18 Investigation" R/09_dx_gap_analysis.R confirms documentation block added

    For root cause (e) -- document-only:
      grep "Phase 18 Investigation" R/09_dx_gap_analysis.R confirms documentation block added
      No changes to R/00_config.R or R/utils_icd.R
    </automated>
  </verify>
  <acceptance_criteria>
    - If code fix: R/00_config.R contains the specific missing ICD code with "# Phase 18:" comment
    - If normalization fix: R/utils_icd.R normalize_icd() updated with "# Phase 18:" comment
    - If document-only: no changes to 00_config.R or utils_icd.R
    - R/09_dx_gap_analysis.R contains "Phase 18 Investigation" documentation block with patient codes, finding, conclusion, and pipeline changes noted
    - Documentation block includes the actual DX codes from the CSV (not placeholders)
  </acceptance_criteria>
  <done>Root cause diagnosed from one of 5 possibilities per D-03. Fix applied if warranted (code addition or normalization). Investigation documented in 09_dx_gap_analysis.R with specific patient codes and conclusion.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Validate fix via full pipeline rerun on HiPerGator (per D-07)</name>
  <action>
    Per D-07: If a code fix was applied in Task 2, the user must rerun the full pipeline on HiPerGator to validate the fix and confirm no regressions.

    Per D-08: If Task 2 concluded "correctly excluded" (no code changes), skip this task -- no pipeline rerun needed.

    Steps for the user (if code fix was applied):
    1. Pull the latest code changes to HiPerGator
    2. Run the full pipeline:
       ```r
       source("R/04_build_cohort.R")
       ```
    3. Check the console output for:
       - HL_SOURCE breakdown: expect +1 in "DIAGNOSIS only" or "Both", -1 in "Neither" compared to pre-fix
       - HL_VERIFIED=0 count: should be 0 (no more Neither patients) or still 1 if the fix was for a different root cause
       - Total cohort count: should be unchanged (Neither patients are retained with HL_VERIFIED=0, not excluded)
    4. Share the HL_SOURCE breakdown from the console output
    5. Optionally re-run `source("R/09_dx_gap_analysis.R")` to verify the patient no longer appears in excluded_no_hl_evidence.csv

    If Task 2 was document-only (D-08):
    - No pipeline rerun needed
    - Type "document-only, skipping validation" to proceed
  </action>
  <how-to-verify>
    If code fix: User shares console output showing updated HL_SOURCE breakdown with the expected change (+1 in DIAGNOSIS/Both/TR category, -1 in Neither).
    If document-only: User confirms no pipeline rerun needed per D-08.
  </how-to-verify>
  <resume-signal>Share the HL_SOURCE breakdown output, or type "document-only, skipping validation"</resume-signal>
</task>

</tasks>

<verification>
Phase 18 is complete when:
1. Root cause for the 1 "Neither" patient has been identified (one of 5 from D-03)
2. Appropriate action taken: code fix applied OR correct exclusion documented
3. If code fix: full pipeline rerun validates the change with no regressions (per D-07)
4. If document-only: gap analysis notes updated with findings (per D-08)
5. Investigation documented in R/09_dx_gap_analysis.R with actual patient codes
</verification>

<success_criteria>
- The root cause for the single "Neither" patient is diagnosed and documented
- If the patient has a missing HL code: the code is added to ICD_CODES in R/00_config.R and validated via pipeline rerun
- If the patient has non-HL lymphoma codes: the exclusion is confirmed correct and documented
- R/09_dx_gap_analysis.R contains a "Phase 18 Investigation" block with specific codes and conclusion
- No broad code list expansion (per D-04) -- only targeted fix
</success_criteria>

<output>
After completion, create `.planning/phases/18-one-enrolled-person-does-not-have-an-hl-diagnosis-caught/18-01-SUMMARY.md`
</output>
