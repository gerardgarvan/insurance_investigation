---
phase: 55-cancer-summary-refinement-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/55_cancer_summary_refined.R
autonomous: false
requirements:
  - CREF-01
  - CREF-02
  - CREF-03

must_haves:
  truths:
    - "Cancer summary CSV and xlsx contain only C-code rows (no D-codes)"
    - "Only patients with 2+ C81 DIAGNOSIS codes at least 7 days apart are included in outputs"
    - "Each confirmed HL patient has a first_hl_dx_date computed as the true minimum across DIAGNOSIS and TUMOR_REGISTRY"
    - "Each confirmed HL patient has a first_hl_dx_source column indicating DIAGNOSIS, TUMOR_REGISTRY, or Both"
    - "cancer_summary_table.xlsx Category Summary sheet shows Hodgkin Lymphoma at 100% confirmation rate (7-day)"
    - "confirmed_hl_cohort.rds exists with columns ID, first_hl_dx_date, first_hl_dx_source"
  artifacts:
    - path: "R/55_cancer_summary_refined.R"
      provides: "Self-contained script: load CSV, remove D-codes, confirm cohort, compute first_hl_dx_date, aggregate, write styled xlsx"
      contains: "filter(!str_detect(cancer_code"
    - path: "output/tables/cancer_summary.csv"
      provides: "Patient-code level cancer summary (C-codes only, confirmed HL cohort)"
    - path: "output/tables/cancer_summary.xlsx"
      provides: "Single-sheet patient-code level output"
    - path: "output/tables/cancer_summary_table.xlsx"
      provides: "Two-sheet styled workbook (Category Summary + Code Summary)"
    - path: "output/confirmed_hl_cohort.rds"
      provides: "Downstream artifact for Phase 56 and Phase 57"
  key_links:
    - from: "R/55_cancer_summary_refined.R"
      to: "output/tables/cancer_summary.csv"
      via: "read.csv() loads R/53 output, then write.csv() overwrites after filtering"
      pattern: "read\\.csv.*cancer_summary\\.csv"
    - from: "R/55_cancer_summary_refined.R"
      to: "DIAGNOSIS DuckDB table"
      via: "get_pcornet_table('DIAGNOSIS') for C81 cohort confirmation and record counts"
      pattern: "get_pcornet_table.*DIAGNOSIS"
    - from: "R/55_cancer_summary_refined.R"
      to: "TUMOR_REGISTRY DuckDB table"
      via: "get_pcornet_table('TUMOR_REGISTRY_ALL') for first HL diagnosis date"
      pattern: "TUMOR_REGISTRY_ALL"
    - from: "R/55_cancer_summary_refined.R"
      to: "output/confirmed_hl_cohort.rds"
      via: "saveRDS() with ID, first_hl_dx_date, first_hl_dx_source columns"
      pattern: "saveRDS.*confirmed_hl_cohort"
---

<objective>
Create R/55_cancer_summary_refined.R: a self-contained script that removes benign D-codes from the cancer summary, filters to a validated HL cohort (2+ C81 diagnosis codes 7+ days apart), computes first HL diagnosis date from both DIAGNOSIS and TUMOR_REGISTRY sources, and regenerates all cancer summary outputs.

Purpose: Phase 55 establishes the refined cancer summary foundation that Phase 56 (temporal filtering) and Phase 57 (Gantt enhancements) depend on. The confirmed_hl_cohort.rds artifact is the bridge to those downstream phases.

Output: R/55_cancer_summary_refined.R script + overwritten cancer_summary.csv, cancer_summary.xlsx, cancer_summary_table.xlsx + new confirmed_hl_cohort.rds
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-cancer-summary-refinement-foundation/55-CONTEXT.md
@.planning/phases/55-cancer-summary-refinement-foundation/55-RESEARCH.md

@R/53_cancer_summary.R
@R/54_cancer_summary_table.R
@R/02_harmonize_payer.R
@R/51_cancer_site_confirmation_7day.R
@R/00_config.R
@R/01_load_pcornet.R

<interfaces>
<!-- Key patterns and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From R/53_cancer_summary.R (Section 2, lines 59-328):
```r
# PREFIX_MAP: named vector, 309 entries mapping 3-character ICD-10 prefixes to 53 cancer site categories
# Includes C00-C96 (malignant), C42 (ICD-O-3), D00-D49 (benign/in situ/uncertain)
# R/55 MUST copy the full PREFIX_MAP for script independence (per D-01, Claude's discretion)

classify_codes <- function(codes) {
  prefix3 <- substr(codes, 1, 3)
  categories <- unname(PREFIX_MAP[prefix3])
  categories
}
```

From R/53_cancer_summary.R (Section 5, output columns):
```r
# cancer_summary.csv columns (7 columns, exact order):
#   ID, cancer_code, description,
#   two_or_more_unique_dates, two_or_more_unique_dates_gt_7,
#   unique_dates_total, unique_dates_with_sep_gt_7
```

From R/54_cancer_summary_table.R (Section 5-6, aggregation logic):
```r
# Category-level: group_by(category) %>% summarise(total_patients, confirmed_2date, pct_confirmed_2date,
#   confirmed_7day, pct_confirmed_7day, mean_unique_dates, median_unique_dates,
#   mean_dates_7day_sep, median_dates_7day_sep, total_records)
# Code-level: group_by(cancer_code, category) with same metrics
# Both sorted desc(total_patients)
```

From R/54_cancer_summary_table.R (Section 8, styling constants):
```r
DARK_HEADER_FILL <- "FF374151"
WHITE_FONT       <- "FFFFFFFF"
TITLE_FONT_COLOR <- "FF1F2937"
TOTALS_FILL      <- "FFE5E7EB"
# Sheet 1 "Category Summary": 11 columns (A-K), title row 1, headers row 2, data from row 3
# Sheet 2 "Code Summary": 12 columns (A-L), same layout
# Freeze pane at row 3, number formatting: "#,##0" counts, "0.0%" rates, "0.0" decimals
```

From R/51_cancer_site_confirmation_7day.R (lines 425-434, 7-day gap pattern):
```r
# Cohort confirmation pattern:
confirmed_exact <- dx_cancer %>%
  filter(!is.na(DX_DATE)) %>%
  distinct(ID, DX_norm, DX_DATE, category) %>%   # Deduplicate per Pitfall 3
  group_by(ID, DX_norm) %>%
  filter(as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7) %>%
  ungroup()
# Phase 55 applies this at C81 PREFIX level (any C81.xx), not per exact code
```

From R/02_harmonize_payer.R (lines 191-226, first HL dx date pattern):
```r
# Phase 55 REPLACES R/02's TR-preferred logic with TRUE minimum:
# R/02 uses: if_else(!is.na(first_dx_date_tr), first_dx_date_tr, first_dx_date_diagnosis)
# R/55 MUST use: pmin(first_dx_date_diagnosis, first_dx_date_tr, na.rm = TRUE)
# Plus source attribution via case_when() and 1900 sentinel nullification
```

From R/01_load_pcornet.R (DuckDB access):
```r
# source("R/01_load_pcornet.R") opens DuckDB connection
# get_pcornet_table("DIAGNOSIS") returns lazy DuckDB table
# get_pcornet_table("TUMOR_REGISTRY_ALL") returns consolidated TR table
# close_pcornet_con() closes connection at end of script
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/55_cancer_summary_refined.R</name>
  <files>R/55_cancer_summary_refined.R</files>
  <read_first>
    R/54_cancer_summary_table.R
    R/53_cancer_summary.R
    R/02_harmonize_payer.R
    R/51_cancer_site_confirmation_7day.R
    R/00_config.R
    R/01_load_pcornet.R
    .planning/phases/55-cancer-summary-refinement-foundation/55-CONTEXT.md
    .planning/phases/55-cancer-summary-refinement-foundation/55-RESEARCH.md
  </read_first>
  <action>
Create R/55_cancer_summary_refined.R as a single self-contained script with these 12 sections (per D-01, D-02):

**SECTION 1: SETUP**
- suppressPackageStartupMessages for dplyr, stringr, glue, openxlsx2, lubridate
- source("R/00_config.R") and source("R/01_load_pcornet.R")
- Define output paths: OUTPUT_CSV = file.path(CONFIG$output_dir, "tables", "cancer_summary.csv"), OUTPUT_XLSX = file.path(CONFIG$output_dir, "tables", "cancer_summary.xlsx"), OUTPUT_TABLE_XLSX = file.path(CONFIG$output_dir, "tables", "cancer_summary_table.xlsx"), OUTPUT_RDS = file.path(CONFIG$output_dir, "confirmed_hl_cohort.rds")
- Banner: message("=== Phase 8: Cancer Summary Refined (D-code removal + HL cohort confirmation) ===")

**SECTION 2: PREFIX_MAP AND classify_codes()**
- Copy the FULL PREFIX_MAP from R/54_cancer_summary_table.R (lines 51-311) verbatim -- all 309 entries including D-codes (classify_codes() still needs them for the input data before filtering). Per Claude's discretion: copy for script independence, matching R/53 and R/54 pattern.
- Copy classify_codes() function exactly as in R/53 lines 324-328.

**SECTION 3: LOAD AND FILTER INPUT DATA (CREF-01)**
- Read R/53's cancer_summary.csv: `cancer_summary <- read.csv(INPUT_CSV, stringsAsFactors = FALSE)`
- Log loaded count: `message(glue("  Loaded {format(nrow(cancer_summary), big.mark=',')} patient-code rows"))`
- Remove D-codes per CREF-01: `cancer_summary <- cancer_summary %>% filter(!str_detect(cancer_code, "^D"))`
- Log attrition: `message(glue("Removed {format(n_removed, big.mark=',')} D-code rows ({format(nrow(cancer_summary), big.mark=',')} remaining)"))`
- Log unique patients and codes after D-code removal.

**SECTION 4: COHORT CONFIRMATION -- C81 codes, 7-day gap (CREF-02)**
- Per D-06, D-07, D-08: Query DIAGNOSIS directly for C81 codes.
- `dx_c81 <- get_pcornet_table("DIAGNOSIS") %>% filter(DX_TYPE == "10") %>% mutate(DX_norm = toupper(str_remove_all(DX, "\\\\."))) %>% filter(str_detect(DX_norm, "^C81")) %>% select(ID, DX_norm, DX_DATE) %>% collect()`
- Per Pitfall 1 from RESEARCH.md: Deduplicate before date span: `distinct(ID, DX_norm, DX_DATE)`
- Per D-07: Group by ID only (not per sub-code -- any C81.xx counts toward threshold): `confirmed_patients <- dx_c81 %>% filter(!is.na(DX_DATE)) %>% distinct(ID, DX_DATE) %>% group_by(ID) %>% filter(as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7) %>% ungroup() %>% distinct(ID)`
- Log confirmed cohort size: `message(glue("Confirmed HL cohort: {format(nrow(confirmed_patients), big.mark=',')} patients (2+ C81 codes, 7-day gap)"))`

**SECTION 5: COMPUTE FIRST HL DIAGNOSIS DATE (CREF-03)**
- Per D-03, D-04: Compute true minimum across DIAGNOSIS and TUMOR_REGISTRY for ALL patients with ANY C81 code (not just confirmed patients).
- DIAGNOSIS earliest C81 date per patient:
  ```r
  dx_dates <- dx_c81 %>%
    filter(!is.na(DX_DATE)) %>%
    group_by(ID) %>%
    summarise(first_dx_date_diagnosis = min(DX_DATE, na.rm = TRUE), .groups = "drop")
  ```
  (Reuse dx_c81 from Section 4 -- already has all C81 DIAGNOSIS rows.)
- TUMOR_REGISTRY earliest DATE_OF_DIAGNOSIS per patient:
  ```r
  tr_tbl <- tryCatch(get_pcornet_table("TUMOR_REGISTRY_ALL"), error = function(e) NULL)
  if (!is.null(tr_tbl) && "DATE_OF_DIAGNOSIS" %in% colnames(tr_tbl)) {
    tr_dates <- tr_tbl %>%
      filter(!is.na(DATE_OF_DIAGNOSIS)) %>%
      group_by(ID) %>%
      summarise(first_dx_date_tr = min(DATE_OF_DIAGNOSIS, na.rm = TRUE), .groups = "drop")
  } else {
    tr_dates <- data.frame(ID = character(), first_dx_date_tr = as.Date(character()), stringsAsFactors = FALSE)
  }
  ```
- Compute TRUE minimum with source attribution per D-05:
  ```r
  first_dx <- dx_dates %>%
    full_join(tr_dates, by = "ID") %>%
    mutate(
      first_hl_dx_date = pmin(first_dx_date_diagnosis, first_dx_date_tr, na.rm = TRUE),
      first_hl_dx_source = case_when(
        is.na(first_dx_date_diagnosis) & !is.na(first_dx_date_tr) ~ "TUMOR_REGISTRY",
        !is.na(first_dx_date_diagnosis) & is.na(first_dx_date_tr) ~ "DIAGNOSIS",
        !is.na(first_dx_date_diagnosis) & !is.na(first_dx_date_tr) &
          first_dx_date_diagnosis == first_dx_date_tr ~ "Both",
        !is.na(first_dx_date_diagnosis) & !is.na(first_dx_date_tr) &
          first_dx_date_diagnosis < first_dx_date_tr ~ "DIAGNOSIS",
        !is.na(first_dx_date_diagnosis) & !is.na(first_dx_date_tr) &
          first_dx_date_tr < first_dx_date_diagnosis ~ "TUMOR_REGISTRY",
        TRUE ~ NA_character_
      )
    ) %>%
    select(ID, first_hl_dx_date, first_hl_dx_source)
  ```
- Nullify 1900 sentinel dates per Pitfall 3:
  ```r
  n_sentinel <- sum(year(first_dx$first_hl_dx_date) == 1900L, na.rm = TRUE)
  if (n_sentinel > 0) {
    message(glue("  Nullifying {n_sentinel} sentinel first-diagnosis dates (year 1900)"))
    first_dx <- first_dx %>%
      mutate(first_hl_dx_date = if_else(year(first_hl_dx_date) == 1900L, as.Date(NA), first_hl_dx_date))
  }
  ```
- Log source distribution: count of DIAGNOSIS, TUMOR_REGISTRY, Both per first_hl_dx_source.

**SECTION 6: SAVE CONFIRMED HL COHORT RDS (D-10)**
- Inner join confirmed_patients with first_dx on ID to get only confirmed patients with their dates:
  ```r
  confirmed_hl_cohort <- confirmed_patients %>%
    inner_join(first_dx, by = "ID") %>%
    select(ID, first_hl_dx_date, first_hl_dx_source)
  saveRDS(confirmed_hl_cohort, OUTPUT_RDS)
  message(glue("Saved confirmed HL cohort to {OUTPUT_RDS} ({nrow(confirmed_hl_cohort)} patients)"))
  ```

**SECTION 7: FILTER CANCER SUMMARY TO CONFIRMED COHORT**
- Inner join cancer_summary (D-code-free from Section 3) with confirmed_patients on ID.
- Log attrition: patients before vs after, rows before vs after.
- Add category column via classify_codes().

**SECTION 8: QUERY RECORD COUNTS FROM DIAGNOSIS (DuckDB)**
- Replicate R/54 Section 4 pattern: query DIAGNOSIS for DX_TYPE == "10", normalize, filter C-codes only (no D-codes), group by DX_norm, count records.
- Left join record_count onto cancer_summary by cancer_code = DX_norm.

**SECTION 9: CATEGORY-LEVEL AGGREGATION**
- Replicate R/54 Section 5 verbatim: group_by(category) with the 10 summary metrics (total_patients, confirmed_2date, pct_confirmed_2date, confirmed_7day, pct_confirmed_7day, mean_unique_dates, median_unique_dates, mean_dates_7day_sep, median_dates_7day_sep, total_records).
- Build totals_category tibble per R/54 Section 7 pattern.

**SECTION 10: CODE-LEVEL AGGREGATION**
- Replicate R/54 Section 6 verbatim: group_by(cancer_code, category) with same 10 metrics.
- Build totals_code tibble per R/54 Section 7 pattern.

**SECTION 11: WRITE OUTPUTS**
- R/55 consolidates R/53 (patient-code level) and R/54 (summary table) output generation into a single script per D-02. All three files below are written in one pass, eliminating the need to re-run R/54 after R/55 executes. This is the key architectural decision: R/55 is the single source of truth for all cancer summary outputs going forward.
- Write cancer_summary.csv: `write.csv(cancer_summary_output, OUTPUT_CSV, row.names = FALSE)` -- use the 7-column format (ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7) matching R/53 output schema. The cancer_summary data frame at this point has the category column from classify_codes() and record_count from the DuckDB join -- select only the original 7 columns for CSV output.
- Write cancer_summary.xlsx: Replicate R/53 Section 7 pattern (single flat sheet "Cancer Summary", col_names = TRUE, integer numfmt on metric columns, auto widths, freeze top row).
- Write cancer_summary_table.xlsx: Replicate R/54 Section 8 exactly (two sheets "Category Summary" + "Code Summary" with dark header, white font, freeze pane, number formatting, totals row with gray fill, column widths). Use the exact same styling constants: DARK_HEADER_FILL = "FF374151", WHITE_FONT = "FFFFFFFF", TITLE_FONT_COLOR = "FF1F2937", TOTALS_FILL = "FFE5E7EB". Same headers as R/54 lines 491-502 for Sheet 1 and lines 582-595 for Sheet 2. Same column widths as R/54 lines 564-565 and 656-657.
- Per D-09: These overwrite the original R/53 and R/54 output files.

**SECTION 12: CLEANUP**
- close_pcornet_con()
- message("\n=== Phase 8 complete ===")

**Critical implementation notes:**
- Per Pitfall 4: Compute first_hl_dx_date for ALL patients with ANY C81 code BEFORE filtering to confirmed cohort. The date computation (Section 5) is independent of confirmation logic (Section 4).
- Per Pitfall 5: Log attrition at every filtering step with before/after counts.
- Per D-07: For cohort confirmation, group by ID only -- different C81 sub-codes (e.g., C81.10 and C81.90) count toward the 2+ threshold together. Use `distinct(ID, DX_DATE)` before the 7-day gap check (not `distinct(ID, DX_norm, DX_DATE)`) so that different codes on the same date are only counted once.
- Do NOT use data.table syntax (project anti-pattern).
- Do NOT use setwd() (project anti-pattern).
  </action>
  <verify>
    <automated>grep -c "filter(!str_detect(cancer_code" R/55_cancer_summary_refined.R && grep -c "pmin(first_dx_date_diagnosis" R/55_cancer_summary_refined.R && grep -c "saveRDS(confirmed_hl_cohort" R/55_cancer_summary_refined.R && grep -c "first_hl_dx_source" R/55_cancer_summary_refined.R && grep -c "DARK_HEADER_FILL" R/55_cancer_summary_refined.R && grep -c "close_pcornet_con" R/55_cancer_summary_refined.R</automated>
  </verify>
  <acceptance_criteria>
    - R/55_cancer_summary_refined.R exists and contains `filter(!str_detect(cancer_code, "^D"))` for D-code removal (CREF-01)
    - R/55_cancer_summary_refined.R contains `as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7` for 7-day gap confirmation (CREF-02)
    - R/55_cancer_summary_refined.R contains `pmin(first_dx_date_diagnosis, first_dx_date_tr, na.rm = TRUE)` for true minimum date (CREF-03, D-03)
    - R/55_cancer_summary_refined.R contains `first_hl_dx_source` column with case_when for DIAGNOSIS/TUMOR_REGISTRY/Both attribution (D-05)
    - R/55_cancer_summary_refined.R contains `saveRDS(confirmed_hl_cohort` for downstream RDS artifact (D-10)
    - R/55_cancer_summary_refined.R contains `year(first_hl_dx_date) == 1900L` for sentinel date nullification
    - R/55_cancer_summary_refined.R contains `distinct(ID, DX_DATE)` before group_by(ID) for cohort confirmation deduplication (Pitfall 1)
    - R/55_cancer_summary_refined.R contains `DARK_HEADER_FILL <- "FF374151"` for xlsx styling (matching R/54 pattern)
    - R/55_cancer_summary_refined.R contains full PREFIX_MAP with at least 250 entries (grep for `"C81" = "Hodgkin Lymphoma"`)
    - R/55_cancer_summary_refined.R contains `close_pcornet_con()` at end
    - R/55_cancer_summary_refined.R sources R/00_config.R and R/01_load_pcornet.R (contains `source("R/00_config.R")` and `source("R/01_load_pcornet.R")`)
    - R/55_cancer_summary_refined.R writes 3 output files: cancer_summary.csv, cancer_summary.xlsx, cancer_summary_table.xlsx (per D-09)
    - R/55_cancer_summary_refined.R does NOT contain `data.table` or `setwd` (anti-patterns)
    - R/55_cancer_summary_refined.R does NOT contain `if_else(!is.na(first_dx_date_tr), first_dx_date_tr` (the old TR-preferred pattern from R/02 -- must use pmin instead per Pitfall 2)
  </acceptance_criteria>
  <done>R/55_cancer_summary_refined.R is a complete, self-contained script that when run on HiPerGator will: (1) load R/53 CSV and remove all D-code rows, (2) query DIAGNOSIS for C81 codes and filter to patients with 2+ codes 7+ days apart, (3) compute first_hl_dx_date as true minimum across DIAGNOSIS and TUMOR_REGISTRY with source attribution, (4) filter cancer summary to confirmed cohort, (5) aggregate to category and code level summaries with record counts, (6) write cancer_summary.csv, cancer_summary.xlsx, and styled two-sheet cancer_summary_table.xlsx, (7) save confirmed_hl_cohort.rds for downstream phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify R/55 script execution on HiPerGator</name>
  <files>output/tables/cancer_summary_table.xlsx, output/tables/cancer_summary.csv, output/confirmed_hl_cohort.rds</files>
  <read_first>
    R/55_cancer_summary_refined.R
  </read_first>
  <action>
User runs R/55_cancer_summary_refined.R on HiPerGator and verifies outputs. This is a human-verify checkpoint -- Claude does not execute this task, the user does.

Steps for user:
1. On HiPerGator RStudio, run: source("R/55_cancer_summary_refined.R")
2. Check console output for D-code removal count, confirmed HL cohort size, first_hl_dx_source distribution, no errors
3. Open output/tables/cancer_summary_table.xlsx and verify Hodgkin Lymphoma = 100% in Rate (7-Day Gap), no D-code categories
4. Verify output/confirmed_hl_cohort.rds has 3 columns: ID, first_hl_dx_date, first_hl_dx_source
5. Verify output/tables/cancer_summary.csv has no D-prefix cancer_code values
  </action>
  <verify>
    <automated>echo "Checkpoint: human verification required on HiPerGator"</automated>
  </verify>
  <acceptance_criteria>
    - Console output shows D-code rows removed (count > 0)
    - Console output shows confirmed HL cohort size
    - Console output shows first_hl_dx_source distribution with counts per source category
    - cancer_summary_table.xlsx "Category Summary" sheet has Hodgkin Lymphoma at 100% in Rate (7-Day Gap) column
    - cancer_summary_table.xlsx has NO rows for In Situ Neoplasms, Benign Neoplasms, Uncertain Behavior Neoplasms, MDS / Myeloproliferative, or Unspecified Behavior Neoplasms
    - cancer_summary_table.xlsx has dark header styling, freeze pane, totals row with gray fill
    - confirmed_hl_cohort.rds exists with str() showing 3 columns: ID (chr), first_hl_dx_date (Date), first_hl_dx_source (chr)
    - cancer_summary.csv contains zero rows where cancer_code starts with "D"
  </acceptance_criteria>
  <done>User has verified that R/55 runs successfully on HiPerGator, all four output files are correct, Hodgkin Lymphoma reaches 100% confirmation rate, D-codes are absent, and confirmed_hl_cohort.rds is ready for Phase 56 and Phase 57 consumption.</done>
</task>

</tasks>

<verification>
- R/55_cancer_summary_refined.R file exists with all 12 sections
- Script contains D-code filter (CREF-01), 7-day gap cohort confirmation (CREF-02), true minimum first_hl_dx_date with source attribution (CREF-03)
- Script contains confirmed_hl_cohort.rds output (D-10)
- Script uses pmin() not if_else() for date computation (Pitfall 2 avoidance)
- Script deduplicates before date span calculation (Pitfall 1 avoidance)
- Script nullifies 1900 sentinel dates (Pitfall 3 avoidance)
- Script computes first_hl_dx_date for ALL C81 patients before filtering to confirmed cohort (Pitfall 4 avoidance)
- Script logs attrition at every filtering step (Pitfall 5 avoidance)
- Checkpoint confirms Hodgkin Lymphoma reaches 100% in category summary
</verification>

<success_criteria>
1. R/55_cancer_summary_refined.R creates cancer_summary.csv with only C-code rows (CREF-01)
2. Output cohort is filtered to patients with 2+ C81 codes 7+ days apart (CREF-02)
3. confirmed_hl_cohort.rds contains ID, first_hl_dx_date (true minimum), first_hl_dx_source (CREF-03, D-03, D-05)
4. cancer_summary_table.xlsx Category Summary shows 100% HL confirmation rate (CREF-02 success criterion)
5. All D-code categories absent from output (CREF-01)
6. Human verifies correct execution on HiPerGator
</success_criteria>

<output>
After completion, create `.planning/phases/55-cancer-summary-refinement-foundation/55-01-SUMMARY.md`
</output>