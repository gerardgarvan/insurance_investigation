---
phase: 113-investigate-encounters-after-death-date
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/51_post_death_encounter_investigation.R
  - R/88_smoke_test_comprehensive.R
  - R/39_run_all_investigations.R
autonomous: true
requirements:
  - POSTDEATH-01
  - POSTDEATH-02
  - POSTDEATH-03

must_haves:
  truths:
    - "User can run R/51 and see a two-sheet xlsx with per-patient summary and per-event detail for ~200 patients with post-death clinical activity"
    - "User can see how many days after death each encounter/diagnosis/treatment occurred, bucketed into 0-30, 31-90, 91-365, and >1 year ranges"
    - "User can filter the detail sheet by source_table (ENCOUNTER, DIAGNOSIS, TREATMENT) to isolate specific activity types"
    - "R/88 smoke test validates R/51 structural integrity and passes"
    - "R/39 pipeline runner includes R/51 in the investigation scripts stage"
  artifacts:
    - path: "R/51_post_death_encounter_investigation.R"
      provides: "Post-death encounter drill-down investigation"
      min_lines: 200
    - path: "output/post_death_encounter_investigation.xlsx"
      provides: "Two-sheet xlsx with Patient Summary and Event Detail sheets"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Phase 113 smoke test section"
      contains: "Phase 113"
    - path: "R/39_run_all_investigations.R"
      provides: "Pipeline runner entry for R/51"
      contains: "51_post_death_encounter_investigation"
  key_links:
    - from: "R/51_post_death_encounter_investigation.R"
      to: "cache/outputs/validated_death_dates.rds"
      via: "readRDS with death_valid == TRUE and post_death_activity == TRUE filter"
      pattern: "filter.*death_valid.*TRUE.*post_death_activity.*TRUE"
    - from: "R/51_post_death_encounter_investigation.R"
      to: "DuckDB ENCOUNTER/DIAGNOSIS tables"
      via: "get_pcornet_table + inner_join with valid_deaths"
      pattern: "get_pcornet_table.*ENCOUNTER|get_pcornet_table.*DIAGNOSIS"
    - from: "R/51_post_death_encounter_investigation.R"
      to: "cache/outputs/treatment_episodes.rds"
      via: "readRDS with inner_join for post-death treatment episodes"
      pattern: "readRDS.*treatment_episodes"
    - from: "R/51_post_death_encounter_investigation.R"
      to: "output/post_death_encounter_investigation.xlsx"
      via: "openxlsx2 wb_save with two sheets"
      pattern: "wb_save.*OUTPUT_XLSX"
---

<objective>
Create R/51 investigation script that drills into the ~200 patients flagged with post-death clinical activity, quantifying temporal gaps (days after death) for every encounter, diagnosis, and treatment event. Produces a meeting-ready two-sheet xlsx with per-patient summary (bucket distribution, min/max/median gaps) and per-event detail (raw days, source table labels).

Purpose: Data quality investigation answering "how far after the death date do the ~200 patients' encounters occur?" -- distinguishing claims lag artifacts (0-30 days) from truly anomalous records (>1 year).

Output: R/51_post_death_encounter_investigation.R script, output/post_death_encounter_investigation.xlsx, R/88 smoke test section, R/39 pipeline runner entry.
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-investigate-encounters-after-death-date-quantify-how-far-after-death-the-200-patients-encounters-occur/113-CONTEXT.md
@.planning/phases/113-investigate-encounters-after-death-date-quantify-how-far-after-death-the-200-patients-encounters-occur/113-RESEARCH.md

<interfaces>
<!-- Key types, patterns, and contracts from codebase that executor needs. -->

From R/53_death_date_validation.R (Section 5, lines 198-254):
- validated_death_dates.rds columns: ID, DEATH_DATE, DEATH_SOURCE, death_valid, validation_reason, post_death_activity (boolean), post_death_encounters (int), post_death_diagnoses (int), post_death_treatments (int), latest_post_death_encounter (date), latest_post_death_diagnosis (date), latest_post_death_treatment (date)
- Filter pattern: `filter(death_valid == TRUE, post_death_activity == TRUE)` yields ~200 patients
- Post-death encounter query: `get_pcornet_table("ENCOUNTER") %>% select(ID, ADMIT_DATE, ENCOUNTERID) %>% collect() %>% mutate(ADMIT_DATE = parse_pcornet_date(ADMIT_DATE)) %>% filter(!is.na(ADMIT_DATE)) %>% inner_join(valid_deaths %>% select(ID, DEATH_DATE), by = "ID") %>% filter(ADMIT_DATE > DEATH_DATE)`
- Post-death diagnosis query: `get_pcornet_table("DIAGNOSIS") %>% select(ID, DX_DATE, DIAGNOSISID) %>% collect() %>% mutate(DX_DATE = parse_pcornet_date(DX_DATE)) %>% filter(!is.na(DX_DATE)) %>% inner_join(valid_deaths %>% select(ID, DEATH_DATE), by = "ID") %>% filter(DX_DATE > DEATH_DATE)`
- Treatment episodes join key: `by = c("patient_id" = "ID")` then `filter(episode_start > DEATH_DATE)`

From R/59_death_date_summary.R (xlsx styling pattern, lines 187-250):
- wb <- wb_workbook()
- Title: size 16, bold, color FF1F2937
- Subtitle: size 10, color FF6B7280
- Headers: fill FF374151, font white FFFFFFFF, bold, Calibri 11
- Number format: "#,##0"
- Data starts at row 4, freeze pane at row 5

From R/39_run_all_investigations.R (investigation block, lines 176-192):
- Investigation scripts listed in c() vector in SECTION 3
- Pattern: `"R/59_death_date_summary.R",  # G15: Death date cross-tab`
- New script should be added after R/59 entry

From R/88_smoke_test_comprehensive.R:
- Section numbering pattern: SECTION 15h is latest (Phase 112)
- check() function: `check("description", condition_boolean)`
- readLines + grepl pattern for structural checks
- Coverage listing at end of file (lines 3180-3228)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/51 post-death encounter investigation script</name>
  <files>R/51_post_death_encounter_investigation.R</files>
  <read_first>
    R/59_death_date_summary.R
    R/53_death_date_validation.R
    R/00_config.R
    R/utils/utils_duckdb.R
    R/utils/utils_dates.R
    R/utils/utils_assertions.R
  </read_first>
  <action>
Create R/51_post_death_encounter_investigation.R as a standalone investigation script following the R/59 pattern exactly. Script number R/51 (R/60 is already taken by tiered_same_day_payer.R).

**Header block:** Follow R/59 header format with Purpose, Inputs, Outputs, Decision traceability (D-01 through D-06), Dependencies sections.

**SECTION 1: SETUP AND CONFIGURATION**
- suppressPackageStartupMessages for: dplyr, glue, lubridate, openxlsx2
- source("R/00_config.R"), source("R/utils/utils_duckdb.R"), source("R/utils/utils_dates.R"), source("R/utils/utils_assertions.R")
- Define paths:
  - DEATH_RDS <- file.path(CONFIG$cache$outputs_dir, "validated_death_dates.rds")
  - EPISODES_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")
  - OUTPUT_XLSX <- file.path(CONFIG$output_dir, "post_death_encounter_investigation.xlsx")
- Banner message: "=== Phase 113: Post-Death Encounter Investigation ==="
- Log all three paths

**SECTION 2: INPUT VALIDATION**
- assert_rds_exists(DEATH_RDS, script_name = "R/51")
- assert_rds_exists(EPISODES_RDS, script_name = "R/51")
- message("  Both input RDS files validated.")

**SECTION 3: LOAD AND FILTER DEATH DATES**
- validated_deaths <- readRDS(DEATH_RDS)
- valid_deaths <- validated_deaths %>% filter(death_valid == TRUE, post_death_activity == TRUE)
- Log count: "  Patients with post-death activity: {nrow(valid_deaths)}"
- This is the ~200 patient population

**SECTION 4: QUERY POST-DEATH ENCOUNTERS FROM DUCKDB**
- open_pcornet_con()
- Query ENCOUNTER table: get_pcornet_table("ENCOUNTER") %>% select(ID, ADMIT_DATE, ENCOUNTERID) %>% collect() %>% mutate(ADMIT_DATE = parse_pcornet_date(ADMIT_DATE)) %>% filter(!is.na(ADMIT_DATE))
- inner_join with valid_deaths %>% select(ID, DEATH_DATE), by = "ID"
- filter(ADMIT_DATE > DEATH_DATE)
- mutate: days_after_death = as.numeric(ADMIT_DATE - DEATH_DATE), event_date = ADMIT_DATE, event_id = ENCOUNTERID, source_table = "ENCOUNTER"
- select(ID, DEATH_DATE, event_date, event_id, source_table, days_after_death)
- Log count: "  Post-death encounters: {nrow(encounter_post_death)} events from {n_distinct(encounter_post_death$ID)} patients"

**SECTION 5: QUERY POST-DEATH DIAGNOSES FROM DUCKDB**
- Query DIAGNOSIS table: get_pcornet_table("DIAGNOSIS") %>% select(ID, DX_DATE, DIAGNOSISID) %>% collect() %>% mutate(DX_DATE = parse_pcornet_date(DX_DATE)) %>% filter(!is.na(DX_DATE))
- inner_join with valid_deaths %>% select(ID, DEATH_DATE), by = "ID"
- filter(DX_DATE > DEATH_DATE)
- mutate: days_after_death = as.numeric(DX_DATE - DEATH_DATE), event_date = DX_DATE, event_id = DIAGNOSISID, source_table = "DIAGNOSIS"
- select same 6 columns as encounter query
- close_pcornet_con() (per D-02: standalone, clean up connections)
- Log count: "  Post-death diagnoses: {nrow(diagnosis_post_death)} events from {n_distinct(diagnosis_post_death$ID)} patients"

**SECTION 6: QUERY POST-DEATH TREATMENTS FROM RDS**
- treatment_episodes <- readRDS(EPISODES_RDS)
- inner_join with valid_deaths %>% select(ID, DEATH_DATE), by = c("patient_id" = "ID")
- filter(episode_start > DEATH_DATE)
- mutate: days_after_death = as.numeric(episode_start - DEATH_DATE), event_date = episode_start, event_id = paste0("EP_", episode_number), source_table = "TREATMENT", ID = patient_id
- select same 6 columns
- Log count

**SECTION 7: COMBINE ALL POST-DEATH EVENTS AND BUCKET (per D-03, D-04, D-06)**
- post_death_events <- bind_rows(encounter_post_death, diagnosis_post_death, treatment_post_death)
- Add gap_bucket column using case_when (per D-03):
  - days_after_death <= 30 ~ "0-30 days"
  - days_after_death <= 90 ~ "31-90 days"
  - days_after_death <= 365 ~ "91-365 days"
  - days_after_death > 365 ~ ">1 year"
  - TRUE ~ NA_character_
- arrange(ID, days_after_death) for consistent ordering
- Log: "  Total post-death events: {nrow(post_death_events)} across {n_distinct(post_death_events$ID)} patients"
- Log: "  By source: ENCOUNTER={sum(post_death_events$source_table == 'ENCOUNTER')}, DIAGNOSIS={sum(post_death_events$source_table == 'DIAGNOSIS')}, TREATMENT={sum(post_death_events$source_table == 'TREATMENT')}"

**SECTION 8: BUILD PER-PATIENT SUMMARY (per D-01)**
- patient_summary <- post_death_events %>% group_by(ID, DEATH_DATE) %>% summarise:
  - total_events = n()
  - encounter_events = sum(source_table == "ENCOUNTER")
  - diagnosis_events = sum(source_table == "DIAGNOSIS")
  - treatment_events = sum(source_table == "TREATMENT")
  - min_gap_days = min(days_after_death)
  - max_gap_days = max(days_after_death)
  - median_gap_days = median(days_after_death)
  - earliest_post_death_event = min(event_date)
  - latest_post_death_event = max(event_date)
  - gap_bucket = case_when on max_gap_days (same 4 buckets as D-03)
  - .groups = "drop"
- arrange(desc(max_gap_days)) -- longest gaps first for review

**SECTION 9: BUCKET DISTRIBUTION SUMMARY**
- bucket_dist <- patient_summary %>% count(gap_bucket, name = "patient_count") %>% mutate(pct = sprintf("%.1f%%", 100 * patient_count / sum(patient_count)))
- Log each bucket line
- Also compute activity_type_dist cross-tab: post_death_events %>% count(source_table, gap_bucket) %>% tidyr::pivot_wider(names_from = gap_bucket, values_from = n, values_fill = 0)
- This is the optional third summary element (Claude's discretion: include as a third sheet "Bucket by Activity Type" since it adds useful context for the meeting)

**SECTION 10: CREATE STYLED XLSX (per D-01, D-08)**
Follow R/59 xlsx styling pattern exactly:

Sheet 1: "Patient Summary"
- Title row A1: "Post-Death Clinical Activity Investigation" (Calibri 16, bold, color FF1F2937)
- merge_cells A1 through column count
- Subtitle row A2: "Generated: {Sys.Date()} | Population: {nrow(patient_summary)} patients with post-death activity" (Calibri 10, color FF6B7280)
- merge_cells A2 through column count
- Data at row 4 with patient_summary
- Header styling row 4: fill FF374151, font Calibri 11 bold white FFFFFFFF
- Number format "#,##0" for integer columns (total_events through median_gap_days, columns C through I with 1-based indexing)
- freeze_pane firstActiveRow = 5
- Column widths: ID=15, DEATH_DATE=15, total_events=12, encounter_events=14, diagnosis_events=14, treatment_events=14, min_gap_days=12, max_gap_days=12, median_gap_days=14, earliest_post_death_event=20, latest_post_death_event=20, gap_bucket=15

Sheet 2: "Event Detail" (per D-04, D-06)
- Title row A1: "Post-Death Events -- Per-Event Detail" (same title styling)
- Subtitle row A2: "Generated: {Sys.Date()} | Total events: {nrow(post_death_events)}" (same subtitle styling)
- Data at row 4 with post_death_events (columns: ID, DEATH_DATE, event_date, event_id, source_table, days_after_death, gap_bucket)
- Header styling identical to Sheet 1
- Number format "#,##0" for days_after_death column
- freeze_pane firstActiveRow = 5
- Column widths: ID=15, DEATH_DATE=15, event_date=15, event_id=20, source_table=15, days_after_death=16, gap_bucket=15

Sheet 3: "Bucket by Activity Type" (Claude's discretion -- adds meeting value)
- Title row A1: "Gap Distribution Cross-Tabbed by Activity Type"
- Subtitle row A2: "Generated: {Sys.Date()} | Patient count per bucket by source table"
- Data at row 4 with activity_type_dist (pivoted: source_table as rows, bucket labels as columns)
- Same header styling

wb_save(wb, OUTPUT_XLSX, overwrite = TRUE)

**SECTION 11: FINAL SUMMARY**
- Log output path
- Log key metrics: total patients, total events, bucket distribution
- message("Done.")
  </action>
  <verify>
    <automated>Rscript -e "lines <- readLines('R/51_post_death_encounter_investigation.R'); cat('Lines:', length(lines), '\n'); stopifnot(length(lines) >= 200); stopifnot(any(grepl('days_after_death.*as\\.numeric', lines))); stopifnot(any(grepl('gap_bucket.*case_when', lines))); stopifnot(any(grepl('wb_save', lines))); stopifnot(any(grepl('source_table.*ENCOUNTER', lines))); stopifnot(any(grepl('Patient Summary', lines))); stopifnot(any(grepl('Event Detail', lines))); cat('All structural checks passed\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/51_post_death_encounter_investigation.R exists with >= 200 lines
    - File contains `suppressPackageStartupMessages` with dplyr, glue, lubridate, openxlsx2
    - File contains `source("R/00_config.R")`
    - File contains `assert_rds_exists(DEATH_RDS, script_name = "R/51")`
    - File contains `filter(death_valid == TRUE, post_death_activity == TRUE)` for population subsetting
    - File contains `get_pcornet_table("ENCOUNTER")` and `get_pcornet_table("DIAGNOSIS")` for DuckDB queries
    - File contains `days_after_death = as.numeric(ADMIT_DATE - DEATH_DATE)` (correct subtraction direction per Pitfall 1)
    - File contains `days_after_death = as.numeric(DX_DATE - DEATH_DATE)` for diagnosis gap
    - File contains `days_after_death = as.numeric(episode_start - DEATH_DATE)` for treatment gap
    - File contains case_when with four buckets: "0-30 days", "31-90 days", "91-365 days", ">1 year"
    - File contains `source_table = "ENCOUNTER"`, `source_table = "DIAGNOSIS"`, `source_table = "TREATMENT"` (per D-06)
    - File contains `bind_rows(encounter_post_death, diagnosis_post_death, treatment_post_death)`
    - File contains `wb_workbook()` and `wb_save(wb, OUTPUT_XLSX, overwrite = TRUE)`
    - File contains `add_worksheet("Patient Summary")` and `add_worksheet("Event Detail")`
    - File contains `wb_color("FF374151")` for header fill styling
    - File contains `close_pcornet_con()` for DuckDB connection cleanup
    - File contains `open_pcornet_con()` exactly once
  </acceptance_criteria>
  <done>R/51 investigation script exists, follows R/59 standalone investigation pattern, reads validated_death_dates.rds and DuckDB ENCOUNTER/DIAGNOSIS tables and treatment_episodes.rds, computes days_after_death gaps with case_when bucketing into 4 clinical ranges, produces styled two-sheet xlsx (Patient Summary + Event Detail + optional Bucket by Activity Type) with dark gray headers and freeze panes</done>
</task>

<task type="auto">
  <name>Task 2: Add R/88 smoke test section and R/39 pipeline runner entry</name>
  <files>R/88_smoke_test_comprehensive.R, R/39_run_all_investigations.R</files>
  <read_first>
    R/88_smoke_test_comprehensive.R
    R/39_run_all_investigations.R
    R/51_post_death_encounter_investigation.R
  </read_first>
  <action>
**R/88 smoke test -- Add SECTION 15i for Phase 113:**

Insert a new section AFTER the SECTION 15h block (Phase 112, ends at line ~1687) and BEFORE SECTION 15g (proton therapy, starts at line ~1688). The new section validates R/51 structural integrity.

```r
# ==============================================================================
# SECTION 15i: POST-DEATH ENCOUNTER INVESTIGATION (Phase 113) ----
# ==============================================================================

message("\n[Phase 113] Post-death encounter investigation...")

r51_lines <- readLines("R/51_post_death_encounter_investigation.R", warn = FALSE)
r51_text <- paste(r51_lines, collapse = "\n")

# Check 1: R/51 exists and has minimum length
check("R/51 post-death encounter investigation exists (>= 200 lines)",
      length(r51_lines) >= 200)

# Check 2: R/51 reads validated_death_dates.rds
check("R/51 reads validated_death_dates.rds",
      any(grepl("validated_death_dates\\.rds", r51_lines)))

# Check 3: R/51 filters death_valid == TRUE
check("R/51 filters death_valid == TRUE (Pitfall 2 avoidance)",
      any(grepl("death_valid\\s*==\\s*TRUE", r51_lines)))

# Check 4: R/51 filters post_death_activity == TRUE
check("R/51 filters post_death_activity == TRUE",
      any(grepl("post_death_activity\\s*==\\s*TRUE", r51_lines)))

# Check 5: R/51 queries ENCOUNTER table from DuckDB
check("R/51 queries DuckDB ENCOUNTER table (D-05)",
      any(grepl('get_pcornet_table.*ENCOUNTER', r51_lines)))

# Check 6: R/51 queries DIAGNOSIS table from DuckDB
check("R/51 queries DuckDB DIAGNOSIS table (D-05)",
      any(grepl('get_pcornet_table.*DIAGNOSIS', r51_lines)))

# Check 7: R/51 reads treatment_episodes.rds
check("R/51 reads treatment_episodes.rds (D-05)",
      any(grepl("treatment_episodes\\.rds", r51_lines)))

# Check 8: R/51 computes days_after_death using correct subtraction direction
check("R/51 computes days_after_death = as.numeric(date - DEATH_DATE)",
      any(grepl("days_after_death.*as\\.numeric.*DEATH_DATE", r51_lines)))

# Check 9: R/51 has case_when bucket assignment with 4 buckets (D-03)
check("R/51 has case_when gap_bucket with 0-30/31-90/91-365/>1 year (D-03)",
      any(grepl("0-30 days", r51_lines)) &&
      any(grepl("31-90 days", r51_lines)) &&
      any(grepl("91-365 days", r51_lines)) &&
      any(grepl(">1 year", r51_lines)))

# Check 10: R/51 has source_table column with all 3 types (D-06)
check("R/51 labels source_table as ENCOUNTER/DIAGNOSIS/TREATMENT (D-06)",
      any(grepl('source_table.*=.*"ENCOUNTER"', r51_lines)) &&
      any(grepl('source_table.*=.*"DIAGNOSIS"', r51_lines)) &&
      any(grepl('source_table.*=.*"TREATMENT"', r51_lines)))

# Check 11: R/51 creates Patient Summary and Event Detail sheets (D-01)
check("R/51 creates Patient Summary and Event Detail xlsx sheets (D-01)",
      any(grepl("Patient Summary", r51_lines)) &&
      any(grepl("Event Detail", r51_lines)))

# Check 12: R/51 uses styled headers (D-08)
check("R/51 uses dark gray header fill FF374151 (D-08)",
      any(grepl("FF374151", r51_lines)))

# Check 13: R/51 closes DuckDB connection
check("R/51 calls close_pcornet_con() for connection cleanup",
      any(grepl("close_pcornet_con", r51_lines)))

# Check 14: R/51 has freeze_pane for both sheets
check("R/51 freezes panes on output sheets",
      sum(grepl("freeze_pane", r51_lines)) >= 2)
```

Also add to the coverage listing at the end of R/88 (before the `if (failed > 0)` block, after the SORT-02 line):

```r
message("  * POSTDEATH-01: Two-sheet xlsx with per-patient summary and per-event detail (R/51 Phase 113)")
message("  * POSTDEATH-02: R/88 validates R/51 structure, bucketing, source_table labels (Phase 113)")
message("  * POSTDEATH-03: R/39 pipeline runner includes R/51 (Phase 113)")
```

**R/39 pipeline runner -- Add R/51 to investigation scripts:**

In SECTION 3 (investigation scripts, line ~176-192), add R/51 to the `investigation_scripts` vector after the R/59 entry (line ~180). Add this line:

```r
  "R/51_post_death_encounter_investigation.R",  # Post-death encounter drill-down (Phase 113)
```

The entry goes after `"R/59_death_date_summary.R",  # G15: Death date cross-tab` since R/51 is a drill-down of R/59's findings.
  </action>
  <verify>
    <automated>Rscript -e "r88 <- readLines('R/88_smoke_test_comprehensive.R'); stopifnot(any(grepl('Phase 113', r88))); stopifnot(any(grepl('SECTION 15i', r88))); stopifnot(any(grepl('POSTDEATH-01', r88))); r39 <- readLines('R/39_run_all_investigations.R'); stopifnot(any(grepl('51_post_death_encounter_investigation', r39))); cat('Smoke test and pipeline runner checks passed\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/88 contains `SECTION 15i: POST-DEATH ENCOUNTER INVESTIGATION (Phase 113)`
    - R/88 contains `message("\n[Phase 113] Post-death encounter investigation...")`
    - R/88 contains `r51_lines <- readLines("R/51_post_death_encounter_investigation.R", warn = FALSE)`
    - R/88 contains check for `death_valid == TRUE` filtering
    - R/88 contains check for `post_death_activity == TRUE` filtering
    - R/88 contains check for ENCOUNTER and DIAGNOSIS DuckDB queries
    - R/88 contains check for 4 gap buckets (0-30, 31-90, 91-365, >1 year)
    - R/88 contains check for source_table labels (ENCOUNTER, DIAGNOSIS, TREATMENT)
    - R/88 contains check for Patient Summary and Event Detail sheet names
    - R/88 contains check for FF374151 header styling
    - R/88 contains check for close_pcornet_con()
    - R/88 coverage listing contains `POSTDEATH-01`, `POSTDEATH-02`, `POSTDEATH-03`
    - R/39 investigation_scripts vector contains `"R/51_post_death_encounter_investigation.R"`
    - R/39 entry appears after the R/59 entry in the investigation_scripts vector
    - Rscript R/88_smoke_test_comprehensive.R exits 0 (all checks pass)
  </acceptance_criteria>
  <done>R/88 smoke test has Phase 113 section with 14 structural checks validating R/51 investigation script, coverage listing includes POSTDEATH-01/02/03, R/39 pipeline runner includes R/51 in investigation scripts block after R/59</done>
</task>

</tasks>

<verification>
1. R/51 exists with >= 200 lines and correct structural patterns
2. R/88 smoke test passes: `Rscript R/88_smoke_test_comprehensive.R` exits 0
3. R/39 includes R/51 in investigation scripts
4. All Phase 113 requirement IDs (POSTDEATH-01, POSTDEATH-02, POSTDEATH-03) are covered
</verification>

<success_criteria>
- R/51_post_death_encounter_investigation.R is a standalone investigation script following R/59 pattern
- Script reads validated_death_dates.rds, DuckDB ENCOUNTER/DIAGNOSIS, and treatment_episodes.rds
- Script computes days_after_death for all post-death events with correct subtraction direction
- Script buckets gaps into 0-30 days, 31-90 days, 91-365 days, >1 year using case_when
- Script produces two-sheet xlsx (Patient Summary + Event Detail) with styled headers (FF374151 dark gray, white bold text, freeze panes)
- Each detail row has source_table column identifying ENCOUNTER, DIAGNOSIS, or TREATMENT
- R/88 smoke test validates all structural patterns and passes
- R/39 pipeline runner includes R/51 in investigation block
</success_criteria>

<output>
After completion, create `.planning/phases/113-investigate-encounters-after-death-date-quantify-how-far-after-death-the-200-patients-encounters-occur/113-01-SUMMARY.md`
</output>
