---
phase: 110-redo-cancer-summary-table-pre-post-v2-7day
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/49_cancer_summary_pre_post.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - V2FIX-01
  - V2FIX-02
  - V2FIX-03
must_haves:
  truths:
    - "V2 cancer summary table population is restricted to patients with HL-specific 7-day confirmation (C81 + 201.x codes), not any-cancer 7-day confirmation"
    - "K-L-M columns (Pre-HL, Post-HL, Both) only count secondary malignancies that themselves meet the 7-day gap criterion"
    - "V2 xlsx, csv, and rds output files are overwritten with the tighter-filtered data"
    - "V1 output remains untouched as the unfiltered baseline"
    - "R/88 smoke test validates the updated V2 population assertion bounds"
  artifacts:
    - path: "R/49_cancer_summary_pre_post.R"
      provides: "Tighter V2 population filter using HL-specific 7-day IDs + updated assertions + updated xlsx metadata"
      contains: "hl_7day_confirmed_ids"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Updated smoke test checks for V2 population bounds and HL-specific filtering pattern"
      contains: "hl_7day_confirmed_ids"
  key_links:
    - from: "R/49 Section 3 (lines 125-131)"
      to: "R/49 Section 8b (lines 175-177)"
      via: "hl_7day_confirmed_ids vector used in cancer_summary_v2 filter"
      pattern: "ID %in% hl_7day_confirmed_ids"
    - from: "R/49 Section 8b cancer_summary_v2"
      to: "R/49 Section 8b v2_valid_pairs"
      via: "Semi-join on tighter population automatically narrows K-L-M columns"
      pattern: "semi_join.*v2_valid_pairs"
---

<objective>
Tighten the V2 cancer summary table with dual 7-day confirmation criteria: (1) restrict population to patients with HL-specific 7-day gap confirmation, and (2) ensure K-L-M columns only count 7-day confirmed secondary malignancies. Modify R/49 in-place; update R/88 smoke test to validate new bounds.

Purpose: The current V2 table includes patients with 7-day confirmation for ANY cancer code. The user wants only patients whose Hodgkin Lymphoma diagnosis specifically meets the 7-day gap criterion, with secondary malignancies also individually confirmed.

Output: Updated R/49 script, updated R/88 smoke test, overwritten V2 output files (xlsx, csv, rds).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-redo-cancer-summary-table-pre-post-v2-7day-xlsx-but-have-it-so-only-confirmed-7-day-gap-hl-pts-are-in-it-and-rows-k-through-l-only-have-confirmed-7-day-gap-respective-malignancies/110-CONTEXT.md
@.planning/phases/110-redo-cancer-summary-table-pre-post-v2-7day-xlsx-but-have-it-so-only-confirmed-7-day-gap-hl-pts-are-in-it-and-rows-k-through-l-only-have-confirmed-7-day-gap-respective-malignancies/110-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs from R/49. Extracted from codebase. -->

From R/49_cancer_summary_pre_post.R (Section 3, lines 125-131) -- current HL 7-day computation:
```r
n_hl_7day <- hl_with_date %>%
  distinct(ID, DX_DATE) %>%
  group_by(ID) %>%
  filter(n() >= 2, as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7) %>%
  ungroup() %>%
  pull(ID) %>%
  n_distinct()
```

From R/49 (line 154) -- rm() call that removes hl_with_date:
```r
rm(hl_nlphl, hl_classical, hl_dx, hl_with_date)
```

From R/49 (lines 175-177) -- current V2 population filter:
```r
cancer_summary_v2 <- cancer_summary %>%
  filter(two_or_more_unique_dates_gt_7 == 1)
```

From R/49 (lines 191-195) -- current V2 assertion:
```r
checkmate::assert_int(
  as.integer(v2_n_patients),
  lower = 6300L, upper = 7500L,
  .var.name = glue("[R/49 CANCER-02 ERROR] V2 7-day total population expected 6300-7500, got {v2_n_patients}")
)
```

From R/49 (lines 518-530) -- V2 pre/post/both semi-join (NO CHANGE NEEDED):
```r
v2_valid_pairs <- cancer_summary_v2 %>%
  distinct(ID, cancer_code)
patients_pre_v2 <- patients_pre %>%
  semi_join(v2_valid_pairs, by = c("ID", "cancer_code"))
patients_post_v2 <- patients_post %>%
  semi_join(v2_valid_pairs, by = c("ID", "cancer_code"))
patients_both_v2 <- patients_both %>%
  semi_join(v2_valid_pairs, by = c("ID", "cancer_code"))
```

From R/49 (line 950) -- current V2 Sheet 1 title:
```r
"Cancer Summary Table - Pre/Post HL Diagnosis (By Category) - 7-Day Confirmed"
```

From R/49 (line 1053) -- current V2 Sheet 2 title:
```r
"Cancer Summary Table - Pre/Post HL Diagnosis (By ICD-10 Code) - 7-Day Confirmed"
```

From R/49 (line 1026) -- current V2 Sheet 1 footnote:
```r
footnote_text1_v2 <- glue("V2: Filtered to patients with two_or_more_unique_dates_gt_7 == 1. Baseline stats: ...")
```

From R/49 (line 1129) -- current V2 Sheet 2 footnote:
```r
footnote_text2_v2 <- glue("V2: Filtered to patients with two_or_more_unique_dates_gt_7 == 1. Baseline stats: ...")
```

From R/49 (lines 1176-1189) -- V2 RDS metadata:
```r
v2_output <- list(
  code_summary = code_summary_v2,
  category_summary = category_summary_v2,
  totals_code = totals_code_v2,
  totals_category = totals_category_v2,
  metadata = list(
    filter = "two_or_more_unique_dates_gt_7 == 1",
    total_patients = v2_n_patients,
    created = Sys.time(),
    phase = "77"
  )
)
```

From R/88_smoke_test_comprehensive.R (lines 878-881) -- current V2 assertion check:
```r
check(
  "R/49 has checkmate assert_int for v2 population (6300-7500)",
  grepl("assert_int.*6300.*7500", r49_text)
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten R/49 V2 population filter, assertions, and xlsx metadata</name>
  <files>R/49_cancer_summary_pre_post.R</files>
  <read_first>
    - R/49_cancer_summary_pre_post.R (entire file -- executor must see current state of all sections being modified)
    - .planning/phases/110-redo-cancer-summary-table-pre-post-v2-7day-xlsx-but-have-it-so-only-confirmed-7-day-gap-hl-pts-are-in-it-and-rows-k-through-l-only-have-confirmed-7-day-gap-respective-malignancies/110-CONTEXT.md (locked decisions D-01 through D-08)
    - .planning/phases/110-redo-cancer-summary-table-pre-post-v2-7day-xlsx-but-have-it-so-only-confirmed-7-day-gap-hl-pts-are-in-it-and-rows-k-through-l-only-have-confirmed-7-day-gap-respective-malignancies/110-RESEARCH.md (implementation patterns)
  </read_first>
  <action>
Modify R/49_cancer_summary_pre_post.R in four targeted areas. V1 logic (Sections 1-8a, 9a, 10, 11) is NOT touched.

**Change 1 -- Section 3 (lines 125-131): Expand HL 7-day computation to produce ID vector (per D-04)**

Replace the current `n_hl_7day` computation (lines 125-131) with:

```r
# Compute HL-specific 7-day confirmed IDs (Phase 110: expanded to ID vector per D-04)
hl_7day_confirmed <- hl_with_date %>%
  distinct(ID, DX_DATE) %>%
  group_by(ID) %>%
  filter(n() >= 2, as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7) %>%
  ungroup() %>%
  distinct(ID)

n_hl_7day <- nrow(hl_7day_confirmed)
hl_7day_confirmed_ids <- hl_7day_confirmed$ID
```

This MUST appear BEFORE the `rm(hl_nlphl, hl_classical, hl_dx, hl_with_date)` call at line 154. The `hl_7day_confirmed` intermediate tibble can be cleaned up in that same rm() call -- update line 154 to: `rm(hl_nlphl, hl_classical, hl_dx, hl_with_date, hl_7day_confirmed)`.

The `hl_7day_confirmed_ids` vector persists for use in Section 8b.

**Change 2 -- Section 8b (lines 175-177): Tighten V2 population filter (per D-04, D-05)**

Replace:
```r
cancer_summary_v2 <- cancer_summary %>%
  filter(two_or_more_unique_dates_gt_7 == 1)
```

With:
```r
# Phase 110: V2 population restricted to HL-specific 7-day confirmed patients (per D-04, D-05)
# Step 1: restrict to patients whose HL codes meet 7-day gap criterion
# Step 2: then require each patient-code pair to also meet 7-day confirmation
cancer_summary_v2 <- cancer_summary %>%
  filter(ID %in% hl_7day_confirmed_ids) %>%
  filter(two_or_more_unique_dates_gt_7 == 1)
```

Add a diagnostic message after the v2_n_patients computation (after line 185):
```r
message(glue("  V2 HL-confirmed population: {format(length(hl_7day_confirmed_ids), big.mark=',')} patients with HL-specific 7-day gap"))
message(glue("  V2 after per-code 7-day filter: {format(v2_n_patients, big.mark=',')} unique patients"))
```

**Change 3 -- Section 8b (lines 191-196): Update V2 population assertion bounds**

Replace:
```r
checkmate::assert_int(
  as.integer(v2_n_patients),
  lower = 6300L, upper = 7500L,
  .var.name = glue("[R/49 CANCER-02 ERROR] V2 7-day total population expected 6300-7500, got {v2_n_patients}")
)
message(glue("  V2 population assertion PASSED: {v2_n_patients} in [6300, 7500]"))
```

With:
```r
# Phase 110: Bounds adjusted for HL-specific 7-day population (smaller subset than any-code)
checkmate::assert_int(
  as.integer(v2_n_patients),
  lower = 4000L, upper = 7000L,
  .var.name = glue("[R/49 CANCER-02 ERROR] V2 7-day HL-confirmed population expected 4000-7000, got {v2_n_patients}")
)
message(glue("  V2 population assertion PASSED: {v2_n_patients} in [4000, 7000]"))
```

NOTE: Using wide bounds (4000-7000) because exact population is unknown until first run with real data. The previous range was 6300-7500 for any-code; the HL-specific subset will be smaller. The executor should note in the SUMMARY that bounds can be tightened after observing actual output.

**Change 4 -- Section 9b: Update V2 xlsx titles and footnotes**

4a. Sheet 1 title (line 950): Replace
```r
"Cancer Summary Table - Pre/Post HL Diagnosis (By Category) - 7-Day Confirmed"
```
with:
```r
"Cancer Summary Table - Pre/Post HL Diagnosis (By Category) - Confirmed HL 7-Day Gap Patients Only"
```

4b. Sheet 2 title (line 1053): Replace
```r
"Cancer Summary Table - Pre/Post HL Diagnosis (By ICD-10 Code) - 7-Day Confirmed"
```
with:
```r
"Cancer Summary Table - Pre/Post HL Diagnosis (By ICD-10 Code) - Confirmed HL 7-Day Gap Patients Only"
```

4c. Sheet 1 footnote (line 1026): Replace the footnote_text1_v2 assignment with:
```r
footnote_text1_v2 <- glue("V2 (Phase 110): Restricted to patients with 7-day confirmed HL diagnosis (C81 + 201.x codes with 2+ unique dates spanning 7+ days). Secondary malignancies in Pre/Post/Both columns also require two_or_more_unique_dates_gt_7 == 1. Baseline: {v2_n_patients} HL-confirmed patients ({length(hl_7day_confirmed_ids)} had HL 7-day gap). Pre/Post/Both: {nrow(cohort_with_dates)} patients with known first_hl_dx_date. Pre: DX_DATE <= first_hl_dx_date. Post: DX_DATE > first_hl_dx_date. Both: patient had code pre AND post. C81 + 201.x pre/post/both left blank (anchor diagnosis).")
```

4d. Sheet 2 footnote (line 1129): Replace the footnote_text2_v2 assignment with the same text:
```r
footnote_text2_v2 <- glue("V2 (Phase 110): Restricted to patients with 7-day confirmed HL diagnosis (C81 + 201.x codes with 2+ unique dates spanning 7+ days). Secondary malignancies in Pre/Post/Both columns also require two_or_more_unique_dates_gt_7 == 1. Baseline: {v2_n_patients} HL-confirmed patients ({length(hl_7day_confirmed_ids)} had HL 7-day gap). Pre/Post/Both: {nrow(cohort_with_dates)} patients with known first_hl_dx_date. Pre: DX_DATE <= first_hl_dx_date. Post: DX_DATE > first_hl_dx_date. Both: patient had code pre AND post. C81 + 201.x pre/post/both left blank (anchor diagnosis).")
```

**Change 5 -- Section 10b (lines 1181-1186): Update V2 RDS metadata**

Replace:
```r
  metadata = list(
    filter = "two_or_more_unique_dates_gt_7 == 1",
    total_patients = v2_n_patients,
    created = Sys.time(),
    phase = "77"
  )
```
With:
```r
  metadata = list(
    filter = "HL-specific 7-day confirmed (ID %in% hl_7day_confirmed_ids) AND two_or_more_unique_dates_gt_7 == 1",
    total_patients = v2_n_patients,
    hl_7day_confirmed_count = length(hl_7day_confirmed_ids),
    created = Sys.time(),
    phase = "110"
  )
```

**What NOT to change:**
- V1 Sections (1-8a, 9a, 10, 11) -- untouched per D-01
- Lines 518-530 (v2_valid_pairs semi-join) -- the existing pattern automatically narrows K-L-M when cancer_summary_v2 is tighter. No code change needed per research Pattern 3.
- Lines 229-235 (HL anchor code exclusion from pre/post) -- defensive safeguard stays as-is
- V1 vs V2 comparison table (lines 641-659) -- generic logic works with tighter V2 automatically
  </action>
  <verify>
    <automated>grep -n "hl_7day_confirmed_ids" R/49_cancer_summary_pre_post.R | head -10</automated>
  </verify>
  <acceptance_criteria>
    - R/49_cancer_summary_pre_post.R contains `hl_7day_confirmed_ids <- hl_7day_confirmed$ID` (ID vector extraction)
    - R/49_cancer_summary_pre_post.R contains `filter(ID %in% hl_7day_confirmed_ids)` in V2 filter section
    - R/49_cancer_summary_pre_post.R contains `lower = 4000L, upper = 7000L` (updated assertion bounds)
    - R/49_cancer_summary_pre_post.R contains `Confirmed HL 7-Day Gap Patients Only` in V2 Sheet 1 title
    - R/49_cancer_summary_pre_post.R contains `Confirmed HL 7-Day Gap Patients Only` in V2 Sheet 2 title
    - R/49_cancer_summary_pre_post.R contains `V2 (Phase 110): Restricted to patients with 7-day confirmed HL diagnosis` in footnote text
    - R/49_cancer_summary_pre_post.R contains `phase = "110"` in V2 RDS metadata
    - R/49_cancer_summary_pre_post.R contains `hl_7day_confirmed_count = length(hl_7day_confirmed_ids)` in metadata
    - R/49_cancer_summary_pre_post.R rm() call at former line 154 includes `hl_7day_confirmed` but NOT `hl_7day_confirmed_ids`
    - V1 output paths (OUTPUT_TABLE_XLSX, OUTPUT_CSV) are unchanged
    - Lines containing `patients_pre %>%` and `semi_join(v2_valid_pairs` are unchanged (existing K-L-M logic)
  </acceptance_criteria>
  <done>R/49 V2 population filter uses HL-specific 7-day confirmed IDs, V2 assertion bounds widened for smaller population, V2 xlsx titles and footnotes describe the dual 7-day confirmation criteria, V2 RDS metadata updated to phase 110. V1 output completely untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update R/88 smoke test for V2 population assertion bounds</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (entire file -- executor must see current check patterns and section structure)
    - R/49_cancer_summary_pre_post.R (to confirm the new assertion bounds match what smoke test should validate)
  </read_first>
  <action>
Modify R/88_smoke_test_comprehensive.R Section 13D (7-Day Gap Extension and R/49 V2 Validation).

**Change 1 -- Update Check 3 (lines 878-882): Match new assertion bounds**

Replace:
```r
# Check 3: R/49 has checkmate population assertion for v2 range (widened for ICD-9 cohort expansion)
check(
  "R/49 has checkmate assert_int for v2 population (6300-7500)",
  grepl("assert_int.*6300.*7500", r49_text)
)
```

With:
```r
# Check 3: R/49 has checkmate population assertion for v2 range (Phase 110: HL-specific 7-day confirmed)
check(
  "R/49 has checkmate assert_int for v2 population (4000-7000)",
  grepl("assert_int.*4000.*7000", r49_text)
)
```

**Change 2 -- Add new check after Check 2 (after line 876): Validate HL-specific filtering pattern**

Insert a new check between existing Check 2 and Check 3:

```r
# Check 2b: R/49 filters V2 population by HL-specific 7-day confirmed IDs (Phase 110)
check(
  "R/49 V2 filters by HL-specific 7-day confirmed IDs (hl_7day_confirmed_ids)",
  any(grepl("hl_7day_confirmed_ids", r49_lines))
)
```

**What NOT to change:**
- Check 1 (OUTPUT_TABLE_V2_XLSX path) -- unchanged
- Check 2 (two_or_more_unique_dates_gt_7 == 1) -- KEEP this check; the per-code filter still exists in R/49
- Check 4 (NLPHL diagnostic split) -- unchanged
- Check 5 (saveRDS with v2) -- unchanged
- Check 6 (NLPHL-aware category names) -- unchanged
- Check 7 (V2 comparison console-only) -- unchanged
- All other sections of R/88 -- unchanged
  </action>
  <verify>
    <automated>grep -n "hl_7day_confirmed_ids\|4000.*7000" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains `grepl("assert_int.*4000.*7000", r49_text)` (updated bounds check)
    - R/88_smoke_test_comprehensive.R contains `grepl("hl_7day_confirmed_ids", r49_lines)` (new HL-specific check)
    - R/88_smoke_test_comprehensive.R still contains `grepl("two_or_more_unique_dates_gt_7 == 1", r49_lines, fixed = TRUE)` (per-code filter check preserved)
    - R/88_smoke_test_comprehensive.R does NOT contain `6300.*7500` in Section 13D (old bounds removed)
  </acceptance_criteria>
  <done>R/88 smoke test validates R/49 uses HL-specific 7-day confirmed IDs for V2 population filter and checks the updated assertion bounds (4000-7000). Existing checks for per-code 7-day filter, NLPHL split, RDS output, and comparison table preserved.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full change set:

1. `grep -c "hl_7day_confirmed_ids" R/49_cancer_summary_pre_post.R` returns 4+ (definition, filter, footnote x2, metadata)
2. `grep "Confirmed HL 7-Day Gap Patients Only" R/49_cancer_summary_pre_post.R` returns 2 lines (Sheet 1 + Sheet 2 titles)
3. `grep "4000.*7000" R/49_cancer_summary_pre_post.R` returns at least 1 line (assertion bounds)
4. `grep "4000.*7000" R/88_smoke_test_comprehensive.R` returns at least 1 line (smoke test bounds)
5. V1 output paths are unchanged: `grep "OUTPUT_TABLE_XLSX\b" R/49_cancer_summary_pre_post.R` still shows original V1 path
6. Semi-join pattern is unchanged: `grep "semi_join.*v2_valid_pairs" R/49_cancer_summary_pre_post.R` returns 3 lines (patients_pre_v2, patients_post_v2, patients_both_v2)
</verification>

<success_criteria>
- R/49 V2 population is filtered to HL-specific 7-day confirmed patients (per D-04, D-05)
- R/49 K-L-M columns naturally inherit tighter population via existing semi-join pattern (per D-06, D-07)
- R/49 V2 assertion bounds accommodate smaller HL-specific population
- R/49 V2 xlsx titles and footnotes clearly describe the dual 7-day confirmation criteria
- R/49 V2 RDS metadata references Phase 110 and HL-specific filter description
- R/88 smoke test validates the new assertion bounds and HL-specific filtering pattern
- V1 output remains completely untouched (per D-01)
- Both sheets apply same filtering (per D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/110-redo-cancer-summary-table-pre-post-v2-7day-xlsx-but-have-it-so-only-confirmed-7-day-gap-hl-pts-are-in-it-and-rows-k-through-l-only-have-confirmed-7-day-gap-respective-malignancies/110-01-SUMMARY.md`
</output>
