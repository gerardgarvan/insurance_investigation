---
phase: 15-rds-caching-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - .gitignore
autonomous: true
requirements:
  - CACHE-03
  - GIT-01
  - GIT-02

must_haves:
  truths:
    - "User can see CONFIG$cache$cache_dir set to /blue/erin.mobley-hl.bcu/clean/rds/raw in 00_config.R"
    - "User can see CONFIG$cache$force_reload defaulting to FALSE in 00_config.R"
    - "User can see comment next to cache_dir noting it is gitignored and must not be a repo-internal path"
    - "User can see /blue/erin.mobley-hl.bcu/clean/ in .gitignore"
  artifacts:
    - path: "R/00_config.R"
      provides: "Cache configuration entries in CONFIG list"
      contains: "cache_dir"
    - path: ".gitignore"
      provides: "Git exclusion for blue storage RDS cache"
      contains: "/blue/erin.mobley-hl.bcu/clean/"
  key_links:
    - from: "R/00_config.R"
      to: "R/01_load_pcornet.R"
      via: "CONFIG$cache$cache_dir and CONFIG$cache$force_reload referenced by loader"
      pattern: "CONFIG\\$cache"
---

<objective>
Add RDS cache configuration entries to 00_config.R and gitignore the cache directory.

Purpose: Establish the configuration foundation that Plan 02 depends on. The loader function in 01_load_pcornet.R will reference CONFIG$cache$cache_dir and CONFIG$cache$force_reload. The .gitignore exclusion prevents large binary RDS files from being committed.

Output: Updated 00_config.R with cache settings, updated .gitignore with blue storage exclusion.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-rds-caching-infrastructure/15-CONTEXT.md

@R/00_config.R
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONFIG$cache settings to 00_config.R and update .gitignore</name>
  <files>R/00_config.R, .gitignore</files>
  <read_first>R/00_config.R, .gitignore</read_first>
  <action>
**In R/00_config.R:**

Add a new `cache` entry to the CONFIG list. Insert it AFTER the existing `performance` entry (after line 39, before the closing `)` of the CONFIG list on line 40). The CONFIG list closing paren on line 40 needs to move down to accommodate the new entry.

The CONFIG list currently ends at line 40 with `)`. Modify the `performance` entry to add a comma after its closing paren, then add the cache block:

```r
CONFIG <- list(
  # Data directory: Raw PCORnet CDM CSV files (Mailhot HL cohort extract 2025-09-15)
  data_dir = "/orange/erin.mobley-hl.bcu/Mailhot_V1_20250915",

  # Project directory: R scripts and workspace
  project_dir = "/blue/erin.mobley-hl.bcu/R",

  # Output directory: Figures, tables, cohort files
  output_dir = "output",

  # Performance tuning (vroom multi-threaded CSV loading)
  # Match to SLURM --cpus-per-task allocation (Open OnDemand RStudio: 16 cores)
  performance = list(
    num_threads = 16
  ),

  # ---------------------------------------------------------------------------
  # RDS Cache Settings (Phase 15)
  # ---------------------------------------------------------------------------
  # Persistent RDS cache for loaded PCORnet tables. After first CSV parse,
  # tables are serialized to .rds files. Subsequent runs load from cache
  # if the .rds file is newer than the source CSV (file.mtime() comparison).
  #
  # IMPORTANT: cache_dir is GITIGNORED and must NOT be a repo-internal path.
  # See .gitignore: /blue/erin.mobley-hl.bcu/clean/ is excluded from git.
  # RDS files are 100MB-2GB each; committing them would break the repository.
  cache = list(
    cache_dir    = "/blue/erin.mobley-hl.bcu/clean/rds/raw",
    force_reload = FALSE   # Set to TRUE to bypass cache and re-parse all CSVs
  )
)
```

Per D-06: Cache directory is `/blue/erin.mobley-hl.bcu/clean/rds/raw/`.
Per CACHE-03: `force_reload` defaults to FALSE.
Per GIT-02: The comment explicitly states cache_dir is gitignored and must not be repo-internal.

**In .gitignore:**

Add the following lines at the end of the file, BEFORE any trailing blank lines:

```
# RDS cache on blue storage (Phase 15 -- large binary files, 100MB-2GB each)
/blue/erin.mobley-hl.bcu/clean/
```

Per GIT-01: Add `/blue/erin.mobley-hl.bcu/clean/` to .gitignore.
  </action>
  <verify>
    <automated>grep -c "cache_dir" R/00_config.R && grep -c "force_reload" R/00_config.R && grep -c "/blue/erin.mobley-hl.bcu/clean/" .gitignore && grep -c "GITIGNORED" R/00_config.R</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains `cache_dir    = "/blue/erin.mobley-hl.bcu/clean/rds/raw"`
    - R/00_config.R contains `force_reload = FALSE`
    - R/00_config.R contains the word "GITIGNORED" in a comment near cache_dir
    - R/00_config.R CONFIG list has a `cache = list(` entry
    - .gitignore contains `/blue/erin.mobley-hl.bcu/clean/`
    - R/00_config.R still sources utils at the end (source("R/utils_dates.R"), etc.)
    - R/00_config.R CONFIG$analysis still exists after CONFIG list (line 264+ area unchanged)
  </acceptance_criteria>
  <done>CONFIG$cache$cache_dir and CONFIG$cache$force_reload exist in 00_config.R with documentation comment. .gitignore excludes blue storage cache directory.</done>
</task>

</tasks>

<verification>
- `grep "cache_dir" R/00_config.R` returns line with `/blue/erin.mobley-hl.bcu/clean/rds/raw`
- `grep "force_reload" R/00_config.R` returns line with `FALSE`
- `grep "GITIGNORED" R/00_config.R` returns comment line near cache_dir
- `grep "/blue/erin.mobley-hl.bcu/clean/" .gitignore` returns the exclusion line
- `grep "CONFIG\\$analysis" R/00_config.R` confirms downstream config sections still present
</verification>

<success_criteria>
- CONFIG$cache$cache_dir points to /blue/erin.mobley-hl.bcu/clean/rds/raw
- CONFIG$cache$force_reload defaults to FALSE
- Cache directory is documented as gitignored in inline comment (GIT-02)
- .gitignore excludes /blue/erin.mobley-hl.bcu/clean/ (GIT-01)
- No existing CONFIG entries or downstream code disrupted
</success_criteria>

<output>
After completion, create `.planning/phases/15-rds-caching-infrastructure/15-01-SUMMARY.md`
</output>
