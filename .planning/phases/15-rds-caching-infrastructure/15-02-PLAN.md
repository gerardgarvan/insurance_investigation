---
phase: 15-rds-caching-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - R/01_load_pcornet.R
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
  - CACHE-04

must_haves:
  truths:
    - "User can see [CACHE HIT] or [CSV PARSE] logged for each table during pipeline startup"
    - "User can see cached RDS files written to CONFIG$cache$cache_dir after first CSV load"
    - "User can see wall-clock time saved per table when loading from cache"
    - "User can set FORCE_RELOAD to TRUE in 00_config.R and see all tables re-parsed from CSV"
    - "User can see TUMOR_REGISTRY_ALL cached as its own separate RDS file"
    - "User can see post-load diagnostics (PROVIDER specialty, LAB_RESULT_CM null rate) skipped on cache hits"
  artifacts:
    - path: "R/01_load_pcornet.R"
      provides: "Cache-check logic in load_pcornet_table() and main loading block"
      contains: "CACHE HIT"
    - path: "R/01_load_pcornet.R"
      provides: "Cache write with csv_parse_seconds attribute"
      contains: "csv_parse_seconds"
    - path: "R/01_load_pcornet.R"
      provides: "TUMOR_REGISTRY_ALL caching in main loading block"
      contains: "TUMOR_REGISTRY_ALL.rds"
  key_links:
    - from: "R/01_load_pcornet.R"
      to: "R/00_config.R"
      via: "CONFIG$cache$cache_dir and CONFIG$cache$force_reload"
      pattern: "CONFIG\\$cache\\$cache_dir"
    - from: "R/01_load_pcornet.R"
      to: "/blue/erin.mobley-hl.bcu/clean/rds/raw/"
      via: "saveRDS() and readRDS() calls"
      pattern: "saveRDS|readRDS"
---

<objective>
Add RDS cache-check and cache-write logic to load_pcornet_table() and the main loading block in 01_load_pcornet.R.

Purpose: This is the core implementation. After this plan, every pipeline run will check for cached RDS files before parsing CSVs, write caches after first parse, log cache hit/miss status with time-savings data, and support FORCE_RELOAD bypass. TUMOR_REGISTRY_ALL (the combined TR1+TR2+TR3 table) gets its own cache entry per D-03.

Output: Modified R/01_load_pcornet.R with cache integration in both the function and the main loading block.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-rds-caching-infrastructure/15-CONTEXT.md
@.planning/phases/15-rds-caching-infrastructure/15-RESEARCH.md
@.planning/phases/15-rds-caching-infrastructure/15-01-SUMMARY.md

@R/01_load_pcornet.R
@R/00_config.R

<interfaces>
<!-- Key contracts from 00_config.R that the executor needs (created by Plan 01). -->

From R/00_config.R (after Plan 01):
```r
CONFIG$cache$cache_dir     # "/blue/erin.mobley-hl.bcu/clean/rds/raw"
CONFIG$cache$force_reload  # FALSE (default)
```

From R/01_load_pcornet.R (current function signature, line 359):
```r
load_pcornet_table <- function(table_name, file_path, col_spec)
```

From R/01_load_pcornet.R (main loading block, line 491):
```r
pcornet <- imap(PCORNET_PATHS, function(path, table_name) {
    spec <- TABLE_SPECS[[table_name]]
    ...
    load_pcornet_table(table_name, path, spec)
})
```

From R/01_load_pcornet.R (TUMOR_REGISTRY_ALL binding, lines 517-531):
```r
tr_tables <- compact(list(pcornet$TUMOR_REGISTRY1, pcornet$TUMOR_REGISTRY2, pcornet$TUMOR_REGISTRY3))
if (length(tr_tables) > 0) {
    pcornet$TUMOR_REGISTRY_ALL <- bind_rows(tr_tables)
    ...
}
```

From R/01_load_pcornet.R (Phase 10 diagnostic logging, lines 534-556):
```r
# PROVIDER: log distinct PROVIDER_SPECIALTY_PRIMARY values (sample of 20)
if (!is.null(pcornet$PROVIDER)) { ... }
# LAB_RESULT_CM: log row count and LAB_LOINC null rate
if (!is.null(pcornet$LAB_RESULT_CM)) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cache-check and cache-write logic to load_pcornet_table()</name>
  <files>R/01_load_pcornet.R</files>
  <read_first>R/01_load_pcornet.R, R/00_config.R</read_first>
  <action>
Modify the `load_pcornet_table()` function (currently lines 359-478) to add two new parameters and integrate cache logic. The function signature changes from:

```r
load_pcornet_table <- function(table_name, file_path, col_spec)
```

to:

```r
load_pcornet_table <- function(table_name, file_path, col_spec,
                                cache_dir = NULL, force_reload = FALSE)
```

**Cache-check logic (insert AFTER the file.exists guard at line 364, BEFORE the vroom() call at line 367):**

After the existing file existence check (`if (!file.exists(file_path)) { ... return(NULL) }`), add the following cache-check block:

```r
  # ---------------------------------------------------------------------------
  # RDS Cache Check (Phase 15: CACHE-01, CACHE-02, CACHE-04)
  # ---------------------------------------------------------------------------
  # Per D-02: file.mtime() comparison only. RDS newer than CSV = cache hit.
  # Per D-01: csv_parse_seconds stored as attr() on cached object.
  # Per D-04: Skip post-load diagnostics on cache hit (data already trusted).
  if (!is.null(cache_dir) && !force_reload) {
    cache_path <- file.path(cache_dir, paste0(table_name, ".rds"))
    if (file.exists(cache_path) && file.mtime(cache_path) > file.mtime(file_path)) {
      cache_start <- Sys.time()
      df <- readRDS(cache_path)
      cache_seconds <- as.numeric(difftime(Sys.time(), cache_start, units = "secs"))

      original_parse_seconds <- attr(df, "csv_parse_seconds")
      if (!is.null(original_parse_seconds)) {
        time_saved <- original_parse_seconds - cache_seconds
        message(glue(
          "[CACHE HIT] {table_name}: {round(cache_seconds, 1)}s (cache) vs ",
          "{round(original_parse_seconds, 1)}s (CSV) -- saved {round(time_saved, 1)}s ",
          "({format(nrow(df), big.mark=',')} rows)"
        ))
      } else {
        message(glue(
          "[CACHE HIT] {table_name}: {round(cache_seconds, 1)}s ",
          "({format(nrow(df), big.mark=',')} rows)"
        ))
      }
      return(df)
    }
  }
```

**Timing wrapper around existing CSV parse (wrap the entire existing vroom + date parsing + validation section):**

Wrap the existing CSV loading code (from the `vroom()` call through the end of the function) with timing:

BEFORE the vroom() call (currently line 367), add:
```r
  # ---------------------------------------------------------------------------
  # CSV Parse (existing logic, now timed for cache metadata)
  # ---------------------------------------------------------------------------
  parse_start <- Sys.time()
```

AFTER the existing load summary message block (currently lines 471-475, the `message(glue("Loaded {table_name}..."))` block), REPLACE the existing summary message with a cache-aware version and add the cache-write logic. The existing block:

```r
  # Print load summary (per D-12)
  message(glue("Loaded {table_name}: {format(nrow(df), big.mark=',')} rows, {ncol(df)} columns"))
  if (n_parse_problems > 0) {
    message(glue("  WARNING: {n_parse_problems} parse failures in {table_name}"))
  }

  return(df)
```

Replace with:

```r
  # ---------------------------------------------------------------------------
  # Cache Write + Load Summary (Phase 15: CACHE-01, CACHE-04)
  # ---------------------------------------------------------------------------
  parse_seconds <- as.numeric(difftime(Sys.time(), parse_start, units = "secs"))

  # Store parse time as attribute (per D-01: readRDS preserves attributes)
  attr(df, "csv_parse_seconds") <- parse_seconds

  # Write to cache if cache_dir is configured
  if (!is.null(cache_dir)) {
    # Auto-create cache directory if it does not exist
    if (!dir.exists(cache_dir)) {
      dir.create(cache_dir, recursive = TRUE, showWarnings = FALSE)
      message(glue("  Created cache directory: {cache_dir}"))
    }
    cache_path <- file.path(cache_dir, paste0(table_name, ".rds"))
    saveRDS(df, cache_path, compress = TRUE)
    message(glue(
      "[CSV PARSE] {table_name}: {round(parse_seconds, 1)}s -- ",
      "{format(nrow(df), big.mark=',')} rows, {ncol(df)} columns -- ",
      "cached to {basename(cache_path)}"
    ))
  } else {
    message(glue(
      "Loaded {table_name}: {format(nrow(df), big.mark=',')} rows, {ncol(df)} columns ",
      "({round(parse_seconds, 1)}s)"
    ))
  }

  if (n_parse_problems > 0) {
    message(glue("  WARNING: {n_parse_problems} parse failures in {table_name}"))
  }

  return(df)
```

**Update the @details section of the function's roxygen-style doc block** (currently lines 354-358) to add cache documentation:

```r
#' @details
#' - Checks file existence before attempting load (warns and returns NULL if missing)
#' - If cache_dir is set and force_reload is FALSE, checks for cached RDS file
#'   that is newer than source CSV (file.mtime comparison per D-02)
#' - On CACHE HIT: loads via readRDS(), logs time saved vs original CSV parse
#' - On CSV PARSE: loads via vroom with explicit col_types, parses dates,
#'   validates numeric ranges, then writes RDS cache with csv_parse_seconds attribute
#' - Prints [CACHE HIT] or [CSV PARSE] status per table
#' - Warns if any parse problems occurred
```

Also update the @param section to document the new parameters:

```r
#' @param cache_dir Character or NULL. Path to RDS cache directory. If NULL, caching disabled.
#' @param force_reload Logical. If TRUE, bypass cache and re-parse CSV. Default FALSE.
```
  </action>
  <verify>
    <automated>grep -c "CACHE HIT" R/01_load_pcornet.R && grep -c "CSV PARSE" R/01_load_pcornet.R && grep -c "csv_parse_seconds" R/01_load_pcornet.R && grep -c "readRDS" R/01_load_pcornet.R && grep -c "saveRDS" R/01_load_pcornet.R && grep -c "cache_dir" R/01_load_pcornet.R && grep -c "force_reload" R/01_load_pcornet.R && grep -c "file.mtime" R/01_load_pcornet.R</automated>
  </verify>
  <acceptance_criteria>
    - R/01_load_pcornet.R function signature contains `cache_dir = NULL, force_reload = FALSE`
    - R/01_load_pcornet.R contains `readRDS(cache_path)` in cache-check block
    - R/01_load_pcornet.R contains `saveRDS(df, cache_path, compress = TRUE)` in cache-write block
    - R/01_load_pcornet.R contains `attr(df, "csv_parse_seconds") <- parse_seconds`
    - R/01_load_pcornet.R contains `file.mtime(cache_path) > file.mtime(file_path)` comparison
    - R/01_load_pcornet.R contains `[CACHE HIT]` in a message() call
    - R/01_load_pcornet.R contains `[CSV PARSE]` in a message() call
    - R/01_load_pcornet.R contains `dir.create(cache_dir, recursive = TRUE` for auto-creation
    - R/01_load_pcornet.R cache check returns early (returns df before reaching vroom)
    - R/01_load_pcornet.R existing vroom(), date parsing, and validation logic is unchanged
    - R/01_load_pcornet.R file.exists(file_path) guard still returns NULL for missing CSVs
  </acceptance_criteria>
  <done>load_pcornet_table() checks for cached RDS before CSV parse, writes cache after parse with csv_parse_seconds attribute, logs [CACHE HIT] or [CSV PARSE] with timing data, and supports force_reload bypass.</done>
</task>

<task type="auto">
  <name>Task 2: Update main loading block to pass cache params and cache TUMOR_REGISTRY_ALL</name>
  <files>R/01_load_pcornet.R</files>
  <read_first>R/01_load_pcornet.R</read_first>
  <action>
Modify the main loading block (currently lines 484-557) to:

1. **Pass cache_dir and force_reload to load_pcornet_table() calls**
2. **Cache TUMOR_REGISTRY_ALL separately per D-03**
3. **Skip Phase 10 diagnostic logging on cache hits per D-04**

**Step 1: Update the imap() call (currently line 491-498)**

Change from:
```r
  pcornet <- imap(PCORNET_PATHS, function(path, table_name) {
    spec <- TABLE_SPECS[[table_name]]
    if (is.null(spec)) {
      message(glue("WARNING: No col_types spec defined for {table_name}. Using .default = col_character()."))
      spec <- cols(.default = col_character())
    }
    load_pcornet_table(table_name, path, spec)
  })
```

To:
```r
  # Read cache settings from CONFIG (Plan 01 added CONFIG$cache)
  cache_dir    <- CONFIG$cache$cache_dir
  force_reload <- CONFIG$cache$force_reload

  pcornet <- imap(PCORNET_PATHS, function(path, table_name) {
    spec <- TABLE_SPECS[[table_name]]
    if (is.null(spec)) {
      message(glue("WARNING: No col_types spec defined for {table_name}. Using .default = col_character()."))
      spec <- cols(.default = col_character())
    }
    load_pcornet_table(table_name, path, spec,
                       cache_dir = cache_dir, force_reload = force_reload)
  })
```

**Step 2: Add TUMOR_REGISTRY_ALL caching (after the existing bind_rows block)**

The existing TR_ALL binding block (currently lines 517-531) stays unchanged. AFTER it, add caching logic for the combined table. Insert after the `message(glue("Combined TUMOR_REGISTRY_ALL: ..."))` line and before the `} else {` branch:

```r
    # Cache TUMOR_REGISTRY_ALL separately (per D-03)
    if (!is.null(cache_dir)) {
      tr_all_cache_path <- file.path(cache_dir, "TUMOR_REGISTRY_ALL.rds")

      # Sum CSV parse times from individual TR tables for time-savings tracking
      tr_parse_total <- sum(c(
        attr(pcornet$TUMOR_REGISTRY1, "csv_parse_seconds"),
        attr(pcornet$TUMOR_REGISTRY2, "csv_parse_seconds"),
        attr(pcornet$TUMOR_REGISTRY3, "csv_parse_seconds")
      ), na.rm = TRUE)
      attr(pcornet$TUMOR_REGISTRY_ALL, "csv_parse_seconds") <- tr_parse_total

      saveRDS(pcornet$TUMOR_REGISTRY_ALL, tr_all_cache_path, compress = TRUE)
      message(glue("  Cached TUMOR_REGISTRY_ALL to {basename(tr_all_cache_path)}"))
    }
```

Also add a TUMOR_REGISTRY_ALL cache-check BEFORE the bind_rows block. Insert right before the existing `tr_tables <- compact(...)` line. This checks if a cached TR_ALL exists and all 3 source RDS files are older than it:

```r
  # --------------------------------------------------------------------------
  # COMBINED TUMOR REGISTRY (Phase 14: consolidate repeated bind_rows)
  # --------------------------------------------------------------------------
  # Phase 15: Check for cached TUMOR_REGISTRY_ALL before re-binding
  tr_all_from_cache <- FALSE
  if (!is.null(cache_dir) && !force_reload) {
    tr_all_cache_path <- file.path(cache_dir, "TUMOR_REGISTRY_ALL.rds")
    if (file.exists(tr_all_cache_path)) {
      # TR_ALL cache is valid if it's newer than all individual TR source CSVs
      tr_csv_paths <- PCORNET_PATHS[c("TUMOR_REGISTRY1", "TUMOR_REGISTRY2", "TUMOR_REGISTRY3")]
      tr_csv_paths <- tr_csv_paths[file.exists(tr_csv_paths)]
      tr_all_mtime <- file.mtime(tr_all_cache_path)

      if (length(tr_csv_paths) == 0 || all(tr_all_mtime > file.mtime(tr_csv_paths))) {
        cache_start <- Sys.time()
        pcornet$TUMOR_REGISTRY_ALL <- readRDS(tr_all_cache_path)
        cache_seconds <- as.numeric(difftime(Sys.time(), cache_start, units = "secs"))

        original_parse_seconds <- attr(pcornet$TUMOR_REGISTRY_ALL, "csv_parse_seconds")
        if (!is.null(original_parse_seconds)) {
          time_saved <- original_parse_seconds - cache_seconds
          message(glue(
            "[CACHE HIT] TUMOR_REGISTRY_ALL: {round(cache_seconds, 1)}s (cache) vs ",
            "{round(original_parse_seconds, 1)}s (CSV) -- saved {round(time_saved, 1)}s ",
            "({format(nrow(pcornet$TUMOR_REGISTRY_ALL), big.mark=',')} rows)"
          ))
        } else {
          message(glue(
            "[CACHE HIT] TUMOR_REGISTRY_ALL: {round(cache_seconds, 1)}s ",
            "({format(nrow(pcornet$TUMOR_REGISTRY_ALL), big.mark=',')} rows)"
          ))
        }
        tr_all_from_cache <- TRUE
      }
    }
  }

  if (!tr_all_from_cache) {
    # Original bind_rows logic (only runs if not loaded from cache)
    tr_tables <- compact(list(
      pcornet$TUMOR_REGISTRY1,
      pcornet$TUMOR_REGISTRY2,
      pcornet$TUMOR_REGISTRY3
    ))
    if (length(tr_tables) > 0) {
      pcornet$TUMOR_REGISTRY_ALL <- bind_rows(tr_tables)
      message(glue(
        "Combined TUMOR_REGISTRY_ALL: {format(nrow(pcornet$TUMOR_REGISTRY_ALL), big.mark=',')} rows ",
        "from {length(tr_tables)} TR tables"
      ))

      # Cache TUMOR_REGISTRY_ALL separately (per D-03)
      if (!is.null(cache_dir)) {
        tr_all_cache_path <- file.path(cache_dir, "TUMOR_REGISTRY_ALL.rds")
        tr_parse_total <- sum(c(
          attr(pcornet$TUMOR_REGISTRY1, "csv_parse_seconds"),
          attr(pcornet$TUMOR_REGISTRY2, "csv_parse_seconds"),
          attr(pcornet$TUMOR_REGISTRY3, "csv_parse_seconds")
        ), na.rm = TRUE)
        attr(pcornet$TUMOR_REGISTRY_ALL, "csv_parse_seconds") <- tr_parse_total
        saveRDS(pcornet$TUMOR_REGISTRY_ALL, tr_all_cache_path, compress = TRUE)
        message(glue("  Cached TUMOR_REGISTRY_ALL to {basename(tr_all_cache_path)}"))
      }
    } else {
      pcornet$TUMOR_REGISTRY_ALL <- NULL
      message("No TUMOR_REGISTRY tables loaded -- TUMOR_REGISTRY_ALL is NULL")
    }
  }
```

This REPLACES the entire existing TUMOR_REGISTRY_ALL section (lines 511-531). The old commented header, tr_tables, bind_rows, and message calls are all subsumed into the new conditional block.

**Step 3: Conditionally skip Phase 10 diagnostic logging on cache hits (per D-04)**

The existing diagnostic logging (lines 534-556) runs unconditionally. Wrap it in a condition that checks whether ALL tables came from cache. The simplest approach: track whether any table was parsed from CSV (not cache). Add a variable to detect this:

After the imap() call and before the diagnostic logging section, add:

```r
  # Detect if all tables came from cache (for D-04: skip diagnostics on cache hits)
  any_csv_parsed <- any(sapply(pcornet, function(tbl) {
    if (is.null(tbl)) return(FALSE)
    # Tables parsed from CSV have csv_parse_seconds set in this session
    # Cache hits have it from a prior session. Check if it was recently set
    # by comparing to a threshold (if attr exists and was just set, parse happened)
    # Simpler approach: cache hits return early before saveRDS, so csv_parse_seconds
    # is always present. We need a different signal.
    FALSE
  }))
```

Actually, a simpler approach: if `force_reload` is FALSE and all loaded tables have RDS files in cache_dir, diagnostics were already validated on first run. Just guard the diagnostic block on `force_reload`:

Wrap the existing Phase 10 diagnostic logging in:

```r
  # --------------------------------------------------------------------------
  # Phase 10 diagnostic logging: PROVIDER specialty values and LAB_LOINC null rate
  # --------------------------------------------------------------------------
  # Per D-04: Skip diagnostic logging on cache hits (informational, noisy on repeat runs)
  # Diagnostics run on first load (CSV PARSE) or when FORCE_RELOAD is TRUE
  if (force_reload || !file.exists(file.path(cache_dir, "PROVIDER.rds"))) {
```

Wait, that's fragile. The cleanest approach per D-04 is: track a flag during loading. Add this before the imap():

```r
  # Track whether any table was parsed from CSV (not loaded from cache)
  # Used by D-04 to skip diagnostic logging when all tables came from cache
  tables_parsed_from_csv <- character(0)
```

Then in load_pcornet_table(), we cannot easily set an outer variable. Instead, after imap(), check which tables have a freshly-created cache file (mtime within last 60 seconds) -- still fragile.

**Simplest correct approach:** After the imap() completes, check if PROVIDER.rds and LAB_RESULT_CM.rds were cache hits by checking if their RDS mtime is older than 5 seconds ago (meaning they existed before this run). But this is fragile.

**Best approach per D-04:** The diagnostics are only useful on first load. Just check if force_reload is TRUE or if the cache directory didn't exist before this run. BUT the simplest and most aligned with D-04 is:

Guard with: run diagnostics only if `force_reload` is TRUE OR if cache_dir is NULL (caching disabled) OR if any of the relevant RDS files don't exist yet (first run). This is correct because:
- First run: no RDS files exist, diagnostics run
- Subsequent runs with cache: RDS files exist, diagnostics skip
- FORCE_RELOAD: explicitly requested, diagnostics run

```r
  # --------------------------------------------------------------------------
  # Phase 10 diagnostic logging: PROVIDER specialty values and LAB_LOINC null rate
  # --------------------------------------------------------------------------
  # Per D-04: Skip on cache hits. Run on first load or FORCE_RELOAD.
  run_diagnostics <- force_reload ||
    is.null(cache_dir) ||
    !file.exists(file.path(cache_dir, "PROVIDER.rds")) ||
    !file.exists(file.path(cache_dir, "LAB_RESULT_CM.rds"))

  if (run_diagnostics) {
    # PROVIDER: log distinct PROVIDER_SPECIALTY_PRIMARY values (sample of 20)
    if (!is.null(pcornet$PROVIDER)) {
      message("\n[PROVIDER] Distinct PROVIDER_SPECIALTY_PRIMARY values (sample up to 20):")
      pcornet$PROVIDER %>%
        dplyr::distinct(PROVIDER_SPECIALTY_PRIMARY) %>%
        dplyr::slice_head(n = 20) %>%
        print()
    }

    # LAB_RESULT_CM: log row count and LAB_LOINC null rate
    if (!is.null(pcornet$LAB_RESULT_CM)) {
      n_total <- nrow(pcornet$LAB_RESULT_CM)
      n_na_loinc <- sum(is.na(pcornet$LAB_RESULT_CM$LAB_LOINC))
      message(glue(
        "\n[LAB_RESULT_CM] {format(n_total, big.mark=',')} rows loaded, ",
        "{n_na_loinc} ({round(100 * n_na_loinc / n_total, 1)}%) with NA LAB_LOINC"
      ))
    }
  }
```

This replaces the existing unconditional diagnostic block (lines 534-556).

**Summary of all changes to the main loading block:**

1. Add `cache_dir` and `force_reload` extraction from CONFIG before imap()
2. Pass `cache_dir` and `force_reload` to `load_pcornet_table()` in imap()
3. Replace TUMOR_REGISTRY_ALL section with cache-aware version (check cache first, bind_rows as fallback, cache result)
4. Wrap Phase 10 diagnostic logging in `run_diagnostics` condition
  </action>
  <verify>
    <automated>grep -c "cache_dir = cache_dir" R/01_load_pcornet.R && grep -c "force_reload = force_reload" R/01_load_pcornet.R && grep -c "TUMOR_REGISTRY_ALL.rds" R/01_load_pcornet.R && grep -c "run_diagnostics" R/01_load_pcornet.R && grep -c "tr_all_from_cache" R/01_load_pcornet.R</automated>
  </verify>
  <acceptance_criteria>
    - R/01_load_pcornet.R imap() call passes `cache_dir = cache_dir, force_reload = force_reload` to load_pcornet_table()
    - R/01_load_pcornet.R contains `cache_dir <- CONFIG$cache$cache_dir` before imap()
    - R/01_load_pcornet.R contains `force_reload <- CONFIG$cache$force_reload` before imap()
    - R/01_load_pcornet.R contains TUMOR_REGISTRY_ALL cache-check block with `tr_all_from_cache` flag
    - R/01_load_pcornet.R contains `saveRDS(pcornet$TUMOR_REGISTRY_ALL, tr_all_cache_path` in the non-cached branch
    - R/01_load_pcornet.R contains `attr(pcornet$TUMOR_REGISTRY_ALL, "csv_parse_seconds")` assignment
    - R/01_load_pcornet.R contains `run_diagnostics` guard around PROVIDER and LAB_RESULT_CM diagnostic blocks
    - R/01_load_pcornet.R `run_diagnostics` includes `force_reload` and null cache_dir conditions
    - R/01_load_pcornet.R still has the `exists("pcornet", envir = .GlobalEnv)` session-level guard at the top of main block
    - R/01_load_pcornet.R loading summary section (loaded_tables, skipped_tables) is unchanged
  </acceptance_criteria>
  <done>Main loading block passes cache settings to load_pcornet_table(), TUMOR_REGISTRY_ALL has its own cache entry with summed parse times, and diagnostic logging is skipped on cache hits.</done>
</task>

</tasks>

<verification>
After both tasks, the full cache flow should work as follows:

**First run (no RDS files exist):**
1. Each table: `file.exists(cache_path)` is FALSE -> vroom CSV parse -> `saveRDS()` -> logs `[CSV PARSE] TABLE: X.Xs -- N rows, M cols -- cached to TABLE.rds`
2. TUMOR_REGISTRY_ALL: `tr_all_from_cache` is FALSE -> bind_rows -> `saveRDS()` -> logs cache message
3. Diagnostics: `run_diagnostics` is TRUE (RDS files just created, but check is on pre-existence) -> PROVIDER and LAB_RESULT_CM diagnostics run

**Second run (RDS files exist, FORCE_RELOAD = FALSE):**
1. Each table: `file.mtime(rds) > file.mtime(csv)` is TRUE -> `readRDS()` -> logs `[CACHE HIT] TABLE: X.Xs (cache) vs Y.Ys (CSV) -- saved Z.Zs`
2. TUMOR_REGISTRY_ALL: `tr_all_from_cache` is TRUE -> `readRDS()` -> logs cache hit
3. Diagnostics: `run_diagnostics` is FALSE -> skipped

**FORCE_RELOAD = TRUE:**
1. Each table: `force_reload` is TRUE -> skips cache check -> vroom CSV parse -> `saveRDS()` -> logs `[CSV PARSE]`
2. TUMOR_REGISTRY_ALL: `force_reload` is TRUE -> bind_rows -> `saveRDS()` -> re-caches
3. Diagnostics: `force_reload` is TRUE -> diagnostics run

Verification commands:
- `grep "CACHE HIT" R/01_load_pcornet.R` shows cache-check message strings
- `grep "CSV PARSE" R/01_load_pcornet.R` shows cache-miss message strings
- `grep "csv_parse_seconds" R/01_load_pcornet.R` shows attribute storage and retrieval
- `grep "TUMOR_REGISTRY_ALL.rds" R/01_load_pcornet.R` shows TR_ALL cache path
- `grep "run_diagnostics" R/01_load_pcornet.R` shows conditional diagnostic guard
- `grep "force_reload" R/01_load_pcornet.R` shows FORCE_RELOAD integration
</verification>

<success_criteria>
- load_pcornet_table() accepts cache_dir and force_reload parameters (CACHE-03)
- Cache-check uses file.mtime() comparison per D-02 (CACHE-02)
- Cache hits load via readRDS() and log [CACHE HIT] with time saved (CACHE-02, CACHE-04)
- Cache misses parse CSV, store csv_parse_seconds as attr(), and write RDS (CACHE-01)
- TUMOR_REGISTRY_ALL has its own separate RDS cache per D-03 (CACHE-01)
- Post-load diagnostics skip on cache hits per D-04 (CACHE-02)
- FORCE_RELOAD bypasses all cache checks when TRUE (CACHE-03)
- All existing load behavior unchanged when cache_dir is NULL
</success_criteria>

<output>
After completion, create `.planning/phases/15-rds-caching-infrastructure/15-02-SUMMARY.md`
</output>
