---
phase: 139-zip9-approximation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/utils/utils_address.R
  - tests/testthat/test-utils_address_approximate_zip9.R
autonomous: false
requirements: [ZIP9-APPROX-01]

must_haves:
  truths:
    - "get_zip9_at_date() emits non-NA ZIP5 for address records whose ADDRESS_ZIP9 held a bare 5-digit value"
    - "approximate_zip9() returns the input columns plus zip9_source, zip5_modal_freq, zip5_n_candidates, zip5_modal_share"
    - "match_type is never overwritten by approximate_zip9(); temporal provenance survives approximation"
    - "zip9_source distinguishes zip9_observed / zip5_modal / zip5_no_zip9 / no_zip5 / none"
    - "Modal frequency counts distinct patient IDs, not address-history rows"
    - "Tie-breaking is total: freq, then recency, then ZIP9 string"
    - "An integration test drives the real get_zip9_at_date() from a synthetic CSV and asserts at least one zip5_modal row"
    - "When LDS_ADDRESS_HISTORY_Mailhot_V1.csv is absent, the function logs and returns input unchanged without stopping"
  artifacts:
    - path: R/utils/utils_address.R
      provides: "approximate_zip9() function appended to existing file; get_zip9_at_date() patched for ZIP5 recovery"
      contains: "approximate_zip9"
  key_links:
    - from: "approximate_zip9()"
      to: "LDS_ADDRESS_HISTORY_Mailhot_V1.csv"
      via: "probe-first gate then vroom load on demand (D-08)"
    - from: "zip5_lookup (modal table)"
      to: "result_tbl rows where ZIP9 is NA and match_type != 'none'"
      via: "left_join on ZIP5"
---

<!-- PATCH APPLIED: 139-01-PATCH.md — AMEND-01 through AMEND-08 -->

<objective>
Extend `R/utils/utils_address.R` with two changes:

1. **Task 0 (AMEND-01 gate):** Fix ZIP5 derivation in `get_zip9_at_date()` so rows with a
   bare 5-digit `ADDRESS_ZIP9` emit a non-NA `ZIP5`. Confirm the recovery count is non-trivial
   before proceeding. Stop and report if count is zero.

2. **Task 1:** Append `approximate_zip9(result_tbl)` — fills ZIP9 where a modal ZIP9 exists
   for a ZIP5, adds four provenance/confidence columns, and never mutates `match_type`.

3. **Task 2:** Write `tests/testthat/test-utils_address_approximate_zip9.R` with an
   integration test (Test 0) as the phase acceptance gate, plus unit tests for all branches.

Callers chain: `get_zip9_at_date(ids, dates) |> approximate_zip9()`

`autonomous: false` — Task 0's gate is a human decision point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/139-zip9-approximation/139-CONTEXT.md

# Source file to extend -- read in full before writing
@R/utils/utils_address.R

# Modal + recency tie-break pattern (lines 597-609) and probe-first gate
@R/106_zip_change_frequency.R

# Probe-first gate for LDS_ADDRESS_HISTORY (lines 65-82)
@R/114_zip9_temporal_lookup.R

<interfaces>
<!-- Existing contracts the executor must respect. Extracted from R/utils/utils_address.R. -->

<!-- 5-column tibble schema returned by get_zip9_at_date() — approximate_zip9() must
     accept this and return input columns + 4 new columns: -->
<!-- input columns: ID (chr), query_date (Date), ZIP9 (chr), ZIP5 (chr), match_type (chr) -->
<!-- added columns: zip9_source (chr), zip5_modal_freq (int), zip5_n_candidates (int), zip5_modal_share (dbl) -->
<!-- match_type values already in use: "interval" | "most_recent_before" | "none" -->
<!-- match_type is NEVER modified by approximate_zip9() -->

<!-- Normalization helpers already in utils_address.R: -->
<!--   normalize_zip9(zip)       -> chr, NA for non-ZIP9 strings -->
<!--   normalize_zip5(zip9_clean) -> chr, first 5 chars -->
<!--   normalize_zip5_raw(zip)   -> chr, validates bare ZIP5 strings -->

<!-- Modal + recency tie-break pattern from R/106 lines 597-609: -->
<!--   addr_multi %>%                                          -->
<!--     group_by(ID, zip9_norm) %>%                          -->
<!--     summarise(freq = n(),                                 -->
<!--               latest_date = max(period_start_dt, ...)) %>% -->
<!--     group_by(ID) %>%                                     -->
<!--     arrange(desc(freq), desc(latest_date)) %>%           -->
<!--     slice(1)                                             -->
<!-- Adapt this for group_by(zip5_norm) instead of group_by(ID) -->

<!-- Probe-first gate pattern from R/114 lines 65-82: -->
<!--   if (!file.exists(addr_path)) {                         -->
<!--     message(glue("[...] file not found ..."))             -->
<!--     return(result_tbl)   # return input unchanged         -->
<!--   }                                                       -->
</interfaces>
</context>

<tasks>

<task type="human-gated">
  <name>Task 0: Fix ZIP5 derivation in get_zip9_at_date() (AMEND-01 — BLOCKING)</name>
  <files>R/utils/utils_address.R</files>
  <action>

**Step 0a — Confirm the source column, do not assume.**
Print `names(addr_raw)` against the real `LDS_ADDRESS_HISTORY_Mailhot_V1.csv`. Two cases:

- A separate ZIP5-ish column exists alongside `ADDRESS_ZIP9` → coalesce from it
- `ADDRESS_ZIP9` itself holds bare 5-digit strings for some records → coalesce from that

Do NOT hardcode a column named `ADDRESS_ZIP5`. Report which case is true in the summary.

**Step 0b — Repair `normalize_zip5_raw()` (lines 42-46).**
Current logic pads to width 5 *before* validating `^[0-9]{5}$`, so inputs longer than 5
characters return NA rather than their leading digits. Rewrite as:
1. Strip non-digits
2. `str_sub(z, 1, 5)` when `nchar(z) >= 5`
3. Left-pad only when `nchar(z) == 4`
4. Validate `^[0-9]{5}$`

**Step 0c — Patch line 96 (`zip5_norm` derivation).**
```r
zip5_norm = coalesce(normalize_zip5(zip9_norm), normalize_zip5_raw(<confirmed source>))
```
Replace `<confirmed source>` with the column identified in Step 0a.

**Step 0d — Report and gate.**
Log the count of rows where `ZIP9` is NA and `ZIP5` is now non-NA. **STOP and report to
the user if that count is zero or immaterial.** The premise of the phase fails if ZIP5
recovery yields nothing; Task 1 has nothing to operate on.

**Step 0e — Regression check.**
Every row that previously had a non-NA ZIP9 must be unchanged. Run Phase 137 tests and
confirm they still pass before proceeding.

  </action>
  <verify>
    <automated>Rscript -e "testthat::test_dir('tests/testthat', filter = 'utils_address')"</automated>
  </verify>
  <done>
    - ZIP5 recovery count is logged and non-trivial (or user has approved proceeding after a zero count)
    - All Phase 137 tests still pass
    - Source column confirmed and documented in the summary
  </done>
</task>

<task type="auto">
  <name>Task 1: Implement approximate_zip9() in utils_address.R</name>
  <files>R/utils/utils_address.R</files>
  <action>
Append `approximate_zip9()` to the end of `R/utils/utils_address.R`. Do NOT modify any
existing function other than the Task 0 patches already applied. Follow the existing file's
roxygen comment style and section conventions.

Implementation requirements (all decisions are locked):

**Function signature:**
```r
approximate_zip9 <- function(result_tbl) { ... }
```

**Column existence guard (AMEND-05a):**
```r
req <- c("ID", "ADDRESS_ZIP9", "ADDRESS_PERIOD_START")
missing <- setdiff(req, names(addr_raw))
if (length(missing) > 0) {
  message(glue("[utils_address] approximate_zip9: missing column(s) {paste(missing, collapse=', ')} -- returning input unchanged"))
  return(result_tbl)
}
```
Return unchanged rather than `stop()`, consistent with the probe-first gate philosophy.

**D-08 — Probe-first gate (implement first, before any load):**
- Build `addr_path` from `file.path(CONFIG$data_dir, "LDS_ADDRESS_HISTORY_Mailhot_V1.csv")`
- If `!file.exists(addr_path)`, emit:
  `message("[utils_address] approximate_zip9: LDS_ADDRESS_HISTORY not found at {addr_path} -- returning input unchanged")`
  and `return(result_tbl)`.

**D-01 / D-02 — Identify rows to approximate (AMEND-05d):**
```r
to_approx <- result_tbl |> filter(is.na(ZIP9), !is.na(match_type) & match_type != "none")
```
If `nrow(to_approx) == 0`, return `result_tbl` unchanged (no work needed; no file load).

**D-05 — Load full LDS_ADDRESS_HISTORY (load on demand):**
Use the same `tryCatch(vroom::vroom(...), error = function(e) read.csv(...))` pattern from
`get_zip9_at_date()`. Load with `col_types = vroom::cols(.default = "c")`.
The `read.csv` fallback must carry `colClasses = "character"` (AMEND-05c — prevents leading-zero
stripping on ZIP codes for New England ZIPs).

**D-03 / D-04 — Build ZIP5→modal-ZIP9 lookup (AMEND-04 — corrected block):**
```r
zip5_lookup <- addr_raw %>%
  mutate(
    zip9_norm       = normalize_zip9(ADDRESS_ZIP9),
    zip5_norm       = normalize_zip5(zip9_norm),
    period_start_dt = parse_pcornet_date(ADDRESS_PERIOD_START)
  ) %>%
  filter(!is.na(zip9_norm), !is.na(zip5_norm), !is.na(period_start_dt)) %>%
  group_by(zip5_norm, zip9_norm) %>%
  summarise(
    freq        = n_distinct(ID),          # patients, not rows (AMEND-04a)
    latest_date = max(period_start_dt),    # na.rm not needed; NAs filtered above (AMEND-04c)
    .groups     = "drop"
  ) %>%
  group_by(zip5_norm) %>%
  mutate(
    n_candidates = n(),
    modal_share  = freq / sum(freq)        # approximate; patients may appear at >1 ZIP9
  ) %>%
  arrange(desc(freq), desc(latest_date), zip9_norm, .by_group = TRUE) %>%  # total tie-break (AMEND-04b)
  slice(1) %>%
  ungroup() %>%
  select(
    ZIP5              = zip5_norm,
    modal_zip9        = zip9_norm,
    zip5_modal_freq   = freq,
    zip5_n_candidates = n_candidates,
    zip5_modal_share  = modal_share
  )
```

**Memoisation (AMEND-06):**
Memoise `zip5_lookup` keyed on `file.path(addr_path)`, `file.mtime(addr_path)`, and
`file.size(addr_path)`. This avoids rebuilding the lookup on every call when the file
is unchanged. The mtime/size key forces a rebuild when `CONFIG$data_dir` changes (which
makes Test 5 / the probe-first gate test work correctly).

**D-06 / D-07 — Apply lookup and classify (AMEND-02, AMEND-03):**
Join `to_approx` against `zip5_lookup` on `ZIP5`, then classify into `zip9_source`:

| Condition | `ZIP9` action | `zip9_source` |
|---|---|---|
| Input ZIP9 already present | unchanged | `"zip9_observed"` |
| `modal_zip9` not NA | `ZIP9 = modal_zip9` | `"zip5_modal"` |
| ZIP5 present, no modal ZIP9 | `ZIP9` stays NA | `"zip5_no_zip9"` |
| ZIP9 NA, ZIP5 NA, `match_type != "none"` | `ZIP9` stays NA | `"no_zip5"` |
| `match_type == "none"` | unchanged | `"none"` |

**`match_type` is never written by `approximate_zip9()`** — it retains its Phase 137 values
on every row (AMEND-03).

The diagnostic columns (`zip5_modal_freq`, `zip5_n_candidates`, `zip5_modal_share`) are `NA`
for rows with `zip9_source == "zip9_observed"` or `"none"`.

**Drop join column (AMEND-05b):**
Before `bind_rows()` reassembly, explicitly `select(-modal_zip9)` so the column does not
appear in output.

Reconstruct full result by `bind_rows()` of unchanged rows and updated rows.
Return in the same sort order as the input: `arrange(ID, query_date)`.

**Schema returned:**
Input columns + `zip9_source` (chr) + `zip5_modal_freq` (int) + `zip5_n_candidates` (int)
+ `zip5_modal_share` (dbl). No other columns.

**Diagnostic logging (AMEND-08a):**
After computing the updated result, log a breakdown by `zip9_source` plus modal-share
distribution across approximated rows:
```
[utils_address] approximate_zip9: zip9_source breakdown:
  zip9_observed: {n}  zip5_modal: {n}  zip5_no_zip9: {n}  no_zip5: {n}  none: {n}
  modal_share among zip5_modal rows -- min: {min}  median: {median}  max: {max}
```
Use `glue()` and `message()` consistent with the rest of the file.

**Roxygen block (AMEND-08b — document selection bias):**
Add a `#'` block before the function documenting:
- `@param result_tbl` — tibble from `get_zip9_at_date()`
- `@return` — input columns plus four: `zip9_source`, `zip5_modal_freq`, `zip5_n_candidates`,
  `zip5_modal_share`; `ZIP9` filled in place when approximation succeeds
- `zip9_source` values and their meanings
- `match_type` is **unchanged** on all rows
- **Selection bias caveat:** the modal ZIP+4 is the most frequently *recorded* address in the
  EHR, skewing toward congregate/institutional addresses (nursing facilities, large apartment
  complexes, shelters, student housing, facility-address placeholders). `n_distinct(ID)` reduces
  record-churn weighting but does not correct this bias. Downstream ADI/SVI/SDI consumers
  should treat approximated rows with `zip5_modal_share < 0.10` as especially uncertain.
  (AMEND-08c) Note: a future SES phase should restrict modal selection to valid ZIP+4s from
  the Neighborhood Atlas file — this eliminates transcription-error modal wins in sparse ZIP5s.
- `zip5_modal_share` is approximate: `sum(freq)` across a ZIP5's groups can double-count
  patients who appear at more than one ZIP+4 within the same ZIP5.
- Add `# Requirements: Phase 139 -- D-01 through D-08` in the header block.

**Style requirements (must match existing file):**
- Use `%>%` pipe (match predominant style in existing file)
- Use `dplyr` verbs throughout — no base R subsetting
  </action>
  <verify>
    <automated>Rscript -e "source('R/00_config.R'); stopifnot(existsFunction('approximate_zip9')); cat('OK: approximate_zip9 exists\n')"</automated>
  </verify>
  <done>
    - `approximate_zip9` is defined and callable after sourcing `R/00_config.R`
    - Function accepts a tibble from `get_zip9_at_date()` and returns input columns + 4 new columns
    - `match_type` is never modified
    - `zip9_source` covers all five levels
    - `modal_zip9` join column is dropped from output
    - All AMEND-02 through AMEND-08 decisions implemented
    - No existing function in utils_address.R is modified (beyond Task 0 patches)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test approximate_zip9() with synthetic data (AMEND-07)</name>
  <files>tests/testthat/test-utils_address_approximate_zip9.R</files>
  <behavior>
    - **Test 0 (integration — acceptance gate, AMEND-07a):**
      Write a synthetic `LDS_ADDRESS_HISTORY_Mailhot_V1.csv` to a temp dir containing at
      least one record whose `ADDRESS_ZIP9` is a bare 5-digit string, plus several records for
      that ZIP5 carrying valid 9-digit ZIP9s. Point `CONFIG$data_dir` at the temp dir. Call the
      **real** `get_zip9_at_date(ids, dates)`, then pipe into `approximate_zip9()`. Assert
      `sum(zip9_source == "zip5_modal") > 0`. No hand-built tibble anywhere in this test.
    - **Test 1 (zip5_modal):** A row with ZIP9=NA, match_type="interval", ZIP5="12345" where
      LDS_ADDRESS_HISTORY has multiple records for ZIP5 12345 with a clear modal ZIP9
      → after approximate_zip9(), that row has ZIP9 = the modal ZIP9, zip9_source = "zip5_modal",
      match_type unchanged ("interval")
    - **Test 2 (zip5_no_zip9):** A row with ZIP9=NA, ZIP5="99999" where every LDS record for
      ZIP5 99999 has no valid ZIP9 → ZIP9 remains NA, zip9_source = "zip5_no_zip9",
      match_type unchanged
    - **Test 3 (none unchanged):** A row with match_type="none", ZIP9=NA, ZIP5=NA → returned
      unchanged (ZIP9=NA, match_type="none", zip9_source="none")
    - **Test 4 (already filled — zip9_observed):** A row with ZIP9="123456789" already set
      → returned unchanged with same ZIP9 and match_type; zip9_source = "zip9_observed";
      diagnostic columns NA
    - **Test 5 (probe-first gate):** If LDS file is absent (point CONFIG$data_dir at a temp dir
      with no CSV), approximate_zip9() returns the input tibble unchanged and emits a message
      (no stop(), no error)
    - **Test 6 (tie-break — recency):** Two ZIP9s for same ZIP5 with equal frequency → the one
      with the more recent ADDRESS_PERIOD_START wins
    - **Test 7 (schema):** Output column set equals input columns plus exactly the four from
      AMEND-02; `modal_zip9` absent; `nrow(output) == nrow(input)`
    - **Test 8 (determinism):** Two consecutive calls on identical input return `identical()` results
    - **Test 9 (match_type invariance):** `match_type` values are unchanged for every row
    - **Test 10 (no_zip5):** A row with ZIP9=NA, ZIP5=NA, match_type="most_recent_before"
      → zip9_source = "no_zip5"
    - **Test 11 (modal counts patients, not rows):** A fixture where one patient has 10 rows
      at ZIP9-A and three distinct patients each have 1 row at ZIP9-B → modal selection
      picks **B** (3 distinct patients beats 1)
  </behavior>
  <action>
Create `tests/testthat/test-utils_address_approximate_zip9.R` using `testthat` (v3 style:
`test_that(..., { ... })`).

Build synthetic LDS_ADDRESS_HISTORY data inline with `tibble()` — no real file reads. Use
`withr::with_tempdir()` or a temp file to provide a fake CSV that the probe-first gate
will find (or not find for Test 5).

**CONFIG override mechanism (AMEND-07c):**
`CONFIG` is a list in the global environment, NOT an R option. Do NOT use
`withr::local_options()` — it will not work. Use `withr::defer()` to save and restore
`CONFIG$data_dir`, or `withr::local_bindings()` against the environment `CONFIG` lives in.

Run tests with: `testthat::test_file("tests/testthat/test-utils_address_approximate_zip9.R")`
  </action>
  <verify>
    <automated>Rscript -e "testthat::test_file('tests/testthat/test-utils_address_approximate_zip9.R')"</automated>
  </verify>
  <done>
    - Test 0 (integration) passes with at least one zip5_modal row from real get_zip9_at_date()
    - All 12 test cases (0-11) pass with no failures or warnings
    - Tests are self-contained (no HiPerGator paths required)
    - CONFIG override uses withr::defer() or withr::local_bindings(), not withr::local_options()
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Task 0 gate: ZIP5 recovery count reported and non-trivial; Phase 137 tests still pass
2. Source check: `approximate_zip9` exists and is callable after sourcing `R/00_config.R`
3. Schema check: output = input columns + `zip9_source`, `zip5_modal_freq`,
   `zip5_n_candidates`, `zip5_modal_share`; `modal_zip9` absent
4. `match_type` invariance: no row's `match_type` differs from its input value
5. Integration test (Test 0) passes with at least one `zip5_modal` row
6. Determinism: two consecutive runs return `identical()` results
7. All unit tests pass; no existing tests in `tests/testthat/` broken
</verification>

<success_criteria>
- `get_zip9_at_date()` emits usable ZIP5 for ZIP5-only address records (AMEND-01)
- `approximate_zip9()` fills ZIP9 where a modal ZIP9 exists, flagged via `zip9_source`
- `match_type` semantics from Phase 137 are preserved intact
- Confidence signals (`zip5_modal_freq`, `zip5_n_candidates`, `zip5_modal_share`) survive to the caller
- The integration path, not just hand-built fixtures, is under test
- `R/utils/utils_address.R` parses cleanly (no parse errors)
</success_criteria>

<output>
After completion, create `.planning/phases/139-zip9-approximation/139-01-SUMMARY.md` following the summary template.
</output>
