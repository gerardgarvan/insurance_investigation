---
phase: 145-r116-fan-out-fix-and-ses-reference-gap-fill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/reference/README.md
autonomous: true
requirements: [FANOUT-01, ZIP5MODAL-01, SESDOC-01]
must_haves:
  truths:
    - "The committed fan-out fix in get_zip9_at_date() and R/116 SECTION 5 is confirmed correct by a line-anchored audit"
    - "The ZIP5-modal zero-rows code path (n_to_approx early-exit) is traced and the evidence-to-collect at the checkpoint is written down"
    - "data/reference/README.md documents column contracts for SDI, SVI, and ADI reference files"
  artifacts:
    - path: "data/reference/README.md"
      provides: "SDI, SVI, ADI reference file column contracts"
      contains: "## SDI"
  key_links:
    - from: "data/reference/README.md"
      to: "R/116_encounter_ses_index.R SECTION 6"
      via: "documented probe-gate paths and column names match the code"
      pattern: "zip5_sdi_reference|svi_2020_us_by_zcta|neighborhood_atlas_block_group_crosswalk"
---

<objective>
Complete all static, pre-checkpoint work for Phase 145: (1) audit the already-committed
fan-out fix so the HiPerGator checkpoint is a confirmation, not a discovery; (2) trace the
ZIP5-modal zero-rows code path and record exactly what evidence to look for in the R/116
console output; (3) document SDI, SVI, and ADI reference-file column contracts in
data/reference/README.md.

Purpose: The fan-out code fix (commit 0364c89) is done — this plan verifies it is correct
by reading, and prepares the diagnostic evidence checklist so the post-checkpoint plan
(145-03) can branch cleanly. None of this needs HiPerGator.
Output: Updated data/reference/README.md; an audit note captured in the plan SUMMARY (no
code changes in this plan — all four fix sites are already committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-CONTEXT.md
@.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-RESEARCH.md

<interfaces>
<!-- Fan-out fix, already committed (0364c89). Audit target — do NOT re-implement. -->

R/utils/utils_address.R — get_zip9_at_date() (lines 111-242):
```r
# lines 209-211: uncovered built from DISTINCT keys (the fix)
uncovered <- queries %>%
  distinct(ID, query_date) %>%
  anti_join(covered, by = c("ID", "query_date"))
# lines 235-236: contract assertion
stopifnot("get_zip9_at_date: matched has duplicate (ID, query_date) keys" =
            !any(duplicated(matched[c("ID", "query_date")])))
# lines 238-241: return path also dedupes before left_join
queries %>% distinct(ID, query_date) %>%
  left_join(matched, by = c("ID", "query_date")) %>% arrange(ID, query_date)
```

R/116_encounter_ses_index.R — SECTION 5 (lines 128-155):
```r
encounter_zip <- encounters_raw %>%
  left_join(
    zip_resolved %>% dplyr::rename(PATID = ID, ADMIT_DATE = query_date),
    by = c("PATID", "ADMIT_DATE"),
    relationship = "many-to-one"          # line 148
  )
stopifnot("ZIP resolution fanned out -- zip_resolved has duplicate (PATID, ADMIT_DATE) keys" =
            nrow(encounter_zip) == nrow(encounters_raw))   # lines 151-152
```
NOTE: the comment block above line 144 (lines 140-141) contains the phrase
`relationship = "one-to-many"` in prose while the code uses `"many-to-one"`. This is a
stale-comment inconsistency, not a code bug (many-to-one is the correct declaration:
zip_resolved is the "one" side, encounters_raw is the "many" side). Flag it in the audit
note; do NOT change code in this plan (any edit is deferred to 145-03 only if the checkpoint
warrants touching R/116).

<!-- ZIP5-modal path. approximate_zip9() early-exit — the leading hypothesis. -->

R/utils/utils_address.R — approximate_zip9() (lines 415-519):
```r
# lines 442-448: n_to_approx early-exit
n_to_approx <- result_tbl %>%
  filter(is.na(ZIP9), !is.na(match_type), match_type != "none") %>%
  nrow()
if (n_to_approx == 0) {
  return(.classify_zip9_source(result_tbl, .empty_zip5_lookup(), unavailable = FALSE))
}
```

R/utils/utils_address.R — .classify_zip9_source() case_when (lines 288-295):
```r
zip9_source = case_when(
  !is.na(ZIP9)         ~ "zip9_observed",
  is.na(match_type)    ~ "invalid_input",
  match_type == "none" ~ "none",
  unavailable          ~ "reference_unavailable",
  !is.na(modal_zip9)   ~ "zip5_modal",
  TRUE                 ~ ".needs_centroid_check"
)
```
When zip5_lookup is empty, modal_zip9 is NA for every row, so no row reaches "zip5_modal".

<!-- SES probe gates in R/116 SECTION 6, already coded — README must match these. -->

R/116 SECTION 6 SDI gate (lines 187-212): reads SDI_PATH, requires columns `ZIP5` and
`SDI_score`; select(ZIP5, sdi_score = SDI_score); normalize_zip5_raw(ZIP5); join key ZIP5.

R/116 SECTION 6 SVI gate (lines 250-278): reads SVI_PATH, requires columns `ZCTA` and
`RPL_THEMES`; select(ZIP5 = ZCTA, svi_score = RPL_THEMES); filters svi_score < 0 to NA
(CDC -999 missing); join key ZIP5 (ZCTA ~ ZIP5, approximate).

R/116 SECTION 6 ADI gate (lines 214-248): reads ADI_PATH; ZIP9 key = first of
{ZIP9, zip9, ADDRESS_ZIP9, zip9_norm, GEOID9, ZIP_PLUS4}; rank = first of
{adi_natrank, ADI_NATRANK, natrank}; join key ZIP9. ADI_PATH is
`data/reference/neighborhood_atlas_block_group_crosswalk.csv` — the SAME file the
existing Phase 140 README section (lines 10-70) documents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the committed fan-out fix and trace the ZIP5-modal early-exit path</name>
  <read_first>
    - R/utils/utils_address.R (read get_zip9_at_date() lines 111-242 AND approximate_zip9() lines 415-519 AND .classify_zip9_source() lines 278-335)
    - R/116_encounter_ses_index.R (read SECTION 5 lines 128-155 AND SECTION 7 lines 280-318)
  </read_first>
  <action>
    This is a read-and-record audit. Make NO code changes. Produce audit findings that will
    be captured in the plan SUMMARY. Confirm each of these five points against the source at
    HEAD (commit 0364c89 or later), citing the actual line numbers you observe:

    1. get_zip9_at_date(): `uncovered` is built from `queries %>% distinct(ID, query_date) %>% anti_join(...)` (expected ~lines 209-211), NOT from raw `queries`. Confirm the `distinct(ID, query_date)` call is present.
    2. get_zip9_at_date(): the `stopifnot("get_zip9_at_date: matched has duplicate (ID, query_date) keys" = !any(duplicated(matched[c("ID", "query_date")])))` assertion is present after `matched <- bind_rows(...)` (expected ~lines 235-236) and references the column pair c("ID","query_date").
    3. get_zip9_at_date(): the return statement dedupes via `distinct(ID, query_date)` before the final `left_join(matched, ...)` (expected ~lines 238-241).
    4. R/116 SECTION 5: the `left_join` declares `relationship = "many-to-one"` (expected line 148, NOT "many-to-many"), and `stopifnot(... = nrow(encounter_zip) == nrow(encounters_raw))` is present (expected lines 151-152), anchored to `encounters_raw` (not `zip_resolved`).
    5. No OTHER call site of get_zip9_at_date() in the repo bypasses these guards. Run a grep across R/ for `get_zip9_at_date(` and confirm R/116 SECTION 5 is the only non-test caller that feeds encounter-scale data (list any other callers found).

    ALSO record the stale-comment inconsistency: R/116 lines 140-141 use the phrase
    "one-to-many" in prose while line 148 code uses "many-to-one". State in the audit note
    that many-to-one is the CORRECT declaration and the prose is stale; do NOT edit it in
    this plan (comment-only fix, if wanted, is deferred to 145-03's R/116 touch).

    THEN trace the ZIP5-modal path and write the checkpoint evidence checklist:
    - Confirm approximate_zip9() lines 442-448 compute `n_to_approx` as rows where
      `is.na(ZIP9) & !is.na(match_type) & match_type != "none"`, and early-exit with an
      empty zip5_lookup + `unavailable = FALSE` when `n_to_approx == 0`.
    - Confirm .classify_zip9_source() (lines 288-295) only assigns "zip5_modal" when
      `!is.na(modal_zip9)`, and that modal_zip9 comes from the (empty on early-exit) join,
      so an empty lookup => zero zip5_modal rows BY DESIGN.
    - Write the exact diagnostic the checkpoint (145-02) must collect, in words:
      "In the R/116 console output, read the `table(encounter_zip$zip9_source, useNA='ifany')`
      breakdown (printed at R/116 line 155). If `zip5_modal` is 0 AND there are 0 rows with
      match_type in {interval, most_recent_before} that have ZIP9 = NA, then n_to_approx = 0
      and zip5_modal = 0 is DATA-DRIVEN (expected). If rows with match_type in
      {interval, most_recent_before} and ZIP9 = NA exist but zip5_modal is still 0, that is a
      candidate BUG requiring 145-03 investigation."
  </action>
  <verify>
    <automated>MISSING — static audit; verification is that the SUMMARY records all five confirmation points with observed line numbers and the checkpoint evidence checklist. Run `grep -n "distinct(ID, query_date)" R/utils/utils_address.R` and `grep -n "many-to-one" R/116_encounter_ses_index.R` to anchor the citations.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "distinct(ID, query_date)" R/utils/utils_address.R` returns at least 2 matches (uncovered build + return path)
    - `grep -n "!any(duplicated(matched" R/utils/utils_address.R` returns 1 match (the stopifnot)
    - `grep -n "many-to-one" R/116_encounter_ses_index.R` returns 1 match at the SECTION 5 left_join
    - `grep -n "nrow(encounter_zip) == nrow(encounters_raw)" R/116_encounter_ses_index.R` returns 1 match
    - `grep -rn "get_zip9_at_date(" R/` output is enumerated in the SUMMARY with each caller classified (test vs. production)
    - The SUMMARY contains a written checkpoint evidence checklist distinguishing data-driven vs. bug for zip5_modal == 0
    - No files under R/ are modified by this task (`git diff --name-only` shows no R/ changes)
  </acceptance_criteria>
  <done>All five fan-out audit points confirmed with line citations; the stale one-to-many/many-to-one comment inconsistency is recorded; the ZIP5-modal early-exit path is traced; the checkpoint evidence checklist is written to the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Add SDI, SVI, and ADI reference-file column contracts to data/reference/README.md</name>
  <read_first>
    - data/reference/README.md (read the full existing file — especially the Phase 140 ADI/Neighborhood Atlas section at lines 10-70, to match style and avoid duplication)
    - R/116_encounter_ses_index.R (SECTION 6 lines 157-278 — the SDI/SVI/ADI probe gates that these contracts must match exactly)
  </read_first>
  <action>
    Append two NEW H2 sections to data/reference/README.md (SDI and SVI), and EXTEND the
    existing Phase 140 Neighborhood Atlas section (lines 10-70) with a short subsection noting
    R/116 also consumes that same file for ADI — do NOT create a second, duplicate ADI section
    for the same path (`neighborhood_atlas_block_group_crosswalk.csv`), per RESEARCH Pitfall 3
    and CONTEXT D-03 ("update or replace the Phase 140 ADI entry if it is already there").

    Mirror the existing section's style: H2 heading with source + phase tag, then
    **Expected path:**, **Purpose:**, **Source:**, **Expected columns:**, **R/116 probe gate:**,
    **Status as of 2026-08-17:** lines.

    Write these EXACT contracts (values taken verbatim from CONTEXT D-03 and R/116 SECTION 6):

    --- NEW SECTION: SDI ---
    ## SDI — Social Deprivation Index (Robert Graham Center) (Phase 145)

    **Expected path:** `data/reference/zip5_sdi_reference.csv`

    **Purpose:** Supplies R/116 SECTION 6/7's `sdi_score` column (encounter-level SES),
    joined on ZIP5.

    **Source:** Robert Graham Center Social Deprivation Index
    (https://www.graham-center.org/maps-data-tools/social-deprivation-index.html).
    Portal download — not obtainable via CLI/API.

    **Expected columns:**
    - Join key: `ZIP5` (character, 5-digit; normalized via `normalize_zip5_raw()`)
    - Value: `SDI_score` (numeric, 0–100)

    **R/116 probe gate:** SECTION 6 (lines 187-212) checks `has_sdi` (file.exists on
    `SDI_PATH`) and then requires BOTH columns `ZIP5` and `SDI_score` to be present. If the
    file is absent OR either column is missing, `sdi_lookup` is empty and `sdi_score` is
    `NA_real_` for all encounter rows (a console message names the available columns). If the
    staged file uses different column names, update the `select(ZIP5, sdi_score = SDI_score)`
    in R/116 SECTION 6 rather than renaming the source file.

    **Status as of 2026-08-17:** Not staged. `sdi_score` is NA across all rows.

    --- NEW SECTION: SVI ---
    ## SVI — CDC/ATSDR Social Vulnerability Index 2020 (Phase 145)

    **Expected path:** `data/reference/svi_2020_us_by_zcta.csv`

    **Purpose:** Supplies R/116 SECTION 6/7's `svi_score` column, joined on ZIP5 (ZCTA ≈ ZIP5).

    **Source:** CDC/ATSDR SVI 2020, ZCTA-level file, downloaded from the CDC GRASP portal
    (https://www.atsdr.cdc.gov/placeandhealth/svi/). Portal download — not obtainable via CLI/API.

    **Expected columns:**
    - Join key: `ZCTA` (character, 5-digit; ZCTA is an approximation of ZIP5 and differs in
      edge cases — carried in R/116 as `ZIP5 = ZCTA`)
    - Value: `RPL_THEMES` (numeric, composite overall SVI percentile, 0–1). CDC encodes
      missing as `-999`; R/116 maps any value `< 0` to `NA_real_`.

    **R/116 probe gate:** SECTION 6 (lines 250-278) checks `has_svi` and then requires BOTH
    columns `ZCTA` and `RPL_THEMES`. If absent OR either column missing, `svi_lookup` is empty
    and `svi_score` is `NA_real_` for all rows. If the staged file uses different column names,
    update the `select(ZIP5 = ZCTA, svi_score = RPL_THEMES)` in R/116 SECTION 6.

    **Status as of 2026-08-17:** Not staged. `svi_score` is NA across all rows.

    --- EXTEND (do NOT duplicate) the existing Neighborhood Atlas section ---
    Add this subsection at the END of the existing "Neighborhood Atlas ZIP9-to-Block-Group
    Crosswalk (Phase 140, P-03a)" block (after line 70), so both R/115 and R/116 consumers of
    the same file are documented in one place:

    ### Also consumed by R/116 for ADI (Phase 145)

    R/116_encounter_ses_index.R SECTION 6 (lines 214-248) probes this SAME file
    (`data/reference/neighborhood_atlas_block_group_crosswalk.csv`) to supply its
    encounter-level `adi_natrank` column, joined on **ZIP9** (not ZIP5). R/116 accepts the
    ZIP9 key as the first present of `ZIP9`, `zip9`, `ADDRESS_ZIP9`, `zip9_norm`, `GEOID9`,
    `ZIP_PLUS4`, and the rank as the first present of `adi_natrank`, `ADI_NATRANK`, `natrank`.
    Because Tier 3 centroit ZIP9 resolution is inert (144-CONTEXT.md D-01) AND this crosswalk
    is not staged (P-03a pending), `adi_natrank` is entirely NA on current runs. Acquisition
    status is the same as the Phase 140 block-group tier above — not staged as of 2026-08-17.
  </action>
  <verify>
    <automated>grep -c "^## SDI" data/reference/README.md && grep -c "^## SVI" data/reference/README.md && grep -c "consumed by R/116 for ADI" data/reference/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## SDI" data/reference/README.md` returns exactly 1 match
    - `grep -n "^## SVI" data/reference/README.md` returns exactly 1 match
    - `grep -c "zip5_sdi_reference.csv" data/reference/README.md` >= 1
    - `grep -c "svi_2020_us_by_zcta.csv" data/reference/README.md` >= 1
    - `grep -c "RPL_THEMES" data/reference/README.md` >= 1
    - `grep -c "consumed by R/116 for ADI" data/reference/README.md` == 1
    - There is NO second top-level `## ...ADI...` section for `neighborhood_atlas_block_group_crosswalk.csv` — the ADI-for-R/116 note is a subsection (`###`) under the existing Phase 140 block: `grep -c "^## Neighborhood Atlas" data/reference/README.md` == 1
    - Each new section contains the literal strings `**Expected path:**`, `**Expected columns:**`, and `**R/116 probe gate:**`
  </acceptance_criteria>
  <done>data/reference/README.md has SDI and SVI sections matching the existing style, the ADI-for-R/116 note is a subsection of the existing Phase 140 Neighborhood Atlas block (no duplicate path section), and all documented paths/columns match R/116 SECTION 6's probe gates exactly.</done>
</task>

</tasks>

<verification>
- Fan-out fix audit: all five confirmation points recorded with observed line numbers; grep anchors pass.
- ZIP5-modal path traced; checkpoint evidence checklist written to SUMMARY.
- data/reference/README.md: SDI + SVI sections added, ADI note extends the existing Phase 140 block (no duplication), all contracts match R/116 SECTION 6.
- No R/ source files modified in this plan.
</verification>

<success_criteria>
- `git diff --name-only` shows only `data/reference/README.md` changed (no R/ changes).
- README grep acceptance criteria all pass.
- The SUMMARY carries the audit findings and the checkpoint evidence checklist that 145-02 and 145-03 consume.
</success_criteria>

<output>
After completion, create `.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-01-SUMMARY.md`
</output>
