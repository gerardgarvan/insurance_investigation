---
phase: 145-r116-fan-out-fix-and-ses-reference-gap-fill
plan: 03
type: execute
wave: 3
depends_on: ["145-02"]
files_modified:
  - R/utils/utils_address.R
  - R/116_encounter_ses_index.R
  - data/reference/README.md
autonomous: false
requirements: [FANOUT-01, ZIP5MODAL-01]
must_haves:
  truths:
    - "The ZIP5-modal zero-rows outcome is diagnosed as either data-driven or a code bug, using the HiPerGator zip9_source breakdown"
    - "If data-driven: a console note and/or README note documents that zip5_modal == 0 is expected; if a bug: the defect is fixed and re-run"
    - "The corrected encounter_ses_index RDS and summary workbook are regenerated on HiPerGator (or the regeneration is explicitly scheduled as a HiPerGator follow-up)"
  artifacts:
    - path: "R/116_encounter_ses_index.R"
      provides: "Regenerated corrected output on HiPerGator; optional diagnostic/comment fix"
    - path: "data/reference/README.md"
      provides: "ZIP5-modal diagnosis note (if data-driven)"
  key_links:
    - from: "approximate_zip9() n_to_approx early-exit"
      to: "zip9_source == zip5_modal breakdown"
      via: "diagnosis of why the modal tier fired zero rows"
      pattern: "n_to_approx|zip5_modal"
---

<objective>
Post-checkpoint work: (1) diagnose the ZIP5-modal zero-rows outcome from the 145-02 console
output using the D-02 decision tree; (2) conditionally fix (if a code bug) or document (if
data-driven); (3) regenerate the corrected encounter_ses_index RDS and summary workbook on
HiPerGator with the fixed code.

Purpose: 145-01 traced the code path; 145-02 provided the real zip9_source breakdown. This
plan makes the data-driven-vs-bug call with evidence in hand and produces the final clean
outputs.
Output: A diagnosis (README/console note if data-driven, or a code fix if a bug); regenerated
output/encounter_ses_index_YYYYMMDD.rds and output/encounter_ses_index_summary_YYYYMMDD.xlsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-CONTEXT.md
@.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-RESEARCH.md
@.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-01-SUMMARY.md
@.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-02-SUMMARY.md

<interfaces>
approximate_zip9() early-exit — R/utils/utils_address.R lines 442-448:
```r
n_to_approx <- result_tbl %>%
  filter(is.na(ZIP9), !is.na(match_type), match_type != "none") %>%
  nrow()
if (n_to_approx == 0) {
  return(.classify_zip9_source(result_tbl, .empty_zip5_lookup(), unavailable = FALSE))
}
```
R/116 line 154-155 prints `table(encounter_zip$zip9_source, useNA = "ifany")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose ZIP5-modal zero-rows (D-02 decision tree) and conditionally fix or document</name>
  <read_first>
    - .planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-02-SUMMARY.md (the captured zip9_source breakdown)
    - .planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-01-SUMMARY.md (the checkpoint evidence checklist)
    - R/utils/utils_address.R (approximate_zip9() lines 415-519 and .classify_zip9_source() lines 278-335)
    - R/116_encounter_ses_index.R (SECTION 5 lines 128-155)
  </read_first>
  <action>
    Apply the D-02 decision tree using the 145-02 zip9_source breakdown. Branch as follows:

    BRANCH A — DATA-DRIVEN (no code bug). Conclude data-driven if the breakdown shows
    zip5_modal == 0 AND there are NO rows with match_type in {interval, most_recent_before}
    that have ZIP9 = NA (i.e., every unresolved encounter is match_type == "none", so
    n_to_approx == 0 and the early-exit at lines 446-448 fires correctly). In this case,
    make NO logic change. Do BOTH of the following documentation actions:
      (a) Add a low-noise console note inside approximate_zip9(), immediately after the
          `n_to_approx` computation (after line 444, before the `if (n_to_approx == 0)`),
          exactly:
          ```r
          if (n_to_approx == 0) {
            message(glue(
              "[utils_address] approximate_zip9: 0 approximable rows ",
              "(all NA-ZIP9 rows have match_type == 'none'); ",
              "zip5_modal tier will report zero rows -- expected, not a defect. ",
              "See data/reference/README.md 'ZIP5-modal tier' note."
            ))
          }
          ```
          (place this message call so it fires on the early-exit path; keep the existing
          `return(.classify_zip9_source(...))` after it — do not remove the early-exit).
      (b) Append a short subsection to data/reference/README.md titled
          `## ZIP5-modal imputation tier — why it can report zero rows (Phase 145)` stating:
          the zip5_modal tier only fires for encounters that have a ZIP5 but no ZIP9 AND a
          non-"none" match_type; in the HL cohort run of <DATE from 145-02>, every unresolved
          encounter had match_type == "none" (no covering address record at any date), so
          n_to_approx == 0, the modal lookup was never built, and zip5_modal == 0 by design;
          include the actual observed counts for none / zip5_no_zip9 / no_zip5 / zip9_observed
          from the 145-02 breakdown.

    BRANCH B — CODE BUG. Conclude a bug ONLY if the breakdown shows rows with match_type in
    {interval, most_recent_before} that have ZIP9 = NA existing in the pre-approximation
    output, yet zip5_modal == 0. In that case, identify the defect site (most likely a filter
    mismatch in the n_to_approx computation at lines 442-444, or a column-name mismatch where
    `ZIP9` in result_tbl is not the column get_zip9_at_date() returns), fix it in
    R/utils/utils_address.R (or R/116 if the defect is there), and add a testthat fixture to
    tests/testthat/test-utils-address.R that reproduces a resolvable-via-modal row and asserts
    zip9_source == "zip5_modal" for it. Note that the fix requires re-running the HiPerGator
    checkpoint (145-02) before Task 2's regeneration — flag this in the SUMMARY.

    DO NOT assume a branch. Read the 145-02 breakdown first and state which branch the evidence
    supports, quoting the actual counts.

    OPTIONAL (Claude's discretion, either branch): while editing R/116, you MAY fix the stale
    comment at lines 140-141 that says "one-to-many" in prose (the code correctly uses
    "many-to-one" at line 148) — change the prose to "many-to-one" for consistency. This is a
    comment-only edit; do not alter the `relationship = "many-to-one"` code.
  </action>
  <verify>
    <automated>Rscript -e 'source("R/00_config.R"); testthat::test_file("tests/testthat/test-utils-address.R")' — must exit 0 (only meaningful on HiPerGator; if Rscript unavailable locally, defer to the Task 2 HiPerGator run and record in SUMMARY)</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY states the chosen branch (A data-driven OR B bug) and quotes the observed zip9_source counts from 145-02 that justify it
    - If BRANCH A: `grep -c "zip5_modal tier will report zero rows" R/utils/utils_address.R` == 1 AND `grep -c "ZIP5-modal imputation tier" data/reference/README.md` == 1
    - If BRANCH A: the early-exit logic is unchanged except for the added message (`grep -c "n_to_approx == 0" R/utils/utils_address.R` still >= 1)
    - If BRANCH B: the defect site is edited, and `grep -c "zip5_modal" tests/testthat/test-utils-address.R` >= 1 (a new fixture asserting the tier fires), AND the SUMMARY flags that a re-run of the 145-02 checkpoint is required before Task 2
    - No unrelated logic in approximate_zip9() or .classify_zip9_source() is changed (row-count preservation stopifnot at line 333 remains present)
  </acceptance_criteria>
  <done>The zip5_modal zero-rows outcome is diagnosed with quoted evidence; under Branch A a console note + README note document it as expected; under Branch B the defect is fixed with a reproducing fixture and a re-run flagged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Regenerate corrected RDS + summary workbook on HiPerGator</name>
  <read_first>
    - .planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-02-SUMMARY.md
  </read_first>
  <action>
    User runs the corrected R/116 on HiPerGator to regenerate the RDS + summary workbook and pastes the final row count and output filenames. Claude cannot access HiPerGator; this is a human-action verification gate. See how-to-verify for exact commands and required output.
  </action>
  <what-built>
    The fan-out fix is confirmed (145-02) and the ZIP5-modal outcome is diagnosed (Task 1
    above). The corrected R/116 now produces one output row per input encounter. The old
    inflated output files from the Phase 144 run must be replaced with clean ones.
  </what-built>
  <how-to-verify>
    On HiPerGator, from the project root:

    1. `module load R/4.4.2`
    2. `Rscript R/116_encounter_ses_index.R`
    3. Confirm it writes fresh dated outputs:
       - `output/encounter_ses_index_YYYYMMDD.rds`
       - `output/encounter_ses_index_summary_YYYYMMDD.xlsx`
    4. Confirm the run completed without aborting at any stopifnot.
    5. Paste: the final `nrow(encounter_ses)` line and confirm it equals the `encounters_raw`
       count (no fan-out); the output filenames written; and, if Task 1 chose Branch A, that
       the new "0 approximable rows ... expected, not a defect" console note printed.

    If Task 1 was Branch B (a code fix), this regeneration IS the re-run that verifies the fix —
    confirm zip5_modal now fires with a count > 0 in the zip9_source breakdown.

    Optionally remove the old inflated output files (the ones from the Phase 144 run) if they
    are still present and dated earlier.
  </how-to-verify>
  <resume-signal>Paste the final nrow line, the written output filenames, and any errors; then type "regenerated" (or describe issues).</resume-signal>
</task>

</tasks>

<verification>
- ZIP5-modal diagnosis recorded with quoted evidence; correct branch taken.
- Branch A: console note + README note present. Branch B: fix + fixture + re-run flagged.
- Corrected RDS + summary workbook regenerated on HiPerGator; nrow(encounter_ses) == encounters_raw.
</verification>

<success_criteria>
- The diagnosis branch is justified by the 145-02 breakdown counts.
- Fresh output/encounter_ses_index_YYYYMMDD.{rds,xlsx} written with no fan-out.
- All Task 1 acceptance criteria for the chosen branch pass.
</success_criteria>

<output>
After completion, create `.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-03-SUMMARY.md`
</output>
