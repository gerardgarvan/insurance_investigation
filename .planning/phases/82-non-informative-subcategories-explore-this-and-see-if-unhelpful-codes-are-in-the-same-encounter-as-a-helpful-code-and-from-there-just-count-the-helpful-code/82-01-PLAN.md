---
phase: 82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/57_explore_dx_deduplication.R
autonomous: true
requirements:
  - P82-EXPLORE
  - P82-COOCCUR
  - P82-QUAL

must_haves:
  truths:
    - "R/57 exploration script runs standalone against treatment_episodes.rds and DuckDB DIAGNOSIS"
    - "Diagnostic output quantifies how many Encounter Dx codes have a helpful partner in the same encounter"
    - "Diagnostic output quantifies how many Encounter Dx codes are orphans (dx-only encounters)"
    - "Diagnostic output shows Table 1 row count before vs after deduplication"
  artifacts:
    - path: "R/57_explore_dx_deduplication.R"
      provides: "Encounter-level co-occurrence analysis for non-informative dx codes"
      min_lines: 150
      contains: "str_detect.*Encounter Dx"
  key_links:
    - from: "R/57_explore_dx_deduplication.R"
      to: "cache/outputs/treatment_episodes.rds"
      via: "readRDS() load"
      pattern: "readRDS.*treatment_episodes\\.rds"
    - from: "R/57_explore_dx_deduplication.R"
      to: "R/00_config.R"
      via: "source() for DRUG_GROUPINGS, CANCER_SITE_MAP, TREATMENT_CODES, CODE_SUBCATEGORY_MAP"
      pattern: 'source.*R/00_config\\.R'
---

<objective>
Create R/57_explore_dx_deduplication.R -- a standalone exploration script that performs encounter-level co-occurrence analysis on R/56's data to identify non-informative encounter diagnosis codes, check whether helpful codes exist in the same encounter, and quantify the deduplication impact on Table 1.

Purpose: Validate deduplication logic before integrating into production R/56 script (per D-07). Produce diagnostic output showing dx code counts, partner rates, orphan rates, and before/after Table 1 row counts (per D-09).

Output: R/57_explore_dx_deduplication.R script with console diagnostic output and a refined Table 1 preview.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-CONTEXT.md
@.planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-RESEARCH.md

<interfaces>
<!-- Key patterns and contracts from existing codebase that executor needs -->

From R/56_new_tables_from_groupings.R (the production script this exploration parallels):

Section 4 encounter split pattern (lines 170-175):
```r
episode_encounters <- episodes %>%
  mutate(episode_row = row_number()) %>%
  filter(!is.na(encounter_ids) & encounter_ids != "") %>%
  mutate(ENCOUNTERID = str_split(encounter_ids, ",\\s*")) %>%
  unnest(ENCOUNTERID) %>%
  filter(!is.na(ENCOUNTERID) & ENCOUNTERID != "")
```

Section 5 episode_codes construction (lines 248-257):
```r
episode_codes <- episode_dx %>%
  mutate(code_list = str_split(triggering_codes, ",\\s*")) %>%
  unnest(code_list) %>%
  filter(!is.na(code_list), code_list != "") %>%
  rename(treatment_code = code_list) %>%
  mutate(category = ifelse(
    treatment_code %in% names(DRUG_GROUPINGS),
    DRUG_GROUPINGS[treatment_code],
    treatment_type
  ))
```

Non-informative code pattern (sub_category values matching "Encounter Dx"):
- "Chemo Encounter Dx Code"
- "Radiation Encounter Dx Code"
- "Immunotherapy Encounter Dx Code"

Table 1 current aggregation (line 410-416):
```r
table1 <- episode_codes %>%
  filter(!is.na(cancer_codes)) %>%
  mutate(category = factor(category, levels = category_order)) %>%
  group_by(category, sub_category, treatment_code, code_type, cancer_codes) %>%
  summarise(encounter_count = n(), .groups = "drop") %>%
  arrange(category, desc(encounter_count)) %>%
  mutate(category = as.character(category))
```

From R/00_config.R:
- DRUG_GROUPINGS: 454-entry named vector (code -> category)
- TREATMENT_CODES: nested list of code vectors by type and code system
- CODE_SUBCATEGORY_MAP: 200+ entry code-to-name mapping
- CANCER_SITE_MAP: ICD-10 prefix -> cancer site mapping
- CONFIG$cache$outputs_dir: cache path
- CONFIG$output_dir: output path

From R/utils/utils_duckdb.R:
```r
open_pcornet_con()     # Opens DuckDB connection
get_pcornet_table(tbl) # Returns lazy table reference
```

From R/utils/utils_assertions.R:
```r
assert_rds_exists(path, script_name)
assert_df_valid(df, name, required_cols, script_name)
warn_row_count(df, name, min_expected, max_expected, script_name)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/57 encounter-level dx code co-occurrence exploration script</name>
  <files>R/57_explore_dx_deduplication.R</files>
  <read_first>
    R/56_new_tables_from_groupings.R
    R/00_config.R
    R/utils/utils_duckdb.R
    R/utils/utils_assertions.R
    .planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-CONTEXT.md
  </read_first>
  <action>
Create `R/57_explore_dx_deduplication.R` that replicates R/56's data loading and sub-category classification, then adds encounter-level co-occurrence analysis. The script must follow the exact same structure as R/56 but with additional encounter-level analysis sections.

**Documentation header** (following v2.0 standard, per D-12):
- Purpose: Explore encounter-level co-occurrence of non-informative diagnosis codes with helpful treatment codes. Validate deduplication logic before production integration in R/56.
- Inputs: cache/outputs/treatment_episodes.rds, DuckDB DIAGNOSIS, R/00_config.R (DRUG_GROUPINGS, TREATMENT_CODES, CODE_SUBCATEGORY_MAP, CANCER_SITE_MAP)
- Outputs: Console diagnostic output (co-occurrence stats, orphan counts, Table 1 impact)
- Dependencies: R/00_config.R, R/utils/utils_assertions.R, R/utils/utils_duckdb.R, openxlsx2
- Requirements: P82-EXPLORE, P82-COOCCUR
- Decision Traceability: D-01 through D-12 from Phase 82

**Section 1: SETUP AND CONFIGURATION**
- Clear stale log handler: `try(close(.log_con), silent = TRUE)` then `globalCallingHandlers(NULL)`
- Libraries: dplyr, tidyr, glue, stringr, openxlsx2, checkmate
- Source: R/00_config.R, R/utils/utils_assertions.R, R/utils/utils_duckdb.R
- Define paths: EPISODES_RDS, REFERENCE_XLSX (same as R/56)
- Log to `output/57_explore_dx_deduplication.log`

**Section 2: LOAD AND VALIDATE INPUT DATA**
- Load treatment_episodes.rds with assert_rds_exists and assert_df_valid (required_cols: patient_id, treatment_type, triggering_codes, cancer_category, encounter_ids)
- Log episode count and treatment types

**Section 3: BUILD SUB-CATEGORY MAPPINGS FROM REFERENCE XLSX**
- Replicate R/56 Section 3 exactly: load chemo_map (col 3), rad_map (col 7), sct_map (col 7) from xlsx sheets
- Build combined `code_to_subcategory` lookup

**Section 4: PREPARE ENCOUNTER-LEVEL DATA**
- Replicate R/56 Section 4: split encounter_ids into episode_encounters with episode_row + ENCOUNTERID
- Load DuckDB DIAGNOSIS for all encounter IDs, filter to cancer codes using is_cancer_code() helper (same as R/56)
- Build encounter_dx (cancer codes per encounter, semicolon-separated)
- Join cancer codes back to episode level to create episode_dx

**Section 5: BUILD EPISODE_CODES WITH SUB-CATEGORY CLASSIFICATION**
- Replicate R/56 Section 5 episode_codes construction: split triggering_codes, assign category from DRUG_GROUPINGS with treatment_type fallback
- Replicate the full case_when() sub-category classification (all 3 tiers: xlsx, CODE_SUBCATEGORY_MAP, code-type fallback)
- Replicate code_type assignment

**Section 6: ENCOUNTER-LEVEL CO-OCCURRENCE ANALYSIS (NEW)**
This is the core new logic (per D-03, D-04):

Step 1 - Connect treatment codes to encounters:
The key challenge: `episode_codes` is derived from `episode_dx` which has one row per episode (episode_row removed). Need to re-establish encounter-level granularity. Approach:
- Re-add `episode_row` to episode_dx before building episode_codes, OR
- Build a separate encounter-to-treatment-code mapping by re-splitting triggering_codes at encounter level.

Best approach: Before Section 5, add `episode_row = row_number()` to `episode_dx` and carry it through to `episode_codes`. Then join `episode_codes` to `episode_encounters` via `episode_row` to get per-encounter treatment code rows. This gives one row per (encounter, treatment_code).

```r
# Add episode_row to episode_dx before creating episode_codes
episode_dx <- episode_dx %>%
  mutate(episode_row = row_number())

# ... (build episode_codes from episode_dx as before, episode_row propagates) ...

# Join episode_codes to episode_encounters to get encounter-level treatment codes
# episode_encounters has (episode_row, ENCOUNTERID) - one row per encounter per episode
# episode_codes has (episode_row, treatment_code, sub_category, ...) - one row per code per episode
# Cross-join: every code in an episode appears in every encounter of that episode
episode_codes_enc <- episode_codes %>%
  inner_join(
    episode_encounters %>% select(episode_row, ENCOUNTERID),
    by = "episode_row",
    relationship = "many-to-many"
  )
```

Step 2 - Flag non-informative codes (per D-01, D-10):
```r
episode_codes_enc <- episode_codes_enc %>%
  mutate(is_non_informative = str_detect(sub_category, "Encounter Dx"))
```

Step 3 - Per encounter, check if ANY helpful (non-dx) code exists (per D-03):
```r
encounter_has_helpful <- episode_codes_enc %>%
  group_by(ENCOUNTERID) %>%
  summarise(has_helpful = any(!is_non_informative), .groups = "drop")
```

Step 4 - Join back and flag dx-only rows (per D-05):
```r
episode_codes_enc <- episode_codes_enc %>%
  left_join(encounter_has_helpful, by = "ENCOUNTERID") %>%
  mutate(dx_only = is_non_informative & !has_helpful)
```

**Section 7: DIAGNOSTIC OUTPUT (per D-09)**
Print the following counts with `message()` + `glue()`:
```
=== Encounter Dx Code Co-Occurrence Analysis ===

Total treatment code instances (encounter-level): {nrow(episode_codes_enc)}
Non-informative (Encounter Dx) instances: {n_non_informative}
Helpful (specific treatment) instances: {n_helpful}

--- Co-occurrence within same encounter (per D-03) ---
Encounter Dx codes WITH helpful partner (same encounter): {n_with_partner}
Encounter Dx codes WITHOUT helpful partner (dx_only=TRUE): {n_orphan}
Partner rate: {pct_with_partner}%

--- Unique encounter counts ---
Total unique encounters: {n_encounters}
Encounters with Encounter Dx codes: {n_enc_with_dx}
Encounters with ONLY Encounter Dx codes (dx-only): {n_enc_dx_only}
Encounters with both dx and helpful codes: {n_enc_mixed}

--- Table 1 impact (before vs after deduplication) ---
```

Step 5 - Build Table 1 "before" (original, same as R/56 current logic):
```r
table1_before <- episode_codes %>%
  filter(!is.na(cancer_codes)) %>%
  group_by(category, sub_category, treatment_code, code_type, cancer_codes) %>%
  summarise(encounter_count = n(), .groups = "drop")
```

Step 6 - Build Table 1 "after" (deduplicated: remove dx codes that have a helpful partner in same encounter, keep orphans flagged):
```r
# For deduplication: aggregate back to episode level after encounter-level analysis
# A dx code is removable if, for ANY of its encounters, a helpful code co-exists
# Per D-05: keep orphan dx codes (no helpful partner) with dx_only flag
episode_codes_dedup <- episode_codes_enc %>%
  group_by(episode_row, treatment_code, sub_category, category, code_type, cancer_codes) %>%
  summarise(
    has_helpful_in_any_encounter = any(has_helpful & is_non_informative),
    is_non_informative = first(is_non_informative),
    dx_only = all(dx_only),  # dx_only only if ALL encounters for this code are dx-only
    .groups = "drop"
  ) %>%
  filter(!(is_non_informative & has_helpful_in_any_encounter))  # Remove dx codes with partner

table1_after <- episode_codes_dedup %>%
  filter(!is.na(cancer_codes)) %>%
  group_by(category, sub_category, treatment_code, code_type, cancer_codes, dx_only) %>%
  summarise(encounter_count = n(), .groups = "drop")
```

Log Table 1 impact:
```
Table 1 rows before deduplication: {nrow(table1_before)}
Table 1 rows after deduplication: {nrow(table1_after)}
Reduction: {nrow(table1_before) - nrow(table1_after)} rows ({pct}%)
```

**Section 8: PER-CATEGORY BREAKDOWN**
Show deduplication impact per treatment category:
```r
for each category in c("Chemotherapy", "Immunotherapy", "Radiation", "SCT"):
  n_before = rows in table1_before for this category
  n_after = rows in table1_after for this category
  message("  {category}: {n_before} -> {n_after} rows ({n_before - n_after} removed)")
```

**Section 9: ORPHAN ENCOUNTER DETAIL**
Show top 10 most common orphan (dx_only=TRUE) sub-categories with counts:
```r
orphan_summary <- episode_codes_enc %>%
  filter(dx_only) %>%
  count(category, sub_category, sort = TRUE) %>%
  head(10)
```

**Section 10: CONSOLE SUMMARY**
```
=== Summary ===
  Non-informative Encounter Dx codes successfully identified via str_detect(sub_category, "Encounter Dx")
  Encounter-level co-occurrence checked within same encounter_id (per D-03)
  Orphan dx-only encounters preserved with dx_only flag (per D-05)
  Table 1 deduplication reduces rows by {pct}%
  Ready for integration into R/56
```

Close log connection.

**Quality standards (per D-12):**
- checkmate assertions on all inputs (assert_rds_exists, assert_df_valid, assert_file_exists for xlsx)
- Section headers in `# SECTION N: NAME ----` format
- All message() output uses glue() for interpolation
- No hardcoded sub-category lists -- use str_detect(sub_category, "Encounter Dx") throughout (per D-10)
  </action>
  <verify>
    <automated>grep -c "Encounter Dx" R/57_explore_dx_deduplication.R</automated>
  </verify>
  <acceptance_criteria>
    - R/57_explore_dx_deduplication.R exists with >= 150 lines
    - File contains `str_detect(sub_category, "Encounter Dx")` (pattern matching per D-10, not hardcoded list)
    - File contains `source("R/00_config.R")` for config loading
    - File contains `source("R/utils/utils_assertions.R")` for defensive coding
    - File contains `source("R/utils/utils_duckdb.R")` for DuckDB access
    - File contains `readRDS` for treatment_episodes.rds loading
    - File contains `inner_join` or `left_join` with `episode_encounters` for encounter-level propagation
    - File contains `group_by(ENCOUNTERID)` for per-encounter helpful code check (per D-03)
    - File contains `dx_only` flag column assignment (per D-05)
    - File contains `has_helpful` column for co-occurrence detection
    - File contains `table1_before` and `table1_after` for impact comparison (per D-09)
    - File contains at least 8 SECTION headers matching `^# SECTION.*----` pattern
    - File contains documentation header with Purpose, Inputs, Outputs, Dependencies
    - File contains `checkmate` assertion calls (assert_rds_exists or assert_df_valid or assert_file_exists)
    - File does NOT contain hardcoded sub-category names like `c("Chemo Encounter Dx Code"` -- uses pattern matching instead
  </acceptance_criteria>
  <done>
    R/57_explore_dx_deduplication.R exists as a standalone exploration script that:
    1. Loads treatment_episodes.rds and DuckDB DIAGNOSIS (same inputs as R/56)
    2. Builds sub-category classifications (same 3-tier lookup as R/56)
    3. Performs encounter-level co-occurrence analysis via episode_encounters join
    4. Identifies non-informative codes via str_detect(sub_category, "Encounter Dx")
    5. Flags dx-only encounters (per D-05) rather than removing them
    6. Produces diagnostic output: partner rate, orphan count, Table 1 before/after impact
    7. Follows v2.0 code quality standards (assertions, section structure, documentation)
  </done>
</task>

</tasks>

<verification>
- R/57_explore_dx_deduplication.R exists and has >= 150 lines
- Script has proper documentation header and >= 8 section headers
- Pattern matching uses str_detect (not hardcoded lists)
- Encounter-level join uses episode_encounters with ENCOUNTERID
- dx_only flag column present (not filtering/deleting orphan rows)
- Diagnostic output includes before/after Table 1 counts
</verification>

<success_criteria>
R/57 exploration script validates the encounter-level co-occurrence deduplication logic with complete diagnostic output, ready for integration into R/56 in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-01-SUMMARY.md`
</output>
