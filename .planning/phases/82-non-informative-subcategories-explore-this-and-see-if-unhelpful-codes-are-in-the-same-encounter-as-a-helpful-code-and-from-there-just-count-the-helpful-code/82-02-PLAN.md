---
phase: 82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code
plan: 02
type: execute
wave: 2
depends_on:
  - 82-01
files_modified:
  - R/56_new_tables_from_groupings.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - P82-INTEGRATE
  - P82-FLAG
  - P82-SMOKE
  - P82-QUAL

must_haves:
  truths:
    - "R/56 Table 1 removes non-informative Encounter Dx codes when a helpful code exists in the same encounter"
    - "R/56 Table 1 preserves orphan dx-only rows with a dx_only flag column instead of deleting them"
    - "R/56 deduplication uses pattern matching str_detect(sub_category, Encounter Dx) not hardcoded lists"
    - "Smoke test validates Phase 82 deduplication logic and dx_only flag in R/56"
  artifacts:
    - path: "R/56_new_tables_from_groupings.R"
      provides: "Integrated encounter-level dx deduplication in Table 1"
      contains: "dx_only"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Phase 82 validation checks"
      contains: "dx_only"
  key_links:
    - from: "R/56_new_tables_from_groupings.R"
      to: "episode_encounters"
      via: "inner_join for encounter-level treatment code propagation"
      pattern: "inner_join.*episode_encounters"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/56_new_tables_from_groupings.R"
      via: "readLines + grepl pattern checks"
      pattern: "grepl.*dx_only.*r56_lines"
---

<objective>
Integrate validated encounter-level dx code deduplication logic from R/57 exploration into production R/56 script, and update R/88 smoke test with Phase 82 validation checks.

Purpose: Apply deduplication to Table 1 in R/56 so non-informative Encounter Dx codes with helpful partners in the same encounter are removed from counts, while orphan dx-only encounters are preserved with a flag column (per D-05, D-07, D-08).

Output: Modified R/56 with deduplication logic in Table 1, modified R/88 with Phase 82 smoke test checks.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-CONTEXT.md
@.planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-RESEARCH.md
@.planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-01-SUMMARY.md

<interfaces>
<!-- Current R/56 structure that will be modified -->

From R/56_new_tables_from_groupings.R:

Current Section 4 (lines 170-175) - episode_encounters with episode_row:
```r
episode_encounters <- episodes %>%
  mutate(episode_row = row_number()) %>%
  filter(!is.na(encounter_ids) & encounter_ids != "") %>%
  mutate(ENCOUNTERID = str_split(encounter_ids, ",\\s*")) %>%
  unnest(ENCOUNTERID) %>%
  filter(!is.na(ENCOUNTERID) & ENCOUNTERID != "")
```

Current Section 4 (lines 221-224) - episode_dx loses episode_row:
```r
episode_dx <- episodes %>%
  mutate(episode_row = row_number()) %>%
  left_join(episode_cancer, by = "episode_row") %>%
  select(-episode_row)
```

Current Section 5 (lines 248-257) - episode_codes from episode_dx:
```r
episode_codes <- episode_dx %>%
  mutate(code_list = str_split(triggering_codes, ",\\s*")) %>%
  unnest(code_list) %>%
  filter(!is.na(code_list), code_list != "") %>%
  rename(treatment_code = code_list) %>%
  mutate(category = ifelse(...))
```

Current Table 1 aggregation (lines 410-416):
```r
table1 <- episode_codes %>%
  filter(!is.na(cancer_codes)) %>%
  mutate(category = factor(category, levels = category_order)) %>%
  group_by(category, sub_category, treatment_code, code_type, cancer_codes) %>%
  summarise(encounter_count = n(), .groups = "drop") %>%
  arrange(category, desc(encounter_count)) %>%
  mutate(category = as.character(category))
```

Table 1 current columns: category | sub_category | treatment_code | code_type | cancer_codes | encounter_count
After Phase 82: category | sub_category | treatment_code | code_type | cancer_codes | encounter_count | dx_only

From R/88_smoke_test_comprehensive.R:
- Current check count: 27/27 (will become 28/28)
- Section 13G (lines 982-1034): R/56 drug grouping checks -- Phase 82 checks go after this section
- Section 16 (lines 1162-1201): Summary section -- must update total counter and add requirements
- check() function pattern: `check("description string", boolean_expression)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate encounter-level dx deduplication into R/56 Table 1</name>
  <files>R/56_new_tables_from_groupings.R</files>
  <read_first>
    R/56_new_tables_from_groupings.R
    R/57_explore_dx_deduplication.R
    .planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-CONTEXT.md
    .planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-01-SUMMARY.md
  </read_first>
  <action>
Modify R/56_new_tables_from_groupings.R to integrate encounter-level dx code deduplication into Table 1 (per D-07, D-08). Changes affect Sections 4, 5, 7, and 8 -- and add a new Section 5B between existing Section 5 and Section 6.

**Update documentation header:**
Add to Decision Traceability section:
```
#   - D-01 (P82): Non-informative = sub_categories matching "Encounter Dx" pattern
#   - D-03 (P82): Co-occurrence checked within same encounter_id, not episode
#   - D-05 (P82): Orphan dx-only encounters preserved with dx_only flag column
#   - D-08 (P82): Deduplication applied to Table 1 only (not Table 2)
#   - D-10 (P82): Pattern matching via str_detect, not hardcoded lists
```
Add to Requirements section:
```
#   - P82-INTEGRATE: Encounter-level dx deduplication integrated into R/56 Table 1
#   - P82-FLAG: dx_only flag column added to Table 1 for orphan encounter preservation
```

**Modification 1 -- Section 4: Preserve episode_row in episode_dx**

Currently line 224 removes episode_row with `select(-episode_row)`. Change to KEEP episode_row so it propagates to episode_codes for the encounter join:

Replace:
```r
episode_dx <- episodes %>%
  mutate(episode_row = row_number()) %>%
  left_join(episode_cancer, by = "episode_row") %>%
  select(-episode_row)
```

With:
```r
episode_dx <- episodes %>%
  mutate(episode_row = row_number()) %>%
  left_join(episode_cancer, by = "episode_row")
# NOTE: episode_row kept for encounter-level join in Section 5B (Phase 82)
```

**Modification 2 -- New Section 5B: ENCOUNTER-LEVEL DX DEDUPLICATION (after existing Section 5, before Section 6)**

Insert between the current end of Section 5 (after the logging block around line 431) and Section 6 (Table 2). Add:

```r
# SECTION 5B: ENCOUNTER-LEVEL DX CODE DEDUPLICATION (Phase 82) ----

message()
message("--- Encounter-level Dx code deduplication (Table 1 only, per D-08) ---")

# Step 1: Flag non-informative Encounter Dx codes via pattern matching (D-01, D-10)
episode_codes <- episode_codes %>%
  mutate(is_non_informative = str_detect(sub_category, "Encounter Dx"))

n_non_informative <- sum(episode_codes$is_non_informative, na.rm = TRUE)
n_helpful <- sum(!episode_codes$is_non_informative, na.rm = TRUE)
message(glue("  Non-informative (Encounter Dx) code instances: {n_non_informative}"))
message(glue("  Helpful (specific treatment) code instances: {n_helpful}"))

# Step 2: Connect treatment codes to encounters for co-occurrence check (D-03, D-04)
# episode_codes has episode_row (kept from Section 4 modification)
# episode_encounters has (episode_row, ENCOUNTERID) from Section 4
# Join: every treatment code in an episode maps to every encounter in that episode
episode_codes_enc <- episode_codes %>%
  inner_join(
    episode_encounters %>% select(episode_row, ENCOUNTERID),
    by = "episode_row",
    relationship = "many-to-many"
  )

message(glue("  Encounter-level code instances: {nrow(episode_codes_enc)}"))

# Step 3: Per encounter, check if ANY helpful code exists (D-03)
encounter_has_helpful <- episode_codes_enc %>%
  group_by(ENCOUNTERID) %>%
  summarise(has_helpful = any(!is_non_informative), .groups = "drop")

# Step 4: Join back and compute dx_only flag (D-05)
episode_codes_enc <- episode_codes_enc %>%
  left_join(encounter_has_helpful, by = "ENCOUNTERID") %>%
  mutate(dx_only = is_non_informative & !has_helpful)

n_with_partner <- sum(episode_codes_enc$is_non_informative & episode_codes_enc$has_helpful, na.rm = TRUE)
n_orphan <- sum(episode_codes_enc$dx_only, na.rm = TRUE)
pct_partner <- if (n_non_informative > 0) round(100 * n_with_partner / (n_with_partner + n_orphan), 1) else 0
message(glue("  Encounter Dx codes with helpful partner: {n_with_partner} ({pct_partner}%)"))
message(glue("  Encounter Dx codes orphaned (dx_only): {n_orphan} ({100 - pct_partner}%)"))

# Step 5: Deduplicate back to episode level for Table 1
# Remove dx codes that have a helpful partner in ANY of their encounters
# Keep orphan dx codes with dx_only=TRUE flag (D-05)
episode_codes_dedup <- episode_codes_enc %>%
  group_by(episode_row, treatment_code, sub_category, category, code_type, cancer_codes, is_non_informative) %>%
  summarise(
    removable = any(has_helpful) & first(is_non_informative),
    dx_only = all(dx_only),
    .groups = "drop"
  ) %>%
  filter(!removable) %>%
  select(-removable, -is_non_informative)

n_before <- nrow(episode_codes %>% filter(!is.na(cancer_codes)))
n_after <- nrow(episode_codes_dedup %>% filter(!is.na(cancer_codes)))
message(glue("  Table 1 code instances before dedup: {n_before}"))
message(glue("  Table 1 code instances after dedup: {n_after}"))
message(glue("  Removed: {n_before - n_after} ({round(100 * (n_before - n_after) / max(n_before, 1), 1)}%)"))
```

**Modification 3 -- Section 5 Table 1 aggregation: Use deduplicated data**

Replace the current Table 1 aggregation (around lines 410-416):

FROM:
```r
table1 <- episode_codes %>%
  filter(!is.na(cancer_codes)) %>%
  mutate(category = factor(category, levels = category_order)) %>%
  group_by(category, sub_category, treatment_code, code_type, cancer_codes) %>%
  summarise(encounter_count = n(), .groups = "drop") %>%
  arrange(category, desc(encounter_count)) %>%
  mutate(category = as.character(category))
```

TO:
```r
# Use deduplicated episode_codes for Table 1 (Phase 82: encounter-level dx dedup)
table1 <- episode_codes_dedup %>%
  filter(!is.na(cancer_codes)) %>%
  mutate(category = factor(category, levels = category_order)) %>%
  group_by(category, sub_category, treatment_code, code_type, cancer_codes, dx_only) %>%
  summarise(encounter_count = n(), .groups = "drop") %>%
  arrange(category, desc(encounter_count)) %>%
  mutate(category = as.character(category))
```

Note the key changes:
1. Source is `episode_codes_dedup` instead of `episode_codes`
2. `dx_only` added to group_by (per D-05, D-11: additive column)

**Modification 4 -- Section 8 CONSOLE SUMMARY: Add deduplication stats**

After the existing Table 1 summary lines, add:
```r
message(glue("    Dx codes deduplicated: {n_before - n_after} instances removed"))
message(glue("    Orphan dx-only rows preserved: {sum(table1$dx_only, na.rm = TRUE)}"))
```

**Modification 5 -- Clean up episode_row before Table 2**

Before Section 6 (Table 2), remove episode_row from episode_dx since Table 2 does not need it and it should not appear in output:
```r
# Clean up episode_row before Table 2 (not needed downstream)
episode_dx <- episode_dx %>% select(-episode_row)
```

**What NOT to change (per D-08):**
- Table 2 aggregation (Section 6) is unchanged -- Table 2 already shows all treatments per encounter as a set
- Section 3 (sub-category mappings) is unchanged
- Section 7 xlsx output logic is unchanged (dx_only column is automatically included via table1 data frame)
  </action>
  <verify>
    <automated>grep -c "dx_only" R/56_new_tables_from_groupings.R</automated>
  </verify>
  <acceptance_criteria>
    - R/56 contains `str_detect(sub_category, "Encounter Dx")` for pattern-based detection (not hardcoded list)
    - R/56 contains `is_non_informative` variable assignment
    - R/56 contains `inner_join` with `episode_encounters` and `by = "episode_row"` for encounter-level propagation
    - R/56 contains `group_by(ENCOUNTERID)` for per-encounter helpful code check
    - R/56 contains `has_helpful` variable from encounter-level summarise
    - R/56 contains `dx_only` in the Table 1 group_by call
    - R/56 contains `episode_codes_dedup` as the source for table1 aggregation
    - R/56 contains `# SECTION 5B:` header for the new deduplication section
    - R/56 does NOT remove episode_row from episode_dx before Section 5B (the `select(-episode_row)` from line 224 must be removed or moved after Section 5B)
    - R/56 header contains "D-01 (P82)" or "P82-INTEGRATE" decision reference
    - R/56 Table 2 section is unchanged (no dx_only, no episode_codes_dedup references in Section 6)
  </acceptance_criteria>
  <done>
    R/56 Table 1 aggregation uses encounter-level deduplication: non-informative Encounter Dx codes with helpful partners in the same encounter are removed, orphan dx-only rows are preserved with dx_only flag column. Table 2 is unchanged per D-08. Deduplication stats logged to console.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update R/88 smoke test with Phase 82 validation checks</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    R/88_smoke_test_comprehensive.R
    R/56_new_tables_from_groupings.R
    .planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-CONTEXT.md
  </read_first>
  <action>
Add Phase 82 validation checks to R/88_smoke_test_comprehensive.R. Insert a new section 13H after the existing Section 13G (Drug Grouping Summary Tables, ends around line 1034) and before Section 14 (Death Quality Profiling).

**New section to insert:**

```r
# ==============================================================================
# SECTION 13H: ENCOUNTER DX DEDUPLICATION (Phase 82) ----
# ==============================================================================

message("\n[28/29] Phase 82: Encounter Dx deduplication in R/56...")

# Check 1: R/57 exploration script exists
check(
  "R/57_explore_dx_deduplication.R exists",
  file.exists("R/57_explore_dx_deduplication.R")
)

# Check 2-9: R/56 Phase 82 integration checks (only if R/56 exists, which it does from Section 13G)
check(
  "R/56 has Section 5B header for encounter-level deduplication",
  any(grepl("SECTION 5B.*ENCOUNTER.*DEDUPLICATION", r56_lines, ignore.case = TRUE))
)

check(
  "R/56 uses str_detect pattern matching for Encounter Dx (not hardcoded list, per D-10)",
  any(grepl('str_detect\\(sub_category.*Encounter Dx', r56_lines))
)

check(
  "R/56 has is_non_informative flag variable",
  any(grepl("is_non_informative", r56_lines))
)

check(
  "R/56 joins episode_codes to episode_encounters for encounter-level analysis",
  any(grepl("inner_join.*episode_encounters", r56_lines) |
      grepl("episode_encounters.*inner_join", r56_lines))
)

check(
  "R/56 checks per-encounter helpful code co-occurrence (group_by ENCOUNTERID)",
  any(grepl("group_by\\(ENCOUNTERID\\)", r56_lines))
)

check(
  "R/56 has dx_only flag column (per D-05: preserve orphan encounters)",
  any(grepl("dx_only", r56_lines))
)

check(
  "R/56 Table 1 uses episode_codes_dedup (deduplicated source)",
  any(grepl("episode_codes_dedup", r56_lines))
)

check(
  "R/56 Table 1 group_by includes dx_only column",
  any(grepl("group_by.*dx_only", r56_lines))
)
```

**Update check counter:**
The current format is `[N/27]` for all existing checks. After adding Section 13H with its checks, the total changes. Update ALL `[N/27]` references to `[N/29]`:

Current Section 14 message: `message("\n[26/27] Death quality profiling...")`  -> `message("\n[27/29] Death quality profiling...")`
Current Section 15 message: `message("\n[27/27] Episode enrichment...")`  -> `message("\n[28/29] Episode enrichment...")`

Wait -- actually the exact counter update depends on whether 13H is one "check group" or two. Counting: 13H adds 1 new check group (Phase 82 deduplication). So 27 -> 28 total check groups.

Update:
- Section 13H gets: `message("\n[26/28] Phase 82: Encounter Dx deduplication in R/56...")`
- Section 14 (Death): `[26/27]` -> `[27/28]`
- Section 15 (Episode): `[27/27]` -> `[28/28]`

**Update validated requirements in Section 16 SUMMARY:**

After the existing `message("  * TREAT-03: Drug grouping summary tables (R/56)")` line (line 1197), add:
```r
message("  * P82-INTEGRATE: Encounter-level dx deduplication in R/56 Table 1")
message("  * P82-FLAG: dx_only flag column for orphan encounter preservation")
```

**R/57 checks (if file exists):**

Add within the Section 13H block, after the R/57 existence check:
```r
if (file.exists("R/57_explore_dx_deduplication.R")) {
  r57_lines <- readLines("R/57_explore_dx_deduplication.R", warn = FALSE)

  check("R/57 sources R/00_config.R",
        any(grepl('source\\("R/00_config.R"\\)', r57_lines)))

  check("R/57 uses str_detect for Encounter Dx pattern matching",
        any(grepl('str_detect.*Encounter Dx', r57_lines)))

  check("R/57 has diagnostic output comparing before/after Table 1",
        any(grepl("table1_before", r57_lines)) &&
        any(grepl("table1_after", r57_lines)))
}
```
  </action>
  <verify>
    <automated>grep -c "Phase 82" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88 contains `SECTION 13H` header for Phase 82 checks
    - R/88 contains check for `R/57_explore_dx_deduplication.R` existence
    - R/88 contains check for `str_detect.*Encounter Dx` pattern in R/56
    - R/88 contains check for `dx_only` in R/56
    - R/88 contains check for `episode_codes_dedup` in R/56
    - R/88 contains check for `group_by(ENCOUNTERID)` in R/56
    - R/88 contains check for `is_non_informative` in R/56
    - R/88 Section 14 message counter is updated (no longer [26/27])
    - R/88 Section 15 message counter is updated (no longer [27/27])
    - R/88 summary section contains "P82-INTEGRATE" validated requirement
    - R/88 summary section contains "P82-FLAG" validated requirement
  </acceptance_criteria>
  <done>
    R/88 smoke test validates all Phase 82 changes: R/57 existence, R/56 deduplication section, pattern matching, dx_only flag, encounter-level join, deduplicated Table 1 source. Check counters updated consistently.
  </done>
</task>

</tasks>

<verification>
- R/56 has new Section 5B with encounter-level deduplication logic
- R/56 Table 1 uses episode_codes_dedup as source (not episode_codes)
- R/56 Table 1 includes dx_only column in output
- R/56 Table 2 is unmodified (per D-08)
- R/88 has Section 13H with >= 8 Phase 82 checks
- R/88 check counters are consistent (no [N/27] remaining)
- R/88 summary lists P82-INTEGRATE and P82-FLAG
- Rscript R/88_smoke_test_comprehensive.R exits 0 (all checks pass)
</verification>

<success_criteria>
Encounter-level dx deduplication is integrated into R/56 production code. Non-informative Encounter Dx codes with helpful partners removed from Table 1 counts. Orphan dx-only encounters preserved with flag. Smoke test validates all changes. Table 2 unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/82-non-informative-subcategories-explore-this-and-see-if-unhelpful-codes-are-in-the-same-encounter-as-a-helpful-code-and-from-there-just-count-the-helpful-code/82-02-SUMMARY.md`
</output>
