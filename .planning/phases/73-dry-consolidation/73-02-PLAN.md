---
phase: 73-dry-consolidation
plan: 02
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - R/28_episode_classification.R
  - R/40_cancer_site_frequency.R
  - R/43_cancer_site_confirmation.R
  - R/44_cancer_site_confirmation_7day.R
  - R/45_cancer_summary.R
  - R/46_cancer_summary_table.R
  - R/47_cancer_summary_refined.R
  - R/48_cancer_summary_post_hl.R
  - R/49_cancer_summary_pre_post.R
  - R/51_gantt_data_export.R
autonomous: true
requirements: [DRY-01, DRY-02]

must_haves:
  truths:
    - "No script outside R/00_config.R defines PREFIX_MAP"
    - "No script outside R/utils/utils_cancer.R defines classify_codes()"
    - "All 10 scripts use CANCER_SITE_MAP from config and classify_codes() from utils"
  artifacts:
    - path: "R/28_episode_classification.R"
      provides: "Episode classification using centralized CANCER_SITE_MAP"
      contains: "classify_codes"
    - path: "R/40_cancer_site_frequency.R"
      provides: "Cancer site frequency using centralized CANCER_SITE_MAP"
      contains: "classify_codes"
    - path: "R/51_gantt_data_export.R"
      provides: "Gantt export using centralized CANCER_SITE_MAP"
      contains: "classify_codes"
  key_links:
    - from: "R/40_cancer_site_frequency.R"
      to: "R/00_config.R"
      via: "source() chain provides CANCER_SITE_MAP"
      pattern: "classify_codes\\("
    - from: "R/28_episode_classification.R"
      to: "R/utils/utils_cancer.R"
      via: "Auto-sourced classify_codes() function"
      pattern: "classify_codes\\("
---

<objective>
Remove PREFIX_MAP definitions and classify_codes() function definitions from 10 cancer/treatment scripts, replacing them with the centralized CANCER_SITE_MAP + classify_codes() from Plan 01.

Purpose: Eliminate ~2,860 lines of duplicated PREFIX_MAP definitions across 10 scripts (DRY-01) and 10 duplicated classify_codes() function definitions (DRY-02).
Output: 10 modified scripts using centralized constants and utilities.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/73-dry-consolidation/73-CONTEXT.md
@.planning/phases/73-dry-consolidation/73-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: centralized constants and utilities now available -->
From R/00_config.R (new):
```r
CANCER_SITE_MAP <- c("C00" = "Lip, Oral Cavity and Pharynx", ...) # 324 entries
```

From R/utils/utils_cancer.R (new):
```r
classify_codes <- function(codes) {
  prefix3 <- substr(codes, 1, 3)
  categories <- unname(CANCER_SITE_MAP[prefix3])
  categories
}
```

These are auto-loaded via R/00_config.R source chain. Any script that sources R/00_config.R
already has CANCER_SITE_MAP and classify_codes() available.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove PREFIX_MAP and classify_codes() from cancer scripts R/40-R/49</name>
  <files>R/40_cancer_site_frequency.R, R/43_cancer_site_confirmation.R, R/44_cancer_site_confirmation_7day.R, R/45_cancer_summary.R, R/46_cancer_summary_table.R, R/47_cancer_summary_refined.R, R/48_cancer_summary_post_hl.R, R/49_cancer_summary_pre_post.R</files>
  <read_first>
    - R/40_cancer_site_frequency.R (full file -- understand where PREFIX_MAP and classify_codes are defined and where classify_codes is called)
    - R/43_cancer_site_confirmation.R (full file)
    - R/44_cancer_site_confirmation_7day.R (full file)
    - R/45_cancer_summary.R (full file)
    - R/46_cancer_summary_table.R (full file)
    - R/47_cancer_summary_refined.R (full file)
    - R/48_cancer_summary_post_hl.R (full file)
    - R/49_cancer_summary_pre_post.R (full file)
    - R/utils/utils_cancer.R (verify classify_codes exists from Plan 01)
  </read_first>
  <action>
For EACH of the 8 cancer scripts, perform these exact changes:

1. **Delete the entire PREFIX_MAP definition block.** This starts with `PREFIX_MAP <- c(` and ends with the closing `)`. In each file, this is approximately 324 lines. The surrounding section comments (e.g., `# SECTION 1: DEFINE CANCER SITE CATEGORIES`) should also be removed since the section is no longer needed.

2. **Delete the classify_codes() function definition.** This is typically 4-7 lines:
   ```r
   classify_codes <- function(codes) {
     prefix3 <- substr(codes, 1, 3)
     categories <- unname(PREFIX_MAP[prefix3])
     categories
   }
   ```
   Also delete the roxygen-style comment block above it if present.

3. **Do NOT modify any lines that CALL classify_codes().** Lines like `mutate(category = classify_codes(DX_norm))` remain exactly as they are -- the function is now provided by R/utils/utils_cancer.R via the source chain.

4. **Add a brief comment where the section was removed** to explain where the constant now lives:
   ```r
   # CANCER_SITE_MAP and classify_codes() provided by R/00_config.R + R/utils/utils_cancer.R
   ```

5. **Update the script header** (Dependencies line): Add `CANCER_SITE_MAP (R/00_config.R), classify_codes() (R/utils/utils_cancer.R)` to the Dependencies list.

**File-specific line ranges for PREFIX_MAP blocks** (approximate, verify by reading each file):
- R/40: lines 64-390 (PREFIX_MAP), lines 390-397 (classify_codes)
- R/43: lines 50-373 (PREFIX_MAP), lines 373-379 (classify_codes)
- R/44: lines 50-373 (PREFIX_MAP), lines 373-379 (classify_codes)
- R/45: lines 62-327 (PREFIX_MAP), lines 327-333 (classify_codes)
- R/46: lines 59-324 (PREFIX_MAP), lines 324-330 (classify_codes)
- R/47: lines 81-346 (PREFIX_MAP), lines 346-352 (classify_codes)
- R/48: lines 81-346 (PREFIX_MAP), lines 346-352 (classify_codes)
- R/49: lines 73-338 (PREFIX_MAP), lines 338-344 (classify_codes)

Each script shrinks by approximately 280-330 lines.
  </action>
  <verify>
    <automated>grep -rl "PREFIX_MAP <- c(" R/40_cancer_site_frequency.R R/43_cancer_site_confirmation.R R/44_cancer_site_confirmation_7day.R R/45_cancer_summary.R R/46_cancer_summary_table.R R/47_cancer_summary_refined.R R/48_cancer_summary_post_hl.R R/49_cancer_summary_pre_post.R 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PREFIX_MAP" R/40_cancer_site_frequency.R` returns 0
    - `grep -c "PREFIX_MAP" R/43_cancer_site_confirmation.R` returns 0
    - `grep -c "PREFIX_MAP" R/44_cancer_site_confirmation_7day.R` returns 0
    - `grep -c "PREFIX_MAP" R/45_cancer_summary.R` returns 0
    - `grep -c "PREFIX_MAP" R/46_cancer_summary_table.R` returns 0
    - `grep -c "PREFIX_MAP" R/47_cancer_summary_refined.R` returns 0
    - `grep -c "PREFIX_MAP" R/48_cancer_summary_post_hl.R` returns 0
    - `grep -c "PREFIX_MAP" R/49_cancer_summary_pre_post.R` returns 0
    - `grep -c "classify_codes <- function" R/40_cancer_site_frequency.R` returns 0
    - `grep -c "classify_codes <- function" R/45_cancer_summary.R` returns 0
    - `grep -c "classify_codes(" R/40_cancer_site_frequency.R` returns at least 1 (call sites preserved)
    - Each file contains comment `CANCER_SITE_MAP and classify_codes() provided by`
    - Each file's header Dependencies line references R/00_config.R and utils_cancer.R
  </acceptance_criteria>
  <done>8 cancer scripts (R/40, R/43-R/49) no longer define PREFIX_MAP or classify_codes(); they use the centralized versions from config and utils_cancer.R</done>
</task>

<task type="auto">
  <name>Task 2: Remove PREFIX_MAP and classify_codes() from R/28 and R/51</name>
  <files>R/28_episode_classification.R, R/51_gantt_data_export.R</files>
  <read_first>
    - R/28_episode_classification.R (full file -- understand PREFIX_MAP location and classify_codes usage)
    - R/51_gantt_data_export.R (full file -- understand PREFIX_MAP location and classify_codes usage)
    - R/utils/utils_cancer.R (verify classify_codes exists)
  </read_first>
  <action>
Apply the same removal pattern as Task 1 to the remaining 2 scripts:

**R/28_episode_classification.R:**
1. Delete the PREFIX_MAP definition block (starts at ~line 98, ~260+ lines)
2. Delete the classify_codes() function definition (~line 361)
3. Keep all classify_codes() CALL sites unchanged
4. Add comment: `# CANCER_SITE_MAP and classify_codes() provided by R/00_config.R + R/utils/utils_cancer.R`
5. Update header Dependencies to reference CANCER_SITE_MAP and classify_codes() sources

**R/51_gantt_data_export.R:**
1. Delete the PREFIX_MAP definition block (starts at ~line 108, ~260+ lines)
2. Delete the classify_codes() function definition (~line 371)
3. Keep all classify_codes() CALL sites unchanged
4. Add comment: `# CANCER_SITE_MAP and classify_codes() provided by R/00_config.R + R/utils/utils_cancer.R`
5. Update header Dependencies to reference CANCER_SITE_MAP and classify_codes() sources

After this task, `grep -rl "PREFIX_MAP <- c(" R/` should return ZERO matches in any file (only R/00_config.R has CANCER_SITE_MAP now), and `grep -rl "classify_codes <- function" R/` should return only `R/utils/utils_cancer.R`.
  </action>
  <verify>
    <automated>grep -rl "PREFIX_MAP <- c(" R/ 2>&1; echo "---"; grep -rl "classify_codes <- function" R/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PREFIX_MAP" R/28_episode_classification.R` returns 0
    - `grep -c "PREFIX_MAP" R/51_gantt_data_export.R` returns 0
    - `grep -c "classify_codes <- function" R/28_episode_classification.R` returns 0
    - `grep -c "classify_codes <- function" R/51_gantt_data_export.R` returns 0
    - `grep -c "classify_codes(" R/28_episode_classification.R` returns at least 1 (call sites preserved)
    - `grep -c "classify_codes(" R/51_gantt_data_export.R` returns at least 1 (call sites preserved)
    - `grep -rl "PREFIX_MAP <- c(" R/` returns no results (zero files define PREFIX_MAP)
    - `grep -rl "classify_codes <- function" R/` returns only R/utils/utils_cancer.R
    - Each file contains comment `CANCER_SITE_MAP and classify_codes() provided by`
  </acceptance_criteria>
  <done>R/28 and R/51 no longer define PREFIX_MAP or classify_codes(); complete codebase has exactly one definition of each (in config + utils_cancer.R)</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rl "PREFIX_MAP <- c(" R/` returns NO results
2. `grep -rl "classify_codes <- function" R/` returns ONLY `R/utils/utils_cancer.R`
3. `grep -rl "classify_codes(" R/` returns 10+ files (call sites preserved in all scripts)
4. `grep -c "CANCER_SITE_MAP <- c(" R/00_config.R` returns 1 (single source of truth)
5. All 10 modified scripts have updated Dependencies headers
</verification>

<success_criteria>
- PREFIX_MAP defined nowhere in codebase (replaced by CANCER_SITE_MAP in R/00_config.R)
- classify_codes() defined only in R/utils/utils_cancer.R
- All 10 scripts still call classify_codes() and function is provided via source chain
- ~2,860 lines of duplicate code removed
- No functional behavior change (same classification logic, same outputs)
</success_criteria>

<output>
After completion, create `.planning/phases/73-dry-consolidation/73-02-SUMMARY.md`
</output>
