---
phase: 28-per-patient-source-detection-by-date
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/24_per_patient_source_detection.R
autonomous: false
requirements:
  - PDSRC-01
  - PDSRC-02
  - PDSRC-03
  - PDSRC-04
  - PDSRC-05

must_haves:
  truths:
    - "User can see which ENCOUNTER.SOURCE values are present on every patient-date in one CSV"
    - "User can see source combination frequencies across all patient-dates"
    - "User can see per-source aggregate counts (encounters, patient-dates, patients)"
    - "User can run the script on HiPerGator and get results within seconds (data.table speed)"
    - "User can see HIPAA-suppressed counts in CSV outputs"
  artifacts:
    - path: "R/24_per_patient_source_detection.R"
      provides: "Per-patient-date source enumeration using data.table"
      min_lines: 200
    - path: "output/tables/patient_date_source_detail.csv"
      provides: "One row per (patient, date) with n_sources, source_combo, n_encounters"
    - path: "output/tables/source_combo_frequencies.csv"
      provides: "Source combination frequency summary with rank"
    - path: "output/tables/per_source_summary.csv"
      provides: "Per-SOURCE aggregate counts"
  key_links:
    - from: "R/24_per_patient_source_detection.R"
      to: "R/00_config.R"
      via: "source() at script start"
      pattern: 'source\("R/00_config.R"\)'
    - from: "R/24_per_patient_source_detection.R"
      to: "pcornet$ENCOUNTER"
      via: "Table load from pcornet list"
      pattern: "pcornet\\$ENCOUNTER"
    - from: "R/24_per_patient_source_detection.R"
      to: "output/tables/"
      via: "write_csv to CONFIG output_dir"
      pattern: "write_csv.*patient_date_source_detail"
---

<objective>
Create a standalone R script that enumerates which ENCOUNTER.SOURCE values are present on each patient-date using data.table for speed, replacing the Phase 25-26 pairwise overlap approach with a simpler per-date source detection strategy.

Purpose: The Phase 25-26 pairwise self-join approach was complex and slow. Phase 28 answers the same core question ("which sources are present on each date?") by directly grouping encounters by (patient, date) and listing sources present -- no joins needed.

Output: R/24_per_patient_source_detection.R script + 3 CSV files in output/tables/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-per-patient-source-detection-by-date-redo-phases-25-26-approach-by-detecting-which-sources-are-present-on-each-patient-date-counting-source-presence-per-date-rather-than-pairwise-overlap/28-CONTEXT.md
@.planning/phases/28-per-patient-source-detection-by-date-redo-phases-25-26-approach-by-detecting-which-sources-are-present-on-each-patient-date-counting-source-presence-per-date-rather-than-pairwise-overlap/28-RESEARCH.md

<interfaces>
<!-- Key patterns and contracts the executor needs. Extracted from codebase. -->

From R/00_config.R:
```r
CONFIG <- list(
  data_dir = "/orange/erin.mobley-hl.bcu/Mailhot_V1_20250915",
  output_dir = "output",
  # ... other config fields
)
```

From R/22_multi_source_overlap_detection.R (Phase 25 script -- reference for patterns):
```r
# Script structure pattern:
source("R/00_config.R")
library(...)
if (!exists("pcornet")) source("R/01_load_pcornet.R")

# HIPAA suppression helper (lines 47-60):
hipaa_suppress <- function(x) {
  x_num <- suppressWarnings(as.numeric(x))
  ifelse(!is.na(x_num) & x_num >= 1 & x_num <= 10, "<11", as.character(x))
}

# ADMIT_DATE parsing pattern (lines 86-108):
enc <- enc %>%
  mutate(admit_date_parsed = as.Date(ADMIT_DATE, format = "%Y-%m-%d"))
# ... fallback to parse_pcornet_date() if parse rate < 50%

# Valid encounter filtering (line 111-112):
enc_valid <- enc %>%
  filter(!is.na(admit_date_parsed), !is.na(SOURCE))

# Console banner pattern (lines 66-69):
message(glue("\n{strrep('=', 70)}"))
message("TITLE HERE")
message("Subtitle")
message(glue("{strrep('=', 70)}\n"))

# CSV output pattern (lines 418-431):
output_dir <- file.path(CONFIG$output_dir, "tables")
dir.create(output_dir, showWarnings = FALSE, recursive = TRUE)
write_csv(csv1, file.path(output_dir, "filename.csv"))
```

From R/01_load_pcornet.R:
```r
# Produces: pcornet$ENCOUNTER with columns:
#   ID, ENCOUNTERID, ADMIT_DATE, DISCHARGE_DATE, ENC_TYPE, SOURCE,
#   PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY, PROVIDERID, ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/24_per_patient_source_detection.R</name>
  <files>R/24_per_patient_source_detection.R</files>
  <read_first>
    - R/22_multi_source_overlap_detection.R (reference: script structure, HIPAA helpers, ADMIT_DATE parsing, console output patterns)
    - R/00_config.R (CONFIG object structure, output_dir path)
    - R/01_load_pcornet.R (pcornet$ENCOUNTER table structure, conditional loading pattern)
  </read_first>
  <action>
Create standalone R script R/24_per_patient_source_detection.R with the following structure. Per D-05, use data.table for all data manipulation (not dplyr). Per D-03, source enumeration only -- no field comparison. Per D-04, same-date grouping only -- no same-week window.

HEADER COMMENT BLOCK:
```
# ==============================================================================
# 24_per_patient_source_detection.R -- Per-patient source detection by date
# ==============================================================================
#
# Phase 28: Per-Patient Source Detection by Date
# Requirements: PDSRC-01, PDSRC-02, PDSRC-03, PDSRC-04, PDSRC-05
#
# Purpose: For each patient on each date, detect which ENCOUNTER.SOURCE values
#          are present and how many encounters each source contributes. Replaces
#          Phase 25-26 pairwise overlap approach with simpler per-date enumeration.
#          Uses data.table for speed (D-05).
#
# Output: 3 CSV files in output/tables/:
#   - patient_date_source_detail.csv   (PDSRC-01: all patient-dates)
#   - source_combo_frequencies.csv     (PDSRC-02: combo frequency summary)
#   - per_source_summary.csv           (PDSRC-03: per-source aggregate counts)
#
# Usage: source("R/24_per_patient_source_detection.R")
#
# Dependencies: Sources R/00_config.R (CONFIG, output_dir).
#   Conditionally sources R/01_load_pcornet.R for pcornet tables.
#   Optionally sources R/utils_dates.R if date parse rate < 50%.
#   Requires: pcornet$ENCOUNTER (ID, ENCOUNTERID, ADMIT_DATE, SOURCE)
#
# Standalone script -- NOT part of the main pipeline sequence.
# ==============================================================================
```

SECTION 0 - SETUP:
```r
source("R/00_config.R")
library(data.table)
library(glue)
library(readr)

# Load tables if not already loaded
if (!exists("pcornet")) source("R/01_load_pcornet.R")
```

SECTION 1 - HIPAA SUPPRESSION HELPER:
Reuse the same hipaa_suppress() function from R/22 (lines 47-50):
```r
hipaa_suppress <- function(x) {
  x_num <- suppressWarnings(as.numeric(x))
  ifelse(!is.na(x_num) & x_num >= 1 & x_num <= 10, "<11", as.character(x))
}
```

SECTION 2 - LOAD AND PREPARE ENCOUNTERS:
- Console banner: "PER-PATIENT SOURCE DETECTION BY DATE" / "Phase 28: Per-date source enumeration using data.table"
- Load pcornet$ENCOUNTER
- Convert immediately to data.table: `enc_dt <- as.data.table(pcornet$ENCOUNTER)`
- Log total encounters: `nrow(enc_dt)` with format(big.mark=',')
- Log unique patients: `enc_dt[, uniqueN(ID)]`
- Log unique ENCOUNTER.SOURCE values: `sort(unique(na.omit(enc_dt$SOURCE)))` pasted with collapse=", "
- Log encounters with NA SOURCE: `sum(is.na(enc_dt$SOURCE))`
- Parse ADMIT_DATE: `enc_dt[, admit_date_parsed := as.Date(ADMIT_DATE, format = "%Y-%m-%d")]`
- Compute parse rate; if < 50%, fallback to parse_pcornet_date() from R/utils_dates.R (same pattern as R/22 lines 86-108, but in data.table syntax)
- Overwrite ADMIT_DATE with parsed date: `enc_dt[, ADMIT_DATE := admit_date_parsed]`
- Drop temp column: `enc_dt[, admit_date_parsed := NULL]`
- Filter to valid rows: `enc_dt <- enc_dt[!is.na(ADMIT_DATE) & !is.na(SOURCE)]`
- Log how many encounters remain after filtering

SECTION 3 - PER-PATIENT-DATE SOURCE ENUMERATION (PDSRC-01):
Per D-01, include ALL dates (1+ sources), not just multi-source dates:
```r
patient_date_detail <- enc_dt[, .(
  n_sources    = uniqueN(SOURCE),
  source_combo = paste(sort(unique(SOURCE)), collapse = "+"),
  n_encounters = .N
), by = .(ID, ADMIT_DATE)]
```
- Log total patient-date rows: `nrow(patient_date_detail)` with format(big.mark=',')
- Log single-source count: `patient_date_detail[n_sources == 1, .N]` and percentage
- Log multi-source count: `patient_date_detail[n_sources > 1, .N]` and percentage
- Log patients with at least one multi-source date: `patient_date_detail[n_sources > 1, uniqueN(ID)]`
- Sort: `setorder(patient_date_detail, source_combo, ID, ADMIT_DATE)`

SECTION 4 - SOURCE COMBINATION FREQUENCIES (PDSRC-02):
Per D-02, aggregate summary of source combination frequencies:
```r
combo_freq <- patient_date_detail[, .(
  n_patient_dates    = .N,
  n_total_encounters = sum(n_encounters)
), by = source_combo][
  order(-n_patient_dates)
][
  , rank := .I
]
```
- Log unique source combinations count: `nrow(combo_freq)`
- Log top 10 combinations using for-loop with format: `  #1 UFH: 123,456 patient-dates, 234,567 encounters`
- Log multi-source combinations separately: filter `grepl("\\+", source_combo)` and show count and list

SECTION 5 - PER-SOURCE SUMMARY COUNTS (PDSRC-03):
Per D-02, per-source aggregate:
```r
per_source_summary <- enc_dt[, .(
  total_encounters = .N,
  n_patient_dates  = uniqueN(paste(ID, ADMIT_DATE)),
  n_patients       = uniqueN(ID)
), by = SOURCE][
  order(SOURCE)
]
```
- Log per-source summary in console with for-loop format: `  UFH: 123,456 encounters | 45,678 patient-dates | 1,234 patients`

SECTION 6 - HIPAA SUPPRESSION AND CSV OUTPUT (PDSRC-04):
```r
output_dir <- file.path(CONFIG$output_dir, "tables")
dir.create(output_dir, showWarnings = FALSE, recursive = TRUE)
```

CSV 1: patient_date_source_detail.csv
- Create copy for CSV: `csv1 <- copy(patient_date_detail)`
- HIPAA suppress n_sources and n_encounters: `csv1[, n_sources := hipaa_suppress(n_sources)]` and same for n_encounters
- Write: `write_csv(csv1, file.path(output_dir, "patient_date_source_detail.csv"))`
- Log: row count and filename

CSV 2: source_combo_frequencies.csv
- Create copy: `csv2 <- copy(combo_freq)`
- HIPAA suppress n_patient_dates and n_total_encounters
- Write and log

CSV 3: per_source_summary.csv
- Create copy: `csv3 <- copy(per_source_summary)`
- HIPAA suppress total_encounters, n_patient_dates, n_patients
- Write and log

Use readr::write_csv() for all CSV writes (consistent with Phase 25).

SECTION 7 - FINAL CONSOLE SUMMARY (PDSRC-05):
Follow R/22 final summary pattern (lines 504-554):
```r
message(glue("\n{strrep('=', 70)}"))
message("PER-PATIENT SOURCE DETECTION BY DATE -- SUMMARY")
message(glue("{strrep('=', 70)}"))
```
- Total encounters analyzed (with big.mark comma formatting)
- Total unique patients
- ADMIT_DATE parse rate
- Total patient-date rows
- Single-source vs multi-source patient-date counts and percentages
- Patients with at least one multi-source date
- Per-source breakdown (encounters, patient-dates, patients) -- for-loop
- Top 10 source combinations -- for-loop
- List of 3 CSV files written to output_dir
- End banner: "END OF PER-PATIENT SOURCE DETECTION BY DATE"
  </action>
  <verify>
    <automated>
      powershell -Command "if (Test-Path 'R/24_per_patient_source_detection.R') { $lines = (Get-Content 'R/24_per_patient_source_detection.R').Count; $c = Get-Content 'R/24_per_patient_source_detection.R' -Raw; $checks = @('library(data.table)', 'hipaa_suppress', 'uniqueN(SOURCE)', 'source_combo', 'patient_date_source_detail.csv', 'source_combo_frequencies.csv', 'per_source_summary.csv', 'write_csv'); $pass = $true; foreach ($s in $checks) { if ($c -notmatch [regex]::Escape($s)) { Write-Host \"MISSING: $s\"; $pass = $false } }; if ($lines -lt 200) { Write-Host \"TOO SHORT: $lines lines (need 200+)\"; $pass = $false }; if ($c -match 'library\(dplyr\)') { Write-Host 'FAIL: contains library(dplyr)'; $pass = $false }; if ($pass) { Write-Host \"ALL CHECKS PASSED ($lines lines)\" } } else { Write-Host 'FILE NOT FOUND'; exit 1 }"
    </automated>
  </verify>
  <acceptance_criteria>
    - R/24_per_patient_source_detection.R exists and is 200+ lines
    - File contains `library(data.table)` (per D-05, not library(dplyr))
    - File contains `source("R/00_config.R")` in SECTION 0
    - File contains `if (!exists("pcornet")) source("R/01_load_pcornet.R")`
    - File contains `hipaa_suppress <- function(x)` helper
    - File contains `uniqueN(SOURCE)` for data.table distinct counting
    - File contains `paste(sort(unique(SOURCE)), collapse = "+")` for alphabetical combos
    - File contains `by = .(ID, ADMIT_DATE)` for per-patient-date grouping
    - File does NOT contain `library(dplyr)` (data.table only per D-05)
    - File does NOT contain `same_week` or `near_duplicate` (per D-04, same-date only)
    - File does NOT contain `ENC_TYPE` or `PAYER_TYPE_PRIMARY` in group-by operations (per D-03, source detection only)
    - File writes 3 CSV files: patient_date_source_detail.csv, source_combo_frequencies.csv, per_source_summary.csv
    - File uses `write_csv()` from readr (not base R write.csv)
    - File uses `CONFIG$output_dir` for output path
    - Console summary includes total encounters, parse rate, single vs multi-source counts, per-source breakdown, top 10 combos, file list
  </acceptance_criteria>
  <done>R/24_per_patient_source_detection.R exists with data.table-based per-patient-date source enumeration, ALL dates included (not just multi-source), 3 CSV outputs with HIPAA suppression, console summary following Phase 25 banner pattern</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify script output on HiPerGator</name>
  <files>R/24_per_patient_source_detection.R</files>
  <action>Human verification of HiPerGator execution output. No automated action -- user runs the script on HiPerGator and confirms results.</action>
  <what-built>R/24_per_patient_source_detection.R -- standalone per-patient source detection script using data.table. Produces 3 CSV files with HIPAA-suppressed counts and a console summary.</what-built>
  <how-to-verify>
    1. On HiPerGator RStudio, run: source("R/24_per_patient_source_detection.R")
    2. Verify console output shows:
       - Total encounters and patients loaded
       - ADMIT_DATE parse rate (expect 90%+)
       - Patient-date row count (ALL dates, not just multi-source)
       - Single-source vs multi-source breakdown with percentages
       - Per-source summary (encounters, patient-dates, patients)
       - Top 10 source combinations
       - 3 CSV files written to output/tables/
    3. Check output/tables/patient_date_source_detail.csv:
       - Has columns: ID, ADMIT_DATE, n_sources, source_combo, n_encounters
       - Includes rows with n_sources=1 (single-source dates included per D-01)
       - Count values 1-10 show as "<11" (HIPAA suppression)
    4. Check output/tables/source_combo_frequencies.csv:
       - Has columns: source_combo, n_patient_dates, n_total_encounters, rank
       - Single-source combos (no "+") present alongside multi-source
    5. Check output/tables/per_source_summary.csv:
       - Has columns: SOURCE, total_encounters, n_patient_dates, n_patients
       - All known sources appear (AMS, UMI, FLM, VRT, UFH or similar)
    6. Verify execution speed: script should complete in seconds (data.table)
  </how-to-verify>
  <verify>User confirms console output and CSV files are correct on HiPerGator</verify>
  <done>Script runs on HiPerGator without errors, 3 CSV files produced with correct columns, HIPAA suppression active, console summary complete</done>
  <resume-signal>Type "approved" if output looks correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- R/24_per_patient_source_detection.R exists with 200+ lines
- Script uses data.table (not dplyr) for all data manipulation per D-05
- Script groups by (ID, ADMIT_DATE) only -- no same-week window per D-04
- Script detects source presence only -- no field comparison per D-03
- Script includes ALL patient-dates (1+ sources) per D-01
- Script produces 3 aggregate CSV files per D-02
- HIPAA suppression applied to CSV count columns (1-10 replaced with "<11")
- Console output follows established banner and summary patterns from R/22
- HiPerGator execution produces expected CSV files and console output
</verification>

<success_criteria>
- R/24_per_patient_source_detection.R runs on HiPerGator without errors
- 3 CSV files written to output/tables/ with correct columns and HIPAA suppression
- Console summary shows total encounters, parse rate, single vs multi-source breakdown, per-source counts, top 10 combos
- Script completes in seconds (data.table performance vs Phase 25's dplyr approach)
- All 5 user decisions (D-01 through D-05) honored in implementation
</success_criteria>

<output>
After completion, create `.planning/phases/28-per-patient-source-detection-by-date-redo-phases-25-26-approach-by-detecting-which-sources-are-present-on-each-patient-date-counting-source-presence-per-date-rather-than-pairwise-overlap/28-01-SUMMARY.md`
</output>
