---
phase: 153-patient-zip-calendar-and-best-zip-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/utils/utils_zip_calendar.R
autonomous: true
requirements: [DIST-02, DIST-03]
must_haves:
  truths:
    - "Every encounter's patient ZIP is the ZIP active on its ADMIT_DATE, or the temporally nearest one when no period covers it"
    - "Each picked patient ZIP carries a zip5_patient_source provenance label and a signed days_offset"
    - "ZIP9 on the patient side is reduced to ZIP5 before it is used for distance"
    - "An in-range ZIP5 always beats an out-of-range ZIP9 when picking the best ZIP"
    - "get_zip9_at_date() in utils_address.R is unchanged"
  artifacts:
    - path: "R/utils/utils_zip_calendar.R"
      provides: "build_patient_zip_calendar(), pick_best_zip(), compute_encounter_distance()"
      min_lines: 90
      contains: "build_patient_zip_calendar <- function"
  key_links:
    - from: "R/utils/utils_zip_calendar.R"
      to: "R/utils/utils_address.R"
      via: "normalize_zip9 / normalize_zip5 / is_sentinel_zip5 reuse (no redefinition)"
      pattern: "normalize_zip9"
    - from: "pick_best_zip()"
      to: "two-zone arrange"
      via: "in-range ranked by zip_len desc; out-of-range ranked by abs(days_offset) with zip tier ignored"
      pattern: "days_offset"
---

<objective>
Create `R/utils/utils_zip_calendar.R` with three exported pure functions —
`build_patient_zip_calendar()`, `pick_best_zip()`, and
`compute_encounter_distance()` — implementing AM cleaning rules 2 and 3
(DIST-02: ZIP9 reduced to ZIP5 before distance; DIST-03: encounters with no
in-range patient ZIP receive the temporally nearest ZIP from the same patient's
address history, with provenance). All logic follows Appendix A of
`MILESTONE_encounter_distance.md`, with the `pick_best_zip()` `arrange()`
revised per D-04 (two-zone ranking).

Purpose: A reusable, testable helper file that R/122 (Plan 02) wires in to
replace the backward-only `get_zip9_at_date()` residence lookup, which is wrong
for this domain because "closest" means closest in either direction (D-03).
Output: `R/utils/utils_zip_calendar.R`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-patient-zip-calendar-and-best-zip-selection/153-CONTEXT.md
@MILESTONE_encounter_distance.md

<interfaces>
<!-- Reuse these from R/utils/utils_address.R — do NOT redefine them. -->

From R/utils/utils_address.R:
```r
normalize_zip9(zip)        # -> 9-digit string or NA (strips hyphen; accepts 8/9-digit numeric)
normalize_zip5(zip9_clean) # -> first 5 chars of a clean ZIP9 (str_sub(x, 1, 5))
normalize_zip5_raw(zip)    # -> 5-digit string or NA (free-text safe)
is_sentinel_zip5(zip5)     # -> TRUE for placeholder ZIP5 (repeated digits, < 00501), NA passthrough
haversine_km(lat1,lon1,lat2,lon2)  # test-only cross-check
.centroid_zip9_lookup_cache <- list(key = NULL, value = NULL)  # memoization pattern to follow
```
utils_address.R is auto-sourced by R/00_config.R, so these are in scope wherever
utils_zip_calendar.R is sourced after it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create utils_zip_calendar.R with build_patient_zip_calendar() and pick_best_zip()</name>
  <files>R/utils/utils_zip_calendar.R</files>
  <read_first>
    - MILESTONE_encounter_distance.md (Appendix A — reference code for all three functions; note the D-04 arrange revision)
    - .planning/phases/153-patient-zip-calendar-and-best-zip-selection/153-CONTEXT.md (decisions D-01..D-05, especially D-04 two-zone ranking and D-05 tie-break)
    - R/utils/utils_address.R (normalize_zip9/normalize_zip5/normalize_zip5_raw/is_sentinel_zip5 contracts — reuse, do not redefine)
    - R/115_zip_stability_counts.R (SECTION 1B pure-function-before-probe-gate convention; internal naming/pipe style)
  </read_first>
  <action>
Create `R/utils/utils_zip_calendar.R`. Start with a header comment block (Purpose,
Inputs, Outputs, Dependencies, Requirements: Phase 153 DIST-02/DIST-03) matching
the R/122/R/115 header style. Add `suppressPackageStartupMessages({ library(dplyr); library(zipcodeR) })`.
Do NOT redefine `normalize_zip9`, `normalize_zip5`, `normalize_zip5_raw`, or
`is_sentinel_zip5` — they come from utils_address.R (auto-sourced by R/00_config.R).

Define `build_patient_zip_calendar(addr, study_end = as.Date("2025-03-31"))`:
- `addr` has columns ID, ADDRESS_ZIP9, ADDRESS_ZIP5, ADDRESS_PERIOD_START, ADDRESS_PERIOD_END.
- `transmute()` to: ID; `zip9 = normalize_zip9(ADDRESS_ZIP9)`;
  `zip5 = coalesce(normalize_zip5(ADDRESS_ZIP5), substr(zip9, 1, 5))`;
  `start = as.Date(ADDRESS_PERIOD_START)`;
  `end = coalesce(as.Date(ADDRESS_PERIOD_END), study_end)`.
- `filter(!is.na(zip5))`.
- Add `filter(!is_sentinel_zip5(zip5))` to drop placeholder ZIPs (CONTEXT code_context: use is_sentinel_zip5 to filter placeholder ZIPs from the calendar). Because is_sentinel_zip5 returns NA for NA input, this filter must be NA-safe — the prior `filter(!is.na(zip5))` guarantees zip5 is non-NA here, so `!is_sentinel_zip5(zip5)` is safe.
- `mutate(zip_key = coalesce(zip9, zip5), zip_len = if_else(is.na(zip9), 5L, 9L))`.
- Collapse exact duplicates and touching/overlapping same-ZIP periods into one run:
  `arrange(ID, zip_key, start)`, then `group_by(ID, zip_key)`,
  `mutate(new_run = start > lag(cummax(as.integer(end)), default = -Inf) + 1)`,
  `mutate(run = cumsum(new_run))`,
  `group_by(ID, zip_key, zip9, zip5, zip_len, run)`,
  `summarise(start = min(start), end = max(end), .groups = "drop")`,
  `select(ID, zip9, zip5, zip_len, start, end)`,
  `arrange(ID, start)`.

Define `pick_best_zip(enc, cal)` where `enc` has ID, ENCOUNTERID, ADMIT_DATE:
- `mutate(ADMIT_DATE = as.Date(ADMIT_DATE))`,
  `inner_join(cal, by = "ID", relationship = "many-to-many")`,
- `mutate(in_range = ADMIT_DATE >= start & ADMIT_DATE <= end,`
  ` days_offset = case_when(in_range ~ 0L, ADMIT_DATE < start ~ as.integer(ADMIT_DATE - start), TRUE ~ as.integer(ADMIT_DATE - end)))`.
  days_offset is negative when the period is AFTER the encounter (ADMIT_DATE < start), positive when the period is BEFORE the encounter.
- TWO-ZONE ARRANGE (D-04 — this is the required departure from Appendix A's single flat arrange). Rank so that: (1) in-range candidates always beat out-of-range (an in-range ZIP5 beats an out-of-range ZIP9); (2) WITHIN in-range, ZIP9 beats ZIP5 (`zip_len` desc); (3) WITHIN out-of-range, ZIP tier is IGNORED — rank by `abs(days_offset)` only; (4) tie on `abs(days_offset)` → prefer the earlier period via `desc(days_offset)` (positive offset = period before the encounter sorts ahead of negative = period after). Implement exactly:
  `arrange(ID, ENCOUNTERID, desc(in_range), if_else(in_range, -zip_len, 0L), abs(days_offset), desc(days_offset))`.
  (Rationale: `if_else(in_range, -zip_len, 0L)` ascending puts zip_len=9 (-9) ahead of zip_len=5 (-5) for in-range rows, and applies 0 uniformly to out-of-range rows so their zip tier does not affect ordering — only `abs(days_offset)` then `desc(days_offset)` do. Any clean equivalent branching that yields this exact priority is acceptable per CONTEXT Claude's Discretion.)
- `group_by(ID, ENCOUNTERID)`, `mutate(n_candidates_in_range = sum(in_range))`, `slice(1)`, `ungroup()`.
- `transmute(ID, ENCOUNTERID, ADMIT_DATE, zip9_patient = zip9, zip5_patient = zip5,`
  ` zip5_patient_source = case_when(in_range & zip_len == 9 ~ "in_range_zip9", in_range ~ "in_range_zip5", zip_len == 9 ~ "nearest_zip9", TRUE ~ "nearest_zip5"),`
  ` days_offset, n_candidates_in_range)`.

Follow R/115 SECTION 1B convention: these are pure functions with no file I/O, so a testthat file can source the whole file without HiPerGator data.
  </action>
  <verify>
    <automated>MISSING — Plan 03 creates tests/testthat/test-utils-zip-calendar.R; local check: grep -q "build_patient_zip_calendar <- function" R/utils/utils_zip_calendar.R && grep -q "pick_best_zip <- function" R/utils/utils_zip_calendar.R</automated>
  </verify>
  <acceptance_criteria>
    - R/utils/utils_zip_calendar.R contains `build_patient_zip_calendar <- function`
    - R/utils/utils_zip_calendar.R contains `pick_best_zip <- function`
    - grep finds `desc(in_range)` AND `abs(days_offset)` AND `desc(days_offset)` in the same arrange call region of R/utils/utils_zip_calendar.R
    - grep finds all four literal source labels: `in_range_zip9`, `in_range_zip5`, `nearest_zip9`, `nearest_zip5`
    - grep finds `n_candidates_in_range = sum(in_range)`
    - grep finds `is_sentinel_zip5(zip5)` (placeholder filter present)
    - `grep -c "normalize_zip9 <- function" R/utils/utils_zip_calendar.R` returns 0 (function NOT redefined)
    - grep finds `as.Date("2025-03-31")` (study_end default)
  </acceptance_criteria>
  <done>build_patient_zip_calendar() and pick_best_zip() defined with the D-04 two-zone arrange, all four zip5_patient_source labels, n_candidates_in_range, sentinel filtering, and no redefinition of utils_address.R helpers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add compute_encounter_distance() with distance_status classification</name>
  <files>R/utils/utils_zip_calendar.R</files>
  <read_first>
    - R/utils/utils_zip_calendar.R (current state after Task 1 — the two functions it must call)
    - MILESTONE_encounter_distance.md (Appendix A part C — compute_encounter_distance reference)
    - .planning/phases/153-patient-zip-calendar-and-best-zip-selection/153-CONTEXT.md (D-02: rule 3 patient-side only; distance_status set = {computed, facility_zip_missing, patient_zip_missing, zip_not_in_db})
  </read_first>
  <action>
Append `compute_encounter_distance(enc, cal)` to `R/utils/utils_zip_calendar.R`,
where `enc` has ID, ENCOUNTERID, ADMIT_DATE, zip5_facility (facility ZIP5 already
normalized upstream by R/122):
- `best <- pick_best_zip(enc, cal)`.
- `out <- enc |> select(ID, ENCOUNTERID, zip5_facility) |> left_join(best, by = c("ID", "ENCOUNTERID"))`.
- Compute distance on DISTINCT (zip5_patient, zip5_facility) pairs for performance,
  then join back:
  `pairs <- out |> distinct(zip5_patient, zip5_facility) |> filter(!is.na(zip5_patient), !is.na(zip5_facility))`.
  `pairs$distance_mi <- zip_distance(pairs$zip5_patient, pairs$zip5_facility, units = "miles")$distance`.
- `out |> left_join(pairs, by = c("zip5_patient", "zip5_facility")) |> mutate(`
  ` distance_km = distance_mi * 1.609344,`
  ` distance_status = case_when(is.na(zip5_facility) ~ "facility_zip_missing", is.na(zip5_patient) ~ "patient_zip_missing", is.na(distance_mi) ~ "zip_not_in_db", TRUE ~ "computed"))`.
- Per D-02, rule 3 (nearest-ZIP fill) is PATIENT-SIDE ONLY: a blank facility ZIP is
  reported as `facility_zip_missing`, never imputed. The `case_when` order above
  guarantees this — facility-missing is classified first. Add a short comment stating
  this so a future reader does not "fix" it by imputing.
- Add a roxygen block above the function documenting: inputs, the four
  `distance_status` values, and that `days_offset`/`zip5_patient_source`/
  `n_candidates_in_range` are carried through from pick_best_zip() for provenance.
  </action>
  <verify>
    <automated>MISSING — Plan 03 creates tests/testthat/test-utils-zip-calendar.R; local check: grep -q "compute_encounter_distance <- function" R/utils/utils_zip_calendar.R</automated>
  </verify>
  <acceptance_criteria>
    - R/utils/utils_zip_calendar.R contains `compute_encounter_distance <- function`
    - grep finds all four literal distance_status values: `facility_zip_missing`, `patient_zip_missing`, `zip_not_in_db`, `computed`
    - grep finds `zip_distance(` and `units = "miles"`
    - grep finds `distance_km = distance_mi * 1.609344`
    - grep finds `distinct(zip5_patient, zip5_facility)` (distinct-pairs performance pattern)
    - facility_zip_missing appears BEFORE patient_zip_missing in the case_when (verify by reading the case_when block: facility-missing classified first, no facility imputation)
  </acceptance_criteria>
  <done>compute_encounter_distance() defined, computing zipcodeR miles distance on distinct patient/facility ZIP5 pairs, with the four-value distance_status set and patient-side-only fill (facility_zip_missing never imputed).</done>
</task>

</tasks>

<verification>
- `R/utils/utils_zip_calendar.R` defines exactly three top-level functions:
  build_patient_zip_calendar, pick_best_zip, compute_encounter_distance.
- None of normalize_zip9/normalize_zip5/normalize_zip5_raw/is_sentinel_zip5 are
  redefined (reused from utils_address.R).
- `get_zip9_at_date()` in utils_address.R is untouched (`git diff --stat R/utils/utils_address.R` shows no change).
- Brace/paren balance holds (Rscript unavailable in this Windows environment — use the Node brace-balance script if present, else manual balance check).
</verification>

<success_criteria>
- Three functions present with the D-04 two-zone ranking implemented (not a single flat arrange).
- zip5_patient_source ∈ {in_range_zip9, in_range_zip5, nearest_zip9, nearest_zip5}; days_offset signed integer; n_candidates_in_range computed.
- distance_status ∈ {computed, facility_zip_missing, patient_zip_missing, zip_not_in_db}.
- utils_address.R unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/153-patient-zip-calendar-and-best-zip-selection/153-01-SUMMARY.md`
</output>
