---
phase: 153-patient-zip-calendar-and-best-zip-selection
plan: 03
type: tdd
wave: 2
depends_on: [153-01]
files_modified:
  - tests/testthat/test-utils-zip-calendar.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements: [DIST-07]
must_haves:
  truths:
    - "Duplicate address periods collapse to one calendar row"
    - "Overlapping same-ZIP periods merge; different ZIPs on the same date both survive and ZIP9 wins in-range"
    - "Equal before/after offsets choose the earlier period (D-05)"
    - "A patient with no address history yields patient_zip_missing"
    - "An open-ended period closes at study end (2025-03-31)"
    - "R/88 has a Phase 153 structural-check section for utils_zip_calendar.R"
  artifacts:
    - path: "tests/testthat/test-utils-zip-calendar.R"
      provides: "unit tests for all three functions"
      contains: "test_that"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15ai Phase 153 structural checks"
      contains: "utils_zip_calendar"
  key_links:
    - from: "tests/testthat/test-utils-zip-calendar.R"
      to: "R/utils/utils_zip_calendar.R"
      via: "sources the file and exercises pure functions with in-memory frames (no HiPerGator data)"
      pattern: "utils_zip_calendar"
---

<objective>
Add `tests/testthat/test-utils-zip-calendar.R` covering all three functions from
Plan 01, and add a Phase 153 structural-check section (`Section 15ai`) to
`R/88_smoke_test_comprehensive.R` (DIST-07). Tests exercise the pure functions
with small in-memory frames — no HiPerGator data required — matching the R/115
`sys.source()`/testthat convention.

Purpose: Lock the D-04 two-zone ranking and D-05 tie-break behavior with
computed-value assertions (not just column-name greps), and give R/88 a
single-implementation / registration guard for the new util file.
Output: `tests/testthat/test-utils-zip-calendar.R`,
amended `R/88_smoke_test_comprehensive.R`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-patient-zip-calendar-and-best-zip-selection/153-CONTEXT.md
@MILESTONE_encounter_distance.md
@R/utils/utils_zip_calendar.R

<interfaces>
```r
build_patient_zip_calendar(addr, study_end = as.Date("2025-03-31"))
  # -> tibble(ID, zip9, zip5, zip_len, start, end)
pick_best_zip(enc, cal)
  # -> tibble(ID, ENCOUNTERID, ADMIT_DATE, zip9_patient, zip5_patient,
  #           zip5_patient_source, days_offset, n_candidates_in_range)
compute_encounter_distance(enc, cal)  # enc needs zip5_facility; -> adds distance_mi/km, distance_status
```
zip5_patient_source ∈ {in_range_zip9, in_range_zip5, nearest_zip9, nearest_zip5}
distance_status ∈ {computed, facility_zip_missing, patient_zip_missing, zip_not_in_db}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test-utils-zip-calendar.R covering calendar collapse, best-ZIP ranking, and distance_status</name>
  <files>tests/testthat/test-utils-zip-calendar.R</files>
  <read_first>
    - R/utils/utils_zip_calendar.R (function signatures and exact column outputs to assert against)
    - .planning/phases/153-patient-zip-calendar-and-best-zip-selection/153-CONTEXT.md (D-04 two-zone ranking, D-05 tie-break, success criteria)
    - MILESTONE_encounter_distance.md (Phase 153-03 test list: duplicate collapse, overlap merge, ZIP9 wins in-range, equal offsets → earlier period, no-address → patient_zip_missing, open period closes at study end)
    - tests/testthat/test-utils-address.R (existing convention for sourcing utils files in this project's testthat setup)
  </read_first>
  <behavior>
    - Test 1 (duplicate collapse): two identical (ID, ZIP, period) address rows produce exactly ONE calendar row from build_patient_zip_calendar().
    - Test 2 (overlap merge): two overlapping/touching periods with the SAME ZIP merge into one run with start = min(start), end = max(end).
    - Test 3 (different ZIPs same date, ZIP9 wins in-range): an encounter whose ADMIT_DATE falls inside both a ZIP5 period and a ZIP9 period picks the ZIP9 → zip5_patient_source == "in_range_zip9"; n_candidates_in_range == 2.
    - Test 4 (equal offsets → earlier period, D-05): an encounter with no covering period but two candidate periods at equal |days_offset| — one before (positive offset), one after (negative offset) — picks the EARLIER (before) period. Assert the returned days_offset is POSITIVE (period before encounter).
    - Test 5 (in-range ZIP5 beats out-of-range ZIP9): an encounter with an in-range ZIP5 period and an out-of-range ZIP9 period picks the ZIP5 → zip5_patient_source == "in_range_zip5".
    - Test 6 (no address history → patient_zip_missing): compute_encounter_distance() for an encounter whose ID has no calendar rows yields distance_status == "patient_zip_missing".
    - Test 7 (open-ended period closes at study end): a row with NA ADDRESS_PERIOD_END produces end == as.Date("2025-03-31").
    - Test 8 (out-of-range nearest ignores ZIP tier): among two out-of-range candidates, a nearer-in-time ZIP5 beats a farther ZIP9 → zip5_patient_source == "nearest_zip5" (confirms zip tier is ignored out of range, D-04).
  </behavior>
  <action>
Create `tests/testthat/test-utils-zip-calendar.R`. Source the util file the way
this project's other testthat files do (see tests/testthat/test-utils-address.R —
it relies on R/00_config.R's auto-load, or an explicit source of the utils file).
Because build_patient_zip_calendar/pick_best_zip are pure (no file I/O), construct
small in-memory `addr`/`enc` tibbles inline and assert on computed values.

Note on zipcodeR in Test 6: compute_encounter_distance() calls
`zipcodeR::zip_distance()`. For the patient_zip_missing case the patient ZIP is
NA so `pairs` is empty and zip_distance() is never called with real ZIPs — the
test does not require zipcodeR to resolve any actual centroid, only that the
distance_status classification fires. If zipcodeR is unavailable in the test
environment, wrap Test 6's distance call in `skip_if_not_installed("zipcodeR")`;
Tests 1-5,7,8 exercise build_patient_zip_calendar()/pick_best_zip() only and must
NOT be skipped.

Write these as computed-value assertions, e.g.:
- `expect_equal(nrow(build_patient_zip_calendar(dup_addr)), 1L)` (Test 1).
- `expect_equal(build_patient_zip_calendar(overlap_addr)$start, as.Date("2015-01-01"))` etc. (Test 2).
- `expect_equal(pick_best_zip(enc, cal)$zip5_patient_source, "in_range_zip9")` (Test 3).
- `expect_true(pick_best_zip(enc, cal)$days_offset > 0)` (Test 4 — earlier period wins).
- `expect_equal(build_patient_zip_calendar(open_addr)$end, as.Date("2025-03-31"))` (Test 7).

Use ISO date strings in the input frames so as.Date() parses them directly.
  </action>
  <verify>
    <automated>cd "C:/Users/Owner/Documents/insurance_investigation" && Rscript -e "testthat::test_file('tests/testthat/test-utils-zip-calendar.R')" 2>&1 | tail -20 || echo "Rscript unavailable — defer to HiPerGator run"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-utils-zip-calendar.R exists and contains at least 8 `test_that(` blocks
    - grep finds `in_range_zip9`, `in_range_zip5`, `nearest_zip5` as asserted expected values
    - grep finds `as.Date("2025-03-31")` (study-end close assertion)
    - grep finds `days_offset > 0` OR an equivalent assertion that the earlier period wins the D-05 tie
    - grep finds `patient_zip_missing` (Test 6 distance_status assertion)
    - grep finds `build_patient_zip_calendar(`, `pick_best_zip(`, and `compute_encounter_distance(` (all three functions exercised)
  </acceptance_criteria>
  <done>test-utils-zip-calendar.R covers duplicate collapse, overlap merge, in-range ZIP9 win, in-range-ZIP5-beats-out-of-range-ZIP9, D-05 equal-offset earlier-period tie-break, out-of-range nearest ignoring ZIP tier, no-history patient_zip_missing, and open-period study-end close — as computed-value assertions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add R/88 Section 15ai Phase 153 structural checks</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (Section 15ah Phase 152 block ~lines 5261-5335 as the pattern to copy; the summary-line block ~lines 5470-5476 for the SMOKE-153 footer)
    - R/utils/utils_zip_calendar.R (function names to assert present)
    - R/122_encounter_distance.R (compute_encounter_distance wiring to assert present)
    - R/SCRIPT_INDEX.md (utils_zip_calendar registration to assert)
  </read_first>
  <action>
Add a new `Section 15ai: Phase 153 patient ZIP calendar + best-ZIP selection`
block to `R/88_smoke_test_comprehensive.R`, immediately after the Section 15ah
Phase 152 block (~line 5335). Copy the Section 15ah `check_*()`/counter pattern
exactly (define a `check_153()` helper and `p153_pass`/`p153_fail` counters, ending
with `message(glue("\nSection 15ai: {p153_pass} PASS, {p153_fail} FAIL"))`).

Checks (all grep/file-existence based, no HiPerGator data):
1. `R/utils/utils_zip_calendar.R` exists.
2. `build_patient_zip_calendar` defined in utils_zip_calendar.R (single definition — `sum(grepl("build_patient_zip_calendar <- function", lines)) == 1`).
3. `pick_best_zip` defined in utils_zip_calendar.R (single definition).
4. `compute_encounter_distance` defined in utils_zip_calendar.R (single definition).
5. utils_zip_calendar.R does NOT redefine normalize_zip9 (`sum(grepl("normalize_zip9 <- function", lines)) == 0`).
6. Two-zone arrange present: utils_zip_calendar.R contains `desc(in_range)` AND `abs(days_offset)` AND `desc(days_offset)`.
7. All four zip5_patient_source labels present in utils_zip_calendar.R.
8. All four distance_status labels present in utils_zip_calendar.R.
9. R/122 sources utils_zip_calendar.R (`source("R/utils/utils_zip_calendar.R")` in R/122).
10. R/122 calls compute_encounter_distance().
11. R/122 no longer calls `res_lookup <- get_zip9_at_date` (Phase 153 SECTION 4 replacement) — `sum(grepl("res_lookup <- get_zip9_at_date", r122_lines)) == 0`.
12. R/122 reports `n_candidates_in_range > 1` (QC).
13. tests/testthat/test-utils-zip-calendar.R exists.
14. `utils_zip_calendar` present in R/SCRIPT_INDEX.md.

Add a `SMOKE-153-01` summary line to the footer summary block (~line 5475) matching
the SMOKE-152/SMOKE-139 format, stating this section validates Phase 153
structural integrity (14 checks).
  </action>
  <verify>
    <automated>grep -q "Section 15ai" R/88_smoke_test_comprehensive.R && grep -q "SMOKE-153-01" R/88_smoke_test_comprehensive.R && grep -q "p153_pass" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains `Section 15ai`
    - R/88_smoke_test_comprehensive.R contains `check_153` and `p153_pass` and `p153_fail`
    - R/88_smoke_test_comprehensive.R contains `build_patient_zip_calendar <- function` (as a grep pattern inside a check) and `compute_encounter_distance`
    - R/88_smoke_test_comprehensive.R contains `res_lookup <- get_zip9_at_date` (as the pattern being asserted ABSENT from R/122)
    - R/88_smoke_test_comprehensive.R contains `SMOKE-153-01`
    - `message(glue("\nSection 15ai: {p153_pass} PASS, {p153_fail} FAIL"))` present
  </acceptance_criteria>
  <done>R/88 Section 15ai added with 14 grep/file-existence checks (utils_zip_calendar single-implementation, no normalize_zip9 redefinition, two-zone arrange, provenance/status labels, R/122 wiring + old-lookup-removed, test file, SCRIPT_INDEX registration) plus a SMOKE-153-01 footer line, copying the Section 15ah pattern.</done>
</task>

</tasks>

<verification>
- tests/testthat/test-utils-zip-calendar.R covers all six milestone-listed cases
  plus the in-range-ZIP5-beats-out-of-range-ZIP9 and out-of-range-nearest cases,
  as computed-value assertions.
- R/88 Section 15ai present with p153 counters and SMOKE-153-01 footer.
- Rscript may be unavailable in this Windows environment; if so, test execution is
  deferred to a HiPerGator run (the grep-based acceptance criteria still hold locally).
</verification>

<success_criteria>
- All three util functions exercised by unit tests with computed-value assertions.
- D-05 tie-break (earlier period wins) and D-04 two-zone ranking both asserted.
- R/88 Section 15ai green on structural checks; SMOKE-153-01 registered.
- DIST-07 test-and-smoke portion satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/153-patient-zip-calendar-and-best-zip-selection/153-03-SUMMARY.md`
</output>
