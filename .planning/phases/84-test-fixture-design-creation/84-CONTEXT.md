# Phase 84: Test Fixture Design & Creation - Context

**Gathered:** 2026-06-04
**Status:** Ready for planning

<domain>
## Phase Boundary

Hand-crafted test fixture CSVs exist covering 11 clinical edge cases (dual-eligible, NLPHL, SCT, multiple cancers, death dates, orphan dx, same-day multi-payer, 1900 sentinel dates, ICD-9/ICD-10 cross-system, missing payer, ABVD regimen detection) in a documented, reproducible, git-tracked format. ~20 synthetic patients mapped 1:1 to edge cases with 1-2 baseline patients. 15 PCORnet CDM table CSVs generated by an R script and committed to tests/fixtures/.

</domain>

<decisions>
## Implementation Decisions

### Patient Design
- **D-01:** One edge case per patient (~20 patients). Each patient targets 1-2 specific edge cases for clear traceability. FIXTURE_DESIGN.md reads like a test matrix linking patient IDs to the exact case they exercise.
- **D-02:** Patient IDs follow PT001-PT020 pattern (zero-padded sequential). Matches ROADMAP success criteria references (PT001, PT002, etc.).
- **D-03:** Encounter IDs follow ENC{patient}_{seq} pattern (e.g., ENC001_01, ENC001_02). Links encounters to patients visually for easy debugging.
- **D-04:** Diagnosis dates centered on 2012-2014 range, spanning the ICD-9-to-ICD-10 transition (Oct 2015). Allows testing both DX_TYPE="09" and DX_TYPE="10" patients realistically.
- **D-05:** Include 1-2 baseline "happy path" patients with no edge cases — straightforward HL diagnosis, private insurance, standard chemo. Establishes a reference for what normal pipeline output looks like.

### Fixture Creation
- **D-06:** R generation script is the single source of truth. Lives at `tests/generate_fixtures.R` with tribble() definitions. CSVs are committed to git for convenience but are regenerable by running the script.
- **D-07:** To update fixtures: edit `tests/generate_fixtures.R`, re-run it, commit both the script and regenerated CSVs. Script changes drive CSV changes.

### Table Coverage
- **D-08:** All 15 PCORnet CDM tables get fixture CSVs. Low-traffic tables (CONDITION, LAB_RESULT_CM, PROVIDER) get header + 1-2 minimal rows so DuckDB ingest succeeds and pipeline scripts don't choke on empty tables.
- **D-09:** TUMOR_REGISTRY tables (140-314 columns each) include only pipeline-used columns (~15-20 per table). Missing columns default to NA during vroom read. Keeps fixtures small and readable.

### Edge Case Scope
- **D-10:** 11 edge cases covered (original 8 + 3 additions):
  1. Dual-eligible — Medicare + Medicaid payer codes
  2. NLPHL — C81.0x or 201.4x diagnosis
  3. SCT — Stem cell transplant procedure codes (38240/38241 or Z94.84)
  4. Multiple cancers — HL + another cancer site on same patient
  5. Death dates — DEATH table records for timeline truncation
  6. Orphan dx codes — Z51.11/Z51.12 without paired procedure in same encounter
  7. Same-day multi-payer — 2+ encounters same ADMIT_DATE, different payers
  8. 1900 sentinel dates — SAS/Excel epoch artifacts to filter
  9. ICD-9/ICD-10 cross-system — Patient with both 201.x and C81.x diagnoses (Phase 87 confirmation logic)
  10. Missing payer — Patient with payer code "NI" or NA (exclude_missing_payer predicate)
  11. ABVD regimen detection — Patient with doxorubicin + vinblastine + dacarbazine + bleomycin RXNORM_CUIs (first-line therapy identification)

### Claude's Discretion
- Exact RXNORM_CUI values for ABVD drugs (research authoritative codes)
- Which specific columns to include for TUMOR_REGISTRY1/2/3 (scan pipeline code for used columns)
- Enrollment date ranges per patient (ensure coverage of diagnosis dates)
- Whether DISPENSING vs PRESCRIBING vs MED_ADMIN is used for each drug-related edge case
- Site/SOURCE values for test patients (use realistic OneFlorida+ partner site codes: AMS, UMI, FLM, VRT, UFH)

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Environment Infrastructure (Phase 83 — dependency)
- `R/00_config.R` — IS_LOCAL flag (lines 41-47), CONFIG$data_dir conditional (lines 87-91), PCORNET_TABLES list (line ~240), PCORNET_PATHS construction (line ~246)
- `.planning/phases/83-environment-detection-infrastructure/83-VERIFICATION.md` — Phase 83 verification confirming tests/fixtures/ wiring

### Data Loading & Ingest
- `R/01_load_pcornet.R` — Column type specifications for each table (col_types arguments to vroom)
- `R/03_duckdb_ingest.R` — 13 tables ingested, atomic write pattern, index creation, round-trip verification

### Clinical Edge Case Logic
- `R/10_cohort_predicates.R` — has_hodgkin_diagnosis(), has_chemo(), has_radiation(), has_sct(), with_enrollment_period(), exclude_missing_payer() — defines what data shapes trigger each predicate
- `R/02_harmonize_payer.R` — Payer harmonization logic, dual-eligible detection, 1900 sentinel filtering
- `R/utils/utils_cancer.R` — is_cancer_code(), classify_codes() — ICD-9/ICD-10 unified detection

### Configuration Maps (codes to include in fixtures)
- `R/00_config.R` — CANCER_SITE_MAP (324 entries), ICD9_CANCER_SITE_MAP (78 entries), AMC_PAYER_LOOKUP, CHEMO_CODES, RADIATION_CODES, SCT_CODES, ICD9_NLPHL_CODES, DRUG_GROUPINGS

### Quality
- `R/88_smoke_test_comprehensive.R` — Section 15b environment detection checks, conditional assertions for local mode

### Requirements
- `.planning/REQUIREMENTS.md` — FIX-01 through FIX-05 acceptance criteria

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `PCORNET_TABLES` vector (R/00_config.R): Defines all 15 table names — generation script should iterate this list
- `PCORNET_PATHS` named vector (R/00_config.R): Provides exact CSV filename patterns (`{TABLE}_Mailhot_V1.csv`)
- Column type specs in `R/01_load_pcornet.R`: Defines expected columns per table — fixtures must match these schemas
- `AMC_PAYER_LOOKUP` (R/00_config.R): Maps payer codes to categories — use these exact codes in fixtures
- `CHEMO_CODES`, `RADIATION_CODES`, `SCT_CODES` (R/00_config.R): Exact procedure/diagnosis codes to use in fixture data

### Established Patterns
- tribble() for small inline datasets in R (used in R/00_config.R for lookup tables)
- file.path() for all path construction (INFRA-01 compliance)
- vroom::vroom() with col_types for CSV loading — fixtures must satisfy these type expectations
- DuckDB ingest expects all 13+ tables present as CSVs in data_dir

### Integration Points
- `tests/fixtures/` directory already exists (Phase 83)
- `CONFIG$data_dir` points there when `IS_LOCAL=TRUE`
- `R/03_duckdb_ingest.R` iterates PCORNET_TABLES and reads from PCORNET_PATHS — fixtures must use exact naming convention
- Smoke test (R/88) has conditional checks for local mode paths but no fixture-specific count assertions yet (Phase 85 will add those)

</code_context>

<specifics>
## Specific Ideas

- Dates centered on 2012-2014 to span the ICD-9-to-ICD-10 transition, enabling realistic DX_TYPE="09" and DX_TYPE="10" coexistence
- Cross-system patient (D-10 item 9): should have ICD-9 201.x diagnoses AND ICD-10 C81.x diagnoses with 7+ day gap to test Phase 87's cross-system HL cohort confirmation
- ABVD regimen patient: needs 4 specific RXNORM_CUI codes (doxorubicin, vinblastine, dacarbazine, bleomycin) within a 28-day cycle window to trigger first-line therapy detection
- OneFlorida+ partner site codes (AMS, UMI, FLM, VRT, UFH) should be used as SOURCE values for realism, with at least one patient from each site

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 84-test-fixture-design-creation*
*Context gathered: 2026-06-04*
