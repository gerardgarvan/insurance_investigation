---
phase: 84-test-fixture-design-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/FIXTURE_DESIGN.md
  - tests/generate_fixtures.R
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04]

must_haves:
  truths:
    - "FIXTURE_DESIGN.md maps 20 patients (PT001-PT020) to 11 edge cases with rationale and expected filter behavior"
    - "generate_fixtures.R defines tribble() data for all 15 PCORnet CDM tables with inline comments identifying edge case patients"
    - "generate_fixtures.R sources R/00_config.R and reuses PCORNET_TABLES, PCORNET_PATHS for file naming"
    - "Date columns in tribble() definitions are character strings, not Date objects"
    - "Dual-eligible patient PT002 uses payer code 14, not Medicare+Medicaid combination"
    - "ICD-9/ICD-10 cross-system patient PT010 has diagnoses 7+ days apart"
    - "ABVD patient PT012 has all 4 RXNORM_CUIs (3639, 11213, 67228, 3946)"
  artifacts:
    - path: "tests/fixtures/FIXTURE_DESIGN.md"
      provides: "Patient-to-edge-case mapping documentation"
      contains: "PT001"
    - path: "tests/generate_fixtures.R"
      provides: "Reproducible fixture generation script"
      contains: "generate_enrollment"
      exports: ["generate_enrollment", "generate_diagnosis", "generate_encounter", "generate_demographic", "generate_procedures", "generate_prescribing"]
  key_links:
    - from: "tests/generate_fixtures.R"
      to: "R/00_config.R"
      via: "source() call"
      pattern: 'source\("R/00_config.R"\)'
    - from: "tests/generate_fixtures.R"
      to: "tests/fixtures/"
      via: "PCORNET_PATHS for output file naming"
      pattern: "PCORNET_PATHS"
---

<objective>
Create the test fixture design document (FIXTURE_DESIGN.md) and the R generation script (generate_fixtures.R) that defines ~20 synthetic patients covering 11 clinical edge cases across all 15 PCORnet CDM tables.

Purpose: Establish the single source of truth for test fixture data. The design doc maps patients to edge cases for traceability. The R script uses tribble() definitions that are both human-readable and machine-executable, ensuring fixtures match production column schemas and use production code constants.

Output: tests/fixtures/FIXTURE_DESIGN.md, tests/generate_fixtures.R
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-test-fixture-design-creation/84-CONTEXT.md
@.planning/phases/84-test-fixture-design-creation/84-RESEARCH.md
@.planning/phases/83-environment-detection-infrastructure/83-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From R/00_config.R (lines 225-251) -- PCORNET_TABLES and PCORNET_PATHS:
```r
PCORNET_TABLES <- c(
  "ENROLLMENT", "DIAGNOSIS", "CONDITION", "PROCEDURES", "PRESCRIBING",
  "ENCOUNTER", "DEMOGRAPHIC", "TUMOR_REGISTRY1", "TUMOR_REGISTRY2",
  "TUMOR_REGISTRY3", "DISPENSING", "MED_ADMIN", "LAB_RESULT_CM",
  "PROVIDER", "DEATH"
)
# 15 tables total

PCORNET_PATHS <- setNames(
  file.path(CONFIG$data_dir, paste0(PCORNET_TABLES, "_Mailhot_V1.csv")),
  PCORNET_TABLES
)
# CRITICAL override:
PCORNET_PATHS[["LAB_RESULT_CM"]] <- file.path(CONFIG$data_dir, "LAB_RESULT_Mailhot_V1.csv")
```

From R/01_load_pcornet.R -- Column specs per table:
```
ENROLLMENT_SPEC:   ID, ENR_START_DATE, ENR_END_DATE, CHART, ENR_BASIS, SOURCE (6 cols, all character)
DIAGNOSIS_SPEC:    DIAGNOSISID, ID, ENCOUNTERID, ENC_TYPE, ADMIT_DATE, PROVIDERID, DX, DX_TYPE, DX_DATE, DX_SOURCE, DX_ORIGIN, PDX, DX_POA, SOURCE (14 cols, all character)
CONDITION_SPEC:    CONDITIONID, ID, ENCOUNTERID, CONDITION, CONDITION_TYPE, CONDITION_SOURCE, CONDITION_STATUS, ONSET_DATE, REPORT_DATE, RESOLVE_DATE, RAW_CONDITION_TYPE, RAW_CONDITION_SOURCE (12 cols, all character)
PROCEDURES_SPEC:   PROCEDURESID, ID, ENCOUNTERID, ENC_TYPE, ADMIT_DATE, PROVIDERID, PX_DATE, PX, PX_TYPE, PX_SOURCE, PPX, SOURCE (12 cols, all character)
PRESCRIBING_SPEC:  PRESCRIBINGID, ID, ENCOUNTERID, RX_PROVIDERID, RX_ORDER_DATE, RX_ORDER_TIME, RX_START_DATE, RX_END_DATE, RX_DOSE_ORDERED(double), RX_DOSE_ORDERED_UNIT, RX_QUANTITY(double), RX_DOSE_FORM, RX_REFILLS(integer), RX_DAYS_SUPPLY(integer), RX_FREQUENCY, RX_PRN_FLAG, RX_ROUTE, RX_BASIS, RXNORM_CUI, RX_SOURCE, RX_DISPENSE_AS_WRITTEN, RAW_RX_MED_NAME, RAW_RXNORM_CUI, SOURCE (24 cols)
ENCOUNTER_SPEC:    ENCOUNTERID, ID, ADMIT_DATE, ADMIT_TIME, DISCHARGE_DATE, DISCHARGE_TIME, PROVIDERID, FACILITY_LOCATION, ENC_TYPE, FACILITYID, DISCHARGE_DISPOSITION, DISCHARGE_STATUS, DRG, DRG_TYPE, ADMITTING_SOURCE, PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY, FACILITY_TYPE, SOURCE (19 cols, all character)
DEMOGRAPHIC_SPEC:  ID, BIRTH_DATE, BIRTH_TIME, SEX, SEXUAL_ORIENTATION, GENDER_IDENTITY, HISPANIC, RACE, BIOBANK_FLAG, PAT_PREF_LANGUAGE_SPOKEN, ZIP_CODE, SOURCE (12 cols, all character)
DEATH_SPEC:        ID, DEATH_DATE, DEATH_DATE_IMPUTE, DEATH_SOURCE, DEATH_MATCH_CONFIDENCE, SOURCE (6 cols, all character)
TUMOR_REGISTRY1_SPEC: .default = col_character(), AGE_AT_DIAGNOSIS(int), TUMOR_SIZE_SUMMARY(dbl), TUMOR_SIZE_CLINICAL(dbl), TUMOR_SIZE_PATHOLOGIC(dbl) -- 314 cols, fixtures only need pipeline-used ones
TUMOR_REGISTRY2_SPEC: .default = col_character(), DXAGE(int) -- 140 cols, fixtures only need pipeline-used ones
TUMOR_REGISTRY3_SPEC: .default = col_character(), DXAGE(int) -- 140 cols, fixtures only need pipeline-used ones
DISPENSING_SPEC:   DISPENSINGID, PRESCRIBINGID, ID, DISPENSE_DATE, NDC, DISPENSE_SUP(int), DISPENSE_AMT(dbl), DISPENSE_DOSE_DISP(dbl), DISPENSE_DOSE_DISP_UNIT, DISPENSE_ROUTE, RAW_NDC, RXNORM_CUI, DISPENSE_SOURCE, RAW_DISPENSE_MED_NAME, SOURCE (15 cols)
MED_ADMIN_SPEC:    MEDADMINID, ID, ENCOUNTERID, PRESCRIBINGID, MEDADMIN_CODE, MEDADMIN_TYPE, MEDADMIN_START_DATE, MEDADMIN_STOP_DATE, MEDADMIN_ROUTE, RXNORM_CUI, RAW_MEDADMIN_MED_NAME, SOURCE (12 cols)
LAB_RESULT_CM_SPEC: .default = col_character(), RESULT_NUM(dbl) -- 23 cols listed
PROVIDER_SPEC:     PROVIDERID, PROVIDER_SEX, PROVIDER_SPECIALTY_PRIMARY, SOURCE (4 cols, all character)
```

Edge case code constants from R/00_config.R:
```
ABVD RXNORM_CUIs: 3639 (doxorubicin), 11213 (bleomycin), 67228 (vinblastine), 3946 (dacarbazine)
SCT CPT codes: 38240, 38241 (autologous), 38242 (allogeneic); ICD-10: Z94.84
Dual-eligible payer code: "14" (triggers DUAL_ELIGIBLE flag in R/02_harmonize_payer.R)
NLPHL ICD-10: C81.0x; ICD-9: 201.4x
Orphan dx: Z51.11 (chemo encounter), Z51.0 (radiation encounter)
OneFlorida+ site codes: UFH, AMS, FLM, VRT, UMI
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FIXTURE_DESIGN.md patient-to-edge-case mapping document</name>
  <files>tests/fixtures/FIXTURE_DESIGN.md</files>
  <read_first>
    - .planning/phases/84-test-fixture-design-creation/84-CONTEXT.md (locked decisions D-01 through D-10)
    - .planning/phases/84-test-fixture-design-creation/84-RESEARCH.md (FIXTURE_DESIGN.md template in Code Examples section)
    - R/00_config.R (PCORNET_TABLES list at line 225, ICD_CODES, AMC_PAYER_LOOKUP, TREATMENT_CODES, SCT_CODES)
    - R/01_load_pcornet.R (column specs for all 15 tables -- lines 56-401)
    - R/10_cohort_predicates.R (predicate function signatures -- has_hodgkin_diagnosis, has_chemo, has_sct, exclude_missing_payer)
    - R/02_harmonize_payer.R (dual-eligible detection logic, 1900 sentinel filtering)
  </read_first>
  <action>
Create tests/fixtures/FIXTURE_DESIGN.md with the following exact structure. Per D-01, this reads like a test matrix. Per D-02, patient IDs use PT001-PT020 zero-padded pattern. Per D-03, encounter IDs use ENC{patient}_{seq} pattern. Per D-04, dates centered on 2012-2014.

The document MUST contain these sections:

**Header:**
- Title: "# Test Fixture Design"
- Generated date, version 1.0, source: tests/generate_fixtures.R

**Patient Roster and Edge Case Mapping table** with columns: Patient ID | Edge Case(s) | Rationale | Expected Filter Behavior. Include all 20 patients:

| Patient | Edge Case | Key Data | Expected Behavior |
|---------|-----------|----------|-------------------|
| PT001 | Baseline happy path | C81.10 ICD-10, payer 512 (private) | Passes all filters, included in final cohort |
| PT002 | Dual-eligible | Payer code "14" in ENCOUNTER | Maps to Medicaid; DUAL_ELIGIBLE = TRUE |
| PT003 | NLPHL | C81.00 diagnosis | Classified as NLPHL, not classical HL |
| PT004 | SCT | CPT 38241 in PROCEDURES | has_sct() = TRUE |
| PT005 | Multiple cancers | C81.40 (HL) + C50.911 (breast) | Both cancers in cancer summary |
| PT006 | Death date | DEATH_DATE = 2014-06-15 | Timeline truncated at death |
| PT007 | Orphan dx codes | Z51.11 without paired procedure | Flagged as orphan, not treatment evidence |
| PT008 | Same-day multi-payer | 2 encounters on 2013-07-10, payers "1" and "512" | Tiered payer resolution selects Medicare |
| PT009 | 1900 sentinel dates | ENR_START_DATE = 1900-01-01 | Filtered by exclude_1900_dates() |
| PT010 | ICD-9/ICD-10 cross-system | 201.90 on 2012-11-05 + C81.90 on 2012-11-15 (10-day gap) | Both codes contribute to 7-day gap confirmation |
| PT011 | Missing payer | PAYER_TYPE_PRIMARY = "NI" | Excluded by exclude_missing_payer() |
| PT012 | ABVD regimen | RXNORM_CUIs 3639, 11213, 67228, 3946 all on 2013-02-15 | Identified as ABVD first-line therapy |
| PT013-PT019 | Variation patients (per D-05, additional standard HL patients) | Standard HL diagnoses (C81.1x-C81.9x), varied payer codes, different sites | Pass all filters; provide volume for group-level summaries |
| PT020 | Baseline happy path #2 | C81.90 ICD-10, payer 512 | Passes all filters; second baseline reference |

**Data Summary table** with columns: Table | Row Count | Key Features. Cover all 15 tables:
- ENROLLMENT: 20 rows (one per patient; PT009 has 1900 sentinel date)
- DIAGNOSIS: ~18 rows (PT005 has 2 dx, PT007 has 2 dx, PT010 has 2 dx)
- ENCOUNTER: ~20 rows (PT005/PT007/PT008/PT010 have multiple encounters)
- DEMOGRAPHIC: 20 rows (one per patient)
- PROCEDURES: ~1-2 rows (PT004 SCT)
- PRESCRIBING: 4 rows (PT012 ABVD 4 drugs)
- DISPENSING: 0 data rows (header only)
- MED_ADMIN: 0 data rows (header only)
- CONDITION: 1 minimal row
- LAB_RESULT_CM: 0 data rows (header only)
- PROVIDER: 1-2 minimal rows
- DEATH: 1 row (PT006)
- TUMOR_REGISTRY1/2/3: 0 data rows each (header + pipeline-used columns only)

**Edge Case Coverage Matrix** with columns: Edge Case | Patient | Table(s) | Verification Query. Include all 11 edge cases with exact dplyr filter queries.

**Verification Checklist** (the checklist from RESEARCH.md Code Examples section).

**Regeneration Instructions** (how to update fixtures per D-07).

Do NOT include emojis.
  </action>
  <verify>
    <automated>MISSING -- no automated test exists. Verify by checking file exists and contains expected sections via grep.</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/FIXTURE_DESIGN.md exists and is non-empty
    - File contains "# Test Fixture Design"
    - File contains "Patient Roster and Edge Case Mapping" section
    - File contains all 20 patient IDs: PT001 through PT020
    - File contains "Data Summary" section listing all 15 PCORnet CDM tables
    - File contains "Edge Case Coverage Matrix" with all 11 edge cases
    - File contains "Verification Checklist" section
    - File contains "Regeneration Instructions" section
    - File contains payer code "14" for dual-eligible (PT002)
    - File contains RXNORM_CUIs "3639", "11213", "67228", "3946" for ABVD (PT012)
    - File contains "201.90" and "C81.90" for cross-system patient (PT010)
    - File contains "Z51.11" for orphan dx (PT007)
    - File contains "1900-01-01" for sentinel date (PT009)
    - File contains "NI" for missing payer (PT011)
    - File contains "38241" for SCT (PT004)
    - File contains "C81.00" for NLPHL (PT003)
  </acceptance_criteria>
  <done>FIXTURE_DESIGN.md exists in tests/fixtures/ with complete patient-to-edge-case mapping for all 20 patients, all 11 edge cases documented with verification queries, and data summary covering all 15 PCORnet CDM tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/generate_fixtures.R with all 15 table generators</name>
  <files>tests/generate_fixtures.R</files>
  <read_first>
    - .planning/phases/84-test-fixture-design-creation/84-CONTEXT.md (locked decisions D-01 through D-10, Claude's discretion items)
    - .planning/phases/84-test-fixture-design-creation/84-RESEARCH.md (generation script skeleton, all code examples including ABVD, SCT, enrollment, diagnosis, encounter, demographic tables)
    - R/00_config.R (PCORNET_TABLES at line 225, PCORNET_PATHS at line 244, LAB_RESULT_CM override at line 251, ICD_CODES, TREATMENT_CODES, AMC_PAYER_LOOKUP, DRUG_GROUPINGS, CANCER_SITE_MAP, ICD9_CANCER_SITE_MAP)
    - R/01_load_pcornet.R (all 15 column specs: ENROLLMENT_SPEC through PROVIDER_SPEC, lines 56-401)
    - R/02_harmonize_payer.R (dual-eligible detection -- payer code "14")
    - R/10_cohort_predicates.R (predicate function signatures)
    - R/utils/utils_cancer.R (is_cancer_code, classify_codes)
    - tests/fixtures/FIXTURE_DESIGN.md (created in Task 1 -- read for patient assignments)
  </read_first>
  <action>
Create tests/generate_fixtures.R -- the single source of truth for all fixture data (per D-06). This script:

1. **Sources R/00_config.R** to get PCORNET_TABLES, PCORNET_PATHS, and all code constants.

2. **Loads libraries:** tibble, dplyr, readr, lubridate, glue, purrr (all already in renv).

3. **Prints banner** at start: "PCORnet Test Fixture Generator" with separator lines.

4. **Defines PATIENT_IDS** using `sprintf("PT%03d", 1:20)` per D-02.

5. **Defines 15 generate_*() functions**, one per table. Each function returns a tribble() with:
   - Column names matching the {TABLE}_SPEC from R/01_load_pcornet.R EXACTLY (same order)
   - Inline comments identifying which edge case each row tests (per D-01)
   - All date values as character strings "YYYY-MM-DD" (NOT Date objects -- Pitfall 2)
   - SOURCE column on every table using OneFlorida+ site codes: UFH, AMS, FLM, VRT, UMI

**Table-by-table specifications:**

**generate_enrollment():** 20 rows (one per patient). Per D-04, enrollment spans 2010-2015 range. PT009 has ENR_START_DATE = "1900-01-01" (sentinel date edge case). PT006 has ENR_END_DATE = "2014-06-30" (death truncation). All others: ENR_START_DATE = "2010-01-01", ENR_END_DATE = "2015-12-31". CHART = "Y", ENR_BASIS = "I" for all. Distribute SOURCE across UFH, AMS, FLM, VRT, UMI.

**generate_diagnosis():** ~18 rows. Key rows:
- PT001: DX="C81.10", DX_TYPE="10", DX_DATE="2013-03-15" (baseline)
- PT002: DX="C81.20", DX_TYPE="10", DX_DATE="2013-06-20" (dual-eligible patient, payer in ENCOUNTER)
- PT003: DX="C81.00", DX_TYPE="10", DX_DATE="2013-09-10" (NLPHL)
- PT004: DX="C81.30", DX_TYPE="10", DX_DATE="2012-05-12" (SCT patient, procedure in PROCEDURES)
- PT005: TWO rows -- DX="C81.40" DX_TYPE="10" AND DX="C50.911" DX_TYPE="10" (multiple cancers)
- PT006: DX="C81.70", DX_TYPE="10", DX_DATE="2013-11-05" (death patient)
- PT007: TWO rows -- DX="C81.90" DX_TYPE="10" AND DX="Z51.11" DX_TYPE="10" in DIFFERENT encounters (orphan dx)
- PT008: DX="C81.90", DX_TYPE="10", DX_DATE="2013-07-10" (same-day multi-payer)
- PT009: DX="C81.90", DX_TYPE="10", DX_DATE="2012-12-01" (sentinel dates -- normal dx, bad enrollment)
- PT010: TWO rows -- DX="201.90" DX_TYPE="09" DX_DATE="2012-11-05" AND DX="C81.90" DX_TYPE="10" DX_DATE="2012-11-15" (ICD-9/ICD-10 cross-system, 10-day gap per Pitfall 6)
- PT011: DX="C81.90", DX_TYPE="10", DX_DATE="2013-08-22" (missing payer)
- PT012: DX="C81.90", DX_TYPE="10", DX_DATE="2013-02-14" (ABVD regimen)
- PT013-PT019: Standard HL diagnoses (C81.10-C81.90 variety), DX_TYPE="10"
- PT020: DX="C81.90", DX_TYPE="10", DX_DATE="2013-10-05" (baseline #2)
- All DIAGNOSISID values follow pattern "DX{patient_num}" or "DX{patient_num}A/B" for multi-row patients
- All ENCOUNTERID values follow ENC{patient}_{seq} pattern per D-03
- Include ENC_TYPE, ADMIT_DATE, PROVIDERID, DX_SOURCE="AD", DX_ORIGIN="OD", PDX, DX_POA columns

**generate_encounter():** ~20 rows matching all encounters referenced in DIAGNOSIS. Key:
- PT002: PAYER_TYPE_PRIMARY = "14" (dual-eligible per Pitfall 5)
- PT008: TWO encounters same ADMIT_DATE "2013-07-10", PAYER_TYPE_PRIMARY = "1" and "512" respectively
- PT011: PAYER_TYPE_PRIMARY = "NI" (missing payer)
- PT001/PT003/PT006/PT012/PT020 etc: PAYER_TYPE_PRIMARY = "512" (private insurance)
- PT004/PT010: PAYER_TYPE_PRIMARY = "111" (Medicare)
- PT005: PAYER_TYPE_PRIMARY = "211" (Medicaid)
- IP encounters: include DISCHARGE_DATE, DISCHARGE_DISPOSITION="A", DISCHARGE_STATUS="01", DRG="840", DRG_TYPE="MS"
- AV encounters: DISCHARGE_DATE=NA, DISCHARGE_DISPOSITION=NA, etc.

**generate_demographic():** 20 rows (one per patient). Include: BIRTH_DATE (character strings, ages 30-70 at diagnosis), BIRTH_TIME, SEX (mix M/F), SEXUAL_ORIENTATION=NA, GENDER_IDENTITY=NA, HISPANIC (mostly "N", some "Y"), RACE (mix of "03","04","05"), BIOBANK_FLAG=NA, PAT_PREF_LANGUAGE_SPOKEN (mostly "eng", some "spa"), ZIP_CODE (FL zip codes: 32611, 33101, 32301, 32801, 33140).

**generate_procedures():** ~1-2 rows. PT004: PX="38241", PX_TYPE="CH" (autologous SCT). Include PROCEDURESID, ENCOUNTERID="ENC004_01", PX_DATE="2012-05-18", PX_SOURCE="OD".

**generate_prescribing():** 4 rows for PT012 ABVD regimen. All on same date "2013-02-15":
- RX012_01: RXNORM_CUI="3639", RAW_RX_MED_NAME="Doxorubicin 50mg IV", RX_DOSE_ORDERED=50, RX_DOSE_ORDERED_UNIT="mg"
- RX012_02: RXNORM_CUI="11213", RAW_RX_MED_NAME="Bleomycin 10 units IV", RX_DOSE_ORDERED=10, RX_DOSE_ORDERED_UNIT="units"
- RX012_03: RXNORM_CUI="67228", RAW_RX_MED_NAME="Vinblastine 6mg IV", RX_DOSE_ORDERED=6, RX_DOSE_ORDERED_UNIT="mg"
- RX012_04: RXNORM_CUI="3946", RAW_RX_MED_NAME="Dacarbazine 375mg IV", RX_DOSE_ORDERED=375, RX_DOSE_ORDERED_UNIT="mg"
- All: RX_ROUTE="IV", RX_BASIS="P", RX_SOURCE="OD", RX_REFILLS=0L, RX_DAYS_SUPPLY=1L
- All 24 PRESCRIBING_SPEC columns present

**generate_condition():** 1 minimal row per D-08. CONDITIONID="COND001", ID="PT001", CONDITION="E11.9", CONDITION_TYPE="10", SOURCE="UFH". Include all 12 CONDITION_SPEC columns.

**generate_death():** 1 row for PT006. DEATH_DATE="2014-06-15", DEATH_DATE_IMPUTE="N", DEATH_SOURCE="L", DEATH_MATCH_CONFIDENCE=NA, SOURCE="UFH".

**generate_dispensing():** Empty tibble with correct column names and types per DISPENSING_SPEC. Use `tibble(DISPENSINGID=character(), PRESCRIBINGID=character(), ID=character(), ...)` -- zero rows, all 15 column names present.

**generate_med_admin():** Empty tibble with correct column names per MED_ADMIN_SPEC -- zero rows, all 12 column names present.

**generate_lab_result_cm():** Empty tibble with correct column names per LAB_RESULT_CM_SPEC -- zero rows, key columns present: LAB_RESULTID, ID, ENCOUNTERID, LAB_LOINC, RESULT_NUM, SOURCE.

**generate_provider():** 1-2 minimal rows. PROVIDERID="PROV001", PROVIDER_SPECIALTY_PRIMARY="207RH0003X" (Hematology/Oncology), SOURCE="UFH". And PROVIDERID="PROV002", SOURCE="FLM".

**generate_tumor_registry1():** Empty tibble per D-09. Include only pipeline-used columns: ID, SOURCE, SITE_CODE, HISTOLOGICAL_TYPE, HISTOLOGICAL_TYPE_DESCRIPTION, BEHAVIOR_CODE, GRADE, AGE_AT_DIAGNOSIS, TUMOR_SIZE_SUMMARY, DX_DATE_TR. Zero rows.

**generate_tumor_registry2():** Empty tibble. Include: ID, SOURCE, SITE, MORPH, DXAGE, DX_DATE_TR. Zero rows.

**generate_tumor_registry3():** Empty tibble. Include: ID, SOURCE, SITE, MORPH, DXAGE, DX_DATE_TR. Zero rows.

6. **Assembles fixture_tables list** mapping table names to generate_*() outputs:
```r
fixture_tables <- list(
  ENROLLMENT = generate_enrollment(),
  DIAGNOSIS = generate_diagnosis(),
  ...all 15 tables...
)
```

7. **Writes all CSVs** using PCORNET_PATHS for file naming (handles LAB_RESULT_CM override automatically per Pitfall 1):
```r
walk2(names(fixture_tables), fixture_tables, function(table_name, table_data) {
  output_path <- PCORNET_PATHS[[table_name]]
  write_csv(table_data, output_path, na = "")
  message(glue("Wrote {nrow(table_data)} rows to {basename(output_path)}"))
})
```

8. **Prints summary** at end with table name, row count, and output path.

CRITICAL constraints:
- All date columns MUST be character strings ("2013-03-15"), NOT Date objects. This matches how vroom reads production CSVs (Pitfall 2).
- Numeric columns (RX_DOSE_ORDERED, RX_QUANTITY, RESULT_NUM, etc.) use numeric literals in tribble().
- Integer columns (RX_REFILLS, RX_DAYS_SUPPLY, DISPENSE_SUP) use integer literals (e.g., 0L, 1L).
- Column order in each tribble() MUST match the corresponding {TABLE}_SPEC in R/01_load_pcornet.R (Pitfall 8).
- SOURCE column is LAST in every tribble() definition.
- Use na = "" in write_csv() so NA values become empty strings in CSVs (matching production format).

Script documentation header must include:
- Purpose, Usage ("source('tests/generate_fixtures.R')"), update instructions per D-07
- Edge case listing matching D-10 (all 11 cases with patient assignments)
  </action>
  <verify>
    <automated>MISSING -- R not available in CI. Verify file exists and contains expected function definitions via grep.</automated>
  </verify>
  <acceptance_criteria>
    - tests/generate_fixtures.R exists and is non-empty
    - File contains `source("R/00_config.R")`
    - File contains `library(tibble)` and `library(readr)`
    - File contains 15 generate_* function definitions: generate_enrollment, generate_diagnosis, generate_condition, generate_procedures, generate_prescribing, generate_encounter, generate_demographic, generate_tumor_registry1, generate_tumor_registry2, generate_tumor_registry3, generate_dispensing, generate_med_admin, generate_lab_result_cm, generate_provider, generate_death
    - File contains `fixture_tables <- list(` assembling all 15 tables
    - File contains `walk2(` or equivalent CSV writing loop
    - File contains `PCORNET_PATHS[[table_name]]` for output path resolution
    - File contains `write_csv(` with `na = ""`
    - File contains `tribble(` for data definition
    - File contains ABVD RXNORM_CUIs: "3639", "11213", "67228", "3946"
    - File contains dual-eligible payer code: `"14"`
    - File contains 1900 sentinel: `"1900-01-01"`
    - File contains SCT code: `"38241"`
    - File contains NLPHL code: `"C81.00"`
    - File contains ICD-9 code: `"201.90"` with DX_TYPE "09"
    - File contains missing payer: `"NI"`
    - File contains orphan dx: `"Z51.11"`
    - File contains PT001 through PT020 patient IDs
    - File contains ENC pattern encounter IDs (e.g., "ENC001_01")
    - File does NOT contain ymd() calls that would convert dates to Date objects before write_csv
    - File contains documentation header with Purpose, Usage, edge case listing
  </acceptance_criteria>
  <done>tests/generate_fixtures.R exists with all 15 table generator functions defining ~20 patients covering 11 edge cases, a fixture_tables assembly list, and a CSV writing loop using PCORNET_PATHS. All date columns are character strings. All column names match production specs from R/01_load_pcornet.R.</done>
</task>

</tasks>

<verification>
1. tests/fixtures/FIXTURE_DESIGN.md exists with all 20 patients documented
2. tests/generate_fixtures.R exists with all 15 generate_*() functions
3. All 11 edge cases are present in both the design doc and the generation script
4. Column names in tribble() definitions match {TABLE}_SPEC from R/01_load_pcornet.R
5. All date values are character strings, not Date objects
6. PCORNET_PATHS used for output paths (handles LAB_RESULT_CM override)
7. No deferred ideas implemented
</verification>

<success_criteria>
- FIXTURE_DESIGN.md maps all 20 patients (PT001-PT020) to 11 edge cases with expected filter behavior
- generate_fixtures.R defines tribble() data for all 15 PCORnet CDM tables
- generate_fixtures.R sources R/00_config.R and uses PCORNET_PATHS for file naming
- All edge case codes match production constants (payer "14", RXNORM_CUIs, SCT "38241", etc.)
- Date columns are character strings throughout
</success_criteria>

<output>
After completion, create `.planning/phases/84-test-fixture-design-creation/84-01-SUMMARY.md`
</output>
