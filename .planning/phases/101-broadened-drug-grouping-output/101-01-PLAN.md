---
phase: 101-broadened-drug-grouping-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/57_drug_grouping_instances.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - DRUG-01
  - DRUG-02
  - DRUG-03

must_haves:
  truths:
    - "User can open drug_grouping_instances.xlsx and see ALL treatment encounters (not just cancer-linked) with cancer_linked TRUE/FALSE flag column"
    - "User can open drug_grouping_instances_linked_only.xlsx and see identical content to what R/57 previously produced (no regressions)"
    - "User can see cross-tab summary sheet in broadened xlsx showing linked_count and unlinked_count per treatment type"
    - "User can run R/88 smoke test and see Phase 101 validation section passing"
  artifacts:
    - path: "R/57_drug_grouping_instances.R"
      provides: "Dual-output drug grouping (broadened + linked-only) with cancer_linked flag and cross-tab summary"
      contains: "cancer_linked"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Phase 101 structural validation section"
      contains: "DRUG-01"
  key_links:
    - from: "R/57_drug_grouping_instances.R"
      to: "output/drug_grouping_instances.xlsx"
      via: "wb_broad$save() with 3 sheets including cancer_linked column"
      pattern: "wb_broad.*save"
    - from: "R/57_drug_grouping_instances.R"
      to: "output/drug_grouping_instances_linked_only.xlsx"
      via: "wb_linked$save() with 2 sheets, cancer_linked column removed"
      pattern: "wb_linked.*save"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/57_drug_grouping_instances.R"
      via: "readLines + grepl static analysis"
      pattern: "r57_lines"
---

<objective>
Expand R/57 drug grouping instances output to include ALL treatment encounters (not just cancer-linked) with a new cancer_linked TRUE/FALSE flag column. Preserve the existing cancer-linked-only output as a separate file with _linked_only suffix. Add a linked-vs-unlinked cross-tab summary sheet. Update R/88 smoke test with Phase 101 validation.

Purpose: Meeting team needs for visibility into unlinked treatment encounters. Currently R/57 filters to cancer-linked-only, hiding encounters without cancer diagnosis linkage. The broadened output enables analysis of all treatment encounters (e.g., unlinked chemo for regimen detection) while preserving the existing cancer-focused workflow.

Output: Modified R/57 producing 4 xlsx files (broadened primary pair + linked-only pair) and updated R/88 with Phase 101 validation section.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-broadened-drug-grouping-output/101-CONTEXT.md
@.planning/phases/101-broadened-drug-grouping-output/101-RESEARCH.md
@R/57_drug_grouping_instances.R
@R/88_smoke_test_comprehensive.R

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/57_drug_grouping_instances.R (current state, lines to modify):

Line 61-62 (output paths, currently 2 files):
```r
NEW_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "encounter_level_drug_grouping_instances.xlsx")
OLD_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_instances.xlsx")
```

Lines 374-383 (Table 1 creation with cancer filter):
```r
table1 <- detail_codes %>%
  filter(!is.na(cancer_category_names)) %>%  # <-- REMOVE for broadened
  group_by(patient_id, ENCOUNTERID, treatment_date, treatment_type, cancer_category_names) %>%
  summarise(
    sub_category_names = paste(sort(unique(sub_category)), collapse = ";"),
    .groups = "drop"
  ) %>%
  select(patient_id, ENCOUNTERID, treatment_date, treatment_category = treatment_type,
         sub_category_names, cancer_category_names) %>%
  arrange(patient_id, treatment_date, treatment_category)
```

Lines 398-409 (Table 2 creation with cancer filter):
```r
table2 <- detail_dx %>%
  filter(!is.na(cancer_category_names),  # <-- REMOVE for broadened
         !is.na(triggering_code), triggering_code != "",
         triggering_code %in% valid_reference_codes | treatment_type == "Immunotherapy") %>%
  group_by(patient_id, ENCOUNTERID, treatment_date, treatment_type, cancer_category_names) %>%
  summarise(
    all_treatments = paste(sort(unique(triggering_code)), collapse = ";"),
    .groups = "drop"
  ) %>%
  select(patient_id, ENCOUNTERID, treatment_date, treatment_category = treatment_type,
         all_treatments, cancer_category_names) %>%
  arrange(patient_id, treatment_date, treatment_category)
```

Lines 416-437 (Section 7: single workbook, 2 sheets):
```r
wb <- wb_workbook()
wb$add_worksheet("Enc: Sub-Category Detail")
wb$add_data("Enc: Sub-Category Detail", table1, start_row = 1, col_names = TRUE)
wb$add_worksheet("Enc: Treatment Detail")
wb$add_data("Enc: Treatment Detail", table2, start_row = 1, col_names = TRUE)
wb$save(NEW_OUTPUT_XLSX)
wb$save(OLD_OUTPUT_XLSX)
```

From R/88_smoke_test_comprehensive.R (Section 31 current state):
- Current section numbering: [30/31] for R/30, [31/31] for DuckDB
- New section must increment total to 32: [30/32], [31/32], [32/32]
- Phase 101 section goes between Section 31 (R/57 Phase 88) and Section 30 (R/30)
  OR after Section 30 as new Section 31A — simplest: add AFTER Section 30 as a new numbered section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify R/57 for dual-output with cancer_linked flag and cross-tab summary</name>
  <files>R/57_drug_grouping_instances.R</files>
  <read_first>
    - R/57_drug_grouping_instances.R (current state — read entire file)
    - R/00_config.R (CONFIG$output_dir path pattern)
    - .planning/phases/101-broadened-drug-grouping-output/101-CONTEXT.md (locked decisions D-01 through D-09)
    - .planning/phases/101-broadened-drug-grouping-output/101-RESEARCH.md (architecture patterns and pitfall warnings)
  </read_first>
  <action>
Modify R/57_drug_grouping_instances.R to produce dual-output (broadened + linked-only) with cancer_linked flag. All changes per locked decisions D-01 through D-09.

**1. Add linked-only output paths (after line 62, in Section 1):**

Add 2 new path constants after existing OLD_OUTPUT_XLSX:
```r
# Phase 101: Linked-only output paths (backward compat for cancer-linked-only consumers)
NEW_OUTPUT_LINKED_XLSX <- file.path(CONFIG$output_dir, "encounter_level_drug_grouping_instances_linked_only.xlsx")
OLD_OUTPUT_LINKED_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_instances_linked_only.xlsx")
```

Update the Section 1 startup message to log all 4 output paths:
```r
message(glue("  Output (broadened, new): {NEW_OUTPUT_XLSX}"))
message(glue("  Output (broadened, compat): {OLD_OUTPUT_XLSX}"))
message(glue("  Output (linked-only, new): {NEW_OUTPUT_LINKED_XLSX}"))
message(glue("  Output (linked-only, compat): {OLD_OUTPUT_LINKED_XLSX}"))
```

**2. Modify Section 5 (Table 1) — lines 372-388:**

Replace current `table1` creation with dual tables. Remove `filter(!is.na(cancer_category_names))` for broadened output. Add `cancer_linked` column per D-03 (derived from `!is.na(cancer_codes)`). Note: `cancer_codes` column already exists on `detail_codes` from Section 4 left_join.

```r
# Table 1: Sub-Category Encounter Detail (ALL encounters, per DRUG-01)
table1_all <- detail_codes %>%
  group_by(patient_id, ENCOUNTERID, treatment_date, treatment_type,
           cancer_codes, cancer_category_names) %>%
  summarise(
    sub_category_names = paste(sort(unique(sub_category)), collapse = ";"),
    .groups = "drop"
  ) %>%
  mutate(cancer_linked = !is.na(cancer_codes)) %>%
  select(patient_id, ENCOUNTERID, treatment_date, treatment_category = treatment_type,
         sub_category_names, cancer_category_names, cancer_linked) %>%
  arrange(patient_id, treatment_date, treatment_category)

# Table 1: Linked-only (backward compatibility per DRUG-03)
table1_linked <- table1_all %>%
  filter(!is.na(cancer_category_names)) %>%
  select(-cancer_linked)

message(glue("  Table 1 (all): {nrow(table1_all)} rows"))
message(glue("  Table 1 (linked-only): {nrow(table1_linked)} rows"))
message(glue("  Unique patients (all): {n_distinct(table1_all$patient_id)}"))
message(glue("  Unique encounters (all): {n_distinct(table1_all$ENCOUNTERID)}"))
message(glue("  Unique sub-categories: {n_distinct(unlist(str_split(table1_all$sub_category_names, ';')))}"))
```

**3. Modify Section 6 (Table 2) — lines 391-413:**

Replace current `table2` creation with dual tables. Same broadening pattern: remove `filter(!is.na(cancer_category_names))`, add `cancer_linked`.

```r
# Table 2: Encounter Treatment Code Detail (ALL encounters, per DRUG-01)
table2_all <- detail_dx %>%
  filter(!is.na(triggering_code), triggering_code != "",
         triggering_code %in% valid_reference_codes | treatment_type == "Immunotherapy") %>%
  left_join(encounter_dx, by = "ENCOUNTERID") %>%
  group_by(patient_id, ENCOUNTERID, treatment_date, treatment_type,
           cancer_codes, cancer_category_names) %>%
  summarise(
    all_treatments = paste(sort(unique(triggering_code)), collapse = ";"),
    .groups = "drop"
  ) %>%
  mutate(cancer_linked = !is.na(cancer_codes)) %>%
  select(patient_id, ENCOUNTERID, treatment_date, treatment_category = treatment_type,
         all_treatments, cancer_category_names, cancer_linked) %>%
  arrange(patient_id, treatment_date, treatment_category)

# Table 2: Linked-only (backward compatibility per DRUG-03)
table2_linked <- table2_all %>%
  filter(!is.na(cancer_category_names)) %>%
  select(-cancer_linked)

message(glue("  Table 2 (all): {nrow(table2_all)} rows"))
message(glue("  Table 2 (linked-only): {nrow(table2_linked)} rows"))
message(glue("  Unique patients (all): {n_distinct(table2_all$patient_id)}"))
message(glue("  Unique encounters (all): {n_distinct(table2_all$ENCOUNTERID)}"))
```

IMPORTANT for Table 2: The current code uses `detail_dx` (which has cancer_codes from the Section 4 left_join), NOT `detail_codes` (which went through the reference filter and sub-category lookup). The current Table 2 ALSO applies the reference filter inline (`triggering_code %in% valid_reference_codes | treatment_type == "Immunotherapy"`). Study the existing code carefully — Table 2 currently sources from `detail_dx` not `detail_codes`. The `cancer_codes` and `cancer_category_names` columns are already on `detail_dx` from the Section 4 left_join, so no additional join is needed. Remove the `left_join(encounter_dx, by = "ENCOUNTERID")` from the pattern above — it is unnecessary since `detail_dx` already has those columns.

Corrected Table 2:
```r
table2_all <- detail_dx %>%
  filter(!is.na(triggering_code), triggering_code != "",
         triggering_code %in% valid_reference_codes | treatment_type == "Immunotherapy") %>%
  group_by(patient_id, ENCOUNTERID, treatment_date, treatment_type, cancer_category_names) %>%
  summarise(
    all_treatments = paste(sort(unique(triggering_code)), collapse = ";"),
    .groups = "drop"
  ) %>%
  mutate(cancer_linked = !is.na(cancer_category_names)) %>%
  select(patient_id, ENCOUNTERID, treatment_date, treatment_category = treatment_type,
         all_treatments, cancer_category_names, cancer_linked) %>%
  arrange(patient_id, treatment_date, treatment_category)
```

Note: For Table 2 the `cancer_linked` derivation uses `!is.na(cancer_category_names)` since Table 2 does not group_by `cancer_codes` — it uses `cancer_category_names` which is already available and equivalent for the boolean flag (both are NA/non-NA together from Section 4 mapping). If `cancer_codes` is available on the grouped data, prefer `!is.na(cancer_codes)` for consistency with Table 1. Check the actual columns available after the group_by.

**4. Add new Section 6B: Cross-Tab Summary (after Table 2, before Section 7):**

```r
# SECTION 6B: CROSS-TAB SUMMARY (Phase 101: DRUG-01, D-05, D-06) ----

message()
message("--- Building Cross-Tab: Linked vs Unlinked by Treatment Type ---")

crosstab_summary <- table1_all %>%
  group_by(treatment_category, cancer_linked) %>%
  summarise(n = n(), .groups = "drop") %>%
  tidyr::pivot_wider(
    names_from = cancer_linked,
    values_from = n,
    values_fill = 0
  ) %>%
  rename(
    treatment_type = treatment_category,
    linked_count = `TRUE`,
    unlinked_count = `FALSE`
  ) %>%
  select(treatment_type, linked_count, unlinked_count) %>%
  arrange(desc(linked_count))

message(glue("  Cross-tab summary: {nrow(crosstab_summary)} treatment types"))
for (i in seq_len(nrow(crosstab_summary))) {
  message(glue("    {crosstab_summary$treatment_type[i]}: {crosstab_summary$linked_count[i]} linked, {crosstab_summary$unlinked_count[i]} unlinked"))
}
```

**5. Replace Section 7 (Write XLSX) with dual-workbook output:**

```r
# SECTION 7: WRITE XLSX OUTPUT (Phase 89 grain-labeled + Phase 101 dual-output) ----

message()
message("--- Writing multi-sheet XLSX output (dual: broadened + linked-only) ---")

# --- Broadened output (3 sheets per D-09) ---
wb_broad <- wb_workbook()

# Sheet 1: Sub-Category Detail (all encounters)
wb_broad$add_worksheet("Enc: Sub-Category Detail")
wb_broad$add_data("Enc: Sub-Category Detail", table1_all, start_row = 1, col_names = TRUE)

# Sheet 2: Treatment Detail (all encounters)
wb_broad$add_worksheet("Enc: Treatment Detail")
wb_broad$add_data("Enc: Treatment Detail", table2_all, start_row = 1, col_names = TRUE)

# Sheet 3: Linked vs Unlinked Summary (per D-06)
wb_broad$add_worksheet("Linked vs Unlinked")
wb_broad$add_data("Linked vs Unlinked", crosstab_summary, start_row = 1, col_names = TRUE)

# Save broadened output (grain-labeled + backward compat per D-07)
wb_broad$save(NEW_OUTPUT_XLSX)
message(glue("Saved broadened (3 sheets): {NEW_OUTPUT_XLSX}"))
wb_broad$save(OLD_OUTPUT_XLSX)
message(glue("Saved broadened (3 sheets, compat): {OLD_OUTPUT_XLSX}"))

# --- Linked-only output (2 sheets, exact current structure per D-08, DRUG-03) ---
wb_linked <- wb_workbook()

# Sheet 1: Sub-Category Detail (cancer-linked only, NO cancer_linked column)
wb_linked$add_worksheet("Enc: Sub-Category Detail")
wb_linked$add_data("Enc: Sub-Category Detail", table1_linked, start_row = 1, col_names = TRUE)

# Sheet 2: Treatment Detail (cancer-linked only, NO cancer_linked column)
wb_linked$add_worksheet("Enc: Treatment Detail")
wb_linked$add_data("Enc: Treatment Detail", table2_linked, start_row = 1, col_names = TRUE)

# Save linked-only output with _linked_only suffix (per D-08)
wb_linked$save(NEW_OUTPUT_LINKED_XLSX)
message(glue("Saved linked-only (2 sheets): {NEW_OUTPUT_LINKED_XLSX}"))
wb_linked$save(OLD_OUTPUT_LINKED_XLSX)
message(glue("Saved linked-only (2 sheets, compat): {OLD_OUTPUT_LINKED_XLSX}"))
```

**6. Update Section 8 (Console Summary) to report both outputs:**

Replace existing summary messages with dual-output summary:
```r
# SECTION 8: CONSOLE SUMMARY ----

message()
message("=== Summary ===")
message(glue("  Total detail rows loaded: {nrow(detail)}"))
message(glue("  Rows with cancer category names: {n_with_categories}"))
message(glue("  Rows without cancer category names: {n_without_categories}"))
message()
message(glue("  Table 1 (Enc: Sub-Category Detail):"))
message(glue("    All encounters: {nrow(table1_all)} rows"))
message(glue("    Linked-only: {nrow(table1_linked)} rows"))
message(glue("    Unlinked: {nrow(table1_all) - nrow(table1_linked)} rows"))
message(glue("    Unique sub-categories: {n_distinct(unlist(str_split(table1_all$sub_category_names, ';')))}"))
message(glue("    Unique patients (all): {n_distinct(table1_all$patient_id)}"))
message(glue("    Unique encounters (all): {n_distinct(table1_all$ENCOUNTERID)}"))
message()
message(glue("  Table 2 (Enc: Treatment Detail):"))
message(glue("    All encounters: {nrow(table2_all)} rows"))
message(glue("    Linked-only: {nrow(table2_linked)} rows"))
message(glue("    Unlinked: {nrow(table2_all) - nrow(table2_linked)} rows"))
message(glue("    Unique patients (all): {n_distinct(table2_all$patient_id)}"))
message(glue("    Unique encounters (all): {n_distinct(table2_all$ENCOUNTERID)}"))
message()
message(glue("  Cross-tab summary ({nrow(crosstab_summary)} treatment types):"))
for (i in seq_len(nrow(crosstab_summary))) {
  message(glue("    {crosstab_summary$treatment_type[i]}: {crosstab_summary$linked_count[i]} linked, {crosstab_summary$unlinked_count[i]} unlinked"))
}
message()

# Verify drug_grouping_tables.xlsx was NOT modified by this script
old_r56_file <- file.path(CONFIG$output_dir, "drug_grouping_tables.xlsx")
if (file.exists(old_r56_file)) {
  message(glue("  Verified: drug_grouping_tables.xlsx exists and was NOT modified by this script"))
} else {
  message(glue("  NOTE: drug_grouping_tables.xlsx not found (expected if R/56 not yet run)"))
}

message()
message(glue("  Output files (broadened, 3 sheets):"))
message(glue("    {NEW_OUTPUT_XLSX} (primary)"))
message(glue("    {OLD_OUTPUT_XLSX} (backward compatibility)"))
message(glue("  Output files (linked-only, 2 sheets):"))
message(glue("    {NEW_OUTPUT_LINKED_XLSX} (primary)"))
message(glue("    {OLD_OUTPUT_LINKED_XLSX} (backward compatibility)"))
message()
message("Done.")
```

**7. Update script header comment block (lines 1-36):**

Update the Outputs section in the header to reflect 4 output files:
```r
# Outputs:
#   - output/encounter_level_drug_grouping_instances.xlsx (3-sheet workbook:
#     Sheet 1 = "Enc: Sub-Category Detail" (all encounters, with cancer_linked flag),
#     Sheet 2 = "Enc: Treatment Detail" (all encounters, with cancer_linked flag),
#     Sheet 3 = "Linked vs Unlinked" (cross-tab summary by treatment type))
#   - output/drug_grouping_instances.xlsx (backward compat copy of broadened output)
#   - output/encounter_level_drug_grouping_instances_linked_only.xlsx (2-sheet workbook:
#     Sheet 1 = "Enc: Sub-Category Detail" (cancer-linked only),
#     Sheet 2 = "Enc: Treatment Detail" (cancer-linked only))
#   - output/drug_grouping_instances_linked_only.xlsx (backward compat copy of linked-only)
```

Also add Phase 101 decision traceability to the header:
```r
# Phase 101 Decisions (Broadened Drug Grouping Output):
#   D-01: R/57 only; R/56 stays unchanged (cancer-linked-only)
#   D-02: Only filter(!is.na(cancer_category_names)) removed; reference code filter stays
#   D-03: cancer_linked derived from encounter-level DX presence (!is.na(cancer_codes))
#   D-04: Self-contained within R/57, no dependency on R/28 cancer_category
#   D-05: Cross-tab: treatment_type | linked_count | unlinked_count
#   D-06: Cross-tab as 3rd sheet in broadened xlsx ("Linked vs Unlinked")
#   D-07: Broadened = primary files (both naming patterns)
#   D-08: Linked-only with _linked_only suffix (both naming patterns)
#   D-09: Broadened has 3 sheets; linked-only keeps exact 2-sheet structure
```

**Key pitfalls to avoid (from RESEARCH.md):**
- Pitfall 1: Linked-only output must NOT include cancer_linked column. Use `select(-cancer_linked)` when creating table1_linked and table2_linked.
- Pitfall 2: Both Table 1 AND Table 2 must be broadened. Do not forget Table 2 (line 399).
- Pitfall 3: Cross-tab must use table1_all (encounter-level), not episode data.
- Pitfall 4: Define 4 separate output paths. Do not reuse OLD_OUTPUT_XLSX for linked-only.
  </action>
  <verify>
    <automated>cd /d C:\Users\Owner\Documents\insurance_investigation && powershell -Command "$lines = Get-Content R/57_drug_grouping_instances.R; $checks = @(); $checks += ('cancer_linked' -match ($lines | Select-String 'cancer_linked' | Measure-Object).Count -gt 3); $checks += (($lines | Select-String 'table1_all').Count -ge 2); $checks += (($lines | Select-String 'table1_linked').Count -ge 2); $checks += (($lines | Select-String 'table2_all').Count -ge 2); $checks += (($lines | Select-String 'table2_linked').Count -ge 2); $checks += (($lines | Select-String 'wb_broad').Count -ge 3); $checks += (($lines | Select-String 'wb_linked').Count -ge 3); $checks += (($lines | Select-String 'Linked vs Unlinked').Count -ge 2); $checks += (($lines | Select-String '_linked_only').Count -ge 4); $checks += (($lines | Select-String 'crosstab_summary').Count -ge 3); $checks += (($lines | Select-String 'D-01:').Count -ge 1); Write-Output ('All checks: ' + (($checks | Where-Object {$_}).Count) + '/' + $checks.Count)"</automated>
  </verify>
  <acceptance_criteria>
    - R/57 contains `cancer_linked = !is.na(cancer_codes)` (DRUG-02 flag derivation per D-03)
    - R/57 contains `table1_all` and `table1_linked` (dual Table 1 per DRUG-01/DRUG-03)
    - R/57 contains `table2_all` and `table2_linked` (dual Table 2 per DRUG-01/DRUG-03)
    - R/57 contains `select(-cancer_linked)` at least twice (Pitfall 1: strip from linked-only exports)
    - R/57 contains `wb_broad <- wb_workbook()` and `wb_linked <- wb_workbook()` (two separate workbooks)
    - R/57 contains `"Linked vs Unlinked"` sheet name (D-06 cross-tab sheet)
    - R/57 contains `crosstab_summary` computed from `table1_all` via `group_by(treatment_category, cancer_linked)`
    - R/57 contains `NEW_OUTPUT_LINKED_XLSX` and `OLD_OUTPUT_LINKED_XLSX` path constants (D-08)
    - R/57 contains `_linked_only.xlsx` in at least 4 places (2 path definitions + 2 save calls)
    - R/57 header contains `D-01:` through `D-09:` decision traceability
    - R/57 does NOT contain `filter(!is.na(cancer_category_names))` before `table1_all` or `table2_all` creation (the broadened tables must not filter)
    - R/57 DOES contain `filter(!is.na(cancer_category_names))` for `table1_linked` and `table2_linked` creation (the linked-only tables must filter)
    - R/57 Section 6B header contains `SECTION 6B: CROSS-TAB SUMMARY`
    - R/57 has >= 8 section headers (was 8, adding 6B makes 9)
  </acceptance_criteria>
  <done>R/57 produces 4 xlsx output files: broadened pair (3 sheets each: Sub-Category Detail with cancer_linked, Treatment Detail with cancer_linked, Linked vs Unlinked cross-tab) and linked-only pair (2 sheets each: identical to previous R/57 output without cancer_linked column). Cross-tab summary has 3 columns (treatment_type, linked_count, unlinked_count) with one row per treatment type.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 101 validation section to R/88 smoke test</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (full file — understand section numbering, check() pattern, existing R/57 Section 31 checks)
    - R/57_drug_grouping_instances.R (modified in Task 1 — verify what to check)
    - .planning/phases/101-broadened-drug-grouping-output/101-CONTEXT.md (decisions to validate)
  </read_first>
  <action>
Add a new validation section to R/88_smoke_test_comprehensive.R for Phase 101 broadened drug grouping output. This section validates the structural changes made in Task 1.

**1. Update section numbering:**

Current state: `[30/31]` for R/30 CONDITION, `[31/31]` for DuckDB.
New state: `[30/32]` for R/30 CONDITION, `[31/32]` for Phase 101, `[32/32]` for DuckDB.

Find and replace:
- `[30/31]` -> `[30/32]`
- `[31/31]` -> `[32/32]`

**2. Insert new Section 31 AFTER the R/30 CONDITION section (after the closing `}` around line 1974) and BEFORE the DuckDB section (currently Section 32 at line 1976):**

```r
# ==============================================================================
# SECTION 31: PHASE 101 -- BROADENED DRUG GROUPING OUTPUT (DRUG-01, DRUG-02, DRUG-03) ----
# ==============================================================================
# Validates R/57 broadened drug grouping with cancer_linked flag and dual-output.

message("\n[31/32] Phase 101: Broadened drug grouping output validation...")

if (file.exists("R/57_drug_grouping_instances.R")) {
  r57_lines_101 <- readLines("R/57_drug_grouping_instances.R", warn = FALSE)

  # DRUG-01: Broadened output includes ALL encounters (table1_all, table2_all)
  check("R/57 creates table1_all (broadened, all encounters)",
        any(grepl("table1_all", r57_lines_101)))

  check("R/57 creates table2_all (broadened, all encounters)",
        any(grepl("table2_all", r57_lines_101)))

  # DRUG-02: cancer_linked flag derived from cancer_codes
  check("R/57 derives cancer_linked from !is.na(cancer_codes)",
        any(grepl("cancer_linked.*!is\\.na\\(cancer_codes\\)", r57_lines_101)) ||
        any(grepl("cancer_linked.*!is\\.na\\(cancer_category_names\\)", r57_lines_101)))

  # DRUG-03: Linked-only output preserved
  check("R/57 creates table1_linked (cancer-linked-only backward compat)",
        any(grepl("table1_linked", r57_lines_101)))

  check("R/57 creates table2_linked (cancer-linked-only backward compat)",
        any(grepl("table2_linked", r57_lines_101)))

  # D-02: Reference code filter still present
  check("R/57 retains reference code filter (valid_reference_codes OR Immunotherapy)",
        any(grepl("valid_reference_codes.*Immunotherapy", r57_lines_101)) ||
        any(grepl("triggering_code.*valid_reference_codes", r57_lines_101)))

  # Linked-only strips cancer_linked column (Pitfall 1)
  check("R/57 removes cancer_linked from linked-only export (select(-cancer_linked))",
        sum(grepl("select\\(-cancer_linked\\)", r57_lines_101)) >= 2)

  # D-06: Cross-tab summary sheet
  check("R/57 creates cross-tab summary (crosstab_summary)",
        any(grepl("crosstab_summary", r57_lines_101)))

  check("R/57 has 'Linked vs Unlinked' sheet in broadened workbook",
        any(grepl("Linked vs Unlinked", r57_lines_101)))

  # D-07/D-08: Dual-output file paths
  check("R/57 defines linked-only output paths (_linked_only suffix)",
        any(grepl("_linked_only\\.xlsx", r57_lines_101)))

  # D-09: Broadened has 3 sheets (wb_broad with 3 add_worksheet calls)
  check("R/57 creates separate broadened workbook (wb_broad)",
        any(grepl("wb_broad.*wb_workbook", r57_lines_101)))

  check("R/57 creates separate linked-only workbook (wb_linked)",
        any(grepl("wb_linked.*wb_workbook", r57_lines_101)))

  # Section 6B cross-tab exists
  check("R/57 has Section 6B for cross-tab summary",
        any(grepl("SECTION 6B.*CROSS-TAB", r57_lines_101)))

  # Decision traceability
  check("R/57 header contains Phase 101 decision traceability (D-01 through D-09)",
        any(grepl("D-01:", r57_lines_101)) && any(grepl("D-09:", r57_lines_101)))

  # Optional: output file validation (only if broadened xlsx exists)
  BROADENED_XLSX_PATH <- file.path(CONFIG$output_dir, "encounter_level_drug_grouping_instances.xlsx")
  LINKED_ONLY_XLSX_PATH <- file.path(CONFIG$output_dir, "encounter_level_drug_grouping_instances_linked_only.xlsx")

  if (file.exists(BROADENED_XLSX_PATH) && file.exists(LINKED_ONLY_XLSX_PATH)) {
    tryCatch({
      wb_b <- openxlsx2::wb_load(BROADENED_XLSX_PATH)
      wb_l <- openxlsx2::wb_load(LINKED_ONLY_XLSX_PATH)

      b_sheets <- wb_b$get_sheet_names()
      l_sheets <- wb_l$get_sheet_names()

      check("Broadened xlsx has 3 sheets",
            length(b_sheets) == 3)

      check("Linked-only xlsx has 2 sheets",
            length(l_sheets) == 2)

      check("Broadened xlsx contains 'Linked vs Unlinked' sheet",
            "Linked vs Unlinked" %in% b_sheets)

      # Check cancer_linked column in broadened
      b_data <- openxlsx2::wb_to_df(wb_b, sheet = "Enc: Sub-Category Detail", start_row = 1)
      check("Broadened Sheet 1 contains cancer_linked column",
            "cancer_linked" %in% colnames(b_data))

      # Check cancer_linked column NOT in linked-only
      l_data <- openxlsx2::wb_to_df(wb_l, sheet = "Enc: Sub-Category Detail", start_row = 1)
      check("Linked-only Sheet 1 does NOT contain cancer_linked column",
            !("cancer_linked" %in% colnames(l_data)))

      # Broadened should have >= rows than linked-only
      check("Broadened has >= rows than linked-only (broadened is superset)",
            nrow(b_data) >= nrow(l_data))

      # Cross-tab sheet structure
      ct_data <- openxlsx2::wb_to_df(wb_b, sheet = "Linked vs Unlinked", start_row = 1)
      check("Cross-tab has treatment_type, linked_count, unlinked_count columns",
            all(c("treatment_type", "linked_count", "unlinked_count") %in% colnames(ct_data)))

    }, error = function(e) {
      message(glue("  SKIP: Could not read xlsx files ({e$message})"))
    })
  } else {
    message("  SKIP: Broadened/linked-only xlsx not found (run R/57 first)")
  }

} else {
  message("  FAIL: R/57_drug_grouping_instances.R not found")
  failed <- failed + 1L
}
```

**3. Update the Section 16 SUMMARY validated requirements list (around line 2235-2258):**

Add after the existing Phase 88 entries (after the `P88-D07/D08` line):
```r
message("  * DRUG-01: Broadened output includes ALL treatment encounters (R/57 Phase 101)")
message("  * DRUG-02: cancer_linked TRUE/FALSE flag column on broadened output (R/57 Phase 101)")
message("  * DRUG-03: Linked-only output preserved with _linked_only suffix (R/57 Phase 101)")
```

**4. Update the Section 31 numbering for existing R/57 Phase 88 checks:**

The existing Section 31 (Phase 88 instance-level drug grouping tables) does NOT need renumbering — it is already "SECTION 31" in the section header. But its message line `[31/33]` or similar needs to match the new total. Check current message line and update as needed.

Actually, looking at the code: the existing sections use `SECTION 30:` for ICD-9, `SECTION 31:` for Phase 88, `SECTION 30:` for R/30 CONDITION (yes, there are two Section 30s). The new Phase 101 section should be `SECTION 31A:` or simply use a sequential number. Use `SECTION 101:` to match the phase numbering convention and avoid collision. Better yet: use the existing pattern and call it `SECTION 31B` to follow after Section 31 (Phase 88).

Simplest approach: keep existing section numbers unchanged (they are comments, not functional). Only update the `[N/M]` progress messages:
- Find `[30/31]` and change to `[30/32]`
- Find `[31/31]` and change to `[32/32]`
- New section uses `[31/32]`
  </action>
  <verify>
    <automated>cd /d C:\Users\Owner\Documents\insurance_investigation && powershell -Command "$lines = Get-Content R/88_smoke_test_comprehensive.R; Write-Output ('DRUG-01: ' + (($lines | Select-String 'DRUG-01').Count -ge 1)); Write-Output ('DRUG-02: ' + (($lines | Select-String 'DRUG-02').Count -ge 1)); Write-Output ('DRUG-03: ' + (($lines | Select-String 'DRUG-03').Count -ge 1)); Write-Output ('cancer_linked check: ' + (($lines | Select-String 'cancer_linked').Count -ge 3)); Write-Output ('Phase 101 section: ' + (($lines | Select-String 'PHASE 101').Count -ge 1)); Write-Output ('table1_all check: ' + (($lines | Select-String 'table1_all').Count -ge 1)); Write-Output ('wb_broad check: ' + (($lines | Select-String 'wb_broad').Count -ge 1)); Write-Output ('Linked vs Unlinked check: ' + (($lines | Select-String 'Linked vs Unlinked').Count -ge 1)); Write-Output ('31/32 progress: ' + (($lines | Select-String '31/32').Count -ge 1))"</automated>
  </verify>
  <acceptance_criteria>
    - R/88 contains `PHASE 101 -- BROADENED DRUG GROUPING OUTPUT` section header
    - R/88 contains `DRUG-01, DRUG-02, DRUG-03` in the section header comment
    - R/88 contains `table1_all` in a check() call
    - R/88 contains `table2_all` in a check() call
    - R/88 contains `table1_linked` in a check() call
    - R/88 contains `table2_linked` in a check() call
    - R/88 contains `cancer_linked` in at least 3 check() calls
    - R/88 contains `wb_broad` in a check() call
    - R/88 contains `wb_linked` in a check() call
    - R/88 contains `crosstab_summary` in a check() call
    - R/88 contains `Linked vs Unlinked` in a check() call
    - R/88 contains `_linked_only` in a check() call
    - R/88 contains `select(-cancer_linked)` in a check() call (Pitfall 1 validation)
    - R/88 contains `SECTION 6B.*CROSS-TAB` in a check() call
    - R/88 contains `D-01:` and `D-09:` traceability check
    - R/88 progress messages updated: `[31/32]` exists for Phase 101, `[32/32]` exists for DuckDB
    - R/88 validated requirements list contains `DRUG-01`, `DRUG-02`, `DRUG-03` entries
  </acceptance_criteria>
  <done>R/88 smoke test has a new validation section for Phase 101 broadened drug grouping output with 15+ structural checks covering DRUG-01/02/03, all locked decisions, pitfall prevention (cancer_linked column removal from linked-only), and optional runtime xlsx validation. Section numbering is consistent (total updated to 32). Summary requirements list includes DRUG-01/02/03.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Structural verification of R/57:
   - File contains `table1_all`, `table1_linked`, `table2_all`, `table2_linked`
   - File contains `cancer_linked` column derivation
   - File contains `crosstab_summary` with `pivot_wider`
   - File contains 4 output path constants (NEW_OUTPUT_XLSX, OLD_OUTPUT_XLSX, NEW_OUTPUT_LINKED_XLSX, OLD_OUTPUT_LINKED_XLSX)
   - File contains `wb_broad` and `wb_linked` separate workbooks
   - File contains `select(-cancer_linked)` for linked-only exports
   - File contains Phase 101 decision traceability (D-01 through D-09)

2. Structural verification of R/88:
   - File contains Phase 101 validation section with DRUG-01/02/03 checks
   - File has updated section numbering (32 total)
   - File summary list includes DRUG-01, DRUG-02, DRUG-03

3. No regressions:
   - R/57 still sources R/00_config.R, R/utils/utils_assertions.R, R/utils/utils_duckdb.R, R/utils/utils_cancer.R
   - R/57 still reads treatment_episode_detail.rds input
   - R/57 reference code filter (valid_reference_codes OR Immunotherapy) preserved
   - R/57 Table 2 source is still `detail_dx` (not `detail_codes`)
   - R/56 is NOT modified (per D-01)
</verification>

<success_criteria>
1. R/57 produces dual-output: broadened (3 sheets with cancer_linked flag) + linked-only (2 sheets without cancer_linked, backward compatible)
2. Cross-tab summary sheet has exactly 3 columns: treatment_type, linked_count, unlinked_count
3. Linked-only output matches current R/57 output structure exactly (no cancer_linked column, same column order)
4. R/88 validates all Phase 101 structural requirements
5. All locked decisions (D-01 through D-09) are implemented as specified
6. R/56 was NOT modified
</success_criteria>

<output>
After completion, create `.planning/phases/101-broadened-drug-grouping-output/101-01-SUMMARY.md`
</output>
