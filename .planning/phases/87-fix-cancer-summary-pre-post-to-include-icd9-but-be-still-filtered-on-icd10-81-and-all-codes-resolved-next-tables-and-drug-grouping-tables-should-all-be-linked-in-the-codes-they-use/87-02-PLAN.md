---
phase: 87-fix-cancer-summary-pre-post-to-include-icd9
plan: 02
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - R/45_cancer_summary.R
  - R/47_cancer_summary_refined.R
  - R/48_cancer_summary_post_hl.R
  - R/49_cancer_summary_pre_post.R
autonomous: true
requirements:
  - ICD-01
  - ICD-02
  - ICD-09
  - ICD-10
  - ICD-11
  - ICD-12

must_haves:
  truths:
    - "R/45 loads all diagnosis codes (ICD-9 and ICD-10) instead of filtering to DX_TYPE == 10"
    - "R/47 confirms HL cohort using both C81 and 201.x codes with 7-day gap"
    - "R/47 excludes ICD-9 benign/uncertain codes (210-239) alongside ICD-10 D-codes"
    - "R/48 loads all cancer diagnosis codes for temporal filtering (no DX_TYPE filter)"
    - "R/49 loads all cancer diagnosis codes for pre/post analysis (no DX_TYPE filters)"
    - "R/49 excludes 201.x codes from pre/post counts alongside C81 (HL anchor codes)"
    - "Category-level summaries merge ICD-9 and ICD-10 counts; code-level summaries keep them separate"
  artifacts:
    - path: "R/45_cancer_summary.R"
      provides: "Cancer summary with ICD-9 codes included"
      contains: "is_cancer_code"
    - path: "R/47_cancer_summary_refined.R"
      provides: "HL cohort confirmation with ICD-9 201.x support"
      contains: "201"
    - path: "R/48_cancer_summary_post_hl.R"
      provides: "Post-HL summary with ICD-9 codes included"
      contains: "is_cancer_code"
    - path: "R/49_cancer_summary_pre_post.R"
      provides: "Pre/post summary with ICD-9 codes, C81+201.x exclusion"
      contains: "201"
  key_links:
    - from: "R/45_cancer_summary.R"
      to: "R/utils/utils_cancer.R"
      via: "is_cancer_code() and classify_codes() calls"
      pattern: "is_cancer_code"
    - from: "R/47_cancer_summary_refined.R"
      to: "cache/outputs/confirmed_hl_cohort.rds"
      via: "saveRDS with ICD-9 201.x confirmed patients"
      pattern: "confirmed_hl_cohort"
    - from: "R/49_cancer_summary_pre_post.R"
      to: "R/utils/utils_cancer.R"
      via: "classify_codes() for category-level cross-system merging"
      pattern: "classify_codes"
---

<objective>
Update the cancer summary pipeline scripts (R/45, R/47, R/48, R/49) to include ICD-9 cancer codes alongside ICD-10, expand HL cohort confirmation to include 201.x, and handle cross-system code/category aggregation rules.

Purpose: Per D-01, D-09, D-10, D-11, and D-12, the cancer summary pipeline must become coding-system agnostic. Patients diagnosed before Oct 2015 (ICD-9 era) must be included in cancer summaries and HL cohort confirmation.

Output: Four updated R scripts that load both ICD-9 and ICD-10 cancer codes, confirm HL cohort via C81+201.x, and produce summaries with correct code-level separation and category-level merging.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/87-fix-cancer-summary-pre-post-to-include-icd9-but-be-still-filtered-on-icd10-81-and-all-codes-resolved-next-tables-and-drug-grouping-tables-should-all-be-linked-in-the-codes-they-use/87-CONTEXT.md
@.planning/phases/87-fix-cancer-summary-pre-post-to-include-icd9-but-be-still-filtered-on-icd10-81-and-all-codes-resolved-next-tables-and-drug-grouping-tables-should-all-be-linked-in-the-codes-they-use/87-RESEARCH.md
@.planning/phases/87-fix-cancer-summary-pre-post-to-include-icd9-but-be-still-filtered-on-icd10-81-and-all-codes-resolved-next-tables-and-drug-grouping-tables-should-all-be-linked-in-the-codes-they-use/87-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan consumes -->

From R/utils/utils_cancer.R (created in Plan 01):
```r
# Shared cancer code detection (map-based, gap-free)
is_cancer_code <- function(dx)  # Returns logical vector
# Detects ICD-10 (via CANCER_SITE_MAP keys) and ICD-9 (via ICD9_CANCER_SITE_MAP keys)
# Strips dots internally. Only detects malignant: 140-209 for ICD-9, C/D for ICD-10.
# Benign ICD-9 (210-239) returns FALSE.

# Updated classification with ICD-9 support
classify_codes <- function(codes)  # Returns character vector of category names
# Priority: ICD-10 4-char > ICD-10 3-char > ICD-9 4-char > ICD-9 3-char > NA
# Strips dots internally. Same category strings for both coding systems.
```

From R/00_config.R (created in Plan 01):
```r
ICD9_CANCER_SITE_MAP  # Named vector, 78 entries, keys "140"-"209" + "2010"-"2019"
```

Current patterns being replaced in each script:
```r
# R/45 line 69: filter(DX_TYPE == "10")
# R/47 line 111: filter(DX_TYPE == "10") in C81 cohort confirmation
# R/47 line 96: filter(!str_detect(cancer_code, "^D"))  -- D-code removal (extend for ICD-9)
# R/48 line 119: filter(DX_TYPE == "10")
# R/49 lines 106, 203, 346: filter(DX_TYPE == "10")
# R/49 line 224: filter(!str_detect(DX_norm, "^C81"))  -- extend to include 201.x
```

DX_TYPE column values: "09" for ICD-9, "10" for ICD-10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update R/45 and R/47 -- remove DX_TYPE filter, expand cohort confirmation (per D-01, D-02, D-09, D-10)</name>
  <files>R/45_cancer_summary.R, R/47_cancer_summary_refined.R</files>
  <read_first>
    - R/45_cancer_summary.R (full file -- understand DX_TYPE filter location, classification flow, description lookup)
    - R/47_cancer_summary_refined.R (full file -- understand D-code removal, C81 confirmation, first_hl_dx_date computation, cohort save)
    - R/utils/utils_cancer.R (verify is_cancer_code() and classify_codes() available from Plan 01)
    - R/00_config.R (verify ICD9_CANCER_SITE_MAP exists from Plan 01)
  </read_first>
  <action>
**R/45_cancer_summary.R changes:**

1. **Source utils_cancer.R** if not already sourced (check if `source("R/utils/utils_cancer.R")` exists in the script setup -- it likely does since classify_codes() is called at line 87). If not present, add it after `source("R/00_config.R")`.

2. **Remove DX_TYPE == "10" filter at line 69** (per D-01). Replace the loading section (lines 66-83) to load ALL diagnosis codes, then filter to cancer codes using is_cancer_code():

Replace:
```r
dx_icd10 <- get_pcornet_table("DIAGNOSIS") %>%
  filter(DX_TYPE == "10") %>%
  select(ID, DX, DX_DATE) %>%
  collect()
```

With:
```r
dx_all <- get_pcornet_table("DIAGNOSIS") %>%
  select(ID, DX, DX_TYPE, DX_DATE) %>%
  collect()
```

3. **Update normalization** (lines 76-77): Keep the normalization step but apply to all codes:
```r
dx_all <- dx_all %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\.")))
```

4. **Replace C/D prefix filter** (lines 80-81) with is_cancer_code():

Replace:
```r
dx_cancer <- dx_icd10 %>%
  filter(str_detect(DX_norm, "^[CD]"))
```

With:
```r
dx_cancer <- dx_all %>%
  filter(is_cancer_code(DX))
```

5. **Update log messages**: Change "ICD-10 DIAGNOSIS rows" to "All DIAGNOSIS rows", "ICD-10 only" to "all coding systems". Update variable name from `dx_icd10` to `dx_all` in all subsequent references within the loading section.

6. **Update the section comment** (around line 62-65): Remove references to "DX_TYPE == '10' for ICD-10" and "ICD-10 only". Replace with "All coding systems (ICD-9 + ICD-10), filtered to cancer codes via is_cancer_code()".

**R/47_cancer_summary_refined.R changes:**

1. **Source utils_cancer.R** if not already sourced. Verify by checking the source statements at the top. Add `source("R/utils/utils_cancer.R")` after config if missing.

2. **Extend D-code removal** (line 96, per D-02): The existing filter `filter(!str_detect(cancer_code, "^D"))` removes ICD-10 D-codes. Extend to also exclude ICD-9 benign/uncertain equivalents. Since cancer_summary.csv now includes ICD-9 codes (from R/45 change), ICD-9 codes 210-239 that somehow got through need removal. However, since R/45 now uses is_cancer_code() which only detects 140-209, ICD-9 210-239 should NOT appear. Still, add defensive filtering for safety:

Replace:
```r
cancer_summary <- cancer_summary %>%
  filter(!str_detect(cancer_code, "^D"))
```

With:
```r
# Remove ICD-10 D-codes and ICD-9 benign/uncertain equivalents (210-239) per D-02
# Note: is_cancer_code() in R/45 already excludes 210-239, but this is a defense-in-depth filter
cancer_summary <- cancer_summary %>%
  filter(!str_detect(cancer_code, "^D")) %>%
  filter(!(str_detect(cancer_code, "^2[1-3]") & nchar(str_remove(cancer_code, "\\.")) >= 3))
```

Wait -- cancer_code in cancer_summary.csv is `DX_norm` which is already undotted/uppercased. So ICD-9 benign codes would appear as "210", "2100", etc. A simpler and more robust approach:

```r
# Remove ICD-10 D-codes (per CREF-01) and ICD-9 benign/uncertain/in-situ (210-239, per D-02)
# is_cancer_code() upstream already excludes these, but defense-in-depth prevents data leaks
cancer_summary <- cancer_summary %>%
  filter(!str_detect(cancer_code, "^D")) %>%
  filter(!substr(cancer_code, 1, 3) %in% as.character(210:239))
```

3. **Expand cohort confirmation** (lines 109-128, per D-09, D-10): The current query loads only C81 codes with DX_TYPE=="10". Replace to load BOTH C81 (ICD-10) and 201.x (ICD-9) HL codes.

Replace Section 4 query (lines 110-115):
```r
dx_c81 <- get_pcornet_table("DIAGNOSIS") %>%
  filter(DX_TYPE == "10") %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(str_detect(DX_norm, "^C81")) %>%
  select(ID, DX_norm, DX_DATE) %>%
  collect()
```

With:
```r
# Query all HL diagnosis codes: ICD-10 C81 + ICD-9 201.x (per D-09)
dx_hl <- get_pcornet_table("DIAGNOSIS") %>%
  select(ID, DX, DX_TYPE, DX_DATE) %>%
  collect() %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(str_detect(DX_norm, "^C81") | str_detect(DX_norm, "^201"))
```

4. **Update confirmed_patients computation** (lines 121-127): The confirmation logic (2+ unique dates, 7-day span) already groups by ID only (not per sub-code), so ICD-9 201.x and ICD-10 C81 codes from the same patient will naturally combine at the category level (per D-10). No change to the confirmation algorithm itself -- just rename `dx_c81` to `dx_hl` throughout:

```r
confirmed_patients <- dx_hl %>%
  filter(!is.na(DX_DATE)) %>%
  distinct(ID, DX_DATE) %>%
  group_by(ID) %>%
  filter(as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7) %>%
  ungroup() %>%
  distinct(ID)
```

5. **Update first_hl_dx_date computation** (lines 137-142): Change `dx_c81` to `dx_hl` and update the message:

```r
# DIAGNOSIS earliest HL date per patient (C81 + 201.x, reuse dx_hl from Section 4)
dx_dates <- dx_hl %>%
  filter(!is.na(DX_DATE)) %>%
  group_by(ID) %>%
  summarise(first_dx_date_diagnosis = min(DX_DATE, na.rm = TRUE), .groups = "drop")

message(glue("  DIAGNOSIS: {format(nrow(dx_dates), big.mark=',')} patients with HL dates (C81 + 201.x)"))
```

6. **Update log messages** throughout: Change "C81 diagnosis rows" to "HL diagnosis rows (C81 + 201.x)", change "C81 cohort confirmation" to "HL cohort confirmation (C81 + 201.x)". Update section header comment for Section 4 from "C81 codes" to "C81 + 201.x codes".

7. **Update the confirmed cohort log message** (line 129):
```r
message(glue("Confirmed HL cohort: {format(nrow(confirmed_patients), big.mark=',')} patients (2+ C81/201.x codes, 7-day gap)"))
```
  </action>
  <verify>
    <automated>Rscript -e "source('R/00_config.R'); source('R/utils/utils_cancer.R'); lines_45 <- readLines('R/45_cancer_summary.R'); cat('R/45 DX_TYPE==10 removed:', !any(grepl('DX_TYPE.*==.*\"10\"', lines_45)), '\n'); cat('R/45 has is_cancer_code:', any(grepl('is_cancer_code', lines_45)), '\n'); lines_47 <- readLines('R/47_cancer_summary_refined.R'); cat('R/47 has 201:', any(grepl('201', lines_47)), '\n'); cat('R/47 DX_TYPE==10 removed:', !any(grepl('filter.*DX_TYPE.*==.*\"10\"', lines_47)), '\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/45_cancer_summary.R contains NO `DX_TYPE == "10"` filter
    - R/45_cancer_summary.R calls `is_cancer_code(DX)` for cancer code detection
    - R/45_cancer_summary.R selects DX_TYPE column from DIAGNOSIS table (available for downstream use)
    - R/47_cancer_summary_refined.R contains NO `filter(DX_TYPE == "10")` in cohort confirmation query
    - R/47_cancer_summary_refined.R filters for `str_detect(DX_norm, "^C81") | str_detect(DX_norm, "^201")` in HL query
    - R/47_cancer_summary_refined.R still uses `distinct(ID, DX_DATE)` and 7-day gap for confirmation (algorithm unchanged)
    - R/47_cancer_summary_refined.R D-code filter extended with `filter(!substr(cancer_code, 1, 3) %in% as.character(210:239))`
    - R/47_cancer_summary_refined.R log messages reference "C81 + 201.x" instead of "C81"
    - Variable name `dx_c81` replaced with `dx_hl` throughout R/47
  </acceptance_criteria>
  <done>R/45 loads all coding systems and filters to cancer codes via is_cancer_code(). R/47 confirms HL cohort using both C81 and 201.x codes, excludes ICD-9 benign equivalents, and saves updated confirmed_hl_cohort.rds that may include ICD-9-diagnosed patients.</done>
</task>

<task type="auto">
  <name>Task 2: Update R/48 and R/49 -- remove DX_TYPE filters, expand HL anchor exclusion (per D-01, D-11, D-12)</name>
  <files>R/48_cancer_summary_post_hl.R, R/49_cancer_summary_pre_post.R</files>
  <read_first>
    - R/48_cancer_summary_post_hl.R (full file -- DX_TYPE filter at line 119, C-code filter at line 121, cohort join at line 147)
    - R/49_cancer_summary_pre_post.R (full file -- DX_TYPE filters at lines 106, 203, 346; C81 exclusion at line 224; code vs category aggregation at lines 376-443)
    - R/utils/utils_cancer.R (verify is_cancer_code() available)
    - R/47_cancer_summary_refined.R (understand updated confirmed_hl_cohort.rds structure from Task 1)
  </read_first>
  <action>
**R/48_cancer_summary_post_hl.R changes:**

1. **Source utils_cancer.R** if not already sourced. Add after config source if missing.

2. **Remove DX_TYPE filter and expand C-code filter** (lines 118-124, per D-01). Replace:

```r
dx_raw <- get_pcornet_table("DIAGNOSIS") %>%
  filter(DX_TYPE == "10") %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(str_detect(DX_norm, "^C")) %>%
  select(ID, DX_norm, DX_DATE) %>%
  collect() %>%
  filter(ID %in% cohort_ids)
```

With:
```r
# Load all cancer diagnosis codes (ICD-9 + ICD-10, per D-01)
dx_raw <- get_pcornet_table("DIAGNOSIS") %>%
  select(ID, DX, DX_TYPE, DX_DATE) %>%
  collect() %>%
  filter(ID %in% cohort_ids) %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(is_cancer_code(DX)) %>%
  # Exclude D-codes (ICD-10) and ICD-9 benign/uncertain (210-239) per D-02
  filter(!str_detect(DX_norm, "^D")) %>%
  filter(!substr(DX_norm, 1, 3) %in% as.character(210:239))
```

3. **Update log message** (line 126): Change "C-code diagnosis rows" to "cancer diagnosis rows (C-codes + ICD-9 malignant)".

**R/49_cancer_summary_pre_post.R changes:**

1. **Source utils_cancer.R** if not already sourced.

2. **Update HL diagnostic section** (lines 105-111, per D-09): The current diagnostic query loads only C81 codes with DX_TYPE=="10". Expand to include 201.x:

Replace:
```r
hl_c81_dx <- get_pcornet_table("DIAGNOSIS") %>%
  filter(DX_TYPE == "10") %>%
  select(ID, DX, DX_DATE) %>%
  collect() %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(str_detect(DX_norm, "^C81")) %>%
  filter(ID %in% confirmed_hl_cohort$ID)
```

With:
```r
# HL diagnosis rows: ICD-10 C81 + ICD-9 201.x (per D-09)
hl_dx <- get_pcornet_table("DIAGNOSIS") %>%
  select(ID, DX, DX_TYPE, DX_DATE) %>%
  collect() %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(str_detect(DX_norm, "^C81") | str_detect(DX_norm, "^201")) %>%
  filter(ID %in% confirmed_hl_cohort$ID)
```

Rename `hl_c81_dx` to `hl_dx` throughout the diagnostic section (lines 105-152). Update the NLPHL/classical split (lines 137-138):
```r
# Phase 77 CANCER-01: NLPHL vs Classical HL diagnostic split
# ICD-10 C81.0x OR ICD-9 201.4x = NLPHL
hl_nlphl <- hl_dx %>% filter(str_detect(DX_norm, "^C810") | str_detect(DX_norm, "^2014"))
hl_classical <- hl_dx %>% filter(!str_detect(DX_norm, "^C810") & !str_detect(DX_norm, "^2014"))
```

Update log messages from "C81" to "HL (C81 + 201.x)".

3. **Remove DX_TYPE filter in Section 4** (lines 202-208, per D-01). Replace:

```r
dx_raw <- get_pcornet_table("DIAGNOSIS") %>%
  filter(DX_TYPE == "10") %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(str_detect(DX_norm, "^C")) %>%
  select(ID, DX_norm, DX_DATE) %>%
  collect() %>%
  filter(ID %in% cohort_ids)
```

With:
```r
# Load all cancer diagnosis codes (ICD-9 + ICD-10, per D-01)
dx_raw <- get_pcornet_table("DIAGNOSIS") %>%
  select(ID, DX, DX_TYPE, DX_DATE) %>%
  collect() %>%
  filter(ID %in% cohort_ids) %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(is_cancer_code(DX)) %>%
  # Exclude D-codes and ICD-9 benign/uncertain (210-239) per D-02
  filter(!str_detect(DX_norm, "^D")) %>%
  filter(!substr(DX_norm, 1, 3) %in% as.character(210:239))
```

4. **Extend C81 exclusion to include 201.x** (line 224, per D-11). The current code excludes C81 from pre/post analysis (HL is the anchor diagnosis). ICD-9 201.x codes must also be excluded since they're HL anchor codes too:

Replace:
```r
dx_raw <- dx_raw %>%
  filter(!str_detect(DX_norm, "^C81"))
```

With:
```r
# Exclude HL anchor codes from pre/post analysis (C81 + 201.x per D-11)
dx_raw <- dx_raw %>%
  filter(!str_detect(DX_norm, "^C81") & !str_detect(DX_norm, "^201"))
```

Update the log message from "C81 rows excluded" to "HL anchor rows excluded (C81 + 201.x)".

5. **Remove DX_TYPE filter in Section 6** (lines 345-348, per D-01). This is the record counts query. Replace:

```r
dx_record_counts <- get_pcornet_table("DIAGNOSIS") %>%
  filter(DX_TYPE == "10") %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(str_detect(DX_norm, "^C")) %>%
  select(ID, DX_norm) %>%
  collect() %>%
  filter(ID %in% cohort_ids) %>%
  ...
```

With:
```r
# Record counts query: all cancer codes (ICD-9 + ICD-10)
dx_record_counts <- get_pcornet_table("DIAGNOSIS") %>%
  select(ID, DX, DX_TYPE) %>%
  collect() %>%
  filter(ID %in% cohort_ids) %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(is_cancer_code(DX)) %>%
  filter(!str_detect(DX_norm, "^D")) %>%
  filter(!substr(DX_norm, 1, 3) %in% as.character(210:239)) %>%
  group_by(DX_norm) %>%
  summarise(total_records = n(), .groups = "drop")
```

6. **Update code-level HL anchor NA handling** (lines 383-387, per D-11): The current code sets pre/post/both to NA for C81 codes. Extend to include 201.x:

Replace:
```r
pre_hl_count  = if_else(str_detect(cancer_code, "^C81"), NA_integer_, ...),
post_hl_count = if_else(str_detect(cancer_code, "^C81"), NA_integer_, ...),
both_count    = if_else(str_detect(cancer_code, "^C81"), NA_integer_, ...),
```

With:
```r
# C81 + 201.x = HL anchor codes, NA for pre/post (per D-11)
pre_hl_count  = if_else(str_detect(cancer_code, "^C81") | str_detect(cancer_code, "^201"), NA_integer_, coalesce(as.integer(pre_hl_count), 0L)),
post_hl_count = if_else(str_detect(cancer_code, "^C81") | str_detect(cancer_code, "^201"), NA_integer_, coalesce(as.integer(post_hl_count), 0L)),
both_count    = if_else(str_detect(cancer_code, "^C81") | str_detect(cancer_code, "^201"), NA_integer_, coalesce(as.integer(both_count), 0L)),
```

7. **Update category-level HL anchor NA handling** (lines 438-441, per D-11): Category-level already checks for "Hodgkin Lymphoma (non-NLPHL)" and "NLPHL" strings, which classify_codes() now produces for both ICD-9 201.x and ICD-10 C81. This should work WITHOUT changes since the NA logic is based on category strings, not code prefixes. Verify this is the case and leave unchanged.

8. **Update the V2 population assertion** (lines 186-190): The current assertion expects v2_n_patients in [6300, 6500]. Adding ICD-9 201.x to cohort confirmation may increase the cohort. The pipeline runs on HiPerGator with real data, so the assertion bounds may need widening. Add a comment noting this:

```r
# NOTE: Phase 87 expanded cohort to include ICD-9 201.x confirmed patients.
# Assertion bounds may need adjustment when first run with real data.
# Expected increase: 5-15% based on OneFlorida+ 2011-2025 date range.
checkmate::assert_int(
  as.integer(v2_n_patients),
  lower = 6300L, upper = 7500L,  # Widened upper bound for ICD-9 cohort expansion
  .var.name = glue("[R/49 CANCER-02 ERROR] V2 7-day total population expected 6300-7500, got {v2_n_patients}")
)
```
  </action>
  <verify>
    <automated>Rscript -e "lines_48 <- readLines('R/48_cancer_summary_post_hl.R'); cat('R/48 DX_TYPE==10 removed:', !any(grepl('filter.*DX_TYPE.*==.*\"10\"', lines_48)), '\n'); cat('R/48 has is_cancer_code:', any(grepl('is_cancer_code', lines_48)), '\n'); lines_49 <- readLines('R/49_cancer_summary_pre_post.R'); cat('R/49 DX_TYPE==10 removed:', !any(grepl('filter.*DX_TYPE.*==.*\"10\"', lines_49)), '\n'); cat('R/49 has 201 exclusion:', any(grepl('201.*filter|filter.*201', lines_49)), '\n'); cat('R/49 has is_cancer_code:', any(grepl('is_cancer_code', lines_49)), '\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/48_cancer_summary_post_hl.R contains NO `filter(DX_TYPE == "10")` anywhere
    - R/48_cancer_summary_post_hl.R calls `is_cancer_code(DX)` for cancer code detection
    - R/48_cancer_summary_post_hl.R includes ICD-9 benign exclusion filter (`210:239`)
    - R/49_cancer_summary_pre_post.R contains NO `filter(DX_TYPE == "10")` anywhere (3 instances removed)
    - R/49_cancer_summary_pre_post.R diagnostic section uses `^C81.*|.*^201` pattern for HL detection
    - R/49_cancer_summary_pre_post.R line ~224 excludes `^201` alongside `^C81` from pre/post
    - R/49_cancer_summary_pre_post.R code-level summary sets NA for `str_detect(cancer_code, "^201")` alongside C81
    - R/49_cancer_summary_pre_post.R category-level summary unchanged (already uses category string matching)
    - R/49_cancer_summary_pre_post.R V2 assertion upper bound widened to 7500
    - Variable name `hl_c81_dx` replaced with `hl_dx` in R/49 diagnostic section
  </acceptance_criteria>
  <done>R/48 and R/49 load all cancer codes regardless of coding system. R/49 excludes 201.x from pre/post counts alongside C81. Code-level summaries keep ICD-9 and ICD-10 codes separate; category-level summaries merge via classify_codes() which maps both systems to the same category strings. Downstream effects of cohort expansion documented with widened assertion bounds.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "DX_TYPE.*==.*\"10\"" R/45_cancer_summary.R R/47_cancer_summary_refined.R R/48_cancer_summary_post_hl.R R/49_cancer_summary_pre_post.R` should show 0 for ALL four files
2. `grep -l "is_cancer_code" R/45_cancer_summary.R R/48_cancer_summary_post_hl.R R/49_cancer_summary_pre_post.R` should list all three files
3. `grep "201" R/47_cancer_summary_refined.R` should show 201.x in HL cohort confirmation query
4. `grep "201" R/49_cancer_summary_pre_post.R` should show 201.x in both diagnostic section and pre/post exclusion
5. `grep "210:239" R/47_cancer_summary_refined.R R/48_cancer_summary_post_hl.R R/49_cancer_summary_pre_post.R` should show benign exclusion in all three
</verification>

<success_criteria>
- All four cancer summary scripts (R/45, R/47, R/48, R/49) load ICD-9 and ICD-10 codes
- No DX_TYPE == "10" hard-filters remain in any of the four scripts
- HL cohort confirmation in R/47 uses C81 + 201.x with 7-day gap (cross-system allowed per D-10)
- R/49 excludes both C81 and 201.x from pre/post counts (per D-11)
- ICD-9 benign/uncertain codes (210-239) excluded from all cancer summary scripts (per D-02)
- Code-level summaries maintain separate ICD-9 and ICD-10 entries (per D-11)
- Category-level summaries merge cross-system via classify_codes() (per D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/87-fix-cancer-summary-pre-post-to-include-icd9-but-be-still-filtered-on-icd10-81-and-all-codes-resolved-next-tables-and-drug-grouping-tables-should-all-be-linked-in-the-codes-they-use/87-02-SUMMARY.md`
</output>
