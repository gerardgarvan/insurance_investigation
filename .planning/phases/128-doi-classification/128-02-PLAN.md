---
phase: 128-doi-classification
plan: "02"
type: execute
wave: 2
depends_on: ["128-01"]
files_modified:
  - R/111_doi_classification.R
autonomous: true
requirements: [DOI-CLASS-03]
must_haves:
  truths:
    - "doi_patients.rds is derived from the doi_encounters grain at one row per ID (PATID)"
    - "doi_patients has has_any_doi, doi_categories (ascending), doi_first_date, doi_last_date, n_doi_encounters, in_hl_cohort"
    - "A tabyl(doi_category) clinical-plausibility count review is logged before teardown"
    - "close_pcornet_con() runs at the end of the script"
  artifacts:
    - path: "R/111_doi_classification.R"
      provides: "Section 7 patient-grain rollup + tabyl review + doi_patients.rds writer + close_pcornet_con"
      contains: "doi_patients.rds"
    - path: "R/111_doi_classification.R"
      provides: "tabyl clinical plausibility review of doi_category"
      contains: "tabyl(doi_category)"
  key_links:
    - from: "R/111_doi_classification.R Section 7"
      to: "doi_encounters (in-memory from Section 6)"
      via: "group_by(ID) %>% summarise(...) rollup"
      pattern: "group_by\\(ID\\)"
    - from: "R/111_doi_classification.R"
      to: "doi_patients.rds"
      via: "saveRDS to CONFIG$cache$outputs_dir"
      pattern: "saveRDS\\(.*doi_patients"
---

<objective>
Append Section 7 to R/111_doi_classification.R: derive the patient-level DoI summary `doi_patients.rds` from the encounter-grain `doi_encounters` produced by Plan 128-01, log the `tabyl(doi_category)` clinical-plausibility count review, and tear down the DuckDB connection.

Purpose: doi_patients.rds is the patient-grain rollup (DOI-CLASS-03). It is derived FROM the encounter grain (not re-queried from DIAGNOSIS), giving Phase 129 both grains cheaply. The tabyl review is the clinical sanity gate (RA should dominate; NMO/pemphigus rare). This plan closes the script (owns `close_pcornet_con()`).

Output: R/111_doi_classification.R Section 7 (patient rollup, tabyl review, doi_patients.rds write, close_pcornet_con) + doi_patients.rds artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-doi-classification/128-CONTEXT.md
@.planning/phases/128-doi-classification/128-01-PLAN.md

<interfaces>
<!-- doi_encounters is produced by Plan 128-01 Section 6 and is in-memory when Section 7 runs. -->
doi_encounters columns (from 128-01):
  ID, ENCOUNTERID, DX_DATE (Date), doi_code, doi_category, paraneoplastic_flag (logical), in_hl_cohort (logical)

From R/00_config.R (already sourced by 128-01 Section 1):
  CONFIG$cache$outputs_dir   # .rds artifact directory

janitor::tabyl is available (library(janitor) loaded in 128-01 Section 1).

Multi-value ascending-collapse convention (v3.2 Phase 112, cited in 128-CONTEXT code_context):
  sort + unique + delimiter join, alphabetical ascending. Use "; " as the delimiter
  (matches existing multi-value field convention in the pipeline).

close_pcornet_con() from R/utils/utils_duckdb.R tears down the global connection.
</interfaces>

# Pattern to mirror (close_pcornet_con at end; CONFIG$cache$outputs_dir .rds idiom):
@R/107_med_admin_dispensing_gap_diagnostic.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patient-grain rollup + tabyl review + write doi_patients.rds + close connection</name>
  <files>R/111_doi_classification.R</files>
  <read_first>
    - R/111_doi_classification.R (the script from Plan 128-01 — append Section 7 after Section 6b)
    - .planning/phases/128-doi-classification/128-01-PLAN.md (doi_encounters grain + column contract)
    - .planning/phases/128-doi-classification/128-CONTEXT.md (D-02 in_hl_cohort on patient grain; D-04 L10.81 counts in n_doi_encounters; doi_categories ascending)
    - R/107_med_admin_dispensing_gap_diagnostic.R lines 440-445 (close_pcornet_con teardown + final message idiom)
    - R/100_ruca_rurality_summary.R lines 75-76 (CONFIG$cache$outputs_dir .rds path idiom)
  </read_first>
  <action>
Append SECTION 7 — PATIENT-GRAIN ROLLUP to R/111_doi_classification.R.

7a — Roll up doi_encounters to one row per ID (PATID). L10.81 rows STILL count (D-04): do NOT exclude paraneoplastic rows from n_doi_encounters, doi_categories, or has_any_doi.
```r
doi_patients <- doi_encounters %>%
  group_by(ID) %>%
  summarise(
    has_any_doi      = TRUE,                                    # every ID here has >=1 DoI encounter
    doi_categories   = paste(sort(unique(doi_category)), collapse = "; "),  # ascending alphabetical (v3.2 Phase 112 convention)
    doi_first_date   = suppressWarnings(min(DX_DATE, na.rm = TRUE)),
    doi_last_date    = suppressWarnings(max(DX_DATE, na.rm = TRUE)),
    n_doi_encounters = n_distinct(ENCOUNTERID),
    in_hl_cohort     = any(in_hl_cohort),                       # TRUE if patient is in HL cohort (D-02)
    .groups = "drop"
  )
```
A comment MUST state: "# Patient grain is DERIVED from doi_encounters (Section 6) — DIAGNOSIS is NOT re-queried (DOI-CLASS-03). L10.81/paraneoplastic encounters STILL count toward n_doi_encounters and doi_categories (D-04)."
Handle the min/max-of-all-NA edge (if a patient's DX_DATE are all NA, min/max returns Inf/-Inf): convert non-finite dates to NA:
```r
doi_patients <- doi_patients %>%
  mutate(
    doi_first_date = as.Date(ifelse(is.finite(doi_first_date), doi_first_date, NA), origin = "1970-01-01"),
    doi_last_date  = as.Date(ifelse(is.finite(doi_last_date),  doi_last_date,  NA), origin = "1970-01-01")
  )
```

7b — tabyl clinical-plausibility count review (log to console; DOI-CLASS design constraint / 128-CONTEXT in-scope item):
```r
message("--- DoI category clinical-plausibility review (RA should dominate; NMO/pemphigus rare) ---")
doi_cat_review <- doi_encounters %>% janitor::tabyl(doi_category)
print(doi_cat_review)
n_paraneoplastic <- sum(doi_encounters$paraneoplastic_flag)
message(glue("Paraneoplastic (L10.81) encounters flagged: {n_paraneoplastic}"))
message(glue("Patients with any DoI: {format(nrow(doi_patients), big.mark=',')}; ",
             "in HL cohort: {format(sum(doi_patients$in_hl_cohort), big.mark=',')}"))
```
Add a comment: "# INTERNAL-ONLY review counts: raw, NO small-cell suppression (Phase 127 D-07). Suppress manually before external sharing."

7c — Write doi_patients.rds:
```r
doi_patients_path <- file.path(CONFIG$cache$outputs_dir, "doi_patients.rds")
saveRDS(doi_patients, doi_patients_path, compress = TRUE)
message(glue("Wrote doi_patients.rds: {format(nrow(doi_patients), big.mark=',')} patients -> {doi_patients_path}"))
```

7d — Teardown + final message:
```r
close_pcornet_con()
message("\nDone. (R/111 DoI classification — doi_encounters.rds + doi_patients.rds written)")
```
  </action>
  <verify>
    <automated>grep -q 'group_by(ID)' R/111_doi_classification.R && grep -q 'saveRDS(doi_patients' R/111_doi_classification.R && grep -q 'tabyl(doi_category)' R/111_doi_classification.R && grep -q 'close_pcornet_con()' R/111_doi_classification.R && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains `group_by(ID)` followed by `summarise(` (grep -n: group_by line precedes a summarise line).
    - The summarise block contains all six output fields: `has_any_doi`, `doi_categories`, `doi_first_date`, `doi_last_date`, `n_doi_encounters`, `in_hl_cohort`.
    - File contains `paste(sort(unique(doi_category)), collapse = "; ")` (ascending-collapse convention).
    - File contains `n_distinct(ENCOUNTERID)`.
    - File contains `janitor::tabyl(doi_category)` (grep: `tabyl(doi_category)`).
    - File contains `file.path(CONFIG$cache$outputs_dir, "doi_patients.rds")` and `saveRDS(doi_patients`.
    - File contains `close_pcornet_con()` exactly once (grep -c returns 1).
    - The `saveRDS(doi_patients` line number is GREATER than the `saveRDS(doi_encounters` line number (grep -n: patient rollup writes after the encounter artifact — confirms it derives from the encounter grain).
    - File contains a comment string `DIAGNOSIS is NOT re-queried` (grep -q).
  </acceptance_criteria>
  <done>doi_patients.rds is written at one row per ID with the six required fields, the tabyl(doi_category) review is logged, and close_pcornet_con() tears down the connection at the end of the script.</done>
</task>

</tasks>

<verification>
- Section 7 appended to R/111 with a patient-grain rollup from `doi_encounters` (group_by(ID) %>% summarise), NOT a DIAGNOSIS re-query (DOI-CLASS-03).
- doi_patients has_any_doi / doi_categories(ascending "; ") / doi_first_date / doi_last_date / n_doi_encounters / in_hl_cohort.
- L10.81/paraneoplastic encounters still count in n_doi_encounters and doi_categories (D-04).
- tabyl(doi_category) clinical-plausibility review printed to console.
- doi_patients.rds written to CONFIG$cache$outputs_dir AFTER doi_encounters.rds.
- close_pcornet_con() present exactly once (teardown owned by this plan).
</verification>

<success_criteria>
- DOI-CLASS-03: patient-level DoI summary derived from the encounter grain, one row per PATID, with the six required fields including in_hl_cohort (D-02).
- Clinical-plausibility tabyl review logged (RA dominant; NMO/pemphigus rare) — the sanity gate before hand-off to Phase 129.
- Script tears down cleanly (close_pcornet_con at end).
- Structural verification passes on Windows via grep (real-data counts are the Phase 130 HiPerGator gate).
</success_criteria>

<output>
After completion, create `.planning/phases/128-doi-classification/128-02-SUMMARY.md`
</output>
