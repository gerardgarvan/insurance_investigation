---
phase: 158-surveillance-modality-frequency
plan: 04
type: execute
wave: 4
depends_on: [158-01, 158-02, 158-03]
files_modified:
  - R/39_run_all_investigations.R
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: false
requirements: [SURV-01, SURV-02, SURV-03, SURV-04, SURV-05, SURV-06]

must_haves:
  truths:
    - "R/39 runs the new script through its existing investigation-script registration mechanism"
    - "R/88 has a new section, in R/88's own PASS/FAIL recording style, with invariant checks and no literal codeset row count"
    - "SCRIPT_INDEX.md lists the new script and utils_surveillance.R"
    - "On HiPerGator: both Phase 158 test files pass, R/88 passes, the script runs, and the team reviews A_code_presence in the INTERNAL workbook before the release workbook circulates"
  artifacts:
    - path: "R/39_run_all_investigations.R"
      provides: "Registration of R/{NNN}_surveillance_modality_frequency.R"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Phase 158 section"
    - path: "R/SCRIPT_INDEX.md"
      provides: "Rows for the script and utils_surveillance.R"
---

<objective>
Register the script, add smoke-test coverage, and run the phase gate on HiPerGator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-surveillance-modality-frequency/158-CONTEXT.md
@.planning/phases/158-surveillance-modality-frequency/158-03-SUMMARY.md
@R/39_run_all_investigations.R
@R/88_smoke_test_comprehensive.R
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register in R/39, R/88, SCRIPT_INDEX</name>

  <read_first>
    R/39_run_all_investigations.R — how investigation scripts are registered (recent scripts are entries in an `investigation_scripts` vector sourced in their own environment); whether outputs are listed in an expected-outputs vector
    R/88 smoke test — confirm the file name (expected `R/88_smoke_test_comprehensive.R`), the last section id (e.g. 15ai), and the PASS/FAIL recording helper recent sections use
    R/SCRIPT_INDEX.md — row format
  </read_first>

  <files>R/39_run_all_investigations.R, R/88_smoke_test_comprehensive.R, R/SCRIPT_INDEX.md</files>

  <action>
**R/39:** add `R/{NNN}_surveillance_modality_frequency.R` as the last entry of the investigation-script registration vector, following the R/115 / R/122 precedent. Outputs carry a run date in their names, so do not add them to any fixed expected-filename list.

**R/88:** add the next free section after the last existing one, using the same check-recording helper and footer convention as recent sections (not bare `stopifnot()`, which would halt the whole smoke test). Checks:
1. `data/reference/surveillance_codeset.xlsx` exists
2. `load_surveillance_codeset()` returns without error (this enforces all structural rules)
3. `codeset_row_id` unique and `nrow(codeset) > 0` — no literal row count (D-18)
4. Stress test and Echocardiogram each have rows for 93350, 93351, 93352 (D-20)
5. No modality "Thyroid stimulating hormone"; Thyroid function submodalities are exactly TSH and Free T4 (D-21)
6. Exactly one `component_all_same_day` row, cdm_table LAB_RESULT_CM (D-22)
7. Functions exist: `load_surveillance_codeset`, `get_hl_any_dx_ids`, `match_coded_events`, `build_code_presence`, `compute_followup`, `classify_event_window`, `compute_modality_stats`, `build_patient_modality`, `suppress_table`
8. The script file contains `stopifnot`, `INTERNAL`, `suppress_table`, and does not contain `as.Date(PX_DATE`
9. Both `tests/testthat/test-158-*.R` files exist
Use a footer id `SMOKE-158-01` (or the file's equivalent convention).

**R/SCRIPT_INDEX.md:** add a row for `R/{NNN}_surveillance_modality_frequency.R` (Phase 158; SURV-01..06; input `data/reference/surveillance_codeset.xlsx`; outputs `surveillance_modality_frequency_INTERNAL_<date>.xlsx`, `surveillance_modality_frequency_<date>.xlsx`, `surveillance_modality_patient_<date>.rds`) and, if the index lists utils modules, a row for `R/utils/utils_surveillance.R`. Update any script-count total.
  </action>

  <verify>
    <automated>Rscript -e "a <- readLines('R/39_run_all_investigations.R'); stopifnot(any(grepl('surveillance_modality_frequency', a))); b <- readLines(list.files('R', pattern = '^88_.*[.]R$', full.names = TRUE)[1]); stopifnot(any(grepl('load_surveillance_codeset', b)), !any(grepl('== *108', b))); i <- readLines('R/SCRIPT_INDEX.md'); stopifnot(any(grepl('surveillance_modality_frequency', i))); message('OK')"</automated>
  </verify>

  <done>Script registered in R/39; R/88 section added in house style with no literal row count; SCRIPT_INDEX updated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HiPerGator run and A_code_presence review</name>

  <what-built>
    Codeset, tested helpers, the investigation script, and its registration. Nothing has run on real PCORnet data yet.
  </what-built>

  <how-to-verify>
    On HiPerGator, from the project root:
    1. Tests: `Rscript -e "testthat::test_dir('tests/testthat', filter = '158', stop_on_failure = TRUE)"` — expect 68 passing expectations. If 158-01/158-02 deferred their test runs, this is the first real run.
    2. Smoke test: run R/88 and confirm the new Phase 158 section is all PASS and no previously passing section regressed.
    3. Script: `Rscript R/{NNN}_surveillance_modality_frequency.R 2>&1 | tee 158_run.log`. Every stopifnot must pass; the log reports the denominator, drop counts and rows collected per table.
    4. Open `surveillance_modality_frequency_INTERNAL_<date>.xlsx` (unsuppressed; do not copy off HiPerGator).
       - QC: denominator N and confirmed N plausible; zero/missing follow-up counts; the PX_TYPE and DX_TYPE distributions; "Matched rows, other PX_TYPE/DX_TYPE" — if non-zero, check A's `other_types` (e.g. `C4`/`HC` legacy CPT types) and decide whether the codeset `type_filter` should change.
       - A_code_presence with the team: orange rows (absent) — expected for retired codes whose `last_date` would precede retirement; a red flag for current primary codes. Present rows that look clinically implausible. Rows with `plausibility = verify`.
       - CBC near-miss count on QC vs the CBC component row's n_records.
    5. Codeset corrections go back as edits to `data/reference/surveillance_codeset.xlsx` (then re-run). Only after the review is the release workbook `surveillance_modality_frequency_<date>.xlsx` shared.
  </how-to-verify>

  <resume-signal>
    Type "approved — A_code_presence reviewed, release workbook cleared" to complete the phase,
    or "Issues: [list]" for codeset corrections / fixes before re-run.
  </resume-signal>
</task>

</tasks>

<verification>
Registration checks above; runtime verification is Task 2 on HiPerGator.
</verification>

<success_criteria>
- Script registered in R/39, R/88 section added (PASS on HiPerGator), SCRIPT_INDEX updated
- 68 Phase 158 test expectations pass on HiPerGator
- Script completes on real data; INTERNAL and release workbooks and .rds written
- A_code_presence reviewed before the release workbook circulates
</success_criteria>

<output>
After completion, create `.planning/phases/158-surveillance-modality-frequency/158-04-SUMMARY.md`
</output>
