---
phase: 160-surveillance-lab-accuracy-and-reporting-improvements
plan: 03
type: execute
wave: 3
depends_on: ["160-01", "160-02"]
files_modified:
  - R/147_surveillance_modality_frequency.R
autonomous: true
requirements: [IMP-02, IMP-04, IMP-05]
must_haves:
  truths:
    - "R/147 produces an A3_missing_analyte sheet: block 1 (missing analyte overall + by year) and block 2 (candidate LOINC codes from a DuckDB-restricted sampled query)"
    - "Mammogram and Breast MRI rows in B and C keep every all-patient column and gain female-denominator + other-sex columns"
    - "R/147 produces a Codeset_summary sheet in both workbooks from the loaded codeset"
    - "The release workbook suppresses A3 counts and drops RAW_LAB_CODE/RAW_LAB_NAME from A3 block 2 (LOINC-level only)"
    - "Sheet order is KEY, A_code_presence, A2_analyte_presence, A3_missing_analyte, B, C, D, E (internal only), QC, Codeset_summary"
  artifacts:
    - path: "R/147_surveillance_modality_frequency.R"
      provides: "A3 build + sampled candidate query, eligibility applied to B/C, Codeset_summary, DEMOGRAPHIC.SEX pull"
      contains: "A3_missing_analyte"
  key_links:
    - from: "R/147 A3 block 2"
      to: "LAB_RESULT_CM (DuckDB)"
      via: "temp table of sampled near-miss IDs, semi_join before collect()"
      pattern: "copy_to|semi_join"
    - from: "R/147 B/C sheets"
      to: "compute_eligible_modality_stats() + DEMOGRAPHIC.SEX"
      via: "sex_lookup + eligible_sex from Modalities"
      pattern: "compute_eligible_modality_stats|DEMOGRAPHIC"
---

<objective>
Wire the four Phase 160 functions and the sampled candidate query into R/147, producing the A3 and Codeset_summary sheets and the female-denominator columns, with correct two-workbook suppression.

Purpose: IMP-02 (A3), IMP-04 (eligibility columns + DEMOGRAPHIC.SEX), IMP-05 (Codeset_summary). This is the integration plan; all counting logic already lives in tested pure functions from 160-02, and the codeset already carries the threshold + eligible_sex from 160-01.
Output: an updated `R/147_surveillance_modality_frequency.R`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md
@160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md
@R/147_surveillance_modality_frequency.R

<interfaces>
<!-- R/147 anchor points (current line numbers). -->
SECTION 1 (line 49): loads codeset, analytes, mod_lookup. Add DEMOGRAPHIC pull + eligibility here or SECTION 6.
SECTION 2 (line 77): DuckDB handles + hl_ids_tbl temp table via copy_to(con, ..., name="tmp_surv_hl_ids"). REUSE this copy_to pattern for the sampled near-miss IDs.
SECTION 4B (line 162): analyte pull -> analyte_hits (map_analyte_hits output). A3 block 1 consumes analyte_hits + codeset.
SECTION 7 (line 244): an_rules <- build_analyte_events(analyte_hits, codeset). near_miss table is an_rules$near_miss.
SECTION 8 (line 265): builds A_code_presence, A2, B_primary/S_sens/S_any via stats_for(), C_with_sensitivity, D_pre_vs_post, patient_wide.
SECTION 9 (line 388): write_workbook(path, release) builds the `sheets` list and writes. Sheet order = order of the `sheets` list. Suppression via sup()/suppress_table()/suppress_small().

New functions available (from 160-02, in utils_surveillance.R):
  summarise_missing_analyte(hits, codeset) -> list(by_year=, overall=)
  rank_candidate_codes(candidate_rows, sampled_id_dates, analytes, excluded, master_codes) -> ranked tibble
  compute_eligible_modality_stats(events, followup, keys, tiers, sex_lookup, eligible_sex, by) -> stats + eligible/other-sex cols
  build_codeset_summary(codeset, analytes, analyte_presence) -> list(by_modality=, by_analyte=)
  modality_eligible_sex(lookup) -> named "" / "F" / "M" vector (from 160-01)

DuckDB pull pattern (existing, SECTION 4B): lab_tbl |> semi_join(hl_ids_tbl, by="ID") |> filter(dplyr::sql(where)) |> select(any_of(...)) |> distinct() |> collect(); dates parsed after collect via pick_date().
Lab_Analytes_Excluded sheet: read with readxl for the excluded-reason flag input.
crosswalk MASTER: lab_code_crosswalk.xlsx MASTER sheet — read for master_codes (normalized LOINC set).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEMOGRAPHIC.SEX pull + apply eligibility to B and C</name>
  <read_first>
    - R/147_surveillance_modality_frequency.R (SECTION 1 lines 49-74; SECTION 2 lines 77-116 for lazy_table + hl_ids_tbl; SECTION 6 lines 218-241 for the enc/death pull pattern; SECTION 8 lines 276-307 for stats_for()/C_with_sensitivity)
    - R/utils/utils_surveillance.R (compute_eligible_modality_stats signature from 160-02; modality_eligible_sex from 160-01)
    - .planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md (D-04 headline framing; L-5 add-nothing-removed)
  </read_first>
  <action>
    1. Add a DEMOGRAPHIC pull in SECTION 6 (beside the enc/death pulls, reusing `lazy_table("DEMOGRAPHIC")` + `semi_join(hl_ids_tbl)` + `collect()`): build `sex_lookup <- tibble(ID = demo_raw$ID, sex = trimws(as.character(demo_raw$SEX)))`. Keep only IDs in `followup` (the denominator). Report SEX distribution in QC via the existing `count_by()`.

    2. Read `eligible_sex` from the Modalities lookup: `elig <- modality_eligible_sex(mod_lookup)`. This gives "F" for Mammogram and Breast MRI, "" elsewhere.

    3. In SECTION 8, replace the modality-level all-patient stats for the eligible modalities so that for each modality-level key its `eligible_sex` is applied. Implementation that preserves existing behaviour for every non-eligible modality (L-5): keep `stats_for()` as-is for submodality keys, but compute the modality-level (`submodality == "(all)"`) B/C rows through `compute_eligible_modality_stats(events_win, followup, mod_keys_one, tiers, sex_lookup, eligible_sex = elig[[modality]])` per modality, then bind. The six extra columns (n_patients_eligible, pct_of_eligible, person_years_eligible, events_per_person_year_eligible, n_patients_other_sex, total_event_dates_other_sex) are present only on the two eligible modality rows and NA/absent (coalesced to blank at write) elsewhere.

    4. Assert the all-patient columns are unchanged for a non-eligible modality: add a `stopifnot` comparing the all-patient `n_patients`/`total_event_dates` for e.g. CBC to what plain `compute_modality_stats()` produces (L-5 guard).

    Do NOT touch the D sheet or the per-patient `patient_wide` table (L-5: those stay all-patient). SC-4 continues to apply to the all-patient columns only.
  </action>
  <verify>
    <automated>grep -n "DEMOGRAPHIC\|sex_lookup\|compute_eligible_modality_stats\|modality_eligible_sex" R/147_surveillance_modality_frequency.R</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lazy_table(\"DEMOGRAPHIC\")\|\"DEMOGRAPHIC\"" R/147_surveillance_modality_frequency.R` matches a DEMOGRAPHIC pull
    - `grep -n "sex_lookup" R/147_surveillance_modality_frequency.R` matches
    - `grep -n "compute_eligible_modality_stats" R/147_surveillance_modality_frequency.R` matches
    - B/C for Mammogram and Breast MRI carry the six new column names; other modalities do not (verifiable by column presence + blank/NA)
    - A stopifnot guards that a non-eligible modality's all-patient n_patients equals the plain compute_modality_stats() value (L-5)
    - D sheet and patient_wide code paths are unchanged (no eligibility filtering there)
  </acceptance_criteria>
  <done>Sex is pulled from DEMOGRAPHIC; B/C add female + other-sex columns for the two breast modalities only; all-patient columns provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: A3 build (block 1 + sampled DuckDB block 2) + Codeset_summary + sheet order + suppression</name>
  <read_first>
    - R/147_surveillance_modality_frequency.R (SECTION 4B lines 162-202 for the DuckDB lab pull + copy_to temp-table pattern; SECTION 7 line 251 an_rules$near_miss; SECTION 9 lines 388-475 write_workbook sheets list + sup()/suppress_small())
    - R/utils/utils_surveillance.R (summarise_missing_analyte, rank_candidate_codes, build_codeset_summary signatures from 160-02)
    - .planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md (D-02, D-03 sample 5000 fixed seed; L-2 report-only; L-3 RAW_* internal only; L-4 restrict to sampled IDs before collect; specifics: blank-LAB_LOINC flag at checkpoint)
    - 160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md (IMP-02 block 2 column list + candidate query mapping; IMP-05 Codeset_summary two blocks)
  </read_first>
  <action>
    Add a SECTION 8B (A3) and Codeset_summary build, then wire both into SECTION 9.

    A3 block 1 (in memory, no query):
    - `a3_block1 <- summarise_missing_analyte(analyte_hits, codeset)`. Assert its SC125 (BMP) total at `n_analytes_present == n_listed-1` equals `an_rules$near_miss$n_id_dates` for the BMP `analyte_all_same_day` row where `n_analytes_present == n_listed - 1` (brief success criterion 2). Do this with `stopifnot`.

    A3 block 2 (sampled DuckDB query, D-03 / L-4):
    - Set `A3_SEED <- 42L` (Claude's discretion; put it as a top-of-script constant with a comment). For each rule row with n_listed >= 2, take its n_listed-1 near-miss ID x dates (from `summarise_missing_analyte`'s day-level detail — expose that detail or recompute from analyte_hits), and `dplyr::slice_sample(n = min(5000, nrow), replace = FALSE)` under `set.seed(A3_SEED)`. Record the realized sample size per rule row for the sheet.
    - Build a temp table of the DISTINCT sampled IDs via the SECTION 2 `copy_to(con, tibble(ID=...), name="tmp_surv_a3_ids", temporary=TRUE, overwrite=TRUE)` pattern. Semi-join LAB_RESULT_CM to it and select `ID, LAB_LOINC, LAB_PX, LAB_PX_TYPE, RAW_LAB_CODE, RAW_LAB_NAME, RESULT_UNIT` (via `any_of`, only those that exist) plus the raw date columns, `distinct()`, `collect()`. Parse dates with `pick_date()`, then keep only rows on the sampled ID x dates (semi_join in R).
    - `master_codes <- normalize_surv_code(read MASTER LOINC column from lab_code_crosswalk.xlsx)`; `excluded <- readxl::read_excel(codeset_path, "Lab_Analytes_Excluded")`.
    - `a3_block2 <- rank_candidate_codes(candidate_rows, sampled_id_dates, analytes, excluded, master_codes)`.
    - If any collected candidate row has a blank/NA LAB_LOINC (missing code coded only in RAW_LAB_CODE), set a flag `a3_blank_loinc <- TRUE` and add a QC/console note: "A3: candidate(s) present only in RAW_LAB_CODE — adding needs a new Lab_Analytes match column; flag at checkpoint 1 (do not improvise)." (from CONTEXT specifics / brief pitfall).

    Codeset_summary (IMP-05):
    - `codeset_summary <- build_codeset_summary(codeset, analytes, A2_analyte_presence)`. Prepare a single sheet-ready frame: block 1 rows, a spacer/label, block 2 rows (160-02 returns list; stack with a `block` label column or write both under one sheet with a header row like the QC near-miss block).

    SECTION 9 wiring:
    - Add sheets to the `sheets` list in this exact order: KEY, A_code_presence, A2_analyte_presence, **A3_missing_analyte**, B_modality_primary, C_modality_with_sensitivity, D_pre_vs_post_anchor, E_patient_modality_dates (internal only), QC, **Codeset_summary**.
    - A3 sheet content: block 1 table then block 2 table (either two sheet regions like the QC near-miss block, or a `block` column). Print the per-rule sample size on the sheet (D-03).
    - Release suppression: for the release workbook (`release = TRUE`) suppress A3 counts via `suppress_small()` on `n_id_dates`/`n_near_miss_id_dates` AND DROP the `RAW_LAB_CODE`, `RAW_LAB_NAME` columns from block 2 (L-3 — LOINC-level only). INTERNAL keeps raw columns and unsuppressed counts. Codeset_summary appears in BOTH workbooks; suppress its `n_codes`/`n_present` counts only if <= threshold per the existing suppress convention (counts of codes are structural, not patient counts — leave unsuppressed; only patient-derived counts suppress — confirm none of Codeset_summary's columns are patient counts, so no suppression needed).
    - Add a KEY row explaining A3 (report-only, L-2) and a KEY row for eligibility using the CONTEXT D-04 wording verbatim: "all-patient columns measure any breast imaging across the full cohort; female columns measure screening-population uptake (guideline-style)", stating the eligible female N.
  </action>
  <verify>
    <automated>grep -n "A3_missing_analyte\|Codeset_summary\|tmp_surv_a3_ids\|A3_SEED\|summarise_missing_analyte\|rank_candidate_codes\|build_codeset_summary" R/147_surveillance_modality_frequency.R</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "A3_missing_analyte" R/147_surveillance_modality_frequency.R` matches and it appears in the sheets list between A2_analyte_presence and B_modality_primary
    - `grep -n "Codeset_summary" R/147_surveillance_modality_frequency.R` matches and it is the LAST sheet in the sheets list
    - `grep -n "A3_SEED <- 42" R/147_surveillance_modality_frequency.R` matches (fixed seed, D-03)
    - `grep -n "tmp_surv_a3_ids\|copy_to" R/147` shows a temp table restricting the LAB_RESULT_CM candidate query to sampled IDs before collect() (L-4)
    - A stopifnot ties A3 block 1's BMP total to an_rules$near_miss at n_listed-1 (success criterion 2)
    - Release path drops RAW_LAB_CODE/RAW_LAB_NAME from A3 block 2 and suppresses A3 counts (L-3); INTERNAL keeps them
    - A blank-LAB_LOINC console/QC note exists (pitfall / checkpoint-1 flag)
    - The eligibility KEY row contains the verbatim D-04 phrase "screening-population uptake"
  </acceptance_criteria>
  <done>A3 (both blocks, sampled query) and Codeset_summary are built and written in the correct sheet order with correct INTERNAL/release differences; A3 never changes any count (L-2).</done>
</task>

</tasks>

<verification>
- `grep -n` checks in both tasks match.
- Structural balance: run a brace/paren balance check on R/147 after edits (Rscript may be unavailable on Windows; if so, count parens/braces/brackets and record in SUMMARY, deferring true parse + run to 160-04's HiPerGator checkpoint — this is the established Phase 140+ pattern).
- No change to D sheet, patient_wide, or the all-patient columns of non-eligible modalities (L-5).
</verification>

<success_criteria>
- A3_missing_analyte and Codeset_summary sheets produced in both workbooks; sheet order correct.
- Mammogram/Breast MRI B/C keep all-patient columns and gain female + other-sex columns; KEY explains both (IMP-04, D-04).
- A3 block 2 query is DuckDB-restricted to sampled near-miss IDs; release workbook is LOINC-level only (L-3, L-4).
- A3 is report-only and changes no count (L-2).
</success_criteria>

<output>
After completion, create `.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-03-SUMMARY.md`.
</output>
