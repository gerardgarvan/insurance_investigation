---
phase: 160-surveillance-lab-accuracy-and-reporting-improvements
plan: 02
type: tdd
wave: 2
depends_on: ["160-01"]
files_modified:
  - R/utils/utils_surveillance.R
  - tests/testthat/test-160-diagnostic-and-eligibility.R
autonomous: true
requirements: [IMP-02, IMP-04, IMP-05]
must_haves:
  truths:
    - "summarise_missing_analyte() attributes each n_listed-1 near-miss ID x date to the one missing analyte, overall and by calendar year"
    - "rank_candidate_codes() ranks non-Lab_Analytes codes on sampled near-miss ID x dates by how many of those days they cover, and flags codes already in Lab_Analytes_Excluded or the crosswalk MASTER"
    - "compute_modality_stats() gains a female/eligible-denominator variant that divides by female patients and female person-years while leaving all-patient columns identical"
    - "male/unknown-sex patients with >=1 event surface in n_patients_other_sex / total_event_dates_other_sex"
    - "build_codeset_summary() produces a modality x tier x match block and an analyte block from the loaded codeset"
  artifacts:
    - path: "R/utils/utils_surveillance.R"
      provides: "summarise_missing_analyte, rank_candidate_codes, compute_eligible_modality_stats, build_codeset_summary"
      contains: "summarise_missing_analyte <- function"
    - path: "tests/testthat/test-160-diagnostic-and-eligibility.R"
      provides: "synthetic-fixture tests for all four functions"
      contains: "missing CO2"
  key_links:
    - from: "R/147 SECTION 8 (A3, B/C, Codeset_summary)"
      to: "the four new pure functions"
      via: "direct calls in 160-03"
      pattern: "summarise_missing_analyte|rank_candidate_codes|build_codeset_summary"
---

<objective>
Add the four pure functions Phase 160 needs, each with synthetic-fixture tests, so 160-03 can wire them into R/147 without inventing logic inline.

Purpose: IMP-02 (A3 block 1 + block 2 ranking), IMP-04 (eligible-denominator statistics), IMP-05 (codeset summary). Keeping these as tested pure functions in `utils_surveillance.R` matches the Phase 159 pattern (all matching logic lives there) and lets R/147 stay a thin wiring layer.
Output: four new functions in `utils_surveillance.R` and one testthat file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md
@160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md

<interfaces>
<!-- Existing shapes the new functions must interoperate with (R/utils/utils_surveillance.R). -->

map_analyte_hits() returns: tibble(ID, analyte_row_id, analyte, event_date [Date], type_ok [lgl], type_val).
  -> analyte-per-ID-per-date is: hits |> filter(type_ok) |> distinct(ID, event_date, analyte).

build_analyte_events() near_miss tibble columns:
  codeset_row_id, modality, match, n_listed (int), threshold (int),
  n_analytes_present (int), n_id_dates (int), qualifies (lgl).
  For a rule row, the n_listed-1 near-miss count is n_id_dates where n_analytes_present == n_listed - 1.

compute_modality_stats(events, followup, keys, tiers, by="modality") returns per-key:
  n_patients, dates_per_pt_median/q1/q3/max, total_event_dates,
  pct_of_denominator, person_years_denominator, events_per_person_year.
  It divides by denom_n = nrow(followup) and total_py = sum(followup$person_years).
  events are the windowed events (has columns ID, modality, tier, window, type_ok, event_date).

followup columns include: ID, hl_anchor_date, follow_end, person_years, in_confirmed_cohort.

Codeset (Analysis_Codeset) columns available: codeset_row_id, modality, submodality,
  code_system, code, code_norm, cdm_table, type_filter, match, tier, plausibility,
  min_analyte_count. Analyte rows have match in SURV_ANALYTE_MATCHES; code_norm is a ";"-joined analyte list.
Lab_Analytes columns: analyte_row_id, analyte, code_system, code, code_norm, cdm_table, type_filter.

surv_components(code_norm) splits a ";"-joined normalized code/analyte list into a character vector.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: summarise_missing_analyte() (A3 block 1) + rank_candidate_codes() (A3 block 2) with tests</name>
  <read_first>
    - R/utils/utils_surveillance.R (build_analyte_events() lines 559-603 for near_miss shape; map_analyte_hits() lines 536-550 for hits shape; surv_components() line 44)
    - .planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md (D-02 depth = exactly n_listed-1; D-03 sample = up to 5000, fixed seed; L-2/L-3 rules)
    - 160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md (IMP-02 block 1 and block 2 exact column lists and ranking spec; "Near-miss denominator" pitfall: count ID x dates, not rows)
  </read_first>
  <behavior>
    - summarise_missing_analyte Test 1: given hits for a BMP rule listing 8 analytes where a set of ID x dates each have exactly 7 of 8 present (missing CO2), the result attributes every such day to "CO2" and its total equals the number of near-miss ID x dates. A day present with all 8 does NOT appear.
    - summarise_missing_analyte Test 2: the by-year breakdown splits those days by calendar year of event_date and the year totals sum to the overall total.
    - summarise_missing_analyte Test 3: only rule rows with n_listed >= 2 are processed (a 1-analyte rule like KIDNEY primary yields no block-1 rows).
    - rank_candidate_codes Test 1: given collected candidate rows (ID, event_date, LAB_LOINC, ...) on sampled near-miss ID x dates, a code present on every sampled near-miss day ranks first with coverage share = 1.0.
    - rank_candidate_codes Test 2: a candidate whose normalized code is already in Lab_Analytes is dropped (not a candidate). A candidate whose code is in Lab_Analytes_Excluded is KEPT but flagged with its exclusion reason. A candidate in the crosswalk MASTER is flagged as such.
  </behavior>
  <action>
    Add two functions to `R/utils/utils_surveillance.R` (in the Phase 159 section, after build_analyte_events()).

    `summarise_missing_analyte(hits, codeset)` — A3 block 1 (D-02, exactly n_listed-1):
    - For each Analysis_Codeset rule row (`match %in% SURV_ANALYTE_MATCHES`) whose listed-analyte count `n_listed = length(surv_components(code_norm))` is >= 2:
      - Build the per-ID-per-date analyte set from `hits |> filter(type_ok, !is.na(event_date)) |> distinct(ID, event_date, analyte)`, restricted to the row's listed analytes.
      - Keep ID x dates where exactly `n_listed - 1` distinct listed analytes are present. For each such day, the missing analyte is the single listed analyte NOT present that day.
      - Emit rows: `codeset_row_id, modality, match, n_listed, missing_analyte, calendar_year (int, from event_date), n_id_dates`. Aggregate at ID x date grain (count distinct ID x date, NOT raw rows — the "Near-miss denominator" pitfall).
    - Return a long tibble with an "overall" year value too — implement as one tibble carrying `calendar_year` per row PLUS a companion overall aggregate, OR return a list(by_year=, overall=). Pick the list form and document it in roxygen; 160-03 writes both.
    - Return empty (0-row) tibbles when no rule row qualifies.

    `rank_candidate_codes(candidate_rows, sampled_id_dates, analytes, excluded, master_codes)` — A3 block 2 (IMP-02, L-3):
    - `candidate_rows`: collected LAB_RESULT_CM rows on sampled near-miss patients — columns ID, event_date (Date), LAB_LOINC, LAB_PX, LAB_PX_TYPE, RAW_LAB_CODE, RAW_LAB_NAME, RESULT_UNIT (only those present).
    - `sampled_id_dates`: tibble(ID, event_date) — the sampled near-miss days for the rule row.
    - Keep only candidate rows on a sampled ID x date (semi_join on ID + event_date).
    - Normalize each candidate code (use LAB_LOINC when present, else RAW_LAB_CODE) via `normalize_surv_code()`. DROP any candidate whose normalized code maps to a `Lab_Analytes` code (`analytes$code_norm`) — those already count.
    - For remaining candidates, group by the candidate code identity and compute `n_near_miss_id_dates` = distinct sampled ID x dates it appears on, and `coverage_share` = that count / `nrow(distinct(sampled_id_dates))`. Rank descending by `n_near_miss_id_dates`.
    - Flag `in_excluded` (TRUE + `excluded_reason` from the `Lab_Analytes_Excluded` sheet keyed by normalized code) and `in_master` (code present in `master_codes`).
    - Return columns: `LAB_LOINC, LAB_PX, LAB_PX_TYPE, RAW_LAB_CODE, RAW_LAB_NAME, RESULT_UNIT, code_norm, n_near_miss_id_dates, coverage_share, in_excluded, excluded_reason, in_master` — include only the raw/text columns that exist in the input (L-3: 160-03 drops RAW_* for the release workbook).

    Add both cases to `tests/testthat/test-160-diagnostic-and-eligibility.R` with synthetic fixtures (small in-memory tibbles; no file/DB I/O). The BMP-missing-CO2 fixture is the anchor case named in success criterion 2.
  </action>
  <verify>
    <automated>Rscript -e 'library(testthat); source("R/utils/utils_surveillance.R"); test_file("tests/testthat/test-160-diagnostic-and-eligibility.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "summarise_missing_analyte <- function" R/utils/utils_surveillance.R` matches
    - `grep -n "rank_candidate_codes <- function" R/utils/utils_surveillance.R` matches
    - The CO2 fixture test asserts the missing analyte is "CO2" and the total equals the near-miss ID x date count
    - rank_candidate_codes drops already-Lab_Analytes codes and KEEPS + flags Lab_Analytes_Excluded codes with reason
    - coverage_share for a code present on every sampled day == 1
    - All Task 1 tests pass with 0 failures
  </acceptance_criteria>
  <done>Both A3 functions exist, aggregate at ID x date grain, apply L-2/L-3 flags, and pass synthetic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: eligible-denominator stats + build_codeset_summary() with tests</name>
  <read_first>
    - R/utils/utils_surveillance.R (compute_modality_stats() lines 448-476 — the exact stat columns and denom_n/total_py divisors to mirror)
    - .planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md (D-04/D-05 eligible_sex; L-5 add-nothing-removed; KEY wording)
    - 160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md (IMP-04 exact new column names: n_patients_eligible, pct_of_eligible, person_years_eligible, events_per_person_year_eligible, n_patients_other_sex, total_event_dates_other_sex; IMP-05 two-block Codeset_summary spec)
  </read_first>
  <behavior>
    - compute_eligible_modality_stats Test 1 (pct_of_eligible): for a modality with a female eligible subset, pct_of_eligible = 100 * (female patients with >=1 event) / (female denominator N), and events_per_person_year_eligible = female event dates / female person-years — NOT the all-patient divisors.
    - compute_eligible_modality_stats Test 2 (all-patient identical): the all-patient columns returned equal compute_modality_stats() run on the same inputs with no eligibility (L-5).
    - compute_eligible_modality_stats Test 3 (other-sex visible): a male mammogram event contributes to the all-patient count AND to n_patients_other_sex / total_event_dates_other_sex, and is EXCLUDED from the female columns.
    - build_codeset_summary Test 1: block 1 has one row per modality x tier x match with n_codes, a code list, and threshold; block 2 has one row per analyte with n_codes and n_present (from an A2-like presence input).
  </behavior>
  <action>
    Add two functions to `R/utils/utils_surveillance.R`.

    `compute_eligible_modality_stats(events, followup, keys, tiers, sex_lookup, eligible_sex, by = "modality")`:
    - `sex_lookup`: tibble(ID, sex) where sex in {"F","M", other/unknown}, from DEMOGRAPHIC.SEX.
    - `eligible_sex`: "" (no extra view), "F", or "M" — the target denominator sex for these keys.
    - ALWAYS compute the all-patient stats by delegating to `compute_modality_stats(events, followup, keys, tiers, by)` so the all-patient columns are byte-identical to a no-eligibility run (L-5).
    - When `eligible_sex != ""`: build the eligible subset `followup_elig = followup |> semi_join(sex_lookup[sex == eligible_sex])` and its events, then compute `n_patients_eligible`, `pct_of_eligible = 100 * n_patients_eligible / nrow(followup_elig)`, `person_years_eligible = sum(followup_elig$person_years)`, `events_per_person_year_eligible = eligible_event_dates / person_years_eligible`. Also compute over the NON-eligible sexes (everyone in followup whose sex != eligible_sex): `n_patients_other_sex` (patients with >=1 post event) and `total_event_dates_other_sex`. Join these as extra columns onto the all-patient result, per key.
    - When `eligible_sex == ""`: return the all-patient result unchanged (the six extra columns are absent for that key).
    - The eligible event-date counting must reuse the same grain/filter as compute_modality_stats: `filter(type_ok, tier %in% tiers, window == "post") |> distinct(ID, <by>, event_date)`.

    `build_codeset_summary(codeset, analytes, analyte_presence)` — IMP-05:
    - Block 1 (`by = "modality_tier_match"`): group Analysis_Codeset by modality, tier, match; per group emit `n_codes` (distinct code/code_norm), `code_list` (";"-joined), and `threshold` (min_analyte_count for analyte_min_same_day rows, else "").
    - Block 2 (`analyte`): one row per distinct analyte in `analytes` with `n_codes` (distinct code_norm) and `n_present` (from `analyte_presence` — the A2 table — count of that analyte's rows with present == TRUE, keyed by analyte_row_id then rolled to analyte).
    - Return `list(by_modality = <block1>, by_analyte = <block2>)`; 160-03 stacks/labels them onto one sheet.

    Add all cases to `tests/testthat/test-160-diagnostic-and-eligibility.R` with synthetic fixtures.
  </action>
  <verify>
    <automated>Rscript -e 'library(testthat); source("R/utils/utils_surveillance.R"); test_file("tests/testthat/test-160-diagnostic-and-eligibility.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "compute_eligible_modality_stats <- function" R/utils/utils_surveillance.R` matches
    - `grep -n "build_codeset_summary <- function" R/utils/utils_surveillance.R` matches
    - The all-patient-identical test proves the all-patient columns equal a plain compute_modality_stats() run
    - The male-mammogram test proves the event is in the all-patient count AND n_patients_other_sex, NOT the female columns
    - Output column names exactly: n_patients_eligible, pct_of_eligible, person_years_eligible, events_per_person_year_eligible, n_patients_other_sex, total_event_dates_other_sex
    - build_codeset_summary returns list(by_modality=, by_analyte=) with the specified columns
    - All Task 2 tests pass with 0 failures
  </acceptance_criteria>
  <done>Eligibility stats add female + other-sex columns without changing all-patient columns; codeset summary builds both blocks; tests pass.</done>
</task>

</tasks>

<verification>
- `Rscript -e 'source("R/utils/utils_surveillance.R"); testthat::test_file("tests/testthat/test-160-diagnostic-and-eligibility.R")'` reports 0 failures across all six behavior cases.
- Phase 159 tests still pass (no regression to shared functions).
</verification>

<success_criteria>
- `summarise_missing_analyte`, `rank_candidate_codes`, `compute_eligible_modality_stats`, `build_codeset_summary` exist in utils_surveillance.R.
- The CO2-attribution, coverage-ranking, excluded-flag, pct_of_eligible, all-patient-identical, and male-mammogram cases all pass (brief success criteria 2, 3, 5; IMP-05).
</success_criteria>

<output>
After completion, create `.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-02-SUMMARY.md`.
</output>
