---
phase: 160-surveillance-lab-accuracy-and-reporting-improvements
plan: 04
type: execute
wave: 4
depends_on: [160-01, 160-02, 160-03]
files_modified:
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
  - data/reference/README.md
  - data/reference/surveillance_codeset.xlsx
autonomous: false
requirements: [IMP-03, IMP-06]
must_haves:
  truths:
    - "R/88 has a Phase 160 block in the Section 15ak style (check helper + footer, rolled into SECTION 16 totals) with invariant checks only"
    - "SCRIPT_INDEX and README describe A3, Codeset_summary, eligible_sex and the release rules"
    - "Checkpoint 1 reviews A3 on a real run; Checkpoint 2 adds confirmed code(s) to Lab_Analytes, re-runs and records before/after figures"
---

<objective>
Register and validate Phase 160, then resolve the missing-analyte gap with the team.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md
@.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-03-SUMMARY.md
@R/SCRIPT_INDEX.md
@data/reference/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: R/88, SCRIPT_INDEX, README</name>
  <read_first>R/88_smoke_test_comprehensive.R — Section 15ak (Phase 159) and SECTION 16 SUMMARY; R/SCRIPT_INDEX.md — R/147 row</read_first>
  <files>R/88_smoke_test_comprehensive.R, R/SCRIPT_INDEX.md, data/reference/README.md</files>
  <action>
**R/88:** add a Phase 160 block after 15ak, before SECTION 16, using the same helper, counter and footer pattern (`SMOKE-160-01`), rolled into the SECTION 16 totals. Checks:
1. Functions defined in utils_surveillance.R: `modality_eligible_sex`, `summarise_missing_analyte`, `select_a3_sample`, `surv_sql_date_expr`, `rank_candidate_codes`, `compute_eligible_modality_stats`, `suppress_eligible_columns`, `build_codeset_summary`.
2. R/147 references `A3_missing_analyte`, `Codeset_summary`, `tmp_surv_a3_days`, `A3_SEED`, `suppress_eligible_columns` and `L-5: eligibility`.
3. `load_modality_lookup(codeset = load_surveillance_codeset())` loads, and `modality_eligible_sex()` values are all in {"", "F", "M"}.
4. The CMP `analyte_min_same_day` `min_analyte_count` is > 8, so a full BMP cannot qualify (an invariant, not the literal 11 — D-13).
5. At least 2 `tests/testthat/test-160-*.R` files exist.

No literal row counts.

**SCRIPT_INDEX:** in the R/147 row, add the sheets A3_missing_analyte and Codeset_summary, the female-denominator columns, and requirements IMP-01..IMP-06.

**README:**
- A3: INTERNAL has raw codes/names and unsuppressed counts; release is LOINC-level and suppressed.
- Codeset_summary: generated each run; replaces `code_mapping_summary.xlsx`.
- Complementary suppression of the eligible / other-sex pair.
  </action>
  <verify>
    <automated>Rscript -e "b <- readLines('R/88_smoke_test_comprehensive.R'); stopifnot(any(grepl('SMOKE-160-01', b)), any(grepl('suppress_eligible_columns', b)), !any(grepl('== *.11.', b))); i <- readLines('R/SCRIPT_INDEX.md'); stopifnot(any(grepl('A3_missing_analyte', i))); r <- readLines('data/reference/README.md'); stopifnot(any(grepl('Codeset_summary', r))); message('OK')"</automated>
  </verify>
  <done>R/88, SCRIPT_INDEX and README updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint 1 — HiPerGator run and A3 review</name>
  <how-to-verify>
    On HiPerGator, keeping the 2026-09-25 INTERNAL workbook as the before-baseline:
    1. `Rscript -e "testthat::test_dir('tests/testthat', filter = '15[89]|160', stop_on_failure = TRUE)"` — 216 expectations pass.
    2. Run R/88: SMOKE-160-01 passes and no earlier section regresses.
    3. Run R/147. All stops pass, including the A3 reconciliation and the L-5 guard. If the log warns that under 50% of sampled days returned lab rows, the LAB_RESULT_CM date columns are in a format `surv_sql_date_expr()` does not cover. Report the raw format; it is a one-line fix.
    4. INTERNAL workbook:
       - **A3 block 1:** BMP 7-of-8 days by missing analyte. Expected mostly CO2.
       - **Block 1b:** whether the gap sits in particular years.
       - **Block 2:** for BMP × CO2, the top-lift candidates. Expected 57922-7, or a local code. Note `code_source`, `top_raw_name` and `in_excluded`. If the top candidate is RAW-only (the QC flag is 1), adding it needs a new Lab_Analytes match column — report that; do not improvise.
    5. B/C: Mammogram and Breast MRI all-patient columns match the 2026-09-25 run, with female and other-sex columns beside them. The release copy withholds the pair where either side is 1–10.
  </how-to-verify>
  <resume-signal>Report the confirmed code(s) and whether a new match column is needed, or "sample too small — raise A3_MAX_DAYS to N".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Checkpoint 2 — add confirmed code(s), re-run, record before/after</name>
  <how-to-verify>
    1. Edit `data/reference/surveillance_codeset.xlsx`:
       - If the confirmed code is in `Lab_Analytes_Excluded` (expected: 57922-7 under CO2), remove that row there and add it to `Lab_Analytes` under CO2 with the next free `analyte_row_id` (LA190), matching the CO2 rows' code_system, cdm_table and type_filter, with `source` noting "added Phase 160 from A3".
       - If a new match column is needed, stop and record it as a Phase 161 item instead.
    2. Re-run R/147 and R/88.
    3. Record before → after in the SUMMARY:
       - SC125 near-miss at 7 of 8
       - SC131 near-miss at 13 of 14
       - BMP and CMP primary total_event_dates and n_patients (B sheet)
       - CO2 serum patient-dates (A2)

       Expected: the near-miss counts fall and the primary counts rise.
  </how-to-verify>
  <resume-signal>"re-run complete" with the before/after figures, or "blocked — new match column needed".</resume-signal>
</task>

</tasks>

<output>
After completion, create `.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-04-SUMMARY.md`
</output>
