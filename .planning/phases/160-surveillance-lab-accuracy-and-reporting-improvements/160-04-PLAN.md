---
phase: 160-surveillance-lab-accuracy-and-reporting-improvements
plan: 04
type: execute
wave: 4
depends_on: ["160-03"]
files_modified:
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
  - data/reference/README.md
  - data/reference/surveillance_codeset.xlsx
autonomous: false
requirements: [IMP-03, IMP-06]
user_setup:
  - service: HiPerGator
    why: "R/147 runs against real LAB_RESULT_CM / DEMOGRAPHIC on HiPerGator; Windows cannot execute the pipeline"
    dashboard_config:
      - task: "Run R/147 (and R/88) via RStudio/SLURM on HiPerGator; keep the 2026-09-25 INTERNAL workbook as the before-baseline"
        location: "UF HiPerGator RStudio"
must_haves:
  truths:
    - "R/88 gains a Phase 160 block that checks A3_missing_analyte and Codeset_summary are produced, eligible_sex values are valid, and uses no literal patient-count assertions"
    - "SCRIPT_INDEX and data/reference/README.md reflect the new sheets and the two-workbook A3 suppression"
    - "The team reviews A3 on a real run (Checkpoint 1), confirms the missing code(s), adds them to Lab_Analytes (Checkpoint 2), and re-runs"
    - "BMP/CMP primary and near-miss before/after figures are recorded in the SUMMARY"
  artifacts:
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Phase 160 structural checks"
      contains: "A3_missing_analyte"
    - path: "data/reference/README.md"
      provides: "updated sheet + suppression docs"
      contains: "A3_missing_analyte"
  key_links:
    - from: "R/88"
      to: "R/147 output structure"
      via: "grep-style structural checks, no literal row counts"
      pattern: "A3_missing_analyte|Codeset_summary|eligible_sex"
---

<objective>
Register and validate the Phase 160 changes, then run the two team checkpoints that resolve the missing-analyte gap.

Purpose: IMP-06 (tests already added in 160-01/160-02; here extend R/88 + update SCRIPT_INDEX + README) and IMP-03 (team reviews A3, adds the confirmed code(s) to Lab_Analytes, re-runs, records before/after). This is the phase's definition-of-done: outputs issued from a HiPerGator run.
Output: extended R/88 section, updated docs, an amended codeset (post-checkpoint), and a SUMMARY with before/after figures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md
@160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md

<interfaces>
<!-- R/88 existing Phase 159 section: Section 15ak (line ~5542-5622), with check_159()/p159_pass/p159_fail and a footer message. Add Phase 160 checks in the SAME style: a check_160() counter + footer, added after 15ak and before SECTION 16 SUMMARY (line ~5624). -->
<!-- R/88 rule: structural checks only (grep on read_or_null(file)); NEVER assert literal patient/row counts (IMP-06: "no literal row counts"; matches the honest-tests pattern from Phase 134). Value checks on the codeset file (a specific threshold string, valid eligible_sex enum) are allowed — they are not row counts. -->
SCRIPT_INDEX.md: R/147 already listed (Phase 158/159). Update its description/output list to mention A3_missing_analyte + Codeset_summary sheets.
data/reference/README.md: documents the two-workbook release pattern + surveillance_codeset sheets. Update for eligible_sex, A3 (INTERNAL raw / release LOINC-level), Codeset_summary.
Lab_Analytes add procedure (IMP-03): a code in Lab_Analytes_Excluded is REMOVED from that sheet and ADDED to Lab_Analytes with the next free LA id (analyte_row_id). First suspect: LOINC 57922-7 (serum/plasma CO2 by calculation) for the CO2 analyte.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend R/88 + update SCRIPT_INDEX and README</name>
  <files>R/88_smoke_test_comprehensive.R, R/SCRIPT_INDEX.md, data/reference/README.md</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (Section 15ak lines 5542-5622 — copy its check_159/footer style; SECTION 16 SUMMARY at line 5624)
    - R/SCRIPT_INDEX.md (find the R/147 row)
    - data/reference/README.md (two-workbook + codeset sheet documentation)
    - 160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md (IMP-06 exact check list)
  </read_first>
  <action>
    1. In `R/88_smoke_test_comprehensive.R`, add a Phase 160 block (new `check_160()` closure + `p160_pass`/`p160_fail` counters + footer message, placed after Section 15ak's footer and before SECTION 16 SUMMARY). All source checks are structural (grep on `read_or_null("R/147_surveillance_modality_frequency.R")` and utils_surveillance.R); NO literal patient/row-count assertions. Checks:
       - `A3_missing_analyte` referenced in R/147
       - `Codeset_summary` referenced in R/147
       - `summarise_missing_analyte`, `rank_candidate_codes`, `compute_eligible_modality_stats`, `build_codeset_summary` each defined in utils_surveillance.R
       - `eligible_sex` column present in the Modalities sheet AND every value in {"", "F", "M"} (read the xlsx via readxl and validate the enum — a value check, not a row count)
       - CMP `min_analyte_count == "11"` in Analysis_Codeset (value check)
       - `tmp_surv_a3_ids` and/or `copy_to` for the A3 sampled-ID query present in R/147 (L-4)
       - `A3_SEED` present in R/147 (fixed seed, D-03)
       - both `tests/testthat/test-160-*.R` files exist (>= 2)
       Add a `SMOKE-160-01` footer summarizing pass/fail, mirroring the 15ak footer, and ensure p160 pass/fail roll into the global `passed`/`failed` totals used by SECTION 16.
    2. Update the R/147 row in `R/SCRIPT_INDEX.md` to note the new A3_missing_analyte and Codeset_summary sheets.
    3. Update `data/reference/README.md`: document the `eligible_sex` Modalities column (blank/F/M; F on Mammogram + Breast MRI); the A3_missing_analyte sheet (INTERNAL shows RAW_LAB_CODE/RAW_LAB_NAME + unsuppressed counts; release is LOINC-level + suppressed, L-3); and the Codeset_summary sheet (generated every run; replaces the hand-built code_mapping_summary.xlsx).
  </action>
  <verify>
    <automated>grep -n "check_160\|SMOKE-160-01\|A3_missing_analyte\|Codeset_summary\|eligible_sex" R/88_smoke_test_comprehensive.R && grep -n "A3_missing_analyte\|Codeset_summary\|eligible_sex" data/reference/README.md && grep -n "A3\|Codeset_summary" R/SCRIPT_INDEX.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "check_160" R/88_smoke_test_comprehensive.R` matches and the block sits before SECTION 16 SUMMARY
    - `grep -n "SMOKE-160-01" R/88_smoke_test_comprehensive.R` matches
    - R/88 has NO literal patient/row-count assertion in the Phase 160 block (only structural greps + codeset enum/threshold value checks)
    - README.md contains `A3_missing_analyte`, `Codeset_summary`, and `eligible_sex`
    - SCRIPT_INDEX.md R/147 row mentions the two new sheets
  </acceptance_criteria>
  <done>R/88 Phase 160 block, SCRIPT_INDEX, and README all reflect the new sheets, eligibility column, and suppression rules; no literal row counts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint 1 — HiPerGator run and A3 review</name>
  <files>output/surveillance_modality_frequency_INTERNAL_&lt;date&gt;.xlsx</files>
  <what-built>
    R/147 now produces A3_missing_analyte (block 1 missing-analyte-by-year + block 2 sampled candidate LOINC codes), female-denominator columns on Mammogram/Breast MRI in B/C, and a Codeset_summary sheet, all with two-workbook suppression. R/88 has a Phase 160 structural block.
  </what-built>
  <action>
    Pause for the team to run the pipeline on HiPerGator and review A3. Claude cannot execute this step (HiPerGator-only). The team runs R/147 and R/88, opens the INTERNAL A3_missing_analyte sheet, and reports the confirmed missing code(s), whether a new Lab_Analytes match column is needed, and whether the 5,000-row sample was sufficient — following the how-to-verify steps.
  </action>
  <how-to-verify>
    On HiPerGator (Windows cannot run the pipeline):
    1. Keep the 2026-09-25 INTERNAL workbook as the before-baseline (do not overwrite it).
    2. Run `R/147_surveillance_modality_frequency.R`. Confirm it completes and writes both workbooks + the per-patient rds/csv.
    3. Run `R/88_smoke_test_comprehensive.R`; confirm the SMOKE-160-01 block passes (and no prior section regressed).
    4. Open the INTERNAL A3_missing_analyte sheet. In block 1, confirm the BMP 7-of-8 near-miss total matches the QC near-miss count at n_analytes_present = 7 for SC125. In block 2, confirm the top-ranked candidate code(s) for the BMP rule and whether the missing analyte is CO2. Check the sample size printed on the sheet.
    5. If block 2 shows the candidate is present only in RAW_LAB_CODE (blank LAB_LOINC), note it — that means adding it needs a NEW Lab_Analytes match column (a code change), which must be flagged now, not improvised (CONTEXT specifics / brief pitfall).
    6. Confirm Mammogram/Breast MRI in B/C show all-patient columns unchanged PLUS the female + other-sex columns, and that KEY explains both.

    Report back: the confirmed missing code(s) to add (LOINC/other), whether a new match column is needed, and whether the 5,000 sample was sufficient (if not, the sample size can be raised and R/147 re-run — no redesign).
  </how-to-verify>
  <verify>Team confirms R/147 + R/88 ran on HiPerGator and reports the A3 review outcome (confirmed code(s), any new-match-column need, sample sufficiency).</verify>
  <acceptance_criteria>
    - A real HiPerGator run of R/147 completed and wrote both workbooks
    - R/88 SMOKE-160-01 block passed with no prior-section regression
    - A3 block 1 BMP 7-of-8 total reconciles with the QC near-miss at n_analytes_present = 7 for SC125
    - The confirmed missing code(s) and any new-match-column requirement are reported back
  </acceptance_criteria>
  <done>A real HiPerGator run of R/147 + R/88 is reviewed; the missing code(s) to add are identified and reported (or a larger sample is requested and re-run).</done>
  <resume-signal>Provide the confirmed code(s) to add to Lab_Analytes (and note if a new match column is required), or type "sample too small — raise to N and re-run".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Checkpoint 2 — add confirmed code(s) to Lab_Analytes, re-run, record before/after</name>
  <files>data/reference/surveillance_codeset.xlsx</files>
  <read_first>
    - data/reference/surveillance_codeset.xlsx (Lab_Analytes + Lab_Analytes_Excluded sheets — find the next free LA analyte_row_id and the excluded CO2 row, e.g. LOINC 57922-7)
    - 160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md (IMP-03 procedure + success criterion 4: near-miss falls, BMP/CMP primary rises)
  </read_first>
  <action>
    Using the code(s) confirmed at Checkpoint 1, amend `data/reference/surveillance_codeset.xlsx` (via openxlsx/writexl, preserving all sheets):
    - If the confirmed code is currently in `Lab_Analytes_Excluded` (expected: serum/plasma CO2 by calculation, LOINC 57922-7 — its excluded reason "calculated from blood gas" does not fit a serum/plasma specimen): REMOVE that row from `Lab_Analytes_Excluded` and ADD it to `Lab_Analytes` under the CO2 analyte with the next free `LA` analyte_row_id, matching the CO2 rows' code_system/cdm_table/type_filter.
    - If Checkpoint 1 determined a NEW match column is required (code only in RAW_LAB_CODE), STOP and surface that as a separate follow-up — do not improvise a schema change in this plan.
    - Re-run R/147 + R/88 on HiPerGator.
    - Compare against the 2026-09-25 baseline: SC125 7-of-8 near-miss count (should fall substantially), CMP 13-of-14 near-miss count (should fall), and BMP + CMP primary patient-days (should rise). Record the before/after figures.
  </action>
  <verify>
    <automated>Rscript -e 'library(readxl); la <- read_excel("data/reference/surveillance_codeset.xlsx","Lab_Analytes",col_types="text"); cat("Lab_Analytes CO2 rows:", sum(toupper(la$analyte)=="CO2"), "\n")'</automated>
  </verify>
  <acceptance_criteria>
    - The confirmed CO2 (or other) code appears in `Lab_Analytes` under the CO2 analyte with a fresh LA id and is no longer in `Lab_Analytes_Excluded` (if it was there)
    - R/147 re-runs cleanly on HiPerGator and re-issues both workbooks
    - The SUMMARY records before/after: SC125 7-of-8 near-miss, CMP 13-of-14 near-miss, BMP primary patient-days, CMP primary patient-days
    - If a new match column was needed, that is recorded as a Phase 161 follow-up rather than improvised
  </acceptance_criteria>
  <done>The confirmed code(s) are added to Lab_Analytes, R/147 is re-run, and before/after BMP/CMP primary + near-miss figures are recorded in the SUMMARY (IMP-03).</done>
  <resume-signal>Type "re-run complete" with the before/after figures, or "blocked — new match column needed" to defer.</resume-signal>
</task>

</tasks>

<verification>
- R/88 Phase 160 block passes on HiPerGator; no prior section regresses.
- A3 block 1 BMP total reconciles with the QC near-miss at n=7 for SC125 (success criterion 2).
- After IMP-03: near-miss counts fall and BMP/CMP primary rise; figures in SUMMARY (success criterion 4).
- Codeset_summary present in both workbooks and matches loaded codeset row counts (success criterion 6).
- All earlier Phase 158/159 stops still pass; outputs issued from a HiPerGator run (success criterion 7).
</verification>

<success_criteria>
- R/88 validates A3 + Codeset_summary + valid eligible_sex with no literal row counts (IMP-06).
- SCRIPT_INDEX + README updated (IMP-06).
- Team-confirmed code(s) added to Lab_Analytes; re-run before/after figures recorded (IMP-03).
</success_criteria>

<output>
After completion, create `.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-04-SUMMARY.md`.
</output>
