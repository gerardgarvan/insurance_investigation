---
phase: 160-surveillance-lab-accuracy-and-reporting-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/reference/surveillance_codeset.xlsx
  - R/utils/utils_surveillance.R
  - tests/testthat/test-160-loader-eligible-sex.R
autonomous: true
requirements: [IMP-01, IMP-04]
must_haves:
  truths:
    - "The CMP analyte_min_same_day rule requires 11 of 14 analytes, so a plain BMP (8) no longer qualifies as CMP sensitivity"
    - "The Modalities sheet carries an eligible_sex column set to F for Mammogram and Breast MRI"
    - "load_modality_lookup() returns the same named prefix vector as before AND exposes eligible_sex per modality"
    - "A codeset file with no eligible_sex column still loads (blank = no extra view)"
    - "An eligible_sex value other than blank/F/M stops the load"
  artifacts:
    - path: "data/reference/surveillance_codeset.xlsx"
      provides: "CMP min_analyte_count = 11; Modalities.eligible_sex column"
      contains: "eligible_sex"
    - path: "R/utils/utils_surveillance.R"
      provides: "load_modality_lookup() eligible_sex support + companion accessor"
      contains: "eligible_sex"
    - path: "tests/testthat/test-160-loader-eligible-sex.R"
      provides: "loader validation tests"
      contains: "eligible_sex"
  key_links:
    - from: "R/147 (SECTION 1)"
      to: "load_modality_lookup()"
      via: "named-vector callers unchanged; eligible_sex read via attribute or companion"
      pattern: "load_modality_lookup"
---

<objective>
Make the two codeset-driven changes for Phase 160 and teach the loader about the new `eligible_sex` column, without breaking any Phase 158/159 caller.

Purpose: IMP-01 (CMP threshold 7 -> 11 so a full BMP no longer scores as a CMP sensitivity event) and IMP-04's data half (an `eligible_sex` column on the `Modalities` sheet that the loader validates and exposes). Both are prerequisites: 160-02's `compute_modality_stats` eligibility work and 160-03's R/147 wiring both read `eligible_sex`, and 160-03 reads the changed threshold from the codeset.
Output: an edited `surveillance_codeset.xlsx`, an extended `load_modality_lookup()`, and a new testthat file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md
@160-SURVEILLANCE-IMPROVEMENTS-BRIEF.md
@.planning/phases/159-lab-surveillance-modalities-and-per-patient-date-counts/159-CONTEXT.md

<interfaces>
<!-- Current load_modality_lookup() contract (R/utils/utils_surveillance.R:212). -->
<!-- It reads the Modalities sheet, validates modality/column_prefix/display_order, and returns a NAMED CHARACTER VECTOR: names = modality, values = column_prefix, ordered by display_order. -->
<!-- Callers in R/147 use it as a lookup: mod_lookup[[m]] gives the prefix; names(mod_lookup) gives modalities in display order. These call sites MUST keep working unchanged. -->

Current return (R/147:54): `mod_lookup <- load_modality_lookup(codeset = codeset)` then `names(mod_lookup)`, `mod_lookup[[m]]`, `paste0("n_dates_", lookup[modality])`.

Codeset structure (159 D-19): Modalities sheet has 14 rows, columns `modality`, `column_prefix`, `display_order`. Modality names are exactly: Mammogram, Breast MRI, Echocardiogram, Stress test, ECG, MUGA, PFT, Thyroid function, CBC, BMP, CMP, LIPID, LFT, KIDNEY (display names) with prefixes mammo, breast_mri, echo, stress, ecg, muga, pft, thyroid, cbc, bmp, cmp, lipid, lft, kidney.

CMP rule row: `Analysis_Codeset` has an `analyte_min_same_day` row for CMP carrying `min_analyte_count` (text column). Current value 7; target value 11 (D-01).

Loader validator (159 D-20): `min_analyte_count` must be a whole number >= 1 and < the number of listed analytes on `analyte_min_same_day` rows. CMP lists 14 analytes, so 11 passes (11 < 14).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit the codeset xlsx — CMP threshold 11 and Modalities.eligible_sex</name>
  <read_first>
    - data/reference/surveillance_codeset.xlsx (inspect current Analysis_Codeset CMP row and Modalities sheet — use readxl in an R session or a Bash unzip+grep to confirm current values before editing)
    - .planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-CONTEXT.md (D-01 threshold value; D-04/D-05 eligible_sex values)
    - .planning/phases/159-lab-surveillance-modalities-and-per-patient-date-counts/159-CONTEXT.md (D-19 codeset structure; D-20 loader validation rules)
  </read_first>
  <action>
    Edit `data/reference/surveillance_codeset.xlsx` in place (this is an .xlsx — edit via an R script using `openxlsx`/`readxl`+`writexl`, or `openxlsx::loadWorkbook()` + `writeData()` to preserve other sheets; do NOT hand-author the binary). Two edits:

    1. On the `Analysis_Codeset` sheet, find the single row where `modality == "CMP"` AND `match == "analyte_min_same_day"` and set its `min_analyte_count` cell from `7` to `11` (per D-01: full BMP + >=3 of 6 liver/protein analytes; 11 < 14 listed so the D-20 validator still passes). Change ONLY this cell. Do not touch the BMP (4), LFT (2), LIPID (3 total so min 2), or KIDNEY (1) rows — D-06 keeps them unchanged.

    2. On the `Modalities` sheet, add a new text column named exactly `eligible_sex`. For every row leave it blank ("") EXCEPT: set it to `F` for the row where `modality == "Mammogram"` and the row where `modality == "Breast MRI"` (D-04/D-05). Confirm the exact display names by reading the sheet first — use whatever the sheet actually stores (e.g. "Mammogram", "Breast MRI").

    Preserve all existing sheets (KEY, Analysis_Codeset, Lab_Analytes, Lab_Analytes_Excluded, Modalities) and all other cells byte-for-values-identical. After writing, re-read the file and print the CMP row's min_analyte_count and the Modalities eligible_sex column to confirm.
  </action>
  <verify>
    <automated>Rscript -e 'library(readxl); cs <- read_excel("data/reference/surveillance_codeset.xlsx","Analysis_Codeset",col_types="text"); md <- read_excel("data/reference/surveillance_codeset.xlsx","Modalities",col_types="text"); stopifnot(cs$min_analyte_count[cs$modality=="CMP" & cs$match=="analyte_min_same_day"]=="11"); stopifnot("eligible_sex" %in% names(md)); stopifnot(setequal(md$modality[md$eligible_sex %in% "F"], c("Mammogram","Breast MRI"))); cat("OK\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `read_excel(...,"Analysis_Codeset")` shows exactly one CMP `analyte_min_same_day` row and its `min_analyte_count == "11"`
    - `read_excel(...,"Modalities")` has a column literally named `eligible_sex`
    - `eligible_sex == "F"` on exactly the Mammogram and Breast MRI rows; blank on all 12 others
    - BMP/LFT/LIPID/KIDNEY `min_analyte_count` values are unchanged (4/2/2/1 respectively where present)
    - All 5 sheets still present in the workbook
  </acceptance_criteria>
  <done>CMP threshold is 11; Modalities has eligible_sex with F on the two breast modalities only; all other cells unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend load_modality_lookup() to expose eligible_sex + tests</name>
  <read_first>
    - R/utils/utils_surveillance.R (lines 210-239: current load_modality_lookup(); note it returns setNames(md$column_prefix, md$modality))
    - tests/testthat/test-159-codeset-loader.R (existing loader test style + how fixtures build a temp xlsx)
    - .planning/phases/159-lab-surveillance-modalities-and-per-patient-date-counts/159-CONTEXT.md (D-20 loader-shape rule: a file without a new column still loads)
  </read_first>
  <behavior>
    - Test 1: a Modalities sheet WITHOUT an eligible_sex column loads without error; the returned named vector is unchanged (names = modalities, values = prefixes).
    - Test 2: a Modalities sheet WITH eligible_sex = "F" on Mammogram loads; the eligibility for Mammogram reads back as "F" and blank modalities read back as "" .
    - Test 3: eligible_sex = "X" (not blank/F/M) stops the load with an informative error.
    - Test 4: eligible_sex = "M" is accepted (valid value, even though the codeset only uses F today).
  </behavior>
  <action>
    In `R/utils/utils_surveillance.R`, modify `load_modality_lookup()` (starts line 212) so it ALSO surfaces `eligible_sex`, while keeping its return value the SAME named character vector `setNames(md$column_prefix, md$modality)` (R/147 line 54 and `build_patient_modality_dates()` depend on that exact shape).

    Implementation: after the existing validation block, if the `Modalities` sheet has no `eligible_sex` column, create it as all-"" (D-20: a file without the column still loads). Validate that every `eligible_sex` value is in `c("", "F", "M")`; on any other value call `stop("Modalities: eligible_sex must be blank, F, or M: ", <offending modalities>)`. Attach the per-modality eligibility to the returned vector as an attribute so callers get it without a second file read and without changing the vector's class/values:

    ```r
    lookup <- stats::setNames(md$column_prefix, md$modality)
    attr(lookup, "eligible_sex") <- stats::setNames(md$eligible_sex, md$modality)
    lookup
    ```

    Add a tiny exported companion accessor next to it so R/147 can read eligibility explicitly:

    ```r
    #' Per-modality eligible_sex ("" / "F" / "M") for a load_modality_lookup() result.
    modality_eligible_sex <- function(lookup) {
      es <- attr(lookup, "eligible_sex")
      if (is.null(es)) stats::setNames(rep("", length(lookup)), names(lookup)) else es
    }
    ```

    Create `tests/testthat/test-160-loader-eligible-sex.R` implementing the 4 behavior cases. Build temp .xlsx fixtures with `writexl::write_xlsx()` (or `openxlsx`) mirroring test-159-codeset-loader.R's fixture pattern: a minimal Modalities sheet with `modality`, `column_prefix`, `display_order`, and (for the positive cases) `eligible_sex`. Call `load_modality_lookup(path = <fixture>)` with `codeset = NULL` so the codeset-cross-check is skipped. Assert on `modality_eligible_sex(lookup)[["Mammogram"]]` and on `expect_error(..., "eligible_sex")` for the invalid case.
  </action>
  <verify>
    <automated>Rscript -e 'library(testthat); source("R/utils/utils_surveillance.R"); test_file("tests/testthat/test-160-loader-eligible-sex.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "eligible_sex" R/utils/utils_surveillance.R` shows the validation + attribute code inside load_modality_lookup()
    - `grep -n "modality_eligible_sex <- function" R/utils/utils_surveillance.R` matches
    - `load_modality_lookup()` return value is still `setNames(column_prefix, modality)` — `names()` and `[[` indexing unchanged (Test 1 passes)
    - test-160-loader-eligible-sex.R exists and all 4 tests pass with 0 failures
    - The invalid-value path errors with a message containing the string `eligible_sex`
  </acceptance_criteria>
  <done>load_modality_lookup() validates and carries eligible_sex; the named-vector contract is unchanged; the companion accessor exists; 4 tests pass.</done>
</task>

</tasks>

<verification>
- `Rscript -e 'source("R/utils/utils_surveillance.R"); testthat::test_file("tests/testthat/test-160-loader-eligible-sex.R")'` reports 0 failures.
- The codeset verify command in Task 1 prints OK.
- No Phase 158/159 test regresses: `Rscript -e 'source("R/utils/utils_surveillance.R"); testthat::test_file("tests/testthat/test-159-codeset-loader.R")'` still passes.
</verification>

<success_criteria>
- CMP `min_analyte_count` is 11 in the codeset (IMP-01).
- Modalities sheet has `eligible_sex` = F on Mammogram + Breast MRI, blank elsewhere (IMP-04 data half).
- `load_modality_lookup()` exposes eligibility without breaking existing callers, with validation and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/160-surveillance-lab-accuracy-and-reporting-improvements/160-01-SUMMARY.md`.
</output>
