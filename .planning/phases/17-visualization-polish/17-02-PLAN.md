---
phase: 17-visualization-polish
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - R/11_generate_pptx.R
autonomous: true
requirements: [VIZP-02, VIZP-03]

must_haves:
  truths:
    - "PPTX contains a slide with unique encounter dates per person by payer counted after last treatment date"
    - "PPTX contains a slide with the stacked pre/post-treatment histogram PNG embedded"
    - "Post-treatment unique dates metric uses LAST_ANY_TREATMENT_DATE as anchor, NOT first_hl_dx_date"
    - "Patients with no treatment are excluded from the post-treatment summary slide"
  artifacts:
    - path: "R/11_generate_pptx.R"
      provides: "New PPTX slides for VIZP-02 and VIZP-03"
      contains: "Post-Last Treatment"
    - path: "R/11_generate_pptx.R"
      provides: "Stacked histogram slide embedding"
      contains: "encounters_stacked_pre_post_by_payor.png"
  key_links:
    - from: "R/11_generate_pptx.R"
      to: "output/figures/encounters_stacked_pre_post_by_payor.png"
      via: "add_image_slide embedding"
      pattern: "encounters_stacked_pre_post"
    - from: "R/11_generate_pptx.R"
      to: "all_last_dates tibble"
      via: "LAST_ANY_TREATMENT_DATE for post-treatment date computation"
      pattern: "LAST_ANY_TREATMENT_DATE"
---

<objective>
Add two new PPTX slides: (1) a summary table showing unique encounter dates per person by payer category counted after last treatment, and (2) the stacked pre/post-treatment encounter histogram. Update slide count and output messaging.

Purpose: Complete the visualization suite with post-treatment engagement metrics and the stacked histogram that separates pre-treatment from post-treatment encounter volume.
Output: Updated R/11_generate_pptx.R with Slides 26-28 (summary table + stacked histogram + optional summary stats), updated slide count.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-visualization-polish/17-CONTEXT.md
@.planning/phases/17-visualization-polish/17-RESEARCH.md
@.planning/phases/17-visualization-polish/17-01-SUMMARY.md
@R/11_generate_pptx.R
@R/16_encounter_analysis.R
@R/utils_snapshot.R

<interfaces>
<!-- Key types and contracts from existing code needed by executor. -->

From R/11_generate_pptx.R (available at execution time):
```r
# Already computed in Section 2c (lines 271-285):
all_last_dates  # tibble: ID, LAST_ANY_TREATMENT_DATE (max of LAST_CHEMO/RAD/SCT)

# Already computed (line 288-291):
last_encounter_per_patient  # tibble: ID, LAST_ENCOUNTER_DATE

# Available from Section 1:
PAYER_ORDER  # c("Medicare", "Medicaid", ..., "Missing")
rename_payer(x)  # Consolidates Other/Unavailable/Unknown/NA to Missing

# Available helpers:
add_table_slide(pptx, title, subtitle, tbl_data)
add_image_slide(pptx, title, subtitle, img_path, img_width=8.5, img_height=5.0)
add_footnote(pptx, text)
style_table(ft, total_row)
save_output_data(df, name, subdir = "outputs")
format_count_pct(n, total)

# Slide numbering context (current last slide = 25, Slide 25 = Total Unique Dates by DX Year)
# New slides will be 26, 27, (optionally 28)

# Global objects from cohort:
cohort_full  # Full cohort with all payer columns, treatment flags, etc.
encounters   # Filtered encounters (AV+TH, cohort IDs)
pcornet      # Named list of PCORnet tables
N_TOTAL      # Total cohort count
```

From R/16_encounter_analysis.R (Plan 01 output):
```r
# New PNG produced by Section 7:
# output/figures/encounters_stacked_pre_post_by_payor.png
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post-treatment unique encounter dates summary table slide (VIZP-02)</name>
  <files>R/11_generate_pptx.R</files>
  <read_first>
    R/11_generate_pptx.R
    R/16_encounter_analysis.R
    .planning/phases/17-visualization-polish/17-CONTEXT.md
  </read_first>
  <action>
**Per D-03, D-04, D-05: Add a new PPTX slide after Slide 25 (total unique dates by DX year) showing unique encounter dates per person by payer category, counted only after `max(LAST_CHEMO_DATE, LAST_RADIATION_DATE, LAST_SCT_DATE)`.**

Add the following code in Section 5c (after the Slide 25 block, before Section 6: SAVE PPTX). This will be Slide 26.

```r
# ---- Slide 26: Unique Encounter Dates per Person by Payer (Post-Last Treatment) ----
message("  Slide 26: Post-Last-Treatment Unique Encounter Dates by Payer")

# Per D-05: This uses LAST_ANY_TREATMENT_DATE as anchor (post-LAST-treatment),
# NOT first_hl_dx_date (post-diagnosis) which Section 6 of 16_encounter_analysis.R uses.

# Step 1: Compute per-patient unique post-last-treatment encounter dates
post_last_tx_dates <- encounters %>%
  inner_join(all_last_dates, by = "ID") %>%
  filter(
    !is.na(LAST_ANY_TREATMENT_DATE),       # Only patients with treatment (per D-04)
    !is.na(ADMIT_DATE),
    ADMIT_DATE > LAST_ANY_TREATMENT_DATE,   # Post-last-treatment encounters only
    year(ADMIT_DATE) != 1900L               # Filter 1900 sentinels (per VIZP-01)
  ) %>%
  group_by(ID) %>%
  summarise(N_UNIQUE_DATES_POST_LAST_TX = n_distinct(ADMIT_DATE), .groups = "drop")

# Step 2: Join to treated patients with payer, fill 0 for those with no post-tx encounters
# Only include patients who HAVE treatment (per D-04)
treated_cohort_ids <- cohort_full %>%
  inner_join(all_last_dates %>% filter(!is.na(LAST_ANY_TREATMENT_DATE)), by = "ID") %>%
  select(ID, PAYER_CATEGORY_PRIMARY)

post_last_tx_summary <- treated_cohort_ids %>%
  left_join(post_last_tx_dates, by = "ID") %>%
  mutate(
    N_UNIQUE_DATES_POST_LAST_TX = coalesce(N_UNIQUE_DATES_POST_LAST_TX, 0L),
    PAYER_DISPLAY = rename_payer(PAYER_CATEGORY_PRIMARY)
  ) %>%
  filter(!is.na(PAYER_DISPLAY)) %>%
  group_by(PAYER_DISPLAY) %>%
  summarise(
    N = n(),
    Mean = round(mean(N_UNIQUE_DATES_POST_LAST_TX, na.rm = TRUE), 1),
    Median = median(N_UNIQUE_DATES_POST_LAST_TX, na.rm = TRUE),
    Min = min(N_UNIQUE_DATES_POST_LAST_TX, na.rm = TRUE),
    Max = max(N_UNIQUE_DATES_POST_LAST_TX, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  rename(`Payer Category` = PAYER_DISPLAY)

# Add totals row
post_last_tx_totals <- treated_cohort_ids %>%
  left_join(post_last_tx_dates, by = "ID") %>%
  mutate(N_UNIQUE_DATES_POST_LAST_TX = coalesce(N_UNIQUE_DATES_POST_LAST_TX, 0L)) %>%
  summarise(
    `Payer Category` = "Total",
    N = n(),
    Mean = round(mean(N_UNIQUE_DATES_POST_LAST_TX, na.rm = TRUE), 1),
    Median = median(N_UNIQUE_DATES_POST_LAST_TX, na.rm = TRUE),
    Min = min(N_UNIQUE_DATES_POST_LAST_TX, na.rm = TRUE),
    Max = max(N_UNIQUE_DATES_POST_LAST_TX, na.rm = TRUE)
  )

post_last_tx_summary <- bind_rows(post_last_tx_summary, post_last_tx_totals) %>%
  mutate(N = format(N, big.mark = ","))

n_treated_for_slide <- nrow(treated_cohort_ids)

# Snapshot: table backing data (per SNAP-04)
save_output_data(post_last_tx_summary, "post_last_tx_unique_dates_summary_data")

pptx <- add_table_slide(pptx,
  "Unique Encounter Dates per Person (Post-Last Treatment)",
  glue("Distinct encounter dates after last treatment (any type) -- Treated patients only, N = {format(n_treated_for_slide, big.mark = ',')}"),
  post_last_tx_summary) %>%
  add_footnote("Post-Last Treatment = encounters after max(LAST_CHEMO_DATE, LAST_RADIATION_DATE, LAST_SCT_DATE). Patients with no treatment excluded. Unique dates = distinct ADMIT_DATEs per patient.")
```

Key decision adherence:
- **D-03:** Summary table shows unique encounter dates per person by payer, counted after `max(LAST_CHEMO/RAD/SCT)`.
- **D-04:** Patients with no treatment (all LAST_*_DATE NA) are excluded via `inner_join` with `all_last_dates %>% filter(!is.na(LAST_ANY_TREATMENT_DATE))`.
- **D-05:** Uses `LAST_ANY_TREATMENT_DATE` as anchor, NOT `first_hl_dx_date`. This is a DISTINCT metric from Section 6's `N_UNIQUE_DATES_POST_TX`. Footnote documents the difference.
  </action>
  <verify>
    <automated>grep -c "Post-Last Treatment" R/11_generate_pptx.R</automated>
  </verify>
  <acceptance_criteria>
    - R/11_generate_pptx.R contains `Slide 26:` in a message() call
    - R/11_generate_pptx.R contains `N_UNIQUE_DATES_POST_LAST_TX` variable name
    - R/11_generate_pptx.R contains `ADMIT_DATE > LAST_ANY_TREATMENT_DATE`
    - R/11_generate_pptx.R contains `year(ADMIT_DATE) != 1900L` in the post-treatment filter
    - R/11_generate_pptx.R contains `save_output_data(post_last_tx_summary` or equivalent
    - R/11_generate_pptx.R contains `add_footnote` call mentioning "Post-Last Treatment" or "max(LAST_CHEMO"
    - R/11_generate_pptx.R contains the string `D-05` or `post-last-treatment` in a comment distinguishing from Section 6's post-diagnosis metric
  </acceptance_criteria>
  <done>PPTX Slide 26 added with summary table showing N, Mean, Median, Min, Max of unique encounter dates per person by payer, counted after the last treatment date across all types. Only treated patients included. Metric is distinct from existing post-diagnosis unique dates.</done>
</task>

<task type="auto">
  <name>Task 2: Add stacked histogram slide and update slide count</name>
  <files>R/11_generate_pptx.R</files>
  <read_first>
    R/11_generate_pptx.R
    .planning/phases/17-visualization-polish/17-CONTEXT.md
    .planning/phases/17-visualization-polish/17-01-SUMMARY.md
  </read_first>
  <action>
**Per D-06 and VIZP-03: Add a PPTX slide embedding the stacked histogram PNG produced by Plan 01 Task 2, plus an optional summary statistics companion slide.**

Add after Slide 26 (from Task 1 of this plan), before Section 6: SAVE PPTX.

**Slide 27: Stacked encounter histogram (pre/post treatment)**

```r
# ---- Slide 27: Stacked Encounter Histogram (Pre/Post-Treatment) ----
message("  Slide 27: Stacked Encounters Pre/Post-Treatment by Payer")
stacked_hist_path <- "output/figures/encounters_stacked_pre_post_by_payor.png"
pptx <- add_image_slide(pptx,
  "Encounters per Person by Payor (Pre/Post-Treatment Split)",
  glue("Total encounters split by pre/post-treatment period -- Treated patients only"),
  stacked_hist_path,
  img_width = 9, img_height = 5.5
)
if (file.exists(stacked_hist_path)) {
  pptx <- add_footnote(pptx, "Post-treatment = encounters after max(last chemo, last radiation, last SCT date). Patients with no treatment excluded. Blue = post-treatment, orange = pre-treatment.")
}
```

Use `img_width = 9, img_height = 5.5` for the wider image format (matching the Phase 11 pattern for histogram PNGs which are 12x8 inches).

**Slide 28: Summary statistics for stacked histogram (optional per Claude's discretion)**

Add a companion summary statistics table matching the existing Slide 18 pattern but split by Pre/Post period:

```r
# ---- Slide 28: Summary Statistics -- Pre/Post Encounters by Payer ----
message("  Slide 28: Summary Statistics -- Pre/Post Encounters by Payer")

# Compute pre/post encounter stats per payer for treated patients
stacked_stats <- cohort_full %>%
  inner_join(all_last_dates %>% filter(!is.na(LAST_ANY_TREATMENT_DATE)), by = "ID") %>%
  select(ID, PAYER_CATEGORY_PRIMARY, LAST_ANY_TREATMENT_DATE) %>%
  inner_join(
    encounters %>% filter(!is.na(ADMIT_DATE)) %>%
      mutate(ADMIT_DATE_CLEAN = ADMIT_DATE) %>%
      select(ID, ADMIT_DATE_CLEAN),
    by = "ID",
    relationship = "many-to-many"
  ) %>%
  mutate(
    PERIOD = if_else(ADMIT_DATE_CLEAN > LAST_ANY_TREATMENT_DATE, "Post-treatment", "Pre-treatment"),
    PAYER_DISPLAY = rename_payer(PAYER_CATEGORY_PRIMARY)
  ) %>%
  filter(!is.na(PAYER_DISPLAY)) %>%
  count(ID, PERIOD, PAYER_DISPLAY, name = "n_enc") %>%
  tidyr::complete(ID, PERIOD, fill = list(n_enc = 0)) %>%
  group_by(PAYER_DISPLAY, PERIOD) %>%
  summarise(
    N = n_distinct(ID),
    Mean = round(mean(n_enc, na.rm = TRUE), 1),
    Median = round(median(n_enc, na.rm = TRUE), 1),
    Min = min(n_enc, na.rm = TRUE),
    Q1 = round(quantile(n_enc, 0.25, na.rm = TRUE), 1),
    Q3 = round(quantile(n_enc, 0.75, na.rm = TRUE), 1),
    Max = max(n_enc, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  rename(`Payer Category` = PAYER_DISPLAY) %>%
  arrange(`Payer Category`, PERIOD)

stacked_stats <- stacked_stats %>%
  mutate(N = format(N, big.mark = ","))

# Snapshot: table backing data (per SNAP-04)
save_output_data(stacked_stats, "stacked_encounter_stats_by_payer_period_data")

pptx <- add_table_slide(pptx,
  "Summary Statistics: Pre/Post-Treatment Encounters by Payer",
  glue("Encounter count statistics by primary payer and treatment period -- Treated patients only"),
  stacked_stats) %>%
  add_footnote("Post-treatment = encounters after last treatment date. Pre-treatment = encounters on or before last treatment date. Separate rows per period per payer.")
```

**Update slide count** in the final messages (Section 6: SAVE PPTX). Change line 1412 from:
```r
message(glue("  Slides: 25 (1 glossary + 16 tables + 4 encounter analysis + 4 unique dates)"))
```
to:
```r
message(glue("  Slides: 28 (1 glossary + 16 tables + 4 encounter analysis + 4 unique dates + 3 Phase 17 visualization polish)"))
```

Also update the file header comment block (lines 1-45) to include the new slide descriptions:
```
#  26. Unique Encounter Dates per Person by Payer (Post-Last Treatment) [VIZP-02]
#  27. Stacked Encounters Pre/Post-Treatment by Payer [VIZP-03]
#  28. Summary Statistics: Pre/Post-Treatment Encounters by Payer [VIZP-03]
```

Update the slide count to 28 in the file header.
  </action>
  <verify>
    <automated>grep -c "encounters_stacked_pre_post_by_payor" R/11_generate_pptx.R</automated>
  </verify>
  <acceptance_criteria>
    - R/11_generate_pptx.R contains `Slide 27:` in a message() call
    - R/11_generate_pptx.R contains `encounters_stacked_pre_post_by_payor.png` string
    - R/11_generate_pptx.R contains `add_image_slide(pptx,` call for the stacked histogram
    - R/11_generate_pptx.R contains `img_width = 9` for the stacked histogram slide
    - R/11_generate_pptx.R contains `Slide 28:` in a message() call for the summary stats
    - R/11_generate_pptx.R contains `save_output_data(stacked_stats` or equivalent snapshot call
    - R/11_generate_pptx.R contains `Slides: 28` in the final message
    - R/11_generate_pptx.R file header comments list slides 26, 27, 28
    - R/11_generate_pptx.R contains `add_footnote` call for Slide 27 mentioning "Blue = post-treatment"
  </acceptance_criteria>
  <done>PPTX Slides 27-28 added: stacked histogram PNG embedded with footnote, summary statistics table showing pre/post encounter stats by payer. Slide count updated to 28 in header and final messages. File header updated with new slide descriptions.</done>
</task>

</tasks>

<verification>
1. `grep "Slide 26" R/11_generate_pptx.R` returns the post-last-treatment unique dates slide
2. `grep "Slide 27" R/11_generate_pptx.R` returns the stacked histogram slide
3. `grep "Slide 28" R/11_generate_pptx.R` returns the summary stats slide
4. `grep "LAST_ANY_TREATMENT_DATE" R/11_generate_pptx.R` returns multiple hits (anchor date usage)
5. `grep "encounters_stacked_pre_post" R/11_generate_pptx.R` returns the PNG path reference
6. `grep "Slides: 28" R/11_generate_pptx.R` returns the updated count
7. `grep "save_output_data" R/11_generate_pptx.R` returns snapshots for new slides
</verification>

<success_criteria>
- PPTX Slide 26 shows unique encounter dates per person by payer, counted after last treatment date (not diagnosis date)
- PPTX Slide 27 embeds the stacked pre/post-treatment histogram PNG
- PPTX Slide 28 shows summary statistics for pre/post encounter counts by payer
- All new slides have footnotes explaining terms
- All new data frames have save_output_data() snapshot calls
- Slide count updated to 28 throughout the file
</success_criteria>

<output>
After completion, create `.planning/phases/17-visualization-polish/17-02-SUMMARY.md`
</output>
