---
phase: 17-visualization-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/11_generate_pptx.R
  - R/16_encounter_analysis.R
autonomous: true
requirements: [VIZP-01, VIZP-03, PPTX2-04, PPTX2-07]

must_haves:
  truths:
    - "No 1900 dates appear in any PPTX table data or graph computation"
    - "Stacked histogram PNG exists showing pre/post-treatment encounter breakdown by payer"
    - "Encounter histogram already has 6+Missing payer categories with >500 overflow bin annotation"
    - "Age group bar chart labels are already not clipped at plot top"
  artifacts:
    - path: "R/11_generate_pptx.R"
      provides: "1900 sentinel date filtering at PPTX display layer"
      contains: "year.*!= 1900"
    - path: "R/16_encounter_analysis.R"
      provides: "Stacked pre/post-treatment encounter histogram"
      contains: "encounters_stacked_pre_post"
    - path: "output/figures/encounters_stacked_pre_post_by_payor.png"
      provides: "Stacked histogram PNG for PPTX embedding"
  key_links:
    - from: "R/11_generate_pptx.R"
      to: "R/16_encounter_analysis.R"
      via: "source() call regenerates PNGs"
      pattern: 'source.*16_encounter_analysis'
    - from: "R/16_encounter_analysis.R"
      to: "output/figures/"
      via: "ggsave produces PNG files"
      pattern: "ggsave.*encounters_stacked"
---

<objective>
Filter 1900 sentinel dates from all PPTX display content, add a new stacked encounter histogram (pre/post-treatment split by payer) to the encounter analysis script, and verify that PPTX2-04 and PPTX2-07 gap closures from Phase 12 are correctly implemented.

Purpose: Ensure no masked sentinel dates leak into PPTX output, and provide a new visualization that separates pre-treatment from post-treatment encounters for clinician review. Also formally close the two remaining Phase 12 gap items.
Output: Updated R/11_generate_pptx.R with 1900 filtering, new Section 7 in R/16_encounter_analysis.R producing stacked histogram PNG, verified PPTX2-04 and PPTX2-07 implementations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-visualization-polish/17-CONTEXT.md
@.planning/phases/17-visualization-polish/17-RESEARCH.md
@R/11_generate_pptx.R
@R/16_encounter_analysis.R
@R/utils_snapshot.R

<interfaces>
<!-- Key types, functions, and patterns the executor needs from existing code. -->

From R/11_generate_pptx.R:
```r
# Payer consolidation (lines 66-79)
PAYER_ORDER <- c("Medicare", "Medicaid", "Dual eligible", "Private",
                 "Other government", "No payment / Self-pay", "Missing")

rename_payer <- function(x) {
  case_when(
    x %in% c("Other", "Unavailable", "Unknown") ~ "Missing",
    is.na(x) ~ "Missing",
    TRUE ~ x
  )
}

# LAST_ANY_TREATMENT_DATE already computed (lines 271-285)
all_last_dates  # tibble with ID, LAST_ANY_TREATMENT_DATE

# Slide builder helpers
add_table_slide(pptx, title, subtitle, tbl_data)
add_image_slide(pptx, title, subtitle, img_path, img_width, img_height)
add_footnote(pptx, text)

# Snapshot helper (from utils_snapshot.R)
save_output_data(df, name, subdir = "outputs")
```

From R/16_encounter_analysis.R:
```r
# Global objects available from sourced 04_build_cohort.R:
hl_cohort    # Final cohort data frame (has PAYER_CATEGORY_PRIMARY, N_ENCOUNTERS, etc.)
encounters   # Filtered encounters (ENC_TYPE %in% c("AV","TH"), inner_join with hl_cohort IDs)
pcornet      # Named list of PCORnet tables (pcornet$ENCOUNTER, pcornet$ENROLLMENT, etc.)

# Existing histogram pattern (Section 1, lines 36-87):
# - case_when() consolidates payer to 6+Missing
# - x_cap <- 500
# - overflow_counts computed per-facet
# - N_ENC_CAPPED at cap+1 for overflow bin
# - geom_histogram(binwidth=20) + facet_wrap(~PAYER_CATEGORY_PRIMARY, scales="free_y")
# - geom_text for overflow annotation
# - coord_cartesian(xlim = c(0, x_cap + 40))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter 1900 sentinel dates from PPTX display layer and verify PPTX2-04/PPTX2-07</name>
  <files>R/11_generate_pptx.R</files>
  <read_first>
    R/11_generate_pptx.R
    R/16_encounter_analysis.R
    .planning/phases/17-visualization-polish/17-CONTEXT.md
  </read_first>
  <action>
**Per D-01 and D-02: Filter 1900 sentinel dates at the PPTX display layer in `11_generate_pptx.R`.**

Do NOT modify `04_build_cohort.R` (the existing `first_hl_dx_date` nullification at lines 176-183 stays as-is).

**1900 filtering locations in 11_generate_pptx.R:**

In Section 2 (`compute_last_dates()` function and its callers, lines 107-344), add 1900 filtering after the `all_last_dates` tibble is computed (after line 285). Insert a filter step that nullifies any 1900 sentinel dates in the computed LAST_*_DATE columns:

```r
# Filter 1900 sentinel dates from computed last treatment dates (per VIZP-01)
n_sentinel_last_chemo <- sum(year(last_chemo_dates$LAST_CHEMO_DATE) == 1900L, na.rm = TRUE)
n_sentinel_last_rad <- sum(year(last_rad_dates$LAST_RADIATION_DATE) == 1900L, na.rm = TRUE)
n_sentinel_last_sct <- sum(year(last_sct_dates$LAST_SCT_DATE) == 1900L, na.rm = TRUE)
n_sentinel_total <- n_sentinel_last_chemo + n_sentinel_last_rad + n_sentinel_last_sct

if (n_sentinel_total > 0) {
  message(glue("  VIZP-01: Filtering {n_sentinel_total} sentinel dates (year 1900) from last treatment dates"))
  last_chemo_dates <- last_chemo_dates %>%
    filter(is.na(LAST_CHEMO_DATE) | year(LAST_CHEMO_DATE) != 1900L)
  last_rad_dates <- last_rad_dates %>%
    filter(is.na(LAST_RADIATION_DATE) | year(LAST_RADIATION_DATE) != 1900L)
  last_sct_dates <- last_sct_dates %>%
    filter(is.na(LAST_SCT_DATE) | year(LAST_SCT_DATE) != 1900L)
}
```

Place this BEFORE `all_last_dates` computation (before line 271) so downstream calculations (post-treatment payer, enrollment coverage) all use clean dates.

Also add 1900 filtering to the `cohort_full` assembly (after line 386) for any FIRST_*_DATE columns that appear in PPTX tables:

```r
# Filter 1900 sentinel dates from FIRST treatment dates shown in PPTX (per VIZP-01)
cohort_full <- cohort_full %>%
  mutate(
    across(
      c(FIRST_CHEMO_DATE, FIRST_RADIATION_DATE, FIRST_SCT_DATE),
      ~ if_else(year(.x) == 1900L, as.Date(NA), .x)
    )
  )
```

Note: `first_hl_dx_date` is already nullified in `04_build_cohort.R` and should NOT be touched here.

Also add 1900 filtering in Section 5b before the `n_missing_dx_year` computation (around line 1244-1250). The existing code at line 1245 already correctly handles this via `is.na(cohort_full$DX_YEAR)` since DX_YEAR derives from the already-nullified `first_hl_dx_date`. Verify this is correct and add a comment noting VIZP-01 compliance.

**Verify PPTX2-04 (D-11):** Confirm that `R/16_encounter_analysis.R` lines 36-83 contain:
- `case_when()` consolidating payer to 6+Missing
- `x_cap <- 500`
- `overflow_counts` computed per-facet with `.drop = FALSE`
- `geom_text()` annotation for overflow bin
- `facet_wrap(~ PAYER_CATEGORY_PRIMARY, scales = "free_y")`

These already exist per Phase 12 Plan 01 summary. Add a comment at the top of Section 1 in `16_encounter_analysis.R`:
```r
# PPTX2-04: Verified -- 6+Missing payer, >500 overflow bin with per-facet annotation (Phase 12)
```

**Verify PPTX2-07 (D-12):** Confirm that `R/16_encounter_analysis.R` line 231 contains:
```r
coord_cartesian(clip = "off", ylim = c(0, max_y_p4 * 1.2))
```
This already exists. Add a comment above line 225:
```r
# PPTX2-07: Verified -- clip="off" + ylim expansion prevents label clipping (Phase 12)
```

**Update slide count comment** at end of `11_generate_pptx.R` (line 1412) to note that Phase 17 will add additional slides (updated in Plan 02).
  </action>
  <verify>
    <automated>grep -c "1900" R/11_generate_pptx.R</automated>
  </verify>
  <acceptance_criteria>
    - R/11_generate_pptx.R contains `year(last_chemo_dates$LAST_CHEMO_DATE) == 1900L`
    - R/11_generate_pptx.R contains `year(.x) == 1900L` in across() for FIRST_*_DATE columns
    - R/11_generate_pptx.R contains `VIZP-01` comment string
    - R/16_encounter_analysis.R contains `PPTX2-04: Verified` comment
    - R/16_encounter_analysis.R contains `PPTX2-07: Verified` comment
    - R/16_encounter_analysis.R line 231 still contains `coord_cartesian(clip = "off"`
    - R/16_encounter_analysis.R lines 39-45 still contain `case_when` with `Missing`
  </acceptance_criteria>
  <done>1900 sentinel dates filtered from all LAST_*_DATE and FIRST_*_DATE columns before they reach PPTX tables or graph computations. PPTX2-04 and PPTX2-07 verified with comments added. No 1900 dates can appear in PPTX output.</done>
</task>

<task type="auto">
  <name>Task 2: Add stacked pre/post-treatment encounter histogram to 16_encounter_analysis.R</name>
  <files>R/16_encounter_analysis.R</files>
  <read_first>
    R/16_encounter_analysis.R
    R/11_generate_pptx.R
    .planning/phases/17-visualization-polish/17-RESEARCH.md
  </read_first>
  <action>
**Per D-06 through D-10: Add a NEW Section 7 to `R/16_encounter_analysis.R` that creates a stacked encounter histogram.**

Add after the current Section 6 (after line 400) and before the final "Encounter analysis complete" message.

**New Section 7: STACKED HISTOGRAM -- Pre/Post-Treatment Encounters per Person by Payer**

The stacked histogram splits each patient's total encounters into pre-treatment and post-treatment periods, where "post-treatment" means encounters after `max(LAST_CHEMO_DATE, LAST_RADIATION_DATE, LAST_SCT_DATE)`.

Implementation steps:

```r
# ==============================================================================
# SECTION 7: STACKED HISTOGRAM -- Pre/Post-Treatment Encounters by Payer (VIZP-03)
# ==============================================================================

message("\n--- Stacked Histogram: Pre/Post-Treatment Encounters by Payer ---")

# 7a. Compute LAST_ANY_TREATMENT_DATE per patient
# Reuse the same logic as 11_generate_pptx.R Section 2c
last_chemo <- hl_cohort %>%
  filter(HAD_CHEMO == 1, !is.na(FIRST_CHEMO_DATE)) %>%
  select(ID, FIRST_CHEMO_DATE)
# ... (use the actual compute_last_dates pattern or join from hl_cohort columns)
# Actually, use the simpler approach: compute from hl_cohort's existing columns
# hl_cohort has FIRST_CHEMO_DATE, FIRST_RADIATION_DATE, FIRST_SCT_DATE but NOT LAST_*
# So compute last dates from raw encounter data, similar to 11_generate_pptx.R

# For the histogram, we need per-patient encounter counts split by pre/post period.
# Use the encounters table (already filtered to AV+TH and cohort IDs from 04_build_cohort.R).

# Compute LAST_ANY_TREATMENT_DATE the same way as 11_generate_pptx.R
# Since 16_encounter_analysis.R is sourced BY 11_generate_pptx.R, and we need
# all_last_dates which is computed AFTER this script runs, we must compute it here independently.

# Use hl_cohort's FIRST_*_DATE columns and treatment flags to identify treated patients,
# then compute last treatment dates from the raw procedures/prescribing tables.

# Simpler approach: compute per-patient encounter splits using encounters table directly
# Step 1: Get last treatment date per patient from PROCEDURES (all treatment types)
tx_dates_for_stacked <- bind_rows(
  # Chemo procedures
  pcornet$PROCEDURES %>%
    filter(
      (PX_TYPE == "CH" & PX %in% TREATMENT_CODES$chemo_hcpcs) |
      (PX_TYPE == "09" & PX %in% TREATMENT_CODES$chemo_icd9) |
      (PX_TYPE == "10" & PX %in% TREATMENT_CODES$chemo_icd10pcs_prefixes) |
      (PX_TYPE == "RE" & PX %in% TREATMENT_CODES$chemo_revenue)
    ) %>%
    filter(!is.na(PX_DATE)) %>%
    select(ID, tx_date = PX_DATE),
  # Radiation procedures
  pcornet$PROCEDURES %>%
    filter(
      (PX_TYPE == "CH" & PX %in% TREATMENT_CODES$radiation_cpt) |
      (PX_TYPE == "09" & PX %in% TREATMENT_CODES$radiation_icd9) |
      (PX_TYPE == "RE" & PX %in% TREATMENT_CODES$radiation_revenue)
    ) %>%
    filter(!is.na(PX_DATE)) %>%
    select(ID, tx_date = PX_DATE),
  # SCT procedures
  pcornet$PROCEDURES %>%
    filter(
      (PX_TYPE == "CH" & PX %in% TREATMENT_CODES$sct_cpt) |
      (PX_TYPE == "09" & PX %in% TREATMENT_CODES$sct_icd9) |
      (PX_TYPE == "10" & PX %in% TREATMENT_CODES$sct_icd10pcs) |
      (PX_TYPE == "RE" & PX %in% TREATMENT_CODES$sct_revenue)
    ) %>%
    filter(!is.na(PX_DATE)) %>%
    select(ID, tx_date = PX_DATE)
) %>%
  # Filter 1900 sentinel dates (per VIZP-01)
  filter(year(tx_date) != 1900L) %>%
  group_by(ID) %>%
  summarise(LAST_ANY_TX_DATE = max(tx_date, na.rm = TRUE), .groups = "drop") %>%
  filter(!is.infinite(LAST_ANY_TX_DATE))
```

IMPORTANT: The above is illustrative. The executor should study the actual `compute_last_dates()` function in `11_generate_pptx.R` (lines 107-235) which also pulls from PRESCRIBING, DIAGNOSIS DX codes, ENCOUNTER DRG codes, DISPENSING, and MED_ADMIN. For the stacked histogram, a simplified approach is acceptable since the key requirement is to match the same `max(LAST_CHEMO_DATE, LAST_RADIATION_DATE, LAST_SCT_DATE)` logic. Use whatever sources are actually available from the loaded `pcornet` list.

Then:

```r
# Step 2: Split encounters into pre/post treatment (per D-07)
n_treated_for_stacked <- nrow(tx_dates_for_stacked)
message(glue("  {n_treated_for_stacked} patients with treatment dates for stacked histogram"))

stacked_enc <- encounters %>%
  inner_join(tx_dates_for_stacked, by = "ID") %>%  # Only treated patients (per D-10)
  filter(!is.na(ADMIT_DATE)) %>%
  mutate(
    ENCOUNTER_PERIOD = if_else(
      ADMIT_DATE > LAST_ANY_TX_DATE,
      "Post-treatment",
      "Pre-treatment"
    )
  )

# Step 3: Count per patient per period (per D-09: use raw N_ENCOUNTERS count basis)
stacked_counts <- stacked_enc %>%
  count(ID, ENCOUNTER_PERIOD, name = "n_enc") %>%
  tidyr::complete(ID, ENCOUNTER_PERIOD, fill = list(n_enc = 0))  # Ensure all patients have both periods

# Step 4: Compute total encounters per patient for histogram x-axis
patient_totals_stacked <- stacked_counts %>%
  group_by(ID) %>%
  summarise(N_TOTAL = sum(n_enc), .groups = "drop")

# Step 5: Join payer category (per D-08: faceted by 6+Missing)
stacked_plot <- stacked_counts %>%
  left_join(
    hl_cohort %>% select(ID, PAYER_CATEGORY_PRIMARY),
    by = "ID"
  ) %>%
  left_join(patient_totals_stacked, by = "ID") %>%
  mutate(
    PAYER_CATEGORY_PRIMARY = case_when(
      PAYER_CATEGORY_PRIMARY %in% c("Other", "Unavailable", "Unknown") ~ "Missing",
      is.na(PAYER_CATEGORY_PRIMARY) ~ "Missing",
      TRUE ~ PAYER_CATEGORY_PRIMARY
    ),
    PAYER_CATEGORY_PRIMARY = factor(PAYER_CATEGORY_PRIMARY,
      levels = c("Medicare", "Medicaid", "Dual eligible", "Private",
                 "Other government", "No payment / Self-pay", "Missing")),
    # Post-treatment on bottom (first level = bottom in stacked histogram) per D-07
    ENCOUNTER_PERIOD = factor(ENCOUNTER_PERIOD,
      levels = c("Post-treatment", "Pre-treatment"))
  )

# Step 6: Overflow bin at >500 (matching Section 1 pattern)
x_cap_stk <- 500

overflow_stk <- stacked_plot %>%
  distinct(ID, PAYER_CATEGORY_PRIMARY, N_TOTAL) %>%
  filter(N_TOTAL > x_cap_stk) %>%
  count(PAYER_CATEGORY_PRIMARY, name = "n_overflow", .drop = FALSE)

stacked_plot <- stacked_plot %>%
  mutate(N_TOTAL_CAPPED = if_else(N_TOTAL > x_cap_stk, as.numeric(x_cap_stk + 1), as.numeric(N_TOTAL)))

n_beyond_stk <- sum(patient_totals_stacked$N_TOTAL > x_cap_stk)

# Snapshot: figure backing data (per SNAP-03)
save_output_data(stacked_plot, "encounters_stacked_pre_post_data")

# Step 7: Create stacked histogram
p_stacked <- ggplot(stacked_plot, aes(x = N_TOTAL_CAPPED, fill = ENCOUNTER_PERIOD)) +
  geom_histogram(position = "stack", binwidth = 20, color = "white", linewidth = 0.2) +
  geom_text(data = overflow_stk %>% filter(n_overflow > 0),
            aes(x = x_cap_stk + 10, y = Inf, label = paste0(">", x_cap_stk, ": ", n_overflow)),
            vjust = 1.5, hjust = 0, size = 2.8, inherit.aes = FALSE) +
  facet_wrap(~ PAYER_CATEGORY_PRIMARY, scales = "free_y") +
  coord_cartesian(xlim = c(0, x_cap_stk + 40)) +
  scale_x_continuous(breaks = seq(0, x_cap_stk, by = 100),
                     labels = c(seq(0, x_cap_stk - 100, by = 100), paste0(x_cap_stk, "+"))) +
  scale_fill_manual(values = c("Post-treatment" = "#2c7fb8", "Pre-treatment" = "#ff7f0e")) +
  labs(
    title = "Encounters per Person by Payor (Pre/Post-Treatment Split)",
    subtitle = glue("Treated patients only (N = {format(n_treated_for_stacked, big.mark = ',')}) | {n_beyond_stk} with >{x_cap_stk} encounters in overflow bin"),
    x = "Number of Encounters",
    y = "Number of Patients",
    fill = "Period"
  ) +
  theme_minimal(base_size = 11) +
  theme(strip.text = element_text(face = "bold"),
        legend.position = "bottom")

ggsave("output/figures/encounters_stacked_pre_post_by_payor.png", p_stacked,
       width = 12, height = 8, dpi = 300)
message("  Saved: output/figures/encounters_stacked_pre_post_by_payor.png")
```

Key requirements from locked decisions:
- **D-06:** This is a NEW section, not replacing the existing Section 1 histogram
- **D-07:** Post-treatment on bottom of stack (first factor level), pre-treatment on top
- **D-08:** Faceted by 6+Missing payer categories
- **D-09:** Uses raw encounter counts (not unique dates)
- **D-10:** Patients with no treatment excluded (inner_join on tx_dates)
- Color palette: `"Post-treatment" = "#2c7fb8", "Pre-treatment" = "#ff7f0e"` (matches existing blue/orange pattern from age group chart)
  </action>
  <verify>
    <automated>grep -c "encounters_stacked_pre_post" R/16_encounter_analysis.R</automated>
  </verify>
  <acceptance_criteria>
    - R/16_encounter_analysis.R contains `SECTION 7:` header comment
    - R/16_encounter_analysis.R contains `ENCOUNTER_PERIOD` variable creation
    - R/16_encounter_analysis.R contains `levels = c("Post-treatment", "Pre-treatment")`
    - R/16_encounter_analysis.R contains `position = "stack"` in geom_histogram call
    - R/16_encounter_analysis.R contains `facet_wrap(~ PAYER_CATEGORY_PRIMARY`
    - R/16_encounter_analysis.R contains `ggsave("output/figures/encounters_stacked_pre_post_by_payor.png"`
    - R/16_encounter_analysis.R contains `save_output_data(stacked_plot` or equivalent snapshot call
    - R/16_encounter_analysis.R contains `x_cap_stk <- 500` or similar overflow bin cap
    - R/16_encounter_analysis.R contains `scale_fill_manual` with both `#2c7fb8` and `#ff7f0e`
  </acceptance_criteria>
  <done>New Section 7 in 16_encounter_analysis.R produces stacked pre/post-treatment histogram PNG faceted by 6+Missing payer categories with overflow bin, blue/orange color coding, and RDS snapshot. Post-treatment on bottom of each bar, pre-treatment on top. Only treated patients included.</done>
</task>

</tasks>

<verification>
1. `grep -c "1900" R/11_generate_pptx.R` returns >= 3 (sentinel filtering code)
2. `grep -c "VIZP-01" R/11_generate_pptx.R` returns >= 1 (compliance comment)
3. `grep "PPTX2-04" R/16_encounter_analysis.R` returns verification comment
4. `grep "PPTX2-07" R/16_encounter_analysis.R` returns verification comment
5. `grep "encounters_stacked_pre_post" R/16_encounter_analysis.R` returns >= 2 (ggsave + save_output_data)
6. `grep "SECTION 7" R/16_encounter_analysis.R` returns 1 (new section header)
</verification>

<success_criteria>
- 1900 sentinel dates filtered from all LAST_*_DATE and FIRST_*_DATE columns in PPTX display layer
- Stacked histogram PNG produced at output/figures/encounters_stacked_pre_post_by_payor.png
- PPTX2-04 (overflow bin) and PPTX2-07 (label clipping) verified with comments
- All snapshot calls included for new data frames
</success_criteria>

<output>
After completion, create `.planning/phases/17-visualization-polish/17-01-SUMMARY.md`
</output>
