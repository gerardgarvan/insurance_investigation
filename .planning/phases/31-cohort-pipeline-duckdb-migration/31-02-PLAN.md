---
phase: 31-cohort-pipeline-duckdb-migration
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - R/28_benchmark_cohort.R
  - docs/DUCKDB_TRANSLATION_NOTES.md
autonomous: true
requirements: [DBCOH-03]
must_haves:
  truths:
    - "User can see RDS vs DuckDB benchmark timings from 3 runs per backend"
    - "User can see median comparison in output/logs/duckdb_benchmark.csv"
    - "Benchmark times cohort build only, not data loading"
    - "Translation notes updated with any gaps found during migration"
  artifacts:
    - path: "R/28_benchmark_cohort.R"
      provides: "Standalone benchmark wrapper for cohort build timing comparison"
      contains: "duckdb_benchmark"
    - path: "docs/DUCKDB_TRANSLATION_NOTES.md"
      provides: "Updated translation gap documentation with Phase 31 findings"
      contains: "Phase 31"
  key_links:
    - from: "R/28_benchmark_cohort.R"
      to: "R/04_build_cohort.R"
      via: "sources cohort build under each backend with timing"
      pattern: "source.*04_build_cohort"
    - from: "R/28_benchmark_cohort.R"
      to: "output/logs/duckdb_benchmark.csv"
      via: "write_csv of timing results"
      pattern: "duckdb_benchmark"
---

<objective>
Create a standalone benchmark wrapper script that times the cohort build pipeline under both RDS and DuckDB backends (3 runs each, median comparison), writing results to output/logs/duckdb_benchmark.csv. Also update DUCKDB_TRANSLATION_NOTES.md with any translation gaps discovered during Plan 31-01 migration.

Purpose: Provide empirical evidence of DuckDB performance characteristics vs RDS baseline, establishing the benchmark infrastructure that Phase 32 will reuse for diagnostic script timing.

Output: R/28_benchmark_cohort.R + updated docs/DUCKDB_TRANSLATION_NOTES.md + output/logs/duckdb_benchmark.csv (generated on HiPerGator)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-cohort-pipeline-duckdb-migration/31-CONTEXT.md
@.planning/phases/31-cohort-pipeline-duckdb-migration/31-RESEARCH.md
@.planning/phases/31-cohort-pipeline-duckdb-migration/31-01-SUMMARY.md

@R/utils_duckdb.R
@docs/DUCKDB_TRANSLATION_NOTES.md

<interfaces>
<!-- From Plan 31-01: migrated pipeline -->

The cohort build pipeline (04_build_cohort.R) now uses get_pcornet_table() for all
table access. It works under both USE_DUCKDB = FALSE (RDS mode) and
USE_DUCKDB = TRUE (DuckDB mode) with identical output.

Key globals after pipeline run:
- hl_cohort: tibble with ~80 columns, patient-level HL cohort
- attrition_log: tibble with step_name, n_before, n_after, n_excluded

Timing scope (D-10): Time 04_build_cohort.R execution only, not 00_config.R
or 01_load_pcornet.R data loading. Assumes pcornet list and DuckDB connection
are already established.

From R/00_config.R:
```r
USE_DUCKDB <- FALSE  # Toggle for backend selection
CONFIG$output_dir  # "output" -- for logs subdirectory
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/28_benchmark_cohort.R benchmark wrapper</name>
  <files>R/28_benchmark_cohort.R</files>
  <read_first>
    R/04_build_cohort.R
    R/utils_duckdb.R
    R/00_config.R
    R/27_parity_test_cohort.R
    .planning/phases/31-cohort-pipeline-duckdb-migration/31-CONTEXT.md
  </read_first>
  <action>
Create a standalone benchmark wrapper script per D-10, D-11, D-12.

**Script name:** R/28_benchmark_cohort.R (D-11 specified `R/27_benchmark_cohort.R` but Plan 31-01 used R/27 for the parity test. Use R/28 to avoid collision.)

**Script structure:**

```r
# ==============================================================================
# 28_benchmark_cohort.R -- RDS vs DuckDB cohort build benchmark
# ==============================================================================
# Phase 31: DBCOH-03
# Times the cohort build pipeline (04_build_cohort.R) under both backends.
# 3 runs per backend with median comparison (D-12).
#
# Per D-10: Times cohort build ONLY, not config/data loading.
# Per D-11: Standalone script, separate from production code.
# Per D-12: 3 runs per backend, median comparison.
#
# Output: output/logs/duckdb_benchmark.csv
#
# Usage:
#   source("R/28_benchmark_cohort.R")  # Run interactively on HiPerGator
# ==============================================================================
```

1. **Load infrastructure first (not timed per D-10):**
   ```r
   source("R/00_config.R")
   source("R/01_load_pcornet.R")
   # Ensure DuckDB connection is open for DuckDB runs
   if (!exists("pcornet_con", envir = .GlobalEnv)) {
     open_pcornet_con()
   }
   ```

2. **Define timing function:**
   ```r
   time_cohort_build <- function(backend, run_number) {
     message(glue("\n--- {backend} run {run_number} ---"))

     # Set backend
     USE_DUCKDB <<- (backend == "DuckDB")

     # Clear prior results
     if (exists("hl_cohort", envir = .GlobalEnv)) rm(hl_cohort, envir = .GlobalEnv)
     if (exists("attrition_log", envir = .GlobalEnv)) rm(attrition_log, envir = .GlobalEnv)

     # Time the cohort build only (D-10)
     start_time <- proc.time()
     source("R/04_build_cohort.R", local = FALSE)
     elapsed <- proc.time() - start_time

     # Capture result dimensions for verification
     cohort_rows <- nrow(get("hl_cohort", envir = .GlobalEnv))
     cohort_cols <- ncol(get("hl_cohort", envir = .GlobalEnv))

     tibble(
       backend = backend,
       run = run_number,
       elapsed_seconds = elapsed["elapsed"],
       user_seconds = elapsed["user.self"],
       system_seconds = elapsed["sys.self"],
       cohort_rows = cohort_rows,
       cohort_cols = cohort_cols,
       timestamp = Sys.time()
     )
   }
   ```

3. **Run 3 iterations per backend (D-12):**
   ```r
   n_runs <- 3L
   results <- list()

   message(strrep("=", 60))
   message("BENCHMARK: RDS vs DuckDB Cohort Build")
   message(strrep("=", 60))

   # RDS runs first
   for (i in seq_len(n_runs)) {
     results[[length(results) + 1]] <- time_cohort_build("RDS", i)
   }

   # DuckDB runs
   for (i in seq_len(n_runs)) {
     results[[length(results) + 1]] <- time_cohort_build("DuckDB", i)
   }

   benchmark_results <- bind_rows(results)
   ```

4. **Compute summary statistics:**
   ```r
   benchmark_summary <- benchmark_results %>%
     group_by(backend) %>%
     summarise(
       n_runs = n(),
       median_seconds = median(elapsed_seconds),
       min_seconds = min(elapsed_seconds),
       max_seconds = max(elapsed_seconds),
       mean_seconds = mean(elapsed_seconds),
       sd_seconds = sd(elapsed_seconds),
       .groups = "drop"
     )

   # Compute speedup ratio
   rds_median <- benchmark_summary %>% filter(backend == "RDS") %>% pull(median_seconds)
   ddb_median <- benchmark_summary %>% filter(backend == "DuckDB") %>% pull(median_seconds)
   speedup <- rds_median / ddb_median
   ```

5. **Write CSV output:**
   ```r
   dir.create(file.path(CONFIG$output_dir, "logs"), showWarnings = FALSE, recursive = TRUE)
   output_path <- file.path(CONFIG$output_dir, "logs", "duckdb_benchmark.csv")
   write_csv(benchmark_results, output_path)
   message(glue("\nBenchmark results saved to: {output_path}"))
   ```

6. **Console summary:**
   ```r
   message(strrep("=", 60))
   message("BENCHMARK RESULTS")
   message(strrep("=", 60))
   message(glue("\nRDS:    median {round(rds_median, 2)}s (range: {round(min_rds, 2)}-{round(max_rds, 2)}s)"))
   message(glue("DuckDB: median {round(ddb_median, 2)}s (range: {round(min_ddb, 2)}-{round(max_ddb, 2)}s)"))
   message(glue("\nSpeedup ratio: {round(speedup, 2)}x"))
   if (speedup > 1) {
     message(glue("DuckDB is {round(speedup, 2)}x faster than RDS"))
   } else {
     message(glue("RDS is {round(1/speedup, 2)}x faster than DuckDB"))
   }
   message(glue("\nVariance: RDS sd={round(sd_rds, 3)}s, DuckDB sd={round(sd_ddb, 3)}s"))
   ```

7. **Cleanup:**
   ```r
   USE_DUCKDB <<- FALSE  # Restore default
   message(strrep("=", 60))
   message("Benchmark complete")
   message(strrep("=", 60))
   ```

**CSV column layout:**
backend, run, elapsed_seconds, user_seconds, system_seconds, cohort_rows, cohort_cols, timestamp

This establishes the reusable pattern for Phase 32 diagnostic script benchmarking.
  </action>
  <verify>
    <automated>grep -c "duckdb_benchmark" R/28_benchmark_cohort.R</automated>
  </verify>
  <acceptance_criteria>
    - R/28_benchmark_cohort.R exists and is a valid R script
    - Contains `duckdb_benchmark` in the output path (at least 1 occurrence)
    - Contains `n_runs <- 3` or equivalent 3-iteration loop (D-12)
    - Contains `USE_DUCKDB <<- TRUE` and `USE_DUCKDB <<- FALSE` for backend switching
    - Contains `source("R/04_build_cohort.R")` for timed execution
    - Contains `proc.time()` or `system.time()` for timing
    - Contains `median` for median comparison (D-12)
    - Contains `write_csv` for output to `output/logs/duckdb_benchmark.csv`
    - Contains `elapsed_seconds` column in results tibble
    - Does NOT contain `source("R/00_config.R")` inside the timing block (D-10: config loading excluded from timing)
    - Contains `speedup` ratio calculation
    - Script header references DBCOH-03
  </acceptance_criteria>
  <done>Standalone benchmark wrapper exists at R/28_benchmark_cohort.R. Times cohort build (not data loading per D-10) under both backends, 3 runs each (D-12), computes median comparison and speedup ratio, writes results to output/logs/duckdb_benchmark.csv. Reusable pattern for Phase 32.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/DUCKDB_TRANSLATION_NOTES.md with Phase 31 findings</name>
  <files>docs/DUCKDB_TRANSLATION_NOTES.md</files>
  <read_first>
    docs/DUCKDB_TRANSLATION_NOTES.md
    R/03_cohort_predicates.R
    R/02_harmonize_payer.R
    R/04_build_cohort.R
    R/10_treatment_payer.R
    .planning/phases/31-cohort-pipeline-duckdb-migration/31-01-SUMMARY.md
  </read_first>
  <action>
Update the existing translation notes document with Phase 31 migration findings. The document currently has 6 pre-populated gaps from Phase 30 code analysis. Update with actual workarounds applied.

1. **Update status line** at top from "Pre-populated from code analysis. Update after running R/26_smoke_test_backends.R on HiPerGator." to "Updated Phase 31 (2026-04-23): Workarounds applied during cohort pipeline migration."

2. **Update each of the 6 gaps** with the actual workaround that was implemented:

   Gap 1 (Custom R functions in filter):
   - Add "**Resolution (Phase 31):**" subsection
   - Document: `is_hl_diagnosis(DX, DX_TYPE)` replaced with inline `%in%` matching using pre-computed dotted+undotted ICD code vectors
   - List affected files: R/03_cohort_predicates.R, R/02_harmonize_payer.R, R/04_build_cohort.R
   - Note: `is_hl_diagnosis()` function in utils_icd.R is preserved for standalone use but no longer called inside dplyr filter() chains

   Gap 2 (is_hl_histology str_extract):
   - Add "**Resolution (Phase 31):**" subsection
   - Document: Replaced with `substr(as.character(HISTOLOGICAL_TYPE), 1, 4) %in% ICD_CODES$hl_histology`
   - List affected files: R/03_cohort_predicates.R, R/04_build_cohort.R

   Gap 3 (if_any with all_of):
   - Add "**Resolution (Phase 31):**" subsection
   - Document: Replaced with explicit OR conditions using `rlang::parse_expr()` for dynamic column sets, or hard-coded OR for known column pairs
   - List affected files: R/03_cohort_predicates.R, R/10_treatment_payer.R

   Gap 4 (str_detect for ICD-10-PCS):
   - Add "**Resolution (Phase 31):**" subsection
   - Document: Used materialize-then-filter pattern: filter PX_TYPE == "10" lazily, materialize(), then apply str_detect() R-side
   - List affected files: R/03_cohort_predicates.R, R/10_treatment_payer.R

   Gap 5 (semi_join performance):
   - Add "**Resolution (Phase 31):**" subsection
   - Document whether semi_join was replaced or kept. If kept: "semi_join() preserved -- benchmark (R/28_benchmark_cohort.R) will reveal if performance is an issue. Replace with inner_join() %>% distinct() only if empirical evidence shows slowdown."
   - Note: Materialized the enrolled_patients tibble before semi_join, so the join is tibble-to-tibble (no DuckDB semi_join needed)

   Gap 6 (TUMOR_REGISTRY_ALL VIEW):
   - Add "**Resolution (Phase 31):**" subsection
   - Document: VIEW creation in open_pcornet_con() works. No UNION ALL BY NAME needed. Column checks use colnames() on the lazy tbl_dbi.

3. **Add new section: "Phase 31 Additional Findings"**
   - Document any NEW translation issues discovered during migration that were NOT in the original 6 gaps
   - Common candidates:
     - `nchar(trimws(x))` not translating (replaced with `x != ""`)
     - `names()` vs `colnames()` on tbl_dbi objects
     - `!is.null(pcornet$TABLE)` guard pattern replacement
     - `pcornet$TABLE$COLUMN` direct column access not working on tbl_dbi
     - `do.call(pmin, ...)` not working on tbl_dbi (materialize first)
     - `bind_rows()` on lazy tbl_dbi (materialize first)

4. **Add "Phase 31 Materialization Points" section:**
   - Document where materialize() calls were added in 04_build_cohort.R and why
   - List approximately: after DEMOGRAPHIC select, after enrollment summarise (before lubridate interval), before hl_source_map count/logging, and note that encounters from 02_harmonize_payer.R are materialized at section boundary

5. **Update the "Phase 31 Refactoring Recommendations" section** to reflect completed status:
   - Mark items 1-4 as DONE
   - Update item 5 (semi_join benchmark) as PENDING benchmark results
  </action>
  <verify>
    <automated>grep -c "Phase 31" docs/DUCKDB_TRANSLATION_NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/DUCKDB_TRANSLATION_NOTES.md contains "Phase 31" (at least 5 occurrences)
    - Contains "Resolution (Phase 31)" subsections under gaps 1-6
    - Contains "Phase 31 Additional Findings" section
    - Contains "Phase 31 Materialization Points" section
    - Contains references to specific files modified (R/03_cohort_predicates.R, R/02_harmonize_payer.R, etc.)
    - Recommendations section shows items 1-4 as DONE
    - Updated status line reflects Phase 31 date
  </acceptance_criteria>
  <done>docs/DUCKDB_TRANSLATION_NOTES.md updated with all 6 gap resolutions, new findings from Phase 31 migration, materialization point documentation, and recommendation status updates.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. R/28_benchmark_cohort.R exists and contains benchmark logic for 3 runs per backend
2. docs/DUCKDB_TRANSLATION_NOTES.md contains Phase 31 resolution details for all 6 gaps
3. Benchmark script times cohort build only (not config loading) per D-10
4. Benchmark script writes to output/logs/duckdb_benchmark.csv per DBCOH-03
</verification>

<success_criteria>
- Benchmark wrapper script created per D-11 with 3-run median comparison per D-12
- Benchmark times cohort build only (not data loading) per D-10
- Translation notes comprehensively updated with Phase 31 migration results
- Benchmark infrastructure reusable for Phase 32 diagnostic script timing
</success_criteria>

<output>
After completion, create `.planning/phases/31-cohort-pipeline-duckdb-migration/31-02-SUMMARY.md`
</output>
