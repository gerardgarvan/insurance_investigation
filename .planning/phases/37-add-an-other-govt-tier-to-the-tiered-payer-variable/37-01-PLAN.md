---
phase: 37-add-an-other-govt-tier-to-the-tiered-payer-variable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/36_tiered_same_day_payer.R
autonomous: true
requirements: []
must_haves:
  truths:
    - "Same-day payer resolution distinguishes 'Other govt' from 'Other' as separate tiers"
    - "'Other govt' resolves with higher priority than 'Other' but lower than 'Private'"
    - "Output CSVs contain 'Other govt' as a distinct resolved_payer value"
    - "Safety net assigns maximum rank (8) for NA tier_rank values"
  artifacts:
    - path: "R/36_tiered_same_day_payer.R"
      provides: "8-tier resolution hierarchy with Other govt at position 4"
      contains: "\"Other govt\" = 4L"
  key_links:
    - from: "TIER_MAPPING"
      to: "CODE_TO_TIER"
      via: "CODE_TO_TIER returns 'Other govt' which has rank 4 in TIER_MAPPING"
      pattern: "Other govt.*~.*Other govt"
    - from: "CODE_TO_TIER"
      to: "tier_rank assignment"
      via: "unlist(TIER_MAPPING[tier]) resolves 'Other govt' to rank 4"
      pattern: "unlist\\(TIER_MAPPING"
---

<objective>
Promote "Other govt" from a collapsed category into its own distinct tier in the same-day payer resolution hierarchy within R/36_tiered_same_day_payer.R.

Purpose: The AMC 8-category system already defines "Other govt" as a standard category (VA, TRICARE, state agencies, corrections), but the CODE_TO_TIER function collapses it into generic "Other", losing the distinction. This phase aligns resolution logic with the AMC system.

Output: Updated R/36_tiered_same_day_payer.R with 8-tier hierarchy (Medicaid > Medicare > Private > Other Govt > Other > Self-pay > Uninsured > Missing). Same 12 CSV output files, now showing "Other govt" as a distinct resolved_payer value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-add-an-other-govt-tier-to-the-tiered-payer-variable/37-CONTEXT.md
@.planning/phases/37-add-an-other-govt-tier-to-the-tiered-payer-variable/37-RESEARCH.md

<interfaces>
<!-- Key structures in R/36_tiered_same_day_payer.R that the executor modifies -->

From R/36_tiered_same_day_payer.R lines 75-83 (TIER_MAPPING - current state):
```r
TIER_MAPPING <- list(
  Medicaid   = 1L,
  Medicare   = 2L,
  Private    = 3L,
  Other      = 4L,
  "Self-pay" = 5L,
  Uninsured  = 6L,
  Missing    = 7L
)
```

From R/36_tiered_same_day_payer.R lines 87-100 (CODE_TO_TIER - current state):
```r
CODE_TO_TIER <- function(payer_category) {
  case_when(
    payer_category == "Medicaid"  ~ "Medicaid",
    payer_category == "Medicare"  ~ "Medicare",
    payer_category == "Private"   ~ "Private",
    payer_category == "Other govt" ~ "Other",     # <-- COLLAPSES
    payer_category == "Other"     ~ "Other",
    payer_category == "Self-pay"  ~ "Self-pay",
    payer_category == "Uninsured" ~ "Uninsured",
    payer_category == "Missing"   ~ "Missing",
    is.na(payer_category)         ~ "Missing",
    TRUE ~ "Missing"
  )
}
```

From R/36_tiered_same_day_payer.R line 168 (safety net - current state):
```r
tier_rank = if_else(is.na(tier_rank), 7L, tier_rank)
```

From R/36_tiered_same_day_payer.R line 13 (header comment - current state):
```r
#    Medicaid > Medicare > Private > Other > Self-pay > Uninsured > Missing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand tier hierarchy from 7 to 8 tiers with Other Govt at position 4</name>
  <files>R/36_tiered_same_day_payer.R</files>
  <read_first>
    - R/36_tiered_same_day_payer.R (the sole file being modified — read full current state)
    - R/00_config.R (read-only reference for AMC_PAYER_LOOKUP confirming "Other govt" as a standard category)
  </read_first>
  <action>
Make the following specific changes to R/36_tiered_same_day_payer.R per D-01, D-05:

**Change 1: Header comment (line 13)**
Replace:
```
#    Medicaid > Medicare > Private > Other > Self-pay > Uninsured > Missing
```
With:
```
#    Medicaid > Medicare > Private > Other govt > Other > Self-pay > Uninsured > Missing
```

**Change 2: TIER_MAPPING list (lines 75-83)**
Replace the entire TIER_MAPPING block with:
```r
TIER_MAPPING <- list(
  Medicaid     = 1L,  # Highest priority (includes dual-eligible, codes 93/14, FLM source)
  Medicare     = 2L,
  Private      = 3L,
  "Other govt" = 4L,  # VA, TRICARE, state agencies, corrections
  Other        = 5L,  # Generic other (worker's comp, auto insurance, etc.)
  "Self-pay"   = 6L,
  Uninsured    = 7L,
  Missing      = 8L   # Lowest priority
)
```

**Change 3: Comment above CODE_TO_TIER (line 85-86)**
Replace:
```r
# Map the AMC 8-category payer scheme to the 7 tiers
# AMC categories already align closely with tiers; "Other govt" collapses to "Other"
```
With:
```r
# Map the AMC 8-category payer scheme to the 8 resolution tiers
# AMC categories now align 1:1 with tiers (no collapsing)
```

**Change 4: CODE_TO_TIER function (line 92)**
Change:
```r
    payer_category == "Other govt" ~ "Other",
```
To:
```r
    payer_category == "Other govt" ~ "Other govt",
```

**Change 5: Safety net tier_rank (line 168)**
Change:
```r
    tier_rank = if_else(is.na(tier_rank), 7L, tier_rank)
```
To:
```r
    tier_rank = if_else(is.na(tier_rank), 8L, tier_rank)
```

**Do NOT modify:** R/00_config.R, R/02_harmonize_payer.R, or any other file (per D-06). Do NOT add any new output files (per D-03). Do NOT change output CSV filenames or column structure (per D-03).
  </action>
  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && Rscript -e "source('R/36_tiered_same_day_payer.R')" 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - R/36_tiered_same_day_payer.R line 13 contains "Medicaid > Medicare > Private > Other govt > Other > Self-pay > Uninsured > Missing"
    - TIER_MAPPING contains exactly 8 entries: `"Other govt" = 4L` is present, `Other = 5L`, `"Self-pay" = 6L`, `Uninsured = 7L`, `Missing = 8L`
    - CODE_TO_TIER contains `payer_category == "Other govt" ~ "Other govt"` (not `~ "Other"`)
    - Safety net line contains `8L` (not `7L`): `if_else(is.na(tier_rank), 8L, tier_rank)`
    - Comment above CODE_TO_TIER says "8 resolution tiers" (not "7 tiers")
    - No changes to any file other than R/36_tiered_same_day_payer.R
    - Script sources without R parse errors (Rscript -e "parse(file='R/36_tiered_same_day_payer.R')" exits 0)
  </acceptance_criteria>
  <done>R/36_tiered_same_day_payer.R has an 8-tier resolution hierarchy with "Other govt" at position 4 (priority between Private and Other), CODE_TO_TIER preserves "Other govt" as a distinct tier, safety net uses 8L, and the script parses without errors.</done>
</task>

</tasks>

<verification>
1. Parse check: `Rscript -e "parse(file='R/36_tiered_same_day_payer.R')"` exits without error
2. Grep checks:
   - `grep "Other govt.*= 4L" R/36_tiered_same_day_payer.R` returns a match
   - `grep "Other govt.*~.*Other govt" R/36_tiered_same_day_payer.R` returns a match (CODE_TO_TIER preserves category)
   - `grep "8L" R/36_tiered_same_day_payer.R` shows safety net updated
   - `grep "7 tiers" R/36_tiered_same_day_payer.R` returns NO matches (old comment removed)
3. Full execution: `Rscript -e "source('R/36_tiered_same_day_payer.R')"` runs to completion (requires HiPerGator data access — checkpoint:human-verify at UAT time)
</verification>

<success_criteria>
- R/36_tiered_same_day_payer.R modified with 8-tier hierarchy
- "Other govt" is position 4 between Private (3) and Other (5) per D-01
- CODE_TO_TIER no longer collapses "Other govt" into "Other"
- Safety net uses rank 8 (new max) instead of 7
- No other files modified per D-05, D-06
- Script parses without R errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-add-an-other-govt-tier-to-the-tiered-payer-variable/37-01-SUMMARY.md`
</output>
