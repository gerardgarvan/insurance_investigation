---
phase: 42-treatment-codes-resolved-xlsx-all-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/42_treatment_codes_resolved.R
  - radiation_codes_resolved.xlsx
  - sct_codes_resolved.xlsx
  - immunotherapy_codes_resolved.xlsx
  - supportive_care_codes_resolved.xlsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "radiation_codes_resolved.xlsx exists with Radiation Codes data sheet + Notes sheet"
    - "sct_codes_resolved.xlsx exists with SCT Codes data sheet + Notes sheet"
    - "immunotherapy_codes_resolved.xlsx exists with Immunotherapy Codes data sheet + Notes sheet"
    - "supportive_care_codes_resolved.xlsx exists with Supportive Care Codes data sheet + Notes sheet"
    - "Each resolved xlsx has columns: Code, Meaning, Code Type, Source Table, Records, Patients"
    - "Chemotherapy verification passes: 203 codes match between chemotherapy_codes_resolved.xlsx and combined report Chemotherapy sheet"
  artifacts:
    - path: "R/42_treatment_codes_resolved.R"
      provides: "Per-type resolved xlsx generation + chemo verification"
      min_lines: 100
    - path: "radiation_codes_resolved.xlsx"
      provides: "Radiation treatment codes resolved file"
    - path: "sct_codes_resolved.xlsx"
      provides: "SCT treatment codes resolved file"
    - path: "immunotherapy_codes_resolved.xlsx"
      provides: "Immunotherapy treatment codes resolved file"
    - path: "supportive_care_codes_resolved.xlsx"
      provides: "Supportive Care treatment codes resolved file"
  key_links:
    - from: "R/42_treatment_codes_resolved.R"
      to: "combined_unmatched_report.xlsx"
      via: "openxlsx2 read_xlsx() per category sheet"
      pattern: "read_xlsx.*combined_unmatched_report"
    - from: "R/42_treatment_codes_resolved.R"
      to: "chemotherapy_codes_resolved.xlsx"
      via: "openxlsx2 wb_load() for verification comparison"
      pattern: "wb_load.*chemotherapy_codes_resolved"
    - from: "R/42_treatment_codes_resolved.R"
      to: "write_resolved_xlsx function"
      via: "reusable function called per category"
      pattern: "write_resolved_xlsx"
---

<objective>
Create per-treatment-type resolved xlsx files (Radiation, SCT, Immunotherapy, Supportive Care) from combined_unmatched_report.xlsx, mirroring the chemotherapy_codes_resolved.xlsx format. Verify chemotherapy_codes_resolved.xlsx accuracy against the combined report.

Purpose: Extends the chemotherapy resolved report to all treatment categories, providing per-type reference files for clinical code review.
Output: 4 new resolved xlsx files + R script + chemotherapy verification results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-treatment-codes-resolved-xlsx-all-types/42-CONTEXT.md
@.planning/phases/42-treatment-codes-resolved-xlsx-all-types/42-RESEARCH.md

@R/41_combine_reports.R
@chemotherapy_codes_resolved.xlsx
@combined_unmatched_report.xlsx

<interfaces>
<!-- Key patterns from R/41_combine_reports.R that executor must replicate/adapt -->

Color scheme (lines 30-37):
```r
TREATMENT_TYPE_COLORS <- list(
  Chemotherapy      = list(fill = "FFDCEEFB", font = "FF0B5394"),   # light blue / dark blue
  Radiation         = list(fill = "FFDDF4E1", font = "FF274E13"),   # light green / dark green
  SCT               = list(fill = "FFFFF4D6", font = "FF7F6000"),   # light yellow / dark olive
  Immunotherapy     = list(fill = "FFE8DCF4", font = "FF4C1D7A"),   # light purple / dark purple
  `Supportive Care` = list(fill = "FFD5F5F0", font = "FF0E6655"),   # light teal / dark teal
  Unrelated         = list(fill = "FFF3F4F6", font = "FF6B7280")    # light gray / medium gray
)
```

Combined report sheet columns (from lines 326-334):
```r
# Per-category sheets have columns:
# code, description (renamed from drug_name for NDC), code_type, source_table,
# n_records, n_patients, lookup_status
# Data written starting row 5 with headers at row 4
```

Bulk write pattern (line 336):
```r
wb$add_data(sheet = sheet_name, x = write_df, start_row = 5, col_names = FALSE)
```

Chemotherapy resolved xlsx structure (from 42-CONTEXT.md):
- Sheet 1: "Chemotherapy Codes" -- title row 1, headers row 2, data row 3+
- Sheet 2: "Notes" -- provenance documentation
- Columns: Code, Meaning, Code Type, Source Table, Records, Patients
- 203 rows of data
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/42_treatment_codes_resolved.R script</name>
  <files>R/42_treatment_codes_resolved.R</files>
  <read_first>
    R/41_combine_reports.R
    R/00_config.R
    .planning/phases/42-treatment-codes-resolved-xlsx-all-types/42-CONTEXT.md
    .planning/phases/42-treatment-codes-resolved-xlsx-all-types/42-RESEARCH.md
  </read_first>
  <action>
Create R/42_treatment_codes_resolved.R with the following structure. This is a single script that produces all 4 resolved xlsx files and runs chemotherapy verification. Use openxlsx2 (not openxlsx).

**SECTION 1: SETUP AND CONFIGURATION**

```r
suppressPackageStartupMessages({
  library(dplyr)
  library(glue)
  library(openxlsx2)
})
```

Define paths:
- `COMBINED_REPORT` = `"combined_unmatched_report.xlsx"` (project root)
- `CHEMO_RESOLVED` = `"chemotherapy_codes_resolved.xlsx"` (project root)

Define the 4 categories to produce resolved files for:
```r
RESOLVE_CATEGORIES <- list(
  list(category = "Radiation",        sheet = "Radiation",        output = "radiation_codes_resolved.xlsx"),
  list(category = "SCT",              sheet = "SCT",              output = "sct_codes_resolved.xlsx"),
  list(category = "Immunotherapy",    sheet = "Immunotherapy",    output = "immunotherapy_codes_resolved.xlsx"),
  list(category = "Supportive Care",  sheet = "Supportive Care",  output = "supportive_care_codes_resolved.xlsx")
)
```

Copy the TREATMENT_TYPE_COLORS list from R/41_combine_reports.R (lines 30-37) exactly -- the same color scheme per D-01 (mirror format).

**SECTION 2: write_resolved_xlsx() FUNCTION**

Create a reusable function `write_resolved_xlsx(df, category, output_path)` that:

1. Creates a new workbook via `wb_workbook()`
2. Adds data sheet named `paste(category, "Codes")` (e.g., "Radiation Codes", "SCT Codes", "Immunotherapy Codes", "Supportive Care Codes") per D-01
3. Row 1: Title with code count -- `glue("{category} Codes ({n_codes} codes)")`, Calibri 16pt bold, color FF1F2937, merged across A1:F1
4. Row 2: Headers -- `c("Code", "Meaning", "Code Type", "Source Table", "Records", "Patients")` per D-03. Dark background (FF374151), white bold text (FFFFFFFF), Calibri 11pt
5. Row 3+: Bulk data write using `wb$add_data(x = write_df, start_row = 3, col_names = FALSE)` where write_df is:
   ```r
   write_df <- data.frame(
     Code = df$code,
     Meaning = ifelse(is.na(df$description), "", df$description),  # per D-05: API descriptions as-is, renamed to "Meaning"
     Code_Type = df$code_type,
     Source_Table = df$source_table,
     Records = df$n_records,
     Patients = df$n_patients,
     stringsAsFactors = FALSE
   )
   ```
6. Code column (A3:A{last_row}) styling: category fill color + bold font color from TREATMENT_TYPE_COLORS[[category]]
7. Number formatting: `"#,##0"` on columns E-F (Records, Patients) from row 3 to last_row
8. Column widths: `c(15, 45, 12, 15, 10, 10)` for cols 1:6
9. Add "Notes" sheet per D-04 with content:
   ```
   Data Source: combined_unmatched_report.xlsx (Phase 41)
   Descriptions: NLM/RxNorm API lookups via Phase 39-40
   Generated: {Sys.Date()}
   Classification: {category} codes
   ```
10. Save workbook to output_path
11. Print message: `"Wrote {output_path} ({n_codes} codes)"`

**SECTION 3: verify_chemotherapy() FUNCTION**

Create `verify_chemotherapy()` that performs verification per D-07, D-08, D-09:

1. Load chemotherapy_codes_resolved.xlsx using `wb_load()` -- inspect `$sheet_names` to find the data sheet name (do NOT hardcode; use first non-"Notes" sheet)
2. Read data sheet using `wb_to_df()` -- IMPORTANT: the resolved file has a title row in row 1, so use `start_row = 2, col_names = TRUE` to skip the title and read headers from row 2
3. Read Chemotherapy sheet from combined_unmatched_report.xlsx using `read_xlsx()`
4. Compare row counts: source vs resolved. Print PASS/FAIL for 203-code match per D-07
5. Compare code sets using `setdiff()` in both directions. Print missing/extra codes if any
6. Join on code column and compare n_records vs Records, n_patients vs Patients per D-08. Print count of mismatches and show first 10 if any discrepancies exist
7. Return a list with `pass` (logical), `n_source`, `n_resolved`, `mismatches` (data frame or NULL)

**SECTION 4: MAIN EXECUTION**

```r
message("=== Phase 42: Treatment Codes Resolved XLSX (All Types) ===\n")

# Step 1: Verify source file exists
stopifnot(file.exists(COMBINED_REPORT))

# Step 2: Generate resolved xlsx for each non-chemo category
message("Step 1: Generating per-type resolved xlsx files...\n")
for (item in RESOLVE_CATEGORIES) {
  message(glue("  Reading {item$sheet} sheet from combined report..."))
  df <- read_xlsx(COMBINED_REPORT, sheet = item$sheet)
  message(glue("  Found {nrow(df)} {item$category} codes"))

  # Sort by n_patients descending (most clinically relevant first)
  df <- df %>% arrange(desc(n_patients))

  write_resolved_xlsx(df, item$category, item$output)
  message("")
}

# Step 3: Verify chemotherapy file
message("Step 2: Verifying chemotherapy_codes_resolved.xlsx...\n")
if (file.exists(CHEMO_RESOLVED)) {
  result <- verify_chemotherapy()
  if (result$pass) {
    message("\nChemotherapy verification: ALL CHECKS PASSED")
  } else {
    message("\nChemotherapy verification: ISSUES FOUND (see above)")
  }
} else {
  message(glue("  WARNING: {CHEMO_RESOLVED} not found, skipping verification"))
}

message("\n=== Phase 42 Complete ===")
```

**IMPORTANT NOTES:**
- The combined_unmatched_report.xlsx category sheets use lowercase column names (code, description, code_type, source_table, n_records, n_patients) from R/41_combine_reports.R. The write_df mapping handles the rename to title case per D-03/D-05.
- The chemotherapy_codes_resolved.xlsx may have different header names than the combined report -- use wb_to_df() with start_row to handle title row offset, and tolower() or explicit column name matching for verification joins.
- Handle edge case: if a category sheet is missing from combined report (Phase 41 skips empty categories at line 284), print warning and skip gracefully rather than crashing.
  </action>
  <verify>
    <automated>grep -c "write_resolved_xlsx" R/42_treatment_codes_resolved.R</automated>
  </verify>
  <acceptance_criteria>
    - R/42_treatment_codes_resolved.R exists with 100+ lines
    - File contains `library(openxlsx2)` (not openxlsx)
    - File contains `write_resolved_xlsx <- function` definition
    - File contains `verify_chemotherapy <- function` definition
    - File contains all 4 output filenames: `radiation_codes_resolved.xlsx`, `sct_codes_resolved.xlsx`, `immunotherapy_codes_resolved.xlsx`, `supportive_care_codes_resolved.xlsx`
    - File contains column headers per D-03: `"Code", "Meaning", "Code Type", "Source Table", "Records", "Patients"`
    - File contains Notes sheet creation per D-04: `wb$add_worksheet("Notes")`
    - File contains TREATMENT_TYPE_COLORS with all 5 categories (Chemotherapy, Radiation, SCT, Immunotherapy, Supportive Care)
    - File contains `description` to `Meaning` rename per D-05 (ifelse for NA handling)
    - File contains `wb_load` call for chemotherapy_codes_resolved.xlsx verification per D-07
    - File contains setdiff comparison for code set matching
    - File contains Records/Patients count comparison per D-08
    - File contains console output messages per D-09
    - R syntax valid: no unmatched braces/parens (grep count of opening equals closing)
  </acceptance_criteria>
  <done>
    R/42_treatment_codes_resolved.R is complete, contains write_resolved_xlsx() and verify_chemotherapy() functions, handles all 4 treatment categories (Radiation, SCT, Immunotherapy, Supportive Care per D-02/D-10), and implements chemotherapy verification (D-07/D-08/D-09).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify resolved xlsx files on HiPerGator</name>
  <files>radiation_codes_resolved.xlsx, sct_codes_resolved.xlsx, immunotherapy_codes_resolved.xlsx, supportive_care_codes_resolved.xlsx</files>
  <action>
    User runs R/42_treatment_codes_resolved.R on HiPerGator and verifies:
    (1) all 4 resolved xlsx files are generated,
    (2) each has correct 2-sheet structure (data + Notes),
    (3) chemotherapy verification passes with 203 matching codes and no count discrepancies.
  </action>
  <verify>User confirms console output shows all PASS results and xlsx files exist with correct structure.</verify>
  <done>User types "approved" confirming all 4 resolved xlsx files generated correctly and chemotherapy verification passed.</done>
  <what-built>
    R/42_treatment_codes_resolved.R -- generates 4 resolved xlsx files (radiation, SCT, immunotherapy, supportive care) and verifies chemotherapy_codes_resolved.xlsx against combined report.
  </what-built>
  <how-to-verify>
    1. On HiPerGator, navigate to project directory
    2. Run: `Rscript R/42_treatment_codes_resolved.R`
    3. Verify console output shows:
       - Per-type file generation with code counts for each category
       - Chemotherapy verification results (PASS/FAIL for row count, code set, count match)
    4. Confirm 4 new xlsx files exist: radiation_codes_resolved.xlsx, sct_codes_resolved.xlsx, immunotherapy_codes_resolved.xlsx, supportive_care_codes_resolved.xlsx
    5. Open one resolved file (e.g., radiation_codes_resolved.xlsx) and verify:
       - Has 2 sheets (e.g., "Radiation Codes" + "Notes")
       - Data sheet has title row with code count, then headers (Code, Meaning, Code Type, Source Table, Records, Patients), then data rows
       - Code column has colored background matching the category color
       - Notes sheet documents source provenance
    6. Verify chemotherapy verification passed (203 codes match, no count discrepancies)
  </how-to-verify>
  <resume-signal>Type "approved" if all files generated correctly and verification passed, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- R/42_treatment_codes_resolved.R exists and is syntactically valid R
- Script reads from combined_unmatched_report.xlsx (Phase 41 output)
- Script produces 4 xlsx files: radiation_codes_resolved.xlsx, sct_codes_resolved.xlsx, immunotherapy_codes_resolved.xlsx, supportive_care_codes_resolved.xlsx
- Each xlsx has 2 sheets: "{Category} Codes" + "Notes" per D-01/D-04
- Each data sheet has columns: Code, Meaning, Code Type, Source Table, Records, Patients per D-03
- Chemotherapy verification compares 203 codes and flags mismatches per D-07/D-08/D-09
</verification>

<success_criteria>
- All 4 resolved xlsx files generated with correct structure and styling
- Chemotherapy verification passes (203 codes match, no count discrepancies)
- User confirms output on HiPerGator
</success_criteria>

<output>
After completion, create `.planning/phases/42-treatment-codes-resolved-xlsx-all-types/42-01-SUMMARY.md`
</output>
