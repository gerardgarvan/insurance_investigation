---
phase: 11-pptx-clarity-and-missing-data-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/11_generate_pptx.R
autonomous: true
requirements:
  - PPTX-01
  - PPTX-02
  - PPTX-05

must_haves:
  truths:
    - "PPTX tables show 'Missing' instead of Unknown, Unavailable, or Other payer categories"
    - "Every PPTX table has a Total row at the bottom"
    - "All slide titles, subtitles, and row labels are clear and unambiguous"
    - "POST_TREATMENT_PAYER NA values preserved as NA (not mapped to Missing) so N/A (No Follow-up) row works"
    - "Slide 15 filter updated from 'Unknown' to 'Missing' so it does not produce empty results"
  artifacts:
    - path: "R/11_generate_pptx.R"
      provides: "Updated PPTX generator with 6-category payer display and clear labels"
      contains: "Missing"
  key_links:
    - from: "rename_payer()"
      to: "PAYER_ORDER"
      via: "both must agree on 6 categories"
      pattern: "Missing"
    - from: "Slide 15 filter"
      to: "rename_payer()"
      via: "POST_TREATMENT_PAYER == 'Missing' after rename"
      pattern: 'POST_TREATMENT_PAYER == "Missing"'
---

<objective>
Consolidate payer categories from 9 to 6+Missing for PPTX display and audit all slide labels for clarity.

Purpose: Eliminate ambiguous labels (Unknown, Unavailable, Other) that confuse clinical audiences, ensuring every slide communicates unambiguously.
Output: Updated R/11_generate_pptx.R with consolidated payer categories and clear labels throughout.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pptx-clarity-and-missing-data-consolidation/11-CONTEXT.md
@.planning/phases/11-pptx-clarity-and-missing-data-consolidation/11-RESEARCH.md
@R/11_generate_pptx.R

<interfaces>
<!-- Key structures in 11_generate_pptx.R that this plan modifies -->

Lines 55-58 -- PAYER_ORDER (current 9-category):
```r
PAYER_ORDER <- c(
  "Medicare", "Medicaid", "Dual eligible", "Private",
  "Other government", "Self-pay", "Other", "Unavailable", "Unknown"
)
```

Lines 61-67 -- rename_payer() (current maps Self-pay, NA->Unknown):
```r
rename_payer <- function(x) {
  case_when(
    x == "No payment / Self-pay" ~ "Self-pay",
    is.na(x) ~ "Unknown",
    TRUE ~ x
  )
}
```

Lines 378-390 -- cohort_full payer rename (primary columns + POST_TREATMENT asymmetric):
```r
cohort_full <- cohort_full %>%
  mutate(
    across(
      c(PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX,
        PAYER_AT_CHEMO, PAYER_AT_RADIATION, PAYER_AT_SCT,
        PAYER_AT_LAST_CHEMO, PAYER_AT_LAST_RADIATION, PAYER_AT_LAST_SCT),
      rename_payer
    ),
    across(
      c(POST_TREATMENT_PAYER, POST_CHEMO_PAYER, POST_RAD_PAYER, POST_SCT_PAYER),
      ~ case_when(.x == "No payment / Self-pay" ~ "Self-pay", TRUE ~ .x)
    )
  )
```

Lines 440-475 -- build_enr_coverage_table() has hardcoded "N/A" label at line 464
Lines 502-512 -- build_treatment_enr_table() has hardcoded "N/A" label at line 502
Line 958 -- Slide 15 filter: `POST_TREATMENT_PAYER == "Unknown"`
Line 997-998 -- Slide 15 title/subtitle reference "Unknown"
Lines 1031-1032 -- Slide 16 calls rename_payer() on POST_TREATMENT_PAYER and PAYER_AT_LAST_TX
Line 1064 -- Slide 16 "N/A (No Payer Match)" label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate payer categories to 6+Missing and fix all labels</name>
  <files>R/11_generate_pptx.R</files>
  <action>
Update R/11_generate_pptx.R with the following changes. Read the full file first to confirm line numbers.

**1. Update PAYER_ORDER (lines 55-58):**
Replace 9-category list with 7-entry list:
```r
PAYER_ORDER <- c(
  "Medicare", "Medicaid", "Dual eligible", "Private",
  "Other government", "No payment / Self-pay", "Missing"
)
```

**2. Update rename_payer() (lines 61-67):**
Replace with:
```r
rename_payer <- function(x) {
  case_when(
    x %in% c("Other", "Unavailable", "Unknown") ~ "Missing",
    is.na(x)                                      ~ "Missing",
    TRUE ~ x
  )
}
```
Note: "No payment / Self-pay" is no longer renamed to "Self-pay" -- it keeps its full name per user decision. This matches PAYER_ORDER.

**3. Update POST_TREATMENT_PAYER rename block (lines 386-390):**
The asymmetric rename for POST_TREATMENT columns must collapse Other/Unavailable/Unknown to "Missing" while preserving NA as NA:
```r
across(
  c(POST_TREATMENT_PAYER, POST_CHEMO_PAYER, POST_RAD_PAYER, POST_SCT_PAYER),
  ~ case_when(
      .x %in% c("Other", "Unavailable", "Unknown") ~ "Missing",
      TRUE ~ .x  # preserves NA as NA, preserves all other values
    )
)
```

**4. Update Slide 15 filter (line 958):**
Change `POST_TREATMENT_PAYER == "Unknown"` to `POST_TREATMENT_PAYER == "Missing"`.

**5. Update Slide 15 title and subtitle (lines 997-998):**
- Title: Change "Unknown Post-Treatment Payer" to "Missing Post-Treatment Payer -- Encounter Breakdown"
- Subtitle: Change "Patients with Unknown post-treatment payer" to "Patients with Missing post-treatment payer: how many encounters exist after last treatment?"

**6. Update build_enr_coverage_table() N/A label (line 464):**
Change `"N/A"` to `"No Payer Assigned"`.

**7. Update build_treatment_enr_table() N/A label (line 502):**
Change `"N/A"` to `"No Payer Assigned"`.

**8. Update Slide 16 N/A label (line 1064):**
Change `"N/A (No Payer Match)"` to `"No Payer Assigned"`.

**9. Update Slide 16 rename_payer calls (lines 1031-1032):**
These lines call `rename_payer()` directly on POST_TREATMENT_PAYER and PAYER_AT_LAST_TX. After the rename_payer change, this will correctly map Unknown/Unavailable/Other to Missing. However, POST_TREATMENT_PAYER here should preserve NA for the N/A row logic below. Check whether `rename_payer()` is appropriate here or if the asymmetric pattern is needed. Since line 1031 feeds into the Slide 16 table which has an explicit NA row (line 1061), the NA branch in rename_payer (mapping NA to "Missing") would eliminate those NAs. Fix: use the asymmetric case_when for POST_TREATMENT_PAYER on line 1031:
```r
POST_TREATMENT_PAYER = case_when(
  POST_TREATMENT_PAYER %in% c("Other", "Unavailable", "Unknown") ~ "Missing",
  TRUE ~ POST_TREATMENT_PAYER
),
PAYER_AT_LAST_TX = rename_payer(PAYER_AT_LAST_TX)
```

**10. Audit all slide titles (Slides 1-16) for vague terminology.**
Slide 15 is the main fix (done above). Review all other slide titles in the add_table_slide() calls -- they should already be descriptive based on the file content, but verify none use bare "N/A" or "Unknown" without context. The "N/A (No Follow-up)" label on post-treatment slides MUST remain unchanged per user decision.

**11. Verify PPTX-02 (column totals):**
Confirm that every table builder function (build_payer_table, build_enr_coverage_table, build_treatment_enr_table, and inline tables for Slides 14-16) includes a Total row. Based on the code inspection, all already have Total rows. Log this confirmation in a code comment.
  </action>
  <verify>
    <automated>cd C:/Users/ggarv/OneDrive/Documents/insurance_investigation && Rscript -e "source('R/11_generate_pptx.R')" 2>&1 | head -50</automated>
  </verify>
  <done>
- PAYER_ORDER has 7 entries (6 categories + Missing)
- rename_payer() maps Other/Unavailable/Unknown/NA to "Missing"
- POST_TREATMENT_PAYER columns preserve NA as NA (not "Missing")
- Slide 15 filter uses "Missing" not "Unknown"
- All "N/A" bare labels replaced with "No Payer Assigned"
- "N/A (No Follow-up)" is untouched
- All 16 slide titles are clear and unambiguous
- All tables retain Total rows (PPTX-02 confirmed)
  </done>
</task>

</tasks>

<verification>
- Grep for "Unknown" in R/11_generate_pptx.R: should only appear in comments, not in PAYER_ORDER, rename_payer, or slide titles
- Grep for '"Unavailable"' in R/11_generate_pptx.R: should only appear inside rename_payer's mapping
- Grep for '"Other"' in R/11_generate_pptx.R: should only appear inside rename_payer's mapping (not in PAYER_ORDER)
- Grep for '"N/A"' (bare, not "N/A (No Follow-up)"): should return zero matches
- Grep for '"Missing"' in PAYER_ORDER and rename_payer: should be present
- Grep for "Total" in all table builder functions: should confirm Total rows exist
</verification>

<success_criteria>
- R/11_generate_pptx.R produces PPTX with 6+Missing payer categories instead of 9
- No bare "N/A", "Unknown", "Unavailable", or "Other" payer labels appear in generated slides
- "N/A (No Follow-up)" remains intact on post-treatment slides
- All tables have Total rows
- All slide titles are descriptive and unambiguous
</success_criteria>

<output>
After completion, create `.planning/phases/11-pptx-clarity-and-missing-data-consolidation/11-01-SUMMARY.md`
</output>
