---
phase: 11-pptx-clarity-and-missing-data-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - R/11_generate_pptx.R
autonomous: true
requirements:
  - PPTX-03
  - PPTX-04

must_haves:
  truths:
    - "PPTX contains a slide showing encounters per person by payer category (histogram)"
    - "PPTX contains a slide showing mean post-treatment encounters by DX year"
    - "PPTX contains a slide showing mean total encounters by DX year"
    - "PPTX contains a slide showing post-treatment encounter Yes/No by age group (0-17, 18-39, 40-64, 65+)"
    - "Missing PNG files produce a skip message, not an error"
  artifacts:
    - path: "R/11_generate_pptx.R"
      provides: "PPTX generator with 4 encounter analysis slides appended (Slides 17-20)"
      contains: "add_image_slide"
  key_links:
    - from: "R/11_generate_pptx.R"
      to: "output/figures/encounters_per_person_by_payor.png"
      via: "external_img() embedding"
      pattern: "encounters_per_person_by_payor"
    - from: "R/11_generate_pptx.R"
      to: "output/figures/post_tx_by_age_group.png"
      via: "external_img() embedding"
      pattern: "post_tx_by_age_group"
    - from: "R/16_encounter_analysis.R"
      to: "output/figures/*.png"
      via: "ggsave() produces PNGs consumed by 11_generate_pptx.R"
      pattern: "ggsave"
---

<objective>
Add 4 encounter analysis slides to the PPTX by embedding PNG figures from 16_encounter_analysis.R.

Purpose: Give clinical audiences encounter-level insights (by payer, DX year, age group) alongside the existing insurance tables.
Output: Updated R/11_generate_pptx.R with Slides 17-20 and slide count updated to 20.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pptx-clarity-and-missing-data-consolidation/11-CONTEXT.md
@.planning/phases/11-pptx-clarity-and-missing-data-consolidation/11-RESEARCH.md
@.planning/phases/11-pptx-clarity-and-missing-data-consolidation/11-01-SUMMARY.md
@R/11_generate_pptx.R
@R/16_encounter_analysis.R

<interfaces>
<!-- From 16_encounter_analysis.R -- PNG output paths consumed by new slides -->
output/figures/encounters_per_person_by_payor.png  (12x8 inches, 300 DPI)
output/figures/post_tx_encounters_by_dx_year.png   (10x6 inches, 300 DPI)
output/figures/total_encounters_by_dx_year.png     (10x6 inches, 300 DPI)
output/figures/post_tx_by_age_group.png            (8x6 inches, 300 DPI)

<!-- From 11_generate_pptx.R -- existing constants used by new slides -->
UF_BLUE    <- "#003087"
DARK_TEXT  <- "#333333"
N_TOTAL    (computed as nrow(hl_cohort) in SECTION 2)

<!-- officer API for image embedding -->
officer::external_img(src, width, height)
officer::ph_with(value, location)
officer::ph_location(left, top, width, height)
officer::add_slide(layout = "Blank")
officer::fpar() + officer::ftext() + officer::fp_text()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image slide helper and 4 encounter analysis slides</name>
  <files>R/11_generate_pptx.R</files>
  <action>
Read R/11_generate_pptx.R first to confirm current state (should have Plan 01 changes applied).

**1. Add `add_image_slide()` helper function** in SECTION 3 (Table Building Functions), after the existing helper functions and before the slide generation section. This function:
- Takes pptx, title, subtitle, img_path, img_width (default 8.5), img_height (default 4.2)
- Guards with `file.exists(img_path)` -- if missing, logs a message and returns pptx unchanged
- Creates a Blank slide with positioned title (UF_BLUE, 22pt bold Calibri), subtitle (DARK_TEXT, 12pt italic Calibri), and centered image

```r
add_image_slide <- function(pptx, title, subtitle, img_path,
                             img_width = 8.5, img_height = 4.2) {
  if (!file.exists(img_path)) {
    message(glue("  SKIPPED: {title} -- {img_path} not found. Run 16_encounter_analysis.R first."))
    return(pptx)
  }
  pptx %>%
    add_slide(layout = "Blank") %>%
    ph_with(
      value = fpar(ftext(title, prop = fp_text(font.size = 22, bold = TRUE,
                                               font.family = "Calibri", color = UF_BLUE))),
      location = ph_location(left = 0.5, top = 0.2, width = 9, height = 0.5)
    ) %>%
    ph_with(
      value = fpar(ftext(subtitle, prop = fp_text(font.size = 12, italic = TRUE,
                                                  font.family = "Calibri", color = DARK_TEXT))),
      location = ph_location(left = 0.5, top = 0.65, width = 9, height = 0.35)
    ) %>%
    ph_with(
      value = external_img(img_path, width = img_width, height = img_height),
      location = ph_location(left = (10 - img_width) / 2, top = 1.1,
                              width = img_width, height = img_height)
    )
}
```

**2. Add Slides 17-20** between the end of Slide 16 and the "SAVE PPTX" section. Insert a new subsection:

```r
# ==============================================================================
# SECTION 5b: ENCOUNTER ANALYSIS SLIDES (from 16_encounter_analysis.R figures)
# ==============================================================================

message("\n--- Encounter Analysis Slides ---")

# ---- Slide 17: Encounter histogram by payor ----
message("  Slide 17: Encounters per Person by Payer Category")
pptx <- add_image_slide(pptx,
  "Encounters per Person by Payer Category",
  glue("Distribution of total encounter counts by primary payer -- N = {format(N_TOTAL, big.mark=',')}"),
  "output/figures/encounters_per_person_by_payor.png",
  img_width = 9, img_height = 5.5
)

# ---- Slide 18: Post-treatment encounters by DX year ----
message("  Slide 18: Post-Treatment Encounters by DX Year")
pptx <- add_image_slide(pptx,
  "Mean Post-Treatment Encounters by Year of Diagnosis",
  "Non-acute care encounters per person after last treatment, stratified by HL diagnosis year",
  "output/figures/post_tx_encounters_by_dx_year.png"
)

# ---- Slide 19: Total encounters by DX year ----
message("  Slide 19: Total Encounters by DX Year")
pptx <- add_image_slide(pptx,
  "Mean Total Encounters by Year of Diagnosis",
  "All encounters per person across the full observation window, stratified by HL diagnosis year",
  "output/figures/total_encounters_by_dx_year.png"
)

# ---- Slide 20: Post-treatment encounters by age group ----
message("  Slide 20: Post-Treatment Encounters by Age Group")
pptx <- add_image_slide(pptx,
  "Post-Treatment Encounter Presence by Age Group at Diagnosis",
  "Proportion with any post-treatment encounter by age group (0-17, 18-39, 40-64, 65+)",
  "output/figures/post_tx_by_age_group.png"
)
```

**3. Update slide count** in the final log message (currently `message(glue("  Slides: 16"))`) to:
```r
message(glue("  Slides: 20 (16 tables + 4 encounter analysis)"))
```

**4. Update the file header comment** (lines 8-24) to add Slides 17-20 to the slide list:
```
#  17. Encounters per Person by Payer Category (histogram)
#  18. Mean Post-Treatment Encounters by Year of Diagnosis
#  19. Mean Total Encounters by Year of Diagnosis
#  20. Post-Treatment Encounter Presence by Age Group
```

**5. Add dependency note** to the header comment block (around line 27):
```
#   - 16_encounter_analysis.R should be run first to produce PNG figures
#     in output/figures/ (slides 17-20 will be skipped if PNGs are absent)
```
  </action>
  <verify>
    <automated>cd C:/Users/ggarv/OneDrive/Documents/insurance_investigation && Rscript -e "source('R/11_generate_pptx.R')" 2>&1 | tail -20</automated>
  </verify>
  <done>
- add_image_slide() helper function exists in R/11_generate_pptx.R
- Slides 17-20 added with descriptive titles and subtitles
- Each slide guarded with file.exists() check for its PNG
- Slide count message updated to 20
- Header comment updated with Slides 17-20 descriptions
- Dependency on 16_encounter_analysis.R documented in header
  </done>
</task>

</tasks>

<verification>
- Grep for "add_image_slide" in R/11_generate_pptx.R: function definition + 4 calls
- Grep for "Slide 17\|Slide 18\|Slide 19\|Slide 20" in R/11_generate_pptx.R: all 4 present
- Grep for "external_img" in R/11_generate_pptx.R: used within add_image_slide
- Grep for "file.exists" in R/11_generate_pptx.R: guard check in add_image_slide
- Grep for "Slides: 20" in R/11_generate_pptx.R: updated count
</verification>

<success_criteria>
- R/11_generate_pptx.R produces a 20-slide PPTX when encounter PNGs exist
- R/11_generate_pptx.R gracefully skips encounter slides with message when PNGs are absent
- Slides 17-20 have clear, descriptive titles matching the encounter analysis content
- No errors when running the script (even without PNG files present)
</success_criteria>

<output>
After completion, create `.planning/phases/11-pptx-clarity-and-missing-data-consolidation/11-02-SUMMARY.md`
</output>
