---
phase: 131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column
plan: 04
type: execute
wave: 3
depends_on:
  - "131-01"
  - "131-02"
  - "131-03"
files_modified:
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - SMOKE-131-01
user_setup: []

must_haves:
  truths:
    - "A user can run R/88 locally (structural mode) and see all Phase 131 checks pass, validating that R/50's MED_ADMIN/DISPENSING NDC generalization, per-code source tagging, and Medication column logic all exist in the source -- without needing DuckDB/openxlsx2 installed or HiPerGator access"
    - "The R/88 Section 16 summary total check count and SMOKE-131-01 line reflect the new section"
  artifacts:
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "New Section 15x: 10+ Phase 131 structural checks (get_chemo_hits return_source, R/50 DISPENSING query, dedup pattern, dynamic source_table, MEDICATION_LOOKUP col G wiring, fallback_normalize_medication, Medication column in both writers, SCT/Radiation gating) + SMOKE-131-01 summary line"
      contains: "SECTION 15x"
  key_links:
    - from: "R/88 Section 15x"
      to: "R/utils/utils_treatment.R + R/00_config.R + R/50_all_codes_resolved.R (source text)"
      via: "readLines()+paste()+grepl() structural checks, following the exact Section 15w/15t pattern (no live Rscript/data access required)"
      pattern: "SECTION 15x"
---

<objective>
Add a new R/88 structural smoke-test section validating everything built in 131-01/02/03: `get_chemo_hits()`'s source tagging, R/50's generalized MED_ADMIN/DISPENSING NDC detection and per-code source_table, the Medication column population/gating logic, and the shared xlsx-writer layout helper.

Purpose: R/88 today has zero content-level checks on R/50's actual output/logic (only a file-existence check, per RESEARCH.md's Pitfall 8) — this phase is the first to validate R/50's internals structurally, following the established grep-based convention used by every prior 100+ phase (Sections 15m through 15w).

Output: a new `SECTION 15x` block (next available letter after `15w`, Phase 130's DoI section) with 10+ `check()` calls, plus the corresponding `SMOKE-131-01` line appended to the Section 16 summary.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-CONTEXT.md
@.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-RESEARCH.md
@R/88_smoke_test_comprehensive.R
</context>

<interfaces>
<!-- R/88's established section shape, confirmed by direct read of Section 15t/15w this session -->

`check(description, condition)` helper (R/88, lines 51-59): increments global `passed`/`failed` counters, prints PASS/FAIL. Every new check MUST use this — never a bare `stopifnot()` or raw `if`.

Established section shape (e.g. Section 15t, lines 2587-2728; Section 15w, lines 2912-3017):
```r
message("\n[Phase N] <one-line description>...")

target_lines <- if (file.exists("R/target_file.R")) readLines("R/target_file.R", warn = FALSE) else character(0)
target_text  <- paste(target_lines, collapse = "\n")

check("<description> (Phase 131)", grepl("<pattern>", target_text))
# ... more checks ...

# IS_LOCAL-gated runtime block (optional; SKIPPED-with-TRUE when not applicable, to keep totals honest)
if (IS_LOCAL) {
  check("... -- SKIPPED (reason) (Phase 131, IS_LOCAL)", TRUE)
} else {
  check("... -- SKIPPED (HiPerGator runtime, not IS_LOCAL) (Phase 131)", TRUE)
}
```

R/88 section ordering (confirmed by grep this session): sections run `15m -> 15n -> 15o -> 15p -> 15q -> 15r -> 15s -> 15t -> 15u -> 15v -> 15w` (Phase 130, most recent), THEN an out-of-sequence `15g` (historical insertion anomaly near line 3017 — do not "fix" its position, just insert after it), then Section 30+. **Insert the new Section 15x immediately after Section 15w's closing (before the `SECTION 15g` header, ~line 3017)**, continuing the `15w -> 15x` alphabetical sequence exactly as Phase 130's SUMMARY did for `15v -> 15w`.

Section 16 summary block (R/88, lines 4461-4599): after all sections, prints total pass/fail, then a long list of `message("  * REQ-ID: description")` lines. The LAST such line today is:
```r
message("  * DOI-QA-01/02: R/39 + SCRIPT_INDEX registration and R/88 Section 15w DoI validation (Phase 130)")
```
Append the new `SMOKE-131-01` line immediately after it, following the exact style of prior entries (e.g. `SMOKE-130-01`, `SMOKE-124-01`).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Section 15x with 10+ Phase 131 structural checks</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <action>
Insert a new block titled `# SECTION 15x: MED_ADMIN/DISPENSING NDC GENERALIZATION + MEDICATION COLUMN (Phase 131)` immediately after Section 15w's content ends and before the (out-of-order) `SECTION 15g` header (~line 3017), following the exact shape of Section 15t/15w (message header, `readLines`+`paste` per target file, numbered `check()` calls).

Read source text from all 3 files this phase touches:
```r
utils_t_text_131 <- if (file.exists("R/utils/utils_treatment.R")) paste(readLines("R/utils/utils_treatment.R", warn = FALSE), collapse = "\n") else ""
config_text_131  <- if (file.exists("R/00_config.R")) paste(readLines("R/00_config.R", warn = FALSE), collapse = "\n") else ""
r50_text_131     <- if (file.exists("R/50_all_codes_resolved.R")) paste(readLines("R/50_all_codes_resolved.R", warn = FALSE), collapse = "\n") else ""
```

Write at least these checks (adapt exact `grepl()` patterns to whatever the actual 131-01/02/03 implementations produced — read the modified files first to confirm exact strings, following the Phase 120 precedent of adapting check patterns to the real code rather than the plan's illustrative snippets):

1. `get_chemo_hits` has a `return_source` parameter: `grepl("return_source", utils_t_text_131)`.
2. MED_ADMIN branch tags both RX and NDC sub-paths distinctly: `grepl("MED_ADMIN \\(RX\\)", utils_t_text_131) && grepl("MED_ADMIN \\(NDC\\)", utils_t_text_131)`.
3. R/50 now queries DISPENSING (net-new — R/50 never did before this phase): `grepl("get_chemo_hits\\(\"DISPENSING\"", r50_text_131)`.
4. R/50's RXNORM loop is generalized via a `code_type == "RXNORM"` filter (not hand-enumerated vector names). Do NOT use a `.*`-spanning pattern — `r50_text_131` is built with `collapse = "\n"`, and in R's `grepl()`, `.` does not match across a `\n`, so a pattern like `'code_type_map.*filter\\(...\\)'` can never match once the two tokens land on different lines (they will). Test for the filter call alone, allowing `.data$` and whitespace variation:
```r
check(
  "R/50 RXNORM loop filters code_type == RXNORM (Phase 131)",
  grepl('filter\\(\\s*(\\.data\\$)?code_type\\s*==\\s*"RXNORM"\\s*\\)', r50_text_131)
)
```
Do not OR this with a `str_detect(source_table, ...)` alternative — a check that cannot fail validates nothing.
5. Dedup guard against double-counting present: `grepl("distinct\\(ID, treatment_date", r50_text_131)`.
6. Dynamic per-code source_table coalescing present: `grepl("coalesce\\(dyn_source_table, static_source_table\\)", r50_text_131)`.
7. MEDICATION_LOOKUP's builder wires Supportive Care col G: `grepl('sheet_name == "Supportive Care"', config_text_131) && grepl("ncol\\(sheet_df\\)", config_text_131)`.
8. `fallback_normalize_medication()` exists and handles the HCPCS J-code pattern: `grepl("fallback_normalize_medication <- function", config_text_131) && grepl("\\^Injection,", config_text_131)`.
9. Fallback normalizer's multi-ingredient passthrough (" / " detection) present: `grepl('" / "', config_text_131)` (search near the `fallback_normalize_medication` definition specifically if this literal is ambiguous elsewhere in the file).
10. R/50's `all_codes_df` gains a `medication` column via case_when with Radiation/SCT gating: `grepl('category == "Radiation" ~ NA_character_', r50_text_131) && grepl('category == "SCT" & code_type != "RXNORM" ~ NA_character_', r50_text_131)`.
11. Shared xlsx-writer layout helper used by BOTH writers (exactly 2 call sites — the "stay in sync" guarantee). The helper is `resolved_xlsx_layout <- function(category)` (defined once) and called `resolved_xlsx_layout(category)` at both writer call sites — the definition line itself is not a substring match, so a correct count lands on exactly 2. Count with `fixed = TRUE`, and guard `gregexpr`'s zero-match case explicitly (it returns `-1` as a length-1 vector on zero matches, which a naive `length(...)  == 2` check will not catch as a failure of the right kind — a total failure to wire the helper must not be misread):
```r
layout_matches <- gregexpr("resolved_xlsx_layout(category)", r50_text_131, fixed = TRUE)[[1]]
n_layout_calls <- if (length(layout_matches) == 1 && layout_matches[1] == -1) 0L else length(layout_matches)
check(
  "resolved_xlsx_layout(category) called by both writers, 2 sites (Phase 131)",
  n_layout_calls == 2
)
```
The definition line `resolved_xlsx_layout <- function(category)` is not counted (not a substring match). If 131-03 abbreviated the argument at either call site (e.g. `resolved_xlsx_layout(cat)`), this returns 1 and fails — correct, because the "both writers derive from one helper" guarantee requires both literal call sites. Keep the argument literally `category` at both call sites in 131-03 so this check and 131-03's own `grep -c "resolved_xlsx_layout(category)"` (expecting 2) agree.
12. Radiation excluded from Medication column entirely (no all-blank column): `grepl("has_medication <- category != \"Radiation\"", r50_text_131)`.

Follow every prior section's convention: if a target file doesn't exist, guard all its checks to `FALSE` (fail loudly) rather than silently skipping — these are NOT optional runtime-only checks, they validate structural work that should already exist after 131-01/02/03 land.
  </action>
  <verify>
    <automated>grep -c "SECTION 15x" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <done>Section 15x exists with 10+ `check()` calls validating the return_source parameter, R/50's DISPENSING query and 4-vector generalization, the dedup guard, dynamic source_table coalescing, MEDICATION_LOOKUP's col G wiring, fallback_normalize_medication()'s HCPCS/multi-ingredient handling, the medication case_when gating, and the shared xlsx-writer layout helper's 2 call sites — confirmed by grep for the section header and a manual count of `check(` calls within it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SMOKE-131-01 summary line to Section 16</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <action>
In Section 16 (the summary block, ~line 4595), immediately after the existing line:
```r
message("  * DOI-QA-01/02: R/39 + SCRIPT_INDEX registration and R/88 Section 15w DoI validation (Phase 130)")
```
add:
```r
message("  * SMOKE-131-01: R/88 validates Phase 131 all_codes_resolved.xlsx MED_ADMIN/DISPENSING NDC generalization + Medication column structural integrity (Section 15x, 12 checks)")
```
(adjust the parenthetical check count to whatever Task 1 actually produced). Do not reorder or remove any existing summary lines.
  </action>
  <verify>
    <automated>grep -c "SMOKE-131-01" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <done>Section 16's summary block includes a `SMOKE-131-01` line describing Section 15x, appended after the Phase 130 line without disturbing any prior entries.</done>
</task>

</tasks>

<verification>
Structural (grep-based) verification only — matches every other phase's R/88 addition. Since `Rscript` in this dev environment lacks `glue`/`openxlsx2`/`duckdb` for a full run, confirm parse-safety manually (balanced braces/parens by inspection, or via a host where R packages ARE available) rather than actually executing `Rscript R/88_smoke_test_comprehensive.R`. On HiPerGator (or any environment with the full R package set), running `Rscript R/88_smoke_test_comprehensive.R` should show all Section 15x checks as PASS and the total check count increased by however many checks Task 1 added.

Confirm:
1. `SECTION 15x` appears exactly once, positioned after Section 15w and before the out-of-order Section 15g.
2. All Section 15x checks reference files this phase actually modified (R/utils/utils_treatment.R, R/00_config.R, R/50_all_codes_resolved.R) — no checks against unrelated files.
3. `SMOKE-131-01` appears exactly once in the Section 16 summary block.
</verification>

<success_criteria>
- Section 15x exists with 10+ structural checks covering every artifact built in 131-01/02/03.
- Section 16 summary reflects the new section via a SMOKE-131-01 line.
- No existing R/88 section or summary line is disturbed.
</success_criteria>

<output>
After completion, create `.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-04-SUMMARY.md`
</output>
