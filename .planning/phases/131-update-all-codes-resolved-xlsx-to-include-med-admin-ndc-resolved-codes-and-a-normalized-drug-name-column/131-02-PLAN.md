---
phase: 131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/utils/utils_treatment.R
  - R/50_all_codes_resolved.R
autonomous: true
requirements:
  - MEDXLSX-03
  - MEDXLSX-04
  - MEDXLSX-05
user_setup: []

must_haves:
  truths:
    - "Codes only reachable via MED_ADMIN with MEDADMIN_TYPE == 'ND' (NDC-typed) or via DISPENSING now contribute records/patients counts in R/50's output, across all 4 RXNORM-based vector categories (chemo_rxnorm, sct_rxnorm, immunotherapy_rxnorm, supportive_care_rxnorm) -- not chemo-only"
    - "The Source Table value for each RXNORM code reflects which table/type ACTUALLY matched it (e.g. 'MED_ADMIN (NDC)', 'DISPENSING (NDC)') rather than the old static 'PRESCRIBING|MED_ADMIN' string that every RXNORM vector carried regardless of real detection path"
    - "Records/Patients counts are not inflated when the same (ID, treatment_date) administration is reachable via more than one source path (e.g. both MED_ADMIN-RX and MED_ADMIN-ND, or PRESCRIBING and MED_ADMIN)"
    - "get_chemo_hits()'s 6 existing callers (R/10, R/11, R/25, R/26, R/76, R/109) are completely unaffected -- the new return_source parameter defaults to FALSE and changes nothing when omitted"
  artifacts:
    - path: "R/utils/utils_treatment.R"
      provides: "get_chemo_hits() gains an additive return_source parameter tagging PRESCRIBING / MED_ADMIN (RX) / MED_ADMIN (NDC) / DISPENSING (NDC) per matched row"
      contains: "return_source"
    - path: "R/50_all_codes_resolved.R"
      provides: "Section 3 RXNORM loop calls get_chemo_hits() for PRESCRIBING+MED_ADMIN+DISPENSING across all 4 RXNORM vectors with source tagging and de-duplication; Section 4 uses the per-code dynamic source_table instead of the static code_type_map string for RXNORM rows"
      contains: "get_chemo_hits(\"DISPENSING\""
  key_links:
    - from: "R/50 Section 3 RXNORM loop"
      to: "get_chemo_hits(table_name, codes, ndc_crosswalk, return_source = TRUE)"
      via: "one call per (RXNORM vector x {PRESCRIBING, MED_ADMIN, DISPENSING}) instead of R/50's old hand-rolled PRESCRIBING/MED_ADMIN-RX-only queries"
      pattern: "get_chemo_hits\\(.*return_source\\s*=\\s*TRUE"
    - from: "R/50 Section 3 per-code source aggregation"
      to: "R/50 Section 4 all_codes_df$source_table"
      via: "count_results carries a source_table column for RXNORM vectors; Section 4's left_join brings it in as dyn_source_table, coalesces it over the static code_type_map value, and the result survives an explicit select() before the final mutate"
      pattern: "select\\(code, description, records, patients, source_table\\)"
---

<objective>
Generalize R/50's MED_ADMIN/DISPENSING NDC-resolved code detection (the Phase 122 fix) across all 4 RXNORM-based treatment vector categories, and make the "Source Table" column reflect which table/type actually matched each code instead of a fixed per-vector label.

Purpose: R/50 today only queries PRESCRIBING and MED_ADMIN with `MEDADMIN_TYPE == "RX"` — it has never queried DISPENSING at all, and it has never resolved `MEDADMIN_TYPE == "ND"` (NDC-typed) rows via the Phase 122 crosswalk. This means codes only reachable through those paths are invisible in `all_codes_resolved.xlsx` today, even though 6 other consumers (R/10, R/11, R/25, R/26, R/76, R/109) were already fixed in Phase 122. This plan closes that "one remaining consumer" gap explicitly deferred at the end of Phase 122, and does so generically (not chemo-only) since R/50's RXNORM loop already iterates chemo_rxnorm, sct_rxnorm, immunotherapy_rxnorm, and supportive_care_rxnorm uniformly.

Output: `get_chemo_hits()` gains an additive, backward-compatible source-tagging parameter; R/50's Section 3 RXNORM aggregation is rewritten to use it across 3 tables x 4 vectors with correct de-duplication; Section 4's `all_codes_df` assembly carries the real per-code source label through instead of `code_type_map`'s static string.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-CONTEXT.md
@.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-RESEARCH.md
@R/utils/utils_treatment.R
@R/50_all_codes_resolved.R
</context>

<interfaces>
<!-- Confirmed by direct read of R/utils/utils_treatment.R and R/50_all_codes_resolved.R (current state, this session) -->

`get_chemo_hits()` current signature and behavior (R/utils/utils_treatment.R, lines 172-280):
```r
get_chemo_hits <- function(table_name, chemo_rxnorm, ndc_crosswalk = NULL,
                           return_raw_name = FALSE) {
  # table_name: "PRESCRIBING" | "DISPENSING" | "MED_ADMIN"
  # chemo_rxnorm: ANY character vector of RxNorm CUIs (generic despite the param name --
  #               already called with chemo/sct/immunotherapy/supportive_care vectors elsewhere)
  # Returns tibble(ID, treatment_date, triggering_code[, raw_med_name]) or NULL
  #
  # PRESCRIBING branch: filters RXNORM_CUI %in% chemo_rxnorm
  # DISPENSING branch:  filters NDC via ndc_crosswalk[normalize_ndc(NDC)] %in% chemo_rxnorm
  # MED_ADMIN branch:   rx_hits  = MEDADMIN_TYPE == "RX", MEDADMIN_CODE %in% chemo_rxnorm (direct)
  #                     nd_hits  = MEDADMIN_TYPE == "ND", NDC-crosswalk-resolved
  #                     result   = bind_rows(rx_hits, nd_hits)  <-- loses which sub-path matched
  # All 3 branches degrade gracefully (return NULL) if table/columns/crosswalk absent.
}
```
Every branch returns the match column as `triggering_code` (never `code`). R/50's new loop renames it to `code` exactly once after binding the three source results (Task 2 step 4). Task 3's `count_results` join key is therefore `code`.

6 existing callers (confirmed by direct grep this session — Phase 122 research listed 5 but missed R/109): R/10_cohort_predicates.R, R/11_treatment_payer.R, R/25_treatment_durations.R, R/26_treatment_episodes.R, R/76_treatment_source_coverage.R, R/109_med_admin_dispensing_fix_impact_audit.R — all call with 2-3 positional/named args, none pass `return_raw_name` as TRUE except select R/26 call sites. None will pass the new `return_source` parameter, so its default MUST be `FALSE` and produce byte-identical output to today when omitted.

R/50 Section 3 current RXNORM loop (R/50_all_codes_resolved.R, lines 315-372) — this ENTIRE loop body is replaced by this plan (see Task 2):
```r
rxnorm_vectors <- code_type_map %>% filter(str_detect(source_table, "PRESCRIBING\\|MED_ADMIN"))
for (i in seq_len(nrow(rxnorm_vectors))) {
  vec_name <- rxnorm_vectors$vector_name[i]
  codes <- TREATMENT_CODES[[vec_name]]
  if (is.null(codes) || length(codes) == 0) next
  presc_tbl <- safe_table("PRESCRIBING")
  presc_matches <- ... filter(RXNORM_CUI %in% codes) ...
  medadmin_tbl <- safe_table("MED_ADMIN")
  medadmin_matches <- ... filter(MEDADMIN_CODE %in% codes, MEDADMIN_TYPE == "RX") ...   # ND missing, DISPENSING never queried
  combined <- bind_rows(presc_matches, medadmin_matches)
  counts <- combined %>% group_by(code) %>% summarise(records = n(), patients = n_distinct(ID), .groups = "drop") %>% mutate(vector_name = vec_name)
  count_results <- bind_rows(count_results, counts)
}
```

R/50 `count_results` initialization (line 219-224) and Section 4 assembly loop (lines 417-463) — Task 3 modifies the `vec_df` construction inside the `for (i in seq_len(nrow(code_type_map)))` loop. **Confirmed by direct read of the CURRENT file this session** — the exact code at lines 427-463 today is:
```r
for (i in seq_len(nrow(code_type_map))) {
  vec_name <- code_type_map$vector_name[i]
  category <- code_type_map$category[i]
  code_type <- code_type_map$code_type[i]
  source_table <- code_type_map$source_table[i]

  codes <- TREATMENT_CODES[[vec_name]]
  if (is.null(codes) || length(codes) == 0) next

  # Create per-code rows
  vec_df <- tibble(code = codes) %>%
    # Look up counts
    left_join(
      count_results %>% filter(vector_name == vec_name) %>% select(code, records, patients),
      by = "code"
    ) %>%
    # Fill NA counts with 0
    mutate(
      records = if_else(is.na(records), 0L, records),
      patients = if_else(is.na(patients), 0L, patients)
    ) %>%
    # Look up descriptions (cascade: api > hardcoded > config)
    left_join(api_descriptions %>% rename(api_desc = description), by = "code") %>%
    left_join(hardcoded_desc_tbl %>% rename(hardcoded_desc = description), by = "code") %>%
    left_join(config_comments %>% rename(config_desc = description), by = "code") %>%
    mutate(
      description = coalesce(api_desc, hardcoded_desc, config_desc, "No description available")
    ) %>%
    select(code, description, records, patients) %>%
    mutate(
      code_type = code_type,
      source_table = source_table,
      category = category
    )

  all_codes_df <- bind_rows(all_codes_df, vec_df)
}
```
Note the CRITICAL trap: the `select(code, description, records, patients)` step (currently line 455) drops ANY column not named there — a naive coalesce inserted anywhere between the NA-fill and the final mutate will have its result column stripped by this select before the final mutate runs, unless that select's column list is ALSO updated to include the new column. Task 3 below shows the fully corrected pipe with this select fixed.

`code_type_map` tribble (R/50, lines 57-81): all 4 RXNORM rows currently declare `source_table = "PRESCRIBING|MED_ADMIN"` (a static string, not derived from matches). This plan does NOT change `code_type_map` itself — it changes how `all_codes_df$source_table` is populated downstream for RXNORM-vector rows (dynamic, from actual matches) while leaving non-RXNORM rows (PROCEDURES/ENCOUNTER-sourced) using the unchanged static value.

`load_ndc_crosswalk()` (utils_treatment.R, lines 136-147): returns a named character vector (NDC -> RxCUI) or `character(0)` if `data/reference/ndc_rxnorm_crosswalk.rds` is absent — degrades gracefully, never errors. Call this ONCE in R/50 Section 3 (not per-vector).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add additive return_source parameter to get_chemo_hits()</name>
  <files>R/utils/utils_treatment.R</files>
  <action>
Add a new parameter `return_source = FALSE` to `get_chemo_hits(table_name, chemo_rxnorm, ndc_crosswalk = NULL, return_raw_name = FALSE, return_source = FALSE)`. When `TRUE`, the returned tibble gains a `source` column tagging which sub-path produced each row:
- PRESCRIBING branch: `source = "PRESCRIBING"` for every row.
- DISPENSING branch: `source = "DISPENSING (NDC)"` for every row (this branch is NDC-only already).
- MED_ADMIN branch: `rx_hits` (the `MEDADMIN_TYPE == "RX"` path) get `source = "MED_ADMIN (RX)"`; `nd_hits` (the `MEDADMIN_TYPE == "ND"` crosswalk path) get `source = "MED_ADMIN (NDC)"`. Add the `source` mutate to each sub-branch BEFORE they are `bind_rows()`-ed together, mirroring exactly how `raw_med_name` is already added per-branch before combining.

When `return_source = FALSE` (the default), the return contract is UNCHANGED from today — do not add the `source` column at all (drop it at the end of each branch if present, exactly like the existing `if (!return_raw_name) result <- dplyr::select(result, ID, treatment_date, triggering_code)` pattern in the MED_ADMIN branch). This guarantees all 6 existing callers (R/10, R/11, R/25, R/26, R/76, R/109) see byte-identical behavior since none will pass this new parameter.

Update the function's roxygen-style comment block (`@param return_source`, `@return` clause) to document the new parameter and its column, following the exact style already used for `@param return_raw_name`.
  </action>
  <verify>
    <automated>grep -c "return_source = FALSE" R/utils/utils_treatment.R</automated>
  </verify>
  <done>`get_chemo_hits()` accepts `return_source` (default FALSE), tags PRESCRIBING/MED_ADMIN(RX)/MED_ADMIN(NDC)/DISPENSING(NDC) rows correctly when TRUE, and produces the exact same 3-column return shape as before when omitted — confirmed by grep showing the new parameter default and per-branch `source =` mutations, with no change to the function's behavior when `return_source` is not passed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite R/50 Section 3 RXNORM loop to use get_chemo_hits() across PRESCRIBING/MED_ADMIN/DISPENSING for all 4 vectors, with dedup</name>
  <files>R/50_all_codes_resolved.R</files>
  <action>
Replace the entire RXNORM aggregation block (lines ~315-372: from `rxnorm_vectors <- code_type_map %>% filter(...)` through the closing `}` of its `for` loop) with a rewrite that:

1. Loads the NDC crosswalk ONCE before the loop: `ndc_crosswalk <- load_ndc_crosswalk()` (from `R/utils/utils_treatment.R`, already auto-sourced via R/00_config.R's utils glob).
2. Selects the four RXNORM vectors by filtering `code_type_map` with `filter(code_type == "RXNORM")` — use this exact literal. Do NOT hand-enumerate vector names, and do NOT retain the old `str_detect(source_table, "PRESCRIBING\\|MED_ADMIN")` selection form; 131-04 Section 15x Check 4 greps for `filter(code_type == "RXNORM")` specifically. This naturally covers chemo_rxnorm, sct_rxnorm, immunotherapy_rxnorm, supportive_care_rxnorm.
3. For each RXNORM vector, calls `get_chemo_hits()` three times — `"PRESCRIBING"`, `"MED_ADMIN"`, `"DISPENSING"` — each with `codes`, `ndc_crosswalk`, and `return_source = TRUE`.
4. Binds the three per-source results, then establishes a single canonical `code` column up front so every downstream step and the Task 3 join agree on it: `all_hits <- bind_rows(...) %>% rename(code = triggering_code)`. Do the rename ONCE, immediately after the bind — not per-branch. `get_chemo_hits()` always returns `triggering_code`, so one rename covers all three source calls. If any of the three results is NULL, `bind_rows()` drops it and the rename still applies to whatever columns survive.
5. De-duplicates the source-labelled set once, for the source aggregation only: `hits_by_source <- all_hits %>% distinct(ID, treatment_date, code, source)`.
6. Computes `records`/`patients` from a SEPARATE, source-agnostic de-duplication, so an administration reachable via two paths is counted once: `hits_dedup <- all_hits %>% distinct(ID, treatment_date, code)`, then `counts <- hits_dedup %>% group_by(code) %>% summarise(records = n(), patients = n_distinct(ID), .groups = "drop")`. This is the exact double-counting risk Phase 122 documented for its 6 other consumers — R/50 must not reintroduce it.

   **Behavioral change — document in the SUMMARY.** `records` now counts distinct `(ID, treatment_date, code)` administrations. The OLD loop counted raw joined rows (`group_by(code) %>% summarise(records = n())`, no `distinct()`). For existing single-source codes that had multiple same-day rows, the reported Records number will DROP relative to prior runs. This is the intended Pitfall-2 de-duplication fix, but because `all_codes_resolved.xlsx` is shared with collaborators, the change to existing numbers must be stated explicitly in the SUMMARY — not framed only as "new codes appear."
7. Computes the per-code source label from `hits_by_source`: `source_labels <- hits_by_source %>% group_by(code) %>% summarise(source_table = paste(sort(unique(source)), collapse = ", "), .groups = "drop")` — so a code seen via both PRESCRIBING and MED_ADMIN (RX) shows `"MED_ADMIN (RX), PRESCRIBING"` (alphabetical via `sort()`), while a code ONLY reachable via the new NDC paths shows e.g. `"DISPENSING (NDC)"` or `"MED_ADMIN (NDC)"` alone. Joins `source_labels` onto `counts` by `code`, then `mutate(vector_name = vec_name)`. The resulting per-vector frame (`code, records, patients, source_table, vector_name`) is `bind_rows()`-ed into `count_results` (`count_results`'s initial tibble at line 219-224 needs a `source_table = character()` column added so the PROCEDURES/ENCOUNTER blocks' bind_rows calls, which don't set `source_table`, still work via NA-filling).

Keep the existing `message(glue(...))` progress logging style (one line per vector showing code count), and keep the same graceful-degradation posture as the code being replaced (if `codes` is NULL/empty, `next`; if a table is missing, `get_chemo_hits()` already returns NULL and `bind_rows()` handles NULLs transparently).
  </action>
  <verify>
    <automated>grep -c "get_chemo_hits(\"DISPENSING\"" R/50_all_codes_resolved.R</automated>
  </verify>
  <done>R/50 Section 3's RXNORM loop calls `get_chemo_hits()` for PRESCRIBING, MED_ADMIN, and DISPENSING (with `return_source = TRUE`) for every RXNORM-type vector in `code_type_map` (all 4, filtered generically by `code_type == "RXNORM"`, not hand-enumerated); `count_results` gains a `source_table` column carrying the real per-code aggregated source label; records/patients are computed from a source-agnostic de-duplication to avoid double-counting administrations reachable via multiple paths.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire the dynamic per-code source_table into Section 4's all_codes_df assembly</name>
  <files>R/50_all_codes_resolved.R</files>
  <action>
In Section 4's per-vector assembly loop (lines ~427-463), rename the existing local variable `source_table <- code_type_map$source_table[i]` (line 431) to `static_source_table` to avoid ambiguity with the new dynamic value.

**CRITICAL — the existing `select(code, description, records, patients)` step (currently line 455) drops any column not in its explicit list. A coalesce inserted anywhere in this pipe will be silently stripped before the final mutate unless that select's column list is ALSO updated to include the new column.** Replace the ENTIRE `vec_df <- tibble(code = codes) %>% ... ` pipe (lines 437-460) with this fully corrected version — every step shown, not a diff:

```r
vec_df <- tibble(code = codes) %>%
  # Look up counts AND the per-code dynamic source (added in Task 2); rename
  # count_results$source_table to dyn_source_table so it survives the join
  # without colliding with the static_source_table variable above.
  left_join(
    count_results %>%
      filter(vector_name == vec_name) %>%
      select(code, records, patients, dyn_source_table = source_table),
    by = "code"
  ) %>%
  # Fill NA counts with 0
  mutate(
    records = if_else(is.na(records), 0L, records),
    patients = if_else(is.na(patients), 0L, patients)
  ) %>%
  # Look up descriptions (cascade: api > hardcoded > config)
  left_join(api_descriptions %>% rename(api_desc = description), by = "code") %>%
  left_join(hardcoded_desc_tbl %>% rename(hardcoded_desc = description), by = "code") %>%
  left_join(config_comments %>% rename(config_desc = description), by = "code") %>%
  mutate(
    description = coalesce(api_desc, hardcoded_desc, config_desc, "No description available"),
    # Dynamic per-code source (from actual matches, Task 2) wins when present;
    # falls back to the static code_type_map value for PROCEDURES/ENCOUNTER
    # categories where count_results never sets source_table (dyn_source_table
    # is NA for every row there).
    source_table = dplyr::coalesce(dyn_source_table, static_source_table)
  ) %>%
  # source_table is included here so it is NOT dropped by this select --
  # this is the exact step that silently strips unlisted columns.
  select(code, description, records, patients, source_table) %>%
  mutate(
    code_type = code_type,
    category = category
    # NOTE: do NOT reassign source_table here -- it already carries the
    # coalesced value through the select() above. Reassigning it to the
    # static_source_table variable at this point would silently clobber
    # the dynamic value for every RXNORM row.
  )
```

Do not change anything for non-RXNORM vectors' behavior — since `count_results` never populates `source_table` for PROCEDURES/ENCOUNTER-derived rows, `dyn_source_table` will be `NA` for them and `coalesce()` correctly falls through to `static_source_table`, identical to today's output.
  </action>
  <verify>
    <automated>grep -c "coalesce(dyn_source_table, static_source_table)" R/50_all_codes_resolved.R && grep -c "select(code, description, records, patients, source_table)" R/50_all_codes_resolved.R</automated>
  </verify>
  <done>`all_codes_df$source_table` reflects the real per-code detected source for RXNORM-vector codes (e.g. distinguishing "MED_ADMIN (NDC)"-only codes from "PRESCRIBING"-reachable codes) while PROCEDURES/ENCOUNTER-sourced categories keep their unchanged static `code_type_map` value — confirmed by grep for (a) the coalesce call, AND (b) `source_table` appearing in the `select()` column list that immediately precedes the final `mutate(code_type = ..., category = ...)`, proving the coalesced value survives to the output row rather than being stripped by that select or clobbered by a redundant reassignment in the final mutate.</done>
</task>

</tasks>

<verification>
All three tasks are verified structurally (grep) since `openxlsx2`/`duckdb`/`here` are not installed in this dev environment (confirmed via `Rscript -e 'installed.packages()'`), matching the project's established dual-environment convention (structural verify on Windows, runtime confirm on HiPerGator).

Additional manual sanity check (no R needed): re-read the modified Section 3 and Section 4 blocks and confirm:
1. `ndc_crosswalk <- load_ndc_crosswalk()` appears exactly once, before the RXNORM loop (not inside it — avoid reloading the crosswalk per vector).
2. The 6 existing `get_chemo_hits()` callers outside R/50 (R/10, R/11, R/25, R/26, R/76, R/109) are untouched by this plan (`files_modified` for this plan is `R/utils/utils_treatment.R` + `R/50_all_codes_resolved.R` only).
3. `count_results`'s initial tibble definition (line ~219) includes the new `source_table = character()` column.
4. The `select(code, description, records, patients, source_table)` step in Section 4's `vec_df` pipe includes `source_table` explicitly, and the final `mutate()` after it does NOT reassign `source_table` (only `code_type` and `category`) — this is the specific defect this revision fixes.
</verification>

<success_criteria>
- `get_chemo_hits()` supports source tagging additively; all 6 non-R/50 callers are unaffected.
- R/50's RXNORM loop queries PRESCRIBING + MED_ADMIN (RX and ND) + DISPENSING for all 4 RXNORM vector categories generically.
- Records/Patients counts are deduplicated across overlapping source paths.
- `all_codes_df$source_table` is per-code-dynamic for RXNORM rows, unchanged-static for everything else, and actually survives the intervening `select()` step to reach the output.
</success_criteria>

<output>
After completion, create `.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-02-SUMMARY.md`

The summary MUST explicitly state: "Records-column values for existing single-source codes with multiple same-day rows will decrease relative to prior `all_codes_resolved.xlsx` runs, as a direct result of the `(ID, treatment_date, code)` de-duplication. Intended (Pitfall 2 fix), not a regression." Do not omit or soften this — the workbook is shared with collaborators and this is a visible number change, not just new codes appearing.
</output>
