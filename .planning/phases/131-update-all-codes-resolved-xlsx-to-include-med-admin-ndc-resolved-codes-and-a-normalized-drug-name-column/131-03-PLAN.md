---
phase: 131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column
plan: 03
type: execute
wave: 2
depends_on:
  - "131-01"
  - "131-02"
files_modified:
  - R/50_all_codes_resolved.R
autonomous: true
requirements:
  - MEDXLSX-06
  - MEDXLSX-07
user_setup: []

must_haves:
  truths:
    - "A user opening chemotherapy_codes_resolved.xlsx, supportive_care_codes_resolved.xlsx, immunotherapy_codes_resolved.xlsx, or sct_codes_resolved.xlsx (or the corresponding sheets in the combined all_codes_resolved.xlsx) sees a Medication column with a normalized drug name for every drug code -- never blank for a code that should have one, never raw truncated config-comment text"
    - "The SCT sheet shows a Medication value ONLY for Code Type == RXNORM rows; DRG/ICD-10-PCS/procedure rows show blank Medication"
    - "radiation_codes_resolved.xlsx (and the Radiation sheet in the combined workbook) has NO Medication column at all -- not even an all-blank one"
    - "The per-type xlsx files and the combined all_codes_resolved.xlsx show IDENTICAL Medication values for the same code -- both writers stay in sync, no divergence"
  artifacts:
    - path: "R/50_all_codes_resolved.R"
      provides: "Medication column computed in Section 4 (all_codes_df$medication); both write_resolved_xlsx() and the combined-workbook per-category loop write a Medication column (except Radiation) using a single shared layout so they cannot silently diverge"
      contains: "has_medication"
  key_links:
    - from: "R/50 Section 4 all_codes_df$medication"
      to: "MEDICATION_LOOKUP (primary) / fallback_normalize_medication() (fallback)"
      via: "case_when: Radiation -> NA; SCT & code_type != RXNORM -> NA; code %in% names(MEDICATION_LOOKUP) -> lookup; else -> fallback_normalize_medication(description, code_type)"
      pattern: "fallback_normalize_medication\\(description"
    - from: "write_resolved_xlsx() (per-type files)"
      to: "combined-workbook per-category loop"
      via: "both consume the same shared header/layout helper so the 5 per-type files and all_codes_resolved.xlsx never show different columns for the same category"
      pattern: "has_medication|resolved_xlsx_layout"
---

<objective>
Add the Medication column to Chemotherapy, Supportive Care, Immunotherapy, and SCT sheets in BOTH the 5 per-type xlsx files and the combined `all_codes_resolved.xlsx` — populated via `MEDICATION_LOOKUP` (primary) with `fallback_normalize_medication()` (from 131-01) as the fallback, gated correctly per category/code-type, and excluded entirely from Radiation.

Purpose: R/50 has zero Medication column today (confirmed this session — the "(already has it)" language in CONTEXT.md refers only to the reference Excel that feeds `MEDICATION_LOOKUP`, not to R/50's own generated output). `write_resolved_xlsx()` (per-type files) and the combined-workbook per-category loop are two separate, near-identical code blocks (each hard-codes its own 6-column header) — both must change together or the outputs will silently diverge, exactly the failure mode CONTEXT.md flags for the 5 per-type files.

Output: `all_codes_df` gains a `medication` column in Section 4; both xlsx-writing code paths in Section 6 include a "Medication" column (positioned after "Meaning", matching the reference Excel's own Chemotherapy-sheet column order) for every category except Radiation, via one shared layout definition both writers consume.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-CONTEXT.md
@.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-RESEARCH.md
@R/50_all_codes_resolved.R
</context>

<interfaces>
<!-- Contracts this plan depends on, built by 131-01 (R/00_config.R) and 131-02 (R/50 Section 3/4 restructure) -->

From 131-01 (R/00_config.R):
```r
MEDICATION_LOOKUP            # named character vector: code -> normalized medication name (unchanged interface, now also covers Supportive Care via col G when materialized)
fallback_normalize_medication(description, code_type)  # vectorized; returns bare generic ingredient name / HCPCS-stripped name / unchanged multi-ingredient compound; never blank for non-NA input
```

From 131-02 (R/50_all_codes_resolved.R Section 3/4, already landed in this same file before this plan runs):
```r
all_codes_df   # tibble(code, description, code_type, source_table, category, records, patients)
               # source_table is now per-code-dynamic for RXNORM rows (this plan does not touch source_table further)
```

Current `write_resolved_xlsx()` header/data shape (R/50, confirmed lines 603, 617-626) — 1 of 2 places this plan must change:
```r
headers <- c("Code", "Meaning", "Code Type", "Source Table", "Records", "Patients")
write_df <- data.frame(
  Code = df$code, Meaning = ifelse(is.na(df$description), "", df$description),
  Code_Type = df$code_type, Source_Table = df$source_table,
  Records = df$records, Patients = df$patients, stringsAsFactors = FALSE
)
wb$add_data(sheet = sheet_name, x = write_df, start_row = 3, col_names = FALSE)
wb$set_col_widths(sheet = sheet_name, cols = 1:6, widths = c(15, 45, 12, 15, 10, 10))
# also: wb$merge_cells(dims = "A1:F1"), code_dims <- "A3:A{last_row}", num_dims <- "E3:F{last_row}"
```

Current combined-workbook per-category header/data shape (R/50, confirmed lines 792, 806-814) — 2nd place, must match:
```r
headers_cat <- c("Code", "Meaning", "Code Type", "Source Table", "Records", "Patients")
write_df_cat <- data.frame(
  Code = df_cat$code, Meaning = ifelse(is.na(df_cat$description), "", df_cat$description),
  Code_Type = df_cat$code_type, Source_Table = df_cat$source_table,
  Records = df_cat$records, Patients = df_cat$patients, stringsAsFactors = FALSE
)
# also: wb_all$merge_cells(dims = "A1:F1"), code_dims_cat <- "A3:A{last_row_cat}", num_dims_cat <- "E3:F{last_row_cat}", freeze_pane at row 3
```

**Ground-truth confirmed this session** (openpyxl inspection of the actual reference Excel — see 131-01's interfaces block for the full detail): the Chemotherapy sheet's real column order is `Code, Meaning, Medication, Code Type, Source Table, Records, Patients` — Medication sits directly after Meaning. Use this exact ordering for consistency with the curated source-of-truth file.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Compute the medication column in Section 4's all_codes_df assembly</name>
  <files>R/50_all_codes_resolved.R</files>
  <action>
After `all_codes_df` is fully assembled in Section 4 (after the `for` loop that builds it via `bind_rows`, before the `summary_by_category` computation), add a `mutate()` step that populates a new `medication` column using this exact priority (case_when, evaluated top to bottom):

```r
all_codes_df <- all_codes_df %>%
  mutate(
    medication = dplyr::case_when(
      category == "Radiation" ~ NA_character_,
      category == "SCT" & code_type != "RXNORM" ~ NA_character_,
      code %in% names(MEDICATION_LOOKUP) ~ unname(MEDICATION_LOOKUP[code]),
      TRUE ~ fallback_normalize_medication(description, code_type)
    )
  )
```

This means:
- Radiation: `medication` is always `NA` (the column will not even be written for this category — see Tasks 2/3).
- SCT: `medication` is `NA` for DRG/ICD-10-PCS/procedure rows (Code Type != "RXNORM"), and either curated or fallback-derived for RXNORM rows.
- Chemotherapy / Immunotherapy / Supportive Care: every code gets a curated (`MEDICATION_LOOKUP`) or fallback-derived value — never blank, since `fallback_normalize_medication()` has a never-blank guarantee.

Add a `message(glue(...))` log line reporting how many codes got a curated vs. fallback medication name (e.g. `sum(!is.na(medication) & code %in% names(MEDICATION_LOOKUP))` vs. `sum(!is.na(medication) & !(code %in% names(MEDICATION_LOOKUP)))`), consistent with this script's existing verbose per-section logging style.
  </action>
  <verify>
    <automated>grep -c "fallback_normalize_medication(description, code_type)" R/50_all_codes_resolved.R</automated>
  </verify>
  <done>`all_codes_df` has a `medication` column: NA for Radiation, NA for SCT non-RXNORM rows, curated `MEDICATION_LOOKUP` value when available, `fallback_normalize_medication()` otherwise — confirmed by grep for the case_when's fallback call and the two NA branches.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update write_resolved_xlsx() (per-type files) with a shared category-aware column layout</name>
  <files>R/50_all_codes_resolved.R</files>
  <action>
Introduce a small shared helper (place it immediately before `write_resolved_xlsx()`'s definition, ~line 579) that both this writer AND the combined-workbook loop (Task 3) will call, so the two never diverge:

```r
resolved_xlsx_layout <- function(category) {
  has_medication <- category != "Radiation"
  list(
    has_medication = has_medication,
    headers = if (has_medication) {
      c("Code", "Meaning", "Medication", "Code Type", "Source Table", "Records", "Patients")
    } else {
      c("Code", "Meaning", "Code Type", "Source Table", "Records", "Patients")
    },
    n_cols = if (has_medication) 7L else 6L,
    col_widths = if (has_medication) c(15, 45, 25, 12, 15, 10, 10) else c(15, 45, 12, 15, 10, 10)
  )
}
```

Modify `write_resolved_xlsx(df, category, output_path)` to call `layout <- resolved_xlsx_layout(category)` at the top, then:
- Replace the hard-coded `headers <- c(...)` (line 603) with `headers <- layout$headers`, and use `layout$n_cols` everywhere a literal `6` or column-letter range `F`/`A:F` currently assumes 6 columns (the header loop `for (i in seq_along(headers))`, the `wb$add_fill`/`add_font` dims for the header row, the `wb$merge_cells(dims = "A1:F1")` title merge — becomes `glue("A1:{LETTERS[layout$n_cols]}1")` or equivalent dynamic column-letter construction).
- Replace the `write_df <- data.frame(...)` construction (lines 617-625) to conditionally include `Medication = ifelse(is.na(df$medication), "", df$medication)` right after `Meaning` when `layout$has_medication` is TRUE, omitting it entirely when FALSE (Radiation) — do NOT write an all-blank Medication column for Radiation; the column must not exist there at all.
- Update the `code_dims`/`num_dims` ranges (currently hard-coded `"A3:A{last_row}"` and `"E3:F{last_row}"`) to use dynamic column letters derived from `layout$n_cols` (Records/Patients are always the LAST two columns, so their letters shift from E:F to F:G when Medication is present).
- Replace `wb$set_col_widths(sheet = sheet_name, cols = 1:6, widths = c(...))` with `wb$set_col_widths(sheet = sheet_name, cols = seq_len(layout$n_cols), widths = layout$col_widths)`.

Test this mentally against both cases: Chemotherapy (`has_medication = TRUE`, 7 columns, Medication in column C) and Radiation (`has_medication = FALSE`, 6 columns, unchanged from today).
  </action>
  <verify>
    <automated>grep -c "resolved_xlsx_layout <- function" R/50_all_codes_resolved.R</automated>
  </verify>
  <done>`write_resolved_xlsx()` writes a 7-column sheet (Medication in column C, after Meaning) for every category except Radiation, which keeps its original unchanged 6-column layout — driven entirely by the shared `resolved_xlsx_layout()` helper, confirmed by grep for its definition and for the conditional Medication field in the write_df construction.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update the combined-workbook per-category loop identically, using the same shared layout helper</name>
  <files>R/50_all_codes_resolved.R</files>
  <action>
Apply the SAME `resolved_xlsx_layout(category)` helper (defined in Task 2, already in scope for the rest of the script) to the combined-workbook per-category loop (lines ~765-837, inside `for (category in categories) { ... }`), mirroring every change made to `write_resolved_xlsx()` in Task 2:
- `layout <- resolved_xlsx_layout(category)` at the top of the loop body.
- `headers_cat <- layout$headers` instead of the hard-coded 6-column vector.
- `write_df_cat` conditionally includes `Medication = ifelse(is.na(df_cat$medication), "", df_cat$medication)` right after `Meaning`, exactly matching Task 2's logic (same `has_medication` gate).
- Dynamic column-letter ranges for the header-row merge (`wb_all$merge_cells(dims = "A1:F1")` -> dynamic), `code_dims_cat`, `num_dims_cat`, and `wb_all$set_col_widths(cols = 1:6, ...)` -> `cols = seq_len(layout$n_cols)`.
- `wb_all$freeze_pane(sheet = sheet_name, first_active_row = 3)` is unaffected by column count — leave unchanged.

Because both writers now derive their header/column-count/width from the identical `resolved_xlsx_layout()` function, a future column addition/removal only needs to change one place — this directly closes the "two duplicated xlsx-writer blocks silently diverge" risk flagged in CONTEXT.md and RESEARCH.md's Pitfall 5.
  </action>
  <verify>
    <automated>grep -c "resolved_xlsx_layout(category)" R/50_all_codes_resolved.R</automated>
  </verify>
  <done>The combined-workbook per-category loop calls the same `resolved_xlsx_layout()` helper as `write_resolved_xlsx()` (2 call sites total: 1 per writer), producing byte-identical column layouts/Medication values for the same category across both the per-type files and `all_codes_resolved.xlsx` — confirmed by grep count of 2 for `resolved_xlsx_layout(category)` calls (one per writer).</done>
</task>

</tasks>

<verification>
Structural (grep-based) verification only — `openxlsx2` is not installed in this dev environment (confirmed via `Rscript -e 'installed.packages()'`), so the xlsx cannot actually be generated/opened here. Confirm via grep:
1. `resolved_xlsx_layout <- function` defined exactly once.
2. `resolved_xlsx_layout(category)` called exactly twice (once in `write_resolved_xlsx()`, once in the combined-workbook loop) — this IS the "both writers in sync" guarantee.
3. `fallback_normalize_medication(description, code_type)` called exactly once (Section 4, Task 1) — the writers themselves never call it directly, they only read `df$medication` / `df_cat$medication`.
4. No literal `headers <- c("Code", "Meaning", "Code Type"` (6-column hard-code) remains anywhere in the file — both should now derive from `layout$headers`.

A HiPerGator (or any R host with `openxlsx2` installed) runtime confirmation should additionally verify: opening the generated `chemotherapy_codes_resolved.xlsx` shows a populated Medication column in column C; opening `radiation_codes_resolved.xlsx` shows the original 6-column layout with no Medication column; opening the SCT sheet shows blank Medication for DRG/ICD-10-PCS rows and populated Medication for RXNORM rows.
</verification>

<success_criteria>
- `all_codes_df$medication` is correctly gated by category/code_type per the case_when priority.
- Both xlsx writers derive headers/column-count/widths from one shared `resolved_xlsx_layout()` function — zero risk of future divergence.
- Radiation sheets (both per-type and combined) are byte-identical in shape to today (6 columns, no Medication).
</success_criteria>

<output>
After completion, create `.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-03-SUMMARY.md`
</output>
