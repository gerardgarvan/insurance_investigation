---
status: testing
phase: 131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column
source: [131-01-SUMMARY.md, 131-02-SUMMARY.md, 131-03-SUMMARY.md, 131-04-SUMMARY.md]
started: 2026-07-23T00:00:00Z
updated: 2026-07-23T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: NDC-Only Codes Now Appear
expected: |
  Opening the regenerated all_codes_resolved.xlsx (or any of the 5 per-type files, e.g. chemotherapy_codes_resolved.xlsx), codes that were previously invisible — because they were only ever recorded via MED_ADMIN's NDC-typed rows (MEDADMIN_TYPE == "ND") or via DISPENSING — now show up as rows, with non-zero Records/Patients counts.
awaiting: user response

## Tests

### 1. NDC-Only Codes Now Appear
expected: Opening the regenerated all_codes_resolved.xlsx (or any of the 5 per-type files, e.g. chemotherapy_codes_resolved.xlsx), codes that were previously invisible — because they were only ever recorded via MED_ADMIN's NDC-typed rows (MEDADMIN_TYPE == "ND") or via DISPENSING — now show up as rows, with non-zero Records/Patients counts.
result: [pending]

### 2. Source Table Distinguishes Detection Path
expected: For RXNORM-based codes (Chemotherapy/SCT/Immunotherapy/Supportive Care sheets), the "Source Table" column shows specific labels like "MED_ADMIN (NDC)", "MED_ADMIN (RX)", "DISPENSING (NDC)", or "PRESCRIBING" (or a comma-joined combination of whichever paths actually matched) instead of the old blanket "PRESCRIBING|MED_ADMIN" string on every row regardless of how the code was actually detected.
result: [pending]

### 3. Medication Column Populated
expected: Chemotherapy, Supportive Care, Immunotherapy, and SCT sheets each show a "Medication" column with a normalized generic drug name for every code that has one (e.g. "bendamustine", not "bendamustine hydrochloride 25 MG/ML Injectable Solution [Bendeka]"). On the SCT sheet, Medication is blank only for DRG/ICD-10-PCS rows and populated for RXNORM rows.
result: [pending]

### 4. Radiation Sheet Unchanged
expected: The Radiation sheet (both in the combined workbook and radiation_codes_resolved.xlsx) has no Medication column at all — same 6 columns as before this phase, no blank column added.
result: [pending]

### 5. Records Counts May Drop Slightly for Existing Codes
expected: Some codes that previously had inflated Records counts (from double-counting same-day rows seen via more than one detection path) now show a lower, more accurate Records number. This is an intentional fix, not a bug — flagging it here since the workbook is shared with collaborators who may notice the change.
result: [pending]

### 6. Per-Type Files Match the Combined Workbook
expected: For any given category, its per-type file's data (e.g. chemotherapy_codes_resolved.xlsx) matches that category's sheet in the combined all_codes_resolved.xlsx exactly — same codes, same Medication values, same Source Table labels, same column layout.
result: [pending]

### 7. R/88 Smoke Test Section Passes
expected: Running `Rscript R/88_smoke_test_comprehensive.R` on HiPerGator shows all 12 new Section 15x checks passing (return_source tagging, DISPENSING query, RXNORM generalization, dedup guard, dynamic source_table, MEDICATION_LOOKUP col G wiring, fallback normalizer, medication gating, shared layout helper call sites).
result: [pending]

### 8. Summary Sheet Shows True Unique-Patient Counts
expected: |
  (From the same-session follow-up fix to the Summary sheet, applied after the 4 plans above.) The Summary sheet's per-category "Patients" column and the "Total" row are unique-patient counts — a patient reached via more than one code in the same category is counted once, not once per code. The Total row's Patients value is <= the sum of the per-category Patients values (strictly less if any patient spans multiple categories) — this is correct, not a bug.
result: [pending]

## Summary

total: 8
passed: 0
issues: 0
pending: 8
skipped: 0

## Gaps

[none yet]
