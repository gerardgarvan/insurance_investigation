---
phase: 131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
autonomous: true
requirements:
  - MEDXLSX-01
  - MEDXLSX-02
user_setup: []

must_haves:
  truths:
    - "MEDICATION_LOOKUP consults the Supportive Care sheet's 'Normalized Meaning' column (col G, written by Phase 120's R/105) when it exists, instead of silently reading the wrong column-3 code-type label"
    - "MEDICATION_LOOKUP degrades gracefully (no crash, unchanged behavior) when col G is absent -- true today in this repo's copy of the reference Excel, since R/105 has not yet materialized col G locally (confirmed: rxnorm_ingredient_cache.csv absent, sheet dims A1:F173)"
    - "A fallback_normalize_medication() function exists that turns an RxNorm-STR-style description into a bare generic ingredient name (salts/doses/formulations stripped), turns an HCPCS 'Injection, X, dose' description into 'X', and passes multi-ingredient ' / '-delimited compounds through completely unchanged"
    - "fallback_normalize_medication() never returns blank/NA for a non-blank input (mirrors R/105's rule_based_ingredient never-blank guarantee)"
  artifacts:
    - path: "R/00_config.R"
      provides: "MEDICATION_LOOKUP builder reads col G for Supportive Care (with column-3 fallback) + fallback_normalize_medication() function"
      contains: "fallback_normalize_medication <- function"
  key_links:
    - from: "R/00_config.R MEDICATION_LOOKUP builder"
      to: "Supportive Care sheet col G ('Normalized Meaning')"
      via: "sheet_df[[7]] read when ncol(sheet_df) >= 7 and sheet_name == 'Supportive Care'"
      pattern: "Supportive Care.*ncol\\(sheet_df\\)|ncol\\(sheet_df\\) >= 7"
    - from: "fallback_normalize_medication()"
      to: "canonicalize_drug_name() / DRUG_NAME_ALIASES"
      via: "brand->generic collapsing applied to the stripped result, consistent with MEDICATION_LOOKUP and R/105"
      pattern: "canonicalize_drug_name\\("
---

<objective>
Build the two pieces of "medication naming" infrastructure that Phase 131's downstream R/50 changes will consume: (1) wire the already-existing but currently orphaned Phase 120 "Normalized Meaning" column (col G) on the Supportive Care sheet into `MEDICATION_LOOKUP`, and (2) add a heuristic fallback normalizer for codes `MEDICATION_LOOKUP` doesn't cover (new NDC-resolved MED_ADMIN codes, and any SCT/Immunotherapy/Supportive Care codes without a curated name).

Purpose: `all_codes_resolved.xlsx` needs a single, consistently-populated Medication column across Chemotherapy/Supportive Care/Immunotherapy/SCT. The primary source (`MEDICATION_LOOKUP`) already exists; this plan makes it consult one more real data source it currently ignores (col G) and defines the fallback that covers everything else. This plan touches only `R/00_config.R` — R/50's actual column-population wiring happens in a later plan (131-03) after the MED_ADMIN/DISPENSING detection rework (131-02) lands.

Output: `R/00_config.R` with an updated `MEDICATION_LOOKUP` builder and a new exported `fallback_normalize_medication(description, code_type)` function, ready for 131-03 to call.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-CONTEXT.md
@.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-RESEARCH.md
@R/00_config.R
@R/105_normalize_supportive_care_meaning.R
</context>

<interfaces>
<!-- Confirmed by direct read of R/00_config.R (current state) -->

MEDICATION_LOOKUP builder (R/00_config.R, ~lines 2496-2549):
```r
MEDICATION_LOOKUP <- local({
  if (!file.exists(REFERENCE_XLSX)) { ... }
  ref_wb <- openxlsx2::wb_load(REFERENCE_XLSX)
  sheets <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy", "Supportive Care")
  all_meds <- character(0)
  for (sheet_name in sheets) {
    sheet_df <- openxlsx2::wb_to_df(ref_wb, sheet = sheet_name, start_row = 2)
    sheet_map <- setNames(as.character(sheet_df[[3]]), as.character(sheet_df[[1]]))  # <- column 3 hardcoded
    sheet_map <- sheet_map[!is.na(names(sheet_map)) & !is.na(sheet_map)]
    all_meds <- c(all_meds, sheet_map)
  }
  all_meds <- all_meds[!duplicated(names(all_meds))]
  # normalize_med() title-cases + preserves abbreviations
  # code_type_labels filter drops rows where column value is a code-type label (cpt, rxnorm, drg, ...)
  return(all_meds)
})
```

`canonicalize_drug_name()` (R/00_config.R ~line 2614): vectorized, NA-safe, case-insensitive brand->generic alias lookup via `DRUG_NAME_ALIASES`. Returns input unchanged if no alias match.

R/105's `rule_based_ingredient()` (R/105_normalize_supportive_care_meaning.R, lines 262-320): the reference implementation to adapt for `fallback_normalize_medication()`'s RxNorm-STR stripping branch — strips pack wrappers, dose/quantity tokens, formulation words, brand brackets, salt words, then calls `canonicalize_drug_name()`, then lowercases, with a first-word-of-input safety net if everything strips away.

**Ground-truth confirmed this session** (via `python3 -c "import openpyxl..."` against `data/reference/all_codes_resolved_next_tables_v2.1.xlsx`, since Rscript's openxlsx2 package is not installed in this dev environment):
- Chemotherapy sheet: 9 columns, column 3 = "Medication" (real drug names for BOTH RXNORM rows e.g. "Adcetris" AND CPT/HCPCS J-code rows e.g. "ado-trastuzumab emtansine" already stripped from "Injection, ado-trastuzumab emtansine, 1 mg"). This sheet needs NO change.
- SCT sheet: 7 columns (Code, Meaning, Code Type, Source Table, Records, Patients, + a stray col G "Remove" annotation on some rows). Column 3 = "Code Type", NOT a drug name. No Medication column exists.
- Immunotherapy sheet: 7 columns, column 3 = "Code Type". No Medication column. Confirmed real multi-ingredient text present in column 2 (Meaning), e.g. `"ascorbic acid / beta carotene / copper sulfate / selenite / vitamin E / zinc oxide"`.
- Supportive Care sheet: **dims A1:F173 — only 6 columns, NO col G today.** `git log` on the reference xlsx shows its last commit is `7e615c6` (Phase 77), predating Phase 120 (R/105) entirely. Phase 120's own SUMMARY confirms this explicitly: *"the materialization [of the col-G write] is the Plan 01 runtime-deferred item"* — R/105 has never actually executed against this file (no internet/RxNav access in the dev sandbox); the write only happens when run on HiPerGator. **Your col-G wiring MUST degrade gracefully today (fall back to column 3) and must NOT assume col G exists.**
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire Supportive Care col G ("Normalized Meaning") into MEDICATION_LOOKUP, with graceful column-3 fallback</name>
  <files>R/00_config.R</files>
  <action>
Modify the `MEDICATION_LOOKUP` builder's `for (sheet_name in sheets)` loop (~line 2508-2513) so that for the Supportive Care sheet specifically, it reads column 7 (col G, "Normalized Meaning") when that column exists, instead of column 3 (which today holds "Code Type", not a drug name, for this sheet). For all other sheets, and for Supportive Care when col G does not (yet) exist, behavior is UNCHANGED (read column 3).

Concretely, replace the fixed `sheet_df[[3]]` reference with a per-sheet column selector:

```r
for (sheet_name in sheets) {
  sheet_df <- openxlsx2::wb_to_df(ref_wb, sheet = sheet_name, start_row = 2)
  med_col <- if (sheet_name == "Supportive Care" && ncol(sheet_df) >= 7) 7L else 3L
  sheet_map <- setNames(as.character(sheet_df[[med_col]]), as.character(sheet_df[[1]]))
  sheet_map <- sheet_map[!is.na(names(sheet_map)) & !is.na(sheet_map)]
  all_meds <- c(all_meds, sheet_map)
}
```

Do not touch the `normalize_med()` title-casing step or the `code_type_labels` drop-filter that follow this loop — both continue to apply uniformly to whatever `all_meds` ends up containing, which is correct: once col G is materialized (real ingredient names, all lowercase generic strings per R/105's contract), those values will legitimately pass through `normalize_med()`'s title-casing and will NOT match any `code_type_labels` entry, so they'll be kept. Add a one-line comment above the loop noting WHY (Phase 131 addendum: reuse Phase 120's R/105 col G output instead of re-deriving Supportive Care names via the new fallback normalizer, per CONTEXT.md's post-research decision) and that this degrades to column 3 gracefully when col G is absent (confirmed the current case in this repo's copy of the reference Excel).
  </action>
  <verify>
    <automated>grep -c "med_col <- if (sheet_name == .Supportive Care." R/00_config.R</automated>
    <!-- expect 1. Also confirm: grep -c "sheet_df\[\[med_col\]\]" R/00_config.R -> 1; grep -c "sheet_df\[\[3\]\]" R/00_config.R -> 0 (old hardcoded column-3 reference removed) -->
  </verify>
  <done>MEDICATION_LOOKUP's builder loop reads column 7 for Supportive Care when `ncol(sheet_df) >= 7`, otherwise column 3 (same as all other sheets) — verified by grep, no runtime execution needed since openxlsx2 is not installed in this dev environment.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add fallback_normalize_medication() heuristic normalizer</name>
  <files>R/00_config.R</files>
  <action>
Add a new vectorized function `fallback_normalize_medication(description, code_type)` immediately after the `DRUG_NAME_ALIASES` / `canonicalize_drug_name()` block (~line 2625), before "SECTION 6: ANALYSIS PARAMETERS". Both arguments are character vectors of equal length (`description` = R/50's already-coalesced description field; `code_type` = one of "RXNORM", "CPT/HCPCS", "ICD-9", "ICD-10-PCS", "DRG", "Revenue"). Must be fully vectorized (no per-row loop over rows — loop only over the small fixed lists of formulation/salt words, exactly like `rule_based_ingredient()` does).

Three branches, evaluated per-element in this priority order:

1. **Multi-ingredient compound passthrough**: if `description` contains the literal substring `" / "` (space-slash-space — the RxNorm STR combination-product delimiter confirmed present in real `immunotherapy_rxnorm` data, e.g. `"ascorbic acid / beta carotene / copper sulfate / selenite / vitamin E / zinc oxide"`), return `str_squish(description)` completely UNCHANGED — no stripping, no shortening to first ingredient, no blanking.

2. **HCPCS J-code pattern**: if `code_type == "CPT/HCPCS"` and `description` matches `"^Injection,\\s*([^,]+),"` (e.g. `"Injection, ado-trastuzumab emtansine, 1 mg"`), extract the captured group (`"ado-trastuzumab emtansine"`), trim it, run it through `canonicalize_drug_name()`, and return it lowercased/trimmed. This matches the reference Excel's own Chemotherapy-sheet Medication column style exactly (confirmed: `"ado-trastuzumab emtansine"` is the real curated value for J9354).

3. **RxNorm-STR fallback** (everything else, the default): adapt `rule_based_ingredient()` from `R/105_normalize_supportive_care_meaning.R` (lines 262-320) — vectorized over the whole `description` vector using the same sequence of `str_remove`/`str_remove_all` calls (pack wrappers, quantity prefixes, brand brackets, dose/units/percent tokens, formulation words, salt words), then `str_squish()`, `canonicalize_drug_name()`, `tolower()`. Reuse the exact same `formulations` and `salts` word lists as R/105 (copy them, do not import from R/105 — R/105 is a one-time reference-Excel enrichment script, not a shared utils file).

Never-blank guarantee: if branch 3's result is blank/empty after stripping, fall back to the lowercased first word of the original `description` (`stringr::word(description, 1)`), exactly mirroring R/105's safety net. If `description` itself is `NA` or blank, return `NA_character_` (do not fabricate a name for a missing description).

Add a short header comment documenting: this function's three-tier logic, that it operates on R/50's coalesced `description` field (NOT raw R/00_config.R inline comments, which can be mid-word truncated), and that it is the Phase 131 fallback consulted only after `MEDICATION_LOOKUP` lookup fails.
  </action>
  <verify>
    <automated>grep -n "fallback_normalize_medication <- function" "R/00_config.R" | wc -l</automated>
  </verify>
  <done>`fallback_normalize_medication(description, code_type)` exists in R/00_config.R, is vectorized, handles the multi-ingredient passthrough / HCPCS J-code / RxNorm-STR-strip branches in that priority order, and never returns blank for non-NA input — confirmed by grep for the function definition, the `" / "` detection, the `"^Injection,"` regex, and the `canonicalize_drug_name(` call inside it.</done>
</task>

</tasks>

<verification>
Both tasks are structural (grep-based) because `openxlsx2` (required to actually source `R/00_config.R`'s `MEDICATION_LOOKUP` block) is not installed in this dev environment — confirmed via `Rscript -e 'installed.packages()'` returning FALSE for `openxlsx2`, `duckdb`, and `here`. This matches the project's established dual-environment convention (structural verify locally via grep, runtime confirm on HiPerGator) used throughout Phases 116-130.

Run both grep checks above and confirm:
1. `sheet_df[[3]]` no longer appears unconditionally in the MEDICATION_LOOKUP loop (replaced by the `med_col` conditional).
2. `fallback_normalize_medication <- function` is defined exactly once.
3. `R/00_config.R` still parses as valid R syntax (balanced braces/parens — spot-check with a text editor or `Rscript -e "parse('R/00_config.R')"` on a host where R packages ARE available, e.g. HiPerGator).
</verification>

<success_criteria>
- MEDICATION_LOOKUP's builder consults col G for Supportive Care when present, column 3 otherwise (all sheets, unchanged fallback behavior).
- `fallback_normalize_medication()` is defined, vectorized, and implements the 3-tier priority (multi-ingredient passthrough > HCPCS J-code strip > RxNorm-STR strip), with a never-blank guarantee.
- No other function or config constant in R/00_config.R is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/131-update-all-codes-resolved-xlsx-to-include-med-admin-ndc-resolved-codes-and-a-normalized-drug-name-column/131-01-SUMMARY.md`
</output>
