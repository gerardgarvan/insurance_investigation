---
phase: 146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md
autonomous: true
requirements: [SES-01, SES-02, SES-03]

must_haves:
  truths:
    - "The team can see, for SDI/SVI/ADI, exactly what each publisher releases (geography, format, registration, vintage, columns, terms, missing-value encoding) each with a URL and date accessed"
    - "The team knows whether HiPerGator compute nodes have outbound network, deciding whether download steps run in SLURM or on a login node"
    - "The team knows whether the sentinel ZIPs blocking 157,472 rows are one systematic placeholder or scattered (the D-04 lever)"
    - "The team knows whether normalize_zip5() and normalize_zip9() disagree on sentinel prefixes (the §5 five-row inconsistency)"
  artifacts:
    - path: ".planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md"
      provides: "Verified source-publication facts, network result, sentinel-ZIP frequencies, normalizer disagreement finding"
      contains: "date accessed"
  key_links:
    - from: "146-DISCOVERY.md exact-column findings"
      to: "R/116 SECTION 6 probe gates (ZIP5/SDI_score, ZCTA/RPL_THEMES, ZIP9/adi_natrank)"
      via: "column-name comparison table"
      pattern: "SDI_score|RPL_THEMES|adi_natrank"
---

<objective>
Verify, without downloading anything, exactly what each SES source (SDI, SVI, ADI) publishes,
whether HiPerGator compute nodes can reach the network, whether the sentinel ZIPs are systematic,
and whether the two ZIP normalizers disagree on sentinels. Write it all into 146-DISCOVERY.md so
every later task action cites a verified fact, never an assumption (CONTEXT.md §1 Rule 1).

Purpose: This phase's first rule is "Discover, never assume." No URL, filename, column, or vintage
may appear in a staging task until it is confirmed here. This plan is the sole source of those facts.
Output: .planning/phases/146-.../146-DISCOVERY.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-RESEARCH.md
@data/reference/README.md
@R/116_encounter_ses_index.R

<interfaces>
R/116 SECTION 6 probe gates hard-code these expected column names (facts to compare against):
- SDI: file `data/reference/zip5_sdi_reference.csv`, needs columns `ZIP5` + `SDI_score`
- SVI: file `data/reference/svi_2020_us_by_zcta.csv`, needs columns `ZCTA` + `RPL_THEMES` (maps <0 to NA)
- ADI: file `data/reference/neighborhood_atlas_block_group_crosswalk.csv`, ZIP9 key one of
  `ZIP9,zip9,ADDRESS_ZIP9,zip9_norm,GEOID9,ZIP_PLUS4`; rank one of `adi_natrank,ADI_NATRANK,natrank`
Corrected coverage denominator: 1,950,696 encounters; 77.7% ZIP ceiling; 68.6% ADI figure is WRONG (inflated denom).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify source publications and network, write the DISCOVERY table</name>
  <read_first>
    - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md (§2 table, §1 rules)
    - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-RESEARCH.md (Discovery Findings by Source)
    - data/reference/README.md (what R/116 currently expects per index)
  </read_first>
  <action>
    Create `.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md`.
    For EACH of SDI (Robert Graham Center), SVI (CDC/ATSDR), ADI (Neighborhood Atlas / UW-Madison),
    verify via WebFetch against the publisher pages named in 146-RESEARCH.md Sources and record, each
    with a URL and a "date accessed: 2026-08-17":
      - Publisher, native geography, release format, registration required (yes/no), vintages available.
      - Whether a ZIP5- or ZCTA-level release exists, or only tract/block-group. Explicitly confirm the
        CONTEXT.md §3b locked fact: CDC does NOT publish SVI at ZCTA for 2020 (ZCTA exists 2022 only).
      - Exact downloaded-file column names vs. what R/116 SECTION 6 expects (SDI: ZIP5/SDI_score;
        SVI: ZCTA/RPL_THEMES; ADI: ZIP9 candidate list + adi_natrank candidate list). State per index:
        "columns match R/116 as-is" OR "differ -> README/R116 SECTION 6 select() must change (source file
        is NOT renamed)."
      - Terms of use: redistribution allowed?, may the file live in THIS repo?, is attribution required in
        outputs shown to Erin and Amy? If terms are unclear, say so and name the contact (SDI: policy@aafp.org).
      - Missing-value encoding per index. Confirm CDC SVI uses -999 (Phase 145 D-03); check SDI and ADI sentinels.
    Also record the HiPerGator network finding: state that per 146-RESEARCH.md the safe assumption is compute
    nodes are network-isolated and login nodes have outbound HTTPS, and record the CONTEXT.md §2 curl command
    verbatim as the check to run on a HiPerGator compute node (this Windows box cannot verify it):
    `curl -sS -m 10 -o /dev/null -w "%{http_code}\n" https://www.graham-center.org/ || echo "NO NETWORK"`.
    Mark the compute-node result as "PENDING HiPerGator run" and conclude the practical rule: all three files
    are downloaded on a login node / Windows box and committed (if terms permit), never fetched in SLURM.
    Open items must be flagged explicitly (SDI 2020-vs-2019 vintage, SDI exact columns, Census API key for findSVI,
    ADI who-registers) rather than guessed.
  </action>
  <verify>
    <automated>MISSING — no test harness. Verify by grep: `grep -Ec "date accessed" 146-DISCOVERY.md` returns >= 3, and `grep -E "77.7|SDI_score|RPL_THEMES|adi_natrank|NO NETWORK" 146-DISCOVERY.md` returns matches.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "date accessed" .../146-DISCOVERY.md` >= 3 (one per index, each with a URL).
    - File contains the strings "SDI_score", "RPL_THEMES", and "adi_natrank" (the R/116 column comparison).
    - File states CDC publishes no ZCTA-level SVI for 2020 (matches CONTEXT.md §3b locked fact).
    - File contains the exact curl command string `curl -sS -m 10` and marks the compute-node result PENDING.
    - Each index row lists redistribution/terms status; unclear terms are labelled "unclear — confirm with {contact}".
  </acceptance_criteria>
  <done>146-DISCOVERY.md exists with a verified per-index table (URL + date accessed), the R/116 column-match verdict per index, network finding, and all open items flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Run the sentinel-ZIP frequency and normalizer-disagreement probes; record findings</name>
  <read_first>
    - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md (§4 D-04 query, §5 five-row inconsistency)
    - R/utils/utils_address.R (normalize_zip5, normalize_zip9, is_sentinel_zip5 — the functions §5 says disagree)
  </read_first>
  <action>
    Append two sections to 146-DISCOVERY.md.
    (a) SENTINEL-ZIP FREQUENCY (D-04 lever): Record the CONTEXT.md §4 R query verbatim as the command to run
    on HiPerGator against `LDS_ADDRESS_HISTORY_Mailhot_V1.csv` (counts ZIP values where normalize_zip5() is NA
    but the raw value is not NA, top 20 by frequency). This box has no PCORnet CSV, so mark the result table
    "PENDING HiPerGator run" and state the decision rule: if one or two placeholder values dominate the 157,472
    sentinel rows, that is a systematic single-source placeholder worth raising with the data provider (raises the
    ceiling for every index at once); if scattered, it is not a single-fix lever. Do NOT fabricate counts.
    (b) NORMALIZER DISAGREEMENT (§5): Read R/utils/utils_address.R and inspect normalize_zip5(), normalize_zip9(),
    is_sentinel_zip5(). Determine whether a ZIP9 like "000001234" is accepted by normalize_zip9() while its ZIP5
    prefix "00000" is rejected by normalize_zip5()/is_sentinel_zip5() — the mechanism that produces the five
    match_type="interval", zip9_na=FALSE, zip5_na=TRUE rows. Record the exact functions and lines responsible,
    state whether a fix is trivial (per CONTEXT.md §5: "fix only if trivial"), and if trivial, describe the
    one-line change WITHOUT applying it (application, if any, belongs to a later staging plan or a /gsd:quick).
  </action>
  <verify>
    <automated>MISSING — no test harness. Verify by grep: `grep -E "157,?472|normalize_zip9|normalize_zip5|PENDING" 146-DISCOVERY.md` returns matches.</automated>
  </verify>
  <acceptance_criteria>
    - 146-DISCOVERY.md contains a "sentinel" section citing 157,472 and marks the frequency table PENDING HiPerGator run (no fabricated counts).
    - The exact §4 R query text is present (dplyr::count on normalize_zip5-NA rows).
    - A "normalizer disagreement" section names normalize_zip5, normalize_zip9, is_sentinel_zip5 and states whether the §5 fix is trivial, without having modified utils_address.R.
    - `git diff --stat R/utils/utils_address.R` shows no changes (this plan does not edit code).
  </acceptance_criteria>
  <done>Both probe sections appended; sentinel and §5 findings recorded with PENDING markers where HiPerGator is required; utils_address.R untouched.</done>
</task>

</tasks>

<verification>
- 146-DISCOVERY.md exists and covers all three indices with URL + date accessed.
- R/116 column-match verdict recorded per index.
- Network, sentinel-ZIP, and normalizer-disagreement findings recorded (PENDING markers where HiPerGator is needed).
- No source files or code modified (grep/git diff confirms utils_address.R and R/116 untouched).
</verification>

<success_criteria>
Every fact a later staging plan needs (URL, filename, columns, vintage, terms, network policy) is verified and
written in 146-DISCOVERY.md, or explicitly flagged as an open item / PENDING HiPerGator run. No assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-01-SUMMARY.md`.
</output>
