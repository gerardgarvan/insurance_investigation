---
phase: 146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi
plan: 06
type: execute
wave: 5
depends_on: ["146-05"]
files_modified:
  - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md
autonomous: false
requirements: [SES-01, SES-02, SES-03, SES-04]

must_haves:
  truths:
    - "R/116 re-runs on HiPerGator against real data with the staged files and each newly staged index reports non-zero coverage at or below its 77.7% ceiling"
    - "R/88 passes, and probe gates still degrade any still-absent index to NA without crashing"
    - "The sentinel-ZIP frequency query and the SDI ZIP5-with-no-ZCTA quantifier are run against real data and their results recorded"
  artifacts:
    - path: ".planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md"
      provides: "Recorded real-run coverage per index, R/88 result, sentinel-ZIP top values, SDI unmatched count"
      contains: "coverage"
  key_links:
    - from: "R/116 real run coverage per index"
      to: "the 77.7% ceiling in README / Coverage Ceilings sheet"
      via: "each index coverage <= 77.7% and non-zero for staged indices"
      pattern: "77.7|coverage"
---

<objective>
Confirm the whole phase against real HiPerGator data: re-run R/116 with the staged files, check each
newly staged index reports non-zero coverage at or below 77.7%, confirm R/88 passes and probe gates
still degrade absent indices to NA, and run the two PENDING queries (sentinel-ZIP frequency, SDI
ZIP5-with-no-ZCTA). This is the phase's definition-of-done gate and requires HiPerGator access.

Purpose: Everything before this was verified statically on a Windows box with no R runtime, DuckDB,
or PCORnet CSVs. This plan closes the acceptance criteria that need real data.
Output: real-run results recorded in 146-DISCOVERY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md
@R/116_encounter_ses_index.R
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-run R/116 + R/88 on HiPerGator and run the two PENDING queries</name>
  <read_first>
    - R/116_encounter_ses_index.R (the wired script from 146-05)
    - .planning/phases/146-.../146-DISCOVERY.md (the two PENDING queries: §4 sentinel-ZIP, §3a SDI unmatched)
    - .planning/phases/146-.../146-CONTEXT.md §6 (acceptance criteria checklist)
  </read_first>
  <what-built>
    Waves 1-4 staged SDI (and SVI if D-02a), wired R/116 to read them, and corrected the coverage story to the
    77.7% ceiling. R/116 is a READ-ONLY investigation needing DuckDB + LDS_ADDRESS_HISTORY + the ENCOUNTER table,
    available only on HiPerGator (module load R/4.4.2). This box could not execute it.
  </what-built>
  <how-to-verify>
    On HiPerGator (module load R/4.4.2):
    1. Run `Rscript R/116_encounter_ses_index.R`. Confirm: SDI coverage is non-zero and <= 77.7%; ADI coverage
       non-zero and <= 77.7% if the crosswalk was staged (else still NA by the probe gate, no crash); SVI coverage
       non-zero and <= 77.7% if D-02a staged (else NA, no crash). Confirm the "Coverage Ceilings" sheet is present
       and the fan-out stopifnots did not trip (row count preserved). Record each index's coverage %.
    2. Run R/88 and confirm it passes (probe gates degrade any absent index to NA without crashing).
    3. Run the §4 sentinel-ZIP frequency query and record the top values (is the sentinel one systematic
       placeholder or scattered? — the D-04 finding).
    4. Run the §3a SDI ZIP5-with-no-ZCTA quantifier and record matched/unmatched counts.
    Record all four results in 146-DISCOVERY.md, replacing the PENDING markers.
  </how-to-verify>
  <acceptance_criteria>
    - 146-DISCOVERY.md records real R/116 coverage per index, each non-zero (for staged indices) and <= 77.7%.
    - R/88 result recorded as passing; no probe-gate crash for absent indices.
    - Sentinel-ZIP top values recorded with a systematic-vs-scattered verdict (D-04).
    - SDI matched/unmatched ZIP5-vs-ZCTA counts recorded (replacing PENDING).
  </acceptance_criteria>
  <resume-signal>Report: per-index coverage %, R/88 pass/fail, sentinel-ZIP top values + verdict, SDI unmatched count — or describe issues</resume-signal>
</task>

</tasks>

<verification>
- R/116 real-run coverage recorded per index, non-zero for staged indices, all <= 77.7%.
- R/88 passes; probe gates degrade absent indices to NA without crashing.
- Sentinel-ZIP frequency and SDI ZIP5-with-no-ZCTA results recorded (PENDING markers replaced).
</verification>

<success_criteria>
The phase's §6 acceptance criteria that require real data are satisfied and recorded: staged indices report
honest sub-77.7% coverage, R/88 passes, and the two diagnostic queries have real results.
</success_criteria>

<output>
After completion, create `.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-06-SUMMARY.md`.
</output>
