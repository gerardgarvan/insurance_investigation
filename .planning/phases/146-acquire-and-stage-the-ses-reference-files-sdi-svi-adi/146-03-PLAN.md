---
phase: 146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi
plan: 03
type: execute
wave: 3
depends_on: ["146-02"]
files_modified:
  - data/reference/zip5_sdi_reference.csv
  - .gitignore
  - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md
autonomous: false
requirements: [SES-01]

must_haves:
  truths:
    - "The SDI reference file exists at data/reference/zip5_sdi_reference.csv with columns R/116 SECTION 6 reads (ZIP5, SDI_score) or the exact column difference is recorded for 146-05 to reconcile"
    - "The count of cohort ZIP5s with no corresponding SDI ZCTA is quantified and recorded for the coverage sheet"
    - "sdi_score is labelled a ZCTA-level value attached through ZIP5, never a ZIP5-level measure (D-01)"
  artifacts:
    - path: "data/reference/zip5_sdi_reference.csv"
      provides: "SDI ZCTA scores keyed for R/116's ZIP5 join"
      contains: "ZIP5"
  key_links:
    - from: "data/reference/zip5_sdi_reference.csv columns"
      to: "R/116 SECTION 6 select(ZIP5, sdi_score = SDI_score)"
      via: "column-name match verified against 146-DISCOVERY.md"
      pattern: "ZIP5|SDI_score"
---

<objective>
Stage the SDI file at the exact path R/116 probes, quantify the ZCTA-vs-ZIP5 mismatch below the 77.7%
ceiling, and record the D-01 labelling requirement. Uses only verified facts from 146-DISCOVERY.md —
the download URL, filename, vintage, and column names must already be confirmed there.

Purpose: SDI is the one clean download of the three. This plan lands the file and measures its second
haircut (ZIP5s with no ZCTA) so 146-05 can report it in the coverage sheet.
Output: data/reference/zip5_sdi_reference.csv (or a documented column-diff), plus a recorded unmatched count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md
@R/116_encounter_ses_index.R
@data/reference/README.md

<interfaces>
R/116 SECTION 6 (lines 202-224) probe gate for SDI requires the staged file to contain columns exactly
`ZIP5` and `SDI_score`. If 146-DISCOVERY.md recorded that the downloaded file uses different names
(e.g. ZCTA5, sdi), the fix per CONTEXT.md §7 is to change R/116's select() (in 146-05), NOT to rename
the source file. normalize_zip5_raw() is applied by R/116 to the ZIP5 column; keys are read as character.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Download the SDI file from the RGC portal (login node / Windows box)</name>
  <read_first>
    - .planning/phases/146-.../146-DISCOVERY.md (SDI row: verified URL, vintage, exact columns, terms, commit-to-repo verdict)
  </read_first>
  <what-built>
    The RGC Social Deprivation Index is a portal click with no CLI/API and (per Wave 1) no registration.
    HiPerGator compute nodes are network-restricted, so the download happens on a login node or the Windows box.
  </what-built>
  <how-to-verify>
    Using the exact URL and vintage recorded in 146-DISCOVERY.md, download the SDI ZCTA CSV and place the raw
    file where the executor can read it (repo working tree or a stated local path). Report the raw column names
    observed, so Task 2 can restructure to R/116's expected `ZIP5` + `SDI_score`.
  </how-to-verify>
  <acceptance_criteria>
    - A raw SDI CSV is present locally and its actual column names are reported back to the executor.
    - The vintage downloaded matches the one recorded in 146-DISCOVERY.md.
  </acceptance_criteria>
  <resume-signal>Report: raw SDI file path and its actual column names</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Restructure to data/reference/zip5_sdi_reference.csv and record the D-01 label</name>
  <read_first>
    - .planning/phases/146-.../146-DISCOVERY.md (SDI exact raw columns, commit-to-repo verdict)
    - R/116_encounter_ses_index.R SECTION 6 (the ZIP5/SDI_score contract)
  </read_first>
  <action>
    Write `data/reference/zip5_sdi_reference.csv` with columns `ZIP5` (character, 5-digit ZCTA carried as ZIP5)
    and `SDI_score` (numeric) so R/116 SECTION 6's `select(ZIP5, sdi_score = SDI_score)` matches with NO change
    to R/116. Map the raw column names (from Task 1 / 146-DISCOVERY.md) to these two. Keep ZIP5 as a
    zero-padded 5-character string. Use readr/dplyr with explicit col_types = character for the key (project rule:
    no read.csv, no unspecified types for ZIP codes); no data.table; no setwd/absolute paths — use here()/file.path().
    IF 146-DISCOVERY.md's terms verdict says SDI may NOT be committed: add `data/reference/zip5_sdi_reference.csv`
    to `.gitignore`, leave the file in place (uncommitted), and note the transfer path in 146-DISCOVERY.md instead.
    Append to 146-DISCOVERY.md a one-line D-01 statement to be echoed wherever sdi_score is reported:
    "sdi_score is a ZCTA-level SDI value attached through ZIP5 (ZCTA is approximate; PO-box-only and
    single-building ZIPs have no ZCTA and do not match) — it is NOT a ZIP5-level measure."
  </action>
  <verify>
    <automated>MISSING — no R runtime on this box. Verify with: `head -1 data/reference/zip5_sdi_reference.csv` shows a header containing `ZIP5` and `SDI_score`; if uncommitted, `grep -q zip5_sdi_reference.csv .gitignore`.</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 data/reference/zip5_sdi_reference.csv` contains both `ZIP5` and `SDI_score`.
    - ZIP5 values are 5-character zero-padded strings (spot-check first data rows).
    - No use of read.csv/setwd/absolute paths/data.table in any new code written.
    - If terms forbid committing: `.gitignore` contains `zip5_sdi_reference.csv` and 146-DISCOVERY.md records the transfer path.
    - 146-DISCOVERY.md contains the D-01 "ZCTA-level value attached through ZIP5" sentence.
  </acceptance_criteria>
  <done>SDI file staged at the R/116-probed path with ZIP5/SDI_score columns (or a recorded column-diff for 146-05), commit status handled per terms, D-01 label recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Quantify the ZIP5-with-no-ZCTA haircut (script + recorded result)</name>
  <read_first>
    - .planning/phases/146-.../146-CONTEXT.md §3a (the exact quantification query)
    - data/reference/zip5_sdi_reference.csv (the file just staged)
  </read_first>
  <action>
    Add a small, committed quantification snippet (a self-contained .R script under R/ or an appended
    diagnostic block, using here()/file.path(), dplyr only) implementing CONTEXT.md §3a: load the staged SDI
    file and the latest `output/encounter_ses_index_<date>.rds`, take unique non-NA cohort ZIP5s, and compute
    `sum(zips %in% sdi$ZIP5)` (matched) and `sum(!zips %in% sdi$ZIP5)` (unmatched). Because this box has no
    encounter RDS, mark the RESULT (matched/unmatched counts) as "PENDING HiPerGator run" in 146-DISCOVERY.md
    with the script path — do NOT fabricate counts. State that the unmatched count is the second haircut below
    the 77.7% ceiling and belongs in the coverage sheet labelled "ZIP5s with no corresponding ZCTA" (wired in 146-05).
  </action>
  <verify>
    <automated>MISSING — needs encounter RDS on HiPerGator. Verify the script exists and 146-DISCOVERY.md marks the result PENDING: `grep -E "no corresponding ZCTA|PENDING" 146-DISCOVERY.md`.</automated>
  </verify>
  <acceptance_criteria>
    - A committed quantification script/block implements the §3a matched/unmatched counts using dplyr and here()/file.path().
    - 146-DISCOVERY.md records the result as PENDING HiPerGator run with the script path, no fabricated counts.
    - The phrase "ZIP5s with no corresponding ZCTA" appears in 146-DISCOVERY.md for 146-05 to surface in the coverage sheet.
  </acceptance_criteria>
  <done>Quantification script committed; unmatched-count result recorded PENDING; label reserved for the coverage sheet.</done>
</task>

</tasks>

<verification>
- data/reference/zip5_sdi_reference.csv staged with ZIP5/SDI_score (or a recorded column-diff), commit status per terms.
- ZCTA-vs-ZIP5 quantification script committed; result PENDING HiPerGator run recorded.
- D-01 label sentence recorded.
- No read.csv/setwd/absolute paths/data.table introduced.
</verification>

<success_criteria>
SDI is staged at the exact path R/116 probes with matching columns (or a documented diff), the second
haircut below 77.7% has a runnable quantifier, and sdi_score's ZCTA-not-ZIP5 nature is documented.
</success_criteria>

<output>
After completion, create `.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-03-SUMMARY.md`.
</output>
