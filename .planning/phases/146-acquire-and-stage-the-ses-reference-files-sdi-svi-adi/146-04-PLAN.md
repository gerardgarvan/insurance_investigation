---
phase: 146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi
plan: 04
type: execute
wave: 3
depends_on: ["146-02"]
files_modified:
  - R/147_build_svi_zcta.R
  - data/reference/svi_2020_zcta_derived.csv
  - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md
autonomous: false
requirements: [SES-02]

must_haves:
  truths:
    - "SVI is produced per the D-02 decision — if D-02a, a committed build script derives ZCTA-level SVI with a per-ZCTA coverage share and an NA floor; if D-02c, no SVI file is produced and the drop is recorded"
    - "The derived SVI file carries vintage, method, and source columns (a derivation is not an acquisition)"
    - "-999 (CDC suppression) is mapped to NA in the build script, before writing, not left for R/116 alone"
    - "An areal mean is never published without its svi_areal_coverage share"
  artifacts:
    - path: "R/147_build_svi_zcta.R"
      provides: "Committed, reproducible SVI-to-ZCTA derivation (D-02a) with recorded method"
      min_lines: 30
    - path: "data/reference/svi_2020_zcta_derived.csv"
      provides: "Derived ZCTA SVI with ZCTA, RPL_THEMES, svi_areal_coverage, vintage, method, source"
      contains: "svi_areal_coverage"
  key_links:
    - from: "data/reference/svi_2020_zcta_derived.csv columns ZCTA + RPL_THEMES"
      to: "R/116 SECTION 6 select(ZIP5 = ZCTA, svi_score = RPL_THEMES)"
      via: "column names matched so R/116 needs no change (path change handled in 146-05)"
      pattern: "ZCTA|RPL_THEMES"
---

<objective>
Produce SVI per the D-02 decision from 146-02. This plan is CONDITIONAL: if D-02 resolved to D-02c
(drop), the executor records the drop and does no derivation. If D-02a (recommended), it writes a
committed build script that derives ZCTA-level SVI with a coverage share and an NA floor — the
areal-mean computation deferred at Phase 144 P0-04, now a prerequisite. CDC publishes no 2020 ZCTA
SVI (locked fact), so this is a derivation with vintage/method/source, never a download.

Purpose: SVI is the "not a download" obstacle. A derived file needs a build script, recorded method,
and a per-ZCTA coverage column, or the reader cannot tell a mean over 3 of 40 tracts from one over 40.
Output: R/147_build_svi_zcta.R + data/reference/svi_2020_zcta_derived.csv (D-02a), or a recorded drop (D-02c).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-RESEARCH.md
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md
@R/116_encounter_ses_index.R

<interfaces>
R/116 SECTION 6 (lines 266-290) SVI probe requires columns `ZCTA` + `RPL_THEMES`; it maps RPL_THEMES<0 to NA
and joins ZIP5 = ZCTA. The derived file's output columns MUST be `ZCTA` (5-char string) and `RPL_THEMES`
(numeric) plus the derivation columns, so R/116 needs no logic change (only the PATH changes from
svi_2020_us_by_zcta.csv to svi_2020_zcta_derived.csv — that path edit is done in 146-05).
Derived-file convention (Phase 144/145): every non-downloaded reference file carries vintage, method, source.
findSVI path needs a Census API key (146-DISCOVERY.md records availability); fallback = CDC tract CSV + HUD USPS crosswalk RES_RATIO weights.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Branch on the D-02 decision (skip this plan if D-02c)</name>
  <read_first>
    - .planning/phases/146-.../146-DISCOVERY.md (recorded D-02 option; findSVI-vs-HUD; Census API key status; coverage floor)
  </read_first>
  <decision>Confirm the D-02 option recorded in 146-02 before building anything.</decision>
  <context>
    If 146-DISCOVERY.md records D-02c (drop): record the drop reason in 146-DISCOVERY.md, do NOT create
    R/147 or the derived CSV, and skip Tasks 2-3 (146-05 will describe SVI as dropped). If D-02b (county):
    the build path differs (join ZIP5->county SVI) and must NEVER share a column with tract-derived values —
    document the coarser grain explicitly. If D-02a: proceed to Tasks 2-3.
  </context>
  <options>
    <option id="proceed-a">
      <name>D-02a recorded — build the derivation (Tasks 2-3)</name>
      <pros>Produces the correct ZCTA-level measurement with coverage share</pros>
      <cons>Requires API key or HUD crosswalk</cons>
    </option>
    <option id="proceed-b">
      <name>D-02b recorded — build county-level join, label the coarser grain</name>
      <pros>No derivation cost</pros>
      <cons>Coarse; must be clearly labelled county-level, not ZCTA</cons>
    </option>
    <option id="skip-c">
      <name>D-02c recorded — drop SVI, record it, skip Tasks 2-3</name>
      <pros>Honest; no incorrect column</pros>
      <cons>Two indices, not three</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The executor confirms which D-02 option 146-DISCOVERY.md records and states which of Tasks 2-3 apply.
    - If D-02c: 146-DISCOVERY.md records the drop and Tasks 2-3 are explicitly skipped (no R/147, no derived CSV).
  </acceptance_criteria>
  <resume-signal>Confirm D-02 option; proceed with Tasks 2-3 (a/b) or skip (c)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write R/147_build_svi_zcta.R (D-02a/b only)</name>
  <read_first>
    - .planning/phases/146-.../146-DISCOVERY.md (SVI derivation path, API-key status, coverage floor)
    - .planning/phases/146-.../146-RESEARCH.md ("SVI Build Script Requirements (D-02a)" and the HUD fallback section)
    - R/116_encounter_ses_index.R SECTION 6 (the ZCTA/RPL_THEMES output contract)
  </read_first>
  <behavior>
    - Given tract-level SVI + population weights, output has one row per ZCTA with numeric RPL_THEMES in [0,1] or NA.
    - Any input RPL_THEMES < 0 (CDC -999 suppression) becomes NA before aggregation/writing.
    - Each ZCTA carries svi_areal_coverage = share of ZCTA population represented by contributing tracts; a ZCTA
      whose coverage is below SVI_COVERAGE_FLOOR (named constant, default 0.80) gets RPL_THEMES = NA.
    - Output columns include ZCTA, RPL_THEMES, svi_areal_coverage, vintage, method, source.
  </behavior>
  <action>
    Create `R/147_build_svi_zcta.R`. Path per Claude's-discretion numbering (147 = next free investigation number;
    confirm no existing R/147 first). Follow the D-02a method recorded in 146-DISCOVERY.md:
    - findSVI path: `findSVI::find_svi(year=2020, geography="zcta", ...)` (needs Census API key); OR
    - HUD fallback: load CDC 2020 tract SVI CSV (key FIPS, value RPL_THEMES), join HUD USPS tract->ZIP crosswalk,
      population-weight by RES_RATIO, sum within ZCTA, and set svi_areal_coverage = sum of contributing RES_RATIO.
    Define `SVI_COVERAGE_FLOOR <- 0.80` (named constant, not a magic number). Apply
    `RPL_THEMES = if_else(RPL_THEMES < 0, NA_real_, RPL_THEMES)` BEFORE aggregating. Add vintage="2020",
    method=(exact string describing the path used), source=(citation). Use dplyr only (no data.table), here()/
    file.path() (no setwd/absolute), and read ZIP/FIPS keys as character with explicit col_types. Install findSVI/
    tidycensus/HUD deps interactively + renv::snapshot() (NOT inside any SLURM script). If D-02b was chosen instead,
    build the ZIP5->county SVI join here and label every output value county-level; do not compute svi_areal_coverage.
  </action>
  <verify>
    <automated>MISSING — no R runtime on this box. Verify statically: `grep -E "SVI_COVERAGE_FLOOR|RPL_THEMES < 0|vintage|svi_areal_coverage" R/147_build_svi_zcta.R` matches; `grep -vqi "setwd\|read.csv\|data.table" R/147_build_svi_zcta.R`.</automated>
  </verify>
  <acceptance_criteria>
    - R/147_build_svi_zcta.R defines `SVI_COVERAGE_FLOOR <- 0.80` (or the value recorded in 146-DISCOVERY.md).
    - It maps RPL_THEMES < 0 to NA before aggregation (`grep "RPL_THEMES < 0"`).
    - It sets vintage, method, source and (D-02a) computes svi_areal_coverage.
    - No setwd/read.csv/data.table/absolute paths (grep confirms absent).
  </acceptance_criteria>
  <done>Build script written implementing the recorded D-02 method with coverage floor, -999 guard, and vintage/method/source.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run R/147 on a machine with the API key / network and commit the derived CSV</name>
  <read_first>
    - R/147_build_svi_zcta.R (the script just written)
    - .planning/phases/146-.../146-DISCOVERY.md (Census API key status; SVI commit-to-repo verdict)
  </read_first>
  <what-built>
    R/147 derives the file but needs a Census API key (findSVI) or the HUD crosswalk + tract CSV, plus network
    — not available on this box. A human runs it where those exist (login node / Windows box).
  </what-built>
  <how-to-verify>
    Run `Rscript R/147_build_svi_zcta.R` on a machine with the API key/inputs. Confirm
    `data/reference/svi_2020_zcta_derived.csv` is produced with columns ZCTA, RPL_THEMES, svi_areal_coverage,
    vintage, method, source, and that some ZCTAs below the coverage floor have RPL_THEMES = NA. If SVI terms
    forbid committing, add the file to .gitignore and record the transfer path in 146-DISCOVERY.md instead.
  </how-to-verify>
  <acceptance_criteria>
    - data/reference/svi_2020_zcta_derived.csv exists with columns ZCTA, RPL_THEMES, svi_areal_coverage, vintage, method, source.
    - At least some rows show RPL_THEMES = NA where svi_areal_coverage < the floor.
    - Commit status handled per the SVI terms verdict in 146-DISCOVERY.md.
  </acceptance_criteria>
  <resume-signal>Report: derived CSV produced (row count, coverage-floor NA count) and commit status</resume-signal>
</task>

</tasks>

<verification>
- If D-02a/b: R/147_build_svi_zcta.R committed; derived CSV produced with the required columns and NA floor.
- If D-02c: drop recorded in 146-DISCOVERY.md; no R/147 or CSV created.
- -999 mapped to NA in the build script; svi_areal_coverage present (D-02a); vintage/method/source present.
- No setwd/read.csv/data.table/absolute paths.
</verification>

<success_criteria>
SVI is handled honestly per the D-02 decision: either a reproducible ZCTA derivation with coverage share
(never an areal mean without it), a clearly-labelled county fallback, or a recorded drop.
</success_criteria>

<output>
After completion, create `.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-04-SUMMARY.md`.
</output>
