---
phase: 146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi
plan: 05
type: execute
wave: 4
depends_on: ["146-03", "146-04"]
files_modified:
  - R/116_encounter_ses_index.R
  - data/reference/README.md
autonomous: true
requirements: [SES-01, SES-02, SES-03, SES-04]

must_haves:
  truths:
    - "R/116 reads the actually-staged files (SVI path -> svi_2020_zcta_derived.csv; SDI/SVI column names reconciled to the staged files) without loosening any join or the many-to-one relationship"
    - "The §4 coverage-expectations table (RUCA/SDI/SVI/ADI vs 77.7% ceiling) is in the summary workbook"
    - "The 77.7% ceiling replaces the 68.6% figure everywhere in README and coverage output"
    - "README describes SVI as a derived file (vintage/method/source), SDI's ZCTA-via-ZIP5 nature (D-01), and the ADI ZIP9 ceiling"
    - "The ZIP5-with-no-ZCTA unmatched count (from 146-03) has a labelled home in the coverage sheet"
  artifacts:
    - path: "R/116_encounter_ses_index.R"
      provides: "Reads staged SVI derived path + reconciled columns; §4 expectations table in workbook"
      contains: "svi_2020_zcta_derived"
    - path: "data/reference/README.md"
      provides: "Corrected SVI (derived), SDI (D-01), ADI 77.7% ceiling documentation"
      contains: "77.7"
  key_links:
    - from: "R/116 SVI_PATH / SECTION 6 select()"
      to: "data/reference/svi_2020_zcta_derived.csv (or dropped) and staged SDI columns"
      via: "path + select() reconciled to the files staged in 146-03/146-04"
      pattern: "svi_2020_zcta_derived|SDI_score|ZCTA"
---

<objective>
Wire R/116 and the README to the files actually staged in Waves 2-3, and correct the coverage story:
add the §4 expectations table, replace 68.6% with 77.7% everywhere, describe SVI as derived, label SDI
as ZCTA-via-ZIP5, and give the ZIP5-with-no-ZCTA count a home. This is the single owner of the shared
files R/116 and README, so 146-03 and 146-04 leave those edits here to avoid parallel-write conflicts.

Purpose: The staged files are inert until R/116 points at them and the workbook/README tell the honest
coverage story. This plan does the consumer-side wiring and documentation, without loosening any join.
Output: updated R/116_encounter_ses_index.R and data/reference/README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-RESEARCH.md
@.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-DISCOVERY.md
@R/116_encounter_ses_index.R
@data/reference/README.md

<interfaces>
R/116 current SVI wiring: `SVI_PATH <- file.path("data","reference","svi_2020_us_by_zcta.csv")` (line 68) and
SECTION 6 `select(ZIP5 = ZCTA, svi_score = RPL_THEMES)` (line 276). If D-02a produced svi_2020_zcta_derived.csv,
SVI_PATH must change to that filename; SECTION 6 stays if the derived columns are ZCTA/RPL_THEMES. If D-02c
(dropped), SVI_PATH points at a file that will never exist and svi_score stays NA by the probe gate — leave the
gate, but README must say SVI was dropped. SDI select() (line 212) stays unless 146-DISCOVERY.md recorded a
column diff. The many-to-one relationship (line 160) and the two fan-out stopifnots (lines 163, 316) must NOT
be relaxed (CONTEXT.md Rule 4 / anti-patterns: do not loosen a join to raise coverage).
Corrected numbers: total 1,950,696; ceiling 77.7% (1,516,469 zip9_observed); 22.3% (434,227) unreachable =
14.1% no_zip5 (275,528) + 8.1% none (158,699). 68.6% is WRONG and must be removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point R/116 at the staged files and add the §4 expectations table</name>
  <read_first>
    - R/116_encounter_ses_index.R (SECTION 2 paths, SECTION 6 select()s, SECTION 9 workbook build)
    - .planning/phases/146-.../146-DISCOVERY.md (which SVI file exists / dropped; any SDI column diff; ZIP5-no-ZCTA count status)
    - .planning/phases/146-.../146-04-SUMMARY.md and 146-03-SUMMARY.md (what was actually staged)
  </read_first>
  <action>
    In R/116:
    1. If D-02a produced `data/reference/svi_2020_zcta_derived.csv`: change `SVI_PATH` (SECTION 2, line 68) to
       `file.path("data","reference","svi_2020_zcta_derived.csv")`. If the derived columns are ZCTA/RPL_THEMES,
       leave SECTION 6's `select(ZIP5 = ZCTA, svi_score = RPL_THEMES)` unchanged; if 146-DISCOVERY.md recorded
       different derived column names, update ONLY that select(). If D-02c (dropped), leave SVI_PATH as-is (the
       probe gate keeps svi_score NA) and add a one-line comment that SVI was dropped per D-02c.
    2. If 146-DISCOVERY.md recorded an SDI column diff, update SECTION 6's `select(ZIP5, sdi_score = SDI_score)`
       to the staged names — do NOT rename the source file (CONTEXT.md §7).
    3. Add a new summary-workbook sheet "Coverage Ceilings" in SECTION 9 containing the CONTEXT.md §4 / RESEARCH
       expectations table: rows RUCA (ZIP5, 77.7% achieved, ZIP availability), SDI (ZCTA via ZIP5, <=77.7% minus
       ZIP5s-with-no-ZCTA), SVI (ZCTA via ZIP5 derived, <=77.7% minus ZCTA match minus tracts below coverage floor),
       ADI (ZIP9, <=77.7%). Include a note row: "22.3% (434,227) of encounters receive no index at any geography:
       14.1% sentinel-ZIP (275,528) + 8.1% no-address-record (158,699)." Include the ZIP5-with-no-ZCTA unmatched
       count from 146-03 (or "PENDING HiPerGator run" if still pending), labelled "ZIP5s with no corresponding ZCTA".
    Do NOT relax the many-to-one relationship (line 160) or either fan-out stopifnot (lines 163, 316). Keep KEY/sheet
    ordering conventions. dplyr only; here()/file.path(); no setwd/absolute paths.
  </action>
  <verify>
    <automated>MISSING — no R runtime on this box. Static checks: `grep -E "svi_2020_zcta_derived|Coverage Ceilings|77.7" R/116_encounter_ses_index.R` matches (unless D-02c, in which case grep for the dropped-comment); `grep -c "many-to-one" R/116_encounter_ses_index.R` still == 1; `grep -c "68.6" R/116_encounter_ses_index.R` == 0.</automated>
  </verify>
  <acceptance_criteria>
    - If D-02a: `grep "svi_2020_zcta_derived" R/116_encounter_ses_index.R` matches SVI_PATH.
    - A "Coverage Ceilings" sheet with the §4 table and the 22.3%/434,227 note is added in SECTION 9.
    - `grep -c "many-to-one" R/116...` == 1 and both fan-out stopifnots remain (relationship NOT relaxed).
    - No "68.6" remains in R/116 (`grep -c 68.6` == 0).
    - No setwd/read.csv/data.table/absolute paths introduced.
  </acceptance_criteria>
  <done>R/116 reads the staged SVI/SDI files with reconciled columns, has the §4 ceiling sheet, and preserves all fan-out guards.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the README SVI/SDI/ADI sections and correct the ceiling</name>
  <read_first>
    - data/reference/README.md (SVI, SDI, ADI sections + the ADI ceiling paragraph)
    - .planning/phases/146-.../146-DISCOVERY.md (D-01 label sentence; what was staged/dropped; registration status)
  </read_first>
  <action>
    In data/reference/README.md:
    1. SVI section: if D-02a, rewrite to describe a DERIVED file `data/reference/svi_2020_zcta_derived.csv` with
       `vintage`, `method`, `source`, and `svi_areal_coverage` columns and the coverage-floor rule — replacing the
       current "portal download of svi_2020_us_by_zcta.csv" text (which described a file CDC does not publish). If
       D-02c, state SVI was dropped and why; if D-02b, state it is county-level, not ZCTA.
    2. SDI section: add the D-01 sentence from 146-DISCOVERY.md — sdi_score is a ZCTA-level value attached through
       ZIP5 (ZCTA approximate; PO-box-only/single-building ZIPs unmatched), NOT a ZIP5-level measure. Note the
       ZIP5-with-no-ZCTA unmatched count (or PENDING) and where it is reported.
    3. ADI section: replace the 68.6% figure with 77.7% everywhere it appears; keep the ZIP9-join / block-group
       validated-linkage note; record the registration status from 146-DISCOVERY.md (registered by {name}/{date}
       or blocked+owner). Confirm no "68.6" remains anywhere in the README.
  </action>
  <verify>
    <automated>MISSING — doc file. Static checks: `grep -c "68.6" data/reference/README.md` == 0; `grep -E "77.7|svi_2020_zcta_derived|ZCTA-level" data/reference/README.md` matches.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "68.6" data/reference/README.md` == 0.
    - README SVI section describes a derived file (or dropped/county per D-02), not a portal download of svi_2020_us_by_zcta.csv.
    - README SDI section contains the D-01 "ZCTA-level value attached through ZIP5 ... not a ZIP5-level measure" wording.
    - README ADI section states 77.7% and the registration status.
  </acceptance_criteria>
  <done>README describes the actually-staged files, states the D-01 label, and uses 77.7% throughout (68.6% removed).</done>
</task>

</tasks>

<verification>
- R/116 points at the staged SVI/SDI files with reconciled columns; §4 ceiling sheet added; fan-out guards intact; no 68.6%.
- README rewritten for derived SVI, D-01 SDI label, and 77.7% ADI ceiling; no 68.6% anywhere.
- No join loosened; no setwd/read.csv/data.table/absolute paths.
</verification>

<success_criteria>
The staged files are live in R/116 and the coverage story is honest and consistent: 77.7% ceiling stated,
§4 table present, SVI documented as derived, SDI labelled ZCTA-via-ZIP5 — with no join loosened to inflate coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-05-SUMMARY.md`.
</output>
