---
phase: 72-defensive-coding
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - R/01_load_pcornet.R
  - R/02_harmonize_payer.R
  - R/03_duckdb_ingest.R
  - R/10_cohort_predicates.R
  - R/11_treatment_payer.R
  - R/12_surveillance.R
  - R/13_survivorship_encounters.R
  - R/14_build_cohort.R
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03

must_haves:
  truths:
    - "Foundation scripts validate CSV/RDS file existence before loading data"
    - "Cohort scripts validate data frame structure after critical loads and joins"
    - "Error messages include script name, expected values, and fix hints"
    - "Assertions do not modify existing tryCatch patterns in R/10 and R/03"
  artifacts:
    - path: "R/01_load_pcornet.R"
      provides: "File existence validation for CSV paths and RDS cache"
      contains: "assert_rds_exists"
    - path: "R/02_harmonize_payer.R"
      provides: "Data structure validation after payer mapping"
      contains: "assert_df_valid"
    - path: "R/14_build_cohort.R"
      provides: "Post-join validation for cohort assembly"
      contains: "warn_row_count"
  key_links:
    - from: "R/01_load_pcornet.R"
      to: "R/utils/utils_assertions.R"
      via: "assert_rds_exists calls for RDS cache validation"
      pattern: "assert_rds_exists"
    - from: "R/14_build_cohort.R"
      to: "R/utils/utils_assertions.R"
      via: "assert_df_valid + warn_row_count after cohort joins"
      pattern: "assert_df_valid|warn_row_count"
---

<objective>
Add checkmate assertions to foundation (00-03) and cohort (10-14) scripts.

Purpose: These 8 scripts are the most upstream in the pipeline -- failures here cascade to everything downstream. File existence checks catch missing RDS cache or CSVs before expensive parsing. Data structure validation catches schema drift after loads and joins. Per D-01, D-05, D-07.

Output: 8 scripts with assertion blocks at entry points and after critical loads/joins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-defensive-coding/72-CONTEXT.md
@.planning/phases/72-defensive-coding/72-RESEARCH.md
@.planning/phases/72-defensive-coding/72-01-SUMMARY.md

@R/utils/utils_assertions.R
</context>

<interfaces>
<!-- Key assertion functions available from Plan 01 -->
```r
assert_rds_exists(path, script_name)
assert_df_valid(df, name, required_cols, script_name, allow_empty = FALSE)
assert_col_types(df, type_spec, script_name)
warn_date_range(df, col, lo, hi, script_name)
warn_row_count(df, name, min_expected = NULL, max_expected = NULL, script_name)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add assertions to foundation scripts (R/01, R/02, R/03)</name>
  <files>R/01_load_pcornet.R, R/02_harmonize_payer.R, R/03_duckdb_ingest.R</files>
  <read_first>
    - R/01_load_pcornet.R (find load_pcornet_table function, identify CSV path check points and RDS cache check points; note existing cache_path and file_path variables)
    - R/02_harmonize_payer.R (find where pcornet$ENROLLMENT is first used, find payer_summary creation point)
    - R/03_duckdb_ingest.R (find rds_path usage at line ~141, note 6 existing tryCatch blocks -- DO NOT MODIFY per D-05)
    - R/utils/utils_assertions.R (confirm function signatures from Plan 01)
    - .planning/phases/72-defensive-coding/72-CONTEXT.md (D-05: leave existing tryCatch and stopifnot as-is)
  </read_first>
  <action>
**R/00_config.R** -- No changes (already handled in Plan 01).

**R/01_load_pcornet.R** -- Add assertions inside `load_pcornet_table()` function:
1. After the CSV `file_path` is constructed (around the line `file_path <- PCORNET_PATHS[table_name]`), add:
   ```r
   # SAFE-01: Validate CSV file exists before attempting load
   checkmate::assert_file_exists(
     file_path,
     access = "r",
     .var.name = glue("[R/01 ERROR] CSV file for {table_name}")
   )
   ```
2. After the cache hit branch where `df <- readRDS(cache_path)` (around line 444), add validation:
   ```r
   # SAFE-02: Validate cached RDS loaded correctly
   assert_df_valid(df, table_name, expected_cols, script_name = "R/01",
                   allow_empty = TRUE)
   ```
   where `expected_cols` is `c("ID")` (PATID renamed to ID is universal across all PCORnet tables).
3. After the main loading loop completes (after all 13 tables loaded into `pcornet` list), add validation of the pcornet list:
   ```r
   # SAFE-02: Validate pcornet list has expected tables
   checkmate::assert_names(
     names(pcornet),
     must.include = c("ENROLLMENT", "DIAGNOSIS", "ENCOUNTER", "PROCEDURES"),
     .var.name = "[R/01 ERROR] pcornet list tables"
   )
   ```

**R/02_harmonize_payer.R** -- Add assertions after data is available:
1. After `source("R/01_load_pcornet.R")` and library calls, add an INPUT VALIDATION section:
   ```r
   # SECTION 0: INPUT VALIDATION ----
   assert_df_valid(
     pcornet$ENROLLMENT, "ENROLLMENT",
     required_cols = c("ID", "PAYER_TYPE_PRIMARY", "ENR_START_DATE"),
     script_name = "R/02"
   )
   assert_col_types(
     pcornet$ENROLLMENT,
     type_spec = list(ID = "character", PAYER_TYPE_PRIMARY = "character"),
     script_name = "R/02"
   )
   ```
2. After `payer_summary` tibble is created (after the main payer harmonization pipeline), add:
   ```r
   # SAFE-02: Validate payer_summary output
   assert_df_valid(
     payer_summary, "payer_summary",
     required_cols = c("ID", "payer_category_mode"),
     script_name = "R/02"
   )
   assert_col_types(
     payer_summary,
     type_spec = list(ID = "character"),
     script_name = "R/02"
   )
   ```

**R/03_duckdb_ingest.R** -- Add assertions sparingly (6 existing tryCatch blocks left as-is per D-05):
1. Before the ingest loop, validate that the RDS cache directory exists:
   ```r
   # SAFE-01: Validate RDS source directory exists
   checkmate::assert_directory_exists(
     CONFIG$cache$raw_dir,
     access = "r",
     .var.name = "[R/03 ERROR] RDS cache directory"
   )
   ```
2. Inside the ingest loop, before `readRDS(rds_path)` (around line 141), add:
   ```r
   assert_rds_exists(rds_path, script_name = "R/03")
   ```
3. After readRDS, add data frame validation:
   ```r
   assert_df_valid(df, table_name, required_cols = c("ID"),
                   script_name = "R/03", allow_empty = TRUE)
   ```

All new assertion blocks must:
- Use a `# SECTION 0: INPUT VALIDATION ----` header when adding at script entry (RStudio outline compatible)
- Follow existing comment and section style
- NOT modify any existing tryCatch or stopifnot (per D-05)
- Keep lines under 150 characters
  </action>
  <verify>
    <automated>grep -c "assert_rds_exists\|assert_df_valid\|assert_col_types\|warn_row_count\|warn_date_range\|assert_file_exists\|assert_directory_exists\|assert_names" R/01_load_pcornet.R R/02_harmonize_payer.R R/03_duckdb_ingest.R</automated>
  </verify>
  <acceptance_criteria>
    - grep "assert_file_exists\|assert_rds_exists" R/01_load_pcornet.R returns at least 1 match
    - grep "assert_df_valid" R/01_load_pcornet.R returns at least 1 match
    - grep "assert_df_valid" R/02_harmonize_payer.R returns at least 2 matches (input + output)
    - grep "assert_col_types" R/02_harmonize_payer.R returns at least 1 match
    - grep "assert_rds_exists\|assert_directory_exists" R/03_duckdb_ingest.R returns at least 1 match
    - grep "assert_df_valid" R/03_duckdb_ingest.R returns at least 1 match
    - grep -c "tryCatch" R/03_duckdb_ingest.R returns same count as before (existing tryCatch untouched)
    - grep "SAFE-01\|SAFE-02" R/01_load_pcornet.R returns at least 1 match (requirement traceability)
  </acceptance_criteria>
  <done>Foundation scripts (01, 02, 03) have file existence checks, data structure validation, and column type assertions at critical points</done>
</task>

<task type="auto">
  <name>Task 2: Add assertions to cohort scripts (R/10, R/11, R/12, R/13, R/14)</name>
  <files>R/10_cohort_predicates.R, R/11_treatment_payer.R, R/12_surveillance.R, R/13_survivorship_encounters.R, R/14_build_cohort.R</files>
  <read_first>
    - R/10_cohort_predicates.R (note 18+ existing tryCatch patterns for DuckDB NULL-guards -- DO NOT MODIFY per D-05; identify main predicate functions)
    - R/11_treatment_payer.R (identify where pcornet$PROCEDURES is accessed and date extraction happens)
    - R/12_surveillance.R (identify where pcornet tables are accessed for surveillance detection)
    - R/13_survivorship_encounters.R (note existing stopifnot at line 64 -- DO NOT MODIFY per D-05; identify key data loads)
    - R/14_build_cohort.R (identify source() chain, find join points where warn_row_count applies, find final cohort output)
    - R/utils/utils_assertions.R (confirm function signatures)
    - .planning/phases/72-defensive-coding/72-CONTEXT.md (D-05, D-08, D-09, D-10)
  </read_first>
  <action>
**R/10_cohort_predicates.R** -- Minimal assertions (functions, not data loaders):
1. Do NOT touch the 18+ tryCatch blocks (D-05).
2. No file loading happens here. Skip file existence checks.
3. Add a comment at the top noting that assertions are handled in R/14_build_cohort.R which consumes these predicates:
   ```r
   # NOTE: Input validation for cohort data handled in R/14_build_cohort.R
   # which sources this file and validates pcornet tables before applying predicates.
   # Existing tryCatch patterns (18+) for DuckDB NULL-guards preserved per D-05.
   ```

**R/11_treatment_payer.R** -- Add assertions at function entry:
1. The main function `compute_payer_at_*()` functions access pcornet tables. Add a validation block at the start of each compute function (or a shared entry point if there is one):
   ```r
   # SAFE-02: Validate required tables exist
   assert_df_valid(
     pcornet$PROCEDURES, "PROCEDURES",
     required_cols = c("ID", "PX", "PX_TYPE", "PX_DATE"),
     script_name = "R/11"
   )
   ```
2. After treatment date joins, add:
   ```r
   warn_row_count(
     result, "treatment payer result",
     min_expected = 1,
     script_name = "R/11"
   )
   ```

**R/12_surveillance.R** -- Add assertions before surveillance detection:
1. At the start of `assemble_surveillance_flags()` or equivalent entry point, validate the pcornet tables used:
   ```r
   # SAFE-02: Validate PROCEDURES table for surveillance modality detection
   assert_df_valid(
     pcornet$PROCEDURES, "PROCEDURES",
     required_cols = c("ID", "PX", "PX_TYPE", "PX_DATE"),
     script_name = "R/12"
   )
   ```
2. After surveillance flags assembly, validate output:
   ```r
   assert_df_valid(
     surveillance_flags, "surveillance_flags",
     required_cols = c("ID"),
     script_name = "R/12"
   )
   ```

**R/13_survivorship_encounters.R** -- Add assertions (preserve existing stopifnot at line 64):
1. At the function entry for classify_survivorship_encounters(), validate inputs:
   ```r
   # SAFE-02: Validate cohort input for survivorship classification
   assert_df_valid(
     cohort_data, "cohort for survivorship",
     required_cols = c("ID"),
     script_name = "R/13"
   )
   ```
   Use whatever parameter name the function actually uses for its cohort input.
2. Do NOT touch the stopifnot at line 64 (D-05).

**R/14_build_cohort.R** -- Most critical script for assertions:
1. After source() chain completes and pcornet tables are available, add INPUT VALIDATION section:
   ```r
   # SECTION 0: INPUT VALIDATION ----

   # SAFE-01: Validate critical input tables
   assert_df_valid(
     pcornet$ENROLLMENT, "ENROLLMENT",
     required_cols = c("ID", "ENR_START_DATE", "ENR_END_DATE"),
     script_name = "R/14"
   )
   assert_df_valid(
     pcornet$DIAGNOSIS, "DIAGNOSIS",
     required_cols = c("ID", "DX", "DX_DATE"),
     script_name = "R/14"
   )
   assert_df_valid(
     pcornet$ENCOUNTER, "ENCOUNTER",
     required_cols = c("ID", "ENCOUNTERID", "ADMIT_DATE", "ENC_TYPE"),
     script_name = "R/14"
   )
   assert_col_types(
     pcornet$ENROLLMENT,
     type_spec = list(ID = "character"),
     script_name = "R/14"
   )
   ```
2. After the main cohort filter chain produces the final `cohort` data frame, add:
   ```r
   # SAFE-02: Validate final cohort output
   assert_df_valid(
     cohort, "final HL cohort",
     required_cols = c("ID", "payer_category_mode"),
     script_name = "R/14"
   )
   assert_col_types(
     cohort,
     type_spec = list(ID = "character"),
     script_name = "R/14"
   )
   ```
3. After each major left_join in the cohort assembly (e.g., payer join, surveillance join, survivorship join), add warn_row_count:
   ```r
   n_before <- nrow(cohort)
   cohort <- cohort %>% left_join(payer_summary, by = "ID")
   warn_row_count(
     cohort, "cohort after payer join",
     min_expected = n_before,
     max_expected = as.integer(n_before * 1.05),
     script_name = "R/14"
   )
   ```

All assertion code must:
- Use `# SAFE-01` or `# SAFE-02` comment tags for requirement traceability
- NOT modify existing tryCatch (R/10 has 18+) or stopifnot (R/13 line 64) per D-05
- Keep lines under 150 characters
- Use the actual variable names and column names found in each script (read the script first)
  </action>
  <verify>
    <automated>grep -c "assert_df_valid\|assert_col_types\|warn_row_count\|assert_rds_exists" R/11_treatment_payer.R R/12_surveillance.R R/13_survivorship_encounters.R R/14_build_cohort.R</automated>
  </verify>
  <acceptance_criteria>
    - grep "assert_df_valid" R/14_build_cohort.R returns at least 3 matches (input tables + output)
    - grep "warn_row_count" R/14_build_cohort.R returns at least 1 match (after join)
    - grep "assert_col_types" R/14_build_cohort.R returns at least 1 match
    - grep "assert_df_valid" R/11_treatment_payer.R returns at least 1 match
    - grep "assert_df_valid" R/12_surveillance.R returns at least 1 match
    - grep -c "tryCatch" R/10_cohort_predicates.R unchanged from before (18+ patterns preserved)
    - grep -c "stopifnot" R/13_survivorship_encounters.R unchanged from before (line 64 preserved)
    - grep "SAFE-01\|SAFE-02" R/14_build_cohort.R returns at least 2 matches
    - No lines in modified files exceed 150 characters
  </acceptance_criteria>
  <done>Cohort scripts (10-14) have input validation, post-join row count warnings, and data structure assertions at critical points without modifying existing defensive code</done>
</task>

</tasks>

<verification>
1. Foundation scripts validate file/directory existence before loading
2. Cohort scripts validate data frame structure after loads and joins
3. Existing tryCatch (R/10, R/03) and stopifnot (R/13) patterns untouched
4. All error messages follow [R/XX ACTION] format with glue() interpolation
5. New code passes lintr constraints (150-char lines, magrittr pipe)
</verification>

<success_criteria>
- 8 scripts in decades 00-14 have assertion blocks at entry points and after critical operations
- Assertions use the 5 helper functions from utils_assertions.R (Plan 01)
- No false positives on valid data (assertions check structure, not specific values)
- Existing defensive code (tryCatch, stopifnot) completely untouched per D-05
</success_criteria>

<output>
After completion, create `.planning/phases/72-defensive-coding/72-02-SUMMARY.md`
</output>
