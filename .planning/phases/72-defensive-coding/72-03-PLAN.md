---
phase: 72-defensive-coding
plan: 03
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - R/20_treatment_inventory.R
  - R/21_investigate_unmatched.R
  - R/22_investigate_unmatched_ndc.R
  - R/23_combine_reports.R
  - R/24_treatment_codes_resolved.R
  - R/25_treatment_durations.R
  - R/26_treatment_episodes.R
  - R/27_drug_name_resolution.R
  - R/28_episode_classification.R
  - R/29_first_line_and_death_analysis.R
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03

must_haves:
  truths:
    - "Treatment scripts validate RDS input files exist before readRDS calls"
    - "Data structure validation catches missing columns after critical loads"
    - "Post-join row count warnings detect cartesian products and data loss"
    - "Date range warnings flag implausible dates without stopping execution"
    - "Existing tryCatch patterns in R/21, R/22, R/27, R/28 are untouched"
  artifacts:
    - path: "R/25_treatment_durations.R"
      provides: "Validated treatment duration extraction with post-load assertions"
      contains: "assert_df_valid"
    - path: "R/26_treatment_episodes.R"
      provides: "Episode analysis with RDS existence checks and date range warnings"
      contains: "assert_rds_exists"
    - path: "R/28_episode_classification.R"
      provides: "Episode classification with input validation (stopifnot at line 702 preserved)"
      contains: "assert_df_valid"
    - path: "R/29_first_line_and_death_analysis.R"
      provides: "First-line analysis with 3 RDS existence checks"
      contains: "assert_rds_exists"
  key_links:
    - from: "R/26_treatment_episodes.R"
      to: "R/utils/utils_assertions.R"
      via: "assert_rds_exists for drug_lookup, encounterid_profile, sct_audit RDS files"
      pattern: "assert_rds_exists"
    - from: "R/29_first_line_and_death_analysis.R"
      to: "R/utils/utils_assertions.R"
      via: "assert_rds_exists for 3 upstream RDS artifacts"
      pattern: "assert_rds_exists"
---

<objective>
Add checkmate assertions to treatment analysis scripts (20-29).

Purpose: Treatment scripts consume RDS artifacts from upstream scripts and perform complex multi-table joins. Missing input files and schema drift are the most common failure modes. File existence checks prevent 5+ minute computation on missing inputs (fail-fast per Pitfall 4). Post-join validation catches cartesian products from bad keys.

Output: 10 treatment scripts with assertion blocks at entry points and after critical loads/joins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-defensive-coding/72-CONTEXT.md
@.planning/phases/72-defensive-coding/72-RESEARCH.md
@.planning/phases/72-defensive-coding/72-01-SUMMARY.md

@R/utils/utils_assertions.R
</context>

<interfaces>
<!-- Key assertion functions available from Plan 01 -->
```r
assert_rds_exists(path, script_name)
assert_df_valid(df, name, required_cols, script_name, allow_empty = FALSE)
assert_col_types(df, type_spec, script_name)
warn_date_range(df, col, lo, hi, script_name)
warn_row_count(df, name, min_expected = NULL, max_expected = NULL, script_name)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add assertions to R/20-R/24 (inventory, investigation, reports, codes)</name>
  <files>R/20_treatment_inventory.R, R/21_investigate_unmatched.R, R/22_investigate_unmatched_ndc.R, R/23_combine_reports.R, R/24_treatment_codes_resolved.R</files>
  <read_first>
    - R/20_treatment_inventory.R (sources 00_config and 01_load_pcornet; uses pcornet tables directly)
    - R/21_investigate_unmatched.R (sources 00_config and 01_load_pcornet; has readRDS at line 552 for classified codes; has tryCatch patterns -- DO NOT MODIFY)
    - R/22_investigate_unmatched_ndc.R (sources 00_config and 01_load_pcornet; has readRDS at line 785; has tryCatch patterns -- DO NOT MODIFY)
    - R/23_combine_reports.R (sources 00_config; readRDS at lines 64-65 for HCPCS_RDS and NDC_RDS)
    - R/24_treatment_codes_resolved.R (sources 00_config; creates xlsx output from TREATMENT_CODES config)
    - R/utils/utils_assertions.R (function signatures)
    - .planning/phases/72-defensive-coding/72-CONTEXT.md (D-05 leave tryCatch as-is, D-07 full validation, D-10 severity levels)
  </read_first>
  <action>
**R/20_treatment_inventory.R:**
1. After source() calls, add INPUT VALIDATION section:
   ```r
   # SECTION 0: INPUT VALIDATION ----
   # SAFE-02: Validate critical PCORnet tables for treatment inventory
   assert_df_valid(
     pcornet$PROCEDURES, "PROCEDURES",
     required_cols = c("ID", "PX", "PX_TYPE", "PX_DATE"),
     script_name = "R/20"
   )
   assert_df_valid(
     pcornet$PRESCRIBING, "PRESCRIBING",
     required_cols = c("ID", "RXNORM_CUI"),
     script_name = "R/20"
   )
   ```

**R/21_investigate_unmatched.R:**
1. After source() calls, add table validation:
   ```r
   # SECTION 0: INPUT VALIDATION ----
   assert_df_valid(
     pcornet$PROCEDURES, "PROCEDURES",
     required_cols = c("ID", "PX", "PX_TYPE"),
     script_name = "R/21"
   )
   ```
2. Before `readRDS(classified_codes_path)` at line 552, add:
   ```r
   assert_rds_exists(classified_codes_path, script_name = "R/21")
   ```
3. Do NOT touch existing tryCatch patterns.

**R/22_investigate_unmatched_ndc.R:**
1. After source() calls, add table validation:
   ```r
   # SECTION 0: INPUT VALIDATION ----
   assert_df_valid(
     pcornet$PRESCRIBING, "PRESCRIBING",
     required_cols = c("ID", "RXNORM_CUI"),
     script_name = "R/22"
   )
   ```
2. Before `readRDS(classified_codes_path)` at line 785, add:
   ```r
   assert_rds_exists(classified_codes_path, script_name = "R/22")
   ```
3. Do NOT touch existing tryCatch patterns.

**R/23_combine_reports.R:**
1. Before the two readRDS calls at lines 64-65, add:
   ```r
   # SAFE-01: Validate input RDS files exist
   assert_rds_exists(HCPCS_RDS, script_name = "R/23")
   assert_rds_exists(NDC_RDS, script_name = "R/23")
   ```
2. After both are loaded, validate structure:
   ```r
   assert_df_valid(hcpcs_classified, "hcpcs_classified",
                   required_cols = c("code", "classification"),
                   script_name = "R/23")
   assert_df_valid(ndc_classified, "ndc_classified",
                   required_cols = c("code", "classification"),
                   script_name = "R/23")
   ```

**R/24_treatment_codes_resolved.R:**
1. This script works from CONFIG constants, no external data files to validate. Add minimal validation:
   ```r
   # SECTION 0: INPUT VALIDATION ----
   # SAFE-02: Validate TREATMENT_CODES config structure
   checkmate::assert_list(
     TREATMENT_CODES,
     min.len = 1,
     .var.name = "[R/24 ERROR] TREATMENT_CODES config"
   )
   ```

Use actual variable names found in each script. Confirm column names by reading each script's header block (Inputs section) and the actual data access patterns.
  </action>
  <verify>
    <automated>grep -c "assert_rds_exists\|assert_df_valid\|assert_col_types\|assert_list" R/20_treatment_inventory.R R/21_investigate_unmatched.R R/22_investigate_unmatched_ndc.R R/23_combine_reports.R R/24_treatment_codes_resolved.R</automated>
  </verify>
  <acceptance_criteria>
    - grep "assert_df_valid" R/20_treatment_inventory.R returns at least 1 match
    - grep "assert_rds_exists" R/21_investigate_unmatched.R returns at least 1 match
    - grep "assert_rds_exists" R/22_investigate_unmatched_ndc.R returns at least 1 match
    - grep "assert_rds_exists" R/23_combine_reports.R returns at least 2 matches (2 RDS files)
    - grep "assert_df_valid" R/23_combine_reports.R returns at least 2 matches (2 data frames)
    - grep "assert_list" R/24_treatment_codes_resolved.R returns at least 1 match
    - grep -c "tryCatch" R/21_investigate_unmatched.R unchanged (existing patterns preserved)
    - grep -c "tryCatch" R/22_investigate_unmatched_ndc.R unchanged (existing patterns preserved)
  </acceptance_criteria>
  <done>Treatment scripts R/20-R/24 have file existence checks before readRDS and data structure validation after loads</done>
</task>

<task type="auto">
  <name>Task 2: Add assertions to R/25-R/29 (durations, episodes, classification, first-line)</name>
  <files>R/25_treatment_durations.R, R/26_treatment_episodes.R, R/27_drug_name_resolution.R, R/28_episode_classification.R, R/29_first_line_and_death_analysis.R</files>
  <read_first>
    - R/25_treatment_durations.R (sources 00_config + 01_load_pcornet; creates OUTPUT_RDS; extracts dates from 7 tables)
    - R/26_treatment_episodes.R (sources 00_config + 01_load_pcornet + 25_treatment_durations; readRDS at lines 662, 1130, 1171 for drug_lookup, encounterid_profile, sct_audit)
    - R/27_drug_name_resolution.R (sources 00_config + utils_duckdb; readRDS at line 303 for cached_lookups; has tryCatch -- DO NOT MODIFY)
    - R/28_episode_classification.R (sources 00_config + utils_duckdb + utils_dates; readRDS at lines 396, 399 for episodes and detail; existing stopifnot at line 702 -- DO NOT MODIFY per D-05)
    - R/29_first_line_and_death_analysis.R (sources 00_config + utils_duckdb + utils_dates; readRDS at lines 68, 75, 82 for 3 RDS files)
    - R/utils/utils_assertions.R (function signatures)
    - .planning/phases/72-defensive-coding/72-CONTEXT.md (D-05, D-08, D-09, D-10)
  </read_first>
  <action>
**R/25_treatment_durations.R:**
1. After source() calls and output path definitions, add INPUT VALIDATION:
   ```r
   # SECTION 1b: INPUT VALIDATION ----
   # SAFE-02: Validate critical PCORnet tables for treatment date extraction
   assert_df_valid(
     pcornet$PROCEDURES, "PROCEDURES",
     required_cols = c("ID", "PX", "PX_TYPE", "PX_DATE"),
     script_name = "R/25"
   )
   assert_col_types(
     pcornet$PROCEDURES,
     type_spec = list(ID = "character"),
     script_name = "R/25"
   )
   ```
2. After the final `treatment_durations` tibble is assembled (before saveRDS), add:
   ```r
   # SAFE-02: Validate treatment_durations output
   assert_df_valid(
     treatment_durations, "treatment_durations",
     required_cols = c("ID", "treatment_type", "first_date", "last_date"),
     script_name = "R/25"
   )
   warn_date_range(treatment_durations, "first_date",
                   as.Date("1990-01-01"), as.Date("2030-12-31"),
                   script_name = "R/25")
   ```

**R/26_treatment_episodes.R:**
1. Before each readRDS call (lines ~662, ~1130, ~1171), add assert_rds_exists:
   ```r
   assert_rds_exists(DRUG_LOOKUP_RDS, script_name = "R/26")
   # ... then readRDS(DRUG_LOOKUP_RDS)

   assert_rds_exists(PROFILE_RDS, script_name = "R/26")
   # ... then readRDS(PROFILE_RDS)

   assert_rds_exists(AUDIT_RDS, script_name = "R/26")
   # ... then readRDS(AUDIT_RDS)
   ```
2. After the main episode tibble is assembled, add date range warning:
   ```r
   warn_date_range(treatment_episodes, "episode_start",
                   as.Date("1990-01-01"), as.Date("2030-12-31"),
                   script_name = "R/26")
   warn_date_range(treatment_episodes, "episode_stop",
                   as.Date("1990-01-01"), as.Date("2030-12-31"),
                   script_name = "R/26")
   ```

**R/27_drug_name_resolution.R:**
1. Minimal assertions (heavy tryCatch usage -- DO NOT MODIFY per D-05):
   ```r
   # SECTION 0: INPUT VALIDATION ----
   # SAFE-01: Validate DuckDB connection available
   checkmate::assert_class(
     pcornet_con, "duckdb_connection",
     .var.name = "[R/27 ERROR] DuckDB connection"
   )
   ```
2. Before `readRDS(CACHE_FILE)` at line 303, add existence check only if the file is expected to exist (check if there's an `if (file.exists(...))` guard already):
   - If already guarded by file.exists(), add a comment noting validation is handled by existing guard
   - If NOT guarded, add `assert_rds_exists(CACHE_FILE, script_name = "R/27")`

**R/28_episode_classification.R:**
1. Before readRDS calls at lines 396, 399:
   ```r
   # SAFE-01: Validate input RDS artifacts
   assert_rds_exists(OUTPUT_RDS, script_name = "R/28")
   assert_rds_exists(DETAIL_RDS, script_name = "R/28")
   ```
2. After loading, validate structure:
   ```r
   assert_df_valid(episodes, "treatment_episodes",
                   required_cols = c("ID", "treatment_type", "episode_number",
                                     "episode_start", "episode_stop"),
                   script_name = "R/28")
   assert_df_valid(episode_detail, "treatment_episode_detail",
                   required_cols = c("ID", "treatment_type", "treatment_date"),
                   script_name = "R/28")
   ```
3. Do NOT touch stopifnot at line 702 (D-05).

**R/29_first_line_and_death_analysis.R:**
1. Before the 3 readRDS calls at lines 68, 75, 82:
   ```r
   # SAFE-01: Validate all 3 input RDS files exist before loading
   assert_rds_exists(OUTPUT_RDS, script_name = "R/29")
   assert_rds_exists(DETAIL_RDS, script_name = "R/29")
   assert_rds_exists(DEATH_RDS, script_name = "R/29")
   ```
2. After loading each, validate structure:
   ```r
   assert_df_valid(episodes, "treatment_episodes",
                   required_cols = c("ID", "treatment_type", "episode_number"),
                   script_name = "R/29")
   assert_df_valid(episode_detail, "treatment_episode_detail",
                   required_cols = c("ID", "treatment_type"),
                   script_name = "R/29")
   assert_df_valid(validated_deaths, "validated_deaths",
                   required_cols = c("ID"),
                   script_name = "R/29", allow_empty = TRUE)
   ```

Use actual variable names and paths found in each script. Verify column names against the script's actual data access patterns.
  </action>
  <verify>
    <automated>grep -c "assert_rds_exists\|assert_df_valid\|assert_col_types\|warn_date_range\|warn_row_count" R/25_treatment_durations.R R/26_treatment_episodes.R R/27_drug_name_resolution.R R/28_episode_classification.R R/29_first_line_and_death_analysis.R</automated>
  </verify>
  <acceptance_criteria>
    - grep "assert_df_valid" R/25_treatment_durations.R returns at least 2 matches (input + output)
    - grep "warn_date_range" R/25_treatment_durations.R returns at least 1 match
    - grep "assert_rds_exists" R/26_treatment_episodes.R returns at least 3 matches (drug_lookup, profile, audit)
    - grep "warn_date_range" R/26_treatment_episodes.R returns at least 1 match
    - grep "assert_rds_exists" R/28_episode_classification.R returns at least 2 matches
    - grep "assert_df_valid" R/28_episode_classification.R returns at least 2 matches
    - grep "assert_rds_exists" R/29_first_line_and_death_analysis.R returns at least 3 matches
    - grep "assert_df_valid" R/29_first_line_and_death_analysis.R returns at least 3 matches
    - grep -c "stopifnot" R/28_episode_classification.R unchanged (line 702 preserved)
    - grep -c "tryCatch" R/27_drug_name_resolution.R unchanged (existing patterns preserved)
  </acceptance_criteria>
  <done>Treatment scripts R/25-R/29 have RDS existence checks, data frame validation, date range warnings, and row count sanity checks at critical points</done>
</task>

</tasks>

<verification>
1. All 10 treatment scripts have assertion blocks
2. RDS files validated before readRDS calls (fail-fast per SAFE-01)
3. Data structure validated after loads (SAFE-02)
4. Date range warnings use 1990-2030 boundaries (D-09)
5. Existing tryCatch (R/21, R/22, R/27) and stopifnot (R/28) untouched (D-05)
6. Error messages follow [R/XX ACTION] format with glue() (SAFE-03)
</verification>

<success_criteria>
- 10 scripts in decade 20-29 have assertion blocks
- Every readRDS call is preceded by assert_rds_exists
- Post-load data frames validated for structure and key column presence
- Date columns validated with warn_date_range where applicable
- No false positives on valid data
</success_criteria>

<output>
After completion, create `.planning/phases/72-defensive-coding/72-03-SUMMARY.md`
</output>
