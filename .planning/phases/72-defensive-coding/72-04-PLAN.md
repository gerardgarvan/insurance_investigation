---
phase: 72-defensive-coding
plan: 04
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - R/40_cancer_site_frequency.R
  - R/41_extract_all_codes.R
  - R/42_build_code_descriptions.R
  - R/43_cancer_site_confirmation.R
  - R/44_cancer_site_confirmation_7day.R
  - R/45_cancer_summary.R
  - R/46_cancer_summary_table.R
  - R/47_cancer_summary_refined.R
  - R/48_cancer_summary_post_hl.R
  - R/49_cancer_summary_pre_post.R
  - R/50_all_codes_resolved.R
  - R/51_gantt_data_export.R
  - R/52_gantt_v2_export.R
  - R/53_death_date_validation.R
  - R/60_tiered_same_day_payer.R
  - R/61_tiered_encounter_level.R
  - R/62_tiered_date_level.R
  - R/63_value_audit.R
  - R/64_all_source_missingness.R
  - R/65_uf_insurance_missingness.R
  - R/66_all_site_duplicate_dates.R
  - R/67_multi_source_overlap_detection.R
  - R/68_overlap_classification.R
  - R/69_per_patient_source_detection.R
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03

must_haves:
  truths:
    - "Cancer scripts validate RDS/CSV input files exist before loading"
    - "Gantt export scripts validate all 4+ RDS dependencies before any are loaded"
    - "Payer/QA scripts validate DuckDB connection or pcornet tables exist"
    - "Post-load data frames validated for required columns"
    - "Error messages identify which upstream script must be run first"
  artifacts:
    - path: "R/51_gantt_data_export.R"
      provides: "Gantt export with 5+ RDS existence checks"
      contains: "assert_rds_exists"
    - path: "R/52_gantt_v2_export.R"
      provides: "Gantt v2 export with RDS existence checks"
      contains: "assert_rds_exists"
    - path: "R/60_tiered_same_day_payer.R"
      provides: "Tiered payer with ENCOUNTER table validation"
      contains: "assert_df_valid"
    - path: "R/62_tiered_date_level.R"
      provides: "Date-level payer with episodes RDS validation"
      contains: "assert_rds_exists"
  key_links:
    - from: "R/51_gantt_data_export.R"
      to: "R/utils/utils_assertions.R"
      via: "assert_rds_exists for episodes, detail, deaths, cohort, descriptions RDS"
      pattern: "assert_rds_exists"
    - from: "R/60_tiered_same_day_payer.R"
      to: "R/utils/utils_assertions.R"
      via: "assert_df_valid for ENCOUNTER table"
      pattern: "assert_df_valid"
---

<objective>
Add checkmate assertions to cancer (40-53) and payer/QA (60-69) scripts.

Purpose: Cancer scripts (14 files) process RDS artifacts, CSVs, and pcornet tables. Several Gantt export scripts load 4-5 RDS files each -- a single missing file wastes minutes of computation. Payer/QA scripts (10 files) access DuckDB or pcornet tables. Assertions at entry points catch dependency failures immediately. Per D-01, D-07, D-10.

Output: 24 scripts with assertion blocks at entry points and after critical loads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-defensive-coding/72-CONTEXT.md
@.planning/phases/72-defensive-coding/72-RESEARCH.md
@.planning/phases/72-defensive-coding/72-01-SUMMARY.md

@R/utils/utils_assertions.R
</context>

<interfaces>
<!-- Key assertion functions available from Plan 01 -->
```r
assert_rds_exists(path, script_name)
assert_df_valid(df, name, required_cols, script_name, allow_empty = FALSE)
assert_col_types(df, type_spec, script_name)
warn_date_range(df, col, lo, hi, script_name)
warn_row_count(df, name, min_expected = NULL, max_expected = NULL, script_name)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add assertions to cancer scripts (R/40-R/53)</name>
  <files>R/40_cancer_site_frequency.R, R/41_extract_all_codes.R, R/42_build_code_descriptions.R, R/43_cancer_site_confirmation.R, R/44_cancer_site_confirmation_7day.R, R/45_cancer_summary.R, R/46_cancer_summary_table.R, R/47_cancer_summary_refined.R, R/48_cancer_summary_post_hl.R, R/49_cancer_summary_pre_post.R, R/50_all_codes_resolved.R, R/51_gantt_data_export.R, R/52_gantt_v2_export.R, R/53_death_date_validation.R</files>
  <read_first>
    - R/40_cancer_site_frequency.R (sources 00_config + 01_load_pcornet; uses pcornet$DIAGNOSIS)
    - R/41_extract_all_codes.R (sources 00_config + 01_load_pcornet; uses pcornet$DIAGNOSIS)
    - R/42_build_code_descriptions.R (sources 00_config; readRDS at lines 46, 56 for classified codes)
    - R/43_cancer_site_confirmation.R (sources 00_config + 01_load_pcornet; uses pcornet$DIAGNOSIS)
    - R/44_cancer_site_confirmation_7day.R (sources 00_config + 01_load_pcornet; same pattern as R/43)
    - R/45_cancer_summary.R (sources 00_config + 01_load_pcornet; readRDS at lines 394, 406)
    - R/46_cancer_summary_table.R (sources 00_config + 01_load_pcornet; read.csv at line 332)
    - R/47_cancer_summary_refined.R (sources 00_config + 01_load_pcornet; read.csv at line 352)
    - R/48_cancer_summary_post_hl.R (sources 00_config + 01_load_pcornet; readRDS at line 351, read.csv at line 369)
    - R/49_cancer_summary_pre_post.R (sources 00_config + 01_load_pcornet; readRDS at line 343, read.csv at line 397)
    - R/50_all_codes_resolved.R (sources 00_config; readRDS at lines 94, 107)
    - R/51_gantt_data_export.R (sources 00_config + utils; readRDS at lines 79, 83, 418, 443, 492; read.csv at line 97)
    - R/52_gantt_v2_export.R (sources 00_config + utils; readRDS at lines 137, 141, 172, 280, 392)
    - R/53_death_date_validation.R (sources 00_config + utils; readRDS at lines 105, 155, 262)
    - R/utils/utils_assertions.R (function signatures)
    - .planning/phases/72-defensive-coding/72-CONTEXT.md (D-05 leave existing tryCatch as-is, D-07 full validation)
  </read_first>
  <action>
For each of the 14 cancer scripts, apply the following pattern. Read each script first to identify the actual variable names, file paths, and data access patterns.

**Scripts that source 01_load_pcornet and use pcornet tables (R/40, R/41, R/43, R/44, R/45, R/46, R/47, R/48, R/49):**
Add an INPUT VALIDATION section after source() calls:
```r
# SECTION 0: INPUT VALIDATION ----
# SAFE-02: Validate DIAGNOSIS table
assert_df_valid(
  pcornet$DIAGNOSIS, "DIAGNOSIS",
  required_cols = c("ID", "DX", "DX_DATE"),
  script_name = "R/XX"  # Use actual script number
)
```
For scripts using additional tables, validate those too. For scripts reading CSV files (R/46 read.csv at line 332, R/47 at 352, R/48 at 369, R/49 at 397), add file existence check:
```r
checkmate::assert_file_exists(INPUT_CSV, access = "r",
  .var.name = glue("[R/XX ERROR] Input CSV"))
```
For scripts with readRDS calls (R/45 at 394/406, R/48 at 351, R/49 at 343), add assert_rds_exists before each.

**R/42_build_code_descriptions.R (config + RDS only):**
```r
# SAFE-01: Validate classified code RDS files exist
assert_rds_exists(HCPCS_RDS, script_name = "R/42")
assert_rds_exists(NDC_RDS, script_name = "R/42")
```
After loading, validate structure.

**R/50_all_codes_resolved.R (config + RDS):**
Before readRDS at lines 94 and 107:
```r
assert_rds_exists(rds_39_path, script_name = "R/50")
assert_rds_exists(rds_40_path, script_name = "R/50")
```

**R/51_gantt_data_export.R (highest RDS dependency count: 5 readRDS + 1 read.csv):**
Add a consolidated validation block BEFORE any data is loaded:
```r
# SECTION 0: INPUT VALIDATION ----
# SAFE-01: Validate all input artifacts exist (fail-fast before any loading)
assert_rds_exists(EPISODES_RDS, script_name = "R/51")
assert_rds_exists(DETAIL_RDS, script_name = "R/51")
```
Then before each conditional readRDS deeper in the script (validated_deaths at 418, hl_cohort at 443, code_descriptions at 492):
```r
assert_rds_exists(VALIDATED_DEATHS_RDS, script_name = "R/51")
# etc.
```
For the read.csv at line 97:
```r
checkmate::assert_file_exists(CANCER_SUMMARY_CSV, access = "r",
  .var.name = "[R/51 ERROR] Cancer summary CSV")
```
After loading episodes, validate structure:
```r
assert_df_valid(episodes, "treatment_episodes",
  required_cols = c("ID", "treatment_type", "episode_start", "episode_stop"),
  script_name = "R/51")
```

**R/52_gantt_v2_export.R (5 readRDS calls):**
Same pattern as R/51. Add assert_rds_exists before each readRDS at lines 137, 141, 172, 280, 392. After loading episodes and detail, validate structure.

**R/53_death_date_validation.R (3 readRDS calls):**
Before readRDS at lines 105, 155, 262:
```r
assert_rds_exists(EPISODES_RDS, script_name = "R/53")
# ...before readRDS at 155:
assert_rds_exists(COHORT_RDS, script_name = "R/53")
# ...before readRDS at 262:
assert_rds_exists(COHORT_RDS, script_name = "R/53")
```
After loading treatment_episodes, validate and add date range warning:
```r
assert_df_valid(treatment_episodes, "treatment_episodes",
  required_cols = c("ID", "treatment_type", "episode_start"),
  script_name = "R/53")
warn_date_range(treatment_episodes, "episode_start",
  as.Date("1990-01-01"), as.Date("2030-12-31"),
  script_name = "R/53")
```

CRITICAL RULES:
- Do NOT modify any existing tryCatch patterns (D-05)
- Use actual variable names and paths from each script (read the script first)
- Keep lines under 150 characters
- Use [R/XX ERROR] or [R/XX WARNING] format for messages per D-11
  </action>
  <verify>
    <automated>grep -cl "assert_rds_exists\|assert_df_valid\|assert_col_types\|assert_file_exists" R/40_cancer_site_frequency.R R/41_extract_all_codes.R R/42_build_code_descriptions.R R/43_cancer_site_confirmation.R R/44_cancer_site_confirmation_7day.R R/45_cancer_summary.R R/46_cancer_summary_table.R R/47_cancer_summary_refined.R R/48_cancer_summary_post_hl.R R/49_cancer_summary_pre_post.R R/50_all_codes_resolved.R R/51_gantt_data_export.R R/52_gantt_v2_export.R R/53_death_date_validation.R</automated>
  </verify>
  <acceptance_criteria>
    - All 14 cancer scripts contain at least 1 assertion call
    - grep "assert_rds_exists" R/51_gantt_data_export.R returns at least 3 matches
    - grep "assert_rds_exists" R/52_gantt_v2_export.R returns at least 3 matches
    - grep "assert_rds_exists" R/53_death_date_validation.R returns at least 2 matches
    - grep "assert_rds_exists" R/42_build_code_descriptions.R returns at least 2 matches
    - grep "assert_df_valid" R/40_cancer_site_frequency.R returns at least 1 match
    - grep "assert_file_exists" R/46_cancer_summary_table.R returns at least 1 match (CSV check)
    - grep "warn_date_range" R/53_death_date_validation.R returns at least 1 match
    - No lines in modified files exceed 150 characters
  </acceptance_criteria>
  <done>All 14 cancer scripts (R/40-R/53) have assertion blocks validating input files and data structures</done>
</task>

<task type="auto">
  <name>Task 2: Add assertions to payer/QA scripts (R/60-R/69)</name>
  <files>R/60_tiered_same_day_payer.R, R/61_tiered_encounter_level.R, R/62_tiered_date_level.R, R/63_value_audit.R, R/64_all_source_missingness.R, R/65_uf_insurance_missingness.R, R/66_all_site_duplicate_dates.R, R/67_multi_source_overlap_detection.R, R/68_overlap_classification.R, R/69_per_patient_source_detection.R</files>
  <read_first>
    - R/60_tiered_same_day_payer.R (sources 00_config; conditionally sources 01_load_pcornet; uses get_pcornet_table or pcornet$ENCOUNTER)
    - R/61_tiered_encounter_level.R (sources 00_config; conditionally sources 01_load_pcornet; uses ENCOUNTER table)
    - R/62_tiered_date_level.R (sources 00_config; readRDS at line 89 for episodes)
    - R/63_value_audit.R (sources 01_load_pcornet; iterates over pcornet tables)
    - R/64_all_source_missingness.R (sources 02_harmonize_payer; uses pcornet$ENROLLMENT)
    - R/65_uf_insurance_missingness.R (sources 02_harmonize_payer; uses pcornet$ENROLLMENT)
    - R/66_all_site_duplicate_dates.R (sources 00_config; uses DuckDB/pcornet tables)
    - R/67_multi_source_overlap_detection.R (sources 00_config; uses DuckDB/pcornet tables)
    - R/68_overlap_classification.R (sources 00_config; read_csv at lines 75, 90)
    - R/69_per_patient_source_detection.R (sources 00_config; uses DuckDB/pcornet tables)
    - R/utils/utils_assertions.R (function signatures)
    - .planning/phases/72-defensive-coding/72-CONTEXT.md (D-05, D-07, D-10)
  </read_first>
  <action>
For each of the 10 payer/QA scripts, apply assertions based on their data access patterns.

**R/60_tiered_same_day_payer.R:**
After the conditional source/DuckDB setup (after line ~49), add:
```r
# SECTION 1b: INPUT VALIDATION ----
# SAFE-02: Validate ENCOUNTER table is available
# NOTE: Table accessed via get_pcornet_table() or pcornet list depending on backend
```
If DuckDB mode uses `get_pcornet_table("ENCOUNTER")`, validate the result after materialization:
```r
assert_df_valid(
  encounters, "ENCOUNTER",
  required_cols = c("ID", "ENCOUNTERID", "ADMIT_DATE", "ENC_TYPE",
                    "PAYER_TYPE_PRIMARY"),
  script_name = "R/60"
)
```

**R/61_tiered_encounter_level.R:**
Same pattern as R/60. After ENCOUNTER table is loaded/materialized:
```r
assert_df_valid(
  encounters, "ENCOUNTER",
  required_cols = c("ID", "ENCOUNTERID", "ADMIT_DATE", "ENC_TYPE",
                    "PAYER_TYPE_PRIMARY"),
  script_name = "R/61"
)
```

**R/62_tiered_date_level.R:**
Before readRDS at line 89:
```r
# SAFE-01: Validate episodes RDS exists
assert_rds_exists(episodes_path, script_name = "R/62")
```
After loading:
```r
assert_df_valid(episodes, "treatment_episodes",
  required_cols = c("ID", "treatment_type", "episode_start", "episode_stop"),
  script_name = "R/62")
```

**R/63_value_audit.R:**
After source() and pcornet list is available:
```r
# SECTION 0: INPUT VALIDATION ----
# SAFE-02: Validate pcornet list is populated
checkmate::assert_list(pcornet, min.len = 1,
  .var.name = "[R/63 ERROR] pcornet table list")
```

**R/64_all_source_missingness.R:**
After source("R/02_harmonize_payer.R"):
```r
# SECTION 0: INPUT VALIDATION ----
assert_df_valid(
  pcornet$ENROLLMENT, "ENROLLMENT",
  required_cols = c("ID", "PAYER_TYPE_PRIMARY", "SOURCE"),
  script_name = "R/64"
)
```

**R/65_uf_insurance_missingness.R:**
Same pattern as R/64:
```r
assert_df_valid(
  pcornet$ENROLLMENT, "ENROLLMENT",
  required_cols = c("ID", "PAYER_TYPE_PRIMARY", "SOURCE"),
  script_name = "R/65"
)
```

**R/66_all_site_duplicate_dates.R:**
After config/data setup, validate ENCOUNTER table:
```r
assert_df_valid(
  encounters, "ENCOUNTER",
  required_cols = c("ID", "ENCOUNTERID", "ADMIT_DATE", "SOURCE"),
  script_name = "R/66"
)
```

**R/67_multi_source_overlap_detection.R:**
Same pattern -- validate ENCOUNTER after load/materialize:
```r
assert_df_valid(
  encounters, "ENCOUNTER",
  required_cols = c("ID", "ENCOUNTERID", "ADMIT_DATE", "SOURCE"),
  script_name = "R/67"
)
```

**R/68_overlap_classification.R:**
Before read_csv calls at lines 75, 90:
```r
# SAFE-01: Validate input CSV files from R/67
checkmate::assert_file_exists(
  same_date_csv_path, access = "r",
  .var.name = "[R/68 ERROR] Same-date detail CSV -- run R/67 first"
)
checkmate::assert_file_exists(
  same_week_csv_path, access = "r",
  .var.name = "[R/68 ERROR] Same-week detail CSV -- run R/67 first"
)
```
Use the actual path variable names from the script.

**R/69_per_patient_source_detection.R:**
After config/data setup, validate ENCOUNTER:
```r
assert_df_valid(
  encounters, "ENCOUNTER",
  required_cols = c("ID", "ENCOUNTERID", "ADMIT_DATE", "SOURCE"),
  script_name = "R/69"
)
```

CRITICAL: Read each script to find the actual variable names for encounters (might be `enc`, `encounters`, or result of `get_pcornet_table("ENCOUNTER") %>% collect()`). Use whatever variable name the script actually assigns.

All new code must:
- Keep lines under 150 characters
- Use [R/XX ERROR/WARNING] message format
- Not modify existing tryCatch or error handling patterns
  </action>
  <verify>
    <automated>grep -cl "assert_rds_exists\|assert_df_valid\|assert_col_types\|assert_file_exists\|assert_list\|assert_class" R/60_tiered_same_day_payer.R R/61_tiered_encounter_level.R R/62_tiered_date_level.R R/63_value_audit.R R/64_all_source_missingness.R R/65_uf_insurance_missingness.R R/66_all_site_duplicate_dates.R R/67_multi_source_overlap_detection.R R/68_overlap_classification.R R/69_per_patient_source_detection.R</automated>
  </verify>
  <acceptance_criteria>
    - All 10 payer/QA scripts contain at least 1 assertion call
    - grep "assert_df_valid" R/60_tiered_same_day_payer.R returns at least 1 match
    - grep "assert_df_valid" R/61_tiered_encounter_level.R returns at least 1 match
    - grep "assert_rds_exists" R/62_tiered_date_level.R returns at least 1 match
    - grep "assert_list" R/63_value_audit.R returns at least 1 match
    - grep "assert_df_valid" R/64_all_source_missingness.R returns at least 1 match
    - grep "assert_file_exists" R/68_overlap_classification.R returns at least 2 matches (2 CSV inputs)
    - No lines in modified files exceed 150 characters
    - No existing tryCatch or error handling patterns modified
  </acceptance_criteria>
  <done>All 10 payer/QA scripts (R/60-R/69) have assertion blocks validating input tables, RDS files, and CSV dependencies</done>
</task>

</tasks>

<verification>
1. All 24 scripts in decades 40-69 have assertion blocks
2. RDS/CSV files validated before read operations
3. Data frames validated for structure and required columns after loads
4. Error messages include script name and fix hints (run R/XX first)
5. Date range warnings applied where date columns are processed
6. No existing defensive code patterns modified
</verification>

<success_criteria>
- 14 cancer scripts and 10 payer/QA scripts have assertion blocks
- Every readRDS call preceded by assert_rds_exists
- Every data frame load followed by assert_df_valid for critical columns
- Error messages follow consistent [R/XX ACTION] format with fix hints
- New code passes lintr (150-char lines, magrittr pipe)
</success_criteria>

<output>
After completion, create `.planning/phases/72-defensive-coding/72-04-SUMMARY.md`
</output>
