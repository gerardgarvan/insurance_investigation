---
phase: 20-check-duplicate-dates-of-flm-subjects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/19_flm_duplicate_dates.R
autonomous: false
requirements:
  - FLMDUP-01
  - FLMDUP-02
  - FLMDUP-03
  - FLMDUP-04

must_haves:
  truths:
    - "User can see same-date duplicate encounter counts for all FLM patients (not just HL cohort)"
    - "User can see exact row duplicates (fully identical rows) detected separately from same-date collisions"
    - "User can see which SOURCE values contribute to duplicate-date encounters (multi-source dates)"
    - "User can compare payer data completeness across sources for multi-source duplicate encounters"
    - "User can see source-preference recommendation based on payer completeness rates"
    - "User can see 3 CSV files in output/tables/ with patient-level, date-level, and aggregate summaries"
  artifacts:
    - path: "R/19_flm_duplicate_dates.R"
      provides: "Standalone FLM duplicate date diagnostic script"
      min_lines: 200
    - path: "output/tables/flm_patient_duplicate_summary.csv"
      provides: "Patient-level duplicate summary with encounter counts, duplicate dates, multi-source dates, payer completeness per source"
    - path: "output/tables/flm_date_level_duplicate_detail.csv"
      provides: "Date-level detail for multi-source encounters with SOURCE, ENC_TYPE, payer fields per row"
    - path: "output/tables/flm_duplicate_aggregate_summary.csv"
      provides: "Aggregate rates (duplicate rates, multi-source rates) and source completeness comparison"
  key_links:
    - from: "R/19_flm_duplicate_dates.R"
      to: "R/01_load_pcornet.R"
      via: "source() to load pcornet$ENCOUNTER and pcornet$DEMOGRAPHIC"
      pattern: 'source\("R/01_load_pcornet\.R"\)'
    - from: "R/19_flm_duplicate_dates.R"
      to: "R/00_config.R"
      via: "PAYER_MAPPING$sentinel_values and PAYER_MAPPING$unavailable_codes for missingness definition"
      pattern: "PAYER_MAPPING\\$sentinel_values"
    - from: "R/19_flm_duplicate_dates.R"
      to: "output/tables/"
      via: "write_csv() for 3 CSV output files"
      pattern: "write_csv.*flm_"
---

<objective>
Create a standalone diagnostic R script that investigates whether FLM-sourced patients in the OneFlorida+ cohort have duplicate ENCOUNTER rows on the same date from multiple data sources. Quantify the duplication (same-date collisions and exact row duplicates), compare payer/insurance data completeness across sources for duplicate-date encounters, and recommend which source to prefer when duplicates exist.

Purpose: Determine the extent of encounter date duplication for FLM patients and whether different data sources provide different payer data quality, informing potential pipeline deduplication or source-preference logic.

Output: `R/19_flm_duplicate_dates.R` script + 3 CSV files in `output/tables/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-check-duplicate-dates-of-flm-subjects/20-CONTEXT.md
@.planning/phases/20-check-duplicate-dates-of-flm-subjects/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase. Executor should use these directly. -->

From R/00_config.R (line 106-107):
```r
# NOTE: Patient ID column is "ID" (not "PATID") across all tables
# NOTE: SOURCE column = partner/site identifier (AMS, UMI, FLM, VRT)
```

From R/00_config.R (lines 260-289):
```r
PAYER_MAPPING <- list(
  sentinel_values = c("NI", "UN", "OT"),
  unavailable_codes = c("99", "9999"),
  unknown_codes = c("NI", "UN", "OT", "UNKNOWN"),
  categories = c("Medicare", "Medicaid", "Dual eligible", "Private",
                  "Other government", "No payment / Self-pay", "Other",
                  "Unavailable", "Unknown")
)
CONFIG$output_dir  # "output"
```

From R/01_load_pcornet.R (lines 124-144) ENCOUNTER_SPEC columns:
```
ENCOUNTERID, ID, ADMIT_DATE, ADMIT_TIME, DISCHARGE_DATE, DISCHARGE_TIME,
PROVIDERID, FACILITY_LOCATION, ENC_TYPE, FACILITYID,
DISCHARGE_DISPOSITION, DISCHARGE_STATUS, DRG, DRG_TYPE, ADMITTING_SOURCE,
PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY, FACILITY_TYPE, SOURCE
```
All columns are col_character().

From R/01_load_pcornet.R (lines 149-162) DEMOGRAPHIC_SPEC columns:
```
ID, BIRTH_DATE, BIRTH_TIME, SEX, SEXUAL_ORIENTATION, GENDER_IDENTITY,
HISPANIC, RACE, BIOBANK_FLAG, PAT_PREF_LANGUAGE_SPOKEN, ZIP_CODE, SOURCE
```

Phase 19 missingness definition (reuse per D-10):
```r
# Missing = NA, empty string, NI, UN, OT, 99, 9999
missing_indicators <- c(PAYER_MAPPING$sentinel_values, PAYER_MAPPING$unavailable_codes)
# Yields c("NI", "UN", "OT", "99", "9999")
# Plus: is.na(x) | nchar(trimws(x)) == 0
```

Standalone diagnostic script pattern (from R/date_range_check.R):
```r
source("R/00_config.R")
library(dplyr)
if (!exists("pcornet")) source("R/01_load_pcornet.R")
# ... analysis with message() + glue::glue() logging ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/19_flm_duplicate_dates.R with duplicate detection, multi-source analysis, payer completeness comparison, and CSV outputs</name>
  <files>R/19_flm_duplicate_dates.R</files>
  <read_first>
    - R/00_config.R (PAYER_MAPPING at lines 260-289 for sentinel_values and unavailable_codes; CONFIG$output_dir at line 33; SOURCE comment at line 107)
    - R/01_load_pcornet.R (ENCOUNTER_SPEC at lines 124-144 for column names; DEMOGRAPHIC_SPEC at lines 149-162; load_pcornet_table() function)
    - R/date_range_check.R (standalone diagnostic script pattern: source chain, console logging with cat/message + glue)
    - R/18_uf_insurance_missingness.R (Phase 19 missingness definition pattern: is_missing_payer function, if this file exists)
  </read_first>
  <action>
Create `R/19_flm_duplicate_dates.R` as a standalone diagnostic script with these exact sections:

**HEADER:** Standard comment block:
```r
# ==============================================================================
# FLM Duplicate Date Investigation (Phase 20)
# ==============================================================================
# Standalone diagnostic script -- not part of the main pipeline sequence.
# Investigates whether FLM-sourced patients have duplicate ENCOUNTER rows on
# the same date from multiple data sources. Quantifies duplication, compares
# payer completeness across sources, and recommends preferred source.
#
# Decisions: D-01 to D-18 from 20-CONTEXT.md
# Requirements: FLMDUP-01, FLMDUP-02, FLMDUP-03, FLMDUP-04
#
# Usage: source("R/19_flm_duplicate_dates.R")
# ==============================================================================
```

**DEPENDENCIES:**
```r
source("R/00_config.R")
library(dplyr)
library(lubridate)
library(glue)
library(readr)
library(stringr)
library(janitor)
library(tidyr)

# Load tables if not already loaded
if (!exists("pcornet")) source("R/01_load_pcornet.R")
```

**SECTION 1: Identify FLM patients (D-05, D-06).**
- Filter `pcornet$DEMOGRAPHIC` to `SOURCE == "FLM"`, pull unique `ID` values.
- Per D-06: ALL FLM patients, not just HL cohort members.
- Log: `message(glue("\n{'='|strrep(60)}\nFLM DUPLICATE DATE INVESTIGATION\n{'='|strrep(60)}\n"))` (use `strrep("=", 60)` for the separator).
- Log count: `message(glue("FLM patients in DEMOGRAPHIC: {format(length(flm_patient_ids), big.mark=',')}"))`

**SECTION 2: Filter ENCOUNTER to FLM patients and define missingness (D-07, D-09, D-10).**
- `flm_encounters <- pcornet$ENCOUNTER %>% filter(ID %in% flm_patient_ids)`
- Log total FLM encounters: `message(glue("FLM encounters: {format(nrow(flm_encounters), big.mark=',')}"))`
- Check and log NA count for ENCOUNTER.SOURCE: `n_na_source <- sum(is.na(flm_encounters$SOURCE))`. Log it. Per Pitfall 2: if >5% of encounters have NA SOURCE, flag in console.
- Log unique ENCOUNTER.SOURCE values in FLM encounters: `message(glue("ENCOUNTER.SOURCE values: {paste(sort(unique(na.omit(flm_encounters$SOURCE))), collapse=', ')}"))`
- Define `is_missing_payer()` function per D-10 (Phase 19 missingness definition):
```r
is_missing_payer <- function(payer_value) {
  is.na(payer_value) |
    nchar(trimws(payer_value)) == 0 |
    payer_value %in% c("NI", "UN", "OT", "99", "9999")
}
```

**SECTION 3: Detect same-date duplicates on ADMIT_DATE (D-01, D-03, FLMDUP-01).**
- Since all ENCOUNTER columns are `col_character()` per ENCOUNTER_SPEC, parse dates:
```r
flm_encounters <- flm_encounters %>%
  mutate(
    admit_date_parsed = as.Date(ADMIT_DATE, format = "%Y-%m-%d"),
    discharge_date_parsed = as.Date(DISCHARGE_DATE, format = "%Y-%m-%d")
  )
```
- Note: if `as.Date(x, format = "%Y-%m-%d")` produces many NAs, check the actual format in the CSV. The pipeline's `parse_date_flexible()` from `utils_dates.R` may be needed. Read `utils_dates.R` to confirm. Add a fallback: if >50% of ADMIT_DATE values are non-NA strings but parse to NA, try `parse_date_flexible()` instead.
- Log NA count after parsing: `n_no_admit_date <- sum(is.na(flm_encounters$admit_date_parsed))`. Log it.
- Detect ADMIT_DATE same-date duplicates:
```r
admit_date_dupes <- flm_encounters %>%
  filter(!is.na(admit_date_parsed)) %>%
  add_count(ID, admit_date_parsed, name = "n_encounters_same_date") %>%
  filter(n_encounters_same_date > 1) %>%
  arrange(ID, admit_date_parsed, SOURCE)
```
- Log: number of duplicate rows, number of unique patient-dates with duplicates, number of unique patients with at least one duplicate date.
- Also detect DISCHARGE_DATE same-date duplicates (D-03):
```r
discharge_date_dupes <- flm_encounters %>%
  filter(!is.na(discharge_date_parsed)) %>%
  add_count(ID, discharge_date_parsed, name = "n_encounters_same_date") %>%
  filter(n_encounters_same_date > 1)
```
- Log DISCHARGE_DATE duplicate counts. Per Open Question 4 in RESEARCH.md: if discharge duplicate rate is very low, note that and skip detailed analysis.

**SECTION 4: Detect exact row duplicates (D-02, FLMDUP-01).**
- Use `janitor::get_dupes()` on the full `flm_encounters` data frame (all original columns, excluding parsed date columns):
```r
exact_dupes <- flm_encounters %>%
  select(-admit_date_parsed, -discharge_date_parsed) %>%
  get_dupes()
```
- Per Pitfall 4: check `nrow(exact_dupes) > 0` before processing.
- Log count: if zero, `message("No exact row duplicates found")`. If >0, log count and number of unique patients affected.
- Also check near-exact duplicates (same on all columns except ENCOUNTERID):
```r
near_exact_dupes <- flm_encounters %>%
  select(-admit_date_parsed, -discharge_date_parsed, -ENCOUNTERID) %>%
  get_dupes()
```
- Log near-exact count separately.

**SECTION 5: Identify multi-source dates (D-04, FLMDUP-02).**
- From `admit_date_dupes`, compute per-patient-date summary:
```r
patient_date_summary <- admit_date_dupes %>%
  group_by(ID, admit_date_parsed) %>%
  summarize(
    n_encounters = n(),
    n_sources = n_distinct(SOURCE, na.rm = TRUE),
    sources = paste(sort(unique(na.omit(SOURCE))), collapse = ", "),
    enc_types = paste(sort(unique(na.omit(ENC_TYPE))), collapse = ", "),
    n_enc_types = n_distinct(ENC_TYPE, na.rm = TRUE),
    .groups = "drop"
  )

multi_source_dates <- patient_date_summary %>%
  filter(n_sources > 1)
```
- Log: total patient-dates with duplicates, patient-dates with multiple sources, most common source combinations.
- Per Pitfall 3: also log breakdown of same-source vs multi-source duplicates, and within same-source whether ENC_TYPE differs (clinically valid) or is the same (potential true duplication).

**SECTION 6: Payer completeness comparison across sources for multi-source dates (D-08, D-09, D-10, D-11, FLMDUP-03).**
- Get the actual encounter rows for multi-source dates:
```r
multi_source_encounters <- admit_date_dupes %>%
  semi_join(multi_source_dates, by = c("ID", "admit_date_parsed"))
```
- If `nrow(multi_source_encounters) == 0`, log "No multi-source dates found -- skipping payer completeness comparison" and skip to Section 7.
- Otherwise, compute per-source completeness:
```r
source_completeness <- multi_source_encounters %>%
  filter(!is.na(SOURCE)) %>%
  group_by(SOURCE) %>%
  summarize(
    n_encounters = n(),
    n_primary_present = sum(!is_missing_payer(PAYER_TYPE_PRIMARY)),
    pct_primary_present = round(100 * n_primary_present / n_encounters, 1),
    n_secondary_present = sum(!is_missing_payer(PAYER_TYPE_SECONDARY)),
    pct_secondary_present = round(100 * n_secondary_present / n_encounters, 1),
    n_both_present = sum(!is_missing_payer(PAYER_TYPE_PRIMARY) & !is_missing_payer(PAYER_TYPE_SECONDARY)),
    pct_both_present = round(100 * n_both_present / n_encounters, 1),
    n_either_present = sum(!is_missing_payer(PAYER_TYPE_PRIMARY) | !is_missing_payer(PAYER_TYPE_SECONDARY)),
    pct_either_present = round(100 * n_either_present / n_encounters, 1),
    .groups = "drop"
  ) %>%
  arrange(desc(pct_primary_present))
```
- Log each source's completeness rates to console.
- Per Pitfall 5: also compute ENC_TYPE distribution per source and log it as a caveat if distributions differ substantially.
- Compute per-source per-ENC_TYPE completeness as supplementary analysis:
```r
source_enctype_completeness <- multi_source_encounters %>%
  filter(!is.na(SOURCE)) %>%
  group_by(SOURCE, ENC_TYPE) %>%
  summarize(
    n_encounters = n(),
    pct_primary_present = round(100 * mean(!is_missing_payer(PAYER_TYPE_PRIMARY)), 1),
    .groups = "drop"
  ) %>%
  arrange(SOURCE, desc(n_encounters))
```
- Generate recommendation (D-14):
```r
best_source <- source_completeness$SOURCE[1]
best_pct <- source_completeness$pct_primary_present[1]
message(glue("\nRECOMMENDATION: Prefer SOURCE='{best_source}' for payer data when duplicates exist"))
message(glue("  Primary payer completeness: {best_pct}%"))
```
- If only one source has multi-source encounters, note that recommendation is trivial.

**SECTION 7: Build and write CSV outputs (D-15, D-16, D-17, FLMDUP-04).**

Ensure output directory exists:
```r
output_dir <- file.path(CONFIG$output_dir, "tables")
dir.create(output_dir, showWarnings = FALSE, recursive = TRUE)
```

**CSV 1: Patient-level duplicate summary** (`flm_patient_duplicate_summary.csv`):
Use a two-step approach (pre-compute patient-date stats, then aggregate to patient level):
```r
# Pre-compute per patient-date
patient_date_stats <- flm_encounters %>%
  filter(!is.na(admit_date_parsed)) %>%
  group_by(ID, admit_date_parsed) %>%
  summarize(
    n_enc_this_date = n(),
    n_sources_this_date = n_distinct(SOURCE, na.rm = TRUE),
    .groups = "drop"
  )

patient_summary <- patient_date_stats %>%
  group_by(ID) %>%
  summarize(
    n_unique_dates = n(),
    n_total_encounters = sum(n_enc_this_date),
    n_duplicate_dates = sum(n_enc_this_date > 1),
    n_multi_source_dates = sum(n_sources_this_date > 1),
    .groups = "drop"
  )

# Add per-patient payer completeness and source info
patient_payer <- flm_encounters %>%
  filter(!is.na(admit_date_parsed)) %>%
  group_by(ID) %>%
  summarize(
    sources_in_encounters = paste(sort(unique(na.omit(SOURCE))), collapse = ", "),
    pct_primary_present = round(100 * mean(!is_missing_payer(PAYER_TYPE_PRIMARY)), 1),
    pct_secondary_present = round(100 * mean(!is_missing_payer(PAYER_TYPE_SECONDARY)), 1),
    .groups = "drop"
  )

patient_summary <- patient_summary %>%
  left_join(patient_payer, by = "ID") %>%
  arrange(desc(n_multi_source_dates), desc(n_duplicate_dates))
```
Write: `write_csv(patient_summary, file.path(output_dir, "flm_patient_duplicate_summary.csv"))`

**CSV 2: Date-level detail for multi-source encounters** (`flm_date_level_duplicate_detail.csv`):
- If `nrow(multi_source_encounters) > 0` (or multi_source_encounters exists and is non-empty):
```r
date_detail <- multi_source_encounters %>%
  select(ID, admit_date_parsed, SOURCE, ENC_TYPE, ENCOUNTERID,
         ADMIT_TIME, DISCHARGE_DATE, DISCHARGE_TIME,
         PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY) %>%
  mutate(
    primary_missing = is_missing_payer(PAYER_TYPE_PRIMARY),
    secondary_missing = is_missing_payer(PAYER_TYPE_SECONDARY)
  ) %>%
  arrange(ID, admit_date_parsed, SOURCE)
```
- If no multi-source encounters, write an empty data frame with the expected columns and a console message.
- Write: `write_csv(date_detail, file.path(output_dir, "flm_date_level_duplicate_detail.csv"))`

**CSV 3: Aggregate summary** (`flm_duplicate_aggregate_summary.csv`):
Build a summary tibble with key metrics:
```r
aggregate_summary <- tibble(
  metric = c(
    "Total FLM patients (DEMOGRAPHIC)",
    "Total FLM encounters",
    "FLM encounters with valid ADMIT_DATE",
    "Encounters with NA ADMIT_DATE",
    "Encounters with NA ENCOUNTER.SOURCE",
    "Unique patient-dates",
    "Patient-dates with same-date duplicates",
    "Patient-dates with multiple SOURCEs",
    "Exact row duplicates (all columns)",
    "Near-exact duplicates (all except ENCOUNTERID)",
    "DISCHARGE_DATE same-date duplicate patient-dates"
  ),
  value = c(
    length(flm_patient_ids),
    nrow(flm_encounters),
    sum(!is.na(flm_encounters$admit_date_parsed)),
    n_no_admit_date,
    n_na_source,
    nrow(patient_date_stats),
    sum(patient_date_stats$n_enc_this_date > 1),
    nrow(multi_source_dates),
    nrow(exact_dupes),
    nrow(near_exact_dupes),
    n_distinct(paste(discharge_date_dupes$ID, discharge_date_dupes$discharge_date_parsed))
  )
)
```
If `source_completeness` exists (multi-source encounters found), append source completeness rows:
```r
if (exists("source_completeness") && nrow(source_completeness) > 0) {
  source_rows <- source_completeness %>%
    mutate(
      metric = glue("SOURCE={SOURCE}: primary payer present %"),
      value = pct_primary_present
    ) %>%
    select(metric, value)
  aggregate_summary <- bind_rows(aggregate_summary, source_rows)
}
```
Write: `write_csv(aggregate_summary, file.path(output_dir, "flm_duplicate_aggregate_summary.csv"))`

Also write source_completeness as a separate CSV if multi-source encounters exist:
```r
if (exists("source_completeness") && nrow(source_completeness) > 0) {
  write_csv(source_completeness, file.path(output_dir, "flm_source_payer_completeness.csv"))
}
```

**SECTION 8: Console summary (D-18).**
Print a clear summary block using `strrep("=", 60)` for separators. Include:
- Total FLM patients and encounters
- ADMIT_DATE duplicate rate: N patient-dates with duplicates / total patient-dates (as percentage)
- Multi-source rate: N patient-dates with multiple SOURCEs / total patient-dates with duplicates
- Exact duplicate count
- DISCHARGE_DATE duplicate count (brief note)
- Source completeness comparison (if multi-source encounters exist)
- Recommendation line
- List of CSV files written

**IMPORTANT NOTES:**
- Per D-06: use ALL FLM patients from DEMOGRAPHIC, not HL cohort members only.
- Per D-07: ENCOUNTER table only -- do not touch DIAGNOSIS, PROCEDURES, or other tables.
- Per D-01: group by ID + date only, not by ENC_TYPE.
- Per D-03: check ADMIT_DATE (primary) and DISCHARGE_DATE (secondary) but focus reporting on ADMIT_DATE.
- Per D-10: use Phase 19 missingness definition exactly: NA, empty string, NI, UN, OT, 99, 9999.
- The date parsing should handle the format used by the pipeline. Since ENCOUNTER columns are loaded as character, parse with `as.Date()` using the expected format from the CSV. Check what format ADMIT_DATE is in -- it may be "YYYY-MM-DD" or need the project's `parse_date_flexible()` function from `utils_dates.R`. To be safe, source utils_dates.R and use its parsing if available, otherwise use `as.Date(x, format = "%Y-%m-%d")`.
  </action>
  <verify>
    <automated>grep -c "SECTION" R/19_flm_duplicate_dates.R</automated>
  </verify>
  <acceptance_criteria>
    - R/19_flm_duplicate_dates.R exists with at least 200 lines
    - File contains `source("R/01_load_pcornet.R")` or `source("R/00_config.R")` (dependency chain)
    - File contains `library(janitor)` (for get_dupes() exact duplicate detection per D-02)
    - File contains `SOURCE == "FLM"` (FLM patient filter per D-05)
    - File contains `is_missing_payer` function with `c("NI", "UN", "OT", "99", "9999")` or equivalent using PAYER_MAPPING (D-10 missingness definition)
    - File contains `add_count(ID, admit_date` or equivalent group_by + filter pattern for same-date detection (D-01)
    - File contains `get_dupes()` call for exact row duplicate detection (D-02)
    - File contains `n_distinct(SOURCE` for multi-source date identification (D-04)
    - File contains `PAYER_TYPE_PRIMARY` and `PAYER_TYPE_SECONDARY` completeness comparison (D-09)
    - File contains `DISCHARGE_DATE` duplicate check (D-03)
    - File contains `write_csv` calls for at least 3 output files: flm_patient_duplicate_summary.csv, flm_date_level_duplicate_detail.csv, flm_duplicate_aggregate_summary.csv
    - File contains `RECOMMENDATION` string for source preference output (D-14)
    - File does NOT contain `hl_cohort` or cohort filtering -- uses ALL FLM patients (D-06)
    - File does NOT contain references to DIAGNOSIS, PROCEDURES, or other tables beyond ENCOUNTER and DEMOGRAPHIC (D-07)
    - File contains `ENC_TYPE` in at least one output for contextual interpretation (Pitfall 3)
  </acceptance_criteria>
  <done>
    R/19_flm_duplicate_dates.R is a complete standalone diagnostic script with 8 sections covering: FLM patient identification from DEMOGRAPHIC, ENCOUNTER filtering, same-date duplicate detection on ADMIT_DATE and DISCHARGE_DATE, exact and near-exact row duplicate detection via janitor::get_dupes(), multi-source date identification, payer completeness comparison across sources with recommendation, 3 CSV files written to output/tables/, and console summary. Missingness uses Phase 19 definition (NA, empty, NI, UN, OT, 99, 9999). Script investigates ALL FLM patients (not just HL cohort) and ENCOUNTER table only.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User runs script on HiPerGator and reviews duplicate date findings</name>
  <files>R/19_flm_duplicate_dates.R</files>
  <action>User runs the completed script on HiPerGator and reviews the console output and CSV files for FLM duplicate date patterns. No automated action -- this is a human verification checkpoint.</action>
  <what-built>
    Standalone diagnostic script R/19_flm_duplicate_dates.R that investigates FLM patient encounter date duplication across data sources, compares payer completeness by source, and outputs 3 CSV files to output/tables/.
  </what-built>
  <how-to-verify>
    1. On HiPerGator, open RStudio and run: `source("R/19_flm_duplicate_dates.R")`
    2. Check console output for:
       - Total FLM patient and encounter counts
       - ADMIT_DATE same-date duplicate counts and rates
       - DISCHARGE_DATE duplicate counts (brief)
       - Exact row duplicate count
       - Multi-source date count (patient-dates with encounters from different SOURCEs)
       - Per-source payer completeness rates (if multi-source dates exist)
       - Source preference recommendation
    3. Check output/tables/ for 3 new CSV files:
       - flm_patient_duplicate_summary.csv (one row per FLM patient with duplicate counts)
       - flm_date_level_duplicate_detail.csv (encounter rows for multi-source dates with payer fields)
       - flm_duplicate_aggregate_summary.csv (overall rates and source completeness)
    4. Review findings:
       - What percentage of FLM patient-dates have duplicates?
       - How many of those duplicates involve multiple SOURCEs?
       - Which SOURCE has better payer data completeness?
       - Are same-date encounters with different ENC_TYPE clinically valid or data duplication?
  </how-to-verify>
  <verify>User confirms script runs without error and CSV files contain meaningful data</verify>
  <done>User has reviewed FLM duplicate date findings and confirmed the script produces actionable diagnostic output</done>
  <resume-signal>Type "approved" with any observations about the duplication patterns found, or describe issues with the script output</resume-signal>
</task>

</tasks>

<verification>
Phase 20 verification checklist:

1. `R/19_flm_duplicate_dates.R` exists and is syntactically valid R
2. Script sources its own dependencies (00_config.R + 01_load_pcornet.R chain)
3. Script filters to FLM patients via DEMOGRAPHIC.SOURCE == "FLM" (D-05)
4. Script checks ALL FLM patients, not just HL cohort (D-06)
5. Script uses ENCOUNTER table only, not DIAGNOSIS/PROCEDURES (D-07)
6. Script detects same-date duplicates grouped by ID + date only, not ENC_TYPE (D-01)
7. Script detects exact row duplicates via janitor::get_dupes() (D-02)
8. Script checks both ADMIT_DATE and DISCHARGE_DATE (D-03)
9. Script identifies multi-source dates where n_distinct(SOURCE) > 1 (D-04)
10. Script compares PAYER_TYPE_PRIMARY and PAYER_TYPE_SECONDARY completeness across sources (D-09)
11. Script uses Phase 19 missingness definition: NA, empty, NI, UN, OT, 99, 9999 (D-10)
12. Script produces source preference recommendation (D-14)
13. 3 CSV files written to output/tables/ (D-16, D-17)
14. Console logging provides HiPerGator-friendly summary (D-18)
</verification>

<success_criteria>
- R/19_flm_duplicate_dates.R runs without error on HiPerGator
- 3 CSV files produced in output/tables/ with non-empty content
- Console summary identifies the FLM duplicate date rate and multi-source encounter rate
- Payer completeness comparison across sources enables informed source preference decision
- User can characterize FLM encounter duplication sufficiently to determine next steps (deduplication strategy, source preference rule, or documentation of known limitation)
</success_criteria>

<output>
After completion, create `.planning/phases/20-check-duplicate-dates-of-flm-subjects/20-01-SUMMARY.md`
</output>
