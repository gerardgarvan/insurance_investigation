---
phase: 107-gap-resolution-report-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/37_gap_resolution_report.Rmd
  - R/38_delivery_manifest.R
autonomous: true
requirements: [REPORT-01, REPORT-02]
must_haves:
  truths:
    - "User can render R/37 to self-contained HTML that includes tables from all gap investigations"
    - "User can run R/38 and get an xlsx listing all v3.1+v3.2 output files with descriptions and validation status"
  artifacts:
    - path: "R/37_gap_resolution_report.Rmd"
      provides: "RMarkdown source for gap resolution report"
      contains: "html_document"
    - path: "R/38_delivery_manifest.R"
      provides: "Delivery manifest generator"
      contains: "wb_workbook"
  key_links:
    - from: "R/37_gap_resolution_report.Rmd"
      to: "output/*.xlsx"
      via: "readxl::read_excel()"
      pattern: "read_excel"
    - from: "R/38_delivery_manifest.R"
      to: "output/"
      via: "file.exists() + file.info()"
      pattern: "file\\.exists"
---

<objective>
Create the two core Phase 107 scripts: an RMarkdown gap resolution report (R/37) that reads existing investigation xlsx outputs and renders to self-contained HTML, and a delivery manifest script (R/38) that inventories all v3.1+v3.2 output files with metadata and validation.

Purpose: These scripts compile all investigation findings from Phases 100-106 into presentation-ready and delivery-ready formats. R/37 is the team meeting deliverable; R/38 is the Amy packaging deliverable.
Output: R/37_gap_resolution_report.Rmd, R/38_delivery_manifest.R (both new files)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-gap-resolution-report-delivery/107-CONTEXT.md
@.planning/phases/107-gap-resolution-report-delivery/107-RESEARCH.md
@R/89_generate_reference_manual.R
@R/30_condition_linkage_investigation.R
</context>

<interfaces>
<!-- Key patterns from existing investigation scripts that R/37 and R/38 must follow -->

Established xlsx output pattern (used across R/30-R/36):
```r
wb <- wb_workbook()
wb$add_worksheet("Sheet Name")
wb$add_data(sheet = 1, x = data_frame)
wb$add_style(sheet = 1, dims = "A1:F1", fill_color = "FF374151", font_color = "FFFFFFFF", bold = TRUE)
wb$freeze_panes(sheet = 1, first_row = TRUE)
wb$set_col_widths(sheet = 1, cols = 1:6, widths = "auto")
wb$save("output/filename.xlsx")
```

Source files for R/37 (report data sources, xlsx files to read via readxl):
- output/condition_linkage_investigation.xlsx (G1, Phase 100)
- output/drug_grouping_instances.xlsx (G2, Phase 101)
- output/co_administration_analysis.xlsx (G3, Phase 102)
- output/death_date_summary.xlsx (G15, Phase 103)
- output/pre_diagnosis_treatments.xlsx (G5, Phase 104)
- output/secondary_malignancy_table.xlsx (secondary malignancy, Phase 104)
- output/code_verification.xlsx (G8/G10/G11, Phase 105)
- output/hl_nhl_overlap_validation.xlsx (G4, Phase 105)
- output/tableau_table1_encounter_cancer_codes.xlsx (TABLE-1, Phase 106)
- output/tableau_table2_chemo_drugs_by_class.xlsx (TABLE-2, Phase 106)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create R/37_gap_resolution_report.Rmd</name>
  <files>R/37_gap_resolution_report.Rmd</files>
  <read_first>
    R/89_generate_reference_manual.R
    R/30_condition_linkage_investigation.R
    .planning/phases/107-gap-resolution-report-delivery/107-CONTEXT.md
    .planning/phases/107-gap-resolution-report-delivery/107-RESEARCH.md
    .planning/phases/104-treatment-timing-investigations/104-01-SUMMARY.md
    .planning/phases/105-code-overlap-verification/105-01-SUMMARY.md
    .planning/phases/106-tableau-ready-data-tables/106-01-SUMMARY.md
  </read_first>
  <action>
Create R/37_gap_resolution_report.Rmd, an RMarkdown file that renders to self-contained HTML. Per D-01 through D-08.

**YAML Header (between triple-dash delimiters at top of Rmd file):**
- title: "Gap Resolution Report: v3.1 + v3.2 Investigations"
- author: "PCORnet Payer Variable Investigation Pipeline"
- date: "`r Sys.Date()`"
- output: html_document with self_contained: true, toc: true, toc_float (collapsed: false, smooth_scroll: true), theme: cosmo, highlight: tango, code_folding: hide

**Setup chunk (hidden via include=FALSE):**
Load libraries: readxl, kableExtra, dplyr, glue, stringr. Set output_dir to "output".

**Structure organized by gap number per D-01:**

1. **Executive Summary section (per D-05):** A bulleted list of each gap investigated and its one-line resolution:
   - G1: CONDITION table linkage reduced unlinked episodes
   - G2: Broadened drug grouping includes all encounters with cancer_linked flag
   - G3: Co-administration patterns identified for single-agent chemotherapy
   - G4: HL+NHL dual-code temporal validation completed
   - G5: Pre-diagnosis treatments flagged across 5 treatment types
   - G8: Etanercept correctly excluded from immunotherapy classification
   - G10: Organ transplant code 0362 investigated with SCT cross-reference
   - G11: SCT diagnosis codes above line 22 validated
   - G15: Death date cross-tab with 3-tier breakdown
   - TABLE-1/TABLE-2: Tableau-ready encounter-level tables delivered

2. **Per-gap sections (per D-02, D-03):** Each section has H2 header (## G{N}: {Gap Title}), 1-2 paragraph finding summary, one key table loaded from the corresponding xlsx via readxl::read_excel() per D-04, table rendered via kbl() then kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = FALSE) per D-06/D-07.

3. **Gap sections to include (per D-02):**
   - G1 (CONDITION linkage): Read output/condition_linkage_investigation.xlsx first sheet. Display top rows.
   - G2 (Broadened drug grouping): Read output/drug_grouping_instances.xlsx first sheet. Display head(20).
   - G3 (Co-administration): Read output/co_administration_analysis.xlsx pattern summary sheet. Display head(20).
   - G4 (HL+NHL overlap): Read output/hl_nhl_overlap_validation.xlsx sheet "Summary" or first sheet. Display full summary.
   - G5 (Pre-dx treatments): Read output/pre_diagnosis_treatments.xlsx sheet "Summary". Display full summary.
   - Secondary Malignancy: Read output/secondary_malignancy_table.xlsx sheet "Summary". Display full summary.
   - G8 (Ethna/Etanercept): Read output/code_verification.xlsx sheet "Summary" or first sheet. Display CODE-01 rows.
   - G10 (Transplant code): Same xlsx, CODE-02 rows.
   - G11 (SCT codes): Same xlsx, CODE-03 rows.
   - G15 (Death dates): Read output/death_date_summary.xlsx first sheet. Display summary.
   - TABLE-1/TABLE-2 Delivery: Read both tableau xlsx files. Display head(10) of each as preview.

4. **For each read_excel() call:** Use sheet = 1 as default. Wrap in tryCatch to handle missing files gracefully. If file not found, display italic text "File not available. Run the investigation script to generate output." instead of crashing.

5. **Table display pattern for each gap section:**
```r
data <- tryCatch(
  read_excel(file.path(output_dir, "filename.xlsx"), sheet = 1),
  error = function(e) NULL
)
if (!is.null(data)) {
  data %>%
    head(20) %>%
    kbl(caption = "G{N}: {description}") %>%
    kable_styling(bootstrap_options = c("striped", "hover", "condensed"), full_width = FALSE)
} else {
  cat("*File not available. Run the investigation script to generate output.*")
}
```

6. **Limit table sizes per Pitfall 1 in RESEARCH.md:** Use head(20) for large tables, display full table only for summary tables with fewer than ~30 rows.

Do NOT include the standard R script header block (this is an Rmd, not an R script). Do NOT re-run any analysis; this is a presentation layer per D-04. Do NOT use DT::datatable or any JavaScript table library per D-07.
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path 'R/37_gap_resolution_report.Rmd') { $c = Get-Content 'R/37_gap_resolution_report.Rmd' -Raw; $checks = @(); $checks += if ($c -match 'html_document') {'PASS: html_document'} else {'FAIL: html_document'}; $checks += if ($c -match 'self_contained:\s*true') {'PASS: self_contained'} else {'FAIL: self_contained'}; $checks += if ($c -match 'toc_float') {'PASS: toc_float'} else {'FAIL: toc_float'}; $checks += if ($c -match 'read_excel') {'PASS: read_excel'} else {'FAIL: read_excel'}; $checks += if ($c -match 'kbl\(') {'PASS: kbl'} else {'FAIL: kbl'}; $checks += if ($c -match 'kable_styling') {'PASS: kable_styling'} else {'FAIL: kable_styling'}; $checks += if ($c -match 'Executive Summary') {'PASS: exec_summary'} else {'FAIL: exec_summary'}; $checks += if ($c -match 'condition_linkage') {'PASS: G1_file'} else {'FAIL: G1_file'}; $checks += if ($c -match 'pre_diagnosis_treatments') {'PASS: G5_file'} else {'FAIL: G5_file'}; $checks += if ($c -match 'code_verification') {'PASS: G8_file'} else {'FAIL: G8_file'}; $checks += if ($c -match 'death_date_summary') {'PASS: G15_file'} else {'FAIL: G15_file'}; $checks += if ($c -match 'hl_nhl_overlap') {'PASS: G4_file'} else {'FAIL: G4_file'}; $checks += if ($c -match 'tableau_table1') {'PASS: TABLE1_file'} else {'FAIL: TABLE1_file'}; $checks += if ($c -match 'secondary_malignancy') {'PASS: sec_mal_file'} else {'FAIL: sec_mal_file'}; $checks -join '; ' } else { 'FAIL: file does not exist' }"</automated>
  </verify>
  <acceptance_criteria>
    - R/37_gap_resolution_report.Rmd exists
    - Contains YAML header with html_document, self_contained: true, toc_float
    - Contains library(readxl) and library(kableExtra)
    - Contains read_excel calls for at least these 10 xlsx files: condition_linkage_investigation, drug_grouping_instances, co_administration_analysis, death_date_summary, pre_diagnosis_treatments, secondary_malignancy_table, code_verification, hl_nhl_overlap_validation, tableau_table1_encounter_cancer_codes, tableau_table2_chemo_drugs_by_class
    - Contains kbl() and kable_styling() for table rendering
    - Contains "Executive Summary" section
    - Contains sections for G1, G2, G3, G4, G5, G8, G10, G11, G15, and TABLE-1/TABLE-2
    - Contains tryCatch wrappers around read_excel calls for graceful failure
    - Does NOT contain DT::datatable or reactable (no JavaScript tables per D-07)
    - Does NOT contain saveRDS or source("R/ (presentation layer only per D-04)
  </acceptance_criteria>
  <done>R/37_gap_resolution_report.Rmd exists with YAML header specifying self-contained HTML output, floating TOC, cosmo theme. Contains Executive Summary section and per-gap sections (G1, G2, G3, G4, G5, G8, G10, G11, G15, secondary malignancy, TABLE-1/TABLE-2) each with readxl data loading and kableExtra table rendering. All 10 xlsx output files referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Create R/38_delivery_manifest.R</name>
  <files>R/38_delivery_manifest.R</files>
  <read_first>
    R/30_condition_linkage_investigation.R
    R/36_tableau_ready_tables.R
    .planning/phases/107-gap-resolution-report-delivery/107-CONTEXT.md
    .planning/phases/107-gap-resolution-report-delivery/107-RESEARCH.md
  </read_first>
  <action>
Create R/38_delivery_manifest.R, a standalone R script that generates an xlsx manifest of all v3.1+v3.2 output files. Per D-11 through D-13.

**Standard 5-field header block:** Purpose (generate delivery manifest xlsx), Inputs (output/ directory), Outputs (output/delivery_manifest.xlsx), Dependencies (openxlsx2, dplyr, glue, lubridate), Requirements (REPORT-02).

**Section 1: Setup.** rm(list = ls()), load libraries: openxlsx2, dplyr, glue, lubridate. Log banner.

**Section 2: Define Expected Files.** Create a tribble with columns: filepath, description, phase, gap_ref. Include ALL files per D-13:

v3.1 outputs (Phases 100-103):
- output/condition_linkage_investigation.xlsx | "CONDITION table cancer linkage improvement analysis" | "100" | "G1"
- output/drug_grouping_instances.xlsx | "Broadened drug grouping with cancer_linked flag for all treatment encounters" | "101" | "G2"
- output/drug_grouping_instances_linked_only.xlsx | "Drug grouping filtered to cancer-linked encounters only (backward compatible)" | "101" | "G2"
- output/co_administration_analysis.xlsx | "Single-agent chemotherapy co-administration patterns within 30-day window" | "102" | "G3"
- output/death_date_summary.xlsx | "Death date cross-tab: patients with death dates, last encounter timing, post-death activity" | "103" | "G15"

v3.2 outputs (Phases 104-107):
- output/pre_diagnosis_treatments.xlsx | "Pre-diagnosis treatment episodes flagged by type (chemo, radiation, SCT, immunotherapy, proton)" | "104" | "G5"
- output/secondary_malignancy_table.xlsx | "Secondary malignancy table with 7-day gap criterion and pre/post HL split" | "104" | "G5-related"
- output/code_verification.xlsx | "Etanercept (G8), organ transplant code 0362 (G10), SCT diagnosis codes (G11) verification" | "105" | "G8/G10/G11"
- output/hl_nhl_overlap_validation.xlsx | "HL+NHL dual-code patient temporal validation with data quality assessment" | "105" | "G4"
- output/tableau_table1_encounter_cancer_codes.xlsx | "TABLE 1: Each encounter mapped to comma-separated cancer diagnosis codes for Tableau" | "106" | "TABLE-1"
- output/tableau_table2_chemo_drugs_by_class.xlsx | "TABLE 2: Chemotherapy drugs by class/category with cancer codes for Tableau" | "106" | "TABLE-2"
- output/gap_resolution_report.html | "Self-contained HTML report compiling all gap investigation findings" | "107" | "Report"
- output/delivery_manifest.xlsx | "This file: inventory of all v3.1+v3.2 deliverables" | "107" | "Manifest"

**Section 3: Validate and Gather Metadata (per D-12).** Use rowwise() + mutate() to check file.exists(), get file.info()$size and file.info()$mtime, compute size_kb, format modified date, set status to "OK" or "MISSING". Select final columns: phase, gap_ref, filename, description, size_kb, modified, status.

**Section 4: Console Summary.** Log total files, found files, missing files. Log each missing file by name.

**Section 5: Write XLSX.** Use project openxlsx2 pattern: wb_workbook(), add_worksheet("File Inventory"), add_data(), dark header row (FF374151 fill, FFFFFFFF white bold text), freeze_panes(first_row = TRUE), set_col_widths(widths = "auto"), save to output/delivery_manifest.xlsx.

**Section 6: Final Summary.** Log output path and completion message.

Use 7 SECTION markers following the investigation script pattern. Include message() logging at each step with glue formatting.
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path 'R/38_delivery_manifest.R') { $c = Get-Content 'R/38_delivery_manifest.R' -Raw; $checks = @(); $checks += if ($c -match 'wb_workbook') {'PASS: wb_workbook'} else {'FAIL: wb_workbook'}; $checks += if ($c -match 'file\.exists') {'PASS: file_exists'} else {'FAIL: file_exists'}; $checks += if ($c -match 'file\.info') {'PASS: file_info'} else {'FAIL: file_info'}; $checks += if ($c -match 'condition_linkage_investigation') {'PASS: v31_file'} else {'FAIL: v31_file'}; $checks += if ($c -match 'pre_diagnosis_treatments') {'PASS: v32_file'} else {'FAIL: v32_file'}; $checks += if ($c -match 'gap_resolution_report') {'PASS: report_file'} else {'FAIL: report_file'}; $checks += if ($c -match 'delivery_manifest\.xlsx') {'PASS: output_path'} else {'FAIL: output_path'}; $checks += if ($c -match 'FF374151') {'PASS: header_style'} else {'FAIL: header_style'}; $checks += if ($c -match 'SECTION') {'PASS: sections'} else {'FAIL: sections'}; $checks += if ($c -match 'REPORT-02') {'PASS: requirement'} else {'FAIL: requirement'}; $checks += if ($c -match 'tableau_table1') {'PASS: tableau1'} else {'FAIL: tableau1'}; $checks += if ($c -match 'death_date_summary') {'PASS: death_date'} else {'FAIL: death_date'}; $checks -join '; ' } else { 'FAIL: file does not exist' }"</automated>
  </verify>
  <acceptance_criteria>
    - R/38_delivery_manifest.R exists
    - Contains standard 5-field script header with Purpose, Inputs, Outputs, Dependencies, Requirements fields
    - Contains REPORT-02 in Requirements header field
    - Contains library(openxlsx2) and library(dplyr)
    - Contains file.exists() validation for each expected file per D-12
    - Contains file.info() for gathering file metadata (size, modified date)
    - Lists at least 13 expected files spanning Phases 100-107 per D-13
    - Contains all v3.1 files: condition_linkage_investigation.xlsx, drug_grouping_instances.xlsx, co_administration_analysis.xlsx, death_date_summary.xlsx
    - Contains all v3.2 files: pre_diagnosis_treatments.xlsx, secondary_malignancy_table.xlsx, code_verification.xlsx, hl_nhl_overlap_validation.xlsx, tableau_table1, tableau_table2, gap_resolution_report.html, delivery_manifest.xlsx
    - Contains wb_workbook() with FF374151 header styling (project pattern)
    - Contains freeze_panes and column width setting
    - Output path is output/delivery_manifest.xlsx
    - Has 6+ SECTION markers following investigation script pattern
    - Does NOT contain saveRDS (output is xlsx only)
  </acceptance_criteria>
  <done>R/38_delivery_manifest.R exists with standard header, 6+ SECTION markers, tribble of 13+ expected files from Phases 100-107, file.exists() validation, file.info() metadata gathering, openxlsx2 styled xlsx output to output/delivery_manifest.xlsx with dark header styling.</done>
</task>

</tasks>

<verification>
1. R/37_gap_resolution_report.Rmd contains valid YAML header with self_contained: true
2. R/37 references all 10 investigation xlsx files via read_excel()
3. R/37 uses kbl() + kable_styling() for all tables (no JavaScript table libs)
4. R/38_delivery_manifest.R lists 13+ files from Phases 100-107
5. R/38 validates file existence and gathers metadata
6. R/38 produces styled xlsx output matching project pattern
7. Neither script contains saveRDS (both are presentation/inventory scripts)
</verification>

<success_criteria>
- R/37_gap_resolution_report.Rmd renders to self-contained HTML with all gap sections (REPORT-01)
- R/38_delivery_manifest.R generates validated file inventory xlsx (REPORT-02)
- Both files follow project conventions (section markers, logging, styled output)
</success_criteria>

<output>
After completion, create `.planning/phases/107-gap-resolution-report-delivery/107-01-SUMMARY.md`
</output>
