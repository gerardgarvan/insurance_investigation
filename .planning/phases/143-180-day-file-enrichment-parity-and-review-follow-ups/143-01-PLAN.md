---
phase: 143-180-day-file-enrichment-parity-and-review-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-DISCOVERY.md
autonomous: false
requirements: [EP-180-DISC-01]
must_haves:
  truths:
    - "143-DISCOVERY.md exists with a producer disposition table listing every script that writes enrichment columns, its RDS path reference style, and its parameterisability verdict"
    - "90-day fill rates for the six columns are recorded as concrete percentages — these are the parity benchmark for Plan 03"
    - "Death anomaly is documented: how many patients have multiple death dates, the gap sizes, and which source_hint each date came from"
    - "Patient count parity across windows is stated: n_distinct(patient_id) at 90d and 180d, with pass/fail on equality"
  artifacts:
    - path: ".planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-DISCOVERY.md"
      provides: "Producer disposition table, 90-day fill rates, death anomaly findings, patient-count reconciliation"
  key_links:
    - from: "R/28_episode_classification.R"
      to: "cache/outputs/treatment_episodes.rds"
      via: "OUTPUT_RDS <- file.path(CONFIG$cache$outputs_dir, 'treatment_episodes.rds') then readRDS(OUTPUT_RDS) then saveRDS back"
      pattern: "OUTPUT_RDS.*treatment_episodes\\.rds"
---

<objective>
Read-only discovery: classify every enrichment producer, record 90-day fill rates as parity benchmarks, and run the death-anomaly and patient-count checks. Produces 143-DISCOVERY.md. No script edits.

Purpose: The enrichment architecture (which scripts produce which columns, and how they reference the episodes RDS path) must be confirmed from the source before any parameterisation is attempted. The CONTEXT.md names R/60-63, R/91, and R/112 as candidates; this plan confirms the actual list. The death anomaly (Death 1,300→1,299) and patient-count verification must also run before any enrichment work begins.

Output: 143-DISCOVERY.md with the producer disposition table, fill-rate benchmarks, death anomaly report, and patient-count reconciliation. This file gates Plans 02 and 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-CONTEXT.md
@.planning/phases/142-180-day-treatment-episodes-drug-name-deduplication/142-02-SUMMARY.md

<interfaces>
<!-- Key facts extracted from codebase. Executor should use these directly. -->

From R/28_episode_classification.R (the primary enrichment producer):
- Reads treatment_episodes.rds via: OUTPUT_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")
- Reads treatment_episode_detail.rds via: DETAIL_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail.rds")
- Modifies treatment_episodes.rds in-place (readRDS → enrich → saveRDS, per D-15)
- Joins cancer linkage on: (patient_id, treatment_type, episode_number) — NOT on episode_start
- Produces: drug_group, code_type, source_table, cancer_category, cancer_link_method, regimen_label, medication_name, treatment_line, sct_cross_use_flag, episode_dx_codes, episode_dx_categories, episode_dx_7day_confirmed
- All path references are via CONFIG$cache$outputs_dir (no hardcoded strings), but takes no argument for the RDS path — reads only the one fixed OUTPUT_RDS

From R/52_gantt_v2_export.R (the 90-day exporter):
- Reads the already-enriched treatment_episodes.rds (post-R/28)
- Has guard clauses for absent enrichment columns (lines 210-235) — warns and uses NA defaults
- Schema: drug_group at col 13, episode_dx_codes at col 17, episode_dx_categories at col 18, episode_dx_7day_confirmed at col 19

From R/142_gantt_180_export.R (Phase 142 product):
- Reads treatment_episodes_180.rds / treatment_episode_detail_180.rds directly
- Has the same guard clauses for absent enrichment columns as R/52
- Does NOT call R/28 or any enrichment step — that is why the six columns are blank

Phase 142's R/26 Section 5C loop saves:
- cache/outputs/treatment_episodes_180.rds
- cache/outputs/treatment_episode_detail_180.rds
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-01 — Confirm the file's purpose (boundaries vs content)</name>

  <read_first>
    .planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-CONTEXT.md — Section 1 "BLOCKING — what is the 180-day file for?" contains the exact question and the decision table.
  </read_first>

  <decision>
    Is the 180-day file used for comparing episode BOUNDARIES (counts, durations, consolidation) or episode CONTENT (drugs and diagnoses within each episode)?
  </decision>

  <context>
    This decision controls the entire phase scope:

    - **D-01a (Boundaries only):** Drop the six enrichment columns from both gantt_episodes_180.csv and gantt_detail_180.csv. Write output/gantt_180_README.txt documenting the omission. Phase ends after integrity checks (Tasks 2-3) and documentation (Task 5 / Plan 03). No enrichment parameterisation needed.

    - **D-01b (Content, or Unsure/Both):** Run the full enrichment for 180-day episodes. Plans 02 and 03 execute in full. The CONTEXT.md default is D-01b because drug_group is one of the six blank columns and the original request was about drug names.

    Weight on D-01b: Phase 142 was triggered by a drug-name problem (Vinblastine duplicate). If the colleague opens the 180-day file to check whether the drug-name issue resolved and how regimens distribute, blank drug_group columns directly obstruct that review. Episode counts alone will not answer a drug-name question.
  </context>

  <options>
    <option id="d-01a">
      <name>Boundaries only</name>
      <pros>Simpler; no enrichment refactoring; file is honest about what it contains</pros>
      <cons>Cannot check drug-group or diagnosis distribution in 180-day episodes; blank columns remain if the file is re-exported later without this decision being remembered</cons>
    </option>
    <option id="d-01b">
      <name>Content (or unsure/both) — run enrichment</name>
      <pros>File answers both boundaries and content questions; drug_group populated; parity with 90-day file; the safe default per CONTEXT.md</pros>
      <cons>Requires parameterising R/28 to accept a different input RDS without changing its 90-day defaults</cons>
    </option>
  </options>

  <resume-signal>Type "d-01a" or "d-01b". If unsure, type "d-01b" to proceed with enrichment (the CONTEXT.md default).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Discovery — produce 143-DISCOVERY.md</name>
  <files>.planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-DISCOVERY.md</files>

  <read_first>
    - R/28_episode_classification.R (the primary enrichment producer — full file)
    - R/52_gantt_v2_export.R (lines 140-240, guard clauses and column schema)
    - R/142_gantt_180_export.R (full file — confirm which enrichment columns it guards vs truly skips)
    - R/00_config.R (DRUG_GROUPINGS, CONFIG$cache$outputs_dir path constants)
    - .planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-CONTEXT.md (Sections 2a, 2b, 2c — the discovery checklist)
  </read_first>

  <action>
    This task is read-only. Run the following shell commands to confirm the producer list, then produce 143-DISCOVERY.md.

    **Step 1 — Confirm which scripts write the six enrichment columns:**

    ```bash
    grep -rn "drug_group\|code_type\|source_table\|episode_dx_codes\|episode_dx_categories\|episode_dx_7day_confirmed" R/ --include="*.R" -l
    grep -rn "treatment_episodes\.rds\|treatment_episode_detail\.rds" R/ --include="*.R"
    ```

    The known producer is R/28_episode_classification.R. Confirm whether R/60-63, R/91, R/112 write any of these columns (the CONTEXT.md names them, but grep may show otherwise).

    **Step 2 — Classify R/28's RDS path references:**

    ```bash
    grep -n "OUTPUT_RDS\|DETAIL_RDS\|CONFIG\$cache\|treatment_episodes\|treatment_episode_detail" R/28_episode_classification.R
    ```

    Determine: (a) does R/28 take an argument for the episodes RDS, or read only a fixed OUTPUT_RDS? (b) Does any join use `episode_number` alone as a key?

    **Step 3 — Check for window-dependent logic in R/28:**

    ```bash
    grep -n "\b90\b\|GAP_THRESHOLD\|episode_number" R/28_episode_classification.R | head -40
    ```

    A hardcoded `90` or a `GAP_THRESHOLD` reference means R/28 has window-dependent logic. Record the line number and what it does.

    **Step 4 — Record 90-day fill rates (HiPerGator only; on Windows, record "HiPerGator run required" and use the CONTEXT.md figures as placeholders):**

    ```r
    source("R/00_config.R")
    g90 <- readr::read_csv("output/gantt_episodes.csv", show_col_types = FALSE)
    cols <- c("drug_group","code_type","source_table",
              "episode_dx_codes","episode_dx_categories","episode_dx_7day_confirmed")
    fill <- vapply(cols, function(c) mean(!is.na(g90[[c]]) & trimws(g90[[c]]) != ""), numeric(1))
    print(round(100 * fill, 1))
    ```

    CONTEXT.md states the 90-day range is 32–66%. Record the exact per-column figures.

    **Step 5 — Death anomaly check (HiPerGator only; on Windows, write the R script to a file for later execution):**

    ```r
    source("R/00_config.R")
    ep90 <- readRDS(file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds"))
    d90  <- dplyr::filter(ep90, treatment_type == "Death")

    multi <- d90 %>%
      dplyr::count(patient_id, name = "n_death_episodes") %>%
      dplyr::filter(n_death_episodes > 1)
    print(multi)

    det90 <- readRDS(file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail.rds"))
    det90 %>%
      dplyr::filter(treatment_type == "Death", patient_id %in% multi$patient_id) %>%
      dplyr::arrange(patient_id, treatment_date) %>%
      dplyr::select(patient_id, treatment_date, triggering_code, ENCOUNTERID, source_hint) %>%
      print(n = Inf)
    ```

    Record: how many patients have > 1 death episode at 90d, what the date gap is, which source_hint each date came from.

    **Step 6 — Patient count reconciliation (HiPerGator only):**

    ```r
    source("R/00_config.R")
    ep90  <- readRDS(file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds"))
    ep180 <- readRDS(file.path(CONFIG$cache$outputs_dir, "treatment_episodes_180.rds"))
    n90  <- dplyr::n_distinct(ep90$patient_id)
    n180 <- dplyr::n_distinct(ep180$patient_id)
    cat(sprintf("patients: %d at 90d, %d at 180d\n", n90, n180))
    stopifnot("patient count changed between windows" = n90 == n180)

    dplyr::full_join(
      dplyr::count(ep90,  treatment_type, name = "n_ep_90"),
      dplyr::count(ep180, treatment_type, name = "n_ep_180"),
      by = "treatment_type"
    ) %>%
      dplyr::mutate(pct_change = round(100 * (n_ep_180 - n_ep_90) / n_ep_90, 1)) %>%
      print()
    ```

    Record: patient counts at 90d and 180d (must be equal), per-type episode counts and % change.

    **Step 7 — Write 143-DISCOVERY.md** with exactly these sections:

    ```
    # 143-DISCOVERY.md

    ## D-01 Decision
    [Record user's answer: d-01a or d-01b]

    ## Producer Disposition Table
    | Script | Columns it produces | Reads RDS via | Parameterisable? | Window-dependent logic? |
    |---|---|---|---|---|
    [One row per script confirmed by grep]

    ## Join Key Risk
    [State whether R/28 joins on episode_number. Cite line numbers.
     State whether episode_start is available as an alternative join key in treatment_episodes.rds.]

    ## 90-Day Fill Rates (parity benchmark)
    | Column | Fill Rate (%) |
    |---|---|
    | drug_group | ... |
    | code_type | ... |
    | source_table | ... |
    | episode_dx_codes | ... |
    | episode_dx_categories | ... |
    | episode_dx_7day_confirmed | ... |

    ## Death Anomaly (Task 2 of CONTEXT.md)
    - Patients with > 1 death episode at 90 days: [N]
    - Per-patient date gaps: [list]
    - source_hint values: [list]
    - Affects survival analyses: [yes/no/unknown — see note]

    ## Patient Count Reconciliation (Task 3 of CONTEXT.md)
    - n_distinct(patient_id) at 90d: [N]
    - n_distinct(patient_id) at 180d: [N]
    - Equal: [PASS / FAIL — halt if FAIL]
    - Death episodes: 90d=[N], 180d=[N] (must be invariant)
    - Proton Therapy episodes: 90d=[N], 180d=[N] (must be invariant)
    - Per-type episode count comparison: [paste table]

    ## Parameterisation Design (if D-01b)
    [For each producer in the disposition table: exact parameter names to add,
     default values that reproduce 90-day behaviour, and whether any window-dependent
     logic in the script must also be parameterised.]
    ```

    **On Windows (no Rscript):** Run Steps 1-3 (shell grep commands) and write 143-DISCOVERY.md with the grep findings. For Steps 4-6, write the R code into a section called "HiPerGator verification scripts" — do not invent numbers.
  </action>

  <verify>
    <automated>grep -l "Producer Disposition Table\|90-Day Fill Rates\|Death Anomaly\|Patient Count Reconciliation" .planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-DISCOVERY.md</automated>
  </verify>

  <acceptance_criteria>
    - 143-DISCOVERY.md exists at `.planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-DISCOVERY.md`
    - File contains the string "Producer Disposition Table"
    - File contains the string "Join Key Risk" with at least one line number citation from R/28
    - File contains the string "90-Day Fill Rates"
    - File contains the string "Death Anomaly"
    - File contains the string "Patient Count Reconciliation"
    - If D-01b: File contains the string "Parameterisation Design"
    - If running on Windows: File contains "HiPerGator verification scripts" section with the verbatim R code blocks from Steps 4-6
    - No enrichment script has been edited (confirm via `git diff --name-only` shows only 143-DISCOVERY.md)
  </acceptance_criteria>

  <done>
    143-DISCOVERY.md exists with the producer disposition table (every script that writes any of the six enrichment columns classified), 90-day fill-rate benchmarks, death-anomaly report, and patient-count reconciliation. No script was modified.
  </done>
</task>

</tasks>

<verification>
- `git diff --name-only` shows only `.planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-DISCOVERY.md` — no R scripts modified
- 143-DISCOVERY.md contains all six required sections
- The D-01 decision is recorded in 143-DISCOVERY.md
</verification>

<success_criteria>
- 143-DISCOVERY.md exists with all required sections
- No code files were modified
- D-01 decision recorded so Plans 02 and 03 can proceed without re-asking the user
</success_criteria>

<output>
After completion, create `.planning/phases/143-180-day-file-enrichment-parity-and-review-follow-ups/143-01-SUMMARY.md`
</output>
