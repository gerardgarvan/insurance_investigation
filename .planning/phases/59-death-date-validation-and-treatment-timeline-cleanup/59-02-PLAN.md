---
phase: 59-death-date-validation-and-treatment-timeline-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - R/49_gantt_data_export.R
autonomous: true
requirements:
  - DVAL-01
  - DVAL-02
  - DVAL-04

must_haves:
  truths:
    - "Gantt CSVs exclude impossible death rows (death pseudo-treatment rows removed for patients with invalid death dates)"
    - "HL Diagnosis pseudo-treatment rows appear in both gantt_episodes.csv and gantt_detail.csv for all HL patients"
    - "HL Diagnosis rows use treatment_type = 'HL Diagnosis' with zero-length episode structure matching Death row pattern"
    - "Death rows in Gantt CSVs are sourced from validated_death_dates.rds (only death_valid = TRUE)"
    - "Gantt CSV row ordering is: HL Diagnosis -> Treatments -> Death (chronological sort by patient)"
  artifacts:
    - path: "R/49_gantt_data_export.R"
      provides: "Modified Gantt export with HL Diagnosis rows, validated death dates, impossible death exclusion"
    - path: "output/gantt_episodes.csv"
      provides: "Episode bars with HL Diagnosis rows and validated death rows"
    - path: "output/gantt_detail.csv"
      provides: "Detail ticks with HL Diagnosis rows and validated death rows"
  key_links:
    - from: "R/49_gantt_data_export.R"
      to: "validated_death_dates.rds"
      via: "readRDS() for pre-validated death dates"
      pattern: "readRDS.*validated_death_dates"
    - from: "R/49_gantt_data_export.R"
      to: "confirmed_hl_cohort.rds"
      via: "readRDS() for HL Diagnosis pseudo-treatment rows"
      pattern: "readRDS.*confirmed_hl_cohort"
    - from: "R/49_gantt_data_export.R"
      to: "output/gantt_episodes.csv"
      via: "write.csv() with HL Diagnosis + validated Death rows appended"
      pattern: "write\\.csv.*gantt_episodes"
---

<objective>
Modify R/49_gantt_data_export.R to: (1) replace raw DEATH table loading with validated_death_dates.rds consumption (only death_valid=TRUE rows), (2) add HL Diagnosis pseudo-treatment rows for all HL patients, and (3) update row ordering so HL Diagnosis appears as a timeline reference point.

Purpose: The Gantt chart data currently includes all death dates without validation. After Plan 01 creates validated_death_dates.rds, this plan integrates it so impossible deaths are excluded and HL diagnosis dates provide clinical context in the timeline visualization.

Output: Modified R/49_gantt_data_export.R producing updated gantt_episodes.csv and gantt_detail.csv
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-death-date-validation-and-treatment-timeline-cleanup/59-CONTEXT.md
@.planning/phases/59-death-date-validation-and-treatment-timeline-cleanup/59-RESEARCH.md
@.planning/phases/59-death-date-validation-and-treatment-timeline-cleanup/59-01-SUMMARY.md

<interfaces>
<!-- Key structures the executor needs from Plan 01 output and existing code -->

From validated_death_dates.rds (created by Plan 01 R/59_death_date_validation.R):
```r
# tibble with columns:
# ID (character) - patient identifier
# DEATH_DATE (Date) - validated death date
# DEATH_SOURCE (character) - source of death record
# death_valid (logical) - TRUE if death date is valid, FALSE if impossible
# post_death_activity (logical) - TRUE if patient has clinical activity after death date
```

From confirmed_hl_cohort.rds (created by Phase 55 R/55_cancer_summary_refined.R):
```r
# tibble with columns:
# ID (character) - patient identifier
# first_hl_dx_date (Date) - earliest HL diagnosis date across DIAGNOSIS + TUMOR_REGISTRY
# first_hl_dx_source (character) - "DIAGNOSIS", "TUMOR_REGISTRY", or "Both"
```

From R/49 episodes_export column structure (12 columns):
```r
# patient_id, treatment_type, episode_number,
# episode_start, episode_stop, episode_length_days,
# distinct_dates_in_episode, historical_flag,
# triggering_codes, triggering_code_descriptions,
# cancer_category, is_hodgkin
```

From R/49 detail_export column structure (11 columns):
```r
# patient_id, treatment_type, treatment_date,
# triggering_code, episode_number, episode_start,
# episode_stop, historical_flag,
# triggering_code_description,
# cancer_category, is_hodgkin
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HL Diagnosis pseudo-treatment rows and replace raw death loading with validated_death_dates.rds</name>
  <files>R/49_gantt_data_export.R</files>
  <read_first>
    R/49_gantt_data_export.R (FULL FILE -- must read complete file to understand all sections, especially Section 2C death loading at lines 394-424, Section 4B death row construction at lines 518-611, Section 5 CSV writing at lines 614-645)
    R/55_cancer_summary_refined.R (lines 458-464 for confirmed_hl_cohort.rds structure)
    .planning/phases/59-death-date-validation-and-treatment-timeline-cleanup/59-01-SUMMARY.md (for validated_death_dates.rds schema)
    .planning/phases/59-death-date-validation-and-treatment-timeline-cleanup/59-CONTEXT.md (for D-07 through D-09 decisions)
  </read_first>
  <action>
Modify R/49_gantt_data_export.R with the following specific changes:

**CHANGE 1: Update header comment block (top of file)**
Add these decision references to the existing comment block after the Phase 57 D-08 line:
```
#   D-01 (Phase 59): Impossible death dates excluded (death before earliest treatment)
#   D-02 (Phase 59): Impossible deaths REMOVED from Gantt CSVs (patient keeps treatment rows)
#   D-07 (Phase 59): HL Diagnosis pseudo-treatment row in both CSVs (treatment_type="HL Diagnosis")
#   D-08 (Phase 59): HL Diagnosis rows for ALL patients with HL dx, not only confirmed 7-day cohort
#   D-09 (Phase 59): HL Diagnosis provides timeline reference point for Gantt visualization
```

Add to Inputs section:
```
#   - cache/outputs/confirmed_hl_cohort.rds (Phase 59: HL diagnosis dates for pseudo-treatment rows)
#   - cache/outputs/validated_death_dates.rds (Phase 59: pre-validated death dates, impossible deaths excluded)
```

**CHANGE 2: Add input path for validated deaths and HL cohort (after line ~63 DESCRIPTIONS_RDS)**
Add these two lines after the DESCRIPTIONS_RDS definition:
```r
# Validated death dates (built by R/59_death_date_validation.R, Phase 59)
VALIDATED_DEATHS_RDS <- file.path(CONFIG$cache$outputs_dir, "validated_death_dates.rds")

# Confirmed HL cohort (built by R/55_cancer_summary_refined.R, Phase 55)
COHORT_RDS <- file.path(CONFIG$cache$outputs_dir, "confirmed_hl_cohort.rds")
```

**CHANGE 3: Replace SECTION 2C (death data loading) -- currently lines ~394-424**
Replace the entire SECTION 2C block (which loads from DuckDB DEATH table directly) with loading from validated_death_dates.rds:

```r
# --- SECTION 2C: LOAD VALIDATED DEATH DATA (Phase 59: D-01, D-02) ---

message("\n--- Loading validated death dates (Phase 59) ---")

if (!file.exists(VALIDATED_DEATHS_RDS)) {
  warning("validated_death_dates.rds not found. Run R/59_death_date_validation.R first. Falling back to raw DEATH table.")
  # Fallback: original DuckDB loading pattern (backward compatibility)
  USE_DUCKDB <- TRUE
  open_pcornet_con()
  death_raw <- get_pcornet_table("DEATH")
  if (is.null(death_raw)) {
    warning("DEATH table not found in DuckDB. Death rows will be skipped.")
    death_data <- tibble(ID = character(), DEATH_DATE = as.Date(character()))
  } else {
    death_data <- death_raw %>%
      collect() %>%
      mutate(
        DEATH_DATE = parse_pcornet_date(DEATH_DATE),
        DEATH_DATE = if_else(year(DEATH_DATE) == 1900L, as.Date(NA), DEATH_DATE)
      ) %>%
      filter(!is.na(DEATH_DATE)) %>%
      select(ID, DEATH_DATE) %>%
      group_by(ID) %>%
      summarise(DEATH_DATE = min(DEATH_DATE), .groups = "drop")
  }
  close_pcornet_con()
} else {
  validated_deaths <- readRDS(VALIDATED_DEATHS_RDS)
  # Only use deaths marked as valid (per D-02: impossible deaths removed from Gantt)
  death_data <- validated_deaths %>%
    filter(death_valid == TRUE) %>%
    select(ID, DEATH_DATE)

  n_excluded <- sum(validated_deaths$death_valid == FALSE, na.rm = TRUE)
  message(glue("  Loaded validated death dates: {nrow(death_data)} valid, {n_excluded} impossible excluded (per D-02)"))
}

message(glue("  Patients with valid death dates for Gantt: {nrow(death_data)}"))
```

**CHANGE 4: Add SECTION 2D -- Load HL Cohort for Diagnosis Rows (new section after 2C)**
Insert a new section between the existing SECTION 2C (now modified) and SECTION 3 (column validation):

```r
# --- SECTION 2D: LOAD HL COHORT FOR DIAGNOSIS ROWS (Phase 59: D-07, D-08) ---

message("\n--- Loading HL cohort for diagnosis pseudo-treatment rows ---")

if (!file.exists(COHORT_RDS)) {
  warning("confirmed_hl_cohort.rds not found. Run R/55_cancer_summary_refined.R first. HL Diagnosis rows will be skipped.")
  hl_cohort <- tibble(
    ID = character(),
    first_hl_dx_date = as.Date(character()),
    first_hl_dx_source = character()
  )
} else {
  hl_cohort <- readRDS(COHORT_RDS)
  # Apply 1900 sentinel filtering to HL diagnosis dates (same pattern as death dates)
  hl_cohort <- hl_cohort %>%
    mutate(first_hl_dx_date = if_else(year(first_hl_dx_date) == 1900L, as.Date(NA), first_hl_dx_date)) %>%
    filter(!is.na(first_hl_dx_date))
  message(glue("  Loaded {nrow(hl_cohort)} HL patients with valid first diagnosis dates"))
}
```

**CHANGE 5: Add SECTION 4C -- Build and Append HL Diagnosis Pseudo-Treatment Rows (new section after existing 4B)**
Insert a new section between SECTION 4B (death rows) and SECTION 5 (CSV writing). This section builds HL Diagnosis rows using the EXACT same column structure as death rows (per D-07):

```r
# --- SECTION 4C: BUILD AND APPEND HL DIAGNOSIS PSEUDO-TREATMENT ROWS (Phase 59: D-07, D-08, D-09) ---

if (nrow(hl_cohort) > 0) {
  message("\n--- Building HL Diagnosis pseudo-treatment rows ---")

  # HL Diagnosis rows for episodes table (per D-07: same structure as Death rows)
  hl_dx_episodes <- hl_cohort %>%
    left_join(cancer_categories_per_patient, by = "ID") %>%
    mutate(
      cancer_category = ifelse(is.na(cancer_category), "Hodgkin Lymphoma", cancer_category),
      is_hodgkin = TRUE
    ) %>%
    mutate(
      patient_id = ID,
      treatment_type = "HL Diagnosis",
      episode_number = 1L,
      episode_start = first_hl_dx_date,
      episode_stop = first_hl_dx_date,
      episode_length_days = 0L,
      distinct_dates_in_episode = 1L,
      historical_flag = FALSE,
      triggering_codes = "",
      triggering_code_descriptions = ""
    ) %>%
    select(
      patient_id, treatment_type, episode_number,
      episode_start, episode_stop, episode_length_days,
      distinct_dates_in_episode, historical_flag,
      triggering_codes, triggering_code_descriptions,
      cancer_category, is_hodgkin
    )

  # HL Diagnosis rows for detail table (per D-07: same structure as Death detail rows)
  hl_dx_detail <- hl_cohort %>%
    left_join(cancer_categories_per_patient, by = "ID") %>%
    mutate(
      cancer_category = ifelse(is.na(cancer_category), "Hodgkin Lymphoma", cancer_category),
      is_hodgkin = TRUE
    ) %>%
    mutate(
      patient_id = ID,
      treatment_type = "HL Diagnosis",
      treatment_date = first_hl_dx_date,
      triggering_code = "",
      episode_number = 1L,
      episode_start = first_hl_dx_date,
      episode_stop = first_hl_dx_date,
      historical_flag = FALSE,
      triggering_code_description = ""
    ) %>%
    select(
      patient_id, treatment_type, treatment_date,
      triggering_code, episode_number, episode_start,
      episode_stop, historical_flag,
      triggering_code_description,
      cancer_category, is_hodgkin
    )

  # Verify column alignment before binding (same pattern as Death row validation)
  expected_ep_cols <- colnames(episodes_export)
  hl_dx_ep_cols <- colnames(hl_dx_episodes)
  missing_in_hl_dx_ep <- setdiff(expected_ep_cols, hl_dx_ep_cols)
  extra_in_hl_dx_ep <- setdiff(hl_dx_ep_cols, expected_ep_cols)

  if (length(missing_in_hl_dx_ep) > 0) {
    stop(glue("HL Diagnosis episodes missing columns: {paste(missing_in_hl_dx_ep, collapse = ', ')}"))
  }
  if (length(extra_in_hl_dx_ep) > 0) {
    warning(glue("HL Diagnosis episodes has extra columns: {paste(extra_in_hl_dx_ep, collapse = ', ')}"))
  }

  expected_det_cols <- colnames(detail_export)
  hl_dx_det_cols <- colnames(hl_dx_detail)
  missing_in_hl_dx_det <- setdiff(expected_det_cols, hl_dx_det_cols)
  extra_in_hl_dx_det <- setdiff(hl_dx_det_cols, expected_det_cols)

  if (length(missing_in_hl_dx_det) > 0) {
    stop(glue("HL Diagnosis detail missing columns: {paste(missing_in_hl_dx_det, collapse = ', ')}"))
  }
  if (length(extra_in_hl_dx_det) > 0) {
    warning(glue("HL Diagnosis detail has extra columns: {paste(extra_in_hl_dx_det, collapse = ', ')}"))
  }

  # Append HL Diagnosis rows (per D-09: appears chronologically before treatments/death)
  episodes_export <- bind_rows(episodes_export, hl_dx_episodes) %>%
    arrange(patient_id, episode_start, treatment_type)

  detail_export <- bind_rows(detail_export, hl_dx_detail) %>%
    arrange(patient_id, treatment_date, treatment_type)

  message(glue("  Added {nrow(hl_dx_episodes)} HL Diagnosis episode rows"))
  message(glue("  Added {nrow(hl_dx_detail)} HL Diagnosis detail rows"))

} else {
  message("\n--- No valid HL diagnosis dates found; skipping HL Diagnosis rows ---")
}
```

**CHANGE 6: Update SECTION 6 (final summary) to include HL Diagnosis stats**
After the existing death row count line (line ~641), add:
```r
n_hl_dx_rows <- sum(episodes_export$treatment_type == "HL Diagnosis", na.rm = TRUE)
message(glue("  HL Diagnosis pseudo-treatment rows in episodes: {format(n_hl_dx_rows, big.mark = ',')}"))
```

**IMPORTANT ORDERING NOTE:** The existing code already does `arrange(patient_id, episode_start, treatment_type)` after appending death rows. The HL Diagnosis rows are appended BEFORE the final arrange, so all row types (treatment, HL Diagnosis, Death) will be sorted chronologically per patient. The Death row append in Section 4B already happens before Section 5 CSV write. Section 4C (HL Diagnosis) should be inserted AFTER Section 4B so the final arrange in 4B covers all rows. Alternatively, if the code in 4B already does the arrange, Section 4C should re-arrange after its own bind_rows.

Actually, reading the code carefully: Section 4B appends death rows and arranges. Section 4C must come AFTER 4B so HL Diagnosis rows AND death rows are all present before the final CSV write. Section 4C's bind_rows + arrange will re-sort everything correctly.
  </action>
  <verify>
    <automated>Rscript -e "parse('R/49_gantt_data_export.R')" exits 0 (valid R syntax). grep -c "validated_death_dates" R/49_gantt_data_export.R returns at least 2, grep -c "HL Diagnosis" R/49_gantt_data_export.R returns at least 5, grep -c "confirmed_hl_cohort" R/49_gantt_data_export.R returns at least 2</automated>
  </verify>
  <acceptance_criteria>
    - R/49_gantt_data_export.R parses without error (Rscript -e "parse('R/49_gantt_data_export.R')" exits 0)
    - File contains `VALIDATED_DEATHS_RDS <- file.path(CONFIG$cache$outputs_dir, "validated_death_dates.rds")`
    - File contains `COHORT_RDS <- file.path(CONFIG$cache$outputs_dir, "confirmed_hl_cohort.rds")`
    - File contains `readRDS(VALIDATED_DEATHS_RDS)` for loading pre-validated death data
    - File contains `filter(death_valid == TRUE)` for excluding impossible deaths (per D-02)
    - File contains `readRDS(COHORT_RDS)` for loading HL cohort
    - File contains `treatment_type = "HL Diagnosis"` for pseudo-treatment rows (per D-07)
    - File contains `episode_length_days = 0L` in HL Diagnosis row construction (zero-length event per D-07)
    - File contains `cancer_category = ifelse(is.na(cancer_category), "Hodgkin Lymphoma"` for HL Diagnosis default category
    - File contains `is_hodgkin = TRUE` in HL Diagnosis row construction
    - File contains column validation for HL Diagnosis rows (setdiff pattern matching death row validation)
    - File contains fallback to raw DEATH table loading if validated_death_dates.rds does not exist
    - File header comment block contains "D-01 (Phase 59)" and "D-07 (Phase 59)"
    - File contains `if_else(year(first_hl_dx_date) == 1900L` for sentinel filtering on HL dates
    - SECTION 4C appears AFTER SECTION 4B (death rows) and BEFORE SECTION 5 (CSV writing)
  </acceptance_criteria>
  <done>R/49_gantt_data_export.R loads validated_death_dates.rds (only death_valid=TRUE), builds HL Diagnosis pseudo-treatment rows from confirmed_hl_cohort.rds for both gantt_episodes.csv and gantt_detail.csv, validates column alignment before bind_rows, and produces Gantt CSVs with impossible deaths excluded and HL Diagnosis timeline reference rows included.</done>
</task>

</tasks>

<verification>
1. `Rscript -e "parse('R/49_gantt_data_export.R')"` exits 0 (valid R syntax)
2. File loads validated_death_dates.rds and filters to death_valid == TRUE
3. File has fallback to raw DEATH table if RDS not found (backward compatibility)
4. File loads confirmed_hl_cohort.rds and builds HL Diagnosis pseudo-treatment rows
5. HL Diagnosis rows use treatment_type = "HL Diagnosis", episode_length_days = 0, is_hodgkin = TRUE
6. Column validation (setdiff) applied to HL Diagnosis rows before bind_rows
7. Sentinel date filtering (1900) applied to first_hl_dx_date
8. Section ordering: 2C (validated deaths) -> 2D (HL cohort) -> ... -> 4B (death rows) -> 4C (HL Dx rows) -> 5 (CSV write)
</verification>

<success_criteria>
R/49_gantt_data_export.R modifications:
- Replace direct DEATH table loading with validated_death_dates.rds consumption (per D-01, D-02)
- Add HL Diagnosis pseudo-treatment rows for all HL patients (per D-07, D-08, D-09)
- Maintain backward compatibility with fallback if validated_death_dates.rds missing
- Both gantt_episodes.csv and gantt_detail.csv include HL Diagnosis and validated Death rows
- Column alignment validated before bind_rows (no silent NA columns)
</success_criteria>

<output>
After completion, create `.planning/phases/59-death-date-validation-and-treatment-timeline-cleanup/59-02-SUMMARY.md`
</output>
