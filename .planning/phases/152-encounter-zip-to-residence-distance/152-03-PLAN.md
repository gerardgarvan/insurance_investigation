---
phase: 152-encounter-zip-to-residence-distance
plan: 03
type: execute
wave: 3
depends_on: ["152-02"]
files_modified:
  - R/122_encounter_distance.R
  - R/39_run_all_investigations.R
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: false
requirements:
  - DIST-SUMMARY
  - DIST-DISTRIBUTION
  - DIST-FLAGS
  - DIST-QC
  - DIST-XLSX
  - DIST-REGISTER
  - DIST-RUN
must_haves:
  truths:
    - "R/122 produces B_patient_summary, C_distribution, D_flags, and a QC waterfall sheet"
    - "R/122 writes encounter_distance_YYYYMMDD.xlsx (KEY leftmost) and encounter_distance_YYYYMMDD.rds"
    - "QC sheet notes the WV ZIP9 coverage gap and includes a fallback sensitivity row"
    - "R/122 is registered in R/39, R/88 (Section 15ah), and R/SCRIPT_INDEX.md"
    - "Script runs end-to-end on HiPerGator against the N=9,282 HL cohort and re-issues the workbook"
  artifacts:
    - path: "R/122_encounter_distance.R"
      provides: "SECTION 8-12: patient summary, distribution, flags, QC, xlsx+rds assembly"
      contains: "wb_workbook"
    - path: "R/39_run_all_investigations.R"
      provides: "R/122 registration after R/121"
      contains: "R/122_encounter_distance.R"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15ah structural checks for R/122"
      contains: "Section 15ah"
    - path: "R/SCRIPT_INDEX.md"
      provides: "R/122 investigations row"
      contains: "122_encounter_distance.R"
  key_links:
    - from: "R/122 xlsx assembly"
      to: "output/encounter_distance_YYYYMMDD.xlsx"
      via: "wb_workbook + KEY leftmost + A/B/C/D/QC sheets + wb_save"
      pattern: "wb_workbook"
    - from: "R/39 investigation_scripts"
      to: "R/122_encounter_distance.R"
      via: "vector entry after R/121"
      pattern: "R/122_encounter_distance"
---

<objective>
Complete `R/122_encounter_distance.R` (SECTION 8–12: patient summary, distribution table, far-
distance flags, QC waterfall, and xlsx + rds assembly), then register the script in R/39, R/88
(Section 15ah), and R/SCRIPT_INDEX.md. Finish with a blocking HiPerGator run checkpoint that
confirms the workbook is produced against the real N=9,282 HL cohort.

Purpose: Turns the encounter-level distance tibble into the shipped deliverable and wires it
into the pipeline's run/smoke-test/index registry. The HiPerGator run is the phase's stated
definition-of-done (both centroid reference files must be staged there first).
Output: Full R/122, updated R/39/R/88/SCRIPT_INDEX.md, and (on HiPerGator) the dated workbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/152-encounter-zip-to-residence-distance/152-CONTEXT.md
@.planning/phases/152-encounter-zip-to-residence-distance/152-RESEARCH.md
@R/115_zip_stability_counts.R
@R/39_run_all_investigations.R

<interfaces>
<!-- xlsx assembly pattern from R/115 (openxlsx2) — reuse add_styled_sheet + UF constants. -->

From R/115_zip_stability_counts.R (lines 2115-2126, 2262+):
```r
UF_BLUE   <- "#0021A5"   # NOTE: distinct from utils_pptx.R's UF_BLUE ("#003087")
UF_ORANGE <- "#FA4616"
add_styled_sheet <- function(wb, sheet_name, title_text, subtitle_text, data_tbl, ...) { ... }  # copy verbatim from R/115
wb <- wb_workbook()
add_styled_sheet(wb, "KEY", ...)      # KEY leftmost (D-02 pattern)
# ... A/B/C/D/QC sheets ...
wb_save(wb, OUTPUT_XLSX)
```

Registration — R/39_run_all_investigations.R investigation_scripts vector, last entry currently:
```r
  "R/121_zip_problem_inventory.R"               # ... (Phase 151)
```
Add R/122 after it (add trailing comma to the R/121 line).

R/88 last section is "# Section 15ag: per-patient ZIP problem inventory (Phase 151)" (line ~5188);
add "# Section 15ah: encounter-ZIP to residence distance (Phase 152) ----" after it, mirroring the
15af/15ag grep-based structural-check pattern.

CONTEXT.md sheet spec:
- B_patient_summary: per patient — n_encounters, n_with_distance, median_km, iqr_km, max_km, pct_gt50km, pct_gt200km
- C_distribution: bins (0–5, 5–25, 25–50, 50–200, >200 km) × distance_basis, encounter + patient counts
- D_flags: encounters > 200 km (primary) and > 50 km (secondary), residence interval-matched, for team review
- QC: coverage waterfall (encounters total → normalized ZIP → residence resolved → centroid resolved → distance computed); unmatched ZIP9 count by state; WV gap noted; fallback sensitivity row
- Thresholds D-01: 200 km primary, 50 km secondary (both in D_flags)
- KEY must carry the D-03 open-question text about encounter-ZIP being patient- or facility-sourced.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SECTION 8–11 — patient summary, distribution, flags, QC waterfall</name>
  <read_first>
    - R/122_encounter_distance.R (current SECTION 1–7 from Plan 02 — build on the enc_distance tibble)
    - R/115_zip_stability_counts.R (lines 1960-2115 — waterfall/QC table construction patterns; how summary tibbles are built)
    - .planning/phases/152-encounter-zip-to-residence-distance/152-CONTEXT.md (D-01 thresholds, D-02 fallback inclusion, QC sheet spec, output sheet columns)
    - .planning/phases/152-encounter-zip-to-residence-distance/152-RESEARCH.md (Pitfall 7 WV gap)
  </read_first>
  <action>
    Continue R/122, consuming the `enc_distance` tibble from SECTION 7.

    SECTION 8 — PATIENT SUMMARY (`B_patient_summary`): group_by(ID) with EXACTLY these columns:
    `n_encounters = n()`, `n_with_distance = sum(!is.na(distance_km))`,
    `median_km = median(distance_km, na.rm = TRUE)`,
    `iqr_km = IQR(distance_km, na.rm = TRUE)`, `max_km = max(distance_km, na.rm = TRUE)`,
    `pct_gt50km = mean(distance_km > 50, na.rm = TRUE) * 100`,
    `pct_gt200km = mean(distance_km > 200, na.rm = TRUE) * 100`. Per D-02, fallback encounters
    (res_match_fallback == TRUE) ARE included in this summary. Guard max()/median() against
    all-NA groups (they yield -Inf/NaN — coalesce to NA_real_).

    SECTION 9 — DISTRIBUTION (`C_distribution`): bin distance_km into factor levels
    `"0-5","5-25","25-50","50-200",">200"` (via cut() with breaks c(0,5,25,50,200,Inf)), then
    count encounters AND distinct patients per (bin × distance_basis). Include NA-distance rows
    as an explicit `"NA"` bin category, not dropped.

    SECTION 10 — FLAGS (`D_flags`): filter enc_distance to `distance_km > 50` (secondary) and add
    a boolean `flag_gt200 = distance_km > 200` (primary). Include columns for team review: ID,
    ENCOUNTERID, ADMIT_DATE, enc_zip_norm, res_zip9, res_zip5, res_match_type, res_match_fallback,
    distance_km, distance_basis, plus flag_gt200. Both the 200 km and 50 km thresholds must be
    represented (D-01).

    SECTION 11 — QC WATERFALL (`qc_tbl`): a coverage waterfall with rows in order:
    n_encounters_total → n_enc_norm_zip (enc_zip_norm non-NA) → n_residence_resolved
    (res_match_type != "none" & non-NA) → n_centroid_resolved (both centroids non-NA) →
    n_distance_computed (distance_km non-NA). Also build: `unmatched_zip9_by_state` (count of
    encounters whose ZIP9 centroid was unresolved, grouped by state — derive state from ZIP5
    prefix or leave as a documented count if state is unavailable); an explicit WV-gap NOTE row
    with text: `"WV ZIP9 centroid coverage may be lower due to the Neighborhood Atlas crosswalk gap; see unmatched-by-state counts."` (Pitfall 7); and a FALLBACK SENSITIVITY row isolating
    distances computed only from res_match_fallback == TRUE encounters (median/max over that
    subset) per D-02. Assert `n_encounters_total` equals the DuckDB ENCOUNTER row count pulled
    in SECTION 3 (the exit-criteria reconciliation).
  </action>
  <verify>
    <automated>Rscript -e "l <- readLines('R/122_encounter_distance.R'); stopifnot(any(grepl('B_patient_summary|patient_summary', l)), any(grepl('C_distribution|distribution', l)), any(grepl('D_flags|flag_gt200', l)), any(grepl('WV', l))); cat('OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -En "pct_gt50km|pct_gt200km" R/122_encounter_distance.R` matches both
    - `grep -n "iqr_km" R/122_encounter_distance.R` matches
    - `grep -n "c(0, 5, 25, 50, 200, Inf)\|c(0,5,25,50,200,Inf)" R/122_encounter_distance.R` matches (distribution bins)
    - `grep -n "flag_gt200" R/122_encounter_distance.R` matches
    - `grep -En "> 200|> 50" R/122_encounter_distance.R` matches both thresholds (D-01)
    - `grep -n "res_match_fallback" R/122_encounter_distance.R` matches in the summary/sensitivity context
    - `grep -n "WV ZIP9 centroid coverage" R/122_encounter_distance.R` matches (Pitfall 7)
    - `grep -n "n_encounters_total" R/122_encounter_distance.R` matches (QC waterfall + reconciliation)
  </acceptance_criteria>
  <done>SECTION 8–11 present: B/C/D summary tibbles with exact CONTEXT.md columns, both distance thresholds, fallback included per D-02, and a QC waterfall with the WV note, unmatched-by-state counts, fallback sensitivity row, and total-encounter reconciliation.</done>
</task>

<task type="auto">
  <name>Task 2: SECTION 12 — xlsx (KEY leftmost) + rds; register in R/39, R/88, SCRIPT_INDEX.md</name>
  <read_first>
    - R/115_zip_stability_counts.R (lines 2115-2410 — UF_BLUE/UF_ORANGE constants, add_styled_sheet(), wb_workbook() assembly with KEY first, wb_save)
    - R/39_run_all_investigations.R (lines 215-224 — investigation_scripts vector, R/121 last entry)
    - R/88_smoke_test_comprehensive.R (lines 5126-5258 — Section 15af/15ag structural-check pattern to mirror for 15ah)
    - R/SCRIPT_INDEX.md (Investigations section — the R/121 row to append after)
  </read_first>
  <action>
    SECTION 12 — XLSX ASSEMBLY AND WRITE. Copy `UF_BLUE <- "#0021A5"`, `UF_ORANGE <- "#FA4616"`,
    and `add_styled_sheet()` verbatim from R/115 (lines ~2118-2160). Build
    `wb <- wb_workbook()` and add sheets in this order (KEY LEFTMOST, D-02 pattern):
    1. KEY — sheet index + column dictionary. MUST include the D-03 open-question text verbatim:
       `"Whether encounter ZIP is patient- or facility-sourced in this OneFlorida+ extract is unknown; column description reflects travel distance if facility-sourced, proxy validation if patient-sourced."`
    2. A_encounter_distance — the enc_distance tibble (exact column set from Plan 02).
    3. B_patient_summary
    4. C_distribution
    5. D_flags
    6. QC — qc_tbl (waterfall + WV note + fallback sensitivity + unmatched-by-state)
    Then `wb_save(wb, OUTPUT_XLSX)` and message the path. Write the rds:
    `saveRDS(enc_distance, OUTPUT_RDS)` (Claude's discretion per CONTEXT.md — full encounter-level
    tibble is acceptable; a list with A+B is also acceptable, choose the encounter-level tibble
    and note it in the KEY sheet).

    REGISTRATION:
    - R/39_run_all_investigations.R: add a trailing comma to the `"R/121_zip_problem_inventory.R"`
      line, then add on the next line:
      `  "R/122_encounter_distance.R"                  # Encounter-ZIP to residence haversine distance (Phase 152); outputs encounter_distance_YYYYMMDD.xlsx + .rds`
    - R/88_smoke_test_comprehensive.R: after the Section 15ag block (`message(glue("\nSection 15ag: ...`),
      add `# Section 15ah: encounter-ZIP to residence distance (Phase 152) ----` and a check block
      mirroring 15ag: grep R/122 source text for `haversine_km`, `get_zip_centroid`,
      `FACILITY_LOCATION`, `distance_basis`, the `encounter_distance_{RUN_DATE}` output naming, and
      the presence of KEY/A/B/C/D/QC sheet names; tally `p152_pass`/`p152_fail` and print a
      `Section 15ah: {p152_pass} PASS, {p152_fail} FAIL` summary line.
    - R/SCRIPT_INDEX.md: append to the Investigations group after the R/121 row:
      `| 122_encounter_distance.R | Encounter-ZIP to residence haversine distance; get_zip_centroid() + haversine_km() helpers; encounter-level and patient-level summaries (Phase 152) | 00_config, utils/utils_address |`
      and bump the Total script count (currently 103 → 104) if the index carries a total line.
  </action>
  <verify>
    <automated>Rscript -e "stopifnot(any(grepl('R/122_encounter_distance', readLines('R/39_run_all_investigations.R'))), any(grepl('Section 15ah', readLines('R/88_smoke_test_comprehensive.R'))), any(grepl('122_encounter_distance', readLines('R/SCRIPT_INDEX.md'))), any(grepl('wb_workbook', readLines('R/122_encounter_distance.R')))); cat('OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "wb_workbook()" R/122_encounter_distance.R` matches
    - `grep -n "UF_BLUE" R/122_encounter_distance.R` matches (styled sheets)
    - `grep -n "wb_save(wb, OUTPUT_XLSX)" R/122_encounter_distance.R` matches
    - `grep -n "saveRDS(enc_distance, OUTPUT_RDS)\|saveRDS" R/122_encounter_distance.R` matches
    - `grep -n "patient- or facility-sourced" R/122_encounter_distance.R` matches (D-03 KEY text)
    - `grep -n "R/122_encounter_distance.R" R/39_run_all_investigations.R` matches
    - `grep -n "Section 15ah" R/88_smoke_test_comprehensive.R` matches
    - `grep -n "p152_pass\|p152_fail" R/88_smoke_test_comprehensive.R` matches
    - `grep -n "122_encounter_distance.R" R/SCRIPT_INDEX.md` matches
    - KEY appears before A_encounter_distance in the wb_add/add_styled_sheet call order (KEY leftmost)
  </acceptance_criteria>
  <done>Workbook assembled with KEY leftmost and all six sheets, saved to the dated xlsx; rds written; R/122 registered in R/39, R/88 Section 15ah, and SCRIPT_INDEX.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HiPerGator end-to-end run of R/122 (blocking checkpoint)</name>
  <action>Stage both centroid reference files on HiPerGator, run R/122 end-to-end against the N=9,282 HL cohort, and confirm the workbook is produced and the QC waterfall reconciles to the DuckDB ENCOUNTER row count. See how-to-verify for exact steps.</action>
  <what-built>R/122_encounter_distance.R end-to-end: encounter-ZIP-to-residence great-circle distance for the HL cohort, producing encounter_distance_YYYYMMDD.xlsx (KEY, A_encounter_distance, B_patient_summary, C_distribution, D_flags, QC) + encounter_distance_YYYYMMDD.rds. Registered in R/39, R/88 (Section 15ah), SCRIPT_INDEX.md. Local structural checks and testthat pass; runtime requires HiPerGator DuckDB + both staged centroid reference files.</what-built>
  <how-to-verify>
    1. Stage on HiPerGator under data/reference/ (or the CONFIG paths): `zcta_gazetteer_centroids.csv`
       (Census ZCTA Gazetteer, tab-delimited) and `zip9_bg_centroid_crosswalk.csv` (from
       /blue/erin.mobley.precision/).
    2. `module load R/4.4.2` then `Rscript R/122_encounter_distance.R`.
    3. Confirm both probe gates report "found" and the DuckDB ENCOUNTER gate passes.
    4. Confirm `output/encounter_distance_YYYYMMDD.xlsx` is written with all six sheets, KEY leftmost.
    5. Confirm the QC waterfall's `n_encounters_total` reconciles to the DuckDB ENCOUNTER row count
       for the N=9,282 HL cohort (exit criteria).
    6. Spot-check A_encounter_distance: distance_km values are plausible (most small, a tail > 200 km),
       distance_basis populated, res_match_fallback present.
    7. Run `Rscript -e "testthat::test_file('tests/testthat/test-122-distance.R')"` on HiPerGator (real
       reference files present) — the previously skipped present-file centroid cases should now run.
    8. Optionally run R/88 and confirm the Section 15ah checks pass.
  </how-to-verify>
  <resume-signal>Type "approved" (workbook produced, waterfall reconciles) or describe issues (probe-gate failures, column mismatches, fan-out row inflation, etc.)</resume-signal>
</task>

</tasks>

<verification>
- R/122 SECTION 8–12 present and parse
- xlsx has KEY leftmost + A/B/C/D/QC sheets; rds written
- QC carries WV note, fallback sensitivity row, unmatched-by-state, total-encounter reconciliation
- Registered in R/39, R/88 Section 15ah, SCRIPT_INDEX.md
- HiPerGator run produces the workbook and the waterfall reconciles (blocking checkpoint)
</verification>

<success_criteria>
- B_patient_summary/C_distribution/D_flags/QC built with exact CONTEXT.md columns
- Both 200 km and 50 km thresholds recorded in D_flags
- Fallback encounters included per D-02 with a QC sensitivity row
- WV gap noted in QC (Pitfall 7)
- KEY carries the D-03 open-question text
- Workbook + rds written; R/122 registered in all three registries
- HiPerGator end-to-end run confirmed against N=9,282
</success_criteria>

<output>
After completion, create `.planning/phases/152-encounter-zip-to-residence-distance/152-03-SUMMARY.md`
</output>
