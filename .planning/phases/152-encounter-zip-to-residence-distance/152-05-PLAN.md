---
phase: 152-encounter-zip-to-residence-distance
plan: 05
type: execute
wave: 4
depends_on: ["152-03"]
files_modified:
  - R/00_config.R
autonomous: true
gap_closure: true
requirements:
  - DIST-CONFIG
must_haves:
  truths:
    - "CONFIG$adi_zip9_parquet is defined in R/00_config.R"
    - "CONFIG$tiger_bg_dir is defined in R/00_config.R"
    - "Both keys follow the IS_LOCAL / HiPerGator conditional pattern already used by atlas_zip4_dir"
  artifacts:
    - path: "R/00_config.R"
      provides: "adi_zip9_parquet and tiger_bg_dir CONFIG keys with HiPerGator paths"
      contains: "adi_zip9_parquet"
  key_links:
    - from: "R/122a_build_zip9_centroid_crosswalk.R"
      to: "CONFIG$adi_zip9_parquet"
      via: "CONFIG$adi_zip9_parquet %||% hardcoded fallback (line 27)"
      pattern: "adi_zip9_parquet"
    - from: "R/122a_build_zip9_centroid_crosswalk.R"
      to: "CONFIG$tiger_bg_dir"
      via: "CONFIG$tiger_bg_dir %||% hardcoded fallback (line 28)"
      pattern: "tiger_bg_dir"
---

<objective>
Add the two missing CONFIG keys that R/122a_build_zip9_centroid_crosswalk.R needs: `adi_zip9_parquet`
and `tiger_bg_dir`. Both are present in R/122a as `CONFIG$key %||% hardcoded_path`, meaning the
script runs without error via fallback, but the CONFIG contract from Plan 00 is incomplete. Adding
the keys makes the paths explicit, overridable, and consistent with every other Phase 152 path key.

Purpose: Close Gap 2 from 152-VERIFICATION.md — CONFIG$adi_zip9_parquet and CONFIG$tiger_bg_dir
absent. Single-file edit to R/00_config.R only.
Output: Two new CONFIG entries appended after the existing Phase 152 block (after `atlas_zip4_dir`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-encounter-zip-to-residence-distance/152-VERIFICATION.md

<interfaces>
<!-- Insertion point in R/00_config.R -->
<!-- Current last entry of the Phase 152 block (lines 192-198): -->

```r
  atlas_zip4_dir = if (IS_LOCAL) {
    NULL  # Not available locally; R/122a probes and stops with a message if NULL/missing
  } else {
    "/blue/erin.mobley.precision/ADI_zip4_files"
    # ^^^ UNCONFIRMED placeholder — update after running ls on HiPerGator
  }
)
```

<!-- The closing `)` on line 198 closes the CONFIG list. -->
<!-- The two new keys must be inserted BEFORE that closing `)`. -->

<!-- Paths confirmed from R/122a_build_zip9_centroid_crosswalk.R lines 27-28 (the %||% fallbacks): -->
<!--   adi_zip9_parquet: "/blue/erin.mobley-hl.bcu/ADI/out/adi_2024_zip9_all_dedup.parquet" -->
<!--   tiger_bg_dir:     "/blue/erin.mobley-hl.bcu/ADI/tiger_bg"                           -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add adi_zip9_parquet and tiger_bg_dir to CONFIG in R/00_config.R</name>
  <files>R/00_config.R</files>

  <read_first>
    Read R/00_config.R lines 185-200 to confirm the exact closing `)` line number and that
    `atlas_zip4_dir` is the last entry before it.
    Read R/122a_build_zip9_centroid_crosswalk.R lines 27-28 to confirm the fallback paths that
    the new CONFIG values must match exactly.
  </read_first>

  <action>
Insert two new CONFIG entries immediately before the closing `)` that ends the CONFIG list
(currently line 198, after the `atlas_zip4_dir` block). Add a trailing comma to `atlas_zip4_dir`'s
closing brace if it lacks one (R list entries must be comma-separated).

Text to insert (between the end of `atlas_zip4_dir`'s block and the closing `)`):

```r

  # Neighborhood Atlas ZIP9 ADI parquet — input for R/122a crosswalk builder.
  # Built Aug 2026; 68.6M rows; columns include zip9, bg_geoid, state.
  # Path matches the %||% fallback in R/122a (line 27).
  adi_zip9_parquet = if (IS_LOCAL) {
    NULL  # Not available locally; R/122a probes and stops with a message if NULL/missing
  } else {
    "/blue/erin.mobley-hl.bcu/ADI/out/adi_2024_zip9_all_dedup.parquet"
  },

  # TIGER/Line 2020 block-group zip directory — input for R/122a crosswalk builder.
  # Contains tl_2020_<FIPS2>_bg.zip files (one per state + DC + PR).
  # Path matches the %||% fallback in R/122a (line 28).
  # Run slurm/122a_fetch_tiger_bg.sh on a HiPerGator login node to populate.
  tiger_bg_dir = if (IS_LOCAL) {
    NULL  # Not available locally; R/122a probes and stops with a message if NULL/missing
  } else {
    "/blue/erin.mobley-hl.bcu/ADI/tiger_bg"
  }
```

Notes:
- `adi_zip9_parquet` gets a trailing comma (another entry follows it).
- `tiger_bg_dir` has NO trailing comma (it is the last entry before the closing `)`).
- The IS_LOCAL conditional mirrors the `atlas_zip4_dir` pattern exactly.
- The HiPerGator paths (`/blue/erin.mobley-hl.bcu/ADI/...`) are taken verbatim from
  R/122a_build_zip9_centroid_crosswalk.R lines 27-28 to ensure CONFIG and `%||%` fallback
  agree on the same paths.
  </action>

  <verify>
    <automated>grep -c "adi_zip9_parquet" R/00_config.R</automated>
    Expected output: `2` (the key name and the path string both appear)

    <automated>grep -c "tiger_bg_dir" R/00_config.R</automated>
    Expected output: `2` (the key name and the comment both appear)

    Confirm the HiPerGator paths match the fallback paths in R/122a:
    <automated>grep "adi_zip9_parquet\|tiger_bg_dir" R/00_config.R</automated>
    Expected: lines showing `/blue/erin.mobley-hl.bcu/ADI/out/adi_2024_zip9_all_dedup.parquet`
    and `/blue/erin.mobley-hl.bcu/ADI/tiger_bg`
  </verify>

  <acceptance_criteria>
    - `grep -c "adi_zip9_parquet" R/00_config.R` returns `2`
    - `grep -c "tiger_bg_dir" R/00_config.R` returns `2`
    - `grep "adi_zip9_parquet" R/00_config.R` shows the exact path
      `/blue/erin.mobley-hl.bcu/ADI/out/adi_2024_zip9_all_dedup.parquet`
    - `grep "tiger_bg_dir" R/00_config.R` shows the exact path
      `/blue/erin.mobley-hl.bcu/ADI/tiger_bg`
    - `grep -c "atlas_zip4_dir" R/00_config.R` still returns `2` (unchanged)
    - `grep -c "zip9_bg_centroid_path" R/00_config.R` still returns `2` (unchanged)
  </acceptance_criteria>

  <done>
    CONFIG$adi_zip9_parquet and CONFIG$tiger_bg_dir are defined in R/00_config.R using the
    IS_LOCAL conditional pattern. Both HiPerGator paths match R/122a's %||% fallback values.
    R/122a will now resolve both paths via CONFIG rather than the hardcoded fallback. All
    acceptance-criteria greps pass.
  </done>
</task>

</tasks>

<verification>
Run after Task 1 completes:

```bash
grep -c "adi_zip9_parquet" R/00_config.R   # must be 2
grep -c "tiger_bg_dir" R/00_config.R       # must be 2
grep "adi_zip9_parquet" R/00_config.R      # must show the .parquet path
grep "tiger_bg_dir" R/00_config.R          # must show the tiger_bg dir path
```

Also confirm the CONFIG list syntax is valid (no missing/extra commas) by reviewing the diff:
```bash
git diff R/00_config.R
```
The atlas_zip4_dir closing brace must have a trailing comma; the new tiger_bg_dir entry must
have no trailing comma; the CONFIG closing `)` must be unchanged.
</verification>

<success_criteria>
Gap 2 is closed when:
- `grep -c "adi_zip9_parquet" R/00_config.R` returns `2`
- `grep -c "tiger_bg_dir" R/00_config.R` returns `2`
- Both paths in CONFIG match R/122a's %||% fallback values exactly
- `git diff R/00_config.R` shows only the two new key blocks added, nothing else changed
</success_criteria>

<output>
After completion, create `.planning/phases/152-encounter-zip-to-residence-distance/152-05-SUMMARY.md`
</output>
