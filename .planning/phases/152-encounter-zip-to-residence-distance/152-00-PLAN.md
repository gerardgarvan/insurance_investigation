---
phase: 152-encounter-zip-to-residence-distance
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/122a_build_zip9_centroid_crosswalk.R
  - slurm/122a_build_zip9_centroid_crosswalk.sbatch
  - slurm/122a_fetch_tiger_bg.sh
autonomous: false
requirements:
  - DIST-CROSSWALK
must_haves:
  truths:
    - "data/reference/zip9_bg_centroid_crosswalk.csv AND .parquet exist on HiPerGator with columns ZIP9, GEOID, INTPTLAT, INTPTLON, N_BG (N_BG = distinct block groups the Atlas maps that ZIP9 to; lowest GEOID kept when > 1)"
    - "Every ZIP9 in the crosswalk is a 9-digit character string with leading zeros preserved; every GEOID is a 12-digit block-group FIPS"
    - "The build logs per-state row counts and names the states missing from the Atlas download (West Virginia expected)"
  artifacts:
    - path: "R/122a_build_zip9_centroid_crosswalk.R"
      provides: "One-off builder for the ZIP9 -> block-group centroid crosswalk from the Neighborhood Atlas ZIP+4 files"
      contains: "zip9_bg_centroid_crosswalk"
  key_links:
    - from: "R/122a"
      to: "/blue/erin.mobley-hl.bcu/ADI/out/adi_2024_zip9_all_dedup.parquet"
      via: "arrow open_dataset, distinct zip9/bg_geoid"
      pattern: "open_dataset"
    - from: "R/122a"
      to: "block-group internal points"
      via: "TIGER/Line block-group shapefile attributes (INTPTLAT/INTPTLON) or NHGIS block-group centroid table"
      pattern: "INTPTLAT"
---

<objective>
Build the reference file Plan 01's `get_zip_centroid(zip, "zip9")` reads and Plan 02's probe
gate 2 looks for: `data/reference/zip9_bg_centroid_crosswalk.csv` with columns
`ZIP9, GEOID, INTPTLAT, INTPTLON`. The Phase 152 context lists this file as "not yet staged";
it does not exist anywhere — it has to be derived from the 51 Neighborhood Atlas per-state
ZIP+4 → block-group files already downloaded to `/blue/erin.mobley.precision/` joined to
block-group internal-point coordinates.

Purpose: Without this file the ZIP9 side of Phase 152 degrades to ZIP5 everywhere. The build
is a ~7.5 GB read, so it runs once as a SLURM job and its output is cached under
data/reference/.
Output: The crosswalk CSV plus a build log; no changes to R/122.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-encounter-zip-to-residence-distance/152-CONTEXT.md
@.planning/phases/152-encounter-zip-to-residence-distance/152-RESEARCH.md
@R/utils/utils_address.R
@R/00_config.R

<interfaces>
Output contract consumed by get_zip_centroid() (Plan 01):
```
ZIP9      character, 9 digits, no hyphen, leading zeros preserved
GEOID     character, 12-digit block-group FIPS (state 2 + county 3 + tract 6 + block group 1)
INTPTLAT  double, decimal degrees
INTPTLON  double, decimal degrees (negative for the US)
```
One row per ZIP9. If the Atlas maps a ZIP9 to more than one block group, keep the first by
GEOID order and count the duplicates in the log.

Neighborhood Atlas ZIP+4 file layout (2024 release): the ZIP+4 identifier and the block-group
FIPS column NAMES are not confirmed in this repo — Task 1 inspects a header before anything
is hard-coded. Expect a ZIP+4 identifier that may carry a prefix letter and/or an underscore
between the 5- and 4-digit parts, and a block-group FIPS column alongside ADI_NATRANK /
ADI_STATERNK.
</interfaces>
</context>

<tasks>

<!-- REVISED 2026-09-15 after inspecting /blue/erin.mobley-hl.bcu/ADI/. The ZIP9 -> block-group
     step is ALREADY DONE: out/adi_2024_zip9_all_dedup.parquet has zip9 + bg_geoid (12-digit) for
     68.6M rows. The per-state Atlas CSVs are not re-read. Only block-group centroids are missing. -->

<task type="auto">
  <n>Task 1: Add CONFIG paths and drop the build script + helpers into the repo</n>
  <read_first>
    - R/00_config.R (CONFIG list; path-key style from Plan 01 Task 2)
    - Provided files: R/122a_build_zip9_centroid_crosswalk.R, slurm/122a_build_zip9_centroid_crosswalk.sbatch, slurm/122a_fetch_tiger_bg.sh
  </read_first>
  <action>
    1. Add to CONFIG in R/00_config.R:
       ```r
         adi_zip9_parquet = "/blue/erin.mobley-hl.bcu/ADI/out/adi_2024_zip9_all_dedup.parquet",
         tiger_bg_dir     = "/blue/erin.mobley-hl.bcu/ADI/tiger_bg",
       ```
       (`atlas_zip4_dir` is NOT needed — the parquet replaces the per-state read.)
    2. Copy the three provided files to R/ and slurm/ (create slurm/ and logs/ if absent).
    3. In the sbatch, set `module load R` to the exact module name from `module spider R`,
       and confirm `--account`/`--qos` match the repo's other sbatch files.
    4. Confirm `arrow` and `foreign` load under that R module (`Rscript -e "library(arrow); library(foreign)"`).
  </action>
  <verify>
    <automated>Rscript -e "parse('R/122a_build_zip9_centroid_crosswalk.R'); source('R/00_config.R'); stopifnot(file.exists(CONFIG\$adi_zip9_parquet)); cat('OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - CONFIG$adi_zip9_parquet and CONFIG$tiger_bg_dir exist
    - R/122a parses; sbatch has a real module name
  </acceptance_criteria>
  <done>Config and scripts in place.</done>
</task>

<task type="auto">
  <n>Task 2: Fetch TIGER/Line 2020 block-group files (login node)</n>
  <action>
    On a LOGIN node (compute nodes have no outbound internet):
    `bash slurm/122a_fetch_tiger_bg.sh` — downloads tl_2020_<FIPS>_bg.zip for 50 states + DC + PR
    into CONFIG$tiger_bg_dir (~600 MB total). Re-runnable; skips files already present.
  </action>
  <verify>
    <automated>ls /blue/erin.mobley-hl.bcu/ADI/tiger_bg/tl_2020_*_bg.zip | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 52 zip files present (51 if one download fails — note which, and rerun)
  </acceptance_criteria>
  <done>TIGER block-group zips staged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <n>Task 3: Run the build and validate the crosswalk</n>
  <action>`sbatch slurm/122a_build_zip9_centroid_crosswalk.sbatch`, then validate.</action>
  <what-built>data/reference/zip9_bg_centroid_crosswalk.csv (ZIP9, GEOID, INTPTLAT, INTPTLON, N_BG), one row per ZIP9, from the August ADI parquet joined to TIGER 2020 block-group internal points; plus a BUILDLOG.</what-built>
  <how-to-verify>
    1. Check logs/122a_<jobid>.out finishes with "Wrote ...csv", "Wrote ...parquet" and "Build log: ...".
       (If the CSV was built by an earlier version of R/122a without the parquet step, rerun R/122a — it is idempotent.)
    2. `head -3 data/reference/zip9_bg_centroid_crosswalk.csv` — header exactly ZIP9,GEOID,INTPTLAT,INTPTLON,N_BG.
    3. Rscript -e 'x <- vroom::vroom("data/reference/zip9_bg_centroid_crosswalk.csv", col_types = vroom::cols(ZIP9="c", GEOID="c", .default="d")); stopifnot(all(nchar(x$ZIP9)==9), all(nchar(x$GEOID)==12), !any(duplicated(x$ZIP9))); print(summary(x$INTPTLAT)); print(summary(x$INTPTLON)); print(mean(is.na(x$INTPTLAT) | is.na(x$INTPTLON))); print(table(x$N_BG > 1))'
       Expect lat within 17–72, lon within -180 to -64, NA share well under 1%, and the multi-block-group share (N_BG > 1) small — the BUILDLOG reports it; above ~5% is worth a look before trusting ZIP9 centroids.
    4. Open the BUILDLOG: WV listed as absent from Atlas; unmatched-by-state small everywhere.
    5. Spot-check: `grep "^32611" data/reference/zip9_bg_centroid_crosswalk.csv | head -3` — all near 29.65, -82.32.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (NA share high, wrong header, job OOM).</resume-signal>
</task>

</tasks>

<verification>
- Crosswalk CSV exists at CONFIG$zip9_bg_centroid_path with the four contracted columns
- Build log records per-state counts and missing states
- Plan 02 probe gate 2 reports "found" on the next R/122 run
</verification>

<success_criteria>
- One row per ZIP9, leading zeros intact, block-group (12-digit) GEOIDs
- Lat/lon within CONUS/AK/HI/PR bounds; Gainesville spot-check passes
- WV gap documented in the build log (and surfaced later in the R/122 QC sheet)
</success_criteria>

<o>
After completion, create `.planning/phases/152-encounter-zip-to-residence-distance/152-00-SUMMARY.md`
</o>
