---
phase: 16-dataset-snapshots
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - R/05_visualize_waterfall.R
  - R/06_visualize_sankey.R
  - R/16_encounter_analysis.R
  - R/11_generate_pptx.R
autonomous: true
requirements:
  - SNAP-03
  - SNAP-04
must_haves:
  truths:
    - "User can see figure-backing data frames saved as RDS before each plot is rendered"
    - "User can see table-backing data frames saved as RDS before each PPTX table is built"
    - "User can trace which RDS backs which figure via _data suffix naming"
  artifacts:
    - path: "R/05_visualize_waterfall.R"
      provides: "save_output_data() call for waterfall backing data"
      contains: "save_output_data"
    - path: "R/06_visualize_sankey.R"
      provides: "save_output_data() call for sankey backing data"
      contains: "save_output_data"
    - path: "R/16_encounter_analysis.R"
      provides: "save_output_data() calls for 8 backing data frames"
      contains: "save_output_data"
    - path: "R/11_generate_pptx.R"
      provides: "save_output_data() calls for 6 unique summary tables"
      contains: "save_output_data"
  key_links:
    - from: "R/05_visualize_waterfall.R"
      to: "R/utils_snapshot.R"
      via: "save_output_data() function call"
      pattern: "save_output_data"
    - from: "R/16_encounter_analysis.R"
      to: "outputs subdirectory"
      via: "save_output_data() writes to outputs/"
      pattern: "save_output_data"
    - from: "R/11_generate_pptx.R"
      to: "outputs subdirectory"
      via: "save_output_data() writes to outputs/"
      pattern: "save_output_data"
---

<objective>
Add save_output_data() calls to all four visualization scripts to save figure-backing and table-backing data frames as RDS snapshots before rendering.

Purpose: Ensures every figure and summary table has its source data frame preserved (SNAP-03, SNAP-04) for reproducibility, debugging, and downstream reuse without re-running the full pipeline.

Output: Updated R/05_visualize_waterfall.R, R/06_visualize_sankey.R, R/16_encounter_analysis.R, R/11_generate_pptx.R
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dataset-snapshots/16-CONTEXT.md
@.planning/phases/16-dataset-snapshots/16-RESEARCH.md
@.planning/phases/16-dataset-snapshots/16-01-SUMMARY.md

@R/05_visualize_waterfall.R
@R/06_visualize_sankey.R
@R/16_encounter_analysis.R
@R/11_generate_pptx.R
@R/utils_snapshot.R

<interfaces>
<!-- save_output_data() function signature from Plan 01 -->
From R/utils_snapshot.R:
```r
save_output_data <- function(df, name, subdir = "outputs")
# Saves df to CONFIG$cache$cache_dir/{subdir}/{name}.rds with logging
# Default subdir = "outputs" (no need to specify for figure/table backing data)
```

From R/00_config.R (Plan 01 update):
```r
CONFIG$cache$outputs_dir = "/blue/erin.mobley-hl.bcu/clean/rds/outputs"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backing data snapshots to waterfall and sankey scripts</name>
  <files>R/05_visualize_waterfall.R, R/06_visualize_sankey.R</files>
  <read_first>R/05_visualize_waterfall.R, R/06_visualize_sankey.R, R/utils_snapshot.R</read_first>
  <action>
**R/05_visualize_waterfall.R** -- 1 backing data frame (per D-06, D-10):

After the `attrition_plot_data` transformation (line 36, after the mutate block ends) and BEFORE the `message(glue("Prepared..."))` log on line 38, add:
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(attrition_plot_data, "waterfall_attrition_data")
```

This saves the EXACT data frame passed to `ggplot()` on line 45.

**R/06_visualize_sankey.R** -- 1 backing data frame (per D-06, D-10):

After all sankey_data transformations are complete (after Section 4, line 148, after the stratum labels are created and BEFORE the `message("\n--- Building Sankey Diagram ---")` log on line 154), add:
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(sankey_data, "sankey_patient_flow_data")
```

This saves the EXACT data frame passed to `ggplot()` on line 156, with TREATMENT_CATEGORY, PAYER_LABEL, TREATMENT_LABEL columns already applied.
  </action>
  <verify>
    <automated>grep -c "save_output_data" R/05_visualize_waterfall.R && grep -c "save_output_data" R/06_visualize_sankey.R && grep "waterfall_attrition_data" R/05_visualize_waterfall.R && grep "sankey_patient_flow_data" R/06_visualize_sankey.R</automated>
  </verify>
  <acceptance_criteria>
    - R/05_visualize_waterfall.R contains `save_output_data(attrition_plot_data, "waterfall_attrition_data")`
    - R/05_visualize_waterfall.R: the save_output_data call appears AFTER the mutate block (line ~36) and BEFORE the ggplot call (line ~45)
    - R/06_visualize_sankey.R contains `save_output_data(sankey_data, "sankey_patient_flow_data")`
    - R/06_visualize_sankey.R: the save_output_data call appears AFTER Section 4 stratum labels and BEFORE the ggplot call
    - Both files have exactly 1 save_output_data call each
  </acceptance_criteria>
  <done>05_visualize_waterfall.R saves waterfall_attrition_data.rds. 06_visualize_sankey.R saves sankey_patient_flow_data.rds. Both snapshot the exact ggplot-ready data frame before plot rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Add backing data snapshots to encounter analysis and PPTX scripts</name>
  <files>R/16_encounter_analysis.R, R/11_generate_pptx.R</files>
  <read_first>R/16_encounter_analysis.R, R/11_generate_pptx.R, R/utils_snapshot.R</read_first>
  <action>
**R/16_encounter_analysis.R** -- 8 backing data frames (per D-08, D-10):

Add `save_output_data()` calls for all 7 figures and 1 summary table. Place each call AFTER data transformation is complete but BEFORE the `ggplot()` or `write_csv()` call that consumes it.

1. **Section 1 histogram** (after `hist_data` is fully prepared with capped values, around line 58, BEFORE `p1 <- ggplot(...)` on line 62):
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(hist_data, "encounters_per_person_by_payor_data")
```

2. **Section 2 post-tx encounters by DX year** (after `enc_by_year` summarise completes around line 107, BEFORE `p2 <- ggplot(...)` on line 109):
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(enc_by_year, "post_tx_encounters_by_dx_year_data")
```

3. **Section 3 total encounters by DX year** -- `enc_by_year` is reused from Section 2 (same data frame, different column). Add snapshot BEFORE `p3 <- ggplot(...)` on line 133:
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(enc_by_year, "total_encounters_by_dx_year_data")
```
This IS the same data frame as #2 but saves under a different name for traceability to the output PNG. Per D-10, the name mirrors the output filename.

4. **Section 4 summary table** (after `summary_with_sums` is assembled around line 192, BEFORE `write_csv(...)` on line 194):
```r
# Snapshot: table backing data (per SNAP-04)
save_output_data(summary_with_sums, "encounter_summary_by_payor_age_data")
```

5. **Section 5 post-tx by age group** (after `age_post_tx` is computed around line 208, BEFORE `p4 <- ggplot(...)` on line 212):
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(age_post_tx, "post_tx_by_age_group_data")
```

6. **Section 6c unique dates histogram** (after `hist_data_ud` capped values around line 296, BEFORE `p_ud1 <- ggplot(...)` on line 300):
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(hist_data_ud, "unique_dates_per_person_by_payor_data")
```

7. **Section 6d post-tx unique dates by DX year** (after `enc_ud_by_year` summarise around line 335, BEFORE `p_ud2 <- ggplot(...)` on line 337):
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(enc_ud_by_year, "post_tx_unique_dates_by_dx_year_data")
```

8. **Section 6e total unique dates by DX year** -- `enc_ud_by_year` reused. Add BEFORE `p_ud3 <- ggplot(...)` on line 356:
```r
# Snapshot: figure backing data (per SNAP-03)
save_output_data(enc_ud_by_year, "total_unique_dates_by_dx_year_data")
```

Total for 16_encounter_analysis.R: 8 save_output_data calls. The `age_post_tx` figure (snapshot #5) and CSV share the same backing data frame, so only one snapshot is needed.

**R/11_generate_pptx.R** -- 6 unique summary data frames (per D-07):

Per D-07, save only the UNIQUE summary data frames rendered into tables, not every pivot of the same data. The `tbl2` through `tbl13` are all generated by `build_payer_table()` / `build_payer_table_with_na()` / `build_enr_coverage_table()` directly from `cohort_full` -- those are "same data pivoted differently." Save `cohort_full` as the master source and the truly unique computed tables.

1. **cohort_full** (after Section 2e assembly completes, around line 405, AFTER the `message(glue("\n  Full cohort assembled..."))` line on line 407):
```r
# Snapshot: PPTX master source data (per SNAP-04)
save_output_data(cohort_full, "pptx_cohort_full_data")
```

2. **tbl14** (Last Tx = Last Encounter, after line 1013, BEFORE `pptx <- add_table_slide(...)` on line 1015):
```r
# Snapshot: table backing data (per SNAP-04)
save_output_data(tbl14, "last_tx_equals_last_encounter_data")
```

3. **tbl15** (Missing Post-Treatment Payer breakdown, after line 1063, BEFORE `pptx <- add_table_slide(...)` on line 1065):
```r
# Snapshot: table backing data (per SNAP-04)
save_output_data(tbl15, "missing_post_tx_payer_breakdown_data")
```

4. **tbl16** (Insurance After Last Treatment retention, after line 1147, BEFORE `pptx <- add_table_slide(...)` on line 1152):
```r
# Snapshot: table backing data (per SNAP-04)
save_output_data(tbl16, "insurance_after_last_tx_retention_data")
```

5. **summary_stats** (Encounter summary statistics per payer, after line 1221 where N is formatted, BEFORE `pptx <- add_table_slide(...)` on line 1223):
```r
# Snapshot: table backing data (per SNAP-04)
save_output_data(summary_stats, "encounter_summary_stats_by_payer_data")
```

6. **ud_summary_stats** (Unique dates summary statistics per payer, after line 1353 where N is formatted, BEFORE `pptx <- add_table_slide(...)` on line 1355):
```r
# Snapshot: table backing data (per SNAP-04)
save_output_data(ud_summary_stats, "unique_dates_summary_stats_by_payer_data")
```

Total for 11_generate_pptx.R: 6 save_output_data calls.
  </action>
  <verify>
    <automated>grep -c "save_output_data" R/16_encounter_analysis.R && grep -c "save_output_data" R/11_generate_pptx.R</automated>
  </verify>
  <acceptance_criteria>
    - R/16_encounter_analysis.R contains exactly 8 save_output_data() calls
    - R/16_encounter_analysis.R contains these snapshot names: "encounters_per_person_by_payor_data", "post_tx_encounters_by_dx_year_data", "total_encounters_by_dx_year_data", "encounter_summary_by_payor_age_data", "post_tx_by_age_group_data", "unique_dates_per_person_by_payor_data", "post_tx_unique_dates_by_dx_year_data", "total_unique_dates_by_dx_year_data"
    - R/11_generate_pptx.R contains exactly 6 save_output_data() calls
    - R/11_generate_pptx.R contains these snapshot names: "pptx_cohort_full_data", "last_tx_equals_last_encounter_data", "missing_post_tx_payer_breakdown_data", "insurance_after_last_tx_retention_data", "encounter_summary_stats_by_payer_data", "unique_dates_summary_stats_by_payer_data"
    - Each save_output_data call appears BEFORE the corresponding ggplot()/ggsave()/add_table_slide() call
    - Each save_output_data call is preceded by a comment starting with "# Snapshot:"
  </acceptance_criteria>
  <done>16_encounter_analysis.R saves 8 backing data frames (7 figures + 1 summary table, with shared enc_by_year/enc_ud_by_year saved under different names per D-10). 11_generate_pptx.R saves 6 unique summary data frames (cohort_full master + 5 computed tables). All snapshots placed before rendering, using _data suffix naming convention.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "save_output_data" R/05_visualize_waterfall.R` returns 1
2. `grep -c "save_output_data" R/06_visualize_sankey.R` returns 1
3. `grep -c "save_output_data" R/16_encounter_analysis.R` returns 8
4. `grep -c "save_output_data" R/11_generate_pptx.R` returns 6
5. Total across all 4 viz scripts: 16 save_output_data calls
6. All snapshot names end with `_data` (per D-10 naming convention)
</verification>

<success_criteria>
- All 4 visualization scripts have save_output_data() calls
- 16 total backing data snapshots across all scripts
- Snapshot names mirror output filenames with _data suffix (per D-10)
- Each snapshot placed AFTER data transformation, BEFORE plot/table rendering
- No duplicate snapshots for the same data frame (except where D-10 requires different names for different output files using the same data)
</success_criteria>

<output>
After completion, create `.planning/phases/16-dataset-snapshots/16-02-SUMMARY.md`
</output>
