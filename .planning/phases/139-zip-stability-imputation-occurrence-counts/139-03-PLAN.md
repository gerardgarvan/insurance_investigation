---
phase: 139-zip-stability-imputation-occurrence-counts
plan: 03
type: execute
wave: 3
depends_on:
  - 139-01
  - 139-02
files_modified:
  - R/115_zip_stability_counts.R
  - tests/testthat/test-115-validation-curve.R
autonomous: true
requirements: [B-01, B-02, B-03, B-04]
must_haves:
  truths:
    - "Interval matching coalesces NA period_end_dt (open-ended address records) to a far-future sentinel (period_end_eff) before comparison against ADMIT_DATE -- an encounter covered by an open-ended record classifies as has_direct_zip9, not has_neither (139-05-PATCH FIX-02)"
    - "Encounters and the cohort-restricted address view are both scoped to the study cohort (get_hl_patient_ids()), with the cohort N logged (139-05-PATCH FIX-03a)"
    - "The semi_join pre-filter against addr_coal before the many-to-many interval join is the default, not conditional on profiling (139-05-PATCH FIX-04c)"
    - "'No covering address record exists' and 'a covering record exists with both ZIP9 and ZIP5 NA' are reported as separate counts (has_no_record, has_record_but_empty), with has_neither kept as their union for downstream scenario logic (139-05-PATCH FIX-04e)"
    - "Every ENCOUNTER row is classified into exactly one of: already-has-ZIP9 (baseline), S1, S2, S3, or unresolvable -- the ordered assignment (S1 then S2 then S3) is applied and reported (B-03)"
    - "Unordered eligible-for counts are also reported per scenario so overlap between S1/S2/S3 is visible, not hidden by the ordered assignment alone (B-03)"
    - "S1 and S2 are each split into backward-only, forward-only, and either -- since get_zip9_at_date() only looks backward, the forward variant is new logic this plan adds locally, not a change to utils_address.R (B-04)"
    - "S3 stays backward-only (matches its own definition -- ZIP5 does not match the PRIOR ZIP9's ZIP5) and is reported without presenting either resolution as decided, since S3 was still pending as of the 08/04 notes (B-01)"
    - "The ordered 'S3' column and the unordered 'S3-eligible' column are documented as different quantities -- an encounter eligible only via S1-forward is assigned S1 (ordered) while remaining S3-eligible (unordered); both are reported so nothing is lost (139-05-PATCH FIX-04d)"
    - "Every count is reported at BOTH encounter and patient level, with the denominator named in the cell/column label (B-02)"
  artifacts:
    - path: "R/115_zip_stability_counts.R"
      provides: "SECTION 1B addition: classify_encounter_zip(encounters, addr_coal) as a pure, testable function using period_end_eff (139-05-PATCH FIX-02/FIX-04c/FIX-04e). Part B: cohort-restricted ENCOUNTER pull via DuckDB (FIX-03a), per-encounter classification, S1-S4 ordered and unordered scenario counts, backward/forward/either direction split."
      contains: "classify_encounter_zip"
    - path: "tests/testthat/test-115-validation-curve.R"
      provides: "Appends the open-ended-interval fixture (139-05-PATCH FIX-05, FIX-02): an encounter covered by a period_end_dt = NA record classifies has_direct_zip9, not has_neither"
      contains: "open-ended"
  key_links:
    - from: "R/115_zip_stability_counts.R (Part B)"
      to: "ENCOUNTER table (DuckDB)"
      via: "get_pcornet_table ENCOUNTER, select ID, ENCOUNTERID, ADMIT_DATE, collect(), restricted to COHORT_IDS"
      pattern: "get_pcornet_table"
    - from: "R/115_zip_stability_counts.R (Part B)"
      to: "addr_coal / zip9_seq / zip5_seq (139-01)"
      via: "per-patient reference spells used for both classify_encounter_zip()'s interval match and the S1/S2/S3 backward+forward eligibility search"
      pattern: "scenario_assigned"
    - from: "R/utils/utils_treatment.R"
      to: "R/115_zip_stability_counts.R"
      via: "get_hl_patient_ids() -- the established cohort-definition pattern reused from R/106/R/111/R/100/R/107/R/109 (139-05-PATCH FIX-03a); do not invent a new cohort definition"
      pattern: "get_hl_patient_ids"
---

<objective>
Add Part B to R/115_zip_stability_counts.R: pull the ENCOUNTER table (restricted to the study cohort), classify every encounter's current ZIP9/ZIP5 availability against the coalesced address history, and count how often each imputation scenario (S1-S4, verbatim from the 08/04 meeting notes) would fire -- ordered (S1 then S2 then S3) and unordered (eligible-for, showing overlap), at both encounter and patient level, split by direction (backward/forward/either) for S1 and S2.

Purpose: this is the core "how many times does each imputation scenario occur" deliverable the team asked for. It counts occurrences; it does NOT write an imputed ZIP9 anywhere and does NOT modify get_zip9_at_date() or build approximate_zip9() (both explicitly Out of Scope) -- Part B's counting logic is new, local, script-level code in R/115 only.

This plan has been amended by 139-05-PATCH.md:
- FIX-02: the interval filter must use addr_coal's period_end_eff (sentineled), not period_end_dt (NA for open-ended/current-address records) -- the un-patched design classified every encounter covered by a patient's current address as has_neither, which is most recent encounters, not an edge case.
- FIX-03a: Part B/C's population is the study cohort (get_hl_patient_ids(), the pattern already established by R/106/R/111/R/100/R/107/R/109), applied here to both the ENCOUNTER pull and the address view classify_encounter_zip() consumes. This is what makes Plan 04's C-02 reconciliation (against the notes' 26-patient control total, itself a cohort-specific number) comparable.
- FIX-04c: the semi_join pre-filter against addr_coal is now the default, not conditional on profiling.
- FIX-04d: the ordered "S3" column and unordered "S3-eligible" column are documented as different quantities.
- FIX-04e: has_neither is split into has_no_record and has_record_but_empty, reporting both.

Output: R/115_zip_stability_counts.R extended with classify_encounter_zip() (SECTION 1B), per-encounter scenario_assigned classification, and the aggregated S1-S4 count tables (ordered and unordered, encounter and patient level, direction-split). tests/testthat/test-115-validation-curve.R gains the open-ended-interval fixture. Still no xlsx write (Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-CONTEXT.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-01-SUMMARY.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-02-SUMMARY.md

<interfaces>
Cohort-definition pattern (139-05-PATCH FIX-03a -- reuse verbatim, do not invent a new one; see
R/106 lines 658-665, R/111 lines 69/97-112, R/100 line 190, R/107 lines 79-123, R/109 lines 83-142
for precedent):

  if (!exists("get_hl_patient_ids")) source("R/utils/utils_treatment.R")
  COHORT_IDS <- get_hl_patient_ids()
  message(glue("[R/115] Study cohort (HL patients) size: {length(COHORT_IDS)}"))

get_hl_patient_ids() queries DIAGNOSIS via DuckDB (safe_table("DIAGNOSIS")) -- it needs pcornet_con,
already opened by 139-01 Task 2's probe gate 2, so no new connection setup is required here.

ENCOUNTER pull pattern (mirror R/111_doi_classification.R Section 3-4, lines 93-125 -- same
project convention, do not invent a different one), restricted to COHORT_IDS per FIX-03a:

  USE_DUCKDB <- TRUE   (already set in 139-01 Task 2's probe gate Section 2)
  (pcornet_con already opened in 139-01 Task 2's Section 2 -- do not re-open here)
  enc_tbl <- get_pcornet_table("ENCOUNTER")
  if (is.null(enc_tbl)) stop("ENCOUNTER table not found in DuckDB -- cannot compute Part B scenario counts.")
  encounters <- enc_tbl %>%
    filter(!is.na(ADMIT_DATE), ID %in% COHORT_IDS) %>%
    select(ID, ENCOUNTERID, ADMIT_DATE) %>%
    collect() %>%
    mutate(ADMIT_DATE = parse_pcornet_date(ADMIT_DATE)) %>%
    filter(!is.na(ADMIT_DATE))

addr_coal / zip9_seq / zip5_seq contract (from 139-01): addr_coal has one row per raw address
record with columns ID, zip9_norm, zip5_coalesced, period_start_dt, period_end_dt (NA-preserving),
period_end_open, period_end_eff (sentineled), already sentinel-filtered, date-filtered,
study-period-filtered. zip9_seq / zip5_seq are the consecutive-collapsed spell sequences per
ZIP-type (from 139-01 Task 3). Part B reuses addr_coal directly for its own top-1 interval match
(see Task 1) -- it does NOT call get_zip9_at_date() for this, because get_zip9_at_date()'s own
ZIP5 output is derived-from-ZIP9-only (see 139-01's coalesce_zip5() note) and would mask
"ZIP5-present-but-ZIP9-absent" address records, which Part B needs to detect correctly for
S1/S3 classification. 139-05-PATCH FIX-02: filter against period_end_eff, not period_end_dt --
period_end_dt is NA for every open-ended (current-address) record, and ADMIT_DATE < NA is NA,
which filter() drops -- misclassifying every encounter covered by a patient's current address.

get_zip9_at_date() itself is NOT called anywhere in this plan -- all classification is done via
classify_encounter_zip() (Task 1) plus direct backward/forward searches over zip9_seq/zip5_seq
(Task 2). This keeps Part B fully self-consistent with its own coalesced-ZIP5 data rather than
mixing in get_zip9_at_date()'s derived-only ZIP5 semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cohort-restricted ENCOUNTER pull + classify_encounter_zip() (period_end_eff, semi_join default, has_no_record/has_record_but_empty split)</name>
  <files>R/115_zip_stability_counts.R, tests/testthat/test-115-validation-curve.R</files>
  <behavior>
    For each (ID, ADMIT_DATE) encounter, find the addr_coal record whose interval covers
    ADMIT_DATE (period_start_dt less-or-equal ADMIT_DATE, ADMIT_DATE less-than period_end_eff --
    139-05-PATCH FIX-02, NOT period_end_dt), preferring a non-NA zip9_norm record and breaking
    remaining ties by most-recent period_start_dt (same tie-break priority as get_zip9_at_date()'s
    own interval_hits logic, utils_address.R lines 121-130 -- replicated here, not called, because
    the coalesced ZIP5 is needed alongside it).
    - has_direct_zip9 = a covering record exists AND its zip9_norm is not NA
    - has_direct_zip5_only = a covering record exists AND its zip9_norm is NA AND its
      zip5_coalesced is not NA
    - has_no_record (139-05-PATCH FIX-04e) = no covering record exists at all
    - has_record_but_empty (139-05-PATCH FIX-04e) = a covering record exists with BOTH zip9_norm
      and zip5_coalesced NA
    - has_neither = has_no_record OR has_record_but_empty (kept for downstream scenario logic)
    These four (has_direct_zip9 / has_direct_zip5_only / has_no_record / has_record_but_empty) are
    mutually exclusive and exhaustive over all encounters.

    tests/testthat/test-115-validation-curve.R open-ended-interval fixture (139-05-PATCH FIX-05):
    one patient, one address record with period_start_dt set and period_end_dt = NA (i.e.
    period_end_eff = the far-future sentinel, period_end_open = TRUE), one encounter with
    ADMIT_DATE after period_start_dt. Assert classify_encounter_zip() classifies this encounter
    has_direct_zip9 == TRUE (assuming the record's zip9_norm is non-NA), NOT has_neither. Under the
    pre-patch period_end_dt-based filter this encounter would have been dropped by the interval
    filter (ADMIT_DATE < NA is NA) and misclassified as has_neither.
  </behavior>
  <action>
Insert into R/115_zip_stability_counts.R's SECTION 1B (edit in place, immediately after Plan 02's
aggregate_validation_curve(); do not append at end of file):

    classify_encounter_zip <- function(encounters, addr_coal) {
      # 139-05-PATCH FIX-04c: semi_join pre-filter is now the DEFAULT (not conditional on
      # profiling) -- it is free, strictly reducing, and the many-to-many join below is the
      # largest operation in the script.
      addr_pref <- addr_coal %>%
        semi_join(encounters, by = "ID") %>%
        select(ID, zip9_norm, zip5_coalesced, period_start_dt, period_end_eff)

      # 139-05-PATCH FIX-02: filter against period_end_eff (sentineled), NOT period_end_dt
      # (NA-preserving for open-ended records). ADMIT_DATE < NA evaluates to NA and filter() drops
      # NA rows -- under a period_end_dt-based filter, EVERY encounter covered by an open-ended
      # (current) address record is misclassified as has_neither.
      top1 <- encounters %>%
        left_join(addr_pref, by = "ID", relationship = "many-to-many") %>%
        filter(period_start_dt <= ADMIT_DATE, ADMIT_DATE < period_end_eff) %>%
        group_by(ID, ENCOUNTERID, ADMIT_DATE) %>%
        arrange(is.na(zip9_norm), desc(period_start_dt), .by_group = TRUE) %>%
        slice(1) %>%
        ungroup() %>%
        mutate(has_covering_record = TRUE) %>%
        select(ID, ENCOUNTERID, ADMIT_DATE, direct_zip9 = zip9_norm, direct_zip5 = zip5_coalesced, has_covering_record)

      encounters %>%
        left_join(top1, by = c("ID", "ENCOUNTERID", "ADMIT_DATE")) %>%
        mutate(
          has_covering_record   = coalesce(has_covering_record, FALSE),
          has_direct_zip9       = !is.na(direct_zip9),
          has_direct_zip5_only  = is.na(direct_zip9) & !is.na(direct_zip5),
          # 139-05-PATCH FIX-04e: "no covering record exists" and "a covering record exists with
          # both fields NA" are different data conditions -- the notes' S2 presumes a record
          # exists. Split rather than conflate; has_neither remains their union.
          has_no_record         = !has_covering_record,
          has_record_but_empty  = has_covering_record & is.na(direct_zip9) & is.na(direct_zip5),
          has_neither            = has_no_record | has_record_but_empty
        )
    }

Append to tests/testthat/test-115-validation-curve.R a new test_that() block for the open-ended-
interval fixture from this task's <behavior> section, calling .test_env$classify_encounter_zip()
(the same sys.source()/.test_env pattern Plan 02 established -- classify_encounter_zip() is now
also captured by that same sys.source() call since it is defined in SECTION 1B before the probe
gate).

Now append to R/115_zip_stability_counts.R (Section 10, with a Part B header comment block marking
the universe as "encounters" per Pitfall 1 -- distinct from Part A's address-history universe).

139-05-PATCH FIX-03a -- define the cohort and restrict the ENCOUNTER pull to it (see interfaces
block above for the exact get_hl_patient_ids() pattern). Pull ENCOUNTER using the cohort-restricted
pattern given in this plan's interfaces block.

Call classify_encounter_zip() against the real script-level encounters and addr_coal:

    encounter_zip <- classify_encounter_zip(encounters, addr_coal)

message() a headline count: n encounters total, n has_direct_zip9, n has_direct_zip5_only,
n has_no_record, n has_record_but_empty, n has_neither -- both raw counts and percent of total.
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('classify_encounter_zip', lines)), any(grepl('get_pcornet_table', lines)), any(grepl('COHORT_IDS', lines)), any(grepl('get_hl_patient_ids', lines)), any(grepl('has_direct_zip9', lines)), any(grepl('has_direct_zip5_only', lines)), any(grepl('has_no_record', lines)), any(grepl('has_record_but_empty', lines)), any(grepl('semi_join', lines)), any(grepl('period_end_eff', lines))); library(testthat); res <- test_file('tests/testthat/test-115-validation-curve.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0, sum(df$nb) >= 5); cat('structural + testthat checks OK (', sum(df$nb), ' tests)
')"</automated>
  </verify>
  <done>classify_encounter_zip() exists in SECTION 1B, uses period_end_eff (not period_end_dt) for interval matching, defaults to a semi_join pre-filter, and splits has_neither into has_no_record/has_record_but_empty. encounter_zip is built from the cohort-restricted encounters (COHORT_IDS via get_hl_patient_ids(), cohort N logged) and addr_coal. tests/testthat/test-115-validation-curve.R's open-ended-interval fixture passes, proving an open-ended-covered encounter classifies has_direct_zip9, not has_neither.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: S1-S4 ordered and unordered scenario assignment with direction split</name>
  <files>R/115_zip_stability_counts.R</files>
  <behavior>
    Per-encounter scenario eligibility (independent, unordered -- computed BEFORE the S1-then-S2-
    then-S3 ordering is applied):
    - S1 eligible backward: has_direct_zip5_only AND a zip9_seq spell exists for the same patient
      with period_start_dt less-or-equal ADMIT_DATE whose ZIP5 (first 5 chars of zip9_norm)
      equals direct_zip5.
    - S1 eligible forward: same condition but period_start_dt greater than ADMIT_DATE.
    - S1 eligible either: backward OR forward.
    - S2 eligible backward: has_neither AND a zip5_seq spell exists for the same patient with
      period_start_dt less-or-equal ADMIT_DATE. zip5_seq (not zip9_seq) is used because S2 covers
      BOTH the "take a ZIP9 from another encounter" path and the "ZIP5 centroid" path -- any
      backward zip5_seq spell satisfies at least the centroid path; if that spell also has a
      non-NA zip9_norm it satisfies the take-ZIP9 path too. Add a resolution_path column
      ("take_zip9" if the nearest backward zip5_seq spell also has zip9_norm non-NA, else
      "centroid_only") for transparency in the eventual B_scenario_counts sheet -- computed for
      reporting, not used to gate S2 eligibility itself.
    - S2 eligible forward: same condition but period_start_dt greater than ADMIT_DATE, using
      zip5_seq.
    - S2 eligible either: backward OR forward.
    - S3 eligible (backward only -- S3's own definition is inherently backward, "does not match
      the PRIOR ZIP9's ZIP5"): has_direct_zip5_only AND NOT S1-eligible-backward. (S1-backward and
      S3 are complements within the has_direct_zip5_only set by construction -- a ZIP5-only
      encounter either matches some earlier ZIP9's ZIP5, S1, or it does not, S3.)
    - S4 (complete-case comparator, reported separately, not part of the ordered assignment): the
      complement of has_direct_zip9, i.e. every encounter that is NOT already-has-ZIP9. Report as
      "excluded under S4" = count of !has_direct_zip9, both encounter and patient level.

    Ordered assignment (S1 then S2 then S3), one column scenario_assigned per encounter:
    - "already_has_zip9" if has_direct_zip9
    - else "S1" if S1-eligible-either
    - else "S2" if S2-eligible-either
    - else "S3" if S3-eligible
    - else "unresolvable" (has_neither with no backward or forward ZIP5/ZIP9 anywhere for the
      patient -- this residual feeds Part C's completeness waterfall in Plan 04)

    139-05-PATCH FIX-04d: S3-eligible (unordered) is the complement of S1-eligible-backward, as
    defined above -- this does not change. What changes is that an encounter eligible ONLY via
    S1-forward (not S1-backward) is assigned "S1" by the ordered rule (since S1-eligible-either is
    tested first) while STILL being S3-eligible (unordered) by the complement-of-S1-backward
    definition. The ordered "S3" column therefore undercounts true S3-eligibility relative to the
    unordered "S3-eligible" column. Both must be reported; this must be called out explicitly in
    console output/comments here, and stated in the KEY sheet (Plan 04) so the ordered column is
    not misread as a complete S3 count.
  </behavior>
  <action>
Append to R/115_zip_stability_counts.R (Section 11, immediately after Task 1's block).

Forward-lookup logic is new, local code this plan adds -- get_zip9_at_date() only looks backward,
so the forward variant is implemented here directly against zip9_seq/zip5_seq, not by changing
utils_address.R. Build per-encounter existence flags via a join against zip9_seq/zip5_seq
filtered by direction relative to ADMIT_DATE (any spell on the correct side of ADMIT_DATE counts
for existence -- B-01/B-04 ask whether ANY such spell exists, not specifically the nearest one).

Build s1_elig and s2_elig (each with backward/forward/either logical columns, plus s2_elig's
resolution_path) and s3_elig (logical, backward-only) as one-row-per-encounter tibbles keyed on
(ID, ENCOUNTERID, ADMIT_DATE), then left_join them onto encounter_zip.

Apply the ordered assignment via case_when() exactly as specified in this task's behavior block,
producing scenario_assigned as a factor with levels c("already_has_zip9", "S1", "S2", "S3",
"unresolvable").

139-05-PATCH FIX-04d: immediately after computing scenario_assigned, add a code comment AND a
console message stating the ordered/unordered distinction explicitly:

    message(glue(
      "NOTE (139-05-PATCH FIX-04d): the ordered 'S3' count below is NOT the complete S3-eligible ",
      "population -- an encounter eligible only via S1-forward is assigned 'S1' by the ordered ",
      "rule (S1 tested first) while remaining S3-eligible under S3's own complement-of-S1-backward ",
      "definition. See the unordered 'S3-eligible' count for the complete figure. Both are reported ",
      "on B_scenario_counts (Plan 04) -- do not read the ordered column alone as S3's true count."
    ))

Build two summary tables:

1. Ordered assignment counts (B-03), at both encounter and patient level, denominator named in
   the row/column label:
   - Encounter level: count(scenario_assigned) and pct out of all encounters.
   - Patient level: for each patient, does ANY of their encounters fall into each
     scenario_assigned value? (a patient can appear under multiple scenarios if different
     encounters resolve differently -- report patient-level presence, not a forced single
     category per patient) -- n_distinct(ID) per scenario divided by n_distinct(all patients with
     any encounter), as pct.

2. Unordered eligible-for counts (B-03 overlap visibility) plus direction split (B-04), at both
   encounter and patient level: for S1 and S2, three rows/columns each (backward-only,
   forward-only, either) reporting n and pct at encounter level and at patient level; for S3, one
   row/column (backward only, no direction split, per S3's own definition) -- this is the
   "S3-eligible" figure FIX-04d requires alongside the ordered "S3" count.

message() headline stats: n and pct already_has_zip9, S1, S2, S3, unresolvable (encounter level);
n and pct S3-eligible specifically flagged with the message "S3 resolution still pending as of
08/04 notes -- count reported, not presenting either resolution as decided" (B-01).

Do not write any xlsx in this task -- that is Plan 04.
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('scenario_assigned', lines)), any(grepl('resolution_path', lines)), any(grepl('backward', lines)), any(grepl('forward', lines)), any(grepl('unresolvable', lines)), any(grepl('FIX-04d', lines)), any(grepl('S3-eligible', lines))); cat('structural checks OK\n')"</automated>
  </verify>
  <done>scenario_assigned classifies every encounter into exactly one of already_has_zip9/S1/S2/S3/unresolvable via the ordered S1-then-S2-then-S3 rule; unordered eligible-for counts and the backward/forward/either direction split (S1, S2 only -- S3 stays backward-only) are reported at both encounter and patient level with denominators named; S3 is explicitly flagged as unresolved, not presented as decided; the ordered-S3-vs-unordered-S3-eligible distinction (FIX-04d) is called out in a console message and both counts are computed for Plan 04's sheet.</done>
</task>

</tasks>

<verification>
1. Rscript parse check on R/115_zip_stability_counts.R -- no error, file still parses after this plan's additions.
2. Rscript -e "library(testthat); test_file('tests/testthat/test-115-validation-curve.R')" -- all tests (including the new open-ended-interval fixture) pass, 0 failures.
3. grep for get_pcornet_table in R/115_zip_stability_counts.R -- present.
4. grep for COHORT_IDS / get_hl_patient_ids in R/115_zip_stability_counts.R -- present (139-05-PATCH FIX-03a cohort restriction).
5. grep for scenario_assigned in R/115_zip_stability_counts.R -- present.
6. grep for resolution_path in R/115_zip_stability_counts.R -- present (S2 take_zip9 vs centroid_only transparency).
7. grep for "S3 resolution still pending" in R/115_zip_stability_counts.R -- present (B-01 guard against presenting S3 as decided).
8. grep for "has_no_record" and "has_record_but_empty" in R/115_zip_stability_counts.R -- present (139-05-PATCH FIX-04e).
</verification>

<success_criteria>
- classify_encounter_zip() (SECTION 1B) uses period_end_eff for interval matching, not period_end_dt -- open-ended address records classify covered encounters as has_direct_zip9, not has_neither (139-05-PATCH FIX-02, verified by the appended test fixture).
- Every ENCOUNTER row (restricted to the study cohort, cohort N logged per FIX-03a) is pulled via DuckDB and classified as has_direct_zip9 / has_direct_zip5_only / has_no_record / has_record_but_empty using a locally-computed top-1 interval match with the coalesced ZIP5, not get_zip9_at_date()'s own ZIP5 output.
- The semi_join pre-filter is applied by default (139-05-PATCH FIX-04c).
- S1/S2/S3/S4 are counted both as an ordered, mutually-exclusive assignment (S1 then S2 then S3) and as unordered eligible-for counts showing overlap; the ordered-S3-vs-unordered-S3-eligible distinction is explicitly documented (139-05-PATCH FIX-04d).
- S1 and S2 are split backward-only/forward-only/either; S3 stays backward-only per its own definition.
- Every count is reported at encounter AND patient level with the denominator named.
- S3 is reported as an occurrence count only, explicitly flagged as unresolved -- no scenario resolution is presented as decided.
- No xlsx write yet; console output plus the growing test file are sufficient to independently verify this plan's correctness on a future HiPerGator run.
</success_criteria>

<output>
After completion, create .planning/phases/139-zip-stability-imputation-occurrence-counts/139-03-SUMMARY.md
</output>
