---
phase: 139-zip-stability-imputation-occurrence-counts
plan: 04
type: execute
wave: 4
depends_on:
  - 139-01
  - 139-02
  - 139-03
files_modified:
  - R/115_zip_stability_counts.R
  - tests/testthat/test-115-validation-curve.R
  - R/39_run_all_investigations.R
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements: [C-01, C-02]
must_haves:
  truths:
    - "Part C reports a stepwise waterfall (starting encounters with observed ZIP9, then incremental gain from S1, then S2, then S3, then residual), at both encounter and patient level (C-01)"
    - "C-02 is computed over the study cohort patient list (COHORT_IDS from 139-03), not over addr_coal's own patient set, and patients absent from addr_coal entirely count toward the numerator (139-05-PATCH FIX-03)"
    - "C-02's pre-filter comparison (against addr_raw, before study-period/unparseable-date filters) is computed and reported alongside the post-filter figure so filter-driven denominator drift is visible (139-05-PATCH FIX-03c)"
    - "The C-02 tolerance (+/-5) is documented in the KEY sheet as covering cohort-definition drift only, not methodological uncertainty (139-05-PATCH FIX-03d)"
    - "A materially higher-than-expected final residual produces a prominent, impossible-to-miss QC flag (console AND xlsx cell) rather than a silently-shipped wrong deliverable (C-02)"
    - "The full 8-sheet workbook (KEY, A_stability_patient, A_stability_summary, A_validation_curve, B_scenario_counts, B_direction_split, C_completeness, QC) is written to output/zip_stability_counts_YYYYMMDD.xlsx"
    - "A_stability_summary includes A-05's deciles (Plan 01's gap_days_deciles, the 10th-90th percentile gap-days cutpoints) alongside median/IQR and the fixed-bucket histogram -- not the histogram alone"
    - "R/115 is registered in R/39's investigation_scripts vector (as the final entry, no trailing comma) and R/88 has a new structural-checks section validating this phase's artifacts, including single-implementation checks for is_sentinel_zip5() and coalesce_zip5()"
    - "KEY sheet language is measured and neutral: it describes what was counted and under what definition, without characterizing whether the resulting completeness is adequate -- that is the team's call, not this script's"
    - "KEY sheet documents the F1 framing of A-06 (address-record-anchored, not a literal 'target encounter' reinterpretation) and the ordered-S3-vs-unordered-S3-eligible distinction (139-05-PATCH FIX-01, FIX-04d)"
  artifacts:
    - path: "R/115_zip_stability_counts.R"
      provides: "SECTION 1B addition: compute_c02(cohort_ids, addr_coal) as a pure, testable function (139-05-PATCH FIX-03b). Part C completeness waterfall + QC sheet + full 8-sheet styled xlsx assembly."
      contains: "compute_c02"
    - path: "tests/testthat/test-115-validation-curve.R"
      provides: "Appends the C-02 fixture (139-05-PATCH FIX-05): a cohort patient with zero rows in addr_coal counts toward n_patients_no_zip5_ever"
      contains: "C-02"
    - path: "R/39_run_all_investigations.R"
      provides: "R/115 registered in investigation_scripts vector"
      contains: "R/115_zip_stability_counts.R"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "New structural-checks section for Phase 139 artifacts, including single-implementation checks for is_sentinel_zip5()/coalesce_zip5() (139-05-PATCH revised verification #5/#6)"
      contains: "Phase 139"
    - path: "R/SCRIPT_INDEX.md"
      provides: "R/115 row in the Post-Renumber Investigations table"
      contains: "R/115_zip_stability_counts"
  key_links:
    - from: "R/115_zip_stability_counts.R"
      to: "output/zip_stability_counts_YYYYMMDD.xlsx"
      via: "openxlsx2::wb_workbook() (new workbook -- unlike R/114, R/115 is not appending to an existing file) then wb_save()"
      pattern: "wb_save"
    - from: "R/39_run_all_investigations.R"
      to: "R/115_zip_stability_counts.R"
      via: "investigation_scripts vector entry, run_script()"
      pattern: "R/115_zip_stability_counts.R"
    - from: "compute_c02() (SECTION 1B)"
      to: "COHORT_IDS (defined in 139-03 Task 1, still in scope as a script-level variable when Part C runs)"
      via: "compute_c02(COHORT_IDS, addr_coal) -- cohort_ids left_joined against addr_coal's per-patient zip5-ever summary, coalesce(FALSE) catches patients absent from addr_coal entirely"
      pattern: "compute_c02"
---

<objective>
Complete R/115_zip_stability_counts.R: add Part C's completeness waterfall with the C-02 cohort-scoped reconciliation check, a QC sheet consolidating every drop count logged across Plans 01-03, assemble the full 8-sheet styled xlsx deliverable, and register the script in R/39, R/88, and R/SCRIPT_INDEX.md.

Purpose: this is the final assembly plan. C-02's reconciliation is explicitly called out in CONTEXT.md as "the single most useful validation in the phase" -- it is free, and it checks whether the ZIP5-coalescing fix (done locally in Plan 01 via coalesce_zip5(), since no separate AMEND-01 phase exists) is actually reaching the raw ZIP5 values correctly. A materially higher-than-expected final residual means something upstream is wrong and must be investigated before the workbook ships to Erin/Amy -- this plan makes that finding impossible to miss (prominent console message + QC sheet flag), not a silent pass/fail.

This plan has been amended by 139-05-PATCH.md FIX-03 (BLOCKING): the original C-02 computation counted `all(is.na(zip5_coalesced))` over addr_coal's own patient set -- which is (a) blind to patients with ZERO rows in addr_coal (dropped by Plan 01's study-period/date filters, but exactly the "no usable ZIP5" population C-02 is counting), and (b) compared against the wrong denominator (the notes' 26 is a study-cohort count, not an address-history-wide count). This plan fixes both: compute_c02() takes the study cohort (COHORT_IDS, already defined in 139-03) as its population, left-joins the per-patient ZIP5-ever summary from addr_coal, and treats absence from addr_coal as "no usable ZIP5" (not silently 0). A pre-filter comparison (against addr_raw, via the SAME coalesce_zip5() function from Plan 01 -- not a second implementation) is reported alongside so filter-driven drift is visible (FIX-03c). Also applies FIX-04d's KEY-sheet documentation of the ordered-vs-unordered S3 distinction and FIX-01's corrected A-06 framing language.

Output: output/zip_stability_counts_YYYYMMDD.xlsx (8 sheets) on a HiPerGator run; R/115_zip_stability_counts.R complete; tests/testthat/test-115-validation-curve.R complete (6 fixtures); R/39, R/88, R/SCRIPT_INDEX.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-CONTEXT.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-01-SUMMARY.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-02-SUMMARY.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-03-SUMMARY.md

<interfaces>
add_styled_sheet() pattern (copy verbatim from R/106 lines 750-797 / R/114 lines 234-282 -- do NOT
source() R/106 or R/114, copy the function definition locally per this project's established
"copy, don't source" convention for this helper):

  add_styled_sheet <- function(wb, sheet_name, title_text, subtitle_text, data_tbl,
                                extra_tbl = NULL, extra_label = NULL)

R/115 writes a NEW workbook (wb_workbook(), not wb_load()) -- unlike R/114, which appends to
R/106's existing file, R/115 produces its own separate deliverable
(output/zip_stability_counts_YYYYMMDD.xlsx), so there is nothing to load/preserve.

UF brand colors, per the deliverable spec in 139-CONTEXT.md ("Charts use UF colors (#0021A5,
#FA4616)"): define UF_BLUE <- "#0021A5" and UF_ORANGE <- "#FA4616" locally in R/115 (note: this
is a DIFFERENT blue than utils_pptx.R's UF_BLUE of "#003087" -- 139-CONTEXT.md's explicit hex
values are locked and take precedence for this deliverable; do not reuse utils_pptx.R's constant
or source that file). Use UF_BLUE as the styled-sheet header fill color in place of R/106's
generic DARK_GRAY, and UF_ORANGE as an accent color for flagging the C-02 reconciliation
row/cell if it fails (see Task 1). This project has no precedent for embedding native Excel chart
objects in an xlsx (grep confirmed no wb_add_chart usage anywhere in R/) -- "charts use UF
colors" is implemented here as colored table styling/accent cells, consistent with every other
xlsx this codebase produces, not as embedded native charts.

gap_days_summary (built in Plan 01 Section 6, A-05): a named object holding median/p25/p75/min/max,
gap_days_deciles (quantile(gaps_days, seq(0.1, 0.9, 0.1))), and the fixed-bucket histogram of
days between ZIP9 changes. The A_stability_summary sheet's data table MUST include the deciles
from this object, not just the histogram -- A-05 explicitly requires "median, IQR, deciles" and a
fixed-bucket histogram alone does not satisfy that. Read Plan 01's SUMMARY for this object's exact
shape before building the sheet.

139-05-PATCH FIX-03b -- compute_c02() (define in SECTION 1B, immediately after Plan 03's
classify_encounter_zip()):

  compute_c02 <- function(cohort_ids, addr_coal) {
    zip5_ever <- addr_coal %>%
      group_by(ID) %>%
      summarise(has_any_zip5 = any(!is.na(zip5_coalesced)), .groups = "drop")

    c02_tbl <- tibble(ID = cohort_ids) %>%
      left_join(zip5_ever, by = "ID") %>%
      mutate(has_any_zip5 = coalesce(has_any_zip5, FALSE))   # absent from addr_coal => no ZIP5

    list(
      c02_tbl = c02_tbl,
      n_patients_no_zip5_ever = sum(!c02_tbl$has_any_zip5),
      n_cohort_absent_from_addr = sum(!(cohort_ids %in% addr_coal$ID))
    )
  }

COHORT_IDS (139-03 Task 1, script-level variable, still in scope when this plan's Part C code runs
in the same script execution): the study cohort list from get_hl_patient_ids(). Do not redefine it
here -- reuse the same object so Part B and Part C count over the identical population.

addr_raw (139-01 Task 2, unfiltered, retained as a script-level variable): used for FIX-03c's
pre-filter comparison. Reuse coalesce_zip5() (SECTION 1B, 139-01) against addr_raw directly -- do
NOT re-derive ZIP5-coalescing logic a second time; the patch's revised verification requires
exactly one coalescing implementation in the codebase.

R/39 investigation_scripts vector current last entry (Phase 137): "R/114_zip9_temporal_lookup.R"
(no trailing comma, closing paren follows). R/115 was NOT added by Phase 137 or 138 -- confirm
current state by reading R/39 before editing (state may have shifted if other work landed since
this plan was written).

R/88 last section: 15ac (Phase 138). New section for this phase is 15ad.

R/SCRIPT_INDEX.md "Post-Renumber Investigations (100+)" table currently ends at R/114 (15
entries, R/100-R/114). Adding R/115 makes 16. Also update the summary "Script Count" section at
the bottom of the file (currently states 15 post-renumber investigations, 101 total).

expected_xlsx list in R/39 (Section on "Pre-render check"): R/106's zip_change_frequency.xlsx is
NOT in this list despite being registered in investigation_scripts -- this is established
precedent (a dated/optional-output investigation script does not have to appear in the fixed
expected_xlsx existence check). Follow the SAME precedent for R/115: register in
investigation_scripts, do NOT add zip_stability_counts_YYYYMMDD.xlsx to expected_xlsx (its
filename is date-stamped and would break a fixed-name file.exists() check every day).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Part C completeness waterfall + compute_c02() + C-02 fixture + pre-filter comparison</name>
  <files>R/115_zip_stability_counts.R, tests/testthat/test-115-validation-curve.R</files>
  <behavior>
    C-01 waterfall, both encounter and patient level, stepwise in this fixed order: starting
    count (encounters/patients with has_direct_zip9 i.e. already_has_zip9), + incremental gain
    from S1, + incremental gain from S2, + incremental gain from S3, then a final residual row
    (unresolvable). Each row's count is the CUMULATIVE total after that step, so the final row
    equals total encounters/patients (there is no separate "total" row needed -- the waterfall's
    own bottom row already sums to the full universe by construction).

    139-05-PATCH FIX-03b: C-02 is computed over the STUDY COHORT (COHORT_IDS from 139-03), not
    over addr_coal's own patient set. compute_c02(COHORT_IDS, addr_coal) left-joins the cohort
    onto addr_coal's per-patient "has any usable ZIP5" summary; a cohort patient absent from
    addr_coal entirely (e.g. dropped by Plan 01's study-period or unparseable-date filters, or
    never in LDS_ADDRESS_HISTORY at all) is coalesced to has_any_zip5 = FALSE -- counting toward
    the "no usable ZIP5 ever" numerator rather than being invisible to group_by().

    tests/testthat/test-115-validation-curve.R C-02 fixture (139-05-PATCH FIX-05): a cohort_ids
    vector containing one patient ID that does NOT appear anywhere in a synthetic addr_coal
    tibble. Assert compute_c02()$n_patients_no_zip5_ever includes this patient (i.e. is >= 1 even
    though addr_coal has zero rows for that ID). This is the assertion that catches the original
    "invisible to group_by()" defect.
  </behavior>
  <action>
Insert into R/115_zip_stability_counts.R's SECTION 1B (edit in place, immediately after Plan 03's
classify_encounter_zip()): compute_c02() exactly as given in this plan's interfaces block.

Append to tests/testthat/test-115-validation-curve.R the C-02 test_that() block from this task's
<behavior> section, calling .test_env$compute_c02() (same sys.source()/.test_env pattern). This
completes the file's 6th and final fixture.

Now append to R/115_zip_stability_counts.R (Section 12, Part C header comment block).

Encounter-level waterfall, built directly from Plan 03's scenario_assigned column (already an
ordered, mutually-exclusive S1-then-S2-then-S3 assignment, so the waterfall is a straightforward
cumulative count over its factor levels in order):

    waterfall_encounter <- encounter_zip %>%
      count(scenario_assigned) %>%
      mutate(scenario_assigned = factor(scenario_assigned,
               levels = c("already_has_zip9", "S1", "S2", "S3", "unresolvable"))) %>%
      arrange(scenario_assigned) %>%
      mutate(cumulative_n = cumsum(n), cumulative_pct = round(100 * cumulative_n / sum(n), 1))

Patient-level waterfall: for each patient, the "furthest" resolved scenario across all their
encounters, in the SAME priority order (a patient counts as "already_has_zip9" if ANY encounter
already has one; else "S1" if any encounter resolves via S1; and so on down to "unresolvable"
only if EVERY encounter is unresolvable) -- this differs from Plan 03's per-encounter patient
"presence" reporting (which allowed a patient to appear under multiple scenarios); the waterfall
needs one mutually-exclusive bucket per patient so the cumulative sum reconciles to the full
patient count. Implement via group_by(ID) + slice_min on a numeric priority rank matching the
factor level order, or an equivalent min-priority selection.

C-02 reconciliation: call compute_c02() against the real script-level COHORT_IDS (from 139-03
Task 1, still in scope) and addr_coal:

    c02_result <- compute_c02(COHORT_IDS, addr_coal)
    n_patients_no_zip5_ever <- c02_result$n_patients_no_zip5_ever

    message(glue("[R/115] Cohort N: {length(COHORT_IDS)}"))
    message(glue("[R/115] Cohort patients entirely absent from addr_coal: {c02_result$n_cohort_absent_from_addr}"))

    C02_EXPECTED <- 26L
    C02_TOLERANCE <- 5L   # 139-05-PATCH FIX-03d: covers cohort-DEFINITION drift only (e.g. exact
                          # membership criteria differing slightly from the notes' own count) --
                          # NOT slack for denominator or methodological uncertainty, now that the
                          # denominator itself is cohort-scoped and correct. Document this
                          # narrowed claim in the KEY sheet (Task 2), not as general-purpose slack.
    c02_reconciled <- abs(n_patients_no_zip5_ever - C02_EXPECTED) <= C02_TOLERANCE

    if (!c02_reconciled) {
      message(glue(
        "\n*** C-02 RECONCILIATION FAILURE ***\n",
        "Expected ~{C02_EXPECTED} cohort patients with no usable ZIP5 anywhere in their address ",
        "history (per team meeting notes; cohort N = {length(COHORT_IDS)}). ",
        "Computed: {n_patients_no_zip5_ever}.\n",
        "Per 139-CONTEXT.md: 'If it does not [reconcile], stop and investigate before delivering.'\n",
        "A materially higher number means the ZIP5-coalescing fix is not reaching the raw ZIP5 ",
        "values correctly, and every downstream count in this deliverable may be wrong.\n",
        "*** DO NOT SHIP output/{basename(OUTPUT_XLSX)} TO ERIN/AMY UNTIL THIS IS RESOLVED ***\n"
      ))
    } else {
      message(glue("C-02 reconciliation OK: {n_patients_no_zip5_ever} cohort patients with no usable ZIP5 ever (expected ~{C02_EXPECTED})"))
    }

139-05-PATCH FIX-03c -- pre-filter comparison. Compute the SAME "no usable ZIP5 ever" statistic
against addr_raw (unfiltered, from 139-01 Task 2), reusing coalesce_zip5() (NOT a second
coalescing implementation) so the study-period/unparseable-date filters' effect on the denominator
is visible:

    addr_prefilter <- coalesce_zip5(addr_raw)
    c02_prefilter_result <- compute_c02(COHORT_IDS, addr_prefilter)
    n_patients_no_zip5_ever_prefilter <- c02_prefilter_result$n_patients_no_zip5_ever

    message(glue(
      "[R/115] C-02 pre-filter comparison: {n_patients_no_zip5_ever_prefilter} (before study-period/",
      "unparseable-date filters) vs {n_patients_no_zip5_ever} (post-filter, the reconciled figure). ",
      "A large gap means the filters are removing cohort patients the notes' 26-patient control ",
      "total includes -- report both, do not let C02_TOLERANCE absorb a gap this size."
    ))

Build waterfall_patient the same cumulative-count shape as waterfall_encounter, using the
per-patient furthest-scenario bucket described above.
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('compute_c02', lines)), any(grepl('waterfall_encounter', lines)), any(grepl('waterfall_patient', lines)), any(grepl('C02_EXPECTED', lines)), any(grepl('n_patients_no_zip5_ever', lines)), any(grepl('n_patients_no_zip5_ever_prefilter', lines)), any(grepl('RECONCILIATION FAILURE', lines)), any(grepl('COHORT_IDS', lines))); library(testthat); res <- test_file('tests/testthat/test-115-validation-curve.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0, sum(df$nb) >= 6); cat('structural + testthat checks OK (', sum(df$nb), 'tests)\n')"</automated>
  </verify>
  <done>compute_c02() exists in SECTION 1B and is used for both the post-filter (cohort-scoped, addr_coal) and pre-filter (addr_raw via coalesce_zip5()) C-02 figures -- no second coalescing implementation. waterfall_encounter and waterfall_patient exist as cumulative stepwise tables (already_has_zip9 -> S1 -> S2 -> S3 -> unresolvable). The C-02 reconciliation produces a loud, unmissable console warning when it does not reconcile within tolerance against the cohort-scoped expectation. tests/testthat/test-115-validation-curve.R's 6th fixture (C-02) passes, proving a cohort patient absent from addr_coal counts toward the numerator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QC sheet data, full 8-sheet xlsx assembly (KEY-sheet FIX-01/FIX-04d language), R/39/R/88/SCRIPT_INDEX registration</name>
  <files>R/115_zip_stability_counts.R, R/39_run_all_investigations.R, R/88_smoke_test_comprehensive.R, R/SCRIPT_INDEX.md</files>
  <read_first>
    - R/39_run_all_investigations.R lines 176-205 (investigation_scripts vector -- confirm current last entry before editing)
    - R/88_smoke_test_comprehensive.R (locate the current LAST section -- confirm it is still 15ac, or find whatever is now last, before inserting 15ad)
    - R/SCRIPT_INDEX.md (Post-Renumber Investigations table and the Script Count summary near the bottom -- confirm current last row/counts before editing)
  </read_first>
  <action>
**QC sheet data (Section 13 of R/115):** consolidate every drop/exclusion count logged across
Plans 01-04 into one tibble with columns Metric, Value: n_unparseable_dates, n_out_of_range_dates
(study period), n_zip9_sentinel_nulled, n_zip5_sentinel_nulled, period_end_open count (139-05-PATCH
FIX-02), n_excluded_no_prior (A-06 hold-out), cohort N, n_cohort_absent_from_addr, C02_EXPECTED,
n_patients_no_zip5_ever, n_patients_no_zip5_ever_prefilter (139-05-PATCH FIX-03c), c02_reconciled
(as "PASS"/"FAIL -- SEE ABOVE"), and the has_block_group_crosswalk flag from Plan 02. This is the
single place a reviewer checks before trusting the rest of the workbook.

**KEY sheet data (Section 14, written FIRST as the leftmost tab per the deliverable spec):**
Definitions of every metric used across A/B/C, the universe distinction per Pitfall 1 (address
history for Part A sheets, encounters for Part B/C sheets -- state this explicitly per sheet),
denominators, exclusions applied, source file name (ADDR_FILENAME) and run date (Sys.Date()),
script version ("R/115_zip_stability_counts.R, Phase 139, amended by 139-05-PATCH.md"). Explicitly
document as Claude's-Discretion design notes:

(1) 139-05-PATCH FIX-01 language (supersedes any earlier draft): "A-06 is computed on
address-history records (addr_coal), using each record's period_start_dt as the lookup date. This
measures the same carry-forward rule get_zip9_at_date() applies in production. Per Pitfall 1, Part
A sheets are computed on address history, not encounters." Do NOT describe this as a
reinterpretation of the notes' "target encounter" phrase via Pitfall 1 -- that framing was the
reasoning error the patch corrected (see 139-02's plan text for the full explanation, and
139-CONTEXT.md's amended A-06 note).

(2) 139-05-PATCH FIX-04d language: "B_scenario_counts reports TWO different S3 quantities: the
ordered 'S3' column (assigned only when an encounter is not already_has_zip9, not S1-eligible, and
IS S3-eligible) undercounts relative to the unordered 'S3-eligible' column, because an encounter
eligible only via S1-forward is assigned 'S1' by the ordered rule while remaining S3-eligible. Use
'S3-eligible' for the complete count of how often S3 would fire if evaluated on its own."

(3) the C-02 tolerance of +/-5 patients around the cohort-scoped 26-patient control total, per
139-05-PATCH FIX-03d, documented as covering cohort-definition drift only -- not denominator or
methodological uncertainty (the denominator is now cohort-scoped and correct, so the tolerance's
job is narrower than it would have been against the un-patched denominator).

(4) the ZIP5-coalescing logic applied locally in R/115 via the single coalesce_zip5() function
(preferring the raw ADDRESS_ZIP5 column with derived-from-ZIP9 fallback) since no separate prior
phase shipped this as a shared utility.

Language throughout the KEY sheet and any narrative text must be measured and neutral -- describe
what was counted and under what definition, WITHOUT characterizing whether the resulting
completeness is adequate (that is the team's call, per the deliverable spec).

**xlsx assembly (Section 15):** copy add_styled_sheet() and UF_BLUE/UF_ORANGE color constants
locally (per this plan's interfaces block). When building the A_stability_summary sheet's data
table, include Plan 01's `gap_days_summary` object in full -- median, p25, p75, min, max,
`gap_days_deciles` (the 10th-90th percentile cutpoints), AND the fixed-bucket histogram. The
deciles are A-05's explicit requirement ("median, IQR, deciles") and must appear as their own
rows/columns in this sheet, not be dropped in favor of the histogram alone. Build the workbook with wb_workbook() (new file, not
wb_load()) and write, in this order: KEY, A_stability_patient, A_stability_summary,
A_validation_curve, B_scenario_counts, B_direction_split, C_completeness, QC. Use UF_BLUE for
header fills. If c02_reconciled is FALSE, add a UF_ORANGE-filled flag cell/row to the QC sheet's
C-02 row (do not just rely on console output -- the xlsx itself must carry the warning since that
is what gets forwarded to Erin/Amy). wb_save(wb, OUTPUT_XLSX). message() the final xlsx path.

**R/39 registration:** read the current investigation_scripts vector's last line, add
"R/115_zip_stability_counts.R" as the new final entry (no trailing comma on it; add a trailing
comma to what was previously the last entry), with a comment mirroring R/106/R/114's style:
`# ZIP stability + imputation-scenario occurrence counts (Phase 139, amended by 139-05-PATCH.md); probes LDS_ADDRESS_HISTORY + ENCOUNTER, read-only`.
Do NOT add zip_stability_counts_YYYYMMDD.xlsx to expected_xlsx (see interfaces block --
dated filename, follows R/106's own precedent of non-inclusion).

**R/88 Section 15ad:** insert after the current last section (confirm via read_first it is still
15ac; if not, insert after whichever is actually last), following the exact counter/check_XXX
pattern established in Section 15ac (see R/88 lines ~4862-4924 for the template: p138_pass/
p138_fail counters, check_138() helper, read_or_null()/code_only() guards). Write checks
including at minimum: R/115_zip_stability_counts.R exists; is_sentinel_zip5 defined in
utils_address.R AND exactly once (139-05-PATCH revised verification #5 -- e.g.
`sum(grepl("^is_sentinel_zip5 <- function", r_address_lines)) == 1`); get_zip9_at_date defined in
utils_address.R AND unchanged (grep for the function signature, not a diff -- a structural
existence check is sufficient here, this project's grep-based smoke tests do not diff function
bodies elsewhere either); coalesce_zip5 defined in R/115 AND exactly once (139-05-PATCH revised
verification #6); R/115 contains "zip5_coalesced" (ZIP5 coalescing present); R/115 contains
"scenario_assigned" (Part B present); R/115 contains "compute_c02" (C-02 reconciliation present,
cohort-scoped); R/115 contains "waterfall_encounter" and "waterfall_patient" (Part C present);
R/115 contains "period_end_eff" (139-05-PATCH FIX-02 present); tests/testthat/test-115-validation-
curve.R exists and contains at least 6 test_that( calls (139-05-PATCH FIX-05, all fixtures
present); R/115 registered in R/39's investigation_scripts vector. Add a footer message() line
following the p138/p137 summary-line convention, and add a corresponding SMOKE-139-01 line to the
validated-requirements footer block near the end of SECTION 16 (mirror the SMOKE-138-01 line's
format and placement).

**R/SCRIPT_INDEX.md:** add a row for R/115 in the Post-Renumber Investigations (100+) table
(after the R/114 row), summarizing the script per this project's existing row style (see R/106's
and R/114's rows for the level of detail expected). Update the "Script Count" summary near the
bottom of the file: post-renumber investigations count from 15 to 16, and the file's stated
"Total" script count incremented by 1 to match.
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('wb_workbook', lines)), any(grepl('KEY', lines)), any(grepl('QC', lines)), any(grepl('UF_BLUE', lines)), any(grepl('wb_save', lines)), any(grepl('gap_days_deciles', lines)), any(grepl('gap_days_summary', lines))); r39 <- readLines('R/39_run_all_investigations.R'); stopifnot(any(grepl('R/115_zip_stability_counts.R', r39))); r88 <- readLines('R/88_smoke_test_comprehensive.R'); stopifnot(any(grepl('15ad', r88)), any(grepl('SMOKE-139-01', r88))); idx <- readLines('R/SCRIPT_INDEX.md'); stopifnot(any(grepl('R/115_zip_stability_counts', idx))); addr_lines <- readLines('R/utils/utils_address.R'); stopifnot(sum(grepl('^is_sentinel_zip5 <- function', addr_lines)) == 1); stopifnot(sum(grepl('^coalesce_zip5 <- function', lines)) == 1); test_lines <- readLines('tests/testthat/test-115-validation-curve.R'); stopifnot(sum(grepl('test_that\\(', test_lines)) >= 6); cat('registration + single-implementation checks OK\n')"</automated>
  </verify>
  <done>R/115 writes the full 8-sheet styled xlsx (KEY first, then A/B/C sheets, then QC) with UF brand colors and a visible C-02 failure flag if reconciliation fails; the KEY sheet carries the FIX-01 (A-06 framing) and FIX-04d (ordered-vs-unordered S3) corrected language; the A_stability_summary sheet carries Plan 01's gap_days_summary through in full, including gap_days_deciles; R/115 is registered as the final entry in R/39's investigation_scripts vector; R/88 Section 15ad validates this phase's artifacts structurally with a SMOKE-139-01 footer line, including single-implementation checks for is_sentinel_zip5()/coalesce_zip5() and a >=6-fixture check on the test file; R/SCRIPT_INDEX.md lists R/115 and updated script counts.</done>
</task>

</tasks>

<verification>
1. Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R'))" -- no error, full script parses end to end.
2. Rscript -e "library(testthat); test_file('tests/testthat/test-115-validation-curve.R')" -- all 6 fixtures pass, 0 failures.
3. grep "wb_workbook" R/115_zip_stability_counts.R -- present (new workbook, not wb_load -- R/115 does not append to R/106's file).
4. grep "compute_c02" R/115_zip_stability_counts.R -- present (139-05-PATCH FIX-03b, cohort-scoped C-02).
5. grep "R/115_zip_stability_counts.R" R/39_run_all_investigations.R -- present, as the final vector entry (no trailing comma on it).
6. grep "15ad" R/88_smoke_test_comprehensive.R -- present.
7. grep "SMOKE-139-01" R/88_smoke_test_comprehensive.R -- present.
8. grep "R/115_zip_stability_counts" R/SCRIPT_INDEX.md -- present.
9. Rscript -e "stopifnot(sum(grepl('^is_sentinel_zip5 <- function', readLines('R/utils/utils_address.R'))) == 1, sum(grepl('^coalesce_zip5 <- function', readLines('R/115_zip_stability_counts.R'))) == 1)" -- exactly one definition of each (139-05-PATCH revised verification #5/#6).
10. Rscript R/88_smoke_test_comprehensive.R exits 0 with Section 15ad showing 0 FAIL (structural checks only -- no real LDS_ADDRESS_HISTORY/ENCOUNTER data required for these checks to pass).

Runtime verification (HiPerGator with LDS_ADDRESS_HISTORY and ENCOUNTER present, out of scope for
this planning session but required before the deliverable ships):
- Rscript R/115_zip_stability_counts.R completes without error and writes
  output/zip_stability_counts_YYYYMMDD.xlsx with all 8 sheets.
- The C-02 reconciliation message is reviewed BEFORE the workbook is sent to Erin/Amy; if it
  fails, the root cause is investigated per CONTEXT.md's explicit instruction, not overridden.
</verification>

<success_criteria>
- Part C's stepwise waterfall (encounter and patient level) is built directly from Plan 03's ordered scenario_assigned classification.
- C-02's reconciliation is computed via compute_c02() over the study cohort (not addr_coal's own patient set), with cohort-absent patients counting toward the numerator, a pre-filter comparison reported alongside, and produces an impossible-to-miss warning -- both in console and in the xlsx itself via a QC-sheet flag cell -- when it fails to reconcile against ~26 within the narrowed-claim tolerance.
- The full 8-sheet workbook is assembled with KEY as the leftmost tab, UF brand colors, the FIX-01/FIX-04d corrected KEY-sheet language, and neutral/measured language that describes definitions without judging adequacy.
- R/115 is registered in R/39 (investigation_scripts, not expected_xlsx, matching R/106's precedent), R/88 has a new structural-checks section (15ad) with a SMOKE-139-01 footer line and single-implementation checks for is_sentinel_zip5()/coalesce_zip5(), and R/SCRIPT_INDEX.md lists the new script with updated counts.
- tests/testthat/test-115-validation-curve.R contains all 6 fixtures (stable patient, mover, +4-only mover, single-record patient, open-ended interval, C-02) and passes with 0 failures.
- Rscript R/88_smoke_test_comprehensive.R exits 0 with Section 15ad showing 0 FAIL.
</success_criteria>

<output>
After completion, create .planning/phases/139-zip-stability-imputation-occurrence-counts/139-04-SUMMARY.md
</output>
