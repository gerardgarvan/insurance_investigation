---
phase: 139-zip-stability-imputation-occurrence-counts
plan: 02
type: execute
wave: 2
depends_on:
  - 139-01
files_modified:
  - R/115_zip_stability_counts.R
  - tests/testthat/test-115-validation-curve.R
autonomous: false
requirements: [A-06]
must_haves:
  truths:
    - "A-06 holds out a single address RECORD (not a whole spell) from addr_coal, so unchanged-address cases are retained in the test population and exact-match accuracy is capable of being nonzero (139-05-PATCH FIX-01, framing F1)"
    - "A-06's exact-match rate is verified nonzero on a synthetic stable-patient fixture via tests/testthat/test-115-validation-curve.R -- not asserted only by grepping the script for a column name (139-05-PATCH FIX-05)"
    - "pct_unchanged and n_same_day are reported alongside the gap-bin curve, and a '0 (same-day)' bin is separated from '0-30' so same-day version-correction records do not inflate the shortest bin's accuracy"
    - "Accuracy is reported at two tiers: exact ZIP9 match and same-ZIP5 match; a third block-group tier is attempted only if a Neighborhood Atlas ZIP9-to-block-group crosswalk file is found, and is otherwise clearly reported as unavailable rather than silently omitted"
    - "The validation universe is address-history records (Part A), not encounters -- consistent with Pitfall 1's universe rule -- and the KEY-sheet note documents this as computed on address-history records using each record's period start as the lookup date, NOT as a literal reinterpretation of the notes' 'target encounter' phrase"
    - "Records with no prior record to predict from (single-record patients) are excluded from the hold-out test population and this exclusion is counted and reported, not silently dropped"
  artifacts:
    - path: "R/115_zip_stability_counts.R"
      provides: "SECTION 1B additions: build_validation_cases(addr_coal) and aggregate_validation_curve(cases) as pure, testable functions (139-05-PATCH FIX-01, framing F1); Part A-06 (Section 8/9) calls them against the real addr_coal"
      contains: "build_validation_cases"
    - path: "tests/testthat/test-115-validation-curve.R"
      provides: "Synthetic-fixture behavioral tests for A-06 (139-05-PATCH FIX-05): stable patient (exact-match must be nonzero -- the test that catches FIX-01), mover, +4-only mover, single-record patient. Grows in later plans (139-03 appends the open-ended-interval fixture, 139-04 appends the C-02 fixture)."
      contains: "test_that"
  key_links:
    - from: "R/115_zip_stability_counts.R (SECTION 8/9, A-06)"
      to: "addr_coal (built in 139-01 Task 2)"
      via: "build_validation_cases() operates on addr_coal records directly, NOT on zip9_seq spells -- spell-collapsing removes exactly the unchanged-address cases the hold-out test needs to measure"
      pattern: "build_validation_cases"
    - from: "tests/testthat/test-115-validation-curve.R"
      to: "R/115_zip_stability_counts.R SECTION 1B"
      via: "sys.source() into a fresh environment, capturing function definitions before the probe gate (SECTION 2) stops execution"
      pattern: "sys.source"
---

<objective>
Add the carry-forward validation curve (A-06) to R/115_zip_stability_counts.R -- a hold-out test that measures how well the "most-recent-before" carry-forward rule (the SAME rule get_zip9_at_date() implements) actually predicts a patient's true ZIP9, binned by how many days elapsed since the last known ZIP9. This is the analysis CONTEXT.md flags as "not in the notes... but the analysis that actually answers the underlying question" -- it produces the empirical basis for the team's eventual time-window decision.

Purpose: Raw change counts (Plan 01) tell the team how often ZIP9 changes; they do not tell the team whether carry-forward is SAFE to rely on. This plan produces that answer, at three levels of the precision that actually matters for ADI (exact ZIP9, same ZIP5, same block group -- with block group only if a crosswalk file happens to be available).

139-05-PATCH FIX-01 (BLOCKING) rewrites this plan's approach entirely. The original design held out a `zip9_seq` SPELL and predicted it from the prior spell -- but consecutive `zip9_seq` rows differ in `zip9_norm` by definition (that is what spell-collapsing means), so `exact_match` was FALSE for every test case by construction and `pct_exact_zip9_match` read 0.0 in every bin. This plan uses framing F1 instead: hold out a single `addr_coal` RECORD (not a spell) and predict it from the most recent PRIOR record with a non-NA ZIP9. Patients whose address did not change between records then produce correct predictions -- which is where carry-forward's real accuracy lives. F1 keeps this plan at wave 2, depending only on 139-01 (no ENCOUNTER pull needed).

This plan is also the acceptance gate the patch requires (FIX-05): its failure mode is silent -- the script runs, the sheet populates, and every value in the headline column reads 0.0 with no error. `autonomous` is set to **false** and a human-verify checkpoint closes this plan.

Output: R/115_zip_stability_counts.R extended with build_validation_cases()/aggregate_validation_curve() (SECTION 1B) and a validation_curve tibble (gap-bin x accuracy-tier) with console headline stats; tests/testthat/test-115-validation-curve.R with 4 passing behavioral fixtures. Still no xlsx write (Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-CONTEXT.md
@.planning/phases/139-zip-stability-imputation-occurrence-counts/139-01-SUMMARY.md

<interfaces>
addr_coal contract (built in 139-01 Task 2, one row per raw address record -- NOT one row per
spell): columns ID, period_start_dt, period_end_dt (NA-preserving), period_end_open, period_end_eff
(sentineled), zip9_norm, zip5_coalesced, already study-period-filtered and sentinel-nulled.
139-05-PATCH FIX-01 (F1): A-06's hold-out test operates on addr_coal directly, NOT on zip9_seq --
zip9_seq's spell-collapsing (139-01 Task 3) removes every record where the address did not change,
which is exactly the population the hold-out test needs. Reuse addr_coal directly; do not reload or
re-parse LDS_ADDRESS_HISTORY in this plan.

SECTION 1B testable-functions pattern (139-01 Task 2 placed the section header and defined
coalesce_zip5(); this plan is the second to populate it): pure functions defined before SECTION 2's
probe gate so tests/testthat/test-115-validation-curve.R can capture them via sys.source() into a
fresh environment, tolerating the probe gate's stop()/quit() once source() execution reaches it.

KEY-sheet language (139-05-PATCH FIX-01 correction -- supersedes any earlier draft's framing):
A-06 is computed on address-history records (addr_coal), using each record's period_start_dt as the
lookup date. This measures the same carry-forward rule get_zip9_at_date() applies in production.
Per Pitfall 1, Part A sheets are computed on address history, not encounters -- A-06's own text
("take encounters that do have an observed ZIP9") is read here as describing the KIND of case being
tested (one with a known ZIP9 to compare against), not a mandate to anchor the test on the ENCOUNTER
table. Do NOT frame this as literally reinterpreting "target encounter" via Pitfall 1 -- Pitfall 1
governs universe consistency across sheets, not A-06's sampling frame, and invoking it that way was
the reasoning error that produced the spell-based design FIX-01 replaces. Record this exact
KEY-sheet language for Plan 04 to use verbatim.

Neighborhood Atlas crosswalk file: no such file exists anywhere in this repo (confirmed via grep
across R/ and data/reference/ during planning). Probe for one at
data/reference/neighborhood_atlas_block_group_crosswalk.csv (the naming convention this project
uses for optional enrichment files -- see data/reference/RUCA-codes-2020-zipcode.xlsx used by
R/100). If absent, the block-group tier is skipped with an explicit "not available" note -- do NOT
error, do NOT silently omit the tier from the table (add a row/column stating why it's blank).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record-anchored A-06 hold-out (F1) -- test fixtures + build_validation_cases()/aggregate_validation_curve()</name>
  <files>R/115_zip_stability_counts.R, tests/testthat/test-115-validation-curve.R</files>
  <behavior>
    139-05-PATCH FIX-01 (F1, address-record-anchored): hold out ONE address RECORD from addr_coal
    (not a spell from zip9_seq), predict it from the most recent PRIOR record with a non-NA ZIP9,
    and compare -- the same rule get_zip9_at_date() applies. Operating on addr_coal instead of
    zip9_seq is the fix itself: zip9_seq's spell-collapsing removes every record where the address
    did NOT change, which is exactly the population the hold-out test needs in order to measure
    anything other than 0%.

    tests/testthat/test-115-validation-curve.R fixtures (synthetic addr_coal-shaped tibbles: ID,
    period_start_dt, period_end_eff, zip9_norm at minimum):
    - Test 1 (stable patient): one patient, 5 address records, SAME zip9_norm, period_start_dt
      spread ~9 months apart over 3 years. Assert build_validation_cases() produces 4 test cases
      (5 records - 1 with no prior) and aggregate_validation_curve()'s Overall row has
      pct_exact_zip9_match == 100. THIS IS THE ASSERTION THAT CATCHES FIX-01 -- under the OLD
      spell-based design this reads 0, not 100, because spell-collapsing would have reduced these
      5 identical-ZIP9 records to a single spell with zero prior spells to test against.
    - Test 2 (mover): one patient, 4 records at ZIP9-A then 3 at ZIP9-B, evenly spaced. Assert
      pct_exact_zip9_match is strictly between 0 and 100 (not 0, not 100), and is higher in the
      shortest gap_bin than in the bin spanning the A-to-B transition.
    - Test 3 (+4-only mover): one patient, one record at "326111234", next record at "326115678"
      (same ZIP5, different +4). Assert exact_match is FALSE and zip5_match is TRUE for that case.
    - Test 4 (single-record patient): one patient, one address record. Assert this patient
      contributes zero rows to build_validation_cases()'s output.
  </behavior>
  <action>
Insert into R/115_zip_stability_counts.R's SECTION 1B (placed by 139-01 Task 2, before the SECTION 2
probe gate -- edit in place at that marker, immediately after coalesce_zip5(); do not append at end
of file):

    build_validation_cases <- function(addr_coal) {
      # 139-05-PATCH FIX-01 (F1): hold out one address RECORD, not a spell -- predict it from the
      # most recent PRIOR record with a non-NA ZIP9, the same rule get_zip9_at_date() applies.
      # Operates on addr_coal, NOT zip9_seq: spell-collapsing removes exactly the unchanged-address
      # cases carry-forward is supposed to get right, leaving only failures in the test population.
      holdout_base <- addr_coal %>%
        filter(!is.na(zip9_norm), !is.na(period_start_dt)) %>%
        arrange(ID, period_start_dt, desc(period_end_eff))

      validation_cases <- holdout_base %>%
        group_by(ID) %>%
        mutate(
          predicted_zip9  = lag(zip9_norm),         # prior RECORD -- may repeat the same address
          predicted_start = lag(period_start_dt),
          gap_days        = as.numeric(period_start_dt - predicted_start)
        ) %>%
        ungroup() %>%
        filter(!is.na(predicted_zip9))

      validation_cases %>%
        mutate(
          exact_match = predicted_zip9 == zip9_norm,
          zip5_match  = substr(predicted_zip9, 1, 5) == substr(zip9_norm, 1, 5),
          gap_bin = case_when(
            gap_days == 0   ~ "0 (same-day)",   # version corrections, not elapsed-time tests
            gap_days <= 30  ~ "0-30",           # redefined: 1 <= gap_days <= 30 (same-day split out)
            gap_days <= 90  ~ "31-90",
            gap_days <= 180 ~ "91-180",
            gap_days <= 365 ~ "181-365",
            TRUE            ~ "366+"
          )
        )
    }

    aggregate_validation_curve <- function(validation_cases) {
      bin_levels <- c("0 (same-day)", "0-30", "31-90", "91-180", "181-365", "366+")
      curve <- validation_cases %>%
        mutate(gap_bin = factor(gap_bin, levels = bin_levels)) %>%
        group_by(gap_bin, .drop = FALSE) %>%
        summarise(
          n_cases = n(),
          pct_exact_zip9_match = round(100 * mean(exact_match), 1),
          pct_same_zip5_match  = round(100 * mean(zip5_match), 1),
          .groups = "drop"
        ) %>%
        mutate(gap_bin = as.character(gap_bin))

      overall_row <- tibble(
        gap_bin = "Overall",
        n_cases = nrow(validation_cases),
        pct_exact_zip9_match = round(100 * mean(validation_cases$exact_match), 1),
        pct_same_zip5_match  = round(100 * mean(validation_cases$zip5_match), 1)
      )

      bind_rows(curve, overall_row)
    }

Create tests/testthat/test-115-validation-curve.R (new file). Header comment explaining the
sys.source()-into-fresh-environment pattern and that this file grows across Plans 02/03/04 -- each
plan appends its own test_that() block once its function exists in SECTION 1B, so the file always
reflects only fixtures whose supporting function is already implemented:

    library(testthat)
    library(dplyr)
    library(tibble)

    source("R/00_config.R")
    .test_env <- new.env()
    tryCatch(
      sys.source("R/115_zip_stability_counts.R", envir = .test_env),
      error = function(e) invisible(NULL)   # expected: SECTION 2's probe gate stop()s once reached --
                                             # everything in SECTION 1B is already captured by then.
    )

Then implement the 4 test_that() blocks from this task's <behavior> section, calling
.test_env$build_validation_cases() and .test_env$aggregate_validation_curve() (use the .test_env$
prefix consistently, matching the sourcing pattern above).
  </action>
  <verify>
    <automated>Rscript -e "library(testthat); res <- test_file('tests/testthat/test-115-validation-curve.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0, sum(df$nb) >= 4); cat('testthat: 0 failures across', sum(df$nb), 'tests\n')"</automated>
  </verify>
  <done>build_validation_cases() and aggregate_validation_curve() exist in SECTION 1B as pure functions operating on addr_coal-shaped input; tests/testthat/test-115-validation-curve.R exists with 4 passing test_that() blocks (stable/mover/+4-only/single-record), and the stable-patient test specifically asserts pct_exact_zip9_match == 100 (not 0) -- the assertion that would have failed under the pre-patch spell-based design.</done>
</task>

<task type="auto">
  <name>Task 2: Wire A-06 into Section 8/9 -- block-group probe, pct_unchanged/n_same_day reporting, console summary</name>
  <files>R/115_zip_stability_counts.R</files>
  <action>
Append to R/115_zip_stability_counts.R (Section 8, after 139-01 Task 3's block; this is separate
from the SECTION 1B function definitions Task 1 added near the top of the file).

Call the SECTION 1B functions against the real script-level addr_coal:

    validation_cases <- build_validation_cases(addr_coal)

    n_excluded_no_prior <- addr_coal %>%
      filter(!is.na(zip9_norm), !is.na(period_start_dt)) %>%
      pull(ID) %>% n_distinct() - n_distinct(validation_cases$ID)
    message(glue("A-06: {n_excluded_no_prior} patients excluded from hold-out test (single usable record, no prior to predict from)"))
    message(glue("A-06: {nrow(validation_cases)} hold-out test cases across {n_distinct(validation_cases$ID)} patients"))

    # 139-05-PATCH FIX-01 reporting additions:
    pct_unchanged <- round(100 * mean(validation_cases$exact_match), 1)
    n_same_day    <- sum(validation_cases$gap_days == 0, na.rm = TRUE)
    message(glue("A-06: {pct_unchanged}% of test cases were unchanged-address (base rate the curve measures against)"))
    message(glue("A-06: {n_same_day} same-day record pairs (gap_days == 0) -- reported in their own bin, not folded into 0-30"))

Block-group tier probe (do this once, before calling aggregate_validation_curve()):

    NEIGHBORHOOD_ATLAS_PATH <- file.path("data", "reference", "neighborhood_atlas_block_group_crosswalk.csv")
    has_block_group_crosswalk <- file.exists(NEIGHBORHOOD_ATLAS_PATH)
    message(glue("A-06: Neighborhood Atlas block-group crosswalk {if (has_block_group_crosswalk) 'found' else 'NOT found'} at {NEIGHBORHOOD_ATLAS_PATH}"))

    if (has_block_group_crosswalk) {
      # Load crosswalk (ZIP9 -> block_group_id, one row per ZIP9), join predicted and actual ZIP9
      # to their block group, add a block_group_match column to validation_cases the same way
      # exact_match/zip5_match were added. Column name expectations are unknown ahead of time --
      # inspect the file's header row first and adapt the join key name accordingly; if the
      # expected ZIP9-keyed join produces near-zero matches (data problem, not a code problem),
      # log a warning and fall back to reporting the tier as "found but unusable" rather than
      # publishing a misleading near-zero accuracy number.
    }

SECTION 9: aggregate and console summary.

    validation_curve <- aggregate_validation_curve(validation_cases)

If has_block_group_crosswalk is TRUE and the join succeeded, add pct_block_group_match the same
way (per-bin mean of block_group_match, plus the Overall row); if FALSE, add the column filled
with NA_real_ and rely on the sheet subtitle (Plan 04) to explain why.

    message("\n=== A-06: Carry-Forward Validation Curve (Address-History Universe, Record-Anchored) ===")
    print(validation_curve)
    message(glue("Block-group tier available: {has_block_group_crosswalk}"))
    message(glue("Base rate (unchanged-address test cases): {pct_unchanged}%  |  Same-day pairs: {n_same_day}"))
    message("==========================================================================\n")

Do not write any xlsx in this task -- that is Plan 04.
  </action>
  <verify>
    <automated>Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R')); lines <- readLines('R/115_zip_stability_counts.R'); stopifnot(any(grepl('validation_curve', lines)), any(grepl('pct_unchanged', lines)), any(grepl('n_same_day', lines)), any(grepl('same-day', lines)), any(grepl('n_excluded_no_prior', lines)), any(grepl('neighborhood_atlas_block_group_crosswalk', lines))); cat('structural checks OK\n')"</automated>
  </verify>
  <done>validation_curve exists with the "0 (same-day)" bin plus 0-30/31-90/91-180/181-365/366+/Overall rows, exact-ZIP9 and same-ZIP5 match percentages computed from Task 1's corrected functions, pct_unchanged/n_same_day reported, and a block-group column present (populated if a crosswalk file was found, NA with a documented reason otherwise). Console prints the full table and the block-group-tier availability flag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-VERIFY -- confirm the A-06 acceptance gate actually catches FIX-01's silent-zero failure mode</name>
  <action>
    This is a HUMAN-VERIFY checkpoint, not an autonomous code task -- 139-05-PATCH.md's own summary
    sets autonomous: false for this plan because A-06's original defect was silent (the script ran,
    the sheet populated, every value read 0.0, no error was raised). The executor's action here is
    to run the automated check below, confirm its output, and present the how-to-verify steps to the
    user for final confirmation rather than self-approving.
  </action>
  <what-built>The A-06 carry-forward validation curve, rebuilt per 139-05-PATCH FIX-01 (F1) to hold out address-history RECORDS instead of spells, with tests/testthat/test-115-validation-curve.R as the automated acceptance gate. This is the fix for a defect that would otherwise ship silently: the pre-patch design produced a validation curve reading 0.0% across every bin with no error raised.</what-built>
  <how-to-verify>
    1. Run: Rscript -e "library(testthat); test_file('tests/testthat/test-115-validation-curve.R')" -- confirm 0 failures across all 4 tests.
    2. Open tests/testthat/test-115-validation-curve.R and confirm the stable-patient test asserts pct_exact_zip9_match == 100 (or similarly asserts it is NOT 0). If this assertion is missing or weakened, the FIX-01 defect is not actually caught -- do not approve.
    3. Once this script is run on HiPerGator (out of scope for this planning session), review the console-printed validation_curve table's Overall row: pct_exact_zip9_match should read a plausible non-zero percentage, not 0.0 across every bin.
  </how-to-verify>
  <verify>
    <automated>Rscript -e "library(testthat); res <- test_file('tests/testthat/test-115-validation-curve.R', reporter = 'summary'); df <- as.data.frame(res); stopifnot(sum(df$failed) == 0); cat('checkpoint automated proxy: 0 failures
')"</automated>
  </verify>
  <done>The test file passes with 0 failures, the stable-patient assertion is confirmed to assert a nonzero exact-match rate (not merely a structural grep), and the user has typed "approved" or described issues.</done>
  <resume-signal>Type "approved" once the test file passes and the stable-patient assertion is confirmed non-zero, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Rscript -e "invisible(parse(file = 'R/115_zip_stability_counts.R'))" -- no error, file still parses after this plan's additions.
2. Rscript -e "library(testthat); test_file('tests/testthat/test-115-validation-curve.R')" -- all 4 tests pass, 0 failures.
3. grep "build_validation_cases" R/115_zip_stability_counts.R -- present.
4. grep "validation_curve" R/115_zip_stability_counts.R -- present.
5. grep "neighborhood_atlas_block_group_crosswalk" R/115_zip_stability_counts.R -- present (probe gate, not a hard dependency).
6. grep "n_excluded_no_prior" R/115_zip_stability_counts.R -- present (excluded patients counted, not silently dropped).
</verification>

<success_criteria>
- Hold-out carry-forward validation runs on addr_coal RECORDS (address-history universe, per Pitfall 1, F1 framing per 139-05-PATCH FIX-01), with single-record-patient exclusions counted and logged rather than silently dropped.
- Accuracy is reported at exact-ZIP9 and same-ZIP5 tiers, binned into 6 gap-day buckets (including the new "0 (same-day)" bin) plus an Overall row, with pct_unchanged/n_same_day reported alongside.
- Block-group tier is attempted only if a crosswalk file is found; degrades gracefully (documented NA, not an error or silent omission) when absent -- which is the current state of this repo.
- tests/testthat/test-115-validation-curve.R passes with 0 failures, and specifically proves the stable-patient exact-match rate is nonzero -- the assertion that would have failed under the pre-patch design.
- No xlsx write yet; console output plus the test file are sufficient to independently verify this plan's correctness on a future HiPerGator run.
</success_criteria>

<output>
After completion, create .planning/phases/139-zip-stability-imputation-occurrence-counts/139-02-SUMMARY.md
</output>
