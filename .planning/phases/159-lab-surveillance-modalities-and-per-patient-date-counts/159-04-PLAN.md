---
phase: 159-lab-surveillance-modalities-and-per-patient-date-counts
plan: 04
type: execute
wave: 4
depends_on: [159-01, 159-02, 159-03]
files_modified:
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: false
requirements: [LAB-05, LAB-07]

must_haves:
  truths:
    - "The R/88 Phase 158 section is extended in R/88's own PASS/FAIL style (no bare stop/stopifnot) with Phase 159 checks and no literal row counts"
    - "SCRIPT_INDEX lists the new R/147 outputs and LAB-01..LAB-07"
    - "On HiPerGator: 149 test expectations pass, R/88 passes, R/147 completes, and the team reviews A, A2 and the near-miss table before the release workbook circulates"
  artifacts:
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Phase 159 checks"
    - path: "R/SCRIPT_INDEX.md"
      provides: "Updated R/147 row"
---

<objective>
Extend smoke-test coverage, update the index, and run the phase gate on HiPerGator. R/39 needs no change; R/147 is already registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-lab-surveillance-modalities-and-per-patient-date-counts/159-CONTEXT.md
@.planning/phases/159-lab-surveillance-modalities-and-per-patient-date-counts/159-03-SUMMARY.md
@.planning/phases/158-surveillance-modality-frequency/158-04-SUMMARY.md
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: R/88 and SCRIPT_INDEX</name>

  <read_first>
    .planning/phases/158-surveillance-modality-frequency/158-04-SUMMARY.md — the actual R/88 file name and the Phase 158 section id and check helper used
    The R/88 smoke test file — the Phase 158 section
    R/SCRIPT_INDEX.md — the R/147 row
  </read_first>

  <files>R/88_smoke_test_comprehensive.R (or the R/88 file named in 158-04-SUMMARY), R/SCRIPT_INDEX.md</files>

  <action>
**R/88:** immediately after the Phase 158 section, add a Phase 159 section using the same check-recording helper and footer convention. Checks:
1. `excel_sheets()` of the codeset includes `Lab_Analytes` and `Modalities`
2. `load_lab_analytes(codeset = load_surveillance_codeset())` returns > 0 rows without error
3. `load_modality_lookup(codeset = ...)` returns without error (this checks every modality has a prefix)
4. Each of BMP, CMP, LIPID, LFT and KIDNEY has ≥1 `exact` row, exactly one `analyte_all_same_day` row and exactly one `analyte_min_same_day` row
5. The KIDNEY `analyte_min_same_day` row does not list CREATININE (D-22)
6. CPT 80053 appears under BMP, CMP, LFT and KIDNEY (D-01 nesting)
7. Functions exist: `load_lab_analytes`, `load_modality_lookup`, `map_analyte_hits`, `build_analyte_events`, `build_analyte_presence`, `build_patient_modality_dates`
8. R/147 source contains `E_patient_modality_dates`, `SC-6`, and `surveillance_patient_modality_dates_`
9. `tests/testthat/test-159-codeset-loader.R` and `test-159-analyte-rules.R` exist

Use no literal row counts. Footer id `SMOKE-159-01` (or the file's equivalent convention).

**R/SCRIPT_INDEX.md:** in the R/147 row:
- add outputs `surveillance_patient_modality_dates_<date>.rds` / `.csv`
- note sheets A2 and E (INTERNAL only)
- add requirements `LAB-01..LAB-07`
- add inputs: sheets `Lab_Analytes` and `Modalities`

If utils modules are indexed, update the `utils_surveillance.R` row with the Phase 159 functions.
  </action>

  <verify>
    <automated>Rscript -e "f <- list.files('R', pattern = '^88_.*[.]R$', full.names = TRUE)[1]; b <- readLines(f); stopifnot(any(grepl('load_lab_analytes', b)), any(grepl('SMOKE-159|Phase 159', b))); i <- readLines('R/SCRIPT_INDEX.md'); stopifnot(any(grepl('LAB-01', i)), any(grepl('surveillance_patient_modality_dates', i))); message('OK')"</automated>
  </verify>

  <done>R/88 has a Phase 159 section in house style; SCRIPT_INDEX updated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HiPerGator run and review</name>

  <what-built>Codeset with lab modalities, tested helpers, R/147 wiring, smoke-test coverage.</what-built>

  <how-to-verify>
    On HiPerGator, from the project root:
    1. `Rscript -e "testthat::test_dir('tests/testthat', filter = '15[89]', stop_on_failure = TRUE)"` — expect 149 passing expectations.
    2. Run R/88; the Phase 158 and 159 sections are all PASS and no other section regressed.
    3. `Rscript R/147_surveillance_modality_frequency.R 2>&1 | tee 159_run.log`. Every stopifnot must pass. Section 4B is the largest query; note its row counts and run time in the log.
    4. Open `surveillance_modality_frequency_INTERNAL_<date>.xlsx` (unsuppressed; do not copy off HiPerGator):
       - **A2_analyte_presence:**
         - Which analyte codes are actually used.
         - Rows with a `review_note`: if 2888-6 (urine protein) is common, it is probably urinalysis. Consider moving it to `Lab_Analytes_Excluded`.
         - Glucose rows first.
         - Any `n_results_other_type`.
       - **A_code_presence:** new panel rows (SC109+). Expect sparse panel LOINCs. Panel CPT presence depends on billing.
       - **QC near-miss block:** how many ID × dates fall just below each threshold (e.g. BMP 7 of 8). Adjust `min_analyte_count`, or a panel's analyte list, in the codeset if the team decides.
       - **E_patient_modality_dates:** one row per denominator patient, no blanks. BMP ≥ CMP and KIDNEY ≥ BMP per row.
    5. Codeset corrections are edits to `data/reference/surveillance_codeset.xlsx`, followed by a re-run. Share the release workbook only after review. The per-patient `.rds`/`.csv` stay on HiPerGator.
  </how-to-verify>

  <resume-signal>
    Type "approved — A/A2 reviewed, release workbook cleared" or "Issues: [list]".
  </resume-signal>
</task>

</tasks>

<verification>
Registration checks above; runtime verification is Task 2.
</verification>

<success_criteria>
- R/88 Phase 159 section PASS on HiPerGator; SCRIPT_INDEX updated
- 149 test expectations pass on HiPerGator
- R/147 completes; INTERNAL/release workbooks and per-patient files written
- A, A2 and near-miss reviewed before release
</success_criteria>

<output>
After completion, create `.planning/phases/159-lab-surveillance-modalities-and-per-patient-date-counts/159-04-SUMMARY.md`
</output>
