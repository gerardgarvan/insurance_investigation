---
phase: 77-cancer-classification-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - data/reference/all_codes_resolved_next_tables_v2.1.xlsx
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - TREAT-02
  - QUAL-01

must_haves:
  truths:
    - "DRUG_GROUPINGS named vector exists in R/00_config.R with all treatment codes from xlsx"
    - "all_codes_resolved_next_tables_v2.1.xlsx exists in data/reference/ as versioned snapshot"
    - "Smoke test validates DRUG_GROUPINGS has expected entry count and category coverage"
  artifacts:
    - path: "R/00_config.R"
      provides: "DRUG_GROUPINGS named vector (code = category)"
      contains: "DRUG_GROUPINGS <- c("
    - path: "data/reference/all_codes_resolved_next_tables_v2.1.xlsx"
      provides: "Versioned xlsx snapshot for audit trail"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "DRUG_GROUPINGS smoke test validation"
      contains: "DRUG_GROUPINGS"
  key_links:
    - from: "R/00_config.R"
      to: "all_codes_resolved_next_tables.xlsx"
      via: "One-time extraction (code values copied from xlsx into config)"
      pattern: "DRUG_GROUPINGS <- c\\("
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/00_config.R"
      via: "Validates DRUG_GROUPINGS loaded from config"
      pattern: "DRUG_GROUPINGS"
---

<objective>
Centralize drug treatment groupings from all_codes_resolved_next_tables.xlsx into R/00_config.R as a DRUG_GROUPINGS named vector, copy the xlsx to data/reference/ with version suffix, and add smoke test validation.

Purpose: Eliminate runtime xlsx dependency for Phase 78+ scripts that need treatment code-to-category mappings. Follows the AMC_PAYER_LOOKUP/CANCER_SITE_MAP centralization pattern established in Phase 36.
Output: DRUG_GROUPINGS constant in config, versioned xlsx snapshot, smoke test assertions.
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-cancer-classification-refinements/77-CONTEXT.md
@.planning/phases/77-cancer-classification-refinements/77-RESEARCH.md

@R/00_config.R
@R/88_smoke_test_comprehensive.R
@all_codes_resolved_next_tables.xlsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/00_config.R (existing pattern at line 319):
```r
AMC_PAYER_LOOKUP <- c(
  "MC" = "Medicaid",
  "MD" = "Medicare",
  ...
)
```

From R/00_config.R (CANCER_SITE_MAP pattern at line 433):
```r
CANCER_SITE_MAP <- c(
  "C00" = "Lip, Oral Cavity and Pharynx",
  ...
)
```

From R/88_smoke_test_comprehensive.R (check function at line 47):
```r
check <- function(description, condition) {
  if (condition) {
    message(glue("  PASS: {description}"))
    passed <<- passed + 1L
  } else {
    message(glue("  FAIL: {description}"))
    failed <<- failed + 1L
  }
}
```

Section numbering in R/88:
- Section 13: NLPHL Classification & Death Cause Validation
- Section 13B: TR Removal Validation
- Section 14: Summary (MUST remain last)
New section: 13C (after 13B, before 14)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract drug groupings from xlsx, add DRUG_GROUPINGS to R/00_config.R, copy xlsx snapshot</name>
  <files>R/00_config.R, data/reference/all_codes_resolved_next_tables_v2.1.xlsx</files>
  <read_first>
    - R/00_config.R (see existing CANCER_SITE_MAP pattern at line 433, AMC_PAYER_LOOKUP at line 319, SECTION 5d ends at line 1134, SECTION 6 starts at line 1137)
    - all_codes_resolved_next_tables.xlsx (source data -- read all sheets to verify schema per D-07)
    - .planning/phases/77-cancer-classification-refinements/77-CONTEXT.md (locked decisions D-05, D-06, D-07)
    - .planning/phases/77-cancer-classification-refinements/77-RESEARCH.md (Pattern 3: Named Vector Extraction from xlsx)
  </read_first>
  <action>
**Step 1: Create data/reference/ directory and copy xlsx snapshot (per D-06).**

Create directory `data/reference/` if it does not exist. Copy `all_codes_resolved_next_tables.xlsx` from project root to `data/reference/all_codes_resolved_next_tables_v2.1.xlsx`.

**Step 2: Inspect xlsx schema (per D-07).**

Use R (or readxl via a quick R script) to read the xlsx file and inspect ALL sheets. Expected sheets: "Chemotherapy", "Radiation", "SCT", "Immunotherapy", and possibly "Supportive Care". For each sheet:
- Identify the column containing treatment codes (expected: first column after any title/header rows)
- Identify the sheet name which becomes the category value
- Count codes per sheet
- Note any structural differences between sheets (some may have skip rows, different column names)

Research estimates: Chemotherapy: 203 codes, Radiation: 13, SCT: 41, Immunotherapy: 27. Total ~284 minimum.

**Step 3: Add DRUG_GROUPINGS named vector to R/00_config.R.**

Insert a new section between SECTION 5d (DEATH_CAUSE_MAP, ends ~line 1134) and SECTION 6 (ANALYSIS PARAMETERS, starts ~line 1137). The new section is **SECTION 5e: DRUG GROUPINGS**.

Structure (per D-05, following AMC_PAYER_LOOKUP/CANCER_SITE_MAP pattern):

```r
# ==============================================================================
# SECTION 5e: DRUG GROUPINGS ----
# ==============================================================================
# Treatment code groupings extracted from all_codes_resolved_next_tables.xlsx.
# Maps treatment codes (CPT/HCPCS/NDC/RXNORM/ICD-10-PCS) to treatment categories.
#
# WHY centralized: Phase 78 episode classification and Phase 79 frequency tables
# need these mappings. Avoids runtime xlsx dependency in downstream scripts.
# Follows AMC_PAYER_LOOKUP / CANCER_SITE_MAP pattern (named vector).
#
# Source: data/reference/all_codes_resolved_next_tables_v2.1.xlsx
# Extracted: Phase 77 (2026-06-02)
# ==============================================================================

DRUG_GROUPINGS <- c(
  # Chemotherapy (N codes from sheet "Chemotherapy")
  "CODE1" = "Chemotherapy",
  "CODE2" = "Chemotherapy",
  # ... all codes from Chemotherapy sheet

  # Radiation (N codes from sheet "Radiation")
  "CODE1" = "Radiation",
  # ... all codes from Radiation sheet

  # SCT (N codes from sheet "SCT")
  "CODE1" = "SCT",
  # ... all codes from SCT sheet

  # Immunotherapy (N codes from sheet "Immunotherapy")
  "CODE1" = "Immunotherapy",
  # ... all codes from Immunotherapy sheet

  # Supportive Care (N codes from sheet "Supportive Care", if sheet exists)
  "CODE1" = "Supportive Care",
  # ... all codes from Supportive Care sheet
)
```

Each code must be a character string key mapping to its sheet/category name. Remove NA codes. Remove duplicates (if a code appears in multiple sheets, keep first occurrence and log a warning).

After the DRUG_GROUPINGS definition, add a sanity check message:
```r
message(glue("Defined {length(DRUG_GROUPINGS)} treatment code mappings across {length(unique(DRUG_GROUPINGS))} categories"))
```

**Important:** Codes must be stored as CHARACTER strings (even if numeric). Wrap all codes in quotes.
  </action>
  <verify>
    <automated>Rscript -e "source('R/00_config.R'); cat('DRUG_GROUPINGS length:', length(DRUG_GROUPINGS), '\n'); cat('Categories:', paste(sort(unique(DRUG_GROUPINGS)), collapse=', '), '\n'); stopifnot(length(DRUG_GROUPINGS) >= 200)"</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains the string "SECTION 5e: DRUG GROUPINGS"
    - R/00_config.R contains the string "DRUG_GROUPINGS <- c("
    - R/00_config.R contains the string 'data/reference/all_codes_resolved_next_tables_v2.1.xlsx'
    - R/00_config.R contains category strings: "Chemotherapy", "Radiation", "SCT", "Immunotherapy"
    - data/reference/all_codes_resolved_next_tables_v2.1.xlsx exists (file.exists returns TRUE)
    - Rscript -e "source('R/00_config.R'); stopifnot(length(DRUG_GROUPINGS) >= 200)" exits 0
    - Rscript -e "source('R/00_config.R'); stopifnot(all(c('Chemotherapy','Radiation','SCT','Immunotherapy') %in% unique(DRUG_GROUPINGS)))" exits 0
  </acceptance_criteria>
  <done>DRUG_GROUPINGS named vector in R/00_config.R Section 5e with all treatment codes from xlsx. Versioned xlsx snapshot at data/reference/all_codes_resolved_next_tables_v2.1.xlsx. At least 200 code-to-category mappings across 4+ treatment categories.</done>
</task>

<task type="auto">
  <name>Task 2: Add DRUG_GROUPINGS smoke test validation to R/88</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (see Section 13B ends at line 782, Section 14 starts at line 784)
    - R/00_config.R (DRUG_GROUPINGS definition from Task 1)
  </read_first>
  <action>
Add a new **SECTION 13C: DRUG GROUPINGS VALIDATION** to R/88_smoke_test_comprehensive.R between Section 13B (TR Removal Validation, ends ~line 782) and Section 14 (Summary, starts ~line 784).

Update the section counter in the message line. Current last is "[19/19]" in Section 13B. New section should be "[20/21]" (adjusting total). Also update Section 14's implied total.

Insert the following checks:

```r
# ==============================================================================
# SECTION 13C: DRUG GROUPINGS VALIDATION ----
# ==============================================================================
# Phase 77 (TREAT-02): Validates DRUG_GROUPINGS centralization from xlsx.

message("\n[20/22] Drug groupings validation...")

# Check 1: DRUG_GROUPINGS exists and has sufficient entries
check(
  glue("DRUG_GROUPINGS has >= 200 entries (found {length(DRUG_GROUPINGS)})"),
  exists("DRUG_GROUPINGS") && length(DRUG_GROUPINGS) >= 200
)

# Check 2: All 4 core categories present
core_categories <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy")
found_categories <- intersect(core_categories, unique(DRUG_GROUPINGS))
check(
  glue("DRUG_GROUPINGS covers 4 core categories ({length(found_categories)}/4 found)"),
  length(found_categories) == 4
)

# Check 3: No NA keys (all codes are valid strings)
check(
  "DRUG_GROUPINGS has no NA keys",
  !any(is.na(names(DRUG_GROUPINGS)))
)

# Check 4: No NA values (all categories are valid strings)
check(
  "DRUG_GROUPINGS has no NA values",
  !any(is.na(DRUG_GROUPINGS))
)

# Check 5: Versioned xlsx snapshot exists
check(
  "data/reference/all_codes_resolved_next_tables_v2.1.xlsx exists",
  file.exists("data/reference/all_codes_resolved_next_tables_v2.1.xlsx")
)
```

Also update the Section 14 "Validated requirements" message list to add:
```r
message("  * TREAT-02: DRUG_GROUPINGS centralization from xlsx")
```

Update the counter labels throughout to reflect new total check count (was 19, now will be higher after all Phase 77 additions -- coordinate with Plan 02 additions).
  </action>
  <verify>
    <automated>Rscript -e "source('R/88_smoke_test_comprehensive.R')" 2>&1 | grep -c "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains the string "SECTION 13C: DRUG GROUPINGS VALIDATION"
    - R/88_smoke_test_comprehensive.R contains the string "DRUG_GROUPINGS has >= 200 entries"
    - R/88_smoke_test_comprehensive.R contains the string "DRUG_GROUPINGS covers 4 core categories"
    - R/88_smoke_test_comprehensive.R contains the string "all_codes_resolved_next_tables_v2.1.xlsx exists"
    - R/88_smoke_test_comprehensive.R contains the string "TREAT-02: DRUG_GROUPINGS centralization"
    - Rscript -e "source('R/88_smoke_test_comprehensive.R')" exits 0 (all checks pass)
  </acceptance_criteria>
  <done>R/88 smoke test includes 5 DRUG_GROUPINGS validation checks in Section 13C. All checks pass when executed. Requirements list updated to include TREAT-02.</done>
</task>

</tasks>

<verification>
1. `Rscript -e "source('R/00_config.R'); cat(length(DRUG_GROUPINGS))"` prints >= 200
2. `Rscript -e "source('R/00_config.R'); cat(paste(sort(unique(DRUG_GROUPINGS)), collapse=', '))"` shows at least Chemotherapy, Immunotherapy, Radiation, SCT
3. `file.exists("data/reference/all_codes_resolved_next_tables_v2.1.xlsx")` returns TRUE
4. `Rscript -e "source('R/88_smoke_test_comprehensive.R')"` exits 0 with all checks passing
</verification>

<success_criteria>
- DRUG_GROUPINGS named vector in R/00_config.R with 200+ entries across 4+ categories
- Versioned xlsx snapshot at data/reference/all_codes_resolved_next_tables_v2.1.xlsx
- Smoke test validates DRUG_GROUPINGS structure, count, categories, and snapshot existence
- All existing smoke tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/77-cancer-classification-refinements/77-01-SUMMARY.md`
</output>
