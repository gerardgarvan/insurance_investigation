---
phase: 03-cohort-building
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - R/04_build_cohort.R
autonomous: true
requirements: [CHRT-01, CHRT-02, CHRT-03]
must_haves:
  truths:
    - "User can run source('R/04_build_cohort.R') and get a complete HL cohort with demographics, payer, and treatment flags"
    - "User can see N patients before and after every filter step in console output AND in attrition_log data frame"
    - "Attrition log has columns: step, n_before, n_after, n_excluded, pct_excluded (from existing utils_attrition.R)"
    - "Final cohort has one row per patient with all D-09 columns: ID, SOURCE, SEX, RACE, HISPANIC, age_at_enr_start, age_at_enr_end, first_hl_dx_date, PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX, DUAL_ELIGIBLE, PAYER_TRANSITION, N_ENCOUNTERS, N_ENCOUNTERS_WITH_PAYER, HAD_CHEMO, HAD_RADIATION, HAD_SCT, enrollment_duration_days"
    - "Cohort is saved to output/cohort/hl_cohort.csv AND available as hl_cohort tibble in R environment"
    - "Filter chain runs in order: has_hodgkin_diagnosis -> with_enrollment_period -> exclude_missing_payer -> tag treatments (per D-01)"
  artifacts:
    - path: "R/04_build_cohort.R"
      provides: "Complete cohort build pipeline with attrition logging"
      min_lines: 120
    - path: "output/cohort/hl_cohort.csv"
      provides: "Final cohort dataset (created at runtime)"
  key_links:
    - from: "R/04_build_cohort.R"
      to: "R/03_cohort_predicates.R"
      via: "source() + function calls"
      pattern: "source.*03_cohort_predicates"
    - from: "R/04_build_cohort.R"
      to: "R/utils_attrition.R"
      via: "init_attrition_log() and log_attrition() calls"
      pattern: "log_attrition\\("
    - from: "R/04_build_cohort.R"
      to: "R/02_harmonize_payer.R"
      via: "source() for payer_summary and first_dx tibbles"
      pattern: "source.*02_harmonize_payer"
---

<objective>
Compose the filter chain, add treatment flags, calculate ages, assemble the final patient-level cohort, and save to CSV with full attrition logging at every step.

Purpose: This is the main cohort build script that chains the predicate functions from Plan 01, enriches with treatment and demographic data, and produces the final hl_cohort tibble + CSV that Phase 4 (visualization) consumes. Attrition logging provides the waterfall data for Phase 4.
Output: R/04_build_cohort.R producing hl_cohort tibble + output/cohort/hl_cohort.csv + attrition_log data frame.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cohort-building/03-CONTEXT.md
@.planning/phases/03-cohort-building/03-RESEARCH.md
@.planning/phases/03-cohort-building/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output and upstream dependencies. -->

From R/03_cohort_predicates.R (created in Plan 01):
```r
has_hodgkin_diagnosis <- function(patient_df)
# Tibble-in/tibble-out. Returns patients with HL diagnosis.
# Uses semi_join against pcornet$DIAGNOSIS filtered by is_hl_diagnosis().

with_enrollment_period <- function(patient_df)
# Tibble-in/tibble-out. Returns patients with at least one enrollment record.
# Uses semi_join against pcornet$ENROLLMENT.

exclude_missing_payer <- function(patient_df, payer_summary)
# Tibble-in/tibble-out. Returns patients with valid payer (not NA/Unknown/Unavailable).
# Uses semi_join against filtered payer_summary.

has_chemo <- function()
# Returns tibble: ID, HAD_CHEMO (1L). Patients with chemo evidence from TR + PX + RX.

has_radiation <- function()
# Returns tibble: ID, HAD_RADIATION (1L). Patients with radiation evidence from TR + PX.

has_sct <- function()
# Returns tibble: ID, HAD_SCT (1L). Patients with SCT evidence from TR + PX.
```

From R/utils_attrition.R:
```r
init_attrition_log <- function()
# Returns empty data.frame: step(chr), n_before(int), n_after(int), n_excluded(int), pct_excluded(num)

log_attrition <- function(log_df, step_name, n_after)
# Appends row. n_before inferred from previous step's n_after (first step: n_before = n_after).
# Prints: "[Attrition] {step_name}: {n_before} -> {n_after} ({n_excluded} excluded, {pct_excluded}%)"
```

From R/02_harmonize_payer.R (global tibbles after sourcing):
```r
payer_summary  # tibble: ID, SOURCE, PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX,
               # DUAL_ELIGIBLE, PAYER_TRANSITION, N_ENCOUNTERS, N_ENCOUNTERS_WITH_PAYER
first_dx       # tibble: ID, first_hl_dx_date (Date)
encounters     # tibble: encounter-level data
```

From R/01_load_pcornet.R (global named list after sourcing):
```r
pcornet$DEMOGRAPHIC  # Columns: ID, BIRTH_DATE, BIRTH_TIME, SEX, SEXUAL_ORIENTATION,
                     # GENDER_IDENTITY, HISPANIC, RACE, BIOBANK_FLAG, PAT_PREF_LANGUAGE_SPOKEN,
                     # ZIP_CODE, SOURCE
pcornet$ENROLLMENT   # Columns: ID, ENR_START_DATE, ENR_END_DATE, CHART, ENR_BASIS, SOURCE
```

DEMOGRAPHIC has one row per patient (ID is unique).
ENROLLMENT may have multiple rows per patient (multiple enrollment periods).

Payer categories to retain per D-04: "Medicare", "Medicaid", "Dual eligible", "Private",
"Other government", "No payment / Self-pay", "Other"
Payer categories excluded: NA, "Unknown", "Unavailable"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/04_build_cohort.R -- filter chain, enrollment aggregation, and payer/diagnosis joins</name>
  <files>R/04_build_cohort.R</files>
  <read_first>
    - R/03_cohort_predicates.R (must see actual function signatures and behavior from Plan 01)
    - R/02_harmonize_payer.R (see source chain pattern, payer_summary structure, first_dx structure, console output pattern)
    - R/utils_attrition.R (see init_attrition_log() and log_attrition() exact signatures)
    - R/00_config.R (see CONFIG structure, TREATMENT_CODES, output_dir)
    - R/01_load_pcornet.R (see pcornet$DEMOGRAPHIC and pcornet$ENROLLMENT column names)
    - csv_columns.txt (verify DEMOGRAPHIC columns: BIRTH_DATE, SEX, RACE, HISPANIC, SOURCE; ENROLLMENT columns: ENR_START_DATE, ENR_END_DATE)
    - .planning/phases/03-cohort-building/03-CONTEXT.md (all 14 decisions, especially D-01 filter order, D-09 output columns, D-10 age calculation, D-11 CSV + tibble output, D-13 multi-site, D-14 attrition utilities)
    - .planning/phases/03-cohort-building/03-RESEARCH.md (age calculation pattern, enrollment aggregation, cohort assembly examples)
  </read_first>
  <action>
Create R/04_build_cohort.R as the main cohort build script. This task writes SECTIONS 1-5 of the file (filter chain through payer join). The file will be completed by Task 2 which appends treatment flags, final assembly, and output.

NOTE TO EXECUTOR: Write the ENTIRE file in this task (sections 1-10 as described below PLUS Task 2's sections). The task split is for PLANNING SCOPE only -- the executor creates the complete file in Task 1 and Task 2 verifies the latter half. However, if context is a concern, write sections 1-5 first and leave a clear "# === SECTION 6: Treatment Flags ===" marker for Task 2 to continue from.

PREFERRED APPROACH: Write the complete file in one pass. The sections below plus Task 2's sections form a single coherent R script.

**SECTION 1: Header and dependencies**

```r
# ==============================================================================
# 04_build_cohort.R -- Build HL cohort with attrition logging
# ==============================================================================
#
# Composes named filter predicates into a sequential filter chain, adds treatment
# flags, calculates ages, and assembles the final patient-level HL cohort dataset.
#
# Requirements: CHRT-01 (named predicates), CHRT-02 (attrition logging), CHRT-03 (ICD matching)
#
# Filter chain order (per D-01):
#   1. has_hodgkin_diagnosis() -- identify HL patients from DIAGNOSIS table
#   2. with_enrollment_period() -- require at least one enrollment record
#   3. exclude_missing_payer() -- remove NA/Unknown/Unavailable payer categories
#   4. Tag treatment flags (HAD_CHEMO, HAD_RADIATION, HAD_SCT) -- identification only, not exclusion
#
# Output:
#   - hl_cohort tibble in R environment
#   - output/cohort/hl_cohort.csv
#   - attrition_log data frame in R environment (consumed by Phase 4 waterfall)
#
# Usage:
#   source("R/04_build_cohort.R")
#
# ==============================================================================

source("R/02_harmonize_payer.R")  # Loads 01_load_pcornet.R -> 00_config.R -> utils
source("R/03_cohort_predicates.R")  # Named predicates + treatment flag functions

library(dplyr)
library(lubridate)
library(glue)
library(readr)
```

**SECTION 2: Filter chain with attrition logging (D-01, D-14, CHRT-02)**

Initialize attrition log. Start with all patients from pcornet$DEMOGRAPHIC (one row per patient -- this is the canonical patient-level table). Select columns needed for cohort: ID, SOURCE, SEX, RACE, HISPANIC, BIRTH_DATE.

Apply predicates in D-01 order with log_attrition() after each step. Use n_distinct(cohort$ID) for patient counts (not nrow() -- Pitfall 1 from research). Since DEMOGRAPHIC has one row per patient, nrow and n_distinct(ID) should match at this stage, but use n_distinct for safety.

```r
message("\n", strrep("=", 60))
message("HL Cohort Building Pipeline")
message(strrep("=", 60))

# Initialize attrition tracking (CHRT-02)
attrition_log <- init_attrition_log()

# Step 0: Initial population from DEMOGRAPHIC (one row per patient)
cohort <- pcornet$DEMOGRAPHIC %>%
  select(ID, SOURCE, SEX, RACE, HISPANIC, BIRTH_DATE)
attrition_log <- log_attrition(attrition_log, "Initial population", n_distinct(cohort$ID))

# Step 1: has_hodgkin_diagnosis() (D-01 first filter)
cohort <- cohort %>% has_hodgkin_diagnosis()
attrition_log <- log_attrition(attrition_log, "Has HL diagnosis (ICD-9/10)", n_distinct(cohort$ID))

# Step 2: with_enrollment_period() (D-03: any enrollment, no min days)
cohort <- cohort %>% with_enrollment_period()
attrition_log <- log_attrition(attrition_log, "Has enrollment record", n_distinct(cohort$ID))

# Step 3: exclude_missing_payer() (D-04: remove NA/Unknown/Unavailable)
cohort <- cohort %>% exclude_missing_payer(payer_summary)
attrition_log <- log_attrition(attrition_log, "Valid payer category", n_distinct(cohort$ID))
```

**SECTION 3: Enrollment aggregation (D-10 age calculation)**

Aggregate enrollment periods per patient: min(ENR_START_DATE) as enr_start_date, max(ENR_END_DATE) as enr_end_date, total enrollment_duration_days = as.numeric(enr_end_date - enr_start_date).

Multi-site handling (D-13, Claude's discretion): Use primary site strategy. Join ENROLLMENT to DEMOGRAPHIC by both ID and SOURCE to keep only primary-site enrollment periods. DEMOGRAPHIC has one row per patient with canonical SOURCE.

Calculate age using lubridate interval() + time_length() per research pattern:
- age_at_enr_start = as.integer(time_length(interval(BIRTH_DATE, enr_start_date), "years"))
- age_at_enr_end = as.integer(time_length(interval(BIRTH_DATE, enr_end_date), "years"))

```r
message("\n--- Enrollment Aggregation ---")

# Get primary site enrollment only (D-13: primary site strategy)
enrollment_primary <- pcornet$ENROLLMENT %>%
  inner_join(
    pcornet$DEMOGRAPHIC %>% select(ID, SOURCE),
    by = c("ID", "SOURCE")
  )

# Aggregate enrollment dates per patient
enrollment_dates <- enrollment_primary %>%
  group_by(ID) %>%
  summarise(
    enr_start_date = min(ENR_START_DATE, na.rm = TRUE),
    enr_end_date = max(ENR_END_DATE, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  mutate(
    enrollment_duration_days = as.numeric(enr_end_date - enr_start_date)
  )

# Join enrollment dates and calculate ages (D-10)
cohort <- cohort %>%
  left_join(enrollment_dates, by = "ID") %>%
  mutate(
    age_at_enr_start = as.integer(
      time_length(interval(BIRTH_DATE, enr_start_date), "years")
    ),
    age_at_enr_end = as.integer(
      time_length(interval(BIRTH_DATE, enr_end_date), "years")
    )
  )

message(glue("  Patients with enrollment dates: {sum(!is.na(cohort$enr_start_date))}"))
message(glue("  Median enrollment duration: {median(cohort$enrollment_duration_days, na.rm = TRUE)} days"))
```

**SECTION 4: Join first HL diagnosis date**

Join first_dx tibble (from 02_harmonize_payer.R) which has columns ID, first_hl_dx_date.

```r
message("\n--- First HL Diagnosis Date ---")

cohort <- cohort %>%
  left_join(first_dx, by = "ID")

message(glue("  Patients with first_hl_dx_date: {sum(!is.na(cohort$first_hl_dx_date))}"))
message(glue("  Patients missing first_hl_dx_date: {sum(is.na(cohort$first_hl_dx_date))}"))
```

**SECTION 5: Join payer summary fields (D-09)**

Join payer fields from payer_summary: PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX, DUAL_ELIGIBLE, PAYER_TRANSITION, N_ENCOUNTERS, N_ENCOUNTERS_WITH_PAYER.

NOTE: exclude_missing_payer() already filtered to valid-payer patients, so this join should match all cohort patients. Use left_join to be safe, then check for mismatches.

```r
message("\n--- Payer Summary ---")

cohort <- cohort %>%
  left_join(
    payer_summary %>%
      select(ID, PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX,
             DUAL_ELIGIBLE, PAYER_TRANSITION, N_ENCOUNTERS, N_ENCOUNTERS_WITH_PAYER),
    by = "ID"
  )

n_missing_payer <- sum(is.na(cohort$PAYER_CATEGORY_PRIMARY))
if (n_missing_payer > 0) {
  message(glue("  WARNING: {n_missing_payer} patients missing PAYER_CATEGORY_PRIMARY after join"))
} else {
  message("  All patients have PAYER_CATEGORY_PRIMARY assigned")
}
```

CRITICAL IMPLEMENTATION NOTES FOR SECTIONS 1-5:
- The source chain is: 04 sources 02, which sources 01, which sources 00_config (which auto-sources utils). Then 04 sources 03.
- Use n_distinct(cohort$ID) for all attrition counts, never nrow() (Pitfall 1)
- Multi-site dedup (D-13): Filter ENROLLMENT to primary site by inner_join on (ID, SOURCE) with DEMOGRAPHIC
- Age calculation uses lubridate::interval() + time_length(), NOT date subtraction / 365.25
  </action>
  <verify>
    <automated>grep -c "log_attrition\|init_attrition_log\|has_hodgkin_diagnosis\|with_enrollment_period\|exclude_missing_payer\|enrollment_duration_days\|time_length" R/04_build_cohort.R</automated>
  </verify>
  <done>R/04_build_cohort.R sections 1-5 complete: source chain, filter chain with 4 attrition log entries (initial + 3 filter steps), enrollment aggregation with primary-site strategy (D-13), lubridate age calculation (D-10), first_dx join, and payer summary join (D-09 fields).</done>
</task>

<task type="auto">
  <name>Task 2: Complete R/04_build_cohort.R -- treatment flags, final assembly, summary, and CSV output</name>
  <files>R/04_build_cohort.R</files>
  <read_first>
    - R/04_build_cohort.R (must see what Task 1 created -- verify sections 1-5 exist before appending/completing)
    - R/03_cohort_predicates.R (verify has_chemo(), has_radiation(), has_sct() signatures)
    - .planning/phases/03-cohort-building/03-CONTEXT.md (D-02 treatment flags, D-06 integer 0/1, D-09 column list, D-11 CSV output)
  </read_first>
  <action>
Complete R/04_build_cohort.R by adding sections 6-10 (treatment flags, final assembly, cohort summary, attrition summary, CSV output). If Task 1 already wrote the complete file, verify these sections exist and are correct. If Task 1 only wrote sections 1-5, append sections 6-10.

**SECTION 6: Treatment flags (D-02, D-05, D-06)**

Call has_chemo(), has_radiation(), has_sct() from 03_cohort_predicates.R. Left-join each result to cohort. Replace NA with 0L for patients without evidence. Per D-02: these are identification flags only, not exclusion filters.

```r
message("\n--- Treatment Flags ---")

# Get treatment evidence (D-05: multi-source, D-06: integer 0/1)
chemo_flags <- has_chemo()
rad_flags <- has_radiation()
sct_flags <- has_sct()

# Join treatment flags to cohort (D-02: flags only, not exclusion)
cohort <- cohort %>%
  left_join(chemo_flags, by = "ID") %>%
  left_join(rad_flags, by = "ID") %>%
  left_join(sct_flags, by = "ID") %>%
  mutate(
    HAD_CHEMO = replace_na(HAD_CHEMO, 0L),
    HAD_RADIATION = replace_na(HAD_RADIATION, 0L),
    HAD_SCT = replace_na(HAD_SCT, 0L)
  )

message(glue("  HAD_CHEMO = 1: {sum(cohort$HAD_CHEMO == 1)} patients ({round(100 * mean(cohort$HAD_CHEMO), 1)}%)"))
message(glue("  HAD_RADIATION = 1: {sum(cohort$HAD_RADIATION == 1)} patients ({round(100 * mean(cohort$HAD_RADIATION), 1)}%)"))
message(glue("  HAD_SCT = 1: {sum(cohort$HAD_SCT == 1)} patients ({round(100 * mean(cohort$HAD_SCT), 1)}%)"))
```

**SECTION 7: Final cohort assembly (D-09 column order)**

Select and order columns per D-09: ID, SOURCE, SEX, RACE, HISPANIC, age_at_enr_start, age_at_enr_end, first_hl_dx_date, PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX, DUAL_ELIGIBLE, PAYER_TRANSITION, N_ENCOUNTERS, N_ENCOUNTERS_WITH_PAYER, HAD_CHEMO, HAD_RADIATION, HAD_SCT, enrollment_duration_days.

Drop BIRTH_DATE (per D-10: use ages instead of raw dates), enr_start_date, enr_end_date (intermediate columns).

Assign to hl_cohort variable name per D-11.

```r
message("\n--- Final Cohort Assembly ---")

hl_cohort <- cohort %>%
  select(
    ID,
    SOURCE,
    SEX,
    RACE,
    HISPANIC,
    age_at_enr_start,
    age_at_enr_end,
    first_hl_dx_date,
    PAYER_CATEGORY_PRIMARY,
    PAYER_CATEGORY_AT_FIRST_DX,
    DUAL_ELIGIBLE,
    PAYER_TRANSITION,
    N_ENCOUNTERS,
    N_ENCOUNTERS_WITH_PAYER,
    HAD_CHEMO,
    HAD_RADIATION,
    HAD_SCT,
    enrollment_duration_days
  )
```

**SECTION 8: Cohort summary (console output)**

Print a comprehensive summary:
- Total patients in final cohort
- Payer category distribution (count per PAYER_CATEGORY_PRIMARY)
- Treatment flag totals
- Age range (min/max/median age_at_enr_start)
- Enrollment duration (min/max/median)
- SOURCE distribution

```r
message("\n", strrep("=", 60))
message("HL COHORT SUMMARY")
message(strrep("=", 60))

message(glue("\nTotal patients: {nrow(hl_cohort)}"))

message("\n--- Payer Distribution ---")
payer_dist <- hl_cohort %>%
  count(PAYER_CATEGORY_PRIMARY, name = "n") %>%
  arrange(desc(n))
for (i in seq_len(nrow(payer_dist))) {
  message(glue("  {payer_dist$PAYER_CATEGORY_PRIMARY[i]}: {payer_dist$n[i]}"))
}

message("\n--- Treatment Flags ---")
message(glue("  Chemotherapy: {sum(hl_cohort$HAD_CHEMO == 1)} ({round(100 * mean(hl_cohort$HAD_CHEMO), 1)}%)"))
message(glue("  Radiation: {sum(hl_cohort$HAD_RADIATION == 1)} ({round(100 * mean(hl_cohort$HAD_RADIATION), 1)}%)"))
message(glue("  SCT: {sum(hl_cohort$HAD_SCT == 1)} ({round(100 * mean(hl_cohort$HAD_SCT), 1)}%)"))

message("\n--- Demographics ---")
message(glue("  Age at enrollment start: median {median(hl_cohort$age_at_enr_start, na.rm = TRUE)}, range [{min(hl_cohort$age_at_enr_start, na.rm = TRUE)}, {max(hl_cohort$age_at_enr_start, na.rm = TRUE)}]"))
message(glue("  Enrollment duration: median {median(hl_cohort$enrollment_duration_days, na.rm = TRUE)} days"))

message("\n--- Sites ---")
site_dist <- hl_cohort %>%
  count(SOURCE, name = "n") %>%
  arrange(desc(n))
for (i in seq_len(nrow(site_dist))) {
  message(glue("  {site_dist$SOURCE[i]}: {site_dist$n[i]}"))
}
```

**SECTION 9: Attrition summary (CHRT-02)**

Print the complete attrition log as a table to console. Keep attrition_log in global environment for Phase 4 waterfall chart.

```r
message("\n--- Attrition Log ---")
print(attrition_log)
```

**SECTION 10: CSV output (D-11)**

Save hl_cohort to output/cohort/hl_cohort.csv using readr::write_csv(). Create output/cohort/ directory if it does not exist.

```r
# Create output directory
dir.create(file.path(CONFIG$output_dir, "cohort"), showWarnings = FALSE, recursive = TRUE)

output_path <- file.path(CONFIG$output_dir, "cohort", "hl_cohort.csv")
write_csv(hl_cohort, output_path)
message(glue("\nCohort saved to: {output_path}"))
message(glue("Rows: {nrow(hl_cohort)}, Columns: {ncol(hl_cohort)}"))
message(glue("Columns: {paste(names(hl_cohort), collapse = ', ')}"))

message("\n", strrep("=", 60))
message("Cohort building complete")
message(strrep("=", 60))
```

**File footer:**
```r
# ==============================================================================
# End of 04_build_cohort.R
# ==============================================================================
```

CRITICAL IMPLEMENTATION NOTES FOR SECTIONS 6-10:
- Treatment flags are LEFT JOINed then replace_na(0L) -- they do NOT exclude patients (D-02)
- BIRTH_DATE is dropped from final output -- replaced by age_at_enr_start and age_at_enr_end (D-10)
- attrition_log and hl_cohort both remain in global R environment after sourcing (Phase 4 needs both)
- Final select() must include exactly 18 columns in D-09 order
- If Task 1 already wrote the complete file, verify all sections 6-10 are present and correct rather than appending duplicate sections
  </action>
  <verify>
    <automated>grep -c "has_chemo\|has_radiation\|has_sct\|replace_na\|hl_cohort\|write_csv\|HAD_CHEMO\|HAD_RADIATION\|HAD_SCT" R/04_build_cohort.R</automated>
  </verify>
  <done>R/04_build_cohort.R is complete with all 10 sections: source chain, filter chain with attrition logging, enrollment aggregation, first_dx join, payer join, treatment flags (left_join + replace_na(0L)), final assembly (18 D-09 columns), cohort summary, attrition summary, and CSV output. hl_cohort and attrition_log remain in global R environment for Phase 4.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. R/04_build_cohort.R sources 02_harmonize_payer.R and 03_cohort_predicates.R
2. Filter chain follows D-01 order with attrition logging after each step
3. Treatment flags use has_chemo(), has_radiation(), has_sct() as lookup joins (not exclusion filters)
4. Age calculation uses lubridate interval()/time_length() pattern
5. Final hl_cohort has exactly 18 columns matching D-09 specification
6. CSV output at output/cohort/hl_cohort.csv
7. attrition_log and hl_cohort available in global R environment for Phase 4
</verification>

<success_criteria>
- source("R/04_build_cohort.R") executes the full pipeline: load data -> harmonize payer -> apply filter chain -> add treatment flags -> calculate ages -> assemble cohort -> save CSV
- Console shows attrition at every step: "Initial population", "Has HL diagnosis", "Has enrollment record", "Valid payer category"
- attrition_log data frame has 4 rows (one per step) with columns: step, n_before, n_after, n_excluded, pct_excluded
- hl_cohort tibble and output/cohort/hl_cohort.csv have identical content with 18 columns
- Cohort summary printed to console with payer distribution, treatment flag totals, demographics, and site distribution
</success_criteria>

<output>
After completion, create `.planning/phases/03-cohort-building/03-02-SUMMARY.md`
</output>