---
phase: 58-cancer-summary-pre-post-hl-counts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/58_cancer_summary_pre_post.R
  - output/tables/cancer_summary_table_pre_post.xlsx
autonomous: false
requirements:
  - SC-01
  - SC-02
  - SC-03
  - SC-04
  - SC-05
  - SC-06

must_haves:
  truths:
    - "D codes are excluded from cancer_summary_table_pre_post.xlsx (C codes only)"
    - "Population is limited to confirmed 7-day HL patients"
    - "Each cancer code row has pre_hl_count, post_hl_count, and both_count columns"
    - "Pre-HL uses DX_DATE <= first_hl_dx_date; Post-HL uses DX_DATE > first_hl_dx_date"
    - "C81 (Hodgkin Lymphoma) rows are excluded from the output"
    - "All values are raw counts with no percentages"
  artifacts:
    - path: "R/58_cancer_summary_pre_post.R"
      provides: "Pre/post HL cancer summary script"
      min_lines: 400
    - path: "output/tables/cancer_summary_table_pre_post.xlsx"
      provides: "Two-sheet styled xlsx with pre/post HL counts"
      contains: "Category Summary"
  key_links:
    - from: "R/58_cancer_summary_pre_post.R"
      to: "output/confirmed_hl_cohort.rds"
      via: "readRDS()"
      pattern: "readRDS.*confirmed_hl_cohort"
    - from: "R/58_cancer_summary_pre_post.R"
      to: "DIAGNOSIS DuckDB table"
      via: "get_pcornet_table()"
      pattern: 'get_pcornet_table.*DIAGNOSIS'
    - from: "R/58_cancer_summary_pre_post.R"
      to: "output/tables/cancer_summary_table_pre_post.xlsx"
      via: "wb$save()"
      pattern: "wb.*save"
---

<objective>
Create R/58_cancer_summary_pre_post.R that produces cancer_summary_table_pre_post.xlsx -- a two-sheet styled workbook showing per-cancer-code patient counts split by timing relative to first HL diagnosis date (pre-HL, post-HL, both). Population is the confirmed 7-day HL cohort. Counts only, no percentages. C81 excluded since it is the anchor diagnosis.

Purpose: This table answers the clinical question "what other cancers did HL patients have before vs after their HL diagnosis?" by splitting baseline cancer summary metrics into temporal partitions relative to each patient's first HL diagnosis date.

Output: R/58_cancer_summary_pre_post.R (standalone script) and output/tables/cancer_summary_table_pre_post.xlsx (two-sheet workbook: Category Summary + Code Summary).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-cancer-summary-pre-post-hl-counts/58-CONTEXT.md
@.planning/phases/58-cancer-summary-pre-post-hl-counts/58-RESEARCH.md
@.planning/phases/55-cancer-summary-refinement-foundation/55-01-SUMMARY.md

@R/55_cancer_summary_refined.R
@R/56_cancer_summary_post_hl.R
@R/00_config.R
@R/01_load_pcornet.R

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From R/55_cancer_summary_refined.R:
- PREFIX_MAP: Named character vector (309 entries) mapping 3-char ICD prefixes to cancer site categories
- classify_codes(codes): Takes normalized uppercase no-dot code vector, returns category names via PREFIX_MAP
- Styling constants: DARK_HEADER_FILL = "FF374151", WHITE_FONT = "FFFFFFFF", TITLE_FONT_COLOR = "FF1F2937", TOTALS_FILL = "FFE5E7EB"
- confirmed_hl_cohort.rds schema: tibble with columns ID (char), first_hl_dx_date (Date), first_hl_dx_source (char)
- cancer_summary.csv schema: ID, cancer_code, description, two_or_more_unique_dates, two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7

From R/56_cancer_summary_post_hl.R:
- DuckDB query pattern: get_pcornet_table("DIAGNOSIS") %>% filter(DX_TYPE == "10") %>% mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>% filter(str_detect(DX_norm, "^C")) %>% select(ID, DX_norm, DX_DATE) %>% collect() %>% filter(ID %in% cohort_ids)
- Sentinel exclusion: filter(is.na(DX_DATE) | DX_DATE >= SENTINEL_CUTOFF) where SENTINEL_CUTOFF <- as.Date("1910-01-01")
- Temporal filter pattern: DX_DATE > first_hl_dx_date (strict >)

From R/00_config.R:
- CONFIG$output_dir = "output"
- USE_DUCKDB = TRUE

From R/01_load_pcornet.R:
- get_pcornet_table(table_name): Returns tbl_dbi for DuckDB queries
- close_pcornet_con(): Closes DuckDB connection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/58_cancer_summary_pre_post.R</name>
  <files>R/58_cancer_summary_pre_post.R</files>
  <read_first>
    R/55_cancer_summary_refined.R
    R/56_cancer_summary_post_hl.R
    R/00_config.R
    R/01_load_pcornet.R
    .planning/phases/58-cancer-summary-pre-post-hl-counts/58-CONTEXT.md
  </read_first>
  <action>
Create R/58_cancer_summary_pre_post.R as a standalone script following the 11-section structure from 58-RESEARCH.md. The script must implement all 16 locked decisions (D-01 through D-16).

**SECTION 1: SETUP**
- suppressPackageStartupMessages for: dplyr, stringr, glue, openxlsx2, lubridate
- source("R/00_config.R") and source("R/01_load_pcornet.R")
- Define paths:
  - INPUT_RDS = file.path(CONFIG$output_dir, "confirmed_hl_cohort.rds")
  - INPUT_CSV = file.path(CONFIG$output_dir, "tables", "cancer_summary.csv")
  - OUTPUT_TABLE_XLSX = file.path(CONFIG$output_dir, "tables", "cancer_summary_table_pre_post.xlsx")
  - OUTPUT_CSV = file.path(CONFIG$output_dir, "tables", "cancer_summary_table_pre_post.csv")
- Define SENTINEL_CUTOFF <- as.Date("1910-01-01") per D-11
- Message banner: "=== Phase 58: Cancer Summary - Pre/Post HL Counts ==="

**SECTION 2: PREFIX_MAP AND classify_codes()**
- Copy PREFIX_MAP and classify_codes() verbatim from R/55 lines 69-338 (all 309 entries) for script independence (Claude's discretion: copy from R/55, same pattern as R/56)
- Include all D-code entries (needed for completeness even though they won't appear in output)

**SECTION 3: LOAD INPUTS**
- Load confirmed_hl_cohort.rds per D-12: `confirmed_hl_cohort <- readRDS(INPUT_RDS)`
- Filter to patients with non-NA first_hl_dx_date (same pattern as R/56 Section 3)
- Log cohort size
- Load cancer_summary.csv for baseline metrics and description lookup per D-01
- Filter cancer_summary to remove D-codes: `filter(!str_detect(cancer_code, "^D"))` and remove C81 rows: `filter(!str_detect(cancer_code, "^C81"))` per D-09
- Filter cancer_summary to confirmed cohort only: `inner_join(confirmed_hl_cohort %>% select(ID), by = "ID")`

**SECTION 4: QUERY DIAGNOSIS FOR RAW DATE ROWS**
- DuckDB query following R/56 Section 4 pattern exactly:
  ```r
  cohort_ids <- confirmed_hl_cohort$ID
  dx_raw <- get_pcornet_table("DIAGNOSIS") %>%
    filter(DX_TYPE == "10") %>%
    mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
    filter(str_detect(DX_norm, "^C")) %>%
    select(ID, DX_norm, DX_DATE) %>%
    collect() %>%
    filter(ID %in% cohort_ids)
  ```
- Exclude sentinel dates per D-11: `filter(is.na(DX_DATE) | DX_DATE >= SENTINEL_CUTOFF)`
- Exclude C81 rows from dx_raw per D-09: `filter(!str_detect(DX_norm, "^C81"))`
- Log row counts at each step

**SECTION 5: COMPUTE PRE/POST/BOTH SETS**
- Join dx_raw with confirmed_hl_cohort to get first_hl_dx_date per row:
  ```r
  dx_with_hl_date <- dx_raw %>%
    inner_join(confirmed_hl_cohort %>% select(ID, first_hl_dx_date), by = "ID") %>%
    filter(!is.na(DX_DATE))
  ```
- Pre-HL per D-08 (same-day included): `dx_pre_hl <- dx_with_hl_date %>% filter(DX_DATE <= first_hl_dx_date)`
- Post-HL per D-08 (strict >): `dx_post_hl <- dx_with_hl_date %>% filter(DX_DATE > first_hl_dx_date)`
- Compute patient-code pairs per temporal set:
  ```r
  patients_pre <- dx_pre_hl %>% distinct(ID, DX_norm) %>% rename(cancer_code = DX_norm)
  patients_post <- dx_post_hl %>% distinct(ID, DX_norm) %>% rename(cancer_code = DX_norm)
  patients_both <- patients_pre %>% inner_join(patients_post, by = c("ID", "cancer_code"))
  ```
- Aggregate counts by code per D-04 (Both = intersection, Pre/Post non-exclusive):
  ```r
  pre_counts <- patients_pre %>% group_by(cancer_code) %>% summarise(pre_hl_count = n_distinct(ID), .groups = "drop")
  post_counts <- patients_post %>% group_by(cancer_code) %>% summarise(post_hl_count = n_distinct(ID), .groups = "drop")
  both_counts <- patients_both %>% group_by(cancer_code) %>% summarise(both_count = n_distinct(ID), .groups = "drop")
  ```
- Log row counts and patient counts for pre, post, both

**SECTION 6: COMPUTE BASELINE METRICS**
- From cancer_summary (loaded in Section 3, already filtered to confirmed cohort, no D-codes, no C81):
  ```r
  code_baseline <- cancer_summary %>%
    group_by(cancer_code) %>%
    summarise(
      total_patients  = n_distinct(ID),
      confirmed_2date = n_distinct(ID[two_or_more_unique_dates == 1]),
      confirmed_7day  = n_distinct(ID[two_or_more_unique_dates_gt_7 == 1]),
      .groups = "drop"
    )
  ```
- Query DuckDB record counts per code (same pattern as R/55 Section 8, but filtered to confirmed cohort IDs and excluding C81):
  ```r
  dx_record_counts <- get_pcornet_table("DIAGNOSIS") %>%
    filter(DX_TYPE == "10") %>%
    mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
    filter(str_detect(DX_norm, "^C")) %>%
    group_by(DX_norm) %>%
    summarise(total_records = n(), .groups = "drop") %>%
    collect() %>%
    filter(!str_detect(DX_norm, "^C81"))
  ```
- Add category via classify_codes()

**SECTION 7: MERGE AND BUILD CODE-LEVEL TABLE**
- Left join pre_counts, post_counts, both_counts onto code_baseline:
  ```r
  code_summary <- code_baseline %>%
    mutate(category = classify_codes(cancer_code)) %>%
    left_join(pre_counts, by = "cancer_code") %>%
    left_join(post_counts, by = "cancer_code") %>%
    left_join(both_counts, by = "cancer_code") %>%
    left_join(dx_record_counts, by = c("cancer_code" = "DX_norm")) %>%
    mutate(
      pre_hl_count  = replace(pre_hl_count, is.na(pre_hl_count), 0L),
      post_hl_count = replace(post_hl_count, is.na(post_hl_count), 0L),
      both_count    = replace(both_count, is.na(both_count), 0L),
      total_records = replace(total_records, is.na(total_records), 0L)
    )
  ```
- Handle unclassified codes (set to "Unclassified")
- Select columns per D-07: cancer_code, category, total_patients, confirmed_2date, confirmed_7day, pre_hl_count, post_hl_count, both_count, total_records
- Arrange by desc(total_patients)

**SECTION 8: BUILD CATEGORY-LEVEL TABLE**
- Aggregate code_summary to category level per D-06:
  ```r
  category_summary <- code_summary %>%
    group_by(category) %>%
    summarise(
      total_patients  = sum(total_patients),
      confirmed_2date = sum(confirmed_2date),
      confirmed_7day  = sum(confirmed_7day),
      pre_hl_count    = sum(pre_hl_count),
      post_hl_count   = sum(post_hl_count),
      both_count      = sum(both_count),
      total_records   = sum(total_records),
      .groups = "drop"
    ) %>%
    arrange(desc(total_patients))
  ```
- Build totals rows for both category and code summaries:
  ```r
  totals_category <- tibble(
    category        = "TOTAL",
    total_patients  = sum(category_summary$total_patients),
    confirmed_2date = sum(category_summary$confirmed_2date),
    confirmed_7day  = sum(category_summary$confirmed_7day),
    pre_hl_count    = sum(category_summary$pre_hl_count),
    post_hl_count   = sum(category_summary$post_hl_count),
    both_count      = sum(category_summary$both_count),
    total_records   = sum(category_summary$total_records)
  )
  totals_code <- tibble(
    cancer_code     = "TOTAL",
    category        = "",
    total_patients  = sum(code_summary$total_patients),
    confirmed_2date = sum(code_summary$confirmed_2date),
    confirmed_7day  = sum(code_summary$confirmed_7day),
    pre_hl_count    = sum(code_summary$pre_hl_count),
    post_hl_count   = sum(code_summary$post_hl_count),
    both_count      = sum(code_summary$both_count),
    total_records   = sum(code_summary$total_records)
  )
  ```

**SECTION 9: WRITE XLSX OUTPUT**
Per D-15 and D-16, create two-sheet styled workbook with R/55 styling:

Styling constants (same as R/55):
```r
DARK_HEADER_FILL <- "FF374151"
WHITE_FONT       <- "FFFFFFFF"
TITLE_FONT_COLOR <- "FF1F2937"
TOTALS_FILL      <- "FFE5E7EB"
```

**Sheet 1: "Category Summary"** per D-06 column order:
- Row 1: Title "Cancer Summary Table - Pre/Post HL Diagnosis (By Category)" merged A1:H1, Calibri 16pt bold, color TITLE_FONT_COLOR
- Row 2: Headers = Cancer Site Category | Total Patients | Confirmed (2+ Dates) | Confirmed (7-Day Gap) | Pre-HL | Post-HL | Both | Total Records
  - Fill DARK_HEADER_FILL, font Calibri 11pt bold WHITE_FONT, range A2:H2
- Freeze pane: first_active_row = 3, first_active_col = 1
- Data rows starting at row 3, col_names = FALSE
- Number format "#,##0" for columns B through H (all count columns per D-05, no percentages)
- Totals row: light gray fill TOTALS_FILL, bold, same number format
- Column widths: c(40, 14, 18, 18, 10, 10, 10, 14)
- Footnote row 2 rows below totals (Calibri 10pt italic, color "FF6B7280", merged A:H):
  "Population: Confirmed 7-day HL cohort ({N} patients). Pre: DX_DATE <= first_hl_dx_date. Post: DX_DATE > first_hl_dx_date. Both: patient had code pre AND post. C81 excluded."

**Sheet 2: "Code Summary"** per D-07 column order:
- Row 1: Title "Cancer Summary Table - Pre/Post HL Diagnosis (By ICD-10 Code)" merged A1:I1, Calibri 16pt bold, color TITLE_FONT_COLOR
- Row 2: Headers = ICD-10 Code | Cancer Site Category | Total Patients | Confirmed (2+ Dates) | Confirmed (7-Day Gap) | Pre-HL | Post-HL | Both | Total Records
  - Fill DARK_HEADER_FILL, font Calibri 11pt bold WHITE_FONT, range A2:I2
- Freeze pane: first_active_row = 3, first_active_col = 1
- Data rows starting at row 3, col_names = FALSE
- Number format "#,##0" for columns C through I (all count columns)
- Totals row: light gray fill TOTALS_FILL, bold, same number format
- Column widths: c(14, 40, 14, 18, 18, 10, 10, 10, 14)
- Footnote row 2 rows below totals (same style as Sheet 1, merged A:I)

Save workbook to OUTPUT_TABLE_XLSX.

**SECTION 10: WRITE COMPANION CSV** (Claude's discretion: yes, include a CSV for easy programmatic access)
- Write code_summary (with category column) to OUTPUT_CSV

**SECTION 11: CLEANUP**
- close_pcornet_con()
- Log completion message with row counts
  </action>
  <verify>
    <automated>grep -c "suppressPackageStartupMessages" R/58_cancer_summary_pre_post.R &amp;&amp; grep -c "PREFIX_MAP" R/58_cancer_summary_pre_post.R &amp;&amp; grep -c "pre_hl_count" R/58_cancer_summary_pre_post.R &amp;&amp; grep -c "post_hl_count" R/58_cancer_summary_pre_post.R &amp;&amp; grep -c "both_count" R/58_cancer_summary_pre_post.R &amp;&amp; grep -c "Category Summary" R/58_cancer_summary_pre_post.R &amp;&amp; grep -c "Code Summary" R/58_cancer_summary_pre_post.R &amp;&amp; grep -c "close_pcornet_con" R/58_cancer_summary_pre_post.R</automated>
  </verify>
  <acceptance_criteria>
    - R/58_cancer_summary_pre_post.R exists and is >= 400 lines
    - File contains `suppressPackageStartupMessages` (library loading)
    - File contains `readRDS.*confirmed_hl_cohort` (D-12: loads cohort RDS)
    - File contains `get_pcornet_table("DIAGNOSIS")` (D-13: DuckDB DIAGNOSIS query)
    - File contains `DX_DATE <= first_hl_dx_date` (D-08: pre-HL same-day included)
    - File contains `DX_DATE > first_hl_dx_date` (D-08: post-HL strict)
    - File contains `filter(!str_detect(` with `"^C81"` (D-09: C81 exclusion)
    - File contains `filter(!str_detect(` with `"^D"` (D-code exclusion)
    - File contains `SENTINEL_CUTOFF <- as.Date("1910-01-01")` (D-11: sentinel dates)
    - File contains `pre_hl_count` column (D-03)
    - File contains `post_hl_count` column (D-03)
    - File contains `both_count` column (D-03)
    - File does NOT contain `pct_confirmed` or `Rate` or `mean_unique` or `median_unique` (D-02/D-05: no percentages or date stats)
    - File contains `"Category Summary"` sheet name (D-15)
    - File contains `"Code Summary"` sheet name (D-15)
    - File contains `FF374151` (D-16: dark header fill)
    - File contains `FFE5E7EB` (D-16: totals fill)
    - File contains `close_pcornet_con()` (cleanup)
    - File contains `wb$save` (xlsx output)
    - File contains `cancer_summary_table_pre_post.xlsx` in output path (D-14)
  </acceptance_criteria>
  <done>
    R/58_cancer_summary_pre_post.R is a complete, self-contained script that:
    (1) Loads confirmed_hl_cohort.rds and cancer_summary.csv
    (2) Queries DIAGNOSIS via DuckDB for C-code rows (excluding C81 and D-codes)
    (3) Splits patient-code-date rows into pre-HL (<=), post-HL (>), and both sets
    (4) Aggregates baseline metrics (Total Patients, Confirmed 2+ Dates, Confirmed 7-Day Gap) plus pre/post/both counts and total records
    (5) Produces Category Summary and Code Summary sheets in cancer_summary_table_pre_post.xlsx with R/55 styling
    (6) All count columns use #,##0 format, no percentages anywhere, C81 excluded, D-codes excluded
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify R/58 execution on HiPerGator</name>
  <files>output/tables/cancer_summary_table_pre_post.xlsx</files>
  <action>
User runs R/58_cancer_summary_pre_post.R on HiPerGator and verifies output correctness. This is a human-verify checkpoint -- Claude cannot execute R scripts on HiPerGator.
  </action>
  <verify>User confirms all verification items below pass</verify>
  <what-built>R/58_cancer_summary_pre_post.R script producing cancer_summary_table_pre_post.xlsx with pre/post HL diagnosis counts for each cancer code (C81 excluded, D-codes excluded, counts only)</what-built>
  <how-to-verify>
    1. On HiPerGator, run: `Rscript R/58_cancer_summary_pre_post.R`
    2. Verify console output shows:
       - Confirmed HL cohort loaded with patient count
       - DIAGNOSIS query completed with row counts
       - Sentinel dates excluded count logged
       - Pre-HL and Post-HL row counts logged
       - Pre/Post/Both patient counts logged
       - Category and Code summary row counts logged
       - Output file path logged
    3. Open output/tables/cancer_summary_table_pre_post.xlsx and verify:
       - Sheet 1 "Category Summary" has columns: Cancer Site Category | Total Patients | Confirmed (2+ Dates) | Confirmed (7-Day Gap) | Pre-HL | Post-HL | Both | Total Records
       - Sheet 2 "Code Summary" has columns: ICD-10 Code | Cancer Site Category | Total Patients | Confirmed (2+ Dates) | Confirmed (7-Day Gap) | Pre-HL | Post-HL | Both | Total Records
       - No "Hodgkin Lymphoma" category row or C81* code rows appear
       - No D-code rows appear
       - No percentage columns (Rate, Mean, Median) appear
       - All count columns have comma-formatted integers
       - Dark gray headers, white font, totals row with light gray fill
       - Frozen header row
       - Pre-HL + Post-HL - Both roughly makes sense relative to Total Patients
    4. Verify companion CSV exists: output/tables/cancer_summary_table_pre_post.csv
  </how-to-verify>
  <acceptance_criteria>
    - Script runs without errors on HiPerGator
    - cancer_summary_table_pre_post.xlsx exists in output/tables/
    - Sheet 1 named "Category Summary" with 8 columns per D-06
    - Sheet 2 named "Code Summary" with 9 columns per D-07
    - No C81 or D-code rows in output
    - All values are integer counts (no percentages)
    - Pre-HL + Post-HL - Both is a reasonable number relative to Total Patients
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
  <done>User has verified R/58 execution on HiPerGator produces correct cancer_summary_table_pre_post.xlsx with all expected columns, no C81/D-code rows, counts only, and proper styling</done>
</task>

</tasks>

<verification>
- R/58_cancer_summary_pre_post.R runs without errors on HiPerGator
- cancer_summary_table_pre_post.xlsx has 2 sheets (Category Summary, Code Summary)
- No C81 rows in either sheet
- No D-code rows in either sheet
- All 8 columns in Category Summary per D-06 ordering
- All 9 columns in Code Summary per D-07 ordering
- Pre/Post/Both are integer counts (no percentages) per D-05
- Same-day handling: Pre includes same-day (<=), Post excludes (>) per D-08
- Sentinel dates excluded per D-11
- Styling matches R/55 per D-16
</verification>

<success_criteria>
1. R/58_cancer_summary_pre_post.R exists as a standalone script (does not modify R/55 or R/56 outputs per D-14)
2. cancer_summary_table_pre_post.xlsx exists with two sheets per D-15
3. D codes excluded (C codes only) -- satisfies ROADMAP SC-1
4. Population limited to confirmed 7-day HL patients -- satisfies ROADMAP SC-2
5. Pre-HL, Post-HL, and Both count columns present -- satisfies ROADMAP SC-3, SC-4, SC-5
6. All values are raw counts (no percentages) -- satisfies ROADMAP SC-6
7. C81 excluded from output per D-09
8. User approves execution output on HiPerGator
</success_criteria>

<output>
After completion, create `.planning/phases/58-cancer-summary-pre-post-hl-counts/58-01-SUMMARY.md`
</output>
