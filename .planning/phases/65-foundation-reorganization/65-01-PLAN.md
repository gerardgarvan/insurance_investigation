---
phase: 65-foundation-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/utils/utils_attrition.R
  - R/utils/utils_dates.R
  - R/utils/utils_icd.R
  - R/utils/utils_snapshot.R
  - R/utils/utils_duckdb.R
  - R/utils/utils_treatment.R
  - R/utils/utils_payer.R
  - R/utils/utils_pptx.R
  - R/03_duckdb_ingest.R
  - R/11_generate_pptx.R
  - R/19_flm_duplicate_dates.R
  - R/21_all_site_duplicate_dates.R
  - R/22a_multi_source_overlap_detection.R
  - R/22b_generate_phase19_20_pptx.R
  - R/24_per_patient_source_detection.R
  - R/25_duckdb_ingest.R
  - R/27_parity_test_cohort.R
  - R/28_benchmark_cohort.R
  - R/33_multi_source_overlap_av_th.R
  - R/49_gantt_data_export.R
  - R/59_death_date_validation.R
  - R/60_drug_name_resolution.R
  - R/61_episode_classification.R
  - R/62_first_line_and_death_analysis.R
  - R/63_gantt_v2_export.R
autonomous: true
requirements:
  - REORG-01
  - REORG-03
  - REORG-04

must_haves:
  truths:
    - "All 8 utils_*.R files exist in R/utils/ and none remain in R/ root"
    - "00_config.R dynamically auto-sources all files in R/utils/ via list.files()"
    - "25_duckdb_ingest.R is renamed to 03_duckdb_ingest.R"
    - "All source() calls referencing utils files point to R/utils/ paths"
    - "No deprecated foundation scripts exist (REORG-04 confirmed as N/A for Phase 65)"
  artifacts:
    - path: "R/utils/utils_dates.R"
      provides: "Date parsing utilities"
    - path: "R/utils/utils_attrition.R"
      provides: "Attrition logging utilities"
    - path: "R/utils/utils_icd.R"
      provides: "ICD code normalization"
    - path: "R/utils/utils_snapshot.R"
      provides: "Snapshot helper"
    - path: "R/utils/utils_duckdb.R"
      provides: "DuckDB backend abstraction"
    - path: "R/utils/utils_treatment.R"
      provides: "Treatment helpers"
    - path: "R/utils/utils_payer.R"
      provides: "Payer helpers"
    - path: "R/utils/utils_pptx.R"
      provides: "PPTX generation utilities"
    - path: "R/03_duckdb_ingest.R"
      provides: "DuckDB ingest (renumbered from 25)"
    - path: "R/00_config.R"
      provides: "Dynamic utils auto-sourcing"
      contains: "list.files"
  key_links:
    - from: "R/00_config.R"
      to: "R/utils/*.R"
      via: "list.files() + lapply(source)"
      pattern: 'list\.files.*R/utils.*lapply.*source'
    - from: "R/11_generate_pptx.R"
      to: "R/utils/utils_pptx.R"
      via: "direct source()"
      pattern: 'source.*R/utils/utils_pptx'
    - from: "R/03_duckdb_ingest.R"
      to: "R/00_config.R"
      via: "source() chain"
      pattern: 'source.*R/00_config'
---

<objective>
Move 8 utils_*.R modules into R/utils/ subfolder with dynamic auto-sourcing, renumber 25_duckdb_ingest.R to 03_duckdb_ingest.R, and update all 21 direct source() cross-references across 16 scripts.

Purpose: Establish the foundation file organization for the v2.0 decade-based numbering scheme (REORG-01) and create the R/utils/ subfolder structure (REORG-03) that subsequent phases build on.

Output: Reorganized R/ directory with utils/ subfolder, renumbered foundation script 03, and zero broken source() references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-foundation-reorganization/65-CONTEXT.md
@.planning/phases/65-foundation-reorganization/65-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/utils/ subfolder, move 8 utils files, replace auto-sourcing in 00_config.R with dynamic pattern</name>
  <files>
    R/utils/utils_attrition.R, R/utils/utils_dates.R, R/utils/utils_icd.R,
    R/utils/utils_snapshot.R, R/utils/utils_duckdb.R, R/utils/utils_treatment.R,
    R/utils/utils_payer.R, R/utils/utils_pptx.R, R/00_config.R
  </files>
  <read_first>
    R/00_config.R (lines 1495-1512 — current auto-source block to be replaced)
  </read_first>
  <action>
    **Step 1: Create R/utils/ directory**
    ```bash
    mkdir R/utils
    ```

    **Step 2: Move all 8 utils files using git mv (per D-02, keep utils_ prefix)**
    ```bash
    git mv R/utils_attrition.R R/utils/utils_attrition.R
    git mv R/utils_dates.R R/utils/utils_dates.R
    git mv R/utils_icd.R R/utils/utils_icd.R
    git mv R/utils_snapshot.R R/utils/utils_snapshot.R
    git mv R/utils_duckdb.R R/utils/utils_duckdb.R
    git mv R/utils_treatment.R R/utils/utils_treatment.R
    git mv R/utils_payer.R R/utils/utils_payer.R
    git mv R/utils_pptx.R R/utils/utils_pptx.R
    ```

    **Step 3: Replace 00_config.R lines 1495-1511 (per D-04)**

    Replace the existing auto-source block (lines 1495-1511):
    ```r
    # ------------------------------------------------------------------------------
    # 6. AUTO-SOURCE UTILITY FUNCTIONS
    # ------------------------------------------------------------------------------

    # Load date parsing and attrition logging utilities
    # These are sourced automatically when 00_config.R is loaded
    source("R/utils_dates.R")
    source("R/utils_attrition.R")
    source("R/utils_icd.R")
    source("R/utils_snapshot.R")  # Phase 16: snapshot helper
    source("R/utils_duckdb.R")    # Phase 30: backend abstraction helpers
    source("R/utils_treatment.R")  # Phase quick: shared treatment helpers
    source("R/utils_payer.R")      # Quick 260518-i3w: shared payer helpers

    # ==============================================================================
    # End of configuration
    # ==============================================================================
    ```

    With this dynamic auto-sourcing block:
    ```r
    # ------------------------------------------------------------------------------
    # 6. AUTO-SOURCE UTILITY FUNCTIONS
    # ------------------------------------------------------------------------------
    #
    # Load all utility modules from R/utils/ subfolder.
    # New utils files added in future phases are auto-discovered (D-04).

    utils_files <- list.files(
      path = "R/utils",
      pattern = "\\.R$",
      full.names = TRUE
    )

    if (length(utils_files) == 0) {
      warning("No utility files found in R/utils/ -- expected at least 8 modules")
    } else {
      invisible(lapply(utils_files, source))
      message(sprintf("Loaded %d utility modules from R/utils/", length(utils_files)))
    }

    # ==============================================================================
    # End of configuration
    # ==============================================================================
    ```

    IMPORTANT: The `full.names = TRUE` parameter is critical — without it, `list.files()` returns filenames only and `source()` will fail to find files.
  </action>
  <verify>
    <automated>grep -c "list.files" R/00_config.R</automated>
    Expect output: 1

    Also verify old explicit source lines are gone:
    ```bash
    grep -c "source(\"R/utils_dates.R\")" R/00_config.R
    ```
    Expect output: 0

    Verify all 8 files exist in R/utils/:
    ```bash
    ls R/utils/utils_*.R | wc -l
    ```
    Expect output: 8

    Verify no utils files remain in R/ root:
    ```bash
    ls R/utils_*.R 2>/dev/null | wc -l
    ```
    Expect output: 0
  </verify>
  <acceptance_criteria>
    - R/utils/ directory contains exactly 8 files: utils_attrition.R, utils_dates.R, utils_duckdb.R, utils_icd.R, utils_payer.R, utils_pptx.R, utils_snapshot.R, utils_treatment.R
    - R/ root contains zero files matching utils_*.R
    - R/00_config.R contains `list.files(` (dynamic sourcing)
    - R/00_config.R contains `path = "R/utils"` (correct path)
    - R/00_config.R contains `full.names = TRUE` (critical parameter)
    - R/00_config.R contains `invisible(lapply(utils_files, source))` (execution line)
    - R/00_config.R does NOT contain `source("R/utils_dates.R")` (old explicit line gone)
    - R/00_config.R does NOT contain `source("R/utils_attrition.R")` (old explicit line gone)
  </acceptance_criteria>
  <done>8 utils files moved to R/utils/, 00_config.R uses dynamic auto-sourcing via list.files(), no utils files remain in R/ root</done>
</task>

<task type="auto">
  <name>Task 2: Update all direct source() references to utils files across 16 scripts and renumber 25_duckdb_ingest.R to 03</name>
  <files>
    R/11_generate_pptx.R, R/19_flm_duplicate_dates.R, R/21_all_site_duplicate_dates.R,
    R/22a_multi_source_overlap_detection.R, R/22b_generate_phase19_20_pptx.R,
    R/24_per_patient_source_detection.R, R/25_duckdb_ingest.R, R/27_parity_test_cohort.R,
    R/28_benchmark_cohort.R, R/33_multi_source_overlap_av_th.R, R/49_gantt_data_export.R,
    R/59_death_date_validation.R, R/60_drug_name_resolution.R, R/61_episode_classification.R,
    R/62_first_line_and_death_analysis.R, R/63_gantt_v2_export.R, R/03_duckdb_ingest.R
  </files>
  <read_first>
    R/25_duckdb_ingest.R (lines 1-30 — header block with self-reference comment and source() calls)
    R/11_generate_pptx.R (line 90 — utils_pptx source)
    R/22b_generate_phase19_20_pptx.R (line 43 — utils_pptx source)
    R/19_flm_duplicate_dates.R (line 97 — conditional utils_dates source)
  </read_first>
  <action>
    **Step 1: Update all 19 direct source() calls to utils files across 16 scripts**

    Every occurrence of `source("R/utils_` must become `source("R/utils/utils_`. The complete list of changes (each is a single-line path prefix update):

    **utils_pptx.R callers (per D-01):**
    - R/11_generate_pptx.R line 90: `source("R/utils_pptx.R")` -> `source("R/utils/utils_pptx.R")`
    - R/22b_generate_phase19_20_pptx.R line 43: `source("R/utils_pptx.R")` -> `source("R/utils/utils_pptx.R")`

    **utils_dates.R callers:**
    - R/19_flm_duplicate_dates.R line 97: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")` (inside conditional block — also update the file.exists check on same/prior line from `"R/utils_dates.R"` to `"R/utils/utils_dates.R"`)
    - R/21_all_site_duplicate_dates.R line 175: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")` (also update any file.exists check for same path)
    - R/22a_multi_source_overlap_detection.R line 86: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")` (also update any file.exists check, and update comment on line 26 referencing the old path)
    - R/24_per_patient_source_detection.R line 89: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")` (also update any file.exists check, and update comment on line 22 referencing the old path)
    - R/33_multi_source_overlap_av_th.R line 118: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")` (also update any file.exists check, and update comment on line 27 referencing the old path)
    - R/49_gantt_data_export.R line 61: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")`
    - R/59_death_date_validation.R line 21: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")`
    - R/61_episode_classification.R line 59: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")`
    - R/62_first_line_and_death_analysis.R line 32: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")`
    - R/63_gantt_v2_export.R line 91: `source("R/utils_dates.R")` -> `source("R/utils/utils_dates.R")`

    **utils_duckdb.R callers:**
    - R/25_duckdb_ingest.R line 26: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`
    - R/27_parity_test_cohort.R line 59: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`
    - R/28_benchmark_cohort.R line 35: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`
    - R/49_gantt_data_export.R line 60: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`
    - R/59_death_date_validation.R line 20: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`
    - R/60_drug_name_resolution.R line 29: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`
    - R/61_episode_classification.R line 58: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`
    - R/62_first_line_and_death_analysis.R line 31: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`
    - R/63_gantt_v2_export.R line 90: `source("R/utils_duckdb.R")` -> `source("R/utils/utils_duckdb.R")`

    Also update comments that reference old paths:
    - R/22a line 26 comment: `R/utils_dates.R` -> `R/utils/utils_dates.R`
    - R/24 line 22 comment: `R/utils_dates.R` -> `R/utils/utils_dates.R`
    - R/33 line 27 comment: `R/utils_dates.R` -> `R/utils/utils_dates.R`

    **Step 2: Renumber 25_duckdb_ingest.R to 03_duckdb_ingest.R (per D-03)**
    ```bash
    git mv R/25_duckdb_ingest.R R/03_duckdb_ingest.R
    ```

    **Step 3: Update header in the renamed file**
    In R/03_duckdb_ingest.R (formerly R/25_duckdb_ingest.R):
    - Line 2: Change `# 25_duckdb_ingest.R -- Ingest PCORnet CDM tables from RDS cache into DuckDB` to `# 03_duckdb_ingest.R -- Ingest PCORnet CDM tables from RDS cache into DuckDB`
    - Line 10: Change `#   source("R/25_duckdb_ingest.R")` to `#   source("R/03_duckdb_ingest.R")`

    **Step 4: Confirm REORG-04 (deprecated scripts)**
    Verify no deprecated foundation scripts exist. Foundation scripts are 00_config.R, 01_load_pcornet.R, 02_harmonize_payer.R, 03_duckdb_ingest.R (renumbered). All are active. REORG-04 for foundation layer is confirmed N/A — no scripts to archive. Full archival happens in Phase 68.
  </action>
  <verify>
    <automated>grep -rn "source(\"R/utils_" R/*.R | grep -v "R/utils/" | grep -v "^#"</automated>
    Expect: NO output (all old-path source() calls are gone)

    Also verify renaming:
    ```bash
    test -f R/03_duckdb_ingest.R && echo "03 exists" || echo "03 MISSING"
    test ! -f R/25_duckdb_ingest.R && echo "25 removed" || echo "25 STILL EXISTS"
    ```
    Expect: "03 exists" and "25 removed"

    Count total source() calls to utils with new paths:
    ```bash
    grep -rn 'source("R/utils/utils_' R/*.R | wc -l
    ```
    Expect: 19 (the 19 direct callers now using new paths, NOT counting 00_config.R which uses dynamic sourcing)
  </verify>
  <acceptance_criteria>
    - `grep -rn 'source("R/utils_' R/*.R` returns zero matches (no old-style paths remain in executable code)
    - R/03_duckdb_ingest.R exists and contains `# 03_duckdb_ingest.R` in line 2
    - R/03_duckdb_ingest.R contains `source("R/utils/utils_duckdb.R")` (updated path)
    - R/25_duckdb_ingest.R does NOT exist
    - R/11_generate_pptx.R contains `source("R/utils/utils_pptx.R")`
    - R/22b_generate_phase19_20_pptx.R contains `source("R/utils/utils_pptx.R")`
    - R/19_flm_duplicate_dates.R contains `source("R/utils/utils_dates.R")`
    - `grep -c 'source("R/utils/utils_' R/*.R` returns 19 (all direct callers updated)
    - R/03_duckdb_ingest.R line 10 contains `source("R/03_duckdb_ingest.R")` (self-reference comment updated)
  </acceptance_criteria>
  <done>All 19 direct source() calls to utils files updated to R/utils/ paths, 25_duckdb_ingest.R renumbered to 03_duckdb_ingest.R with updated header, no broken references remain</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ls R/utils/utils_*.R | wc -l` returns 8
2. `ls R/utils_*.R 2>/dev/null | wc -l` returns 0
3. `grep -rn 'source("R/utils_' R/*.R` returns 0 matches
4. `grep 'list.files' R/00_config.R` returns 1 match
5. `test -f R/03_duckdb_ingest.R` succeeds
6. `test ! -f R/25_duckdb_ingest.R` succeeds
7. Foundation scripts exist at: R/00_config.R, R/01_load_pcornet.R, R/02_harmonize_payer.R, R/03_duckdb_ingest.R
</verification>

<success_criteria>
- 8 utils files live exclusively in R/utils/ (none in R/ root)
- 00_config.R auto-sources dynamically via list.files("R/utils")
- 25_duckdb_ingest.R renamed to 03_duckdb_ingest.R
- All 19 direct source() calls across 16 scripts point to R/utils/ paths
- Zero grep matches for old-style utils paths in executable R code
- REORG-04 confirmed N/A for foundation scripts (no deprecated scripts to archive)
</success_criteria>

<output>
After completion, create `.planning/phases/65-foundation-reorganization/65-01-SUMMARY.md`
</output>
