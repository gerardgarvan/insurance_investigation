---
phase: 147-read-address-zip5-retract-downstream-artefacts
plan: 04
type: execute
wave: 4
depends_on: [147-03]
files_modified:
  - .planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-DISCOVERY.md
  - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md
  - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md
  - data/reference/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All six retraction locations have a dated superseded-by-147 note at the top of the affected section"
    - "Original text is preserved exactly — no deletions"
    - "data/reference/README.md no longer states 77.7% is a ceiling or that zip5_modal zero is expected"
    - "147-CONTEXT.md §0 is annotated noting centroid crosswalk population changed"
  artifacts:
    - path: ".planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-DISCOVERY.md"
      provides: "Retraction notes on D-02 diagnostic and Branch C cause"
      contains: "Superseded by Phase 147"
    - path: ".planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md"
      provides: "Retraction notes on 77.7% ceiling and D-04 phantom population"
      contains: "Superseded by Phase 147"
    - path: "data/reference/README.md"
      provides: "Updated ceiling and zip5_modal notes with post-148 figures"
      contains: "Superseded by Phase 147"
    - path: ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md"
      provides: "Retraction note on §0 centroid crosswalk conclusion"
      contains: "Superseded by Phase 147"
  key_links:
    - from: "all retraction locations"
      to: "148-DISCOVERY.md"
      via: "see 148-DISCOVERY.md in each note"
      pattern: "148-DISCOVERY.md"
---

<objective>
Annotate six documented conclusions that were artefacts of the ADDRESS_ZIP5 omission. Each
retraction adds a dated note at the top of the affected section and points to 148-DISCOVERY.md.
Original text is preserved verbatim. data/reference/README.md's ceiling note and zip5_modal-zero
note are updated with the post-fix figures from Plan 3.

Purpose: A later reader must know these conclusions are superseded without having to already
know about Phase 147/148. The retraction note is a pointer, not a rewrite.
Output: Four files annotated; README.md updated with post-148 figures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md
@148-CONTEXT.md
@.planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retract Phase 145 findings in 145-DISCOVERY.md (2 locations)</name>
  <files>.planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-DISCOVERY.md</files>
  <read_first>
    - .planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-DISCOVERY.md — FULL FILE.
      Find the D-02 diagnostic section ("0 rows have ZIP5 present and ZIP9 missing") and the
      Branch C section ("sentinel-nulled"). Both need retraction notes. Read exact section headers
      before editing.
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md — D-05
      (retraction format: `**[2026-08-17] Superseded by Phase 147 — see 148-DISCOVERY.md.** [one-line summary]`)
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md — §4
      before/after table, so the one-line summary can reference the corrected figures.
  </read_first>
  <action>
Make two additive edits to 145-DISCOVERY.md. Do NOT delete or modify any existing text.

**RETRACTION 1 — D-02 diagnostic: "0 rows have ZIP5 present and ZIP9 missing"**

Find the section in 145-DISCOVERY.md that records the D-02 diagnostic result showing
"0 rows have ZIP5 present and ZIP9 missing". This is typically in a table or prose block
under a section about the ZIP5-modal investigation or the D-02 analysis.

At the TOP of that section (before the existing first line of the section), insert:

```
> **[2026-08-17] Superseded by Phase 147 — see 148-DISCOVERY.md.** The "0 rows" figure is
> an artefact: the diagnostic derived ZIP5 from ADDRESS_ZIP9, so the cell where ADDRESS_ZIP5
> is present and ADDRESS_ZIP9 is absent could structurally never be non-zero. ADDRESS_ZIP5
> has always been in the extract (18,731 records with ADDRESS_ZIP5 present and ADDRESS_ZIP9
> absent). After the Phase 147 fix, this cell is non-zero. See 148-DISCOVERY.md §1 for the
> corrected 2×2.
```

**RETRACTION 2 — Branch C cause: "sentinel-nulled"**

Find the section in 145-DISCOVERY.md that records the Branch C conclusion:
"approximable rows exist but all have ZIP5 = NA (sentinel-nulled)".

At the TOP of that section, insert:

```
> **[2026-08-17] Superseded by Phase 147 — see 148-DISCOVERY.md.** The "sentinel-nulled"
> cause is wrong. The ZIP5 was NA because ADDRESS_ZIP5 was never read, not because sentinel
> values were present. After the Phase 147 fix, zip5_modal fires for the first time (18,731
> previously-invisible records now have a valid ZIP5). See 148-DISCOVERY.md §2 (incorrect
> comment) and §4 (before/after table).
```
  </action>
  <verify>
    <automated>
grep -c "Superseded by Phase 147" ".planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-DISCOVERY.md"
# Must return 2

grep -c "148-DISCOVERY.md" ".planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-DISCOVERY.md"
# Must return >= 2

grep -c "sentinel-nulled" ".planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/145-DISCOVERY.md"
# Must return >= 1 (original text preserved — not deleted)
    </automated>
  </verify>
  <done>
    - Two retraction notes added; original text preserved in both sections
    - Each note contains "Superseded by Phase 147", "148-DISCOVERY.md", and a one-line summary
    - The "sentinel-nulled" text still appears in the file (original not deleted)
    - The "0 rows" finding text still appears in the file (original not deleted)
  </done>
</task>

<task type="auto">
  <name>Task 2: Retract Phase 146 findings in 146-CONTEXT.md and update data/reference/README.md (4 locations)</name>
  <files>
    .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md,
    data/reference/README.md
  </files>
  <read_first>
    - .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md — FULL FILE.
      Find: (a) §0 where "77.7% is a hard ceiling" appears, (b) §4 where "D-04 sentinel-ZIP lever,
      157,472 rows" appears. Read the exact section headers and surrounding text before editing.
    - data/reference/README.md — FULL FILE. Find: (a) the "77.7% ceiling" section (around line 96-100
      based on prior grep), (b) the "zip5_modal" section (around lines 184-211 based on prior grep).
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md — D-05
      (retraction format)
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md — §4
      before/after table with post-148 figures to use in the README update.
  </read_first>
  <action>
**146-CONTEXT.md — RETRACTION 3: "77.7% is a hard ceiling" (§0 and any ceiling references)**

Find the section in 146-CONTEXT.md §0 (and any related sections) where "77.7% is a hard ceiling
for every ZIP-keyed index" or similar text appears. At the TOP of that section, insert:

```
> **[2026-08-17] Superseded by Phase 147 — see 148-DISCOVERY.md.** The 77.7% figure reflects
> ADDRESS_ZIP9 coverage (50.1% of address records), not ZIP availability (96.9% of records
> carry a usable ZIP5 once ADDRESS_ZIP5 is read). The ceiling rises substantially after the
> Phase 147 fix. See 148-DISCOVERY.md §4 for the corrected before/after table.
```

**146-CONTEXT.md — RETRACTION 4: "D-04 sentinel-ZIP lever, 157,472 rows" (§4)**

Find the section in 146-CONTEXT.md §4 where "D-04 sentinel-ZIP lever, 157,472 rows" or the
phantom-population finding appears. At the TOP of that section, insert:

```
> **[2026-08-17] Superseded by Phase 147 — see 148-DISCOVERY.md.** The 157,472 figure is a
> phantom: those are empty ADDRESS_ZIP9 cells, not sentinel placeholder values. The sentinel
> query returns almost nothing. After the Phase 147 fix, Tier 3's actual population is
> zip5_no_zip9 from 148-DISCOVERY.md §4 (measured in Plan 3's HiPerGator re-run).
```

**data/reference/README.md — RETRACTION 5: "77.7% ceiling" section**

Find the paragraph around lines 96-100 containing "ADI ceiling (Phase 145, corrected 2026-08-17)"
and "77.7%". At the TOP of that section, insert:

```
> **[2026-08-17] Superseded by Phase 147 — see 148-DISCOVERY.md.** The 77.7% ceiling was
> computed when get_zip9_at_date() ignored ADDRESS_ZIP5. After the Phase 147 fix, RUCA
> coverage rises because RUCA joins on ZIP5. Updated figures: see 148-DISCOVERY.md §4.
```

Do NOT remove the existing text. The pre-fix figures are the historical record.

**data/reference/README.md — RETRACTION 6: "zip5_modal fires zero rows" section**

Find the paragraph around lines 184-211 containing "zip5_modal" and "expected, not a defect"
and "Branch C" and "157472". At the TOP of that section, insert:

```
> **[2026-08-17] Superseded by Phase 147 — see 148-DISCOVERY.md.** The zero-row figure was
> an artefact of ADDRESS_ZIP5 never being read, not a data characteristic. After the Phase 147
> fix, zip5_modal is non-zero for the first time (18,731 records now provide a ZIP5 for Tier 2).
> The "Branch C" console message was emitted because all approx candidates had ZIP5 = NA by
> construction. That console message has been corrected in utils_address.R. Updated figures:
> see 148-DISCOVERY.md §4.
```

Do NOT remove the existing text (the Branch C console message quote and the explanation are
historical record of how the column omission propagated).

**147-CONTEXT.md — RETRACTION 6 (self-retraction for §0)**

Read the 147-CONTEXT.md file (already in context). Find §0 where "Centroid crosswalk resolves
0 encounters" appears. At the TOP of §0, insert:

```
> **[2026-08-17] Superseded by Phase 147 — see 148-DISCOVERY.md.** The "0 encounters"
> figure is void: Tier 3's population (zip5_no_zip9) was 0 only because Tier 2 (zip5_modal)
> had no ZIP5 keys to match on. After the Phase 147 fix, Tier 2 receives 18,731 ZIP5 values.
> The actual Tier 3 population is recorded in 148-DISCOVERY.md §4. Phase 147's centroid
> crosswalk scope depends on that new figure.
```
  </action>
  <verify>
    <automated>
grep -c "Superseded by Phase 147" ".planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/146-CONTEXT.md"
# Must return >= 2

grep -c "Superseded by Phase 147" "data/reference/README.md"
# Must return >= 2

grep -c "Superseded by Phase 147" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md"
# Must return >= 1

grep -c "148-DISCOVERY.md" "data/reference/README.md"
# Must return >= 2

grep -c "157,472\|157472" "data/reference/README.md"
# Must return >= 1 (original text preserved)

grep -c "77.7" "data/reference/README.md"
# Must return >= 1 (original text preserved)
    </automated>
  </verify>
  <done>
    - 146-CONTEXT.md has retraction note in §0 (77.7% ceiling) and §4 (D-04 phantom)
    - data/reference/README.md has retraction note in the ceiling section and zip5_modal section
    - 147-CONTEXT.md §0 has the self-retraction note
    - All original text is preserved in all four files
    - All retraction notes contain "148-DISCOVERY.md" as the pointer
    - Total retraction notes across all files: 6 (2 in 145-DISCOVERY.md from Task 1 + 2 in 146-CONTEXT.md + 2 in README.md + 1 in 147-CONTEXT.md = 7, with 147-CONTEXT.md §0 being the 6th retraction location per D-05)
  </done>
</task>

</tasks>

<verification>
grep -rc "Superseded by Phase 147" .planning/phases/145-r116-fan-out-fix-and-ses-reference-gap-fill/
grep -rc "Superseded by Phase 147" .planning/phases/146-acquire-and-stage-the-ses-reference-files-sdi-svi-adi/
grep -c "Superseded by Phase 147" data/reference/README.md
grep -c "Superseded by Phase 147" .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md
</verification>

<success_criteria>
- 6 retraction notes total across 4 files (145-DISCOVERY.md x2, 146-CONTEXT.md x2, README.md x2,
  147-CONTEXT.md x1 — the 7th note is the 6th location from D-05 because README.md and Phase 145
  console note count as one location in D-05 but appear in two separate file sections).
- Every retraction note: (a) dated 2026-08-17, (b) says "Superseded by Phase 147", (c) references
  "148-DISCOVERY.md", (d) has a one-line summary of what changed.
- Every original text block is preserved (no deletions).
- data/reference/README.md retraction notes point to the post-148 figures in 148-DISCOVERY.md §4.
</success_criteria>

<output>
After completion, create `.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-04-SUMMARY.md`
</output>
