---
phase: 147-read-address-zip5-retract-downstream-artefacts
plan: 02
type: execute
wave: 2
depends_on: [147-01]
files_modified:
  - R/utils/utils_address.R
  - tests/fixtures/LDS_ADDRESS_HISTORY_Mailhot_V1.csv
  - tests/testthat/test-utils-address.R
autonomous: true
requirements: []
must_haves:
  truths:
    - "get_zip9_at_date() reads ADDRESS_ZIP5 using coalesce(normalize_zip5(ADDRESS_ZIP5), normalize_zip5(substr(ADDRESS_ZIP9, 1,5)))"
    - "The incorrect comment 'No separate ADDRESS_ZIP5 column exists' is removed from utils_address.R"
    - "The missing-column guard checks all five required columns and names Phase 148 in its error message"
    - "The fixture has four rows covering all 2x2 cells; a test asserts a ZIP5-only record returns non-NA ZIP5"
    - "R/88 parse checks pass on the Windows dev box"
  artifacts:
    - path: "R/utils/utils_address.R"
      provides: "Fixed get_zip9_at_date() reading ADDRESS_ZIP5"
      contains: "normalize_zip5(ADDRESS_ZIP5)"
    - path: "tests/fixtures/LDS_ADDRESS_HISTORY_Mailhot_V1.csv"
      provides: "Four-row fixture covering all 2x2 cells"
      contains: "ADDRESS_ZIP5"
    - path: "tests/testthat/test-utils-address.R"
      provides: "Test asserting ZIP5-only record yields non-NA ZIP5"
      contains: "ZIP5-only"
  key_links:
    - from: "tests/testthat/test-utils-address.R"
      to: "R/utils/utils_address.R"
      via: "addr_full injection seam (addr_full parameter)"
      pattern: "addr_full"
    - from: "R/utils/utils_address.R"
      to: "ADDRESS_ZIP5 column"
      via: "coalesce in mutate block"
      pattern: "normalize_zip5\\(ADDRESS_ZIP5\\)"
---

<objective>
Implement the ADDRESS_ZIP5 fix in get_zip9_at_date(). Three edits: (1) replace the ZIP5
coalesce logic in the mutate block, (2) extend the missing-column guard, (3) remove the
incorrect comment. Extend the fixture to cover all four 2x2 cells and add a test that
exercises the ZIP5-only path (ADDRESS_ZIP5 present, ADDRESS_ZIP9 absent).

Purpose: After this plan, zip5_modal will receive non-NA ZIP5 for 18,731 previously-invisible
records on HiPerGator.
Output: Three modified files; R/88 parse checks passing locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md
@148-CONTEXT.md
@.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md

<interfaces>
<!-- From R/utils/utils_address.R — key signatures executors must replicate/respect -->

normalize_zip5 <- function(zip9_clean) {
  str_sub(zip9_clean, 1, 5)
}
# Takes a CLEAN zip9 string (already normalized by normalize_zip9). Returns first 5 chars.
# IMPORTANT for this phase: normalize_zip5() also works on a raw ADDRESS_ZIP5 string
# because ADDRESS_ZIP5 is already 5 chars — str_sub(zip5, 1, 5) is identity.

normalize_zip5_raw <- function(zip, pad4 = FALSE) { ... }
# For bare ZIP5 strings — still needed as fallback for 291 ZIP9-only records.
# NOT used for ADDRESS_ZIP5 (it's already a clean 5-char string per the extract).

is_sentinel_zip5 <- function(zip5) {
  str_detect(zip5, "^(\\d)\\1{4}$")
}

# get_zip9_at_date() signature (unchanged):
get_zip9_at_date <- function(ids, dates, addr_full = NULL)
# Returns tibble: ID, query_date, ZIP9, ZIP5, match_type
# One row per distinct (ID, query_date) — Phase 145 stopifnot enforces this.

# Current zip5_norm computation (lines 153-162) — REPLACE THIS:
zip5_norm = coalesce(normalize_zip5(zip9_norm), normalize_zip5_raw(ADDRESS_ZIP9)),

# Current ID guard (lines 138-140):
if (!"ID" %in% names(addr_raw)) {
  stop(glue("[utils_address] LDS_ADDRESS_HISTORY missing required column 'ID'. Found: {paste(names(addr_raw), collapse=', ')}"))
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix get_zip9_at_date() — ZIP5 coalesce, column guard, incorrect comment</name>
  <files>R/utils/utils_address.R</files>
  <read_first>
    - R/utils/utils_address.R — full file, particularly lines 111-173 (get_zip9_at_date load block,
      ID guard at ~138-140, mutate block at ~151-173 with the zip5_norm line at ~160 and the
      incorrect comment at ~157-159). Read the full function before editing.
    - 148-CONTEXT.md §3 — exact required code for the coalesce expression and the guard's
      required error message (verbatim).
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md — D-02
      (coalesce order), D-03 (guard message wording).
  </read_first>
  <behavior>
    - A record with ADDRESS_ZIP5="32611" and ADDRESS_ZIP9=NA returns ZIP5="32611" (not NA)
    - A record with ADDRESS_ZIP5="32611" and ADDRESS_ZIP9="326114567" returns ZIP5="32611"
      (ADDRESS_ZIP5 wins)
    - A record with ADDRESS_ZIP5=NA and ADDRESS_ZIP9="326229999" returns ZIP5="32622"
      (fallback path still works)
    - A record with ADDRESS_ZIP5=NA and ADDRESS_ZIP9=NA returns ZIP5=NA
    - Missing ADDRESS_ZIP5 column in addr_raw triggers stop() with message containing
      "ADDRESS_ZIP5" and "Phase 148"
  </behavior>
  <action>
Make three targeted edits to R/utils/utils_address.R inside get_zip9_at_date():

**EDIT 1 — Replace the missing-column guard (lines ~138-140 area)**

Replace the single-column ID guard:
```r
if (!"ID" %in% names(addr_raw)) {
  stop(glue("[utils_address] LDS_ADDRESS_HISTORY missing required column 'ID'. Found: {paste(names(addr_raw), collapse=', ')}"))
}
```

With a multi-column guard (place immediately after the existing ID check OR replace it entirely
with this single guard that covers all required columns):
```r
required_cols <- c("ID", "ADDRESS_ZIP5", "ADDRESS_ZIP9",
                   "ADDRESS_PERIOD_START", "ADDRESS_PERIOD_END")
missing_cols  <- setdiff(required_cols, names(addr_raw))
if (length(missing_cols) > 0) {
  stop("[utils_address] address file missing required column(s): ",
       paste(missing_cols, collapse = ", "),
       ". ADDRESS_ZIP5 was added as a ZIP5 source in Phase 148; an extract without it ",
       "would silently fall back to deriving ZIP5 from ADDRESS_ZIP9 and lose ~47% of ZIP5 coverage.")
}
```

**EDIT 2 — Remove the incorrect comment and fix the zip5_norm coalesce (lines ~153-162)**

Remove these lines entirely (the incorrect comment + old coalesce):
```r
      # Phase 139 AMEND-01 / Step 0c: coalesce ZIP5 from two sources:
      #   (1) normalize_zip5() of a valid 9-digit ZIP9 (existing path)
      #   (2) normalize_zip5_raw() applied directly to ADDRESS_ZIP9 for rows where
      #       ADDRESS_ZIP9 holds a bare 5-digit string that normalize_zip9() rejects
      # No separate ADDRESS_ZIP5 column exists in LDS_ADDRESS_HISTORY (confirmed from
      # plan analysis: ADDRESS_ZIP9 holds bare ZIP5s for some records -- Step 0a case 2).
      zip5_norm       = coalesce(normalize_zip5(zip9_norm), normalize_zip5_raw(ADDRESS_ZIP9)),
```

Replace with:
```r
      # Phase 148 D-02: ZIP5 sourced from ADDRESS_ZIP5 first (the field the source
      # system populated deliberately), then from the ZIP9 prefix (fallback for the
      # 291 ZIP9-only records). ADDRESS_ZIP5 wins the 12 prefix-disagreement rows
      # (0.061% of dual-populated records). See 147-DISCOVERY.md §4 for the decision
      # and §3 for the 12 disagreement rows.
      zip5_norm       = dplyr::coalesce(
        normalize_zip5(ADDRESS_ZIP5),
        normalize_zip5(zip9_norm)
      ),
```

Note: `normalize_zip5(ADDRESS_ZIP5)` calls `str_sub(ADDRESS_ZIP5, 1, 5)` — since ADDRESS_ZIP5
is already a 5-char string in the extract, this is correct and does NOT need normalize_zip5_raw().
The normalize_zip5_raw() fallback on ADDRESS_ZIP9 is replaced by `normalize_zip5(zip9_norm)`
(the already-normalized ZIP9 prefix path), which handles the 291 ZIP9-only rows.

**EDIT 3 — Remove the Branch C console note in approximate_zip9()**

Search for the string "sentinel-nulled" OR "Branch C" in utils_address.R (it appears in the
approximate_zip9() function, which emits a console message saying the wrong cause). Find the
message() or cat() call containing the Branch C explanation and remove it or replace it with:
```r
message(glue("[approximate_zip9] {n_to_approx} row(s) remain with ZIP5 = NA after coalescing ADDRESS_ZIP5 and ADDRESS_ZIP9 prefix; zip5_modal tier will report zero rows for these."))
```
If no such message() call exists (it may have been in a comment only), remove the comment.

**VERIFY brace/paren balance before committing:**
After all three edits, do a character count of `(`, `)`, `{`, `}` in utils_address.R to
confirm balance has not changed from the pre-edit counts (or changed by an equal number of
open/close). This is especially important given the prior-known pre-existing imbalance noted
in 140-07-SUMMARY.md.
  </action>
  <verify>
    <automated>
grep -n "normalize_zip5(ADDRESS_ZIP5)" R/utils/utils_address.R
# Must return one hit inside the mutate block of get_zip9_at_date()

grep -c "No separate ADDRESS_ZIP5 column exists" R/utils/utils_address.R
# Must return 0

grep -c "ADDRESS_ZIP5" R/utils/utils_address.R
# Must return >= 3 (guard, coalesce, comment)

grep -c "Phase 148" R/utils/utils_address.R
# Must return >= 2 (guard message + coalesce comment)

grep -c "ADDRESS_PERIOD_END" R/utils/utils_address.R
# Must return >= 2 (guard + existing period_end_dt usage — confirms guard was added)
    </automated>
  </verify>
  <done>
    - The multi-column guard checks ID, ADDRESS_ZIP5, ADDRESS_ZIP9, ADDRESS_PERIOD_START, ADDRESS_PERIOD_END
    - The guard's error message contains "ADDRESS_ZIP5" and "Phase 148"
    - zip5_norm = dplyr::coalesce(normalize_zip5(ADDRESS_ZIP5), normalize_zip5(zip9_norm)) is in the mutate block
    - The incorrect comment ("No separate ADDRESS_ZIP5 column exists") is gone
    - The Phase 148 comment above the coalesce references 147-DISCOVERY.md §4
    - Branch C / sentinel-nulled console message is removed or corrected
    - Brace/paren balance check performed and documented
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend fixture to 4 rows and add ZIP5-only test</name>
  <files>tests/fixtures/LDS_ADDRESS_HISTORY_Mailhot_V1.csv, tests/testthat/test-utils-address.R</files>
  <read_first>
    - tests/fixtures/LDS_ADDRESS_HISTORY_Mailhot_V1.csv — current content (1 row, 3 columns: ID, ADDRESS_ZIP9, ADDRESS_PERIOD_START)
    - tests/testthat/test-utils-address.R — existing tests using addr_full seam; read to understand fixture shape and assertion patterns
    - 148-CONTEXT.md §2 (rule 2: fixture must carry all four 2x2 cells) and §7 (anti-pattern: testing against the old fixture)
    - R/utils/utils_address.R — after Task 1's edits, to confirm the function signature and column requirements
  </read_first>
  <behavior>
    - Test: addr_full with a row where ADDRESS_ZIP5="32615" and ADDRESS_ZIP9=NA (ZIP5-only cell)
      → get_zip9_at_date() returns ZIP5="32615", ZIP9=NA, match_type="interval"
    - Test: addr_full with a row where ADDRESS_ZIP5=NA and ADDRESS_ZIP9="326229999" (ZIP9-only cell)
      → returns ZIP5="32622" (from ZIP9 prefix fallback), ZIP9="326229999"
    - Test: addr_full with a row where ADDRESS_ZIP5="32611" and ADDRESS_ZIP9="326114567" (both present)
      → returns ZIP5="32611" (ADDRESS_ZIP5 wins)
    - Test: addr_full with a row where ADDRESS_ZIP5=NA and ADDRESS_ZIP9=NA (neither)
      → returns ZIP5=NA, ZIP9=NA
  </behavior>
  <action>
**Part A — Rewrite tests/fixtures/LDS_ADDRESS_HISTORY_Mailhot_V1.csv**

Replace the single-row file with a four-row file covering all 2x2 cells from 148-CONTEXT.md §0.
The file must have these columns: ID, ADDRESS_ZIP5, ADDRESS_ZIP9, ADDRESS_PERIOD_START.
(ADDRESS_PERIOD_END is optional in the fixture — open-ended is allowed; the guard requires
the column exist in addr_raw but the fixture is passed via addr_full which bypasses the guard
in the existing test. However, to allow direct CSV-path tests in future, add ADDRESS_PERIOD_END
as well with blank values for open-ended records.)

New file content (write exactly):
```
ID,ADDRESS_ZIP5,ADDRESS_ZIP9,ADDRESS_PERIOD_START,ADDRESS_PERIOD_END
P1,32611,326114567,2020-01-01,
P2,32615,,2020-01-01,
P3,,326229999,2020-01-01,
P4,,,2020-01-01,
```

Row P1: both ADDRESS_ZIP5 and ADDRESS_ZIP9 present (2x2 cell: yes/yes)
Row P2: ADDRESS_ZIP5 present, ADDRESS_ZIP9 absent (2x2 cell: yes/no — the 18,731 invisible records)
Row P3: ADDRESS_ZIP5 absent, ADDRESS_ZIP9 present (2x2 cell: no/yes — the 291 fallback records)
Row P4: both absent (2x2 cell: no/no — genuinely unusable)

**Part B — Add tests to tests/testthat/test-utils-address.R**

Append a new test block after the existing tests (do NOT modify existing tests). Add:

```r
# ==============================================================================
# Phase 147 / 148: ADDRESS_ZIP5 column fix
# Verifies all four 2x2 cells from 148-CONTEXT.md §0
# ==============================================================================

test_that("get_zip9_at_date reads ADDRESS_ZIP5 directly (ZIP5-only record, 2x2 yes/no cell)", {
  # 18,731 records in the real extract have ADDRESS_ZIP5 present and ADDRESS_ZIP9 absent.
  # Before the fix these returned ZIP5 = NA. After the fix they must return a valid ZIP5.
  addr_zip5_only <- tibble::tibble(
    ID                   = "P_zip5only",
    ADDRESS_ZIP5         = "32615",
    ADDRESS_ZIP9         = NA_character_,
    ADDRESS_PERIOD_START = "2020-01-01",
    ADDRESS_PERIOD_END   = NA_character_
  )
  result <- get_zip9_at_date("P_zip5only", as.Date("2020-06-15"), addr_full = addr_zip5_only)
  expect_equal(nrow(result), 1L)
  expect_false(is.na(result$ZIP5), label = "ZIP5-only record must yield non-NA ZIP5 after Phase 148 fix")
  expect_equal(result$ZIP5, "32615")
  expect_true(is.na(result$ZIP9), label = "ZIP9 stays NA when ADDRESS_ZIP9 is absent")
  expect_equal(result$match_type, "interval")
})

test_that("get_zip9_at_date ADDRESS_ZIP5 wins disagreement (2x2 yes/yes cell)", {
  # ADDRESS_ZIP5 takes precedence over the ZIP9 prefix when both are present.
  addr_both <- tibble::tibble(
    ID                   = "P_both",
    ADDRESS_ZIP5         = "32611",
    ADDRESS_ZIP9         = "326114567",
    ADDRESS_PERIOD_START = "2020-01-01",
    ADDRESS_PERIOD_END   = NA_character_
  )
  result <- get_zip9_at_date("P_both", as.Date("2020-06-15"), addr_full = addr_both)
  expect_equal(result$ZIP5, "32611")
  expect_equal(result$ZIP9, "326114567")
})

test_that("get_zip9_at_date ZIP9-prefix fallback works when ADDRESS_ZIP5 absent (2x2 no/yes cell)", {
  # 291 records in the real extract have ADDRESS_ZIP9 only. ZIP5 must come from ZIP9 prefix.
  addr_zip9_only <- tibble::tibble(
    ID                   = "P_zip9only",
    ADDRESS_ZIP5         = NA_character_,
    ADDRESS_ZIP9         = "326229999",
    ADDRESS_PERIOD_START = "2020-01-01",
    ADDRESS_PERIOD_END   = NA_character_
  )
  result <- get_zip9_at_date("P_zip9only", as.Date("2020-06-15"), addr_full = addr_zip9_only)
  expect_equal(result$ZIP5, "32622")
  expect_equal(result$ZIP9, "326229999")
})

test_that("get_zip9_at_date returns NA ZIP5 when both columns absent (2x2 no/no cell)", {
  addr_neither <- tibble::tibble(
    ID                   = "P_neither",
    ADDRESS_ZIP5         = NA_character_,
    ADDRESS_ZIP9         = NA_character_,
    ADDRESS_PERIOD_START = "2020-01-01",
    ADDRESS_PERIOD_END   = NA_character_
  )
  result <- get_zip9_at_date("P_neither", as.Date("2020-06-15"), addr_full = addr_neither)
  expect_true(is.na(result$ZIP5))
  expect_true(is.na(result$ZIP9))
})
```
  </action>
  <verify>
    <automated>
head -1 tests/fixtures/LDS_ADDRESS_HISTORY_Mailhot_V1.csv
# Must contain ADDRESS_ZIP5 as a column header

grep -c "^P" tests/fixtures/LDS_ADDRESS_HISTORY_Mailhot_V1.csv
# Must return 4 (four data rows)

grep -c "ZIP5-only record" tests/testthat/test-utils-address.R
# Must return >= 1

grep -c "test_that" tests/testthat/test-utils-address.R
# Must return >= 6 (2 existing + 4 new)

grep "32615" tests/testthat/test-utils-address.R
# Must appear in the ZIP5-only test assertion
    </automated>
  </verify>
  <done>
    - Fixture has 5 columns (ID, ADDRESS_ZIP5, ADDRESS_ZIP9, ADDRESS_PERIOD_START, ADDRESS_PERIOD_END)
    - Fixture has exactly 4 data rows covering all 2x2 cells
    - test-utils-address.R has 4 new tests, one per 2x2 cell, all using addr_full seam
    - The ZIP5-only test asserts result$ZIP5 == "32615" and !is.na(result$ZIP5)
    - Existing tests (Task 2 of 140-07) are untouched
  </done>
</task>

</tasks>

<verification>
grep -n "normalize_zip5(ADDRESS_ZIP5)" R/utils/utils_address.R
grep -c "No separate ADDRESS_ZIP5 column exists" R/utils/utils_address.R
grep -c "Phase 148" R/utils/utils_address.R
head -1 tests/fixtures/LDS_ADDRESS_HISTORY_Mailhot_V1.csv
grep -c "ZIP5-only" tests/testthat/test-utils-address.R
</verification>

<success_criteria>
- utils_address.R: normalize_zip5(ADDRESS_ZIP5) appears in the mutate block; incorrect comment gone;
  multi-column guard references Phase 148; Branch C message removed or corrected.
- Fixture: ADDRESS_ZIP5 column present; 4 data rows.
- Tests: 4 new tests covering all 2x2 cells; ZIP5-only test asserts non-NA ZIP5 from ADDRESS_ZIP5.
- R/88 parse checks pass on Windows dev box (no Rscript; structural validation only).
</success_criteria>

<output>
After completion, create `.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-02-SUMMARY.md`
</output>
