---
phase: 147-read-address-zip5-retract-downstream-artefacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "148-DISCOVERY.md records names() of the real address file and lists every ZIP-ish column"
    - "The raw-column 2x2 table (ADDRESS_ZIP5 vs ADDRESS_ZIP9) is recorded with four cells totalling 40,005 records"
    - "All 12 prefix-disagreement rows are printed in 148-DISCOVERY.md"
    - "The D-02 precedence decision (ADDRESS_ZIP5 wins) is recorded with rationale"
  artifacts:
    - path: ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md"
      provides: "Column audit, 2x2 table, 12-row disagreement table, D-02 decision"
      min_lines: 50
  key_links:
    - from: "148-DISCOVERY.md"
      to: "R/utils/utils_address.R Plan 2 change"
      via: "D-02 precedence locked before code is touched"
      pattern: "ADDRESS_ZIP5 wins"
---

<objective>
Produce 148-DISCOVERY.md before any code is modified. Record the names() audit proving ADDRESS_ZIP5
exists, the raw-column 2x2 cross-tabulation, the 12 prefix-disagreement rows, and the locked D-02
precedence decision. This document is the evidentiary foundation for Plan 2's code change.

Purpose: Prevent the same column-blindness defect from recurring. The fix must be preceded by
documented proof of what the extract contains.
Output: 148-DISCOVERY.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@148-CONTEXT.md
@.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 148-DISCOVERY.md — column audit, 2x2, disagreements, D-02 decision</name>
  <files>.planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md</files>
  <read_first>
    - 148-CONTEXT.md — §0 (the 2x2 measurement), §3 (D-02 and D-03 change spec, disagreement-print code), §7 (anti-patterns)
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md — D-02 (precedence), D-05 (retraction format)
    - R/utils/utils_address.R — lines 111-173 (get_zip9_at_date load block + mutate block, to see exactly what is currently read and what the incorrect comment says)
  </read_first>
  <action>
Create `.planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md`
with the following sections:

**§0 — Column Audit**

State that this document was produced by Phase 147 Plan 1 (wave 1) before any code change to
`get_zip9_at_date()`. Record:

```
names(LDS_ADDRESS_HISTORY_Mailhot_V1.csv) [from vroom with .default="c"]:
  Confirmed ZIP-ish columns: ADDRESS_ZIP5, ADDRESS_ZIP9
  Other columns present: ID, ADDRESS_PERIOD_START, ADDRESS_PERIOD_END [plus any others]
```

NOTE: Because the real HiPerGator file is not available in the Windows dev environment,
this section records what the CONTEXT docs assert the extract contains. State explicitly
that the names() output is from 148-CONTEXT.md §0 (pre-run documentation) and will be
confirmed against the real file in Plan 3's HiPerGator run. List the asserted columns.

**§1 — Raw-column 2x2 (from 148-CONTEXT.md §0)**

Record the pre-fix measurement exactly as documented in 148-CONTEXT.md §0:

| ADDRESS_ZIP5 present | ADDRESS_ZIP9 present | records | share | note |
|---|---|---|---|---|
| yes | yes | 19,741 | 49.3% | both sources available |
| yes | no | 18,731 | 46.8% | invisible to current code |
| no | yes | 291 | 0.7% | ZIP5 recoverable from ZIP9 prefix |
| no | no | 1,242 | 3.1% | genuinely unusable |
| (total) | | 40,005 | | |

Records with a usable ZIP5 today (code path): 20,032 (50.1%)
Records with a usable ZIP5 after the fix: 38,763 (96.9%) (+46.8 pp)

Character-length profile: ADDRESS_ZIP5 is 5 chars or NA; ADDRESS_ZIP9 is 9 chars or NA.
No truncation.

**§2 — The incorrect comment**

Quote the exact text from utils_address.R lines 157-159:
```
# No separate ADDRESS_ZIP5 column exists in LDS_ADDRESS_HISTORY (confirmed from
# plan analysis: ADDRESS_ZIP9 holds bare ZIP5s for some records -- Step 0a case 2).
```
State: This comment is factually wrong. ADDRESS_ZIP5 has always been in the extract.
Plan 2 will remove this comment.

**§3 — Prefix-disagreement rows (the 12 rows)**

Per 148-CONTEXT.md §3, in 12 of 19,741 rows where both ADDRESS_ZIP5 and ADDRESS_ZIP9 are
present, `substr(ADDRESS_ZIP9, 1, 5) != ADDRESS_ZIP5`. These 12 rows represent 0.061% of
rows with both columns populated.

Because the real data is not accessible from the Windows dev box, state:
"The 12 disagreement rows are not printable from this planning environment. Their existence is
documented in 148-CONTEXT.md §3. Plan 3 (HiPerGator re-run) must execute the following query
against the real extract and paste the result here before this section is considered complete:

```r
addr |>
  dplyr::filter(!is.na(ADDRESS_ZIP5), !is.na(ADDRESS_ZIP9),
                substr(ADDRESS_ZIP9, 1, 5) != ADDRESS_ZIP5) |>
  dplyr::select(ID, ADDRESS_ZIP5, ADDRESS_ZIP9, ADDRESS_PERIOD_START) |>
  print(n = 12)
```

STATUS: PLACEHOLDER — to be filled in by Plan 3."

**§4 — D-02 Precedence Decision**

Record:
```
D-02: ADDRESS_ZIP5 WINS disagreements.

Coalesce order:
  zip5 = dplyr::coalesce(
    normalize_zip5(ADDRESS_ZIP5),
    normalize_zip5(substr(ADDRESS_ZIP9, 1, 5))
  )

Rationale:
- ADDRESS_ZIP5 is the field the source system populated deliberately as a ZIP5.
- ADDRESS_ZIP9 is a ZIP+4 field; extracting its first 5 digits to derive ZIP5 is a
  fallback, not an authoritative source.
- The 0.061% disagreement rate (12/19,741 rows) does not affect any downstream result.
- The 12 disagreement rows have not yet been inspected (HiPerGator-only). If Plan 3's
  inspection reveals a systematic pattern suggesting ADDRESS_ZIP9 is more reliable
  (e.g., transposition errors in ADDRESS_ZIP5), this decision must be revisited and
  documented here.
- The provisional decision is: ADDRESS_ZIP5 wins. Plan 2 implements this.
```

**§5 — R/116 diagnostic note (D-04 per 148-CONTEXT.md §3)**

State: R/116's pre-approximation table is `table(match_type, is.na(ZIP9), is.na(ZIP5))`.
This produced a misleading zero in the "ZIP5 present, ZIP9 absent" cell because ZIP5 was
derived from ZIP9, so that cell structurally could not be non-zero. After Plan 2's fix,
this table will change. 148-DISCOVERY.md distinguishes:
- The raw-column 2x2 (§1 above): what the extract CONTAINS
- The match_type table: what the resolver PRODUCES (encounter-level, after date matching)
These must not be conflated. The resolver's output depends on which records match which
encounter dates, so the record-level 2x2 does not predict the encounter-level breakdown.
  </action>
  <verify>
    <automated>
grep -c "ADDRESS_ZIP5" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md"
# Must return >= 5

grep -c "19,741\|18,731\|40,005" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md"
# Must return >= 3 (all three counts from 2x2 present)

grep -c "D-02\|ADDRESS_ZIP5 wins\|PLACEHOLDER" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md"
# Must return >= 3
    </automated>
  </verify>
  <done>
    - 148-DISCOVERY.md exists in the phase directory
    - §0 column audit records ADDRESS_ZIP5 and ADDRESS_ZIP9 as confirmed ZIP-ish columns
    - §1 2x2 table has all four cells with records summing to 40,005
    - §2 quotes the incorrect comment verbatim from utils_address.R
    - §3 disagreement section has the R query to run in Plan 3 and is marked PLACEHOLDER
    - §4 D-02 records ADDRESS_ZIP5-wins decision with rationale and the provisional caveat
    - §5 distinguishes raw-column 2x2 from match_type table
  </done>
</task>

</tasks>

<verification>
grep -c "40,005" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md"
grep -c "PLACEHOLDER" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md"
grep "D-02" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/148-DISCOVERY.md"
</verification>

<success_criteria>
148-DISCOVERY.md exists, contains the 2x2 with 40,005 total, the incorrect-comment quote,
the §3 PLACEHOLDER for Plan 3 to fill, and the D-02 locked decision with rationale.
The file is the authoritative pre-change record that makes Plan 2 auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-01-SUMMARY.md`
</output>
