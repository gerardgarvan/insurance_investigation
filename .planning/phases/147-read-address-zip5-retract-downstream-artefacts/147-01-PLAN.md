---
phase: 147-read-address-zip5-retract-downstream-artefacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "147-DISCOVERY.md records names() of the real address file and lists every ZIP-ish column"
    - "The raw-column 2x2 table (ADDRESS_ZIP5 vs ADDRESS_ZIP9) is recorded with four cells totalling 40,005 records"
    - "All 12 prefix-disagreement rows are printed in 147-DISCOVERY.md from a real HiPerGator run, BEFORE Plan 2 writes any code"
    - "names() of the real extract is printed and recorded — the D-01 audit that has never been run"
    - "D-02 is LOCKED (confirmed or flipped) on the evidence of those 12 rows, not left provisional for Plan 3"
  artifacts:
    - path: ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md"
      provides: "Column audit, 2x2 table, 12-row disagreement table, D-02 decision"
      min_lines: 50
  key_links:
    - from: "147-DISCOVERY.md"
      to: "R/utils/utils_address.R Plan 2 change"
      via: "D-02 precedence locked before code is touched"
      pattern: "ADDRESS_ZIP5 wins"
---

<objective>
Produce 147-DISCOVERY.md before any code is modified. Record the names() audit proving ADDRESS_ZIP5
exists, the raw-column 2x2 cross-tabulation, the 12 prefix-disagreement rows, and the locked D-02
precedence decision. This document is the evidentiary foundation for Plan 2's code change.

Purpose: Prevent the same column-blindness defect from recurring. The fix must be preceded by
documented proof of what the extract contains.
Output: 147-DISCOVERY.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@148-CONTEXT.md
@.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 147-DISCOVERY.md — column audit, 2x2, disagreements, D-02 decision</name>
  <files>.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md</files>
  <read_first>
    - 148-CONTEXT.md — §0 (the 2x2 measurement), §3 (D-02 and D-03 change spec, disagreement-print code), §7 (anti-patterns)
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md — D-02 (precedence), D-05 (retraction format)
    - R/utils/utils_address.R — lines 111-173 (get_zip9_at_date load block + mutate block, to see exactly what is currently read and what the incorrect comment says)
  </read_first>
  <action>
Create `.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md`
with the following sections:

**§0 — Column Audit**

State that this document was produced by Phase 147 Plan 1 (wave 1) before any code change to
`get_zip9_at_date()`. Record:

```
names(LDS_ADDRESS_HISTORY_Mailhot_V1.csv) [from vroom with .default="c"]:
  Confirmed ZIP-ish columns: ADDRESS_ZIP5, ADDRESS_ZIP9
  Other columns present: ID, ADDRESS_PERIOD_START, ADDRESS_PERIOD_END [plus any others]
```

NOTE ON PROVENANCE — do not mislabel this. The 2x2 in §1 and the character-length profile
are **HiPerGator output already run and pasted by the user**, not assertions from a document.
Record them as measured, with the date. What is genuinely NOT yet verified is the full
`names()` listing of the extract — no one has printed it. That is the one gap, and Task 2
closes it before any code is written (D-01 requires the audit BEFORE the fix, not after).

**§1 — Raw-column 2x2 (measured on HiPerGator 2026-08-17)**

Record the pre-fix measurement. Source: `addr |> count(zip5_ok = !is.na(ADDRESS_ZIP5),
zip9_ok = !is.na(ADDRESS_ZIP9))` run against the real extract; output pasted by the user and
transcribed into 148-CONTEXT.md §0. These are measured values, not assumptions:

| ADDRESS_ZIP5 present | ADDRESS_ZIP9 present | records | share | note |
|---|---|---|---|---|
| yes | yes | 19,741 | 49.3% | both sources available |
| yes | no | 18,731 | 46.8% | invisible to current code |
| no | yes | 291 | 0.7% | ZIP5 recoverable from ZIP9 prefix |
| no | no | 1,242 | 3.1% | genuinely unusable |
| (total) | | 40,005 | | |

Records with a usable ZIP5 today (code path): 20,032 (50.1%)
Records with a usable ZIP5 after the fix: 38,763 (96.9%) (+46.8 pp)

Character-length profile: ADDRESS_ZIP5 is 5 chars or NA; ADDRESS_ZIP9 is 9 chars or NA.
No truncation.

**§2 — The incorrect comment**

Quote the exact text from utils_address.R lines 157-159:
```
# No separate ADDRESS_ZIP5 column exists in LDS_ADDRESS_HISTORY (confirmed from
# plan analysis: ADDRESS_ZIP9 holds bare ZIP5s for some records -- Step 0a case 2).
```
State: This comment is factually wrong. ADDRESS_ZIP5 has always been in the extract.
Plan 2 will remove this comment.

**§3 — Prefix-disagreement rows (the 12 rows)**

Per 148-CONTEXT.md §3, in 12 of 19,741 rows where both ADDRESS_ZIP5 and ADDRESS_ZIP9 are
present, `substr(ADDRESS_ZIP9, 1, 5) != ADDRESS_ZIP5`. These 12 rows represent 0.061% of
rows with both columns populated.

Leave this section as a stub headed "filled by Task 2 below". The rows are printed in Task 2,
NOT deferred to Plan 3 — see the wave-order note in that task.

**§4 — D-02 Precedence Decision**

Record:
```
D-02: ADDRESS_ZIP5 WINS disagreements.

Coalesce order:
  zip5 = dplyr::coalesce(
    normalize_zip5(ADDRESS_ZIP5),
    normalize_zip5(substr(ADDRESS_ZIP9, 1, 5))
  )

Rationale:
- ADDRESS_ZIP5 is the field the source system populated deliberately as a ZIP5.
- ADDRESS_ZIP9 is a ZIP+4 field; extracting its first 5 digits to derive ZIP5 is a
  fallback, not an authoritative source.
- The 0.061% disagreement rate (12/19,741 rows) does not affect any downstream result.
- The 12 disagreement rows are inspected in Task 2 of THIS plan, before Plan 2 writes any
  code. If they show a systematic pattern suggesting ADDRESS_ZIP9 is more reliable (one
  transposed digit, a PO-box pairing, a single source system), flip the precedence HERE and
  record the reason. Plan 2 implements whatever this section concludes.
- Provisional pending Task 2: ADDRESS_ZIP5 wins.
```

**§5 — R/116 diagnostic note (D-04 per 148-CONTEXT.md §3)**

State: R/116's pre-approximation table is `table(match_type, is.na(ZIP9), is.na(ZIP5))`.
This produced a misleading zero in the "ZIP5 present, ZIP9 absent" cell because ZIP5 was
derived from ZIP9, so that cell structurally could not be non-zero. After Plan 2's fix,
this table will change. 147-DISCOVERY.md distinguishes:
- The raw-column 2x2 (§1 above): what the extract CONTAINS
- The match_type table: what the resolver PRODUCES (encounter-level, after date matching)
These must not be conflated. The resolver's output depends on which records match which
encounter dates, so the record-level 2x2 does not predict the encounter-level breakdown.
  </action>
  <verify>
    <automated>
grep -c "ADDRESS_ZIP5" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md"
# Must return >= 5

grep -c "19,741\|18,731\|40,005" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md"
# Must return >= 3 (all three counts from 2x2 present)

grep -c "D-02\|ADDRESS_ZIP5 wins" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md"
# Must return >= 2
    </automated>
  </verify>
  <done>
    - 147-DISCOVERY.md exists in the phase directory
    - §0 column audit records ADDRESS_ZIP5 and ADDRESS_ZIP9 as confirmed ZIP-ish columns
    - §1 2x2 table has all four cells with records summing to 40,005
    - §2 quotes the incorrect comment verbatim from utils_address.R
    - §3 contains the 12 real rows and a pattern verdict (no PLACEHOLDER remains)
    - §4 D-02 is locked with rationale; if flipped, the coalesce order shown is the one Plan 2 must implement
    - §5 distinguishes raw-column 2x2 from match_type table
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HiPerGator — print names() and the 12 disagreement rows, then lock D-02</name>
  <read_first>
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md (§0 stub, §3 stub, §4 provisional D-02)
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-CONTEXT.md — D-01 (audit before code), D-02 (discovery-gated precedence)
  </read_first>
  <what-built>
    Task 1 wrote 147-DISCOVERY.md with the measured 2x2 and a provisional D-02. Two things are
    still unverified and BOTH gate Plan 2:
      (a) D-01 requires the names() audit of the real extract BEFORE the code change. Nobody has
          printed it — that omission is the entire cause of this phase.
      (b) D-02 was chosen "discovery-gated" (see 147-DISCUSSION-LOG.md). The gate is these 12
          rows. Deferring them to Plan 3 would lock the precedence in code one wave BEFORE the
          inspection meant to confirm it, and Plan 3 re-runs R/115 and R/116 on that precedence.
          If the rows argue for flipping it, both re-runs would be wasted.
    This is one query against a file already on disk. It runs now, not in Plan 3.
  </what-built>
  <how-to-verify>
    On HiPerGator (module load R/4.4.2), in an R session:

    ```r
    source("R/00_config.R")
    addr <- vroom::vroom(file.path(CONFIG$data_dir, "LDS_ADDRESS_HISTORY_Mailhot_V1.csv"),
                         col_types = vroom::cols(.default = "c"))

    # (a) D-01: the names() audit that has never been run
    cat("columns:\n"); print(names(addr))
    cat("\nZIP-ish columns:\n"); print(grep("zip", names(addr), ignore.case = TRUE, value = TRUE))

    # (b) D-02 gate: the 12 prefix disagreements
    addr |>
      dplyr::filter(!is.na(ADDRESS_ZIP5), !is.na(ADDRESS_ZIP9),
                    substr(ADDRESS_ZIP9, 1, 5) != ADDRESS_ZIP5) |>
      dplyr::select(ID, ADDRESS_ZIP5, ADDRESS_ZIP9, ADDRESS_PERIOD_START) |>
      print(n = 12)
    ```

    Paste both outputs. Then judge the 12 rows:
      - No pattern (scattered IDs, scattered ZIPs, no shared prefix) -> D-02 confirmed,
        ADDRESS_ZIP5 wins.
      - A pattern (all one source system, all a single transposed digit, all PO-box style)
        -> record it and state whether the precedence flips. Flipping is a one-argument change
        in Plan 2, and it is free to make now and expensive to make after Plan 3.

    If a ZIP-ish column beyond ADDRESS_ZIP5/ADDRESS_ZIP9 appears in the names() output, STOP
    and report it — the same class of omission would apply to it.
  </how-to-verify>
  <resume-signal>Paste the names() output and the 12 rows, then state "D-02 confirmed" or "D-02 flipped: {reason}".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Task 2 results in 147-DISCOVERY.md §0, §3 and §4</name>
  <files>.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md</files>
  <read_first>
    - The user's pasted output from Task 2
    - .planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md (§0 and §3 stubs, §4 provisional D-02)
  </read_first>
  <action>
    §0 — replace the stub with the real `names()` listing and the ZIP-ish column list, dated,
    marked "measured on HiPerGator {date}". State whether any ZIP-ish column beyond
    ADDRESS_ZIP5/ADDRESS_ZIP9 exists.

    §3 — replace the stub with the 12 printed rows, then add the pattern verdict in one line.

    §4 — change "Provisional pending Task 2" to the locked decision. If flipped, rewrite the
    coalesce order shown there so Plan 2 implements the correct one, and record why.

    No PLACEHOLDER text may remain in 147-DISCOVERY.md after this task.
  </action>
  <verify>
    <automated>D=".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md"; test "$(grep -c PLACEHOLDER "$D")" -eq 0 && grep -q "ADDRESS_PERIOD_START" "$D" && echo OK</automated>
  </verify>
  <done>
    - §0 has the real names() output, dated
    - §3 has the 12 real rows plus a pattern verdict
    - §4 D-02 is locked (confirmed or flipped) with rationale
    - No PLACEHOLDER remains
  </done>
</task>

</tasks>

<verification>
grep -c "40,005" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md"
# after Task 3 no PLACEHOLDER may remain
test "$(grep -c PLACEHOLDER ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md")" -eq 0
grep "D-02" ".planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-DISCOVERY.md"
</verification>

<success_criteria>
147-DISCOVERY.md exists and is COMPLETE before Plan 2 runs: the 2x2 with 40,005 total, the
incorrect-comment quote, the real names() audit (D-01), the 12 printed disagreement rows, and a
LOCKED D-02 decision with rationale. No PLACEHOLDER remains. Plan 2 implements a confirmed
precedence, not a provisional one.
</success_criteria>

<output>
After completion, create `.planning/phases/147-read-address-zip5-retract-downstream-artefacts/147-01-SUMMARY.md`
</output>
