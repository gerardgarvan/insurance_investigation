---
phase: 63-enhanced-gantt-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/63_gantt_v2_export.R
autonomous: true
requirements:
  - OUT-01
  - OUT-02

must_haves:
  truths:
    - "gantt_episodes_v2.csv exists in output/ with 17 columns (14 v1 + 3 new)"
    - "gantt_detail_v2.csv exists in output/ with 15 columns (12 v1 + 3 new)"
    - "v2 files include cancer_link_method, regimen_label, is_first_line columns"
    - "v2 files include Death and HL Diagnosis pseudo-treatment rows"
    - "Original gantt_episodes.csv and gantt_detail.csv remain unchanged"
  artifacts:
    - path: "R/63_gantt_v2_export.R"
      provides: "Gantt v2 CSV export script"
      min_lines: 200
      contains: "cancer_link_method"
    - path: "output/gantt_episodes_v2.csv"
      provides: "v2 episode-level Gantt bars"
    - path: "output/gantt_detail_v2.csv"
      provides: "v2 detail-level Gantt ticks"
  key_links:
    - from: "R/63_gantt_v2_export.R"
      to: "cache/outputs/treatment_episodes.rds"
      via: "readRDS with guard clauses for Phase 61/62 columns"
      pattern: "readRDS.*treatment_episodes\\.rds"
    - from: "R/63_gantt_v2_export.R"
      to: "cache/outputs/treatment_episode_detail.rds"
      via: "readRDS for detail-level data"
      pattern: "readRDS.*treatment_episode_detail\\.rds"
    - from: "R/63_gantt_v2_export.R"
      to: "cache/outputs/validated_death_dates.rds"
      via: "readRDS for Death pseudo-treatment rows"
      pattern: "readRDS.*validated_death_dates\\.rds"
    - from: "R/63_gantt_v2_export.R"
      to: "output/confirmed_hl_cohort.rds"
      via: "readRDS for HL Diagnosis pseudo-treatment rows"
      pattern: "readRDS.*confirmed_hl_cohort\\.rds"
---

<objective>
Create R/63_gantt_v2_export.R — a standalone script that produces Gantt v2 CSV files (gantt_episodes_v2.csv, gantt_detail_v2.csv) integrating all v1.8 enhancements: encounter-level cancer categories, cancer linkage method, HL flags, specific drug names, regimen labels, and first-line flags.

Purpose: v2 CSVs are a superset of v1, adding 3 new columns (cancer_link_method, regimen_label, is_first_line) that reflect encounter-level classification from Phases 61-62. The key simplification over R/49 is that all enrichment columns are pre-computed in treatment_episodes.rds — no PREFIX_MAP re-derivation needed. v1 files (gantt_episodes.csv, gantt_detail.csv) are preserved unchanged for backward compatibility per D-04.

Output: R/63_gantt_v2_export.R, gantt_episodes_v2.csv (17 columns), gantt_detail_v2.csv (15 columns)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-enhanced-gantt-export/63-CONTEXT.md
@.planning/phases/63-enhanced-gantt-export/63-RESEARCH.md

@R/49_gantt_data_export.R
@R/62_first_line_and_death_analysis.R
@R/61_episode_classification.R

<interfaces>
<!-- Key patterns from R/49 that R/63 mirrors (v1 column selections, pseudo-row construction). -->
<!-- R/63 does NOT import from R/49 — it duplicates patterns per D-07. -->

From R/49 (v1 episodes column order, lines 534-548):
```r
# v1 episodes: 14 columns
patient_id, treatment_type, episode_number,
episode_start, episode_stop, episode_length_days,
distinct_dates_in_episode, historical_flag, triggering_codes,
encounter_ids, drug_names,
triggering_code_descriptions,
cancer_category, is_hodgkin
```

From R/49 (v1 detail column order, lines 551-564):
```r
# v1 detail: 13 columns
patient_id, treatment_type, treatment_date, triggering_code,
ENCOUNTERID, drug_name,
episode_number, episode_start, episode_stop, historical_flag,
triggering_code_description,
cancer_category, is_hodgkin
```

From R/62 (guard clause pattern, lines 79-85):
```r
if (!"regimen_label" %in% names(episodes)) {
  warning("regimen_label column not found in treatment_episodes.rds")
  episodes <- episodes %>% mutate(regimen_label = NA_character_)
}
```

From R/49 (column alignment verification pattern, lines 734-756):
```r
expected_ep_cols <- colnames(episodes_export)
hl_dx_ep_cols <- colnames(hl_dx_episodes)
missing_in_hl_dx_ep <- setdiff(expected_ep_cols, hl_dx_ep_cols)
if (length(missing_in_hl_dx_ep) > 0) {
  stop(glue("HL Diagnosis episodes missing columns: {paste(missing_in_hl_dx_ep, collapse = ', ')}"))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/63_gantt_v2_export.R — Gantt v2 CSV export</name>
  <files>R/63_gantt_v2_export.R</files>
  <read_first>
    R/49_gantt_data_export.R
    R/62_first_line_and_death_analysis.R
    R/61_episode_classification.R
    R/00_config.R
    .planning/phases/63-enhanced-gantt-export/63-CONTEXT.md
    .planning/phases/63-enhanced-gantt-export/63-RESEARCH.md
  </read_first>
  <action>
Create a new standalone script `R/63_gantt_v2_export.R` following the structure of R/49 but simplified per D-06 (no PREFIX_MAP re-derivation). The script has 6 sections:

**Header comment block (per D-08):** Document the v2 schema with column name, type, source, and description for all 17 episode columns and all 15 detail columns. Include decision traceability referencing D-01 through D-10 from 63-CONTEXT.md. List inputs (treatment_episodes.rds, treatment_episode_detail.rds, validated_death_dates.rds, confirmed_hl_cohort.rds, code_descriptions.rds) and outputs (gantt_episodes_v2.csv, gantt_detail_v2.csv).

**SECTION 1: SETUP AND CONFIGURATION**
- Load libraries: `dplyr`, `glue`, `stringr`, `lubridate` (same as R/49, no `openxlsx2` needed)
- Source: `R/00_config.R`, `R/utils_duckdb.R`, `R/utils_dates.R`
- Define input paths using CONFIG: EPISODES_RDS = `file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")`, DETAIL_RDS = `file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail.rds")`, DESCRIPTIONS_RDS = `file.path(CONFIG$cache$outputs_dir, "code_descriptions.rds")`, VALIDATED_DEATHS_RDS = `file.path(CONFIG$cache$outputs_dir, "validated_death_dates.rds")`, COHORT_RDS = `file.path(CONFIG$output_dir, "confirmed_hl_cohort.rds")`
- Define output paths: OUTPUT_EPISODES_V2 = `file.path(CONFIG$output_dir, "gantt_episodes_v2.csv")`, OUTPUT_DETAIL_V2 = `file.path(CONFIG$output_dir, "gantt_detail_v2.csv")`

**SECTION 2: LOAD INPUT DATA**
- Verify each RDS exists with `file.exists()` check, `stop()` with descriptive error if missing
- Load `treatment_episodes.rds` via `readRDS()`
- Load `treatment_episode_detail.rds` via `readRDS()`
- **Guard clauses (per R/62 pattern, lines 79-85):** Check for Phase 61 columns (`cancer_category`, `cancer_link_method`, `is_hodgkin`, `regimen_label`) and Phase 62 column (`is_first_line`). If any is missing, issue `warning()` with message identifying which Phase hasn't been run, and add the column with a default value: `cancer_category = NA_character_`, `cancer_link_method = "none"`, `is_hodgkin = FALSE`, `regimen_label = NA_character_`, `is_first_line = FALSE`
- Log row counts with `message(glue(...))`

**SECTION 3: CODE DESCRIPTION LOOKUP** (duplicated from R/49 Section 3)
- Load `code_descriptions.rds` if it exists (non-fatal if missing — set descriptions to empty strings)
- Create `lookup_description(code)` function: returns description from lookup table, or `""` if code not found (same as R/49 lines 508-518)
- Create `map_codes_to_descriptions(codes_str)` function: splits comma-separated codes string, maps each via `lookup_description()`, joins back with commas (same as R/49 lines 523-528)

**SECTION 4: SELECT AND ORDER COLUMNS**
- Build `episodes_export` by selecting 17 columns from episodes in this exact order (per D-01):
  `patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, encounter_ids, drug_names` (11 base columns from RDS), then `mutate(triggering_code_descriptions = sapply(triggering_codes, map_codes_to_descriptions, USE.NAMES = FALSE))`, then direct read from RDS: `cancer_category, is_hodgkin, cancer_link_method, regimen_label, is_first_line`
  - Fill NA cancer_category with `""`, NA is_hodgkin with `FALSE` (matching R/49 pattern)
  - Do NOT join cancer_categories_per_patient (unlike R/49) — cancer_category is already in the RDS per D-02, D-05
- Build `detail_export` by first joining v2 episode-level columns to detail rows:
  - Extract `episodes_v2_cols <- episodes %>% select(patient_id, episode_number, cancer_link_method, regimen_label, is_first_line)`
  - Join: `detail %>% left_join(episodes_v2_cols, by = c("patient_id", "episode_number"))`
  - Select 15 columns in this exact order: `patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name, episode_number, episode_start, episode_stop, historical_flag`, then `mutate(triggering_code_description = sapply(triggering_code, lookup_description, USE.NAMES = FALSE))`, then direct from RDS or join: `cancer_category, is_hodgkin, cancer_link_method, regimen_label, is_first_line`
  - Fill NA cancer_category with `""`, NA is_hodgkin with `FALSE`
  - Detail table does NOT have `triggering_codes` (plural), `encounter_ids`, `drug_names` — it has singular `triggering_code`, `ENCOUNTERID`, `drug_name`

**SECTION 4B: DEATH PSEUDO-TREATMENT ROWS (per D-09, D-10)**
- Load `validated_death_dates.rds`, filter `!is.na(DEATH_DATE)`, select `ID`, `DEATH_DATE`
- If `nrow(death_data) > 0`:
  - Build `death_episodes` with all 17 v2 columns. Values per D-10: `patient_id=ID`, `treatment_type="Death"`, `episode_number=1L`, `episode_start=DEATH_DATE`, `episode_stop=DEATH_DATE`, `episode_length_days=0L`, `distinct_dates_in_episode=1L`, `historical_flag=FALSE`, `triggering_codes=""`, `encounter_ids=""`, `drug_names=""`, `triggering_code_descriptions=""`, `cancer_category=""`, `is_hodgkin=FALSE`, `cancer_link_method="none"`, `regimen_label=NA_character_`, `is_first_line=FALSE`
  - Use `select()` to enforce exact column order matching `episodes_export`
  - Build `death_detail` with all 15 v2 detail columns: `patient_id=ID`, `treatment_type="Death"`, `treatment_date=DEATH_DATE`, `triggering_code=""`, `ENCOUNTERID=NA_character_`, `drug_name=NA_character_`, `episode_number=1L`, `episode_start=DEATH_DATE`, `episode_stop=DEATH_DATE`, `historical_flag=FALSE`, `triggering_code_description=""`, `cancer_category=""`, `is_hodgkin=FALSE`, `cancer_link_method="none"`, `regimen_label=NA_character_`, `is_first_line=FALSE`
  - Use `select()` to enforce exact column order matching `detail_export`
  - Verify column alignment with `setdiff()` pattern from R/49 lines 629-652: check `missing_in_death_ep` and `extra_in_death_ep`, `stop()` if missing columns, `warning()` if extra
  - Same verification for detail columns
  - `bind_rows()` and `arrange(patient_id, episode_start, treatment_type)` for episodes
  - `bind_rows()` and `arrange(patient_id, treatment_date, treatment_type)` for detail

**SECTION 4C: HL DIAGNOSIS PSEUDO-TREATMENT ROWS (per D-09, D-10)**
- Load `confirmed_hl_cohort.rds`, filter for valid `first_hl_dx_date` (not NA)
- If `nrow(hl_cohort) > 0`:
  - Build `hl_dx_episodes` with all 17 v2 columns: `patient_id=ID`, `treatment_type="HL Diagnosis"`, `episode_number=1L`, `episode_start=first_hl_dx_date`, `episode_stop=first_hl_dx_date`, `episode_length_days=0L`, `distinct_dates_in_episode=1L`, `historical_flag=FALSE`, `triggering_codes=""`, `encounter_ids=""`, `drug_names=""`, `triggering_code_descriptions=""`, `cancer_category="Hodgkin Lymphoma"`, `is_hodgkin=TRUE`, `cancer_link_method="none"`, `regimen_label=NA_character_`, `is_first_line=FALSE`
  - Build `hl_dx_detail` with all 15 v2 detail columns: `patient_id=ID`, `treatment_type="HL Diagnosis"`, `treatment_date=first_hl_dx_date`, `triggering_code=""`, `ENCOUNTERID=NA_character_`, `drug_name=NA_character_`, `episode_number=1L`, `episode_start=first_hl_dx_date`, `episode_stop=first_hl_dx_date`, `historical_flag=FALSE`, `triggering_code_description=""`, `cancer_category="Hodgkin Lymphoma"`, `is_hodgkin=TRUE`, `cancer_link_method="none"`, `regimen_label=NA_character_`, `is_first_line=FALSE`
  - Same column alignment verification with `setdiff()` as Death rows
  - `bind_rows()` and `arrange()`

**SECTION 5: WRITE CSV OUTPUTS**
- `write.csv(episodes_export, OUTPUT_EPISODES_V2, row.names = FALSE)`
- `write.csv(detail_export, OUTPUT_DETAIL_V2, row.names = FALSE)`
- Log row counts and file paths

**SECTION 6: FINAL SUMMARY**
- Log unique patient count, total episode rows, total detail rows
- Log v2-specific stats: episodes with cancer_link_method != "none", episodes with non-NA regimen_label, episodes with is_first_line == TRUE
- Log Death and HL Diagnosis pseudo-treatment row counts
- Log v1 vs v2 column comparison: `message("  v1 episodes: 14 columns | v2 episodes: 17 columns (+cancer_link_method, regimen_label, is_first_line)")` and `message("  v1 detail: 13 columns | v2 detail: 15 columns (+cancer_link_method, regimen_label, is_first_line)")`  (Note: v1 detail shown as 13 based on R/49 output; if it has evolved, use actual count)
  </action>
  <verify>
    <automated>grep -c "cancer_link_method" R/63_gantt_v2_export.R</automated>
  </verify>
  <acceptance_criteria>
    - R/63_gantt_v2_export.R exists and is at least 200 lines
    - R/63_gantt_v2_export.R header comment contains "v2 Schema" or "V2 SCHEMA" documenting all 17 episode columns and 15 detail columns per D-08
    - R/63_gantt_v2_export.R contains `readRDS` calls for treatment_episodes.rds, treatment_episode_detail.rds, validated_death_dates.rds, confirmed_hl_cohort.rds, code_descriptions.rds
    - R/63_gantt_v2_export.R contains guard clauses: `if (!"cancer_link_method" %in% names(episodes))`, `if (!"regimen_label" %in% names(episodes))`, `if (!"is_first_line" %in% names(episodes))`
    - R/63_gantt_v2_export.R does NOT contain `PREFIX_MAP` or `read.csv.*cancer_summary` (per D-06)
    - R/63_gantt_v2_export.R does NOT contain `source("R/49` or modify R/49 in any way (per D-04)
    - R/63_gantt_v2_export.R contains `cancer_link_method = "none"` for Death and HL Diagnosis pseudo-rows (per D-10)
    - R/63_gantt_v2_export.R contains `regimen_label = NA_character_` for Death and HL Diagnosis pseudo-rows (per D-10)
    - R/63_gantt_v2_export.R contains `is_first_line = FALSE` for Death and HL Diagnosis pseudo-rows (per D-10)
    - R/63_gantt_v2_export.R contains `setdiff()` column alignment verification before `bind_rows()` calls
    - R/63_gantt_v2_export.R writes to `gantt_episodes_v2.csv` and `gantt_detail_v2.csv` (not overwriting v1 files)
    - R/63_gantt_v2_export.R contains `left_join(episodes_v2_cols` for propagating v2 columns to detail table
    - R/49_gantt_data_export.R is NOT modified (file unchanged from before plan execution)
  </acceptance_criteria>
  <done>
    R/63_gantt_v2_export.R is a complete standalone script that reads enriched RDS artifacts and produces gantt_episodes_v2.csv (17 columns) and gantt_detail_v2.csv (15 columns). v2 files are a superset of v1 with cancer_link_method, regimen_label, is_first_line appended. Death and HL Diagnosis pseudo-treatment rows include v2 column defaults per D-10. v1 files remain untouched. Script header documents full v2 schema per D-08.
  </done>
</task>

</tasks>

<verification>
1. R/63_gantt_v2_export.R exists with 200+ lines
2. Script contains no PREFIX_MAP or cancer_summary.csv references (D-06)
3. Script does not modify R/49 (D-04)
4. Script has guard clauses for missing Phase 61/62 columns
5. Death and HL Diagnosis pseudo-rows include all 3 v2 column defaults (D-10)
6. Column alignment verification via setdiff() before all bind_rows() calls
7. v2 detail table gets cancer_link_method/regimen_label/is_first_line via left_join from episodes
8. Output file names are gantt_episodes_v2.csv and gantt_detail_v2.csv (not overwriting v1)
</verification>

<success_criteria>
- R/63_gantt_v2_export.R is a complete, self-contained R script
- Script produces gantt_episodes_v2.csv with exactly 17 columns (14 v1 + 3 new)
- Script produces gantt_detail_v2.csv with exactly 15 columns (12 v1 + 3 new, accounting for detail's singular columns)
- v2 columns are: cancer_link_method, regimen_label, is_first_line (appended after v1 columns)
- Original gantt_episodes.csv and gantt_detail.csv remain unchanged (OUT-01)
- v2 schema is documented in script header comments (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/63-enhanced-gantt-export/63-01-SUMMARY.md`
</output>
