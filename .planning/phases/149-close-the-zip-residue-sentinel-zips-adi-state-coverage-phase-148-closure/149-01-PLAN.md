---
phase: 149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/utils/utils_address.R
  - tests/testthat/test-utils-address.R
  - .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "is_sentinel_zip5() is defined exactly once in utils_address.R"
    - "is_sentinel_zip5() rejects ZIPs below 00501 in addition to repeated-digit ZIPs"
    - "is_sentinel_zip5() returns FALSE (not NA) for NA_character_ input"
    - "00501, 12345, and 32611 are NOT rejected by is_sentinel_zip5()"
    - "149-DISCOVERY.md records the pre-change enumeration and positive control before any filter change"
    - "All 8 new unit-test assertions pass"
  artifacts:
    - path: "R/utils/utils_address.R"
      provides: "Single is_sentinel_zip5() definition with two-class filter"
      contains: "n < 501L"
    - path: "tests/testthat/test-utils-address.R"
      provides: "Unit tests covering both placeholder classes, 00501 boundary, 12345, 32611, NA"
      contains: "is_sentinel_zip5 catches both placeholder classes"
    - path: ".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"
      provides: "Pre-change enumeration results (sub-00501 list + positive control count)"
  key_links:
    - from: "tests/testthat/test-utils-address.R"
      to: "R/utils/utils_address.R"
      via: "source() or testthat auto-load through R/00_config.R"
      pattern: "is_sentinel_zip5"
---

<objective>
Widen is_sentinel_zip5() to catch invalid sub-00501 ZIPs (e.g., 00009) in addition to repeated-digit ZIPs. Precede every source change with a read-only enumeration committed to 149-DISCOVERY.md.

Purpose: 854+ encounters sit in zip5_no_zip9 because 00009 passes the current repeated-digit-only filter. Widening the filter moves those encounters to no_zip5 (invalid, correctly excluded), shrinking the residue.

Output: Updated utils_address.R (one definition), new test block in test-utils-address.R, 149-DISCOVERY.md with pre-change evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@149-CONTEXT.md
@R/utils/utils_address.R
@tests/testthat/test-utils-address.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate sub-00501 ZIPs and write 149-DISCOVERY.md §1</name>

  <read_first>
    - R/utils/utils_address.R — locate both is_sentinel_zip5() definitions; note line numbers and the NA behaviour difference between them
    - tests/testthat/test-utils-address.R — see existing test structure before adding new block
    - 149-CONTEXT.md §2a — exact probe R code to run/transcribe
    - output/encounter_ses_index_20260820.rds — baseline for enumeration (read via Sys.glob in probe)
  </read_first>

  <action>
Create `.planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md` with the following two sections.

**Section 1 — Enumerated sub-00501 ZIPs (pre-change, read-only probe)**

The probe code from 149-CONTEXT.md §2a cannot be run on Windows (no Rscript). Transcribe it faithfully into 149-DISCOVERY.md as the planned probe, mark its output as PENDING (to be filled in on HiPerGator before the filter change goes into production). Use the exact probe block from §2a verbatim.

Record the following structure in 149-DISCOVERY.md §1:

```markdown
## §1 Enumeration — sub-00501 ZIPs (Task 1 pre-change probe)

**Status:** PENDING — probe must run on HiPerGator before filter change goes into production.
Results to fill in before closing this section:
- List of ZIP5 values where as.integer(ZIP5) < 501 and not already a repeated-digit sentinel
- Total encounters affected
- Positive control: count of repeated-digit sentinels in LDS_ADDRESS_HISTORY (must be > 0)

Probe code (from 149-CONTEXT.md §2a):
[paste the exact probe block from §2a here]

### Decision gate
Do NOT commit the widened filter to production if:
- The positive control returns 0 (existing filter has never fired — investigate first)
- Any enumerated ZIP appears in the real US ZIP registry as a valid delivery address
```

**Section 2 — ADI state coverage (placeholder, Task 2 will fill)**

```markdown
## §2 ADI state coverage (Task 2 — to be filled)
PENDING.
```

**Section 5 — before/after zip9_source (placeholder, Task 4 will fill)**

```markdown
## §5 Before/after zip9_source breakdown (Task 4 — to be filled)
| zip9_source | current (0820) | post-149 |
|---|---|---|
| zip9_observed | 1,516,469 | PENDING |
| zip5_modal | 198,768 | PENDING |
| zip5_representative | 47,036 | PENDING |
| zip5_no_zip9 | 12,782 | PENDING |
| no_zip5 | 16,942 | PENDING |
| none | 158,699 | PENDING |
| total | 1,950,696 | PENDING |
```

Note in the file: "Filter change implemented in plan 149-01 Task 2; probe results must be recorded here before the widened filter is used in a production re-run (Task 4)."
  </action>

  <verify>
    <automated>grep -c "Enumeration" ".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"</automated>
  </verify>

  <acceptance_criteria>
    - 149-DISCOVERY.md exists at the expected path
    - File contains the literal probe code block from 149-CONTEXT.md §2a (grep: `as.integer(ZIP5) < 501`)
    - File contains "Decision gate" section documenting the positive-control requirement
    - File contains a §5 before/after table with the current (0820) column pre-filled with the exact values from 149-CONTEXT.md §0
    - File does NOT claim the probe has run (all output rows are PENDING)
  </acceptance_criteria>

  <done>149-DISCOVERY.md created with §1 enumeration placeholder (probe code + decision gate), §2 ADI placeholder, and §5 before/after table with 0820 baseline pre-filled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen is_sentinel_zip5() and add unit tests</name>

  <read_first>
    - R/utils/utils_address.R — read the FULL file to find BOTH is_sentinel_zip5() definitions; note exact line numbers of each definition block before editing
    - tests/testthat/test-utils-address.R — read current content so the new test_that block is appended after existing tests without duplicating them
    - 149-CONTEXT.md §2b — exact replacement function body (copy verbatim, including comment block)
    - 149-CONTEXT.md §2c — exact test block (copy verbatim)
  </read_first>

  <behavior>
    - Test: is_sentinel_zip5("00000") returns TRUE (repeated digits)
    - Test: is_sentinel_zip5("99999") returns TRUE (repeated digits)
    - Test: is_sentinel_zip5("00009") returns TRUE (below 00501, Phase 149 new class)
    - Test: is_sentinel_zip5("00001") returns TRUE (below 00501)
    - Test: is_sentinel_zip5("00501") returns FALSE (lowest real US ZIP — boundary, must NOT be rejected)
    - Test: is_sentinel_zip5("12345") returns FALSE (Schenectady NY, real ZIP — FIX-06)
    - Test: is_sentinel_zip5("32611") returns FALSE (Gainesville FL, real ZIP)
    - Test: is_sentinel_zip5(NA_character_) returns FALSE (not NA — caller safety)
  </behavior>

  <action>
**Step 1 — Delete the dead duplicate definition.**

Read utils_address.R and identify both `is_sentinel_zip5 <- function` definition blocks. The second one silently overrides the first and differs in NA behaviour. Delete the dead (first) definition entirely, keeping only one definition in the file.

**Step 2 — Replace the surviving definition with the exact text from 149-CONTEXT.md §2b:**

```r
# Placeholder ZIP5s. Two classes, both invalid:
#   (1) five repeated digits -- 00000, 11111 ... 99999 (Phase 139, Pitfall 2)
#   (2) numerically below 00501, the lowest real US ZIP (Holtsville, NY).
#       Phase 149: 00009 carried 854 encounters into the zip5_no_zip9 residue
#       because it is not five repeated digits and so passed (1).
# NOTE: 12345 is a real ZIP (Schenectady, NY) and is NOT a sentinel. (FIX-06)
is_sentinel_zip5 <- function(zip5) {
  n <- suppressWarnings(as.integer(zip5))
  !is.na(zip5) & (
    stringr::str_detect(zip5, "^(\\d)\\1{4}$") |
    (!is.na(n) & n < 501L)
  )
}
```

Copy this block exactly — including the comment. Do not paraphrase, condense, or reorder lines.

**Step 3 — Add the test block from 149-CONTEXT.md §2c to tests/testthat/test-utils-address.R:**

Append this exact block at the end of the file (after all existing tests):

```r
test_that("is_sentinel_zip5 catches both placeholder classes", {
  expect_true (is_sentinel_zip5("00000"))   # repeated digits
  expect_true (is_sentinel_zip5("99999"))
  expect_true (is_sentinel_zip5("00009"))   # below 00501 -- Phase 149
  expect_true (is_sentinel_zip5("00001"))
  expect_false(is_sentinel_zip5("00501"))   # lowest REAL ZIP -- boundary
  expect_false(is_sentinel_zip5("12345"))   # Schenectady NY, real (FIX-06)
  expect_false(is_sentinel_zip5("32611"))   # Gainesville FL
  expect_false(is_sentinel_zip5(NA_character_))
})
```

Copy this block exactly — including the inline comments. Do not alter spacing, parenthesis style, or comment text.
  </action>

  <verify>
    <automated>grep -c "^is_sentinel_zip5 <- function" R/utils/utils_address.R</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^is_sentinel_zip5 <- function" R/utils/utils_address.R` returns exactly `1`
    - `grep -c "n < 501L" R/utils/utils_address.R` returns `1` (new numeric class present)
    - `grep -c "FIX-06" R/utils/utils_address.R` returns `1` (Schenectady comment present)
    - `grep -c "!is.na(zip5) &" R/utils/utils_address.R` returns `1` (NA guard present)
    - `grep -c "is_sentinel_zip5 catches both placeholder classes" tests/testthat/test-utils-address.R` returns `1`
    - `grep -c "00501" tests/testthat/test-utils-address.R` returns `1` (boundary test present)
    - `grep -c "12345" tests/testthat/test-utils-address.R` returns `1` (Schenectady real-ZIP test present)
    - `grep -c "NA_character_" tests/testthat/test-utils-address.R` returns at least `1`
  </acceptance_criteria>

  <done>utils_address.R has exactly one is_sentinel_zip5() definition (two-class, with NA guard); test-utils-address.R has the new 8-assertion test_that block verbatim from 149-CONTEXT.md §2c.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
grep -c "^is_sentinel_zip5 <- function" R/utils/utils_address.R
# Must return: 1

grep "n < 501L" R/utils/utils_address.R
# Must return a matching line

grep -c "is_sentinel_zip5 catches both placeholder classes" tests/testthat/test-utils-address.R
# Must return: 1

grep -c "as.integer(ZIP5) < 501" ".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"
# Must return: 1 (probe code recorded)
```

On HiPerGator only (Rscript not available on Windows dev box):
```r
testthat::test_file("tests/testthat/test-utils-address.R")
# Must show 0 failures, 8 new expectations pass
```
</verification>

<success_criteria>
- 149-DISCOVERY.md exists with pre-change probe code, decision gate, and 0820 baseline table
- utils_address.R has exactly one is_sentinel_zip5() definition matching §2b verbatim
- test-utils-address.R contains the §2c test block verbatim (8 assertions, 00501 boundary included)
- No other changes to R/ or tests/ files
</success_criteria>

<output>
After completion, create `.planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-01-SUMMARY.md`
</output>
