---
phase: 149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "149-DISCOVERY.md §1 records the ACTUAL enumeration output and the positive-control count from a real HiPerGator run — not a PENDING placeholder"
    - "The decision gate is evaluated on that output: positive control > 0, and every enumerated ZIP5 confirmed invalid"
    - "The has_adi_zip5 supply path in utils_address.R is located and recorded before that file is edited (149-02 makes the edit)"
    - "NO source file is modified by this plan — the filter change is 149-02, gated on this plan's evidence"
  artifacts:
    - path: ".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"
      provides: "Enumeration output, positive-control count, decision-gate verdict, has_adi_zip5 supply path"
      contains: "positive control"
  key_links:
    - from: "149-DISCOVERY.md §1 decision gate"
      to: "149-02 filter change"
      via: "149-02 must not run until the gate reads PASS"
      pattern: "decision gate"
---

<objective>
Run the read-only enumeration on HiPerGator and record its ACTUAL output, so 149-02's filter change
is gated on evidence rather than on a placeholder.

Wave note: this plan does NOT modify any source file. The CONTEXT's locked rule is "enumerate before
widening: every ZIP5 newly rejected must be confirmed invalid before the filter changes". An earlier
draft of this plan wrote the probe as PENDING and then changed the filter in its own Task 2 — the
gate could never fire, because the plan was autonomous and the probe needs HiPerGator. The filter
change now lives in 149-02, behind this plan's verdict.

Purpose: 854+ encounters sit in zip5_no_zip9 because 00009 passes the repeated-digit-only filter.
Widening the filter REMOVES DATA — every newly rejected ZIP5 must be confirmed invalid first.

Output: 149-DISCOVERY.md §1 with real enumeration output, the positive-control count, a PASS/HALT
verdict, and the located has_adi_zip5 supply path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@149-CONTEXT.md
@R/utils/utils_address.R
@tests/testthat/test-utils-address.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 149-DISCOVERY.md with the probe block and the decision gate</name>

  <read_first>
    - 149-CONTEXT.md §2a — exact probe R code (copy verbatim)
    - 149-CONTEXT.md §0 — the 0820 baseline figures for the §5 table
  </read_first>

  <action>
Create `.planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md`
with §1 (probe + gate, output to be filled by Task 2), §2 (ADI placeholder for 149-02), and §5
(before/after table with the 0820 column pre-filled).

```markdown
## §1 Enumeration — sub-00501 ZIPs

Widening a sentinel filter REMOVES DATA. Every ZIP5 it newly rejects must be confirmed
invalid before the change is made. This section is filled from a real HiPerGator run in
Task 2; 149-02 must not execute until the verdict below reads PASS.

### Probe (from 149-CONTEXT.md §2a)

[paste the exact probe block from 149-CONTEXT.md §2a here]

### Output
(filled by Task 2)

### Positive control
Count of repeated-digit sentinels rejected by the CURRENT filter in LDS_ADDRESS_HISTORY:
(filled by Task 2)

### Decision gate — verdict: (filled by Task 2)
HALT and investigate before 149-02 if EITHER:
- the positive control returns 0 — the existing filter has never fired, so widening it is
  not the fix; something else is wrong
- any enumerated ZIP5 is a valid US delivery ZIP — the widened filter would discard real data

Otherwise: PASS.

### has_adi_zip5 supply path
zip5_representative resolved 47,036 encounters, so has_adi_zip5 exists when
.classify_zip9_source() evaluates it — but it is NOT in the five-column select() at
utils_address.R lines 526-532. Locate where it comes from before 149-02 edits this file:
(filled by Task 2)
```

Then §2 and §5:

```markdown
## §2 ADI state coverage (149-02 fills this)
PENDING.

## §5 Before/after zip9_source breakdown (149-03 fills the post-149 column)
| zip9_source | current (0820) | post-149 |
|---|---|---|
| zip9_observed | 1,516,469 | PENDING |
| zip5_modal | 198,768 | PENDING |
| zip5_representative | 47,036 | PENDING |
| zip5_no_zip9 | 12,782 | PENDING |
| no_zip5 | 16,942 | PENDING |
| none | 158,699 | PENDING |
| total | 1,950,696 | PENDING |
```
  </action>

  <verify>
    <automated>D=".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"; grep -q "as.integer(ZIP5) < 501" "$D" && grep -q "Decision gate" "$D" && grep -q "has_adi_zip5 supply path" "$D" && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - 149-DISCOVERY.md exists with §1, §2 and §5
    - §1 contains the verbatim probe block (grep: `as.integer(ZIP5) < 501`)
    - §1 contains the decision gate with BOTH halt conditions
    - §1 contains the has_adi_zip5 supply-path section
    - §5's current-(0820) column carries the exact figures from 149-CONTEXT.md §0
    - No source file modified: `git diff --name-only` shows only 149-DISCOVERY.md
  </acceptance_criteria>

  <done>149-DISCOVERY.md created with the probe, both halt conditions, the has_adi_zip5 question, and the 0820 baseline.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HiPerGator — run the probe, locate has_adi_zip5, record the verdict</name>

  <read_first>
    - 149-DISCOVERY.md §1 (the probe block written in Task 1)
    - 149-CONTEXT.md §2a
  </read_first>

  <what-built>
    Task 1 wrote the probe and the gate. Nothing has been changed in any source file. This
    checkpoint produces the evidence that decides whether 149-02 may run — the CONTEXT's locked
    rule is that every newly-rejected ZIP5 is confirmed invalid BEFORE the filter changes.
  </what-built>

  <how-to-verify>
On HiPerGator, `module load R/4.4.2`, then in R:

```r
source("R/00_config.R")
new <- readRDS(Sys.glob("output/encounter_ses_index_*.rds") |> sort() |> tail(1))

# (a) what a widened filter would newly reject
cand <- new |>
  dplyr::filter(!is.na(ZIP5)) |>
  dplyr::mutate(n5 = suppressWarnings(as.integer(ZIP5))) |>
  dplyr::filter(!is.na(n5), n5 < 501) |>
  dplyr::count(ZIP5, sort = TRUE)
print(cand, n = Inf)
cat("encounters affected:", sum(cand$n), "\n")

# (b) POSITIVE CONTROL — does the current filter fire at all?
addr <- vroom::vroom(file.path(CONFIG$data_dir, "LDS_ADDRESS_HISTORY_Mailhot_V1.csv"),
                     col_types = vroom::cols(.default = "c"), progress = FALSE)
cat("repeated-digit sentinels in ADDRESS_ZIP5:",
    sum(is_sentinel_zip5(addr$ADDRESS_ZIP5), na.rm = TRUE), "\n")
```

And, in the shell:

```bash
grep -n "has_adi_zip5\|adi_summary\|zip5_adi" R/utils/utils_address.R
```

Paste all three outputs. Then judge:

- **Positive control 0** -> HALT. The existing filter has never fired; widening it is not the
  fix. Report and stop.
- **Any enumerated ZIP5 is a real delivery ZIP** -> HALT. The lowest real US ZIP is 00501
  (Holtsville, NY); anything below it is invalid by definition, so a hit here means the query
  or the data is not what we think.
- **Otherwise** -> PASS, and 149-02 may run.
  </how-to-verify>

  <resume-signal>Paste the enumeration, the positive-control count, and the has_adi_zip5 grep; then state "gate PASS" or "gate HALT: {reason}".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Task 2 output in 149-DISCOVERY.md §1</name>

  <read_first>
    - The user's pasted output from Task 2
    - 149-DISCOVERY.md §1 (the stubs to fill)
  </read_first>

  <action>
Fill §1's Output, Positive control, verdict, and has_adi_zip5 supply path with the real values.
No PENDING may remain in §1.

If the verdict is HALT, write it and STOP — do not create 149-02's summary, and flag in this
plan's SUMMARY that the phase is blocked pending investigation.
  </action>

  <verify>
    <automated>D=".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"; sed -n '/## §1/,/## §2/p' "$D" | grep -c PENDING</automated>
  </verify>

  <acceptance_criteria>
    - §1 has no PENDING (verify command returns 0)
    - The verdict line reads PASS or HALT with a reason
    - The has_adi_zip5 supply path is stated, with a file and line reference
  </acceptance_criteria>

  <done>§1 complete with real evidence and an explicit verdict; 149-02 unblocked only if PASS.</done>
</task>

</tasks>

<verification>
```bash
D=".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"

grep -c "as.integer(ZIP5) < 501" "$D"      # 1 — probe recorded
grep -c "Decision gate" "$D"                # 1
sed -n '/## §1/,/## §2/p' "$D" | grep -c PENDING   # 0 after Task 3

# NO source file touched by this plan
git diff --name-only | grep -E "^R/|^tests/" | wc -l   # must be 0
```
</verification>

<success_criteria>
- 149-DISCOVERY.md §1 carries the REAL enumeration output and positive-control count from HiPerGator
- The decision gate has an explicit PASS/HALT verdict
- The has_adi_zip5 supply path is located and recorded with a file:line reference
- No R/ or tests/ file was modified — the filter change is 149-02, gated on this evidence
</success_criteria>

<output>
After completion, create `.planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-01-SUMMARY.md`
</output>
