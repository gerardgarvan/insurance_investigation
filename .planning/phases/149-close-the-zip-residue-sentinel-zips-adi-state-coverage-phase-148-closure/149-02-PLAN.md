---
phase: 149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 149-01
files_modified:
  - R/utils/utils_address.R
  - tests/testthat/test-utils-address.R
  - .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md
  - .planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "149-01's decision gate reads PASS before any edit to utils_address.R — if HALT, this plan does not run"
    - "is_sentinel_zip5() is defined exactly once, with the two-class filter and the !is.na() guard"
    - "00501, 12345 and 32611 are NOT rejected; 00000, 99999, 00009, 00001 are"
    - "148-DISCOVERY.md contains the D-01 RESOLVED closure text from 149-CONTEXT.md §4 verbatim"
    - "The stale zip5_no_zip9=0 reading in 148-DISCOVERY.md is annotated as superseded (not deleted)"
    - "149-DISCOVERY.md §2 records ADI diagnosis probe results and missing states"
    - "149-DISCOVERY.md §2 records which states were already present and which were added"
    - "149-DISCOVERY.md §2 documents the SCP requirement for the 478MB file before the HiPerGator re-run"
  artifacts:
    - path: ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md"
      provides: "D-01 closure text + superseded annotation"
      contains: "D-01 RESOLVED"
    - path: ".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"
      provides: "ADI state coverage diagnosis recorded"
      contains: "states already present"
  key_links:
    - from: ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md"
      to: "149-CONTEXT.md §4"
      via: "verbatim copy of D-01 closure text"
      pattern: "D-01 RESOLVED"
---

<objective>
Close Phase 148 formally by writing the D-01 RESOLVED text and superseding annotation into 148-DISCOVERY.md. Record the ADI state coverage diagnosis in 149-DISCOVERY.md §2, documenting which states are present, which are missing, and the SCP requirement before re-run.

Purpose: Phase 148's D-01 gate is now measured — the centroid crosswalk is not proportionate. This closes it with the reasoning recorded. The ADI coverage section establishes exactly what needs to be downloaded and staged on HiPerGator before Task 4's re-run.

Output: Updated 148-DISCOVERY.md (D-01 closed, stale zero annotated). Updated 149-DISCOVERY.md §2 (ADI coverage diagnosis + download checklist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@149-CONTEXT.md
@.planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen is_sentinel_zip5() and add unit tests (gated on 149-01)</name>

  <read_first>
    - 149-DISCOVERY.md §1 — THE GATE. Read the verdict line first. If it reads HALT, stop and do
      not edit any source file. If PASS, the enumerated ZIP5s were confirmed invalid and the
      positive control fired, so widening the filter is safe.
    - 149-DISCOVERY.md §1 has_adi_zip5 supply path — you are editing the file that supplies it;
      do not disturb whatever 149-01 located.
    - R/utils/utils_address.R — read the FULL file; find BOTH is_sentinel_zip5() definitions and
      note their line numbers. The second silently overrides the first and they differ on NA.
    - tests/testthat/test-utils-address.R — read it so the new block appends after existing tests
    - 149-CONTEXT.md §2b and §2c — the replacement function and test block, to copy verbatim
  </read_first>

  <behavior>
    - is_sentinel_zip5("00000") / ("99999") -> TRUE   (repeated digits)
    - is_sentinel_zip5("00009") / ("00001") -> TRUE   (below 00501, new class)
    - is_sentinel_zip5("00501")             -> FALSE  (lowest REAL US ZIP — boundary)
    - is_sentinel_zip5("12345")             -> FALSE  (Schenectady NY, real — FIX-06)
    - is_sentinel_zip5("32611")             -> FALSE  (Gainesville FL)
    - is_sentinel_zip5(NA_character_)       -> FALSE  (not NA — caller safety)
  </behavior>

  <action>
**GATE CHECK FIRST.** Open 149-DISCOVERY.md §1 and read the verdict. HALT means stop here.

**Step 1 — delete the dead duplicate.** Two `is_sentinel_zip5 <- function` definitions exist; the
second overrides the first. Delete the dead one so exactly one remains.

**Step 2 — replace the surviving definition** with the block from 149-CONTEXT.md §2b, verbatim
including the comment. Do not paraphrase, condense, or reorder.

**Step 3 — append the test block** from 149-CONTEXT.md §2c to tests/testthat/test-utils-address.R,
verbatim including inline comments.
  </action>

  <verify>
    <automated>test "$(grep -c '^is_sentinel_zip5 <- function' R/utils/utils_address.R)" -eq 1 && grep -q 'n < 501L' R/utils/utils_address.R && grep -q 'is_sentinel_zip5 catches both placeholder classes' tests/testthat/test-utils-address.R && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - 149-DISCOVERY.md §1 verdict reads PASS (this plan must not run on HALT)
    - `grep -c "^is_sentinel_zip5 <- function" R/utils/utils_address.R` == 1
    - `grep -c "n < 501L" R/utils/utils_address.R` == 1
    - `grep -c "FIX-06" R/utils/utils_address.R` == 1
    - `grep -c "!is.na(zip5) &" R/utils/utils_address.R` == 1
    - `grep -c "is_sentinel_zip5 catches both placeholder classes" tests/testthat/test-utils-address.R` == 1
    - `grep -c "00501" tests/testthat/test-utils-address.R` >= 1   (>= not ==: the file may already mention it)
    - `grep -c "12345" tests/testthat/test-utils-address.R` >= 1
    - `grep -c "NA_character_" tests/testthat/test-utils-address.R` >= 1
    - Whatever supplies has_adi_zip5 (per 149-01) is unchanged
  </acceptance_criteria>

  <done>One two-class is_sentinel_zip5() with the NA guard; the 8-assertion test block appended; the gate was PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Close Phase 148 — D-01 RESOLVED + superseded annotation</name>

  <read_first>
    - FIRST resolve the path. Two candidate .planning trees have been seen in this repo:
      `.planning/phases/148-centroid-zip9-crosswalk-tier3/` (this plan's assumption) and
      `R/.planning/phases/148-148-context-centroid/` (used by an earlier 148 plan). Run:
        ls -d .planning/phases/148* R/.planning/phases/148* 2>/dev/null
        find . -name "148-DISCOVERY.md" -not -path "./.git/*"
      Write the closure into the file that actually exists. If BOTH trees exist, say so in
      149-DISCOVERY.md — a second .planning root is a defect worth consolidating, and adding a
      third document to the wrong one makes it worse.
    - <the resolved 148-DISCOVERY.md path> — read the FULL file to locate: (a) where D-01's gate discussion lives so the closure text can be appended in context, (b) the exact zip5_no_zip9=0 line/section that must be annotated as superseded
    - 149-CONTEXT.md §4 — exact D-01 closure text to copy verbatim (the multi-line block starting "D-01 RESOLVED")
  </read_first>

  <action>
**Step 1 — Append D-01 closure text to 148-DISCOVERY.md.**

Locate the D-01 section in 148-DISCOVERY.md. Append (do not replace) the following block verbatim from 149-CONTEXT.md §4, immediately after the D-01 discussion:

```
D-01 RESOLVED — do not build the centroid crosswalk.

zip5_no_zip9 = 12,782 encounters (0.66% of 1,950,696) across 153 distinct ZIP5s.

Tier 2 (modal) and the zip5_representative tier absorbed 245,804 of the 258,586 rows
recovered by Phase 147 -- 95%. The residue decomposes as:
  ~850+   invalid placeholder ZIPs (00009 et al.) -- Phase 149 Task 1
  ~11,000 out-of-state ZIP5s absent from a 23-of-50-state ADI file -- Phase 149 Task 2
  small   genuine remainder

A national ZIP+4 centroid crosswalk would cover ~41,000 ZCTAs to serve 153 ZIP5s, 92 of
which are in states the ADI file simply does not include yet. Cost: a USPS Web Tools run
over 41,000 ZCTAs, or a commercial ZIP+4 centroid licence. Not proportionate to 0.66% of
encounters that two cheaper fixes address directly.

Reopen only if, after Phase 149, the residue remains material AND is not explained by
missing reference coverage.
```

Copy this block exactly. Do not paraphrase or reformat.

**Step 2 — Annotate the stale zip5_no_zip9=0 entry.**

Find the line(s) in 148-DISCOVERY.md that recorded `zip5_no_zip9 = 0` (or equivalent phrasing about the residue being zero or all encounters being approximable). Do NOT delete them. Immediately before or after the stale section, add:

```
[SUPERSEDED — 2026-08-20, Phase 149]
The zip5_no_zip9 = 0 reading above came from a run predating commit 477ed7c, before
ADDRESS_ZIP5 was read. When ADDRESS_ZIP5 is read (Phase 147), zip5_no_zip9 = 12,782
(0.66%). The "all approximable rows sentinel-nulled" explanation was an artefact of the
unread column, not a genuine data finding. This note is preserved as a record of how the
artefact propagated. See 149-DISCOVERY.md for the correct post-147 baseline.
```
  </action>

  <verify>
    <automated>grep -c "D-01 RESOLVED" ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "D-01 RESOLVED" .planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md` returns `1`
    - `grep -c "do not build the centroid crosswalk" .planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md` returns `1`
    - `grep -c "SUPERSEDED" .planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md` returns `1`
    - `grep -c "477ed7c" .planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md` returns `1` (commit reference in the superseded annotation)
    - The original zip5_no_zip9=0 line is still present in the file (not deleted) — verify with: `grep -i "zip5_no_zip9.*0" .planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md | wc -l` returns >= 1
  </acceptance_criteria>

  <done>148-DISCOVERY.md has D-01 RESOLVED text (verbatim from §4) appended, and the stale zero reading is annotated as superseded with the commit reference and artefact explanation.</done>
</task>

<task type="auto">
  <name>Task 3: Record ADI coverage diagnosis in 149-DISCOVERY.md §2</name>

  <read_first>
    - .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md — read current content (written by plan 149-01) to see the §2 PENDING placeholder so this task fills it correctly
    - 149-CONTEXT.md §3a — the diagnosis probe code and the state-prefix table (§3b)
    - 149-CONTEXT.md §3b — the priority state list (AR/LA/OK/TX/MS/PR with prefix ranges)
  </read_first>

  <action>
Replace the `§2 ADI state coverage (Task 2 — to be filled) PENDING.` placeholder in 149-DISCOVERY.md with the following content:

```markdown
## §2 ADI State Coverage — Task 2

### Diagnosis probe (run on HiPerGator before re-run)

The probe confirms that residue ZIP5s are absent from zip5_adi_summary.csv because the
Neighborhood Atlas data covers only 23 of 50 states. Probe code from 149-CONTEXT.md §3a:

[paste exact probe block from 149-CONTEXT.md §3a here]

**Expected result:** residue ZIP5s present in ADI summary ≈ 0 of 153; prefix-7 coverage ≈ 0.

### States to download (priority order from residue evidence)

| Prefix range | State | Residue ZIP5 coverage | Priority |
|---|---|---|---|
| 716–729 | Arkansas (AR) | bulk of the 92 prefix-7 ZIPs | 1 |
| 700–714 | Louisiana (LA) | prefix-7 | 2 |
| 730–741 | Oklahoma (OK) | prefix-7 | 3 |
| 750–799 | Texas (TX) | prefix-7 | 4 |
| 386–397 | Mississippi (MS) | | 5 |
| 006–009 | Puerto Rico (PR) | 00983 = 366 encounters | 6 |

Download all remaining states beyond these six if feasible in the same session.

### Download checklist (fill in before re-run — 149-03 Task 2 requires ALL of these filled)

- [ ] States already present in zip5_adi_summary.csv: TBD
- [ ] States downloaded this session: TBD
- [ ] Download date: TBD
- [ ] File vintage (Neighborhood Atlas data year): TBD
- [ ] Collated ZIP9 file size confirmed ≈ 478 MB: TBD
- [ ] File SCP'd to HiPerGator and confirmed present (ls -lh on HiPerGator): TBD

NOTE: these use TBD, not PENDING. 149-03's completeness check greps for zero PENDING in this
file; leaving PENDING here would fail a correctly-executed phase.

### SCP requirement

The 478 MB collated Neighborhood Atlas ZIP9 file is listed in .gitignore and will NOT be
pushed to the remote repo. It must be SCP'd to HiPerGator manually before R/118 is re-run:

  scp neighborhood_atlas_zip9_collated.csv <netid>@hpg.rc.ufl.edu:<repo_path>/data/reference/

Confirm presence with `ls -lh data/reference/neighborhood_atlas_zip9_collated.csv` on
HiPerGator before running R/118_build_zip5_adi_summary.R. An absent file makes R/118
report 0% coverage and look like a join failure, not a missing-file error.

### Post-rebuild record (fill after R/118 runs — 149-03 Task 3)

- ZIP5 count in rebuilt zip5_adi_summary.csv: TBD
- Non-NA median adi_natrank count: TBD
- zip5_representative count (from R/116 after re-run): TBD
```

After updating 149-DISCOVERY.md, insert the exact probe code from 149-CONTEXT.md §3a into the marked location above (the block starting with `adi <- readr::read_csv(...)`).
  </action>

  <verify>
    <automated>grep -c "SCP" ".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Arkansas" .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md` returns `1`
    - `grep -c "Puerto Rico" .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md` returns `1`
    - `grep -c "SCP" .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md` returns `1` (SCP requirement documented)
    - `grep -c "gitignore" .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md` returns `1` (gitignore warning present)
    - `grep -c "read_csv.*zip5_adi_summary" .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md` returns `1` (§3a probe code present)
    - `grep -c "00983" .planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md` returns `1` (Puerto Rico encounter count noted)
  </acceptance_criteria>

  <done>149-DISCOVERY.md §2 filled with ADI diagnosis probe, priority download table, SCP checklist, and post-rebuild record template.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
grep -c "D-01 RESOLVED" ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md"
# Must return: 1

grep -c "SUPERSEDED" ".planning/phases/148-centroid-zip9-crosswalk-tier3/148-DISCOVERY.md"
# Must return: 1

grep -c "Arkansas" ".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"
# Must return: 1

grep -c "SCP" ".planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-DISCOVERY.md"
# Must return: 1
```
</verification>

<success_criteria>
- 148-DISCOVERY.md has D-01 RESOLVED closure text (verbatim from 149-CONTEXT.md §4) and the superseded-zero annotation
- 149-DISCOVERY.md §2 has the ADI diagnosis probe, priority state table, SCP checklist, and post-rebuild template
- No R source files modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/149-close-the-zip-residue-sentinel-zips-adi-state-coverage-phase-148-closure/149-02-SUMMARY.md`
</output>
