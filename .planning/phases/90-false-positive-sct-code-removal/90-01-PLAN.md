---
phase: 90-false-positive-sct-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "Z94.84, T86.5, T86.09, Z48.290, HEMATOLOGIC_TRANSPLANT_AND_ENDOC are absent from DRUG_GROUPINGS"
    - "SCT section comment reads '36 codes' (previously 41)"
    - "Smoke test Section 15c validates all 5 codes absent from DRUG_GROUPINGS"
    - "Smoke test validates SCT code count comment updated"
    - "R/10_cohort_predicates.R and R/11_treatment_payer.R are NOT modified"
    - "R/42_build_code_descriptions.R and R/58_code_reference_tables.R are NOT modified"
  artifacts:
    - path: "R/00_config.R"
      provides: "SCT section of DRUG_GROUPINGS with 36 codes (5 false positives removed)"
      contains: "# SCT (36 codes)"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15c validating deprecated codes absent from DRUG_GROUPINGS"
      contains: "SECTION 15c: FALSE-POSITIVE SCT CODE REMOVAL"
  key_links:
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/00_config.R"
      via: "readLines() + grepl() within DRUG_GROUPINGS boundaries"
      pattern: "grepl.*Z94\\.84.*drug_groupings_section"
---

<objective>
Remove 5 false-positive SCT codes (status/complication codes that don't represent actual transplant procedures) from the DRUG_GROUPINGS treatment detection map in R/00_config.R, and add a dedicated smoke test section that validates their absence.

Purpose: These ICD-10-CM status/aftercare/complication codes (Z94.84, T86.5, T86.09, Z48.290) and one tumor registry field (HEMATOLOGIC_TRANSPLANT_AND_ENDOC) currently trigger false-positive SCT treatment episodes in R/28_episode_classification.R. Removing them from DRUG_GROUPINGS eliminates spurious SCT episodes while preserving their use as cohort inclusion signals in R/10/R/11.

Output: Updated R/00_config.R with 36 SCT codes in DRUG_GROUPINGS (down from 41), and R/88_smoke_test_comprehensive.R with new Section 15c validating the removal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-false-positive-sct-code-removal/90-CONTEXT.md
@.planning/phases/90-false-positive-sct-code-removal/90-RESEARCH.md

<interfaces>
<!-- Key structures the executor needs -->

From R/00_config.R (lines 1587-1629, DRUG_GROUPINGS SCT section):
```r
  # SCT (41 codes)
  "Z94.84" = "SCT",
  "HEMATOLOGIC_TRANSPLANT_AND_ENDOC" = "SCT",
  "38241" = "SCT",
  "016" = "SCT",
  "30243Y0" = "SCT",
  "T86.5" = "SCT",
  "38240" = "SCT",
  "41.04" = "SCT",
  "0362" = "SCT",
  "30233Y0" = "SCT",
  "014" = "SCT",
  "T86.09" = "SCT",
  "41.05" = "SCT",
  "Z48.290" = "SCT",
  "30243Y2" = "SCT",
  # ... (26 more legitimate codes follow)
```

From R/88_smoke_test_comprehensive.R (smoke test patterns):
```r
# Section header pattern:
# ==============================================================================
# SECTION NN: TITLE (REQ-IDs) ----
# ==============================================================================

# Progress message pattern:
message("\n[NN/MM] Description...")

# Assertion pattern:
check(
  "description of what's being checked",
  <logical_condition>
)
```

Current smoke test progress: [1/29] through [33/33] (33 actual sections, numbering inconsistent)
Last section before SUMMARY: Section 33 (line 1564/1677)
SUMMARY section: line 1681
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 5 false-positive SCT codes from DRUG_GROUPINGS (CLEAN-01)</name>
  <files>R/00_config.R</files>
  <read_first>
    - R/00_config.R (lines 1580-1635 for SCT section context)
    - .planning/phases/90-false-positive-sct-code-removal/90-CONTEXT.md (decisions D-01 through D-04)
  </read_first>
  <action>
In R/00_config.R, within the DRUG_GROUPINGS named vector (SCT section starting at line 1587):

1. Change the section comment from `# SCT (41 codes)` to `# SCT (36 codes)` (line 1587)

2. Delete these 5 lines entirely (per D-01, D-04 — inline comments explain removal rationale in the code):
   - Line 1588: `"Z94.84" = "SCT",` — false positive: transplant STATUS code (Z chapter), not a procedure
   - Line 1589: `"HEMATOLOGIC_TRANSPLANT_AND_ENDOC" = "SCT",` — false positive: tumor registry flag, not a standard procedure code
   - Line 1593: `"T86.5" = "SCT",` — false positive: transplant COMPLICATION code (T chapter), not a procedure
   - Line 1599: `"T86.09" = "SCT",` — false positive: transplant COMPLICATION code (T chapter), not a procedure
   - Line 1601: `"Z48.290" = "SCT",` — false positive: transplant AFTERCARE code (Z chapter), not a procedure

Per D-04: Add a single block comment ABOVE the SCT section (before `# SCT (36 codes)`) documenting the removal:
```r
  # NOTE: 5 false-positive codes removed (v2.3 Phase 90, CLEAN-01):
  #   Z94.84 (transplant status), T86.5/T86.09 (transplant complications),
  #   Z48.290 (aftercare), HEMATOLOGIC_TRANSPLANT_AND_ENDOC (tumor registry flag)
  #   These are status/complication/aftercare codes, not procedures.
  #   Still used for cohort inclusion in R/10 has_sct() -- just no longer trigger episodes.
```

Per D-02: Do NOT modify R/10_cohort_predicates.R or R/11_treatment_payer.R.
Per D-03: Do NOT modify R/42_build_code_descriptions.R or R/58_code_reference_tables.R.
  </action>
  <verify>
    <automated>cd "C:\Users\Owner\Documents\insurance_investigation" && Rscript -e "config_lines <- readLines('R/00_config.R', warn=FALSE); dg_start <- which(grepl('^DRUG_GROUPINGS <- c\\\\(', config_lines)); dg_end <- which(grepl('^\\\\)$', config_lines) & seq_along(config_lines) > dg_start)[1]; dg_section <- config_lines[dg_start:dg_end]; bad <- c('Z94.84', 'T86.5', 'T86.09', 'Z48.290', 'HEMATOLOGIC_TRANSPLANT_AND_ENDOC'); found <- sapply(bad, function(x) any(grepl(paste0('\"', x, '\"\\\\s*='), dg_section))); if(any(found)) stop(paste('Still found:', paste(names(found[found]), collapse=', '))) else cat('All 5 codes removed from DRUG_GROUPINGS\n'); if(!any(grepl('# SCT \\\\(36 codes\\\\)', config_lines))) stop('SCT comment not updated to 36') else cat('SCT comment updated to 36 codes\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains `# SCT (36 codes)` (was `# SCT (41 codes)`)
    - R/00_config.R DRUG_GROUPINGS section does NOT contain `"Z94.84" = "SCT"`
    - R/00_config.R DRUG_GROUPINGS section does NOT contain `"HEMATOLOGIC_TRANSPLANT_AND_ENDOC" = "SCT"`
    - R/00_config.R DRUG_GROUPINGS section does NOT contain `"T86.5" = "SCT"`
    - R/00_config.R DRUG_GROUPINGS section does NOT contain `"T86.09" = "SCT"`
    - R/00_config.R DRUG_GROUPINGS section does NOT contain `"Z48.290" = "SCT"`
    - R/00_config.R contains comment block with "5 false-positive codes removed"
    - R/10_cohort_predicates.R is NOT modified (git diff shows no changes)
    - R/11_treatment_payer.R is NOT modified (git diff shows no changes)
    - R/42_build_code_descriptions.R is NOT modified
    - R/58_code_reference_tables.R is NOT modified
  </acceptance_criteria>
  <done>Five false-positive SCT codes deleted from DRUG_GROUPINGS, section comment updated to "36 codes", inline rationale comment block added above SCT section. No other files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke test Section 15c for false-positive SCT code removal validation (CLEAN-02)</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (lines 1155-1230 for Section 15 context and Section 15b pattern)
    - R/88_smoke_test_comprehensive.R (lines 1681-1745 for SUMMARY section that needs requirement list update)
    - R/00_config.R (lines 1580-1630 to verify final state after Task 1)
  </read_first>
  <action>
In R/88_smoke_test_comprehensive.R, add a new Section 15c AFTER the existing Section 15b (Environment Detection, which ends around line 1303). Insert the new section between the end of Section 15b (line ~1303) and Section 30 (line 1305).

Insert the following code block:

```r
# ==============================================================================
# SECTION 15c: FALSE-POSITIVE SCT CODE REMOVAL (CLEAN-01, CLEAN-02) ----
# ==============================================================================

message("\n[34/37] False-positive SCT code removal validation (CLEAN-01, CLEAN-02)...")

# Read R/00_config.R and find DRUG_GROUPINGS section boundaries
config_lines <- readLines("R/00_config.R", warn = FALSE)
drug_groupings_start <- which(grepl("^DRUG_GROUPINGS <- c\\(", config_lines))
drug_groupings_end <- which(grepl("^\\)$", config_lines) &
                            seq_along(config_lines) > drug_groupings_start)[1]
drug_groupings_section <- config_lines[drug_groupings_start:drug_groupings_end]

# CLEAN-01: Each deprecated code is absent from DRUG_GROUPINGS
deprecated_codes <- c("Z94.84", "T86.5", "T86.09", "Z48.290",
                      "HEMATOLOGIC_TRANSPLANT_AND_ENDOC")

for (code in deprecated_codes) {
  check(
    glue("DRUG_GROUPINGS does not contain deprecated code {code}"),
    !any(grepl(paste0('"', code, '"'), drug_groupings_section, fixed = TRUE))
  )
}

# CLEAN-01: SCT section comment updated to reflect 36 codes
check(
  "SCT section comment updated to 36 codes (was 41)",
  any(grepl("# SCT \\(36 codes\\)", drug_groupings_section))
)

# CLEAN-02: Code descriptions still preserved (not accidentally removed from R/42)
check(
  "R/42 still has Z94.84 code description (not accidentally removed)",
  any(grepl("Z94\\.84", readLines("R/42_build_code_descriptions.R", warn = FALSE)))
)

# CLEAN-02: Cohort predicates still reference these codes (not accidentally removed from R/10)
r10_lines <- readLines("R/10_cohort_predicates.R", warn = FALSE)
check(
  "R/10 has_sct() still references Z94.84 for cohort inclusion",
  any(grepl("Z94\\.84", r10_lines))
)
```

Then update the progress counter denominators. The smoke test currently goes up to `[33/33]`. The new section adds 1 more, making it `[34/34]`... BUT since the numbering is already inconsistent (some say /29, some say /33), adopt the following approach:

- For the NEW section 15c, use message index `[34/37]` (adding 4 to final denominator to account for current sections plus new one — actually, just match the actual total).

SIMPLER APPROACH: Leave existing `[NN/MM]` messages untouched (they are legacy and changing them risks breaking existing validations). For the new section, use a progress message that fits the pattern but uses an independent counter:

```r
message("\n[CLEAN] False-positive SCT code removal validation (CLEAN-01, CLEAN-02)...")
```

This avoids renumbering existing sections. Use `[CLEAN]` prefix tag instead of numeric index to indicate this is a requirements-tagged section.

Additionally, add to the SUMMARY section (around line 1738, before the `if (failed > 0)` block at line 1745), append this message line:

```r
message("  * CLEAN-01: False-positive SCT codes removed from DRUG_GROUPINGS (Z94.84, T86.5, T86.09, Z48.290, HEMATOLOGIC_TRANSPLANT_AND_ENDOC)")
message("  * CLEAN-02: Smoke test validates deprecated codes absent and descriptions preserved")
```
  </action>
  <verify>
    <automated>cd "C:\Users\Owner\Documents\insurance_investigation" && Rscript -e "lines <- readLines('R/88_smoke_test_comprehensive.R', warn=FALSE); has_section <- any(grepl('SECTION 15c.*FALSE-POSITIVE SCT CODE REMOVAL', lines)); has_check_fn <- any(grepl('DRUG_GROUPINGS does not contain deprecated code', lines)); has_clean01 <- any(grepl('CLEAN-01', lines)); has_clean02 <- any(grepl('CLEAN-02.*Smoke test validates', lines)); if(!has_section) stop('Missing Section 15c header'); if(!has_check_fn) stop('Missing deprecated code check'); if(!has_clean01) stop('Missing CLEAN-01 reference'); if(!has_clean02) stop('Missing CLEAN-02 in summary'); cat('All smoke test checks present\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains `SECTION 15c: FALSE-POSITIVE SCT CODE REMOVAL (CLEAN-01, CLEAN-02)`
    - R/88_smoke_test_comprehensive.R contains `deprecated_codes <- c("Z94.84", "T86.5", "T86.09", "Z48.290"`
    - R/88_smoke_test_comprehensive.R contains `DRUG_GROUPINGS does not contain deprecated code`
    - R/88_smoke_test_comprehensive.R contains `SCT section comment updated to 36 codes`
    - R/88_smoke_test_comprehensive.R contains `R/42 still has Z94.84 code description`
    - R/88_smoke_test_comprehensive.R contains `R/10 has_sct() still references Z94.84`
    - R/88_smoke_test_comprehensive.R SUMMARY section contains `CLEAN-01` and `CLEAN-02`
    - Section 15c is positioned AFTER Section 15b (environment detection) and BEFORE Section 30
  </acceptance_criteria>
  <done>Smoke test Section 15c added with 7 check() assertions validating: (1) all 5 deprecated codes absent from DRUG_GROUPINGS boundaries, (2) SCT comment count updated, (3) code descriptions preserved in R/42, (4) cohort predicates preserved in R/10. SUMMARY section updated with CLEAN-01/CLEAN-02 requirement messages.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full smoke test to confirm no regressions:

```bash
cd "C:\Users\Owner\Documents\insurance_investigation" && Rscript R/88_smoke_test_comprehensive.R
```

Expected: All checks pass (0 failures). The new Section 15c checks validate CLEAN-01 and CLEAN-02.

Additional manual verification:
- `grep -c '"SCT"' R/00_config.R` should show 36 occurrences in DRUG_GROUPINGS
- `git diff --stat` should show only R/00_config.R and R/88_smoke_test_comprehensive.R modified
</verification>

<success_criteria>
1. DRUG_GROUPINGS in R/00_config.R contains exactly 36 SCT entries (5 removed from 41)
2. SCT section comment reads "# SCT (36 codes)"
3. Smoke test Section 15c passes all 7 assertions (5 code absence + 1 comment + 1 preservation)
4. R/10, R/11, R/42, R/58 remain unmodified (git diff confirms)
5. Full smoke test run produces 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/90-false-positive-sct-code-removal/90-01-SUMMARY.md`
</output>
