---
phase: 74-smoke-testing-reference-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements: [REORG-05, SAFE-06]

must_haves:
  truths:
    - "R/88_smoke_test_comprehensive.R runs without error on Windows via Rscript"
    - "Smoke test validates all 10 utils modules present in R/utils/"
    - "Smoke test validates no duplicate PREFIX_MAP or TIER_MAPPING definitions outside R/00_config.R"
    - "Smoke test validates all source() calls resolve to existing files"
    - "Smoke test validates config constants (CONFIG, ICD_CODES, PAYER_MAPPING, CANCER_SITE_MAP, TIER_MAPPING) exist"
    - "Smoke test validates defensive coding infrastructure (checkmate loaded, utils_assertions.R present)"
    - "Smoke test gates data-dependent checks behind DATA_AVAILABLE auto-detection"
    - "SCRIPT_INDEX.md reflects 10 utils (not 8)"
  artifacts:
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Consolidated smoke test covering structural integrity, DRY compliance, config validation, cross-platform gating"
      min_lines: 250
    - path: "R/SCRIPT_INDEX.md"
      provides: "Corrected utility count reflecting utils_assertions.R and utils_cancer.R additions"
  key_links:
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/00_config.R"
      via: "source() call to load config and validate constants"
      pattern: 'source\("R/00_config\.R"\)'
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/utils/"
      via: "list.files() to enumerate utils modules"
      pattern: 'list\.files\("R/utils"'
---

<objective>
Create a comprehensive standalone smoke test (R/88_smoke_test_comprehensive.R) that consolidates and extends R/86 + R/87 checks into a single authoritative structural validation script. Fix SCRIPT_INDEX.md to reflect the current 10 utils modules.

Purpose: Delivers REORG-05 (smoke test validates no broken cross-references) and SAFE-06 (comprehensive smoke test suite) as the final validation layer for the v2.0 codebase cleanup milestone. The comprehensive test supersedes R/86 and R/87 by including all their checks plus new validation for DRY consolidation (Phase 73), defensive coding infrastructure (Phase 72), config constants, and cross-platform data availability gating.

Output: R/88_smoke_test_comprehensive.R (standalone, runs via `Rscript R/88_smoke_test_comprehensive.R`), updated R/SCRIPT_INDEX.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-smoke-testing-reference-manual/74-CONTEXT.md
@.planning/phases/74-smoke-testing-reference-manual/74-RESEARCH.md

@R/86_smoke_test_foundation.R
@R/87_smoke_test_full_pipeline.R
@R/00_config.R
@R/SCRIPT_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/88_smoke_test_comprehensive.R</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/86_smoke_test_foundation.R (existing check() pattern, utils validation, foundation chain checks)
    - R/87_smoke_test_full_pipeline.R (decade validation, source() parsing, dependency chain checks)
    - R/00_config.R (config constants to validate: CONFIG, EXTRACT_DATE, PCORNET_TABLES, PCORNET_PATHS, ICD_CODES, PAYER_MAPPING, AMC_PAYER_LOOKUP, TREATMENT_CODES, ANALYSIS_PARAMS, CANCER_SITE_MAP, TIER_MAPPING)
    - R/utils/utils_assertions.R (assert_rds_exists, assert_df_valid, assert_col_types, warn_date_range, warn_row_count)
    - R/utils/utils_cancer.R (classify_codes function)
    - R/utils/utils_payer.R (classify_payer_tier function)
    - R/utils/utils_snapshot.R (build_output_path function)
  </read_first>
  <action>
Create R/88_smoke_test_comprehensive.R as a standalone structural smoke test using the manual check() pattern (per D-01). This script consolidates all checks from R/86 and R/87 and adds new validation for Phases 72-73 work. Structure:

**Header block** (matching Phase 69 DOC-01 format):
```
# Purpose: Comprehensive structural smoke test validating pipeline integrity after v2.0 codebase cleanup. Consolidates R/86 + R/87 checks and adds DRY consolidation, defensive coding, and config constant validation.
# Inputs: R/ directory filesystem structure, R/00_config.R (sourced for constant validation)
# Outputs: Console output (PASS/FAIL per check group), exit code 1 on any failure
# Dependencies: (standalone verification script), glue
# Requirements: REORG-05, SAFE-06
```

**SECTION 1: SETUP** -- Exactly replicate the check() function pattern from R/86:
- `rm(list = ls())` to clear workspace
- `library(glue)` only dependency
- `passed <- 0L` and `failed <- 0L` counters
- `check <- function(description, condition)` with `message(glue("  PASS: {description}"))` or `message(glue("  FAIL: {description}"))` and increment counters
- Title banner: `message(strrep("=", 70))` / `message("SMOKE TEST: Comprehensive Pipeline Validation (v2.0)")` / `message(strrep("=", 70))`

**SECTION 2: UTILS MODULE COMPLETENESS** (extends R/86 Test 1-2):
- Check `dir.exists("R/utils")`
- `expected_utils <- c("utils_assertions.R", "utils_attrition.R", "utils_cancer.R", "utils_dates.R", "utils_duckdb.R", "utils_icd.R", "utils_payer.R", "utils_pptx.R", "utils_snapshot.R", "utils_treatment.R")` -- all 10 modules
- `utils_files <- list.files("R/utils", pattern = "\\.R$")`
- Check `length(utils_files) == 10` (updated from R/86's 8)
- Check `length(setdiff(expected_utils, utils_files)) == 0` (all expected present)
- Check no `utils_*.R` files in `R/` root (stale copies): `length(list.files("R", pattern = "^utils_.*\\.R$")) == 0`

**SECTION 3: CONFIG LOADING AND AUTO-SOURCING** (extends R/86 Test 3):
- `tryCatch({ source("R/00_config.R"); check("00_config.R loads without error", TRUE) }, error = ...)`
- Validate 11 expected config constants exist: `CONFIG`, `EXTRACT_DATE`, `PCORNET_TABLES`, `PCORNET_PATHS`, `ICD_CODES`, `PAYER_MAPPING`, `AMC_PAYER_LOOKUP`, `TREATMENT_CODES`, `ANALYSIS_PARAMS`, `CANCER_SITE_MAP`, `TIER_MAPPING`
- For each: `check(glue("{const_name} defined in config"), exists(const_name))`
- Validate CANCER_SITE_MAP structure: `is.character(CANCER_SITE_MAP) && !is.null(names(CANCER_SITE_MAP))` and `length(CANCER_SITE_MAP) == 324`
- Validate TIER_MAPPING structure: `is.list(TIER_MAPPING)` and `length(TIER_MAPPING) == 8`
- Validate key auto-sourced functions exist (extended from R/86's 8 to include Phase 72-73 additions):
  - utils_dates: `parse_pcornet_date`
  - utils_attrition: `log_attrition`
  - utils_icd: `normalize_icd`
  - utils_snapshot: `save_output_data`, `build_output_path` (Phase 73)
  - utils_duckdb: `open_pcornet_con`
  - utils_treatment: `safe_table`
  - utils_payer: `is_missing_payer`, `classify_payer_tier` (Phase 73)
  - utils_pptx: `style_table`
  - utils_assertions: `assert_rds_exists`, `assert_df_valid`, `assert_col_types` (Phase 72)
  - utils_cancer: `classify_codes` (Phase 73)

**SECTION 4: FOUNDATION CHAIN** (from R/86 Test 4-5):
- Check existence: `R/00_config.R`, `R/01_load_pcornet.R`, `R/02_harmonize_payer.R`, `R/03_duckdb_ingest.R`
- Check old `R/25_duckdb_ingest.R` does not exist

**SECTION 5: DECADE VALIDATION** (from R/87 Tests 1-8):
- Cohort (10-14): 5 scripts with exact filenames from R/87
- Treatment (20-29): 10 scripts with exact filenames from R/87
- Cancer (40-53): 14 scripts with exact filenames from R/87
- Payer/QA (60-69): 10 scripts with exact filenames from R/87
- Output (70-75): 6 scripts with exact filenames from R/87
- Test (80-87): 8 scripts (80-87) -- note: add R/88 itself to expected list making it 9 total
- Ad-hoc (90-99): 10 scripts with exact filenames from R/87
- Use same counting pattern: iterate with `file.exists(file.path("R", s))`, count found, check against expected

**SECTION 6: NO STALE FILES** (from R/87 Tests 9-10):
- Check old-numbered files don't exist (same list as R/87)
- Check no a/b suffixed files: `grep("^[0-9]+[ab]_", r_files)`

**SECTION 7: SOURCE() REFERENCE VALIDATION** (from R/87 Test 11):
- Parse all `.R` files in `R/` for uncommented `source("R/...")` calls
- Verify each referenced path exists via `file.exists(path)`
- Collect broken refs, check `length(broken_refs) == 0`
- Also check no old-style `source("R/utils_` paths (from R/86 Test 6)

**SECTION 8: KEY DEPENDENCY CHAINS** (from R/87 Test 12):
- Replicate all 5 dependency chain checks from R/87:
  - `14_build_cohort.R` sources `10_cohort_predicates.R`
  - `14_build_cohort.R` sources `11_treatment_payer.R`
  - `26_treatment_episodes.R` sources `25_treatment_durations.R`
  - `72_generate_pptx.R` sources `75_encounter_analysis.R`
  - `73_generate_phase19_20_pptx.R` sources `94_flm_duplicate_dates.R`

**SECTION 9: DRY CONSOLIDATION VALIDATION** (NEW - Phase 73):
- Check no `PREFIX_MAP <-` definitions outside R/00_config.R. Scan these 11 scripts: R/28_episode_classification.R, R/40-R/49 cancer scripts, R/51_gantt_data_export.R, R/52_gantt_v2_export.R. Use `grepl("^PREFIX_MAP\\s*<-", lines)` on uncommented lines.
- Check no `TIER_MAPPING <-` definitions outside R/00_config.R. Scan R/60, R/61, R/62. Use `grepl("^TIER_MAPPING\\s*<-", lines)`.
- Check no `classify_codes <-` function definitions outside R/utils/utils_cancer.R. Scan same cancer scripts. Use `grepl("^classify_codes\\s*<-", lines)`.

**SECTION 10: DEFENSIVE CODING INFRASTRUCTURE** (NEW - Phase 72):
- Check `library(checkmate)` appears in R/00_config.R: `any(grepl("library\\(checkmate\\)", readLines("R/00_config.R", warn = FALSE)))`
- Check `R/utils/utils_assertions.R` exists
- Check key assertion functions exist after config load: `assert_rds_exists`, `assert_df_valid`, `assert_col_types`, `warn_date_range`, `warn_row_count`

**SECTION 11: ARCHIVE VALIDATION**:
- Check `dir.exists("R/archive")`
- Check `file.exists("R/archive/README.md")`
- Expected 8 archived files: `check_deleted_proton_code.R`, `date_range_check.R`, `payer_frequency_from_resolved.R`, `run_phase12_outputs.R`, `sct_code_inventory.R`, `search_C8190.R`, `tiered_payer_summary.R`, `treatment_cross_reference.R`
- Check count matches 8

**SECTION 12: CROSS-PLATFORM DATA AVAILABILITY** (per D-06, D-07):
- After config is loaded: `DATA_AVAILABLE <- dir.exists(CONFIG$data_dir)`
- `PLATFORM <- .Platform$OS.type`
- `message(glue("\nPlatform: {PLATFORM}"))`
- `message(glue("Data available: {DATA_AVAILABLE}"))`
- If `DATA_AVAILABLE`:
  - Check ENROLLMENT.csv exists at `file.path(CONFIG$data_dir, "ENROLLMENT.csv")`
  - Check cache directories exist: `CONFIG$cache$raw_dir`, `CONFIG$cache$cohort_dir`
- Else: `message("\n[Skipping data-dependent checks -- data not available on this platform]")`

**SUMMARY SECTION**:
- Print total passed/failed with banner
- Print validated requirement list: REORG-01, REORG-02, REORG-03, REORG-04, REORG-05, SAFE-06, DRY-01, SAFE-01/02/03
- `if (failed > 0) quit(status = 1)` for SLURM compatibility
  </action>
  <verify>
    <automated>cd C:/Users/Owner/Documents/insurance_investigation && Rscript R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R exists and is > 250 lines
    - File contains `check <- function(description, condition)` pattern
    - File contains all 10 expected utils names: `utils_assertions.R`, `utils_attrition.R`, `utils_cancer.R`, `utils_dates.R`, `utils_duckdb.R`, `utils_icd.R`, `utils_payer.R`, `utils_pptx.R`, `utils_snapshot.R`, `utils_treatment.R`
    - File contains `CANCER_SITE_MAP` validation check
    - File contains `TIER_MAPPING` validation check
    - File contains `PREFIX_MAP` DRY duplication check
    - File contains `classify_codes` DRY duplication check
    - File contains `library(checkmate)` infrastructure check
    - File contains `DATA_AVAILABLE <- dir.exists(CONFIG$data_dir)` cross-platform gating
    - File contains `quit(status = 1)` for SLURM compatibility
    - Rscript R/88_smoke_test_comprehensive.R exits with code 0 (all checks pass on Windows)
    - Console output shows "ALL {N} CHECKS PASSED" where N >= 30
  </acceptance_criteria>
  <done>R/88_smoke_test_comprehensive.R runs via Rscript on Windows, all structural checks pass, data-dependent checks are correctly gated behind DATA_AVAILABLE, output shows PASS for all checks with zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Update SCRIPT_INDEX.md and R/87 expected counts</name>
  <files>R/SCRIPT_INDEX.md, R/87_smoke_test_full_pipeline.R</files>
  <read_first>
    - R/SCRIPT_INDEX.md (current state: says "8 utils" and "8 in R/utils/" but actual count is 10)
    - R/87_smoke_test_full_pipeline.R (line 64-68: expects `utils_count == 8`, needs update to 10)
    - R/86_smoke_test_foundation.R (line 63-66: expects `length(utils_files) == 8`, needs update to 10)
  </read_first>
  <action>
**Update R/SCRIPT_INDEX.md:**

1. In the "Foundation (00-03)" table, update R/00_config.R Sources column from `(auto-sources all 8 R/utils/ modules)` to `(auto-sources all 10 R/utils/ modules)`

2. In the "Foundation (00-03)" table, update R/00_config.R Purpose from mentioning "8 utility modules" to "10 utility modules" (in the Purpose text: `Auto-sources all 8 utility modules from R/utils/` -> `Auto-sources all 10 utility modules from R/utils/`)

3. In the "Utility Libraries" section heading, update from "Sourced by 00_config.R (auto-loaded via list.files() from R/utils/ subfolder). These define reusable functions, not standalone analyses." (keep as is but ensure table has 10 rows)

4. Add the 2 missing utils to the Utility Libraries table (add after existing entries in alphabetical order):
   - `utils/utils_assertions.R` | Checkmate-based defensive coding helpers (assert_rds_exists, assert_df_valid, assert_col_types, warn_date_range, warn_row_count) | 00_config
   - `utils/utils_cancer.R` | Cancer site classification using CANCER_SITE_MAP (classify_codes) | 00_config

5. In the "Script Count" section at bottom:
   - Change `**Utility libraries:** 8 (in R/utils/ subfolder)` to `**Utility libraries:** 10 (in R/utils/ subfolder)`
   - Change `**Total:** 83` to `**Total:** 85` (67 + 10 + 8)

6. In Testing (80-87) section, add new row for R/88:
   - `88_smoke_test_comprehensive.R` | Comprehensive structural smoke test: consolidates R/86+R/87 checks plus DRY validation, config constants, defensive coding infrastructure, cross-platform data gating | 00_config

7. Update Test decade count in Script Count from `Tests (80-87): 8` to `Tests (80-88): 9`

8. Update total numbered from 67 to 68, and Total from 85 to 86 (68 + 10 + 8)

**Update R/87_smoke_test_full_pipeline.R:**
- Line 64-68: Change `utils_count == 8` check to `utils_count == 10`
- Update the message string on line 66 from `"R/utils/ contains 8 modules"` to `"R/utils/ contains 10 modules"`

**Update R/86_smoke_test_foundation.R:**
- Line 63-66: Change the count check from `length(utils_files) == 8` to `length(utils_files) == 10`
- Update the message string from `"R/utils/ contains 8 files"` to `"R/utils/ contains 10 files"`
- Update the `expected_utils` vector (lines 68-72) to add `"utils_assertions.R"` and `"utils_cancer.R"` to the list
  </action>
  <verify>
    <automated>cd C:/Users/Owner/Documents/insurance_investigation && Rscript R/86_smoke_test_foundation.R && Rscript R/87_smoke_test_full_pipeline.R</automated>
  </verify>
  <acceptance_criteria>
    - R/SCRIPT_INDEX.md contains "auto-sources all 10" (not "all 8")
    - R/SCRIPT_INDEX.md contains "utils_assertions.R" in Utility Libraries table
    - R/SCRIPT_INDEX.md contains "utils_cancer.R" in Utility Libraries table
    - R/SCRIPT_INDEX.md contains "Utility libraries:** 10"
    - R/SCRIPT_INDEX.md contains "88_smoke_test_comprehensive.R" in Testing section
    - R/SCRIPT_INDEX.md contains "Tests (80-88): 9"
    - R/SCRIPT_INDEX.md contains "Total numbered:** 68"
    - R/87_smoke_test_full_pipeline.R contains "utils_count == 10"
    - R/86_smoke_test_foundation.R contains "length(utils_files) == 10"
    - R/86_smoke_test_foundation.R contains "utils_assertions.R"
    - R/86_smoke_test_foundation.R contains "utils_cancer.R"
    - Rscript R/86_smoke_test_foundation.R exits with code 0
    - Rscript R/87_smoke_test_full_pipeline.R exits with code 0
  </acceptance_criteria>
  <done>SCRIPT_INDEX.md accurately reflects 10 utils modules, 68 numbered scripts, 86 total files. R/86 and R/87 pass with updated expected counts of 10 utils. R/88 listed in testing decade.</done>
</task>

</tasks>

<verification>
1. `Rscript R/88_smoke_test_comprehensive.R` exits 0 with all checks passing
2. `Rscript R/86_smoke_test_foundation.R` exits 0 (updated utils count)
3. `Rscript R/87_smoke_test_full_pipeline.R` exits 0 (updated utils count)
4. R/SCRIPT_INDEX.md Script Count section shows: 68 numbered, 10 utils, 8 archived, 86 total
</verification>

<success_criteria>
- Comprehensive smoke test validates: utils completeness (10), config constants (11), source() resolution (0 broken), DRY compliance (no duplicate PREFIX_MAP/TIER_MAPPING), defensive coding infrastructure (checkmate + assertions), decade counts, archive structure, cross-platform data gating
- All three smoke tests (R/86, R/87, R/88) pass on Windows
- SCRIPT_INDEX.md is fully accurate with current filesystem state
</success_criteria>

<output>
After completion, create `.planning/phases/74-smoke-testing-reference-manual/74-01-SUMMARY.md`
</output>
