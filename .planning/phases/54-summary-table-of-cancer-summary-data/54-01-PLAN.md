---
phase: 54-summary-table-of-cancer-summary-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/54_cancer_summary_table.R
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15

must_haves:
  truths:
    - "User can open cancer_summary_table.xlsx and see category-level patient counts, confirmation rates, date stats, and record counts on Sheet 1"
    - "User can open Sheet 2 and see the same metrics broken down by individual ICD-10 code"
    - "Both sheets are sorted by total_patients descending so the most common cancers appear first"
    - "Both sheets have a styled totals row at the bottom with summed counts"
    - "Confirmation rates show both absolute counts AND percentages"
    - "Record counts reflect total DIAGNOSIS rows (not just unique dates) from DuckDB"
  artifacts:
    - path: "R/54_cancer_summary_table.R"
      provides: "Aggregation script producing cancer_summary_table.xlsx"
      min_lines: 200
      contains: "PREFIX_MAP"
    - path: "output/tables/cancer_summary_table.xlsx"
      provides: "Two-sheet styled xlsx with category and code summaries"
  key_links:
    - from: "R/54_cancer_summary_table.R"
      to: "output/tables/cancer_summary.csv"
      via: "read.csv() to load Phase 6 patient-code level data"
      pattern: "cancer_summary\\.csv"
    - from: "R/54_cancer_summary_table.R"
      to: "DIAGNOSIS DuckDB table"
      via: "get_pcornet_table('DIAGNOSIS') for record counts"
      pattern: "get_pcornet_table.*DIAGNOSIS"
    - from: "R/54_cancer_summary_table.R"
      to: "output/tables/cancer_summary_table.xlsx"
      via: "wb$save() writing styled two-sheet workbook"
      pattern: "cancer_summary_table\\.xlsx"
---

<objective>
Create R/54_cancer_summary_table.R that reads the Phase 6 cancer_summary patient-code level dataset, queries DuckDB for record counts, aggregates to category-level and code-level summaries, and writes a styled two-sheet cancer_summary_table.xlsx to output/tables/.

Purpose: Provide a high-level companion to cancer_summary.xlsx — someone can open it to get the aggregate picture (patient counts, confirmation rates, date distribution, encounter volume) without needing the patient-level detail.

Output: R/54_cancer_summary_table.R script + cancer_summary_table.xlsx (produced on HiPerGator)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-summary-table-of-cancer-summary-data/07-CONTEXT.md
@.planning/phases/07-summary-table-of-cancer-summary-data/07-RESEARCH.md

@R/53_cancer_summary.R
@R/50_cancer_site_confirmation.R
@R/47_cancer_site_frequency.R
@R/52_all_codes_resolved.R
@R/01_load_pcornet.R
@R/00_config.R

<interfaces>
<!-- Key patterns and contracts the executor needs from existing scripts -->

From R/53_cancer_summary.R — Phase 6 output columns (the INPUT to this script):
```
Columns:
  ID                            - Patient identifier
  cancer_code                   - Normalized ICD-10 code (uppercase, no dots)
  description                   - Cancer site category | code-level description
  two_or_more_unique_dates      - 1 if 2+ distinct non-NA DX_DATEs, else 0
  two_or_more_unique_dates_gt_7 - 1 if 2+ distinct dates AND span >= 7 days, else 0
  unique_dates_total            - Count of distinct non-NA DX_DATEs
  unique_dates_with_sep_gt_7    - Count of dates >= 7 days from at least one other date
```
Output files: output/tables/cancer_summary.csv and cancer_summary.xlsx

From R/50_cancer_site_confirmation.R — openxlsx2 styling constants:
```R
DARK_HEADER_FILL <- "FF374151"
WHITE_FONT       <- "FFFFFFFF"
TITLE_FONT_COLOR <- "FF1F2937"
TOTALS_FILL      <- "FFE5E7EB"
```

From R/50 — Styling pattern (title row 1, headers row 2, data row 3+, totals after data):
```R
wb <- wb_workbook()
wb$add_worksheet(SHEET_NAME)
# Row 1: Title with merged cells, Calibri 16pt bold, TITLE_FONT_COLOR
# Row 2: Headers with DARK_HEADER_FILL background, WHITE_FONT, Calibri 11pt bold
# Freeze pane at row 3
# Data rows from row 3: add_numfmt "#,##0" for integers, "0.0%" for percentages, "0.0" for decimals
# Totals row: TOTALS_FILL background, bold, TITLE_FONT_COLOR, summed counts, NA for rates/means
```

From R/01_load_pcornet.R — DuckDB access:
```R
get_pcornet_table("DIAGNOSIS")  # Returns lazy DuckDB table
close_pcornet_con()             # Cleanup
```

From R/00_config.R:
```R
CONFIG$output_dir  # "output"
```

From R/53 — PREFIX_MAP and classify_codes() (copy for script independence):
```R
PREFIX_MAP <- c("C00" = "Lip, Oral Cavity and Pharynx", ...)  # 319 prefixes -> 54 categories
classify_codes <- function(codes) {
  prefix3 <- substr(codes, 1, 3)
  categories <- unname(PREFIX_MAP[prefix3])
  categories
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/54_cancer_summary_table.R aggregation and styled xlsx script</name>
  <files>R/54_cancer_summary_table.R</files>

  <read_first>
    R/53_cancer_summary.R
    R/50_cancer_site_confirmation.R
    R/47_cancer_site_frequency.R
    R/52_all_codes_resolved.R
    R/01_load_pcornet.R
    R/00_config.R
  </read_first>

  <action>
Create R/54_cancer_summary_table.R with the following sections:

**SECTION 1: SETUP**
- Load libraries: dplyr, stringr, glue, openxlsx2 (suppressPackageStartupMessages)
- source("R/00_config.R") and source("R/01_load_pcornet.R")
- Define OUTPUT_PATH <- file.path(CONFIG$output_dir, "tables", "cancer_summary_table.xlsx") per D-10, D-11
- dir.create(dirname(OUTPUT_PATH), showWarnings = FALSE, recursive = TRUE)
- message("=== Phase 7: Cancer Summary Table ===")

**SECTION 2: PREFIX_MAP AND classify_codes()**
- Copy the complete PREFIX_MAP named vector from R/53_cancer_summary.R (lines 59-319, all 319 prefix-to-category mappings). This is the standard pattern — each script copies it for independence.
- Copy the classify_codes() function from R/53 (lines 324-328):
```R
classify_codes <- function(codes) {
  prefix3 <- substr(codes, 1, 3)
  categories <- unname(PREFIX_MAP[prefix3])
  categories
}
```

**SECTION 3: LOAD SOURCE DATA**
- Read Phase 6 output: `cancer_summary <- read.csv(file.path(CONFIG$output_dir, "tables", "cancer_summary.csv"), stringsAsFactors = FALSE)` per D-12, D-13
- Log: message(glue("Loaded {nrow(cancer_summary)} patient-code rows"))
- Classify codes to add category column: `cancer_summary$category <- classify_codes(cancer_summary$cancer_code)`
- Handle unclassified: if any NA in category, set to "Unclassified" (same pattern as R/53 lines 363-370)
- Log unique patients, codes, categories

**SECTION 4: QUERY RECORD COUNTS FROM DIAGNOSIS (DuckDB)**
Per D-07 — record counts are total DIAGNOSIS rows, not unique dates.
```R
message("\nQuerying DIAGNOSIS for record counts...")
dx_record_counts <- get_pcornet_table("DIAGNOSIS") %>%
  filter(DX_TYPE == "10") %>%
  mutate(DX_norm = toupper(str_remove_all(DX, "\\."))) %>%
  filter(str_detect(DX_norm, "^[CD]")) %>%
  group_by(DX_norm) %>%
  summarise(record_count = n(), .groups = "drop") %>%
  collect()
message(glue("  Found record counts for {nrow(dx_record_counts)} unique codes"))
```
Join to patient-level data:
```R
cancer_summary <- cancer_summary %>%
  left_join(dx_record_counts, by = c("cancer_code" = "DX_norm")) %>%
  mutate(record_count = ifelse(is.na(record_count), 0L, as.integer(record_count)))
```

**SECTION 5: CATEGORY-LEVEL AGGREGATION**
Per D-01, D-02, D-04, D-05, D-06, D-07, D-14:
```R
category_summary <- cancer_summary %>%
  group_by(category) %>%
  summarise(
    total_patients       = n_distinct(ID),
    confirmed_2date      = sum(two_or_more_unique_dates, na.rm = TRUE),
    pct_confirmed_2date  = sum(two_or_more_unique_dates, na.rm = TRUE) / n_distinct(ID),
    confirmed_7day       = sum(two_or_more_unique_dates_gt_7, na.rm = TRUE),
    pct_confirmed_7day   = sum(two_or_more_unique_dates_gt_7, na.rm = TRUE) / n_distinct(ID),
    mean_unique_dates    = mean(unique_dates_total, na.rm = TRUE),
    median_unique_dates  = median(unique_dates_total, na.rm = TRUE),
    mean_dates_7day_sep  = mean(unique_dates_with_sep_gt_7, na.rm = TRUE),
    median_dates_7day_sep = median(unique_dates_with_sep_gt_7, na.rm = TRUE),
    total_records        = sum(record_count, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  arrange(desc(total_patients))
```
Log: message(glue("Category summary: {nrow(category_summary)} rows"))

**SECTION 6: CODE-LEVEL AGGREGATION**
Per D-01, D-03, same metrics grouped by cancer_code + category:
```R
code_summary <- cancer_summary %>%
  group_by(cancer_code, category) %>%
  summarise(
    total_patients       = n_distinct(ID),
    confirmed_2date      = sum(two_or_more_unique_dates, na.rm = TRUE),
    pct_confirmed_2date  = sum(two_or_more_unique_dates, na.rm = TRUE) / n_distinct(ID),
    confirmed_7day       = sum(two_or_more_unique_dates_gt_7, na.rm = TRUE),
    pct_confirmed_7day   = sum(two_or_more_unique_dates_gt_7, na.rm = TRUE) / n_distinct(ID),
    mean_unique_dates    = mean(unique_dates_total, na.rm = TRUE),
    median_unique_dates  = median(unique_dates_total, na.rm = TRUE),
    mean_dates_7day_sep  = mean(unique_dates_with_sep_gt_7, na.rm = TRUE),
    median_dates_7day_sep = median(unique_dates_with_sep_gt_7, na.rm = TRUE),
    total_records        = sum(record_count, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  arrange(desc(total_patients))
```
Note: each row in cancer_summary is already one patient-code, so n_distinct(ID) is effectively n() for the code-level grouping. But use n_distinct(ID) for safety in case data has duplicates.

Log: message(glue("Code summary: {nrow(code_summary)} rows"))

**SECTION 7: BUILD TOTALS ROWS**
Follow R/50 pattern (lines 496-514) — sum counts, NA for rates/means:
```R
totals_category <- tibble(
  category              = "TOTAL",
  total_patients        = sum(category_summary$total_patients),
  confirmed_2date       = sum(category_summary$confirmed_2date),
  pct_confirmed_2date   = NA_real_,
  confirmed_7day        = sum(category_summary$confirmed_7day),
  pct_confirmed_7day    = NA_real_,
  mean_unique_dates     = NA_real_,
  median_unique_dates   = NA_real_,
  mean_dates_7day_sep   = NA_real_,
  median_dates_7day_sep = NA_real_,
  total_records         = sum(category_summary$total_records)
)

totals_code <- tibble(
  cancer_code           = "TOTAL",
  category              = "",
  total_patients        = sum(code_summary$total_patients),
  confirmed_2date       = sum(code_summary$confirmed_2date),
  pct_confirmed_2date   = NA_real_,
  confirmed_7day        = sum(code_summary$confirmed_7day),
  pct_confirmed_7day    = NA_real_,
  mean_unique_dates     = NA_real_,
  median_unique_dates   = NA_real_,
  mean_dates_7day_sep   = NA_real_,
  median_dates_7day_sep = NA_real_,
  total_records         = sum(code_summary$total_records)
)
```

**SECTION 8: WRITE STYLED XLSX**
Per D-08, D-09 — two-sheet xlsx with dark header styling matching R/47 and R/50 patterns.

Define styling constants (same as R/50):
```R
DARK_HEADER_FILL <- "FF374151"
WHITE_FONT       <- "FFFFFFFF"
TITLE_FONT_COLOR <- "FF1F2937"
TOTALS_FILL      <- "FFE5E7EB"
```

**Sheet 1: "Category Summary"**
- Row 1: Title "Cancer Summary Table - By Category" merged across all 11 columns (A1:K1), Calibri 16pt bold, TITLE_FONT_COLOR
- Row 2: Headers with DARK_HEADER_FILL + WHITE_FONT, Calibri 11pt bold. Header names (11 columns):
  1. "Cancer Site Category"
  2. "Total Patients"
  3. "Confirmed (2+ Dates)"
  4. "Rate (2+ Dates)"
  5. "Confirmed (7-Day Gap)"
  6. "Rate (7-Day Gap)"
  7. "Mean Unique Dates"
  8. "Median Unique Dates"
  9. "Mean Dates (7-Day Sep)"
  10. "Median Dates (7-Day Sep)"
  11. "Total Records"
- Freeze pane: first_active_row = 3, first_active_col = 1
- Data rows starting at row 3: write category_summary as data.frame, col_names = FALSE
- Number formatting:
  - Columns B, C, E (integer counts): "#,##0"
  - Columns D, F (percentage rates): "0.0%"
  - Columns G, H, I, J (mean/median decimals): "0.0"
  - Column K (total records): "#,##0"
- Totals row after data: write totals_category, apply TOTALS_FILL background, bold font TITLE_FONT_COLOR, numfmt "#,##0" on count columns (B, C, E, K)
- Column widths: c(40, 14, 18, 14, 18, 14, 16, 16, 18, 18, 14)

**Sheet 2: "Code Summary"**
- Row 1: Title "Cancer Summary Table - By ICD-10 Code" merged across all 12 columns (A1:L1), Calibri 16pt bold, TITLE_FONT_COLOR
- Row 2: Headers (12 columns):
  1. "ICD-10 Code"
  2. "Cancer Site Category"
  3. "Total Patients"
  4. "Confirmed (2+ Dates)"
  5. "Rate (2+ Dates)"
  6. "Confirmed (7-Day Gap)"
  7. "Rate (7-Day Gap)"
  8. "Mean Unique Dates"
  9. "Median Unique Dates"
  10. "Mean Dates (7-Day Sep)"
  11. "Median Dates (7-Day Sep)"
  12. "Total Records"
- Same styling pattern as Sheet 1 (dark header, freeze pane at row 3)
- Data rows: write code_summary, col_names = FALSE
- Number formatting:
  - Columns C, D, F (integer counts): "#,##0"
  - Columns E, G (percentage rates): "0.0%"
  - Columns H, I, J, K (mean/median decimals): "0.0"
  - Column L (total records): "#,##0"
- Totals row: totals_code with TOTALS_FILL, bold, numfmt on count columns (C, D, F, L)
- Column widths: c(14, 40, 14, 18, 14, 18, 14, 16, 16, 18, 18, 14)

Save: wb$save(OUTPUT_PATH)
Log summary of what was written.

**SECTION 9: CLEANUP**
- close_pcornet_con()
- message("\n=== Phase 7 complete ===")

**Important notes:**
- Per D-03: Include ALL codes, no minimum patient count threshold
- Per D-12: All patients in DIAGNOSIS (not restricted to HL cohort) — consistent with Phase 6 input
- Per D-09: xlsx only, no CSV output
- Per D-14: Both sheets sorted by total_patients descending
- The description column from Phase 6 input is NOT included in the summary table — it is a patient-level field. The category column provides the grouping label.
- For the code-level sheet, the category column is included so the user can see which cancer site each code belongs to.
  </action>

  <verify>
    <automated>Rscript -e "source('R/54_cancer_summary_table.R')" should parse without syntax errors on local R (script won't fully execute without DuckDB data, but syntax validation confirms structure)</automated>
  </verify>

  <acceptance_criteria>
    - R/54_cancer_summary_table.R exists and is >= 200 lines
    - File contains "PREFIX_MAP <- c(" (the full 319-prefix mapping)
    - File contains "classify_codes <- function(codes)"
    - File contains 'get_pcornet_table("DIAGNOSIS")' for DuckDB record count query per D-07
    - File contains "group_by(category)" for category-level aggregation per D-02
    - File contains "group_by(cancer_code, category)" for code-level aggregation per D-03
    - File contains "n_distinct(ID)" for patient counts per D-04
    - File contains "sum(two_or_more_unique_dates" for confirmation counts per D-05
    - File contains "sum(two_or_more_unique_dates_gt_7" for 7-day confirmation per D-05
    - File contains "mean(unique_dates_total" and "median(unique_dates_total" per D-06
    - File contains "mean(unique_dates_with_sep_gt_7" and "median(unique_dates_with_sep_gt_7" per D-06
    - File contains "sum(record_count" for total DIAGNOSIS rows per D-07
    - File contains "arrange(desc(total_patients))" for descending sort per D-14
    - File contains 'DARK_HEADER_FILL <- "FF374151"' per D-08
    - File contains 'WHITE_FONT <- "FFFFFFFF"' per D-08
    - File contains 'TOTALS_FILL <- "FFE5E7EB"' per D-08
    - File contains 'wb$add_worksheet("Category Summary")' per D-01
    - File contains 'wb$add_worksheet("Code Summary")' per D-01
    - File contains "wb$freeze_pane" per D-08
    - File contains 'cancer_summary_table.xlsx' in the output path per D-10
    - File contains "close_pcornet_con()" for cleanup
    - File does NOT contain "write.csv" (no CSV output per D-09)
  </acceptance_criteria>

  <done>
    R/54_cancer_summary_table.R is a complete, self-contained script that:
    (1) reads cancer_summary.csv from Phase 6,
    (2) queries DuckDB DIAGNOSIS for record counts,
    (3) aggregates to category-level and code-level summaries with patient counts, confirmation rates (absolute + %), date distribution stats (mean + median), and record counts,
    (4) writes a styled two-sheet cancer_summary_table.xlsx with dark headers, freeze panes, number formatting, totals rows, and descending patient count sort.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute R/54_cancer_summary_table.R on HiPerGator and verify output</name>
  <files>output/tables/cancer_summary_table.xlsx</files>

  <action>
User executes the script on HiPerGator where the DuckDB data resides. Claude cannot access HiPerGator directly.

1. On HiPerGator, navigate to the project directory
2. Run: Rscript R/54_cancer_summary_table.R
3. Confirm script completes without errors and reports row counts for both sheets
4. Open output/tables/cancer_summary_table.xlsx in Excel or a viewer
5. Verify Sheet 1 "Category Summary":
   - Has dark grey header row with white text
   - Columns: Cancer Site Category, Total Patients, Confirmed (2+ Dates), Rate (2+ Dates), Confirmed (7-Day Gap), Rate (7-Day Gap), Mean Unique Dates, Median Unique Dates, Mean Dates (7-Day Sep), Median Dates (7-Day Sep), Total Records
   - Rows sorted by Total Patients descending (most common category at top)
   - Totals row at bottom with grey background
   - Patient counts have comma separators; rates show as percentages with 1 decimal
6. Verify Sheet 2 "Code Summary":
   - Same styling pattern with an extra ICD-10 Code column
   - ALL codes present (no minimum threshold)
   - Sorted by Total Patients descending
   - Totals row at bottom
7. Spot-check: confirm record counts are higher than patient counts for common categories (patients have multiple encounters per code)
  </action>

  <verify>User confirms script ran without errors and output/tables/cancer_summary_table.xlsx contains two correctly styled sheets</verify>

  <done>
    cancer_summary_table.xlsx exists in output/tables/ with two correctly styled sheets:
    (1) Category Summary with ~54 rows sorted by patient count descending,
    (2) Code Summary with all unique ICD-10 codes sorted by patient count descending.
    Both have dark headers, freeze panes, number formatting, and a totals row.
  </done>
</task>

</tasks>

<verification>
- R/54_cancer_summary_table.R parses without syntax errors
- Script produces output/tables/cancer_summary_table.xlsx on HiPerGator
- Sheet 1 "Category Summary" has ~54 data rows (one per category with patients)
- Sheet 2 "Code Summary" has one row per unique ICD-10 code
- Both sheets have styled headers, freeze panes, number formatting, totals rows
- Both sheets sorted by total_patients descending
- Record counts (from DuckDB) are present and higher than patient counts for common codes
</verification>

<success_criteria>
- cancer_summary_table.xlsx exists in output/tables/ with two sheets
- Category sheet shows patient counts, confirmation rates (absolute + %), date stats (mean + median), and record counts per cancer site category
- Code sheet shows the same metrics per individual ICD-10 code
- Styling matches project standard: dark headers (FF374151), white font, freeze panes, number formatting, totals row with grey background
- No CSV output (xlsx only per D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/07-summary-table-of-cancer-summary-data/07-01-SUMMARY.md`
</output>
