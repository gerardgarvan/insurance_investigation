---
phase: 57-gantt-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/01_load_pcornet.R
  - R/49_gantt_data_export.R
autonomous: false
requirements: [GANTT-01, GANTT-02, GANTT-03]
user_setup:
  - service: HiPerGator DuckDB re-ingest
    why: "DEATH table must be ingested into DuckDB before R/49 can query it"
    dashboard_config:
      - task: "Re-run R/25_duckdb_ingest.R after config changes to ingest DEATH table"
        location: "HiPerGator RStudio session"

must_haves:
  truths:
    - "Each treatment episode row in gantt_episodes.csv has a cancer_category column with comma-separated cancer site category names"
    - "Each treatment episode row in gantt_detail.csv has a cancer_category column"
    - "Each episode row has an is_hodgkin column that is TRUE when Hodgkin Lymphoma appears in cancer_category"
    - "Death dates from DEATH table appear as pseudo-treatment rows with treatment_type = Death"
    - "Death rows appear in BOTH gantt_episodes.csv and gantt_detail.csv"
    - "1900 sentinel death dates are nullified and excluded"
    - "DEATH table is registered in PCORNET_TABLES and has a DEATH_SPEC load definition"
  artifacts:
    - path: "R/00_config.R"
      provides: "DEATH in PCORNET_TABLES vector"
      contains: '"DEATH"'
    - path: "R/01_load_pcornet.R"
      provides: "DEATH_SPEC column specification"
      contains: "DEATH_SPEC"
    - path: "R/49_gantt_data_export.R"
      provides: "Cancer category enrichment, is_hodgkin flag, death pseudo-treatment rows"
      contains: "cancer_category"
  key_links:
    - from: "R/49_gantt_data_export.R"
      to: "output/tables/cancer_summary.csv"
      via: "read.csv to load patient-code data, PREFIX_MAP to derive categories"
      pattern: "cancer_summary.*read\\.csv|PREFIX_MAP"
    - from: "R/49_gantt_data_export.R"
      to: "DuckDB DEATH table"
      via: "get_pcornet_table('DEATH') query"
      pattern: 'get_pcornet_table.*DEATH'
    - from: "R/49_gantt_data_export.R"
      to: "gantt_episodes.csv"
      via: "write.csv with cancer_category, is_hodgkin, and death rows"
      pattern: "write\\.csv.*episodes"
    - from: "R/00_config.R"
      to: "R/25_duckdb_ingest.R"
      via: "PCORNET_TABLES vector consumed by ingest script"
      pattern: "PCORNET_TABLES"
---

<objective>
Enrich Gantt chart CSV exports with cancer category labels, is_hodgkin binary flags, and death date pseudo-treatment rows. Also add full pipeline integration for the DEATH table (config registration + load spec).

Purpose: Enable downstream Gantt chart visualization to color-code treatment episodes by cancer type, identify Hodgkin Lymphoma patients at a glance, and show death as a clinical endpoint on patient timelines.

Output: Modified R/00_config.R (DEATH in PCORNET_TABLES), R/01_load_pcornet.R (DEATH_SPEC), R/49_gantt_data_export.R (cancer categories, is_hodgkin, death rows), and updated gantt_episodes.csv + gantt_detail.csv with new columns and death rows.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-gantt-enhancements/57-CONTEXT.md
@.planning/phases/57-gantt-enhancements/57-RESEARCH.md
@.planning/phases/55-cancer-summary-refinement-foundation/55-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/49_gantt_data_export.R (current episode export columns):
```r
# Episode-level bars table columns (current):
# patient_id, treatment_type, episode_number, episode_start, episode_stop,
# episode_length_days, distinct_dates_in_episode, historical_flag,
# triggering_codes, triggering_code_descriptions

# Detail-level ticks table columns (current):
# patient_id, treatment_type, treatment_date, triggering_code,
# episode_number, episode_start, episode_stop, historical_flag,
# triggering_code_description
```

From output/tables/cancer_summary.csv (R/55 output, 7 columns):
```
ID, cancer_code, description, two_or_more_unique_dates,
two_or_more_unique_dates_gt_7, unique_dates_total, unique_dates_with_sep_gt_7
```
NOTE: cancer_summary.csv does NOT contain a "category" column.
The category must be derived from cancer_code using PREFIX_MAP + classify_codes().

From R/55_cancer_summary_refined.R (classify_codes pattern):
```r
classify_codes <- function(codes) {
  prefixes <- substr(toupper(codes), 1, 3)
  categories <- PREFIX_MAP[prefixes]
  unname(categories)
}
```

From R/utils_duckdb.R:
```r
open_pcornet_con(db_path = CONFIG$cache$duckdb_path, read_only = TRUE)
get_pcornet_table(table_name, con = NULL)  # returns tbl_dbi lazy query or tibble
close_pcornet_con()
```

From R/00_config.R (PCORNET_TABLES vector, lines 108-123):
```r
PCORNET_TABLES <- c(
  "ENROLLMENT", "DIAGNOSIS", "CONDITION", "PROCEDURES",
  "PRESCRIBING", "ENCOUNTER", "DEMOGRAPHIC",
  "TUMOR_REGISTRY1", "TUMOR_REGISTRY2", "TUMOR_REGISTRY3",
  "DISPENSING", "MED_ADMIN", "LAB_RESULT_CM", "PROVIDER"
)
```

From R/01_load_pcornet.R (DEMOGRAPHIC_SPEC pattern, lines 172-185):
```r
DEMOGRAPHIC_SPEC <- cols(
  ID = col_character(),
  BIRTH_DATE = col_character(),
  # ... all columns as col_character() ...
  SOURCE = col_character()
)
```

DEATH table columns (from user, D-04):
ID, DEATH_DATE, DEATH_DATE_IMPUTE, DEATH_SOURCE, DEATH_MATCH_CONFIDENCE, SOURCE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DEATH table to pipeline infrastructure (config + load spec)</name>
  <files>R/00_config.R, R/01_load_pcornet.R</files>
  <read_first>R/00_config.R, R/01_load_pcornet.R</read_first>
  <action>
**R/00_config.R changes (per D-05):**

Find the PCORNET_TABLES vector (lines 108-123). Add "DEATH" as the last entry after "PROVIDER". The result should be:

```r
PCORNET_TABLES <- c(
  "ENROLLMENT",
  "DIAGNOSIS",
  "CONDITION",        # PCORnet CDM: diagnosed/self-reported health conditions
  "PROCEDURES",
  "PRESCRIBING",
  "ENCOUNTER",
  "DEMOGRAPHIC",
  "TUMOR_REGISTRY1",
  "TUMOR_REGISTRY2",
  "TUMOR_REGISTRY3",
  "DISPENSING",       # Phase 9: expanded treatment detection
  "MED_ADMIN",        # Phase 9: expanded treatment detection
  "LAB_RESULT_CM",    # Phase 10: surveillance lab values (LOINC-based matching)
  "PROVIDER",         # Phase 10: oncology provider specialty matching
  "DEATH"             # Phase 57: death dates for Gantt chart endpoint
)
```

Note: "PROVIDER" line needs a comma added after it since it's no longer the last entry.

**R/01_load_pcornet.R changes (per D-05):**

After the DEMOGRAPHIC_SPEC block (line 185), add a new DEATH_SPEC block. Follow the exact pattern of DEMOGRAPHIC_SPEC. All columns loaded as col_character() because DEATH_DATE must be parsed in R after loading (per RESEARCH.md anti-pattern warning about date parsing). The DEATH table has 6 columns per D-04:

```r
# ------------------------------------------------------------------------------
# 6b. DEATH (6 columns)
# ------------------------------------------------------------------------------
# Phase 57: Death dates for Gantt chart endpoint visualization.
# DEATH_DATE loaded as character for downstream lubridate parsing
# (same pattern as BIRTH_DATE in DEMOGRAPHIC_SPEC).
# Source file: DEATH_Mailhot_V1.csv
DEATH_SPEC <- cols(
  ID = col_character(),
  DEATH_DATE = col_character(),
  DEATH_DATE_IMPUTE = col_character(),
  DEATH_SOURCE = col_character(),
  DEATH_MATCH_CONFIDENCE = col_character(),
  SOURCE = col_character()
)
```

Also, find the section in R/01_load_pcornet.R where table specs are mapped to table names for loading (look for where DEMOGRAPHIC_SPEC is referenced in a dispatch/map or a loading loop). If there is a named list or dispatch that maps table names to specs, add `DEATH = DEATH_SPEC` to it. If the loading uses PCORNET_TABLES with a default spec, the DEATH_SPEC just needs to be defined.

Search for where specs are used (grep for "SPEC" assignments being used in loading logic). The key is that when R/25_duckdb_ingest.R runs, it reads RDS files from cache. The RDS files are created by the loading pipeline. Ensure DEATH_SPEC is wired into whatever mechanism creates `DEATH.rds` in the cache directory.
  </action>
  <verify>
    <automated>grep -n "DEATH" R/00_config.R && grep -n "DEATH_SPEC" R/01_load_pcornet.R</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains the string `"DEATH"` in the PCORNET_TABLES vector
    - R/00_config.R line after "PROVIDER" has a comma (PROVIDER is no longer last element)
    - R/01_load_pcornet.R contains `DEATH_SPEC <- cols(`
    - R/01_load_pcornet.R DEATH_SPEC has exactly 6 columns: ID, DEATH_DATE, DEATH_DATE_IMPUTE, DEATH_SOURCE, DEATH_MATCH_CONFIDENCE, SOURCE
    - All 6 columns in DEATH_SPEC use col_character()
    - DEATH_SPEC block includes a comment mentioning "Phase 57" and "DEATH_Mailhot_V1.csv"
  </acceptance_criteria>
  <done>DEATH table is registered in PCORNET_TABLES and has a DEATH_SPEC column specification. After user re-runs R/25_duckdb_ingest.R, DEATH table will be available via get_pcornet_table("DEATH").</done>
</task>

<task type="auto">
  <name>Task 2: Enrich R/49 with cancer categories, is_hodgkin, and death rows</name>
  <files>R/49_gantt_data_export.R</files>
  <read_first>R/49_gantt_data_export.R, R/55_cancer_summary_refined.R, R/utils_duckdb.R, R/00_config.R</read_first>
  <action>
Modify R/49_gantt_data_export.R to add three enrichments. The script currently has 6 sections. The modified script will have 9 sections. Here is the exact modification plan:

**SECTION 1 changes: Add lubridate to library block and add new input paths.**

Add `library(lubridate)` to the suppressPackageStartupMessages block.

Add after the DESCRIPTIONS_RDS path definition:
```r
# Cancer summary source (R/55 output, patient-code level with cancer_code column)
CANCER_SUMMARY_CSV <- file.path(CONFIG$output_dir, "tables", "cancer_summary.csv")
```

Add `source("R/utils_duckdb.R")` after `source("R/00_config.R")`.

**NEW SECTION 2B: LOAD AND AGGREGATE CANCER CATEGORIES (after Section 2, before Section 3)**

This implements D-01, D-02, D-03 (cancer category assignment from cancer_summary.csv).

```r
# --- SECTION 2B: LOAD AND AGGREGATE CANCER CATEGORIES (per D-01, D-02, D-03) ---

# Cancer summary CSV has columns: ID, cancer_code, description, ...
# It does NOT have a "category" column -- we must derive it via PREFIX_MAP
if (!file.exists(CANCER_SUMMARY_CSV)) {
  stop(glue("ERROR: {CANCER_SUMMARY_CSV} not found. Run R/55_cancer_summary_refined.R first."))
}

cancer_summary <- read.csv(CANCER_SUMMARY_CSV, stringsAsFactors = FALSE)
message(glue("  Loaded {format(nrow(cancer_summary), big.mark = ',')} cancer summary rows"))

# PREFIX_MAP: maps 3-character ICD-10-CM prefixes to cancer site categories
# Copied from R/55_cancer_summary_refined.R for script independence (project pattern)
PREFIX_MAP <- c(
  # [FULL PREFIX_MAP CONTENTS HERE - copy from R/55 lines 69-336]
  # The executor MUST read R/55_cancer_summary_refined.R and copy the complete
  # PREFIX_MAP definition (approximately 270 lines, ~309 prefix mappings)
)

# classify_codes: derive category from cancer_code using PREFIX_MAP
classify_codes <- function(codes) {
  prefixes <- substr(toupper(codes), 1, 3)
  categories <- PREFIX_MAP[prefixes]
  unname(categories)
}

# Derive category for each cancer code
cancer_summary$category <- classify_codes(cancer_summary$cancer_code)
cancer_summary$category[is.na(cancer_summary$category)] <- "Unclassified"

# Aggregate to patient level: comma-separated sorted list of distinct categories (per D-02)
# Alphabetical sort for reproducibility (Claude's discretion, see RESEARCH.md Pitfall 1)
cancer_categories_per_patient <- cancer_summary %>%
  group_by(ID) %>%
  summarise(
    cancer_category = paste(sort(unique(category)), collapse = ","),
    .groups = "drop"
  ) %>%
  mutate(
    is_hodgkin = str_detect(cancer_category, "Hodgkin Lymphoma")  # per D-03
  )

message(glue("  Cancer categories aggregated for {format(nrow(cancer_categories_per_patient), big.mark = ',')} patients"))
message(glue("  Hodgkin Lymphoma patients: {sum(cancer_categories_per_patient$is_hodgkin)}"))
```

**NEW SECTION 2C: LOAD AND PROCESS DEATH DATA (after Section 2B)**

This implements D-04, D-05, D-06, D-07, D-08.

```r
# --- SECTION 2C: LOAD DEATH DATA (per D-04, D-05, D-08) ---

message("\n--- Loading DEATH table ---")

USE_DUCKDB <- TRUE
open_pcornet_con()

death_raw <- get_pcornet_table("DEATH")

if (is.null(death_raw)) {
  warning("DEATH table not found in DuckDB. Death rows will be skipped. Re-run R/25_duckdb_ingest.R after config update.")
  death_data <- tibble(
    ID = character(),
    DEATH_DATE = as.Date(character())
  )
} else {
  death_data <- death_raw %>%
    collect() %>%
    mutate(
      DEATH_DATE = ymd(DEATH_DATE),  # Parse character to Date (per RESEARCH.md Pitfall 3)
      DEATH_DATE = if_else(year(DEATH_DATE) == 1900L, as.Date(NA), DEATH_DATE)  # 1900 sentinel nullification (per D-08)
    ) %>%
    filter(!is.na(DEATH_DATE)) %>%  # Exclude patients with no valid death date (per D-08)
    select(ID, DEATH_DATE)
}

close_pcornet_con()

message(glue("  Patients with valid death dates: {nrow(death_data)}"))
```

**SECTION 4 changes: Add cancer_category and is_hodgkin to episodes_export and detail_export.**

Modify the episodes_export pipeline to left_join cancer_categories_per_patient and handle NAs:

```r
episodes_export <- episodes %>%
  select(
    patient_id, treatment_type, episode_number,
    episode_start, episode_stop, episode_length_days,
    distinct_dates_in_episode, historical_flag, triggering_codes
  ) %>%
  mutate(
    triggering_code_descriptions = sapply(triggering_codes, map_codes_to_descriptions, USE.NAMES = FALSE)
  ) %>%
  left_join(cancer_categories_per_patient, by = c("patient_id" = "ID")) %>%
  mutate(
    cancer_category = ifelse(is.na(cancer_category), "", cancer_category),
    is_hodgkin = ifelse(is.na(is_hodgkin), FALSE, is_hodgkin)
  )
```

Modify the detail_export pipeline similarly:

```r
detail_export <- detail %>%
  select(
    patient_id, treatment_type, treatment_date, triggering_code,
    episode_number, episode_start, episode_stop, historical_flag
  ) %>%
  mutate(
    triggering_code_description = sapply(triggering_code, lookup_description, USE.NAMES = FALSE)
  ) %>%
  left_join(cancer_categories_per_patient, by = c("patient_id" = "ID")) %>%
  mutate(
    cancer_category = ifelse(is.na(cancer_category), "", cancer_category),
    is_hodgkin = ifelse(is.na(is_hodgkin), FALSE, is_hodgkin)
  )
```

**NEW SECTION 4B: BUILD AND APPEND DEATH PSEUDO-TREATMENT ROWS (after Section 4)**

This implements D-06, D-07. Death rows get the patient's cancer categories (Claude's discretion -- makes Death rows filterable by cancer type).

```r
# --- SECTION 4B: BUILD AND APPEND DEATH PSEUDO-TREATMENT ROWS (per D-06, D-07) ---

if (nrow(death_data) > 0) {
  message("\n--- Building death pseudo-treatment rows ---")

  # Join cancer categories to death data (Claude's discretion: Death rows get patient's categories)
  death_with_categories <- death_data %>%
    left_join(cancer_categories_per_patient, by = "ID") %>%
    mutate(
      cancer_category = ifelse(is.na(cancer_category), "", cancer_category),
      is_hodgkin = ifelse(is.na(is_hodgkin), FALSE, is_hodgkin)
    )

  # Build death rows for episodes table (per D-06)
  death_episodes <- death_with_categories %>%
    mutate(
      patient_id = ID,
      treatment_type = "Death",
      episode_number = 1L,
      episode_start = DEATH_DATE,
      episode_stop = DEATH_DATE,
      episode_length_days = 0L,
      distinct_dates_in_episode = 1L,
      historical_flag = FALSE,
      triggering_codes = "",
      triggering_code_descriptions = ""
    ) %>%
    select(
      patient_id, treatment_type, episode_number,
      episode_start, episode_stop, episode_length_days,
      distinct_dates_in_episode, historical_flag,
      triggering_codes, triggering_code_descriptions,
      cancer_category, is_hodgkin
    )

  # Build death rows for detail table (per D-06, D-07)
  death_detail <- death_with_categories %>%
    mutate(
      patient_id = ID,
      treatment_type = "Death",
      treatment_date = DEATH_DATE,
      triggering_code = "",
      episode_number = 1L,
      episode_start = DEATH_DATE,
      episode_stop = DEATH_DATE,
      historical_flag = FALSE,
      triggering_code_description = ""
    ) %>%
    select(
      patient_id, treatment_type, treatment_date,
      triggering_code, episode_number, episode_start,
      episode_stop, historical_flag,
      triggering_code_description,
      cancer_category, is_hodgkin
    )

  # Verify column alignment before binding (per RESEARCH.md Pitfall 4)
  expected_ep_cols <- colnames(episodes_export)
  death_ep_cols <- colnames(death_episodes)
  missing_in_death_ep <- setdiff(expected_ep_cols, death_ep_cols)
  extra_in_death_ep <- setdiff(death_ep_cols, expected_ep_cols)

  if (length(missing_in_death_ep) > 0) {
    stop(glue("Death episodes missing columns: {paste(missing_in_death_ep, collapse = ', ')}"))
  }
  if (length(extra_in_death_ep) > 0) {
    warning(glue("Death episodes has extra columns: {paste(extra_in_death_ep, collapse = ', ')}"))
  }

  expected_det_cols <- colnames(detail_export)
  death_det_cols <- colnames(death_detail)
  missing_in_death_det <- setdiff(expected_det_cols, death_det_cols)
  extra_in_death_det <- setdiff(death_det_cols, expected_det_cols)

  if (length(missing_in_death_det) > 0) {
    stop(glue("Death detail missing columns: {paste(missing_in_death_det, collapse = ', ')}"))
  }
  if (length(extra_in_death_det) > 0) {
    warning(glue("Death detail has extra columns: {paste(extra_in_death_det, collapse = ', ')}"))
  }

  # Append death rows (per D-07: both CSVs)
  episodes_export <- bind_rows(episodes_export, death_episodes) %>%
    arrange(patient_id, episode_start, treatment_type)

  detail_export <- bind_rows(detail_export, death_detail) %>%
    arrange(patient_id, treatment_date, treatment_type)

  message(glue("  Added {nrow(death_episodes)} death episode rows"))
  message(glue("  Added {nrow(death_detail)} death detail rows"))

} else {
  message("\n--- No valid death dates found; skipping death rows ---")
}
```

**SECTION 6 (final summary) changes: Update to reflect new columns and death rows.**

Add after existing summary lines:
```r
# Cancer category and death stats
message(glue("  Episodes with cancer_category: {sum(episodes_export$cancer_category != '', na.rm = TRUE)}"))
message(glue("  Episodes with is_hodgkin=TRUE: {sum(episodes_export$is_hodgkin, na.rm = TRUE)}"))
n_death_rows <- sum(episodes_export$treatment_type == "Death", na.rm = TRUE)
message(glue("  Death pseudo-treatment rows in episodes: {format(n_death_rows, big.mark = ',')}"))
```

**Update header comment block** at top of file to include Phase 57 decision traceability:
```r
#   D-01 (Phase 57): Cancer categories from cancer_summary.csv via PREFIX_MAP classification
#   D-02 (Phase 57): Comma-separated cancer_category column (matches triggering_codes pattern)
#   D-03 (Phase 57): is_hodgkin = TRUE when "Hodgkin Lymphoma" in cancer_category
#   D-04 (Phase 57): Death dates from DEATH table (DEATH_Mailhot_V1.csv)
#   D-05 (Phase 57): Full pipeline integration for DEATH (config + load spec + DuckDB)
#   D-06 (Phase 57): Death rows: treatment_type="Death", single-point, episode_length_days=0
#   D-07 (Phase 57): Death rows in BOTH gantt_episodes.csv and gantt_detail.csv
#   D-08 (Phase 57): 1900 sentinel date nullification on DEATH_DATE
```

Add to Inputs section:
```r
#   - output/tables/cancer_summary.csv (Phase 55/57: cancer code -> category mapping)
#   - DuckDB DEATH table (Phase 57: death dates for endpoint visualization)
```
  </action>
  <verify>
    <automated>grep -c "cancer_category" R/49_gantt_data_export.R && grep -c "is_hodgkin" R/49_gantt_data_export.R && grep -c "Death" R/49_gantt_data_export.R && grep -c "PREFIX_MAP" R/49_gantt_data_export.R && grep -c "get_pcornet_table" R/49_gantt_data_export.R</automated>
  </verify>
  <acceptance_criteria>
    - R/49_gantt_data_export.R contains `library(lubridate)` in the library block
    - R/49_gantt_data_export.R contains `source("R/utils_duckdb.R")`
    - R/49_gantt_data_export.R contains `CANCER_SUMMARY_CSV <- file.path(CONFIG$output_dir, "tables", "cancer_summary.csv")`
    - R/49_gantt_data_export.R contains a complete PREFIX_MAP definition (copied from R/55, approximately 270 lines)
    - R/49_gantt_data_export.R contains `classify_codes <- function(codes)`
    - R/49_gantt_data_export.R contains `cancer_category = paste(sort(unique(category)), collapse = ",")`
    - R/49_gantt_data_export.R contains `is_hodgkin = str_detect(cancer_category, "Hodgkin Lymphoma")`
    - R/49_gantt_data_export.R contains `get_pcornet_table("DEATH")`
    - R/49_gantt_data_export.R contains `DEATH_DATE = ymd(DEATH_DATE)` (parse before sentinel check)
    - R/49_gantt_data_export.R contains `year(DEATH_DATE) == 1900L` (sentinel nullification)
    - R/49_gantt_data_export.R contains `treatment_type = "Death"` in death row construction
    - R/49_gantt_data_export.R contains `episode_length_days = 0L` in death row construction
    - R/49_gantt_data_export.R contains `triggering_codes = ""` in death row construction
    - R/49_gantt_data_export.R contains `bind_rows(episodes_export, death_episodes)`
    - R/49_gantt_data_export.R contains `bind_rows(detail_export, death_detail)`
    - R/49_gantt_data_export.R contains `left_join(cancer_categories_per_patient, by = c("patient_id" = "ID"))` for both episodes_export and detail_export
    - R/49_gantt_data_export.R contains column alignment verification (`setdiff(expected_ep_cols, death_ep_cols)`)
    - R/49_gantt_data_export.R header comment block includes "Phase 57" decision references (D-01 through D-08)
  </acceptance_criteria>
  <done>R/49_gantt_data_export.R is modified to: (1) derive cancer categories from cancer_summary.csv via PREFIX_MAP, join to episodes and detail as comma-separated cancer_category column (GANTT-01); (2) compute is_hodgkin flag via str_detect for Hodgkin Lymphoma (GANTT-02); (3) query DEATH table from DuckDB, nullify 1900 sentinels, construct pseudo-treatment rows with treatment_type="Death", and append to both CSVs (GANTT-03).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Gantt enrichment on HiPerGator</name>
  <files>output/gantt_episodes.csv, output/gantt_detail.csv</files>
  <action>
User re-runs the DuckDB ingest pipeline and modified R/49 on HiPerGator, then verifies the enriched Gantt CSV outputs contain cancer_category, is_hodgkin, and Death pseudo-treatment rows.
  </action>
  <verify>
    <automated>grep -c "cancer_category" R/49_gantt_data_export.R</automated>
  </verify>
  <what-built>
    Config changes (DEATH in PCORNET_TABLES, DEATH_SPEC in load spec) and R/49 modifications adding cancer_category, is_hodgkin columns and death pseudo-treatment rows to both Gantt CSVs.
  </what-built>
  <how-to-verify>
    1. In HiPerGator RStudio, re-run the DuckDB ingest to add the DEATH table:
       ```r
       source("R/25_duckdb_ingest.R")
       ```
       Verify output includes "DEATH" in the ingest log (15 tables total instead of 14).

    2. Run the modified Gantt export:
       ```r
       source("R/49_gantt_data_export.R")
       ```

    3. Verify gantt_episodes.csv:
       - Open output/gantt_episodes.csv
       - Confirm `cancer_category` column exists with comma-separated category names
       - Confirm `is_hodgkin` column exists with TRUE/FALSE values
       - Confirm rows with `treatment_type = "Death"` exist
       - Death rows should have `episode_length_days = 0` and empty triggering_codes

    4. Verify gantt_detail.csv:
       - Open output/gantt_detail.csv
       - Confirm same `cancer_category` and `is_hodgkin` columns
       - Confirm `treatment_type = "Death"` rows exist

    5. Verify console output shows:
       - Cancer categories aggregated for N patients
       - Hodgkin Lymphoma patients count
       - Patients with valid death dates count
       - Death episode/detail rows added count
  </how-to-verify>
  <done>User confirms gantt_episodes.csv and gantt_detail.csv contain cancer_category, is_hodgkin columns and Death pseudo-treatment rows with valid data.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. R/00_config.R PCORNET_TABLES includes "DEATH" (15 entries total)
2. R/01_load_pcornet.R has DEATH_SPEC with 6 col_character() columns
3. R/49_gantt_data_export.R produces gantt_episodes.csv with columns: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, triggering_code_descriptions, cancer_category, is_hodgkin
4. R/49_gantt_data_export.R produces gantt_detail.csv with columns: patient_id, treatment_type, treatment_date, triggering_code, episode_number, episode_start, episode_stop, historical_flag, triggering_code_description, cancer_category, is_hodgkin
5. Both CSVs contain "Death" treatment_type rows with valid death dates
6. No 1900 sentinel dates appear in death rows
7. cancer_category is comma-separated for multi-cancer patients, empty string for patients not in cancer_summary
8. is_hodgkin is TRUE when "Hodgkin Lymphoma" appears anywhere in cancer_category
</verification>

<success_criteria>
- GANTT-01: Every treatment episode row has a cancer_category column derived from CancerSiteCategories PREFIX_MAP (D-codes excluded because cancer_summary.csv already excludes them per Phase 55)
- GANTT-02: Every treatment episode row has is_hodgkin binary column (TRUE when "Hodgkin Lymphoma" in cancer_category)
- GANTT-03: Death dates from DEATH table appear as pseudo-treatment rows (treatment_type = "Death") in both gantt_episodes.csv and gantt_detail.csv, with 1900 sentinel dates nullified and excluded
</success_criteria>

<output>
After completion, create `.planning/phases/57-gantt-enhancements/57-01-SUMMARY.md`
</output>
