---
phase: 51-confirm-cancer-site-codes-with-7-day-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/51_cancer_site_confirmation_7day.R
autonomous: false
requirements:
  - C7DAY-01
  - C7DAY-02
  - C7DAY-03
  - C7DAY-04

must_haves:
  truths:
    - "R/51 script runs without error and produces cancer_site_confirmation_7day.xlsx"
    - "Exact Code sheet shows confirmation at exact ICD-10 code level using 7-day date span"
    - "Prefix Level sheet shows confirmation at 3-char prefix level using 7-day date span"
    - "Confirmation rates are lower than or equal to Phase 3 rates (7-day gap is stricter than 2-date count)"
  artifacts:
    - path: "R/51_cancer_site_confirmation_7day.R"
      provides: "Cancer site confirmation with 7-day gap filter"
      contains: "as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7"
    - path: "output/tables/cancer_site_confirmation_7day.xlsx"
      provides: "Styled two-sheet xlsx with 7-day confirmation results"
  key_links:
    - from: "R/51_cancer_site_confirmation_7day.R"
      to: "DuckDB DIAGNOSIS table"
      via: "get_pcornet_table('DIAGNOSIS')"
      pattern: 'get_pcornet_table.*DIAGNOSIS'
    - from: "R/51_cancer_site_confirmation_7day.R"
      to: "output/tables/cancer_site_confirmation_7day.xlsx"
      via: "wb$save(OUTPUT_PATH)"
      pattern: "cancer_site_confirmation_7day\\.xlsx"
---

<objective>
Create R/51_cancer_site_confirmation_7day.R -- a clone of R/50 with the confirmation filter changed from "2+ distinct dates" to "dates spanning 7+ days apart." Produces a standalone styled xlsx with two sheets (Exact Code, Prefix Level) for side-by-side comparison with Phase 3 output.

Purpose: Strengthen cancer site code confirmation by requiring temporal separation (7+ calendar days between earliest and latest diagnosis dates), not just repetition. This filters out same-week follow-up visits that inflate confirmation counts.

Output: R/51_cancer_site_confirmation_7day.R script + cancer_site_confirmation_7day.xlsx (produced on HiPerGator)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confirm-cancer-site-codes-with-7-day-separation-same-as-phase-3-but-the-unique-dates-must-be-at-least-7-days-apart/04-CONTEXT.md
@.planning/phases/04-confirm-cancer-site-codes-with-7-day-separation-same-as-phase-3-but-the-unique-dates-must-be-at-least-7-days-apart/04-RESEARCH.md

@R/50_cancer_site_confirmation.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/51 cancer site confirmation with 7-day gap filter</name>
  <files>R/51_cancer_site_confirmation_7day.R</files>
  <read_first>R/50_cancer_site_confirmation.R</read_first>
  <action>
Clone R/50_cancer_site_confirmation.R to R/51_cancer_site_confirmation_7day.R with the following targeted modifications (per D-01):

**Header comments (lines 1-19):**
- Change title from "Phase 3: Cancer Site Confirmation by Distinct Date Count" to "Phase 4: Cancer Site Confirmation with 7-Day Separation"
- Change description from "requiring at least 2 distinct dates per code per patient" to "requiring diagnosis dates at least 7 calendar days apart per code per patient"
- Update confirmation level descriptions to mention "7-day gap" instead of "2+ distinct dates"
- Update output path reference to cancer_site_confirmation_7day.xlsx
- Update usage to: `Rscript R/51_cancer_site_confirmation_7day.R`

**Output path (line 31):**
- Change `cancer_site_confirmation.xlsx` to `cancer_site_confirmation_7day.xlsx`

**Opening message (line 34):**
- Change to: `"=== Phase 4: Cancer Site Confirmation with 7-Day Separation ==="`

**SECTION 1 (PREFIX_MAP, CATEGORY_ORDER, classify_codes):**
- Copy verbatim from R/50 -- no changes.

**SECTION 2 (Load and classify diagnosis data):**
- Copy verbatim from R/50 -- no changes. Same DIAGNOSIS query, same ICD-10 filter, same normalization, same classification (per D-04).

**SECTION 3 (Exact code confirmation) -- THE KEY CHANGE:**
- Change section header comment to: `# SECTION 3: EXACT CODE CONFIRMATION WITH 7-DAY GAP (per D-03, D-06)`
- Change opening message to: `"=== EXACT CODE CONFIRMATION (7-DAY GAP) ==="`
- Step 1 (total_exact): Unchanged -- total patients per category before any filter.
- Step 2 (confirmed_exact): Replace the filter on line 429 from:
  ```r
  filter(n_distinct(DX_DATE) >= 2)
  ```
  to:
  ```r
  filter(as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7)
  ```
  This checks that the span between earliest and latest diagnosis date is >= 7 days (per D-06). The span-based filter implicitly requires 2+ distinct dates since a single date has span 0.

  Add a comment above this line: `# 7-day span: max(date) - min(date) >= 7 days (per D-06)`
- Step 3 (join and compute): Unchanged.
- Step 4 (ordering): Unchanged.
- Update log messages to say "(exact code, 7-day gap)" instead of "(exact code)".

**SECTION 4 (Prefix level confirmation) -- THE OTHER KEY CHANGE:**
- Change section header to: `# SECTION 4: PREFIX LEVEL CONFIRMATION WITH 7-DAY GAP (per D-03, D-06)`
- Change opening message to: `"=== PREFIX LEVEL CONFIRMATION (7-DAY GAP) ==="`
- Step 3 (confirmed_prefix): Replace the filter on line 478 from:
  ```r
  filter(n_distinct(DX_DATE) >= 2)
  ```
  to:
  ```r
  filter(as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7)
  ```
  Add same comment: `# 7-day span: max(date) - min(date) >= 7 days (per D-06)`

  CRITICAL per Pitfall 2: Verify this section uses `group_by(ID, prefix3)` (NOT `group_by(ID, DX_norm)`). The grouping variable determines whether dates are pooled across the 3-char prefix or kept per exact code.
- Update log messages to say "(prefix, 7-day gap)" instead of "(prefix)".

**SECTION 5 (Totals rows):**
- Copy verbatim from R/50 -- no changes.

**SECTION 6 (Write styled xlsx) -- per D-02, D-07:**
- Sheet 1 title: Change from "Cancer Site Confirmation - Exact Code Level" to "Cancer Site Confirmation - Exact Code (7-Day Gap)"
- Sheet 2 title: Change from "Cancer Site Confirmation - Prefix Level" to "Cancer Site Confirmation - Prefix Level (7-Day Gap)"
- All styling (dark header, freeze panes, number formatting, column widths, totals row fill): Copy verbatim from R/50.

**Closing message (line 664):**
- Change to: `"=== Phase 4 Cancer Site Confirmation (7-Day Gap) Complete ==="`

Everything else (library loads, source() calls, DuckDB connection open/close, color constants, wb_workbook creation, column headers, column widths) is identical to R/50.
  </action>
  <verify>
    <automated>grep -c "as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7" R/51_cancer_site_confirmation_7day.R</automated>
  </verify>
  <acceptance_criteria>
- R/51_cancer_site_confirmation_7day.R exists as a complete script
- File contains exactly 2 occurrences of `as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7` (one for exact code section, one for prefix section)
- File contains `cancer_site_confirmation_7day.xlsx` as the output path
- File does NOT contain `n_distinct(DX_DATE) >= 2` (the Phase 3 count-based filter must be replaced, not added alongside)
- File contains `group_by(ID, DX_norm)` in Section 3 (exact code confirmation)
- File contains `group_by(ID, prefix3)` in Section 4 (prefix confirmation)
- File contains `7-Day Gap` in both sheet titles
- File contains `Phase 4` in the header comment block
- File sources `R/00_config.R` and `R/01_load_pcornet.R`
- File contains `close_pcornet_con()` at the end
  </acceptance_criteria>
  <done>R/51_cancer_site_confirmation_7day.R is a complete, runnable script that clones R/50's logic with 7-day gap filter replacing the 2-date count filter, producing cancer_site_confirmation_7day.xlsx with two styled sheets</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify R/51 script on HiPerGator</name>
  <files>output/tables/cancer_site_confirmation_7day.xlsx</files>
  <action>
User transfers R/51_cancer_site_confirmation_7day.R to HiPerGator, runs `Rscript R/51_cancer_site_confirmation_7day.R`, and verifies the output xlsx.

Steps:
1. Transfer R/51_cancer_site_confirmation_7day.R to HiPerGator
2. Run: `Rscript R/51_cancer_site_confirmation_7day.R`
3. Verify output/tables/cancer_site_confirmation_7day.xlsx is created
4. Open the xlsx and check:
   - Sheet "Exact Code (7-Day Gap)" has columns: Cancer Site Category, Total Patients, Confirmed Patients, Unconfirmed Patients, Confirmation Rate
   - Sheet "Prefix Level (7-Day Gap)" has same column structure
   - Confirmation rates are lower than or equal to Phase 3 rates (7-day gap is a stricter filter)
   - Dark header styling, freeze panes, number formatting, and totals row are present
5. Compare side-by-side with cancer_site_confirmation.xlsx (Phase 3) to verify the tightening effect of the 7-day requirement
  </action>
  <verify>User confirms xlsx output is correct</verify>
  <done>cancer_site_confirmation_7day.xlsx produced on HiPerGator with correct two-sheet structure and 7-day gap confirmation rates</done>
</task>

</tasks>

<verification>
- R/51_cancer_site_confirmation_7day.R exists with 7-day gap filter (grep for `as.numeric(max(DX_DATE) - min(DX_DATE)) >= 7`)
- R/50_cancer_site_confirmation.R is NOT modified (Phase 3 baseline preserved per D-01)
- Script produces cancer_site_confirmation_7day.xlsx with 2 sheets when run on HiPerGator
- Confirmation rates in 7-day output are <= Phase 3 rates (stricter filter cannot increase rates)
</verification>

<success_criteria>
- R/51 script created and committed
- Script runs successfully on HiPerGator producing cancer_site_confirmation_7day.xlsx
- Both sheets (Exact Code, Prefix Level) show confirmation data with 7-day gap filter
- R/50 unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-confirm-cancer-site-codes-with-7-day-separation-same-as-phase-3-but-the-unique-dates-must-be-at-least-7-days-apart/04-01-SUMMARY.md`
</output>
