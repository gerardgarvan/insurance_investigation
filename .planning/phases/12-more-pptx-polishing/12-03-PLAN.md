---
phase: 12-more-pptx-polishing
plan: 03
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - R/11_generate_pptx.R
autonomous: true
requirements: [PPTX2-02, PPTX2-06]

must_haves:
  truths:
    - "Every data slide (Slides 2-16) has a footnote at the bottom defining the terms used on that slide"
    - "Footnotes are small italic text (~8pt, gray) positioned at the bottom of each slide"
    - "Short column headers are preserved -- definitions live only in footnotes"
    - "Slides 19-20 (DX year bar charts) have a footnote about masked DX_YEAR=1900 exclusion count"
  artifacts:
    - path: "R/11_generate_pptx.R"
      provides: "PPTX generator with per-slide footnotes on all data slides"
      contains: "footnote_prop"
  key_links:
    - from: "R/11_generate_pptx.R"
      to: "officer::ph_with"
      via: "Footnote placement at slide bottom"
      pattern: "ph_location.*top = 5"
---

<objective>
Add contextual footnotes to every data slide in the PPTX (Slides 2-16 tables, Slides 17-21 encounter analysis), defining the payer terms used on each specific slide. Add DX_YEAR=1900 exclusion footnote to Slides 19-20.

Purpose: Readers need term definitions in context -- the glossary slide (Plan 02) provides a single reference, but per-slide footnotes eliminate the need to flip back. The DX_YEAR=1900 footnote provides transparency about excluded data points (per D-11).

Output: Updated R/11_generate_pptx.R with footnote text on every data slide.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-more-pptx-polishing/12-CONTEXT.md
@.planning/phases/12-more-pptx-polishing/12-RESEARCH.md
@.planning/phases/12-more-pptx-polishing/12-02-SUMMARY.md

@R/11_generate_pptx.R

<interfaces>
<!-- Key patterns for footnote placement -->

From 12-RESEARCH.md (Pattern 1):
```r
# Footnote at bottom of slide
pptx <- pptx %>%
  ph_with(
    value = fpar(ftext("*text here",
                       prop = fp_text(font.size = 8, italic = TRUE,
                                      font.family = "Calibri", color = "#666666"))),
    location = ph_location(left = 0.5, top = 5.0, width = 9, height = 0.4)
  )
```

From R/11_generate_pptx.R -- add_table_slide helper (returns pptx after adding slide):
```r
add_table_slide <- function(pptx, title, subtitle, tbl_data) {
  ...
  pptx
}
```

IMPORTANT: add_table_slide() returns the pptx object. To chain a footnote after it,
use: `pptx <- add_table_slide(...) %>% ph_with(...footnote...)`

From R/11_generate_pptx.R -- add_image_slide helper (returns pptx after adding slide):
```r
add_image_slide <- function(pptx, title, subtitle, img_path, ...) {
  if (!file.exists(img_path)) { ... return(pptx) }
  pptx %>% add_slide(...) %>% ph_with(...) %>% ph_with(...) %>% ph_with(...)
}
```

IMPORTANT: add_image_slide() skips when PNG absent (returns pptx unchanged).
When PNG exists, it already adds a slide. Footnote must be added AFTER the call,
but ONLY if the slide was actually created. The file.exists guard means we need
to check the same condition before adding footnote to image slides.

Term definitions (per D-04):
- Primary Insurance = most prevalent payer across all encounters
- First Diagnosis = payer mode within +/-30 days of first HL diagnosis date
- First Chemo/Radiation/SCT = payer mode within +/-30 day window of first treatment date
- Last Chemo/Radiation/SCT = payer mode within +/-30 day window of last treatment date
- Post-Treatment Insurance = most prevalent payer after last treatment of any type
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add footnote helper and footnotes to all table slides (D-02, D-03)</name>
  <files>R/11_generate_pptx.R</files>
  <read_first>
    - R/11_generate_pptx.R (full file -- especially each slide block from Slide 2 through Slide 16, the add_table_slide helper, and the add_image_slide helper)
    - .planning/phases/12-more-pptx-polishing/12-CONTEXT.md (D-02, D-03, D-04 decisions with exact definitions)
    - .planning/phases/12-more-pptx-polishing/12-RESEARCH.md (Pattern 1: footnote placement, Pitfall 1: positioning)
  </read_first>
  <action>
**1. Create a reusable footnote text property and helper function:**

Add after the `add_image_slide()` function definition (in SECTION 5 preamble area, after the helpers):

```r
# Footnote text formatting (8pt italic gray at bottom of slide)
footnote_prop <- fp_text(font.size = 8, italic = TRUE, font.family = "Calibri", color = "#666666")
footnote_location <- ph_location(left = 0.5, top = 5.05, width = 9, height = 0.45)

# Helper to add a footnote to the current (last-added) slide
add_footnote <- function(pptx, text) {
  pptx %>%
    ph_with(
      value = fpar(ftext(text, prop = footnote_prop)),
      location = footnote_location
    )
}
```

**2. Add footnotes to each table slide:**

After each `add_table_slide()` call, chain an `add_footnote()` call. The footnotes should define ONLY the terms that appear on THAT specific slide.

**Slide 2 (Insurance Coverage Overview):**
Columns: Primary Insurance, First Diagnosis
```r
pptx <- add_table_slide(pptx, ..., tbl2) %>%
  add_footnote("Primary Insurance = most prevalent payer across all encounters. First Diagnosis = payer mode within \u00b130 days of first HL diagnosis date.")
```

**Slide 3 (Post-Treatment Insurance -- All Patients):**
Columns: Post-Treatment Insurance
```r
pptx <- add_table_slide(pptx, ..., tbl3) %>%
  add_footnote("Post-Treatment Insurance = most prevalent payer after last treatment of any type. N/A (No Follow-up) = last treatment was patient's final encounter (\u00b130 days).")
```

**Slide 4 (Chemotherapy Insurance):**
Columns: Primary Insurance, First Chemo, Last Chemo
```r
pptx <- add_table_slide(pptx, ..., tbl4) %>%
  add_footnote("Primary Insurance = most prevalent payer across all encounters. First/Last Chemo = payer mode within \u00b130 days of first/last chemotherapy date.")
```

**Slide 5 (Chemotherapy Post-Treatment Insurance):**
Columns: Post-Treatment Insurance
```r
pptx <- add_table_slide(pptx, ..., tbl5) %>%
  add_footnote("Post-Treatment Insurance = most prevalent payer after last chemotherapy. N/A (No Follow-up) = last chemo was patient's final encounter (\u00b130 days).")
```

**Slide 6 (Radiation Insurance):**
Columns: Primary Insurance, First Radiation, Last Radiation
```r
pptx <- add_table_slide(pptx, ..., tbl6) %>%
  add_footnote("Primary Insurance = most prevalent payer across all encounters. First/Last Radiation = payer mode within \u00b130 days of first/last radiation date.")
```

**Slide 7 (Radiation Post-Treatment Insurance):**
Columns: Post-Treatment Insurance
```r
pptx <- add_table_slide(pptx, ..., tbl7) %>%
  add_footnote("Post-Treatment Insurance = most prevalent payer after last radiation. N/A (No Follow-up) = last radiation was patient's final encounter (\u00b130 days).")
```

**Slide 8 (SCT Insurance):**
Columns: Primary Insurance, First SCT, Last SCT
```r
pptx <- add_table_slide(pptx, ..., tbl8) %>%
  add_footnote("Primary Insurance = most prevalent payer across all encounters. First/Last SCT = payer mode within \u00b130 days of first/last stem cell transplant date.")
```

**Slide 9 (SCT Post-Treatment Insurance):**
Columns: Post-Treatment Insurance
```r
pptx <- add_table_slide(pptx, ..., tbl9) %>%
  add_footnote("Post-Treatment Insurance = most prevalent payer after last SCT. N/A (No Follow-up) = last SCT was patient's final encounter (\u00b130 days).")
```

**Slide 10 (Diagnosis -- Enrollment Coverage):**
Columns: ENR Covers Window, ENR Does Not Cover
```r
pptx <- add_table_slide(pptx, ..., tbl10) %>%
  add_footnote("ENR Covers Window = enrollment records span the full \u00b130 day window around first HL diagnosis. ENR Does Not Cover = enrollment gap in that window.")
```

**Slide 11 (Chemo -- Enrollment Coverage):**
```r
pptx <- add_table_slide(pptx, ..., tbl11) %>%
  add_footnote("ENR Covers = enrollment records span the full \u00b130 day window around first/last chemotherapy. ENR Gap = enrollment gap in that window.")
```

**Slide 12 (Radiation -- Enrollment Coverage):**
```r
pptx <- add_table_slide(pptx, ..., tbl12) %>%
  add_footnote("ENR Covers = enrollment records span the full \u00b130 day window around first/last radiation. ENR Gap = enrollment gap in that window.")
```

**Slide 13 (SCT -- Enrollment Coverage):**
```r
pptx <- add_table_slide(pptx, ..., tbl13) %>%
  add_footnote("ENR Covers = enrollment records span the full \u00b130 day window around first/last SCT. ENR Gap = enrollment gap in that window.")
```

**Slide 14 (Last Treatment = Last Encounter):**
```r
pptx <- add_table_slide(pptx, ..., tbl14) %>%
  add_footnote("Last Tx = Last Encounter: patient's last treatment date is within \u00b130 days of their last encounter date in the dataset.")
```

**Slide 15 (Missing Post-Treatment Payer):**
```r
pptx <- add_table_slide(pptx, ..., tbl15) %>%
  add_footnote("Shows how many post-treatment encounters exist for patients whose post-treatment payer is Missing or unassigned.")
```

**Slide 16 (Insurance After Last Treatment -- Dataset Retention):**
```r
pptx <- add_table_slide(pptx, ..., tbl16) %>%
  add_footnote("Still in Dataset = patient has encounters after last treatment. No Longer in Dataset = last treatment was final encounter (\u00b130 days). Payer shown is post-treatment (still) or at last treatment (no longer).")
```

**3. Short column headers preserved (D-03):**
Do NOT change any column header labels in `build_payer_table()` calls or `col_specs` lists. Headers remain "Primary Insurance", "First Chemo", etc. All definitions are in footnotes only.
  </action>
  <verify>
    <automated>grep -c "add_footnote" R/11_generate_pptx.R && grep -n "footnote_prop" R/11_generate_pptx.R | head -3 && grep -n "footnote_location" R/11_generate_pptx.R | head -3</automated>
  </verify>
  <acceptance_criteria>
    - R/11_generate_pptx.R contains `footnote_prop <- fp_text(font.size = 8, italic = TRUE`
    - R/11_generate_pptx.R contains `footnote_location <- ph_location(left = 0.5, top = 5.05`
    - R/11_generate_pptx.R contains `add_footnote <- function(pptx, text)`
    - `grep -c "add_footnote" R/11_generate_pptx.R` returns at least 15 (definition + 15 slide calls on Slides 2-16)
    - R/11_generate_pptx.R Slide 2 footnote contains "Primary Insurance = most prevalent payer"
    - R/11_generate_pptx.R Slide 3 footnote contains "Post-Treatment Insurance = most prevalent"
    - R/11_generate_pptx.R Slide 10 footnote contains "ENR Covers Window"
    - R/11_generate_pptx.R column headers remain unchanged: "Primary Insurance", "First Chemo", "Last Chemo", "First Diagnosis" etc (no definition text in col_specs)
  </acceptance_criteria>
  <done>All 15 table slides (Slides 2-16) have contextual footnotes defining the specific terms used on each slide; footnote helper function defined once and reused</done>
</task>

<task type="auto">
  <name>Task 2: Add footnotes to encounter analysis slides including DX_YEAR exclusion (D-02, D-11)</name>
  <files>R/11_generate_pptx.R</files>
  <read_first>
    - R/11_generate_pptx.R (SECTION 5b encounter analysis slides -- look for the Slide 17-21 blocks after Plan 02 changes)
    - .planning/phases/12-more-pptx-polishing/12-CONTEXT.md (D-11: footnote about masked DX_YEAR=1900 exclusion)
    - .planning/phases/12-more-pptx-polishing/12-RESEARCH.md (Pattern 3: masked date exclusion count for footnote)
  </read_first>
  <action>
Add footnotes to the encounter analysis slides (Slides 17-21) in SECTION 5b of R/11_generate_pptx.R.

**Slide 17 (Encounters per Person histogram):**
The image slide uses `add_image_slide()` which has a file.exists guard. The footnote can only be added if the PNG exists. Modify the Slide 17 block:
```r
# ---- Slide 17: Encounter histogram by payor ----
message("  Slide 17: Encounters per Person by Payer Category")
enc_hist_path <- "output/figures/encounters_per_person_by_payor.png"
pptx <- add_image_slide(pptx,
  "Encounters per Person by Payer Category",
  glue("Distribution of total encounter counts by primary payer -- N = {format(N_TOTAL, big.mark=',')}"),
  enc_hist_path,
  img_width = 9, img_height = 5.0
)
if (file.exists(enc_hist_path)) {
  pptx <- add_footnote(pptx, "Primary Insurance = most prevalent payer across all encounters. Payer categories consolidated to 6 + Missing.")
}
```

Note: Reduce img_height from 5.5 to 5.0 to make room for the footnote at top=5.05.

**Slide 18 (Summary Statistics table):**
This slide was added by Plan 02 using add_table_slide(). After that call, add:
```r
pptx <- add_table_slide(pptx, ..., summary_stats) %>%
  add_footnote("Primary Insurance = most prevalent payer across all encounters. N > 500 = patients with more than 500 total encounters.")
```

(Modify the existing add_table_slide call from Plan 02 to chain add_footnote.)

**Slides 19-20 (DX year bar charts -- D-11):**
These slides embed PNGs. Before the Slide 19 block, compute the masked date count from cohort_full:
```r
# Count masked diagnosis dates for footnote
n_masked_dx <- cohort_full %>%
  filter(!is.na(DX_YEAR), DX_YEAR == 1900) %>%
  nrow()
masked_footnote <- if (n_masked_dx > 0) {
  glue("{n_masked_dx} patients with masked diagnosis date (year 1900) excluded from this analysis.")
} else {
  ""
}
```

Then for Slide 19 (post-treatment encounters by DX year):
```r
post_tx_dx_path <- "output/figures/post_tx_encounters_by_dx_year.png"
pptx <- add_image_slide(pptx,
  "Mean Post-Treatment Encounters by Year of Diagnosis",
  "Non-acute care encounters per person after last treatment, stratified by HL diagnosis year",
  post_tx_dx_path
)
if (file.exists(post_tx_dx_path) && nchar(masked_footnote) > 0) {
  pptx <- add_footnote(pptx, masked_footnote)
}
```

For Slide 20 (total encounters by DX year):
```r
total_enc_dx_path <- "output/figures/total_encounters_by_dx_year.png"
pptx <- add_image_slide(pptx,
  "Mean Total Encounters by Year of Diagnosis",
  "All encounters per person across the full observation window, stratified by HL diagnosis year",
  total_enc_dx_path
)
if (file.exists(total_enc_dx_path) && nchar(masked_footnote) > 0) {
  pptx <- add_footnote(pptx, masked_footnote)
}
```

**Slide 21 (Age group bar chart):**
```r
age_group_path <- "output/figures/post_tx_by_age_group.png"
pptx <- add_image_slide(pptx,
  "Post-Treatment Encounter Presence by Age Group at Diagnosis",
  "Proportion with any post-treatment encounter by age group (0-17, 18-39, 40-64, 65+)",
  age_group_path
)
if (file.exists(age_group_path)) {
  pptx <- add_footnote(pptx, "Age group determined at date of first HL diagnosis. Post-treatment = any encounter after last treatment of any type.")
}
```

**Image slide height adjustment:**
For slides 19-21, the default img_height in add_image_slide is 4.2 which leaves room for footnote at top=5.05. But for Slide 17, reduce img_height from 5.5 to 5.0 to leave room for the footnote.
  </action>
  <verify>
    <automated>grep -c "add_footnote" R/11_generate_pptx.R && grep -n "n_masked_dx" R/11_generate_pptx.R | head -5 && grep -n "masked_footnote" R/11_generate_pptx.R | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "add_footnote" R/11_generate_pptx.R` returns at least 20 (15 table slides + 5 encounter slides + function definition)
    - R/11_generate_pptx.R contains `n_masked_dx <- cohort_full %>%` with `DX_YEAR == 1900`
    - R/11_generate_pptx.R contains `masked_footnote <- if (n_masked_dx > 0)` with glue template
    - R/11_generate_pptx.R Slide 19 block has `if (file.exists(post_tx_dx_path) && nchar(masked_footnote) > 0)`
    - R/11_generate_pptx.R Slide 20 block has `if (file.exists(total_enc_dx_path) && nchar(masked_footnote) > 0)`
    - R/11_generate_pptx.R Slide 17 img_height is 5.0 (reduced from 5.5 to accommodate footnote)
    - R/11_generate_pptx.R Slide 18 summary stats has add_footnote chained
    - R/11_generate_pptx.R Slide 21 has add_footnote with "Age group determined at date of first HL diagnosis"
  </acceptance_criteria>
  <done>All 5 encounter analysis slides (17-21) have contextual footnotes; Slides 19-20 include DX_YEAR=1900 masked date exclusion count; image heights adjusted to accommodate footnote area</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "add_footnote" R/11_generate_pptx.R` returns at least 20 (function definition + ~20 slide calls)
2. `grep "footnote_prop" R/11_generate_pptx.R` returns a match
3. `grep "n_masked_dx" R/11_generate_pptx.R` returns matches
4. `grep "masked_footnote" R/11_generate_pptx.R` returns matches
5. `grep "Primary Insurance = most prevalent" R/11_generate_pptx.R` returns multiple matches
6. Column headers (col_specs labels) remain unchanged from Phase 11
</verification>

<success_criteria>
- All 15 table slides (Slides 2-16) have contextual footnotes at slide bottom
- All 5 encounter analysis slides (Slides 17-21) have contextual footnotes
- DX year slides (19-20) include masked date exclusion count in footnote
- Footnotes use consistent 8pt italic gray Calibri styling
- Short column headers preserved -- no definition text added to column names
- Image slide heights accommodate footnote area (Slide 17 reduced to 5.0)
- All footnotes use the reusable add_footnote() helper function
</success_criteria>

<output>
After completion, create `.planning/phases/12-more-pptx-polishing/12-03-SUMMARY.md`
</output>
