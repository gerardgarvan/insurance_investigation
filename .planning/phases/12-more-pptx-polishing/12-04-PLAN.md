---
phase: 12-more-pptx-polishing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - R/run_phase12_outputs.R
autonomous: false
gap_closure: true
requirements:
  - PPTX2-04
  - PPTX2-07

must_haves:
  truths:
    - "User can run a single R script on HiPerGator that generates all 4 encounter analysis PNGs and the final PPTX"
    - "User can see the histogram PNG with 6+Missing payer facets and overflow bin annotations"
    - "User can see the age group bar chart PNG with no label clipping at the top"
  artifacts:
    - path: "R/run_phase12_outputs.R"
      provides: "Single execution script that sources encounter analysis and PPTX generation in correct order"
      contains: "source.*16_encounter_analysis"
  key_links:
    - from: "R/run_phase12_outputs.R"
      to: "R/16_encounter_analysis.R"
      via: "source() call"
      pattern: 'source.*16_encounter_analysis'
    - from: "R/run_phase12_outputs.R"
      to: "R/11_generate_pptx.R"
      via: "source() call"
      pattern: 'source.*11_generate_pptx'
---

<objective>
Create a single execution helper script that the user can run on HiPerGator to generate all Phase 12 outputs (4 encounter analysis PNGs + final PPTX), closing the two verification gaps where code is correct but PNGs have not yet been generated.

Purpose: The R scripts run on HiPerGator with actual patient data and cannot be executed in this development environment. Both PPTX2-04 (histogram PNG) and PPTX2-07 (age group bar chart PNG) are blocked solely by the scripts not having been run yet. This plan creates a convenient execution helper and provides a human verification checkpoint.

Output: R/run_phase12_outputs.R helper script + human visual verification of generated PNGs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-more-pptx-polishing/12-VERIFICATION.md
@R/16_encounter_analysis.R
@R/11_generate_pptx.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HiPerGator execution helper script</name>
  <files>R/run_phase12_outputs.R</files>
  <read_first>
    - R/16_encounter_analysis.R
    - R/11_generate_pptx.R
    - R/04_build_cohort.R
  </read_first>
  <action>
Create R/run_phase12_outputs.R -- a single script the user sources on HiPerGator to generate all Phase 12 outputs. The script must:

1. Print a header banner explaining what it does and what gaps it closes (PPTX2-04 histogram, PPTX2-07 age group chart).

2. Check that the working directory contains the R/ and output/ directories (fail early with helpful message if not).

3. Create output/figures/ directory if it does not exist (dir.create with recursive=TRUE).

4. Source R/16_encounter_analysis.R (which itself sources R/04_build_cohort.R). This generates:
   - output/figures/encounters_per_person_by_payor.png (PPTX2-04)
   - output/figures/post_tx_encounters_by_dx_year.png
   - output/figures/total_encounters_by_dx_year.png
   - output/figures/post_tx_by_age_group.png (PPTX2-07)

5. After sourcing, verify each of the 4 PNG files exists and print file size. If any are missing, print a clear error message identifying which file failed.

6. Source R/11_generate_pptx.R to generate the final PPTX with embedded PNGs.

7. Print a verification checklist telling the user exactly what to visually inspect:
   - Open encounters_per_person_by_payor.png: confirm 7 facets (Medicare, Medicaid, Dual eligible, Private, Other government, No payment / Self-pay, Missing), no "Other"/"Unavailable"/"Unknown" facets, ">500: N" annotation per facet
   - Open post_tx_by_age_group.png: confirm count labels above bars are fully visible with no clipping at top edge
   - Open the generated PPTX: confirm Slide 1 is glossary, all slides have footnotes, histogram and age group slides look correct

The script should use message() for all output (not cat() or print()) for consistency with the rest of the pipeline. Wrap each source() call in tryCatch to provide clear error messages if a script fails.

Do NOT include library() calls -- the sourced scripts handle their own dependencies.
Do NOT include setwd() -- user sets working directory before sourcing.
Use here::here() if available, but fall back gracefully to relative paths.
  </action>
  <verify>
    <automated>grep -c "source.*16_encounter_analysis" R/run_phase12_outputs.R && grep -c "source.*11_generate_pptx" R/run_phase12_outputs.R && grep -c "file.exists" R/run_phase12_outputs.R && grep -c "encounters_per_person_by_payor" R/run_phase12_outputs.R && grep -c "post_tx_by_age_group" R/run_phase12_outputs.R</automated>
  </verify>
  <acceptance_criteria>
    - R/run_phase12_outputs.R exists and is syntactically valid R
    - Script sources R/16_encounter_analysis.R and R/11_generate_pptx.R in correct order
    - Script checks for existence of all 4 PNG files after generation
    - Script prints verification checklist with specific visual inspection instructions for PPTX2-04 (histogram facets) and PPTX2-07 (label clipping)
    - Script uses tryCatch around source() calls for error handling
    - grep "source.*16_encounter_analysis" returns at least 1 match
    - grep "source.*11_generate_pptx" returns at least 1 match
    - grep "file.exists" returns at least 4 matches (one per PNG)
  </acceptance_criteria>
  <done>R/run_phase12_outputs.R exists with source calls for encounter analysis and PPTX generation, PNG existence checks, and visual verification checklist</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run helper on HiPerGator and verify PNG outputs</name>
  <files>output/figures/encounters_per_person_by_payor.png, output/figures/post_tx_by_age_group.png</files>
  <read_first>
    - R/run_phase12_outputs.R
  </read_first>
  <action>
CHECKPOINT: Human runs the helper script on HiPerGator and visually verifies generated PNGs.

The code for all graph fixes was already verified correct in Plans 01-03. This step generates the actual visual outputs on HiPerGator where the patient data lives. Claude cannot execute this step.

Steps for the user:
1. On HiPerGator RStudio, set working directory to project root
2. Run: source("R/run_phase12_outputs.R")
3. Visually inspect the generated PNG files per the checklist printed by the script
4. Report back with "approved" or describe issues
  </action>
  <verify>
    <automated>MISSING -- requires human visual verification on HiPerGator where patient data resides. Cannot be automated in development environment.</automated>
  </verify>
  <acceptance_criteria>
    - output/figures/encounters_per_person_by_payor.png exists and shows 7 facets (6 payer + Missing) with overflow annotations
    - output/figures/post_tx_by_age_group.png exists and shows fully visible bar labels with no clipping
    - User confirms both visual checks pass
  </acceptance_criteria>
  <done>User has confirmed histogram shows 6+Missing facets with overflow bin (PPTX2-04) and age group bar chart has no label clipping (PPTX2-07)</done>
</task>

</tasks>

<verification>
After human verification on HiPerGator:
- PPTX2-04: Histogram PNG generated with 6+Missing payer facets and overflow bin annotations
- PPTX2-07: Age group bar chart PNG generated with no label clipping
- All 4 encounter analysis PNGs exist in output/figures/
- PPTX generated with embedded PNGs
</verification>

<success_criteria>
1. R/run_phase12_outputs.R exists and can be sourced on HiPerGator
2. User confirms encounters_per_person_by_payor.png shows 7 facets (not 9) with overflow annotations
3. User confirms post_tx_by_age_group.png has no label clipping at top
4. Both PPTX2-04 and PPTX2-07 gaps are closed
</success_criteria>

<output>
After completion, create `.planning/phases/12-more-pptx-polishing/12-04-SUMMARY.md`
</output>
