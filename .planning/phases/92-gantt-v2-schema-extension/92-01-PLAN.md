---
phase: 92-gantt-v2-schema-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/52_gantt_v2_export.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements: [GANTT-06, GANTT-07]

must_haves:
  truths:
    - "gantt_episodes_v2.csv has 21 columns (16 existing + 5 Phase 92 additions)"
    - "gantt_detail_v2.csv has 19 columns (14 existing + 5 Phase 92 additions)"
    - "Existing v1 Gantt exports (R/51 output) unchanged — no modifications to R/51"
    - "Death and HL Diagnosis pseudo-rows have NA for 5 new columns"
    - "medication_name, code_type, source_table cleaned as multi-value fields (semicolons, deduped)"
    - "Smoke test Section 15e validates 21/19 column schema"
  artifacts:
    - path: "R/52_gantt_v2_export.R"
      provides: "Extended Gantt v2 export with 5 new metadata columns"
      contains: "medication_name, code_type, source_table, treatment_line, sct_cross_use_flag"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15e schema validation for Phase 92"
      contains: "SECTION 15e"
  key_links:
    - from: "R/52_gantt_v2_export.R"
      to: "cache/outputs/treatment_episodes.rds"
      via: "readRDS + select() for 5 new columns"
      pattern: "medication_name.*code_type.*source_table.*treatment_line.*sct_cross_use_flag"
    - from: "R/52_gantt_v2_export.R"
      to: "output/gantt_episodes_v2.csv"
      via: "write.csv with 21-column schema"
      pattern: "expected_ep_cols <- 21"
    - from: "R/52_gantt_v2_export.R"
      to: "output/gantt_detail_v2.csv"
      via: "write.csv with 19-column schema"
      pattern: "expected_detail_cols <- 19"
---

<objective>
Extend Gantt v2 CSV exports (gantt_episodes_v2.csv and gantt_detail_v2.csv) with 5 new metadata columns from Phase 91's enriched treatment_episodes.rds, while preserving v1 backward compatibility.

Purpose: Phase 91 pre-computed 5 metadata columns (medication_name, code_type, source_table, treatment_line, sct_cross_use_flag) in treatment_episodes.rds. Phase 92 propagates these to the Gantt v2 CSV export layer so downstream consumers (Tableau dashboards) can display medication names, code types, source tables, treatment line labels, and SCT cross-use flags per episode.

Output: Modified R/52_gantt_v2_export.R (21-column episodes, 19-column detail), updated smoke test Section 15e.
</objective>

<execution_context>
@.claude/skills/get-shit-done/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-reference-data-loader-metadata-enrichment/91-01-SUMMARY.md

<interfaces>
<!-- Current R/52 column schemas (from codebase inspection) -->

Current episodes_export final columns (16, after Step 6 trimming at lines 789-797):
  1. patient_id          2. treatment_type    3. episode_number
  4. episode_start       5. episode_stop      6. episode_length_days
  7. distinct_dates_in_episode  8. historical_flag  9. triggering_codes
 10. drug_names         11. triggering_code_descriptions  12. cancer_category
 13. regimen_label      14. is_first_line    15. drug_group
 16. cause_of_death

Current detail_export final columns (14, after Step 6 trimming at lines 799-807):
  1. patient_id          2. treatment_type    3. treatment_date
  4. triggering_code     5. drug_name         6. episode_number
  7. episode_start       8. episode_stop      9. historical_flag
 10. triggering_code_description  11. cancer_category  12. regimen_label
 13. is_first_line      14. cause_of_death

Phase 91 new columns in treatment_episodes.rds (22-column RDS):
  - medication_name (comma-separated parallel list matching triggering_codes positions)
  - code_type (comma-separated parallel list)
  - source_table (comma-separated parallel list)
  - treatment_line (single value: F, S, E, or N after aggregation)
  - sct_cross_use_flag (single value after any-positive aggregation)

Key Phase 91 decisions:
  - D-04: Metadata uses COMMAS in R/28 (matching triggering_codes format)
  - R/52 must convert commas to semicolons during Phase 64 cleanup (matching existing pattern)
  - medication_name, code_type, source_table are multi-value (need clean_multi_value)
  - treatment_line and sct_cross_use_flag are single-value (skip clean_multi_value)

Column trimming context (Step 6, lines 788-809):
  - Step 6 drops encounter_ids, is_hodgkin, cancer_link_method from BOTH tables
  - These columns exist during Section 4 processing but are removed before CSV output
  - Phase 92 columns must survive Step 6 (add them to the final select() lists)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend R/52 Gantt v2 export with 5 new metadata columns</name>
  <files>R/52_gantt_v2_export.R</files>
  <read_first>
    R/52_gantt_v2_export.R
    .planning/phases/91-reference-data-loader-metadata-enrichment/91-01-SUMMARY.md
    .planning/phases/92-gantt-v2-schema-extension/92-RESEARCH.md
  </read_first>
  <action>
Modify R/52_gantt_v2_export.R to propagate 5 Phase 91 columns through the entire export pipeline. Changes span 7 locations in the file:

**1. Update header comments (lines 1-103):**
- Change episodes schema documentation from "16 columns" to "21 columns" and add 5 new column descriptions:
  - 17. medication_name (chr) - Semicolon-separated medication names from xlsx (Phase 92)
  - 18. code_type (chr) - Semicolon-separated code types: RXNORM, CPT/HCPCS, ICD-10-CM (Phase 92)
  - 19. source_table (chr) - Semicolon-separated source tables: PRESCRIBING, PROCEDURES, DIAGNOSIS (Phase 92)
  - 20. treatment_line (chr) - Treatment line label: F (first), S (second), E (extension), N (neither), or empty (Phase 92)
  - 21. sct_cross_use_flag (chr) - SCT cross-use flag or empty (Phase 92)
- Change detail schema documentation from "14 columns" to "19 columns" and add same 5 column descriptions numbered 15-19
- Update output comment lines to say "21 columns" and "19 columns"
- Add D-92-01 decision traceability: "5 Phase 91 metadata columns appended at end (non-breaking)"

**2. Add guard clauses for 5 new columns (after line 183, following the existing drug_group guard clause pattern):**
Add 5 guard clause blocks following the exact pattern at lines 180-183:

```r
if (!"medication_name" %in% names(episodes)) {
  warning("medication_name column not found in treatment_episodes.rds — Phase 91 not yet run. Using default NA.")
  episodes <- episodes %>% mutate(medication_name = NA_character_)
}
if (!"code_type" %in% names(episodes)) {
  warning("code_type column not found in treatment_episodes.rds — Phase 91 not yet run. Using default NA.")
  episodes <- episodes %>% mutate(code_type = NA_character_)
}
if (!"source_table" %in% names(episodes)) {
  warning("source_table column not found in treatment_episodes.rds — Phase 91 not yet run. Using default NA.")
  episodes <- episodes %>% mutate(source_table = NA_character_)
}
if (!"treatment_line" %in% names(episodes)) {
  warning("treatment_line column not found in treatment_episodes.rds — Phase 91 not yet run. Using default NA.")
  episodes <- episodes %>% mutate(treatment_line = NA_character_)
}
if (!"sct_cross_use_flag" %in% names(episodes)) {
  warning("sct_cross_use_flag column not found in treatment_episodes.rds — Phase 91 not yet run. Using default NA.")
  episodes <- episodes %>% mutate(sct_cross_use_flag = NA_character_)
}
```

**3. Modify episodes_export construction (Section 4, lines 261-292):**
- In the left_join's inner select() at lines 274-278, add the 5 new columns after is_first_line:
  `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`
- In the final select() at lines 285-292, append 5 new columns after cause_of_death:
  `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`

The full final select() becomes:
```r
  select(
    patient_id, treatment_type, episode_number,
    episode_start, episode_stop, episode_length_days,
    distinct_dates_in_episode, historical_flag, triggering_codes,
    encounter_ids, drug_names, triggering_code_descriptions,
    cancer_category, is_hodgkin, cancer_link_method, regimen_label, is_first_line,
    drug_group, cause_of_death,
    # --- Phase 92: 5 new metadata columns (GANTT-06) ---
    medication_name, code_type, source_table, treatment_line, sct_cross_use_flag
  )
```

**4. Modify detail_export construction (Section 4, lines 299-327):**
- In episodes_v2_cols select() at lines 300-303, add 5 new columns after is_first_line:
  `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`
- In the final detail_export select() at lines 320-327, append 5 new columns after cause_of_death:
  `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`

The full detail final select() becomes:
```r
  select(
    patient_id, treatment_type, treatment_date, triggering_code,
    ENCOUNTERID, drug_name,
    episode_number, episode_start, episode_stop, historical_flag,
    triggering_code_description,
    cancer_category, is_hodgkin, cancer_link_method, regimen_label, is_first_line,
    cause_of_death,
    # --- Phase 92: 5 new metadata columns (GANTT-06) ---
    medication_name, code_type, source_table, treatment_line, sct_cross_use_flag
  )
```

**5. Modify Death pseudo-treatment rows (Section 4B, lines 380-480):**
- In death_episodes mutate() at lines 382-403, add 5 new columns with NA after drug_group:
  ```r
  medication_name = NA_character_,    # Phase 92: Death rows have no treatment codes
  code_type = NA_character_,
  source_table = NA_character_,
  treatment_line = NA_character_,
  sct_cross_use_flag = NA_character_
  ```
- In death_episodes select() at lines 404-411, append 5 columns after cause_of_death:
  `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`
- In death_detail mutate() at lines 427-445, add same 5 NA columns after is_first_line:
  ```r
  medication_name = NA_character_,    # Phase 92: Death rows have no treatment codes
  code_type = NA_character_,
  source_table = NA_character_,
  treatment_line = NA_character_,
  sct_cross_use_flag = NA_character_
  ```
- In death_detail select() at lines 447-454, append 5 columns after cause_of_death:
  `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`

**6. Modify HL Diagnosis pseudo-treatment rows (Section 4C, lines 486-603):**
- In hl_dx_episodes mutate() at lines 502-523, add 5 new columns with NA after cause_of_death:
  ```r
  medication_name = NA_character_,    # Phase 92: HL Diagnosis rows have no treatment codes
  code_type = NA_character_,
  source_table = NA_character_,
  treatment_line = NA_character_,
  sct_cross_use_flag = NA_character_
  ```
- In hl_dx_episodes select() at lines 524-531, append 5 columns after cause_of_death:
  `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`
- In hl_dx_detail mutate() at lines 547-566, add same 5 NA columns after cause_of_death:
  ```r
  medication_name = NA_character_,    # Phase 92: HL Diagnosis rows have no treatment codes
  code_type = NA_character_,
  source_table = NA_character_,
  treatment_line = NA_character_,
  sct_cross_use_flag = NA_character_
  ```
- In hl_dx_detail select() at lines 567-574, append 5 columns after cause_of_death:
  `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`

**7. Extend Phase 64 cleanup (Section 4D):**
- In Step 1 (multi-value cleanup, lines 724-730), add 3 new multi-value columns to episodes_export mutate:
  ```r
  medication_name = sapply(medication_name, clean_multi_value, USE.NAMES = FALSE),
  code_type = sapply(code_type, clean_multi_value, USE.NAMES = FALSE),
  source_table = sapply(source_table, clean_multi_value, USE.NAMES = FALSE)
  ```
  NOTE: treatment_line and sct_cross_use_flag are single-value fields — do NOT apply clean_multi_value to them.

- In Step 1 detail_export mutate (lines 732-736), add same 3 multi-value columns:
  ```r
  medication_name = sapply(medication_name, clean_multi_value, USE.NAMES = FALSE),
  code_type = sapply(code_type, clean_multi_value, USE.NAMES = FALSE),
  source_table = sapply(source_table, clean_multi_value, USE.NAMES = FALSE)
  ```

- In Step 6 column trimming (lines 789-807), add 5 new columns to BOTH select() calls:
  Episodes select (after cause_of_death):
  ```r
  medication_name, code_type, source_table, treatment_line, sct_cross_use_flag
  ```
  Detail select (after cause_of_death):
  ```r
  medication_name, code_type, source_table, treatment_line, sct_cross_use_flag
  ```

- In Step 7 (column count verification, lines 813-821), update expected counts:
  ```r
  expected_ep_cols <- 21  # was 16, Phase 92: +5 metadata columns
  expected_detail_cols <- 19  # was 14, Phase 92: +5 metadata columns
  ```

**8. Update Section 6 summary messages (lines 839-885):**
- Update v1 vs v2 column comparison message (line 875-877) to reflect new counts:
  ```r
  message("    v1 episodes: 14 columns | v2 episodes: 21 columns (Phase 92: +medication_name, code_type, source_table, treatment_line, sct_cross_use_flag)")
  message("    v1 detail: 13 columns | v2 detail: 19 columns (Phase 92: +5 metadata columns)")
  ```
- Add medication_name coverage stat after the death cause stats:
  ```r
  episodes_with_medication <- episodes_export %>%
    filter(medication_name != "" & !is.na(medication_name) & !treatment_type %in% c("Death", "HL Diagnosis")) %>%
    nrow()
  treatment_episodes_count <- episodes_export %>%
    filter(!treatment_type %in% c("Death", "HL Diagnosis")) %>%
    nrow()
  message(glue("  Episodes with medication_name: {format(episodes_with_medication, big.mark = ',')} / {format(treatment_episodes_count, big.mark = ',')} treatment episodes ({round(100 * episodes_with_medication / max(treatment_episodes_count, 1), 1)}%)"))
  ```

IMPORTANT: Do NOT modify R/51_gantt_data_export.R (v1 export). Requirement GANTT-07 demands v1 unchanged.
  </action>
  <verify>
    <automated>grep -c "medication_name" R/52_gantt_v2_export.R</automated>
  </verify>
  <acceptance_criteria>
    - R/52_gantt_v2_export.R contains `expected_ep_cols <- 21`
    - R/52_gantt_v2_export.R contains `expected_detail_cols <- 19`
    - R/52_gantt_v2_export.R contains 5 guard clauses: `!"medication_name" %in% names(episodes)`, `!"code_type" %in% names(episodes)`, `!"source_table" %in% names(episodes)`, `!"treatment_line" %in% names(episodes)`, `!"sct_cross_use_flag" %in% names(episodes)`
    - R/52_gantt_v2_export.R death_episodes mutate contains `medication_name = NA_character_`
    - R/52_gantt_v2_export.R death_detail mutate contains `medication_name = NA_character_`
    - R/52_gantt_v2_export.R hl_dx_episodes mutate contains `medication_name = NA_character_`
    - R/52_gantt_v2_export.R hl_dx_detail mutate contains `medication_name = NA_character_`
    - R/52_gantt_v2_export.R Step 1 cleanup contains `medication_name = sapply(medication_name, clean_multi_value`
    - R/52_gantt_v2_export.R Step 6 episodes select contains `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`
    - R/52_gantt_v2_export.R Step 6 detail select contains `medication_name, code_type, source_table, treatment_line, sct_cross_use_flag`
    - R/52_gantt_v2_export.R contains `21 columns` in header documentation
    - R/52_gantt_v2_export.R contains `19 columns` in header documentation
    - R/51_gantt_data_export.R is NOT modified (GANTT-07 backward compatibility)
  </acceptance_criteria>
  <done>R/52 produces 21-column episodes CSV and 19-column detail CSV with medication_name, code_type, source_table, treatment_line, sct_cross_use_flag appended at end. Death/HL Diagnosis pseudo-rows have NA for all 5 new columns. Multi-value fields (medication_name, code_type, source_table) cleaned via existing clean_multi_value() pattern. Column count verification passes (21/19). v1 exports untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke test Section 15e for Gantt v2 schema validation</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    R/88_smoke_test_comprehensive.R
    R/52_gantt_v2_export.R
  </read_first>
  <action>
Add Section 15e to R/88_smoke_test_comprehensive.R immediately after Section 15d (after line 1419, before Section 30). This section validates Phase 92's schema extension.

Insert the following section:

```r
# ==============================================================================
# SECTION 15e: GANTT V2 SCHEMA EXTENSION VALIDATION (GANTT-06, GANTT-07) ----
# ==============================================================================

message("\n[GANTT] Gantt v2 schema extension validation (GANTT-06, GANTT-07)...")

r52_lines <- readLines("R/52_gantt_v2_export.R", warn = FALSE)

# GANTT-06: Check 1-5: R/52 select() includes all 5 new Phase 92 columns
phase92_cols <- c("medication_name", "code_type", "source_table", "treatment_line", "sct_cross_use_flag")
for (col in phase92_cols) {
  check(
    glue("R/52 select() includes {col} (GANTT-06)"),
    any(grepl(col, r52_lines, fixed = TRUE))
  )
}

# GANTT-06: Check 6: R/52 episodes expected column count is 21
check(
  "R/52 episodes expected column count is 21",
  any(grepl("expected_ep_cols <- 21", r52_lines, fixed = TRUE))
)

# GANTT-06: Check 7: R/52 detail expected column count is 19
check(
  "R/52 detail expected column count is 19",
  any(grepl("expected_detail_cols <- 19", r52_lines, fixed = TRUE))
)

# GANTT-06: Check 8: R/52 has guard clauses for Phase 91 columns
check(
  "R/52 has guard clause for medication_name",
  any(grepl('!"medication_name" %in% names', r52_lines, fixed = TRUE))
)

# GANTT-06: Check 9: Death pseudo-rows include new columns with NA
check(
  "R/52 death_episodes has medication_name = NA_character_",
  any(grepl("medication_name = NA_character_", r52_lines, fixed = TRUE))
)

# GANTT-06: Check 10: Multi-value cleanup applied to medication_name
check(
  "R/52 applies clean_multi_value to medication_name",
  any(grepl("medication_name = sapply.*clean_multi_value", r52_lines))
)

# GANTT-07: Check 11: R/51 v1 export unchanged (no Phase 92 columns)
r51_lines <- readLines("R/51_gantt_data_export.R", warn = FALSE)
check(
  "R/51 v1 export has NO medication_name (GANTT-07 backward compat)",
  !any(grepl("medication_name", r51_lines, fixed = TRUE))
)
```

Also update Section 16 summary (at end of file) to add GANTT-06 and GANTT-07 requirement messages after the GANTT-05 line:
```r
message("  * GANTT-06: 5 metadata columns in gantt_detail_v2.csv at per-date level (Phase 92)")
message("  * GANTT-07: v1 Gantt exports unchanged -- R/51 has no Phase 92 columns")
```
  </action>
  <verify>
    <automated>grep -c "SECTION 15e" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains `SECTION 15e: GANTT V2 SCHEMA EXTENSION VALIDATION`
    - R/88_smoke_test_comprehensive.R contains `GANTT-06, GANTT-07` in Section 15e header
    - R/88_smoke_test_comprehensive.R contains `expected_ep_cols <- 21` check
    - R/88_smoke_test_comprehensive.R contains `expected_detail_cols <- 19` check
    - R/88_smoke_test_comprehensive.R contains `medication_name = NA_character_` check for death pseudo-rows
    - R/88_smoke_test_comprehensive.R contains `R/51 v1 export has NO medication_name` check
    - R/88_smoke_test_comprehensive.R Section 16 summary contains `GANTT-06` message
    - R/88_smoke_test_comprehensive.R Section 16 summary contains `GANTT-07` message
  </acceptance_criteria>
  <done>Smoke test Section 15e validates: all 5 new columns present in R/52 select(), expected column counts updated to 21/19, guard clauses exist, death pseudo-rows have NA for new columns, multi-value cleanup applied, and R/51 v1 export remains free of Phase 92 columns. Section 16 summary includes GANTT-06 and GANTT-07.</done>
</task>

</tasks>

<verification>
1. `grep "expected_ep_cols <- 21" R/52_gantt_v2_export.R` returns a match
2. `grep "expected_detail_cols <- 19" R/52_gantt_v2_export.R` returns a match
3. `grep -c "medication_name" R/52_gantt_v2_export.R` returns >= 10 (guard, episodes select, detail select, death episodes/detail, HL episodes/detail, cleanup, trimming)
4. `grep -c "sct_cross_use_flag" R/52_gantt_v2_export.R` returns >= 10 (same locations)
5. `grep "SECTION 15e" R/88_smoke_test_comprehensive.R` returns a match
6. `grep "medication_name" R/51_gantt_data_export.R` returns NO match (GANTT-07)
</verification>

<success_criteria>
- R/52 updated to produce 21-column gantt_episodes_v2.csv (was 16) and 19-column gantt_detail_v2.csv (was 14)
- 5 new columns appended at end: medication_name, code_type, source_table, treatment_line, sct_cross_use_flag
- Guard clauses gracefully handle missing Phase 91 columns (fall back to NA)
- Death and HL Diagnosis pseudo-rows populate new columns with NA
- Multi-value columns (medication_name, code_type, source_table) receive clean_multi_value() treatment
- Single-value columns (treatment_line, sct_cross_use_flag) skip multi-value cleanup
- Column count verification updated (21 episodes, 19 detail)
- R/51 v1 export completely unchanged (GANTT-07)
- Smoke test Section 15e validates schema extension and backward compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/92-gantt-v2-schema-extension/92-01-SUMMARY.md`
</output>
