---
phase: 04-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/05_visualize_waterfall.R
  - R/06_visualize_sankey.R
autonomous: false
requirements: [VIZ-01, VIZ-02, VIZ-03]

must_haves:
  truths:
    - "User can source 05_visualize_waterfall.R and see a waterfall bar chart in RStudio viewer"
    - "User can source 06_visualize_sankey.R and see a payer-to-treatment Sankey diagram in RStudio viewer"
    - "Waterfall chart PNG exists at output/figures/waterfall_attrition.png (10x7 inches, 300 DPI)"
    - "Sankey diagram PNG exists at output/figures/sankey_patient_flow.png (10x7 inches, 300 DPI)"
    - "Waterfall bars decrease left-to-right with N and % annotations above each bar"
    - "Sankey flows are colored by payer category using colorblind-safe palette"
    - "Rare treatment combinations (<=10 patients) are collapsed into broader category"
  artifacts:
    - path: "R/05_visualize_waterfall.R"
      provides: "Attrition waterfall chart from attrition_log"
      contains: "geom_col"
    - path: "R/06_visualize_sankey.R"
      provides: "Payer-stratified Sankey/alluvial diagram from hl_cohort"
      contains: "geom_alluvium"
    - path: "output/figures/waterfall_attrition.png"
      provides: "Saved waterfall chart image"
    - path: "output/figures/sankey_patient_flow.png"
      provides: "Saved Sankey diagram image"
  key_links:
    - from: "R/05_visualize_waterfall.R"
      to: "R/04_build_cohort.R"
      via: "source() loads attrition_log"
      pattern: 'source.*04_build_cohort'
    - from: "R/06_visualize_sankey.R"
      to: "R/04_build_cohort.R"
      via: "source() loads hl_cohort"
      pattern: 'source.*04_build_cohort'
    - from: "R/05_visualize_waterfall.R"
      to: "output/figures/waterfall_attrition.png"
      via: "ggsave()"
      pattern: 'ggsave.*waterfall_attrition'
    - from: "R/06_visualize_sankey.R"
      to: "output/figures/sankey_patient_flow.png"
      via: "ggsave()"
      pattern: 'ggsave.*sankey_patient_flow'
---

<objective>
Create the two visualization scripts for Phase 4: an attrition waterfall chart (VIZ-01) and a payer-stratified Sankey/alluvial diagram (VIZ-02). Both display in RStudio viewer and save to PNG in output/figures/. VIZ-03 (HIPAA suppression) is deferred to v2 per D-11.

Purpose: Complete the visualization layer of the pipeline -- the final phase that produces the visual outputs showing cohort attrition and payer-stratified patient flows.

Output: Two R scripts (05_visualize_waterfall.R, 06_visualize_sankey.R) and two PNG images (waterfall_attrition.png, sankey_patient_flow.png)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visualization/04-CONTEXT.md
@.planning/phases/04-visualization/04-RESEARCH.md
@.planning/phases/03-cohort-building/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From R/utils_attrition.R -- attrition_log structure:
```r
# Data frame with columns:
#   step        (character) - filter step name
#   n_before    (integer)   - patients before this step
#   n_after     (integer)   - patients remaining after this step
#   n_excluded  (integer)   - patients removed at this step
#   pct_excluded (numeric)  - percentage excluded at this step
#
# Produced by init_attrition_log() + log_attrition() calls in 04_build_cohort.R
# Available in R global environment after source("R/04_build_cohort.R")
```

From R/04_build_cohort.R -- attrition_log has 4 rows:
```r
# Row 1: "Initial population"        (n_excluded = 0)
# Row 2: "Has HL diagnosis (ICD-9/10)"
# Row 3: "Has enrollment record"
# Row 4: "Valid payer category"
```

From R/04_build_cohort.R -- hl_cohort structure (18 columns):
```r
# hl_cohort tibble columns:
#   ID, SOURCE, SEX, RACE, HISPANIC,
#   age_at_enr_start, age_at_enr_end,
#   first_hl_dx_date,
#   PAYER_CATEGORY_PRIMARY,         # 9-category system (character)
#   PAYER_CATEGORY_AT_FIRST_DX,     # 9-category system (character)
#   DUAL_ELIGIBLE,                  # integer 0/1
#   PAYER_TRANSITION,               # integer 0/1
#   N_ENCOUNTERS, N_ENCOUNTERS_WITH_PAYER,
#   HAD_CHEMO,                      # integer 0/1
#   HAD_RADIATION,                  # integer 0/1
#   HAD_SCT,                        # integer 0/1
#   enrollment_duration_days
#
# Available in R global environment after source("R/04_build_cohort.R")
```

From R/00_config.R -- PAYER_MAPPING categories:
```r
PAYER_MAPPING$categories = c(
  "Medicare", "Medicaid", "Dual eligible", "Private",
  "Other government", "No payment / Self-pay", "Other",
  "Unavailable", "Unknown"
)
```

From R/00_config.R -- CONFIG output_dir:
```r
CONFIG$output_dir = "output"
# output/figures/ directory already exists with .gitkeep
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create waterfall attrition chart (05_visualize_waterfall.R)</name>
  <files>R/05_visualize_waterfall.R</files>
  <read_first>
    - R/04_build_cohort.R (understand attrition_log structure and how it is populated)
    - R/utils_attrition.R (understand log_attrition column names: step, n_before, n_after, n_excluded, pct_excluded)
    - R/00_config.R (CONFIG$output_dir for output path)
    - .planning/phases/04-visualization/04-RESEARCH.md (Pattern 1: Waterfall Chart with Annotations -- verified code template)
  </read_first>
  <action>
Create R/05_visualize_waterfall.R with the following structure (per D-01 through D-04, D-12 through D-15):

**Header and dependencies:**
```r
# ==============================================================================
# 05_visualize_waterfall.R -- Attrition waterfall chart
# ==============================================================================
# Produces vertical bar chart showing cohort reduction through filter steps.
# Each bar shows patients remaining, annotated with N and % excluded.
# Requirements: VIZ-01
# ==============================================================================

source("R/04_build_cohort.R")  # Loads attrition_log, hl_cohort, all upstream

library(ggplot2)
library(dplyr)
library(glue)
library(scales)
```

**Data preparation:**
- Convert attrition_log$step to factor with `levels = unique(attrition_log$step)` to preserve execution order (per D-01: left-to-right decreasing)
- Create annotation label column using glue: `"{comma(n_after)}\n(-{round(pct_excluded, 1)}%)"` (per D-02: N remaining AND % excluded)
- For the first row (Initial population), use a special label without the negative percentage: `"{comma(n_after)}"` since pct_excluded = 0

**Build ggplot:**
- `ggplot(attrition_plot_data, aes(x = step, y = n_after))`
- `geom_col(fill = "steelblue3", width = 0.7, alpha = 0.9)` (per D-03: single steel blue color for all bars)
- `geom_text(aes(label = label), vjust = -0.5, size = 3.5, fontface = "bold")` for annotations above bars
- `scale_y_continuous(labels = comma, expand = expansion(mult = c(0, 0.15)))` -- extra top space for labels
- `labs(title = "Cohort Attrition Through Filter Steps", subtitle = glue("Hodgkin Lymphoma cohort: {comma(max(...))} -> {comma(min(...))} patients"), x = "Filter Step", y = "Patients Remaining", caption = "Annotations show N remaining and % excluded from previous step")`
- `theme_minimal(base_size = 12)` (per D-12)
- `theme(axis.text.x = element_text(angle = 45, hjust = 1), panel.grid.major.x = element_blank(), panel.grid.minor = element_blank(), plot.title = element_text(face = "bold", size = 14), plot.caption = element_text(hjust = 0, face = "italic", color = "gray40"))`

NOTE: Do NOT facet by payer (per D-04: total cohort attrition only).

**Output:**
- `print(p_waterfall)` to display in RStudio viewer (per D-15)
- `ggsave(filename = file.path(CONFIG$output_dir, "figures", "waterfall_attrition.png"), plot = p_waterfall, width = 10, height = 7, units = "in", dpi = 300, bg = "white")` (per D-14: 10x7, 300 DPI)
- `message(glue("Waterfall chart saved to {file.path(CONFIG$output_dir, 'figures', 'waterfall_attrition.png')}"))` for console confirmation

Use `file.path(CONFIG$output_dir, "figures", ...)` not `here()` to stay consistent with existing codebase pattern (04_build_cohort.R uses `file.path(CONFIG$output_dir, ...)`).
  </action>
  <verify>
    <automated>cd "C:/Users/Owner/Documents/insurance_investigation" && grep -c "geom_col\|ggsave\|attrition_log\|steelblue\|theme_minimal\|waterfall_attrition.png" R/05_visualize_waterfall.R</automated>
  </verify>
  <acceptance_criteria>
    - R/05_visualize_waterfall.R exists and is non-empty
    - File contains `source("R/04_build_cohort.R")` (loads upstream data)
    - File contains `geom_col(fill = "steelblue3"` or similar steel blue fill (D-03)
    - File contains `geom_text(` for bar annotations (D-02)
    - File contains `theme_minimal(` (D-12)
    - File contains `ggsave(` with `width = 10, height = 7` and `dpi = 300` (D-14)
    - File contains `waterfall_attrition.png` as output filename (VIZ-01)
    - File contains `print(` to display in RStudio viewer (D-15)
    - File does NOT contain `facet_` (D-04: no payer faceting)
    - File contains `factor(step, levels = unique` to preserve step order (D-01)
    - File contains `comma(` from scales package for number formatting
    - File contains `pct_excluded` for percentage annotation (D-02)
  </acceptance_criteria>
  <done>R/05_visualize_waterfall.R creates a vertical bar chart from attrition_log with steel blue bars decreasing left-to-right, each annotated with N remaining and % excluded. Saves to output/figures/waterfall_attrition.png at 10x7 inches 300 DPI and displays in RStudio viewer.</done>
</task>

<task type="auto">
  <name>Task 2: Create payer-stratified Sankey diagram (06_visualize_sankey.R)</name>
  <files>R/06_visualize_sankey.R</files>
  <read_first>
    - R/04_build_cohort.R (understand hl_cohort structure: PAYER_CATEGORY_PRIMARY, HAD_CHEMO, HAD_RADIATION, HAD_SCT)
    - R/00_config.R (PAYER_MAPPING$categories for the 9-category list, CONFIG$output_dir)
    - .planning/phases/04-visualization/04-RESEARCH.md (Pattern 2: Payer-Stratified Sankey, Pattern 3: Mutually Exclusive Treatment Categories, Pitfall 1: alluvial form error, Pitfall 6: small cell collapsing)
  </read_first>
  <action>
Create R/06_visualize_sankey.R with the following structure (per D-05 through D-10, D-12 through D-15):

**Header and dependencies:**
```r
# ==============================================================================
# 06_visualize_sankey.R -- Payer-stratified Sankey/alluvial diagram
# ==============================================================================
# Produces two-axis alluvial diagram: Payer Category -> Treatment Type
# Flows colored by payer category using colorblind-safe viridis palette.
# Requirements: VIZ-02
# NOTE: HIPAA small-cell suppression deferred to v2 (D-11). Outputs are exploratory, remain on HiPerGator.
# ==============================================================================

source("R/04_build_cohort.R")  # Loads hl_cohort, all upstream

library(ggplot2)
library(ggalluvial)
library(dplyr)
library(forcats)
library(glue)
library(scales)
```

**Derive mutually exclusive treatment categories (per D-06):**
Use hierarchical case_when() -- SCT most intensive, checked first:
```r
sankey_data <- hl_cohort %>%
  mutate(
    TREATMENT_CATEGORY = case_when(
      HAD_SCT == 1                           ~ "SCT (any combination)",
      HAD_CHEMO == 1 & HAD_RADIATION == 1    ~ "Chemo + Radiation",
      HAD_CHEMO == 1                         ~ "Chemo only",
      HAD_RADIATION == 1                     ~ "Radiation only",
      TRUE                                   ~ "No treatment evidence"
    )
  )
```
ALWAYS include the `TRUE ~ "No treatment evidence"` catch-all to prevent NAs.

**Collapse rare treatment combinations (per D-10: <=10 patients merged):**
Use mutate + if_else to RECODE (not filter -- never drop rows):
```r
sankey_data <- sankey_data %>%
  group_by(TREATMENT_CATEGORY) %>%
  mutate(
    n_in_category = n(),
    TREATMENT_CATEGORY = if_else(
      n_in_category <= 10 & TREATMENT_CATEGORY != "No treatment evidence",
      "Multiple treatments",
      TREATMENT_CATEGORY
    )
  ) %>%
  ungroup() %>%
  select(-n_in_category)
```
Verify no patients dropped: `stopifnot(nrow(sankey_data) == nrow(hl_cohort))`

**Collapse small payer categories (per D-08):**
```r
sankey_data <- sankey_data %>%
  mutate(
    PAYER_CATEGORY_PRIMARY = fct_lump_n(
      fct_infreq(PAYER_CATEGORY_PRIMARY),
      n = 7,
      other_level = "Other"
    )
  )
```
This keeps top 7 payer categories by frequency, merges rest into "Other". The fct_infreq() orders by frequency first (largest first per D-09 implicit ordering).

**Create stratum labels with N counts (per D-09):**
The stratum labels need "Category (N=X,XXX)" format. Since ggalluvial's built-in stratum stat only gives category names via after_stat(stratum), encode N into the factor levels:
```r
# Encode N into factor levels for payer axis
payer_counts <- sankey_data %>% count(PAYER_CATEGORY_PRIMARY, name = "n_patients")
sankey_data <- sankey_data %>%
  left_join(payer_counts, by = "PAYER_CATEGORY_PRIMARY") %>%
  mutate(
    PAYER_LABEL = glue("{PAYER_CATEGORY_PRIMARY}\n(N={comma(n_patients)})"),
    PAYER_LABEL = fct_reorder(PAYER_LABEL, n_patients, .desc = TRUE)
  ) %>%
  select(-n_patients)

# Same for treatment axis
tx_counts <- sankey_data %>% count(TREATMENT_CATEGORY, name = "n_patients")
sankey_data <- sankey_data %>%
  left_join(tx_counts, by = "TREATMENT_CATEGORY") %>%
  mutate(
    TREATMENT_LABEL = glue("{TREATMENT_CATEGORY}\n(N={comma(n_patients)})"),
    TREATMENT_LABEL = fct_reorder(TREATMENT_LABEL, n_patients, .desc = TRUE)
  ) %>%
  select(-n_patients)
```

**Build ggalluvial plot (per D-05, D-07):**
Use wide format with axis1/axis2 (simpler for two-axis):
```r
p_sankey <- ggplot(
  sankey_data,
  aes(axis1 = PAYER_LABEL, axis2 = TREATMENT_LABEL)
) +
  geom_alluvium(
    aes(fill = PAYER_LABEL),
    curve_type = "xspline",
    alpha = 0.7,
    width = 1/12
  ) +
  geom_stratum(width = 1/12, fill = "gray90", color = "gray50") +
  geom_text(
    stat = "stratum",
    aes(label = after_stat(stratum)),
    size = 3
  ) +
  scale_x_discrete(
    limits = c("Payer Category", "Treatment Type"),
    expand = c(0.15, 0.15)
  ) +
  scale_fill_viridis_d(
    option = "mako",
    name = "Payer Category",
    begin = 0.1,
    end = 0.9
  ) +
  labs(
    title = "Patient Flow: Payer Category to Treatment Type",
    subtitle = glue("Hodgkin Lymphoma cohort (N = {comma(nrow(sankey_data))})"),
    caption = "Flow width proportional to patient count; rare categories collapsed"
  ) +
  theme_minimal(base_size = 12) +
  theme(
    legend.position = "bottom",
    legend.title = element_text(face = "bold"),
    axis.text.x = element_text(face = "bold", size = 11),
    axis.text.y = element_blank(),
    axis.title = element_blank(),
    panel.grid = element_blank(),
    plot.title = element_text(face = "bold", size = 14),
    plot.caption = element_text(hjust = 0, face = "italic", color = "gray40")
  )
```

Per D-07: flows colored by payer category (fill = PAYER_LABEL which derives from PAYER_CATEGORY_PRIMARY).
Per D-09: stratum labels show "category name\n(N=X,XXX)" via encoded factor levels.
Per D-13: colorblind-safe viridis "mako" discrete palette with begin=0.1, end=0.9 for contrast.

**Output:**
- `print(p_sankey)` to display in RStudio viewer (per D-15)
- `ggsave(filename = file.path(CONFIG$output_dir, "figures", "sankey_patient_flow.png"), plot = p_sankey, width = 10, height = 7, units = "in", dpi = 300, bg = "white")` (per D-14)
- `message(glue("Sankey diagram saved to {file.path(CONFIG$output_dir, 'figures', 'sankey_patient_flow.png')}"))` for console confirmation

**Console summary before plot:**
- Print treatment category distribution: `sankey_data %>% count(TREATMENT_CATEGORY) %>% arrange(desc(n))`
- Print payer category distribution (after collapsing): `sankey_data %>% count(PAYER_CATEGORY_PRIMARY) %>% arrange(desc(n))`
- Verify row count matches hl_cohort
  </action>
  <verify>
    <automated>cd "C:/Users/Owner/Documents/insurance_investigation" && grep -c "geom_alluvium\|geom_stratum\|ggsave\|case_when\|fct_lump_n\|fct_infreq\|viridis\|sankey_patient_flow.png\|theme_minimal\|ggalluvial" R/06_visualize_sankey.R</automated>
  </verify>
  <acceptance_criteria>
    - R/06_visualize_sankey.R exists and is non-empty
    - File contains `source("R/04_build_cohort.R")` (loads upstream data)
    - File contains `library(ggalluvial)` (D-05 Sankey requirement)
    - File contains `case_when(` with all 5 treatment categories: "SCT (any combination)", "Chemo + Radiation", "Chemo only", "Radiation only", "No treatment evidence" (D-06)
    - File contains `TRUE ~` catch-all in case_when (prevents NA)
    - File contains `if_else(` for collapsing rare combos, NOT `filter(` (D-10, prevents patient loss)
    - File contains `<= 10` or `<=10` threshold for rare treatment collapse (D-10)
    - File contains `fct_lump_n(` for collapsing small payer categories (D-08)
    - File contains `fct_infreq(` for frequency-based ordering
    - File contains `geom_alluvium(` with `fill =` aesthetic for payer coloring (D-07)
    - File contains `geom_stratum(` for category boxes
    - File contains `scale_fill_viridis_d(` with `option = "mako"` (D-13)
    - File contains `theme_minimal(` (D-12)
    - File contains `ggsave(` with `width = 10, height = 7` and `dpi = 300` (D-14)
    - File contains `sankey_patient_flow.png` as output filename (VIZ-02)
    - File contains `print(` to display in RStudio viewer (D-15)
    - File contains `stopifnot(nrow(sankey_data) == nrow(hl_cohort))` or equivalent row count verification
    - File contains a comment referencing D-11 or VIZ-03 deferral
    - File contains `N=` in label construction (D-09: stratum labels show N)
  </acceptance_criteria>
  <done>R/06_visualize_sankey.R creates a two-axis alluvial diagram (Payer -> Treatment) with flows colored by payer category using viridis mako palette. Treatment categories derived from HAD_* flags via case_when(). Rare combos (<=10 patients) collapsed. Small payer categories merged into "Other". Stratum labels show category name + N. Saves to output/figures/sankey_patient_flow.png at 10x7 inches 300 DPI and displays in RStudio viewer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of both charts</name>
  <files>output/figures/waterfall_attrition.png, output/figures/sankey_patient_flow.png</files>
  <action>
User visually verifies both charts in RStudio on HiPerGator. This is a human-verify checkpoint -- Claude has completed all automation in Tasks 1-2. The user runs the scripts and confirms the visual output meets expectations.

Steps for user:
1. In RStudio on HiPerGator, run: `source("R/05_visualize_waterfall.R")`
2. In RStudio on HiPerGator, run: `source("R/06_visualize_sankey.R")`
3. Check RStudio Plots pane and saved PNG files in output/figures/
  </action>
  <verify>
    <automated>cd "C:/Users/Owner/Documents/insurance_investigation" && ls output/figures/waterfall_attrition.png output/figures/sankey_patient_flow.png 2>/dev/null && echo "Both PNGs exist"</automated>
  </verify>
  <acceptance_criteria>
    - output/figures/waterfall_attrition.png exists (VIZ-01 output)
    - output/figures/sankey_patient_flow.png exists (VIZ-02 output)
    - User confirms waterfall bars decrease left-to-right with annotations
    - User confirms Sankey shows payer-to-treatment flows with distinguishable colors
  </acceptance_criteria>
  <done>User has visually confirmed both charts render correctly in RStudio and saved PNGs look publication-ready.</done>
</task>

</tasks>

<verification>
Phase 4 verification:
1. `R/05_visualize_waterfall.R` exists with geom_col + steelblue fill + geom_text annotations + ggsave to waterfall_attrition.png
2. `R/06_visualize_sankey.R` exists with geom_alluvium + geom_stratum + viridis mako palette + ggsave to sankey_patient_flow.png
3. Both scripts source("R/04_build_cohort.R") for upstream data
4. Treatment categories derived via case_when() with 5 mutually exclusive categories
5. Rare treatment combos (<=10 patients) collapsed via recode, not filter
6. Small payer categories collapsed via fct_lump_n()
7. Both use theme_minimal(), 10x7 inches, 300 DPI PNG output
8. VIZ-03 deferred per D-11 with documented comment
</verification>

<success_criteria>
- VIZ-01: waterfall_attrition.png exists showing 4-step cohort reduction with annotated bars
- VIZ-02: sankey_patient_flow.png exists showing payer-to-treatment flows with colorblind-safe palette
- VIZ-03: Acknowledged as deferred (D-11) with code comment
- Both charts display in RStudio viewer before saving
- User visually confirms charts look correct (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/04-visualization/04-01-SUMMARY.md`
</output>
