---
phase: 35-tiered-same-day-payer-categorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/36_tiered_same_day_payer.R
autonomous: true
requirements: [TIER-01, TIER-02, TIER-03, TIER-04, TIER-05, TIER-06, TIER-07, TIER-08, TIER-09, TIER-10]

must_haves:
  truths:
    - "User can see every distinct raw PAYER_TYPE_PRIMARY and PAYER_TYPE_SECONDARY code with occurrence counts, cross-referenced against PayerVariable.xlsx, for BOTH all encounters and AV+TH encounters"
    - "User can see a configurable tier mapping at the top of the script that PIs can edit with one-line changes"
    - "User can see same-day payer resolution for each patient-date using Medicaid>Medicare>Private>Other>Self-pay>Uninsured>Missing hierarchy"
    - "User can see FLM source override applied at the ENCOUNTER.SOURCE level per patient-date (not patient level)"
    - "User can see codes 93 and 14 explicitly mapped to Medicaid tier"
    - "User can see before vs after category distribution showing the impact of hierarchical resolution"
    - "User can see 10 CSV files in output/tables/ -- 4 frequency CSVs + 6 resolution CSVs across both scopes"
  artifacts:
    - path: "R/36_tiered_same_day_payer.R"
      provides: "Standalone diagnostic script: dual-scope frequency tables + same-day payer resolution"
      min_lines: 400
      contains: "TIER_MAPPING"
    - path: "output/tables/payer_primary_code_freq_all.csv"
      provides: "Primary payer code frequency (all encounters)"
    - path: "output/tables/payer_secondary_code_freq_all.csv"
      provides: "Secondary payer code frequency (all encounters)"
    - path: "output/tables/payer_category_summary_all.csv"
      provides: "Category-level summary (all encounters)"
    - path: "output/tables/payer_primary_code_freq_av_th_v2.csv"
      provides: "Primary payer code frequency (AV+TH scope, from this script)"
    - path: "output/tables/payer_secondary_code_freq_av_th_v2.csv"
      provides: "Secondary payer code frequency (AV+TH scope, from this script)"
    - path: "output/tables/payer_category_summary_av_th_v2.csv"
      provides: "Category-level summary (AV+TH scope, from this script)"
    - path: "output/tables/payer_resolved_detail_all.csv"
      provides: "CSV A: Per-patient-per-date resolved payer (all encounters)"
    - path: "output/tables/payer_resolved_detail_av_th.csv"
      provides: "CSV A: Per-patient-per-date resolved payer (AV+TH)"
    - path: "output/tables/payer_resolved_patient_summary_all.csv"
      provides: "CSV B: Patient-level modal resolved payer (all encounters)"
    - path: "output/tables/payer_resolved_patient_summary_av_th.csv"
      provides: "CSV B: Patient-level modal resolved payer (AV+TH)"
    - path: "output/tables/payer_resolved_impact_all.csv"
      provides: "CSV C: Before vs after category distribution (all encounters)"
    - path: "output/tables/payer_resolved_impact_av_th.csv"
      provides: "CSV C: Before vs after category distribution (AV+TH)"
  key_links:
    - from: "R/36_tiered_same_day_payer.R"
      to: "R/utils_duckdb.R"
      via: "get_pcornet_table('ENCOUNTER') %>% materialize()"
      pattern: "get_pcornet_table.*ENCOUNTER.*materialize"
    - from: "R/36_tiered_same_day_payer.R"
      to: "PayerVariable.xlsx"
      via: "readxl::read_excel()"
      pattern: "read_excel.*PayerVariable"
    - from: "R/36_tiered_same_day_payer.R"
      to: "R/02_harmonize_payer.R"
      via: "Replicates compute_effective_payer + map_payer_category logic inline (does NOT source 02)"
      pattern: "compute_effective_payer|map_payer_category"
---

<objective>
Create a standalone diagnostic script R/36_tiered_same_day_payer.R that produces two deliverables per Amy Crisp's framework:

1. Raw payer frequency tables with PayerVariable.xlsx cross-reference for BOTH all-encounter and AV+TH scopes (4 frequency CSVs + 2 category summary CSVs = 6 frequency outputs)
2. Hierarchical same-day payer resolution using the priority hierarchy Medicaid > Medicare > Private > Other > Self-pay > Uninsured > Missing for BOTH scopes (3 resolution CSVs per scope = 6 resolution outputs)

Purpose: Give PIs (Amy Crisp) a configurable tool to resolve multi-encounter same-day payer conflicts and see the impact of hierarchical resolution on payer category distributions.

Output: R/36_tiered_same_day_payer.R + 10 CSV files in output/tables/ (on HiPerGator)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@payer_framework.txt
@R/35_payer_code_frequency_av_th.R
@R/33_multi_source_overlap_av_th.R
@R/02_harmonize_payer.R
@R/00_config.R
@R/utils_duckdb.R
@PayerVariable.xlsx

<interfaces>
<!-- Key functions and patterns the executor needs -->

From R/utils_duckdb.R:
- get_pcornet_table(name, con) -- returns pipeable dplyr object (lazy or tibble)
- open_pcornet_con() -- opens DuckDB connection, stores in .GlobalEnv
- close_pcornet_con() -- closes DuckDB connection
- materialize(x) -- collects lazy DuckDB query to tibble

From R/00_config.R (lines 285-314):
- PAYER_MAPPING$medicare_prefix = "1"
- PAYER_MAPPING$medicaid_prefix = "2"
- PAYER_MAPPING$private_prefix = c("5", "6")
- PAYER_MAPPING$other_gov_prefix = c("3", "4")
- PAYER_MAPPING$no_payment_prefix = "8"
- PAYER_MAPPING$other_prefix = c("7", "9")
- PAYER_MAPPING$unavailable_codes = c("99", "9999")
- PAYER_MAPPING$unknown_codes = c("NI", "UN", "OT", "UNKNOWN")
- PAYER_MAPPING$dual_eligible_codes = c("14", "141", "142")
- PAYER_MAPPING$sentinel_values = c("NI", "UN", "OT")

From R/02_harmonize_payer.R:
- compute_effective_payer(primary, secondary) -- returns primary if valid, else secondary if valid, else NA
- map_payer_category(effective_payer, dual_eligible_encounter) -- returns 9-category string
- detect_dual_eligible(primary, secondary) -- returns 0L or 1L

From R/35_payer_code_frequency_av_th.R:
- PayerVariable.xlsx loading pattern: read_excel("PayerVariable.xlsx", sheet = "Sheet2"), rename cols to c("code", "description", "category"), trimws all
- Frequency counting pattern: mutate code (NA->"<NA>", ""->"<EMPTY>"), count(), left_join payer_lookup, flag "NOT IN XLSX"
- Output path pattern: file.path(CONFIG$output_dir, "tables")

From payer_framework.txt (Amy Crisp email):
- Hierarchy: Medicaid (incl. 93, 14, FLM source) > Medicare > Private > Other > Self-pay > Uninsured > Missing
- FLM override: "if FLM is a source, then all encounters on that day are Medicaid"
- Scope: "If a patient has multiple encounters on the same day (can come from one source or multiple sources)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/36_tiered_same_day_payer.R -- setup, tier mapping, data loading, and frequency tables (dual scope)</name>
  <files>R/36_tiered_same_day_payer.R</files>
  <read_first>
    R/35_payer_code_frequency_av_th.R
    R/33_multi_source_overlap_av_th.R
    R/02_harmonize_payer.R
    R/00_config.R
    R/utils_duckdb.R
    payer_framework.txt
  </read_first>
  <action>
Create a new standalone diagnostic script `R/36_tiered_same_day_payer.R` with the following sections. This script does NOT modify any existing script (per D-06, D-10). Use the Phase 33/34 structural pattern: `source("R/00_config.R")`, DuckDB materialize-early, conditional RDS fallback (per D-09).

**SECTION 0: Setup and Tier Configuration (per D-01, D-02, D-03)**

```r
source("R/00_config.R")
library(dplyr)
library(glue)
library(readr)
library(readxl)
library(stringr)

# Load tables if not already loaded (RDS mode)
if (!USE_DUCKDB && !exists("pcornet")) source("R/01_load_pcornet.R")
# DuckDB mode: open connection if needed
if (USE_DUCKDB && !exists("pcornet_con", envir = .GlobalEnv)) {
  open_pcornet_con()
}

# Path to PayerVariable.xlsx (repo root)
PAYER_XLSX_PATH <- "PayerVariable.xlsx"
```

Define `TIER_MAPPING` as a NAMED LIST at the top of the script (per D-01 -- configurable, not buried in case_when):

```r
# ==========================================================================
# TIER HIERARCHY CONFIGURATION (per Amy Crisp framework)
# Lower rank = higher priority. PIs can edit this with one-line changes.
# ==========================================================================
TIER_MAPPING <- list(
  Medicaid   = 1L,  # Highest priority (includes dual-eligible, codes 93/14, FLM source)
  Medicare   = 2L,
  Private    = 3L,
  Other      = 4L,
  "Self-pay" = 5L,
  Uninsured  = 6L,
  Missing    = 7L   # Lowest priority
)
```

Define `CODE_TO_TIER()` function that maps the R pipeline's 9-category payer scheme to the 6 tiers (per D-02):
- "Medicaid" -> "Medicaid"
- "Dual eligible" -> "Medicaid" (D-02 collapse)
- "Medicare" -> "Medicare"
- "Private" -> "Private"
- "Other government" -> "Other" (D-02 collapse)
- "Other" -> "Other"
- "No payment / Self-pay" -> "Self-pay"
- "Unavailable" -> "Missing" (D-02 collapse)
- "Unknown" -> "Missing" (D-02 collapse)
- is.na -> "Missing"
- TRUE -> "Missing"

Define `SPECIAL_CODE_TIER()` function that checks raw payer codes 93 and 14 and returns "Medicaid" for those, NA_character_ otherwise (per D-03). Check BOTH PAYER_TYPE_PRIMARY and PAYER_TYPE_SECONDARY.

Replicate `compute_effective_payer()` and `map_payer_category()` logic INLINE (do NOT source R/02_harmonize_payer.R since that sources R/01_load_pcornet.R which would re-load data). Copy the function definitions from R/02_harmonize_payer.R lines 45-133 verbatim, or define equivalent helper functions in this script:
- `compute_effective_payer_local(primary, secondary)` -- same logic as original
- `map_payer_category_local(effective_payer, dual_eligible_encounter)` -- same logic as original
- `detect_dual_eligible_local(primary, secondary)` -- same logic as original

**SECTION 1: Load PayerVariable.xlsx**

Identical pattern to R/35_payer_code_frequency_av_th.R SECTION 1:
- `readxl::read_excel(PAYER_XLSX_PATH, sheet = "Sheet2")`
- Rename columns to `c("code", "description", "category")`
- `trimws(as.character(.))` on all columns
- Log row count, unique categories, sample rows

**SECTION 2: Load ENCOUNTER table and prepare both scopes**

```r
enc_raw <- get_pcornet_table("ENCOUNTER") %>% materialize()
```

Ensure PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY, SOURCE are character. Parse ADMIT_DATE to Date if needed (same pattern as R/33 lines 104-127).

Compute effective_payer, dual_eligible, payer_category (9-cat), and tier (6-tier) for ALL encounters:

```r
enc <- enc_raw %>%
  mutate(
    PAYER_TYPE_PRIMARY   = as.character(PAYER_TYPE_PRIMARY),
    PAYER_TYPE_SECONDARY = as.character(PAYER_TYPE_SECONDARY),
    SOURCE               = as.character(SOURCE),
    admit_date_parsed    = as.Date(ADMIT_DATE, format = "%Y-%m-%d"),
    # Compute effective payer (primary if valid, else secondary)
    effective_payer = compute_effective_payer_local(PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY),
    # Detect dual-eligible
    dual_eligible = detect_dual_eligible_local(PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY),
    # Map to 9-category
    payer_category_9cat = map_payer_category_local(effective_payer, dual_eligible),
    # Map to 6-tier (D-02)
    tier = CODE_TO_TIER(payer_category_9cat),
    # Override with special codes 93/14 (D-03) -- check raw codes, not effective
    tier = coalesce(
      case_when(
        PAYER_TYPE_PRIMARY %in% c("93", "14") ~ "Medicaid",
        PAYER_TYPE_SECONDARY %in% c("93", "14") ~ "Medicaid",
        TRUE ~ NA_character_
      ),
      tier
    ),
    # Assign tier rank
    tier_rank = unlist(TIER_MAPPING[tier])
  )
```

Create two scoped datasets:
- `enc_all` = `enc` (no filter, all encounters)
- `enc_av_th` = `enc %>% filter(ENC_TYPE %in% c("AV", "TH"))`

Log counts for both scopes.

**SECTION 3: Frequency Tables (dual scope, per D-05, D-08)**

Create a helper function `build_frequency_tables(enc_scope, suffix, payer_lookup, output_dir)` that:

1. Builds PAYER_TYPE_PRIMARY frequency table (same pattern as R/35 SECTION 3):
   - `mutate(code = case_when(is.na(PTP) ~ "<NA>", PTP == "" ~ "<EMPTY>", TRUE ~ PTP))`
   - `count(code, name = "n") %>% arrange(desc(n))`
   - `left_join(payer_lookup, by = "code")`
   - Flag "NOT IN XLSX" for codes not in lookup
   - Add `pct = round(100 * n / nrow(enc_scope), 2)`
   - Write to `payer_primary_code_freq{suffix}.csv`

2. Builds PAYER_TYPE_SECONDARY frequency table (same pattern as R/35 SECTION 4):
   - Same logic but on PAYER_TYPE_SECONDARY
   - Write to `payer_secondary_code_freq{suffix}.csv`

3. Builds category-level summary (same pattern as R/35 SECTION 5):
   - Group primary_freq by category, sum n, compute pct
   - Group secondary_freq by category, sum n, compute pct
   - bind_rows with field column ("PRIMARY", "SECONDARY")
   - Write to `payer_category_summary{suffix}.csv`

Call with `suffix = "_all"` for all encounters and `suffix = "_av_th_v2"` for AV+TH (using `_v2` suffix to avoid overwriting Phase 34 baseline files per D-06).

Log distinct code counts and "NOT IN XLSX" counts for each scope.

**SECTION 4: Same-Day Payer Resolution (dual scope, per D-03, D-04, D-07)**

Create a helper function `resolve_same_day_payer(enc_scope, suffix, output_dir)` that:

1. **Groups encounters by (ID, admit_date_parsed)** and resolves:

```r
resolved_detail <- enc_scope %>%
  filter(!is.na(admit_date_parsed)) %>%
  group_by(ID, admit_date_parsed) %>%
  summarise(
    n_encounters = n(),
    n_distinct_tiers = n_distinct(tier),
    has_flm = any(SOURCE == "FLM", na.rm = TRUE),
    has_special_code = any(PAYER_TYPE_PRIMARY %in% c("93", "14") |
                           PAYER_TYPE_SECONDARY %in% c("93", "14"), na.rm = TRUE),
    original_tiers = paste(sort(unique(tier)), collapse = "+"),
    original_codes_primary = paste(sort(unique(na.omit(PAYER_TYPE_PRIMARY))), collapse = ","),
    # Resolution logic (D-03, D-04):
    resolved_payer = case_when(
      any(SOURCE == "FLM", na.rm = TRUE) ~ "Medicaid",           # FLM override (D-04)
      any(PAYER_TYPE_PRIMARY %in% c("93", "14") |
          PAYER_TYPE_SECONDARY %in% c("93", "14"), na.rm = TRUE) ~ "Medicaid",  # Code override (D-03)
      TRUE ~ tier[which.min(tier_rank)]                            # Tier hierarchy
    ),
    resolution_reason = case_when(
      n() == 1 ~ "single encounter",
      any(SOURCE == "FLM", na.rm = TRUE) ~ "FLM source override",
      any(PAYER_TYPE_PRIMARY %in% c("93", "14") |
          PAYER_TYPE_SECONDARY %in% c("93", "14"), na.rm = TRUE) ~ "special code override (93/14)",
      n_distinct(tier) == 1 ~ "all encounters same tier",
      TRUE ~ paste0("tier hierarchy (", n_distinct(tier), " tiers)")
    ),
    .groups = "drop"
  ) %>%
  rename(ADMIT_DATE = admit_date_parsed) %>%
  arrange(ID, ADMIT_DATE)
```

Write `resolved_detail` to `payer_resolved_detail{suffix}.csv` (CSV A per D-07).

2. **Patient-level modal summary (CSV B per D-07):**

```r
patient_summary <- resolved_detail %>%
  group_by(ID, resolved_payer) %>%
  summarise(n_dates_with_payer = n(), .groups = "drop") %>%
  arrange(ID, desc(n_dates_with_payer), resolved_payer) %>%
  group_by(ID) %>%
  slice(1) %>%
  ungroup() %>%
  rename(modal_resolved_payer = resolved_payer) %>%
  mutate(
    total_dates = sum(n_dates_with_payer)
  )
```

Actually, compute total_dates correctly by joining back:
```r
patient_total_dates <- resolved_detail %>%
  group_by(ID) %>%
  summarise(total_dates = n(), .groups = "drop")

patient_summary <- resolved_detail %>%
  count(ID, resolved_payer, name = "n_dates_with_payer") %>%
  arrange(ID, desc(n_dates_with_payer), resolved_payer) %>%
  group_by(ID) %>%
  slice(1) %>%
  ungroup() %>%
  rename(modal_resolved_payer = resolved_payer) %>%
  left_join(patient_total_dates, by = "ID")
```

Write to `payer_resolved_patient_summary{suffix}.csv`.

3. **Before vs after impact (CSV C per D-07):**

"Before" = encounter-level tier distribution (one row per encounter, counting by tier).
"After" = patient-date-level resolved payer distribution (one row per patient-date, counting by resolved_payer).

```r
before_resolution <- enc_scope %>%
  filter(!is.na(admit_date_parsed)) %>%
  count(tier, name = "n_encounters_before") %>%
  arrange(match(tier, names(TIER_MAPPING)))

after_resolution <- resolved_detail %>%
  count(resolved_payer, name = "n_patient_dates_after") %>%
  arrange(match(resolved_payer, names(TIER_MAPPING)))

impact <- before_resolution %>%
  full_join(after_resolution, by = c("tier" = "resolved_payer")) %>%
  mutate(
    n_encounters_before = coalesce(n_encounters_before, 0L),
    n_patient_dates_after = coalesce(n_patient_dates_after, 0L),
    pct_encounters_before = round(100 * n_encounters_before / sum(n_encounters_before), 2),
    pct_patient_dates_after = round(100 * n_patient_dates_after / sum(n_patient_dates_after), 2)
  ) %>%
  rename(category = tier)
```

Write to `payer_resolved_impact{suffix}.csv`.

Call `resolve_same_day_payer()` with `suffix = "_all"` and `suffix = "_av_th"`.

**SECTION 5: Console Summary**

Print a comprehensive console summary including:
- Total encounters analyzed per scope
- Frequency table highlights (top 5 codes, NOT IN XLSX codes) per scope
- Resolution summary per scope: total patient-dates, single vs multi-encounter, resolution reason breakdown
- Before vs after impact table per scope
- List of all CSV files written (10 total)
- Header/footer banners following Phase 33/34 pattern

**SECTION 6: Script end**

Final banner message. Do NOT close DuckDB connection (leave it for interactive use).
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path 'C:\Users\Owner\Documents\insurance_investigation\R\36_tiered_same_day_payer.R') { $content = Get-Content 'C:\Users\Owner\Documents\insurance_investigation\R\36_tiered_same_day_payer.R' -Raw; $checks = @('TIER_MAPPING', 'CODE_TO_TIER', 'compute_effective_payer', 'map_payer_category', 'resolve_same_day_payer', 'build_frequency_tables', 'payer_resolved_detail', 'payer_resolved_patient_summary', 'payer_resolved_impact', 'FLM', '_all', '_av_th', 'read_excel', 'get_pcornet_table', 'materialize', 'source.*00_config'); $missing = @(); foreach ($c in $checks) { if ($content -notmatch $c) { $missing += $c } }; if ($missing.Count -eq 0) { 'ALL CHECKS PASS' } else { 'MISSING: ' + ($missing -join ', ') } } else { 'FILE NOT FOUND' }"</automated>
  </verify>
  <acceptance_criteria>
    - R/36_tiered_same_day_payer.R exists and is >400 lines
    - File contains `TIER_MAPPING <- list(` with ranks 1L through 7L for Medicaid, Medicare, Private, Other, Self-pay, Uninsured, Missing
    - File contains `CODE_TO_TIER` function mapping 9 categories to 6 tiers (Dual eligible -> Medicaid, Other government -> Other, No payment / Self-pay -> Self-pay, Unavailable -> Missing, Unknown -> Missing)
    - File contains compute_effective_payer logic (primary if valid, else secondary, else NA) using PAYER_MAPPING$sentinel_values
    - File contains map_payer_category logic matching R/02_harmonize_payer.R (exact-match overrides before prefix rules, dual-eligible override)
    - File contains FLM source override: `any(SOURCE == "FLM"` inside group_by(ID, admit_date_parsed) summarise (per D-04 -- ENCOUNTER.SOURCE, not DEMOGRAPHIC.SOURCE)
    - File contains special code override: `%in% c("93", "14")` for both PRIMARY and SECONDARY fields (per D-03)
    - File contains `build_frequency_tables` function (or inline equivalent) called with suffix "_all" and suffix "_av_th_v2"
    - File contains `resolve_same_day_payer` function (or inline equivalent) called with suffix "_all" and suffix "_av_th"
    - File writes 6 frequency CSVs: payer_primary_code_freq_all.csv, payer_secondary_code_freq_all.csv, payer_category_summary_all.csv, payer_primary_code_freq_av_th_v2.csv, payer_secondary_code_freq_av_th_v2.csv, payer_category_summary_av_th_v2.csv
    - File writes 6 resolution CSVs: payer_resolved_detail_all.csv, payer_resolved_detail_av_th.csv, payer_resolved_patient_summary_all.csv, payer_resolved_patient_summary_av_th.csv, payer_resolved_impact_all.csv, payer_resolved_impact_av_th.csv
    - File contains `resolution_reason` column logic with values: "single encounter", "FLM source override", "special code override (93/14)", "all encounters same tier", "tier hierarchy (N tiers)"
    - File contains before vs after impact comparison (n_encounters_before vs n_patient_dates_after)
    - File does NOT contain `source("R/02_harmonize_payer.R")` (per D-10)
    - File does NOT modify R/35_payer_code_frequency_av_th.R (per D-06)
    - File contains `source("R/00_config.R")` as first source line (per D-09)
    - File contains DuckDB connection guard: `if (USE_DUCKDB && !exists("pcornet_con"` pattern
    - File contains `filter(ENC_TYPE %in% c("AV", "TH"))` for AV+TH scope
    - Console summary section exists with encounter counts, resolution breakdown, and CSV file list
  </acceptance_criteria>
  <done>
    R/36_tiered_same_day_payer.R is a complete, standalone diagnostic script that:
    (1) Loads ENCOUNTER table via DuckDB materialize-early pattern
    (2) Produces dual-scope (all + AV+TH) payer code frequency tables with PayerVariable.xlsx cross-reference (6 CSVs)
    (3) Resolves same-day multi-encounter payer conflicts using configurable Medicaid>Medicare>Private>Other>Self-pay>Uninsured>Missing hierarchy with FLM source override and codes 93/14 override (6 CSVs)
    (4) Shows before vs after resolution impact
    (5) Prints comprehensive console summary
    Total: 12 CSV outputs, no modification to existing pipeline scripts
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify script structure, fix edge cases, and validate output file naming</name>
  <files>R/36_tiered_same_day_payer.R</files>
  <read_first>
    R/36_tiered_same_day_payer.R
    R/35_payer_code_frequency_av_th.R
    payer_framework.txt
  </read_first>
  <action>
Review the complete R/36_tiered_same_day_payer.R created in Task 1 and verify/fix the following:

1. **Tier mapping editability (D-01 check):** Confirm TIER_MAPPING is a named list at the TOP of the script (within the first 50 lines after source/library calls), NOT buried inside a function. Confirm changing a tier rank (e.g., swapping Medicare and Private priorities) requires editing only one number in TIER_MAPPING.

2. **FLM override granularity (D-04 check):** Verify the FLM override checks `SOURCE == "FLM"` INSIDE `group_by(ID, admit_date_parsed) %>% summarise(...)`. The check must be `any(SOURCE == "FLM")` per patient-date group, NOT per patient. Confirm the column used is `SOURCE` (from ENCOUNTER table), not a DEMOGRAPHIC join.

3. **Edge case: tier_rank NA propagation:** If a tier value is not found in TIER_MAPPING (e.g., an unexpected payer_category produces an unrecognized tier), `TIER_MAPPING[tier]` returns NULL which causes `which.min()` to fail. Add a safety net:
   - After computing tier, add: `tier = if_else(is.na(tier), "Missing", tier)`
   - After computing tier_rank, add: `tier_rank = if_else(is.na(tier_rank), 7L, tier_rank)`

4. **Edge case: admit_date_parsed all NA:** If ADMIT_DATE parsing fails for all encounters (unlikely but possible), the resolution section should log a warning and skip rather than error. Add: `if (sum(!is.na(enc_scope$admit_date_parsed)) == 0) { message("WARNING: No valid ADMIT_DATE values -- skipping resolution"); return(invisible(NULL)) }` at the top of the resolution function.

5. **Edge case: single-encounter dates (Claude's discretion):** Verify that patient-dates with only 1 encounter are INCLUDED in CSV A with `resolution_reason = "single encounter"` and `resolved_payer = tier` (the encounter's own tier). This gives PIs the full patient-date timeline.

6. **Output file naming collision check:** Verify that AV+TH frequency CSVs use suffix `_av_th_v2` (NOT `_av_th` which would overwrite Phase 34 baseline files). Verify that resolution CSVs use `_av_th` suffix (no collision since Phase 34 did not produce resolution files). Specifically check:
   - `payer_primary_code_freq_av_th_v2.csv` (NOT `payer_primary_code_freq_av_th.csv` -- that's Phase 34)
   - `payer_secondary_code_freq_av_th_v2.csv` (NOT `payer_secondary_code_freq_av_th.csv` -- that's Phase 34)
   - `payer_category_summary_av_th_v2.csv` (NOT `payer_category_summary_av_th.csv` -- that's Phase 34)
   - `payer_resolved_detail_av_th.csv` (OK -- no Phase 34 collision)
   - `payer_resolved_patient_summary_av_th.csv` (OK)
   - `payer_resolved_impact_av_th.csv` (OK)

7. **Console summary completeness:** Verify the console summary includes:
   - Banner header: "TIERED SAME-DAY PAYER CATEGORIZATION"
   - Per scope: total encounters, total patients, frequency table highlights
   - Per scope: total patient-dates, single vs multi-encounter breakdown, resolution reason counts
   - Per scope: before vs after impact table (tier name, n_before, pct_before, n_after, pct_after)
   - List of all 12 CSV files written
   - Banner footer

8. **Script header comment block:** Verify the script has a comment header matching Phase 33/34 pattern:
   - Script name and purpose
   - Phase number and requirements (TIER-01 through TIER-10)
   - Output file list (12 CSVs)
   - Usage: `source("R/36_tiered_same_day_payer.R")`
   - Dependencies listed
   - "Standalone script -- NOT part of the main pipeline sequence."
  </action>
  <verify>
    <automated>powershell -Command "$content = Get-Content 'C:\Users\Owner\Documents\insurance_investigation\R\36_tiered_same_day_payer.R' -Raw; $lines = ($content -split '\n').Count; $checks = @(); if ($lines -lt 400) { $checks += 'UNDER_400_LINES' }; if ($content -notmatch 'if_else\(is\.na\(tier\)') { $checks += 'MISSING_TIER_NA_GUARD' }; if ($content -notmatch 'single encounter') { $checks += 'MISSING_SINGLE_ENC_REASON' }; if ($content -notmatch '_av_th_v2') { $checks += 'MISSING_V2_SUFFIX' }; if ($content -notmatch 'TIERED SAME-DAY PAYER') { $checks += 'MISSING_BANNER' }; if ($content -notmatch 'Standalone script') { $checks += 'MISSING_STANDALONE_NOTE' }; if ($checks.Count -eq 0) { 'ALL CHECKS PASS (' + $lines + ' lines)' } else { 'ISSUES: ' + ($checks -join ', ') }"</automated>
  </verify>
  <acceptance_criteria>
    - R/36_tiered_same_day_payer.R is >400 lines
    - TIER_MAPPING appears within the first 60 lines after `source("R/00_config.R")`
    - `if_else(is.na(tier)` guard present (tier NA safety net)
    - `if_else(is.na(tier_rank)` guard present (tier_rank NA safety net)
    - `resolution_reason` includes "single encounter" case for n()==1 dates
    - All AV+TH frequency CSV filenames contain `_av_th_v2` suffix (not `_av_th` alone)
    - Console summary contains "TIERED SAME-DAY PAYER" banner text
    - Script header contains "Standalone script -- NOT part of the main pipeline sequence."
    - Script header contains "Phase 35" reference
    - No occurrence of `source("R/02_harmonize_payer.R")` in the file
    - No occurrence of writing to filenames `payer_primary_code_freq_av_th.csv` (without _v2) or `payer_secondary_code_freq_av_th.csv` (without _v2) or `payer_category_summary_av_th.csv` (without _v2)
  </acceptance_criteria>
  <done>
    R/36_tiered_same_day_payer.R passes all edge case checks: tier NA guards present, single-encounter dates included in resolution output, AV+TH frequency CSVs use _v2 suffix to preserve Phase 34 baseline, FLM override operates at patient-date level (not patient level), console summary is complete with banners and full CSV listing.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Verify R/36_tiered_same_day_payer.R exists and is syntactically valid R (no unclosed brackets, no syntax errors visible in structure)
2. Verify TIER_MAPPING is configurable at script top with 7 tiers and integer ranks
3. Verify 12 unique CSV output filenames are referenced in write_csv() calls
4. Verify FLM override is inside group_by(ID, admit_date_parsed) and uses SOURCE (not DEMOGRAPHIC.SOURCE)
5. Verify no modification to R/35_payer_code_frequency_av_th.R, R/02_harmonize_payer.R, or R/00_config.R
6. Verify AV+TH frequency outputs use _v2 suffix to preserve Phase 34 baselines
</verification>

<success_criteria>
- R/36_tiered_same_day_payer.R is a complete standalone script (>400 lines) that can be run via `source("R/36_tiered_same_day_payer.R")` on HiPerGator
- The script produces 12 CSV files: 6 frequency tables (3 per scope) + 6 resolution outputs (3 per scope)
- Tier mapping is a configurable named list at the script top, editable by PIs without touching resolution logic
- Amy Crisp's hierarchy (Medicaid>Medicare>Private>Other>Self-pay>Uninsured>Missing) is implemented with FLM source override and codes 93/14 override
- No existing pipeline scripts are modified
- Phase 34 baseline CSVs are preserved (AV+TH frequency files use _v2 suffix)
</success_criteria>

<output>
After completion, create `.planning/phases/35-tiered-same-day-payer-categorization-raw-payer-frequency-table-all-encounters-and-hierarchical-same-day-resolution-medicaid-medicare-private-other-self-pay-uninsured-missing-per-amy-crisp-framework/35-01-SUMMARY.md`
</output>
