---
phase: 23-make-visual-presentation-of-tables-from-last-2-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - R/11_generate_pptx.R
autonomous: false
requirements:
  - PPTX3-05
  - PPTX3-06
  - PPTX3-07
user_setup: []

must_haves:
  truths:
    - "User can see Phase 22 duplicate date investigation tables as formatted slides in the PPTX"
    - "User can see duplicate date rate bar chart embedded as a slide"
    - "User can see detail-level CSVs summarized into presentation-friendly aggregates (not raw 9332/262K rows)"
    - "User can see updated slide count in the PPTX save message reflecting all new Phase 21/22 slides"
    - "User can visually verify all new slides render correctly on HiPerGator"
  artifacts:
    - path: "R/11_generate_pptx.R"
      provides: "Section 9: Phase 22 Duplication Slides + updated Section 10 SAVE"
      contains: "SECTION 9"
    - path: "insurance_tables_YYYY-MM-DD.pptx"
      provides: "Final PPTX with 50+ slides including Phase 21/22 diagnostics"
  key_links:
    - from: "R/11_generate_pptx.R Section 9"
      to: "output/tables/all_site_*.csv"
      via: "read_csv() loading Phase 22 CSV outputs"
      pattern: "read_csv.*all_site"
    - from: "R/11_generate_pptx.R Section 9"
      to: "output/figures/phase22_duplication_by_site.png"
      via: "add_image_slide() embedding duplication bar chart"
      pattern: "add_image_slide.*duplication"
---

<objective>
Add Phase 22 duplicate date investigation slides (tables + chart) to the PPTX as Section 9, update the save section to Section 10 with dynamic slide count, and verify all slides render correctly on HiPerGator.

Purpose: Completes the PPTX extension by adding the remaining 5 Phase 22 CSVs as formatted slides with proper summarization of detail-level data, then validates the full presentation.

Output: Modified R/11_generate_pptx.R with Section 9 (Phase 22 Duplication) containing ~6-8 slides, updated Section 10 (SAVE), and a human-verified PPTX output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-make-visual-presentation-of-tables-from-last-2-pages/23-CONTEXT.md
@.planning/phases/23-make-visual-presentation-of-tables-from-last-2-pages/23-RESEARCH.md
@.planning/phases/23-make-visual-presentation-of-tables-from-last-2-pages/23-01-SUMMARY.md

@R/11_generate_pptx.R

<interfaces>
<!-- Reuse same helpers from Plan 01 -- already defined in R/11_generate_pptx.R -->

Helpers: add_table_slide(), add_image_slide(), add_footnote(), style_table()
Color constants: UF_BLUE="#003087", UF_ORANGE="#FA4616", DARK_TEXT="#333333"

Phase 22 CSV column schemas (from R/21_all_site_duplicate_dates.R):

1. all_site_cross_site_summary.csv:
   SITE, n_patients, n_encounters, n_unique_dates, n_dupe_patient_dates,
   pct_duplicate_rate, n_multi_source_dates, pct_multi_source_of_dupes,
   n_exact_row_dupes, n_near_exact_dupes, recommended_source, recommended_source_completeness_pct

2. all_site_patient_duplicate_summary.csv (9332 rows -- NEEDS AGGREGATION per D-03):
   SITE, ID, n_unique_dates, n_total_encounters, n_duplicate_dates,
   n_multi_source_dates, sources_in_encounters, pct_primary_present, pct_secondary_present

3. all_site_date_level_duplicate_detail.csv (262K rows -- NEEDS AGGREGATION per D-03):
   SITE, ID, admit_date_parsed, ENCOUNTER_SOURCE, ENC_TYPE, ENCOUNTERID,
   ADMIT_TIME, DISCHARGE_DATE, DISCHARGE_TIME, PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY,
   primary_missing, secondary_missing

4. all_site_duplicate_aggregate_summary.csv:
   SITE, metric, value (long format -- ~11 metrics per site + source completeness rows)

5. all_site_source_payer_completeness.csv:
   SITE, ENCOUNTER_SOURCE, n_encounters, n_primary_present, pct_primary_present,
   n_secondary_present, pct_secondary_present, n_both_present, pct_both_present,
   n_either_present, pct_either_present

Variable from Plan 01 still in scope:
- p22_cross_site: already loaded from all_site_cross_site_summary.csv in Section 7
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 22 duplication slides and update SAVE section</name>
  <files>R/11_generate_pptx.R</files>
  <read_first>
    - R/11_generate_pptx.R (full file -- understand Section 7/8 added by Plan 01, find insertion point after Section 8, and the current SECTION 6: SAVE block)
    - R/21_all_site_duplicate_dates.R (CSV column schemas from write_csv calls at lines 449, 483, 564, 574, 661; understand aggregate_summary long format at lines 517-561)
  </read_first>
  <action>
Insert a new Section 9 into R/11_generate_pptx.R AFTER Section 8 (Phase 21 Missingness Slides added by Plan 01) and BEFORE the current `SECTION 6: SAVE PPTX` block. Then renumber the SAVE section to Section 10 and update its slide count to be dynamic.

**Step A: Add Section 9 -- Phase 22 Duplication Slides.**

Insert this code block after the last Section 8 slide (after the temporal missingness slide's add_footnote call) and before the SAVE section:

```r
# ==============================================================================
# SECTION 9: PHASE 22 DUPLICATION SLIDES (D-01, D-03, D-04, D-06)
# ==============================================================================

message("\n--- Adding Phase 22 Duplication Slides ---")

# ---- Slide: Cross-Site Duplicate Date Summary (all_site_cross_site_summary.csv) ----
message("  Adding slide: Cross-Site Duplicate Date Summary")

# p22_cross_site was already loaded in Section 7 (chart generation)
p22_cross_display <- p22_cross_site %>%
  mutate(
    SITE = if_else(SITE == "ALL", "ALL (Aggregate)", SITE),
    pct_duplicate_rate = paste0(pct_duplicate_rate, "%"),
    pct_multi_source_of_dupes = paste0(pct_multi_source_of_dupes, "%"),
    recommended_source_completeness_pct = if_else(
      is.na(recommended_source_completeness_pct), "--",
      paste0(recommended_source_completeness_pct, "%")),
    recommended_source = if_else(is.na(recommended_source), "--", recommended_source),
    n_patients = format(n_patients, big.mark = ","),
    n_encounters = format(n_encounters, big.mark = ","),
    n_unique_dates = format(n_unique_dates, big.mark = ","),
    n_dupe_patient_dates = format(n_dupe_patient_dates, big.mark = ",")
  ) %>%
  select(
    `Site` = SITE,
    `Patients` = n_patients,
    `Encounters` = n_encounters,
    `Unique Dates` = n_unique_dates,
    `Dup Dates` = n_dupe_patient_dates,
    `% Dup Rate` = pct_duplicate_rate,
    `Rec. Source` = recommended_source
  )

pptx <- add_table_slide(pptx,
  "Duplicate Dates: Cross-Site Comparison",
  "Same-date duplicate encounters by partner site with recommended source for payer data",
  p22_cross_display) %>%
  add_footnote("Duplicate = >1 encounter on same ADMIT_DATE for same patient. Rec. Source = ENCOUNTER.SOURCE with highest primary payer completeness for multi-source duplicates.")

# ---- Slide: Bar chart -- Duplicate Date Rate by Site (D-05) ----
message("  Adding slide: Duplicate Date Rate by Site (bar chart)")

pptx <- add_image_slide(pptx,
  "Duplicate Date Rate by Partner Site",
  "Percentage of patient-dates with duplicate encounters, sorted descending",
  "output/figures/phase22_duplication_by_site.png",
  img_width = 9, img_height = 5.0) %>%
  add_footnote("Duplicate date = patient has >1 encounter row on the same ADMIT_DATE. Excludes ALL aggregate row.")

# ---- Slide: Aggregate Summary per Site (all_site_duplicate_aggregate_summary.csv) ----
message("  Adding slide: Per-Site Aggregate Duplicate Metrics")

p22_aggregate <- read_csv("output/tables/all_site_duplicate_aggregate_summary.csv",
                           show_col_types = FALSE)

# Pivot from long to wide: rows=metrics, cols=sites
# Filter to core metrics only (exclude per-source completeness rows)
core_metrics <- c(
  "Total patients (DEMOGRAPHIC)",
  "Total encounters",
  "Encounters with valid ADMIT_DATE",
  "Unique patient-dates",
  "Patient-dates with same-date duplicates",
  "Patient-dates with multiple SOURCEs",
  "Exact row duplicates",
  "Near-exact duplicates"
)

agg_wide <- p22_aggregate %>%
  filter(metric %in% core_metrics) %>%
  mutate(
    value = format(as.numeric(value), big.mark = ",", trim = TRUE),
    metric = factor(metric, levels = core_metrics)
  ) %>%
  arrange(metric) %>%
  pivot_wider(names_from = SITE, values_from = value) %>%
  rename(`Metric` = metric)

# If table is too wide (>7 sites), split across 2 slides
n_site_cols <- ncol(agg_wide) - 1  # minus Metric column
if (n_site_cols > 7) {
  site_names <- names(agg_wide)[-1]
  half <- ceiling(n_site_cols / 2)
  chunk1_cols <- c("Metric", site_names[1:half])
  chunk2_cols <- c("Metric", site_names[(half+1):n_site_cols])

  pptx <- add_table_slide(pptx,
    "Per-Site Duplicate Metrics (1/2)",
    glue("Core duplication statistics -- Sites: {paste(site_names[1:half], collapse=', ')}"),
    agg_wide[, chunk1_cols]) %>%
    add_footnote("Same-date = >1 encounter on same ADMIT_DATE. Multi-source = encounters from different ENCOUNTER.SOURCE values on same date.")

  pptx <- add_table_slide(pptx,
    "Per-Site Duplicate Metrics (2/2)",
    glue("Core duplication statistics -- Sites: {paste(site_names[(half+1):n_site_cols], collapse=', ')}"),
    agg_wide[, chunk2_cols]) %>%
    add_footnote("Same-date = >1 encounter on same ADMIT_DATE. Multi-source = encounters from different ENCOUNTER.SOURCE values on same date.")
} else {
  pptx <- add_table_slide(pptx,
    "Per-Site Duplicate Metrics",
    "Core duplication statistics by partner site",
    agg_wide) %>%
    add_footnote("Same-date = >1 encounter on same ADMIT_DATE. Multi-source = encounters from different ENCOUNTER.SOURCE values on same date.")
}

# ---- Slide: Source Payer Completeness (all_site_source_payer_completeness.csv) ----
message("  Adding slide: Source Payer Completeness for Multi-Source Duplicates")

p22_source_comp <- read_csv("output/tables/all_site_source_payer_completeness.csv",
                             show_col_types = FALSE)

if (nrow(p22_source_comp) > 0) {
  source_comp_display <- p22_source_comp %>%
    mutate(
      pct_primary_present = paste0(pct_primary_present, "%"),
      pct_secondary_present = paste0(pct_secondary_present, "%"),
      n_encounters = format(n_encounters, big.mark = ",")
    ) %>%
    select(
      `Site` = SITE,
      `ENC Source` = ENCOUNTER_SOURCE,
      `Encounters` = n_encounters,
      `% Primary Present` = pct_primary_present,
      `% Secondary Present` = pct_secondary_present
    )

  # Split by site chunks if many sites
  comp_sites <- sort(unique(source_comp_display$Site))
  comp_chunks <- split(comp_sites, ceiling(seq_along(comp_sites) / 5))

  for (i in seq_along(comp_chunks)) {
    chunk <- source_comp_display %>% filter(Site %in% comp_chunks[[i]])
    slide_title <- if (length(comp_chunks) > 1) {
      glue("Source Payer Completeness ({i}/{length(comp_chunks)})")
    } else {
      "Source Payer Completeness for Multi-Source Dates"
    }

    pptx <- add_table_slide(pptx,
      slide_title,
      "Payer data completeness by ENCOUNTER.SOURCE for multi-source duplicate dates",
      chunk) %>%
      add_footnote("Shows only encounters on dates where >1 ENCOUNTER.SOURCE contributed. Higher % = more payer data available from that source.")
  }
} else {
  message("  SKIPPED: Source payer completeness -- no multi-source encounters found.")
}

# ---- Slide: Patient Duplicate Summary -- aggregated per D-03 ----
message("  Adding slide: Patient Duplicate Summary (aggregated)")

p22_patient_summary <- read_csv("output/tables/all_site_patient_duplicate_summary.csv",
                                 show_col_types = FALSE)

# D-03: Summarize 9332 patient rows into per-site aggregates
patient_agg <- p22_patient_summary %>%
  group_by(SITE) %>%
  summarise(
    n_patients = n(),
    n_with_any_dupes = sum(n_duplicate_dates > 0),
    pct_with_dupes = round(100 * n_with_any_dupes / n_patients, 1),
    n_with_multi_source = sum(n_multi_source_dates > 0),
    pct_with_multi_source = round(100 * n_with_multi_source / n_patients, 1),
    mean_pct_primary_present = round(mean(pct_primary_present, na.rm = TRUE), 1),
    median_dup_dates = median(n_duplicate_dates),
    max_dup_dates = max(n_duplicate_dates),
    .groups = "drop"
  ) %>%
  mutate(
    pct_with_dupes = paste0(pct_with_dupes, "%"),
    pct_with_multi_source = paste0(pct_with_multi_source, "%"),
    mean_pct_primary_present = paste0(mean_pct_primary_present, "%"),
    n_patients = format(n_patients, big.mark = ","),
    n_with_any_dupes = format(n_with_any_dupes, big.mark = ","),
    n_with_multi_source = format(n_with_multi_source, big.mark = ",")
  ) %>%
  select(
    `Site` = SITE,
    `Patients` = n_patients,
    `With Dupes` = n_with_any_dupes,
    `% Duped` = pct_with_dupes,
    `Multi-Src` = n_with_multi_source,
    `% Multi-Src` = pct_with_multi_source,
    `Med Dup Dates` = median_dup_dates,
    `Max Dup Dates` = max_dup_dates,
    `Mean % Prim Present` = mean_pct_primary_present
  )

pptx <- add_table_slide(pptx,
  "Patient Duplicate Summary by Site",
  "Per-site aggregation of patient-level duplicate encounter statistics",
  patient_agg) %>%
  add_footnote("Aggregated from 9,332 patient rows. Dupes = patient-dates with >1 encounter. Multi-Src = dates with encounters from different ENCOUNTER.SOURCE values. Full patient detail in all_site_patient_duplicate_summary.csv.")

# ---- Slide: Date-Level Detail -- aggregated per D-03 ----
message("  Adding slide: Date-Level Duplicate Detail (aggregated)")

p22_date_detail <- read_csv("output/tables/all_site_date_level_duplicate_detail.csv",
                             show_col_types = FALSE)

if (nrow(p22_date_detail) > 0) {
  # D-03: Summarize 262K rows into per-site source breakdown
  date_detail_agg <- p22_date_detail %>%
    group_by(SITE, ENCOUNTER_SOURCE) %>%
    summarise(
      n_encounters = n(),
      n_primary_missing = sum(primary_missing, na.rm = TRUE),
      pct_primary_missing = round(100 * n_primary_missing / n_encounters, 1),
      .groups = "drop"
    ) %>%
    mutate(
      pct_primary_missing = paste0(pct_primary_missing, "%"),
      n_encounters = format(n_encounters, big.mark = ","),
      n_primary_missing = format(n_primary_missing, big.mark = ",")
    ) %>%
    select(
      `Site` = SITE,
      `ENC Source` = ENCOUNTER_SOURCE,
      `Encounters` = n_encounters,
      `Primary Missing` = n_primary_missing,
      `% Missing` = pct_primary_missing
    )

  pptx <- add_table_slide(pptx,
    "Multi-Source Date Detail: Payer Missingness by Source",
    "Payer data quality for encounters on multi-source duplicate dates, aggregated by site and ENCOUNTER.SOURCE",
    date_detail_agg) %>%
    add_footnote("Aggregated from 262K encounter rows on multi-source dates. Shows which ENCOUNTER.SOURCE values have better payer data. Full detail in all_site_date_level_duplicate_detail.csv.")
} else {
  message("  SKIPPED: Date-level detail -- no multi-source encounters found.")
}
```

**Step B: Renumber and update the SAVE section.**

Find the existing `SECTION 6: SAVE PPTX` block (currently the last section) and update it:

1. Change the section header comment from `SECTION 6` to `SECTION 10`
2. Replace the hard-coded slide count message. Change:
   ```r
   message(glue("  Slides: 38 (30 original + 8 treated-only unique dates)"))
   ```
   To:
   ```r
   n_slides <- length(pptx)
   message(glue("  Slides: {n_slides} (38 original + {n_slides - 38} Phase 21/22 diagnostics)"))
   ```
3. Keep the rest of the SAVE section unchanged (output_filename, print, cohort message, date message).

**Step C: Update the file header comment block (lines 1-48).**

Add new slide descriptions to the slide index comment at the top of the file, after line 47 (`#  38. Summary Statistics: Pre/Post Unique Dates by Payer (Treated Only) [table]`):

Add these lines:
```r
#  --- Phase 21: Payer Missingness Slides (Section 8) ---
#  39. Payer Missingness: Cross-Site Comparison [table]
#  40. Primary Payer Missingness by Site [bar chart]
#  41. Raw PAYER_TYPE_PRIMARY: Top 5 Values per Site [table]
#  42-43. Payer Missingness by Encounter Type (split by site) [tables]
#  44. Payer Missingness by Encounter Type and Site [grouped bar chart]
#  45. Raw vs Harmonized Payer Missingness by Site [table]
#  46. Highest Missingness: Year x Enc Type Combinations [table]
#  47. Payer Missingness by Year (Recent 5 Years per Site) [table]
#  --- Phase 22: Duplicate Date Slides (Section 9) ---
#  48. Duplicate Dates: Cross-Site Comparison [table]
#  49. Duplicate Date Rate by Site [bar chart]
#  50-51. Per-Site Duplicate Metrics [table, split if >7 sites]
#  52. Source Payer Completeness for Multi-Source Dates [table]
#  53. Patient Duplicate Summary by Site [aggregated table]
#  54. Multi-Source Date Detail: Payer Missingness by Source [aggregated table]
```

Note: Slide numbers 39-54 are approximate. The actual count depends on how many split slides are created.
  </action>
  <verify>
    <automated>grep -c "SECTION 9\|SECTION 10\|phase22_duplication_by_site\|all_site_duplicate_aggregate\|all_site_source_payer_completeness\|all_site_patient_duplicate\|all_site_date_level\|n_slides.*length" R/11_generate_pptx.R</automated>
  </verify>
  <acceptance_criteria>
    - R/11_generate_pptx.R contains string "SECTION 9: PHASE 22 DUPLICATION SLIDES"
    - R/11_generate_pptx.R contains string "SECTION 10: SAVE PPTX"
    - R/11_generate_pptx.R contains `n_slides <- length(pptx)` for dynamic slide count
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_site_duplicate_aggregate_summary.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_site_source_payer_completeness.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_site_patient_duplicate_summary.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_site_date_level_duplicate_detail.csv"`
    - R/11_generate_pptx.R contains `add_image_slide(pptx` with `phase22_duplication_by_site.png`
    - R/11_generate_pptx.R contains `group_by(SITE)` in patient_agg summarization (D-03 aggregation)
    - R/11_generate_pptx.R contains `pivot_wider(names_from = SITE` for aggregate summary wide format
    - R/11_generate_pptx.R does NOT contain the old string "Slides: 38 (30 original"
    - File header comment block includes "Phase 21: Payer Missingness" and "Phase 22: Duplicate Date"
    - File parses without R syntax errors: `R -e "parse('R/11_generate_pptx.R')"` exits 0
  </acceptance_criteria>
  <done>
    R/11_generate_pptx.R has Section 9 (Phase 22 Duplication, ~6-8 slides) and updated Section 10 (SAVE with dynamic slide count). All 5 Phase 22 CSVs are represented as slides. Detail CSVs (9332 and 262K rows) are properly aggregated per D-03. File header comment lists all new slides.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PPTX renders correctly on HiPerGator</name>
  <files>R/11_generate_pptx.R</files>
  <action>
    User runs the full pipeline on HiPerGator and visually inspects the generated PPTX to confirm all new Phase 21/22 slides render correctly with proper formatting, chart colors, table splits, and aggregated detail summaries. No automated action needed -- this is a human visual verification checkpoint.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification of PPTX output on HiPerGator"</automated>
  </verify>
  <done>
    User has confirmed: (1) all ~16 new slides render with proper UF blue/orange styling, (2) bar charts display readable labels with correct percentages, (3) wide tables are split across slides without overflow, (4) detail CSVs show aggregated summaries not raw rows, (5) no regressions in existing Slides 1-38, (6) dynamic slide count in console output is correct.
  </done>
</task>

</tasks>

<verification>
After Task 1 + Task 2 completion:
- `grep -c "SECTION 7\|SECTION 8\|SECTION 9\|SECTION 10" R/11_generate_pptx.R` returns 4 (one per section)
- `grep -c "read_csv.*all_source_\|read_csv.*all_site_" R/11_generate_pptx.R` returns >= 11 (all 11 CSVs loaded)
- `grep -c "ggsave" R/11_generate_pptx.R` returns 3 (the 3 bar chart PNGs)
- `grep -c "add_table_slide\|add_image_slide" R/11_generate_pptx.R` count increases by 14+ from baseline
- `grep "n_slides <- length" R/11_generate_pptx.R` matches (dynamic slide count)
- `R -e "parse('R/11_generate_pptx.R')"` exits 0 (no syntax errors)
- PPTX file generated on HiPerGator opens in PowerPoint with 50+ slides
- User approves visual quality of all new slides
</verification>

<success_criteria>
1. R/11_generate_pptx.R has Sections 7, 8, 9, 10 (chart gen, Phase 21 slides, Phase 22 slides, save)
2. All 5 Phase 22 CSVs have corresponding slides with proper aggregation for detail-level data (D-03)
3. Duplicate date rate bar chart is embedded as a slide (D-05)
4. Aggregate summary is pivoted from long to wide format for readability
5. Dynamic slide count in save message (D-04 pattern from Pitfall 4)
6. User visually confirms all new slides render correctly on HiPerGator
7. No regressions in existing Slides 1-38
</success_criteria>

<output>
After completion, create `.planning/phases/23-make-visual-presentation-of-tables-from-last-2-pages/23-02-SUMMARY.md`
</output>
