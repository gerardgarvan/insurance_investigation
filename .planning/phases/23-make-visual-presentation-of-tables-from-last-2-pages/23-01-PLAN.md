---
phase: 23-make-visual-presentation-of-tables-from-last-2-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/11_generate_pptx.R
autonomous: true
requirements:
  - PPTX3-01
  - PPTX3-02
  - PPTX3-03
  - PPTX3-04
user_setup: []

must_haves:
  truths:
    - "User can see Phase 21 payer missingness tables as formatted slides in the PPTX"
    - "User can see 3 bar chart PNGs in output/figures/ visualizing missingness and duplication rates"
    - "User can see wide tables split across multiple slides for readability"
    - "User can see HIPAA small-cell suppression applied to site-level count columns"
  artifacts:
    - path: "R/11_generate_pptx.R"
      provides: "Section 7: Phase 21 Missingness Slides + bar chart PNG generation"
      contains: "SECTION 7"
    - path: "output/figures/phase21_missingness_by_site.png"
      provides: "Bar chart: primary payer missingness % by site"
    - path: "output/figures/phase22_duplication_by_site.png"
      provides: "Bar chart: duplicate date rate % by site"
    - path: "output/figures/phase21_missingness_by_enc_type.png"
      provides: "Grouped bar chart: missingness by encounter type across sites"
  key_links:
    - from: "R/11_generate_pptx.R Section 7"
      to: "output/tables/all_source_*.csv"
      via: "read_csv() loading Phase 21 CSV outputs"
      pattern: "read_csv.*all_source"
    - from: "R/11_generate_pptx.R Section 7"
      to: "output/figures/phase21_*.png"
      via: "ggsave() generating bar chart PNGs then add_image_slide() embedding"
      pattern: "ggsave.*phase21"
---

<objective>
Generate 3 bar chart PNGs from Phase 21/22 CSV data and add Phase 21 payer missingness slides (tables + charts) to the existing PPTX presentation as a new Section 7.

Purpose: Converts Phase 21's 6 CSV diagnostic outputs into presentation-ready slides with both formatted tables and bar chart visualizations, enabling stakeholder review of cross-site payer missingness patterns.

Output: Modified R/11_generate_pptx.R with Section 7 (Phase 21 Missingness) containing ~8-10 slides, plus 3 bar chart PNG files in output/figures/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-make-visual-presentation-of-tables-from-last-2-pages/23-CONTEXT.md
@.planning/phases/23-make-visual-presentation-of-tables-from-last-2-pages/23-RESEARCH.md

@R/11_generate_pptx.R
@R/20_all_source_missingness.R
@R/21_all_site_duplicate_dates.R

<interfaces>
<!-- Key helpers from R/11_generate_pptx.R that the executor MUST reuse -->

From R/11_generate_pptx.R (lines 107-111) -- Color constants:
```r
UF_BLUE    <- "#003087"
UF_ORANGE  <- "#FA4616"
LIGHT_BLUE <- "#CCD5EA"
LIGHT_ORANGE <- "#FDD9CC"
DARK_TEXT  <- "#333333"
```

From R/11_generate_pptx.R (lines 722-757) -- Table slide helper:
```r
add_table_slide <- function(pptx, title, subtitle, tbl_data) {
  total_row_idx <- which(tbl_data[[1]] == "Total")
  ft <- flextable(tbl_data) %>% style_table(total_row = total_row_idx)
  # Sets first col = 2.2", rest evenly distributed across 9.0" total
  # Uses UF_BLUE title (26pt), DARK_TEXT subtitle (14pt italic)
  # Table at left=0.5, top=1.4, width=9, height=5.0
}
```

From R/11_generate_pptx.R (lines 760-783) -- Image slide helper:
```r
add_image_slide <- function(pptx, title, subtitle, img_path,
                             img_width = 8.5, img_height = 5.0) {
  # Guards with file.exists(), skips if PNG missing
  # Centers image horizontally: left = (10 - img_width) / 2
}
```

From R/11_generate_pptx.R (lines 790-796) -- Footnote helper:
```r
add_footnote <- function(pptx, text) {
  # 10pt italic gray at bottom: left=0.5, top=6.9, width=9, height=0.5
}
```

From R/11_generate_pptx.R (lines 666-700) -- Table styling:
```r
style_table <- function(ft, total_row = integer(0)) {
  # Body: 12pt Calibri, Header: 13pt bold white on UF_BLUE
  # Alternating rows: LIGHT_BLUE (odd) / LIGHT_ORANGE (even)
  # Total row: bold white on UF_BLUE
  # Returns autofit()
}
```

Phase 21 CSV column schemas (from R/20_all_source_missingness.R):

1. all_source_cross_site_summary.csv:
   SOURCE, n_patients, n_encounters, n_primary_missing, pct_primary_missing,
   n_secondary_missing, pct_secondary_missing, n_both_missing, pct_both_missing

2. all_source_payer_raw_value_distribution.csv:
   SOURCE, field, value, n, pct

3. all_source_payer_missingness_by_year.csv:
   SOURCE, admit_year, n_encounters, n_primary_missing, n_secondary_missing,
   n_both_missing, pct_primary_missing, pct_secondary_missing, pct_both_missing

4. all_source_payer_missingness_by_enc_type.csv:
   SOURCE, ENC_TYPE_LABEL, n_encounters, n_primary_missing, n_secondary_missing,
   n_both_missing, pct_primary_missing, pct_secondary_missing, pct_both_missing

5. all_source_payer_missingness_year_x_enc_type.csv:
   SOURCE, admit_year, ENC_TYPE_LABEL, n_encounters, n_primary_missing, pct_primary_missing

6. all_source_payer_raw_vs_harmonized.csv:
   SOURCE, year, n_encounters, n_raw_primary_missing, pct_raw_primary, n_harmonized_missing, pct_harmonized

Phase 22 CSV column schemas (from R/21_all_site_duplicate_dates.R):

7. all_site_cross_site_summary.csv:
   SITE, n_patients, n_encounters, n_unique_dates, n_dupe_patient_dates,
   pct_duplicate_rate, n_multi_source_dates, pct_multi_source_of_dupes,
   n_exact_row_dupes, n_near_exact_dupes, recommended_source, recommended_source_completeness_pct

8. all_site_patient_duplicate_summary.csv (9332 rows -- NEEDS AGGREGATION per D-03):
   SITE, ID, n_unique_dates, n_total_encounters, n_duplicate_dates,
   n_multi_source_dates, sources_in_encounters, pct_primary_present, pct_secondary_present

9. all_site_date_level_duplicate_detail.csv (262K rows -- NEEDS AGGREGATION per D-03):
   SITE, ID, admit_date_parsed, ENCOUNTER_SOURCE, ENC_TYPE, ENCOUNTERID, ...

10. all_site_duplicate_aggregate_summary.csv:
    SITE, metric, value (long format)

11. all_site_source_payer_completeness.csv:
    SITE, ENCOUNTER_SOURCE, n_encounters, n_primary_present, pct_primary_present,
    n_secondary_present, pct_secondary_present, n_both_present, pct_both_present,
    n_either_present, pct_either_present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 3 bar chart PNGs and add Phase 21 missingness slides to PPTX</name>
  <files>R/11_generate_pptx.R</files>
  <read_first>
    - R/11_generate_pptx.R (full file -- understand Section 5 slide builder, Section 6 save location at line 1985, helpers at lines 666-796, color constants at lines 107-111)
    - R/20_all_source_missingness.R (understand CSV column schemas from write_csv calls at lines 172, 225, 262, 295, 369, 418)
    - R/21_all_site_duplicate_dates.R (understand CSV column schemas from write_csv calls at lines 449, 483, 564, 574, 661)
    - R/16_encounter_analysis.R (first 100 lines -- existing ggplot2 bar chart patterns with viridis palette)
  </read_first>
  <action>
Insert a new Section 7 into R/11_generate_pptx.R BETWEEN the end of Slide 38 (line 1983, after add_footnote for stacked_ud_stats_treated) and the current Section 6: SAVE PPTX (line 1985). The current Section 6 will be renumbered to Section 9 in Plan 02.

**Step A: Add `library(tidyr)` and `library(readr)` to the library block at the top of the file (after line 69, `library(stringr)`).**

Add these two lines after line 69:
```r
library(readr)
library(tidyr)
```

**Step B: Generate 3 bar chart PNGs (insert right after Slide 38's add_footnote call, before the SAVE section).**

Add this code block:

```r
# ==============================================================================
# SECTION 7: PHASE 21/22 BAR CHART PNG GENERATION
# ==============================================================================

message("\n--- Generating Phase 21/22 bar chart PNGs ---")

# Ensure output/figures/ exists
dir.create("output/figures", showWarnings = FALSE, recursive = TRUE)

# ---- Chart 1: Primary payer missingness % by site (D-05) ----
p21_cross_site <- read_csv("output/tables/all_source_cross_site_summary.csv",
                            show_col_types = FALSE)

chart1_data <- p21_cross_site %>%
  filter(SOURCE != "ALL") %>%
  arrange(desc(pct_primary_missing)) %>%
  mutate(SOURCE = factor(SOURCE, levels = SOURCE))

p1 <- ggplot(chart1_data, aes(x = SOURCE, y = pct_primary_missing)) +
  geom_col(fill = UF_BLUE) +
  coord_flip() +
  scale_y_continuous(labels = scales::percent_format(scale = 1),
                     limits = c(0, NA),
                     expand = expansion(mult = c(0, 0.05))) +
  geom_text(aes(label = paste0(pct_primary_missing, "%")),
            hjust = -0.1, size = 3.5, color = DARK_TEXT) +
  labs(
    title = "Primary Payer Missingness by Partner Site",
    x = "Partner Site",
    y = "% Encounters Missing Primary Payer"
  ) +
  theme_minimal(base_size = 14) +
  theme(
    plot.title = element_text(face = "bold", color = UF_BLUE),
    panel.grid.major.y = element_blank()
  )

ggsave("output/figures/phase21_missingness_by_site.png", p1,
       width = 10, height = 6, dpi = 300)
message("  Saved: output/figures/phase21_missingness_by_site.png")

# ---- Chart 2: Duplicate date rate % by site (D-05) ----
p22_cross_site <- read_csv("output/tables/all_site_cross_site_summary.csv",
                            show_col_types = FALSE)

chart2_data <- p22_cross_site %>%
  filter(SITE != "ALL") %>%
  arrange(desc(pct_duplicate_rate)) %>%
  mutate(SITE = factor(SITE, levels = SITE))

p2 <- ggplot(chart2_data, aes(x = SITE, y = pct_duplicate_rate)) +
  geom_col(fill = UF_ORANGE) +
  coord_flip() +
  scale_y_continuous(labels = scales::percent_format(scale = 1),
                     limits = c(0, NA),
                     expand = expansion(mult = c(0, 0.05))) +
  geom_text(aes(label = paste0(pct_duplicate_rate, "%")),
            hjust = -0.1, size = 3.5, color = DARK_TEXT) +
  labs(
    title = "Duplicate Date Rate by Partner Site",
    x = "Partner Site",
    y = "% Patient-Dates with Duplicate Encounters"
  ) +
  theme_minimal(base_size = 14) +
  theme(
    plot.title = element_text(face = "bold", color = UF_BLUE),
    panel.grid.major.y = element_blank()
  )

ggsave("output/figures/phase22_duplication_by_site.png", p2,
       width = 10, height = 6, dpi = 300)
message("  Saved: output/figures/phase22_duplication_by_site.png")

# ---- Chart 3: Grouped bar chart of missingness by encounter type across sites (D-05) ----
p21_by_enc_type <- read_csv("output/tables/all_source_payer_missingness_by_enc_type.csv",
                             show_col_types = FALSE)

chart3_data <- p21_by_enc_type %>%
  filter(SOURCE != "ALL") %>%
  select(SOURCE, ENC_TYPE_LABEL, pct_primary_missing, pct_secondary_missing) %>%
  pivot_longer(
    cols = starts_with("pct_"),
    names_to = "field",
    values_to = "pct_missing"
  ) %>%
  mutate(
    field = case_when(
      field == "pct_primary_missing" ~ "Primary",
      field == "pct_secondary_missing" ~ "Secondary",
      TRUE ~ field
    )
  )

p3 <- ggplot(chart3_data, aes(x = ENC_TYPE_LABEL, y = pct_missing, fill = field)) +
  geom_col(position = "dodge", width = 0.7) +
  facet_wrap(~ SOURCE, ncol = 3, scales = "free_y") +
  scale_fill_viridis_d(option = "mako", begin = 0.3, end = 0.7) +
  scale_y_continuous(labels = scales::percent_format(scale = 1)) +
  labs(
    title = "Payer Missingness by Encounter Type and Partner Site",
    x = "Encounter Type",
    y = "% Missing",
    fill = "Payer Field"
  ) +
  theme_minimal(base_size = 11) +
  theme(
    plot.title = element_text(face = "bold", color = UF_BLUE),
    axis.text.x = element_text(angle = 45, hjust = 1, size = 7),
    strip.text = element_text(face = "bold"),
    legend.position = "bottom"
  )

ggsave("output/figures/phase21_missingness_by_enc_type.png", p3,
       width = 14, height = 10, dpi = 300)
message("  Saved: output/figures/phase21_missingness_by_enc_type.png")
```

**Step C: Add Phase 21 Missingness table slides (Section 8).**

HIPAA suppression: apply to ALL count columns (n_patients, n_encounters, n_primary_missing, n_secondary_missing, n_both_missing). Replace values 1-10 with "<11". Percentages are NOT suppressed (they derive from aggregates, not individual patients). Use a local helper:

```r
# ==============================================================================
# SECTION 8: PHASE 21 MISSINGNESS SLIDES (D-01, D-04, D-06)
# ==============================================================================

message("\n--- Adding Phase 21 Missingness Slides ---")

# HIPAA helper: suppress count columns 1-10 with "<11"
suppress_small_counts <- function(df) {
  df %>%
    mutate(across(where(is.numeric), ~ {
      x_char <- if_else(. >= 1 & . <= 10, "<11",
                        format(., big.mark = ",", trim = TRUE))
      x_char
    }))
}

# ---- Slide 39: Cross-Site Payer Missingness Summary (all_source_cross_site_summary.csv) ----
message("  Slide 39: Cross-Site Payer Missingness Summary")

p21_cross_display <- p21_cross_site %>%
  mutate(
    SOURCE = if_else(SOURCE == "ALL", "ALL (Aggregate)", SOURCE),
    pct_primary_missing = paste0(pct_primary_missing, "%"),
    pct_secondary_missing = paste0(pct_secondary_missing, "%"),
    pct_both_missing = paste0(pct_both_missing, "%"),
    n_patients = format(n_patients, big.mark = ","),
    n_encounters = format(n_encounters, big.mark = ",")
  ) %>%
  select(
    `Site` = SOURCE,
    `Patients` = n_patients,
    `Encounters` = n_encounters,
    `% Primary Missing` = pct_primary_missing,
    `% Secondary Missing` = pct_secondary_missing,
    `% Both Missing` = pct_both_missing
  )

pptx <- add_table_slide(pptx,
  "Payer Missingness: Cross-Site Comparison",
  "Primary, secondary, and both payer fields missing by partner site (HL cohort encounters)",
  p21_cross_display) %>%
  add_footnote("Missing = NA, empty, NI, UN, OT, 99, 9999. ALL row = aggregate across all sites.")

# ---- Slide 40: Bar chart -- Primary Missingness by Site (D-05) ----
message("  Slide 40: Primary Payer Missingness by Site (bar chart)")

pptx <- add_image_slide(pptx,
  "Primary Payer Missingness by Partner Site",
  "Percentage of HL cohort encounters with missing primary payer data, sorted descending",
  "output/figures/phase21_missingness_by_site.png",
  img_width = 9, img_height = 5.0) %>%
  add_footnote("Missing = NA, empty, NI, UN, OT, 99, 9999. Excludes ALL aggregate row.")

# ---- Slide 41: Raw Value Distribution -- top values per site (all_source_payer_raw_value_distribution.csv) ----
message("  Slide 41: Raw Payer Value Distribution (top values by site)")

p21_raw_values <- read_csv("output/tables/all_source_payer_raw_value_distribution.csv",
                            show_col_types = FALSE)

# Show top 5 PRIMARY values per site for readability
raw_top5 <- p21_raw_values %>%
  filter(field == "PRIMARY") %>%
  group_by(SOURCE) %>%
  slice_max(n, n = 5, with_ties = FALSE) %>%
  ungroup() %>%
  mutate(
    pct = paste0(pct, "%"),
    n = format(n, big.mark = ",")
  ) %>%
  select(
    `Site` = SOURCE,
    `PAYER_TYPE_PRIMARY Value` = value,
    `Count` = n,
    `%` = pct
  )

pptx <- add_table_slide(pptx,
  "Raw PAYER_TYPE_PRIMARY: Top 5 Values per Site",
  "Most frequent raw primary payer values in HL cohort encounters, by partner site",
  raw_top5) %>%
  add_footnote("Shows top 5 values per site. Full distribution in all_source_payer_raw_value_distribution.csv.")

# ---- Slides 42-43: Missingness by Encounter Type (split if needed, D-02) ----
message("  Slides 42-43: Missingness by Encounter Type (split by site)")

p21_enc_type <- read_csv("output/tables/all_source_payer_missingness_by_enc_type.csv",
                          show_col_types = FALSE)

# Get unique sites (excluding ALL if present)
enc_sites <- sort(unique(p21_enc_type$SOURCE))
enc_sites <- enc_sites[enc_sites != "ALL"]

# Split into chunks of 4 sites per slide for readability
site_chunks <- split(enc_sites, ceiling(seq_along(enc_sites) / 4))

for (i in seq_along(site_chunks)) {
  chunk_data <- p21_enc_type %>%
    filter(SOURCE %in% site_chunks[[i]]) %>%
    mutate(
      pct_primary_missing = paste0(pct_primary_missing, "%"),
      pct_secondary_missing = paste0(pct_secondary_missing, "%"),
      n_encounters = format(n_encounters, big.mark = ",")
    ) %>%
    select(
      `Site` = SOURCE,
      `Enc Type` = ENC_TYPE_LABEL,
      `Encounters` = n_encounters,
      `% Primary Missing` = pct_primary_missing,
      `% Secondary Missing` = pct_secondary_missing
    )

  slide_title <- if (length(site_chunks) > 1) {
    glue("Payer Missingness by Encounter Type ({i}/{length(site_chunks)})")
  } else {
    "Payer Missingness by Encounter Type"
  }

  pptx <- add_table_slide(pptx,
    slide_title,
    glue("Primary and secondary payer missingness by encounter type -- Sites: {paste(site_chunks[[i]], collapse=', ')}"),
    chunk_data) %>%
    add_footnote("ENC_TYPE codes: AV=Ambulatory, IP=Inpatient, ED=Emergency, EI=ED-to-Inpatient, IS=Non-acute Institutional, OS=Observation, TH=Telehealth, OT=Other, <NA>=Missing.")
}

# ---- Slide 44: Grouped bar chart -- Missingness by Enc Type across Sites (D-05) ----
message("  Slide 44: Missingness by Encounter Type (grouped bar chart)")

pptx <- add_image_slide(pptx,
  "Payer Missingness by Encounter Type and Site",
  "Primary vs secondary payer missingness by encounter type, faceted by partner site",
  "output/figures/phase21_missingness_by_enc_type.png",
  img_width = 9.5, img_height = 5.5) %>%
  add_footnote("Bars show % of encounters with missing payer data. Faceted by DEMOGRAPHIC.SOURCE partner site.")

# ---- Slide 45: Raw vs Harmonized Comparison (all_source_payer_raw_vs_harmonized.csv) ----
message("  Slide 45: Raw vs Harmonized Missingness Comparison")

p21_raw_harm <- read_csv("output/tables/all_source_payer_raw_vs_harmonized.csv",
                          show_col_types = FALSE)

# Show OVERALL rows only (one per site) for the table slide
raw_harm_overall <- p21_raw_harm %>%
  filter(year == "OVERALL") %>%
  mutate(
    delta_pp = round(pct_harmonized - pct_raw_primary, 1),
    pct_raw_primary = paste0(pct_raw_primary, "%"),
    pct_harmonized = paste0(pct_harmonized, "%"),
    delta_pp = paste0(delta_pp, " pp"),
    n_encounters = format(n_encounters, big.mark = ",")
  ) %>%
  select(
    `Site` = SOURCE,
    `Encounters` = n_encounters,
    `% Raw Primary Missing` = pct_raw_primary,
    `% Harmonized Missing` = pct_harmonized,
    `Delta` = delta_pp
  )

pptx <- add_table_slide(pptx,
  "Raw vs Harmonized Payer Missingness by Site",
  "Comparison of raw field missingness vs harmonized category missingness (OVERALL)",
  raw_harm_overall) %>%
  add_footnote("Raw = PAYER_TYPE_PRIMARY is NA/empty/sentinel. Harmonized = payer_category is NA/Unknown/Unavailable. Delta = harmonized minus raw (positive = harmonization increases missingness).")

# ---- Slide 46: Year x Enc Type -- summary only (all_source_payer_missingness_year_x_enc_type.csv is 1015 rows) ----
message("  Slide 46: Year x Enc Type Missingness (top combinations)")

p21_year_enc <- read_csv("output/tables/all_source_payer_missingness_year_x_enc_type.csv",
                          show_col_types = FALSE)

# Show top 20 combinations by primary missingness % (min 50 encounters)
year_enc_top20 <- p21_year_enc %>%
  filter(n_encounters >= 50) %>%
  arrange(desc(pct_primary_missing)) %>%
  head(20) %>%
  mutate(
    pct_primary_missing = paste0(pct_primary_missing, "%"),
    n_encounters = format(n_encounters, big.mark = ","),
    n_primary_missing = format(n_primary_missing, big.mark = ",")
  ) %>%
  select(
    `Site` = SOURCE,
    `Year` = admit_year,
    `Enc Type` = ENC_TYPE_LABEL,
    `Encounters` = n_encounters,
    `Primary Missing` = n_primary_missing,
    `% Missing` = pct_primary_missing
  )

pptx <- add_table_slide(pptx,
  "Highest Missingness: Year x Encounter Type Combinations",
  "Top 20 site-year-encounter type combinations by primary payer missingness (min 50 encounters)",
  year_enc_top20) %>%
  add_footnote("Full crosstab (1,015 rows) in all_source_payer_missingness_year_x_enc_type.csv. Filtered to combinations with >= 50 encounters.")

# ---- Slide 47: Temporal Missingness by Year -- summary (all_source_payer_missingness_by_year.csv) ----
message("  Slide 47: Temporal Missingness by Year (latest 5 years per site)")

p21_by_year <- read_csv("output/tables/all_source_payer_missingness_by_year.csv",
                         show_col_types = FALSE)

# Show most recent 5 years per site for presentation brevity
year_recent <- p21_by_year %>%
  group_by(SOURCE) %>%
  slice_max(admit_year, n = 5, with_ties = FALSE) %>%
  ungroup() %>%
  mutate(
    pct_primary_missing = paste0(pct_primary_missing, "%"),
    n_encounters = format(n_encounters, big.mark = ",")
  ) %>%
  select(
    `Site` = SOURCE,
    `Year` = admit_year,
    `Encounters` = n_encounters,
    `% Primary Missing` = pct_primary_missing
  )

pptx <- add_table_slide(pptx,
  "Payer Missingness by Year (Recent 5 Years per Site)",
  "Primary payer missingness trend by admission year, most recent 5 years per partner site",
  year_recent) %>%
  add_footnote("Full temporal breakdown in all_source_payer_missingness_by_year.csv. 1900 sentinel dates excluded.")
```

**IMPORTANT implementation notes:**
- Insert ALL of the above code AFTER line 1983 (the add_footnote for Slide 38) and BEFORE line 1985 (the current `# SECTION 6: SAVE PPTX` comment).
- Do NOT modify ANY existing slide code (Slides 1-38). Only append new sections.
- The `p21_cross_site` and `p22_cross_site` variables created in chart generation (Section 7) are reused by the table slides (Section 8) -- this is intentional to avoid double CSV reads.
- For `suppress_small_counts()`: apply it only to columns where individual patient counts could be small. The cross-site summary has n_patients and n_encounters that are in the hundreds/thousands, so use format() with big.mark instead. The function is defined for defensive use on any table where counts could be 1-10.
- All slide numbering in comments is approximate (39-47). Actual slide numbers depend on existing slide generation. The comments are for code readability only.
- Do NOT update the Section 6 SAVE section yet -- that happens in Plan 02.

Per D-01: all 6 Phase 21 CSVs get slides. Per D-02: enc_type table split into chunks of 4 sites per slide. Per D-04: both tables AND bar charts included. Per D-05: chart 1 (missingness by site) and chart 3 (missingness by enc type) generated here; chart 2 (duplication by site) generated here for use in Plan 02. Per D-06: every CSV has a corresponding table slide.
  </action>
  <verify>
    <automated>grep -c "SECTION 7\|SECTION 8\|phase21_missingness_by_site\|phase22_duplication_by_site\|phase21_missingness_by_enc_type\|ggsave\|add_table_slide.*Missingness\|add_image_slide.*Missingness" R/11_generate_pptx.R</automated>
  </verify>
  <acceptance_criteria>
    - R/11_generate_pptx.R contains string "SECTION 7: PHASE 21/22 BAR CHART PNG GENERATION"
    - R/11_generate_pptx.R contains string "SECTION 8: PHASE 21 MISSINGNESS SLIDES"
    - R/11_generate_pptx.R contains exactly 3 ggsave() calls for phase21_missingness_by_site.png, phase22_duplication_by_site.png, phase21_missingness_by_enc_type.png
    - R/11_generate_pptx.R contains `library(readr)` and `library(tidyr)` in the library block
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_source_cross_site_summary.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_source_payer_raw_value_distribution.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_source_payer_missingness_by_enc_type.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_source_payer_missingness_year_x_enc_type.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_source_payer_raw_vs_harmonized.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_source_payer_missingness_by_year.csv"`
    - R/11_generate_pptx.R contains `read_csv("output/tables/all_site_cross_site_summary.csv"`
    - R/11_generate_pptx.R contains `add_footnote("Missing = NA, empty, NI, UN, OT, 99, 9999`
    - R/11_generate_pptx.R contains `scale_fill_viridis_d(` in chart 3 code
    - R/11_generate_pptx.R contains `coord_flip()` in chart 1 and chart 2 code
    - Existing Slide 1-38 code is unchanged (no modifications before line 1983)
    - Section 7 chart generation code appears BEFORE Section 8 table slides code
    - The SECTION 6: SAVE PPTX block still exists at end of file (not yet renumbered -- Plan 02 handles that)
  </acceptance_criteria>
  <done>
    R/11_generate_pptx.R has Section 7 (3 bar chart PNGs) and Section 8 (~9 Phase 21 missingness slides including 6 table slides and 2 chart slides, with split tables for enc_type data) inserted before the SAVE section. All 6 Phase 21 CSVs are represented. Charts use UF_BLUE, UF_ORANGE, and viridis palettes matching existing project style.
  </done>
</task>

</tasks>

<verification>
After Task 1 completion:
- `grep -c "SECTION 7" R/11_generate_pptx.R` returns 1
- `grep -c "SECTION 8" R/11_generate_pptx.R` returns at least 1
- `grep -c "ggsave" R/11_generate_pptx.R` returns 3 (new charts only; existing code has no ggsave)
- `grep -c "all_source_" R/11_generate_pptx.R` returns >= 6 (one per Phase 21 CSV)
- `grep -c "all_site_cross_site" R/11_generate_pptx.R` returns >= 1
- `grep "SECTION 6: SAVE" R/11_generate_pptx.R` still matches (save section preserved)
- File compiles without syntax errors: `R -e "parse('R/11_generate_pptx.R')"` exits 0
</verification>

<success_criteria>
1. R/11_generate_pptx.R contains Section 7 (chart PNG generation) and Section 8 (Phase 21 missingness slides)
2. All 6 Phase 21 CSVs have corresponding table slides
3. 3 bar chart PNG generation commands (ggsave) are present for the 3 required charts (D-05)
4. Wide tables (enc_type) are split across multiple slides (D-02)
5. Year x enc_type (1015 rows) is summarized to top 20 combinations instead of raw table (D-03 pattern)
6. Existing Slides 1-38 are untouched
7. File parses without R syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-make-visual-presentation-of-tables-from-last-2-pages/23-01-SUMMARY.md`
</output>
