---
phase: 39-investigate-unmatched-codes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/39_investigate_unmatched.R
  - output/unmatched_codes_report.xlsx
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07

must_haves:
  truths:
    - "All CPT/HCPCS codes in PROCEDURES table for HL patients are scanned with widened heuristic ranges"
    - "Unmatched codes (not in TREATMENT_CODES) are identified and counted"
    - "Each unmatched code has a description from NLM HCPCS API (or NA if lookup fails)"
    - "Each unmatched code is auto-classified into one of: Chemotherapy, Radiation, SCT, Immunotherapy, Supportive Care, Unrelated"
    - "A styled xlsx report shows all unmatched codes with descriptions, classifications, and patient counts"
  artifacts:
    - path: "R/39_investigate_unmatched.R"
      provides: "Unmatched code investigation script"
      min_lines: 350
    - path: "output/unmatched_codes_report.xlsx"
      provides: "Styled xlsx report with classification results"
  key_links:
    - from: "R/39_investigate_unmatched.R"
      to: "R/00_config.R"
      via: "source() to load TREATMENT_CODES"
      pattern: "source.*00_config"
    - from: "R/39_investigate_unmatched.R"
      to: "R/01_load_pcornet.R"
      via: "source() for get_pcornet_table()"
      pattern: "source.*01_load_pcornet"
    - from: "R/39_investigate_unmatched.R"
      to: "https://clinicaltables.nlm.nih.gov/api/hcpcs/v3/search"
      via: "httr::GET() for code description lookup"
      pattern: "clinicaltables\\.nlm\\.nih\\.gov"
---

<objective>
Create the investigation script that extracts all CPT/HCPCS codes from the PROCEDURES table using widened heuristic ranges (per D-02), filters out codes already in TREATMENT_CODES, looks up descriptions via the NLM HCPCS API (per D-04), auto-classifies each unmatched code (per D-05, D-06), and produces a styled xlsx report (per D-07).

Purpose: Identify treatment-relevant procedure codes that Phase 38's curated lists miss, producing a classification report that Plan 02 will use to update TREATMENT_CODES.
Output: R/39_investigate_unmatched.R script + output/unmatched_codes_report.xlsx workbook
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-investigate-unmatched-codes/39-CONTEXT.md
@.planning/phases/39-investigate-unmatched-codes/39-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/00_config.R (lines 412-659) -- TREATMENT_CODES structure:
```r
TREATMENT_CODES <- list(
  chemo_hcpcs = c("J9000", "J9040", "J9360", "J9130", "J9042", "J9299"),
  chemo_rxnorm = c("3639", "11213", "67228", "3946"),
  radiation_cpt = c("77401", "77402", "77407", "77412", "77427"),
  sct_cpt = c("38230", "38232", "38240", "38241", "38242", "38243"),
  sct_hcpcs = c("S2140", "S2142", "S2150"),
  chemo_icd9 = c("99.25", "99.28"),
  chemo_icd10pcs_prefixes = c("3E03305", "3E04305", "3E05305", "3E06305"),
  radiation_icd9 = c("92.20", "92.21", ...),  # 14 codes
  radiation_icd10pcs_prefixes = c("D70", "D71", "D72", "D7Y"),
  sct_icd9 = c("41.00", "41.01", ...),  # 10 codes
  sct_icd10pcs = c("30230C0", "30230G0", ...),  # ~40 codes
  cart_icd10pcs_prefixes = c("XW033C7", "XW033G7", ...),  # 16 codes
  chemo_dx_icd10 = c("Z51.11", "Z51.12"),
  chemo_dx_icd9 = c("V58.11", "V58.12"),
  radiation_dx_icd10 = c("Z51.0"),
  radiation_dx_icd9 = c("V58.0"),
  sct_dx_icd10 = c("Z94.84", "T86.5", "T86.09", "Z48.290", "T86.0"),
  chemo_drg = c("837", "838", "839", "846", "847", "848"),
  radiation_drg = c("849"),
  sct_drg = c("014", "016", "017"),
  chemo_revenue = c("0331", "0332", "0335"),
  radiation_revenue = c("0330", "0333"),
  sct_revenue = c("0362", "0815")
)
```

From R/38_treatment_inventory.R (lines 57-73) -- current CPT_HCPCS_RANGES:
```r
CPT_HCPCS_RANGES <- list(
  Chemotherapy = list(j9_codes = "^J9[0-9]{3}$"),
  Radiation = list(delivery = "^774[0-9]{2}$"),
  SCT = list(transplant = "^382[3-4][0-9]$"),
  Immunotherapy = list(car_t_admin = "^XW0[34]3[A-Z][0-9]$")
)
```

From R/38_treatment_inventory.R (lines 48-55) -- TREATMENT_TYPE_COLORS:
```r
TREATMENT_TYPE_COLORS <- list(
  Chemotherapy  = list(fill = "FFDCEEFB", font = "FF0B5394"),
  Radiation     = list(fill = "FFDDF4E1", font = "FF274E13"),
  SCT           = list(fill = "FFFFF4D6", font = "FF7F6000"),
  Immunotherapy = list(fill = "FFE8DCF4", font = "FF4C1D7A")
)
```

From R/38_treatment_inventory.R -- detect_unknown_codes() function signature:
```r
detect_unknown_codes <- function(treatment_type)
# Returns: tibble(code, code_type, source_table, n, drug_name, treatment_type)
# Queries PROCEDURES where PX_TYPE == "CH" (or "10" for Immunotherapy)
# Filters by CPT_HCPCS_RANGES regex, excludes known TREATMENT_CODES
```

From R/38_treatment_inventory.R -- write_treatment_sheet() function signature:
```r
write_treatment_sheet <- function(wb, sheet_name, df_summary, df_codes, df_unmatched, treatment_type)
# Creates styled xlsx sheet with title, subtitle, By Source Table summary,
# Detailed Codes section, optional Unmatched Codes section
# Uses TREATMENT_TYPE_COLORS for pill styling
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/39_investigate_unmatched.R -- extraction, API lookup, classification, and xlsx report</name>
  <files>R/39_investigate_unmatched.R</files>
  <read_first>
    R/00_config.R
    R/38_treatment_inventory.R
    R/01_load_pcornet.R
  </read_first>
  <action>
Create `R/39_investigate_unmatched.R` with the following sections:

**SECTION 1: SETUP AND CONFIGURATION**
- `source("R/00_config.R")` and `source("R/01_load_pcornet.R")`
- `library(httr)`, `library(jsonlite)`, `library(openxlsx2)`, `library(dplyr)`, `library(stringr)`, `library(glue)`, `library(tidyr)`
- `OUTPUT_PATH <- file.path(CONFIG$output_dir, "unmatched_codes_report.xlsx")`

**SECTION 2: WIDENED HEURISTIC RANGES (per D-02)**

Define `CPT_HCPCS_RANGES_WIDENED` as a named list, expanding beyond Phase 38's `CPT_HCPCS_RANGES`:

```r
CPT_HCPCS_RANGES_WIDENED <- list(
  Chemotherapy = list(
    j9_codes = "^J9[0-9]{3}$",           # J9000-J9999 (same as Phase 38)
    j0_j8_drugs = "^J[0-8][0-9]{3}$"     # J0000-J8999 full range for classification
  ),
  Radiation = list(
    delivery = "^774[0-9]{2}$",           # 77400-77499 (same as Phase 38)
    planning = "^773[0-9]{2}$"            # 77300-77399 treatment planning (NEW per D-02)
  ),
  SCT = list(
    transplant = "^382[3-4][0-9]$"        # 38230-38249 (no change per D-02)
  ),
  Immunotherapy = list(
    car_t_admin = "^XW0[34]3[A-Z][0-9]$" # CAR T-cell ICD-10-PCS (no change per D-02)
  )
)
```

Note: Per D-01, CPT/HCPCS only -- no ICD-10-PCS, ICD-9, DRG, revenue, RXNORM, or NDC. Per D-03, skip NDC entirely.

Define `TREATMENT_TYPE_COLORS` extending Phase 38's palette with Supportive Care and Unrelated:
```r
TREATMENT_TYPE_COLORS <- list(
  Chemotherapy    = list(fill = "FFDCEEFB", font = "FF0B5394"),
  Radiation       = list(fill = "FFDDF4E1", font = "FF274E13"),
  SCT             = list(fill = "FFFFF4D6", font = "FF7F6000"),
  Immunotherapy   = list(fill = "FFE8DCF4", font = "FF4C1D7A"),
  `Supportive Care` = list(fill = "FFD5F5F0", font = "FF0E6655"),
  Unrelated       = list(fill = "FFF3F4F6", font = "FF6B7280")
)
```

**SECTION 3: EXTRACT UNMATCHED CODES**

Create `extract_unmatched_codes()` function. For each treatment type in CPT_HCPCS_RANGES_WIDENED:
1. Build combined regex from all patterns in the list entry
2. Query PROCEDURES table (via `safe_table()` pattern from Phase 38 or direct `get_pcornet_table("PROCEDURES")`)
3. Filter to `PX_TYPE == "CH"` for Chemo/Radiation/SCT, `PX_TYPE == "10"` for Immunotherapy
4. Filter codes matching the combined regex
5. Exclude codes already in the relevant TREATMENT_CODES vectors:
   - Chemotherapy: exclude `TREATMENT_CODES$chemo_hcpcs`
   - Radiation: exclude `TREATMENT_CODES$radiation_cpt`
   - SCT: exclude `c(TREATMENT_CODES$sct_cpt, TREATMENT_CODES$sct_hcpcs)`
   - Immunotherapy: exclude `TREATMENT_CODES$cart_icd10pcs_prefixes`
6. Count occurrences per code AND count distinct patients (PATID) per code
7. Return tibble with columns: `code`, `n_records`, `n_patients`, `heuristic_type` (which treatment type range matched it)

Run for all treatment types and `bind_rows()` into `all_unmatched`.

**SECTION 4: NLM HCPCS API LOOKUP (per D-04)**

Create `lookup_hcpcs_batch(codes, sleep_sec = 0.15)` function:
- For each code, call `GET(glue("https://clinicaltables.nlm.nih.gov/api/hcpcs/v3/search?terms={code}&ef=display"))` with `timeout(10)`
- Parse JSON response: `json[[1]]` = total count, `json[[2]]` = code matches, `json[[4]]` = display strings
- If `json[[1]] > 0` and the first matched code `json[[2]][1]` exactly equals the query code, take `json[[4]][1]` as description; else mark `lookup_status = "not_found"`
- Handle HTTP errors and timeouts gracefully (set `lookup_status = "error: {message}"`)
- Log progress every 10 codes: `message(glue("  Looked up {i}/{length(codes)} codes"))`
- Sleep `sleep_sec` between requests (default 0.15 sec for ~7 req/sec)
- Return tibble with columns: `code`, `description`, `lookup_status`

Call `lookup_hcpcs_batch()` on the unique codes from `all_unmatched`. Left-join descriptions back.

Log summary: how many succeeded, how many not_found, how many errored.

**SECTION 5: AUTO-CLASSIFICATION (per D-05, D-06)**

Create `classify_unmatched_code(code, description, heuristic_type)` function using `case_when()`:

Order matters -- more specific checks first:

```r
classify_unmatched_code <- function(code, description, heuristic_type) {
  desc_lower <- tolower(ifelse(is.na(description), "", description))

  case_when(
    # 1. Supportive care: J0-J8 codes with specific supportive care keywords
    #    MUST come before Chemotherapy to avoid misclassification (Pitfall 2)
    str_detect(code, "^J[0-8]") & str_detect(desc_lower,
      "filgrastim|pegfilgrastim|neulasta|neupogen|zarxio|granix|udenyca|nyvepria|ziextenzo|stimufend|fylnetra|releuko|ondansetron|zofran|granisetron|kytril|palonosetron|aloxi|fosaprepitant|emend|aprepitant|dexamethasone|colony.stimulating|growth factor|antiemetic|epoetin|procrit|darbepoetin|aranesp"
    ) ~ "Supportive Care",

    # 2. Chemotherapy: J9xxx always chemo; J0-J8 with antineoplastic keywords
    str_detect(code, "^J9") ~ "Chemotherapy",
    str_detect(code, "^J[0-8]") & str_detect(desc_lower,
      "chemotherapy|antineoplastic|doxorubicin|cisplatin|carboplatin|etoposide|vincristine|bleomycin|dacarbazine|cyclophosphamide|methotrexate|cytarabine|fludarabine|bendamustine|gemcitabine|ifosfamide|brentuximab|nivolumab|pembrolizumab|rituximab|obinutuzumab"
    ) ~ "Chemotherapy",

    # 3. Radiation: 773xx-774xx or radiation keywords
    str_detect(code, "^77[34]") & str_detect(desc_lower,
      "radiation|radiotherapy|irradiation|beam|brachytherapy|dosimetry|treatment.planning|isodose|teletherapy|treatment.delivery|treatment.management"
    ) ~ "Radiation",
    str_detect(code, "^774") ~ "Radiation",  # 774xx delivery codes always radiation

    # 4. SCT: 382xx or transplant keywords
    str_detect(code, "^382[34]") ~ "SCT",
    str_detect(desc_lower, "transplant|bone marrow|stem cell|hematopoietic|allogeneic|autologous") ~ "SCT",

    # 5. Immunotherapy: XW0xx or immunotherapy keywords
    str_detect(code, "^XW0[34]3") ~ "Immunotherapy",
    str_detect(desc_lower, "car.t|chimeric antigen|immunotherapy|checkpoint inhibitor") ~ "Immunotherapy",

    # 6. Default: Unrelated
    TRUE ~ "Unrelated"
  )
}
```

Apply classification: `all_unmatched <- all_unmatched %>% mutate(classification = classify_unmatched_code(code, description, heuristic_type))`

Log classification summary: `count(all_unmatched, classification) %>% arrange(desc(n))`

**SECTION 6: XLSX REPORT GENERATION (per D-07)**

Create `write_unmatched_report(df, output_path)` function following Phase 38's `write_treatment_sheet()` visual pattern:

- Create workbook with `wb_workbook()`
- One sheet per classification category in order: Chemotherapy, Radiation, SCT, Immunotherapy, Supportive Care, Unrelated
- Skip categories with 0 codes
- Each sheet layout:
  - Row 1: Title "Unmatched CPT/HCPCS Codes Investigation" (Calibri 16pt bold, dark gray FF1F2937, merged A1:F1)
  - Row 2: Subtitle "{category}: {N} unmatched codes" (Calibri 10pt, gray FF6B7280, merged A2:F2)
  - Row 3: blank
  - Row 4: Column headers "Code", "Description", "Heuristic Match", "Records", "Patients", "Lookup Status" (dark gray fill FF374151, white text FFFFFFFF, Calibri 11pt bold)
  - Row 5+: Data rows sorted by n_patients descending
    - Code column: Calibri 10pt bold dark gray FF374151 with treatment-type colored pill fill
    - Description column: Calibri 10pt color FF111827
    - Other columns: Calibri 10pt color FF111827
    - Records and Patients columns: number format "#,##0"
  - Freeze panes at row 5 (first_active_row = 5)
  - Column widths: c(12, 55, 18, 12, 12, 15)
- Add a "Summary" sheet as the first sheet:
  - Row 1: Title "Unmatched Codes Investigation Summary"
  - Row 3: Column headers "Classification", "Count", "% of Total"
  - Data rows: one per classification category
  - Total row at bottom with dark fill

Save workbook to `output_path`.

**SECTION 7: MAIN EXECUTION**

```r
message("=== Phase 39: Investigate Unmatched CPT/HCPCS Codes ===")
message("")

# Step 1: Extract unmatched codes
message("Step 1: Extracting unmatched codes with widened heuristic ranges...")
all_unmatched <- extract_unmatched_codes()
message(glue("  Found {nrow(all_unmatched)} unmatched codes across all treatment types"))

# Step 2: Look up descriptions
message("")
message("Step 2: Looking up code descriptions via NLM HCPCS API...")
descriptions <- lookup_hcpcs_batch(unique(all_unmatched$code))
all_unmatched <- all_unmatched %>% left_join(descriptions, by = "code")
n_found <- sum(descriptions$lookup_status == "success")
message(glue("  Descriptions found: {n_found}/{nrow(descriptions)} codes"))

# Step 3: Auto-classify
message("")
message("Step 3: Auto-classifying unmatched codes...")
all_unmatched <- all_unmatched %>%
  mutate(classification = classify_unmatched_code(code, description, heuristic_type))
class_summary <- count(all_unmatched, classification) %>% arrange(desc(n))
message("  Classification summary:")
for (i in seq_len(nrow(class_summary))) {
  message(glue("    {class_summary$classification[i]}: {class_summary$n[i]}"))
}

# Step 4: Write xlsx report
message("")
message("Step 4: Writing xlsx report...")
write_unmatched_report(all_unmatched, OUTPUT_PATH)
message(glue("  Report saved to {OUTPUT_PATH}"))

# Step 5: Save RDS for Plan 02 config update
message("")
message("Step 5: Saving classified codes for config update...")
saveRDS(all_unmatched, file.path(CONFIG$output_dir, "unmatched_codes_classified.rds"))
message("  Saved output/unmatched_codes_classified.rds")

message("")
message("=== Phase 39 Investigation Complete ===")
```
  </action>
  <verify>
    <automated>Rscript -e "parse('R/39_investigate_unmatched.R'); message('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - R/39_investigate_unmatched.R exists and has >= 350 lines
    - File contains `source("R/00_config.R")` and `source("R/01_load_pcornet.R")`
    - File contains `CPT_HCPCS_RANGES_WIDENED` with widened ranges: `j0_j8_drugs` pattern for Chemo AND `planning` pattern for Radiation
    - File contains `lookup_hcpcs_batch` function with `clinicaltables.nlm.nih.gov` URL
    - File contains `classify_unmatched_code` function with `case_when()` and categories: Supportive Care, Chemotherapy, Radiation, SCT, Immunotherapy, Unrelated
    - Supportive Care case appears BEFORE Chemotherapy case in case_when()
    - File contains `write_unmatched_report` function using `wb_workbook()`
    - File contains `TREATMENT_TYPE_COLORS` with entries for all 6 categories including `Supportive Care` and `Unrelated`
    - File contains `saveRDS(all_unmatched` for Plan 02 consumption
    - `Rscript -e "parse('R/39_investigate_unmatched.R')"` exits 0 (valid R syntax)
    - No references to NDC, RXNORM, ICD-10-PCS, ICD-9, DRG, or revenue codes (per D-01, D-03 -- CPT/HCPCS only)
  </acceptance_criteria>
  <done>R/39_investigate_unmatched.R is syntactically valid R, implements widened heuristic extraction (D-02), NLM API lookup (D-04), auto-classification (D-05/D-06), and xlsx report generation (D-07). Script saves classified codes as RDS for Plan 02.</done>
</task>

<task type="checkpoint:human-verify" gate="soft">
  <name>Task 2: Verify investigation script runs and produces correct xlsx report</name>
  <files>output/unmatched_codes_report.xlsx, output/unmatched_codes_classified.rds</files>
  <action>Human verification of script execution output and xlsx report quality. Script should be run on HiPerGator where the DuckDB data is available.</action>
  <what-built>R/39_investigate_unmatched.R -- investigation script that extracts unmatched CPT/HCPCS codes, looks up descriptions via NLM API, auto-classifies them, and produces a styled xlsx report.</what-built>
  <how-to-verify>
    1. On HiPerGator, run: `Rscript R/39_investigate_unmatched.R`
    2. Confirm console output shows Steps 1-5 completing without errors
    3. Confirm NLM API lookup reports > 0 successful descriptions
    4. Confirm classification summary shows counts for each category
    5. Open `output/unmatched_codes_report.xlsx` and verify:
       a. Summary sheet exists with classification breakdown
       b. Treatment-type sheets have colored header pills
       c. Codes have descriptions filled in (not all NA)
       d. Sort order is by patient count descending
    6. Confirm `output/unmatched_codes_classified.rds` exists
  </how-to-verify>
  <verify>User confirms xlsx report is correct and script ran without errors</verify>
  <done>Script runs end-to-end on HiPerGator, xlsx report has correct styling and classifications, RDS file saved for Plan 02</done>
  <resume-signal>Type "approved" to proceed to Plan 02, or describe issues for correction</resume-signal>
</task>

</tasks>

<verification>
- `R/39_investigate_unmatched.R` parses without error: `Rscript -e "parse('R/39_investigate_unmatched.R')"`
- Script sources R/00_config.R and R/01_load_pcornet.R
- Widened heuristic ranges include J0-J8 codes and 773xx radiation planning codes
- NLM API lookup function handles errors gracefully (timeout, HTTP errors, not found)
- Classification uses case_when() with Supportive Care BEFORE Chemotherapy
- xlsx report has consistent styling with Phase 38 patterns (dark gray headers, treatment-type colored pills, frozen panes)
- No NDC, RXNORM, ICD-10-PCS, ICD-9, DRG, or revenue code investigation (per D-01, D-03)
</verification>

<success_criteria>
- R/39_investigate_unmatched.R runs end-to-end on HiPerGator
- output/unmatched_codes_report.xlsx produced with per-category sheets
- output/unmatched_codes_classified.rds saved for Plan 02 consumption
- All unmatched codes classified as one of: Chemotherapy, Radiation, SCT, Immunotherapy, Supportive Care, Unrelated
</success_criteria>

<output>
After completion, create `.planning/phases/39-investigate-unmatched-codes/39-01-SUMMARY.md`
</output>
