---
phase: 76-treatment-source-analysis-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/26_treatment_episodes.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - TREAT-01
  - QUAL-01

must_haves:
  truths:
    - "Tumor registry (TR) is no longer a treatment data source in the pipeline"
    - "Chemotherapy uses 6 sources (PX, RX, DX, DRG, DISP, MA) — not 7"
    - "Radiation uses 3 sources (PX, DX, DRG) — not 4"
    - "SCT uses 2 sources (PX, DRG) — not 3"
    - "Immunotherapy is unchanged at 2 sources (PX, DRG)"
    - "Pipeline halts with error if episode count drops >20% unexpectedly"
    - "Smoke test validates TR removal correctness"
  artifacts:
    - path: "R/26_treatment_episodes.R"
      provides: "Treatment episode extraction without tumor registry sources"
      contains: "Phase 76: Tumor registry source removed"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Smoke test section validating TR removal"
      contains: "TR removal validation"
  key_links:
    - from: "R/26_treatment_episodes.R"
      to: "stack_and_dedup_with_codes()"
      via: "sources list excluding TR"
      pattern: "sources = list.*PX.*RX.*DRG(?!.*TR)"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/26_treatment_episodes.R"
      via: "grep validation that TUMOR_REGISTRY is absent from extraction functions"
      pattern: "TUMOR_REGISTRY"
---

<objective>
Remove tumor registry (TR) treatment data from the treatment episode pipeline (R/26) and validate the removal via smoke test (R/88).

Purpose: TR data has 8-32% accuracy per SEER literature vs 95-100% for EHR claims. Removing TR improves data source reliability for treatment episode analysis. The user chose direct removal without pre-analysis (REQUIREMENTS.md Out of Scope).

Output: Modified R/26 without TR source blocks, assertion guarding against >20% episode count drop, updated smoke test validating TR removal.
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-treatment-source-analysis-removal/76-RESEARCH.md
@R/26_treatment_episodes.R
@R/88_smoke_test_comprehensive.R
@R/00_config.R
</context>

<interfaces>
<!-- Key functions and constants the executor needs from existing codebase -->

From R/26_treatment_episodes.R:
- extract_chemo_dates_with_codes() — lines 120-232, TR block at lines 195-223, sources list at line 228
- extract_radiation_dates_with_codes() — lines 236-311, TR block at lines 277-305, sources list at line 308
- extract_sct_dates_with_codes() — lines 315-378, TR block at lines 341-372, sources list at line 375
- extract_immunotherapy_dates_with_codes() — lines 382-409, NO TR source (already clean)
- stack_and_dedup_with_codes(sources, type_name) — line 93, accepts named list of source dataframes
- calculate_episodes_detailed(dates_df, gap_threshold) — line 453, returns episode-level tibble

From R/00_config.R:
- TREATMENT_TYPES <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy")
- GAP_THRESHOLD <- 90

From R/88_smoke_test_comprehensive.R:
- check(description, condition) — pass/fail logging function (line 45)
- Current sections: 1-14, section numbering [1/18] through [18/18]
- Section 13 (lines 597-695): NLPHL & death cause validation (last content section before summary)
- Section 14 (lines 697-726): Summary section
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove tumor registry source blocks from R/26 and add episode count assertion</name>
  <files>R/26_treatment_episodes.R</files>
  <read_first>
    - R/26_treatment_episodes.R (full file — understand current TR block locations and source list structure)
    - R/00_config.R (TREATMENT_TYPES, GAP_THRESHOLD constants)
    - R/utils/utils_assertions.R (checkmate assertion patterns)
  </read_first>
  <action>
    Modify R/26_treatment_episodes.R to remove all tumor registry (TR) treatment data sources. Three specific changes in three extraction functions, plus one assertion addition.

    **1. extract_chemo_dates_with_codes() (lines 120-232):**
    - DELETE the entire TR block from line 195 (`# 7. TUMOR_REGISTRY_ALL:`) through line 223 (closing brace). This is 29 lines.
    - REPLACE the deleted block with a 3-line comment:
      ```r
      # Phase 76: Tumor registry source removed — claims-based sources only
      # TR data accuracy 8-32% per SEER literature vs 95-100% for EHR claims
      # Coverage analysis documented in output/source_coverage_analysis.xlsx
      ```
    - UPDATE the stack_and_dedup_with_codes() call (originally line 225-231) to remove `TR = tr_dates` from the sources list:
      ```r
      stack_and_dedup_with_codes(
        sources = list(
          PX = px_dates, RX = rx_dates, DX = dx_dates,
          DRG = drg_dates, DISP = disp_dates, MA = ma_dates
        ),
        type_name = "Chemotherapy"
      )
      ```

    **2. extract_radiation_dates_with_codes() (lines 236-311):**
    - DELETE the entire TR block from line 277 (`# 4. TUMOR_REGISTRY_ALL:`) through line 305 (closing brace). This is 29 lines.
    - REPLACE the deleted block with a 3-line comment (same as above).
    - UPDATE the stack_and_dedup_with_codes() call (originally line 307-309) to remove `TR = tr_dates`:
      ```r
      stack_and_dedup_with_codes(
        sources = list(PX = px_dates, DX = dx_dates, DRG = drg_dates),
        type_name = "Radiation"
      )
      ```

    **3. extract_sct_dates_with_codes() (lines 315-378):**
    - DELETE the entire TR block from line 341 (`# 3. TUMOR_REGISTRY_ALL:`) through line 372 (closing brace). This is 32 lines.
    - REPLACE the deleted block with a 3-line comment (same as above).
    - UPDATE the stack_and_dedup_with_codes() call (originally line 374-376) to remove `TR = tr_dates`:
      ```r
      stack_and_dedup_with_codes(
        sources = list(PX = px_dates, DRG = drg_dates),
        type_name = "SCT"
      )
      ```

    **4. Add episode count assertion in SECTION 5 (main execution loop):**
    After the `episodes_list` and `detail_list` initialization (around line 615-616), and INSIDE the for loop (after `episodes_df <- calculate_episodes_detailed(dates_df)` on line 624, before `episodes_df <- episodes_df %>% mutate(treatment_type = type)` on line 627), add an assertion block:

    ```r
    # Phase 76: Guard against unexpected episode count drop after TR removal
    # Percentage-based threshold (>20%) prevents silent data loss from source changes.
    # Baseline will be populated after first post-TR-removal run.
    # If assertion fires, investigate whether upstream cohort changes or source
    # modifications caused the drop — do NOT simply increase the threshold.
    if (exists("EPISODE_COUNT_BASELINE") && !is.null(EPISODE_COUNT_BASELINE[[type]])) {
      expected <- EPISODE_COUNT_BASELINE[[type]]
      actual <- nrow(episodes_df)
      pct_delta <- abs(actual - expected) / expected * 100
      checkmate::assert_true(
        pct_delta <= 20,
        .var.name = glue(
          "[R/26] {type} episode count delta {round(pct_delta, 1)}% exceeds 20% threshold ",
          "(expected ~{expected}, got {actual})"
        )
      )
    }
    ```

    **5. Add EPISODE_COUNT_BASELINE constant near the top of SECTION 5 (before the for loop):**
    After the `message("=== Phase 44: Treatment Episode Start/Stop Dates ===\n")` line (613), add:

    ```r
    # Phase 76: Episode count baseline for >20% drop detection after TR source removal
    # Update these values after first successful post-TR-removal pipeline run.
    # Set to NULL to skip assertion (e.g., during initial calibration run).
    EPISODE_COUNT_BASELINE <- NULL
    ```

    **6. Update the script header (lines 1-43):**
    - Add to the Decision traceability block:
      ```
      #   Phase 76 additions:
      #   D-76-01: Tumor registry (TR) source removed from chemo, radiation, SCT extraction
      #   D-76-02: TR data accuracy 8-32% vs 95-100% claims (SEER literature)
      #   D-76-03: Episode count assertion with >20% drop threshold
      #   D-76-04: EPISODE_COUNT_BASELINE = NULL until first post-removal run
      ```
    - Update the source count comment on line 81 (`# TUMOR_REGISTRY dates are date evidence only...`) — remove or update this comment since TR dates are no longer extracted.

    **7. Verify no other references to TUMOR_REGISTRY remain in extraction functions:**
    After making changes, search the file for any remaining "TUMOR_REGISTRY", "TR = tr_dates", or "tr_dates" references. The ONLY acceptable remaining references are in comments (the Phase 76 removal comments) and possibly in the annotate_detail_with_episodes function docstring if it mentions TR. Remove any dead references.
  </action>
  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && grep -c "tr_dates <- NULL" R/26_treatment_episodes.R</automated>
    The count must be 0 (all tr_dates blocks removed).

    Also verify:
    - `grep -c "TR = tr_dates" R/26_treatment_episodes.R` returns 0
    - `grep -c "TUMOR_REGISTRY_ALL" R/26_treatment_episodes.R` returns 0 (no live references, only in comments)
    - `grep -c "Phase 76: Tumor registry source removed" R/26_treatment_episodes.R` returns 3 (one per extraction function)
    - `grep -c "EPISODE_COUNT_BASELINE" R/26_treatment_episodes.R` returns at least 2 (definition + usage)
    - `grep -c "D-76-01" R/26_treatment_episodes.R` returns 1 (header traceability)
    - File parses without R syntax errors: `Rscript -e "parse('R/26_treatment_episodes.R')"` exits 0
  </verify>
  <acceptance_criteria>
    - R/26_treatment_episodes.R contains zero instances of `tr_dates <- NULL` (all 3 TR extraction blocks removed)
    - R/26_treatment_episodes.R contains zero instances of `TR = tr_dates` in any sources list
    - R/26_treatment_episodes.R contains zero instances of `get_pcornet_table("TUMOR_REGISTRY_ALL")` outside comments
    - R/26_treatment_episodes.R contains exactly 3 instances of `Phase 76: Tumor registry source removed` (one per function)
    - extract_chemo_dates_with_codes() sources list has 6 entries: PX, RX, DX, DRG, DISP, MA
    - extract_radiation_dates_with_codes() sources list has 3 entries: PX, DX, DRG
    - extract_sct_dates_with_codes() sources list has 2 entries: PX, DRG
    - extract_immunotherapy_dates_with_codes() is unchanged (already has no TR source)
    - EPISODE_COUNT_BASELINE defined as NULL in SECTION 5
    - checkmate::assert_true() call present with 20% threshold and pct_delta variable
    - Script header contains `D-76-01` through `D-76-04` decision traceability
    - File parses as valid R: `Rscript -e "parse('R/26_treatment_episodes.R')"` exits with code 0
  </acceptance_criteria>
  <done>
    All tumor registry extraction blocks removed from R/26. Chemotherapy reduced from 7 to 6 sources, Radiation from 4 to 3, SCT from 3 to 2. Episode count assertion with >20% threshold added. No dead TR references remain. File parses as valid R.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add TR removal validation to smoke test</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (full file — understand section structure, check() function, section numbering)
    - R/26_treatment_episodes.R (post-Task-1 state — verify what was removed)
  </read_first>
  <action>
    Add a new smoke test section to R/88_smoke_test_comprehensive.R that validates tumor registry removal from R/26.

    **1. Insert new SECTION 13B after existing Section 13 (NLPHL/death cause, ending at line 695) and before Section 14 (Summary, starting at line 697):**

    Add this new section:

    ```r
    # ==============================================================================
    # SECTION 13B: TR REMOVAL VALIDATION ----
    # ==============================================================================
    # Phase 76 (TREAT-01): Validates tumor registry source removed from R/26.

    message("\n[19/19] TR removal validation...")

    # Read R/26 source code for static analysis
    r26_lines <- readLines("R/26_treatment_episodes.R")
    r26_text <- paste(r26_lines, collapse = "\n")

    # Check 1: No live tr_dates assignments (blocks should be deleted)
    tr_dates_assignments <- sum(grepl("tr_dates <- NULL", r26_lines, fixed = TRUE))
    check(
      glue("R/26 has no tr_dates <- NULL assignments (found {tr_dates_assignments})"),
      tr_dates_assignments == 0
    )

    # Check 2: No TR in any sources list
    tr_source_refs <- sum(grepl("TR = tr_dates", r26_lines, fixed = TRUE))
    check(
      glue("R/26 has no TR = tr_dates in sources lists (found {tr_source_refs})"),
      tr_source_refs == 0
    )

    # Check 3: No live TUMOR_REGISTRY_ALL calls (only in comments)
    # Lines that call get_pcornet_table("TUMOR_REGISTRY_ALL") should not exist
    # Comments (lines starting with #) are acceptable
    live_tr_lines <- r26_lines[!grepl("^\\s*#", r26_lines)]
    live_tr_refs <- sum(grepl("TUMOR_REGISTRY_ALL", live_tr_lines, fixed = TRUE))
    check(
      glue("R/26 has no live TUMOR_REGISTRY_ALL references (found {live_tr_refs})"),
      live_tr_refs == 0
    )

    # Check 4: Phase 76 removal comments present (one per extraction function)
    removal_comments <- sum(grepl("Phase 76: Tumor registry source removed", r26_lines, fixed = TRUE))
    check(
      glue("R/26 has 3 Phase 76 removal comments (found {removal_comments})"),
      removal_comments == 3
    )

    # Check 5: EPISODE_COUNT_BASELINE defined
    baseline_def <- sum(grepl("EPISODE_COUNT_BASELINE", r26_lines, fixed = TRUE))
    check(
      glue("R/26 defines EPISODE_COUNT_BASELINE (found {baseline_def} refs)"),
      baseline_def >= 2
    )

    # Check 6: Chemo sources list has 6 entries (no TR)
    # Find the chemo stack_and_dedup_with_codes call and verify source count
    chemo_section <- r26_text
    chemo_has_6 <- grepl(
      "PX = px_dates.*RX = rx_dates.*DX = dx_dates.*DRG = drg_dates.*DISP = disp_dates.*MA = ma_dates",
      chemo_section
    ) && !grepl("TR = tr_dates.*type_name = .Chemotherapy", chemo_section)
    check("Chemotherapy uses 6 sources (PX, RX, DX, DRG, DISP, MA)", chemo_has_6)

    # Check 7: Radiation sources list has 3 entries (no TR)
    rad_has_3 <- grepl(
      'sources = list\\(PX = px_dates, DX = dx_dates, DRG = drg_dates\\)',
      r26_text
    )
    check("Radiation uses 3 sources (PX, DX, DRG)", rad_has_3)

    # Check 8: SCT sources list has 2 entries (no TR)
    sct_has_2 <- grepl(
      'sources = list\\(PX = px_dates, DRG = drg_dates\\)',
      r26_text
    )
    check("SCT uses 2 sources (PX, DRG)", sct_has_2)
    ```

    **2. Update section counter from [1/18] through [18/18] to [1/19] through [19/19]:**
    - Every `message("\n[N/18]` becomes `message("\n[N/19]`
    - The new section uses `[19/19]`

    **3. Update the SECTION 14 summary block:**
    - Add to the validated requirements list at the end:
      ```r
      message("  * TREAT-01: Tumor registry source removed from treatment pipeline")
      ```

    **4. Update the SECTION 14 header comment (Purpose section at top of file):**
    - Update the Purpose comment to mention Phase 76 TR removal validation:
      Add after the Phase 72-73 mention: `Phase 76 TR removal (TREAT-01) validation.`
  </action>
  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && Rscript -e "parse('R/88_smoke_test_comprehensive.R')" && echo "PARSE OK"</automated>
    File must parse as valid R.

    Also verify:
    - `grep -c "TR removal validation" R/88_smoke_test_comprehensive.R` returns at least 1
    - `grep -c "19/19" R/88_smoke_test_comprehensive.R` returns at least 1
    - `grep -c "TREAT-01" R/88_smoke_test_comprehensive.R` returns at least 1
    - `grep -c "tr_dates <- NULL" R/88_smoke_test_comprehensive.R` returns at least 1 (inside the check pattern string)
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains `SECTION 13B: TR REMOVAL VALIDATION`
    - R/88_smoke_test_comprehensive.R contains `[19/19] TR removal validation`
    - All section counters updated from `/18]` to `/19]`
    - 8 new check() calls validating: no tr_dates assignments, no TR in sources, no live TUMOR_REGISTRY_ALL, 3 removal comments, EPISODE_COUNT_BASELINE defined, chemo 6 sources, radiation 3 sources, SCT 2 sources
    - Validated requirements list contains `TREAT-01: Tumor registry source removed`
    - File parses as valid R: `Rscript -e "parse('R/88_smoke_test_comprehensive.R')"` exits with code 0
  </acceptance_criteria>
  <done>
    Smoke test extended with 8 checks validating TR removal from R/26. Section counter updated to 19. TREAT-01 listed in validated requirements summary. File parses as valid R.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `Rscript -e "parse('R/26_treatment_episodes.R')"` exits 0 (valid R syntax)
2. `Rscript -e "parse('R/88_smoke_test_comprehensive.R')"` exits 0 (valid R syntax)
3. `grep -c "tr_dates <- NULL" R/26_treatment_episodes.R` returns 0
4. `grep -c "TR = tr_dates" R/26_treatment_episodes.R` returns 0
5. `grep -c "Phase 76: Tumor registry source removed" R/26_treatment_episodes.R` returns 3
6. `grep -c "SECTION 13B" R/88_smoke_test_comprehensive.R` returns 1
7. `grep -c "19/19" R/88_smoke_test_comprehensive.R` returns at least 1
</verification>

<success_criteria>
- TREAT-01 satisfied: All tumor registry treatment data sources removed from R/26 extraction functions
- QUAL-01 satisfied: Smoke test updated with TR removal validation, checkmate assertion added, Phase 76 decision traceability in header
- Chemotherapy: 7 sources reduced to 6 (PX, RX, DX, DRG, DISP, MA)
- Radiation: 4 sources reduced to 3 (PX, DX, DRG)
- SCT: 3 sources reduced to 2 (PX, DRG)
- Immunotherapy: unchanged at 2 sources (PX, DRG)
- Episode count assertion prevents >20% drop from going undetected
- Both modified files parse as valid R
</success_criteria>

<output>
After completion, create `.planning/phases/76-treatment-source-analysis-removal/76-01-SUMMARY.md`
</output>
