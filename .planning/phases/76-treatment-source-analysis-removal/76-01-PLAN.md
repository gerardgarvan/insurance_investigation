---
phase: 76-treatment-source-analysis-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/76_treatment_source_coverage.R
autonomous: true
requirements:
  - TREAT-01

must_haves:
  truths:
    - "Coverage analysis produces CSV and XLSX showing TR-only, claims-only, and both-source episode counts per treatment type"
    - "Chemotherapy, Radiation, and SCT each have TR overlap quantified"
    - "Immunotherapy reported as zero TR coverage (no TR source)"
    - "Output includes percentage of TR-only dates to assess data loss risk from removal"
  artifacts:
    - path: "R/76_treatment_source_coverage.R"
      provides: "Pre-removal coverage analysis quantifying TR vs claims overlap"
      contains: "anti_join"
    - path: "output/source_coverage_analysis.csv"
      provides: "Coverage summary table"
    - path: "output/source_coverage_analysis.xlsx"
      provides: "Multi-sheet coverage report with detail per treatment type"
  key_links:
    - from: "R/76_treatment_source_coverage.R"
      to: "R/00_config.R"
      via: "source() for TREATMENT_TYPES and CONFIG"
      pattern: "source.*00_config"
    - from: "R/76_treatment_source_coverage.R"
      to: "R/01_load_pcornet.R"
      via: "source() for get_pcornet_table()"
      pattern: "get_pcornet_table"
    - from: "R/76_treatment_source_coverage.R"
      to: "output/source_coverage_analysis.xlsx"
      via: "openxlsx2 wb_workbook() multi-sheet write"
      pattern: "wb_workbook|wb\\$save"
---

<objective>
Create a standalone coverage analysis script (R/76_treatment_source_coverage.R) that quantifies tumor registry (TR) vs claims-based source overlap before TR removal from the treatment pipeline.

Purpose: Roadmap success criterion #1 requires this analysis to document episode counts by source combination (TR-only, claims-only, both). The output (source_coverage_analysis.csv/xlsx) is also needed by the validation report (success criterion #3) to confirm that post-removal count reduction matches the coverage analysis prediction.

Output: R/76_treatment_source_coverage.R, output/source_coverage_analysis.csv, output/source_coverage_analysis.xlsx
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-treatment-source-analysis-removal/76-RESEARCH.md
@R/26_treatment_episodes.R
@R/00_config.R
@R/01_load_pcornet.R
</context>

<interfaces>
<!-- Key functions and constants the executor needs from existing codebase -->

From R/26_treatment_episodes.R:
- extract_chemo_dates_with_codes() — lines 120-232, TR block at lines 195-223
- extract_radiation_dates_with_codes() — lines 236-311, TR block at lines 277-305
- extract_sct_dates_with_codes() — lines 315-378, TR block at lines 341-372
- extract_immunotherapy_dates_with_codes() — lines 382-409, NO TR source
- stack_and_dedup_with_codes(sources, type_name) — line 93, accepts named list of source dataframes
- Each extraction function returns tibble: ID, treatment_date, triggering_code, ENCOUNTERID

From R/00_config.R:
- TREATMENT_TYPES <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy")
- CONFIG$output_dir — path to output directory
- CONFIG$cache$outputs_dir — path to cache/outputs directory

From R/01_load_pcornet.R:
- get_pcornet_table(table_name) — returns lazy DuckDB table or NULL if not available

From R/26 openxlsx2 pattern (lines 780-1113):
- wb_workbook() + wb$add_worksheet() + wb$add_data() + wb$add_fill() + wb$add_font()
- Header row: dark fill (#374151), white bold text
- Column widths: wb$set_col_widths()
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create coverage analysis script R/76_treatment_source_coverage.R</name>
  <files>R/76_treatment_source_coverage.R</files>
  <read_first>
    - R/26_treatment_episodes.R (full file — understand TR extraction blocks at lines 195-223, 277-305, 341-372 and claims extraction patterns)
    - R/00_config.R (TREATMENT_TYPES, CONFIG paths)
    - R/01_load_pcornet.R (get_pcornet_table pattern)
    - R/98_radiation_cpt_audit.R (diagnostic script header/structure pattern — first 30 lines for header template)
  </read_first>
  <action>
    Create R/76_treatment_source_coverage.R following the RESEARCH.md Pattern 2 structure and the diagnostic script conventions from R/98. This script quantifies TR vs claims overlap BEFORE TR removal.

    **Script structure:**

    **1. Header block (following R/98 diagnostic pattern):**
    ```r
    # ==============================================================================
    # 76_treatment_source_coverage.R -- Treatment Source Coverage Analysis
    # ==============================================================================
    # Purpose: Quantify tumor registry (TR) treatment coverage before removal (Phase 76).
    #          Produces per-treatment-type breakdown of TR-only, claims-only, and
    #          dual-source treatment dates to assess data loss risk from TR removal.
    #
    # Inputs:  PCORnet tables (TUMOR_REGISTRY_ALL, PROCEDURES, PRESCRIBING,
    #          DIAGNOSIS, ENCOUNTER, DISPENSING, MED_ADMIN) via R/01_load_pcornet.R
    #
    # Outputs: output/source_coverage_analysis.csv
    #          output/source_coverage_analysis.xlsx
    #
    # Dependencies: R/00_config.R, R/01_load_pcornet.R
    #
    # Requirements: TREAT-01 (Phase 76 — pre-removal coverage analysis)
    #
    # Decision traceability:
    #   D-76-COV-01: Coverage analysis runs BEFORE TR source removal
    #   D-76-COV-02: Uses dplyr anti_join/semi_join for set operations (per RESEARCH.md)
    #   D-76-COV-03: Output format matches openxlsx2 audit pattern from R/26
    # ==============================================================================
    ```

    **2. Setup section:**
    ```r
    suppressPackageStartupMessages({
      library(dplyr)
      library(tidyr)
      library(glue)
      library(openxlsx2)
      library(checkmate)
    })

    source("R/00_config.R")
    source("R/01_load_pcornet.R")
    ```

    **3. TR extraction helper functions:**
    Extract the TR date extraction logic from R/26's three extraction functions (chemo lines 195-223, radiation lines 277-305, SCT lines 341-372) into standalone helper functions. Each should return a tibble with columns (ID, treatment_date) using distinct() to deduplicate.

    For chemotherapy TR:
    - Use `get_pcornet_table("TUMOR_REGISTRY_ALL")`
    - Look for columns: `c("CHEMO_START_DATE_SUMMARY", "DT_CHEMO")`
    - pivot_longer, filter !is.na(treatment_date), distinct(ID, treatment_date)

    For radiation TR:
    - Look for columns: `c("RAD_START_DATE_SUMMARY", "DT_RAD")`
    - Same pivot + filter + distinct pattern

    For SCT TR:
    - Look for columns: `c("STEM_CELL_START_DATE_SUMMARY", "DT_STEM_CELL")`
    - Same pivot + filter + distinct pattern

    If TUMOR_REGISTRY_ALL doesn't exist or has no matching columns, return empty tibble.

    **4. Claims extraction helper functions:**
    Extract claims-only dates (excluding TR) from each treatment type. Reuse the SAME extraction logic as R/26's extraction functions but WITHOUT the TR block. For each type, stack all non-TR sources using bind_rows() and distinct(ID, treatment_date).

    For chemotherapy claims: PX, RX, DX, DRG, DISP, MA sources
    For radiation claims: PX, DX, DRG sources
    For SCT claims: PX, DRG sources

    IMPORTANT: Copy the extraction code from R/26 faithfully. Use the same filter conditions, same column selections, same code matching patterns (TREATMENT_CODES). The point is to measure EXACTLY what the pipeline uses.

    **5. Coverage analysis loop:**
    For each treatment type in c("Chemotherapy", "Radiation", "SCT"):
    ```r
    tr_dates <- extract_tr_dates(type)       # Helper from step 3
    claims_dates <- extract_claims_dates(type) # Helper from step 4

    # Set operations per RESEARCH.md Pattern 2
    tr_only <- anti_join(tr_dates, claims_dates, by = c("ID", "treatment_date"))
    claims_only <- anti_join(claims_dates, tr_dates, by = c("ID", "treatment_date"))
    both_sources <- semi_join(tr_dates, claims_dates, by = c("ID", "treatment_date"))

    # Build summary row
    tibble(
      treatment_type = type,
      tr_total_dates = nrow(tr_dates),
      claims_total_dates = nrow(claims_dates),
      tr_only_dates = nrow(tr_only),
      claims_only_dates = nrow(claims_only),
      both_sources_dates = nrow(both_sources),
      pct_tr_only = round(100 * nrow(tr_only) / max(nrow(tr_dates), 1), 1),
      pct_redundant = round(100 * nrow(both_sources) / max(nrow(tr_dates), 1), 1),
      tr_only_patients = n_distinct(tr_only$ID),
      both_patients = n_distinct(both_sources$ID)
    )
    ```

    For Immunotherapy: Include a row with all zeros and a note "No TR source (expected 0% coverage)" — per RESEARCH.md Open Question #3.

    Log each type's results with message():
    ```r
    message(glue("{type}: {nrow(tr_only)} TR-only dates ({pct_tr_only}%), {nrow(both_sources)} dates in both sources ({pct_redundant}%)"))
    ```

    **6. CSV output:**
    ```r
    coverage_summary <- bind_rows(coverage_results)
    COVERAGE_CSV <- file.path(CONFIG$output_dir, "source_coverage_analysis.csv")
    readr::write_csv(coverage_summary, COVERAGE_CSV)
    message(glue("Coverage analysis saved: {COVERAGE_CSV}"))
    ```

    **7. XLSX output (multi-sheet, following R/26 audit pattern):**
    - Sheet 1 "Summary": coverage_summary table with styled header row (dark fill #374151, white bold text)
    - Sheet 2 "Chemotherapy Detail": tr_only dates for chemo (patient ID, treatment_date) — up to 1000 rows with note if truncated
    - Sheet 3 "Radiation Detail": tr_only dates for radiation
    - Sheet 4 "SCT Detail": tr_only dates for SCT
    - Save to: `file.path(CONFIG$output_dir, "source_coverage_analysis.xlsx")`

    **8. Checkmate assertion:**
    After computing coverage_summary, add assertion:
    ```r
    checkmate::assert_true(
      nrow(coverage_summary) >= 3,
      .var.name = "[R/76] Coverage analysis must produce results for at least 3 treatment types"
    )
    ```

    **9. Final summary message:**
    ```r
    message("\n=== Coverage Analysis Complete ===")
    message(glue("Total TR-only dates across all types: {sum(coverage_summary$tr_only_dates)}"))
    message(glue("These dates will be LOST after TR removal"))
    message(glue("Output: {COVERAGE_CSV}"))
    message(glue("Output: {COVERAGE_XLSX}"))
    ```
  </action>
  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && Rscript -e "parse('R/76_treatment_source_coverage.R')" && echo "PARSE OK"</automated>
    File must parse as valid R.

    Also verify:
    - `grep -c "anti_join" R/76_treatment_source_coverage.R` returns at least 3 (one per treatment type with TR)
    - `grep -c "semi_join" R/76_treatment_source_coverage.R` returns at least 3
    - `grep -c "source_coverage_analysis" R/76_treatment_source_coverage.R` returns at least 2 (csv + xlsx paths)
    - `grep -c "TUMOR_REGISTRY_ALL" R/76_treatment_source_coverage.R` returns at least 1
    - `grep -c "checkmate" R/76_treatment_source_coverage.R` returns at least 1
    - `grep -c "D-76-COV" R/76_treatment_source_coverage.R` returns at least 3 (decision traceability)
  </verify>
  <done>
    R/76_treatment_source_coverage.R exists, parses as valid R, contains anti_join/semi_join coverage analysis for Chemotherapy, Radiation, and SCT, produces both CSV and XLSX output, includes checkmate assertion, follows diagnostic script header conventions.
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `Rscript -e "parse('R/76_treatment_source_coverage.R')"` exits 0 (valid R syntax)
2. `grep -c "anti_join" R/76_treatment_source_coverage.R` returns at least 3
3. `grep -c "source_coverage_analysis.csv" R/76_treatment_source_coverage.R` returns at least 1
4. `grep -c "source_coverage_analysis.xlsx" R/76_treatment_source_coverage.R` returns at least 1
5. `grep -c "TREAT-01" R/76_treatment_source_coverage.R` returns at least 1
</verification>

<success_criteria>
- Roadmap success criterion #1 addressed: Coverage analysis script produces source_coverage_analysis.csv showing episode counts by source combinations (TR-only, claims-only, both)
- XLSX output provides multi-sheet detail for review
- Script reuses same extraction logic as R/26 for accurate coverage measurement
- Immunotherapy included with zero TR coverage baseline
- Checkmate assertion validates output completeness
- File follows v2.0 standards (header, checkmate, decision traceability)
</success_criteria>

<output>
After completion, create `.planning/phases/76-treatment-source-analysis-removal/76-01-SUMMARY.md`
</output>
