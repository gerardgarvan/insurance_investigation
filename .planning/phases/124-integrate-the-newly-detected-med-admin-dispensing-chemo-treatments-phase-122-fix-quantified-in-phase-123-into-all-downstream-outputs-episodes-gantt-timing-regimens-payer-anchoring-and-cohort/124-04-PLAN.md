---
phase: 124-integrate-med-admin-dispensing-chemo-downstream
plan: 04
type: execute
wave: 4
depends_on: ["124-03"]
files_modified: []
autonomous: false
requirements: [D-01, D-03, D-10, D-11, D-13, D-14, D-15]
must_haves:
  truths:
    - "The pre-fix baseline is snapshotted on HiPerGator BEFORE any regeneration (treatment_episodes_pre_p124.rds + gantt_episodes_pre_p124.csv)"
    - "All in-scope downstream outputs (D-10) are regenerated in the correct dependency order with the expanded chemo sources"
    - "R/52 runs BEFORE R/101 and R/104 (Gantt chain), not trusting R/39 internal ordering"
    - "gantt_episodes.csv Source Table column contains DISPENSING and MED_ADMIN values (D-05 proven at runtime)"
    - "R/110 output_level_before_after_report.xlsx is produced and shows the output-level deltas (D-02)"
    - "Cohort membership is unchanged — total HL patient count matches the prior run (D-14: chemo is a flag, not a filter)"
    - "R/88 Section 15v (SMOKE-124-01) passes"
  artifacts:
    - path: "cache/outputs/treatment_episodes.rds"
      provides: "regenerated episodes with source_hints, canonical drug names, correct labels"
    - path: "output/output_level_before_after_report.xlsx"
      provides: "the D-02 Amy-ready deliverable"
  key_links:
    - from: "R/26 (Wave 1)"
      to: "R/28 -> R/29 -> R/52 -> R/101 -> R/104"
      via: "treatment_episodes.rds regeneration chain"
      pattern: "treatment_episodes.rds"
---

<objective>
Execute the full regeneration on HiPerGator (D-01/D-10) and confirm the Phase 122 fix flows
through to all final products, then generate the D-02 report. The Windows executor has NO
Rscript, so this plan is a human-action checkpoint: it hands the user an exact, ordered
runbook, the user runs it in RStudio on HiPerGator, and pastes back the confirmation numbers.
Mirrors the Phase 122/123 runtime-confirmation pattern.

Purpose: Plans 01-03 are structurally verified on Windows but NOT executed. This plan is the
only place actual output files are produced and D-02/D-05/D-14 are proven at runtime.
Output: regenerated in-scope outputs + output/output_level_before_after_report.xlsx, plus a
pasted-back confirmation block recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/124-integrate-the-newly-detected-med-admin-dispensing-chemo-treatments-phase-122-fix-quantified-in-phase-123-into-all-downstream-outputs-episodes-gantt-timing-regimens-payer-anchoring-and-cohort/124-CONTEXT.md
@.planning/phases/124-integrate-the-newly-detected-med-admin-dispensing-chemo-treatments-phase-122-fix-quantified-in-phase-123-into-all-downstream-outputs-episodes-gantt-timing-regimens-payer-anchoring-and-cohort/124-RESEARCH.md
@.planning/phases/124-integrate-the-newly-detected-med-admin-dispensing-chemo-treatments-phase-122-fix-quantified-in-phase-123-into-all-downstream-outputs-episodes-gantt-timing-regimens-payer-anchoring-and-cohort/124-01-PLAN.md
@.planning/phases/124-integrate-the-newly-detected-med-admin-dispensing-chemo-treatments-phase-122-fix-quantified-in-phase-123-into-all-downstream-outputs-episodes-gantt-timing-regimens-payer-anchoring-and-cohort/124-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: HiPerGator baseline snapshot + ordered regeneration + report generation</name>
  <read_first>
    - .planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-VERIFICATION.md (prior runtime-checkpoint pattern + baseline numbers)
    - .planning/phases/123-.../123-VERIFICATION.md (treatment_episodes.rds was ABSENT on HiPerGator at Phase 123 — R/26 MUST run first)
    - .planning/phases/124-.../124-RESEARCH.md (run order section + Pitfalls 4/5/6/7)
    - R/39_run_all_investigations.R (canonical order lines 136-165; note R/101/R/104 sit in investigation_scripts BEFORE R/52 — do NOT trust that ordering for a targeted run)
  </read_first>
  <what-built>
    Plans 01-03 added (structurally verified, not yet run on production data):
    - R/26 source_hints column + canonical raw-name fallback (Plan 01)
    - R/28 source_table/code_type override + R/20 code_type fix (Plan 02)
    - R/110 output-level before/after report + R/88 Section 15v (Plan 03)
    This checkpoint runs everything on HiPerGator and produces the final outputs.
  </what-built>
  <action>
    Present the ordered HiPerGator runbook (Steps 0-7 in <how-to-verify>) to the user and PAUSE
    — this is a checkpoint:human-action because the Windows executor has NO Rscript, so the
    pipeline can only run in RStudio on HiPerGator. Do NOT attempt to run any R script locally.

    The user must execute Steps 0-7 in the EXACT order shown:
    - STEP 0: snapshot cache/outputs/treatment_episodes.rds -> treatment_episodes_pre_p124.rds
      and output/gantt_episodes.csv -> gantt_episodes_pre_p124.csv FIRST (D-03 baseline; must
      predate regeneration or the D-02 diff is meaningless).
    - STEP 1: source("R/14_build_cohort.R") (auto-sources R/11 payer anchoring; D-14 flag-only).
    - STEP 2: source("R/26_treatment_episodes.R") BEFORE R/28/29/52/56 (Pitfall 7; R/26 sources
      R/25 internally — do NOT run R/25 separately, Pitfall 6).
    - STEP 3: source R/28 then R/29 (enrichment order).
    - STEP 4: source R/52 then R/101 then R/104 (STRICT Gantt order; do NOT trust R/39 order).
    - STEP 5: source R/76, R/20, R/36, R/56 (after R/29 — Pitfall 4), R/57.
    - STEP 6: source("R/110_output_level_before_after_report.R") LAST (D-02 report).
    - STEP 7: source("R/88_smoke_test_comprehensive.R") — confirm SMOKE-124-01 / Section 15v PASS.

    After the user runs the blocks, collect the 7 pasted-back confirmation values and record
    them VERBATIM in the SUMMARY. If any step errors, capture the error text and stop — do NOT
    fabricate downstream numbers. Do NOT run R/72/R/73/R/70/R/71 (D-11 out of scope) and do NOT
    edit chemo_rxnorm (D-13).
  </action>
  <how-to-verify>
    Run the following IN ORDER in RStudio on HiPerGator (module load R/4.4.2), from the project
    root. Each block is a hard dependency for the next (RESEARCH run-order + Pitfall 7:
    treatment_episodes.rds was absent, so R/26 MUST succeed before R/28/29/52/56 run).

    STEP 0 — BASELINE SNAPSHOT (D-03, do this FIRST, before any regeneration):
      # In R, project root:
      ep <- file.path("cache","outputs","treatment_episodes.rds")
      if (file.exists(ep)) file.copy(ep, file.path("cache","outputs","treatment_episodes_pre_p124.rds"), overwrite = FALSE)
      g  <- file.path("output","gantt_episodes.csv")
      if (file.exists(g))  file.copy(g,  file.path("output","gantt_episodes_pre_p124.csv"), overwrite = FALSE)
      # If treatment_episodes.rds is ABSENT (Phase 123 precedent), that's fine — the report's
      # "before" episode metrics will show the committed gantt_episodes_pre_p124.csv only; note it.

    STEP 1 — COHORT + PAYER (Chain 4; D-14 flag-only, membership must not change):
      source("R/14_build_cohort.R")   # auto-sources R/11 for treatment-anchored payer
      # RECORD: total HL patient count (should match the prior/committed cohort — D-14).

    STEP 2 — EPISODES (Chain 1; MUST run before 28/29/52/56 — Pitfall 7):
      source("R/26_treatment_episodes.R")   # sources R/25 internally — do NOT run R/25 separately (Pitfall 6)
      # RECORD: chemo episode count; confirm source_hints column present with DISPENSING/MED_ADMIN values.

    STEP 3 — ENRICHMENT (Chain 2, in order):
      source("R/28_episode_classification.R")   # regimen labels + source_table/code_type override
      source("R/29_first_line_and_death_analysis.R")

    STEP 4 — GANTT EXPORT (Chain 3, STRICT order R/52 -> R/101 -> R/104; do NOT use R/39 order):
      source("R/52_gantt_v2_export.R")          # writes output/gantt_episodes.csv (overwrites)
      source("R/101_gantt_lifespan_collapse.R") # consumes gantt_episodes.csv
      source("R/104_gantt_entire_history.R")    # consumes gantt_lifespan.csv + gantt_episodes.csv

    STEP 5 — INVESTIGATION / TABLES (Chain 5; R/56 after R/29 — Pitfall 4; R/76/R/20 self-contained — Pitfall 5):
      source("R/76_treatment_source_coverage.R")
      source("R/20_treatment_inventory.R")
      source("R/36_tableau_ready_tables.R")
      source("R/56_new_tables_from_groupings.R")
      source("R/57_drug_grouping_instances.R")

    STEP 6 — REPORT (D-02, LAST, after all regeneration):
      source("R/110_output_level_before_after_report.R")   # writes output/output_level_before_after_report.xlsx

    STEP 7 — SMOKE:
      source("R/88_smoke_test_comprehensive.R")   # confirm SMOKE-124-01 / Section 15v PASS

    Then verify and PASTE BACK:
    1. STEP 1 total HL patient count == prior cohort count (D-14 — unchanged membership). Value: ____
    2. STEP 2 chemo episode count and whether it INCREASED vs the pre_p124 baseline (D-01 integration flowed). Value: ____
    3. In output/gantt_episodes.csv, the Source Table column contains "DISPENSING" and "MED_ADMIN"
       (D-05). e.g. `table(read.csv("output/gantt_episodes.csv")$source_table)` — paste the unique
       Source Table values. Values: ____
    4. output/output_level_before_after_report.xlsx exists; paste the Summary sheet
       before/after/delta rows (D-02 headline). Values: ____
    5. Unmapped Names sheet row count (D-08). Value: ____
    6. R/88 SMOKE-124-01 / Section 15v result. PASS/FAIL: ____
    7. Confirm NO errors touched immuno branches / chemo_rxnorm was not edited (D-12/D-13) —
       expected: no immuno-related changes in the run log.
  </how-to-verify>
  <verify>
    Runtime confirmation is the paste-back itself (no Windows Rscript). Each acceptance_criterion
    is satisfied by a specific pasted-back value: cohort count unchanged (D-14), chemo episode
    count increased (D-01/D-10), gantt_episodes.csv Source Table includes DISPENSING+MED_ADMIN
    (D-05), report xlsx created with all 5 sheets (D-02/D-08), Section 15v PASS, and
    out-of-scope scripts/chemo_rxnorm untouched (D-11/D-13).
  </verify>
  <acceptance_criteria>
    - User pastes back: HL cohort count unchanged vs prior run (D-14)
    - User pastes back: chemo episode count increased vs pre_p124 baseline (D-01/D-10 integration flowed)
    - User pastes back: gantt_episodes.csv Source Table unique values include DISPENSING and MED_ADMIN (D-05)
    - User confirms: output/output_level_before_after_report.xlsx created with populated Summary + Regimen + Timing + Payer + Unmapped Names sheets (D-02/D-08)
    - User confirms: R/88 Section 15v (SMOKE-124-01) PASS
    - User confirms: no run touched R/72/R/73/R/70/R/71 (D-11 out of scope) and chemo_rxnorm unedited (D-13)
  </acceptance_criteria>
  <done>User has run Steps 0-7 on HiPerGator and pasted back all 7 confirmation values; output/output_level_before_after_report.xlsx exists; gantt_episodes.csv shows DISPENSING/MED_ADMIN Source Table values; cohort membership unchanged; SMOKE-124-01 PASS.</done>
  <resume-signal>Paste the 7 confirmation values above, or describe any errors/blockers encountered during the ordered run.</resume-signal>
</task>

</tasks>

<verification>
- This plan's verification IS the human runtime confirmation (no Windows Rscript).
- The paste-back numbers prove D-01 (integration flowed), D-05 (new Source Table values),
  D-02 (report produced), D-14 (membership unchanged), D-11/D-13 (out-of-scope untouched).
- Record all pasted-back values verbatim in the SUMMARY.
</verification>

<success_criteria>
- In-scope outputs (D-10) regenerated in dependency order (R/26 first; R/52->R/101->R/104; report last)
- gantt_episodes.csv shows DISPENSING/MED_ADMIN source_table values (D-05)
- output_level_before_after_report.xlsx produced (D-02) with HIPAA suppression (D-15)
- Cohort membership unchanged (D-14); out-of-scope scripts untouched (D-11); chemo_rxnorm unedited (D-13)
- SMOKE-124-01 passes
</success_criteria>

<output>
After completion, create `.planning/phases/124-integrate-the-newly-detected-med-admin-dispensing-chemo-treatments-phase-122-fix-quantified-in-phase-123-into-all-downstream-outputs-episodes-gantt-timing-regimens-payer-anchoring-and-cohort/124-04-SUMMARY.md`
Record all pasted-back runtime confirmation values verbatim.
</output>
