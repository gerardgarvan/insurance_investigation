---
phase: 124-integrate-med-admin-dispensing-chemo-downstream
plan: 03
type: execute
wave: 3
depends_on: ["124-02"]
files_modified:
  - R/110_output_level_before_after_report.R
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements: [D-02, D-03, D-08, D-09, D-15]
must_haves:
  truths:
    - "A new script R/110 produces an Amy-ready styled multi-sheet xlsx comparing pre-fix vs regenerated OUTPUT-level metrics"
    - "The report covers # treatment episodes, # patients with any chemo episode, first-line regimen-label distribution, first-chemo timing shifts, and payer-anchor window changes (D-02)"
    - "The before baseline reads the committed pre-Phase-122 gantt CSVs and the snapshotted treatment_episodes_pre_p124.rds; after reads the freshly regenerated artifacts (D-03)"
    - "A dedicated Unmapped Names audit sheet lists raw/cleaned drug names with no canonical mapping (D-08)"
    - "All patient counts 1-10 are HIPAA-suppressed via suppress_small() (D-15)"
    - "R/110 is registered in SCRIPT_INDEX (NOT R/39) and has an R/88 Section 15v smoke test"
  artifacts:
    - path: "R/110_output_level_before_after_report.R"
      provides: "output-level before/after diff + unmapped-name audit xlsx"
      min_lines: 200
      contains: "output/output_level_before_after_report.xlsx"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15v (SMOKE-124-01) structural checks for R/110"
      contains: "SMOKE-124-01"
    - path: "R/SCRIPT_INDEX.md"
      provides: "R/110 row in Post-Renumber Investigations (100+)"
      contains: "R/110"
  key_links:
    - from: "R/110"
      to: "cache/outputs/treatment_episodes_pre_p124.rds + treatment_episodes.rds"
      via: "readRDS before/after"
      pattern: "treatment_episodes_pre_p124"
    - from: "R/110"
      to: "output/output_level_before_after_report.xlsx"
      via: "add_styled_sheet + wb_save"
      pattern: "add_styled_sheet"
---

<objective>
Create the phase headline deliverable: a NEW output-level before/after comparison report
(D-02) plus an unmapped-drug-name audit sheet (D-08), delivered as a single Amy-ready
styled multi-sheet xlsx, mirroring the R/109 / R/51 pattern. Register it (SCRIPT_INDEX +
R/88 Section 15v), NOT in R/39 (one-off analysis, like R/109).

Phase 123 quantified the SOURCE-level impact (+1,328 patients / +13,762 dates). This report
proves the wired consumers actually changed the FINAL PRODUCTS (output-level): treatment
episode counts, patients with any chemo episode, first-line regimen distribution, first-chemo
timing shifts, and payer-anchor windows — before (pre-Phase-122) vs after (regenerated).

Baseline mechanism (D-03, Claude's Discretion — decided): the committed gantt_episodes.csv
predates the Phase 122 fix (git 07184cd patched consumers AFTER those CSVs were committed) —
valid output-level "before" for Gantt/regimen/timing metrics. For episode-level RDS metrics,
Plan 04 snapshots cache/outputs/treatment_episodes.rds to treatment_episodes_pre_p124.rds on
HiPerGator BEFORE R/26 runs. R/110 reads both pre and post side-by-side. No separate snapshot
script — Plan 04's HiPerGator file.copy() creates the baseline.

Purpose: single unmistakable proof-of-integration artifact for Amy.
Output: R/110_output_level_before_after_report.R + R/88 + SCRIPT_INDEX edits. Structurally
verifiable on Windows; the xlsx itself is produced at runtime in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/124-integrate-the-newly-detected-med-admin-dispensing-chemo-treatments-phase-122-fix-quantified-in-phase-123-into-all-downstream-outputs-episodes-gantt-timing-regimens-payer-anchoring-and-cohort/124-CONTEXT.md
@.planning/phases/124-integrate-the-newly-detected-med-admin-dispensing-chemo-treatments-phase-122-fix-quantified-in-phase-123-into-all-downstream-outputs-episodes-gantt-timing-regimens-payer-anchoring-and-cohort/124-RESEARCH.md

<interfaces>
<!-- Extracted from R/109 (2026-07-14). R/110 mirrors this exactly. -->

R/109 styled-xlsx pattern (R/109_med_admin_dispensing_fix_impact_audit.R):
  suppress_small (line 111): HIPAA helper — patient counts 1-10 -> NA (D-15). Copy verbatim.
  add_styled_sheet(wb, sheet_name, title, subtitle_text, data) (line 717): header fill
    FF374151, white font FFFFFFFF, Calibri 11 bold, data at row 4, freeze at row 5. Copy verbatim.
  wb <- wb_workbook(); wb <- add_styled_sheet(wb, ...) per sheet; wb_save(wb, OUT_XLSX).
  IS_LOCAL (R/00_config.R): TRUE on Windows, FALSE on HiPerGator. Gate any runtime read that
    needs cache/outputs RDS behind file.exists() so the script parses on Windows.

Baseline / regenerated artifacts to compare:
  BEFORE:
    - output/gantt_episodes.csv  (committed, pre-Phase-122; read.csv colClasses="character")
      Columns include: patient_id, treatment_type, regimen_label (or equivalent), source_table,
      code_type, treatment_line, episode_start ... (20 cols). Read defensively with any_of().
    - cache/outputs/treatment_episodes_pre_p124.rds  (snapshot created by Plan 04 on HiPerGator)
  AFTER:
    - cache/outputs/treatment_episodes.rds  (regenerated by R/26+R/28+R/29 in Plan 04)
    - output/gantt_episodes.csv AFTER regeneration (Plan 04 runs R/52 which overwrites it) —
      so R/110 must run AFTER Plan 04 regeneration, reading the pre-snapshot for "before".

  Because R/52 OVERWRITES gantt_episodes.csv, the pre-fix CSV must be snapshotted too. Plan 04
  will copy output/gantt_episodes.csv -> output/gantt_episodes_pre_p124.csv BEFORE regeneration.
  R/110 reads *_pre_p124.* for BEFORE and the live files for AFTER.

R/00_config.R: canonicalize_drug_name(), MEDICATION_LOOKUP, DRUG_NAME_ALIASES (for D-08
  unmapped detection: a raw/cleaned name is "unmapped" if canonicalize_drug_name(cleaned)
  returns the input unchanged AND cleaned is not a MEDICATION_LOOKUP value).

R/88 smoke section sequence: 15t = Phase 122 (SMOKE-122-01), 15u = Phase 123 (SMOKE-123-01,
  R/109). NEXT is Section 15v / SMOKE-124-01. Summary lines currently at lines 4394-4395.

SCRIPT_INDEX Post-Renumber Investigations (100+): currently 10 entries (R/100..R/109);
  count "10", grand "Total: 95" (lines 207-211). R/110 is the next free number.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/110 output-level before/after report + unmapped-name audit xlsx</name>
  <read_first>
    - R/109_med_admin_dispensing_fix_impact_audit.R (FULL read — copy suppress_small (line 111), add_styled_sheet (lines 717-756), header/IS_LOCAL/wb_save structure verbatim)
    - R/51_post_death_encounter_investigation.R (styled-xlsx + HIPAA suppression reference)
    - R/79_drug_name_consistency_audit.R (template for the D-08 two-column-plus-counts unmapped-name audit sheet)
    - R/00_config.R (canonicalize_drug_name lines 2413-2419; MEDICATION_LOOKUP lines 2295-2365; IS_LOCAL flag)
    - R/52_gantt_v2_export.R (lines 144-165 — EPISODES_SCHEMA 20-col names so R/110 reads gantt_episodes.csv by correct column names)
  </read_first>
  <action>
    Create R/110_output_level_before_after_report.R. Header block must state: Phase 124 D-02
    output-level before/after report + D-08 unmapped-name audit; read-only analysis; NOT wired
    into R/39; does NOT modify R/26/R/28/treatment_episodes.rds/cohort (mirror R/109 header
    lines 30-55). Self-bootstrap: source R/00_config.R for helpers/paths; guard every
    cache/outputs read behind file.exists() so the script PARSES on Windows (cache/outputs is
    empty locally) and RUNS on HiPerGator.

    Copy VERBATIM from R/109: suppress_small() (D-15 HIPAA), add_styled_sheet(), the
    wb_workbook()/wb_save() flow, and the FF374151/FFFFFFFF styling constants.

    Define paths:
      EPISODES_AFTER  <- here::here("cache","outputs","treatment_episodes.rds")
      EPISODES_BEFORE <- here::here("cache","outputs","treatment_episodes_pre_p124.rds")
      GANTT_AFTER     <- here::here("output","gantt_episodes.csv")
      GANTT_BEFORE    <- here::here("output","gantt_episodes_pre_p124.csv")
      OUT_XLSX        <- here::here("output","output_level_before_after_report.xlsx")

    Build these sheets (each via add_styled_sheet), all patient counts suppress_small()-wrapped:

    Sheet 1 "Summary" — headline before/after with delta columns:
      metric | before | after | delta
      Rows: total treatment episodes; total chemo episodes; patients with any chemo episode;
      patients with any DISPENSING-sourced chemo episode; patients with any MED_ADMIN-sourced
      chemo episode. Compute from EPISODES_BEFORE vs EPISODES_AFTER (readRDS, filter
      treatment_type=="Chemotherapy"). Source columns via source_table string-contains
      "DISPENSING"/"MED_ADMIN" (the Plan 02 labels). Guard: if EPISODES_BEFORE missing,
      emit a single-row placeholder noting the snapshot was absent (do not error).

    Sheet 2 "Regimen Distribution" — first-line regimen-label distribution before vs after
      (D-02, D-09 silent aggregate): regimen_label | n_before | n_after | delta. Filter to
      first-line chemo episodes (is_first_line flag if present via any_of; else all chemo).
      Include the NA/unclassified bucket.

    Sheet 3 "First-Chemo Timing Shift" — per-patient earliest chemo episode_start before vs
      after; report distribution of shift in days (patients earlier / same / later; median
      days earlier). suppress_small() on patient counts.

    Sheet 4 "Payer-Anchor Window" — before/after change in the treatment-anchored payer window.
      Read the payer-at-chemo artifact if present (from R/11/R/14 output; guard with
      file.exists/any_of). If the artifact is unavailable at report time, emit a documented
      placeholder row (the payer-anchor numbers can be filled from R/11 output on HiPerGator);
      do NOT block the report.

    Sheet 5 "Unmapped Names" (D-08) — from EPISODES_AFTER detail (or treatment_episode_detail.rds
      via any_of guard), collect drug-name strings that are UNMAPPED: cleaned = toupper(trimws(x));
      unmapped if canonicalize_drug_name(x) returns x unchanged AND cleaned is not among
      unname(MEDICATION_LOOKUP) canonical values. Columns:
      code | raw_name | cleaned_name_in_output | source_table | n_episodes | n_patients
      (n_patients suppress_small()). Model layout on R/79. This is the SME review list; do not
      resolve names here (D-08).

    Console summary at end: >= 4 message() lines prefixed "P124" or "OUTLVL" reporting sheet
    row counts and the OUT_XLSX path (mirror R/109's end summary), so the Plan 04 runtime
    checkpoint has parseable confirmation output.

    Named-predicate style; no data.table needed here (dplyr/tidyverse). No cohort filtering
    (D-14 — read-only report). Do NOT edit chemo_rxnorm (D-13).
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows executor. Structural grep verification below; xlsx produced + confirmed in Plan 04 (HiPerGator).</automated>
  </verify>
  <acceptance_criteria>
    - File R/110_output_level_before_after_report.R exists and is >= 200 lines
    - `grep -n "treatment_episodes_pre_p124" R/110_output_level_before_after_report.R` returns >= 1 hit (BEFORE baseline)
    - `grep -n "gantt_episodes_pre_p124" R/110_output_level_before_after_report.R` returns >= 1 hit
    - `grep -n "add_styled_sheet" R/110_output_level_before_after_report.R` returns >= 6 hits (defn + 5 sheets)
    - `grep -n "suppress_small" R/110_output_level_before_after_report.R` returns >= 5 hits (HIPAA on every patient count, D-15)
    - `grep -n "output/output_level_before_after_report.xlsx\|output_level_before_after_report.xlsx" R/110_output_level_before_after_report.R` returns >= 1 hit
    - `grep -n "canonicalize_drug_name" R/110_output_level_before_after_report.R` returns >= 1 hit (D-08 unmapped detection)
    - `grep -ni "regimen" R/110_output_level_before_after_report.R` returns hits (Sheet 2)
    - `grep -ni "payer" R/110_output_level_before_after_report.R` returns hits (Sheet 4)
    - `grep -n "file.exists" R/110_output_level_before_after_report.R` returns >= 2 hits (Windows-parse guards)
  </acceptance_criteria>
  <done>R/110 builds a 5-sheet before/after + unmapped-name xlsx reading pre_p124 baselines vs regenerated artifacts, HIPAA-suppressed, parseable on Windows via file.exists guards.</done>
</task>

<task type="auto">
  <name>Task 2: Register R/110 — SCRIPT_INDEX row + R/88 Section 15v smoke test</name>
  <read_first>
    - R/SCRIPT_INDEX.md (lines 140-211: Post-Renumber Investigations (100+) table + counts/totals)
    - R/88_smoke_test_comprehensive.R (Section 15u = Phase 123 / R/109; the summary lines 4394-4395; the Section-15u check block preceding them — copy its structure for 15v)
    - R/110_output_level_before_after_report.R (Task 1 output — so the smoke checks grep for strings that actually exist in R/110)
  </read_first>
  <action>
    (a) SCRIPT_INDEX.md: add an R/110 row to the Post-Renumber Investigations (100+) table
    (after the R/109 row at line 155). Description: "Read-only Phase 124 output-level
    before/after report + D-08 unmapped-name audit (NOT wired into R/39, mirrors R/107/108/109).
    Compares pre-Phase-122 baseline (treatment_episodes_pre_p124.rds + gantt_episodes_pre_p124.csv)
    vs regenerated artifacts across treatment-episode counts, chemo-patient counts, first-line
    regimen distribution, first-chemo timing shift, and payer-anchor window; adds an Unmapped
    Names SME-review sheet. Single styled openxlsx2 workbook output/output_level_before_after_report.xlsx,
    HIPAA-suppressed. Phase: 124." Update the count line 208 from "10" to "11" and add R/110 to
    the parenthetical list; update grand "Total:" line 211 from 95 to 96.

    (b) R/88 Section 15v: add a new structural-check block (continue the 15t -> 15u -> 15v
    sequence) that self-reads R/110 source and asserts it contains the load-bearing strings.
    Include >= 12 checks, e.g.: file exists; contains "treatment_episodes_pre_p124"; contains
    "gantt_episodes_pre_p124"; >= 6 add_styled_sheet calls; >= 5 suppress_small calls; contains
    OUT_XLSX path string; contains "canonicalize_drug_name"; regimen sheet; payer sheet; timing
    shift; >= 2 file.exists guards; NOT wired into R/39 (grep R/39 does NOT contain "R/110" —
    negative check). Follow the EXACT assertion form used in the existing Section 15u block
    (read it first and mirror its PASS/FAIL accounting so the total stays honest). Register a
    per-check PASS count and an else-branch SKIPPED accounting like prior sections.
    Add a summary line AFTER line 4395:
      message("  * SMOKE-124-01: R/88 validates Phase 124 R/110 output-level before/after report + unmapped-name audit structural integrity (Section 15v, N checks)")
    where N is the actual number of checks you wrote.

    Do NOT wire R/110 into R/39_run_all_investigations.R (D: one-off analysis, like R/109) —
    the negative smoke check enforces this.
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows executor. Structural grep verification below; runtime (15v PASS) confirmed in Plan 04 (HiPerGator).</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "R/110" R/SCRIPT_INDEX.md` returns a new table row
    - SCRIPT_INDEX 100+ count updated: `grep -n "Post-renumber investigations (100+):\*\* 11\|(100+):\*\* 11" R/SCRIPT_INDEX.md` OR read line ~208 shows 11
    - SCRIPT_INDEX grand total updated: read line ~211 shows "Total:** 96"
    - `grep -n "SMOKE-124-01" R/88_smoke_test_comprehensive.R` returns 1 hit (summary line after 4395)
    - `grep -n "Section 15v\|15v" R/88_smoke_test_comprehensive.R` returns the new section marker
    - `grep -n "treatment_episodes_pre_p124" R/88_smoke_test_comprehensive.R` returns >= 1 hit (a Section 15v check greps R/110 for it)
    - R/110 NOT in R/39: `grep -n "R/110\|110_output_level" R/39_run_all_investigations.R` returns NOTHING
  </acceptance_criteria>
  <done>R/110 registered in SCRIPT_INDEX (count 11 / total 96) and covered by R/88 Section 15v (SMOKE-124-01, >=12 checks including a negative "not in R/39" check); not wired into R/39.</done>
</task>

</tasks>

<verification>
- Both tasks are structural (new script + registration edits) verified by grep on Windows.
- Actual xlsx generation and Section 15v PASS happen in Plan 04 on HiPerGator.
- D-15 HIPAA suppression present in R/110; D-08 audit sheet present; D-02/D-09 metrics present.
</verification>

<success_criteria>
- R/110 exists, reads pre_p124 baselines vs regenerated artifacts, 5 sheets incl. D-08 audit (D-02/D-08)
- HIPAA suppression via suppress_small throughout (D-15)
- Registered in SCRIPT_INDEX + R/88 15v; NOT in R/39
- Baseline mechanism = committed pre-fix CSV + HiPerGator RDS snapshot (D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/124-integrate-the-newly-detected-med-admin-dispensing-chemo-treatments-phase-122-fix-quantified-in-phase-123-into-all-downstream-outputs-episodes-gantt-timing-regimens-payer-anchoring-and-cohort/124-03-SUMMARY.md`
</output>
