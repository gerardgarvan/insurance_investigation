---
phase: 40-investigate-unmatched-ndc-codes
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - R/40_investigate_unmatched_ndc.R
  - R/00_config.R
autonomous: true
requirements: [D-10, D-11]

must_haves:
  truths:
    - "New NDC vectors (chemo_ndc, supportive_care_ndc, etc.) are added to TREATMENT_CODES in R/00_config.R"
    - "Existing chemo_rxnorm vector is expanded with newly discovered treatment-relevant RXNORM CUIs"
    - "New RXNORM vectors are created for categories that need them (immunotherapy_rxnorm, sct_rxnorm, supportive_care_rxnorm)"
    - "R/00_config.R remains valid R after programmatic update (parse + source validates)"
    - "A backup is created before modification and rolled back on validation failure"
  artifacts:
    - path: "R/40_investigate_unmatched_ndc.R"
      provides: "update_config_ndc_codes() function added"
      contains: "update_config_ndc_codes"
    - path: "R/00_config.R"
      provides: "TREATMENT_CODES with new NDC vectors and expanded RXNORM"
      contains: "chemo_ndc"
  key_links:
    - from: "R/40_investigate_unmatched_ndc.R"
      to: "R/00_config.R"
      via: "readLines/writeLines programmatic update with parse/source validation"
      pattern: 'update_config_ndc_codes'
    - from: "R/40_investigate_unmatched_ndc.R"
      to: "output/unmatched_ndc_classified.rds"
      via: "readRDS() to load Plan 01 classification results"
      pattern: 'readRDS.*unmatched_ndc_classified'
---

<objective>
Add config update function to R/40_investigate_unmatched_ndc.R that reads the classified RDS artifact from Plan 01, creates new NDC vectors (chemo_ndc, supportive_care_ndc, immunotherapy_ndc, sct_ndc) and expands existing/creates new RXNORM vectors in TREATMENT_CODES, with parse/source validation and backup-rollback safety.

Purpose: Integrate discovered drug codes into the config so downstream treatment detection scripts can match on NDC and expanded RXNORM codes.

Output: Updated R/00_config.R with new TREATMENT_CODES vectors, updated R/40_investigate_unmatched_ndc.R with config update function
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-investigate-unmatched-ndc-codes/40-CONTEXT.md
@.planning/phases/40-investigate-unmatched-ndc-codes/40-01-SUMMARY.md

@R/40_investigate_unmatched_ndc.R
@R/39_investigate_unmatched.R
@R/00_config.R

<interfaces>
<!-- Phase 39's config update function is the template. Key patterns: -->

From R/39_investigate_unmatched.R (lines 480-725):
```r
update_config_treatment_codes <- function(classified_codes_path) {
  # 1. Load classified codes from RDS
  classified <- readRDS(classified_codes_path)

  # 2. Filter to treatment-relevant (exclude "Unrelated")
  treatment_codes_new <- classified %>%
    filter(classification != "Unrelated") %>%
    select(code, classification, description)

  # 3. Map classifications to vector names
  category_map <- c(
    "Chemotherapy" = "chemo_hcpcs",
    "Radiation" = "radiation_cpt",
    "SCT" = "sct_cpt",
    "Immunotherapy" = "cart_icd10pcs_prefixes",
    "Supportive Care" = "supportive_care_hcpcs"
  )

  # 4. Read config, create backup
  config_path <- "R/00_config.R"
  backup_path <- paste0(config_path, ".bak")
  file.copy(config_path, backup_path, overwrite = TRUE)
  config_lines <- readLines(config_path)

  # 5. For each category: find vector, add codes before closing paren
  #    or create new vector if it doesn't exist

  # 6. Validate: writeLines -> parse() -> source() -> check TREATMENT_CODES
  #    Rollback from backup on failure

  # 7. Log summary
}
```

Key pattern details:
- Vector detection: `grep("^\\s*{vec_name}\\s*=\\s*c\\(", config_lines, perl = TRUE)`
- Closing paren detection: loop from vec_start to find line matching `"^\\s*\\)"`
- Existing code extraction: `str_extract_all(line, '"([^"]+)"')` to deduplicate
- Last line comma fix: ensure last existing code line has trailing comma before appending
- New vector creation: insert block before a known anchor vector (e.g., chemo_revenue)
- Validation: parse(config_path) + source(config_path, local = new.env()) + check TREATMENT_CODES not NULL
- Rollback: file.copy(backup_path, config_path, overwrite = TRUE) on error

From R/00_config.R TREATMENT_CODES structure:
```r
TREATMENT_CODES <- list(
  chemo_hcpcs = c(...),
  chemo_rxnorm = c("3639", "11213", "67228", "3946"),  # expand this
  radiation_cpt = c(...),
  sct_cpt = c(...),
  sct_hcpcs = c(...),
  # ... ICD codes ...
  # NEW vectors go here (before chemo_revenue):
  # chemo_ndc = c(...),
  # supportive_care_ndc = c(...),
  # immunotherapy_ndc = c(...),
  # immunotherapy_rxnorm = c(...),
  # sct_ndc = c(...),
  # sct_rxnorm = c(...),
  # supportive_care_rxnorm = c(...),
  supportive_care_hcpcs = c(...),
  chemo_revenue = c(...),
  radiation_revenue = c(...),
  sct_revenue = c(...)
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config update function and wire into main execution</name>
  <files>R/40_investigate_unmatched_ndc.R, R/00_config.R</files>

  <read_first>
    R/40_investigate_unmatched_ndc.R
    R/39_investigate_unmatched.R
    R/00_config.R
  </read_first>

  <action>
Add SECTION 8 to R/40_investigate_unmatched_ndc.R with `update_config_ndc_codes()` function, and add Step 6 to SECTION 7 main execution to call it.

**SECTION 8: UPDATE TREATMENT_CODES IN R/00_config.R (per D-10, D-11)**

Create `update_config_ndc_codes(classified_codes_path)` function following Phase 39's `update_config_treatment_codes()` as exact template, adapted for NDC/RXNORM vectors:

1. **Load classified codes:** `readRDS(classified_codes_path)`
2. **Filter:** Exclude "Unrelated" and "Radiation" (unlikely for drug codes). Keep Chemotherapy, Supportive Care, Immunotherapy, SCT-related. Select columns: code, code_type, classification, drug_name.
3. **Category-to-vector mapping** (per D-10, D-11 — separate vectors per category AND code type):
   ```r
   # Map (classification, code_type) -> TREATMENT_CODES vector name
   # NDC vectors (D-10: new vectors)
   ndc_category_map <- c(
     "Chemotherapy" = "chemo_ndc",
     "Supportive Care" = "supportive_care_ndc",
     "Immunotherapy" = "immunotherapy_ndc",
     "SCT-related" = "sct_ndc"
   )
   # RXNORM vectors (D-11: expand existing or create new)
   rxnorm_category_map <- c(
     "Chemotherapy" = "chemo_rxnorm",          # Expand existing (4 CUIs)
     "Supportive Care" = "supportive_care_rxnorm",  # Create new
     "Immunotherapy" = "immunotherapy_rxnorm",       # Create new
     "SCT-related" = "sct_rxnorm"                    # Create new
   )
   ```
4. **Read R/00_config.R and create backup:**
   ```r
   config_path <- "R/00_config.R"
   backup_path <- paste0(config_path, ".bak")
   file.copy(config_path, backup_path, overwrite = TRUE)
   config_lines <- readLines(config_path)
   ```
5. **Process each category for each code type (NDC then RXNORM):**
   For each (category, code_type, vec_name) combination:
   - Get new codes for this combination from classified data
   - Skip if no codes
   - Look for existing vector: `grep(glue("^\\s*{vec_name}\\s*=\\s*c\\("), config_lines, perl = TRUE)`
   - **If vector exists (chemo_rxnorm case):**
     - Find closing paren by looping from vec_start to find `"^\\s*\\)"`
     - Extract existing codes via `str_extract_all(line, '"([^"]+)"')`
     - Filter new codes to exclude already-existing
     - Ensure last data line has trailing comma after closing quote: `sub('(.*")', '\\1,', config_lines[last_data_idx])` only if no comma after quote
     - Build insert lines with Phase 40 attribution comments:
       ```r
       glue('    "{code}",   # Phase 40: {substr(drug_name, 1, 40)}')
       ```
       Last line omits trailing comma.
     - Insert before closing paren
   - **If vector doesn't exist (all NDC vectors, new RXNORM vectors):**
     - Find insertion anchor: look for `supportive_care_hcpcs` vector position, or if not found, `chemo_revenue`
     - Insert new vector block BEFORE the anchor:
       ```r
       c("",
         glue("  # {category} {code_type} codes (Phase 40: drug investigation)"),
         glue("  {vec_name} = c("),
         # ... one line per code with comment ...
         "  ),",
         "")
       ```
6. **Validate updated config** (critical safety step):
   ```r
   writeLines(config_lines, config_path)
   validation_error <- tryCatch({
     parse(config_path)
     env <- new.env()
     source(config_path, local = env)
     if (is.null(env$TREATMENT_CODES)) stop("TREATMENT_CODES is NULL")
     # Verify each new vector exists
     for (vec_name in c(ndc_vectors_added, rxnorm_vectors_added)) {
       if (is.null(env$TREATMENT_CODES[[vec_name]])) {
         warning(glue("Vector {vec_name} is NULL after update"))
       }
     }
     NULL
   }, error = function(e) e$message)
   ```
7. **Rollback on failure:**
   ```r
   if (!is.null(validation_error)) {
     message(glue("  Config update failed: {validation_error}"))
     message("  Restoring backup...")
     file.copy(backup_path, config_path, overwrite = TRUE)
     stop(glue("Config validation failed: {validation_error}"))
   }
   ```
8. **Cleanup and log:**
   ```r
   message("  Config update validated successfully")
   file.remove(backup_path)
   # Log summary of changes per vector
   ```

**Update SECTION 7 main execution:**
After Step 5 (save RDS), add:
```r
# Step 6: Update R/00_config.R with new codes
message("")
message("Step 6: Updating R/00_config.R with new treatment codes...")
update_config_ndc_codes(RDS_PATH)
```
  </action>

  <verify>
    <automated>Rscript -e "parse('R/40_investigate_unmatched_ndc.R')" && Rscript -e "parse('R/00_config.R')"</automated>
  </verify>

  <acceptance_criteria>
    - R/40_investigate_unmatched_ndc.R contains `update_config_ndc_codes <- function(`
    - R/40_investigate_unmatched_ndc.R contains `SECTION 8` header comment
    - Function contains `readRDS(classified_codes_path)` to load Plan 01 output
    - Function contains `chemo_ndc` in the category map (per D-10)
    - Function contains `supportive_care_ndc` in the category map (per D-10)
    - Function contains `chemo_rxnorm` in the RXNORM category map (per D-11)
    - Function contains `supportive_care_rxnorm` in the RXNORM category map
    - Function contains `file.copy(config_path, backup_path` (backup creation)
    - Function contains `parse(config_path)` (validation step)
    - Function contains `source(config_path, local =` (validation step)
    - Function contains rollback logic: `file.copy(backup_path, config_path` on error
    - Function contains `file.remove(backup_path)` on success
    - Main execution (SECTION 7) contains `Step 6` calling update_config_ndc_codes
    - R/40_investigate_unmatched_ndc.R parse check passes
    - R/00_config.R parse check passes (not broken by any changes)
  </acceptance_criteria>

  <done>
R/40_investigate_unmatched_ndc.R has a complete update_config_ndc_codes() function that reads the classified RDS, creates new NDC vectors (chemo_ndc, supportive_care_ndc, immunotherapy_ndc, sct_ndc) and new/expanded RXNORM vectors (chemo_rxnorm expanded, supportive_care_rxnorm, immunotherapy_rxnorm, sct_rxnorm created) in R/00_config.R, with parse/source validation and backup-rollback safety. The function is wired into the main execution as Step 6. Both files parse cleanly.
  </done>
</task>

</tasks>

<verification>
- `Rscript -e "parse('R/40_investigate_unmatched_ndc.R')"` exits 0
- `Rscript -e "parse('R/00_config.R')"` exits 0
- R/40_investigate_unmatched_ndc.R contains update_config_ndc_codes function with full validation pipeline
- Category maps include both NDC and RXNORM vector names
- Backup/rollback pattern matches Phase 39 proven approach
</verification>

<success_criteria>
- update_config_ndc_codes() function exists and follows Phase 39 template
- Handles both new vector creation (NDC) and existing vector expansion (RXNORM)
- Parse/source validation with rollback on failure
- Main execution calls the update function as Step 6
- Both modified files parse cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/40-investigate-unmatched-ndc-codes/40-02-SUMMARY.md`
</output>
