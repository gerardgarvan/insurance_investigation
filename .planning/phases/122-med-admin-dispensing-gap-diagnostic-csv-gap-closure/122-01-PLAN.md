---
phase: 122-med-admin-dispensing-gap-diagnostic-csv-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/01_load_pcornet.R
  - R/utils/utils_treatment.R
  - tests/fixtures/MED_ADMIN_Mailhot_V1.csv
  - tests/fixtures/DISPENSING_Mailhot_V1.csv
  - data/reference/ndc_rxnorm_crosswalk.rds
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07]

must_haves:
  truths:
    - "R/01 DISPENSING_SPEC and MED_ADMIN_SPEC no longer declare a phantom RXNORM_CUI column (vroom can no longer inject an all-NA column)"
    - "A shared get_chemo_hits(table_name, chemo_rxnorm, ndc_crosswalk) helper exists in utils_treatment.R implementing PRESCRIBING via RXNORM_CUI, MED_ADMIN via MEDADMIN_TYPE=='RX'+'ND', DISPENSING via NDC crosswalk"
    - "A load_ndc_crosswalk() helper exists returning a named char vector or character(0) with a message when the RDS is absent (graceful, never crashes)"
    - "The MED_ADMIN and DISPENSING fixtures match the real extract layout (no RXNORM_CUI; DISPENSING also has no RAW_DISPENSE_MED_NAME) and exercise BOTH the RX-direct and NDC-crosswalk paths"
    - "A synthetic crosswalk RDS exists so the DISPENSING NDC path and MED_ADMIN ND path run locally without a network call"
  artifacts:
    - path: "R/utils/utils_treatment.R"
      provides: "get_chemo_hits() + load_ndc_crosswalk() + normalize_ndc() shared helpers"
      contains: "get_chemo_hits"
    - path: "R/01_load_pcornet.R"
      provides: "DISPENSING_SPEC / MED_ADMIN_SPEC without phantom RXNORM_CUI"
      contains: "D-12 revised Phase 122"
    - path: "tests/fixtures/MED_ADMIN_Mailhot_V1.csv"
      provides: "corrected MED_ADMIN fixture with RX + ND + NI rows, no RXNORM_CUI"
    - path: "tests/fixtures/DISPENSING_Mailhot_V1.csv"
      provides: "corrected DISPENSING fixture with NDC hit + NDC miss, no RXNORM_CUI, no RAW_DISPENSE_MED_NAME"
    - path: "data/reference/ndc_rxnorm_crosswalk.rds"
      provides: "synthetic NDC->RxCUI named vector so local NDC path exercises"
  key_links:
    - from: "R/utils/utils_treatment.R get_chemo_hits('MED_ADMIN')"
      to: "TREATMENT_CODES$chemo_rxnorm"
      via: "MEDADMIN_TYPE=='RX' & MEDADMIN_CODE %in% chemo_rxnorm"
      pattern: "MEDADMIN_TYPE == \"RX\""
    - from: "R/utils/utils_treatment.R get_chemo_hits('DISPENSING')"
      to: "ndc_crosswalk"
      via: "named-vector NDC lookup then %in% chemo_rxnorm"
      pattern: "ndc_crosswalk\\[NDC\\]"
---

<objective>
Establish the foundation for the MED_ADMIN/DISPENSING chemo-detection gap fix:
remove the root-cause phantom `RXNORM_CUI` col_spec declarations from R/01, add the
shared `get_chemo_hits()` + `load_ndc_crosswalk()` helpers to `utils_treatment.R`, and
correct the two fixtures (plus a synthetic crosswalk RDS) so the RX-direct AND
NDC-crosswalk paths both exercise locally.

Purpose: Every consumer patch in Plan 02 calls this helper, so it must exist first.
Removing the phantom col_spec is the root-cause fix (vroom silently injects an all-NA
RXNORM_CUI column when declared but absent, which is why the old guard passed on
fixtures but silently produced zero rows in production).

Output: Corrected R/01 col_specs, new helper functions, corrected fixtures, synthetic
crosswalk RDS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-CONTEXT.md
@.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-RESEARCH.md

<interfaces>
<!-- Verified from codebase. Executor should use these directly. -->

R/01_load_pcornet.R DISPENSING_SPEC current phantom declaration (line 322):
```r
RAW_NDC = col_character(),
RXNORM_CUI = col_character(), # KEY: chemo matching per D-12   <-- REMOVE THIS LINE
DISPENSE_SOURCE = col_character(),
RAW_DISPENSE_MED_NAME = col_character(),   <-- ALSO not in real extract; keep spec harmless but note
```

R/01_load_pcornet.R MED_ADMIN_SPEC current phantom declaration (line 345):
```r
MEDADMIN_ROUTE = col_character(),
RXNORM_CUI = col_character(), # KEY: chemo matching per D-12   <-- REMOVE THIS LINE
RAW_MEDADMIN_MED_NAME = col_character(),
```

R/107 verified-correct MED_ADMIN RX access (lift this pattern):
```r
ma_tbl %>%
  filter(MEDADMIN_TYPE == "RX",
         MEDADMIN_CODE %in% TREATMENT_CODES$chemo_rxnorm,
         !is.na(MEDADMIN_START_DATE)) %>%
  select(ID, treatment_date = MEDADMIN_START_DATE, triggering_code = MEDADMIN_CODE) %>%
  collect()
```

Existing utils_treatment.R already defines safe_table(), empty_result(), get_hl_patient_ids().
It is sourced via R/00_config.R so consumers get the new helpers for free.

MED_ADMIN fixture current header:
MEDADMINID,ID,ENCOUNTERID,PRESCRIBINGID,MEDADMIN_CODE,MEDADMIN_TYPE,MEDADMIN_START_DATE,MEDADMIN_STOP_DATE,MEDADMIN_ROUTE,RXNORM_CUI,RAW_MEDADMIN_MED_NAME,SOURCE

DISPENSING fixture current header:
DISPENSINGID,PRESCRIBINGID,ID,DISPENSE_DATE,NDC,DISPENSE_SUP,DISPENSE_AMT,DISPENSE_DOSE_DISP,DISPENSE_DOSE_DISP_UNIT,DISPENSE_ROUTE,RAW_NDC,RXNORM_CUI,DISPENSE_SOURCE,RAW_DISPENSE_MED_NAME,SOURCE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove phantom RXNORM_CUI col_spec declarations + revise D-12 comments in R/01</name>
  <files>R/01_load_pcornet.R</files>
  <read_first>
    - R/01_load_pcornet.R lines 145-159 (PRESCRIBING_SPEC — leave RXNORM_CUI intact here), lines 305-348 (DISPENSING_SPEC + MED_ADMIN_SPEC)
    - 122-RESEARCH.md "D-12 Revision Scope" table + "Critical insight on root-cause mechanism" (line 351)
  </read_first>
  <action>
    In R/01_load_pcornet.R:
    1. In DISPENSING_SPEC: DELETE the line `RXNORM_CUI = col_character(), # KEY: chemo matching per D-12` (currently line 322). Do NOT touch NDC (col_character stays) or RAW_NDC. Leave RAW_DISPENSE_MED_NAME as-is in the spec (harmless if absent) but the consumer patch in Plan 02 stops referencing it.
    2. In MED_ADMIN_SPEC: DELETE the line `RXNORM_CUI = col_character(), # KEY: chemo matching per D-12` (currently line 345). Keep MEDADMIN_CODE and MEDADMIN_TYPE declarations.
    3. Update the block comment above DISPENSING_SPEC (line 307) from `# RXNORM_CUI is the key matching column for chemo drugs (D-12: no NDC matching)` to `# NDC is the key column in this extract; RXNORM_CUI absent (D-12 revised Phase 122: NDC->RxNorm crosswalk used for chemo matching)`.
    4. Update the block comment above MED_ADMIN_SPEC (line 332) from `# RXNORM_CUI is the key matching column for chemo drugs (D-12: no NDC matching)` to `# MEDADMIN_CODE + MEDADMIN_TYPE encode the drug code; RXNORM_CUI absent (D-12 revised Phase 122: RX-typed=RxNorm CUI, ND-typed=NDC via crosswalk)`.
    Do NOT modify PRESCRIBING_SPEC — PRESCRIBING genuinely has RXNORM_CUI.
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows executor. Verify structurally: (a) grep -c 'RXNORM_CUI' R/01_load_pcornet.R returns 2 (only the two PRESCRIBING lines: RXNORM_CUI + RAW_RXNORM_CUI remain), (b) grep 'D-12 revised Phase 122' R/01_load_pcornet.R returns 2 matches. HiPerGator runtime confirmation deferred to Plan 03 user step.</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "RXNORM_CUI = col_character" R/01_load_pcornet.R returns exactly 1 (PRESCRIBING only; RAW_RXNORM_CUI is a separate string)
    - grep -n "D-12 revised Phase 122" R/01_load_pcornet.R returns exactly 2 lines (DISPENSING + MED_ADMIN comments)
    - grep -n "RXNORM_CUI = col_character(), # KEY: chemo matching per D-12" R/01_load_pcornet.R returns 0 matches
    - PRESCRIBING_SPEC still contains "RXNORM_CUI = col_character()" (grep confirms it is under the PRESCRIBING block, lines ~145-159)
  </acceptance_criteria>
  <done>Both phantom declarations removed; both D-12 comments revised; PRESCRIBING_SPEC untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add get_chemo_hits() + load_ndc_crosswalk() + normalize_ndc() to utils_treatment.R</name>
  <files>R/utils/utils_treatment.R</files>
  <read_first>
    - R/utils/utils_treatment.R lines 1-60 (existing safe_table/empty_result/get_hl_patient_ids patterns + header format)
    - 122-RESEARCH.md "Pattern 1" (get_chemo_hits full body, lines 96-177), "Pattern 3" (load_ndc_crosswalk, lines 202-219), "NDC Format Normalization" (lines 322-333), "Open Questions" #1 (ENCOUNTERID handling, lines 570-573)
    - R/107_med_admin_dispensing_gap_diagnostic.R lines 155-260 (verified correct MED_ADMIN RX access + PRESCRIBING baseline logic)
  </read_first>
  <action>
    Append three functions to R/utils/utils_treatment.R (after existing functions, keeping the roxygen-comment header style):

    (a) normalize_ndc(ndc): `stringr::str_remove_all(ndc, "-") |> stringr::str_pad(width = 11, side = "left", pad = "0")`. Vectorized.

    (b) load_ndc_crosswalk(): read `here::here("data", "reference", "ndc_rxnorm_crosswalk.rds")`. If !file.exists -> message (two lines: not found + "Run R/108 on HiPerGator to build it") and return character(0). Else readRDS + message with length, return the named vector. NEVER stop().

    (c) get_chemo_hits(table_name, chemo_rxnorm, ndc_crosswalk = NULL) returning tibble(ID, treatment_date, triggering_code) or NULL. Use the exact Pattern-1 body from RESEARCH.md with these adaptations:
       - Use safe_table(table_name) (already in this file) instead of inline tryCatch(get_pcornet_table).
       - PRESCRIBING branch: filter RXNORM_CUI %in% chemo_rxnorm, treatment_date = coalesce(RX_ORDER_DATE, RX_START_DATE), triggering_code = RXNORM_CUI.
       - DISPENSING branch: guard `!"NDC" %in% colnames(tbl)` -> message + return NULL. Collect ID, treatment_date=DISPENSE_DATE, NDC (filter !is.na on both). If ndc_crosswalk null/empty -> message + return NULL. Else mutate rxcui = ndc_crosswalk[normalize_ndc(NDC)], filter !is.na(rxcui) & rxcui %in% chemo_rxnorm, select ID, treatment_date, triggering_code = rxcui.
       - MED_ADMIN branch: require c("MEDADMIN_TYPE","MEDADMIN_CODE","MEDADMIN_START_DATE"); if any missing -> message + NULL. rx_hits: MEDADMIN_TYPE=="RX" & MEDADMIN_CODE %in% chemo_rxnorm & !is.na(MEDADMIN_START_DATE) -> triggering_code = MEDADMIN_CODE. nd_hits (only if crosswalk non-empty): MEDADMIN_TYPE=="ND" & !is.na(MEDADMIN_CODE) & !is.na(MEDADMIN_START_DATE), mutate rxcui = ndc_crosswalk[normalize_ndc(MEDADMIN_CODE)], filter !is.na(rxcui) & rxcui %in% chemo_rxnorm -> triggering_code = rxcui. bind_rows(rx_hits, nd_hits).
       - Do NOT select ENCOUNTERID inside the helper (Open Question 1 — DISPENSING may lack it). Callers that need ENCOUNTERID add it after. Add a roxygen note: "ENCOUNTERID intentionally omitted; callers add it if the source has it."
       - Final: after building the per-table result, apply `distinct(ID, treatment_date, triggering_code)` inside each branch's return to pre-dedup RX/ND overlap (Pitfall 4).
    Load order: utils_treatment.R is sourced by R/00_config.R; TREATMENT_CODES and get_pcornet_table are available at call time (not source time), so referencing them inside function bodies is safe.
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows. Structural grep verification (state-machine brace balance per Phase 119-120 style): confirm the three functions parse-balance and contain the required expressions. HiPerGator runtime deferred to Plan 03 user step.</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^get_chemo_hits <- function" R/utils/utils_treatment.R returns 1
    - grep -c "^load_ndc_crosswalk <- function" R/utils/utils_treatment.R returns 1
    - grep -c "^normalize_ndc <- function" R/utils/utils_treatment.R returns 1
    - grep 'MEDADMIN_TYPE == "RX"' R/utils/utils_treatment.R returns >=1 match
    - grep 'MEDADMIN_TYPE == "ND"' R/utils/utils_treatment.R returns >=1 match
    - grep "ndc_crosswalk\[normalize_ndc" R/utils/utils_treatment.R returns >=2 matches (DISPENSING + MED_ADMIN ND)
    - grep "return(character(0))" R/utils/utils_treatment.R returns >=1 (graceful crosswalk-absent)
    - grep "ENCOUNTERID" R/utils/utils_treatment.R inside get_chemo_hits returns 0 select() references (only the roxygen note)
    - open/close paren and brace counts balanced across the three new functions (state-machine check)
  </acceptance_criteria>
  <done>Three helpers appended; get_chemo_hits handles all 3 tables with graceful absence guards; NDC paths normalize before lookup; RX/ND pre-dedup present.</done>
</task>

<task type="auto">
  <name>Task 3: Correct MED_ADMIN + DISPENSING fixtures and add synthetic crosswalk RDS</name>
  <files>tests/fixtures/MED_ADMIN_Mailhot_V1.csv, tests/fixtures/DISPENSING_Mailhot_V1.csv, data/reference/ndc_rxnorm_crosswalk.rds</files>
  <read_first>
    - tests/fixtures/MED_ADMIN_Mailhot_V1.csv (current header + rows)
    - tests/fixtures/DISPENSING_Mailhot_V1.csv (current header + rows)
    - 122-RESEARCH.md "Fixture Corrections" (lines 356-386) + Open Question 3 (lines 580-583)
    - R/00_config.R TREATMENT_CODES$chemo_rxnorm (to pick a real chemo CUI for the RX fixture row and the crosswalk target)
  </read_first>
  <action>
    1. MED_ADMIN_Mailhot_V1.csv: rewrite header to drop RXNORM_CUI ->
       MEDADMINID,ID,ENCOUNTERID,PRESCRIBINGID,MEDADMIN_CODE,MEDADMIN_TYPE,MEDADMIN_START_DATE,MEDADMIN_STOP_DATE,MEDADMIN_ROUTE,RAW_MEDADMIN_MED_NAME,SOURCE
       Provide rows that exercise every branch (reuse existing ID values already in the fixture so joins still work):
       - >=1 row MEDADMIN_TYPE=RX with MEDADMIN_CODE = a real chemo_rxnorm CUI (e.g. doxorubicin CUI from TREATMENT_CODES$chemo_rxnorm) -> should be a hit
       - >=1 row MEDADMIN_TYPE=ND with MEDADMIN_CODE = the synthetic NDC used in the crosswalk RDS (below) -> should be a hit via crosswalk
       - >=1 row MEDADMIN_TYPE=NI (or OT) with an arbitrary code -> must be filtered out
       - >=1 row MEDADMIN_TYPE=RX with a NON-chemo code -> must be filtered out
       Realign every data row to the new 11-column layout (delete the old RXNORM_CUI column value).
    2. DISPENSING_Mailhot_V1.csv: rewrite header to drop RXNORM_CUI AND RAW_DISPENSE_MED_NAME ->
       DISPENSINGID,PRESCRIBINGID,ID,DISPENSE_DATE,NDC,DISPENSE_SUP,DISPENSE_AMT,DISPENSE_DOSE_DISP,DISPENSE_DOSE_DISP_UNIT,DISPENSE_ROUTE,RAW_NDC,DISPENSE_SOURCE,SOURCE
       Rows:
       - >=1 row NDC = the synthetic crosswalk NDC (maps to a chemo CUI) -> hit
       - >=1 row NDC = an NDC NOT in the crosswalk -> graceful miss (filtered by !is.na(rxcui))
       Realign every data row to the new 13-column layout.
    3. Create data/reference/ndc_rxnorm_crosswalk.rds: a named character vector mapping the synthetic 11-digit NDC(s) used in the fixtures to a chemo CUI that is in TREATMENT_CODES$chemo_rxnorm. Since the Windows executor has no Rscript, generate the RDS via the Bash tool using a portable method IF R is unavailable there too — if neither R nor Rscript is available on the executor, instead COMMIT a placeholder note and defer RDS creation to the Plan 03 HiPerGator step, BUT still create it if any R binary exists. Preferred: write a tiny R one-liner run through whatever R is on PATH: `saveRDS(setNames(c("<chemoCUI>"), c("<11digitNDC>")), "data/reference/ndc_rxnorm_crosswalk.rds")`. If no R binary exists on the executor, document in the SUMMARY that the synthetic RDS must be created on HiPerGator before the local NDC-path smoke check can pass, and have load_ndc_crosswalk() degrade gracefully (already does).
       Use the SAME 11-digit normalized NDC string as the fixture NDC values (run them through normalize_ndc mentally: strip hyphens, left-pad to 11).
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows. Structural: grep fixture headers confirm columns dropped/kept; confirm RX+ND+NI rows present in MED_ADMIN; confirm hit+miss NDC rows in DISPENSING; confirm RDS file exists (or SUMMARY documents deferral). HiPerGator runtime (helper returns non-zero hits from fixtures) confirmed by user in Plan 03.</automated>
  </verify>
  <acceptance_criteria>
    - head -1 tests/fixtures/MED_ADMIN_Mailhot_V1.csv does NOT contain "RXNORM_CUI" and DOES contain "MEDADMIN_CODE" and "MEDADMIN_TYPE"
    - head -1 tests/fixtures/DISPENSING_Mailhot_V1.csv does NOT contain "RXNORM_CUI" and does NOT contain "RAW_DISPENSE_MED_NAME" and DOES contain "NDC"
    - grep -c ",RX," tests/fixtures/MED_ADMIN_Mailhot_V1.csv returns >=1 ; grep -c ",ND," returns >=1 ; grep for NI or OT returns >=1
    - every data row column count matches the new header count (awk -F, 'NR>1{print NF}' shows constant 11 for MED_ADMIN, 13 for DISPENSING)
    - data/reference/ndc_rxnorm_crosswalk.rds exists OR 122-01-SUMMARY.md documents the HiPerGator-deferred RDS creation with the exact NDC->CUI mapping to use
  </acceptance_criteria>
  <done>Both fixtures reflect the real extract layout and exercise RX-direct + NDC-crosswalk + filtered-out branches; synthetic crosswalk RDS created (or deferral documented).</done>
</task>

</tasks>

<verification>
Structural (local, Windows executor — no Rscript, per Phases 119-120 convention):
- R/01: exactly one `RXNORM_CUI = col_character` (PRESCRIBING); two `D-12 revised Phase 122` comments.
- utils_treatment.R: get_chemo_hits / load_ndc_crosswalk / normalize_ndc defined and brace-balanced.
- Fixtures: headers corrected; branch-exercising rows present; column counts consistent.

Runtime (HiPerGator, user-performed, confirmed in Plan 03):
- Sourcing R/00_config.R loads the three new helpers without error.
- get_chemo_hits("MED_ADMIN", TREATMENT_CODES$chemo_rxnorm, load_ndc_crosswalk()) on fixtures returns >=1 RX hit.
</verification>

<success_criteria>
- Root-cause phantom col_spec removed (no vroom all-NA injection possible).
- Shared helper exists and is the single point all consumers will call.
- Fixtures no longer mask the bug; they exercise both code paths.
</success_criteria>

<output>
After completion, create `.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-01-SUMMARY.md`
</output>
