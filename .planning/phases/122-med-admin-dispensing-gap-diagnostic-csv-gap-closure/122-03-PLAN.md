---
phase: 122-med-admin-dispensing-gap-diagnostic-csv-gap-closure
plan: 03
type: execute
wave: 2
depends_on: ["122-01", "122-02"]
files_modified:
  - R/108_build_ndc_rxnorm_crosswalk.R
autonomous: false
requirements: [D-02, D-03]
user_setup:
  - service: RxNav API (rxnav.nlm.nih.gov)
    why: "One-time NDC->RxCUI crosswalk build; network access available on HiPerGator only (Windows executor has no Rscript and no production NDC values)"
    dashboard_config:
      - task: "Run Rscript R/108_build_ndc_rxnorm_crosswalk.R on HiPerGator to populate data/reference/ndc_rxnorm_crosswalk.rds, then commit the RDS"
        location: "HiPerGator RStudio / SLURM"
      - task: "Re-run Rscript R/88 (Section 15t 14/14) and re-run R/107 to confirm the MED_ADMIN/DISPENSING increment now flows into episodes"
        location: "HiPerGator RStudio"

must_haves:
  truths:
    - "R/108 harvests distinct NDCs from DISPENSING + MED_ADMIN ND-typed rows, resolves each via RxNav rxcui.json?idtype=NDC, and writes a named-vector RDS to data/reference/ndc_rxnorm_crosswalk.rds"
    - "R/108 normalizes NDCs to 11-digit no-hyphen before lookup and before storing keys (leading-zero-safe)"
    - "R/108 is offline-capable after build (all consumers load the RDS; no network at pipeline run) and is registered in SCRIPT_INDEX only (not R/39, mirroring R/107)"
    - "The crosswalk RDS being absent does NOT break Wave-1 code (load_ndc_crosswalk degrades gracefully to character(0))"
  artifacts:
    - path: "R/108_build_ndc_rxnorm_crosswalk.R"
      provides: "one-time NDC->RxNorm crosswalk builder (HiPerGator, network)"
      contains: "rxcui.json?idtype=NDC"
      min_lines: 80
  key_links:
    - from: "R/108"
      to: "data/reference/ndc_rxnorm_crosswalk.rds"
      via: "saveRDS(setNames(rxcui_vec, ndc_vec))"
      pattern: "saveRDS\\(.*ndc_rxnorm_crosswalk"
    - from: "R/108"
      to: "RxNav rxcui.json?idtype=NDC"
      via: "httr2 request per distinct NDC with retry + sleep"
      pattern: "idtype=NDC"
---

<objective>
Create the one-time NDC->RxNorm crosswalk builder R/108 that populates
`data/reference/ndc_rxnorm_crosswalk.rds`. This activates the DISPENSING and MED_ADMIN
ND-typed chemo contributions that the Wave-1 helper already reads (but which stay
gracefully inert until the RDS exists).

Purpose: D-02 requires NDC-coded records (DISPENSING NDC, MED_ADMIN MEDADMIN_TYPE=='ND')
to resolve to RxNorm and match chemo_rxnorm. This is HiPerGator-only (network + real
production NDC values), hence its own wave and a user-performed run step.

Output: R/108 script (committed by executor) + the RDS (built + committed by the user
on HiPerGator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-CONTEXT.md
@.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-RESEARCH.md
@.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-01-PLAN.md

<interfaces>
<!-- RxNav endpoint verified in R/27 lines 214-261: -->
GET https://rxnav.nlm.nih.gov/REST/rxcui.json?idtype=NDC&id={ndc_11digit}
Response path: data$idGroup$rxnormId[[1]]  (character or NULL)

<!-- normalize_ndc() and load_ndc_crosswalk() already exist in utils_treatment.R (Plan 01). -->
<!-- R/107 is the precedent for a SCRIPT_INDEX-only, HiPerGator-run, non-R/39 script. -->
<!-- Verified lookup shape (RESEARCH Code Examples): httr2 request + req_timeout(10) + req_retry(max_tries=3, transient 429/503/504) + resp_body_json. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/108_build_ndc_rxnorm_crosswalk.R</name>
  <files>R/108_build_ndc_rxnorm_crosswalk.R</files>
  <read_first>
    - R/107_med_admin_dispensing_gap_diagnostic.R (self-bootstrap DuckDB pattern, header format, HiPerGator-run convention, ID column usage)
    - R/27_drug_name_resolution.R lines 214-261 (verified RxNav httr2 lookup + retry + sleep pattern)
    - R/utils/utils_treatment.R normalize_ndc() + load_ndc_crosswalk() (Plan 01)
    - 122-RESEARCH.md "Pattern 2" (R/108, lines 179-196), "Code Examples" (lines 460-503), Open Question 2 (distinct-NDC count / progress counter)
  </read_first>
  <action>
    Create R/108_build_ndc_rxnorm_crosswalk.R following the R/107 header + self-bootstrap style:
    1. Header block: Purpose (one-time NDC->RxNorm crosswalk build, HiPerGator-only, offline-after-build), Inputs (DISPENSING.NDC + MED_ADMIN MEDADMIN_TYPE=='ND'.MEDADMIN_CODE), Outputs (data/reference/ndc_rxnorm_crosswalk.rds + output/ndc_rxnorm_crosswalk_audit.csv), Dependencies (httr2, purrr, dplyr, stringr, glue), Requirements (D-02, D-03). Note it is NOT in R/39 (data-prep, not investigation) and IS registered in SCRIPT_INDEX (done in Plan 02).
    2. Self-bootstrap DuckDB connection guarded (USE_DUCKDB + open_pcornet_con if pcornet_con absent), mirroring R/107/R/27.
    3. Harvest distinct NDCs: from DISPENSING (NDC column, !is.na, != "") and from MED_ADMIN where MEDADMIN_TYPE=='ND' (MEDADMIN_CODE as NDC). Union + distinct. Apply normalize_ndc() to every value. Report the distinct-NDC count via message BEFORE any API calls (Open Question 2).
    4. lookup_ndc_to_rxcui(ndc, sleep_sec=0.1): use the RESEARCH "Code Examples" body verbatim (httr2 request, req_timeout(10), req_retry max_tries=3 transient 429/503/504, resp_body_json, data$idGroup$rxnormId[[1]] or NA_character_; Sys.sleep at end). Wrap in tryCatch -> NA_character_.
    5. Batch: rxcui_vec <- purrr::map_chr(ndc_vec, lookup_ndc_to_rxcui) with a progress message every 100 calls. Build crosswalk <- setNames(rxcui_vec, ndc_vec); drop NA misses.
    6. Write: saveRDS(crosswalk, here::here("data","reference","ndc_rxnorm_crosswalk.rds")). Also write output/ndc_rxnorm_crosswalk_audit.csv with columns NDC, rxcui, lookup_status (matched/miss).
    7. Final message: n distinct NDCs, n matched, n misses, output paths.
    Use dplyr/stringr/glue only (no data.table). Keys stored are the 11-digit normalized form so load_ndc_crosswalk() consumers (which also normalize before lookup) match.
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows and the build itself requires HiPerGator network. Structural grep + brace balance locally; the actual build + RDS population is the user HiPerGator step below.</automated>
  </verify>
  <acceptance_criteria>
    - R/108_build_ndc_rxnorm_crosswalk.R exists with >=80 lines
    - grep "idtype=NDC" R/108_build_ndc_rxnorm_crosswalk.R returns >=1
    - grep "saveRDS" R/108_build_ndc_rxnorm_crosswalk.R returns >=1 and references ndc_rxnorm_crosswalk
    - grep 'MEDADMIN_TYPE == "ND"' R/108_build_ndc_rxnorm_crosswalk.R returns >=1
    - grep "normalize_ndc" R/108_build_ndc_rxnorm_crosswalk.R returns >=1
    - grep "req_retry" R/108_build_ndc_rxnorm_crosswalk.R returns >=1 (retry pattern from R/27)
    - grep -c "data.table\|\\bDT\\[" R/108_build_ndc_rxnorm_crosswalk.R returns 0 (tidyverse only)
    - brace/paren state-machine balanced
  </acceptance_criteria>
  <done>R/108 created: harvests+normalizes NDCs, RxNav lookup with retry, writes RDS + audit CSV, tidyverse-only, brace-balanced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HiPerGator runtime confirmation (build crosswalk, run R/88 Section 15t, re-run R/107)</name>
  <read_first>
    - R/108_build_ndc_rxnorm_crosswalk.R (built in Task 1)
    - R/88_smoke_test_comprehensive.R Section 15t (built in Plan 02)
    - R/107_med_admin_dispensing_gap_diagnostic.R (the baseline diagnostic)
    - 122-CONTEXT.md sized-impact numbers (+1,139 patients / +10,752 dates)
  </read_first>
  <what-built>
    R/108 crosswalk builder plus the full Wave-1 fix (Plans 01-02): col_spec fix,
    get_chemo_hits helper, 7 patched consumers, corrected fixtures, R/88 Section 15t.
    All structurally verified locally; runtime confirmation requires HiPerGator (the
    Windows executor has no Rscript).
  </what-built>
  <action>
    PAUSE for the user to perform the HiPerGator run (no Rscript on the Windows executor).
    Present the how-to-verify steps below. Do not proceed until the user reports back
    the distinct-NDC count, matched/miss counts, Section 15t pass tally, and whether
    R/107 now shows non-zero DISPENSING/MED_ADMIN-ND contributions.
  </action>
  <how-to-verify>
    On HiPerGator (module load R/4.4.2; RStudio or SLURM):
    1. Build the crosswalk: `Rscript R/108_build_ndc_rxnorm_crosswalk.R`
       - Expect: a printed distinct-NDC count, progress every 100 calls, then
         "n matched / n misses" and both output paths. Commit
         data/reference/ndc_rxnorm_crosswalk.rds.
    2. Run the smoke test: `Rscript R/88_smoke_test_comprehensive.R`
       - Expect: Section 15t reports 14/14 PASS (including the now-present RDS check
         and, if run with IS_LOCAL fixtures, the get_chemo_hits RX-hit assertion).
    3. Re-run the diagnostic: `Rscript R/107_med_admin_dispensing_gap_diagnostic.R`
       - Expect: the MED_ADMIN RX increment (~+1,139 patients / +10,752 dates baseline)
         now flows through, and DISPENSING + MED_ADMIN ND rows now contribute non-zero
         chemo hits (previously zero). Confirm the increment appears in episode/timing
         outputs when those are regenerated.
    4. Spot-check a known chemo NDC in the RDS resolves to a CUI in
       TREATMENT_CODES$chemo_rxnorm.
    Report back: the distinct-NDC count, matched/miss counts, Section 15t pass tally,
    and whether R/107 now shows non-zero DISPENSING/MED_ADMIN-ND contributions.
  </how-to-verify>
  <verify>
    <automated>MISSING — HiPerGator-only (no Rscript on Windows executor). User confirms Section 15t 14/14 PASS and R/107 non-zero DISPENSING/MED_ADMIN-ND contributions.</automated>
  </verify>
  <acceptance_criteria>
    - User reports data/reference/ndc_rxnorm_crosswalk.rds built + committed with a stated matched-NDC count
    - User reports R/88 Section 15t 14/14 PASS
    - User reports R/107 now shows non-zero DISPENSING and MED_ADMIN-ND chemo contributions (previously zero)
  </acceptance_criteria>
  <done>User has run the three HiPerGator steps and confirmed the crosswalk is built, Section 15t passes 14/14, and the NDC paths now contribute.</done>
  <resume-signal>Type "approved" with the reported counts, or describe any check that failed.</resume-signal>
</task>

</tasks>

<verification>
Structural (local): R/108 exists, tidyverse-only, RxNav+retry+normalize+saveRDS present, brace-balanced.
Runtime (HiPerGator, user): crosswalk RDS built + committed; R/88 Section 15t 14/14; R/107 shows
non-zero DISPENSING/MED_ADMIN-ND chemo contributions and the MED_ADMIN RX increment flowing through.
Graceful degradation: with the RDS absent (pre-build), Wave-1 code runs without error
(load_ndc_crosswalk returns character(0); NDC paths skip with a message).
</verification>

<success_criteria>
- D-02 satisfied: NDC-coded DISPENSING + MED_ADMIN ND rows resolve to RxNorm and match chemo_rxnorm once the RDS is built.
- Offline-after-build: no network access during any pipeline run; consumers load the RDS.
- SCRIPT_INDEX-only registration (not R/39), mirroring R/107.
</success_criteria>

<output>
After completion, create `.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-03-SUMMARY.md`
</output>
