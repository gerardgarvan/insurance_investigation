---
phase: 122-med-admin-dispensing-gap-diagnostic-csv-gap-closure
plan: 02
type: execute
wave: 1
depends_on: ["122-01"]
files_modified:
  - R/10_cohort_predicates.R
  - R/26_treatment_episodes.R
  - R/25_treatment_durations.R
  - R/11_treatment_payer.R
  - R/27_drug_name_resolution.R
  - R/20_treatment_inventory.R
  - R/76_treatment_source_coverage.R
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]

must_haves:
  truths:
    - "All 7 chemo consumers (R/10, R/26, R/25, R/11, R/27, R/20, R/76) use corrected column access — no consumer still gates chemo detection on the phantom RXNORM_CUI-present guard"
    - "MED_ADMIN chemo detection reads MEDADMIN_CODE where MEDADMIN_TYPE=='RX' (RX-direct) plus ND-typed rows via the crosswalk; DISPENSING reads NDC via the crosswalk"
    - "Immunotherapy branches in R/26 (lines 371,382) are left UNCHANGED (chemo-only phase)"
    - "Expanded sources are deduped so episode/date/patient counts are not double-inflated"
    - "R/88 Section 15t (14 checks, SMOKE-122-01) structurally validates the fix, and SCRIPT_INDEX reflects R/108"
  artifacts:
    - path: "R/26_treatment_episodes.R"
      provides: "chemo episode extraction via get_chemo_hits for DISPENSING + MED_ADMIN"
      contains: "get_chemo_hits"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15t Phase 122 structural checks + SMOKE-122-01 summary line"
      contains: "SMOKE-122-01"
    - path: "R/SCRIPT_INDEX.md"
      provides: "R/108 row; 100+ count 8->9, Total 94->95"
      contains: "R/108"
  key_links:
    - from: "R/26 extract_chemo_dates_with_codes()"
      to: "get_chemo_hits('DISPENSING'/'MED_ADMIN', chemo_rxnorm, ndc_crosswalk)"
      via: "helper call replacing the old RXNORM_CUI guard"
      pattern: "get_chemo_hits\\(\"(DISPENSING|MED_ADMIN)\""
    - from: "R/11 treatment-payer date functions"
      to: "get_chemo_hits then date aggregate (min/max)"
      via: "helper result grouped by ID"
      pattern: "get_chemo_hits"
---

<objective>
Patch all seven chemo consumers to call the shared `get_chemo_hits()` helper (built in
Plan 01), replacing the broken `"RXNORM_CUI" %in% colnames(...)` guards. Leave the R/26
immunotherapy branches untouched (chemo-only phase). Add R/88 Section 15t structural
validation and update SCRIPT_INDEX for R/108 (built in Plan 03). Update the remaining
D-12 comments in the consumers.

Purpose: D-06 requires ALL consumers see the same expanded source set, so episodes,
timing, payer anchoring, drug names, cohort membership, inventory, and coverage stay
consistent. Missing even one site reintroduces the silent gap.

Output: 7 corrected consumers, R/88 Section 15t, SCRIPT_INDEX updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-CONTEXT.md
@.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-RESEARCH.md
@.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-01-PLAN.md

<interfaces>
<!-- Helper signature from Plan 01 (utils_treatment.R): -->
get_chemo_hits(table_name, chemo_rxnorm, ndc_crosswalk = NULL)
  -> tibble(ID, treatment_date, triggering_code) or NULL
load_ndc_crosswalk() -> named char vector (NDC->RxCUI) or character(0)

<!-- Confirmed broken call sites (grep-verified line numbers): -->
R/10:   336 (DISPENSING), 347 (MED_ADMIN)   [PRESCRIBING guard nearby uses same pattern but is valid]
R/26:   185 (DISPENSING), 196 (MED_ADMIN) chemo ; 371,382 immuno -> LEAVE
R/25:   172 (DISPENSING), 183 (MED_ADMIN)   [selects ENCOUNTERID]
R/11:   186,197 (fn1 min-date), 623,631 (fn2 max-date)
R/27:   345 (DISPENSING harvest), 358 (MED_ADMIN harvest), 372 (2nd DISPENSING NDC harvest)  [332 PRESCRIBING -> keep]
R/20:   ~209 (DISPENSING RXNORM_CUI+RAW_DISPENSE_MED_NAME), ~242 (MED_ADMIN RXNORM_CUI+RAW_MEDADMIN_MED_NAME)
R/76:   232 (DISPENSING), 242 (MED_ADMIN)   [200 PRESCRIBING -> keep]

<!-- R/88: last section is 15s (line ~2501); next is 15t. SMOKE-121-01 summary at line ~4158. -->
<!-- SCRIPT_INDEX: line 206 "Post-renumber investigations (100+): 8 (...R/107...)"; line 209 "Total: 94" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch cohort/episode/timing/payer consumers (R/10, R/26, R/25, R/11) to use get_chemo_hits</name>
  <files>R/10_cohort_predicates.R, R/26_treatment_episodes.R, R/25_treatment_durations.R, R/11_treatment_payer.R</files>
  <read_first>
    - R/10_cohort_predicates.R lines 334-354 (DISPENSING+MED_ADMIN chemo-id blocks)
    - R/26_treatment_episodes.R lines 182-202 (chemo sources #5/#6) AND lines 359-390 (immuno — DO NOT TOUCH)
    - R/25_treatment_durations.R lines 169-189 (sources #5/#6, note ENCOUNTERID in select)
    - R/11_treatment_payer.R lines 184-204 (fn1 min-date) and 620-640 (fn2 max-date)
    - 122-RESEARCH.md "Exact Consumer Call-Site Enumeration" (R/10, R/26, R/25, R/11) + Pitfall 4 (dedup)
    - R/utils/utils_treatment.R get_chemo_hits (Plan 01 output)
  </read_first>
  <action>
    Introduce a crosswalk once per function scope where used: `ndc_crosswalk <- load_ndc_crosswalk()` near the top of each affected function (or reuse if already loaded in scope).

    R/10 (has_chemo, ~334-354): Replace the DISPENSING block (335-343) with:
      `disp_hits <- get_chemo_hits("DISPENSING", TREATMENT_CODES$chemo_rxnorm, ndc_crosswalk); if (!is.null(disp_hits)) { chemo_ids <- c(chemo_ids, disp_hits %>% distinct(ID) %>% pull(ID)); n_disp <- n_distinct(disp_hits$ID) }`
      Replace the MED_ADMIN block (346-354) analogously with get_chemo_hits("MED_ADMIN", ...). Update the two `# ...: RXNORM_CUI matching per D-12` comments to note the corrected access (D-12 revised Phase 122).

    R/26 (extract_chemo_dates_with_codes, sources #5/#6 at 182-202): Replace disp_dates block with `disp_dates <- get_chemo_hits("DISPENSING", TREATMENT_CODES$chemo_rxnorm, ndc_crosswalk)` and ma_dates block with `get_chemo_hits("MED_ADMIN", ...)`. The helper returns ID, treatment_date, triggering_code (no ENCOUNTERID). If stack_and_dedup_with_codes requires ENCOUNTERID, add `%>% mutate(ENCOUNTERID = NA_character_)` after each helper call to preserve the column contract. Update the `# 5. DISPENSING: RXNORM_CUI` / `# 6. MED_ADMIN: RXNORM_CUI` comments. DO NOT modify the immunotherapy function (lines 359-390).

    R/25 (sources #5/#6 at 169-189): Replace disp_dates and ma_dates blocks with get_chemo_hits calls. R/25 selects ENCOUNTERID — add `%>% mutate(ENCOUNTERID = NA_character_) %>% select(ID, treatment_date, ENCOUNTERID)` after each helper call (drop triggering_code which R/25 doesn't use). Confirm the downstream dedup (distinct(ID, treatment_date) or equivalent) before first/last aggregation exists; if not, add `bind_rows(...) %>% distinct(ID, treatment_date, .keep_all = TRUE)` before the min/max.

    R/11 (fn1 184-204, fn2 620-640): In fn1, replace disp_dates block with `disp_hits <- get_chemo_hits("DISPENSING", TREATMENT_CODES$chemo_rxnorm, ndc_crosswalk); disp_dates <- if (!is.null(disp_hits)) disp_hits %>% group_by(ID) %>% summarise(disp_date = min(treatment_date, na.rm = TRUE), .groups="drop") else NULL`. Same for ma_dates with ma_date. In fn2, same pattern but with max(treatment_date) into tx_date. Update both functions' D-12 comments. Preserve the existing min_or_na/max semantics if that pattern is used nearby (check for min_or_na()).
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows. Structural grep: old guards removed at these sites, get_chemo_hits present, immuno untouched, brace balance holds. HiPerGator runtime confirmed by user in Plan 03.</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"RXNORM_CUI" %in% colnames' R/10_cohort_predicates.R returns 0
    - grep -c 'get_chemo_hits' R/10_cohort_predicates.R returns >=2
    - grep -c 'get_chemo_hits' R/26_treatment_episodes.R returns >=2 (chemo #5/#6 only)
    - grep -c '"RXNORM_CUI" %in% colnames(get_pcornet_table("DISPENSING"))' R/26_treatment_episodes.R returns 1 (the immuno DISPENSING guard at ~371 REMAINS) — i.e. immuno untouched
    - grep -c '"RXNORM_CUI" %in% colnames(get_pcornet_table("MED_ADMIN"))' R/26_treatment_episodes.R returns 1 (immuno MED_ADMIN guard at ~382 REMAINS)
    - grep -c '"RXNORM_CUI" %in% colnames' R/25_treatment_durations.R returns 1 (only the PRESCRIBING guard at ~137 remains)
    - grep -c '"RXNORM_CUI" %in% colnames' R/11_treatment_payer.R returns 0
    - grep -c 'get_chemo_hits' R/11_treatment_payer.R returns >=4 (two funcs x two tables)
    - PRESCRIBING guards preserved: grep 'RXNORM_CUI %in% TREATMENT_CODES\$chemo_rxnorm' still present in R/26 (line ~152) and R/25 PRESCRIBING block
    - brace/paren state-machine balanced in all four files
  </acceptance_criteria>
  <done>R/10, R/26 (chemo only), R/25, R/11 use get_chemo_hits; immuno branches untouched; dedup present; D-12 comments revised.</done>
</task>

<task type="auto">
  <name>Task 2: Patch drug-name/inventory/coverage consumers (R/27, R/20, R/76)</name>
  <files>R/27_drug_name_resolution.R, R/20_treatment_inventory.R, R/76_treatment_source_coverage.R</files>
  <read_first>
    - R/27_drug_name_resolution.R lines 330-390 (PRESCRIBING 332 keep; DISPENSING 345; MED_ADMIN 358; 2nd DISPENSING NDC harvest 372)
    - R/20_treatment_inventory.R lines 200-250 (DISPENSING RXNORM+NDC blocks ~209/~225; MED_ADMIN block ~242)
    - R/76_treatment_source_coverage.R lines 198-250 (PRESCRIBING 200 keep; DISPENSING 232; MED_ADMIN 242)
    - 122-RESEARCH.md consumer enumeration for R/27/R/20/R/76 + Pitfall 7 (R/20 tryCatch) + Pitfall 8 (R/27 NDC harvest gating)
  </read_first>
  <action>
    R/27 (drug-name code harvest): Load `ndc_crosswalk <- load_ndc_crosswalk()` in scope.
      - Line 332 PRESCRIBING guard: KEEP unchanged.
      - Line 345 DISPENSING (currently harvests RXNORM_CUI + NDC gated on RXNORM_CUI present): DISPENSING has NDC only. Replace with: gate on `"NDC" %in% colnames(get_pcornet_table("DISPENSING"))`; harvest distinct NDC values from chemo-matching DISPENSING rows (use get_chemo_hits("DISPENSING", ...) to identify chemo hits, then their triggering_code is already the resolved RxCUI; for the drug-name harvest emit code_type="NDC" from the underlying NDC). Remove the RXNORM_CUI path for DISPENSING entirely.
      - Line 358 MED_ADMIN: access MEDADMIN_CODE where MEDADMIN_TYPE=='RX' as RXNORM codes and MEDADMIN_TYPE=='ND' as NDC codes. Gate on the MEDADMIN_CODE/MEDADMIN_TYPE columns present, not RXNORM_CUI.
      - Line 372 2nd DISPENSING NDC harvest: change the gate from `"RXNORM_CUI" %in% colnames(...)` to `"NDC" %in% colnames(get_pcornet_table("DISPENSING"))`.

    R/20 (inventory, tryCatch-wrapped): 
      - DISPENSING RXNORM block (~209): change `group_by(code = RXNORM_CUI, drug_name = RAW_DISPENSE_MED_NAME)` to `group_by(code = NDC, drug_name = NA_character_)` and filter `!is.na(NDC) & NDC != ""`; set code_type = "NDC". Remove the RAW_DISPENSE_MED_NAME reference entirely (absent in extract). Keep the separate NDC block (~225) but ensure it doesn't reference RAW_DISPENSE_MED_NAME either — set drug_name = NA_character_. (If the two blocks now duplicate, keep one NDC block.)
      - MED_ADMIN block (~242): change `group_by(code = RXNORM_CUI, drug_name = RAW_MEDADMIN_MED_NAME)` to `filter(MEDADMIN_TYPE %in% c("RX","ND")) %>% group_by(code = MEDADMIN_CODE, drug_name = RAW_MEDADMIN_MED_NAME)`; keep RAW_MEDADMIN_MED_NAME (present in extract).
      - Pitfall 7: add `message(glue::glue("  [R/20 DISPENSING error]: {e$message}"))` (and analogous MED_ADMIN) inside each tryCatch error handler before returning empty_result(), so future failures are visible.

    R/76 (coverage): Line 200 PRESCRIBING KEEP. Lines 232/242 DISPENSING/MED_ADMIN: replace with get_chemo_hits("DISPENSING"/"MED_ADMIN", TREATMENT_CODES$chemo_rxnorm, ndc_crosswalk) and derive the coverage counts (n_distinct ID, n rows) from the returned tibble; handle NULL (source contributes 0) gracefully.
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows. Structural grep per below. HiPerGator runtime confirmed by user in Plan 03.</automated>
  </verify>
  <acceptance_criteria>
    - R/27: grep -c '"RXNORM_CUI" %in% colnames' returns 1 (only PRESCRIBING guard at ~332); grep '"NDC" %in% colnames' returns >=1
    - R/20: grep -c 'RAW_DISPENSE_MED_NAME' returns 0 (reference removed); grep 'MEDADMIN_TYPE %in% c("RX", *"ND")' returns >=1; grep '\[R/20 DISPENSING error\]' returns 1
    - R/20: grep 'RAW_MEDADMIN_MED_NAME' still returns >=1 (kept — present in extract)
    - R/76: grep -c '"RXNORM_CUI" %in% colnames' returns 1 (only PRESCRIBING at ~200); grep -c 'get_chemo_hits' returns >=2
    - global recheck: grep -rn '"RXNORM_CUI" %in% colnames' R/ returns ONLY: R/10 (0), R/25 (1 PRESCRIBING), R/26 (3: 1 chemo-PRESCRIBING + 2 immuno), R/27 (1 PRESCRIBING), R/76 (1 PRESCRIBING), R/107 (its own diagnostic — unchanged) — i.e. no DISPENSING/MED_ADMIN chemo guard remains except the intentionally-preserved immuno pair in R/26
    - brace/paren balanced in all three files
  </acceptance_criteria>
  <done>R/27, R/20, R/76 corrected; NDC/MEDADMIN_CODE access replaces phantom RXNORM_CUI; RAW_DISPENSE_MED_NAME removed; R/20 tryCatch surfaces errors; PRESCRIBING guards preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add R/88 Section 15t (14 checks + SMOKE-122-01) and update SCRIPT_INDEX for R/108</name>
  <files>R/88_smoke_test_comprehensive.R, R/SCRIPT_INDEX.md</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R lines 2501-2580 (Section 15s pattern to mirror) and lines 4150-4160 (SMOKE summary block)
    - R/SCRIPT_INDEX.md lines 200-210 (Post-renumber count + Total)
    - 122-RESEARCH.md "R/88 smoke-test section" (checks 1-14 list, lines 400-414) + "SCRIPT_INDEX.md" (lines 416-420)
    - R/utils/utils_treatment.R (helper signatures to grep for), R/01 (col_spec state), fixtures (headers)
  </read_first>
  <action>
    Insert a new `# SECTION 15t: MED_ADMIN/DISPENSING CHEMO-DETECTION GAP FIX (Phase 122) ----` block after the Section 15s block (after line ~2580, before the next section), following the 14-check convention and the IS_LOCAL guard style used in 15s. The 14 checks:
      1. MED_ADMIN fixture header has NO "RXNORM_CUI" (read header, !grepl)
      2. DISPENSING fixture header has NO "RXNORM_CUI" AND NO "RAW_DISPENSE_MED_NAME"
      3. get_chemo_hits defined in utils_treatment.R (grep "get_chemo_hits <- function")
      4. get_chemo_hits DISPENSING path references ndc_crosswalk (grep "ndc_crosswalk\[normalize_ndc")
      5. get_chemo_hits MED_ADMIN uses MEDADMIN_TYPE=='RX' (grep 'MEDADMIN_TYPE == "RX"')
      6. load_ndc_crosswalk defined + degrades gracefully (grep "return(character(0))")
      7. R/108_build_ndc_rxnorm_crosswalk.R exists (file.exists)
      8. R/01 DISPENSING_SPEC/MED_ADMIN_SPEC no longer declare phantom RXNORM_CUI (grep count "RXNORM_CUI = col_character" == 1)
      9. R/01 D-12 revision comment present (grep "D-12 revised Phase 122" count == 2)
      10. R/10 old guard removed (grep '"RXNORM_CUI" %in% colnames' R/10 == 0)
      11. R/11 old guard removed (== 0) and get_chemo_hits present
      12. R/26 chemo uses get_chemo_hits AND immuno guards preserved (grep get_chemo_hits >=2 AND the 2 immuno RXNORM_CUI guards still present)
      13. R/27/R/20/R/76 corrected: R/20 has no RAW_DISPENSE_MED_NAME; R/76 uses get_chemo_hits
      14. IS_LOCAL-gated runtime check: if IS_LOCAL, source utils + fixtures and assert get_chemo_hits("MED_ADMIN", TREATMENT_CODES$chemo_rxnorm, load_ndc_crosswalk()) returns >=1 row (RX hit); else SKIPPED message (HiPerGator runtime). Guard the runtime path so it never crashes the smoke test if fixtures/RDS absent.
    Add the summary line near line 4158 (after SMOKE-121-01):
      `message("  * SMOKE-122-01: R/88 validates Phase 122 MED_ADMIN/DISPENSING chemo-detection gap fix structural integrity (Section 15t, 14 checks)")`

    SCRIPT_INDEX.md:
      - Add an R/108 row to the Post-Renumber Investigations (100+) table: "R/108 | Build NDC->RxNorm crosswalk (one-time HiPerGator build; data prep, not an investigation) | 122 | ..." matching the R/107 row format.
      - Line 206: change "(100+): 8 (...R/107 ...)" to "9" and append ", R/108 NDC->RxNorm crosswalk builder".
      - Line 209: change "Total: 94" to "Total: 95".
  </action>
  <verify>
    <automated>MISSING — no Rscript on Windows. Structural grep + state-machine brace balance on the new 15t block (Phases 119-120 style). HiPerGator: user runs Rscript R/88 and confirms Section 15t 14/14 PASS (Plan 03 user step).</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "SECTION 15t" R/88_smoke_test_comprehensive.R returns 1
    - grep -c "SMOKE-122-01" R/88_smoke_test_comprehensive.R returns 1
    - the 15t block contains 14 distinct check("...") calls (grep -c 'check(' within the 15t section bounds >= 14)
    - grep -c "IS_LOCAL" within the 15t section returns >=1 (runtime gate present)
    - grep -c "R/108" R/SCRIPT_INDEX.md returns >=1
    - grep "Total:\*\* 95" R/SCRIPT_INDEX.md returns 1 ; grep "(100+):\*\* 9" returns 1
    - 15t block brace/paren state-machine balanced
  </acceptance_criteria>
  <done>Section 15t added with 14 checks + IS_LOCAL runtime gate; SMOKE-122-01 summary line added; SCRIPT_INDEX updated (100+ 8->9, Total 94->95, R/108 row).</done>
</task>

</tasks>

<verification>
Structural (local, Windows executor — no Rscript):
- No DISPENSING/MED_ADMIN chemo guard on phantom RXNORM_CUI remains anywhere (global grep) except the intentionally-preserved R/26 immuno pair.
- All 7 consumers reference get_chemo_hits (or corrected NDC/MEDADMIN_CODE access for R/20/R/27).
- R/88 Section 15t present with 14 checks + IS_LOCAL gate; SCRIPT_INDEX counts updated.
- Brace/paren balance verified via state-machine scan for every edited file.

Runtime (HiPerGator, user-performed, tracked in Plan 03):
- Rscript R/88 -> Section 15t reports 14/14 PASS.
- Re-run R/107 -> the MED_ADMIN/DISPENSING increment now flows into episodes/timing (confirms the fix is wired end-to-end).
</verification>

<success_criteria>
- D-06 satisfied: every chemo consumer sees the expanded source set consistently.
- Immunotherapy branches untouched (chemo-only scope).
- Counts not double-inflated (dedup verified at each aggregation point).
- Smoke test and index reflect the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/122-med-admin-dispensing-gap-diagnostic-csv-gap-closure/122-02-SUMMARY.md`
</output>
