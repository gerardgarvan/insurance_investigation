---
phase: 78-episode-enhancement-death-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - R/52_gantt_v2_export.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - DEATH-02
  - CANCER-03
  - QUAL-01

must_haves:
  truths:
    - "Gantt episodes v2 CSV includes cause_of_death and drug_group columns (14 -> 16 columns)"
    - "Gantt detail v2 CSV includes cause_of_death column (13 -> 14 columns)"
    - "Death rows in Gantt export have cause_of_death mapped via DEATH_CAUSE_MAP"
    - "Treatment rows in Gantt export have cause_of_death = NA"
    - "Missingness > 40% produces console warning in R/52"
    - "Smoke test validates new R/28 columns and death quality script existence"
  artifacts:
    - path: "R/52_gantt_v2_export.R"
      provides: "Gantt v2 export with cause_of_death and drug_group columns"
      contains: "cause_of_death"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Updated smoke test with Phase 78 validation sections"
      contains: "DEATH-01"
  key_links:
    - from: "R/52_gantt_v2_export.R"
      to: "DEATH_CAUSE_MAP in R/00_config.R"
      via: "map_death_cause() function using 3-char ICD-10 prefix"
      pattern: "DEATH_CAUSE_MAP"
    - from: "R/52_gantt_v2_export.R"
      to: "validated_death_dates.rds"
      via: "readRDS for death cause codes"
      pattern: "validated_death_dates"
    - from: "R/52_gantt_v2_export.R"
      to: "treatment_episodes.rds drug_group column"
      via: "readRDS picks up drug_group from Plan 01 R/28 enrichment"
      pattern: "drug_group"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/35_death_cause_quality.R"
      via: "file.exists check for new script"
      pattern: "35_death_cause_quality"
---

<objective>
Integrate cause of death and drug group into Gantt v2 CSV exports, and update smoke test to validate all Phase 78 changes.

Purpose: Complete DEATH-02 (cause of death in outputs) by adding the column to Gantt CSVs, propagate CANCER-03 drug_group to Gantt, and extend smoke test coverage for all Phase 78 artifacts (QUAL-01).
Output: R/52_gantt_v2_export.R (modified), R/88_smoke_test_comprehensive.R (modified)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-episode-enhancement-death-integration/78-CONTEXT.md
@.planning/phases/78-episode-enhancement-death-integration/78-RESEARCH.md
@.planning/phases/78-episode-enhancement-death-integration/78-01-SUMMARY.md

@R/00_config.R
@R/52_gantt_v2_export.R
@R/88_smoke_test_comprehensive.R
@R/28_episode_classification.R

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/28_episode_classification.R (after Plan 01 enrichment):
  treatment_episodes.rds now has 17 columns:
  1. patient_id, 2. treatment_type, 3. episode_number, 4. episode_start,
  5. episode_stop, 6. episode_length_days, 7. distinct_dates_in_episode,
  8. historical_flag, 9. triggering_codes, 10. encounter_ids, 11. drug_names,
  12. cancer_category, 13. cancer_link_method, 14. is_hodgkin, 15. regimen_label,
  16. triggering_code_description, 17. drug_group

From R/00_config.R (Phase 75):
  DEATH_CAUSE_MAP: named character vector (~100 entries)
  Maps 3-char ICD-10 prefixes to cause categories
  Example: c("C00" = "Malignant Neoplasms", "I25" = "Ischemic Heart Disease", ...)

From R/52_gantt_v2_export.R current state:
  - VALIDATED_DEATHS_RDS points to cache/outputs/validated_death_dates.rds
  - validated_deaths has columns: ID, DEATH_DATE, DEATH_SOURCE (and possibly DEATH_CAUSE)
  - Death pseudo-treatment rows constructed in Section 4B (lines 284-395)
  - Column trimming in Section 4D Step 6 (lines 678-696):
    episodes_export final select: 14 columns (patient_id through is_first_line)
    detail_export final select: 13 columns (patient_id through is_first_line)
  - Column count verification in Section 4D Step 7 (lines 700-711):
    expected_ep_cols <- 14; expected_detail_cols <- 13

From R/35_death_cause_quality.R (Plan 01 output):
  Saves death_cause_quality_result.rds with:
    list(missingness_rate, death_cause_available, recommendation)

From R/88_smoke_test_comprehensive.R:
  - SECTION 14: SUMMARY at line 876-910
  - Current check count visible in summary
  - check() function: check(description, condition) -- increments passed/failed
  - check_source() function: check_source(file, pattern, description)
  - Pattern: numbered SECTION headers with message("[N/M] section name...")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cause_of_death and drug_group to R/52 Gantt v2 export</name>
  <files>R/52_gantt_v2_export.R</files>
  <read_first>
    R/52_gantt_v2_export.R
    R/00_config.R
    R/28_episode_classification.R
    R/35_death_cause_quality.R
    .planning/phases/78-episode-enhancement-death-integration/78-01-SUMMARY.md
  </read_first>
  <action>
Modify R/52_gantt_v2_export.R to add cause_of_death and drug_group columns per D-09, D-10, D-11, D-12, D-14.

**UPDATE 1: Script header**
- Update schema documentation block (lines 36-72):
  - gantt_episodes_v2.csv: 14 -> 16 columns. Add after line for is_first_line (col 14):
    ```
    #    15. drug_group (chr) - Semicolon-separated drug category labels (Phase 78)
    #    16. cause_of_death (chr) - Mapped cause of death category or NA (Phase 78)
    ```
  - gantt_detail_v2.csv: 13 -> 14 columns. Add after line for is_first_line (col 13):
    ```
    #    14. cause_of_death (chr) - Mapped cause of death category or NA (Phase 78)
    ```
  - Update header line counts: "14 columns" -> "16 columns" for episodes, "13 columns" -> "14 columns" for detail
- Add Phase 78 decision traceability to header:
  ```
  #   D-78-09: cause_of_death appended as last column (non-breaking)
  #   D-78-10: Missing/unmapped ICD-10 -> "Unknown or Unspecified"
  #   D-78-11: >40% missingness triggers console warning
  #   D-78-12: Both gantt_episodes_v2.csv and gantt_detail_v2.csv get cause_of_death
  #   D-78-14: drug_group propagated from treatment_episodes.rds to episodes CSV
  ```

**UPDATE 2: Add source("R/utils/utils_cancer.R") if not already sourced** (for DEATH_CAUSE_MAP access via R/00_config.R -- DEATH_CAUSE_MAP is already available from source("R/00_config.R"))

**UPDATE 3: Add new Section 3B after Section 3 (Code Description Lookup)**
New section: `# --- SECTION 3B: DEATH CAUSE MAPPING ----`

Add death cause mapping function:
```r
# Helper: map single ICD-10 code to cause of death category via DEATH_CAUSE_MAP
# D-78-10: Missing/unmapped -> "Unknown or Unspecified"
map_death_cause <- function(death_cause_code) {
  if (is.na(death_cause_code) || death_cause_code == "") return("Unknown or Unspecified")
  prefix_3char <- str_sub(death_cause_code, 1, 3)
  if (prefix_3char %in% names(DEATH_CAUSE_MAP)) return(DEATH_CAUSE_MAP[[prefix_3char]])
  return("Unknown or Unspecified")
}
```

Load quality decision artifact from Plan 01:
```r
QUALITY_RESULT_RDS <- file.path(CONFIG$cache$outputs_dir, "death_cause_quality_result.rds")
death_quality <- NULL
cause_of_death_available <- TRUE  # default: proceed with integration
if (file.exists(QUALITY_RESULT_RDS)) {
  death_quality <- readRDS(QUALITY_RESULT_RDS)
  if (!death_quality$death_cause_available) {
    message("  WARNING: DEATH_CAUSE field not available per quality profiling (R/35)")
    cause_of_death_available <- FALSE
  } else if (death_quality$missingness_rate > 60) {
    message(glue("  WARNING: Cause of death {death_quality$missingness_rate}% missing -- column will contain mostly 'Unknown or Unspecified'"))
  }
} else {
  message("  WARNING: death_cause_quality_result.rds not found -- proceeding with cause_of_death integration")
}
```

**UPDATE 4: Guard clause for drug_group column in Section 2 (after loading episodes)**
After the existing guard clauses (lines 152-171), add:
```r
if (!"drug_group" %in% names(episodes)) {
  warning("drug_group column not found in treatment_episodes.rds -- Phase 78 R/28 not yet run. Using default NA.")
  episodes <- episodes %>% mutate(drug_group = NA_character_)
}
if (!"triggering_code_description" %in% names(episodes)) {
  warning("triggering_code_description column not found in treatment_episodes.rds -- Phase 78 R/28 not yet run. Using default NA.")
  episodes <- episodes %>% mutate(triggering_code_description = NA_character_)
}
```

**UPDATE 5: Modify Section 4 (Select and Order Columns)**
Add drug_group to the episodes_export select (after the existing columns, before the left_join):
In the initial episodes select (~line 218-223), add `drug_group` after `drug_names`.
In the left_join select of enriched columns, keep as-is (drug_group is already in the base episodes).
In the final select order (~line 240-246), add `drug_group` after `is_first_line`.

For detail_export: drug_group is an episode-level field. Add it to the episodes_v2_cols select (line 253-257) and then carry it through the left_join. In detail_export final select, add it BEFORE cause_of_death (but Phase 78 detail does NOT get drug_group per D-14 -- only episodes get drug_group. Gantt detail is treatment-date level, drug_group is episode-level from treatment_episodes.rds, so check D-14: "Gantt episodes CSV grows from 15 to 16 columns (cause_of_death + drug_group)". Detail CSV gets cause_of_death but NOT drug_group based on D-14 only mentioning episodes for drug_group.

Actually re-reading D-14: "drug_group column propagates to Gantt v2 export (R/52). Gantt episodes CSV grows from 15 to 16 columns (cause_of_death + drug_group)." The mention of "15 to 16" for episodes suggests +2 columns (cause_of_death + drug_group). And D-09 says detail goes from "13 -> 14 columns" -- only +1 column (cause_of_death). So:
- Episodes: +drug_group, +cause_of_death = 14 -> 16 columns
- Detail: +cause_of_death = 13 -> 14 columns

**UPDATE 6: Modify Section 4B (Death Pseudo-Treatment Rows)**
In the death_episodes construction (~lines 299-325):
- Add `drug_group = NA_character_,` after drug_names
- Add `cause_of_death = NA_character_,` -- BUT wait: for Death rows, cause_of_death should be the actual cause. This is where we map DEATH_CAUSE via DEATH_CAUSE_MAP.
- Revised approach: load DEATH_CAUSE from validated_death_dates.rds. Check if DEATH_CAUSE column exists in validated_deaths. If yes, map it.

Modify the validated_deaths loading (currently line 294-295: `select(ID, DEATH_DATE)`):
```r
death_data <- validated_deaths %>%
  filter(!is.na(DEATH_DATE))

# Check if DEATH_CAUSE is available
if ("DEATH_CAUSE" %in% names(death_data) && cause_of_death_available) {
  death_data <- death_data %>%
    mutate(cause_of_death = sapply(DEATH_CAUSE, map_death_cause, USE.NAMES = FALSE)) %>%
    select(ID, DEATH_DATE, cause_of_death)
} else {
  death_data <- death_data %>%
    mutate(cause_of_death = NA_character_) %>%
    select(ID, DEATH_DATE, cause_of_death)
}
```

Then in death_episodes construction, use `cause_of_death = cause_of_death,` (from death_data) instead of hardcoded NA.
Add `drug_group = NA_character_,` (Death rows have no drug group).

In death_detail construction, add `cause_of_death = cause_of_death,` and NO drug_group (detail doesn't get drug_group).

**IMPORTANT: DEATH_CAUSE may not be in validated_death_dates.rds.** R/53 only selects ID, DEATH_DATE, DEATH_SOURCE when creating this artifact. If DEATH_CAUSE is not present, need to query it separately from DEATH table. Strategy:
1. First check if DEATH_CAUSE exists in validated_deaths
2. If not: re-query DEATH table for DEATH_CAUSE, join to death_data by ID
3. If DEATH table also lacks DEATH_CAUSE: all death rows get cause_of_death = "Unknown or Unspecified"

Implementation for re-query:
```r
if (!"DEATH_CAUSE" %in% names(validated_deaths) && cause_of_death_available) {
  message("  DEATH_CAUSE not in validated_death_dates.rds -- querying DEATH table directly")
  USE_DUCKDB <- TRUE
  open_pcornet_con()
  death_cause_data <- get_pcornet_table("DEATH") %>% collect()
  close_pcornet_con()

  if ("DEATH_CAUSE" %in% names(death_cause_data)) {
    death_cause_lookup <- death_cause_data %>%
      select(ID, DEATH_CAUSE) %>%
      filter(!is.na(DEATH_CAUSE) & DEATH_CAUSE != "") %>%
      group_by(ID) %>%
      summarise(DEATH_CAUSE = first(DEATH_CAUSE), .groups = "drop")

    death_data <- death_data %>%
      left_join(death_cause_lookup, by = "ID") %>%
      mutate(cause_of_death = sapply(DEATH_CAUSE, map_death_cause, USE.NAMES = FALSE))
  } else {
    message("  DEATH_CAUSE column not available in DEATH table")
    death_data <- death_data %>%
      mutate(cause_of_death = "Unknown or Unspecified")
  }
}
```

Also add the cause_of_death field to treatment_episode rows (non-death episodes):
After building episodes_export, add: `episodes_export <- episodes_export %>% mutate(cause_of_death = NA_character_)` -- treatment rows get NA per D-10.

Then when Death rows get appended via bind_rows, their cause_of_death flows through.

For HL Diagnosis pseudo-treatment rows (Section 4C), add:
- `cause_of_death = NA_character_,` (HL Diagnosis rows are not death events)
- `drug_group = NA_character_,` (HL Diagnosis rows have no drug group)

**UPDATE 7: Modify Section 4D Step 6 (Column Trimming)**
Update the episodes_export final select (~line 679-686) to include drug_group and cause_of_death:
```r
episodes_export <- episodes_export %>%
  select(
    patient_id, treatment_type, episode_number,
    episode_start, episode_stop, episode_length_days,
    distinct_dates_in_episode, historical_flag,
    triggering_codes, drug_names, triggering_code_descriptions,
    cancer_category, regimen_label, is_first_line,
    drug_group, cause_of_death
  )
```

Update the detail_export final select (~line 688-695) to include cause_of_death:
```r
detail_export <- detail_export %>%
  select(
    patient_id, treatment_type, treatment_date,
    triggering_code, drug_name, episode_number,
    episode_start, episode_stop, historical_flag,
    triggering_code_description, cancer_category,
    regimen_label, is_first_line,
    cause_of_death
  )
```

**UPDATE 8: Modify Section 4D Step 7 (Column Count Verification)**
Change expected counts (~lines 701-709):
```r
expected_ep_cols <- 16  # was 14
expected_detail_cols <- 14  # was 13
```

**UPDATE 9: Add missingness warning (D-11)**
After building all episodes_export (after Death and HL Diagnosis rows appended), before Section 4D cleanup:
```r
# D-78-11: Missingness warning for cause_of_death
if (cause_of_death_available && any(!is.na(episodes_export$cause_of_death))) {
  death_rows_export <- episodes_export %>% filter(treatment_type == "Death")
  if (nrow(death_rows_export) > 0) {
    n_unknown <- sum(death_rows_export$cause_of_death == "Unknown or Unspecified" | is.na(death_rows_export$cause_of_death), na.rm = TRUE)
    pct_missing <- round(100 * n_unknown / nrow(death_rows_export), 1)
    if (pct_missing > 40) {
      message(glue("  WARNING: Cause of death missing/unmapped for {pct_missing}% of death rows (D-78-11)"))
    } else {
      message(glue("  Cause of death completeness: {100 - pct_missing}% of death rows have mapped cause"))
    }
  }
}
```

**UPDATE 10: Update Section 6 (Final Summary)**
- Update v1 vs v2 column comparison message (~lines 762-765):
  ```r
  message("    v1 episodes: 14 columns | v2 episodes: 16 columns (Phase 78: +drug_group, +cause_of_death)")
  message("    v1 detail: 13 columns | v2 detail: 14 columns (Phase 78: +cause_of_death)")
  ```
- Add cause_of_death stats:
  ```r
  deaths_with_cause <- episodes_export %>%
    filter(treatment_type == "Death" & !is.na(cause_of_death) & cause_of_death != "" & cause_of_death != "Unknown or Unspecified") %>%
    nrow()
  message(glue("  Deaths with mapped cause: {format(deaths_with_cause, big.mark = ',')}"))
  ```
  </action>
  <verify>
    <automated>grep -c "cause_of_death" R/52_gantt_v2_export.R && grep -c "drug_group" R/52_gantt_v2_export.R && grep "expected_ep_cols" R/52_gantt_v2_export.R && grep "expected_detail_cols" R/52_gantt_v2_export.R</automated>
  </verify>
  <acceptance_criteria>
    - R/52_gantt_v2_export.R contains "cause_of_death" in both episodes and detail final select()
    - R/52_gantt_v2_export.R contains "drug_group" in episodes final select() only (not detail)
    - R/52_gantt_v2_export.R contains "expected_ep_cols <- 16" (was 14)
    - R/52_gantt_v2_export.R contains "expected_detail_cols <- 14" (was 13)
    - R/52_gantt_v2_export.R contains "map_death_cause" function definition
    - R/52_gantt_v2_export.R contains "DEATH_CAUSE_MAP" reference
    - R/52_gantt_v2_export.R contains "Unknown or Unspecified" (D-10 unmapped fallback)
    - R/52_gantt_v2_export.R contains "> 40" or "40%" for missingness warning (D-11)
    - R/52_gantt_v2_export.R contains "death_cause_quality_result.rds" (quality decision integration)
    - R/52_gantt_v2_export.R schema documentation shows "16 columns" for episodes and "14 columns" for detail
    - R/52_gantt_v2_export.R Death pseudo-treatment rows include cause_of_death (not NA)
    - R/52_gantt_v2_export.R HL Diagnosis pseudo-treatment rows include cause_of_death = NA
    - R/52_gantt_v2_export.R treatment rows get cause_of_death = NA_character_
    - R/52_gantt_v2_export.R guard clause for missing drug_group column present
  </acceptance_criteria>
  <done>R/52 Gantt v2 exports include cause_of_death (mapped via DEATH_CAUSE_MAP for death rows, NA for treatment/HL rows) and drug_group (from treatment_episodes.rds). Episodes CSV: 14 -> 16 columns. Detail CSV: 13 -> 14 columns. Missingness > 40% triggers console warning.</done>
</task>

<task type="auto">
  <name>Task 2: Update R/88 smoke test with Phase 78 validation sections</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    R/88_smoke_test_comprehensive.R
    R/28_episode_classification.R
    R/52_gantt_v2_export.R
    R/35_death_cause_quality.R
    R/00_config.R
  </read_first>
  <action>
Add two new SECTION blocks to R/88_smoke_test_comprehensive.R for Phase 78 validation. Insert BEFORE the existing SECTION 14 (SUMMARY) at line 876.

**NEW SECTION: DEATH QUALITY PROFILING VALIDATION (DEATH-01)**
Renumber from current last check group. If current last section is 13, add as Section 14 and 15, then renumber SUMMARY to Section 16.

```r
# ==============================================================================
# SECTION 14: DEATH QUALITY PROFILING VALIDATION (DEATH-01) ----
# ==============================================================================

message("\n[XX/YY] Death quality profiling validation (DEATH-01)...")

# Check 1: R/35_death_cause_quality.R exists
check(
  "R/35_death_cause_quality.R exists",
  file.exists("R/35_death_cause_quality.R")
)

# Check 2: R/35 sources R/00_config.R
if (file.exists("R/35_death_cause_quality.R")) {
  r35_lines <- readLines("R/35_death_cause_quality.R", warn = FALSE)

  check(
    "R/35 sources R/00_config.R",
    any(grepl('source\\("R/00_config.R"\\)', r35_lines))
  )

  # Check 3: R/35 references DEATH_CAUSE_MAP
  check(
    "R/35 references DEATH_CAUSE_MAP for cause mapping",
    any(grepl("DEATH_CAUSE_MAP", r35_lines))
  )

  # Check 4: R/35 has DEATH_CAUSE field availability guard
  check(
    "R/35 has DEATH_CAUSE field availability check",
    any(grepl("death_cause_available|DEATH_CAUSE.*names", r35_lines))
  )

  # Check 5: R/35 outputs death_cause_quality.xlsx
  check(
    "R/35 outputs death_cause_quality.xlsx",
    any(grepl("death_cause_quality\\.xlsx", r35_lines))
  )

  # Check 6: R/35 saves quality decision artifact
  check(
    "R/35 saves death_cause_quality_result.rds",
    any(grepl("death_cause_quality_result\\.rds", r35_lines))
  )

  # Check 7: R/35 has proper section headers
  n_sections_r35 <- sum(grepl("^# SECTION.*----", r35_lines) | grepl("^# ---.*SECTION.*----", r35_lines))
  check(
    glue("R/35 has >= 6 section headers (found: {n_sections_r35})"),
    n_sections_r35 >= 6
  )
} else {
  message("  SKIP: R/35_death_cause_quality.R not found -- skipping detail checks")
}
```

**NEW SECTION: EPISODE ENRICHMENT AND GANTT INTEGRATION (CANCER-03, DEATH-02)**

```r
# ==============================================================================
# SECTION 15: EPISODE ENRICHMENT AND GANTT INTEGRATION (CANCER-03, DEATH-02) ----
# ==============================================================================

message("\n[XX/YY] Episode enrichment and Gantt integration (CANCER-03, DEATH-02)...")

# Check 1: R/28 has triggering_code_description column in final select
r28_lines <- readLines("R/28_episode_classification.R", warn = FALSE)

check(
  "R/28 final select includes triggering_code_description",
  any(grepl("triggering_code_description", r28_lines))
)

# Check 2: R/28 has drug_group column in final select
check(
  "R/28 final select includes drug_group",
  any(grepl("drug_group", r28_lines))
)

# Check 3: R/28 references DRUG_GROUPINGS
check(
  "R/28 references DRUG_GROUPINGS for drug group mapping",
  any(grepl("DRUG_GROUPINGS", r28_lines))
)

# Check 4: R/28 references code_descriptions.rds
check(
  "R/28 references code_descriptions.rds for description lookup",
  any(grepl("code_descriptions\\.rds", r28_lines))
)

# Check 5: R/52 has cause_of_death in episodes export
r52_lines <- readLines("R/52_gantt_v2_export.R", warn = FALSE)

check(
  "R/52 episodes export includes cause_of_death",
  any(grepl("cause_of_death", r52_lines))
)

# Check 6: R/52 has drug_group in episodes export
check(
  "R/52 episodes export includes drug_group",
  any(grepl("drug_group", r52_lines))
)

# Check 7: R/52 has DEATH_CAUSE_MAP reference
check(
  "R/52 references DEATH_CAUSE_MAP for cause mapping",
  any(grepl("DEATH_CAUSE_MAP", r52_lines))
)

# Check 8: R/52 expected episodes column count is 16
check(
  "R/52 expected_ep_cols is 16 (was 14)",
  any(grepl("expected_ep_cols\\s*<-\\s*16", r52_lines))
)

# Check 9: R/52 expected detail column count is 14
check(
  "R/52 expected_detail_cols is 14 (was 13)",
  any(grepl("expected_detail_cols\\s*<-\\s*14", r52_lines))
)

# Check 10: R/52 has missingness warning threshold
check(
  "R/52 has cause_of_death missingness warning (>40% threshold)",
  any(grepl("40", r52_lines) & grepl("missing|WARNING", r52_lines, ignore.case = TRUE))
)
```

**UPDATE: Renumber SECTION 14 (SUMMARY) to SECTION 16**
- Change `# SECTION 14: SUMMARY ----` to `# SECTION 16: SUMMARY ----`
- Update all `[N/M]` check group counters in message() calls throughout the file to reflect 2 additional check groups
- Update validated requirements list at the bottom to include:
  ```r
  message("  * CANCER-03: Per-episode triggering_code_description and drug_group columns (R/28)")
  message("  * DEATH-01: Death cause quality profiling (R/35)")
  message("  * DEATH-02: Cause of death in Gantt v2 exports (R/52)")
  ```
  </action>
  <verify>
    <automated>grep -c "CANCER-03" R/88_smoke_test_comprehensive.R && grep -c "DEATH-01" R/88_smoke_test_comprehensive.R && grep -c "DEATH-02" R/88_smoke_test_comprehensive.R && grep "SECTION 16" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains "SECTION 14: DEATH QUALITY PROFILING" (or similar)
    - R/88_smoke_test_comprehensive.R contains "SECTION 15: EPISODE ENRICHMENT" (or similar)
    - R/88_smoke_test_comprehensive.R contains "SECTION 16: SUMMARY" (renumbered from 14)
    - R/88_smoke_test_comprehensive.R contains check for "R/35_death_cause_quality.R exists"
    - R/88_smoke_test_comprehensive.R contains check for "triggering_code_description" in R/28
    - R/88_smoke_test_comprehensive.R contains check for "drug_group" in R/28
    - R/88_smoke_test_comprehensive.R contains check for "cause_of_death" in R/52
    - R/88_smoke_test_comprehensive.R contains check for "expected_ep_cols" = 16
    - R/88_smoke_test_comprehensive.R contains check for "expected_detail_cols" = 14
    - R/88_smoke_test_comprehensive.R contains "CANCER-03" in validated requirements
    - R/88_smoke_test_comprehensive.R contains "DEATH-01" in validated requirements
    - R/88_smoke_test_comprehensive.R contains "DEATH-02" in validated requirements
    - R/88_smoke_test_comprehensive.R adds at minimum 10 new check() calls
  </acceptance_criteria>
  <done>R/88 smoke test validates Phase 78: R/35 existence and structure (6 checks), R/28 column enrichment (4 checks), R/52 Gantt integration including column counts and DEATH_CAUSE_MAP (6 checks). Total 16 new checks. Summary section lists CANCER-03, DEATH-01, DEATH-02 requirements.</done>
</task>

</tasks>

<verification>
1. R/52 Gantt episodes CSV schema: 16 columns (patient_id through cause_of_death)
2. R/52 Gantt detail CSV schema: 14 columns (patient_id through cause_of_death)
3. R/52 Death pseudo-treatment rows have mapped cause_of_death (not NA)
4. R/52 Treatment rows have cause_of_death = NA
5. R/88 smoke test runs without errors: `Rscript R/88_smoke_test_comprehensive.R`
6. R/88 smoke test reports PASS for all Phase 78 checks
</verification>

<success_criteria>
- gantt_episodes_v2.csv grows from 14 to 16 columns (+drug_group, +cause_of_death)
- gantt_detail_v2.csv grows from 13 to 14 columns (+cause_of_death)
- Cause of death mapped via DEATH_CAUSE_MAP with "Unknown or Unspecified" fallback
- >40% missingness triggers console WARNING in R/52
- Smoke test validates all Phase 78 artifacts with 16+ new checks
- All requirements covered: DEATH-02, CANCER-03, QUAL-01
</success_criteria>

<output>
After completion, create `.planning/phases/78-episode-enhancement-death-integration/78-02-SUMMARY.md`
</output>
