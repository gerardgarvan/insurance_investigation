---
phase: 01-foundation-data-loading
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - R/01_load_pcornet.R
autonomous: true
requirements:
  - LOAD-01

must_haves:
  truths:
    - "User can load 9 PCORnet CDM CSV tables (ENROLLMENT, DIAGNOSIS, PROCEDURES, PRESCRIBING, ENCOUNTER, DEMOGRAPHIC, TUMOR_REGISTRY1-3) into R with explicit column type specifications"
    - "User can access loaded tables as pcornet$ENROLLMENT, pcornet$DIAGNOSIS, etc."
    - "All date columns are automatically parsed via parse_pcornet_date() during loading"
    - "Missing or inaccessible CSV files produce a warning and NULL entry instead of a fatal error"
    - "Each table load prints a summary line with table name, row count, and column count"
    - "All ID columns are loaded as character (not numeric) to prevent leading-zero truncation"
  artifacts:
    - path: "R/01_load_pcornet.R"
      provides: "Data loading script with col_types specs for all 9 primary tables and load_pcornet_table() function"
      contains: "load_pcornet_table"
      min_lines: 100
  key_links:
    - from: "R/01_load_pcornet.R"
      to: "R/00_config.R"
      via: "source() at top of file"
      pattern: 'source\("R/00_config\.R"\)'
    - from: "R/01_load_pcornet.R"
      to: "readr::read_csv"
      via: "library(readr) and read_csv call in load_pcornet_table"
      pattern: "read_csv\\("
    - from: "R/01_load_pcornet.R"
      to: "parse_pcornet_date"
      via: "Called on date columns after CSV load"
      pattern: "parse_pcornet_date\\("
    - from: "R/01_load_pcornet.R"
      to: "PCORNET_PATHS"
      via: "Uses paths from config to locate CSV files"
      pattern: "PCORNET_PATHS"
---

<objective>
Create the PCORnet CDM CSV loading script with explicit column type specifications for all 9 primary tables, a reusable load function, and automatic date parsing.

Purpose: This is the data loading infrastructure that all downstream phases depend on. Phase 2 (payer harmonization) needs pcornet$ENROLLMENT and pcornet$ENCOUNTER. Phase 3 (cohort building) needs pcornet$DIAGNOSIS, pcornet$PROCEDURES, pcornet$PRESCRIBING. Phase 4 (visualization) needs all tables through the built cohort.

Output: R/01_load_pcornet.R with col_types for 9 tables, load_pcornet_table() helper, and a pcornet named list containing all loaded data
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-loading/01-CONTEXT.md
@.planning/phases/01-foundation-data-loading/01-RESEARCH.md
@.planning/phases/01-foundation-data-loading/01-01-SUMMARY.md
@csv_columns.txt

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From R/00_config.R (created in Plan 01):
```r
# CONFIG$data_dir = "/orange/erin.mobley-hl.bcu/Mailhot_V1_20250915"
# PCORNET_TABLES = character vector of 9 table names
# PCORNET_PATHS = named character vector: PCORNET_PATHS$ENROLLMENT = "...path..."
# Auto-sources: R/utils_dates.R (provides parse_pcornet_date)
#               R/utils_attrition.R (provides init_attrition_log, log_attrition)
```

From R/utils_dates.R (created in Plan 01):
```r
parse_pcornet_date <- function(date_char) {
  # Input: character vector of dates in various formats
  # Output: Date vector with NA for unparseable values
  # Tries: YYYY-MM-DD -> Excel serial -> SAS DATE9 -> YYYYMMDD
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create column type specifications for all 9 primary tables</name>
  <files>R/01_load_pcornet.R</files>
  <read_first>
    - csv_columns.txt (CRITICAL: exact column names and counts for every table -- the col_types specs MUST match these exactly)
    - R/00_config.R (verify PCORNET_PATHS and PCORNET_TABLES structure for integration)
    - .planning/phases/01-foundation-data-loading/01-CONTEXT.md (D-07: primary load set, D-08: readr with explicit col_types, D-11: column names as-is, D-13: named list storage, D-14: file naming pattern)
    - .planning/phases/01-foundation-data-loading/01-RESEARCH.md (Pattern 2: Explicit col_types, Pitfall 1: ID truncation, Pitfall 6: large col_types)
  </read_first>
  <action>
Create R/01_load_pcornet.R with the complete data loading infrastructure.

**File header:**
```r
# 01_load_pcornet.R -- Load PCORnet CDM CSV tables with explicit column types
#
# Loads 9 primary tables into a named list (pcornet$ENROLLMENT, pcornet$DIAGNOSIS, etc.)
# All date columns are parsed via parse_pcornet_date() (multi-format fallback)
# All ID columns are loaded as character (prevents leading-zero truncation)
# Missing files produce a warning and NULL entry (per D-10)
#
# Usage:
#   source("R/00_config.R")  # Auto-loads utils
#   source("R/01_load_pcornet.R")
#   pcornet$ENROLLMENT  # Access loaded table
#
# Requirement: LOAD-01 (load 22 CDM tables with explicit col_types)
# Phase 1 loads 9 primary tables; remaining 13 added as needed

source("R/00_config.R")

library(readr)
library(stringr)
library(purrr)
library(glue)
```

**Column type specifications -- derived from csv_columns.txt:**

Strategy per D-08 and Research Pitfall 6:
- All ID columns: col_character() (CRITICAL: prevents leading-zero truncation)
- All date/time columns: col_character() (parsed separately by parse_pcornet_date)
- All coded values (ENC_TYPE, DX_TYPE, CHART, etc.): col_character()
- For tables with many columns (TUMOR_REGISTRY1: 314 cols): use cols(.default = col_character()) with specific numeric overrides
- For tables with few columns (ENROLLMENT: 6 cols): list all columns explicitly

Define these named specs:

**ENROLLMENT_SPEC** (6 columns from csv_columns.txt):
```r
ENROLLMENT_SPEC <- cols(
  ID = col_character(),
  ENR_START_DATE = col_character(),
  ENR_END_DATE = col_character(),
  CHART = col_character(),
  ENR_BASIS = col_character(),
  SOURCE = col_character()
)
```

**DIAGNOSIS_SPEC** (14 columns from csv_columns.txt):
```r
DIAGNOSIS_SPEC <- cols(
  DIAGNOSISID = col_character(),
  ID = col_character(),
  ENCOUNTERID = col_character(),
  ENC_TYPE = col_character(),
  ADMIT_DATE = col_character(),
  PROVIDERID = col_character(),
  DX = col_character(),
  DX_TYPE = col_character(),
  DX_DATE = col_character(),
  DX_SOURCE = col_character(),
  DX_ORIGIN = col_character(),
  PDX = col_character(),
  DX_POA = col_character(),
  SOURCE = col_character()
)
```

**PROCEDURES_SPEC** (12 columns from csv_columns.txt):
```r
PROCEDURES_SPEC <- cols(
  PROCEDURESID = col_character(),
  ID = col_character(),
  ENCOUNTERID = col_character(),
  ENC_TYPE = col_character(),
  ADMIT_DATE = col_character(),
  PROVIDERID = col_character(),
  PX_DATE = col_character(),
  PX = col_character(),
  PX_TYPE = col_character(),
  PX_SOURCE = col_character(),
  PPX = col_character(),
  SOURCE = col_character()
)
```

**PRESCRIBING_SPEC** (24 columns from csv_columns.txt):
```r
PRESCRIBING_SPEC <- cols(
  PRESCRIBINGID = col_character(),
  ID = col_character(),
  ENCOUNTERID = col_character(),
  RX_PROVIDERID = col_character(),
  RX_ORDER_DATE = col_character(),
  RX_ORDER_TIME = col_character(),
  RX_START_DATE = col_character(),
  RX_END_DATE = col_character(),
  RX_DOSE_ORDERED = col_double(),
  RX_DOSE_ORDERED_UNIT = col_character(),
  RX_QUANTITY = col_double(),
  RX_DOSE_FORM = col_character(),
  RX_REFILLS = col_integer(),
  RX_DAYS_SUPPLY = col_integer(),
  RX_FREQUENCY = col_character(),
  RX_PRN_FLAG = col_character(),
  RX_ROUTE = col_character(),
  RX_BASIS = col_character(),
  RXNORM_CUI = col_character(),
  RX_SOURCE = col_character(),
  RX_DISPENSE_AS_WRITTEN = col_character(),
  RAW_RX_MED_NAME = col_character(),
  RAW_RXNORM_CUI = col_character(),
  SOURCE = col_character()
)
```

**ENCOUNTER_SPEC** (19 columns from csv_columns.txt):
```r
ENCOUNTER_SPEC <- cols(
  ENCOUNTERID = col_character(),
  ID = col_character(),
  ADMIT_DATE = col_character(),
  ADMIT_TIME = col_character(),
  DISCHARGE_DATE = col_character(),
  DISCHARGE_TIME = col_character(),
  PROVIDERID = col_character(),
  FACILITY_LOCATION = col_character(),
  ENC_TYPE = col_character(),
  FACILITYID = col_character(),
  DISCHARGE_DISPOSITION = col_character(),
  DISCHARGE_STATUS = col_character(),
  DRG = col_character(),
  DRG_TYPE = col_character(),
  ADMITTING_SOURCE = col_character(),
  PAYER_TYPE_PRIMARY = col_character(),
  PAYER_TYPE_SECONDARY = col_character(),
  FACILITY_TYPE = col_character(),
  SOURCE = col_character()
)
```

**DEMOGRAPHIC_SPEC** (12 columns from csv_columns.txt):
```r
DEMOGRAPHIC_SPEC <- cols(
  ID = col_character(),
  BIRTH_DATE = col_character(),
  BIRTH_TIME = col_character(),
  SEX = col_character(),
  SEXUAL_ORIENTATION = col_character(),
  GENDER_IDENTITY = col_character(),
  HISPANIC = col_character(),
  RACE = col_character(),
  BIOBANK_FLAG = col_character(),
  PAT_PREF_LANGUAGE_SPOKEN = col_character(),
  ZIP_CODE = col_character(),
  SOURCE = col_character()
)
```

**TUMOR_REGISTRY1_SPEC** (314 columns -- use .default strategy per Pitfall 6):
```r
TUMOR_REGISTRY1_SPEC <- cols(
  .default = col_character(),
  AGE_AT_DIAGNOSIS = col_integer(),
  TUMOR_SIZE_SUMMARY = col_double(),
  TUMOR_SIZE_CLINICAL = col_double(),
  TUMOR_SIZE_PATHOLOGIC = col_double()
)
```

**TUMOR_REGISTRY2_SPEC** (140 columns -- use .default strategy):
```r
TUMOR_REGISTRY2_SPEC <- cols(
  .default = col_character(),
  DXAGE = col_integer()
)
```

**TUMOR_REGISTRY3_SPEC** (140 columns -- use .default strategy):
```r
TUMOR_REGISTRY3_SPEC <- cols(
  .default = col_character(),
  DXAGE = col_integer()
)
```

**Table specs list** -- Map table names to their specs:
```r
TABLE_SPECS <- list(
  ENROLLMENT = ENROLLMENT_SPEC,
  DIAGNOSIS = DIAGNOSIS_SPEC,
  PROCEDURES = PROCEDURES_SPEC,
  PRESCRIBING = PRESCRIBING_SPEC,
  ENCOUNTER = ENCOUNTER_SPEC,
  DEMOGRAPHIC = DEMOGRAPHIC_SPEC,
  TUMOR_REGISTRY1 = TUMOR_REGISTRY1_SPEC,
  TUMOR_REGISTRY2 = TUMOR_REGISTRY2_SPEC,
  TUMOR_REGISTRY3 = TUMOR_REGISTRY3_SPEC
)
```

**load_pcornet_table() function** (per D-10, D-12):
```r
load_pcornet_table <- function(table_name, file_path, col_spec) {
  # Check file exists (per D-10: warn and skip)
  if (!file.exists(file_path)) {
    message(glue("WARNING: {table_name} not found at {file_path}. Skipping."))
    return(NULL)
  }

  # Load with explicit col_types (per D-08)
  df <- read_csv(file_path, col_types = col_spec, show_col_types = FALSE)

  # Parse all date columns with multi-format parser
  # Detect date columns by name pattern (DATE, TIME, BDATE, DOD, DT_*)
  date_cols <- names(df)[str_detect(names(df), "(?i)(DATE|^DT_|^BDATE$|^DOD$|^DT_FU$|DXDATE)")]
  for (col in date_cols) {
    if (is.character(df[[col]])) {
      df[[col]] <- parse_pcornet_date(df[[col]])
    }
  }

  # Print load summary (per D-12)
  n_parse_problems <- nrow(problems(df))
  message(glue("Loaded {table_name}: {format(nrow(df), big.mark=',')} rows, {ncol(df)} columns"))
  if (n_parse_problems > 0) {
    message(glue("  WARNING: {n_parse_problems} parse failures in {table_name}"))
  }

  return(df)
}
```

**Main loading block** (per D-13: store in named list):
```r
message("=" %>% strrep(60))
message("Loading PCORnet CDM tables...")
message("=" %>% strrep(60))

pcornet <- imap(PCORNET_PATHS, function(path, table_name) {
  spec <- TABLE_SPECS[[table_name]]
  if (is.null(spec)) {
    message(glue("WARNING: No col_types spec defined for {table_name}. Using .default = col_character()."))
    spec <- cols(.default = col_character())
  }
  load_pcornet_table(table_name, path, spec)
})

# Summary
loaded_tables <- names(pcornet)[!sapply(pcornet, is.null)]
skipped_tables <- names(pcornet)[sapply(pcornet, is.null)]

message(glue("\n{'=' %>% strrep(60)}"))
message(glue("Loading complete: {length(loaded_tables)}/{length(PCORNET_PATHS)} tables loaded"))
if (length(skipped_tables) > 0) {
  message(glue("Skipped: {paste(skipped_tables, collapse = ', ')}"))
}
message(glue("{'=' %>% strrep(60)}"))
```
  </action>
  <verify>
    <automated>grep -c "ENROLLMENT_SPEC" R/01_load_pcornet.R && grep -c "DIAGNOSIS_SPEC" R/01_load_pcornet.R && grep -c "ENCOUNTER_SPEC" R/01_load_pcornet.R && grep -c "TUMOR_REGISTRY1_SPEC" R/01_load_pcornet.R && grep -c "load_pcornet_table" R/01_load_pcornet.R && grep -c "parse_pcornet_date" R/01_load_pcornet.R && grep -c "PAYER_TYPE_PRIMARY" R/01_load_pcornet.R && grep -c "PAYER_TYPE_SECONDARY" R/01_load_pcornet.R && grep -c "file.exists" R/01_load_pcornet.R && grep -c "pcornet" R/01_load_pcornet.R</automated>
  </verify>
  <acceptance_criteria>
    - R/01_load_pcornet.R contains `source("R/00_config.R")` at top
    - R/01_load_pcornet.R contains `library(readr)` and `library(stringr)` and `library(purrr)`
    - ENROLLMENT_SPEC defines exactly 6 columns: ID, ENR_START_DATE, ENR_END_DATE, CHART, ENR_BASIS, SOURCE -- all col_character()
    - DIAGNOSIS_SPEC defines exactly 14 columns including DX (col_character) and DX_TYPE (col_character)
    - PROCEDURES_SPEC defines exactly 12 columns including PX and PX_TYPE
    - PRESCRIBING_SPEC defines exactly 24 columns including RX_DOSE_ORDERED (col_double) and RX_REFILLS (col_integer)
    - ENCOUNTER_SPEC defines exactly 19 columns including PAYER_TYPE_PRIMARY and PAYER_TYPE_SECONDARY (both col_character)
    - DEMOGRAPHIC_SPEC defines exactly 12 columns including BIRTH_DATE (col_character) and ZIP_CODE (col_character)
    - TUMOR_REGISTRY1_SPEC uses `.default = col_character()` with AGE_AT_DIAGNOSIS as col_integer()
    - TUMOR_REGISTRY2_SPEC and TUMOR_REGISTRY3_SPEC use `.default = col_character()` with DXAGE as col_integer()
    - TABLE_SPECS list maps all 9 table names to their specs
    - load_pcornet_table function contains `file.exists(file_path)` check
    - load_pcornet_table function contains `read_csv(file_path, col_types = col_spec, show_col_types = FALSE)`
    - load_pcornet_table function contains `parse_pcornet_date(` call for date column parsing
    - load_pcornet_table function contains `message(glue(` for load summary
    - Main loading block creates `pcornet` via `imap(PCORNET_PATHS, ...)`
    - Loading summary prints count of loaded vs skipped tables
    - All ID columns (ID, ENCOUNTERID, DIAGNOSISID, PROCEDURESID, PRESCRIBINGID, PROVIDERID) are col_character()
  </acceptance_criteria>
  <done>R/01_load_pcornet.R exists with explicit col_types specifications for all 9 primary tables (matching csv_columns.txt exactly), load_pcornet_table() helper with file-exists check and auto date parsing, and main loading block that creates pcornet named list. All IDs are character, all dates are loaded as character then parsed via parse_pcornet_date(). Missing files produce warnings not errors.</done>
</task>

</tasks>

<verification>
After Plan 02 completes:
1. Verify R/01_load_pcornet.R sources R/00_config.R (which auto-loads utils)
2. Verify all 9 table specs are defined and mapped in TABLE_SPECS
3. Verify ENROLLMENT_SPEC has 6 columns, DIAGNOSIS_SPEC has 14, ENCOUNTER_SPEC has 19 (matching csv_columns.txt)
4. Verify PAYER_TYPE_PRIMARY and PAYER_TYPE_SECONDARY are in ENCOUNTER_SPEC (critical for Phase 2)
5. Verify DX and DX_TYPE are in DIAGNOSIS_SPEC (critical for Phase 3 ICD matching)
6. Verify load_pcornet_table handles missing files gracefully (file.exists check)
7. Verify date columns are identified and parsed via parse_pcornet_date
8. Verify pcornet named list is created via imap over PCORNET_PATHS
</verification>

<success_criteria>
- R/01_load_pcornet.R exists with col_types specs for all 9 primary tables matching csv_columns.txt column names and counts exactly
- load_pcornet_table() function: checks file existence, loads with explicit types, parses dates, prints summary
- pcornet named list created with all loaded tables accessible as pcornet$TABLENAME
- All ID columns specified as col_character() (no numeric inference)
- All date columns loaded as character and parsed via parse_pcornet_date()
- LOAD-01 requirement satisfied: user can load CDM tables with explicit col_types
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-loading/01-02-SUMMARY.md`
</output>
