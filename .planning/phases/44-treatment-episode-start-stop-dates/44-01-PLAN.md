---
phase: 44-treatment-episode-start-stop-dates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/44_treatment_episodes.R
  - R/44_test_episodes.R
autonomous: false
requirements: [PHASE-44-GOAL]

must_haves:
  truths:
    - "Each patient-type combination has one row per treatment episode (not collapsed to patient-level)"
    - "Episode numbering is contiguous per patient per treatment type (1, 2, 3...)"
    - "Episodes with ALL dates before 2012-01-01 are flagged historical_flag=TRUE"
    - "Historical single-date episodes have start=stop and length=0"
    - "Modern multi-date episodes have start <= stop and length >= 0"
    - "RDS artifact, styled xlsx, and per-type CSVs are all produced"
    - "Phase 43 outputs are unchanged"
  artifacts:
    - path: "R/44_treatment_episodes.R"
      provides: "Per-episode treatment date extraction and output generation"
      min_lines: 200
    - path: "R/44_test_episodes.R"
      provides: "Verification script for episode-level output"
      min_lines: 80
    - path: "treatment_episodes.rds (in CONFIG$cache$outputs_dir)"
      provides: "Episode-level RDS artifact"
    - path: "treatment_episodes.xlsx (in CONFIG$output_dir)"
      provides: "Styled xlsx with summary + per-type sheets"
    - path: "chemotherapy_episodes.csv (in CONFIG$output_dir)"
      provides: "Per-type CSV output"
    - path: "radiation_episodes.csv (in CONFIG$output_dir)"
      provides: "Per-type CSV output"
    - path: "sct_episodes.csv (in CONFIG$output_dir)"
      provides: "Per-type CSV output"
    - path: "immunotherapy_episodes.csv (in CONFIG$output_dir)"
      provides: "Per-type CSV output"
  key_links:
    - from: "R/44_treatment_episodes.R"
      to: "R/43_treatment_durations.R"
      via: "source() to reuse extract_all_dates() and stack_and_dedup()"
      pattern: 'source\("R/43_treatment_durations.R"\)'
    - from: "R/44_treatment_episodes.R"
      to: "R/00_config.R"
      via: "source() for CONFIG paths and TREATMENT_CODES"
      pattern: 'source\("R/00_config.R"\)'
    - from: "R/44_treatment_episodes.R"
      to: "treatment_episodes.rds"
      via: "saveRDS()"
      pattern: "saveRDS.*treatment_episodes\\.rds"
    - from: "R/44_test_episodes.R"
      to: "treatment_episodes.rds"
      via: "readRDS() to load and validate"
      pattern: "readRDS.*treatment_episodes\\.rds"
---

<objective>
Create per-patient, per-episode treatment start/stop date output with historical date flagging.

Purpose: Phase 43 collapses episode-level data to per-patient summaries. Phase 44 exposes the intermediate per-episode detail -- one row per patient per treatment type per episode -- enabling longitudinal treatment course analysis, relapse detection, and separation of modern PCORnet data (2012-2025) from historical tumor registry records (1970s-2000s).

Output: R/44_treatment_episodes.R script producing RDS artifact + styled xlsx + 4 per-type CSVs, plus R/44_test_episodes.R verification script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-establish-treatment-lengths-for-sct-chemo-and-radiation/43-01-SUMMARY.md

<interfaces>
<!-- Key functions and patterns the executor needs from Phase 43. -->
<!-- Executor should reuse these directly via source("R/43_treatment_durations.R"). -->

From R/43_treatment_durations.R:
```r
# Line 48: Gap threshold constant
GAP_THRESHOLD <- 90

# Line 51: Treatment type list
TREATMENT_TYPES <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy")

# Lines 62-67: Color scheme for xlsx styling
TREATMENT_TYPE_COLORS <- list(
  Chemotherapy  = list(fill = "FFDCEEFB", font = "FF0B5394"),
  Radiation     = list(fill = "FFDDF4E1", font = "FF274E13"),
  SCT           = list(fill = "FFFFF4D6", font = "FF7F6000"),
  Immunotherapy = list(fill = "FFE8DCF4", font = "FF4C1D7A")
)

# Lines 87-102: Main extraction dispatcher
extract_all_dates <- function(type)
# Returns tibble with columns: ID (character), treatment_date (Date)

# Lines 408-447: Stack and deduplicate helper
stack_and_dedup <- function(sources, type_name)
# Returns tibble with columns: ID (character), treatment_date (Date)

# Lines 464-503: Duration calculation (Phase 44 modifies this pattern)
calculate_durations_and_episodes <- function(dates_df, gap_threshold = GAP_THRESHOLD)
# Returns tibble: ID, first_treatment_date, last_treatment_date,
#   overall_span_days, distinct_treatment_dates, episode_count
# KEY: Lines 486-492 compute per-episode intermediate that Phase 44 needs
```

From R/00_config.R:
```r
CONFIG$output_dir        # Path for xlsx, CSV, PNG outputs
CONFIG$cache$outputs_dir # Path for RDS artifacts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/44_treatment_episodes.R with per-episode output pipeline</name>
  <files>R/44_treatment_episodes.R</files>
  <read_first>
    R/43_treatment_durations.R
    R/00_config.R
    R/01_load_pcornet.R
    .planning/phases/44-treatment-episode-start-stop-dates/44-CONTEXT.md
    .planning/phases/44-treatment-episode-start-stop-dates/44-RESEARCH.md
  </read_first>
  <action>
Create `R/44_treatment_episodes.R` following the Phase 43 script structure. The script sources Phase 43's functions directly (`source("R/43_treatment_durations.R")`) to reuse `extract_all_dates()` -- NO duplication of extraction logic. It defines a NEW function `calculate_episodes_detailed()` that stops at the per-episode level instead of collapsing to per-patient summary.

**SECTION 1: SETUP AND CONFIGURATION**
- `suppressPackageStartupMessages()` block loading: dplyr, stringr, glue, purrr, openxlsx2
- `source("R/00_config.R")` and `source("R/01_load_pcornet.R")`
- `source("R/43_treatment_durations.R")` to reuse `extract_all_dates()`, `GAP_THRESHOLD`, `TREATMENT_TYPES`, `TREATMENT_TYPE_COLORS`
- Define output paths:
  - `OUTPUT_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")`
  - `OUTPUT_XLSX <- file.path(CONFIG$output_dir, "treatment_episodes.xlsx")`
- Define `HISTORICAL_CUTOFF <- as.Date("2012-01-01")` per D-02

**SECTION 2: EPISODE CALCULATION FUNCTION**
Define `calculate_episodes_detailed(dates_df, gap_threshold = GAP_THRESHOLD)`:
- Empty input guard returning empty tibble with all 7 columns from D-08 schema (patient_id=character, episode_number=integer, episode_start=Date, episode_stop=Date, episode_length_days=numeric, distinct_dates_in_episode=integer, historical_flag=logical)
- Core pipeline (adapted from R/43 lines 476-492, stopping BEFORE the per-patient collapse at line 495):
  ```r
  dates_df %>%
    group_by(ID) %>%
    arrange(treatment_date, .by_group = TRUE) %>%
    mutate(
      days_since_prev = as.numeric(treatment_date - lag(treatment_date)),
      new_episode = is.na(days_since_prev) | days_since_prev >= gap_threshold,
      episode_id = cumsum(new_episode)
    ) %>%
    group_by(ID, episode_id) %>%
    summarise(
      episode_start = min(treatment_date),
      episode_stop = max(treatment_date),
      episode_length_days = as.numeric(max(treatment_date) - min(treatment_date)),
      distinct_dates_in_episode = n(),
      .groups = "drop"
    )
  ```
- Then add episode_number per patient (per D-08, D-09 -- numbering resets per patient, computed WITHIN the already-filtered-by-type data so episode_number is per-patient-per-type):
  ```r
  %>% group_by(ID) %>%
     mutate(episode_number = row_number()) %>%
     ungroup()
  ```
- Then add historical_flag per D-02/D-03 (using `episode_stop < HISTORICAL_CUTOFF` because if the LAST date is pre-2012, ALL dates are pre-2012):
  ```r
  %>% mutate(historical_flag = episode_stop < HISTORICAL_CUTOFF)
  ```
- Final select to match D-08 schema exactly:
  ```r
  %>% select(
       patient_id = ID,
       episode_number,
       episode_start,
       episode_stop,
       episode_length_days,
       distinct_dates_in_episode,
       historical_flag
     )
  ```

**SECTION 3: CONSOLE SUMMARY FUNCTION**
Define `log_episode_stats(episodes_df, type_name)`:
- Guard for 0-row input
- Compute: n_patients (n_distinct(patient_id)), n_episodes (nrow), n_historical (sum(historical_flag)), pct_historical (round(100 * mean(historical_flag), 1)), median_length (median(episode_length_days)), median_dates (median(distinct_dates_in_episode))
- Print via glue() messages:
  ```
  {type_name} Summary:
    Patients: {n_patients}
    Episodes: {n_episodes} ({n_historical} historical, {pct_historical}%)
    Episode length (days): median={median_length}
    Dates per episode: median={median_dates}
  ```

**SECTION 4: MAIN EXECUTION LOOP**
- `message("=== Phase 44: Treatment Episode Start/Stop Dates ===\n")`
- Loop over `TREATMENT_TYPES`:
  - `dates_df <- extract_all_dates(type)` (reused from R/43)
  - `episodes_df <- calculate_episodes_detailed(dates_df)`
  - `episodes_df <- episodes_df %>% mutate(treatment_type = type)`
  - `log_episode_stats(episodes_df, type)`
  - Store in `episodes_list[[type]] <- episodes_df`
- Combine: `all_episodes <- bind_rows(episodes_list) %>% select(patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag)`
- Save RDS: `saveRDS(all_episodes, OUTPUT_RDS)`
- Log: `message(glue("\nRDS saved: {OUTPUT_RDS} ({nrow(all_episodes)} rows)"))`

**SECTION 5: PER-TYPE CSV OUTPUT** (per D-07)
- `message("\n--- Writing per-type CSV files ---")`
- Loop over TREATMENT_TYPES:
  - `type_data <- episodes_list[[type]]`
  - `csv_name <- paste0(tolower(gsub(" ", "_", type)), "_episodes.csv")`
  - `csv_path <- file.path(CONFIG$output_dir, csv_name)`
  - `write_df <- type_data %>% select(patient_id, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag)`
  - `write.csv(write_df, csv_path, row.names = FALSE)`
  - `message(glue("  Wrote {csv_path} ({nrow(write_df)} episodes)"))`

**SECTION 6: STYLED XLSX REPORT** (per D-07, following R/43 pattern lines 599-742)
- Create workbook: `wb <- wb_workbook()`

Sheet 1 "Summary":
- Row 1: Title "Treatment Episodes by Type" (Calibri 16, bold, FF1F2937, merged A1:H1)
- Row 2: Subtitle `"Generated: {Sys.Date()} | Gap threshold: {GAP_THRESHOLD} days | Historical cutoff: 2012-01-01"` (Calibri 10, FF6B7280, merged A2:H2)
- Row 4: Headers with dark fill (FF374151) and white font (FFFFFFFF):
  `c("Treatment Type", "Patients", "Episodes", "Historical Episodes", "% Historical", "Median Length (days)", "Median Dates/Episode", "Max Episodes")`
- Rows 5-8: One row per treatment type, computed from episodes_list:
  - n_patients = n_distinct(patient_id)
  - n_episodes = n()
  - n_historical = sum(historical_flag)
  - pct_historical = round(100 * mean(historical_flag), 1)
  - median_length = median(episode_length_days)
  - median_dates = median(distinct_dates_in_episode)
  - max_episodes = max(episode_number)
  - Type name cell gets TREATMENT_TYPE_COLORS fill and font (same as R/43 lines 668-673)
- Number formatting: `#,##0` on numeric columns, `#,##0.0` on pct column
- Column widths: c(20, 12, 12, 18, 14, 22, 20, 16)

Sheets 2-5: Per-type detail sheets (named "{Type} Episodes"):
- Row 1: Title `"{Type} Treatment Episodes ({n} episodes, {p} patients)"` (Calibri 16, bold, merged)
- Row 2: Headers with type-specific TREATMENT_TYPE_COLORS fill/font:
  `c("Patient ID", "Episode #", "Start Date", "Stop Date", "Length (days)", "Distinct Dates", "Historical")`
- Row 3+: Bulk data write using data.frame with columns:
  - Patient_ID = patient_id
  - Episode_Num = episode_number
  - Start_Date = as.character(episode_start)
  - Stop_Date = as.character(episode_stop)
  - Length_Days = episode_length_days
  - Distinct_Dates = distinct_dates_in_episode
  - Historical = historical_flag
- For rows where historical_flag is TRUE, apply gray fill (FFE5E5E5) to make historical rows visually distinct
- Number formatting: `#,##0` on Length_Days and Distinct_Dates columns
- Column widths: c(20, 12, 15, 15, 15, 15, 12)

Sheet 6 "Historical Summary" (optional but useful):
- Title: "Historical Episodes (pre-2012)"
- Table: Filter all_episodes to historical_flag==TRUE, show by type: count, decade distribution
- If no historical episodes exist, write "No historical episodes found"

Save workbook: `wb$save(OUTPUT_XLSX)`

**SECTION 7: FINAL SUMMARY**
- Print output file paths and total episode count
- Format like R/43 lines 779-787

**Decision traceability comments at top of file:**
```r
# Decision traceability:
#   D-01: historical episodes included with historical_flag boolean column
#   D-02: historical cutoff = before 2012-01-01
#   D-03: episode flagged historical when ALL dates < 2012-01-01 (using episode_stop)
#   D-04: single-date historical episodes get start=stop, length=0
#   D-05: new script alongside Phase 43 (Phase 43 unchanged)
#   D-06: new file R/44_treatment_episodes.R
#   D-07: outputs RDS + styled xlsx + per-type CSVs
#   D-08: columns: patient_id, treatment_type, episode_number, episode_start,
#          episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag
#   D-09: one row per patient per treatment type per episode
#   D-10: 90-day gap threshold (from Phase 43)
#   D-11: all chemo codes pooled (from Phase 43)
#   D-12: four treatment types (from Phase 43)
#   D-13: pre-2000 dates are real tumor registry data
```
  </action>
  <verify>
    <automated>Rscript -e "source('R/44_treatment_episodes.R')" 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - R/44_treatment_episodes.R contains `source("R/43_treatment_durations.R")`
    - R/44_treatment_episodes.R contains `calculate_episodes_detailed <- function(`
    - R/44_treatment_episodes.R contains `HISTORICAL_CUTOFF <- as.Date("2012-01-01")`
    - R/44_treatment_episodes.R contains `historical_flag = episode_stop < HISTORICAL_CUTOFF`
    - R/44_treatment_episodes.R contains `episode_number = row_number()`
    - R/44_treatment_episodes.R contains `select(patient_id = ID,`
    - R/44_treatment_episodes.R contains `saveRDS(all_episodes, OUTPUT_RDS)`
    - R/44_treatment_episodes.R contains `treatment_episodes.rds`
    - R/44_treatment_episodes.R contains `treatment_episodes.xlsx`
    - R/44_treatment_episodes.R contains `_episodes.csv`
    - R/44_treatment_episodes.R contains `wb_workbook()`
    - R/44_treatment_episodes.R contains `Historical Summary` or `Historical` sheet reference
    - R/44_treatment_episodes.R contains all decision IDs D-01 through D-13 in comments
    - Script runs without error and produces RDS, xlsx, and 4 CSV files
  </acceptance_criteria>
  <done>R/44_treatment_episodes.R produces per-episode output with correct D-08 schema (patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag). Historical episodes flagged per D-02/D-03. RDS saved to CONFIG$cache$outputs_dir, xlsx and CSVs to CONFIG$output_dir. Phase 43 outputs unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Create R/44_test_episodes.R verification script</name>
  <files>R/44_test_episodes.R</files>
  <read_first>
    R/43_test_durations.R
    R/44_treatment_episodes.R
    R/00_config.R
    .planning/phases/44-treatment-episode-start-stop-dates/44-CONTEXT.md
  </read_first>
  <action>
Create `R/44_test_episodes.R` following the Phase 43 verification script pattern (R/43_test_durations.R). Loads the `treatment_episodes.rds` artifact and runs structural, data quality, historical flag, and clinical plausibility checks.

**SECTION 1: SETUP**
```r
suppressPackageStartupMessages({
  library(dplyr)
  library(glue)
})
source("R/00_config.R")

RDS_PATH <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")
if (!file.exists(RDS_PATH)) {
  stop(glue("RDS not found: {RDS_PATH}\nRun R/44_treatment_episodes.R first."))
}
d <- readRDS(RDS_PATH)
message("=== Phase 44 Verification: Treatment Episode Checks ===\n")
```

**SECTION 2: STRUCTURE CHECK**
- Verify all 8 expected columns present: `patient_id`, `treatment_type`, `episode_number`, `episode_start`, `episode_stop`, `episode_length_days`, `distinct_dates_in_episode`, `historical_flag`
- Log row count, unique patient count, types present
- Verify all 4 expected types present: Chemotherapy, Radiation, SCT, Immunotherapy
- Verify column types: patient_id is character, episode_number is integer/numeric, episode_start/stop are Date, episode_length_days is numeric, distinct_dates_in_episode is integer/numeric, historical_flag is logical

**SECTION 3: PER-TYPE SUMMARY STATS**
- Group by treatment_type, compute: n_patients (n_distinct(patient_id)), n_episodes (n()), n_historical (sum(historical_flag)), pct_historical, median_length, max_length, median_dates, max_dates, max_episode_number
- Print each type's stats via glue() messages

**SECTION 4: DATA QUALITY CHECKS**
- Negative episode_length_days: `filter(episode_length_days < 0)` -- should be 0
- episode_stop before episode_start: `filter(episode_stop < episode_start)` -- should be 0
- Zero length but multiple dates: `filter(episode_length_days == 0 & distinct_dates_in_episode > 1)` -- WARN (same-day from different sources)
- Episode numbering contiguity: Per patient per treatment_type, verify n_episodes == max(episode_number):
  ```r
  contiguity <- d %>%
    group_by(patient_id, treatment_type) %>%
    summarise(n_ep = n(), max_ep = max(episode_number), .groups = "drop") %>%
    filter(n_ep != max_ep)
  ```
  Should be 0 rows. If nonzero, print FAIL with details.
- Duplicate patient-type-episode combos: `count(patient_id, treatment_type, episode_number) %>% filter(n > 1)` -- should be 0
- Future dates: `filter(episode_stop > Sys.Date())` -- should be 0
- historical_flag consistency: Verify `all(d$historical_flag == (d$episode_stop < as.Date("2012-01-01")))` -- should be TRUE

**SECTION 5: HISTORICAL DATE ANALYSIS**
- Count historical episodes by type
- Decade distribution of historical episode_start dates (using `10 * (as.integer(format(episode_start, "%Y")) %/% 10)`)
- Verify D-04: For historical single-date episodes (distinct_dates_in_episode == 1), confirm episode_start == episode_stop and episode_length_days == 0
- Check for "bridge episodes" (episode with start < 2012 AND stop >= 2012) -- should be 0 because 90-day gap splitting should separate historical from modern dates. If found, WARN.

**SECTION 6: CROSS-REFERENCE WITH PHASE 43**
- Load Phase 43 RDS: `d43 <- readRDS(file.path(CONFIG$cache$outputs_dir, "treatment_durations.rds"))`
- Per-patient episode counts from Phase 44 should match Phase 43's `episode_count`:
  ```r
  ep_counts_44 <- d %>%
    group_by(patient_id, treatment_type) %>%
    summarise(n_episodes_44 = n(), .groups = "drop")
  ep_counts_43 <- d43 %>%
    select(patient_id = ID, treatment_type, episode_count)
  merged <- left_join(ep_counts_44, ep_counts_43, by = c("patient_id", "treatment_type"))
  mismatches <- merged %>% filter(n_episodes_44 != episode_count)
  ```
  If mismatches > 0, print FAIL. If 0, print OK.

**SECTION 7: OUTPUT FILE CHECKS**
- Check existence and size of: treatment_episodes.rds, treatment_episodes.xlsx, chemotherapy_episodes.csv, radiation_episodes.csv, sct_episodes.csv, immunotherapy_episodes.csv
- Use same `check_file()` helper pattern as R/43_test_durations.R

**SECTION 8: FINAL**
- `message("\n=== Phase 44 Verification Complete ===")`
  </action>
  <verify>
    <automated>Rscript -e "source('R/44_test_episodes.R')" 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - R/44_test_episodes.R contains `treatment_episodes.rds`
    - R/44_test_episodes.R contains `treatment_durations.rds` (cross-reference with Phase 43)
    - R/44_test_episodes.R contains check for all 8 columns: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag
    - R/44_test_episodes.R contains `episode_stop < as.Date("2012-01-01")` historical flag consistency check
    - R/44_test_episodes.R contains episode numbering contiguity check (`n_ep != max_ep`)
    - R/44_test_episodes.R contains cross-reference with Phase 43 episode_count
    - R/44_test_episodes.R contains output file existence checks for xlsx and 4 CSVs
    - Script runs without error after R/44_treatment_episodes.R has been run
    - All checks pass (OK status, no FAIL messages)
  </acceptance_criteria>
  <done>R/44_test_episodes.R validates the treatment_episodes.rds artifact: correct schema, no data quality issues, contiguous episode numbering, historical_flag consistency with 2012 cutoff, cross-reference with Phase 43 episode counts matches, all output files exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify episode output on HiPerGator</name>
  <files>R/44_treatment_episodes.R, R/44_test_episodes.R</files>
  <action>
User verifies that the Phase 44 episode output runs correctly on HiPerGator and produces expected results. All automated work is complete -- this checkpoint confirms visual and functional correctness.
  </action>
  <verify>User confirms all checks pass and xlsx looks correct</verify>
  <done>User approves episode output: correct schema, historical flagging works, xlsx is well-formatted, Phase 43 outputs unchanged</done>
  <what-built>Per-episode treatment timeline output (R/44_treatment_episodes.R) producing RDS, styled xlsx, and 4 per-type CSVs with historical date flagging. Verification script (R/44_test_episodes.R) validates structural integrity and cross-references Phase 43.</what-built>
  <how-to-verify>
    1. On HiPerGator, run: `Rscript -e "source('R/44_treatment_episodes.R')"`
    2. Verify console output shows episode counts per type and historical episode percentages
    3. Run verification: `Rscript -e "source('R/44_test_episodes.R')"`
    4. Confirm all checks pass (no FAIL messages)
    5. Open treatment_episodes.xlsx and verify:
       - Summary sheet shows episode counts, historical counts, and percentages per type
       - Per-type detail sheets have correct column headers (Patient ID, Episode #, Start Date, Stop Date, Length (days), Distinct Dates, Historical)
       - Historical rows (if any) have gray fill for visual distinction
       - Historical Summary sheet shows decade distribution
    6. Spot-check a CSV file (e.g., chemotherapy_episodes.csv) to confirm D-08 column schema
    7. Confirm Phase 43 outputs (treatment_durations.rds, treatment_durations.xlsx) are unchanged
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase 44 is complete when:
1. R/44_treatment_episodes.R runs end-to-end producing all output artifacts
2. R/44_test_episodes.R passes all checks including Phase 43 cross-reference
3. Output schema matches D-08: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag
4. Historical episodes correctly flagged per D-02/D-03 (episode_stop < 2012-01-01)
5. Phase 43 outputs remain unchanged
</verification>

<success_criteria>
- treatment_episodes.rds exists with one row per patient per treatment type per episode
- treatment_episodes.xlsx has Summary + 4 per-type detail sheets + Historical Summary sheet
- 4 per-type CSVs (chemotherapy_episodes.csv, radiation_episodes.csv, sct_episodes.csv, immunotherapy_episodes.csv) exist in CONFIG$output_dir
- Episode numbering is contiguous per patient per type (1, 2, 3...)
- historical_flag correctly flags all pre-2012 episodes
- Phase 43 episode counts match Phase 44 episode row counts per patient per type
- No FAIL messages in verification script output
</success_criteria>

<output>
After completion, create `.planning/phases/44-treatment-episode-start-stop-dates/44-01-SUMMARY.md`
</output>
