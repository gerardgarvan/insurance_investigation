---
phase: 129-attribution-linkage-and-output
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - R/112_doi_attribution_report.R
autonomous: true
requirements: [DOI-ATTR-01, DOI-ATTR-02, DOI-ATTR-03]
must_haves:
  truths:
    - "A new script R/112_doi_attribution_report.R exists and is classification-independent (R/111 unchanged)"
    - "Rituximab/MTX administrations are read from treatment_episode_detail.rds (read-only), filtered to RITUXIMAB_CODES | MTX_CODES on triggering_code"
    - "A dated HL-diagnosis DuckDB pull retains DX_DATE and is native-filtered (DX_TYPE-gated, hl_icd10/hl_icd9) before collect()"
    - "Drug administrations link to DoI encounters two-tier: ENCOUNTERID equi-join (tier 1) then ±DOI_ATTRIBUTION_WINDOW_DAYS PATID window (tier 2)"
    - "attribution_method column carries exactly encounter_id / temporal_window / none"
    - "likely_non_lymphoma_directed is three-state TRUE / FALSE / NA; NA fires when a dated HL dx falls within ±window of the drug admin; NA is never coerced to FALSE"
    - "No column or prose uses '_for_' causal language"
  artifacts:
    - path: "R/112_doi_attribution_report.R"
      provides: "Attribution engine: inputs load, dated HL pull, two-tier linkage, three-state flag, attribution_method"
      min_lines: 120
      contains: "DOI_ATTRIBUTION_WINDOW_DAYS"
  key_links:
    - from: "R/112_doi_attribution_report.R"
      to: "treatment_episode_detail.rds"
      via: "readRDS + filter triggering_code %in% c(RITUXIMAB_CODES..., MTX_CODES...)"
      pattern: "triggering_code"
    - from: "R/112_doi_attribution_report.R"
      to: "doi_encounters.rds"
      via: "ENCOUNTERID equi-join then PATID temporal window"
      pattern: "ENCOUNTERID"
    - from: "R/112_doi_attribution_report.R"
      to: "DIAGNOSIS (DuckDB)"
      via: "native DX_TYPE + hl_icd10/hl_icd9 filter, retain DX_DATE, before collect()"
      pattern: "hl_icd10"
---

<objective>
Build the analytic engine of the new attribution script `R/112_doi_attribution_report.R`: load the Phase 128 artifacts and drug administrations, run the only-permitted new DuckDB pull (a DATED HL-diagnosis pull), perform the two-tier drug↔DoI linkage, and derive the three-state `likely_non_lymphoma_directed` flag plus the `attribution_method` column.

Purpose: Produces the honest co-occurrence signal (DOI-ATTR-01/02/03) that Plan 129-02 renders into the 4-sheet workbook. Co-occurrence only — never causal.

Output: `R/112_doi_attribution_report.R` Sections 1–5 (setup/bootstrap, inputs load, dated HL pull, two-tier linkage, three-state flag). An in-memory linked frame (`doi_drug_links`) ready for Plan 02's sheet assembly. NO xlsx write in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-attribution-linkage-and-output/129-CONTEXT.md

<interfaces>
<!-- Concrete contracts the executor needs. Do NOT re-explore the codebase for these. -->

treatment_episode_detail.rds (R/26, read-only) columns:
  patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID,
  drug_name, episode_number, episode_start, episode_stop, historical_flag

doi_encounters.rds (R/111, read-only) columns:
  ID, ENCOUNTERID, DX_DATE, doi_code, doi_category, paraneoplastic_flag, in_hl_cohort

doi_patients.rds (R/111, read-only) columns:
  ID, has_any_doi, doi_categories, doi_first_date, doi_last_date, n_doi_encounters, in_hl_cohort

Config constants (R/00_config.R):
  RITUXIMAB_CODES = list(hcpcs=c("J9310","J9311","J9312"), rxnorm=c("121191"))
  MTX_CODES       = list(hcpcs=c("J9250","J9260"), rxnorm=c("6851","105585", ... 22 CUIs))
  DOI_ATTRIBUTION_WINDOW_DAYS <- 90L   # named constant, NEVER a literal 90
  ICD_CODES$hl_icd10 / ICD_CODES$hl_icd9   # HL code vectors (dotted, e.g. "C81.00", "201.0")
  CONFIG$cache$outputs_dir                 # where the .rds artifacts live

DuckDB idiom (R/utils/utils_duckdb.R):
  USE_DUCKDB <- TRUE; open_pcornet_con() (idempotent)
  get_pcornet_table("DIAGNOSIS")  -> lazy tbl; filter() BEFORE collect() = native pushdown
  close_pcornet_con()  -> teardown (call at end of Plan 02, NOT here)

get_hl_patient_ids() HL filter to MIRROR (utils_treatment.R §62-88) — but it returns IDs only:
  filter((DX_TYPE == "10" & DX %in% ICD_CODES$hl_icd10) |
         (DX_TYPE == "09" & DX %in% ICD_CODES$hl_icd9))
  YOU MUST retain DX_DATE (select ID, DX_DATE) — IDs-only is INSUFFICIENT for the NA test (D-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script header, DuckDB bootstrap, inputs load, dated HL pull</name>
  <files>R/112_doi_attribution_report.R</files>
  <read_first>
    - R/111_doi_classification.R (§1-48 header block + §1 setup + §3 self-bootstrap — mirror header format, library set, source() order, USE_DUCKDB <- TRUE / open_pcornet_con())
    - R/107_med_admin_dispensing_gap_diagnostic.R (representative investigation-script header + DuckDB self-bootstrap idiom)
    - R/utils/utils_treatment.R §62-88 (get_hl_patient_ids HL filter to mirror — MUST add DX_DATE)
    - R/utils/utils_duckdb.R (open_pcornet_con, get_pcornet_table, native-filter-then-collect idiom)
    - R/00_config.R §564-590 (RITUXIMAB_CODES, MTX_CODES, DOI_ATTRIBUTION_WINDOW_DAYS) and §282-324 (ICD_CODES$hl_icd10 / hl_icd9) and the CONFIG$cache$outputs_dir definition (§142)
    - .planning/phases/128-doi-classification/128-01-SUMMARY.md and 128-02-SUMMARY.md (doi_encounters.rds / doi_patients.rds columns as built)
  </read_first>
  <action>
    Create R/112_doi_attribution_report.R (NEW file — do NOT touch R/111; D-04).

    HEADER (mirror R/111 §1-48 banner format): title line "112_doi_attribution_report.R -- DoI Drug Co-occurrence Attribution Report". Purpose block states: reads doi_encounters.rds + doi_patients.rds (R/111) + treatment_episode_detail.rds (R/26, read-only), performs two-tier drug↔DoI linkage, derives three-state likely_non_lymphoma_directed, and (Plan 02) writes doi_attribution_report.xlsx. State READ-ONLY w.r.t. R/111 artifacts, treatment_episode_detail.rds, utils_cancer.R, R/28. State: "Registration (R/39, SCRIPT_INDEX, R/88) is Phase 130 — not registered here." State Requirements: DOI-ATTR-01/02/03 (this plan), DOI-OUT-01/02/03 (Plan 02).

    SECTION 1 (Setup): suppressPackageStartupMessages({ library(dplyr); library(glue); library(stringr); library(lubridate); library(janitor) }). source("R/00_config.R"); source("R/utils/utils_duckdb.R"); source("R/utils/utils_dates.R"). Defensive: if (!exists("get_hl_patient_ids")) source("R/utils/utils_treatment.R"). Banner message with DOI_ATTRIBUTION_WINDOW_DAYS embedded via glue.

    SECTION 2 (Load inputs, read-only):
      - doi_enc  <- readRDS(file.path(CONFIG$cache$outputs_dir, "doi_encounters.rds"))
      - doi_pat  <- readRDS(file.path(CONFIG$cache$outputs_dir, "doi_patients.rds"))
      - tx_detail <- readRDS(file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail.rds"))
      Build the drug-code vector: rituximab_mtx_codes <- unique(c(RITUXIMAB_CODES$hcpcs, RITUXIMAB_CODES$rxnorm, MTX_CODES$hcpcs, MTX_CODES$rxnorm)).
      drug_admins <- tx_detail %>% filter(triggering_code %in% rituximab_mtx_codes) %>%
        mutate(drug_class = if_else(triggering_code %in% c(RITUXIMAB_CODES$hcpcs, RITUXIMAB_CODES$rxnorm), "rituximab", "methotrexate")) %>%
        select(patient_id, treatment_date, triggering_code, ENCOUNTERID, drug_name, drug_class, historical_flag).
      Rename patient_id -> ID for a consistent PATID join key with doi_enc (which uses ID). Log nrow(drug_admins) and n_distinct(ID) via glue.

    SECTION 3 (Dated HL pull — the ONLY new DuckDB query; D-03):
      USE_DUCKDB <- TRUE; open_pcornet_con() (idempotent). Mirror get_hl_patient_ids() HL filter but RETAIN DX_DATE and keep DX_TYPE gating, native-filtered before collect():
        hl_dx_dated <- get_pcornet_table("DIAGNOSIS") %>%
          filter((DX_TYPE == "10" & DX %in% ICD_CODES$hl_icd10) |
                 (DX_TYPE == "09" & DX %in% ICD_CODES$hl_icd9)) %>%
          select(ID, DX_DATE) %>%
          collect()
      Then parse dates: hl_dx_dated <- hl_dx_dated %>% mutate(DX_DATE = parse_pcornet_date(DX_DATE)) %>% filter(!is.na(DX_DATE) & year(DX_DATE) != 1900L) %>% distinct(ID, DX_DATE). Log nrow and n_distinct(ID). Do NOT close the connection here (Plan 02 owns close_pcornet_con() teardown).

    Comment discipline: the filter-before-collect ordering must be visually obvious — do NOT write the word "collect" earlier in an inline comment on the filter line (a prior phase hit that grep gotcha). Keep DOI_ATTRIBUTION_WINDOW_DAYS as the named constant everywhere; never write a literal 90.
  </action>
  <verify>
    <automated>test -f R/112_doi_attribution_report.R && grep -q "readRDS" R/112_doi_attribution_report.R && grep -q "hl_icd10" R/112_doi_attribution_report.R && grep -q "DX_DATE" R/112_doi_attribution_report.R</automated>
  </verify>
  <acceptance_criteria>
    - `test -f R/112_doi_attribution_report.R` succeeds (new file created)
    - `git status --short R/111_doi_classification.R` shows no modification (R/111 untouched; D-04)
    - `grep -c 'treatment_episode_detail.rds' R/112_doi_attribution_report.R` >= 1
    - `grep -c 'triggering_code %in%' R/112_doi_attribution_report.R` >= 1 (drug filter present)
    - `grep -q 'RITUXIMAB_CODES' R/112_doi_attribution_report.R` and `grep -q 'MTX_CODES' R/112_doi_attribution_report.R`
    - `grep -q 'get_pcornet_table("DIAGNOSIS")' R/112_doi_attribution_report.R` (the dated HL pull)
    - `grep -q 'ICD_CODES\$hl_icd10' R/112_doi_attribution_report.R` and `grep -q 'ICD_CODES\$hl_icd9' R/112_doi_attribution_report.R`
    - `grep -q 'select(ID, DX_DATE)' R/112_doi_attribution_report.R` (DX_DATE retained — D-03; IDs-only insufficient)
    - `grep -q 'DX_TYPE == "10"' R/112_doi_attribution_report.R` (DX_TYPE gating retained)
    - `grep -c 'close_pcornet_con' R/112_doi_attribution_report.R` == 0 (teardown deferred to Plan 02)
    - `grep -c 'DOI_ATTRIBUTION_WINDOW_DAYS' R/112_doi_attribution_report.R` >= 1
  </acceptance_criteria>
  <done>R/112 exists with header, setup, inputs load (drug_admins filtered to rituximab/MTX from treatment_episode_detail.rds), and the dated HL DuckDB pull (hl_dx_dated with DX_DATE retained). R/111 unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Two-tier linkage + three-state flag + attribution_method</name>
  <files>R/112_doi_attribution_report.R</files>
  <read_first>
    - R/112_doi_attribution_report.R (current state from Task 1 — see drug_admins, doi_enc, hl_dx_dated frames and their columns)
    - .planning/phases/128-doi-classification/128-01-SUMMARY.md (doi_encounters.rds columns: ID, ENCOUNTERID, DX_DATE, doi_code, doi_category, paraneoplastic_flag, in_hl_cohort)
    - .planning/phases/129-attribution-linkage-and-output/129-CONTEXT.md (D-03 three-state semantics; §42-44 attribution_method values + co-occurrence language)
    - R/00_config.R §584-590 (DOI_ATTRIBUTION_WINDOW_DAYS = 90L rationale)
  </read_first>
  <action>
    Append SECTION 4 (Two-tier linkage) and SECTION 5 (Three-state flag) to R/112.

    SECTION 4 — Two-tier drug↔DoI linkage (DOI-ATTR-01). Mirrors R/28 D-01/D-02 (ENCOUNTERID-first, then temporal window):

    TIER 1 (ENCOUNTERID equi-join, higher confidence): inner-join drug_admins to doi_enc on ENCOUNTERID (both non-NA). Guard against NA ENCOUNTERID on either side (filter !is.na(ENCOUNTERID) before the join so blanks don't spuriously match).
      tier1 <- drug_admins %>% filter(!is.na(ENCOUNTERID) & ENCOUNTERID != "") %>%
        inner_join(doi_enc %>% filter(!is.na(ENCOUNTERID) & ENCOUNTERID != ""),
                   by = "ENCOUNTERID", suffix = c("_drug", "_dx")) %>%
        mutate(attribution_method = "encounter_id")
      (After the join ID may appear as ID_drug / ID_dx — coalesce to a single ID: mutate(ID = coalesce(ID_drug, ID_dx)). Keep both DX_DATE and treatment_date.)

    TIER 2 (±window PATID temporal window, for drug admins NOT matched in tier 1): take drug_admins whose (ID, treatment_date, ENCOUNTERID) tuple is not in tier1, PATID-join to doi_enc on ID (= PATID), then keep pairs where abs(as.integer(DX_DATE - treatment_date)) <= DOI_ATTRIBUTION_WINDOW_DAYS.
      tier2 <- drug_unmatched %>%
        inner_join(doi_enc, by = "ID", suffix = c("_drug", "_dx")) %>%
        filter(abs(as.integer(DX_DATE - treatment_date)) <= DOI_ATTRIBUTION_WINDOW_DAYS) %>%
        mutate(attribution_method = "temporal_window")
      Use DOI_ATTRIBUTION_WINDOW_DAYS as the named constant — NEVER a literal 90.

    Combine matched links: doi_drug_links_matched <- bind_rows(tier1, tier2) with a consistent column set — at minimum: ID, drug_class, triggering_code, treatment_date, ENCOUNTERID (drug side), drug_name, doi_code, doi_category, DX_DATE (dx side), paraneoplastic_flag, in_hl_cohort, attribution_method. (Standardize column names before bind_rows so tier1/tier2 align; if a suffix produced ENCOUNTERID.x/.y, retain the drug-side ENCOUNTERID.)

    Also identify drug admins with NO DoI co-occurrence (attribution_method = "none"): drug admins present in neither tier1 nor tier2. Represent these as rows with doi_category = NA and attribution_method = "none" (these carry the FALSE flag in Section 5).
      doi_drug_links <- bind_rows(doi_drug_links_matched, drug_admins_none)  # drug_admins_none has attribution_method = "none"
    attribution_method must take exactly the values: encounter_id / temporal_window / none (DOI-ATTR-03).

    SECTION 5 — Three-state likely_non_lymphoma_directed (DOI-ATTR-02). For each matched drug↔DoI pair, test whether a dated HL diagnosis is active in the same ±window (D-03): join to hl_dx_dated on ID, test any HL DX_DATE within ±DOI_ATTRIBUTION_WINDOW_DAYS of treatment_date.
      hl_active <- doi_drug_links %>% filter(attribution_method != "none") %>%
        select(ID, treatment_date) %>% distinct() %>%
        left_join(hl_dx_dated, by = "ID") %>%
        group_by(ID, treatment_date) %>%
        summarise(hl_active_in_window =
                    any(!is.na(DX_DATE) & abs(as.integer(DX_DATE - treatment_date)) <= DOI_ATTRIBUTION_WINDOW_DAYS),
                  .groups = "drop")
    Join hl_active back to doi_drug_links on (ID, treatment_date), then set the THREE-STATE flag with case_when — order matters, NA must NOT collapse to FALSE:
      likely_non_lymphoma_directed = case_when(
        attribution_method == "none"                 ~ FALSE,   # no drug↔DoI co-occurrence
        hl_active_in_window == TRUE                   ~ NA,      # ambiguous — HL also active in same window
        TRUE                                          ~ TRUE     # DoI co-occurs AND no HL in window
      )
    Force logical NA type: use NA (logical) so the column is a logical vector (TRUE/FALSE/NA), never coerced. Confirm with a comment that NA is preserved intentionally (D-03: NA is the clinically interesting ambiguous state).

    CO-OCCURRENCE LANGUAGE (DOI-ATTR-03): every column and comment uses "with [dx]" phrasing. NO column named rituximab_for_*, mtx_reason_*, or anything containing "_for_". Do not write the word pattern "_for_" anywhere in the script.

    Log a janitor::tabyl(attribution_method) and a tabyl of likely_non_lymphoma_directed (which will show TRUE/FALSE/NA counts) to console for review. Do NOT write any file in this plan.
  </action>
  <verify>
    <automated>grep -q "attribution_method" R/112_doi_attribution_report.R && grep -q "likely_non_lymphoma_directed" R/112_doi_attribution_report.R && test $(grep -c "_for_" R/112_doi_attribution_report.R) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'by = "ENCOUNTERID"' R/112_doi_attribution_report.R` (tier 1 equi-join)
    - `grep -q 'by = "ID"' R/112_doi_attribution_report.R` (tier 2 / hl PATID join)
    - `grep -q 'abs(as.integer(DX_DATE - treatment_date)) <= DOI_ATTRIBUTION_WINDOW_DAYS' R/112_doi_attribution_report.R` (tier 2 temporal window; named constant)
    - `grep -o '"encounter_id"' R/112_doi_attribution_report.R | head -1` matches AND `grep -q '"temporal_window"' R/112...R` AND `grep -q '"none"' R/112...R` (all three attribution_method values present)
    - `grep -q 'hl_active_in_window' R/112_doi_attribution_report.R` (dated HL co-occurrence test from hl_dx_dated)
    - `grep -q 'case_when' R/112_doi_attribution_report.R` and the case_when contains `~ NA` and `~ FALSE` and `~ TRUE` (three states)
    - `grep -c '_for_' R/112_doi_attribution_report.R` == 0 (co-occurrence language enforced — DOI-ATTR-03)
    - `grep -c 'suppress_small' R/112_doi_attribution_report.R` == 0 (D-01: no auto-suppression, even in the engine)
    - `grep -c 'saveWorkbook\|wb\$save\|saveRDS' R/112_doi_attribution_report.R` == 0 (no file write in this plan)
    - Script line count >= 120: `test $(grep -c "" R/112_doi_attribution_report.R) -ge 120`
  </acceptance_criteria>
  <done>doi_drug_links frame exists in-memory carrying ID, drug_class, treatment_date, ENCOUNTERID, doi_category, DX_DATE, in_hl_cohort, attribution_method (encounter_id/temporal_window/none), and the three-state likely_non_lymphoma_directed logical column (TRUE/FALSE/NA, NA not collapsed). No file written; no "_for_" language; no suppress_small.</done>
</task>

</tasks>

<verification>
- R/112_doi_attribution_report.R exists; R/111 unchanged (git status).
- Structural greps in acceptance_criteria all pass (Windows-safe; HiPerGator runtime deferred to Phase 130).
- Two-tier linkage present: ENCOUNTERID equi-join (tier 1) THEN ±DOI_ATTRIBUTION_WINDOW_DAYS PATID window (tier 2).
- attribution_method takes exactly encounter_id / temporal_window / none.
- likely_non_lymphoma_directed is three-state; NA driven by dated HL pull; no NA→FALSE coercion.
- Zero occurrences of "_for_" and zero occurrences of "suppress_small".
</verification>

<success_criteria>
- DOI-ATTR-01: two-tier ENCOUNTERID-then-±90-day-PATID linkage with DOI_ATTRIBUTION_WINDOW_DAYS named constant.
- DOI-ATTR-02: three-state likely_non_lymphoma_directed (TRUE/FALSE/NA) with NA = dated HL active in window.
- DOI-ATTR-03: attribution_method column (encounter_id/temporal_window/none); co-occurrence language throughout (no "_for_").
</success_criteria>

<output>
After completion, create `.planning/phases/129-attribution-linkage-and-output/129-01-SUMMARY.md` documenting the doi_drug_links contract (columns + attribution_method values + three-state flag), the dated HL pull, and that no file is written yet (Plan 02 owns the xlsx write + close_pcornet_con()).
</output>
