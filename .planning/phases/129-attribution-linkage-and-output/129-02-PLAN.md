---
phase: 129-attribution-linkage-and-output
plan: "02"
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - R/112_doi_attribution_report.R
autonomous: true
requirements: [DOI-OUT-01, DOI-OUT-02, DOI-OUT-03]
must_haves:
  truths:
    - "A 4-sheet Tableau-ready workbook doi_attribution_report.xlsx is written: Patient Prevalence, Encounter Co-occurrence, Drug x DoI Summary, Metadata"
    - "Every sheet carries RAW n_patients / n_encounters counts — NO automated small-cell suppression (suppress_small NOT called)"
    - "Every sheet carries the INTERNAL-ONLY raw-counts note and the CAVEATS footnote"
    - "The in_hl_cohort dimension is present on every sheet so HL vs non-HL co-occurrence is comparable"
    - "Encounter Co-occurrence sheet includes the attribution_method column; column names use co-occurrence ('with') language, never '_for_'"
    - "Metadata sheet documents DOI_ATTRIBUTION_WINDOW_DAYS = 90 plus ±30-day and ±180-day sensitivity comparison counts"
    - "The DuckDB connection is closed with close_pcornet_con() at script end"
  artifacts:
    - path: "R/112_doi_attribution_report.R"
      provides: "Sheet assembly (4 data frames), openxlsx2 workbook build, internal-only note + CAVEATS footnote, sensitivity counts, save + teardown"
      contains: "addWorksheet"
  key_links:
    - from: "R/112_doi_attribution_report.R"
      to: "doi_attribution_report.xlsx"
      via: "openxlsx2 wb_workbook() -> 4 worksheets -> wb$save()"
      pattern: "doi_attribution_report.xlsx"
    - from: "R/112_doi_attribution_report.R Metadata sheet"
      to: "DOI_ATTRIBUTION_WINDOW_DAYS + sensitivity"
      via: "±30 / ±90 / ±180 comparison recompute"
      pattern: "180"
---

<objective>
Render the `doi_drug_links` frame from Plan 129-01 into the 4-sheet Tableau-ready workbook `doi_attribution_report.xlsx`, carrying RAW counts (NO suppression per D-01), the internal-only note, the CAVEATS footnote, the `in_hl_cohort` comparison dimension on every sheet, and a Metadata sheet with ±30/±90/±180 sensitivity counts. Close the DuckDB connection.

Purpose: Delivers the internal SME investigation workbook (DOI-OUT-01/02/03) — the hand-off artifact to Phase 130 registration.

Output: `R/112_doi_attribution_report.R` Sections 6–8 (sheet data frames, workbook assembly + save, close_pcornet_con()) and the workbook `doi_attribution_report.xlsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-attribution-linkage-and-output/129-CONTEXT.md
@.planning/phases/129-attribution-linkage-and-output/129-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 129-01. Do NOT re-derive the linkage. -->

doi_drug_links (in-memory, from Plan 129-01 Sections 4-5) columns (at least):
  ID, drug_class ("rituximab"/"methotrexate"), triggering_code, treatment_date,
  ENCOUNTERID, drug_name, doi_code, doi_category, DX_DATE, paraneoplastic_flag,
  in_hl_cohort (logical), attribution_method ("encounter_id"/"temporal_window"/"none"),
  likely_non_lymphoma_directed (logical TRUE/FALSE/NA)

drug_admins (from Plan 129-01 Section 2): ID, treatment_date, triggering_code,
  ENCOUNTERID, drug_name, drug_class, historical_flag  — used for sensitivity recompute
doi_enc (from Plan 129-01 Section 2): ID, ENCOUNTERID, DX_DATE, doi_code, doi_category,
  paraneoplastic_flag, in_hl_cohort  — used for sensitivity recompute
DOI_ATTRIBUTION_WINDOW_DAYS <- 90L (config)
CONFIG$cache$outputs_dir (config)  — output dir for the xlsx

openxlsx2 writer pattern (R/110 §575-683, VERBATIM-adaptable add_styled_sheet helper):
  library(openxlsx2)
  wb <- wb_workbook()
  wb$add_worksheet("Sheet Name")
  wb$add_data(sheet=..., x=title, start_row=1, start_col=1)  # title row 1
  wb$add_data(sheet=..., x=subtitle_text, start_row=2, ...)  # subtitle row 2 (use for note/footnote)
  wb$add_data(sheet=..., x=data, start_row=4, start_col=1)    # data table row 4
  wb$freeze_pane(sheet=..., firstActiveRow=5)
  wb$save(OUT_XLSX)  # wrap in tryCatch so a Windows run with no data does not hard-fail
NOTE: R/110 defines a local suppress_small() — do NOT copy or call it here (D-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 4 sheet data frames (raw counts, in_hl_cohort dimension, sensitivity)</name>
  <files>R/112_doi_attribution_report.R</files>
  <read_first>
    - R/112_doi_attribution_report.R (current state from Plan 129-01 — doi_drug_links, drug_admins, doi_enc column contracts)
    - .planning/phases/129-attribution-linkage-and-output/129-CONTEXT.md (D-01 raw counts + internal-only note text; D-02 in_hl_cohort on every sheet; D-03 sensitivity; §45-47 sheet list + CAVEATS footnote)
    - R/109_med_admin_dispensing_fix_impact_audit.R and R/110_output_level_before_after_report.R (multi-sheet rollup shapes — READ for mechanics only; do NOT call their suppress_small())
    - R/00_config.R §584-590 (DOI_ATTRIBUTION_WINDOW_DAYS)
  </read_first>
  <action>
    Append SECTION 6 (Sheet data frames) to R/112. All counts are RAW — do NOT call suppress_small() (D-01). Multi-value fields collapse ascending-alphabetical with "; " delimiter (v3.2 Phase 112 convention: paste(sort(unique(x)), collapse = "; ")). Carry in_hl_cohort on every sheet (D-02).

    SHEET 1 df_patient_prevalence (patient grain): from doi_drug_links, keep matched links (attribution_method != "none"), group_by(in_hl_cohort, doi_category, drug_class) and summarise:
      n_patients = n_distinct(ID),   # RAW
      n_encounters = n_distinct(ENCOUNTERID),   # RAW
      n_likely_non_lymphoma_directed = sum(likely_non_lymphoma_directed %in% TRUE),   # TRUE only
      n_ambiguous_hl_active = sum(is.na(likely_non_lymphoma_directed))   # NA count — the clinically interesting slice
      Arrange by in_hl_cohort desc, doi_category, drug_class.

    SHEET 2 df_encounter_cooccurrence (encounter grain — MUST include attribution_method, DOI-ATTR-03): from doi_drug_links matched rows, select/rename to co-occurrence columns:
      ID, treatment_date, drug_class, drug_name, ENCOUNTERID,
      doi_category (rename header spirit "co-occurs with doi_category"), DX_DATE,
      attribution_method, in_hl_cohort, likely_non_lymphoma_directed.
      NO column may contain "_for_". Header/prose uses "with" language.
      (This is a row-per-co-occurrence detail sheet; keep it detail-grain, one row per matched drug↔DoI pair.)

    SHEET 3 df_drug_doi_summary (drug × DoI matrix, RAW counts — the sheet the stale roadmap wanted suppressed; D-01 says RAW): group_by(drug_class, doi_category, in_hl_cohort) and summarise:
      n_patients = n_distinct(ID),   # RAW — rare categories (NMO, pemphigus, GPA) will show single digits BY DESIGN
      n_encounters = n_distinct(ENCOUNTERID),   # RAW
      n_encounter_id_method = sum(attribution_method == "encounter_id"),
      n_temporal_window_method = sum(attribution_method == "temporal_window").
      Arrange drug_class, doi_category, in_hl_cohort desc.

    SHEET 4 df_metadata (window documentation + sensitivity, DOI-OUT-03): a small key/value tibble. Include rows:
      - attribution_window_days = DOI_ATTRIBUTION_WINDOW_DAYS  (=90; use the constant, no literal)
      - window_rationale = "One clinical quarter; RA/psoriasis/IBD indication-to-drug timelines span months"
      - n_drug_admins_total, n_drug_admins_rituximab, n_drug_admins_methotrexate (from drug_admins)
      - n_matched_encounter_id, n_matched_temporal_window, n_no_cooccurrence (from doi_drug_links attribution_method tabyl)
      - three-state counts: n_true, n_false, n_na (from likely_non_lymphoma_directed)
      SENSITIVITY (D-03 / DOI-OUT-03): recompute the tier-2-style temporal-window match count at ±30 and ±180 days for comparison against the ±90 baseline. Define a small helper inline, e.g.:
        count_window_matches <- function(win) {
          drug_admins %>% inner_join(doi_enc, by = "ID") %>%
            filter(abs(as.integer(DX_DATE - treatment_date)) <= win) %>%
            summarise(n_pairs = n(), n_patients = n_distinct(ID))
        }
      Add metadata rows: sensitivity_30d_pairs, sensitivity_90d_pairs, sensitivity_180d_pairs (and matching n_patients). Use 30L, DOI_ATTRIBUTION_WINDOW_DAYS, 180L — the 90 must be the named constant; 30 and 180 are explicitly labeled sensitivity bounds.

    Define the two shared strings ONCE near the top of Section 6 for reuse across all sheets (D-01 / DOI-OUT-02 + DOI-OUT-03):
      internal_only_note <- "INTERNAL-ONLY: raw counts, no automated small-cell suppression — suppress manually before external sharing"
      caveats_footnote   <- "Co-occurrence does not imply treatment attribution. Clinical chart review required for confirmation."
  </action>
  <verify>
    <automated>grep -q "internal_only_note" R/112_doi_attribution_report.R && grep -q "caveats_footnote" R/112_doi_attribution_report.R && test $(grep -c "suppress_small" R/112_doi_attribution_report.R) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'suppress_small' R/112_doi_attribution_report.R` == 0 (D-01: NO automated suppression — supersedes stale roadmap SC#3)
    - `grep -q 'INTERNAL-ONLY: raw counts, no automated small-cell suppression — suppress manually before external sharing' R/112_doi_attribution_report.R` (exact note string, DOI-OUT-02)
    - `grep -q 'Co-occurrence does not imply treatment attribution. Clinical chart review required for confirmation.' R/112_doi_attribution_report.R` (exact CAVEATS string, DOI-OUT-03)
    - `grep -c 'in_hl_cohort' R/112_doi_attribution_report.R` >= 3 (dimension carried on multiple sheets, D-02)
    - `grep -q 'n_patients = n_distinct(ID)' R/112_doi_attribution_report.R` (RAW patient counts)
    - `grep -q 'n_encounters = n_distinct(ENCOUNTERID)' R/112_doi_attribution_report.R` (RAW encounter counts)
    - `grep -q 'attribution_method' R/112_doi_attribution_report.R` in the Sheet 2 assembly
    - `grep -q '180' R/112_doi_attribution_report.R` and `grep -q '30L\|<= 30' R/112_doi_attribution_report.R` (±30/±180 sensitivity, DOI-OUT-03)
    - `grep -c '_for_' R/112_doi_attribution_report.R` == 0
    - `grep -q 'paste(sort(unique(' R/112_doi_attribution_report.R` OR no multi-value collapse needed (detail grain) — if collapse used, ascending "; " delimiter
  </acceptance_criteria>
  <done>Four data frames (df_patient_prevalence, df_encounter_cooccurrence, df_drug_doi_summary, df_metadata) exist in-memory with RAW counts, in_hl_cohort dimension, attribution_method on Sheet 2, and ±30/±90/±180 sensitivity on the metadata sheet. internal_only_note and caveats_footnote defined as exact-string constants. No suppress_small.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble openxlsx2 workbook, embed note + footnote, save, close connection</name>
  <files>R/112_doi_attribution_report.R</files>
  <read_first>
    - R/112_doi_attribution_report.R (current state — the 4 df_* frames + internal_only_note + caveats_footnote from Task 1)
    - R/110_output_level_before_after_report.R §575-683 (add_styled_sheet helper: title row 1, subtitle row 2, data row 4, freeze_pane row 5; wb_workbook(); wb$add_worksheet(); wb$save() in tryCatch)
    - R/107_med_admin_dispensing_gap_diagnostic.R (close_pcornet_con() teardown at script end)
    - R/00_config.R (CONFIG$cache$outputs_dir for the output path)
  </read_first>
  <action>
    Append SECTION 7 (Workbook assembly + save) and SECTION 8 (teardown) to R/112.

    Add library(openxlsx2) to the Section 1 suppressPackageStartupMessages block (edit the existing block from Plan 129-01 to include it).

    Adapt the add_styled_sheet helper from R/110 §586-615 (title row 1 / subtitle row 2 / data row 4 / freeze row 5). EXTEND it so EACH sheet carries BOTH the internal_only_note AND the caveats_footnote (D-01 + DOI-OUT-03: every sheet):
      - Put internal_only_note into the subtitle row (row 2) — or combine with the sheet-specific subtitle via glue: subtitle_text = glue("{internal_only_note} | Generated: {run_date}").
      - Write caveats_footnote as a trailing row two rows below the data table on each sheet: after add_data(data, start_row = 4), compute footer_row <- 4 + nrow(data) + 2 and wb$add_data(sheet=..., x = caveats_footnote, start_row = footer_row, start_col = 1). This guarantees the CAVEATS footnote appears on ALL FOUR sheets.

    Build the workbook:
      OUT_XLSX <- file.path(CONFIG$cache$outputs_dir, "doi_attribution_report.xlsx")
      run_date <- format(Sys.Date(), "%Y-%m-%d")
      wb <- wb_workbook()
      Sheet 1: wb$add_worksheet("Patient Prevalence")     + add_styled_sheet(..., df_patient_prevalence)
      Sheet 2: wb$add_worksheet("Encounter Co-occurrence") + add_styled_sheet(..., df_encounter_cooccurrence)
      Sheet 3: wb$add_worksheet("Drug x DoI Summary")      + add_styled_sheet(..., df_drug_doi_summary)
      Sheet 4: wb$add_worksheet("Metadata")               + add_styled_sheet(..., df_metadata)
      (Exactly 4 add_worksheet calls, exactly these 4 sheet names.)

    Save wrapped in tryCatch so a Windows run with no materialized inputs does not hard-fail (mirror R/110 §678-683):
      tryCatch({ wb$save(OUT_XLSX); message(glue("  Wrote deliverable xlsx: {OUT_XLSX}")) },
               error = function(e) message(glue("  [WARN] Could not write xlsx (expected on Windows with no data): {conditionMessage(e)}")))

    SECTION 8 (Teardown): close_pcornet_con() (this script owns teardown — the connection opened in Plan 129-01 Section 3). Final Done message via glue.
  </action>
  <verify>
    <automated>test $(grep -c "add_worksheet" R/112_doi_attribution_report.R) -eq 4 && grep -q "doi_attribution_report.xlsx" R/112_doi_attribution_report.R && grep -q "close_pcornet_con()" R/112_doi_attribution_report.R</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'add_worksheet' R/112_doi_attribution_report.R` == 4 (exactly four sheets — DOI-OUT-01)
    - `grep -q '"Patient Prevalence"' R/112...R` AND `grep -q '"Encounter Co-occurrence"' R/112...R` AND `grep -q '"Drug x DoI Summary"' R/112...R` AND `grep -q '"Metadata"' R/112...R` (exact sheet names)
    - `grep -q 'doi_attribution_report.xlsx' R/112_doi_attribution_report.R` (output filename — D-04)
    - `grep -q 'wb\$save(OUT_XLSX)' R/112_doi_attribution_report.R` inside a tryCatch (Windows-safe write)
    - `grep -q 'library(openxlsx2)' R/112_doi_attribution_report.R`
    - `grep -c 'close_pcornet_con()' R/112_doi_attribution_report.R` == 1 (teardown owned here)
    - `grep -c 'suppress_small' R/112_doi_attribution_report.R` == 0 (still no suppression after assembly — D-01)
    - `grep -q 'caveats_footnote' R/112_doi_attribution_report.R` referenced in add_styled_sheet footer write (footnote on every sheet)
    - `grep -c '_for_' R/112_doi_attribution_report.R` == 0
  </acceptance_criteria>
  <done>R/112 writes doi_attribution_report.xlsx with exactly 4 sheets (Patient Prevalence, Encounter Co-occurrence, Drug x DoI Summary, Metadata), internal-only note + CAVEATS footnote on every sheet, RAW counts (no suppress_small), and closes the DuckDB connection. Save wrapped in tryCatch (Windows structural pass; runtime write confirmed on HiPerGator in Phase 130).</done>
</task>

</tasks>

<verification>
- Exactly 4 add_worksheet calls with the exact sheet names; output filename doi_attribution_report.xlsx.
- internal-only note (exact string) + CAVEATS footnote (exact string) present and wired onto every sheet.
- Zero suppress_small occurrences (D-01 supersedes stale roadmap SC#3); zero "_for_" occurrences.
- in_hl_cohort dimension present on the summary/prevalence sheets (D-02).
- Metadata sheet documents DOI_ATTRIBUTION_WINDOW_DAYS = 90 with ±30/±180 sensitivity counts.
- close_pcornet_con() called exactly once at script end.
</verification>

<success_criteria>
- DOI-OUT-01: 4-sheet Tableau-ready doi_attribution_report.xlsx (Patient Prevalence, Encounter Co-occurrence w/ attribution_method, Drug × DoI Summary, Metadata).
- DOI-OUT-02: RAW counts, NO suppress_small; exact internal-only note on every sheet.
- DOI-OUT-03: Metadata documents ±90 window + ±30/±180 sensitivity; CAVEATS footnote on every sheet.
</success_criteria>

<output>
After completion, create `.planning/phases/129-attribution-linkage-and-output/129-02-SUMMARY.md` documenting the 4 sheets, the raw-counts / internal-only decision (D-01 supersedes SC#3), the exact note + footnote strings, the sensitivity block, and that R/112 is structurally complete and closes the DuckDB connection (HiPerGator runtime + registration = Phase 130).
</output>
