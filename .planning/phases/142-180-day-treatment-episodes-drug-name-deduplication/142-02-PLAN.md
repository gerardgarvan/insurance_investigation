---
phase: 142-180-day-treatment-episodes-drug-name-deduplication
plan: 02
type: execute
wave: 2
depends_on: [142-01]
files_modified:
  - R/26_treatment_episodes.R
  - R/142_gantt_180_export.R
autonomous: true
requirements: [EP-180-01, EP-180-02]

must_haves:
  truths:
    - "R/26 produces treatment_episodes_180.rds and treatment_episode_detail_180.rds alongside the 90-day files"
    - "180-day RDS files contain the same columns as 90-day counterparts"
    - "gantt_episodes_180.csv has exactly 20 columns matching the gantt_episodes.csv schema"
    - "gantt_detail_180.csv has exactly 14 columns matching the gantt_detail.csv schema"
    - "Both 180-day CSV files use semicolons as multi-value field separators (per D-02)"
    - "Drug name deduplication fix (142-01) is in effect when the 180-day files are written"
  artifacts:
    - path: "R/26_treatment_episodes.R"
      provides: "180-day episode calculation block after the existing 90-day saveRDS calls"
      contains: "treatment_episodes_180.rds"
    - path: "R/142_gantt_180_export.R"
      provides: "Standalone script that reads 180-day RDS and writes *_180.csv files"
      contains: "gantt_episodes_180.csv"
  key_links:
    - from: "R/26_treatment_episodes.R"
      to: "cache/outputs/treatment_episodes_180.rds"
      via: "saveRDS() after calculate_episodes_detailed(all_detail_for_episodes, gap_threshold = 180L)"
      pattern: "treatment_episodes_180\\.rds"
    - from: "R/142_gantt_180_export.R"
      to: "output/gantt_episodes_180.csv"
      via: "write.csv() / readr::write_csv() using same enrichment as R/52"
      pattern: "gantt_episodes_180\\.csv"
---

<objective>
Extend `R/26_treatment_episodes.R` to also produce 180-day episode RDS files using the
same resolved `all_detail` data (no re-running the expensive drug-name lookup), then
create `R/142_gantt_180_export.R` to enrich and export those files as Gantt CSVs with
the same schema as the existing 90-day `gantt_episodes.csv` / `gantt_detail.csv`.

Purpose: Colleague needs a 180-day Gantt CSV to compare episode window definitions.
The gap logic is identical to 90-day (window from episode start, not consecutive gap
between dates — D-01). Output filenames use `*_180` suffix (D-03). RDS names use
`*_180.rds` suffix (D-04).

Output:
- `cache/outputs/treatment_episodes_180.rds`
- `cache/outputs/treatment_episode_detail_180.rds`
- `output/gantt_episodes_180.csv` (20 columns)
- `output/gantt_detail_180.csv` (14 columns)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-180-day-treatment-episodes-drug-name-deduplication/CONTEXT.md
@.planning/phases/142-180-day-treatment-episodes-drug-name-deduplication/142-01-SUMMARY.md

<!-- Key interfaces the executor needs. No codebase exploration required. -->
<interfaces>
From R/26_treatment_episodes.R:
```r
# Function signatures
calculate_episodes_detailed <- function(dates_df, gap_threshold = GAP_THRESHOLD)
# CRITICAL: dates_df MUST contain a source_hint column — it is referenced directly
# inside the summarise() block at line 553:
#   source_hint[!is.na(triggering_code)]
# Passing dates_df without source_hint causes an immediate crash.
#
# Returns: tibble(patient_id, episode_number, episode_start, episode_stop,
#   episode_length_days, distinct_dates_in_episode, historical_flag,
#   triggering_codes, source_hints, encounter_ids)

annotate_detail_with_episodes <- function(dates_df, episodes_df, gap_threshold = GAP_THRESHOLD)
# dates_df columns: ID, treatment_date, triggering_code, ENCOUNTERID
# Extra columns in dates_df (e.g. drug_name) are carried through via the join.
# Returns: tibble(patient_id, treatment_date, triggering_code, ENCOUNTERID,
#   episode_number, episode_start, episode_stop, historical_flag)
# NOTE: any extra columns present in dates_df are NOT automatically returned —
# they must be joined back from the input if needed.

# At ~line 851 (existing select that narrows all_detail):
all_detail <- all_detail %>%
  select(
    patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name,
    episode_number, episode_start, episode_stop, historical_flag
    # source_hint was DROPPED here — must be added (see Task 1 Step 0)
  )

# At ~line 866 (90-day save block):
saveRDS(all_episodes, OUTPUT_RDS)      # treatment_episodes.rds
saveRDS(all_detail,   OUTPUT_DETAIL_RDS) # treatment_episode_detail.rds
```

From R/52_gantt_v2_export.R:
```r
# Input paths:
EPISODES_RDS  <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")
DETAIL_RDS    <- file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail.rds")
DESCRIPTIONS_RDS <- file.path(CONFIG$cache$outputs_dir, "code_descriptions.rds")
VALIDATED_DEATHS_RDS <- file.path(CONFIG$cache$outputs_dir, "validated_death_dates.rds")
COHORT_RDS    <- file.path(CONFIG$output_dir, "confirmed_hl_cohort.rds")
CANCER_SUMMARY_CSV <- build_output_path("tables", "cancer_summary.csv")

# Output paths:
OUTPUT_EPISODES <- file.path(CONFIG$output_dir, "gantt_episodes.csv")
OUTPUT_DETAIL   <- file.path(CONFIG$output_dir, "gantt_detail.csv")

# gantt_episodes.csv schema (20 columns, EPISODES_SCHEMA vector in R/52):
# patient_id, treatment_type, episode_number, episode_start, episode_stop,
# episode_length_days, distinct_dates_in_episode,
# triggering_codes, drug_names, triggering_code_descriptions,
# cancer_category, is_hodgkin, drug_group,
# code_type, source_table, sct_cross_use_flag,
# episode_dx_codes, episode_dx_categories,
# episode_dx_7day_confirmed, age_at_episode

# gantt_detail.csv schema (14 columns, DETAIL_SCHEMA vector in R/52):
# patient_id, treatment_type, treatment_date, triggering_code, drug_name,
# episode_number, episode_start, episode_stop,
# triggering_code_description, cancer_category, is_hodgkin,
# code_type, source_table, sct_cross_use_flag

# Multi-value separator: semicolon (D-02). Empty strings instead of NA (D-03).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 180-day episode calculation block to R/26_treatment_episodes.R</name>
  <files>R/26_treatment_episodes.R</files>
  <action>
Two edits are required: one pre-existing select fix, then the new Section 5C block.

---

**Step 0 — Fix the existing `all_detail` select at ~line 851 to retain `source_hint`**

`calculate_episodes_detailed()` references `source_hint` directly inside its `summarise()`
(line 553: `source_hint[!is.na(triggering_code)]`). By line 851, `source_hint` is in
`all_detail` but is then dropped by the narrowing `select()`. The 180-day block reuses
`all_detail` and will crash unless `source_hint` is preserved.

Find this block (the `all_detail` narrowing select, ~line 850-855):

```r
# Update all_detail select to include drug_name
all_detail <- all_detail %>%
  select(
    patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name,
    episode_number, episode_start, episode_stop, historical_flag
  )
```

Add `source_hint` to that select so it reads:

```r
# Update all_detail select to include drug_name and source_hint
# source_hint retained here (Phase 142) so the 180-day block can pass it to
# calculate_episodes_detailed(), which references it directly in its summarise().
all_detail <- all_detail %>%
  select(
    patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID,
    source_hint, drug_name,
    episode_number, episode_start, episode_stop, historical_flag
  )
```

This is the only change to existing code above the 90-day saveRDS calls. The 90-day
RDS written immediately after still has `drug_name` and all previous columns — adding
`source_hint` is additive and non-breaking.

---

**Step 1 — Insert Section 5C immediately after the 90-day saveRDS calls (~line 870)**

After:

```r
saveRDS(all_detail, OUTPUT_DETAIL_RDS)
message(glue("Detail RDS saved: {OUTPUT_DETAIL_RDS} ({nrow(all_detail)} rows)"))
```

Insert:

```r
# --- SECTION 5C: 180-DAY EPISODE CALCULATION (Phase 142) ---
# Re-run episode assignment on the same drug-name-resolved all_detail data
# using gap_threshold = 180. The expensive 3-tier drug-name lookup above runs
# only once; this block reuses the resolved drug_name column.
# D-01: Window logic identical to 90-day (date - episode_start >= threshold).
# D-04: RDS names use *_180.rds suffix.

message("\n--- Computing 180-day episodes (Phase 142) ---")

OUTPUT_RDS_180        <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes_180.rds")
OUTPUT_DETAIL_RDS_180 <- file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail_180.rds")

episodes_list_180 <- list()
detail_list_180   <- list()

for (type in TREATMENT_TYPES) {
  # Subset all_detail to this treatment type, carrying drug_name and source_hint.
  # drug_name is carried directly — no post-hoc re-join needed (avoids fan-out
  # when the same code appears on the same date in two encounters, Phase 142 fix).
  # source_hint is required by calculate_episodes_detailed()'s summarise() block.
  dates_df_180 <- all_detail %>%
    filter(treatment_type == type) %>%
    select(ID = patient_id, treatment_date, triggering_code, ENCOUNTERID,
           source_hint, drug_name)

  episodes_df_180 <- calculate_episodes_detailed(dates_df_180, gap_threshold = 180L) %>%
    mutate(treatment_type = type)

  episodes_list_180[[type]] <- episodes_df_180

  # annotate_detail_with_episodes() uses dates_df for its join keys; drug_name is
  # NOT returned by that function automatically, so we join it back from dates_df_180
  # using the unique (ID, treatment_date, triggering_code, ENCOUNTERID) key —
  # which is fan-out-safe because dates_df_180 already has one drug_name per row.
  detail_df_180 <- annotate_detail_with_episodes(
    dates_df_180, episodes_df_180, gap_threshold = 180L
  ) %>%
    mutate(treatment_type = type) %>%
    left_join(
      dates_df_180 %>% select(ID, treatment_date, triggering_code, ENCOUNTERID, drug_name),
      by = c("patient_id" = "ID", "treatment_date", "triggering_code", "ENCOUNTERID")
    )

  detail_list_180[[type]] <- detail_df_180
}

all_episodes_180 <- bind_rows(episodes_list_180) %>%
  select(
    patient_id, treatment_type, episode_number, episode_start, episode_stop,
    episode_length_days, distinct_dates_in_episode, historical_flag,
    triggering_codes, source_hints, encounter_ids
  )

# Aggregate drug_names per 180-day episode from the detail rows (drug_name
# was carried in dates_df_180 and joined onto detail_df_180, so no fan-out).
drug_names_per_episode_180 <- bind_rows(detail_list_180) %>%
  filter(!is.na(drug_name)) %>%
  group_by(patient_id, treatment_type, episode_number) %>%
  summarise(
    drug_names = paste(sort(unique(drug_name)), collapse = ","),
    .groups = "drop"
  )

all_episodes_180 <- all_episodes_180 %>%
  left_join(drug_names_per_episode_180, by = c("patient_id", "treatment_type", "episode_number")) %>%
  mutate(drug_names = ifelse(is.na(drug_names), "", drug_names)) %>%
  select(
    patient_id, treatment_type, episode_number, episode_start, episode_stop,
    episode_length_days, distinct_dates_in_episode, historical_flag,
    triggering_codes, source_hints, encounter_ids, drug_names
  )

all_detail_180 <- bind_rows(detail_list_180) %>%
  select(
    patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name,
    episode_number, episode_start, episode_stop, historical_flag
  )

saveRDS(all_episodes_180, OUTPUT_RDS_180)
message(glue("180-day RDS saved: {OUTPUT_RDS_180} ({nrow(all_episodes_180)} rows)"))

saveRDS(all_detail_180, OUTPUT_DETAIL_RDS_180)
message(glue("180-day detail RDS saved: {OUTPUT_DETAIL_RDS_180} ({nrow(all_detail_180)} rows)"))
```

---

**Key constraints:**
- Do NOT change any 90-day logic, OUTPUT_RDS, or OUTPUT_DETAIL_RDS path names.
- Step 0's change to the existing `select()` is the only edit above the 90-day saveRDS calls.
- `gap_threshold = 180L` must be an explicit integer literal, not the global `GAP_THRESHOLD`.
- `drug_name` travels with each row in `dates_df_180` from the start — no second join
  against the full `all_detail` frame, so no ENCOUNTERID fan-out risk.
- `source_hint` is included in `dates_df_180` so `calculate_episodes_detailed()` does not
  crash in its `summarise()` block (it references `source_hint` directly, not via a passed
  parameter).
- After inserting the block, verify SECTION 6 per-type CSV output still runs without error.
  </action>
  <verify>
    <automated>
# After re-running R/26 (or sourcing just the new block with all_detail in memory),
# verify both 180-day RDS files exist and have plausible row counts:
Rscript -e "
  source('R/00_config.R')
  ep  <- readRDS(file.path(CONFIG\$cache\$outputs_dir, 'treatment_episodes_180.rds'))
  det <- readRDS(file.path(CONFIG\$cache\$outputs_dir, 'treatment_episode_detail_180.rds'))
  stopifnot(nrow(ep)  > 0)
  stopifnot(nrow(det) > 0)
  expected_ep_cols <- c('patient_id','treatment_type','episode_number','episode_start',
                         'episode_stop','episode_length_days','distinct_dates_in_episode',
                         'historical_flag','triggering_codes','source_hints','encounter_ids','drug_names')
  stopifnot(all(expected_ep_cols %in% colnames(ep)))
  expected_det_cols <- c('patient_id','treatment_type','treatment_date','triggering_code',
                          'ENCOUNTERID','drug_name','episode_number','episode_start',
                          'episode_stop','historical_flag')
  stopifnot(all(expected_det_cols %in% colnames(det)))
  # 180-day episodes should be fewer or equal in count (wider windows = fewer splits)
  ep90 <- readRDS(file.path(CONFIG\$cache\$outputs_dir, 'treatment_episodes.rds'))
  stopifnot(nrow(ep) <= nrow(ep90))
  cat('PASS: 180-day RDS files exist with correct columns and plausible row counts\n')
  cat('  Episodes 90-day:', nrow(ep90), '| 180-day:', nrow(ep), '\n')
"
    </automated>
  </verify>
  <done>
- `treatment_episodes_180.rds` and `treatment_episode_detail_180.rds` exist in `CONFIG$cache$outputs_dir`.
- Both files have the same column schema as their 90-day counterparts.
- 180-day episode count is less than or equal to 90-day count (wider window merges more episodes).
- Existing 90-day RDS files are unchanged (same row counts as before).
- No errors when the rest of SECTION 6 runs after the new block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create R/142_gantt_180_export.R mirroring R/52 enrichment and CSV export</name>
  <files>R/142_gantt_180_export.R</files>
  <action>
Create `R/142_gantt_180_export.R` as a standalone script that reads the 180-day RDS
files from Task 1, applies the same enrichment pipeline as `R/52_gantt_v2_export.R`,
and writes `gantt_episodes_180.csv` + `gantt_detail_180.csv`.

**Header and setup:**

```r
# ==============================================================================
# Phase 142: 180-Day Gantt CSV Export
# ==============================================================================
#
# Purpose:
#   Produce 180-day Gantt CSV files with the same schema as gantt_episodes.csv /
#   gantt_detail.csv (R/52), using treatment_episodes_180.rds and
#   treatment_episode_detail_180.rds produced by R/26 Section 5C.
#
# Inputs:
#   - cache/outputs/treatment_episodes_180.rds      (Phase 142, Task 1)
#   - cache/outputs/treatment_episode_detail_180.rds (Phase 142, Task 1)
#   - cache/outputs/code_descriptions.rds           (Phase 48b)
#   - cache/outputs/validated_death_dates.rds        (Phase 59)
#   - output/confirmed_hl_cohort.rds                (Phase 55)
#   - output/tables/cancer_summary.csv              (Phase 115: 7-day confirmed data)
#
# Outputs:
#   - output/gantt_episodes_180.csv  (20 columns, same schema as gantt_episodes.csv)
#   - output/gantt_detail_180.csv    (14 columns, same schema as gantt_detail.csv)
#
# Requirements: EP-180-01, EP-180-02
# D-01: Gap window = from episode_start (same logic as 90-day)
# D-02: Semicolons for multi-value field separators
# D-03: Output filenames use *_180.csv suffix
# ==============================================================================
```

**Implementation strategy — source R/52 helpers, change only paths:**

The enrichment pipeline in R/52 is substantial. Rather than duplicating it, this script:
1. Sources `R/00_config.R`, `R/utils/utils_duckdb.R`, `R/utils/utils_dates.R`,
   `R/utils/utils_cancer.R` — same as R/52.
2. Sources `R/52_gantt_v2_export.R` is NOT appropriate (it runs immediately on source).
   Instead, copy the enrichment logic (not the file setup or output paths) from R/52
   into this script.

**Concrete steps to implement:**

Step A — Define input/output paths, identical to R/52 except for the 180-day RDS names
and `*_180.csv` output names:

```r
suppressPackageStartupMessages({
  library(dplyr)
  library(glue)
  library(stringr)
  library(lubridate)
})

source("R/00_config.R")
source("R/utils/utils_duckdb.R")
source("R/utils/utils_dates.R")
source("R/utils/utils_cancer.R")

EPISODES_RDS        <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes_180.rds")
DETAIL_RDS          <- file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail_180.rds")
DESCRIPTIONS_RDS    <- file.path(CONFIG$cache$outputs_dir, "code_descriptions.rds")
VALIDATED_DEATHS_RDS <- file.path(CONFIG$cache$outputs_dir, "validated_death_dates.rds")
COHORT_RDS          <- file.path(CONFIG$output_dir, "confirmed_hl_cohort.rds")
CANCER_SUMMARY_CSV  <- build_output_path("tables", "cancer_summary.csv")

OUTPUT_EPISODES     <- file.path(CONFIG$output_dir, "gantt_episodes_180.csv")
OUTPUT_DETAIL       <- file.path(CONFIG$output_dir, "gantt_detail_180.csv")
```

Step B — Copy the SCHEMA DEFINITIONS vectors from R/52 verbatim (EPISODES_SCHEMA,
DETAIL_SCHEMA). These define the 20- and 14-column contracts. Do not modify them.

Step C — Read the 180-day RDS files:
```r
episodes <- readRDS(EPISODES_RDS)
detail   <- readRDS(DETAIL_RDS)
```

Step D — Apply the same enrichment pipeline from R/52 in order:
1. Load `code_descriptions.rds` and join `triggering_code_descriptions` to episodes
   and `triggering_code_description` (singular) to detail.
2. Load `confirmed_hl_cohort.rds` for `age_at_episode` calculation (use patient birth
   dates joined on patient_id, then `floor(as.numeric(episode_start - birth_date) / 365.25)`).
3. Load `validated_death_dates.rds` — not needed for the 20-column schema (cause_of_death
   was dropped in Phase 99 schema consolidation). Skip if not used.
4. Derive `cancer_category` and `is_hodgkin` from the episodes RDS (these columns are
   already enriched in the RDS by the upstream pipeline in R/26/R/60-63/R/91).
   If the column is absent (older RDS), set to `NA` / `FALSE` with a warning.
5. Derive `episode_dx_codes`, `episode_dx_categories`, `episode_dx_7day_confirmed`
   from `cancer_summary.csv` (same +/-30 day window join as R/52 Section 5).
6. Ensure multi-value columns use semicolons, not commas (R/52 D-02): apply
   `str_replace_all(x, ",", ";")` to `triggering_codes`, `drug_names`,
   `triggering_code_descriptions`, `drug_group`, `code_type`, `source_table`.
7. Replace NA with empty string for all character columns in both CSV outputs (D-03).
8. Apply `EPISODES_SCHEMA` column select (exactly 20 columns) and `DETAIL_SCHEMA`
   column select (exactly 14 columns) before writing.

Step E — Write CSVs:
```r
write.csv(episodes_out, OUTPUT_EPISODES, row.names = FALSE, na = "")
write.csv(detail_out,   OUTPUT_DETAIL,   row.names = FALSE, na = "")
message(glue("gantt_episodes_180.csv written: {nrow(episodes_out)} rows"))
message(glue("gantt_detail_180.csv written:   {nrow(detail_out)} rows"))
```

**Anti-patterns to avoid:**
- Do NOT use `setwd()` — use `CONFIG$output_dir` and `CONFIG$cache$outputs_dir` from
  `R/00_config.R` for all paths.
- Do NOT re-source R/52 as a whole (it writes files on source).
- Do NOT use `read.csv()` for PCORnet inputs — use `readRDS()` for the cache files.
- Do NOT add columns not in EPISODES_SCHEMA / DETAIL_SCHEMA.
- Multi-value field separator must be semicolon (D-02 from R/52), not comma.

**If any enrichment column (e.g., cancer_category, drug_group) is already present in the
180-day RDS files from the upstream pipeline:** use it directly. Only re-derive if absent.
Log which path was taken with `message()`.
  </action>
  <verify>
    <automated>
Rscript -e "
  source('R/00_config.R')
  ep  <- read.csv(file.path(CONFIG\$output_dir, 'gantt_episodes_180.csv'), nrows = 5)
  det <- read.csv(file.path(CONFIG\$output_dir, 'gantt_detail_180.csv'),   nrows = 5)

  EPISODES_SCHEMA <- c('patient_id','treatment_type','episode_number',
    'episode_start','episode_stop','episode_length_days','distinct_dates_in_episode',
    'triggering_codes','drug_names','triggering_code_descriptions',
    'cancer_category','is_hodgkin','drug_group',
    'code_type','source_table','sct_cross_use_flag',
    'episode_dx_codes','episode_dx_categories','episode_dx_7day_confirmed','age_at_episode')
  DETAIL_SCHEMA <- c('patient_id','treatment_type','treatment_date','triggering_code',
    'drug_name','episode_number','episode_start','episode_stop',
    'triggering_code_description','cancer_category','is_hodgkin',
    'code_type','source_table','sct_cross_use_flag')

  stopifnot(identical(colnames(ep),  EPISODES_SCHEMA))
  stopifnot(identical(colnames(det), DETAIL_SCHEMA))
  stopifnot(ncol(ep)  == 20)
  stopifnot(ncol(det) == 14)
  stopifnot(!any(ep  == 'NA', na.rm = TRUE))
  stopifnot(!any(det == 'NA', na.rm = TRUE))
  cat('PASS: 180-day Gantt CSVs have correct schema (20 + 14 columns, no NA literals)\n')
"
    </automated>
  </verify>
  <done>
- `output/gantt_episodes_180.csv` exists with exactly 20 columns in the same order as `gantt_episodes.csv`.
- `output/gantt_detail_180.csv` exists with exactly 14 columns in the same order as `gantt_detail.csv`.
- Multi-value fields use semicolons as separators (D-02).
- No literal "NA" strings appear in either file (empty string used instead, D-03).
- Script runs to completion without errors when executed as `Rscript R/142_gantt_180_export.R` from the project root.
  </done>
</task>

</tasks>

<verification>
End-to-end check after both tasks:
1. Re-run `Rscript R/26_treatment_episodes.R` — confirm both 90-day and 180-day RDS
   files are written, and the 90-day row counts are unchanged.
2. Run `Rscript R/142_gantt_180_export.R` — confirm both `*_180.csv` files appear in
   `output/`.
3. Spot-check: open `gantt_episodes_180.csv` and `gantt_episodes.csv` side by side.
   The 180-day file should have fewer rows in the episodes file (wider windows merge
   more encounters into single episodes). Column headers must be identical.
4. Confirm "Vinblastine Sulfate" does not appear as a standalone entry in `drug_names`
   column of either CSV (alias from 142-01 should have collapsed it to "Vinblastine").
</verification>

<success_criteria>
`Rscript R/142_gantt_180_export.R` completes without errors, producing `gantt_episodes_180.csv` (20 columns) and `gantt_detail_180.csv` (14 columns) in `output/`. Both files have the same column order as the 90-day equivalents. Episode count in the 180-day file is less than or equal to the 90-day file (wider window). "Vinblastine Sulfate" does not appear as a standalone drug name.
</success_criteria>

<output>
After completion, create `.planning/phases/142-180-day-treatment-episodes-drug-name-deduplication/142-02-SUMMARY.md`
</output>
