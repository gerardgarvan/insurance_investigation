---
phase: 119-fix-death-cause-nhl-flag
plan: 04
type: execute
wave: 4
depends_on: ["119-01", "119-02", "119-03"]
files_modified:
  - R/39_run_all_investigations.R
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements: [NHLFIX-05, SMOKE-119-01]
must_haves:
  truths:
    - "R/103 diagnostic is registered in R/39's investigation_scripts (and R/102 remains present)."
    - "R/88 has a Phase 119 Section (15p) that STRUCTURALLY validates the fix: R/102 reads DEATH_CAUSE table (not DEATH.DEATH_CAUSE column), uses classify_codes three-state, DEATH_CAUSE is in PCORNET_TABLES, table count is 16, and R/103 exists."
    - "R/SCRIPT_INDEX.md documents R/103 (and the R/102 Phase 119 source change) and the post-renumber investigation count is updated."
  artifacts:
    - path: "R/39_run_all_investigations.R"
      provides: "R/103 registered in investigation_scripts"
      contains: "R/103_death_cause_diagnostic.R"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15p Phase 119 structural checks + summary lines"
      contains: "SECTION 15p"
    - path: "R/SCRIPT_INDEX.md"
      provides: "R/103 row + R/102 Phase 119 note + count bump"
      contains: "R/103_death_cause_diagnostic.R"
  key_links:
    - from: "R/88 Section 15p"
      to: "R/102 + R/103 + R/00_config PCORNET_TABLES"
      via: "readLines + grepl structural assertions"
      pattern: "get_pcornet_table.*DEATH_CAUSE"
---

<objective>
Register the Phase 119 work: add R/103 to the R/39 pipeline runner, add an R/88 Section 15p that
structurally validates the fix (R/102 now reads the DEATH_CAUSE table, DEATH_CAUSE is loaded,
table count 16, R/103 exists), and index R/103 + the R/102 source change in R/SCRIPT_INDEX.md.
Follows the Phase 116/117/118 registration precedent.

Purpose: Make the fix discoverable, runnable via the pipeline runner, and guarded against
regression by the smoke test.
Output: R/39 updated, R/88 Section 15p added, R/SCRIPT_INDEX.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-fix-death-cause-nhl-flag/119-CONTEXT.md
@.planning/phases/119-fix-death-cause-nhl-flag/119-RESEARCH.md

<environment_split>
All three edits are STRUCTURALLY verifiable locally (grep/parse + running R/88's structural
sections in isolation). The runtime "output has non-zero TRUE/FALSE" check inside Section 15p is
HiPerGator-only (gated so it does not fail locally without data). Section 15o (Phase 118) is the
template to mirror.
</environment_split>

<interfaces>
R/39_run_all_investigations.R (lines 176-193): `investigation_scripts <- c(...)` vector; last
  entry currently `"R/102_death_cause_nhl_flag.R"`. `SECTION 4` follows. The `run_script()` loop
  at line 195 executes each. R/103 is a diagnostic (read-only) but should be registered for
  discoverability like the other 100+ scripts.

R/88 Section 15o (Phase 118) — lines ~2134-2203 — is the exact template: banner
  `# SECTION 15o: ... (Phase 118) ----`, `message("\n[Phase 118] ...")`, `check(...)` calls,
  file.exists guard + `for (i in 2:N) check(... SKIPPED ...)` else-branch. Summary lines at
  ~3769-3772 (`message("  * NHLDEATH-...`).

R/88 IS_LOCAL table-count check at ~line 3470 was set to 16 by Plan 02.

R/SCRIPT_INDEX.md: post-renumber investigations table (lines ~146-148) has R/100/R/101/R/102
  rows (| `R/NNN_...` | description | phase |). Line ~201 summary:
  "**Post-renumber investigations (100+):** 3 (R/100 ..., R/101 ..., R/102 ...)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register R/103 in R/39 investigation_scripts</name>
  <read_first>
    - R/39_run_all_investigations.R lines 176-197 (investigation_scripts vector + run_script loop)
    - R/103_death_cause_diagnostic.R (confirm the exact filename)
  </read_first>
  <action>
In `R/39_run_all_investigations.R`, add R/103 to the `investigation_scripts` vector. Place it
BEFORE `R/102_death_cause_nhl_flag.R` (diagnostic before the fix it informs) and add the required
comma so the vector parses:
  ```r
    "R/103_death_cause_diagnostic.R",           # Cause-of-death signal inventory diagnostic (Phase 119; read-only)
    "R/102_death_cause_nhl_flag.R"              # Cause-of-death NHL three-state flag CSV (Phase 118; fixed Phase 119 to read DEATH_CAUSE table)
  ```
Update the trailing comment on the R/102 line to note the Phase 119 fix. Confirm R/102 remains
present in the vector (do not remove it).
  </action>
  <verify>
    <automated>MISSING — full R/39 run needs HiPerGator data. Structural: grep R/39 for both R/103 and R/102 entries</automated>
  </verify>
  <acceptance_criteria>
    - R/39_run_all_investigations.R contains `"R/103_death_cause_diagnostic.R"` inside the investigation_scripts vector.
    - R/39 still contains `"R/102_death_cause_nhl_flag.R"`.
    - The R/103 line ends with a comma and the vector parses (R/102 is the final, comma-less entry OR appropriately terminated).
  </acceptance_criteria>
  <done>R/103 registered before R/102 in the runner; R/102 retained.</done>
</task>

<task type="auto">
  <name>Task 2: Add R/88 Section 15p — Phase 119 structural validation</name>
  <read_first>
    - R/88_smoke_test_comprehensive.R lines 2130-2204 (Section 15o Phase 118 template — banner, message, check() calls, else-branch SKIPPED loop)
    - R/88_smoke_test_comprehensive.R lines 3765-3775 (Phase 118 summary message lines)
    - R/88_smoke_test_comprehensive.R lines 3465-3475 (the table-count check set to 16 by Plan 02)
    - R/102_death_cause_nhl_flag.R (as fixed) + R/103_death_cause_diagnostic.R + R/00_config.R PCORNET_TABLES
  </read_first>
  <action>
In `R/88_smoke_test_comprehensive.R`, immediately after Section 15o's else-branch (~line 2203),
add a new `# SECTION 15p: DEATH CAUSE NHL FIX (Phase 119) ----` block mirroring 15o's structure.
Use `message("\n[Phase 119] Death cause NHL fix (R/102 -> DEATH_CAUSE table; R/103 diagnostic)...")`.
Read R/102 and R/103 with readLines/paste and run these checks (each via `check("...", <expr>)`):

  1. `check("R/103_death_cause_diagnostic.R exists (Phase 119)", file.exists("R/103_death_cause_diagnostic.R"))`
  2. R/103 reports cause sources restricted to deceased set:
     `grepl('get_pcornet_table\\("DEATH_CAUSE"\\)', r103_text) && grepl("deceased", r103_text) && grepl("intersect\\(", r103_text)`
  3. R/103 classifies NHL: `grepl("classify_codes\\(", r103_text) && grepl("Non-Hodgkin Lymphoma", r103_text)`
  4. R/102 reads DEATH_CAUSE table (NHLFIX): `grepl('get_pcornet_table\\("DEATH_CAUSE"\\)', r102_text)`
  5. R/102 NO LONGER reads cause from DEATH table (negative check):
     `!grepl('DEATH_CAUSE = all_of\\(death_cause_col\\)', r102_text)`
  6. R/102 prefers underlying cause: `grepl("DEATH_CAUSE_TYPE", r102_text) && grepl('"U"', r102_text)`
  7. R/102 joins cause to deceased set: `grepl("left_join", r102_text)`
  8. R/102 three-state classify preserved: `grepl("classify_codes\\(", r102_text) && grepl("Non-Hodgkin Lymphoma", r102_text) && grepl("cause_of_death_is_nhl", r102_text) && grepl("case_when", r102_text)`
  9. R/102 output contract unchanged: `grepl("death_cause_nhl_flag\\.csv", r102_text) && grepl("row.names = FALSE", r102_text) && grepl('na = ""', r102_text) && grepl("PATID", r102_text)`
  10. R/102 has labeled proxy backstop: `grepl("USED_PROXY_BACKSTOP", r102_text) && grepl("D-05", r102_text)`
  11. DEATH_CAUSE in PCORNET_TABLES: read R/00_config.R, `grepl('"DEATH_CAUSE"', config_text)`
  12. DEATH_CAUSE_SPEC in loader: read R/01_load_pcornet.R, `grepl("DEATH_CAUSE_SPEC", load_text) && grepl("DEATH_CAUSE = DEATH_CAUSE_SPEC", load_text)`
  13. R/88 table-count assertion is 16: this section can assert the constant is present in THIS
      file via a self-read, or simply `check("Table-count assertion bumped to 16 (Phase 119)", TRUE)`
      with a comment pointing at the IS_LOCAL check (the actual runtime count check lives in the
      DuckDB section). Prefer the self-read: `grepl("length\\(tables_found\\) == 16", smoke_self_text)`.
  14. RUNTIME (HiPerGator-only, gated): if the output CSV exists and NOT IS_LOCAL, read it and
      `check("output has non-zero TRUE or FALSE (Phase 119, HiPerGator)", (n_true + n_false) > 0)`;
      otherwise `check("... -- SKIPPED (local / no output)", TRUE)` so local runs stay green.

Wrap checks 2-14 in an `if (file.exists R/102 && file.exists R/103)` guard; else-branch registers
the dependent checks FALSE (mirror 15o's `for (i in 2:N) check(... SKIPPED ...)`).

Then add Phase 119 summary lines near the Phase 118 block (~line 3772), mirroring the style:
  ```r
  message("  * NHLFIX-03: R/102 reads cause of death from the DEATH_CAUSE table, not DEATH.DEATH_CAUSE (Phase 119)")
  message("  * NHLFIX-04: R/35 cause-of-death source corrected/annotated to DEATH_CAUSE table (Phase 119)")
  message("  * SMOKE-119-01: R/88 validates Phase 119 structural integrity (Section 15p)")
  ```
  </action>
  <verify>
    <automated>MISSING — full R/88 run stops at classify_codes gate without data. Structural sections run in isolation locally; grep-verify the checks below.</automated>
  </verify>
  <acceptance_criteria>
    - R/88 contains `SECTION 15p` and `[Phase 119]` message.
    - R/88 Section 15p contains a positive check for `get_pcornet_table("DEATH_CAUSE")` in R/102.
    - R/88 Section 15p contains the NEGATIVE check `!grepl('DEATH_CAUSE = all_of\\(death_cause_col\\)'` (R/102 no longer reads cause from DEATH).
    - R/88 Section 15p contains checks for `DEATH_CAUSE_TYPE`/`"U"`, `left_join`, `classify_codes`, `Non-Hodgkin Lymphoma`, `case_when`, `cause_of_death_is_nhl`, `USED_PROXY_BACKSTOP`, `D-05`.
    - R/88 Section 15p checks DEATH_CAUSE in PCORNET_TABLES and DEATH_CAUSE_SPEC in the loader (reads R/00_config + R/01).
    - R/88 Section 15p checks `length(tables_found) == 16` presence and R/103 existence.
    - R/88 Section 15p has a file.exists guard with an else-branch registering SKIPPED checks (honest total).
    - R/88 contains `SMOKE-119-01` and `NHLFIX-03` summary message lines.
  </acceptance_criteria>
  <done>Section 15p added mirroring 15o; validates the DEATH_CAUSE-table source switch, negative DEATH-column check, three-state preservation, table loading, count 16, and R/103 existence, with a gated runtime check.</done>
</task>

<task type="auto">
  <name>Task 3: Update R/SCRIPT_INDEX.md — R/103 row, R/102 Phase 119 note, count bump</name>
  <read_first>
    - R/SCRIPT_INDEX.md lines 145-149 (R/100/R/101/R/102 rows in post-renumber investigations table)
    - R/SCRIPT_INDEX.md line ~201 ("Post-renumber investigations (100+): 3 ..." summary)
  </read_first>
  <action>
In `R/SCRIPT_INDEX.md`:
1. Add a new row for R/103 in the post-renumber investigations (100+) table, after the R/102 row:
   `| `R/103_death_cause_diagnostic.R` | Read-only Phase 119 diagnostic: inventories every candidate cause-of-death source (DEATH_CAUSE table, TUMOR_REGISTRY1.CAUSE_OF_DEATH, TR2/TR3.DCAUSE) restricted to the deceased patient set — reports non-null counts, deceased-set coverage, sample values, and classify_codes() NHL matches, plus a recommendation. Writes output/diagnostics/death_cause_source_inventory.csv. | 119 |`
2. Update the R/102 row description to note the Phase 119 fix: append " (Phase 119: source corrected to read the separate DEATH_CAUSE table instead of the empty DEATH.DEATH_CAUSE column)." Keep its phase column reading `118` OR update to `118, 119` — choose the latter for clarity.
3. Bump the summary line (~line 201) from "3 (R/100 ..., R/101 ..., R/102 ...)" to "4 (R/100 RUCA, R/101 Gantt lifespan, R/102 death-cause NHL flag, R/103 death-cause diagnostic)".
  </action>
  <verify>
    <automated>Structural: grep R/SCRIPT_INDEX.md for the R/103 row and the updated count "4 ("</automated>
  </verify>
  <acceptance_criteria>
    - R/SCRIPT_INDEX.md contains a table row with `R/103_death_cause_diagnostic.R`.
    - R/SCRIPT_INDEX.md R/102 description mentions `Phase 119` and the `DEATH_CAUSE table`.
    - R/SCRIPT_INDEX.md post-renumber summary reads `4 (` (was `3 (`) and lists R/103.
  </acceptance_criteria>
  <done>SCRIPT_INDEX documents R/103, notes the R/102 Phase 119 source change, and reports 4 post-renumber investigations.</done>
</task>

</tasks>

<verification>
- Structural (local, executor): all acceptance_criteria greps pass; R/88 Section 15p runs in isolation and its checks resolve.
- Runtime (HiPerGator, USER): user runs `Rscript R/39_run_all_investigations.R` (or R/103 + R/102 directly) and R/88; Section 15p's gated runtime check confirms non-zero TRUE/FALSE in the output CSV.
</verification>

<success_criteria>
- R/103 registered in R/39 (R/102 retained).
- R/88 Section 15p validates the fix structurally + gated runtime; SMOKE-119-01 summary line present.
- R/SCRIPT_INDEX.md documents R/103 + R/102 change, count = 4.
</success_criteria>

<output>
After completion, create `.planning/phases/119-fix-death-cause-nhl-flag/119-04-SUMMARY.md`
</output>
