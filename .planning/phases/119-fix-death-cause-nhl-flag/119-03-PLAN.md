---
phase: 119-fix-death-cause-nhl-flag
plan: 03
type: execute
wave: 3
depends_on: ["119-01", "119-02"]
files_modified:
  - R/102_death_cause_nhl_flag.R
  - R/35_death_cause_quality.R
autonomous: true
requirements: [NHLFIX-03, NHLFIX-04]
must_haves:
  truths:
    - "R/102 reads cause of death from the DEATH_CAUSE table (get_pcornet_table(\"DEATH_CAUSE\")), NOT from a DEATH.DEATH_CAUSE column."
    - "R/102 prefers the underlying-cause record (DEATH_CAUSE_TYPE == \"U\") when present, else falls back to the first available cause per patient."
    - "cause_of_death_is_nhl remains three-state (TRUE=NHL / FALSE=other coded / blank=uncoded) via classify_codes() == \"Non-Hodgkin Lymphoma\", output contract unchanged (PATID + flag, write.csv row.names=FALSE na='')."
    - "A documented proxy backstop branch exists (deceased + NHL in confirmed cancer history) that only activates when the DEATH_CAUSE source yields zero coded causes for the deceased set."
    - "R/35's stale DEATH.DEATH_CAUSE assumption is corrected or clearly annotated to point at the DEATH_CAUSE table."
  artifacts:
    - path: "R/102_death_cause_nhl_flag.R"
      provides: "Cause-of-death NHL flag sourced from the DEATH_CAUSE table with underlying-cause preference + documented proxy backstop"
      contains: "get_pcornet_table(\"DEATH_CAUSE\")"
    - path: "R/35_death_cause_quality.R"
      provides: "Corrected/annotated cause-of-death source reference"
  key_links:
    - from: "R/102 DEATH deceased set"
      to: "DEATH_CAUSE table cause codes"
      via: "left_join(death_cause_by_patient, by = \"ID\")"
      pattern: "left_join"
    - from: "R/102 DEATH_CAUSE code"
      to: "cause_of_death_is_nhl flag"
      via: "classify_codes() == NHL_CATEGORY three-state case_when"
      pattern: "classify_codes\\("
---

<objective>
Rewrite `R/102_death_cause_nhl_flag.R` so `cause_of_death_is_nhl` is sourced from the newly
loaded DEATH_CAUSE table (Plan 02) instead of the empty DEATH.DEATH_CAUSE column. Keep the exact
output contract (PATID + three-state flag). Prefer the underlying cause of death. Include a
clearly-labeled proxy backstop that only fires if the DEATH_CAUSE source is empty for the
deceased set. Also correct R/35's identical stale assumption.

Purpose: This is the actual fix. Phase 118 proved the shape/plumbing (1,344 rows, correct PATID,
three-state) — only the input field was empty because DEATH has no cause column. Now that
DEATH_CAUSE is loaded, R/102 reads real ICD cause codes.
Output: R/102 reads DEATH_CAUSE table; R/35 annotated/corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-fix-death-cause-nhl-flag/119-CONTEXT.md
@.planning/phases/119-fix-death-cause-nhl-flag/119-RESEARCH.md

<gate_note>
Depends on Plan 01 (diagnostic gate — confirms DEATH_CAUSE is the populated source) and Plan 02
(DEATH_CAUSE loaded into the pipeline). RESEARCH found the DEATH_CAUSE table exists, so the
PRIMARY path (real cause codes from DEATH_CAUSE) is the expected implementation. If Plan 01's
diagnostic instead reported DEATH_CAUSE empty but a TUMOR_REGISTRY source populated, the executor
should read that source via TUMOR_REGISTRY_ALL (RESEARCH "Case B") — but still route through the
same three-state classify logic. The proxy backstop (D-05) is last resort only.
</gate_note>

<environment_split>
Runtime (row counts, actual TRUE/FALSE tallies) is HiPerGator-only. Executor verifies R/102
STRUCTURALLY (grep): it no longer reads cause from DEATH, it reads DEATH_CAUSE, preserves the
three-state + output contract, and contains a labeled proxy branch. The user runs R/102 on
HiPerGator to confirm non-zero TRUE/FALSE counts.
</environment_split>

<interfaces>
Current R/102 (Phase 118) structure to MODIFY:
  Section 3: self-bootstrap DuckDB (USE_DUCKDB + open_pcornet_con) — KEEP verbatim.
  Section 4: DEATH load -> parse_pcornet_date -> 1900 sentinel -> filter !is.na(DEATH_DATE)
             -> group_by(ID)/summarise(min DEATH_DATE, first DEATH_CAUSE). The DEATH_CAUSE here
             comes from the DEATH table (empty) — THIS is what must change.
  Section 5: three-state case_when producing cause_of_death_is_nhl — KEEP the logic, just feed it
             the DEATH_CAUSE column joined from the DEATH_CAUSE table.
  Section 6: transmute(PATID = ID, cause_of_death_is_nhl) + write.csv(row.names=FALSE, na="") — KEEP.

DEATH_CAUSE columns: ID, DEATH_CAUSE, DEATH_CAUSE_CODE, DEATH_CAUSE_TYPE (U/C/O/I),
  DEATH_CAUSE_SOURCE, DEATH_CAUSE_CONFIDENCE, SOURCE.
classify_codes() normalizes internally; pass raw DEATH_CAUSE. NHL_CATEGORY = "Non-Hodgkin Lymphoma".
NAACCR sentinels only apply to the TR fallback path: c("0000","7777","7797").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite R/102 to read cause from the DEATH_CAUSE table (underlying-cause preferred) + proxy backstop</name>
  <read_first>
    - R/102_death_cause_nhl_flag.R (full — all 7 sections; the DEATH-table cause read in Section 4 and the three-state case_when in Section 5 are what change/stay)
    - R/35_death_cause_quality.R lines 62-117 (DEATH-load + field-availability guard reference)
    - .planning/phases/119-fix-death-cause-nhl-flag/119-RESEARCH.md "R/102 Fix Pattern" (Case A + Case B), "Common Pitfalls" (2, 3, 4, 5)
    - R/utils/utils_cancer.R (classify_codes)
  </read_first>
  <action>
Modify `R/102_death_cause_nhl_flag.R`:

1. HEADER: update the Purpose/Inputs block and the trailing "Phase 118" line to note the
   Phase 119 fix — "cause of death is read from the DEATH_CAUSE table (separate PCORnet CDM
   table joined by ID), not from a DEATH.DEATH_CAUSE column, which does not exist in this
   extract." Change Inputs to mention the DuckDB DEATH_CAUSE table. Keep Outputs identical.

2. SECTION 4 — keep the DEATH-table deceased set derivation EXACTLY (parse_pcornet_date, 1900
   sentinel, filter !is.na(DEATH_DATE), group_by(ID)/min DEATH_DATE) BUT remove the DEATH-table
   DEATH_CAUSE read (the `first(DEATH_CAUSE)` from DEATH and the DEATH-table field-availability
   guard on DEATH_CAUSE). The deceased set becomes just `ID, DEATH_DATE` (one row per patient).
   Rename this frame `deceased_set`. Do NOT call close_pcornet_con() yet (need DEATH_CAUSE next).

3. NEW SECTION 4b — load cause codes from the DEATH_CAUSE table with underlying-cause preference:
   ```r
   dc_tbl <- get_pcornet_table("DEATH_CAUSE")
   if (is.null(dc_tbl)) {
     message("  WARNING: DEATH_CAUSE table not in DuckDB — run R/01 (force_reload) + R/03 to load it.")
     death_cause_by_patient <- tibble(ID = character(), DEATH_CAUSE = character())
   } else {
     death_cause_by_patient <- dc_tbl %>% collect() %>%
       mutate(type_rank = case_when(
         DEATH_CAUSE_TYPE == "U" ~ 1L,   # underlying cause preferred
         DEATH_CAUSE_TYPE == "C" ~ 2L,   # contributing
         TRUE                    ~ 3L)) %>%
       filter(!is.na(DEATH_CAUSE), trimws(DEATH_CAUSE) != "") %>%
       arrange(ID, type_rank) %>%
       group_by(ID) %>%
       summarise(DEATH_CAUSE = first(DEATH_CAUSE), .groups = "drop")
   }
   death_data <- deceased_set %>% left_join(death_cause_by_patient, by = "ID")
   close_pcornet_con()
   ```
   (This is RESEARCH "Case A". If Plan 01's diagnostic reported DEATH_CAUSE empty but a TR source
   populated, implement RESEARCH "Case B" instead — coalesce CAUSE_OF_DEATH/DCAUSE from
   TUMOR_REGISTRY_ALL, filtering NAACCR sentinels c("0000","7777","7797") — and note the choice
   in the SUMMARY. Given RESEARCH confirmed DEATH_CAUSE exists, Case A is the expected path.)

4. SECTION 5 — KEEP the three-state case_when verbatim (cause_missing / cause_category via
   classify_codes / case_when NA/TRUE/FALSE). It now operates on the joined DEATH_CAUSE column.

5. NEW SECTION 5b — PROXY BACKSTOP (D-05, last resort, clearly labeled):
   ```r
   # PROXY BACKSTOP (CONTEXT D-05): only if NO coded cause exists for ANY deceased patient
   # (real cause-of-death signal genuinely unavailable). Flags deceased patients whose CONFIRMED
   # cancer history includes NHL via classify_codes() on DIAGNOSIS. This is a PROXY —
   # "NHL in cancer diagnosis history", NOT literal cause of death — and must be labeled as such.
   n_coded <- sum(!is.na(death_flagged$cause_of_death_is_nhl))
   USED_PROXY_BACKSTOP <- FALSE
   if (n_coded == 0) {
     message("  NOTE: No coded cause of death found for any deceased patient.")
     message("        Falling back to DIAGNOSIS-history PROXY (D-05, last resort).")
     USED_PROXY_BACKSTOP <- TRUE
     # ... derive NHL-in-history per deceased ID via get_pcornet_table("DIAGNOSIS") + classify_codes(),
     #     set cause_of_death_is_nhl TRUE/FALSE accordingly (still three-state), and note proxy in console.
   }
   ```
   Keep this branch OFF by default (RESEARCH found DEATH_CAUSE exists). Ensure the message and a
   `USED_PROXY_BACKSTOP` flag make it unmistakable when the proxy was used.

6. SECTION 6/7 — KEEP the transmute(PATID = ID, cause_of_death_is_nhl), arrange(PATID),
   write.csv(OUTPUT_CSV, row.names = FALSE, na = ""), and the summary tallies unchanged. Update
   the final "Phase 118 -- NHLDEATH-..." message to also reference "Phase 119 fix (DEATH_CAUSE table)".

Do NOT broaden NHL beyond classify_codes()=="Non-Hodgkin Lymphoma" (D-06). Do NOT add cause-code
or category columns to the output (deferred). Keep exactly PATID + cause_of_death_is_nhl.
  </action>
  <verify>
    <automated>MISSING — TRUE/FALSE counts are HiPerGator runtime. Structural greps below.</automated>
  </verify>
  <acceptance_criteria>
    - R/102 contains `get_pcornet_table("DEATH_CAUSE")`.
    - R/102 does NOT read cause from the DEATH table: it must NOT contain `first(DEATH_CAUSE` applied to the DEATH-table frame, and must NOT select `DEATH_CAUSE = all_of(death_cause_col)` from the DEATH table (the old DEATH-table DEATH_CAUSE field guard is removed). Negative grep target: the DEATH-table `select(ID, DEATH_DATE, DEATH_CAUSE = all_of(death_cause_col))` pattern is gone.
    - R/102 still contains `get_pcornet_table("DEATH")` (deceased-set derivation kept).
    - R/102 contains `DEATH_CAUSE_TYPE` and a `"U"` underlying-cause preference (type_rank or filter).
    - R/102 contains `left_join(` joining cause codes to the deceased set by `"ID"`.
    - R/102 contains `classify_codes(` and the exact string `Non-Hodgkin Lymphoma`.
    - R/102 contains the three-state `case_when` producing `cause_of_death_is_nhl` with an NA branch.
    - R/102 contains `transmute(` with `PATID` and `cause_of_death_is_nhl`, and `write.csv(` with `row.names = FALSE` and `na = ""` targeting `death_cause_nhl_flag.csv`.
    - R/102 contains a `USED_PROXY_BACKSTOP` flag and a labeled PROXY backstop comment referencing `D-05`.
    - R/102 contains a `Phase 119` reference in its header/summary.
    - R/102 handles 1900 sentinel and `filter(!is.na(DEATH_DATE)` (deceased set intact).
  </acceptance_criteria>
  <done>R/102 sources cause of death from the DEATH_CAUSE table (underlying-cause preferred), keeps the three-state flag + exact output contract, and has a labeled proxy backstop that is off by default.</done>
</task>

<task type="auto">
  <name>Task 2: Correct / annotate R/35's stale DEATH.DEATH_CAUSE assumption</name>
  <read_first>
    - R/35_death_cause_quality.R lines 62-136 (DEATH load, DEATH_CAUSE field guard, completeness computation)
    - .planning/phases/119-fix-death-cause-nhl-flag/119-RESEARCH.md "R/35 Update"
    - R/102_death_cause_nhl_flag.R (as rewritten in Task 1 — the DEATH_CAUSE-table read to mirror)
  </read_first>
  <action>
R/35 has the same "DEATH_CAUSE lives inside the DEATH table" assumption (Section 2 field-
availability guard). Per CONTEXT discretion ("update R/35 too, at minimum note it"), do the
CHEAP correct thing:

Option A (preferred, cheap): change R/35 Section 2 to read cause codes from the DEATH_CAUSE
table (mirror R/102 Task 1's `get_pcornet_table("DEATH_CAUSE")` + underlying-cause-preferred
per-patient summarise), then `left_join` onto the DEATH-derived deceased set so the rest of
R/35's completeness/payer/site profiling operates on real DEATH_CAUSE codes.

If wiring the full join into R/35 risks disturbing its 5-sheet xlsx logic, fall back to Option B:
add a prominent comment block at the top of R/35 Section 2 stating: "Phase 119: cause of death
is NOT a column in the DEATH table in this extract — it lives in the separate DEATH_CAUSE table
(see R/102). This script's DEATH.DEATH_CAUSE guard will report ~100% missingness; read from
DEATH_CAUSE for real completeness." — and keep the guard.

Pick ONE and document which in the SUMMARY. Do not break R/35's output shape.
  </action>
  <verify>
    <automated>MISSING — HiPerGator runtime. Structural: grep R/35 for a Phase 119 DEATH_CAUSE-table reference/comment</automated>
  </verify>
  <acceptance_criteria>
    - R/35_death_cause_quality.R contains a `Phase 119` reference AND the string `DEATH_CAUSE table` (annotation) OR `get_pcornet_table("DEATH_CAUSE")` (full correction).
    - R/35's 5-sheet xlsx output structure is unchanged (no worksheets removed; wb_save target still death_cause_quality.xlsx).
  </acceptance_criteria>
  <done>R/35 no longer silently implies DEATH.DEATH_CAUSE is the source — it either reads DEATH_CAUSE or clearly annotates the correct source.</done>
</task>

</tasks>

<verification>
- Structural (local, executor): all acceptance_criteria greps pass; negative grep confirms R/102 no longer reads cause from the DEATH table.
- Runtime (HiPerGator, USER): after Plan 02's DuckDB rebuild, user runs R/102 and confirms
  output/death_cause_nhl_flag.csv now has non-zero TRUE and/or FALSE counts (not 100% blank).
</verification>

<success_criteria>
- R/102 reads cause of death from the DEATH_CAUSE table, prefers underlying cause, keeps the
  three-state flag + exact PATID/flag output contract, and has a documented off-by-default proxy backstop.
- R/35's stale assumption corrected or annotated.
</success_criteria>

<output>
After completion, create `.planning/phases/119-fix-death-cause-nhl-flag/119-03-SUMMARY.md`
</output>
