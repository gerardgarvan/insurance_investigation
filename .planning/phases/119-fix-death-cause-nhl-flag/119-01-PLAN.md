---
phase: 119-fix-death-cause-nhl-flag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/103_death_cause_diagnostic.R
autonomous: true
requirements: [NHLFIX-01]
must_haves:
  truths:
    - "User can run R/103 on HiPerGator and see non-null counts + sample values for every candidate cause-of-death source (DEATH_CAUSE table, TR1.CAUSE_OF_DEATH, TR2/TR3.DCAUSE), restricted to the deceased patient set."
    - "The diagnostic prints a RECOMMENDATION line telling the user which source to use (Source 1 DEATH_CAUSE / TR fallback / proxy backstop)."
    - "The diagnostic writes a small summary artifact the user can paste back."
  artifacts:
    - path: "R/103_death_cause_diagnostic.R"
      provides: "Read-only cause-of-death signal inventory diagnostic restricted to deceased PATIDs"
      min_lines: 100
    - path: "output/diagnostics/death_cause_source_inventory.csv"
      provides: "Per-source non-null / coverage summary (written at runtime on HiPerGator)"
  key_links:
    - from: "R/103_death_cause_diagnostic.R"
      to: "DEATH_CAUSE table + TUMOR_REGISTRY_ALL view + DEATH deceased set"
      via: "get_pcornet_table() + classify_codes()"
      pattern: "get_pcornet_table\\(\"DEATH"
---

<objective>
Create `R/103_death_cause_diagnostic.R`, a READ-ONLY diagnostic that the user runs on
HiPerGator to determine WHERE a populated cause-of-death signal lives for the 1,344
deceased patients (valid DEATH_DATE). This is the Wave 0 gate: the executor is Windows-local
without the data, so this script produces the evidence that confirms which source Wave 2's
R/102 rewrite reads. It does NOT modify any pipeline output.

Purpose: De-risk the whole phase. RESEARCH confirmed the DEATH_CAUSE table SCHEMA exists but
its POPULATION is unknown until this runs. The user pastes back the console summary / CSV so
we know whether to proceed with Source 1 (DEATH_CAUSE table), a TUMOR_REGISTRY fallback, or
the proxy backstop.

Output: R/103 script + (at runtime on HiPerGator) output/diagnostics/death_cause_source_inventory.csv
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-fix-death-cause-nhl-flag/119-CONTEXT.md
@.planning/phases/119-fix-death-cause-nhl-flag/119-RESEARCH.md

<environment_split>
CRITICAL (Phase 116/117/118 precedent): the pipeline runs on RStudio on HiPerGator against
real PCORnet data. The executor is Windows-local WITHOUT the data / DuckDB file. Therefore:
- R/103 is HiPerGator-only for RUNTIME (the user runs it and pastes back the numbers).
- The executor can only STRUCTURALLY verify R/103 locally (grep/parse for the required calls,
  sections, and output write). Design acceptance_criteria as grep/file-read checks, not runtime.
</environment_split>

<interfaces>
From R/utils/utils_duckdb.R:
  open_pcornet_con()                      # opens the global pcornet_con connection (idempotent)
  close_pcornet_con()
  get_pcornet_table("DEATH")              # returns a lazy tbl; %>% collect() to pull
  get_pcornet_table("DEATH_CAUSE")        # returns NULL if not loaded into DuckDB
  # open_pcornet_con() also creates a TEMP VIEW: TUMOR_REGISTRY_ALL (UNION ALL BY NAME of TR1/TR2/TR3)

From R/utils/utils_dates.R:
  parse_pcornet_date(x)                   # returns Date

From R/utils/utils_cancer.R:
  classify_codes(codes)                   # returns category label; "Non-Hodgkin Lymphoma" for NHL

From R/00_config.R:
  CONFIG$output_dir                       # output/ directory
  PCORNET_PATHS[["DEATH_CAUSE"]]          # only present after Plan 02 adds DEATH_CAUSE to PCORNET_TABLES;
                                          # R/103 must NOT assume it exists — use file.path(CONFIG$data_dir, "DEATH_CAUSE_Mailhot_V1.csv") directly with file.exists()

DEATH_CAUSE columns (RESEARCH): ID, DEATH_CAUSE, DEATH_CAUSE_CODE, DEATH_CAUSE_TYPE,
  DEATH_CAUSE_SOURCE, DEATH_CAUSE_CONFIDENCE, SOURCE
  DEATH_CAUSE_TYPE values: U=underlying, C=contributing, O=other, I=inferred
TUMOR_REGISTRY cause fields: TR1.CAUSE_OF_DEATH (NAACCR #1910), TR2/TR3.DCAUSE (#1910)
NAACCR cause sentinels to EXCLUDE before classify: "0000" (alive), "7777" (cert unavailable), "7797" (uncoded)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/103_death_cause_diagnostic.R — deceased-set cause-of-death signal inventory</name>
  <read_first>
    - R/102_death_cause_nhl_flag.R (deceased-set derivation Section 4: DEATH load, parse_pcornet_date, 1900 sentinel, filter !is.na(DEATH_DATE), group_by(ID)/min; self-bootstrap Section 3)
    - R/35_death_cause_quality.R (DEATH-load block + field-availability guard pattern)
    - R/00_config.R lines 225-253 (PCORNET_TABLES, PCORNET_PATHS, {TABLE}_Mailhot_V1.csv convention, CONFIG$data_dir)
    - R/utils/utils_duckdb.R lines 118-150 (open_pcornet_con, get_pcornet_table, TUMOR_REGISTRY_ALL view)
    - R/utils/utils_cancer.R (classify_codes signature/behavior)
    - .planning/phases/119-fix-death-cause-nhl-flag/119-RESEARCH.md "Diagnostic Script Spec" + "Priority Order" + "Common Pitfalls"
  </read_first>
  <action>
Create `R/103_death_cause_diagnostic.R` following the R/102 header + section style (5-field
header block: Purpose / Inputs / Outputs / Dependencies / Requirements + RStudio "SECTION N:"
banners). READ-ONLY: it must NOT write any pipeline output; it may write ONE small diagnostic CSV.

SECTION 1 — Setup: `suppressPackageStartupMessages({ library(dplyr); library(glue); library(stringr); library(lubridate) })`
then `source("R/00_config.R"); source("R/utils/utils_duckdb.R"); source("R/utils/utils_dates.R")`.
(utils_cancer is auto-sourced by 00_config; if classify_codes not found, also source utils_cancer.)
Print `message("=== Phase 119 Diagnostic: Cause-of-Death Signal Inventory ===\n")`.

SECTION 2 — Constants:
  `NHL_CATEGORY <- "Non-Hodgkin Lymphoma"`
  `NAACCR_DEATH_SENTINELS <- c("0000", "7777", "7797")`
  `OUTPUT_CSV <- file.path(CONFIG$output_dir, "diagnostics", "death_cause_source_inventory.csv")`
  `dir.create(dirname(OUTPUT_CSV), recursive = TRUE, showWarnings = FALSE)`

SECTION 3 — Self-bootstrap DuckDB (copy R/102 pattern verbatim):
  `USE_DUCKDB <- TRUE`
  `if (!exists("pcornet_con", envir = .GlobalEnv)) { open_pcornet_con() }`

SECTION 4 — Deceased PATID set (reuse R/102 derivation):
  ```r
  deceased_ids <- get_pcornet_table("DEATH") %>% collect() %>%
    mutate(DEATH_DATE = parse_pcornet_date(DEATH_DATE)) %>%
    mutate(DEATH_DATE = if_else(year(DEATH_DATE) == 1900L, as.Date(NA), DEATH_DATE)) %>%
    filter(!is.na(DEATH_DATE)) %>% pull(ID) %>% unique()
  message(glue("Deceased patient set (valid DEATH_DATE): {length(deceased_ids)}"))
  ```

SECTION 5 — Source 1: DEATH_CAUSE table. Check the CSV first, then DuckDB:
  ```r
  dc_path <- file.path(CONFIG$data_dir, "DEATH_CAUSE_Mailhot_V1.csv")
  message(glue("--- Source 1: DEATH_CAUSE table ---"))
  message(glue("  CSV exists at {dc_path}? {file.exists(dc_path)}"))
  dc_tbl <- get_pcornet_table("DEATH_CAUSE")   # NULL if not yet loaded into DuckDB
  ```
  If `is.null(dc_tbl)` message that DEATH_CAUSE is not yet in DuckDB (expected before Plan 02) and
  set all Source-1 counts to NA. Otherwise `dc <- dc_tbl %>% collect()` and report:
    - row count; non-null DEATH_CAUSE count + pct;
    - DEATH_CAUSE_TYPE distribution via `count(DEATH_CAUSE_TYPE)`;
    - rows with `DEATH_CAUSE_TYPE == "U"`;
    - `n_distinct(intersect(dc$ID, deceased_ids))` = deceased-set coverage;
    - first 10 non-null DEATH_CAUSE sample values;
    - classify: on the underlying-cause-preferred one-per-ID set, report NHL / non-NHL / unclassified counts
      using `classify_codes()` (skip NAACCR sentinels via `!DEATH_CAUSE %in% NAACCR_DEATH_SENTINELS`).

SECTION 6 — Sources 2 & 3: TUMOR_REGISTRY via the view. Guard columns conditionally (Pitfall 5):
  ```r
  tr <- get_pcornet_table("TUMOR_REGISTRY_ALL") %>% collect()
  has_cod    <- "CAUSE_OF_DEATH" %in% names(tr)
  has_dcause <- "DCAUSE" %in% names(tr)
  tr <- tr %>% mutate(cause_code = coalesce(
    if (has_cod) CAUSE_OF_DEATH else NA_character_,
    if (has_dcause) DCAUSE else NA_character_))
  ```
  Report for CAUSE_OF_DEATH and DCAUSE separately: non-null counts, deceased-set coverage
  (`n_distinct(intersect(tr$ID[...], deceased_ids))`), sample values, and classify_codes() NHL
  matches AFTER filtering out NAACCR sentinels + blanks.

SECTION 7 — Coverage summary + recommendation. Compute deceased-set coverage per source; print a
  `--- Recommendation ---` line: if Source 1 coverage > 0 print `[PROCEED WITH SOURCE 1: DEATH_CAUSE table]`,
  else if any TR source coverage > 0 print `[FALLBACK TO TUMOR_REGISTRY SOURCE]`, else
  `[PROXY BACKSTOP REQUIRED — no coded cause found for deceased set]`.

SECTION 8 — Write the small artifact: a tibble with one row per source
  (source_name, column, n_nonnull, deceased_coverage, n_nhl) via
  `write.csv(inventory, OUTPUT_CSV, row.names = FALSE, na = "")`. Then `close_pcornet_con()`.
  Print final `message(glue("Wrote inventory: {OUTPUT_CSV}"))`.

Do NOT touch R/102, R/00_config, or any output CSV. This is investigation only.
  </action>
  <verify>
    <automated>MISSING — runtime is HiPerGator-only. Local structural verify: grep R/103 for get_pcornet_table("DEATH_CAUSE"), classify_codes(, "Non-Hodgkin Lymphoma", NAACCR sentinels, deceased_ids intersect, RECOMMENDATION, write.csv inventory</automated>
  </verify>
  <acceptance_criteria>
    - File `R/103_death_cause_diagnostic.R` exists and has >= 100 lines.
    - Contains `source("R/00_config.R")` and sources utils_duckdb + utils_dates.
    - Contains `USE_DUCKDB` and `open_pcornet_con` (self-bootstrap).
    - Contains `get_pcornet_table("DEATH")` (deceased set derivation) AND `get_pcornet_table("DEATH_CAUSE")`.
    - Contains `get_pcornet_table("TUMOR_REGISTRY_ALL")` and both `CAUSE_OF_DEATH` and `DCAUSE`.
    - Contains `parse_pcornet_date` and `1900` sentinel handling and `filter(!is.na(DEATH_DATE)`.
    - Contains `classify_codes(` and the exact string `Non-Hodgkin Lymphoma`.
    - Contains the NAACCR sentinel vector including `"0000"`, `"7777"`, `"7797"`.
    - Contains `deceased_ids` and an `intersect(` call (deceased-set restriction).
    - Contains a recommendation string matching `PROCEED WITH SOURCE 1` and `PROXY BACKSTOP`.
    - Contains `write.csv(` targeting `death_cause_source_inventory.csv` with `row.names = FALSE` and `na = ""`.
    - Contains NO `ggplot`, `ggsave`, or `geom_` (data/diagnostic only).
    - Does NOT contain `write.csv(` targeting `death_cause_nhl_flag.csv` (must not touch the real output).
  </acceptance_criteria>
  <done>R/103 exists, structurally verifiable, and (on HiPerGator) prints per-source non-null/coverage numbers + a recommendation and writes the inventory CSV.</done>
</task>

</tasks>

<verification>
- Structural (local, executor): all acceptance_criteria greps pass on R/103.
- Runtime (HiPerGator, USER GATE): user runs `Rscript R/103_death_cause_diagnostic.R`, reviews the
  console inventory + output/diagnostics/death_cause_source_inventory.csv, and pastes back which
  source is populated. This gates Plan 03 (R/102 rewrite) source selection.
</verification>

<success_criteria>
- R/103 exists, is read-only, and structurally passes all greps.
- On HiPerGator it reports, restricted to the ~1,344 deceased PATIDs: DEATH_CAUSE non-null +
  coverage + NHL classify counts; TR1.CAUSE_OF_DEATH and TR2/TR3.DCAUSE non-null + coverage + NHL;
  and a single-line recommendation.
</success_criteria>

<output>
After completion, create `.planning/phases/119-fix-death-cause-nhl-flag/119-01-SUMMARY.md`
</output>
