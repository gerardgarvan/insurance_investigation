---
phase: 26-overlap-classification-and-recommendations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/23_overlap_classification.R
autonomous: false
requirements:
  - OVRLP-01
  - OVRLP-02
  - OVRLP-03
  - OVRLP-04
  - OUTPT-01
  - OUTPT-02
  - OUTPT-03

must_haves:
  truths:
    - "User can see field-by-field match/mismatch flags for ENC_TYPE, PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY, PROVIDERID, and DISCHARGE_DATE for each same-date multi-source group"
    - "User can see each multi-source group classified as Identical (all match), Partial (some match), or Distinct (none match) with raw match counts"
    - "User can see per-site overlap profiles showing % Identical vs Partial vs Distinct"
    - "User can see same-week groups classified with 4-field comparison (excluding DISCHARGE_DATE) alongside basis column"
    - "User can see 4 CSV files in output/tables/ with classified detail and per-site summaries"
    - "User can see console summary with per-site rates, classification breakdown, and key findings"
    - "User can read per-site actionable recommendations with preferred source suggestion for sites where deduplication is appropriate"
  artifacts:
    - path: "R/23_overlap_classification.R"
      provides: "Standalone overlap classification script with field comparison, labeling, CSV output, console summary, and per-site recommendations"
      min_lines: 300
    - path: "output/tables/classified_same_date_detail.csv"
      provides: "Per-pair same-date classification with match flags and Identical/Partial/Distinct labels"
    - path: "output/tables/classified_same_week_detail.csv"
      provides: "Per-pair same-week classification with 4-field match flags and labels"
    - path: "output/tables/per_site_overlap_profile.csv"
      provides: "Per-site aggregate: % Identical/Partial/Distinct with recommendation and preferred source"
    - path: "output/tables/overlap_source_payer_completeness.csv"
      provides: "Per-site, per-source payer completeness for multi-source encounters"
  key_links:
    - from: "R/23_overlap_classification.R"
      to: "output/tables/multi_source_same_date_detail.csv"
      via: "readr::read_csv to load Phase 25 same-date detail"
      pattern: "read_csv.*multi_source_same_date_detail"
    - from: "R/23_overlap_classification.R"
      to: "output/tables/multi_source_same_week_detail.csv"
      via: "readr::read_csv to load Phase 25 same-week detail"
      pattern: "read_csv.*multi_source_same_week_detail"
    - from: "R/23_overlap_classification.R"
      to: "pcornet$ENCOUNTER"
      via: "inner_join on ID + ADMIT_DATE to extract comparison fields"
      pattern: "inner_join.*pcornet\\$ENCOUNTER"
    - from: "R/23_overlap_classification.R"
      to: "pcornet$DEMOGRAPHIC"
      via: "left_join to get SITE assignment from DEMOGRAPHIC.SOURCE"
      pattern: "left_join.*pcornet\\$DEMOGRAPHIC"
---

<objective>
Create standalone R script R/23_overlap_classification.R that classifies each multi-source encounter group (same-date and same-week) from Phase 25 output as Identical, Partial, or Distinct via field-by-field comparison, with CSV outputs, console summary, and per-site actionable recommendations.

Purpose: Enable the investigator to assess multi-source encounter quality per site and decide whether to deduplicate or retain overlapping encounters, with data-driven preferred source suggestions.

Output: R/23_overlap_classification.R (standalone script), 4 CSV files in output/tables/, console summary with per-site recommendations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-overlap-classification-and-recommendations/26-CONTEXT.md
@.planning/phases/26-overlap-classification-and-recommendations/26-RESEARCH.md
@.planning/phases/25-multi-source-overlap-detection/25-01-SUMMARY.md

@R/22_multi_source_overlap_detection.R
@R/21_all_site_duplicate_dates.R
@R/00_config.R

<interfaces>
<!-- Key patterns and contracts the executor needs from upstream code. -->

From R/22_multi_source_overlap_detection.R (Phase 25 -- input producer):
```r
# CSV 1: multi_source_same_date_detail.csv
# Columns: ID, ADMIT_DATE, n_sources, n_encounters, source_combo, sources_list
# Note: n_sources and n_encounters are HIPAA-suppressed strings in CSV (e.g., "<11")
# ADMIT_DATE is Date formatted as YYYY-MM-DD string

# CSV 2: multi_source_same_week_detail.csv
# Columns: ID, admit_date_1, source_1, admit_date_2, source_2, day_gap, source_combo
# Each row is one cross-source pair within 1-7 day gap
# source_1 < source_2 alphabetically (deduplication convention)
```

From R/21_all_site_duplicate_dates.R (Phase 22 -- pattern reference):
```r
# Payer missingness helper (lines 48-52):
is_missing_payer <- function(payer_value) {
  is.na(payer_value) |
    nchar(trimws(payer_value)) == 0 |
    payer_value %in% c("NI", "UN", "OT", "99", "9999")
}

# SOURCE column collision pattern (lines 91-94):
# Rename ENCOUNTER.SOURCE to ENCOUNTER_SOURCE, then join DEMOGRAPHIC.SOURCE as SITE
all_encounters <- pcornet$ENCOUNTER %>%
  rename(ENCOUNTER_SOURCE = SOURCE) %>%
  left_join(pcornet$DEMOGRAPHIC %>% select(ID, SOURCE), by = "ID") %>%
  rename(SITE = SOURCE)
```

From R/22_multi_source_overlap_detection.R (HIPAA helpers, lines 47-60):
```r
hipaa_suppress <- function(x) {
  x_num <- suppressWarnings(as.numeric(x))
  ifelse(!is.na(x_num) & x_num >= 1 & x_num <= 10, "<11", as.character(x))
}

suppress_counts <- function(df) {
  count_cols <- grep("^n_|^n$|_count$|_pairs$|_affected$|_dates$|_encounters$|_patients$|^rank$",
                     names(df), value = TRUE)
  count_cols <- count_cols[!grepl("pct_|_rate$|_pct$", count_cols)]
  df %>%
    mutate(across(all_of(count_cols), ~ hipaa_suppress(.x)))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/23_overlap_classification.R</name>
  <files>R/23_overlap_classification.R</files>

  <read_first>
    R/22_multi_source_overlap_detection.R
    R/21_all_site_duplicate_dates.R
    R/00_config.R
    R/01_load_pcornet.R
    .planning/phases/26-overlap-classification-and-recommendations/26-CONTEXT.md
  </read_first>

  <action>
Create standalone R script `R/23_overlap_classification.R` following the established diagnostic script pattern (source config, conditional load, section-based structure, console output, CSV writes). The script has 8 sections:

**File header:** Match R/22_multi_source_overlap_detection.R pattern exactly -- comment block with phase, requirements (OVRLP-01 through OVRLP-04, OUTPT-01 through OUTPT-03), purpose, output CSV list, usage, dependencies, "Standalone script" note.

**Library loading:** `source("R/00_config.R")`, then `library(dplyr)`, `library(lubridate)`, `library(glue)`, `library(readr)`, `library(stringr)`, `library(tidyr)`. Conditional load: `if (!exists("pcornet")) source("R/01_load_pcornet.R")`.

**SECTION 0: Helper functions.** Define these three helpers at the top:

1. `is_missing_payer(payer_value)` -- copy from R/21_all_site_duplicate_dates.R lines 48-52: returns `is.na(payer_value) | nchar(trimws(payer_value)) == 0 | payer_value %in% c("NI", "UN", "OT", "99", "9999")`. Per D-03.

2. `hipaa_suppress(x)` and `suppress_counts(df)` -- copy from R/22_multi_source_overlap_detection.R lines 47-60, exactly as defined there.

3. `field_match(val1, val2)` -- vectorized field comparison helper implementing D-01 and D-02:
```r
field_match <- function(val1, val2) {
  both_na <- is.na(val1) & is.na(val2)
  one_na  <- xor(is.na(val1), is.na(val2))
  both_na | (!one_na & !is.na(val1) & (val1 == val2))
}
```
This returns TRUE when both NA (D-01: match), FALSE when one NA one present (D-02: mismatch), and val1==val2 when both present.

**SECTION 1: Load Phase 25 CSVs and prepare ENCOUNTER data.**
- Read `output/tables/multi_source_same_date_detail.csv` via `read_csv()`. Note: the `n_sources` and `n_encounters` columns are HIPAA-suppressed strings -- do NOT use them for numeric operations. Only use `ID`, `ADMIT_DATE`, `source_combo`, `sources_list` columns.
- Read `output/tables/multi_source_same_week_detail.csv` via `read_csv()`. Columns: `ID`, `admit_date_1`, `source_1`, `admit_date_2`, `source_2`, `day_gap`, `source_combo`.
- Log row counts for both CSVs.
- Prepare ENCOUNTER: copy `pcornet$ENCOUNTER`, rename `SOURCE` to `ENCOUNTER_SOURCE` (Phase 22 pattern), parse `ADMIT_DATE` and `DISCHARGE_DATE` to Date with standard `as.Date(x, format="%Y-%m-%d")` + fallback to `parse_pcornet_date()` if parse rate < 50% (same pattern as R/22 lines 86-105).
- Normalize payer fields per D-03: `mutate(payer_primary_norm = if_else(is_missing_payer(PAYER_TYPE_PRIMARY), NA_character_, PAYER_TYPE_PRIMARY), payer_secondary_norm = if_else(is_missing_payer(PAYER_TYPE_SECONDARY), NA_character_, PAYER_TYPE_SECONDARY))`.
- Join to DEMOGRAPHIC for SITE: `left_join(pcornet$DEMOGRAPHIC %>% select(ID, SOURCE), by = "ID") %>% rename(SITE = SOURCE)`.

**SECTION 2: Same-date field comparison (OVRLP-01, D-04).**
- For each unique (ID, ADMIT_DATE) in same_date_detail, fetch ALL encounters from the prepared ENCOUNTER data for that patient-date.
- Strategy: `inner_join(same_date_detail %>% select(ID, ADMIT_DATE) %>% distinct(), enc_prepared, by = c("ID", "ADMIT_DATE" = "admit_date_parsed"))`. This gives all encounter rows for multi-source patient-dates.
- Create pairwise comparisons WITHIN each (ID, ADMIT_DATE) group where ENCOUNTER_SOURCE differs. Use a self-join approach: `inner_join(sd_encounters, sd_encounters, by = c("ID", "ADMIT_DATE"), suffix = c("_1", "_2")) %>% filter(ENCOUNTER_SOURCE_1 < ENCOUNTER_SOURCE_2)` to get unique pairs per D-05 deduplication convention.
- Apply `field_match()` to create 5 boolean match columns per D-04:
  - `enc_type_match = field_match(ENC_TYPE_1, ENC_TYPE_2)`
  - `payer_pri_match = field_match(payer_primary_norm_1, payer_primary_norm_2)`
  - `payer_sec_match = field_match(payer_secondary_norm_1, payer_secondary_norm_2)`
  - `providerid_match = field_match(PROVIDERID_1, PROVIDERID_2)`
  - `discharge_match = field_match(discharge_date_parsed_1, discharge_date_parsed_2)`
- Log total pairs created.

**SECTION 3: Same-date classification (OVRLP-02, D-05, D-06).**
- Count matches per pair: `match_count = enc_type_match + payer_pri_match + payer_sec_match + providerid_match + discharge_match` (summing logicals gives integer count).
- Set `n_fields = 5L`.
- Apply classification per D-05: `classification = case_when(match_count == n_fields ~ "Identical", match_count == 0L ~ "Distinct", TRUE ~ "Partial")`.
- Create detailed label per D-06: `classification_detail = paste0(classification, " (", match_count, "/", n_fields, ")")`.
- Add `basis = "same_date (5 fields)"` per D-10.
- Log classification distribution: count of Identical, Partial, Distinct with percentages.

**SECTION 4: Same-week field comparison and classification (OVRLP-04, D-09, D-10).**
- For same-week pairs from Phase 25 CSV: each row already identifies a pair (ID, admit_date_1, source_1, admit_date_2, source_2).
- For each pair, fetch encounter data for (ID, admit_date_1, source_1) and (ID, admit_date_2, source_2) from the prepared ENCOUNTER table.
- Join the same_week_detail to ENCOUNTER twice: once for encounter 1 (by ID, admit_date_1, source_1) and once for encounter 2 (by ID, admit_date_2, source_2).
- When a (patient, date, source) has multiple encounters, pick one representative encounter per group (e.g., `slice_min(ENCOUNTERID, n=1)` after grouping by ID, admit_date, ENCOUNTER_SOURCE) to avoid Cartesian explosion.
- Apply `field_match()` to 4 fields per D-09 (exclude DISCHARGE_DATE):
  - `enc_type_match`, `payer_pri_match`, `payer_sec_match`, `providerid_match`
- `match_count = enc_type_match + payer_pri_match + payer_sec_match + providerid_match`, `n_fields = 4L`.
- Same classification logic: Identical=4/4, Partial=1-3/4, Distinct=0/4. Per D-05.
- `classification_detail = paste0(classification, " (", match_count, "/", n_fields, ")")`.
- `basis = "same_week (4 fields)"` per D-10.
- Log classification distribution.

**SECTION 5: Per-site overlap profiles (OVRLP-03, D-07).**
- For same-date classified pairs, join SITE (already in enc_prepared via DEMOGRAPHIC join). Use SITE from one of the encounters in the pair (both should belong to the same DEMOGRAPHIC.SOURCE site for patients assigned to one site). Use the SITE column already joined on the encounter data.
- Group by SITE, compute: `n_pairs`, `n_identical`, `n_partial`, `n_distinct_class` (rename to avoid collision with `dplyr::n_distinct`), `pct_identical`, `pct_partial`, `pct_distinct`.
- Apply D-07 recommendation thresholds:
  - `pct_identical >= 70` -> "Safe to deduplicate by keeping preferred source"
  - `pct_identical >= 30` -> "Mixed overlap -- review partial matches before deduplication"
  - `pct_identical < 30` -> "Encounters are largely distinct -- retain all"
- Create same profile for same-week classified pairs.
- Ensure all 5 sites appear even if zero multi-source pairs exist for a site (use `complete(SITE, fill=...)` or similar).

**SECTION 6: Preferred source from payer completeness (D-08).**
- From the same-date encounter pairs data (before classification aggregation), compute per (SITE, ENCOUNTER_SOURCE) payer completeness:
  - `n_encounters`, `n_primary_present = sum(!is_missing_payer(PAYER_TYPE_PRIMARY))`, `pct_primary_present = round(100 * n_primary_present / n_encounters, 1)`, same for secondary.
- Per site, identify preferred source as the one with highest `pct_primary_present` using `slice_max(pct_primary_present, n=1, with_ties=FALSE)`.
- Join preferred source back to per-site profile.
- Update recommendation text for sites with `pct_identical >= 70` to include preferred source: `glue("Safe to deduplicate -- prefer {preferred_source} ({preferred_source_pct}% payer completeness)")`.
- Write source completeness to `output/tables/overlap_source_payer_completeness.csv` with HIPAA suppression on count columns.

**SECTION 7: Write CSV outputs (OUTPT-01).**
All CSVs go to `file.path(CONFIG$output_dir, "tables")`.

CSV 1: `classified_same_date_detail.csv`
- Columns: SITE, ID, ADMIT_DATE, ENCOUNTER_SOURCE_1, ENCOUNTER_SOURCE_2, source_combo, ENC_TYPE_1, ENC_TYPE_2, enc_type_match, payer_primary_norm_1, payer_primary_norm_2, payer_pri_match, payer_secondary_norm_1, payer_secondary_norm_2, payer_sec_match, PROVIDERID_1, PROVIDERID_2, providerid_match, discharge_date_parsed_1, discharge_date_parsed_2, discharge_match, match_count, n_fields, classification, classification_detail, basis.
- No HIPAA suppression needed on match flags (booleans) or classification (labels). IDs are already present in Phase 25 CSVs.

CSV 2: `classified_same_week_detail.csv`
- Columns: SITE, ID, admit_date_1, source_1, admit_date_2, source_2, day_gap, source_combo, ENC_TYPE_1, ENC_TYPE_2, enc_type_match, payer_primary_norm_1, payer_primary_norm_2, payer_pri_match, payer_secondary_norm_1, payer_secondary_norm_2, payer_sec_match, PROVIDERID_1, PROVIDERID_2, providerid_match, match_count, n_fields, classification, classification_detail, basis.

CSV 3: `per_site_overlap_profile.csv`
- Columns: SITE, basis, n_pairs, n_identical, n_partial, n_distinct_class, pct_identical, pct_partial, pct_distinct, preferred_source, preferred_source_pct, recommendation.
- Two rows per site (one for same_date, one for same_week) plus an ALL aggregate row for each basis.
- HIPAA suppress count columns.

CSV 4: `overlap_source_payer_completeness.csv`
- Already described in Section 6. Columns: SITE, ENCOUNTER_SOURCE, n_encounters, n_primary_present, pct_primary_present, n_secondary_present, pct_secondary_present.
- HIPAA suppress count columns.

Log each CSV filename and row count.

**SECTION 8: Console summary (OUTPT-02, OUTPT-03).**
- Header: `strrep('=', 70)`, "OVERLAP CLASSIFICATION AND RECOMMENDATIONS", "Phase 26: ...", `strrep('=', 70)`.
- Overall stats: total same-date pairs classified, total same-week pairs classified.
- Same-date classification breakdown: N and % for Identical, Partial, Distinct.
- Same-week classification breakdown: same format.
- Per-site summary (compact one-line-per-site): `{SITE}: {n_pairs} pairs | {pct_identical}% Identical | {pct_partial}% Partial | {pct_distinct}% Distinct | Rec: {recommendation_short}`.
- Per-site recommendations (OUTPT-03): For each site, print full recommendation text including preferred source.
- List of 4 CSV files written.
- Phase 26 completion note.
- Footer: `strrep('=', 70)`, "END OF OVERLAP CLASSIFICATION AND RECOMMENDATIONS", `strrep('=', 70)`.
  </action>

  <verify>
    <automated>grep -c "field_match" R/23_overlap_classification.R</automated>
    <automated>grep -c "classification" R/23_overlap_classification.R</automated>
    <automated>grep -c "classified_same_date_detail" R/23_overlap_classification.R</automated>
    <automated>grep -c "classified_same_week_detail" R/23_overlap_classification.R</automated>
    <automated>grep -c "per_site_overlap_profile" R/23_overlap_classification.R</automated>
    <automated>grep -c "overlap_source_payer_completeness" R/23_overlap_classification.R</automated>
    <automated>grep -c "is_missing_payer" R/23_overlap_classification.R</automated>
    <automated>grep -c "recommendation" R/23_overlap_classification.R</automated>
    <automated>grep -c "basis" R/23_overlap_classification.R</automated>
  </verify>

  <acceptance_criteria>
    - R/23_overlap_classification.R exists and contains >= 300 lines
    - File header contains "Phase 26" and "OVRLP-01" and "OUTPT-03"
    - File contains `source("R/00_config.R")` on its own line
    - File contains `if (!exists("pcornet")) source("R/01_load_pcornet.R")`
    - File contains `is_missing_payer <- function(payer_value)` with body matching `c("NI", "UN", "OT", "99", "9999")`
    - File contains `hipaa_suppress <- function(x)` and `suppress_counts <- function(df)`
    - File contains `field_match <- function(val1, val2)` with both-NA match logic (D-01) and one-NA mismatch logic (D-02)
    - File contains `payer_primary_norm` and `payer_secondary_norm` columns created via `is_missing_payer()` normalization before comparison (D-03)
    - File compares exactly 5 fields for same-date: `enc_type_match`, `payer_pri_match`, `payer_sec_match`, `providerid_match`, `discharge_match` (D-04)
    - File compares exactly 4 fields for same-week (no `discharge_match`): `enc_type_match`, `payer_pri_match`, `payer_sec_match`, `providerid_match` (D-09)
    - File contains `classification = case_when(match_count == n_fields ~ "Identical", match_count == 0` for Distinct, and `TRUE ~` for Partial (D-05)
    - File contains `classification_detail` with match_count/n_fields format like `"Partial (3/5)"` (D-06)
    - File contains `pct_identical >= 70` threshold for "Safe to deduplicate" (D-07)
    - File contains `pct_identical >= 30` threshold for "Mixed overlap" (D-07)
    - File contains preferred source computation using `slice_max(pct_primary_present` (D-08)
    - File contains `basis` column with values `"same_date (5 fields)"` and `"same_week (4 fields)"` (D-10)
    - File writes 4 CSVs: `classified_same_date_detail.csv`, `classified_same_week_detail.csv`, `per_site_overlap_profile.csv`, `overlap_source_payer_completeness.csv`
    - File contains console summary with `strrep('=', 70)` section headers
    - File contains per-site recommendation output
    - File ends with "END OF OVERLAP CLASSIFICATION AND RECOMMENDATIONS"
  </acceptance_criteria>

  <done>
    R/23_overlap_classification.R is a complete, standalone diagnostic script that:
    (1) reads Phase 25 same-date and same-week CSVs,
    (2) joins to ENCOUNTER for 5-field comparison with payer normalization (D-03),
    (3) classifies pairs as Identical/Partial/Distinct with match counts (D-05, D-06),
    (4) handles same-week with 4 fields excluding DISCHARGE_DATE (D-09) and basis column (D-10),
    (5) computes per-site overlap profiles with recommendation thresholds (D-07) and preferred source (D-08),
    (6) writes 4 CSVs to output/tables/ with HIPAA suppression on count columns,
    (7) prints console summary with per-site recommendations.
    All 10 user decisions (D-01 through D-10) are implemented. All 7 requirement IDs (OVRLP-01 through OVRLP-04, OUTPT-01 through OUTPT-03) are addressed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify R/23_overlap_classification.R output on HiPerGator</name>
  <files>R/23_overlap_classification.R</files>
  <action>
    Human verification checkpoint. Run `source("R/23_overlap_classification.R")` on HiPerGator and verify:
    (1) Console shows classification breakdown for same-date and same-week pairs.
    (2) 4 CSV files are created in output/tables/.
    (3) Per-site recommendations appear with preferred source suggestions.
    (4) Spot-check: "Identical" rows have match_count equal to n_fields.
  </action>
  <verify>
    <automated>ls output/tables/classified_same_date_detail.csv output/tables/classified_same_week_detail.csv output/tables/per_site_overlap_profile.csv output/tables/overlap_source_payer_completeness.csv</automated>
  </verify>
  <done>
    Script runs without error on HiPerGator, 4 CSV files created with non-zero rows, console summary shows per-site overlap classification and actionable recommendations, user approves output.
  </done>
</task>

</tasks>

<verification>
After Task 1 (script creation):
- R/23_overlap_classification.R exists with >= 300 lines
- All 10 user decisions (D-01 through D-10) have corresponding implementation
- All 7 requirement IDs are addressed in script sections
- Script follows established standalone pattern (source config, conditional load, sections, console output, CSV writes)

After Task 2 (HiPerGator execution):
- 4 CSV files exist in output/tables/ with non-zero rows
- Console summary shows per-site recommendations
- Classification labels are coherent (Identical pairs have all match flags TRUE)
</verification>

<success_criteria>
- R/23_overlap_classification.R runs without error on HiPerGator
- 4 CSV output files created with correct columns and non-zero content
- Per-site overlap profiles show % Identical/Partial/Distinct for each site
- Recommendations use D-07 thresholds (70%/30%) with preferred source from D-08
- Same-date uses 5 fields, same-week uses 4 fields, both have basis column per D-10
- Console summary provides actionable per-site guidance
</success_criteria>

<output>
After completion, create `.planning/phases/26-overlap-classification-and-recommendations/26-01-SUMMARY.md`
</output>
