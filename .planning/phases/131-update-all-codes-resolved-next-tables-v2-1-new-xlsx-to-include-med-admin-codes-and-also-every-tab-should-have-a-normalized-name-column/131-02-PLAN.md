---
phase: 131-update-all-codes-resolved-next-tables-v2-1-new-xlsx-to-include-med-admin-codes-and-also-every-tab-should-have-a-normalized-name-column
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/105_normalize_supportive_care_meaning.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements: [131-C]

must_haves:
  truths:
    - "R/105 writes 'normalized_name' as the column header (not 'Normalized Meaning')"
    - "R/105 round-trip check asserts 'normalized_name' in names(sc)"
    - "R/88 section 15r Check 12 greps for normalized_name not Normalized Meaning"
    - "R/88 section 15r comments reference normalized_name"
    - "R/88 parses without error"
    - "R/105 parses without error"
  artifacts:
    - path: "R/105_normalize_supportive_care_meaning.R"
      provides: "Updated Supportive Care normalization script using normalized_name header"
      contains: "normalized_name"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Smoke test with updated section 15r assertions"
      contains: "normalized_name"
  key_links:
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/105_normalize_supportive_care_meaning.R"
      via: "grep check on r105_text for 'normalized_name'"
      pattern: "normalized_name.*r105_text|r105_text.*normalized_name"
---

<objective>
Update R/105 and R/88 to use the new column name `normalized_name` in place of the old `"Normalized Meaning"` string. These are targeted line-level edits — no logic changes, only the string constant changes.

Purpose: R/105 writes the column header when it runs on HiPerGator. R/88 section 15r verifies R/105's output. Both must agree on `normalized_name` or R/88 will fail after Phase 131.
Output: Three line changes in R/105, two assertion changes + comment updates in R/88 section 15r.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-update-all-codes-resolved-next-tables-v2-1-new-xlsx-to-include-med-admin-codes-and-also-every-tab-should-have-a-normalized-name-column/131-RESEARCH.md
@R/105_normalize_supportive_care_meaning.R
@R/88_smoke_test_comprehensive.R

<interfaces>
<!-- Exact current lines to change in R/105 (from direct read): -->

Line 363: wb$add_data(sheet = SHEET, x = "Normalized Meaning", dims = "G2")
  → wb$add_data(sheet = SHEET, x = "normalized_name", dims = "G2")

Line 385: message(glue("  saved '{SHEET}' with new 'Normalized Meaning' column to {XLSX_PATH}"))
  → message(glue("  saved '{SHEET}' with new 'normalized_name' column to {XLSX_PATH}"))

Line 404: if (!("Normalized Meaning" %in% names(sc)))
  → if (!("normalized_name" %in% names(sc)))

Line 407: nm <- sc[["Normalized Meaning"]]
  → nm <- sc[["normalized_name"]]

<!-- Exact current lines to change in R/88 section 15r (from direct read lines 2479-2482): -->

Lines 2479-2482:
  # Check 12: appends the Normalized Meaning column at col G (header G2, data G3)
  check("R/105 appends Normalized Meaning at col G (G2 header + G3 data)",
        grepl("Normalized Meaning", r105_text) &&
          grepl("G2", r105_text) && grepl("G3", r105_text))

  → Replace with:
  # Check 12: appends normalized_name at col G (header G2, data G3)
  check("R/105 appends normalized_name at col G (G2 header + G3 data)",
        grepl("normalized_name", r105_text) &&
          grepl("G2", r105_text) && grepl("G3", r105_text))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update R/105 — replace "Normalized Meaning" string constants with normalized_name</name>
  <read_first>
    - R/105_normalize_supportive_care_meaning.R (full file — confirm exact line numbers for lines 363, 385, 404, 407 before editing)
  </read_first>
  <files>R/105_normalize_supportive_care_meaning.R</files>
  <action>
Make exactly four targeted edits to R/105_normalize_supportive_care_meaning.R:

Edit 1 — line 363: Change the add_data header write from "Normalized Meaning" to "normalized_name":
  BEFORE: `wb$add_data(sheet = SHEET, x = "Normalized Meaning", dims = "G2")`
  AFTER:  `wb$add_data(sheet = SHEET, x = "normalized_name", dims = "G2")`

Edit 2 — line 385 (the save message): Change "Normalized Meaning" in the glue message:
  BEFORE: `message(glue("  saved '{SHEET}' with new 'Normalized Meaning' column to {XLSX_PATH}"))`
  AFTER:  `message(glue("  saved '{SHEET}' with new 'normalized_name' column to {XLSX_PATH}"))`

Edit 3 — line 404 (round-trip check): Change the column name assertion:
  BEFORE: `if (!("Normalized Meaning" %in% names(sc)))`
  AFTER:  `if (!("normalized_name" %in% names(sc)))`

Edit 4 — line 407 (column access): Change the column subscript:
  BEFORE: `nm <- sc[["Normalized Meaning"]]`
  AFTER:  `nm <- sc[["normalized_name"]]`

Do NOT change line 402 (`ncol(sc) != 7`) — the column count stays 7 after a rename, no change needed.
Do NOT change the stop() message text at line 404 if it only references the shape, not the name.
After edits, verify NO remaining occurrence of the string "Normalized Meaning" in the file (grep must return 0 matches).
  </action>
  <verify>
    <automated>grep -n "Normalized Meaning" R/105_normalize_supportive_care_meaning.R; Rscript --parse R/105_normalize_supportive_care_meaning.R</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Normalized Meaning" R/105_normalize_supportive_care_meaning.R` returns 0 matches
    - `grep '"normalized_name"' R/105_normalize_supportive_care_meaning.R` returns at least 4 matches (lines 363, 385, 404, 407)
    - `Rscript --parse R/105_normalize_supportive_care_meaning.R` exits 0
    - `grep 'ncol(sc) != 7' R/105_normalize_supportive_care_meaning.R` still returns 1 match (unchanged)
  </acceptance_criteria>
  <done>R/105 uses "normalized_name" everywhere the old "Normalized Meaning" string appeared, parses cleanly, ncol check unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update R/88 section 15r — Check 12 assertion and comment text</name>
  <read_first>
    - R/88_smoke_test_comprehensive.R lines 2414-2501 (full section 15r to locate exact line numbers for Check 12 label and grep pattern)
  </read_first>
  <files>R/88_smoke_test_comprehensive.R</files>
  <action>
Make targeted edits to R/88_smoke_test_comprehensive.R in section 15r (around lines 2479-2482):

Edit 1 — Check 12 comment (line 2479):
  BEFORE: `  # Check 12: appends the Normalized Meaning column at col G (header G2, data G3)`
  AFTER:  `  # Check 12: appends normalized_name at col G (header G2, data G3)`

Edit 2 — Check 12 label string (line 2480):
  BEFORE: `  check("R/105 appends Normalized Meaning at col G (G2 header + G3 data)",`
  AFTER:  `  check("R/105 appends normalized_name at col G (G2 header + G3 data)",`

Edit 3 — Check 12 grep pattern (line 2481):
  BEFORE: `        grepl("Normalized Meaning", r105_text) &&`
  AFTER:  `        grepl("normalized_name", r105_text) &&`

If there are any other occurrences of "Normalized Meaning" in section 15r (lines 2419 and 2423 reference section header comments according to RESEARCH.md), update those as well:
  - Any comment referencing "Normalized Meaning" in the 15r block → change to "normalized_name"
  - Any message() string in the 15r block referencing "Normalized Meaning" → change to "normalized_name"

After all edits: `grep -n "Normalized Meaning" R/88_smoke_test_comprehensive.R` must return 0 matches in the section 15r region (lines 2400-2510). It is acceptable if other sections mention it in historical comments — only section 15r is the target.
  </action>
  <verify>
    <automated>grep -n "Normalized Meaning" R/88_smoke_test_comprehensive.R; Rscript --parse R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Normalized Meaning" R/88_smoke_test_comprehensive.R` returns 0 matches in lines 2400-2510 (section 15r)
    - `grep -n '"normalized_name"' R/88_smoke_test_comprehensive.R` returns at least 1 match in section 15r
    - `grep 'grepl("normalized_name", r105_text)' R/88_smoke_test_comprehensive.R` returns 1 match
    - `Rscript --parse R/88_smoke_test_comprehensive.R` exits 0
  </acceptance_criteria>
  <done>R/88 section 15r Check 12 greps for "normalized_name" not "Normalized Meaning", file parses cleanly</done>
</task>

</tasks>

<verification>
grep "Normalized Meaning" R/105_normalize_supportive_care_meaning.R   # expect 0 matches
grep "Normalized Meaning" R/88_smoke_test_comprehensive.R | head -20   # expect 0 in 15r region
Rscript --parse R/105_normalize_supportive_care_meaning.R
Rscript --parse R/88_smoke_test_comprehensive.R
</verification>

<success_criteria>
- R/105 has zero occurrences of "Normalized Meaning" and parses cleanly
- R/88 section 15r Check 12 uses grepl("normalized_name", r105_text) and parses cleanly
- Both files are syntactically valid R
</success_criteria>

<output>
After completion, create `.planning/phases/131-update-all-codes-resolved-next-tables-v2-1-new-xlsx-to-include-med-admin-codes-and-also-every-tab-should-have-a-normalized-name-column/131-02-SUMMARY.md`
</output>
