---
phase: 94-make-proton-therapy-a-distinct-category-from-radiation
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - R/20_treatment_inventory.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - PROTON-05
  - PROTON-06

must_haves:
  truths:
    - "Treatment inventory detects and reports proton therapy codes separately from radiation"
    - "Unknown code detection has a Proton Therapy branch for heuristic CPT range scanning"
    - "Smoke test validates 6 core DRUG_GROUPINGS categories (was 5, now includes Proton Therapy)"
    - "Smoke test validates TREATMENT_TYPES has 5 elements"
    - "Smoke test validates proton codes map to Proton Therapy in DRUG_GROUPINGS"
    - "Smoke test validates proton codes are NOT in radiation_cpt"
  artifacts:
    - path: "R/20_treatment_inventory.R"
      provides: "extract_proton_codes() function for treatment inventory"
      contains: "extract_proton_codes"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15g proton therapy validation"
      contains: "PROTON THERAPY CATEGORY SPLIT"
  key_links:
    - from: "R/20_treatment_inventory.R"
      to: "R/00_config.R"
      via: "TREATMENT_CODES$proton_cpt used by extract_proton_codes()"
      pattern: "TREATMENT_CODES\\$proton_cpt"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/00_config.R"
      via: "Validates Proton Therapy exists in DRUG_GROUPINGS and TREATMENT_TYPES"
      pattern: "Proton Therapy"
---

<objective>
Add proton therapy support to the treatment inventory reporting script (R/20) and comprehensive smoke test (R/88), completing the downstream integration of the Proton Therapy category split.

Purpose: R/20 has explicit extract_*_codes() functions called in its main execution (not driven by TREATMENT_TYPES loop), so it needs a dedicated extract_proton_codes() function. R/88 hardcodes expected category counts and category names, so it needs updated validation for the 5th treatment type. Without these changes, R/20 would miss proton codes in its inventory output and R/88 would fail on category count mismatches.

Output: extract_proton_codes() function in R/20 with proton added to the main execution bind_rows call and detect_unknown_codes switch. Smoke test Section 15g validating the proton category split.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-make-proton-therapy-a-distinct-category-from-radiation/94-CONTEXT.md
@.planning/phases/94-make-proton-therapy-a-distinct-category-from-radiation/94-RESEARCH.md
@.planning/phases/94-make-proton-therapy-a-distinct-category-from-radiation/94-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on -->

From R/00_config.R (after Plan 01):
```r
TREATMENT_TYPES <- c("Chemotherapy", "Radiation", "Proton Therapy", "SCT", "Immunotherapy")

TREATMENT_CODES$proton_cpt = c("77520", "77522", "77523", "77525")

DRUG_GROUPINGS includes:
  "77520" = "Proton Therapy",
  "77522" = "Proton Therapy",
  "77523" = "Proton Therapy",
  "77525" = "Proton Therapy",
```

From R/20_treatment_inventory.R -- current explicit function calls (lines 1089-1094):
```r
all_codes <- bind_rows(
  extract_chemo_codes(),
  extract_radiation_codes(),
  extract_sct_codes(),
  extract_immunotherapy_codes()
)
```

From R/20_treatment_inventory.R -- detect_unknown_codes switch (lines 727-754):
```r
code_info <- switch(treatment_type,
    "Chemotherapy" = list(...),
    "Radiation" = list(
      codes = c(TREATMENT_CODES$radiation_cpt, TREATMENT_CODES$radiation_icd9, TREATMENT_CODES$radiation_revenue),
      px_type = "CH"
    ),
    "SCT" = list(...),
    "Immunotherapy" = list(...)
  )
```

From R/20_treatment_inventory.R -- CPT_HCPCS_RANGES (lines 63-78):
```r
CPT_HCPCS_RANGES <- list(
  Chemotherapy = list(j9_codes = "^J9[0-9]{3}$", j0_j8_drugs = "^J[0-8][0-9]{3}$"),
  Radiation = list(delivery = "^774[0-9]{2}$", planning = "^773[0-9]{2}$"),
  SCT = list(transplant = "^382[3-4][0-9]$"),
  Immunotherapy = list(car_t_admin = "^XW0[34]3[A-Z][0-9]$")
)
```

From R/20_treatment_inventory.R -- extract_radiation_codes() pattern (lines 349-486):
```r
extract_radiation_codes <- function() {
  message("  Extracting radiation codes...")
  results <- list()
  # Queries PROCEDURES (CPT, ICD-9, ICD-10-PCS, Revenue), DIAGNOSIS, ENCOUNTER (DRG), TUMOR_REGISTRY
  # Returns tibble with: code, code_type, source_table, n, drug_name, treatment_type
  bind_rows(results) %>% mutate(treatment_type = "Radiation")
}
```

From R/88_smoke_test_comprehensive.R (lines 830-836):
```r
core_categories <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy", "Supportive Care")
found_categories <- intersect(core_categories, unique(DRUG_GROUPINGS))
check(
  glue("DRUG_GROUPINGS covers 5 core categories ({length(found_categories)}/5 found)"),
  length(found_categories) == 5
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extract_proton_codes() to treatment inventory and update main execution</name>
  <files>R/20_treatment_inventory.R</files>
  <read_first>
    - R/20_treatment_inventory.R (lines 340-486 for extract_radiation_codes() pattern, lines 63-78 for CPT_HCPCS_RANGES, lines 725-754 for detect_unknown_codes switch, lines 1082-1100 for main execution)
    - R/00_config.R (to verify TREATMENT_CODES$proton_cpt exists)
  </read_first>
  <action>
**Per D-08, D-10:**

**A. CPT_HCPCS_RANGES (around line 63-78):** Add a Proton Therapy entry after the Radiation entry. Proton CPT codes are 77520-77525:
```r
  `Proton Therapy` = list(
    proton_delivery = "^7752[0-9]$"  # 77520-77529 proton beam delivery
  ),
```
Place this after the `Radiation = list(...)` entry and before the `SCT = list(...)` entry.

**B. New extract_proton_codes() function:** Add after extract_radiation_codes() (after approximately line 486). Copy the pattern from extract_radiation_codes() but simpler -- proton only has one source (PROCEDURES CPT). No ICD-9, ICD-10-PCS, revenue, diagnosis, DRG, or tumor registry sources.

```r
# ------------------------------------------------------------------------------
# extract_proton_codes()
# ------------------------------------------------------------------------------
#' Extract proton therapy code frequencies from PROCEDURES
#'
#' Queries: PROCEDURES (CPT only -- proton has no ICD-9, ICD-10-PCS, DX, DRG, or Revenue codes)
#'
#' @return Tibble with columns: code, code_type, source_table, n, drug_name, treatment_type
extract_proton_codes <- function() {
  message("  Extracting proton therapy codes...")
  results <- list()

  # --- PROCEDURES ---
  proc_tbl <- safe_table("PROCEDURES")
  if (!is.null(proc_tbl)) {
    px_cpt <- tryCatch(
      {
        proc_tbl %>%
          filter(PX_TYPE == "CH" & PX %in% TREATMENT_CODES$proton_cpt) %>%
          group_by(PX) %>%
          summarise(n = n(), .groups = "drop") %>%
          collect() %>%
          transmute(
            code = PX,
            code_type = "CPT",
            source_table = "PROCEDURES",
            n = n,
            drug_name = sapply(PX, function(x) {
              if (x %in% names(CODE_DESCRIPTIONS)) CODE_DESCRIPTIONS[[x]]
              else NA_character_
            })
          )
      },
      error = function(e) empty_result()
    )
    results <- c(results, list(px_cpt))
  }

  bind_rows(results) %>%
    mutate(treatment_type = "Proton Therapy")
}
```

**C. detect_unknown_codes switch (around line 727):** Add a `"Proton Therapy"` branch to the `switch(treatment_type, ...)` after the Radiation branch:
```r
    "Proton Therapy" = list(
      codes = TREATMENT_CODES$proton_cpt,
      px_type = "CH"
    ),
```

**D. Main execution bind_rows (lines 1089-1094):** Add `extract_proton_codes()` to the bind_rows call, after `extract_radiation_codes()`:
```r
all_codes <- bind_rows(
  extract_chemo_codes(),
  extract_radiation_codes(),
  extract_proton_codes(),
  extract_sct_codes(),
  extract_immunotherapy_codes()
)
```
  </action>
  <verify>
    <automated>grep -n "extract_proton_codes\|Proton Therapy\|proton_cpt\|proton_delivery" R/20_treatment_inventory.R</automated>
  </verify>
  <acceptance_criteria>
    - R/20_treatment_inventory.R contains `extract_proton_codes <- function()`
    - R/20_treatment_inventory.R contains `TREATMENT_CODES$proton_cpt` inside extract_proton_codes
    - R/20_treatment_inventory.R contains `mutate(treatment_type = "Proton Therapy")` in extract_proton_codes
    - R/20_treatment_inventory.R main execution calls `extract_proton_codes()` in bind_rows
    - R/20_treatment_inventory.R switch contains `"Proton Therapy" = list(`
    - R/20_treatment_inventory.R CPT_HCPCS_RANGES contains `` `Proton Therapy` = list( ``
    - R/20_treatment_inventory.R contains `proton_delivery = "^7752[0-9]$"` in CPT_HCPCS_RANGES
  </acceptance_criteria>
  <done>extract_proton_codes() defined in R/20, called in main execution bind_rows alongside other 4 extract functions. detect_unknown_codes switch handles "Proton Therapy". CPT_HCPCS_RANGES has proton range heuristic for unknown code scanning.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke test Section 15g for proton therapy category split validation</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (lines 820-850 for existing core_categories check, lines 1306-1420 for Section 15c/15d pattern)
    - R/00_config.R (to verify TREATMENT_TYPES has 5 elements, DRUG_GROUPINGS has proton codes, proton_cpt exists)
    - R/10_cohort_predicates.R (to verify has_proton exists)
  </read_first>
  <action>
**Per D-08:**

**A. Update existing core_categories check (lines 830-836):** Change the core_categories vector from 5 to 6 categories and update the expected count:
```r
core_categories <- c("Chemotherapy", "Radiation", "Proton Therapy", "SCT", "Immunotherapy", "Supportive Care")
found_categories <- intersect(core_categories, unique(DRUG_GROUPINGS))
check(
  glue("DRUG_GROUPINGS covers 6 core categories ({length(found_categories)}/6 found)"),
  length(found_categories) == 6
)
```

**B. Add Section 15g after the last existing Section 15 block** (after Section 15f, which ends around line 1500-ish -- find the last Section 15 block and add after it). Create a new section:

```r
# ==============================================================================
# SECTION 15g: PROTON THERAPY CATEGORY SPLIT VALIDATION (PROTON-05, PROTON-06) ----
# ==============================================================================

message("\n[PROTON] Proton therapy category split validation (Phase 94)...")

# Check 1: TREATMENT_TYPES has 5 elements
check(
  "TREATMENT_TYPES has 5 elements (was 4 before Phase 94)",
  length(TREATMENT_TYPES) == 5
)

# Check 2: "Proton Therapy" is in TREATMENT_TYPES
check(
  "TREATMENT_TYPES contains 'Proton Therapy'",
  "Proton Therapy" %in% TREATMENT_TYPES
)

# Check 3: 4 proton codes map to "Proton Therapy" in DRUG_GROUPINGS
proton_codes <- c("77520", "77522", "77523", "77525")
proton_mappings <- DRUG_GROUPINGS[proton_codes]
check(
  "All 4 proton codes (77520, 77522, 77523, 77525) map to 'Proton Therapy' in DRUG_GROUPINGS",
  all(!is.na(proton_mappings)) && all(proton_mappings == "Proton Therapy")
)

# Check 4: Proton codes are NOT in radiation_cpt (no double-counting)
check(
  "Proton codes (77520, 77522, 77523, 77525) are NOT in TREATMENT_CODES$radiation_cpt",
  !any(proton_codes %in% TREATMENT_CODES$radiation_cpt)
)

# Check 5: proton_cpt list exists in TREATMENT_CODES
check(
  "TREATMENT_CODES$proton_cpt exists with 4 codes",
  !is.null(TREATMENT_CODES$proton_cpt) && length(TREATMENT_CODES$proton_cpt) == 4
)

# Check 6: Proton Therapy has color in TREATMENT_TYPE_COLORS
check(
  "TREATMENT_TYPE_COLORS has 'Proton Therapy' entry",
  "Proton Therapy" %in% names(TREATMENT_TYPE_COLORS)
)

# Check 7: has_proton() function exists in R/10
r10_lines <- readLines("R/10_cohort_predicates.R", warn = FALSE)
check(
  "R/10 defines has_proton() function",
  any(grepl("has_proton <- function", r10_lines, fixed = TRUE))
)

# Check 8: R/14 calls has_proton() and joins HAD_PROTON
r14_lines <- readLines("R/14_build_cohort.R", warn = FALSE)
check(
  "R/14 calls has_proton()",
  any(grepl("has_proton()", r14_lines, fixed = TRUE))
)

check(
  "R/14 joins HAD_PROTON to cohort",
  any(grepl("HAD_PROTON", r14_lines))
)

# Check 9: R/26 has extract_proton_dates_with_codes() function
r26_lines <- readLines("R/26_treatment_episodes.R", warn = FALSE)
check(
  "R/26 defines extract_proton_dates_with_codes()",
  any(grepl("extract_proton_dates_with_codes <- function", r26_lines, fixed = TRUE))
)

# Check 10: R/25 has extract_proton_dates() function
r25_lines <- readLines("R/25_treatment_durations.R", warn = FALSE)
check(
  "R/25 defines extract_proton_dates()",
  any(grepl("extract_proton_dates <- function", r25_lines, fixed = TRUE))
)

# Check 11: R/20 has extract_proton_codes() function
r20_lines <- readLines("R/20_treatment_inventory.R", warn = FALSE)
check(
  "R/20 defines extract_proton_codes()",
  any(grepl("extract_proton_codes <- function", r20_lines, fixed = TRUE))
)

# Check 12: Radiation section updated to 11 codes
config_lines_dg <- config_lines[drug_groupings_start:drug_groupings_end]
check(
  "DRUG_GROUPINGS Radiation section updated to 11 codes (was 15)",
  any(grepl("# Radiation \\(11 codes\\)", config_lines_dg))
)
```

Note: This section reuses the `config_lines`, `drug_groupings_start`, and `drug_groupings_end` variables already defined in Section 15c (line 1312-1316). If Section 15g is placed after 15f, these variables are already in scope.
  </action>
  <verify>
    <automated>grep -n "PROTON THERAPY CATEGORY SPLIT\|Proton Therapy\|proton_codes\|PROTON-05\|PROTON-06" R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains `SECTION 15g: PROTON THERAPY CATEGORY SPLIT VALIDATION`
    - R/88_smoke_test_comprehensive.R core_categories vector includes `"Proton Therapy"` (6 elements)
    - R/88_smoke_test_comprehensive.R check for DRUG_GROUPINGS says `6 core categories` (was 5)
    - R/88_smoke_test_comprehensive.R validates `length(TREATMENT_TYPES) == 5`
    - R/88_smoke_test_comprehensive.R validates proton codes are NOT in radiation_cpt
    - R/88_smoke_test_comprehensive.R validates TREATMENT_CODES$proton_cpt has 4 codes
    - R/88_smoke_test_comprehensive.R validates has_proton() exists in R/10
    - R/88_smoke_test_comprehensive.R validates extract_proton_dates_with_codes() exists in R/26
    - R/88_smoke_test_comprehensive.R validates extract_proton_codes() exists in R/20
    - R/88_smoke_test_comprehensive.R validates Radiation section comment says "11 codes"
  </acceptance_criteria>
  <done>Smoke test Section 15g validates the complete proton therapy category split: config vectors, code mappings, predicate functions, extraction functions, and no double-counting. Core categories check updated from 5 to 6. TREATMENT_TYPES check validates 5 elements.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "Proton Therapy" R/20_treatment_inventory.R` returns >= 3 (extract function, switch case, CPT range)
2. `grep -c "Proton Therapy" R/88_smoke_test_comprehensive.R` returns >= 8 (core_categories + Section 15g checks)
3. `grep "extract_proton_codes" R/20_treatment_inventory.R` shows function definition and call in bind_rows
4. `grep "SECTION 15g" R/88_smoke_test_comprehensive.R` shows the new validation section
5. `grep "6 core categories" R/88_smoke_test_comprehensive.R` confirms updated count
</verification>

<success_criteria>
- R/20 has extract_proton_codes() defined and called in main execution
- R/20 detect_unknown_codes switch handles "Proton Therapy" type
- R/20 CPT_HCPCS_RANGES has proton delivery range pattern
- R/88 core_categories includes "Proton Therapy" (6 total)
- R/88 Section 15g validates all aspects of proton category split
- R/88 validates no double-counting (proton codes not in radiation_cpt)
- R/88 validates all new functions exist (has_proton, extract_proton_dates, extract_proton_dates_with_codes, extract_proton_codes)
</success_criteria>

<output>
After completion, create `.planning/phases/94-make-proton-therapy-a-distinct-category-from-radiation/94-02-SUMMARY.md`
</output>
