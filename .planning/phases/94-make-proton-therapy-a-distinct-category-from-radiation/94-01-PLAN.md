---
phase: 94-make-proton-therapy-a-distinct-category-from-radiation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/00_config.R
  - R/10_cohort_predicates.R
  - R/14_build_cohort.R
  - R/25_treatment_durations.R
  - R/26_treatment_episodes.R
autonomous: true
requirements:
  - PROTON-01
  - PROTON-02
  - PROTON-03
  - PROTON-04

must_haves:
  truths:
    - "Proton Therapy is a distinct treatment category from Radiation in all config vectors"
    - "4 proton CPT codes (77520, 77522, 77523, 77525) map to 'Proton Therapy' in DRUG_GROUPINGS"
    - "Proton codes are NOT in TREATMENT_CODES$radiation_cpt (no double-counting)"
    - "has_proton() detects patients with proton therapy CPT codes in PROCEDURES"
    - "R/14 cohort has HAD_PROTON column alongside HAD_CHEMO, HAD_RADIATION, HAD_SCT"
    - "Episode detection dispatches Proton Therapy to its own extraction function"
    - "Duration analysis dispatches Proton Therapy to its own extraction function"
  artifacts:
    - path: "R/00_config.R"
      provides: "TREATMENT_TYPES with 5 elements, DRUG_GROUPINGS with Proton Therapy codes, TREATMENT_CODES$proton_cpt, TREATMENT_TYPE_COLORS with Proton Therapy"
      contains: "Proton Therapy"
    - path: "R/10_cohort_predicates.R"
      provides: "has_proton() predicate function"
      contains: "has_proton <- function"
    - path: "R/14_build_cohort.R"
      provides: "HAD_PROTON cohort flag"
      contains: "HAD_PROTON"
    - path: "R/25_treatment_durations.R"
      provides: "extract_proton_dates() and dispatch branch"
      contains: "extract_proton_dates"
    - path: "R/26_treatment_episodes.R"
      provides: "extract_proton_dates_with_codes() and dispatch branch"
      contains: "extract_proton_dates_with_codes"
  key_links:
    - from: "R/00_config.R"
      to: "R/10_cohort_predicates.R"
      via: "TREATMENT_CODES$proton_cpt used by has_proton()"
      pattern: "TREATMENT_CODES\\$proton_cpt"
    - from: "R/00_config.R"
      to: "R/26_treatment_episodes.R"
      via: "DRUG_GROUPINGS maps proton codes to 'Proton Therapy' category"
      pattern: '"Proton Therapy"'
    - from: "R/10_cohort_predicates.R"
      to: "R/14_build_cohort.R"
      via: "has_proton() called and joined to cohort"
      pattern: "has_proton\\(\\)"
---

<objective>
Split proton beam therapy (CPT 77520, 77522, 77523, 77525) from the general "Radiation" treatment category into a distinct "Proton Therapy" category in the core pipeline infrastructure: config definitions, cohort predicates, duration analysis, and episode detection.

Purpose: Enables proton-specific treatment tracking, separate Gantt visualization, and distinct sub-category summaries. The pipeline currently lumps proton beam codes with conventional radiation, hiding proton therapy in aggregate radiation counts.

Output: Updated R/00_config.R with 5-element TREATMENT_TYPES and split code lists; new has_proton() predicate in R/10; HAD_PROTON flag in R/14 cohort; proton dispatch branches in R/25 and R/26 with dedicated extraction functions.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-make-proton-therapy-a-distinct-category-from-radiation/94-CONTEXT.md
@.planning/phases/94-make-proton-therapy-a-distinct-category-from-radiation/94-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/00_config.R (lines 3348-3374) -- current TREATMENT_TYPES and TREATMENT_TYPE_COLORS:
```r
TREATMENT_TYPES <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy")

TREATMENT_TYPE_COLORS <- list(
  Chemotherapy      = list(fill = "FFDCEEFB", font = "FF0B5394"),
  Radiation         = list(fill = "FFDDF4E1", font = "FF274E13"),
  SCT               = list(fill = "FFFFF4D6", font = "FF7F6000"),
  Immunotherapy     = list(fill = "FFE8DCF4", font = "FF4C1D7A"),
  `HL Diagnosis`    = list(fill = "FFFFF0D6", font = "FF8B6914"),
  Death             = list(fill = "FFFDE8E8", font = "FF991B1B"),
  `Supportive Care` = list(fill = "FFD5F5F0", font = "FF0E6655"),
  Unrelated         = list(fill = "FFF3F4F6", font = "FF6B7280")
)

GANTT_TREATMENT_TYPES <- c(TREATMENT_TYPES, "HL Diagnosis")
```

From R/00_config.R (lines 1570-1585) -- current Radiation section of DRUG_GROUPINGS:
```r
  # Radiation (15 codes)
  "77417" = "Radiation",
  "77470" = "Radiation",
  "77421" = "Radiation",
  "77413" = "Radiation",
  "77418" = "Radiation",
  "77414" = "Radiation",
  "77416" = "Radiation",
  "77435" = "Radiation",
  "77431" = "Radiation",
  "77432" = "Radiation",
  "77408" = "Radiation",
  "77404" = "Radiation",
  "77387" = "Radiation",
  "77385" = "Radiation",
  "77412" = "Radiation",
```

From R/00_config.R (lines 2583-2587) -- proton codes inside radiation_cpt:
```r
    # --- Proton Beam Treatment Delivery (77520-77525) ---
    "77520", # Proton treatment delivery; simple, without compensation
    "77522", # Proton treatment delivery; simple, with compensation
    "77523", # Proton treatment delivery; intermediate
    "77525", # Proton treatment delivery; complex
```

From R/10_cohort_predicates.R -- has_radiation() pattern (lines 388-488):
```r
has_radiation <- function() {
  rad_ids <- character(0)
  n_tr <- 0L; n_px <- 0L; n_dx <- 0L; n_drg <- 0L; n_rev <- 0L
  # Sources: TUMOR_REGISTRY_ALL, PROCEDURES (CPT, ICD-9, ICD-10-PCS, Revenue), DIAGNOSIS, ENCOUNTER DRG, Revenue
  result <- tibble(ID = unique(rad_ids), HAD_RADIATION = 1L)
  message(glue("[Treatment] has_radiation: {nrow(result)} patients total"))
  result
}
```

From R/14_build_cohort.R (lines 356-369):
```r
chemo_flags <- has_chemo()
rad_flags <- has_radiation()
sct_flags <- has_sct()
cohort <- cohort %>%
  left_join(chemo_flags, by = "ID") %>%
  left_join(rad_flags, by = "ID") %>%
  left_join(sct_flags, by = "ID") %>%
  mutate(
    HAD_CHEMO = coalesce(HAD_CHEMO, 0L),
    HAD_RADIATION = coalesce(HAD_RADIATION, 0L),
    HAD_SCT = coalesce(HAD_SCT, 0L)
  )
```

From R/25_treatment_durations.R -- extract_all_dates() dispatch (lines 95-109):
```r
extract_all_dates <- function(type) {
  if (type == "Chemotherapy") return(extract_chemo_dates())
  else if (type == "Radiation") return(extract_radiation_dates())
  else if (type == "SCT") return(extract_sct_dates())
  else if (type == "Immunotherapy") return(extract_immunotherapy_dates())
  else stop(glue("Unknown treatment type: {type}"))
}
```

From R/26_treatment_episodes.R -- extract_dates_with_codes() dispatch (lines 393-407):
```r
extract_dates_with_codes <- function(type) {
  if (type == "Chemotherapy") return(extract_chemo_dates_with_codes())
  else if (type == "Radiation") return(extract_radiation_dates_with_codes())
  else if (type == "SCT") return(extract_sct_dates_with_codes())
  else if (type == "Immunotherapy") return(extract_immunotherapy_dates_with_codes())
  else stop(glue("Unknown treatment type: {type}"))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split proton codes from Radiation config and register Proton Therapy category</name>
  <files>R/00_config.R</files>
  <read_first>
    - R/00_config.R (lines 1570-1600 for DRUG_GROUPINGS Radiation section, lines 2510-2605 for TREATMENT_CODES$radiation_cpt, lines 3348-3374 for TREATMENT_TYPES/TREATMENT_TYPE_COLORS/GANTT_TREATMENT_TYPES)
  </read_first>
  <action>
**Per D-01, D-02, D-03, D-06, D-07, D-09:**

1. **DRUG_GROUPINGS** (around line 1570): Change the Radiation section comment from `# Radiation (15 codes)` to `# Radiation (11 codes)`. After the last Radiation entry (`"77412" = "Radiation"`), add a new section:
```r
  # Proton Therapy (4 codes) -- Phase 94: split from Radiation (D-07)
  "77520" = "Proton Therapy",
  "77522" = "Proton Therapy",
  "77523" = "Proton Therapy",
  "77525" = "Proton Therapy",
```

2. **TREATMENT_CODES$radiation_cpt** (around line 2583-2587): Remove the 4 proton codes ("77520", "77522", "77523", "77525") and their comments from `radiation_cpt`. Replace the `# --- Proton Beam Treatment Delivery (77520-77525) ---` section with a comment: `# Proton codes moved to proton_cpt (Phase 94, D-03)`. After the closing of `radiation_cpt`, add a new list entry:
```r
  # Proton therapy CPT codes (split from radiation_cpt, Phase 94, D-03)
  proton_cpt = c(
    "77520",  # Proton treatment delivery; simple, without compensation
    "77522",  # Proton treatment delivery; simple, with compensation
    "77523",  # Proton treatment delivery; intermediate
    "77525"   # Proton treatment delivery; complex
  ),
```
Place this new `proton_cpt` entry immediately after `radiation_cpt`'s closing parenthesis/comma (before `sct_cpt`).

3. **TREATMENT_TYPES** (line 3353): Change from:
```r
TREATMENT_TYPES <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy")
```
to:
```r
TREATMENT_TYPES <- c("Chemotherapy", "Radiation", "Proton Therapy", "SCT", "Immunotherapy")
```

4. **TREATMENT_TYPE_COLORS** (lines 3357-3366): Add `Proton Therapy` entry after Radiation:
```r
  `Proton Therapy`  = list(fill = "FFFDE7CC", font = "FF8B4513"), # light orange / saddle brown
```

5. **No changes needed** to GANTT_TREATMENT_TYPES (line 3370) because it derives from TREATMENT_TYPES: `GANTT_TREATMENT_TYPES <- c(TREATMENT_TYPES, "HL Diagnosis")` -- auto-picks up Proton Therapy.
  </action>
  <verify>
    <automated>grep -c "Proton Therapy" R/00_config.R</automated>
  </verify>
  <acceptance_criteria>
    - R/00_config.R contains `"77520" = "Proton Therapy"` in DRUG_GROUPINGS
    - R/00_config.R contains `"77522" = "Proton Therapy"` in DRUG_GROUPINGS
    - R/00_config.R contains `"77523" = "Proton Therapy"` in DRUG_GROUPINGS
    - R/00_config.R contains `"77525" = "Proton Therapy"` in DRUG_GROUPINGS
    - R/00_config.R contains `# Radiation (11 codes)` (updated count)
    - R/00_config.R contains `proton_cpt = c(` as a new TREATMENT_CODES entry
    - R/00_config.R contains `"Proton Therapy"` in TREATMENT_TYPES (5 elements total)
    - R/00_config.R contains `` `Proton Therapy`  = list(fill = "FFFDE7CC", font = "FF8B4513") `` in TREATMENT_TYPE_COLORS
    - R/00_config.R does NOT contain "77520" inside radiation_cpt section
    - R/00_config.R does NOT contain "77522" inside radiation_cpt section
    - R/00_config.R does NOT contain "77523" inside radiation_cpt section
    - R/00_config.R does NOT contain "77525" inside radiation_cpt section
    - grep for `proton_cpt` in R/00_config.R returns matches
  </acceptance_criteria>
  <done>DRUG_GROUPINGS maps 4 proton codes to "Proton Therapy". TREATMENT_CODES has proton_cpt list separate from radiation_cpt (now 11 codes). TREATMENT_TYPES has 5 elements. TREATMENT_TYPE_COLORS has orange/brown entry for Proton Therapy. GANTT_TREATMENT_TYPES auto-includes Proton Therapy via derivation.</done>
</task>

<task type="auto">
  <name>Task 2: Add has_proton() predicate, cohort integration, and episode/duration dispatch</name>
  <files>R/10_cohort_predicates.R, R/14_build_cohort.R, R/25_treatment_durations.R, R/26_treatment_episodes.R</files>
  <read_first>
    - R/10_cohort_predicates.R (lines 383-488 for has_radiation() pattern to copy)
    - R/14_build_cohort.R (lines 350-375 for treatment flag section)
    - R/25_treatment_durations.R (lines 95-109 for extract_all_dates() dispatch, lines 229-305 for extract_radiation_dates() pattern)
    - R/26_treatment_episodes.R (lines 219-267 for extract_radiation_dates_with_codes() pattern, lines 390-407 for extract_dates_with_codes() dispatch)
    - R/00_config.R (to verify TREATMENT_CODES$proton_cpt exists from Task 1)
  </read_first>
  <action>
**Per D-04, D-05, D-10:**

**A. R/10_cohort_predicates.R -- Add has_proton() after has_radiation() (after line 488):**

Add the following function. It is simpler than has_radiation() because proton therapy only has one detection source (PROCEDURES CPT). No tumor registry dates, no diagnosis codes, no DRG, no revenue codes.

```r
#' Identify patients with proton therapy evidence
#'
#' Combines evidence from:
#'   - PROCEDURES: PX_TYPE == "CH" and PX in TREATMENT_CODES$proton_cpt
#'
#' Note: Proton therapy does NOT use TUMOR_REGISTRY (no proton-specific date columns),
#' DIAGNOSIS (no proton-specific ICD codes), DRG, or Revenue codes.
#' Only detection source is CPT codes in PROCEDURES.
#'
#' @return Tibble with columns: ID, HAD_PROTON (integer 1 for all rows)
#'
has_proton <- function() {
  proton_ids <- character(0)

  # Initialize source counter for aggregate logging
  n_px <- 0L

  # PROCEDURES: CPT codes only
  proc_tbl <- tryCatch(get_pcornet_table("PROCEDURES"), error = function(e) NULL)
  if (!is.null(proc_tbl)) {
    proton_px <- proc_tbl %>%
      filter(PX_TYPE == "CH" & PX %in% TREATMENT_CODES$proton_cpt) %>%
      distinct(ID) %>%
      pull(ID)
    proton_ids <- c(proton_ids, proton_px)
    n_px <- length(proton_px)
  }

  result <- tibble(ID = unique(proton_ids), HAD_PROTON = 1L)
  message(glue("[Treatment] has_proton: {nrow(result)} patients total"))
  message(glue("  Sources: PX={n_px}"))
  result
}
```

Also update the module-level documentation comment (lines 8-9) from `has_chemo, has_radiation, has_sct` to `has_chemo, has_radiation, has_proton, has_sct`.

**B. R/14_build_cohort.R -- Add proton flag to cohort (around line 358):**

After `sct_flags <- has_sct()` (line 358), add:
```r
proton_flags <- has_proton()
```

In the `left_join` chain (lines 361-364), add after the `left_join(sct_flags, by = "ID")`:
```r
  left_join(proton_flags, by = "ID") %>%
```

In the `mutate(...)` block (lines 365-369), add:
```r
    HAD_PROTON = coalesce(HAD_PROTON, 0L),
```

After the existing message lines (371-373), add:
```r
message(glue("  HAD_PROTON = 1: {sum(cohort$HAD_PROTON == 1)} patients ({round(100 * mean(cohort$HAD_PROTON), 1)}%)"))
```

**C. R/25_treatment_durations.R -- Add extract_proton_dates() and dispatch branch:**

1. In `extract_all_dates()` (lines 95-109), add a new branch after the "Radiation" branch (after line 101):
```r
  } else if (type == "Proton Therapy") {
    return(extract_proton_dates())
```

2. After `extract_radiation_dates()` function (ends around line 305), add:
```r
#' Extract all proton therapy dates from 1 source
#' Simpler than radiation -- only PROCEDURES CPT codes (no DX, DRG, TR, Revenue)
extract_proton_dates <- function() {
  # 1. PROCEDURES: CPT codes only
  px_dates <- NULL
  if (!is.null(get_pcornet_table("PROCEDURES"))) {
    px_dates <- get_pcornet_table("PROCEDURES") %>%
      filter(PX_TYPE == "CH" & PX %in% TREATMENT_CODES$proton_cpt) %>%
      filter(!is.na(PX_DATE)) %>%
      select(ID, treatment_date = PX_DATE, ENCOUNTERID) %>%
      collect()
  }

  stack_and_dedup(
    sources = list(PX = px_dates),
    type_name = "Proton Therapy"
  )
}
```

Also update the function docstring for `extract_all_dates()` `@param type` (line 93) to include "Proton Therapy":
```r
#' @param type Character. One of "Chemotherapy", "Radiation", "Proton Therapy", "SCT", "Immunotherapy"
```

**D. R/26_treatment_episodes.R -- Add extract_proton_dates_with_codes() and dispatch branch:**

1. In `extract_dates_with_codes()` (lines 393-407), add a new branch after the "Radiation" branch (after line 399):
```r
  } else if (type == "Proton Therapy") {
    return(extract_proton_dates_with_codes())
```

2. After `extract_radiation_dates_with_codes()` function (ends around line 268), add:
```r
#' Extract proton therapy dates with triggering codes from 1 source (PX)
#' Simpler than radiation -- only PROCEDURES CPT codes (no DX, DRG, TR, Revenue)
#' @return Tibble with columns: ID, treatment_date, triggering_code, ENCOUNTERID
extract_proton_dates_with_codes <- function() {
  # PROCEDURES: CPT codes only
  px_dates <- NULL
  if (!is.null(get_pcornet_table("PROCEDURES"))) {
    px_dates <- get_pcornet_table("PROCEDURES") %>%
      filter(PX_TYPE == "CH" & PX %in% TREATMENT_CODES$proton_cpt) %>%
      filter(!is.na(PX_DATE)) %>%
      select(ID, treatment_date = PX_DATE, triggering_code = PX, ENCOUNTERID) %>%
      collect()
  }

  stack_and_dedup_with_codes(
    sources = list(PX = px_dates),
    type_name = "Proton Therapy"
  )
}
```

Also update the `@param type` docstring for `extract_dates_with_codes()` (line 391) to include "Proton Therapy":
```r
#' @param type Character. One of "Chemotherapy", "Radiation", "Proton Therapy", "SCT", "Immunotherapy"
```

**Gap threshold note:** Proton therapy uses the default 90-day GAP_THRESHOLD (same as radiation). No custom threshold needed in R/25's get_gap_threshold() if that function exists -- proton falls through to default. Add an inline comment at the proton branch in R/25: `# Uses default 90-day gap threshold (same as radiation); validate with clinical SME`
  </action>
  <verify>
    <automated>grep -n "has_proton\|extract_proton_dates\|HAD_PROTON\|Proton Therapy" R/10_cohort_predicates.R R/14_build_cohort.R R/25_treatment_durations.R R/26_treatment_episodes.R</automated>
  </verify>
  <acceptance_criteria>
    - R/10_cohort_predicates.R contains `has_proton <- function()`
    - R/10_cohort_predicates.R contains `TREATMENT_CODES$proton_cpt`
    - R/10_cohort_predicates.R contains `HAD_PROTON = 1L`
    - R/14_build_cohort.R contains `proton_flags <- has_proton()`
    - R/14_build_cohort.R contains `left_join(proton_flags, by = "ID")`
    - R/14_build_cohort.R contains `HAD_PROTON = coalesce(HAD_PROTON, 0L)`
    - R/25_treatment_durations.R contains `extract_proton_dates <- function()`
    - R/25_treatment_durations.R contains `"Proton Therapy"` in extract_all_dates dispatch
    - R/25_treatment_durations.R contains `TREATMENT_CODES$proton_cpt` in extract_proton_dates
    - R/26_treatment_episodes.R contains `extract_proton_dates_with_codes <- function()`
    - R/26_treatment_episodes.R contains `"Proton Therapy"` in extract_dates_with_codes dispatch
    - R/26_treatment_episodes.R contains `TREATMENT_CODES$proton_cpt` in extract_proton_dates_with_codes
    - R/26_treatment_episodes.R contains `stack_and_dedup_with_codes` call in extract_proton_dates_with_codes
  </acceptance_criteria>
  <done>has_proton() defined in R/10, called in R/14 to produce HAD_PROTON cohort flag. R/25 extract_all_dates() and R/26 extract_dates_with_codes() both dispatch "Proton Therapy" to dedicated extraction functions that query PROCEDURES for proton_cpt codes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "Proton Therapy" R/00_config.R` returns >= 8 (DRUG_GROUPINGS 4 entries + TREATMENT_TYPES + TREATMENT_TYPE_COLORS + code comments)
2. `grep "proton_cpt" R/00_config.R` shows the new list entry
3. `grep "77520\|77522\|77523\|77525" R/00_config.R` confirms codes appear in proton_cpt section and DRUG_GROUPINGS, but NOT in radiation_cpt
4. `grep "has_proton" R/10_cohort_predicates.R R/14_build_cohort.R` confirms predicate definition and usage
5. `grep "Proton Therapy" R/25_treatment_durations.R R/26_treatment_episodes.R` confirms dispatch branches exist
</verification>

<success_criteria>
- TREATMENT_TYPES has exactly 5 elements: Chemotherapy, Radiation, Proton Therapy, SCT, Immunotherapy
- DRUG_GROUPINGS maps 77520/77522/77523/77525 to "Proton Therapy" (not "Radiation")
- TREATMENT_CODES$proton_cpt exists with 4 codes; radiation_cpt no longer has those 4 codes
- TREATMENT_TYPE_COLORS has `Proton Therapy` with orange/brown color (FFFDE7CC/FF8B4513)
- has_proton() function exists in R/10 and is called in R/14
- HAD_PROTON appears in cohort output
- R/25 and R/26 both have proton-specific extraction functions and dispatch branches
- No double-counting: proton codes exist in exactly one treatment category
</success_criteria>

<output>
After completion, create `.planning/phases/94-make-proton-therapy-a-distinct-category-from-radiation/94-01-SUMMARY.md`
</output>
