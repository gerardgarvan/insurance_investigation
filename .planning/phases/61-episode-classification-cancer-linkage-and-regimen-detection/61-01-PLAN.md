---
phase: 61-episode-classification-cancer-linkage-and-regimen-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/61_episode_classification.R
  - cache/outputs/treatment_episodes.rds
  - output/episode_classification_audit.xlsx
  - output/episode_classification_audit.csv
autonomous: true
requirements: [LINK-01, LINK-02, LINK-03, LINK-04, REG-01, REG-02, REG-03, REG-04]

must_haves:
  truths:
    - "Each treatment episode has a cancer_category derived from encounter-level DIAGNOSIS, not patient-level"
    - "Episodes with ENCOUNTERID match get cancer_link_method='encounter_id'"
    - "Episodes without ENCOUNTERID match but with diagnosis within 30 days get cancer_link_method='closest_date'"
    - "Episodes with neither match get cancer_link_method='none' and cancer_category=NA"
    - "is_hodgkin is TRUE only when cancer_category equals 'Hodgkin Lymphoma'"
    - "Chemotherapy episodes containing doxorubicin+bleomycin+vinblastine+dacarbazine get regimen_label='ABVD'"
    - "AVD variant (doxorubicin+vinblastine+dacarbazine, no bleomycin) also gets regimen_label='ABVD'"
    - "ABVD + any extra chemo agent gets regimen_label=NA (added-agent disqualification)"
    - "BV+AVD only assigned for episodes starting on or after 2019-01-01"
    - "Nivo+AVD only assigned for episodes starting on or after 2024-01-01"
    - "R/62 runs without 'regimen_label column not found' warning after Phase 61"
  artifacts:
    - path: "R/61_episode_classification.R"
      provides: "Standalone episode classification script"
      min_lines: 200
    - path: "output/episode_classification_audit.xlsx"
      provides: "Multi-sheet audit workbook"
    - path: "output/episode_classification_audit.csv"
      provides: "Flat CSV export of episode classification results"
  key_links:
    - from: "R/61_episode_classification.R"
      to: "cache/outputs/treatment_episodes.rds"
      via: "readRDS + saveRDS in-place enrichment"
      pattern: "readRDS.*treatment_episodes\\.rds"
    - from: "R/61_episode_classification.R"
      to: "cache/outputs/treatment_episode_detail.rds"
      via: "readRDS for encounter_ids per episode"
      pattern: "readRDS.*treatment_episode_detail\\.rds"
    - from: "R/61_episode_classification.R"
      to: "DuckDB DIAGNOSIS table"
      via: "get_pcornet_table('DIAGNOSIS') for encounter-level linkage"
      pattern: "get_pcornet_table.*DIAGNOSIS"
    - from: "R/62_first_line_and_death_analysis.R"
      to: "R/61_episode_classification.R"
      via: "regimen_label column in treatment_episodes.rds"
      pattern: "regimen_label.*%in%.*names"
---

<objective>
Create R/61_episode_classification.R that classifies treatment episodes by (1) linking cancer diagnoses at encounter level via ENCOUNTERID with temporal fallback, and (2) labeling chemotherapy episodes with regimen names (ABVD, BV+AVD, Nivo+AVD) using drug composition matching with dropped-agent tolerance.

Purpose: Replace patient-level cancer linkage (R/49) with encounter-level precision for RDS artifacts. Enable Phase 62 first-line therapy identification which requires regimen_label column. Produce audit workbook documenting linkage method distribution and regimen classification results.

Output: Modified treatment_episodes.rds with 4 new columns (cancer_category, cancer_link_method, is_hodgkin, regimen_label), episode_classification_audit.xlsx (multi-sheet), episode_classification_audit.csv (flat export).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-episode-classification-cancer-linkage-and-regimen-detection/61-CONTEXT.md
@.planning/phases/61-episode-classification-cancer-linkage-and-regimen-detection/61-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/44a_treatment_episodes.R (treatment_episodes.rds schema — line 681):
```r
# Columns: patient_id, treatment_type, episode_number, episode_start, episode_stop,
#           episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes,
#           encounter_ids, drug_names
# - encounter_ids: comma-separated string of ENCOUNTERIDs (or "" if none)
# - drug_names: comma-separated unique sorted drug names (or "" if none)
# - treatment_type: one of "Chemotherapy", "Radiation", "SCT", "Immunotherapy"
```

From R/44a_treatment_episodes.R (treatment_episode_detail.rds schema — line 687):
```r
# Columns: patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name,
#           episode_number, episode_start, episode_stop, historical_flag
```

From R/49_gantt_data_export.R (PREFIX_MAP — lines 120-380):
```r
PREFIX_MAP <- c(
  "C00" = "Lip, Oral Cavity and Pharynx",
  # ... 380 lines mapping 3-char ICD-10-CM prefixes to cancer site categories ...
  "C81" = "Hodgkin Lymphoma",
  "C82" = "Non-Hodgkin Lymphoma",
  # ... etc ...
)

classify_codes <- function(codes) {
  prefixes <- substr(toupper(codes), 1, 3)
  categories <- PREFIX_MAP[prefixes]
  unname(categories)
}
```

From R/utils_duckdb.R:
```r
open_pcornet_con()         # Opens DuckDB connection
close_pcornet_con()        # Closes DuckDB connection
get_pcornet_table("DIAGNOSIS")  # Returns lazy DuckDB table reference
```

From R/utils_dates.R:
```r
parse_pcornet_date(date_col)  # Parses PCORnet date strings to Date objects
```

From R/01_load_pcornet.R (DIAGNOSIS table columns):
```r
# DIAGNOSIS: ID, ENCOUNTERID, DX, DX_DATE, DX_TYPE, PDX, DX_SOURCE, ...
```

From R/62_first_line_and_death_analysis.R (downstream consumer — lines 78-82):
```r
# Guard for missing columns from Phase 61 (regimen_label, drug_names)
if (!"regimen_label" %in% names(episodes)) {
  warning("regimen_label column not found in treatment_episodes.rds — Phase 61 not yet run.")
  episodes <- episodes %>% mutate(regimen_label = NA_character_)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cancer Linkage - ENCOUNTERID Direct Match and Temporal Fallback</name>
  <files>R/61_episode_classification.R</files>
  <read_first>
    - R/49_gantt_data_export.R (lines 118-387 for PREFIX_MAP definition and classify_codes function — copy the full PREFIX_MAP verbatim)
    - R/44a_treatment_episodes.R (lines 440-493 for episode schema with encounter_ids; lines 655-695 for drug_names aggregation and final column order)
    - R/55_cancer_summary_refined.R (for 7-day confirmation pattern — reference for LINK-04 second cancer confirmation)
    - R/utils_duckdb.R (for open_pcornet_con, close_pcornet_con, get_pcornet_table patterns)
    - R/utils_dates.R (for parse_pcornet_date function)
    - R/00_config.R (lines 1-50 for CONFIG object structure with $cache$outputs_dir, $output_dir)
  </read_first>
  <action>
Create R/61_episode_classification.R with the following structure. This task builds SECTIONS 1-4; Task 2 adds SECTIONS 5-7.

**SECTION 1: SETUP AND CONFIGURATION**
- Header comment block documenting Phase 61, decision traceability (D-01 through D-18), inputs, and outputs
- suppressPackageStartupMessages for: dplyr, stringr, glue, lubridate, openxlsx2
- source("R/00_config.R"), source("R/utils_duckdb.R"), source("R/utils_dates.R")
- Define output paths:
  - OUTPUT_RDS = file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")
  - DETAIL_RDS = file.path(CONFIG$cache$outputs_dir, "treatment_episode_detail.rds")
  - OUTPUT_XLSX = file.path(CONFIG$output_dir, "episode_classification_audit.xlsx")
  - OUTPUT_CSV = file.path(CONFIG$output_dir, "episode_classification_audit.csv")
- Console message block listing input/output files (following R/62 pattern)

**SECTION 2: PREFIX_MAP AND HELPERS**
- Copy the FULL PREFIX_MAP named vector from R/49_gantt_data_export.R lines 120-380 VERBATIM (all ~260 entries). Add comment: "Copied from R/49 for script independence (project pattern — see also R/53, R/54, R/55)"
- Copy classify_codes() function from R/49 lines 382-387
- Define helper function has_drug():
  ```r
  has_drug <- function(drug_names, drug_substring) {
    str_detect(tolower(drug_names), fixed(tolower(drug_substring)))
  }
  ```

**SECTION 3: LOAD DATA**
- Load treatment_episodes.rds with file existence check and stop() if missing
- Load treatment_episode_detail.rds with file existence check and stop() if missing
- message() after each load showing row count (following R/62 pattern)
- USE_DUCKDB <- TRUE, open_pcornet_con()

**SECTION 4: CANCER LINKAGE (per D-01 through D-08)**

Step 4a — Extract unique encounter IDs from episodes:
- episodes have encounter_ids as comma-separated string
- Use str_split(encounter_ids, ",") then unnest(cols = encounter_ids_list)
- CRITICAL: filter(!is.na(encounter_ids_list) & encounter_ids_list != "") to handle empty strings (Pitfall 1 from RESEARCH.md)
- Select patient_id, treatment_type, episode_number, encounter_ids_list, rename encounter_ids_list to ENCOUNTERID

Step 4b — Query DIAGNOSIS table via DuckDB:
- get_pcornet_table("DIAGNOSIS") %>% select(ID, ENCOUNTERID, DX, DX_DATE, DX_TYPE, PDX) %>% collect()
- Parse DX_DATE with parse_pcornet_date()
- Filter to malignant C-codes only: filter(str_sub(DX, 1, 1) == "C") — D-codes excluded per D-05
- Filter to rows with non-NA DX_DATE and non-NA ENCOUNTERID (for the direct match step)
- Log: message(glue("  DIAGNOSIS query: {nrow(dx_data)} C-code rows with ENCOUNTERID"))

Step 4c — Direct ENCOUNTERID match (per D-01):
- inner_join(episode_encounters, dx_data, by = "ENCOUNTERID")
- Classify cancer codes: mutate(prefix = str_sub(DX, 1, 3), cancer_category = classify_codes(DX))
- Handle multiple diagnoses per encounter: group_by(patient_id, treatment_type, episode_number) then arrange by whether category is "Hodgkin Lymphoma" (desc), then slice(1) — per D-04 (prefer HL)
- Set cancer_link_method = "encounter_id"
- Log count of episodes linked via ENCOUNTERID

Step 4d — Temporal fallback for unlinked episodes (per D-02, D-03):
- Identify unlinked: anti_join(episodes, encounter_linked, by = c("patient_id", "treatment_type", "episode_number"))
- Get ALL DIAGNOSIS rows (including those with NA ENCOUNTERID) for unlinked patients: filter(str_sub(DX, 1, 1) == "C"), filter(!is.na(DX_DATE))
- For each unlinked episode, left_join to diagnosis candidates by patient_id
- Filter DX_DATE <= episode_start (backward only, per D-03)
- Calculate days_before = as.numeric(episode_start - DX_DATE)
- Filter days_before <= 30 (30-day window, per D-02)
- Classify with classify_codes()
- Deduplicate: group_by(patient_id, treatment_type, episode_number), arrange(days_before, desc(cancer_category == "Hodgkin Lymphoma")), slice(1) — closest date wins, prefer HL on tie (per D-04)
- Set cancer_link_method = "closest_date"
- CRITICAL: Pitfall 2 — this group_by + slice(1) prevents duplicate episodes
- Log count of episodes linked via temporal fallback

Step 4e — Combine and merge back to episodes:
- bind_rows(encounter_linked, temporal_linked) selecting patient_id, treatment_type, episode_number, cancer_category, cancer_link_method
- left_join back to full episodes dataset
- For unmatched: mutate(cancer_link_method = if_else(is.na(cancer_link_method), "none", cancer_link_method))
- Derive is_hodgkin: mutate(is_hodgkin = (!is.na(cancer_category) & cancer_category == "Hodgkin Lymphoma")) — per D-06
- Per D-08: unmatched episodes get cancer_category = NA, cancer_link_method = "none"
- Log summary: "Linkage summary: {n_encounter_id} encounter_id, {n_closest} closest_date, {n_none} none"

Step 4f — Second cancer confirmation (per D-07, LINK-04):
- This is an INFORMATIONAL check for the audit, not a filter on episodes
- Group all diagnosis data by patient + cancer_category (non-HL)
- For each patient+category, check if max(DX_DATE) - min(DX_DATE) >= 7 days
- Store confirmed_second_cancers as a separate tibble for the audit sheet
- Log: "Second cancer confirmation: {n} patient-cancer pairs with 2+ diagnoses 7+ days apart"

close_pcornet_con() after all DuckDB queries complete.

End of Task 1 scope. Do NOT write Sections 5-7 yet (Task 2). Do NOT save the RDS or create xlsx yet. The script should be syntactically complete through Section 4 but with placeholder comments for Sections 5-7.
  </action>
  <verify>
    <automated>Rscript -e "source('R/61_episode_classification.R')" should fail gracefully at the placeholder for Section 5, but SECTIONS 1-4 should execute without error if treatment_episodes.rds exists. Alternatively: grep -c "cancer_link_method" R/61_episode_classification.R should return 5+ matches; grep -c "PREFIX_MAP" R/61_episode_classification.R should return 2+ matches; grep -c "classify_codes" R/61_episode_classification.R should return 2+ matches.</automated>
  </verify>
  <acceptance_criteria>
    - R/61_episode_classification.R exists with 200+ lines
    - File contains "PREFIX_MAP <- c(" (full map copied from R/49)
    - File contains 'classify_codes <- function'
    - File contains 'has_drug <- function'
    - File contains 'cancer_link_method = "encounter_id"'
    - File contains 'cancer_link_method = "closest_date"'
    - File contains 'cancer_link_method.*"none"'
    - File contains 'is_hodgkin = ' derived from cancer_category
    - File contains 'get_pcornet_table("DIAGNOSIS")'
    - File contains 'filter(days_before <= 30)' for 30-day temporal window
    - File contains 'DX_DATE <= episode_start' for backward-only temporal fallback
    - File contains 'slice(1)' for deduplication of multiple diagnoses per episode
    - File contains 'filter(!is.na(encounter_ids_list) & encounter_ids_list != "")' for empty string handling
    - File contains 'open_pcornet_con()' and 'close_pcornet_con()'
    - File contains 'source("R/00_config.R")' and 'source("R/utils_duckdb.R")' and 'source("R/utils_dates.R")'
  </acceptance_criteria>
  <done>R/61_episode_classification.R exists with Sections 1-4 complete: PREFIX_MAP copied, DIAGNOSIS queried via DuckDB, ENCOUNTERID direct match implemented, 30-day backward temporal fallback implemented with deduplication, is_hodgkin derived from encounter-level cancer_category, second cancer confirmation computed for audit.</done>
</task>

<task type="auto">
  <name>Task 2: Regimen Detection, RDS Save, and Audit Output</name>
  <files>R/61_episode_classification.R, output/episode_classification_audit.xlsx, output/episode_classification_audit.csv</files>
  <read_first>
    - R/61_episode_classification.R (the file created in Task 1 — read current state before modifying)
    - R/59_death_date_validation.R (lines 370-420 for openxlsx2 multi-sheet audit workbook pattern with styled headers)
    - R/62_first_line_and_death_analysis.R (lines 78-82 for regimen_label guard — confirm downstream expectation)
    - R/60_drug_name_resolution.R (to understand drug_name_lookup.rds structure and drug name formats returned by RxNorm)
    - R/00_config.R (lines 419-427 for ABVD + BV + Nivo HCPCS code comments confirming drug names)
  </read_first>
  <action>
Complete R/61_episode_classification.R by adding SECTIONS 5-7 after the Section 4 code from Task 1.

**SECTION 5: REGIMEN DETECTION (per D-09 through D-14)**

Step 5a — Filter to chemotherapy episodes only:
- Regimen detection only applies to treatment_type == "Chemotherapy" episodes
- Episodes without drug_names (empty string or NA) cannot be classified — these get regimen_label = NA

Step 5b — Drug detection flags (per D-10):
- For each chemotherapy episode, detect the presence of each drug using case-insensitive substring matching via the has_drug() helper defined in Section 2:
  - has_dox = has_drug(drug_names, "doxorubicin")
  - has_bleo = has_drug(drug_names, "bleomycin")
  - has_vin = has_drug(drug_names, "vinblastine")
  - has_dac = has_drug(drug_names, "dacarbazine")
  - has_brex = has_drug(drug_names, "brentuximab")
  - has_nivo = has_drug(drug_names, "nivolumab")

Step 5c — Count unique drugs for added-agent disqualification (per D-12):
- Count drugs: n_unique_drugs = if_else(drug_names == "" | is.na(drug_names), 0L, as.integer(str_count(drug_names, ",") + 1))
- CRITICAL: Pitfall 5 — handle empty string/NA case to avoid counting 1 drug when there are 0

Step 5d — Regimen classification with case_when (per D-10, D-11, D-12, D-13, D-14):
```r
regimen_label = case_when(
  # BV+AVD: brentuximab + dox + vin + dac, no bleomycin, exactly 4 drugs, post-2019 (D-10, D-13)
  has_brex & has_dox & has_vin & has_dac & !has_bleo & n_unique_drugs == 4L &
    episode_start >= as.Date("2019-01-01") ~ "BV+AVD",

  # Nivo+AVD: nivolumab + dox + vin + dac, no bleomycin, exactly 4 drugs, post-2024 (D-10, D-13)
  has_nivo & has_dox & has_vin & has_dac & !has_bleo & n_unique_drugs == 4L &
    episode_start >= as.Date("2024-01-01") ~ "Nivo+AVD",

  # ABVD (full): all 4 drugs, no brentuximab, no nivolumab, max 4 drugs (D-10, D-12)
  has_dox & has_bleo & has_vin & has_dac & !has_brex & !has_nivo & n_unique_drugs <= 4L ~ "ABVD",

  # AVD variant (dropped bleomycin): dox + vin + dac, no bleo, no brentuximab, no nivolumab,
  # max 3 drugs (D-11 — RATHL trial standard of care)
  has_dox & has_vin & has_dac & !has_bleo & !has_brex & !has_nivo & n_unique_drugs <= 3L ~ "ABVD",

  # Non-matching chemotherapy episodes (D-14)
  TRUE ~ NA_character_
)
```
- Order matters: BV+AVD and Nivo+AVD checked BEFORE ABVD because they share 3 of 4 drugs with ABVD. If brentuximab is present, it is NOT ABVD.
- Remove intermediate columns: select(-starts_with("has_"), -n_unique_drugs)

Step 5e — Merge regimen_label back to full episodes:
- Left join the regimen labels from chemo subset back to the full episodes tibble
- Non-chemotherapy episodes get regimen_label = NA (they were never candidates)
- Log: "Regimen detection: {n_abvd} ABVD, {n_bv} BV+AVD, {n_nivo} Nivo+AVD, {n_na} unclassified chemo, {n_non_chemo} non-chemotherapy"

**SECTION 6: SAVE ENRICHED RDS (per D-15, D-16)**
- Final column order for treatment_episodes.rds: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, encounter_ids, drug_names, cancer_category, cancer_link_method, is_hodgkin, regimen_label
- saveRDS(episodes, OUTPUT_RDS)
- message(glue("Saved enriched treatment_episodes.rds: {nrow(episodes)} episodes, {ncol(episodes)} columns"))
- Verify column presence with stopifnot(all(c("cancer_category", "cancer_link_method", "is_hodgkin", "regimen_label") %in% names(episodes)))

**SECTION 7: AUDIT OUTPUT (per D-17, D-18)**

Create multi-sheet audit xlsx following R/59 openxlsx2 pattern:

Sheet 1 — "Linkage Summary":
- Title: "Episode Classification Audit" (Calibri 16pt bold, color #1F2937)
- Subtitle: "Generated: {Sys.Date()} | Total episodes: {nrow(episodes)}"
- Summary table starting row 4 with columns: Metric, Count, Percent
  - Total episodes
  - Linked via ENCOUNTERID (count and % of total)
  - Linked via temporal fallback (count and % of total)
  - Unlinked (count and % of total)
- Header styling: Calibri 11pt bold white text on #1F2937 background
- Freeze top rows, auto-width columns

Sheet 2 — "Cancer Categories":
- Frequency table: cancer_category, n_episodes, pct_episodes, n_patients, pct_patients
- Sorted by n_episodes descending
- Include NA category row for unlinked episodes
- Same header styling as Sheet 1

Sheet 3 — "Regimen Distribution":
- Frequency table for chemotherapy episodes only: regimen_label, n_episodes, pct_chemo_episodes, n_patients
- Include NA row for unclassified chemotherapy
- Add AVD_variant_count row or note showing how many ABVD episodes are the dropped-bleomycin variant

Sheet 4 — "Second Cancer Confirmation":
- The confirmed_second_cancers tibble from Step 4f
- Columns: patient_id, cancer_category, n_diagnoses, min_dx_date, max_dx_date, days_span
- Filtered to those with days_span >= 7

Sheet 5 — "Unlinked Episodes":
- All episodes with cancer_link_method == "none"
- Columns: patient_id, treatment_type, episode_number, episode_start, encounter_ids, drug_names
- Helps clinical review understand which episodes could not be linked

Save workbook: wb_save(wb, OUTPUT_XLSX)

CSV export (per D-18): Write the full episodes tibble (with all 4 new columns) to OUTPUT_CSV using write.csv(row.names = FALSE).

Final console summary message with counts for each linkage method and each regimen.
  </action>
  <verify>
    <automated>grep -c "regimen_label" R/61_episode_classification.R should return 5+; grep -c "case_when" R/61_episode_classification.R should return 1+; grep -c "wb_workbook" R/61_episode_classification.R should return 1; grep -c "saveRDS" R/61_episode_classification.R should return 1+; grep "episode_classification_audit" R/61_episode_classification.R should show both .xlsx and .csv paths.</automated>
  </verify>
  <acceptance_criteria>
    - R/61_episode_classification.R is a complete, runnable script (no placeholder sections remain)
    - File contains 'case_when(' with regimen classification logic
    - File contains "BV+AVD" string literal in case_when
    - File contains "Nivo+AVD" string literal in case_when
    - File contains "ABVD" string literal in case_when (at least 2 occurrences — full ABVD and AVD variant)
    - File contains 'as.Date("2019-01-01")' for BV+AVD temporal availability
    - File contains 'as.Date("2024-01-01")' for Nivo+AVD temporal availability
    - File contains 'n_unique_drugs' computation with empty string guard
    - File contains '!has_brex & !has_nivo' in ABVD classification (added-agent disqualification)
    - File contains 'n_unique_drugs <= 4' for ABVD and 'n_unique_drugs <= 3' for AVD variant
    - File contains 'n_unique_drugs == 4' for BV+AVD and Nivo+AVD
    - File contains 'wb_workbook()' for audit xlsx creation
    - File contains at least 4 'add_worksheet' calls (Linkage Summary, Cancer Categories, Regimen Distribution, Second Cancer Confirmation or Unlinked Episodes)
    - File contains 'write.csv' for flat CSV export
    - File contains 'saveRDS(episodes' for in-place RDS enrichment
    - File contains 'stopifnot(all(c("cancer_category", "cancer_link_method", "is_hodgkin", "regimen_label")'
    - The final select/column order includes all 15 columns: patient_id through regimen_label
  </acceptance_criteria>
  <done>R/61_episode_classification.R is a complete, runnable script with all 7 sections. Regimen detection classifies ABVD (including AVD variant), BV+AVD (post-2019), and Nivo+AVD (post-2024) with added-agent disqualification. treatment_episodes.rds saved with 4 new columns. Audit xlsx has 5 sheets documenting linkage methods, cancer categories, regimen distribution, second cancer confirmation, and unlinked episodes. Flat CSV export produced.</done>
</task>

</tasks>

<verification>
1. R/61_episode_classification.R exists and is syntactically valid R (no parse errors)
2. treatment_episodes.rds contains columns: cancer_category, cancer_link_method, is_hodgkin, regimen_label
3. cancer_link_method has exactly 3 possible values: "encounter_id", "closest_date", "none"
4. is_hodgkin is TRUE only for cancer_category == "Hodgkin Lymphoma"
5. regimen_label is one of: "ABVD", "BV+AVD", "Nivo+AVD", or NA
6. episode_classification_audit.xlsx has 5 sheets
7. episode_classification_audit.csv contains all episode rows with 4 new columns
8. R/62_first_line_and_death_analysis.R runs without "regimen_label column not found" warning
</verification>

<success_criteria>
- treatment_episodes.rds enriched with cancer_category (encounter-level), cancer_link_method, is_hodgkin, and regimen_label
- Linkage method distribution visible in audit xlsx (encounter_id vs closest_date vs none with percentages)
- Regimen classification produces non-zero ABVD count for chemotherapy episodes with appropriate drugs
- Phase 62 downstream consumption verified (regimen_label column exists)
- No modification to R/44a, R/49, or any other existing script
</success_criteria>

<output>
After completion, create `.planning/phases/61-episode-classification-cancer-linkage-and-regimen-detection/61-01-SUMMARY.md`
</output>
