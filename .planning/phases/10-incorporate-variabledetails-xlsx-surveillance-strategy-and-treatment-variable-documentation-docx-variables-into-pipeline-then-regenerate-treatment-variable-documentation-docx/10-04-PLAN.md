---
phase: 10-incorporate-variabledetails-xlsx-surveillance-strategy-and-treatment-variable-documentation-docx-variables-into-pipeline-then-regenerate-treatment-variable-documentation-docx
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - R/04_build_cohort.R
autonomous: true
requirements:
  - SURV-04
  - SVENC-04
  - TDOC-01

must_haves:
  truths:
    - "hl_cohort.csv contains all surveillance modality columns (HAD/DATE/COUNT per modality)"
    - "hl_cohort.csv contains all survivorship encounter columns (HAD/COUNT/DATE per level)"
    - "hl_cohort.csv contains DAYS_DX_TO_CHEMO, DAYS_DX_TO_RADIATION, DAYS_DX_TO_SCT timing columns"
    - "Pipeline runs end-to-end with new columns appearing in cohort summary"
  artifacts:
    - path: "R/04_build_cohort.R"
      provides: "Cohort with surveillance, survivorship, and timing columns integrated"
      contains: "assemble_surveillance_flags"
  key_links:
    - from: "R/04_build_cohort.R"
      to: "R/13_surveillance.R"
      via: "source() then assemble_surveillance_flags(post_dx_date_map)"
      pattern: "source.*13_surveillance"
    - from: "R/04_build_cohort.R"
      to: "R/14_survivorship_encounters.R"
      via: "source() then classify_survivorship_encounters(post_dx_date_map)"
      pattern: "source.*14_survivorship"
    - from: "R/04_build_cohort.R"
      to: "output/cohort/hl_cohort.csv"
      via: "write_csv with all new columns in select()"
      pattern: "write_csv.*hl_cohort"
---

<objective>
Integrate surveillance flags, survivorship encounter flags, and timing derivation columns into 04_build_cohort.R so they appear in the final hl_cohort.csv output.

Purpose: Implements D-12 (timing derivation) and wires the outputs of Plans 02 and 03 into the cohort pipeline. Without this integration, the new detection scripts exist but produce no output.

Output: Updated R/04_build_cohort.R with new sections 6.6 (timing), 6.7 (surveillance), 6.8 (survivorship) and updated Section 7 select() with all new columns.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-incorporate-variabledetails-xlsx-surveillance-strategy-and-treatment-variable-documentation-docx-variables-into-pipeline-then-regenerate-treatment-variable-documentation-docx/10-CONTEXT.md
@.planning/phases/10-incorporate-variabledetails-xlsx-surveillance-strategy-and-treatment-variable-documentation-docx-variables-into-pipeline-then-regenerate-treatment-variable-documentation-docx/10-RESEARCH.md

<interfaces>
<!-- From 04_build_cohort.R current structure -->
Section order in 04_build_cohort.R:
```
Section 6:   Treatment flags (HAD_CHEMO, HAD_RADIATION, HAD_SCT)
Section 6.5: Treatment-anchored payer mode
Section 7:   Final cohort assembly (select())
Section 8:   Cohort summary (console output)
Section 9:   Attrition summary
Section 10:  CSV output
```

Key variable available after Section 4:
```r
cohort$first_hl_dx_date  # Date of first HL diagnosis, available for timing derivation
```

Key variables available after Section 6:
```r
cohort$FIRST_CHEMO_DATE      # Date (may be NA)
cohort$FIRST_RADIATION_DATE  # Date (may be NA)
cohort$FIRST_SCT_DATE        # Date (may be NA)
```

<!-- From Plan 02 output -->
From R/13_surveillance.R:
```r
assemble_surveillance_flags(post_dx_date_map)
# post_dx_date_map: tibble(ID, first_hl_dx_date)
# Returns: tibble(ID, HAD_MAMMOGRAM, FIRST_MAMMOGRAM_DATE, N_MAMMOGRAM, ...) -- ~57 columns total
```

<!-- From Plan 03 output -->
From R/14_survivorship_encounters.R:
```r
classify_survivorship_encounters(post_dx_date_map)
# post_dx_date_map: tibble(ID, first_hl_dx_date)
# Returns: tibble(ID, HAD_ENC_NONACUTE, N_ENC_NONACUTE, FIRST_ENC_NONACUTE_DATE, ...) -- 12 columns + ID
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timing derivation, surveillance, and survivorship integration to 04_build_cohort.R</name>
  <files>R/04_build_cohort.R</files>
  <action>
Insert three new sections into 04_build_cohort.R between the existing Section 6.5 (treatment-anchored payer) and Section 7 (final assembly):

1. **Section 6.6: Timing Derivation (D-12)**
   Insert after Section 6.5 and before Section 7:
```r
# ==============================================================================
# SECTION 6.6: TIMING DERIVATION (D-12)
# ==============================================================================

message("\n--- Timing Derivation ---")

cohort <- cohort %>%
  mutate(
    DAYS_DX_TO_CHEMO     = as.integer(FIRST_CHEMO_DATE - first_hl_dx_date),
    DAYS_DX_TO_RADIATION = as.integer(FIRST_RADIATION_DATE - first_hl_dx_date),
    DAYS_DX_TO_SCT       = as.integer(FIRST_SCT_DATE - first_hl_dx_date)
  )

message(glue("  DAYS_DX_TO_CHEMO: median {median(cohort$DAYS_DX_TO_CHEMO, na.rm = TRUE)} days (N={sum(!is.na(cohort$DAYS_DX_TO_CHEMO))})"))
message(glue("  DAYS_DX_TO_RADIATION: median {median(cohort$DAYS_DX_TO_RADIATION, na.rm = TRUE)} days (N={sum(!is.na(cohort$DAYS_DX_TO_RADIATION))})"))
message(glue("  DAYS_DX_TO_SCT: median {median(cohort$DAYS_DX_TO_SCT, na.rm = TRUE)} days (N={sum(!is.na(cohort$DAYS_DX_TO_SCT))})"))
```

2. **Section 6.7: Surveillance Flags**
   Source 13_surveillance.R and join results:
```r
# ==============================================================================
# SECTION 6.7: SURVEILLANCE MODALITY FLAGS (D-01, D-02, D-03, D-04)
# ==============================================================================

message("\n--- Surveillance Modality Detection ---")
source("R/13_surveillance.R")

post_dx_date_map <- cohort %>% select(ID, first_hl_dx_date)
surveillance_flags <- assemble_surveillance_flags(post_dx_date_map)

cohort <- cohort %>%
  left_join(surveillance_flags, by = "ID")
```

3. **Section 6.8: Survivorship Encounter Flags**
   Source 14_survivorship_encounters.R and join results:
```r
# ==============================================================================
# SECTION 6.8: SURVIVORSHIP ENCOUNTER CLASSIFICATION (D-05 through D-10)
# ==============================================================================

message("\n--- Survivorship Encounter Classification ---")
source("R/14_survivorship_encounters.R")

survivorship_flags <- classify_survivorship_encounters(post_dx_date_map)

cohort <- cohort %>%
  left_join(survivorship_flags, by = "ID")
```

4. **Update Section 7 select():**
   The current select() lists all columns explicitly. Add the new columns. Since surveillance produces many columns (3 per modality x ~19 modalities = ~57 columns), use a pragmatic approach:
   - Keep the existing explicit column list
   - After the existing columns, add timing columns explicitly:
     ```r
     DAYS_DX_TO_CHEMO,
     DAYS_DX_TO_RADIATION,
     DAYS_DX_TO_SCT,
     ```
   - Then use starts_with() for the surveillance and survivorship columns:
     ```r
     starts_with("HAD_MAMMOGRAM"), starts_with("FIRST_MAMMOGRAM"), starts_with("N_MAMMOGRAM"),
     # ... or more efficiently:
     matches("^(HAD|FIRST|N)_(MAMMOGRAM|BREAST_MRI|ECHO|STRESS_TEST|ECG|MUGA|PFT|TSH|CBC|CRP|ALT|AST|ALP|GGT|BILIRUBIN|PLATELETS|FOBT)"),
     starts_with("HAD_ENC_"), starts_with("N_ENC_"), starts_with("FIRST_ENC_"),
     ```
   - Alternatively, use everything() at the end if column ordering is not critical, or list all new columns explicitly if the set is stable.

5. **Update Section 8 (Cohort Summary):**
   Add summary lines for the new variables:
```r
message("\n--- Surveillance Modalities ---")
surv_modalities <- c("MAMMOGRAM", "BREAST_MRI", "ECHO", "STRESS_TEST", "ECG", "MUGA", "PFT", "TSH", "CBC")
for (mod in surv_modalities) {
  had_col <- paste0("HAD_", mod)
  if (had_col %in% names(hl_cohort)) {
    n <- sum(hl_cohort[[had_col]] == 1, na.rm = TRUE)
    message(glue("  {mod}: {n} patients ({round(100*n/nrow(hl_cohort), 1)}%)"))
  }
}

message("\n--- Lab Results ---")
lab_types <- c("CRP", "ALT", "AST", "ALP", "GGT", "BILIRUBIN", "PLATELETS", "FOBT")
for (lab in lab_types) {
  had_col <- paste0("HAD_", lab)
  if (had_col %in% names(hl_cohort)) {
    n <- sum(hl_cohort[[had_col]] == 1, na.rm = TRUE)
    message(glue("  {lab}: {n} patients ({round(100*n/nrow(hl_cohort), 1)}%)"))
  }
}

message("\n--- Survivorship Encounters ---")
message(glue("  Non-acute care (L1): {sum(hl_cohort$HAD_ENC_NONACUTE == 1, na.rm = TRUE)} patients"))
message(glue("  Cancer-related (L2): {sum(hl_cohort$HAD_ENC_CANCER_RELATED == 1, na.rm = TRUE)} patients"))
message(glue("  Cancer provider (L3): {sum(hl_cohort$HAD_ENC_CANCER_PROVIDER == 1, na.rm = TRUE)} patients"))
message(glue("  Survivorship (L4): {sum(hl_cohort$HAD_ENC_SURVIVORSHIP == 1, na.rm = TRUE)} patients"))

message("\n--- Timing ---")
message(glue("  DAYS_DX_TO_CHEMO: median {median(hl_cohort$DAYS_DX_TO_CHEMO, na.rm = TRUE)} days"))
message(glue("  DAYS_DX_TO_RADIATION: median {median(hl_cohort$DAYS_DX_TO_RADIATION, na.rm = TRUE)} days"))
message(glue("  DAYS_DX_TO_SCT: median {median(hl_cohort$DAYS_DX_TO_SCT, na.rm = TRUE)} days"))
```

IMPORTANT: Do not change existing sections (1-6.5). Only insert new sections and extend Section 7/8. The column count in hl_cohort.csv will increase significantly (~70+ new columns).
  </action>
  <verify>
    <automated>grep -c "SECTION 6.6\|SECTION 6.7\|SECTION 6.8\|DAYS_DX_TO_CHEMO\|assemble_surveillance_flags\|classify_survivorship_encounters" R/04_build_cohort.R</automated>
  </verify>
  <done>04_build_cohort.R has Sections 6.6 (timing derivation with 3 DAYS_DX_TO_* columns), 6.7 (surveillance flags via assemble_surveillance_flags), 6.8 (survivorship encounter flags via classify_survivorship_encounters). Section 7 select() includes all new columns. Section 8 summary reports new variable distributions. Pipeline produces hl_cohort.csv with all new columns.</done>
</task>

</tasks>

<verification>
- grep "SECTION 6.6\|SECTION 6.7\|SECTION 6.8" R/04_build_cohort.R returns 3 matches
- grep "DAYS_DX_TO_CHEMO" R/04_build_cohort.R returns matches
- grep "assemble_surveillance_flags" R/04_build_cohort.R returns matches
- grep "classify_survivorship_encounters" R/04_build_cohort.R returns matches
- grep "source.*13_surveillance\|source.*14_survivorship" R/04_build_cohort.R returns 2 matches
</verification>

<success_criteria>
04_build_cohort.R integrates all Phase 10 outputs: timing derivation (D-12), surveillance flags (D-01 through D-04), and survivorship encounters (D-05 through D-10). hl_cohort.csv includes all new columns with proper select(). Cohort summary reports distribution of all new variables.
</success_criteria>

<output>
After completion, create `.planning/phases/10-incorporate-variabledetails-xlsx-surveillance-strategy-and-treatment-variable-documentation-docx-variables-into-pipeline-then-regenerate-treatment-variable-documentation-docx/10-04-SUMMARY.md`
</output>
