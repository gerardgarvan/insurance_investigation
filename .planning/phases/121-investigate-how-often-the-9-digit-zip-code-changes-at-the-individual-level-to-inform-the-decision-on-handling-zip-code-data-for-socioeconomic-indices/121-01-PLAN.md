---
phase: 121-investigate-how-often-the-9-digit-zip-code-changes-at-the-individual-level-to-inform-the-decision-on-handling-zip-code-data-for-socioeconomic-indices
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/106_zip_change_frequency.R
  - R/39_run_all_investigations.R
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements:
  - ZIP-01
  - ZIP-02
  - ZIP-03
  - ZIP-04
  - SMOKE-121-01
user_setup: []

must_haves:
  truths:
    - "A user on HiPerGator can run R/106 and, if LDS_ADDRESS_HISTORY is present, see per-patient ZIP change frequency at BOTH ZIP9 and ZIP5 granularity (D-04)"
    - "If LDS_ADDRESS_HISTORY is absent (e.g. locally), R/106 reports that clearly and exits gracefully with status 0 (no crash) (D-02)"
    - "R/106 produces a 5-sheet styled xlsx matching the R/100 look-and-feel and logs headline stats to the console before writing it (D-06, D-09)"
    - "The report surfaces the ZIP9-change-only distinction (ZIP9 changed but ZIP5 did not) so the ADI-vs-SVI decision can be made with full info (D-05)"
    - "The report presents the most-recent vs modal single-ZIP tie-break disagreement rate without committing to one rule (D-11)"
    - "R/106 is registered in R/39, smoke-tested in R/88 Section 15s, and indexed in R/SCRIPT_INDEX.md (D-08)"
    - "A user can run R/88 locally and see all Phase 121 structural checks pass (R/106 present + probe gate + ZIP normalization + xlsx + HIPAA suppression)"
  artifacts:
    - path: "R/106_zip_change_frequency.R"
      provides: "Read-only ZIP change frequency investigation: probe gate + 5-sheet styled xlsx + console summary"
      min_lines: 150
      contains: "normalize_zip9"
    - path: "R/39_run_all_investigations.R"
      provides: "R/106 registered as the new comma-less final investigation_scripts entry (R/105 gains a trailing comma)"
      contains: "R/106_zip_change_frequency.R"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15s: 14 Phase 121 structural checks + SMOKE-121-01 summary line"
      contains: "SECTION 15s"
    - path: "R/SCRIPT_INDEX.md"
      provides: "R/106 row in Post-Renumber Investigations (100+) table; counts 6->7, Total 92->93"
      contains: "R/106_zip_change_frequency.R"
  key_links:
    - from: "R/106_zip_change_frequency.R"
      to: "file.path(CONFIG$data_dir, ADDR_FILENAME)"
      via: "file.exists() probe + quit(status = 0) when absent"
      pattern: "file\\.exists.*ADDR|quit\\(status = 0\\)"
    - from: "R/39_run_all_investigations.R"
      to: "R/106_zip_change_frequency.R"
      via: "investigation_scripts vector entry"
      pattern: "R/106_zip_change_frequency\\.R"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/106_zip_change_frequency.R"
      via: "Section 15s file.exists + grep checks"
      pattern: "R/106_zip_change_frequency\\.R"
---

<objective>
Create the Phase 121 read-only investigation `R/106_zip_change_frequency.R` and wire it
into the pipeline (R/39 runner, R/88 smoke test, R/SCRIPT_INDEX.md).

R/106 probes for the PCORnet CDM `LDS_ADDRESS_HISTORY` table (the only source with a
time-varying 9-digit ZIP), and — if present — quantifies how often an individual
patient's ZIP changes at BOTH ZIP9 and ZIP5 granularity, producing a 5-sheet styled
xlsx plus a console summary. The purpose is to INFORM a downstream decision on how ZIP
data should feed socioeconomic indices (ADI needs ZIP9 block-group precision; SVI needs
only ZIP5). The script does not compute any SES index and does not add
LDS_ADDRESS_HISTORY to the permanent load set — both are explicitly deferred.

Purpose: Give the team a defensible, data-driven answer to "is a single ZIP per patient
good enough, or do we need time-varying handling?" before any SES-index work begins.

Output: R/106_zip_change_frequency.R (new); output/zip_change_frequency.xlsx (at runtime
on HiPerGator); R/39 + R/88 + R/SCRIPT_INDEX.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-investigate-how-often-the-9-digit-zip-code-changes-at-the-individual-level-to-inform-the-decision-on-handling-zip-code-data-for-socioeconomic-indices/121-CONTEXT.md
@.planning/phases/121-investigate-how-often-the-9-digit-zip-code-changes-at-the-individual-level-to-inform-the-decision-on-handling-zip-code-data-for-socioeconomic-indices/121-RESEARCH.md

<interfaces>
<!-- Verbatim-reuse patterns extracted from the codebase. Executor should copy these
     directly — no codebase exploration needed. -->

## add_styled_sheet() + color constants (COPY VERBATIM from R/100 lines 328-367)
```r
DARK_GRAY <- wb_color("FF374151")
WHITE     <- wb_color("FFFFFFFF")
DARK_TEXT <- wb_color("FF1F2937")

add_styled_sheet <- function(wb, sheet_name, title_text, subtitle_text, data_tbl) {
  wb$add_worksheet(sheet_name)
  n_cols           <- ncol(data_tbl)
  last_col_letter  <- openxlsx2::int2col(n_cols)
  wb$add_data(sheet = sheet_name, x = title_text,    dims = "A1")
  wb$add_data(sheet = sheet_name, x = subtitle_text, dims = "A2")
  wb$add_data(sheet = sheet_name, x = data_tbl,      dims = "A4", col_names = TRUE)
  wb$merge_cells(sheet = sheet_name, dims = paste0("A1:", last_col_letter, "1"))
  wb$merge_cells(sheet = sheet_name, dims = paste0("A2:", last_col_letter, "2"))
  wb$add_font(sheet = sheet_name, dims = "A1", name = "Calibri", size = 14, bold = TRUE, color = DARK_TEXT)
  wb$add_font(sheet = sheet_name, dims = "A2", name = "Calibri", size = 10, italic = TRUE, color = DARK_TEXT)
  header_range <- paste0("A4:", last_col_letter, "4")
  wb$add_fill(sheet = sheet_name, dims = header_range, color = DARK_GRAY)
  wb$add_font(sheet = sheet_name, dims = header_range, name = "Calibri", size = 11, bold = TRUE, color = WHITE)
  wb$freeze_pane(sheet = sheet_name, firstActiveRow = 5)
  wb$set_col_widths(sheet = sheet_name, cols = 1:n_cols, widths = "auto")
}
```

## Probe-first gate (ADAPT from R/103 lines 137-152)
```r
addr_path <- file.path(CONFIG$data_dir, ADDR_FILENAME)
message(glue("  CSV probe: {addr_path}"))
message(glue("  File exists? {file.exists(addr_path)}"))
if (!file.exists(addr_path)) {
  message(glue(
    "\n[R/106] LDS_ADDRESS_HISTORY not found at expected path.\n",
    "  Expected: {addr_path}\n",
    "  This table is NOT in the permanent PCORNET_TABLES load set.\n",
    "  Confirm the exact filename with the data custodian and re-run on HiPerGator.\n"
  ))
  quit(status = 0)   # graceful exit, NOT stop()
}
```

## Config conventions (from R/00_config.R lines 252-260)
- Patient ID column is `ID` (NOT PATID) across all tables.
- Filename-override precedent: `PCORNET_PATHS[["LAB_RESULT_CM"]] <- file.path(CONFIG$data_dir, "LAB_RESULT_Mailhot_V1.csv")`.
- Runtime-unknown filename comment precedent: R/00_config.R lines 256-257 (DEATH_CAUSE).
- `parse_pcornet_date()` (utils_dates) is auto-sourced by R/00_config.R for period-date parsing.

## R/39 investigation_scripts vector tail (CURRENT — lines 194-196)
```r
  "R/104_gantt_entire_history.R",             # ...
  "R/105_normalize_supportive_care_meaning.R"  # ...  <- CURRENTLY comma-less final entry
)
```

## R/88 Section 15r 14-check template (lines 2412-2498) — MIRROR EXACTLY
Structure: `check(... exists)` -> `if (rNNN_exists) { readLines + paste; 13 grep checks } else { for (i in 2:14) check(..., FALSE) }`.
IS_LOCAL runtime-gate precedent (Section 15p Check 14, lines 2301-2315):
```r
  if (!IS_LOCAL && file.exists(out_xlsx_121)) {
    check("...runtime...", <condition>)
  } else {
    check("... -- SKIPPED (local / no output) (Phase 121)", TRUE)
  }
```
`check()` signature: `check(description_string, boolean_condition)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create R/106_zip_change_frequency.R (probe gate + ZIP metrics + 5-sheet xlsx + console summary)</name>
  <read_first>
    - R/106_zip_change_frequency.R (target — does not exist yet; create it)
    - R/100_ruca_rurality_summary.R (add_styled_sheet + color constants + ZIP normalization + console-summary style — COPY the helper verbatim)
    - R/103_death_cause_diagnostic.R lines 1-160 (probe-first file.exists gate + quit(status=0) + self-bootstrap header style)
    - R/00_config.R lines 248-261 (PCORNET_PATHS override pattern, DEATH_CAUSE runtime-unknown comment, ID-not-PATID note)
    - .planning/phases/121-...-socioeconomic-indices/121-RESEARCH.md (Sections: Script Structure, Patterns 1-3, Suggested Metrics/Sheet Design, Common Pitfalls 1-7)
  </read_first>
  <files>R/106_zip_change_frequency.R</files>
  <action>
Create a new self-contained, read-only investigation script `R/106_zip_change_frequency.R`
with a header comment block in the R/100/R/103 style (Purpose / Inputs / Outputs /
Dependencies / Requirements: Phase 121 / Usage / Note that it is READ-ONLY and structurally
verified locally, runtime on HiPerGator only). Implement these sections:

SECTION 1 — SETUP AND LIBRARIES:
  suppressPackageStartupMessages({ library(dplyr); library(glue); library(stringr);
  library(tidyr); library(openxlsx2); library(tibble); library(lubridate) })
  source("R/00_config.R")   # auto-sources utils (parse_pcornet_date, etc.)
  message("=== Phase 121: ZIP Change Frequency ===\n")

SECTION 2 — CONSTANTS AND PROBE (D-02):
  Store the filename as a constant with a runtime-unknown comment mirroring R/00_config.R
  lines 256-257:
    # Runtime unknown: actual HiPerGator filename may differ. Default {TABLE}_Mailhot_V1.csv
    # pattern. If the data custodian named it differently, update ADDR_FILENAME.
    ADDR_FILENAME <- "LDS_ADDRESS_HISTORY_Mailhot_V1.csv"
    OUTPUT_XLSX   <- file.path(CONFIG$output_dir, "zip_change_frequency.xlsx")
  Then the probe-first gate ADAPTED from R/103 (see <interfaces>): file.exists() on
  file.path(CONFIG$data_dir, ADDR_FILENAME); if absent, message() + quit(status = 0).
  MUST use quit(status = 0), NOT stop() (Pitfall / D-02).

SECTION 3 — LOAD ADDRESS TABLE (read CSV DIRECTLY by path — NOT get_pcornet_table, since
  LDS_ADDRESS_HISTORY is not in PCORNET_TABLES):
    addr <- vroom::vroom(addr_path, col_types = vroom::cols(.default = "c"), progress = FALSE)
  (base read.csv(..., colClasses="character") fallback acceptable if vroom errors).
  Validate the ID column and ADDRESS_ZIP9 column are present (stopifnot with a clear
  message); use `ID` throughout (NOT PATID — Pitfall 4). Log nrow + n_distinct(ID).
  Log non-NA counts for ADDRESS_ZIP5 vs ADDRESS_ZIP9 and for ADDRESS_PREFERRED == "Y"
  (Open Questions 2-3): n_addr_zip5_nonNA, n_addr_zip9_nonNA, n_preferred_Y.

SECTION 4 — HELPER FUNCTIONS:
    normalize_zip9 <- function(zip) {
      z <- str_remove_all(str_trim(zip), "-")     # strip hyphen BEFORE pad (Pitfall 1)
      z <- str_pad(z, 9, pad = "0")
      if_else(str_detect(z, "^[0-9]{9}$"), z, NA_character_)
    }
    normalize_zip5 <- function(zip9_clean) str_sub(zip9_clean, 1, 5)
  ZIP5 SOURCING RULE (Pitfall 3): prefer the ADDRESS_ZIP5 column when populated
  (normalize it like ZIP9 but to 5 digits / "^[0-9]{5}$"); derive from normalize_zip9()
  when ADDRESS_ZIP5 is blank/NA. Log the count of records where the ADDRESS_ZIP5 column
  disagrees with str_sub(zip9_norm,1,5) (mismatch count). Do NOT filter on
  ADDRESS_PERIOD_END (Pitfall 2 — open-ended current addresses have NA end).

SECTION 5 — PER-PATIENT ZIP METRICS (group_by(ID)): after filtering !is.na on each zip
  BEFORE n_distinct (Pitfall 5a), compute per patient: n_zip9_distinct, n_zip5_distinct,
  zip9_ever_changed (n_zip9_distinct > 1), zip5_ever_changed (n_zip5_distinct > 1),
  n_address_records (total rows). Log the count of patients dropped because ALL their ZIP
  values were NA.

SECTION 6 — SHEET 1 (ZIP Change Distribution, D-04/D-05): a single table with ZIP9 and
  ZIP5 side-by-side: rows keyed by distinct-count bucket (1 = never changed, 2, 3, 4+),
  columns n_patients_zip9, pct_zip9, n_patients_zip5, pct_zip5, with a Total row. Sheet 1/2
  show COUNTS-OF-PATIENTS-WITH-N-DISTINCT-ZIPS (never individual ZIP codes), which is
  HIPAA-safe by construction.

SECTION 7 — SHEET 2 (Change Rate Summary + histogram, D-05): headline stats table
  (total patients, % any ZIP9 change, % any ZIP5 change, % ZIP9-change-ONLY [n_zip9>1 AND
  n_zip5==1], median/p25/p75 of n_zip9_distinct and n_zip5_distinct) PLUS a change-count
  histogram (n_changes = n_distinct - 1: 0, 1, 2, 3+). Apply HIPAA suppression: any
  histogram cell with 1-10 patients replaced with "<11" (Pitfall 5b) via a small
  suppress_small() helper (e.g. if_else(n >= 1 & n <= 10, "<11", as.character(n))). Include
  a literal comment tagging the suppression as the project HIPAA rule.

SECTION 8 — SHEET 3 (Time Between Changes, D-07): parse ADDRESS_PERIOD_START via
  parse_pcornet_date; per patient, take the DISTINCT sorted set of start dates (Pitfall 7),
  compute lead(date) - date gaps in days; guard patients with only 1 distinct date (gap =
  NA — document the count). Distribution table: median/p25/p75/min/max gap-days + histogram
  buckets (<30d, 30-180d, 181-365d, 1-2y, >2y). Suppress small bucket cells (<=10).

SECTION 9 — SHEET 4 (Tie-Break Comparison, D-11): for patients with 2+ distinct ZIP9,
  compute most_recent_zip9 (ZIP9 on the record with max ADDRESS_PERIOD_START; break ties by
  ADDRESS_PREFERRED == "Y" WHEN populated) and modal_zip9 (most frequent ZIP9; ties broken
  by recency). Report n_patients_evaluated, n_agree, n_disagree, pct_disagree. FALLBACK: if
  ADDRESS_PREFERRED is <5% populated, use recency alone and record that choice in the sheet
  subtitle/metadata. Do NOT emit the individual ZIP9 values — output only the agree/disagree
  counts (HIPAA).

SECTION 10 — SHEET 5 (Recommendation & Metadata): metadata table (source file basename,
  row count, distinct patient count, run date via Sys.Date(), NA ZIP9 count, NA ZIP5 count,
  ADDRESS_ZIP5 vs derived mismatch count, ADDRESS_PREFERRED fill rate, HL-cohort overlap
  footnote using get_hl_patient_ids() if available — Open Question 4) plus a 2-3 sentence
  written recommendation string keyed off pct_ever_changed and pct_disagree stating whether
  a single ZIP is defensible for SVI (ZIP5) and whether ZIP9 change frequency warrants
  time-varying handling for ADI.

  Write all 5 sheets with add_styled_sheet() (copy the helper + DARK_GRAY/WHITE/DARK_TEXT
  constants VERBATIM from R/100), then wb_save(wb, OUTPUT_XLSX). NO ggplot/ggsave anywhere
  (data output only).

SECTION 11 — CONSOLE SUMMARY (D-09) — MUST print BEFORE wb_save is reached in execution
  order is not required, but the headline stats block must exist: n_patients_total,
  pct_ever_changed_zip9, pct_ever_changed_zip5, pct_zip9_change_only, median_distinct_zip9,
  n_with_na_zip via message(glue(...)).

Keep the file >= 150 lines. Use message(glue(...)) for all logging (not print/cat). Use
`ID` (never PATID). Read the CSV by path (never get_pcornet_table("LDS_ADDRESS_HISTORY")).
  </action>
  <verify>
    <automated>grep -c "normalize_zip9\|normalize_zip5\|quit(status = 0)\|ADDRESS_ZIP9\|ADDRESS_PERIOD_START\|add_styled_sheet\|group_by(ID)\|Normalized\|wb_save" R/106_zip_change_frequency.R</automated>
  </verify>
  <acceptance_criteria>
    - `test -f R/106_zip_change_frequency.R` succeeds (file exists)
    - `grep -c "" R/106_zip_change_frequency.R` returns >= 150 (line count)
    - `grep -q 'source("R/00_config.R")' R/106_zip_change_frequency.R` succeeds
    - `grep -q 'ADDR_FILENAME <- "LDS_ADDRESS_HISTORY_Mailhot_V1.csv"' R/106_zip_change_frequency.R` succeeds
    - `grep -q "file.exists" R/106_zip_change_frequency.R` succeeds AND `grep -q "quit(status = 0)" R/106_zip_change_frequency.R` succeeds
    - `grep -qv "stop(" ...` — no `stop(` used as the missing-file handler (probe uses quit, not stop)
    - `grep -q "ADDRESS_ZIP9" R/106_zip_change_frequency.R` AND `grep -q "ADDRESS_PERIOD_START" R/106_zip_change_frequency.R` succeed
    - `grep -q "normalize_zip9" R/106_zip_change_frequency.R` AND `grep -q "normalize_zip5" R/106_zip_change_frequency.R` succeed
    - `grep -q 'str_remove_all(str_trim' R/106_zip_change_frequency.R` OR `grep -q 'str_remove_all(.*"-")' R/106_zip_change_frequency.R` succeeds (hyphen strip before pad)
    - `grep -q '\^\[0-9\]{9}\$' R/106_zip_change_frequency.R` succeeds (ZIP9 validation regex)
    - `grep -q "group_by(ID)" R/106_zip_change_frequency.R` AND `grep -q "n_distinct" R/106_zip_change_frequency.R` succeed
    - `grep -q "add_styled_sheet" R/106_zip_change_frequency.R` AND `grep -q "wb_save" R/106_zip_change_frequency.R` succeed
    - `grep -q "<11\|<= 10\|<=10" R/106_zip_change_frequency.R` succeeds (HIPAA small-cell suppression)
    - `grep -q "pct_disagree\|n_disagree" R/106_zip_change_frequency.R` succeeds (tie-break, D-11)
    - `grep -q "zip9_change_only\|ZIP9-change-only\|n_zip9 > 1 & n_zip5 == 1\|n_zip9_distinct > 1.*n_zip5_distinct == 1" R/106_zip_change_frequency.R` succeeds (ZIP9-change-only distinction, D-05)
    - `! grep -q "get_pcornet_table(\"LDS_ADDRESS_HISTORY\")" R/106_zip_change_frequency.R` (reads CSV by path, not via dispatcher)
    - `! grep -qE "ggplot|ggsave|geom_" R/106_zip_change_frequency.R` (data output only)
    - `! grep -q "PATID" R/106_zip_change_frequency.R` OR every PATID occurrence is inside a comment (uses ID, not PATID)
    - PowerShell paren-balance check on R/106 returns balanced: open `(` count == close `)` count
  </acceptance_criteria>
  <done>R/106_zip_change_frequency.R exists (>=150 lines), self-bootstraps R/00_config, probes LDS_ADDRESS_HISTORY and quits gracefully when absent, normalizes ZIP9/ZIP5 with hyphen-strip + regex validation, computes per-patient distinct ZIP counts + ever-changed flags grouped by ID, builds the 5-sheet styled xlsx via a verbatim add_styled_sheet helper (ZIP9/ZIP5 side-by-side distribution, change-rate + ZIP9-change-only + histogram, time-between-changes, tie-break disagreement, recommendation/metadata), applies HIPAA <=10 suppression, logs a headline console summary, uses ID throughout, and contains no ggplot. Parens balance.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register R/106 in R/39 and add R/88 Section 15s (14 structural checks + SMOKE-121-01 summary)</name>
  <read_first>
    - R/39_run_all_investigations.R lines 176-200 (investigation_scripts vector; R/105 is the current comma-less final entry at line 195)
    - R/88_smoke_test_comprehensive.R lines 2412-2503 (Section 15r 14-check template + else-branch; Section 15g header immediately after)
    - R/88_smoke_test_comprehensive.R lines 2295-2315 (Section 15p Check 14 IS_LOCAL runtime-gate pattern)
    - R/88_smoke_test_comprehensive.R lines 4068-4077 (SMOKE summary message block + final quit)
    - R/106_zip_change_frequency.R (the file whose text the Section 15s greps must match — created in Task 1)
  </read_first>
  <files>R/39_run_all_investigations.R, R/88_smoke_test_comprehensive.R</files>
  <action>
(A) R/39 vector edit — preserve the "exactly one comma-less final entry" convention:
  - Line 195: add a trailing comma to
      `"R/105_normalize_supportive_care_meaning.R"  # Supportive Care ...`
    so it becomes
      `"R/105_normalize_supportive_care_meaning.R",  # Supportive Care ...`
  - Insert a NEW final comma-less entry immediately after (before the closing `)` on line 196):
      `"R/106_zip_change_frequency.R"                # ZIP change frequency investigation (Phase 121); probes LDS_ADDRESS_HISTORY, read-only`
  Result: the vector has exactly 20 entries and exactly one comma-less entry (R/106).

(B) R/88 Section 15s — insert a new section AFTER Section 15r's closing (after line 2498,
  the blank line following the `else { for (i in 2:14) ... }` block) and BEFORE the
  `# ===` header of Section 15g (currently line 2500). MIRROR the Section 15r 14-check
  structure exactly. Use these EXACT check conditions:

  # ==============================================================================
  # SECTION 15s: ZIP CHANGE FREQUENCY (Phase 121) ----
  # ==============================================================================
  # Phase 121 added R/106_zip_change_frequency.R: a read-only investigation that probes
  # for LDS_ADDRESS_HISTORY and, if present, quantifies per-patient ZIP change frequency
  # at ZIP9 and ZIP5 granularity, writing a 5-sheet styled xlsx. STRUCTURAL greps against
  # the R/106 file text (no HiPerGator data locally); they pass LOCALLY.
  message("\n[Phase 121] ZIP change frequency (R/106)...")

  Check 1:  r106_exists <- file.exists("R/106_zip_change_frequency.R"); check("R/106_zip_change_frequency.R exists (Phase 121)", r106_exists)
  if (r106_exists) {
    r106_lines <- readLines("R/106_zip_change_frequency.R", warn = FALSE)
    r106_text  <- paste(r106_lines, collapse = "\n")
    Check 2:  check("R/106 has 150+ lines (Phase 121)", length(r106_lines) >= 150)
    Check 3:  check("R/106 sources R/00_config.R", grepl("source.*00_config", r106_text))
    Check 4:  check("R/106 probes LDS_ADDRESS_HISTORY via file.exists()", grepl("file\\.exists", r106_text) && grepl("LDS_ADDRESS_HISTORY", r106_text))
    Check 5:  check("R/106 exits gracefully with quit(status = 0), not stop()", grepl("quit\\(status = 0\\)", r106_text))
    Check 6:  check("R/106 reads the ADDRESS_ZIP9 column", grepl("ADDRESS_ZIP9", r106_text))
    Check 7:  check("R/106 reads the ADDRESS_PERIOD_START column (time-between-changes)", grepl("ADDRESS_PERIOD_START", r106_text))
    Check 8:  check("R/106 has normalize_zip9 with hyphen strip", grepl("normalize_zip9", r106_text) && grepl('str_remove_all', r106_text))
    Check 9:  check("R/106 has normalize_zip5 (str_sub 1,5)", grepl("normalize_zip5", r106_text) && grepl("str_sub", r106_text))
    Check 10: check("R/106 validates ZIP9 as 9 digits (^[0-9]{9}$)", grepl("\\^\\[0-9\\]\\{9\\}\\$", r106_text))
    Check 11: check("R/106 computes per-patient distinct ZIP counts (group_by(ID) + n_distinct)", grepl("group_by\\(ID\\)", r106_text) && grepl("n_distinct", r106_text))
    Check 12: check("R/106 writes a styled xlsx (add_styled_sheet + wb_save)", grepl("add_styled_sheet", r106_text) && grepl("wb_save", r106_text))
    Check 13: check("R/106 applies HIPAA small-cell suppression (<=10)", grepl("<11|<= 10|<=10", r106_text))
    Check 14 (RUNTIME, IS_LOCAL-gated — mirror Section 15p lines 2301-2315):
      out_xlsx_121 <- file.path(CONFIG$output_dir, "zip_change_frequency.xlsx")
      if (!IS_LOCAL && file.exists(out_xlsx_121)) {
        check("R/106 output xlsx exists with sheets (Phase 121, HiPerGator)", file.exists(out_xlsx_121))
      } else {
        check("R/106 output xlsx present -- SKIPPED (local / no output) (Phase 121)", TRUE)
      }
  } else {
    for (i in 2:14) check(paste0("R/106 dependent check #", i, " -- SKIPPED (script missing)"), FALSE)
  }

  Note Check 8 uses a different grep than R/105's Check 12 because R/106 has no "Normalized
  Meaning" column — the checks target R/106's ACTUAL content (Rule-3 discipline from prior
  phases: greps must match the file that exists, not a template).

(C) SMOKE-121-01 summary line — add after the SMOKE-120-01 line (currently line 4073):
    message("  * SMOKE-121-01: R/88 validates Phase 121 ZIP change frequency structural integrity (Section 15s, 14 checks)")
  </action>
  <verify>
    <automated>grep -n "R/106_zip_change_frequency.R\|SECTION 15s\|SMOKE-121-01" R/39_run_all_investigations.R R/88_smoke_test_comprehensive.R</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'R/106_zip_change_frequency.R' R/39_run_all_investigations.R` succeeds
    - `grep -q '"R/105_normalize_supportive_care_meaning.R",' R/39_run_all_investigations.R` succeeds (R/105 now has trailing comma)
    - In R/39, the line containing `R/106_zip_change_frequency.R` has NO trailing comma before its comment/closing paren (R/106 is the sole comma-less final entry)
    - `grep -c 'R/1[0-9][0-9]_\|R/[0-9][0-9]_' ...` — investigation_scripts vector contains 20 entries (was 19)
    - `grep -q "SECTION 15s: ZIP CHANGE FREQUENCY (Phase 121)" R/88_smoke_test_comprehensive.R` succeeds
    - `grep -q 'r106_exists <- file.exists("R/106_zip_change_frequency.R")' R/88_smoke_test_comprehensive.R` succeeds
    - `grep -c 'check(' R/88_smoke_test_comprehensive.R` in the Section 15s block region shows 14 check() invocations (Check 1 + 13 dependent + else-branch mirror)
    - `grep -q "quit\\\\(status = 0\\\\)" R/88_smoke_test_comprehensive.R` in Section 15s (Check 5 grep target)
    - `grep -q 'out_xlsx_121 <- file.path(CONFIG$output_dir, "zip_change_frequency.xlsx")' R/88_smoke_test_comprehensive.R` succeeds (Check 14 runtime path)
    - `grep -q "!IS_LOCAL && file.exists(out_xlsx_121)" R/88_smoke_test_comprehensive.R` succeeds (IS_LOCAL gate)
    - `grep -q "SMOKE-121-01" R/88_smoke_test_comprehensive.R` succeeds
    - Section 15s appears BEFORE `SECTION 15g` in R/88 (line number of 15s < line number of 15g)
    - PowerShell paren-balance check on R/88_smoke_test_comprehensive.R returns balanced (open `(` == close `)`)
  </acceptance_criteria>
  <done>R/39 registers R/106 as the sole comma-less final entry with R/105 gaining a trailing comma (20-entry vector, parse-safe). R/88 has a new Section 15s (14 checks mirroring 15r: file-exists gate + 12 structural greps targeting R/106's actual content + an IS_LOCAL-gated runtime xlsx check) inserted between 15r and 15g, plus a SMOKE-121-01 summary line. Parens balance in both files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update R/SCRIPT_INDEX.md (R/106 row + count 6->7 + Total 92->93)</name>
  <read_first>
    - R/SCRIPT_INDEX.md lines 140-152 (Post-Renumber Investigations (100+) table; R/105 is the last row)
    - R/SCRIPT_INDEX.md lines 192-207 (Script Count block: 100+ count line + Total line)
  </read_first>
  <files>R/SCRIPT_INDEX.md</files>
  <action>
(A) Add a new row to the Post-Renumber Investigations (100+) table, immediately after the
  R/105 row (line 151):
    | `R/106_zip_change_frequency.R` | Read-only Phase 121 investigation: probes for LDS_ADDRESS_HISTORY (the only CDM table with a time-varying 9-digit ZIP), and if present quantifies per-patient ZIP change frequency at BOTH ZIP9 and ZIP5 granularity — distinct-ZIP distribution, % ever-changed (incl. ZIP9-change-only), time-between-changes (from ADDRESS_PERIOD_START), and most-recent-vs-modal tie-break disagreement rate. Produces a 5-sheet styled xlsx (`output/zip_change_frequency.xlsx`) + console summary to inform the downstream SES-index (ADI/SVI) ZIP-handling decision. Reads the CSV directly by path (not in PCORNET_TABLES); exits gracefully if absent. | 121 |

(B) Update the Script Count block:
  - Line 204: change the 100+ count from 6 to 7 and append R/106 to the parenthetical:
      `- **Post-renumber investigations (100+):** 7 (R/100 RUCA, R/101 Gantt lifespan, R/102 death-cause NHL flag, R/103 death-cause diagnostic, R/104 Gantt entire-history, R/105 Supportive Care Normalized Meaning, R/106 ZIP change frequency)`
  - Line 207: change `- **Total:** 92` to `- **Total:** 93`
  </action>
  <verify>
    <automated>grep -n "R/106_zip_change_frequency.R\|Post-renumber investigations (100+):\|Total:" R/SCRIPT_INDEX.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'R/106_zip_change_frequency.R' R/SCRIPT_INDEX.md` succeeds
    - `grep -q '| 121 |' R/SCRIPT_INDEX.md` succeeds on the R/106 row (Phase column = 121)
    - `grep -q 'Post-renumber investigations (100+):\*\* 7' R/SCRIPT_INDEX.md` succeeds (count 6->7)
    - `grep -q 'R/106 ZIP change frequency' R/SCRIPT_INDEX.md` succeeds (parenthetical updated)
    - `grep -q '\*\*Total:\*\* 93' R/SCRIPT_INDEX.md` succeeds (Total 92->93)
    - `! grep -q '\*\*Total:\*\* 92' R/SCRIPT_INDEX.md` (old total removed)
  </acceptance_criteria>
  <done>R/SCRIPT_INDEX.md documents R/106 as a Phase 121 row in the Post-Renumber Investigations (100+) table; the 100+ count reads 7 (with R/106 in the parenthetical) and Total reads 93.</done>
</task>

</tasks>

<verification>
## Structural verification (runnable locally on Windows)

All Phase 121 verification is STRUCTURAL and runnable on the Windows executor. Runtime
(actually running R/106 and generating the xlsx) is DEFERRED to HiPerGator because
(a) Rscript is not installed on this Windows executor and (b) the local test fixtures do
NOT contain `LDS_ADDRESS_HISTORY_Mailhot_V1.csv` — so R/106's probe gate would correctly
`quit(status = 0)` locally with no output.

1. **File existence + content greps** — run the `<acceptance_criteria>` grep commands for
   all three tasks. Every one must pass.

2. **Paren balance** (state-machine check on the two R files, since Rscript is unavailable):
   ```powershell
   foreach ($f in "R/106_zip_change_frequency.R","R/88_smoke_test_comprehensive.R") {
     $t = Get-Content $f -Raw
     $open  = ([regex]::Matches($t, "\(")).Count
     $close = ([regex]::Matches($t, "\)")).Count
     "$f  open=$open close=$close  balanced=$($open -eq $close)"
   }
   ```
   Both must report `balanced=True`.

3. **R/39 vector integrity** — exactly ONE comma-less entry (R/106) and 20 total entries;
   R/105 now ends with a comma.

4. **R/88 Section 15s placement** — 15s between 15r and 15g; 14 `check()` calls; else-branch
   registers checks 2-14 FALSE when R/106 is missing (honest total).

## Runtime confirmation (user performs on HiPerGator — NOT a blocker for this phase)

The user runs, on HiPerGator with the real extract:
```
Rscript R/106_zip_change_frequency.R
Rscript R/88_smoke_test_comprehensive.R
```
and pastes back:
- Whether `LDS_ADDRESS_HISTORY_Mailhot_V1.csv` exists at the probed path (Open Q1 — if the
  filename differs, add a PCORNET_PATHS-style override note / update ADDR_FILENAME).
- The headline console stats (cohort size, % ever-changed ZIP9/ZIP5, median distinct ZIP9).
- ADDRESS_ZIP5 fill rate vs ADDRESS_ZIP9 (Open Q2) and ADDRESS_PREFERRED fill rate (Open Q3)
  as logged by R/106 — these drive the ZIP5-sourcing and tie-break-fallback branches.
- Whether Section 15s Check 14 flips from SKIPPED to a real xlsx-present PASS.

These open questions are encoded as graceful runtime branches in R/106 (log-and-adapt),
NOT as planning blockers.
</verification>

<success_criteria>
- R/106_zip_change_frequency.R exists (>=150 lines), is read-only, self-bootstraps
  R/00_config, and mirrors R/103's probe gate + R/100's styled-xlsx output.
- Probe-first gate: file.exists() on the LDS_ADDRESS_HISTORY CSV; quit(status = 0) (not
  stop()) when absent (D-02).
- Per-patient ZIP change frequency computed at BOTH ZIP9 and ZIP5, grouped by ID, with NA
  ZIPs filtered before distinct counts (D-04).
- 5-sheet styled xlsx: distribution (ZIP9/ZIP5 side-by-side), change-rate + ZIP9-change-only
  + histogram, time-between-changes, tie-break disagreement, recommendation/metadata (D-06,
  D-07); console headline summary before write (D-09); tie-break presented without
  committing (D-11); HIPAA <=10 suppression applied.
- R/106 registered in R/39 as the sole comma-less final entry (20-entry parse-safe vector).
- R/88 Section 15s (14 checks) added between 15r and 15g; SMOKE-121-01 summary line added.
- R/SCRIPT_INDEX.md: R/106 row added; 100+ count 6->7; Total 92->93.
- All grep acceptance criteria pass and both edited R files have balanced parentheses.
</success_criteria>

<output>
After completion, create `.planning/phases/121-investigate-how-often-the-9-digit-zip-code-changes-at-the-individual-level-to-inform-the-decision-on-handling-zip-code-data-for-socioeconomic-indices/121-01-SUMMARY.md`
</output>
