---
phase: 95-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/utils/utils_dt.R
  - R/00_config.R
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "User can source R/utils/utils_dt.R and call ensure_dt(), to_tibble_safe(), get_lookup_dt() without errors"
    - "User can access LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP in R console and see keyed data.table with correct rows"
    - "User can access all 6 LOOKUP_TABLES_DT entries and each has setkey() applied"
    - "TREATMENT_CODES is flattened to a long-format 3-column data.table keyed on code"
  artifacts:
    - path: "R/utils/utils_dt.R"
      provides: "Conversion helpers for tibble/data.table boundary management"
      exports: ["ensure_dt", "to_tibble_safe", "get_lookup_dt"]
      min_lines: 80
    - path: "R/00_config.R"
      provides: "LOOKUP_TABLES_DT list with 6 keyed data.tables, library(data.table) call"
      contains: "LOOKUP_TABLES_DT"
  key_links:
    - from: "R/00_config.R"
      to: "R/utils/utils_dt.R"
      via: "auto-source via list.files at line 3428"
      pattern: "list\\.files.*R/utils"
    - from: "R/00_config.R"
      to: "LOOKUP_TABLES_DT"
      via: "named list construction after existing lookup vectors"
      pattern: "LOOKUP_TABLES_DT\\s*<-\\s*list"
---

<objective>
Create data.table conversion helpers and keyed lookup table infrastructure for Phases 96-98.

Purpose: Build the foundational data.table utilities (ensure_dt, to_tibble_safe, get_lookup_dt) and convert all 6 existing lookup tables into keyed data.tables stored in LOOKUP_TABLES_DT. This infrastructure enables Phases 96-98 to use fast keyed joins instead of named vector indexing.

Output: R/utils/utils_dt.R (new file), modified R/00_config.R (library(data.table) + LOOKUP_TABLES_DT list)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-infrastructure-setup/95-CONTEXT.md
@.planning/phases/95-infrastructure-setup/95-RESEARCH.md

@R/utils/utils_assertions.R
@R/00_config.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/utils/utils_dt.R with defensive conversion helpers</name>
  <files>R/utils/utils_dt.R</files>

  <read_first>
    R/utils/utils_assertions.R
    R/00_config.R
  </read_first>

  <action>
Create a new file R/utils/utils_dt.R following the same header documentation pattern as R/utils/utils_assertions.R (Purpose, Inputs, Outputs, Dependencies, Requirements block).

The file must define exactly 3 exported functions per INFRA-02:

**1. ensure_dt(df, name = "input", script_name = "unknown")**
Converts input to data.table defensively, per D-05:
- NULL input: `stop(glue::glue("[{script_name} ERROR] {name} is NULL"))` (immediate stop, matches utils_assertions.R error format)
- Empty input (0 rows): `warning(glue::glue("[{script_name} WARNING] {name} is empty (0 rows)"))` then return `data.table::as.data.table(df)` (empty data.table preserving column structure)
- Already data.table (`data.table::is.data.table(df)` returns TRUE): return `df` as-is (no-op)
- Otherwise: return `data.table::as.data.table(df)` (creates copy, does NOT mutate input -- use as.data.table not setDT per anti-pattern guidance)

**2. to_tibble_safe(dt, name = "output", script_name = "unknown")**
Converts data.table back to tibble for dplyr pipeline compatibility, per D-05:
- NULL input: `stop(glue::glue("[{script_name} ERROR] {name} is NULL"))`
- Empty input (0 rows): `warning(glue::glue("[{script_name} WARNING] {name} is empty (0 rows)"))` then return `tibble::as_tibble(dt)` (empty tibble preserving column structure)
- Already tibble (`tibble::is_tibble(dt)` returns TRUE): return `dt` as-is (no-op)
- Otherwise: return `tibble::as_tibble(dt)`

**3. get_lookup_dt(table_name, lookup_list = LOOKUP_TABLES_DT)**
Retrieves a keyed data.table from the LOOKUP_TABLES_DT list by string name. Claude's discretion: accepts string name.
- `table_name` not a character string: `stop(glue::glue("[get_lookup_dt ERROR] table_name must be character, got {class(table_name)[1]}"))`
- `table_name` not found in `lookup_list`: `stop(glue::glue("[get_lookup_dt ERROR] '{table_name}' not found in lookup list. Available: {paste(names(lookup_list), collapse=', ')}"))`
- Found: return `lookup_list[[table_name]]`

Each function must have roxygen-style documentation comments (#' @param, #' @return, #' @examples) matching the style in utils_assertions.R.

Header comment block must include:
```
# ==============================================================================
# utils/utils_dt.R -- data.table conversion helpers
# ==============================================================================
#
# Purpose:
#   Provides 3 helper functions for safe tibble/data.table boundary management.
#   Used by Phase 96+ functions that operate in data.table internally but return
#   tibbles for dplyr pipeline compatibility. Follows the same defensive coding
#   pattern as utils_assertions.R (checkmate-style error messages with glue).
#
# Inputs:
#   - None (utility function library, not a standalone script)
#
# Outputs:
#   - None (defines functions loaded into calling scripts' environment)
#     - ensure_dt(): Convert input to data.table with NULL/empty guards
#     - to_tibble_safe(): Convert data.table back to tibble with NULL/empty guards
#     - get_lookup_dt(): Retrieve keyed data.table from LOOKUP_TABLES_DT by name
#
# Dependencies:
#   - data.table: is.data.table(), as.data.table() (loaded in R/00_config.R)
#   - tibble: is_tibble(), as_tibble() (loaded via tidyverse in calling scripts)
#   - glue: String interpolation for error messages (loaded in R/00_config.R)
#
# Requirements:
#   - INFRA-02: Conversion helpers for tibble/data.table boundary management
#
# ==============================================================================
```

Do NOT include `library()` calls in the file -- data.table and glue are loaded by R/00_config.R before utils_dt.R is auto-sourced at line 3428.
  </action>

  <verify>
    <automated>grep -c "ensure_dt\|to_tibble_safe\|get_lookup_dt" R/utils/utils_dt.R</automated>
  </verify>

  <acceptance_criteria>
    - R/utils/utils_dt.R exists as a new file
    - File contains `ensure_dt <- function(df, name = "input", script_name = "unknown")`
    - File contains `to_tibble_safe <- function(dt, name = "output", script_name = "unknown")`
    - File contains `get_lookup_dt <- function(table_name, lookup_list = LOOKUP_TABLES_DT)`
    - ensure_dt has NULL check with `stop(` and empty check with `warning(`
    - to_tibble_safe has NULL check with `stop(` and empty check with `warning(`
    - get_lookup_dt validates table_name is character and exists in lookup_list
    - File has `data.table::is.data.table` and `data.table::as.data.table` (explicit namespace per D-02)
    - File has `tibble::is_tibble` and `tibble::as_tibble` (explicit namespace)
    - File does NOT contain any `library(` calls
    - File header matches utils_assertions.R documentation pattern (Purpose, Inputs, Outputs, Dependencies, Requirements)
    - File has roxygen-style #' comments for each function
  </acceptance_criteria>

  <done>R/utils/utils_dt.R exists with 3 exported functions (ensure_dt, to_tibble_safe, get_lookup_dt), each with defensive error handling per D-05, explicit namespace prefixes per D-02, and documentation matching utils_assertions.R style.</done>
</task>

<task type="auto">
  <name>Task 2: Add library(data.table) and LOOKUP_TABLES_DT to R/00_config.R</name>
  <files>R/00_config.R</files>

  <read_first>
    R/00_config.R
    R/utils/utils_dt.R
    .planning/phases/95-infrastructure-setup/95-CONTEXT.md
  </read_first>

  <action>
Modify R/00_config.R with two additions:

**Addition 1: Load data.table library (per D-01)**

Insert `library(data.table)` immediately AFTER the existing `library(checkmate)` at line 3414, in a new section:

```r
# ==============================================================================
# SECTION 7c: DATA.TABLE LIBRARY (Phase 95, v3.0) ----
# ==============================================================================
# WHY load globally: Per D-01, data.table is loaded once in R/00_config.R so it's
# available to all scripts. Phase 96+ functions use data.table for keyed joins
# and fast aggregation. Coexists with dplyr -- conflict-prone functions (between,
# first, last, transpose) use explicit package::function() per D-02.
library(data.table)
```

This must appear BEFORE Section 8 (auto-source utility functions) so that data.table is available when utils_dt.R is sourced.

**Addition 2: Build LOOKUP_TABLES_DT list (per INFRA-03, D-03, D-04)**

Insert a new section AFTER all existing lookup table definitions (after the PROVIDER_SPECIALTIES block that ends around line 3405) and AFTER the `library(data.table)` call, but BEFORE Section 8 (auto-source utility functions). This ensures data.table is loaded and all source named vectors exist.

The new section:

```r
# ==============================================================================
# SECTION 7d: KEYED DATA.TABLE LOOKUP TABLES (Phase 95, v3.0) ----
# ==============================================================================
# Converts existing named vectors/lists to keyed data.tables for fast binary-search
# joins in Phases 96-98. Original named vectors are preserved (no behavior change).
# Each data.table is setkey()-ed on its lookup column for O(log n) joins.
#
# Column naming follows D-03: semantic, table-specific names that self-document
# in join syntax. Example: LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP[.("219"), payer_category]
#
# WHY a list: Single access point for all keyed lookups; get_lookup_dt() retrieves
# by name. Downstream code: LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP[.(code), payer_category]

LOOKUP_TABLES_DT <- list(

  # --- AMC_PAYER_LOOKUP: code -> payer_category (per D-03) ---
  AMC_PAYER_LOOKUP = {
    dt <- data.table(
      code = names(AMC_PAYER_LOOKUP),
      payer_category = unname(AMC_PAYER_LOOKUP)
    )
    setkey(dt, code)
    dt
  },

  # --- DRUG_GROUPINGS: code -> drug_group (per D-03) ---
  DRUG_GROUPINGS = {
    dt <- data.table(
      code = names(DRUG_GROUPINGS),
      drug_group = unname(DRUG_GROUPINGS)
    )
    setkey(dt, code)
    dt
  },

  # --- CODE_SUBCATEGORY_MAP: code -> subcategory (per D-03) ---
  CODE_SUBCATEGORY_MAP = {
    dt <- data.table(
      code = names(CODE_SUBCATEGORY_MAP),
      subcategory = unname(CODE_SUBCATEGORY_MAP)
    )
    setkey(dt, code)
    dt
  },

  # --- CANCER_SITE_MAP: prefix -> cancer_site (per D-03) ---
  CANCER_SITE_MAP = {
    dt <- data.table(
      prefix = names(CANCER_SITE_MAP),
      cancer_site = unname(CANCER_SITE_MAP)
    )
    setkey(dt, prefix)
    dt
  },

  # --- TIER_MAPPING: payer_category -> tier (per D-03) ---
  # Source is a list (not named vector), so conversion differs slightly
  TIER_MAPPING = {
    dt <- data.table(
      payer_category = names(TIER_MAPPING),
      tier = as.integer(unlist(TIER_MAPPING))
    )
    setkey(dt, payer_category)
    dt
  },

  # --- TREATMENT_CODES: code / code_system / treatment_type (per D-04) ---
  # Flattened from nested list. Each element name is "treatmenttype_codesystem"
  # (e.g., "chemo_hcpcs", "radiation_cpt", "sct_icd10pcs").
  # Parse name to extract treatment_type and code_system.
  TREATMENT_CODES = {
    rows <- vector("list", length(TREATMENT_CODES))
    for (i in seq_along(TREATMENT_CODES)) {
      element_name <- names(TREATMENT_CODES)[i]
      codes <- TREATMENT_CODES[[i]]

      # Split name on first underscore to get treatment_type and code_system
      # e.g., "chemo_hcpcs" -> treatment_type="chemo", code_system="hcpcs"
      # e.g., "chemo_icd10pcs_prefixes" -> treatment_type="chemo", code_system="icd10pcs_prefixes"
      # e.g., "immunotherapy_dx_icd10" -> treatment_type="immunotherapy", code_system="dx_icd10"
      first_underscore <- regexpr("_", element_name)
      treatment_type <- substr(element_name, 1, first_underscore - 1)
      code_system <- substr(element_name, first_underscore + 1, nchar(element_name))

      rows[[i]] <- data.table(
        code = codes,
        code_system = code_system,
        treatment_type = treatment_type
      )
    }
    dt <- rbindlist(rows)
    setkey(dt, code)
    dt
  }
)

# Sanity check: verify all 6 lookup tables are present and keyed
stopifnot(
  length(LOOKUP_TABLES_DT) == 6,
  all(c("AMC_PAYER_LOOKUP", "DRUG_GROUPINGS", "CODE_SUBCATEGORY_MAP",
        "CANCER_SITE_MAP", "TIER_MAPPING", "TREATMENT_CODES") %in% names(LOOKUP_TABLES_DT))
)

message(sprintf(
  "Built %d keyed data.table lookups: %s",
  length(LOOKUP_TABLES_DT),
  paste(names(LOOKUP_TABLES_DT), collapse = ", ")
))
```

**IMPORTANT placement order in R/00_config.R:**
1. All existing named vectors/lists (AMC_PAYER_LOOKUP, CANCER_SITE_MAP, etc.) -- already exist, no changes
2. library(checkmate) -- already exists at line 3414
3. library(data.table) -- NEW, insert after checkmate
4. LOOKUP_TABLES_DT construction -- NEW, insert after data.table library load
5. Section 8: Auto-source utility functions -- already exists at line 3417, moves down

The auto-source section (list.files + lapply source) must remain LAST so that utils_dt.R can reference LOOKUP_TABLES_DT (used as default argument in get_lookup_dt).

Do NOT modify any existing named vectors (AMC_PAYER_LOOKUP, DRUG_GROUPINGS, etc.) -- they must remain unchanged for zero behavior change (INFRA-04).
  </action>

  <verify>
    <automated>grep -c "LOOKUP_TABLES_DT\|library(data.table)" R/00_config.R</automated>
  </verify>

  <acceptance_criteria>
    - R/00_config.R contains `library(data.table)` after `library(checkmate)` and before Section 8
    - R/00_config.R contains `LOOKUP_TABLES_DT <- list(`
    - LOOKUP_TABLES_DT has exactly 6 entries: AMC_PAYER_LOOKUP, DRUG_GROUPINGS, CODE_SUBCATEGORY_MAP, CANCER_SITE_MAP, TIER_MAPPING, TREATMENT_CODES
    - AMC_PAYER_LOOKUP data.table has columns `code` and `payer_category` with `setkey(dt, code)`
    - DRUG_GROUPINGS data.table has columns `code` and `drug_group` with `setkey(dt, code)`
    - CODE_SUBCATEGORY_MAP data.table has columns `code` and `subcategory` with `setkey(dt, code)`
    - CANCER_SITE_MAP data.table has columns `prefix` and `cancer_site` with `setkey(dt, prefix)`
    - TIER_MAPPING data.table has columns `payer_category` and `tier` with `setkey(dt, payer_category)`
    - TREATMENT_CODES data.table has columns `code`, `code_system`, `treatment_type` with `setkey(dt, code)` (per D-04)
    - TREATMENT_CODES uses `rbindlist(rows)` for flattening (per research: don't hand-roll with rbind)
    - `stopifnot(length(LOOKUP_TABLES_DT) == 6` sanity check is present
    - Original named vectors (AMC_PAYER_LOOKUP, DRUG_GROUPINGS, etc.) are NOT modified
    - Auto-source Section 8 (list.files) remains the LAST section in the file
    - No `library(dplyr)` call added (dplyr is loaded by downstream scripts, not config)
  </acceptance_criteria>

  <done>R/00_config.R loads data.table globally per D-01, builds LOOKUP_TABLES_DT with 6 keyed data.tables using semantic column names per D-03, flattens TREATMENT_CODES per D-04, and preserves all existing code unchanged for INFRA-04 compatibility.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "ensure_dt\|to_tibble_safe\|get_lookup_dt" R/utils/utils_dt.R` returns 3+ (function definitions present)
2. `grep "LOOKUP_TABLES_DT" R/00_config.R` shows list construction with 6 entries
3. `grep "library(data.table)" R/00_config.R` shows data.table is loaded globally
4. `grep "setkey" R/00_config.R` shows 6 setkey() calls (one per lookup table)
5. No existing named vectors (AMC_PAYER_LOOKUP, DRUG_GROUPINGS, etc.) are modified (diff shows only additions)
</verification>

<success_criteria>
- R/utils/utils_dt.R exists with 3 functions: ensure_dt(), to_tibble_safe(), get_lookup_dt()
- R/00_config.R loads library(data.table) and defines LOOKUP_TABLES_DT with 6 keyed data.tables
- All column names match D-03 specifications (code/payer_category, code/drug_group, etc.)
- TREATMENT_CODES is flattened to 3-column long format per D-04
- No existing code is modified (additions only)
</success_criteria>

<output>
After completion, create `.planning/phases/95-infrastructure-setup/95-01-SUMMARY.md`
</output>
