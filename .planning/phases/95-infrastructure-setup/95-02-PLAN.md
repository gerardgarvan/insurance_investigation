---
phase: 95-infrastructure-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - R/95_validate_dt_infrastructure.R
autonomous: false
requirements:
  - INFRA-01
  - INFRA-04

must_haves:
  truths:
    - "User can run renv::status() and see data.table 1.18.4+ installed"
    - "User can source R/00_config.R and see all 6 LOOKUP_TABLES_DT entries built without errors"
    - "User can run existing R/60_tiered_same_day_payer.R unchanged and outputs match pre-Phase-95 baseline"
    - "User can verify zero namespace conflicts between data.table and dplyr"
  artifacts:
    - path: "R/95_validate_dt_infrastructure.R"
      provides: "Validation script that checks all INFRA requirements"
      min_lines: 60
  key_links:
    - from: "R/95_validate_dt_infrastructure.R"
      to: "R/00_config.R"
      via: "source() to load config including LOOKUP_TABLES_DT"
      pattern: 'source.*00_config'
    - from: "R/95_validate_dt_infrastructure.R"
      to: "R/utils/utils_dt.R"
      via: "auto-sourced via R/00_config.R Section 8"
      pattern: "ensure_dt|to_tibble_safe|get_lookup_dt"
---

<objective>
Create renv setup instructions and a validation script that proves all Phase 95 infrastructure works correctly with zero behavior change.

Purpose: INFRA-01 requires data.table as a project dependency (renv), and INFRA-04 requires proving existing scripts run unchanged. This plan creates a validation script the user can run to confirm everything works, plus renv setup guidance.

Output: R/95_validate_dt_infrastructure.R (validation script), renv setup as checkpoint
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-infrastructure-setup/95-CONTEXT.md
@.planning/phases/95-infrastructure-setup/95-RESEARCH.md
@.planning/phases/95-infrastructure-setup/95-01-SUMMARY.md

@R/00_config.R
@R/utils/utils_dt.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/95_validate_dt_infrastructure.R validation script</name>
  <files>R/95_validate_dt_infrastructure.R</files>

  <read_first>
    R/00_config.R
    R/utils/utils_dt.R
    R/utils/utils_assertions.R
  </read_first>

  <action>
Create R/95_validate_dt_infrastructure.R -- a standalone validation script that the user runs once to confirm all Phase 95 infrastructure is correct. The script must source R/00_config.R and then run a series of checks, printing PASS/FAIL for each.

**Header block** (matching existing script documentation pattern):
```r
# ==============================================================================
# 95_validate_dt_infrastructure.R -- Phase 95 infrastructure validation
# ==============================================================================
#
# Purpose:
#   One-time validation script confirming data.table infrastructure is correctly
#   installed and all LOOKUP_TABLES_DT entries are keyed and contain expected data.
#   Run after Phase 95 implementation to verify INFRA-01 through INFRA-04.
#
# Usage:
#   source("R/95_validate_dt_infrastructure.R")
#
# Expected output:
#   Series of [PASS] / [FAIL] messages. All must show [PASS].
#
# Requirements:
#   - INFRA-01: data.table 1.18.4+ installed
#   - INFRA-02: utils_dt.R functions available
#   - INFRA-03: LOOKUP_TABLES_DT has 6 keyed data.tables
#   - INFRA-04: Zero behavior change (existing objects intact)
#
# ==============================================================================
```

**Script body:**

```r
source("R/00_config.R")

pass_count <- 0L
fail_count <- 0L

check <- function(description, condition) {
  if (isTRUE(condition)) {
    message(sprintf("[PASS] %s", description))
    pass_count <<- pass_count + 1L
  } else {
    message(sprintf("[FAIL] %s", description))
    fail_count <<- fail_count + 1L
  }
}
```

Then run the following checks:

**INFRA-01 checks:**
- `check("data.table is installed", requireNamespace("data.table", quietly = TRUE))`
- `check("data.table version >= 1.18.4", packageVersion("data.table") >= "1.18.4")`
- `check("data.table is loaded (library call in 00_config.R)", "data.table" %in% .packages())`

**INFRA-02 checks:**
- `check("ensure_dt() exists", exists("ensure_dt") && is.function(ensure_dt))`
- `check("to_tibble_safe() exists", exists("to_tibble_safe") && is.function(to_tibble_safe))`
- `check("get_lookup_dt() exists", exists("get_lookup_dt") && is.function(get_lookup_dt))`
- `check("ensure_dt() converts tibble to data.table", data.table::is.data.table(ensure_dt(tibble::tibble(x = 1:3))))`
- `check("ensure_dt() no-op on data.table", data.table::is.data.table(ensure_dt(data.table::data.table(x = 1:3))))`
- `check("ensure_dt() errors on NULL", tryCatch({ensure_dt(NULL); FALSE}, error = function(e) TRUE))`
- `check("ensure_dt() warns on empty", length(tryCatch(ensure_dt(tibble::tibble(x = integer(0))), warning = function(w) w)) > 0)`
- `check("to_tibble_safe() converts data.table to tibble", tibble::is_tibble(to_tibble_safe(data.table::data.table(x = 1:3))))`
- `check("to_tibble_safe() errors on NULL", tryCatch({to_tibble_safe(NULL); FALSE}, error = function(e) TRUE))`
- `check("get_lookup_dt() retrieves AMC_PAYER_LOOKUP", data.table::is.data.table(get_lookup_dt("AMC_PAYER_LOOKUP")))`
- `check("get_lookup_dt() errors on bad name", tryCatch({get_lookup_dt("NONEXISTENT"); FALSE}, error = function(e) TRUE))`

**INFRA-03 checks:**
- `check("LOOKUP_TABLES_DT exists", exists("LOOKUP_TABLES_DT") && is.list(LOOKUP_TABLES_DT))`
- `check("LOOKUP_TABLES_DT has 6 entries", length(LOOKUP_TABLES_DT) == 6)`
- `check("All 6 names present", all(c("AMC_PAYER_LOOKUP", "DRUG_GROUPINGS", "CODE_SUBCATEGORY_MAP", "CANCER_SITE_MAP", "TIER_MAPPING", "TREATMENT_CODES") %in% names(LOOKUP_TABLES_DT)))`

For each of the 6 tables, check:
- Is data.table: `check("AMC_PAYER_LOOKUP is data.table", data.table::is.data.table(LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP))`
- Has key: `check("AMC_PAYER_LOOKUP has key", length(data.table::key(LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP)) > 0)`
- Has expected columns:
  - AMC_PAYER_LOOKUP: `check("AMC_PAYER_LOOKUP cols: code, payer_category", all(c("code", "payer_category") %in% names(LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP)))`
  - DRUG_GROUPINGS: `check("DRUG_GROUPINGS cols: code, drug_group", all(c("code", "drug_group") %in% names(LOOKUP_TABLES_DT$DRUG_GROUPINGS)))`
  - CODE_SUBCATEGORY_MAP: `check("CODE_SUBCATEGORY_MAP cols: code, subcategory", all(c("code", "subcategory") %in% names(LOOKUP_TABLES_DT$CODE_SUBCATEGORY_MAP)))`
  - CANCER_SITE_MAP: `check("CANCER_SITE_MAP cols: prefix, cancer_site", all(c("prefix", "cancer_site") %in% names(LOOKUP_TABLES_DT$CANCER_SITE_MAP)))`
  - TIER_MAPPING: `check("TIER_MAPPING cols: payer_category, tier", all(c("payer_category", "tier") %in% names(LOOKUP_TABLES_DT$TIER_MAPPING)))`
  - TREATMENT_CODES: `check("TREATMENT_CODES cols: code, code_system, treatment_type", all(c("code", "code_system", "treatment_type") %in% names(LOOKUP_TABLES_DT$TREATMENT_CODES)))`
- Row count sanity (verify conversion preserved data):
  - `check("AMC_PAYER_LOOKUP row count matches named vector", nrow(LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP) == length(AMC_PAYER_LOOKUP))`
  - `check("DRUG_GROUPINGS row count matches named vector", nrow(LOOKUP_TABLES_DT$DRUG_GROUPINGS) == length(DRUG_GROUPINGS))`
  - `check("CODE_SUBCATEGORY_MAP row count matches named vector", nrow(LOOKUP_TABLES_DT$CODE_SUBCATEGORY_MAP) == length(CODE_SUBCATEGORY_MAP))`
  - `check("CANCER_SITE_MAP row count matches named vector", nrow(LOOKUP_TABLES_DT$CANCER_SITE_MAP) == length(CANCER_SITE_MAP))`
  - `check("TIER_MAPPING row count matches list", nrow(LOOKUP_TABLES_DT$TIER_MAPPING) == length(TIER_MAPPING))`
  - `check("TREATMENT_CODES has rows (flattened)", nrow(LOOKUP_TABLES_DT$TREATMENT_CODES) > 0)`

**INFRA-04 checks (zero behavior change):**
- `check("Original AMC_PAYER_LOOKUP still exists as named vector", is.character(AMC_PAYER_LOOKUP) && !is.null(names(AMC_PAYER_LOOKUP)))`
- `check("Original DRUG_GROUPINGS still exists as named vector", is.character(DRUG_GROUPINGS) && !is.null(names(DRUG_GROUPINGS)))`
- `check("Original TIER_MAPPING still exists as list", is.list(TIER_MAPPING) && !data.table::is.data.table(TIER_MAPPING))`
- `check("Original TREATMENT_CODES still exists as list", is.list(TREATMENT_CODES) && !data.table::is.data.table(TREATMENT_CODES))`
- `check("Lookup value preserved: AMC_PAYER_LOOKUP['219'] == 'Medicaid'", AMC_PAYER_LOOKUP["219"] == "Medicaid")`
- `check("Keyed join matches: LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP[.('219'), payer_category] == 'Medicaid'", LOOKUP_TABLES_DT$AMC_PAYER_LOOKUP[.("219"), payer_category] == "Medicaid")`

**Namespace conflict check:**
- `check("dplyr::between accessible", is.function(dplyr::between))`
- `check("data.table::between accessible", is.function(data.table::between))`

**Summary at end:**
```r
message("")
message(sprintf("========================================"))
message(sprintf("Phase 95 Validation: %d PASS, %d FAIL", pass_count, fail_count))
if (fail_count == 0) {
  message("All checks passed -- infrastructure ready for Phase 96")
} else {
  message(sprintf("WARNING: %d check(s) failed -- review output above", fail_count))
}
message(sprintf("========================================"))
```
  </action>

  <verify>
    <automated>grep -c "check(" R/95_validate_dt_infrastructure.R</automated>
  </verify>

  <acceptance_criteria>
    - R/95_validate_dt_infrastructure.R exists
    - File begins with `source("R/00_config.R")`
    - File contains check() helper function with pass_count/fail_count counters
    - File has INFRA-01 checks: data.table installed, version >= 1.18.4, loaded in session
    - File has INFRA-02 checks: ensure_dt, to_tibble_safe, get_lookup_dt exist and behave correctly
    - File has INFRA-03 checks: LOOKUP_TABLES_DT has 6 entries, each is keyed data.table with correct columns
    - File has INFRA-04 checks: original named vectors still exist unchanged, lookup values preserved
    - File has namespace conflict check for dplyr::between vs data.table::between
    - File prints summary with pass/fail counts at end
    - File has header documentation block matching project script pattern
  </acceptance_criteria>

  <done>R/95_validate_dt_infrastructure.R exists as a runnable validation script covering all 4 INFRA requirements with individual PASS/FAIL checks and a summary count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 95 infrastructure via validation script and R/60 regression test</name>
  <files>R/95_validate_dt_infrastructure.R</files>
  <action>
User must install data.table and run the validation script. This is a human checkpoint because:
1. data.table must be installed via R console (INFRA-01): `install.packages("data.table")`
2. The validation script must be run in the user's R environment to prove INFRA-01 through INFRA-04
3. R/60_tiered_same_day_payer.R must be re-run to confirm zero behavior change (INFRA-04)
  </action>
  <what-built>
Phase 95 data.table infrastructure: utils_dt.R (3 conversion helpers), LOOKUP_TABLES_DT in R/00_config.R (6 keyed data.tables), library(data.table) loaded globally, and a validation script R/95_validate_dt_infrastructure.R.

Before running validation, data.table must be installed:
1. Open R console (RStudio or `R` in terminal)
2. Run: `install.packages("data.table")`
3. Verify: `packageVersion("data.table")` shows 1.18.4 or higher
  </what-built>
  <how-to-verify>
    1. In RStudio or R console, run: `source("R/95_validate_dt_infrastructure.R")`
    2. Check that ALL lines show `[PASS]` -- zero `[FAIL]` lines
    3. Final summary should show "All checks passed -- infrastructure ready for Phase 96"
    4. Verify existing pipeline still works: `source("R/60_tiered_same_day_payer.R")` completes without errors (outputs match pre-Phase-95 if you have baseline CSVs to diff)
  </how-to-verify>
  <verify>User confirms all [PASS] and R/60 runs unchanged</verify>
  <done>All validation checks pass, R/60 produces identical output, user types "approved"</done>
  <resume-signal>Type "approved" if all validation checks pass and R/60 runs unchanged, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. R/95_validate_dt_infrastructure.R exists and contains 30+ check() calls
2. User has confirmed all checks pass by running the validation script
3. R/60_tiered_same_day_payer.R runs unchanged (INFRA-04)
4. data.table is installed as project dependency (INFRA-01)
</verification>

<success_criteria>
- data.table 1.18.4+ is installed and available (INFRA-01)
- R/95_validate_dt_infrastructure.R runs with all PASS, zero FAIL (covers INFRA-01 through INFRA-04)
- Existing R/60 script produces identical output to pre-Phase-95 baseline (INFRA-04)
- User has approved the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/95-infrastructure-setup/95-02-SUMMARY.md`
</output>
