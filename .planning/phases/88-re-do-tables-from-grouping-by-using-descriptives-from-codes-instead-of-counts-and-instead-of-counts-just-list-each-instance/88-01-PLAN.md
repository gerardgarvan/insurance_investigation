---
phase: 88-instance-level-tables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/57_drug_grouping_instances.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - P88-D01
  - P88-D02
  - P88-D03
  - P88-D04
  - P88-D05
  - P88-D06
  - P88-D07
  - P88-D08
  - P88-SMOKE

must_haves:
  truths:
    - "R/57 creates a new xlsx file (drug_grouping_instances.xlsx) without modifying drug_grouping_tables.xlsx"
    - "Table 1 (Sheet 1) shows one row per patient+treatment_type+episode with sub-category names instead of raw codes"
    - "Table 2 (Sheet 2) shows one row per patient+treatment_type+episode with all treatment codes for that episode"
    - "Cancer codes are replaced by human-readable cancer site category names sorted in descending order"
    - "R/88 smoke test validates R/57 script structure and output patterns"
  artifacts:
    - path: "R/57_drug_grouping_instances.R"
      provides: "Instance-level drug grouping tables with descriptive names"
      min_lines: 200
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Smoke test section for R/57 validation"
      contains: "57_drug_grouping_instances"
  key_links:
    - from: "R/57_drug_grouping_instances.R"
      to: "R/00_config.R"
      via: "source() for CANCER_SITE_MAP, ICD9_CANCER_SITE_MAP, CODE_SUBCATEGORY_MAP, DRUG_GROUPINGS"
      pattern: 'source\("R/00_config\.R"\)'
    - from: "R/57_drug_grouping_instances.R"
      to: "cache/outputs/treatment_episodes.rds"
      via: "readRDS() for episode data"
      pattern: "treatment_episodes\\.rds"
    - from: "R/57_drug_grouping_instances.R"
      to: "output/drug_grouping_instances.xlsx"
      via: "openxlsx2 wb_workbook() save"
      pattern: "drug_grouping_instances\\.xlsx"
    - from: "R/57_drug_grouping_instances.R"
      to: "DuckDB DIAGNOSIS table"
      via: "get_pcornet_table() for cancer ICD codes per encounter"
      pattern: "get_pcornet_table.*DIAGNOSIS"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/57_drug_grouping_instances.R"
      via: "readLines() + grep validation"
      pattern: "57_drug_grouping_instances"
---

<objective>
Create R/57_drug_grouping_instances.R -- a new script that produces an instance-level xlsx file (drug_grouping_instances.xlsx) showing one row per patient+treatment_type+episode with human-readable descriptive names instead of raw codes and aggregated counts. Update R/88 smoke test to validate the new script.

Purpose: The existing R/56 drug_grouping_tables.xlsx shows aggregated counts by sub-category. The user needs to trace individual patient episodes with readable labels (drug names, cancer site categories) for clinical review. This phase creates a companion output file without modifying the existing one.

Output: R/57_drug_grouping_instances.R script + updated R/88_smoke_test_comprehensive.R with Section 31 for R/57 validation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-re-do-tables-from-grouping-by-using-descriptives-from-codes-instead-of-counts-and-instead-of-counts-just-list-each-instance/88-CONTEXT.md
@.planning/phases/88-re-do-tables-from-grouping-by-using-descriptives-from-codes-instead-of-counts-and-instead-of-counts-just-list-each-instance/88-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/28_episode_classification.R (treatment_episodes.rds columns, line 498-505):
```r
# Final column order (17 columns):
# patient_id, treatment_type, episode_number, episode_start, episode_stop,
# episode_length_days, distinct_dates_in_episode, historical_flag,
# triggering_codes, encounter_ids, drug_names,
# cancer_category, cancer_link_method, is_hodgkin, regimen_label,
# triggering_code_description, drug_group
```

From R/56_new_tables_from_groupings.R (Section 2 validation, line 111-116):
```r
assert_df_valid(
  episodes,
  name = "treatment_episodes",
  required_cols = c("patient_id", "treatment_type", "triggering_codes", "cancer_category", "encounter_ids"),
  script_name = "R/56"
)
```

From R/56 Section 3 (sub-category mapping from reference xlsx, lines 130-150):
```r
# Chemo: code -> medication name (column C, "Medication")
chemo_map <- setNames(as.character(chemo_sheet[[3]]), as.character(chemo_sheet[[1]]))
# Radiation: code -> type (column G: IMRT, Other radiation, Proton Therapy)
rad_map <- setNames(as.character(rad_sheet[[7]]), as.character(rad_sheet[[1]]))
# SCT: code -> type (column G: Allogeneic, Autologous, etc.)
sct_map <- setNames(as.character(sct_sheet[[7]]), as.character(sct_sheet[[1]]))
# Combined lookup: code -> sub-category label
code_to_subcategory <- c(chemo_map, rad_map, sct_map)
```

From R/56 Section 4 (cancer code extraction via DuckDB, lines 172-219):
```r
# Split encounter_ids -> individual ENCOUNTERIDs -> join DIAGNOSIS -> filter is_cancer_code() -> aggregate per episode
# Result: episode_dx with cancer_codes column (semicolon-separated ICD codes per episode)
```

From R/56 Section 5 (3-tier sub-category resolution, lines 322-369):
```r
sub_category = case_when(
  # Tier 1: xlsx reference sub-categories (most authoritative)
  treatment_code %in% names(code_to_subcategory) ~ code_to_subcategory[treatment_code],
  # Tier 2: CODE_SUBCATEGORY_MAP supplement
  treatment_code %in% names(CODE_SUBCATEGORY_MAP) ~ CODE_SUBCATEGORY_MAP[treatment_code],
  # Tier 3: Code-type fallback labels
  ...
)
```

From R/88 (smoke test section numbering, line 1310):
```r
# Latest section: SECTION 30 (Phase 87)
# Next available: SECTION 31
# Counter: [30/30] -- needs update to [30/31] or renumber
```

From R/00_config.R (available maps):
```r
CANCER_SITE_MAP     # Named vector: 3-4 char ICD-10 prefix -> category name (200+ entries)
ICD9_CANCER_SITE_MAP # Named vector: 3-4 char ICD-9 prefix -> category name (70+ entries)
CODE_SUBCATEGORY_MAP # Named vector: treatment code -> sub-category name (326+ entries)
DRUG_GROUPINGS       # Named vector: treatment code -> category name (454+ entries)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R/57_drug_grouping_instances.R with instance-level tables</name>
  <files>R/57_drug_grouping_instances.R</files>
  <read_first>
    - R/56_new_tables_from_groupings.R (source of patterns for sub-category resolution, cancer code extraction, xlsx output)
    - R/00_config.R (CANCER_SITE_MAP, ICD9_CANCER_SITE_MAP, CODE_SUBCATEGORY_MAP, DRUG_GROUPINGS structure)
    - R/28_episode_classification.R lines 495-512 (treatment_episodes.rds column set)
    - R/utils/utils_cancer.R (is_cancer_code(), classify_codes() signatures)
    - R/utils/utils_assertions.R (assert_rds_exists, assert_df_valid signatures)
    - R/utils/utils_duckdb.R (open_pcornet_con, get_pcornet_table signatures)
    - data/reference/all_codes_resolved_next_tables_v2.1.xlsx (sub-category mapping source)
  </read_first>
  <action>
Create new file R/57_drug_grouping_instances.R following R/56's established patterns. The script has 7 sections:

**SECTION 1: SETUP AND CONFIGURATION**
- Documentation header following R/56 pattern with Purpose, Inputs, Outputs, Dependencies, Requirements, Decision Traceability sections
- Purpose: "Generate instance-level drug grouping tables showing one row per patient+treatment type+episode with human-readable sub-category names and cancer site category names instead of raw codes and aggregated counts."
- Inputs: cache/outputs/treatment_episodes.rds, data/reference/all_codes_resolved_next_tables_v2.1.xlsx, DuckDB DIAGNOSIS table, R/00_config.R
- Outputs: output/drug_grouping_instances.xlsx (2-sheet workbook)
- Decision traceability: D-01 through D-08 from Phase 88 CONTEXT.md
- Libraries: dplyr, tidyr, glue, stringr, openxlsx2, checkmate (same as R/56)
- Source: R/00_config.R, R/utils/utils_assertions.R, R/utils/utils_duckdb.R, R/utils/utils_cancer.R
- Define paths:
  - EPISODES_RDS <- file.path(CONFIG$cache$outputs_dir, "treatment_episodes.rds")
  - REFERENCE_XLSX <- "data/reference/all_codes_resolved_next_tables_v2.1.xlsx"
  - OUTPUT_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_instances.xlsx")
- Log file: file.path(CONFIG$output_dir, "57_drug_grouping_instances.log")
- Console log handler (same pattern as R/56 lines 83-94)
- Startup banner: "=== Phase 88: Drug Grouping Instance-Level Tables ==="

**SECTION 2: LOAD AND VALIDATE INPUT DATA**
- assert_rds_exists(EPISODES_RDS, script_name = "R/57")
- episodes <- readRDS(EPISODES_RDS)
- assert_df_valid with required_cols: c("patient_id", "treatment_type", "episode_number", "episode_start", "episode_stop", "triggering_codes", "encounter_ids", "cancer_category")
- Log episode count and treatment types

**SECTION 3: BUILD SUB-CATEGORY MAPPINGS FROM REFERENCE XLSX**
- Replicate R/56 Section 3 exactly (lines 122-160): load reference xlsx, build chemo_map (col 3), rad_map (col 7), sct_map (col 7), combined code_to_subcategory
- Also build valid_reference_codes set (same as R/56 lines 152-160)

**SECTION 4: EXTRACT CANCER CODES AND MAP TO CATEGORY NAMES**
- Replicate R/56 Section 4 DuckDB encounter linkage (lines 172-242): split encounter_ids -> join DIAGNOSIS -> filter is_cancer_code() -> aggregate per episode
- This produces episode_dx with cancer_codes column (semicolon-separated raw ICD codes per episode)
- THEN add the new mapping step (per D-04): define helper function map_cancer_codes_to_categories(cancer_codes_str) that:
  1. Splits semicolon-separated cancer_codes string
  2. For each individual code, applies 4-tier cascade: substr(code,1,4) in CANCER_SITE_MAP -> substr(code,1,3) in CANCER_SITE_MAP -> substr(code,1,4) in ICD9_CANCER_SITE_MAP -> substr(code,1,3) in ICD9_CANCER_SITE_MAP -> NA
  3. Removes NAs, keeps unique(), sorts descending (decreasing=TRUE), collapses with ";"
  4. Returns NA_character_ if no categories found
- Apply: episode_dx <- episode_dx %>% mutate(cancer_category_names = sapply(cancer_codes, map_cancer_codes_to_categories, USE.NAMES = FALSE))
- Log: number of episodes with/without cancer_category_names, sample mappings for verification

**SECTION 5: TABLE 1 -- SUB-CATEGORY INSTANCE DETAIL (per D-01, D-03, D-05, D-06)**
- Split triggering_codes into individual codes: episode_dx %>% mutate(code_list = str_split(triggering_codes, ",\\s*")) %>% unnest(code_list) %>% filter(!is.na(code_list), code_list != "") %>% rename(treatment_code = code_list)
- Derive category per code: mutate(category = ifelse(treatment_code %in% names(DRUG_GROUPINGS), DRUG_GROUPINGS[treatment_code], treatment_type))
- Filter to valid_reference_codes OR Immunotherapy category (same as R/56 lines 262-267)
- Apply 3-tier sub-category resolution (replicate R/56 lines 322-369 case_when logic exactly, including all Tier 1/2/3 branches for Immunotherapy, Chemotherapy, Radiation, SCT)
  - Need to build the same code-type lookup vectors from TREATMENT_CODES (R/56 lines 293-319) before the case_when
- Aggregate BACK to episode level: group_by(patient_id, episode_number, episode_start, episode_stop, treatment_type, cancer_category_names) %>% summarise(sub_category_names = paste(sort(unique(sub_category)), collapse = ";"), .groups = "drop")
- Filter: filter(!is.na(cancer_category_names)) per D-01 equivalent
- Final select for Table 1 (per D-06): patient_id, episode_start, episode_stop, episode_number, treatment_type (renamed to "treatment_category"), sub_category_names, cancer_category_names
- Sort: arrange(patient_id, episode_start, treatment_type) for patient-centric browsing
- Rename columns for xlsx readability: rename(treatment_category = treatment_type)
- Log row count and sample

**SECTION 6: TABLE 2 -- ENCOUNTER TREATMENT INSTANCE DETAIL (per D-01, D-05, D-06, D-08)**
- Table 2 shows each episode with its full triggering_codes (not split into individual codes)
- Filter valid reference codes within triggering_codes string (same sapply pattern as R/56 lines 547-554)
- Build table2: episode_dx %>% filter(!is.na(cancer_category_names), !is.na(triggering_codes)) %>% select(patient_id, episode_start, episode_stop, episode_number, treatment_type, triggering_codes, cancer_category_names)
- Rename: rename(treatment_category = treatment_type, all_treatments = triggering_codes)
- Sort: arrange(patient_id, episode_start, treatment_category)
- Log row count

**SECTION 7: WRITE XLSX OUTPUT (per D-07, D-08)**
- wb <- wb_workbook()
- Sheet 1: "Treatment Sub-Category Detail" with table1 data
- Sheet 2: "Encounter Treatment Detail" with table2 data
- wb$save(OUTPUT_XLSX)
- Log: "Saved: {OUTPUT_XLSX}"

**SECTION 8: CONSOLE SUMMARY**
- Total episodes processed, episodes with/without cancer category names
- Table 1 row count, unique sub-categories, unique patients
- Table 2 row count, unique treatment sets, unique patients
- Verify drug_grouping_tables.xlsx was NOT modified (check file modification time vs script start)
- close(.log_con)

Key anti-patterns to AVOID:
- Do NOT modify R/56 or drug_grouping_tables.xlsx (per D-02, D-07)
- Do NOT use group_by() + summarise() with counts on final output -- each row IS an instance (per D-05)
- Do NOT leave raw ICD codes in cancer column -- must be category names (per D-04)
- Do NOT sort cancer_codes before mapping -- map FIRST then sort category names descending (per D-04)
- Do NOT try to look up "C81.10;C77.9" as a single key -- split on semicolon first, map each code individually
  </action>
  <verify>
    <automated>Rscript -e "lines <- readLines('R/57_drug_grouping_instances.R'); stopifnot(any(grepl('drug_grouping_instances\\.xlsx', lines))); stopifnot(any(grepl('source\\(\"R/00_config\\.R\"\\)', lines))); stopifnot(any(grepl('map_cancer_codes_to_categories', lines))); stopifnot(any(grepl('Treatment Sub-Category Detail', lines))); stopifnot(any(grepl('Encounter Treatment Detail', lines))); stopifnot(any(grepl('CANCER_SITE_MAP', lines))); stopifnot(any(grepl('ICD9_CANCER_SITE_MAP', lines))); stopifnot(any(grepl('code_to_subcategory', lines))); stopifnot(any(grepl('CODE_SUBCATEGORY_MAP', lines))); stopifnot(sum(grepl('^# SECTION', lines)) >= 7); cat('All R/57 structural checks passed\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/57_drug_grouping_instances.R exists with >= 200 lines
    - File contains `source("R/00_config.R")`
    - File contains `source("R/utils/utils_assertions.R")`
    - File contains `source("R/utils/utils_duckdb.R")`
    - File contains `source("R/utils/utils_cancer.R")`
    - File contains `drug_grouping_instances.xlsx` (output path)
    - File contains `map_cancer_codes_to_categories` function definition
    - Function body contains both `CANCER_SITE_MAP` and `ICD9_CANCER_SITE_MAP` lookups
    - Function body contains `sort(unique(categories), decreasing = TRUE)` for descending sort (D-04)
    - File contains `Treatment Sub-Category Detail` and `Encounter Treatment Detail` sheet names
    - File has >= 7 SECTION headers following `# SECTION N:` pattern
    - File contains `treatment_episodes.rds` reference
    - File contains 3-tier sub-category resolution (code_to_subcategory, CODE_SUBCATEGORY_MAP, case_when fallback)
    - File does NOT contain `drug_grouping_tables.xlsx` (must not reference existing output)
    - File does NOT contain `encounter_count` column in final select (instance-level, no counts)
    - Table 1 select includes: patient_id, episode_start, episode_stop, episode_number, treatment_category, sub_category_names, cancer_category_names
    - Table 2 select includes: patient_id, episode_start, episode_stop, episode_number, treatment_category, all_treatments, cancer_category_names
  </acceptance_criteria>
  <done>R/57_drug_grouping_instances.R exists as a standalone script that reads treatment_episodes.rds, joins DuckDB DIAGNOSIS for cancer codes, maps codes to category names via CANCER_SITE_MAP/ICD9_CANCER_SITE_MAP, resolves sub-categories via 3-tier lookup, and writes a 2-sheet drug_grouping_instances.xlsx with instance-level (one row per episode) data using descriptive names instead of raw codes.</done>
</task>

<task type="auto">
  <name>Task 2: Add R/57 validation to R/88 smoke test</name>
  <files>R/88_smoke_test_comprehensive.R</files>
  <read_first>
    - R/88_smoke_test_comprehensive.R (full file -- need to see Section 30 ending, Section 16 summary, counter format, check() pattern)
    - R/57_drug_grouping_instances.R (just created -- need to know exact patterns to validate)
  </read_first>
  <action>
Add SECTION 31 to R/88_smoke_test_comprehensive.R for Phase 88 R/57 validation. Insert BEFORE the existing SECTION 16: SUMMARY block (currently at line ~1410).

**Step 1: Update counter**
- Change `[30/30]` on line 1310 to `[30/31]` (Section 30 message)
- The new section will use `[31/31]`

**Step 2: Insert SECTION 31 block**
Insert the following between Section 30 (ending ~line 1408) and Section 16 (starting ~line 1410):

```r
# ==============================================================================
# SECTION 31: PHASE 88 -- INSTANCE-LEVEL DRUG GROUPING TABLES ----
# ==============================================================================
# Validates R/57 drug grouping instance-level tables (Phase 88).

message("\n[31/31] Phase 88: Instance-level drug grouping tables...")

check("R/57_drug_grouping_instances.R exists", file.exists("R/57_drug_grouping_instances.R"))

if (file.exists("R/57_drug_grouping_instances.R")) {
  r57_lines <- readLines("R/57_drug_grouping_instances.R", warn = FALSE)

  check("R/57 sources R/00_config.R",
        any(grepl('source\\("R/00_config.R"\\)', r57_lines)))

  check("R/57 sources R/utils/utils_assertions.R",
        any(grepl('source\\("R/utils/utils_assertions.R"\\)', r57_lines)))

  check("R/57 sources R/utils/utils_duckdb.R",
        any(grepl('source\\("R/utils/utils_duckdb.R"\\)', r57_lines)))

  check("R/57 sources R/utils/utils_cancer.R",
        any(grepl('source\\("R/utils/utils_cancer.R"\\)', r57_lines)))

  check("R/57 reads treatment_episodes.rds input",
        any(grepl("treatment_episodes\\.rds", r57_lines)))

  check("R/57 outputs drug_grouping_instances.xlsx (not drug_grouping_tables.xlsx)",
        any(grepl("drug_grouping_instances\\.xlsx", r57_lines)) &&
        !any(grepl("drug_grouping_tables\\.xlsx", r57_lines)))

  check("R/57 has 2-sheet workbook (Treatment Sub-Category Detail + Encounter Treatment Detail)",
        any(grepl("Treatment Sub-Category Detail", r57_lines)) &&
        any(grepl("Encounter Treatment Detail", r57_lines)))

  check("R/57 defines map_cancer_codes_to_categories helper function",
        any(grepl("map_cancer_codes_to_categories", r57_lines)))

  check("R/57 uses both CANCER_SITE_MAP and ICD9_CANCER_SITE_MAP for code-to-category mapping",
        any(grepl("CANCER_SITE_MAP", r57_lines)) &&
        any(grepl("ICD9_CANCER_SITE_MAP", r57_lines)))

  check("R/57 sorts cancer categories descending (per D-04)",
        any(grepl("decreasing\\s*=\\s*TRUE", r57_lines)))

  check("R/57 uses 3-tier sub-category lookup (xlsx, CODE_SUBCATEGORY_MAP, fallback)",
        any(grepl("code_to_subcategory", r57_lines)) &&
        any(grepl("CODE_SUBCATEGORY_MAP", r57_lines)))

  n_sections_r57 <- sum(grepl("^# SECTION.*----", r57_lines))
  check(glue("R/57 has >= 7 section headers (found: {n_sections_r57})"),
        n_sections_r57 >= 7)

  check("R/57 does NOT aggregate with encounter_count (instance-level output)",
        !any(grepl("encounter_count", r57_lines)))

  check("R/57 uses is_cancer_code() from shared utility",
        any(grepl("is_cancer_code", r57_lines)) &&
        !any(grepl("is_cancer_code <- function", r57_lines)))
}
```

**Step 3: Update Section 16 summary**
Add these lines to the validated requirements message block (after line ~1464):
```r
message("  * P88-D01/D02: Instance-level tables in separate xlsx (R/57)")
message("  * P88-D03: Sub-category names via 3-tier resolution")
message("  * P88-D04: Cancer site category names from CANCER_SITE_MAP + ICD9_CANCER_SITE_MAP")
message("  * P88-D05/D06: Per-episode rows with patient_id, dates, treatment_category")
message("  * P88-D07/D08: New drug_grouping_instances.xlsx with 2 sheets")
```
  </action>
  <verify>
    <automated>Rscript -e "lines <- readLines('R/88_smoke_test_comprehensive.R'); stopifnot(any(grepl('SECTION 31.*PHASE 88', lines))); stopifnot(any(grepl('57_drug_grouping_instances', lines))); stopifnot(any(grepl('31/31', lines))); stopifnot(any(grepl('P88-D01', lines))); cat('All R/88 smoke test checks passed\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/88_smoke_test_comprehensive.R contains `SECTION 31: PHASE 88`
    - File contains `[31/31]` counter for new section
    - File contains `[30/31]` updated counter for Section 30
    - Section 31 checks for: R/57 file existence, source() calls (00_config, utils_assertions, utils_duckdb, utils_cancer), treatment_episodes.rds, drug_grouping_instances.xlsx, 2-sheet names, map_cancer_codes_to_categories, CANCER_SITE_MAP + ICD9_CANCER_SITE_MAP, decreasing=TRUE, 3-tier lookup, >= 7 sections, no encounter_count, shared is_cancer_code
    - Summary section contains `P88-D01` through `P88-D08` requirement tags
    - File does NOT contain `[30/30]` (old counter must be updated)
  </acceptance_criteria>
  <done>R/88 smoke test has a new Section 31 validating R/57 script structure: source dependencies, output file name, 2-sheet workbook, cancer code-to-category mapping function, ICD-9/ICD-10 dual map usage, descending sort, 3-tier sub-category lookup, instance-level grain (no encounter_count), and shared utility usage. Summary section lists Phase 88 requirement coverage.</done>
</task>

</tasks>

<verification>
1. R/57_drug_grouping_instances.R exists with >= 200 lines and >= 7 SECTION headers
2. R/57 references drug_grouping_instances.xlsx (NOT drug_grouping_tables.xlsx)
3. R/57 contains map_cancer_codes_to_categories with both CANCER_SITE_MAP and ICD9_CANCER_SITE_MAP
4. R/57 contains 3-tier sub-category resolution (code_to_subcategory, CODE_SUBCATEGORY_MAP, case_when)
5. R/57 does NOT contain encounter_count in output columns
6. R/88 contains SECTION 31 with 14+ checks for R/57 validation
7. R/88 counter updated from [30/30] to [30/31] and new section uses [31/31]
8. R/88 summary lists P88-D01 through P88-D08
</verification>

<success_criteria>
- R/57_drug_grouping_instances.R is a complete, standalone script that produces drug_grouping_instances.xlsx with 2 sheets of instance-level data using descriptive names
- R/88 smoke test validates R/57 structure with 14+ checks
- No modifications to R/56_new_tables_from_groupings.R or existing drug_grouping_tables.xlsx
- All 8 locked decisions (D-01 through D-08) are implemented in R/57
</success_criteria>

<output>
After completion, create `.planning/phases/88-re-do-tables-from-grouping-by-using-descriptives-from-codes-instead-of-counts-and-instead-of-counts-just-list-each-instance/88-01-SUMMARY.md`
</output>
