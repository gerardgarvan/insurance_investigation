---
phase: 130-registration-smoke-test-and-hipergator-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/39_run_all_investigations.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements: [DOI-QA-01]

must_haves:
  truths:
    - "An end-to-end R/39 run executes R/111 (classification) before R/112 (attribution) so R/112's .rds inputs exist first"
    - "R/39's pre-render output check knows to expect doi_attribution_report.xlsx"
    - "SCRIPT_INDEX.md documents R/111 as the .rds classification producer and R/112 as the 4-sheet attribution xlsx producer"
  artifacts:
    - path: "R/39_run_all_investigations.R"
      provides: "R/111 + R/112 registered in investigation_scripts (dependency order); doi_attribution_report.xlsx in expected_xlsx"
      contains: "R/111_doi_classification.R"
    - path: "R/SCRIPT_INDEX.md"
      provides: "Two new Post-Renumber Investigations rows + updated tally line"
      contains: "R/111_doi_classification.R"
  key_links:
    - from: "R/39 investigation_scripts vector"
      to: "R/111 then R/112 execution order"
      via: "vector element order (R/111 precedes R/112)"
      pattern: "R/111_doi_classification.*\\n.*R/112_doi_attribution_report"
---

<objective>
Register both DoI investigation scripts in the pipeline's discovery/orchestration infrastructure (R/39) and its human-readable index (SCRIPT_INDEX.md). This satisfies DOI-QA-01.

Purpose: An end-to-end `Rscript R/39_run_all_investigations.R` run must execute R/111 (classification, emits the two .rds) BEFORE R/112 (attribution, consumes the .rds and emits the xlsx), and its pre-render check must expect the new xlsx. SCRIPT_INDEX.md must correctly name R/111 = classification (.rds producer) and R/112 = attribution (4-sheet xlsx), correcting the roadmap's naming slip.

Output: Edited R/39_run_all_investigations.R and R/SCRIPT_INDEX.md. No R execution required (R is not installed locally) — all deliverables are structurally verifiable via grep/file-read on Windows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register R/111 then R/112 in R/39 investigation_scripts + add xlsx to expected_xlsx</name>
  <read_first>
    - R/39_run_all_investigations.R (the file being modified — read L176-197 for investigation_scripts, L276-287 for expected_xlsx)
    - .planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-CONTEXT.md (D-04 dependency order, D-05 xlsx-only)
    - R/111_doi_classification.R (confirm it writes doi_encounters.rds + doi_patients.rds only — NO xlsx; see L184, L241)
    - R/112_doi_attribution_report.R (confirm it writes doi_attribution_report.xlsx; see L574)
  </read_first>
  <action>
    Two edits to R/39_run_all_investigations.R.

    EDIT 1 — investigation_scripts vector (currently L176-197). The vector's last real entry is:
    ```
      "R/106_zip_change_frequency.R"                # ZIP change frequency investigation (Phase 121); probes LDS_ADDRESS_HISTORY, read-only
    )
    ```
    Add a trailing comma to the R/106 line and insert TWO new lines AFTER it (R/111 MUST precede R/112 per D-04 — R/111 emits the .rds that R/112 consumes). Result:
    ```
      "R/106_zip_change_frequency.R",               # ZIP change frequency investigation (Phase 121); probes LDS_ADDRESS_HISTORY, read-only
      "R/111_doi_classification.R",                 # DoI classification (Phase 128); DuckDB prefix-pull of DIAGNOSIS -> doi_encounters.rds + doi_patients.rds. MUST run before R/112 (emits its .rds inputs).
      "R/112_doi_attribution_report.R"              # DoI attribution (Phase 129); joins rituximab/MTX to DoI dx (co-occurrence) -> doi_attribution_report.xlsx (4 sheets). Consumes R/111's .rds.
    )
    ```
    (Only R/111 and R/112 are added here — R/107-R/110 are deliberately NOT wired in per the one-off-diagnostic convention; do not touch them.)

    EDIT 2 — expected_xlsx vector (currently L276-287). The vector's last entry is:
    ```
      "tableau_table2_chemo_drugs_by_class.xlsx"
    )
    ```
    Add a trailing comma and ONE new line (ONLY the attribution xlsx — R/111 emits .rds only, has no xlsx, per D-05):
    ```
      "tableau_table2_chemo_drugs_by_class.xlsx",
      "doi_attribution_report.xlsx"
    )
    ```
    Do NOT add doi_encounters.rds / doi_patients.rds to expected_xlsx — that vector is xlsx-only and drives the report pre-render check.
  </action>
  <acceptance_criteria>
    - `grep -n 'R/111_doi_classification.R' R/39_run_all_investigations.R` matches inside the investigation_scripts block.
    - `grep -n 'R/112_doi_attribution_report.R' R/39_run_all_investigations.R` matches inside the investigation_scripts block.
    - In R/39, the line number of `R/111_doi_classification.R` is LESS THAN the line number of `R/112_doi_attribution_report.R` (R/111 precedes R/112).
    - `grep -n 'doi_attribution_report.xlsx' R/39_run_all_investigations.R` matches inside the expected_xlsx vector (between the `expected_xlsx <- c(` and its closing `)`).
    - `grep -c 'doi_encounters.rds\|doi_patients.rds' R/39_run_all_investigations.R` returns 0 (the .rds files are NOT added to expected_xlsx).
    - The investigation_scripts vector still parses: exactly one `)` closes it and every non-final element ends in a comma (visual read confirms no dangling/double commas).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — R not installed locally. Structural verification only: run `grep -n "R/111_doi_classification.R\|R/112_doi_attribution_report.R\|doi_attribution_report.xlsx" R/39_run_all_investigations.R` and confirm R/111 line &lt; R/112 line and xlsx present in expected_xlsx.</automated>
  </verify>
  <done>R/39 investigation_scripts contains R/111 immediately followed by R/112; expected_xlsx contains doi_attribution_report.xlsx; no .rds names leaked into expected_xlsx; vector syntax intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add R/111 + R/112 rows to SCRIPT_INDEX.md Post-Renumber table and update the tally line</name>
  <read_first>
    - R/SCRIPT_INDEX.md (the file being modified — read L140-156 for the Post-Renumber table, L209 for the tally line)
    - .planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-CONTEXT.md (D-06 — R/111 = classification, R/112 = attribution; co-occurrence language; corrects roadmap naming slip)
    - .planning/REQUIREMENTS.md (DOI-CLASS-02/03, DOI-ATTR-01/02/03, DOI-OUT-01 — source of correct role prose)
  </read_first>
  <action>
    Two edits to R/SCRIPT_INDEX.md.

    EDIT 1 — Post-Renumber Investigations (100+) table. The table body currently ends with the R/110 row (L156). Append TWO new rows immediately after the R/110 row, before the closing `---` at L158:
    ```
    | `R/111_doi_classification.R` | DoI (diagnosis-of-interest) classification producer. DuckDB-native prefix pull of the DIAGNOSIS table (never loads it fully into R), classifies rituximab/methotrexate non-malignant indications via `DOI_CODE_MAP` (DX_TYPE-gated ICD-9/ICD-10), runs the mutual-exclusivity hard-stop (`sum(is_doi_code(DX) & is_cancer_code(DX)) == 0`) before any output, flags L10.81 paraneoplastic pemphigus, and marks in_hl_cohort membership. Writes two .rds artifacts (NO xlsx): `output/doi_encounters.rds` (encounter grain) and `output/doi_patients.rds` (patient grain). | 128 |
    | `R/112_doi_attribution_report.R` | DoI attribution report producer. Consumes R/111's `doi_encounters.rds` and joins rituximab/MTX administrations to DoI diagnoses via a two-tier linkage (ENCOUNTERID direct match, then ±90-day PATID temporal window; `DOI_ATTRIBUTION_WINDOW_DAYS`), carrying a three-state `likely_non_lymphoma_directed` flag and an `attribution_method` column. All prose is co-occurrence language ("with [dx]"), never causal. Writes a 4-sheet styled xlsx `output/doi_attribution_report.xlsx` (Patient Prevalence, Encounter Co-occurrence, Drug x DoI Summary, Metadata); raw counts, internal-only (suppress manually before external sharing). | 129 |
    ```
    (Roles are authoritative per D-06: R/111 = classification / .rds producer; R/112 = attribution / xlsx producer. Do NOT copy the roadmap's "R/111_doi_attribution_report.R" naming slip.)

    EDIT 2 — tally line (L209). Currently reads `**Post-renumber investigations (100+):** 11 (...R/110 Phase 124 output-level before/after report + unmapped-name audit)`. Change the count `11` to `13` and append the two new entries inside the parenthetical, before the closing `)`:
    ```
    - **Post-renumber investigations (100+):** 13 (R/100 RUCA, R/101 Gantt lifespan, R/102 death-cause NHL flag, R/103 death-cause diagnostic, R/104 Gantt entire-history, R/105 Supportive Care Normalized Meaning, R/106 ZIP change frequency, R/107 MED_ADMIN/DISPENSING chemo-gap sizing, R/108 NDC->RxNorm crosswalk builder, R/109 MED_ADMIN/DISPENSING fix before/after diff + unmatched-NDC audit, R/110 Phase 124 output-level before/after report + unmapped-name audit, R/111 DoI classification (.rds producer), R/112 DoI attribution (4-sheet xlsx))
    ```
    Note: the `**Total:**` line just below (currently `96`) counts numbered(69) + post-renumber(11) + utils(10) + archived(8) = 98 already-inconsistent; leave `**Total:**` as-is unless it is exactly `96`, in which case bump to `98` to reflect the two added scripts (69 + 13 + 10 + 8 = 100... only touch Total if it currently equals 96 → set to 98; otherwise leave). Prefer accuracy of the post-renumber tally (the D-06 deliverable) over reconciling the historically-off Total.
  </action>
  <acceptance_criteria>
    - `grep -n 'R/111_doi_classification.R' R/SCRIPT_INDEX.md` matches inside the Post-Renumber Investigations table (a `| ... | ... | 128 |` row).
    - `grep -n 'R/112_doi_attribution_report.R' R/SCRIPT_INDEX.md` matches inside the same table (a `| ... | ... | 129 |` row).
    - The R/111 row contains the substring `classification` and `doi_encounters.rds`; the R/112 row contains `attribution` and `doi_attribution_report.xlsx`. (Roles correct per D-06.)
    - Neither new row contains the string `R/111_doi_attribution_report` (roadmap naming slip NOT propagated): `grep -c 'R/111_doi_attribution_report' R/SCRIPT_INDEX.md` returns 0.
    - The tally line reads `**Post-renumber investigations (100+):** 13` and its parenthetical contains both `R/111 DoI classification` and `R/112 DoI attribution`.
    - Each new table row has exactly 3 pipe-delimited cells (Script | Purpose | Phase) matching the existing row shape.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — R not installed locally. Structural verification only: `grep -n "R/111_doi_classification.R\|R/112_doi_attribution_report.R" R/SCRIPT_INDEX.md` shows both rows; `grep -c "R/111_doi_attribution_report" R/SCRIPT_INDEX.md` returns 0; tally line shows count 13.</automated>
  </verify>
  <done>SCRIPT_INDEX.md Post-Renumber table has correctly-named R/111 (classification/.rds) and R/112 (attribution/xlsx) rows; tally line updated to 13 with both entries; roadmap naming slip not propagated.</done>
</task>

</tasks>

<verification>
- R/39 investigation_scripts: R/111 registered before R/112 (grep line-order check).
- R/39 expected_xlsx: doi_attribution_report.xlsx present; no .rds names present.
- SCRIPT_INDEX.md: two new rows with correct roles (R/111 classification, R/112 attribution); tally line = 13.
- No roadmap naming slip (`R/111_doi_attribution_report`) anywhere in edits.
- Both files still parse structurally (balanced vector parens; well-formed markdown table rows).
</verification>

<success_criteria>
DOI-QA-01 satisfied structurally on Windows: R/39 knows to run R/111 → R/112 in dependency order and to expect doi_attribution_report.xlsx; SCRIPT_INDEX.md documents both scripts with correct classification/attribution roles. (Real end-to-end R/39 execution on HiPerGator is confirmed via Plan 02's runtime checkpoint.)
</success_criteria>

<output>
After completion, create `.planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-01-SUMMARY.md`
</output>
