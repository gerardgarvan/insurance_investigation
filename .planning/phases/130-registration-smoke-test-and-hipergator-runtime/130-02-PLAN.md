---
phase: 130-registration-smoke-test-and-hipergator-runtime
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/88_smoke_test_comprehensive.R
autonomous: false
requirements: [DOI-QA-02, DOI-QA-03]

must_haves:
  truths:
    - "R/88 fails hard if any DOI_CODE_MAP key collides with a CANCER_SITE_MAP or ICD9_CANCER_SITE_MAP key (mutual-exclusivity guardrail)"
    - "R/88 confirms the DoI code map, utility functions, and both producer scripts exist and are wired correctly on Windows without running R/111 or R/112"
    - "On HiPerGator, a real DIAGNOSIS-table run logs DoI category counts and a human records them verbatim (RA dominant; NMO/pemphigus rare)"
  artifacts:
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15w DoI validation (~13-14 structural checks + IS_LOCAL-gated runtime block) and a SMOKE-130-01 SUMMARY line"
      contains: "SECTION 15w"
  key_links:
    - from: "R/88 Section 15w"
      to: "DOI_CODE_MAP vs CANCER_SITE_MAP / ICD9_CANCER_SITE_MAP"
      via: "intersect(names(DOI_CODE_MAP), c(names(CANCER_SITE_MAP), names(ICD9_CANCER_SITE_MAP))) length == 0"
      pattern: "intersect\\(names\\(DOI_CODE_MAP\\)"
    - from: "R/88 Section 15w runtime block"
      to: "real DIAGNOSIS DoI counts on HiPerGator"
      via: "if (!IS_LOCAL && file.exists(doi_encounters.rds))"
      pattern: "!IS_LOCAL && file\\.exists"
---

<objective>
Add R/88 Section 15w validating the DoI layer (DOI-QA-02), and gate the DoI layer's real-data correctness behind a logged HiPerGator runtime pass (DOI-QA-03, the v3.3 definition-of-done).

Purpose: Structural checks (grep/parse/file-existence + in-memory map/function assertions) run and pass on Windows without executing R/111 or R/112. The single most important assertion is the mutual-exclusivity hard-stop: zero key-collision between DOI_CODE_MAP and the cancer maps. The real-data runtime pass — where DoI category counts are logged against the actual DIAGNOSIS table — is a HUMAN-UAT checkpoint on HiPerGator, recorded verbatim in the phase completion notes. This resolves the PROJECT.md "dual-environment verification ... attested-by-prose only" flag with a real log.

Output: Edited R/88_smoke_test_comprehensive.R (new Section 15w + SUMMARY line). Plus a recorded HiPerGator runtime log (human checkpoint).

CRITICAL: R is NOT installed locally. Do NOT write acceptance criteria that require running R. The autonomous deliverable is the CODE; the runtime pass is the human checkpoint (Task 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-CONTEXT.md

<interfaces>
<!-- Concrete values the executor needs — do NOT re-derive from the codebase. -->

R/00_config.R:
- DOI_CODE_MAP is a named char vector (L434-492); keys are ICD prefixes ("M05","M06","714","M30",...,"D891"); ~35 keys total (>= 20 guaranteed).
- CANCER_SITE_MAP <- c(...)  (L741) — named char vector, cancer prefixes.
- ICD9_CANCER_SITE_MAP <- c(...) (L1038) — named char vector, ICD-9 cancer prefixes.
- IS_LOCAL flag (defined via ENV-01, R/88 L1237-1252).
- CONFIG$cache$outputs_dir — the output cache dir (where the .rds and xlsx land).

R/utils/utils_doi.R:
- is_doi_code(dx, dx_type)  — DX_TYPE-gated ("09"/"10"; NA/SNOMED -> FALSE). is_doi_code("M05.9","10") == TRUE; is_doi_code("C81.90","10") == FALSE.
- classify_doi_codes(codes)  — maps codes to DoI category labels.

R/111_doi_classification.R writes (NO xlsx):
- CONFIG$cache$outputs_dir/doi_encounters.rds — columns: ID, ENCOUNTERID, DX_DATE, doi_code, doi_category, paraneoplastic_flag, in_hl_cohort
- CONFIG$cache$outputs_dir/doi_patients.rds   — columns: ID, has_any_doi, doi_categories, doi_first_date, doi_last_date, n_doi_encounters, in_hl_cohort

R/112_doi_attribution_report.R writes:
- CONFIG$cache$outputs_dir/doi_attribution_report.xlsx — exactly 4 sheets, exact names:
  "Patient Prevalence", "Encounter Co-occurrence", "Drug x DoI Summary", "Metadata"

R/88 patterns to mirror:
- check(<label>, <predicate>) helper drives every check + pass/fail tally (reuse verbatim).
- Section 15v (L2822-2909) — the ~13-check template to mirror: message prefix "[Phase 124] ...", file.exists gate, per-check `check(...)`, an IS_LOCAL runtime branch that stays green both ways, and a fallback `for (i in 2:13) check(..., FALSE)` if the script is missing.
- Section 15p Check 14 (L2303-2317) — the `if (!IS_LOCAL && file.exists(out_path)) { real check } else { check("... SKIPPED (local / no output)", TRUE) }` HiPerGator real-data gate.
- Section 15s Check 14 (L2569-2579) — same gate for an xlsx output.
- SUMMARY block SMOKE-line list (L4482-4488) — append a SMOKE-130-01 line matching the format of SMOKE-124-01 (L4488).
- Lettered sub-sections (15p-15v) use a `[Phase NNN]` message prefix, NOT a numeric `[N/N]` counter; the terminal numeric counter is `[43/43]` at L4239 (fixture schema). Follow the lettered convention — 15w uses `[Phase 130]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add R/88 Section 15w — DoI layer structural validation (mirror 15v, ~14 checks incl. mutual-exclusivity hard-stop)</name>
  <read_first>
    - R/88_smoke_test_comprehensive.R (the file being modified — read Section 15v L2822-2909 as the template, Section 15p L2303-2317 and 15s L2569-2579 for the IS_LOCAL real-data gate, ENV-01 L1237-1252 for IS_LOCAL, the `check()` helper definition)
    - .planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-CONTEXT.md (D-02 slot 15w, D-03 must-include checks, D-03a mirror 15v ~13-14 checks, D-07 fixture asserts schema-validity NOT exact counts)
    - R/00_config.R (DOI_CODE_MAP L434-492; CANCER_SITE_MAP L741; ICD9_CANCER_SITE_MAP L1038)
    - R/utils/utils_doi.R (is_doi_code, classify_doi_codes signatures/behavior)
    - .planning/phases/128-doi-classification/128-01-SUMMARY.md and 128-02-SUMMARY.md (.rds columns)
    - .planning/phases/129-attribution-linkage-and-output/129-01-SUMMARY.md and 129-02-SUMMARY.md (xlsx 4-sheet names/columns)
  </read_first>
  <action>
    Insert a NEW section immediately AFTER Section 15v ends (after its closing block at ~L2909, BEFORE the `# SECTION 15g: PROTON THERAPY ...` header at L2911). Mirror 15v's structure exactly. Use the `check(<label>, <predicate>)` helper for every check.

    Header + intro (mirror 15v style):
    ```r
    # ==============================================================================
    # SECTION 15w: DIAGNOSIS-OF-INTEREST (DoI) LAYER VALIDATION (Phase 130) ----
    # ==============================================================================
    #
    # Phase 130 wires the DoI layer (R/111 classification + R/112 attribution) into
    # the pipeline and gates its correctness. These checks are STRUCTURAL + in-memory
    # (DOI_CODE_MAP / is_doi_code / classify_doi_codes assertions) and pass LOCALLY
    # on Windows without executing R/111 or R/112. The real-data runtime pass (logged
    # DoI category counts on the actual DIAGNOSIS table) is a HiPerGator HUMAN-UAT
    # gate — the IS_LOCAL-gated runtime checks below stay green locally and only
    # exercise real outputs when !IS_LOCAL and the artifacts exist.
    #
    # The mutual-exclusivity hard-stop (Check 3) is the single most important assertion:
    # zero key-collision tolerance between DOI_CODE_MAP and the cancer maps.

    message("\n[Phase 130] Diagnosis-of-Interest (DoI) layer validation (R/111 + R/112)...")
    ```

    Then these checks (this is the D-03 floor + fixture/functional checks per D-03a/D-07; ~14 checks mirroring 15v). Use exactly this logic:

    Check 1 — DOI_CODE_MAP exists and is a named character vector:
    ```r
    check("DOI_CODE_MAP exists as a named character vector (Phase 130)",
          exists("DOI_CODE_MAP") && is.character(DOI_CODE_MAP) &&
            !is.null(names(DOI_CODE_MAP)))
    ```

    Check 2 — DOI_CODE_MAP length >= 20 (D-03):
    ```r
    check(glue("DOI_CODE_MAP has >= 20 keys (found {length(DOI_CODE_MAP)}) (Phase 130)"),
          length(DOI_CODE_MAP) >= 20L)
    ```

    Check 3 — MUTUAL-EXCLUSIVITY HARD-STOP: zero key-collision with cancer maps (D-03, the critical guardrail):
    ```r
    cancer_keys <- c(names(CANCER_SITE_MAP), names(ICD9_CANCER_SITE_MAP))
    doi_cancer_overlap <- intersect(names(DOI_CODE_MAP), cancer_keys)
    check(glue("ZERO DOI_CODE_MAP <-> cancer-map key collision (overlap: {length(doi_cancer_overlap)}{if (length(doi_cancer_overlap)) paste0(' = ', paste(doi_cancer_overlap, collapse=',')) else ''}) (Phase 130)"),
          length(doi_cancer_overlap) == 0L)
    ```

    Check 4 — utils_doi.R file exists:
    ```r
    check("R/utils/utils_doi.R exists (Phase 130)",
          file.exists("R/utils/utils_doi.R"))
    ```

    Check 5 — R/111 producer script exists:
    ```r
    check("R/111_doi_classification.R exists (Phase 130)",
          file.exists("R/111_doi_classification.R"))
    ```

    Check 6 — R/112 producer script exists:
    ```r
    check("R/112_doi_attribution_report.R exists (Phase 130)",
          file.exists("R/112_doi_attribution_report.R"))
    ```

    Check 7 — is_doi_code() functional spot-check, ICD-10 positive + cancer-negative (DX_TYPE-gated):
    ```r
    check("is_doi_code('M05.9','10') is TRUE and is_doi_code('C81.90','10') is FALSE (Phase 130)",
          isTRUE(is_doi_code("M05.9", "10")) && isFALSE(is_doi_code("C81.90", "10")))
    ```

    Check 8 — is_doi_code() DX_TYPE gating rejects NA/SNOMED:
    ```r
    check("is_doi_code('M05.9', NA) is FALSE (DX_TYPE gate rejects non ICD-9/10) (Phase 130)",
          isFALSE(is_doi_code("M05.9", NA)))
    ```

    Check 9 — is_doi_code() ICD-9 positive (DX_TYPE '09'):
    ```r
    check("is_doi_code('714.0','09') is TRUE (ICD-9 RA) (Phase 130)",
          isTRUE(is_doi_code("714.0", "09")))
    ```

    Check 10 — classify_doi_codes() functional spot-check returns expected category:
    ```r
    ra_cat <- classify_doi_codes("M05.9")
    check(glue("classify_doi_codes('M05.9') == 'Rheumatoid Arthritis' (found '{ra_cat}') (Phase 130)"),
          identical(unname(ra_cat), "Rheumatoid Arthritis"))
    ```

    Check 11 — R/39 wiring: R/111 precedes R/112 in investigation_scripts (cross-checks Plan 01):
    ```r
    r39_txt <- if (file.exists("R/39_run_all_investigations.R")) paste(readLines("R/39_run_all_investigations.R", warn = FALSE), collapse = "\n") else ""
    pos_111 <- regexpr("R/111_doi_classification\\.R", r39_txt)
    pos_112 <- regexpr("R/112_doi_attribution_report\\.R", r39_txt)
    check("R/39 registers R/111 before R/112 and expected_xlsx includes doi_attribution_report.xlsx (Phase 130, DOI-QA-01)",
          pos_111 > 0 && pos_112 > 0 && pos_111 < pos_112 &&
            grepl("doi_attribution_report\\.xlsx", r39_txt))
    ```

    Check 12 — RUNTIME (HiPerGator-only, gated): doi_encounters.rds + doi_patients.rds exist with expected columns. Mirror 15p L2303-2317. Assert SCHEMA-VALIDITY + non-empty + three-state-ready, NOT exact counts (D-07):
    ```r
    enc_rds <- file.path(CONFIG$cache$outputs_dir, "doi_encounters.rds")
    pat_rds <- file.path(CONFIG$cache$outputs_dir, "doi_patients.rds")
    if (!IS_LOCAL && file.exists(enc_rds) && file.exists(pat_rds)) {
      enc <- readRDS(enc_rds); pat <- readRDS(pat_rds)
      enc_cols <- c("ID","ENCOUNTERID","DX_DATE","doi_code","doi_category","paraneoplastic_flag","in_hl_cohort")
      pat_cols <- c("ID","has_any_doi","doi_categories","doi_first_date","doi_last_date","n_doi_encounters","in_hl_cohort")
      check("R/111 .rds artifacts have expected columns and are non-empty (Phase 130, HiPerGator)",
            all(enc_cols %in% names(enc)) && all(pat_cols %in% names(pat)) && nrow(enc) > 0 && nrow(pat) > 0)
    } else {
      check("R/111 .rds runtime column/non-empty check -- SKIPPED (local / no output) (Phase 130)", TRUE)
    }
    ```

    Check 13 — RUNTIME (HiPerGator-only, gated): doi_attribution_report.xlsx exists with the 4 expected sheets. Mirror 15s L2569-2579:
    ```r
    doi_xlsx <- file.path(CONFIG$cache$outputs_dir, "doi_attribution_report.xlsx")
    if (!IS_LOCAL && file.exists(doi_xlsx)) {
      sheets_found <- openxlsx2::wb_load(doi_xlsx)$get_sheet_names()
      expected_sheets <- c("Patient Prevalence","Encounter Co-occurrence","Drug x DoI Summary","Metadata")
      check("R/112 xlsx has exactly the 4 expected sheets (Phase 130, HiPerGator)",
            all(expected_sheets %in% sheets_found))
    } else {
      check("R/112 xlsx 4-sheet runtime check -- SKIPPED (local / no output) (Phase 130)", TRUE)
    }
    ```

    Check 14 — RUNTIME (HiPerGator-only, gated): logged DoI category counts are clinically plausible / non-empty. This is the DOI-QA-03 signal (RA dominant; NMO/pemphigus rare). Assert non-empty + RA present, NOT exact numbers (D-07):
    ```r
    if (!IS_LOCAL && file.exists(enc_rds)) {
      enc2 <- readRDS(enc_rds)
      cat_tab <- sort(table(enc2$doi_category), decreasing = TRUE)
      message("  [Phase 130 RUNTIME] DoI category counts (log verbatim into phase notes):")
      for (nm in names(cat_tab)) message(glue("    {nm}: {cat_tab[[nm]]}"))
      check("R/111 real-data DoI counts non-empty with RA present (Phase 130, HiPerGator; log into phase notes)",
            length(cat_tab) > 0 && "Rheumatoid Arthritis" %in% names(cat_tab))
    } else {
      check("R/111 real-data DoI category counts -- SKIPPED (local / no output; logged on HiPerGator) (Phase 130)", TRUE)
    }
    ```

    Do NOT wrap Checks 1-11 in an IS_LOCAL guard — they must PASS locally on Windows (they read the in-memory maps/functions already sourced by R/00_config.R + the utils glob, and grep R/39). Only Checks 12-14 are `!IS_LOCAL`-gated real-data checks that stay green locally via the `else check(... TRUE)` branch, exactly like 15p/15s.
  </action>
  <acceptance_criteria>
    - `grep -n 'SECTION 15w' R/88_smoke_test_comprehensive.R` matches, and the section header appears AFTER the `SECTION 15v` header line and BEFORE the `SECTION 15g: PROTON THERAPY` header line.
    - `grep -c 'intersect(names(DOI_CODE_MAP)' R/88_smoke_test_comprehensive.R` returns >= 1 (mutual-exclusivity hard-stop present).
    - The 15w block contains all of: `DOI_CODE_MAP`, `CANCER_SITE_MAP`, `ICD9_CANCER_SITE_MAP`, `is_doi_code`, `classify_doi_codes`, `utils_doi.R`, `R/111_doi_classification.R`, `R/112_doi_attribution_report.R`, `doi_encounters.rds`, `doi_patients.rds`, `doi_attribution_report.xlsx` (each greppable within lines 15w-header .. 15g-header).
    - `grep -c '!IS_LOCAL && file.exists' R/88_smoke_test_comprehensive.R` increased by >= 2 vs before the edit (the two gated .rds/xlsx runtime blocks). At minimum >= 2 such occurrences appear inside the 15w block.
    - `grep -c 'check(' ` within the 15w block region is between 13 and 15 (mirrors 15v's ~13-14). Count via reading the section.
    - The section does NOT hard-code exact expected DoI counts anywhere (no `== <integer>` on a category count); per D-07 it asserts presence/non-empty only. Confirm by reading Checks 12-14.
    - The new section is syntactically balanced: every `if (...) {` in 15w has a matching `}` and every `check(` has a closing `)` (visual read).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — R not installed locally. Structural verification only: `grep -n "SECTION 15w\|SECTION 15v\|SECTION 15g" R/88_smoke_test_comprehensive.R` confirms 15w sits between 15v and 15g; `grep -c "intersect(names(DOI_CODE_MAP)" R/88_smoke_test_comprehensive.R` >= 1; the 11 required tokens listed in acceptance_criteria all grep-match inside the section. Real execution happens on HiPerGator (Task 3).</automated>
  </verify>
  <done>Section 15w exists between 15v and 15g with ~14 checks mirroring 15v; the mutual-exclusivity hard-stop and both !IS_LOCAL-gated runtime blocks are present; structural checks written to pass locally; no hard-coded exact counts.</done>
</task>

<task type="auto">
  <name>Task 2: Add SMOKE-130-01 line to R/88 SUMMARY block</name>
  <read_first>
    - R/88_smoke_test_comprehensive.R (the file being modified — read the SUMMARY SMOKE-line list L4482-4488, specifically the SMOKE-124-01 line at L4488 as the format template)
    - .planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-CONTEXT.md (D-02 SUMMARY line + counter)
  </read_first>
  <action>
    In the SUMMARY block, the SMOKE-line list currently ends at L4488 with:
    ```r
    message("  * SMOKE-124-01: R/88 validates Phase 124 R/110 output-level before/after report + unmapped-name audit structural integrity (Section 15v, 13 checks)")
    ```
    Insert ONE new `message(...)` line immediately AFTER that SMOKE-124-01 line (and before the `if (failed > 0) { quit(status = 1) }` block at L4490), matching the exact format:
    ```r
    message("  * SMOKE-130-01: R/88 validates Phase 130 DoI layer (R/111 classification + R/112 attribution) structural integrity incl. mutual-exclusivity hard-stop (Section 15w, ~14 checks)")
    ```
    Also add a DOI-QA line documenting the requirements this section validates, immediately after the SMOKE-130-01 line:
    ```r
    message("  * DOI-QA-01/02: R/39 + SCRIPT_INDEX registration and R/88 Section 15w DoI validation (Phase 130)")
    ```
    (No numeric `[N/N]` counter change is required: lettered sub-sections 15p-15v use `[Phase NNN]` message prefixes, not numeric counters; the terminal numeric counter `[43/43]` at L4239 is the fixture-schema section and is unaffected. If the executor finds a literal running total that MUST reflect section count, leave the numeric counters unchanged — the D-02 "bump the [N/N] counter" intent is satisfied by following the established lettered-section convention.)
  </action>
  <acceptance_criteria>
    - `grep -n 'SMOKE-130-01' R/88_smoke_test_comprehensive.R` matches exactly one line, located AFTER the `SMOKE-124-01` line and BEFORE the `if (failed > 0)` line.
    - The SMOKE-130-01 line contains the substrings `Section 15w`, `mutual-exclusivity`, and `Phase 130`.
    - `grep -n 'DOI-QA-01/02' R/88_smoke_test_comprehensive.R` matches (requirement traceability line present).
    - The line uses the exact `message("  * SMOKE-130-01: ...")` prefix format matching the surrounding SMOKE lines.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — R not installed locally. Structural verification only: `grep -n "SMOKE-130-01\|SMOKE-124-01\|if (failed > 0)" R/88_smoke_test_comprehensive.R` confirms SMOKE-130-01 sits between SMOKE-124-01 and the quit block.</automated>
  </verify>
  <done>R/88 SUMMARY block lists SMOKE-130-01 (Section 15w) and a DOI-QA-01/02 traceability line, in the correct position and format.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-UAT — HiPerGator real-data runtime pass with logged DoI category counts (DOI-QA-03, v3.3 DoD gate)</name>
  <action>
    This is a HUMAN-VERIFY checkpoint (DOI-QA-03), not an autonomous code task — R cannot run on the local Windows dev box. The executor's action here is to PAUSE and hand the runtime instructions (in how-to-verify) to the user, then, once the user pastes back the real-data log, RECORD the verbatim DoI category counts and the three confirmations into 130-02-SUMMARY.md and the STATE.md phase-close entry. Do NOT attempt to run R locally. Do NOT mark this phase's DoD satisfied on structural checks alone (D-01a: a structural-only pass is insufficient). The recorded verbatim counts are the artifact that resolves the PROJECT.md "attested-by-prose only" verification flag.
  </action>
  <read_first>
    - .planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-CONTEXT.md (D-01 human-verify checkpoint, D-01a logged counts recorded verbatim + resolves PROJECT.md prose-only flag, D-07 real counts carry clinical meaning)
    - .planning/PROJECT.md (the "Dual-environment verification ... Revisit (Phase 126 attested-by-prose only)" flag this task resolves)
  </read_first>
  <what-built>
    Windows-side deliverables (autonomous, completed in Plan 01 + Task 1/2 above):
    - R/39 registers R/111 -> R/112 (dependency order) and expects doi_attribution_report.xlsx.
    - R/SCRIPT_INDEX.md documents both scripts with correct roles.
    - R/88 Section 15w validates the DoI layer structurally, including the mutual-exclusivity hard-stop, with IS_LOCAL-gated runtime blocks that only exercise real DIAGNOSIS-table outputs on HiPerGator.

    What remains is the ONE thing that cannot run on Windows: the real-data runtime pass on HiPerGator. This is the v3.3 definition-of-done gate (DOI-QA-03) and, per D-01a, resolves the PROJECT.md "attested-by-prose only" verification flag with an actual log.
  </what-built>
  <how-to-verify>
    On HiPerGator (RStudio or SLURM), from the project root, in this order:
    1. `module load R/4.4.2` (or the pinned version), start R.
    2. Ensure IS_LOCAL is FALSE (production mode — real DIAGNOSIS table available).
    3. Run the DoI producers end-to-end (either path is acceptable per CONTEXT D-04-discretion):
       - Option A: `Rscript R/39_run_all_investigations.R` (runs R/111 then R/112 in order among the full pipeline), OR
       - Option B: `source("R/111_doi_classification.R")` then `source("R/112_doi_attribution_report.R")` directly.
    4. Confirm the mutual-exclusivity hard-stop in R/111 did NOT halt the script (zero DoI/cancer code collision on real data).
    5. Run the smoke test: `source("R/88_smoke_test_comprehensive.R")` (or `Rscript R/88_smoke_test_comprehensive.R`). Section 15w Checks 12-14 now execute the real-data branches and PRINT the DoI category counts (look for the `[Phase 130 RUNTIME] DoI category counts` log block).
    6. Confirm ALL R/88 checks pass (0 failures) and Section 15w's runtime checks are green (not SKIPPED).
    7. COPY the printed DoI category count table verbatim.

    Clinical-plausibility sanity signal (D-01a): Rheumatoid Arthritis should DOMINATE; Neurological Autoimmune (NMO) and Pemphigus should be RARE. If the distribution is wildly off (e.g., pemphigus dominant), flag it rather than approving.

    Paste back into this checkpoint: (a) the verbatim DoI category counts, (b) confirmation R/88 passed with 0 failures, (c) confirmation the mutual-exclusivity hard-stop did not fire on real data.
  </how-to-verify>
  <acceptance_criteria>
    - The user has pasted the verbatim DoI category counts from the real DIAGNOSIS-table run.
    - The pasted counts show RA dominant with NMO/pemphigus rare (or the user has explicitly flagged an implausible distribution for investigation).
    - The user confirms R/88 reported 0 failed checks (Section 15w runtime branches executed, not skipped).
    - The user confirms the mutual-exclusivity hard-stop did NOT halt R/111 on real data.
    - The verbatim counts + the three confirmations are recorded in the phase completion/transition notes (the 130-02-SUMMARY.md and/or STATE.md phase-close entry) — NOT prose attestation. This is the artifact that resolves the PROJECT.md "attested-by-prose only" flag.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this gate is a HUMAN action on HiPerGator (R not installed locally). Machine-verifiable proxy: after the user pastes the log, confirm 130-02-SUMMARY.md contains a verbatim DoI category count table and the string "0 failed" / equivalent R/88 pass confirmation — `grep -n "DoI category counts\|Rheumatoid Arthritis" .planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-02-SUMMARY.md`.</automated>
  </verify>
  <done>User has pasted the verbatim real-data DoI category counts (RA dominant; NMO/pemphigus rare) plus confirmations that R/88 passed with 0 failures and the mutual-exclusivity hard-stop did not fire; these are recorded verbatim in 130-02-SUMMARY.md and the STATE.md phase-close entry.</done>
  <resume-signal>Paste the verbatim DoI category counts and the three confirmations, or describe any failure / implausible distribution. Type "approved" only alongside the pasted log.</resume-signal>
</task>

</tasks>

<verification>
- Section 15w exists in R/88 between 15v and 15g with ~14 checks (structural + in-memory), including the mutual-exclusivity hard-stop (intersect DOI vs cancer keys == 0).
- Checks 1-11 written to pass locally on Windows (no R execution needed to author them; verified via grep for required tokens).
- Checks 12-14 are !IS_LOCAL-gated real-data blocks that stay green locally and log DoI counts on HiPerGator.
- SMOKE-130-01 + DOI-QA-01/02 lines in the SUMMARY block.
- HUMAN-UAT: real-data DoI category counts logged verbatim in phase notes (DOI-QA-03 DoD gate); resolves PROJECT.md prose-only flag.
</verification>

<success_criteria>
DOI-QA-02 satisfied: R/88 Section 15w validates the DoI layer structurally with the mutual-exclusivity hard-stop as the primary guardrail, mirroring the 15v pattern.
DOI-QA-03 satisfied: the DoI layer's real-data correctness is confirmed by a logged HiPerGator runtime pass (human-verify checkpoint), with DoI category counts recorded verbatim in the phase completion notes — closing the v3.3 definition-of-done and resolving the PROJECT.md dual-environment prose-only flag.
</success_criteria>

<output>
After completion, create `.planning/phases/130-registration-smoke-test-and-hipergator-runtime/130-02-SUMMARY.md` — and record the verbatim HiPerGator DoI category counts (from Task 3) in it.
</output>
