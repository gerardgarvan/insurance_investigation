---
phase: 41-combine-ndc-hcpcs-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/41_combine_reports.R
  - output/combined_unmatched_report.xlsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Running R/41_combine_reports.R produces output/combined_unmatched_report.xlsx"
    - "Combined report contains codes from both HCPCS/CPT (Phase 39) and NDC/RXNORM (Phase 40)"
    - "Summary sheet shows unified classification counts across all code types"
    - "Per-category sheets display codes from all source tables with consistent columns"
    - "SCT classification is unified (no separate SCT-related category)"
  artifacts:
    - path: "R/41_combine_reports.R"
      provides: "Combined report generation script"
      min_lines: 180
    - path: "output/combined_unmatched_report.xlsx"
      provides: "Consolidated xlsx workbook with Summary + per-category sheets"
  key_links:
    - from: "R/41_combine_reports.R"
      to: "output/unmatched_codes_classified.rds"
      via: "readRDS()"
      pattern: "readRDS.*unmatched_codes_classified"
    - from: "R/41_combine_reports.R"
      to: "output/unmatched_ndc_classified.rds"
      via: "readRDS()"
      pattern: "readRDS.*unmatched_ndc_classified"
    - from: "R/41_combine_reports.R"
      to: "output/combined_unmatched_report.xlsx"
      via: "wb$save()"
      pattern: "wb\\$save.*combined_unmatched_report"
---

<objective>
Create a standalone R script that merges the Phase 39 (CPT/HCPCS) and Phase 40 (NDC/RXNORM) unmatched code investigation RDS artifacts into a single consolidated xlsx report with unified classification, cross-source summary statistics, and per-category detail sheets.

Purpose: Provide a single unified view of all unmatched treatment codes across code types and PCORnet source tables, enabling faster review and consistent classification tracking.
Output: `R/41_combine_reports.R` script and `output/combined_unmatched_report.xlsx` workbook.
</objective>

<execution_context>
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\Documents\insurance_investigation\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-combine-ndc-hcpcs-reports/41-RESEARCH.md

<interfaces>
<!-- Key patterns from Phase 39 and Phase 40 scripts that the executor needs -->

From R/39_investigate_unmatched.R (Phase 39 TREATMENT_TYPE_COLORS):
```r
TREATMENT_TYPE_COLORS <- list(
  Chemotherapy      = list(fill = "FFDCEEFB", font = "FF0B5394"),   # light blue / dark blue
  Radiation         = list(fill = "FFDDF4E1", font = "FF274E13"),   # light green / dark green
  SCT               = list(fill = "FFFFF4D6", font = "FF7F6000"),   # light yellow / dark olive
  Immunotherapy     = list(fill = "FFE8DCF4", font = "FF4C1D7A"),   # light purple / dark purple
  `Supportive Care` = list(fill = "FFD5F5F0", font = "FF0E6655"),   # light teal / dark teal
  Unrelated         = list(fill = "FFF3F4F6", font = "FF6B7280")    # light gray / medium gray
)
```

From R/40_investigate_unmatched_ndc.R (Phase 40 bulk write pattern):
```r
# Bulk data write (not cell-by-cell)
write_df <- data.frame(
  Code = df_cat$code,
  Drug_Name = ifelse(is.na(df_cat$drug_name), "", df_cat$drug_name),
  Code_Type = df_cat$code_type,
  Source_Table = df_cat$source_table,
  Records = df_cat$n_records,
  Patients = df_cat$n_patients,
  Lookup_Status = df_cat$lookup_status,
  stringsAsFactors = FALSE
)
wb$add_data(sheet = sheet_name, x = write_df, start_row = 5, col_names = FALSE)

# Range-based styling (O(categories) not O(n*cols))
last_row <- 4 + nrow(df_cat)
code_dims <- glue("A5:A{last_row}")
wb$add_fill(sheet = sheet_name, dims = code_dims, color = wb_color(fill_color))
wb$add_font(sheet = sheet_name, dims = code_dims,
            name = "Calibri", size = 10, bold = TRUE, color = wb_color(font_color))
```

Phase 39 RDS schema (unmatched_codes_classified.rds):
- code (character): CPT/HCPCS code
- n_records (integer): Record count
- n_patients (integer): Patient count
- heuristic_type (character): Which heuristic matched
- description (character): From NLM HCPCS API
- lookup_status (character): API result status
- classification (character): Category (Chemotherapy/Radiation/SCT/Immunotherapy/Supportive Care/Unrelated)

Phase 40 RDS schema (unmatched_ndc_classified.rds):
- code (character): NDC or RXNORM code
- code_type (character): "NDC" or "RXNORM"
- source_table (character): DISPENSING/PRESCRIBING/MED_ADMIN
- n_records (integer): Record count
- n_patients (integer): Patient count
- raw_drug_name (character): From PCORnet table
- drug_name (character): From RxNorm API (or raw fallback)
- lookup_status (character): API result status
- classification (character): Category (uses "SCT-related" not "SCT")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create combined report script R/41_combine_reports.R</name>
  <files>R/41_combine_reports.R</files>
  <read_first>
    R/40_investigate_unmatched_ndc.R (lines 44-51 for TREATMENT_TYPE_COLORS, lines 500-700 for bulk write pattern)
    R/39_investigate_unmatched.R (lines 69-76 for TREATMENT_TYPE_COLORS, lines 300-460 for report structure)
    R/00_config.R (lines 29-37 for CONFIG$output_dir path)
  </read_first>
  <action>
Create `R/41_combine_reports.R` with these sections:

SECTION 1: SETUP AND CONFIGURATION
- `suppressPackageStartupMessages()` for library(dplyr), library(glue), library(openxlsx2), library(stringr)
- `source("R/00_config.R")`
- Define OUTPUT_PATH as `file.path(CONFIG$output_dir, "combined_unmatched_report.xlsx")`
- Define RDS paths:
  - `HCPCS_RDS <- file.path(CONFIG$output_dir, "unmatched_codes_classified.rds")`
  - `NDC_RDS <- file.path(CONFIG$output_dir, "unmatched_ndc_classified.rds")`
- Define TREATMENT_TYPE_COLORS using the Phase 39 keys (with "SCT" not "SCT-related"):
  ```
  TREATMENT_TYPE_COLORS <- list(
    Chemotherapy      = list(fill = "FFDCEEFB", font = "FF0B5394"),
    Radiation         = list(fill = "FFDDF4E1", font = "FF274E13"),
    SCT               = list(fill = "FFFFF4D6", font = "FF7F6000"),
    Immunotherapy     = list(fill = "FFE8DCF4", font = "FF4C1D7A"),
    `Supportive Care` = list(fill = "FFD5F5F0", font = "FF0E6655"),
    Unrelated         = list(fill = "FFF3F4F6", font = "FF6B7280")
  )
  ```
- Define `category_order <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy", "Supportive Care", "Unrelated")`

SECTION 2: LOAD AND HARMONIZE RDS ARTIFACTS
- Guard: check both RDS files exist with `file.exists()`. If either missing, stop() with message explaining user must run Phase 39/40 scripts first.
- Load Phase 39: `hcpcs_classified <- readRDS(HCPCS_RDS)`
- Load Phase 40: `ndc_classified <- readRDS(NDC_RDS)`
- Harmonize Phase 39 to unified schema:
  ```
  hcpcs_harmonized <- hcpcs_classified %>%
    mutate(
      code_type = "CPT/HCPCS",
      source_table = "PROCEDURES"
    ) %>%
    select(code, code_type, source_table, description, n_records, n_patients,
           classification, heuristic_type, lookup_status)
  ```
- Harmonize Phase 40 to unified schema:
  ```
  ndc_harmonized <- ndc_classified %>%
    mutate(
      description = drug_name,
      heuristic_type = NA_character_,
      classification = if_else(classification == "SCT-related", "SCT", classification)
    ) %>%
    select(code, code_type, source_table, description, n_records, n_patients,
           classification, heuristic_type, lookup_status)
  ```
- Combine: `all_codes <- bind_rows(hcpcs_harmonized, ndc_harmonized)`
- Log: message with total code count, code type breakdown, classification breakdown

SECTION 3: WRITE COMBINED XLSX REPORT (function `write_combined_report(df, output_path)`)

Use openxlsx2 `wb_workbook()` API with Phase 40's bulk-write pattern throughout.

Summary sheet structure:
- Row 1: Title "Combined Unmatched Code Investigation" (Calibri 16pt bold, color FF1F2937)
- Row 2: Subtitle "Phase 39 (CPT/HCPCS) + Phase 40 (NDC/RXNORM) — consolidated view" (Calibri 10pt, color FF6B7280)
- Row 3: Date (Sys.Date())
- Row 5: "By Classification" section header (bold)
- Row 6: Headers "Classification | Codes | Records | Patients" with dark gray fill (FF374151) and white bold font
- Rows 7+: Per-classification summary (group_by(classification), n_distinct(code), sum(n_records), sum(n_patients)), ordered by category_order
- After classification data: Total row with same dark gray fill/white font styling
- Below total: "By Code Type" section with headers and breakdown (CPT/HCPCS, NDC, RXNORM)
- Below code type: "By Source Table" section with headers and breakdown (PROCEDURES, DISPENSING, PRESCRIBING, MED_ADMIN)
- Column widths: c(22, 12, 12, 12)
- Number format "#,##0" on numeric columns

Per-category sheets (one for each non-empty category in category_order):
- Row 1: Title "Combined Unmatched Code Investigation" (Calibri 16pt bold, merged A1:G1)
- Row 2: Subtitle "{category}: {n} codes from {n_types} code types" (Calibri 10pt, merged A2:G2)
- Row 3: blank
- Row 4: Headers "Code | Description | Code Type | Source Table | Records | Patients | Lookup Status" with dark gray fill (FF374151) and white bold font (A4:G4)
- Row 5+: Bulk data write using data.frame:
  ```
  write_df <- data.frame(
    Code = df_cat$code,
    Description = ifelse(is.na(df_cat$description), "", df_cat$description),
    Code_Type = df_cat$code_type,
    Source_Table = df_cat$source_table,
    Records = df_cat$n_records,
    Patients = df_cat$n_patients,
    Lookup_Status = df_cat$lookup_status,
    stringsAsFactors = FALSE
  )
  wb$add_data(sheet = sheet_name, x = write_df, start_row = 5, col_names = FALSE)
  ```
- Range-based styling after bulk write:
  - Code column (A5:A{last_row}): fill_color from TREATMENT_TYPE_COLORS, font bold
  - Text columns (B5:G{last_row}): Calibri 10pt, color FF111827
  - Numeric columns (E5:F{last_row}): numfmt "#,##0"
- Freeze pane at row 5
- Column widths: c(15, 45, 12, 15, 10, 10, 15)
- Sort data within each category by n_patients descending

SECTION 4: MAIN EXECUTION
- Print header message "=== Phase 41: Combine Unmatched Code Reports ==="
- Step 1: Load and harmonize (call Section 2 logic)
- Step 2: Write combined report (call write_combined_report)
- Print completion message with output path

IMPORTANT: Do NOT use cell-by-cell writing for data rows. Use Phase 40's bulk `wb$add_data(x = write_df, col_names = FALSE)` pattern for all per-category sheets. Summary sheet may use cell-by-cell for its multi-section layout (consistent with Phase 40's summary approach).
  </action>
  <verify>
    <automated>cd "C:\Users\Owner\Documents\insurance_investigation" && Rscript -e "parse('R/41_combine_reports.R')" 2>&1 | findstr /V "^$"</automated>
  </verify>
  <acceptance_criteria>
    - R/41_combine_reports.R exists with 180+ lines
    - File contains `readRDS(HCPCS_RDS)` and `readRDS(NDC_RDS)`
    - File contains `classification == "SCT-related", "SCT"` remapping
    - File contains `wb_workbook()` call
    - File contains `wb$add_data(sheet = sheet_name, x = write_df, start_row = 5, col_names = FALSE)` bulk write pattern
    - File contains TREATMENT_TYPE_COLORS with key "SCT" (not "SCT-related")
    - File contains `category_order <- c("Chemotherapy", "Radiation", "SCT", "Immunotherapy", "Supportive Care", "Unrelated")`
    - File contains `wb$save(output_path)`
    - File passes `parse("R/41_combine_reports.R")` without error
    - Summary sheet has three sections: By Classification, By Code Type, By Source Table
    - Per-category sheets have 7 columns: Code, Description, Code Type, Source Table, Records, Patients, Lookup Status
    - File contains guard checking `file.exists(HCPCS_RDS)` and `file.exists(NDC_RDS)` with stop() on missing
  </acceptance_criteria>
  <done>R/41_combine_reports.R is a syntactically valid R script that reads both RDS artifacts, harmonizes schemas (including SCT-related -> SCT remapping), and produces a styled combined xlsx report using Phase 40's bulk-write pattern. The script parses without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify combined report runs and produces correct output</name>
  <files>output/combined_unmatched_report.xlsx</files>
  <action>User runs `source("R/41_combine_reports.R")` in RStudio on HiPerGator and verifies the combined xlsx report is produced with correct content and formatting.</action>
  <what-built>R/41_combine_reports.R script that merges Phase 39 and Phase 40 unmatched code investigation outputs into a single consolidated xlsx workbook.</what-built>
  <how-to-verify>
    1. In RStudio on HiPerGator, run: `source("R/41_combine_reports.R")`
    2. Verify console output shows:
       - Total code count (sum of Phase 39 + Phase 40 codes)
       - Classification breakdown with unified "SCT" (no "SCT-related")
       - Code type breakdown (CPT/HCPCS, NDC, RXNORM)
       - "Saved report: output/combined_unmatched_report.xlsx"
    3. Open output/combined_unmatched_report.xlsx and verify:
       - Summary sheet has By Classification, By Code Type, By Source Table sections
       - Per-category sheets (Chemotherapy, Radiation, etc.) show codes from BOTH procedure and drug sources
       - Code column has colored pill styling matching the classification
       - Columns are: Code, Description, Code Type, Source Table, Records, Patients, Lookup Status
       - No "SCT-related" category exists (unified to "SCT")
  </how-to-verify>
  <verify>
    <automated>Rscript -e "stopifnot(file.exists('output/combined_unmatched_report.xlsx'))"</automated>
  </verify>
  <acceptance_criteria>
    - output/combined_unmatched_report.xlsx exists after running the script
    - Console output contains "Saved report:" message
    - Console output does NOT contain "SCT-related" in classification breakdown
    - xlsx Summary sheet has 3 sections visible
    - Per-category sheets contain rows from multiple code types (CPT/HCPCS and NDC/RXNORM)
  </acceptance_criteria>
  <done>Combined report successfully produced and visually verified by user. Contains unified codes from all sources with consistent formatting.</done>
  <resume-signal>Type "approved" or describe any issues with the report output</resume-signal>
</task>

</tasks>

<verification>
1. `parse("R/41_combine_reports.R")` succeeds (no syntax errors)
2. Script sources R/00_config.R for CONFIG paths
3. Script reads both RDS artifacts (unmatched_codes_classified.rds, unmatched_ndc_classified.rds)
4. Output xlsx has Summary + up to 6 per-category sheets
5. SCT classification unified (no "SCT-related" in output)
6. Bulk write pattern used for data rows (not cell-by-cell)
</verification>

<success_criteria>
- R/41_combine_reports.R parses without error
- Running the script produces output/combined_unmatched_report.xlsx
- Combined report contains codes from both PROCEDURES (Phase 39) and DISPENSING/PRESCRIBING/MED_ADMIN (Phase 40)
- Summary sheet shows cross-source statistics
- Per-category sheets use Phase 40 bulk-write pattern with range-based styling
- "SCT-related" remapped to "SCT" throughout
</success_criteria>

<output>
After completion, create `.planning/phases/41-combine-ndc-hcpcs-reports/41-01-SUMMARY.md`
</output>
