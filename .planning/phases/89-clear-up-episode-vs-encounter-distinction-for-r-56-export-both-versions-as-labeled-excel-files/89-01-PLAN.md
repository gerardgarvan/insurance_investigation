---
phase: 89-clear-up-episode-vs-encounter-distinction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/56_new_tables_from_groupings.R
  - R/57_drug_grouping_instances.R
  - R/58_code_reference_tables.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements:
  - P89-D01
  - P89-D02
  - P89-D03
  - P89-D04
  - P89-D05
  - P89-D06

must_haves:
  truths:
    - "R/56 produces episode_level_drug_grouping_tables.xlsx alongside drug_grouping_tables.xlsx with identical content"
    - "R/57 produces encounter_level_drug_grouping_instances.xlsx alongside drug_grouping_instances.xlsx with identical content"
    - "R/56 sheet names contain 'Ep:' prefix indicating episode-level grain"
    - "R/57 sheet names contain 'Enc:' prefix indicating encounter-level grain"
    - "R/58 reads the new filename and sheet name without breaking"
    - "R/88 smoke test checks verify new filenames and sheet names exist in scripts"
  artifacts:
    - path: "R/56_new_tables_from_groupings.R"
      provides: "Episode-level drug grouping tables with dual-output and grain-prefixed sheets"
      contains: "episode_level_drug_grouping_tables.xlsx"
    - path: "R/57_drug_grouping_instances.R"
      provides: "Encounter-level drug grouping instances with dual-output and grain-prefixed sheets"
      contains: "encounter_level_drug_grouping_instances.xlsx"
    - path: "R/58_code_reference_tables.R"
      provides: "Code reference that reads from new R/56 filename and sheet name"
      contains: "Ep: Sub-Category Summary"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Smoke test validating new filenames and sheet names"
      contains: "episode_level_drug_grouping_tables"
  key_links:
    - from: "R/56_new_tables_from_groupings.R"
      to: "output/episode_level_drug_grouping_tables.xlsx"
      via: "wb$save(NEW_OUTPUT_XLSX) + wb$save(OLD_OUTPUT_XLSX)"
      pattern: "wb\\$save\\("
    - from: "R/58_code_reference_tables.R"
      to: "R/56 output xlsx"
      via: "wb_to_df(drug_wb, sheet = 'Ep: Sub-Category Summary')"
      pattern: "Ep: Sub-Category Summary"
---

<objective>
Rename R/56 and R/57 output filenames and sheet names to self-document their data grain (episode-level vs encounter-level), produce both old and new filenames for backward compatibility, and update downstream consumers (R/58, R/88) to reference new names.

Purpose: Eliminate ambiguity about which file contains episode-level aggregations vs encounter-level detail, enabling users to select the correct file without opening both.

Output: Updated R/56, R/57, R/58, and R/88 scripts with grain-prefixed filenames, abbreviated sheet names (within Excel's 31-char limit), and dual-output for backward compatibility.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-clear-up-episode-vs-encounter-distinction-for-r-56-export-both-versions-as-labeled-excel-files/89-CONTEXT.md
@.planning/phases/89-clear-up-episode-vs-encounter-distinction-for-r-56-export-both-versions-as-labeled-excel-files/89-RESEARCH.md

<interfaces>
<!-- Key patterns from R/56 and R/57 that the executor will modify in-place. -->

From R/56_new_tables_from_groupings.R (current Section 1, line 81):
```r
OUTPUT_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_tables.xlsx")
```

From R/56_new_tables_from_groupings.R (current Section 7, lines 571-588):
```r
wb <- wb_workbook()
wb$add_worksheet("Treatment Sub-Category Summary")
wb$add_data("Treatment Sub-Category Summary", table1, start_row = 1, col_names = TRUE)
wb$add_worksheet("Encounter Treatment Summary")
wb$add_data("Encounter Treatment Summary", table2, start_row = 1, col_names = TRUE)
wb$save(OUTPUT_XLSX)
```

From R/57_drug_grouping_instances.R (current Section 1, line 59):
```r
OUTPUT_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_instances.xlsx")
```

From R/57_drug_grouping_instances.R (current Section 7, lines 403-420):
```r
wb <- wb_workbook()
wb$add_worksheet("Treatment Sub-Category Detail")
wb$add_data("Treatment Sub-Category Detail", table1, start_row = 1, col_names = TRUE)
wb$add_worksheet("Encounter Treatment Detail")
wb$add_data("Encounter Treatment Detail", table2, start_row = 1, col_names = TRUE)
wb$save(OUTPUT_XLSX)
```

From R/58_code_reference_tables.R (lines 40, 358):
```r
DRUG_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_tables.xlsx")
drug_raw <- wb_to_df(drug_wb, sheet = "Treatment Sub-Category Summary")
```

From R/88_smoke_test_comprehensive.R (lines 1005-1010, 1437-1442):
```r
check("R/56 outputs drug_grouping_tables.xlsx",
      any(grepl("drug_grouping_tables\\.xlsx", r56_lines)))
check("R/56 has 2-sheet workbook (Sub-Category Summary + Encounter Treatment Summary)",
      any(grepl("Treatment Sub-Category Summary", r56_lines)) &&
      any(grepl("Encounter Treatment Summary", r56_lines)))

check("R/57 outputs drug_grouping_instances.xlsx",
      any(grepl("drug_grouping_instances\\.xlsx", r57_lines)))
check("R/57 has 2-sheet workbook (Treatment Sub-Category Detail + Encounter Treatment Detail)",
      any(grepl("Treatment Sub-Category Detail", r57_lines)) &&
      any(grepl("Encounter Treatment Detail", r57_lines)))
```
</interfaces>

**CRITICAL CONSTRAINT -- Excel 31-char sheet name limit:**
All D-04/D-05 sheet names from CONTEXT.md exceed Excel's 31-character limit. Research-recommended abbreviations used instead:

| Script | Old Sheet Name | New Sheet Name | Chars |
|--------|----------------|----------------|-------|
| R/56 | "Treatment Sub-Category Summary" | "Ep: Sub-Category Summary" | 26 |
| R/56 | "Encounter Treatment Summary" | "Ep: Encounter Treatment" | 25 |
| R/57 | "Treatment Sub-Category Detail" | "Enc: Sub-Category Detail" | 26 |
| R/57 | "Encounter Treatment Detail" | "Enc: Treatment Detail" | 22 |

"Ep:" = Episode-level, "Enc:" = Encounter-level. All within 31-char limit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update R/56 with episode-level filename, grain-prefixed sheets, and dual output</name>
  <files>R/56_new_tables_from_groupings.R</files>
  <read_first>
    R/56_new_tables_from_groupings.R
    .planning/phases/89-clear-up-episode-vs-encounter-distinction-for-r-56-export-both-versions-as-labeled-excel-files/89-CONTEXT.md
  </read_first>
  <action>
Modify R/56_new_tables_from_groupings.R in-place with these specific changes:

**Section 1 (Setup), around line 81:**
Replace the single OUTPUT_XLSX variable with dual-output paths:
```r
# Phase 89: Dual-output file paths (episode-level grain)
NEW_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "episode_level_drug_grouping_tables.xlsx")
OLD_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_tables.xlsx")  # Backward compat
```

**Section 1, log intro around line 100:**
Update the message that logs the output path from:
```r
message(glue("  Output: {OUTPUT_XLSX}"))
```
to:
```r
message(glue("  Output (new): {NEW_OUTPUT_XLSX}"))
message(glue("  Output (compat): {OLD_OUTPUT_XLSX}"))
```

**Header comment block (lines 20-22):**
Update the Outputs comment from:
```r
#   - output/drug_grouping_tables.xlsx (2-sheet workbook:
#     Sheet 1 = "Treatment Sub-Category Summary", Sheet 2 = "Encounter Treatment Summary")
```
to:
```r
#   - output/episode_level_drug_grouping_tables.xlsx (2-sheet workbook:
#     Sheet 1 = "Ep: Sub-Category Summary", Sheet 2 = "Ep: Encounter Treatment")
#   - output/drug_grouping_tables.xlsx (backward compat copy, identical content)
```

**Section 7 (Write XLSX Output), lines 576-588:**
Replace the current write block with grain-prefixed sheet names and dual save.
Use "Ep: Sub-Category Summary" (26 chars) and "Ep: Encounter Treatment" (25 chars) — both within Excel's 31-char limit.

New Section 7:
```r
# SECTION 7: WRITE XLSX OUTPUT (per D-12, Phase 89: grain-labeled) ----

message()
message("--- Writing multi-sheet XLSX output (episode-level grain) ---")

wb <- wb_workbook()

# Sheet 1: Episode-Level Sub-Category Summary (abbreviated for 31-char Excel limit)
wb$add_worksheet("Ep: Sub-Category Summary")
wb$add_data("Ep: Sub-Category Summary", table1, start_row = 1, col_names = TRUE)

# Sheet 2: Episode-Level Encounter Treatment (abbreviated for 31-char Excel limit)
wb$add_worksheet("Ep: Encounter Treatment")
wb$add_data("Ep: Encounter Treatment", table2, start_row = 1, col_names = TRUE)

# Save to new self-documenting filename (per D-01)
wb$save(NEW_OUTPUT_XLSX)
message(glue("Saved (new): {NEW_OUTPUT_XLSX}"))

# Save to old filename for backward compatibility (per D-03)
wb$save(OLD_OUTPUT_XLSX)
message(glue("Saved (backward compat): {OLD_OUTPUT_XLSX}"))
```

**Section 8 (Console Summary), around line 600:**
Replace the single output path reference. Change:
```r
message(glue("Saved: {OUTPUT_XLSX}"))
```
No — that line is in Section 7 (already updated above). In Section 8, update:
- Line 606 "Table 1 (Sub-Category Summary):" -> "Table 1 (Ep: Sub-Category Summary):"
- Line 613 "Table 2 (Encounter Treatment Summary):" -> "Table 2 (Ep: Encounter Treatment):"
- Add at end before "Done.":
```r
message()
message(glue("  Output files:"))
message(glue("    {NEW_OUTPUT_XLSX} (primary)"))
message(glue("    {OLD_OUTPUT_XLSX} (backward compatibility)"))
```

**All other references to OUTPUT_XLSX in the file:**
Search for any remaining references to the old single `OUTPUT_XLSX` variable and replace with `NEW_OUTPUT_XLSX` or `OLD_OUTPUT_XLSX` as appropriate. The LOG_FILE variable (line 84) should NOT change — it still uses the script number.
  </action>
  <verify>
    <automated>powershell -Command "
      $content = Get-Content 'R/56_new_tables_from_groupings.R' -Raw;
      $checks = @(
        ($content -match 'episode_level_drug_grouping_tables\.xlsx'),
        ($content -match 'Ep: Sub-Category Summary'),
        ($content -match 'Ep: Encounter Treatment'),
        ($content -match 'NEW_OUTPUT_XLSX'),
        ($content -match 'OLD_OUTPUT_XLSX'),
        ($content -match 'wb\$save\(NEW_OUTPUT_XLSX\)'),
        ($content -match 'wb\$save\(OLD_OUTPUT_XLSX\)'),
        (-not ($content -match '(?<!\w)OUTPUT_XLSX(?!\w)' -and $content -notmatch 'NEW_OUTPUT_XLSX|OLD_OUTPUT_XLSX|REFERENCE_XLSX'))
      );
      $passed = ($checks | Where-Object {$_}).Count;
      Write-Output \"PASS: $passed/7 checks\";
      if ($passed -lt 7) { exit 1 }
    "
    </automated>
  </verify>
  <acceptance_criteria>
    - R/56 contains `NEW_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "episode_level_drug_grouping_tables.xlsx")`
    - R/56 contains `OLD_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_tables.xlsx")`
    - R/56 contains `wb$add_worksheet("Ep: Sub-Category Summary")`
    - R/56 contains `wb$add_data("Ep: Sub-Category Summary"`
    - R/56 contains `wb$add_worksheet("Ep: Encounter Treatment")`
    - R/56 contains `wb$add_data("Ep: Encounter Treatment"`
    - R/56 contains two `wb$save()` calls: one for NEW_OUTPUT_XLSX, one for OLD_OUTPUT_XLSX
    - R/56 does NOT contain bare `OUTPUT_XLSX` (only NEW_OUTPUT_XLSX, OLD_OUTPUT_XLSX, REFERENCE_XLSX)
    - R/56 header comment references "episode_level_drug_grouping_tables.xlsx"
  </acceptance_criteria>
  <done>R/56 saves to both episode_level_drug_grouping_tables.xlsx (primary) and drug_grouping_tables.xlsx (backward compat) with "Ep:" prefixed sheet names, all within 31-char limit</done>
</task>

<task type="auto">
  <name>Task 2: Update R/57, R/58, and R/88 for encounter-level naming and downstream compatibility</name>
  <files>R/57_drug_grouping_instances.R, R/58_code_reference_tables.R, R/88_smoke_test_comprehensive.R</files>
  <read_first>
    R/57_drug_grouping_instances.R
    R/58_code_reference_tables.R
    R/88_smoke_test_comprehensive.R
    .planning/phases/89-clear-up-episode-vs-encounter-distinction-for-r-56-export-both-versions-as-labeled-excel-files/89-CONTEXT.md
  </read_first>
  <action>
**A) Modify R/57_drug_grouping_instances.R in-place:**

**Section 1 (Setup), line 59:**
Replace single OUTPUT_XLSX with dual paths:
```r
# Phase 89: Dual-output file paths (encounter-level grain)
NEW_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "encounter_level_drug_grouping_instances.xlsx")
OLD_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_instances.xlsx")  # Backward compat
```

**Section 1, log intro around line 78:**
Update output logging from `message(glue("  Output: {OUTPUT_XLSX}"))` to:
```r
message(glue("  Output (new): {NEW_OUTPUT_XLSX}"))
message(glue("  Output (compat): {OLD_OUTPUT_XLSX}"))
```

**Header comment block (lines 23-25):**
Update Outputs comment to:
```r
#   - output/encounter_level_drug_grouping_instances.xlsx (2-sheet workbook:
#     Sheet 1 = "Enc: Sub-Category Detail" (encounter-level),
#     Sheet 2 = "Enc: Treatment Detail" (encounter-level))
#   - output/drug_grouping_instances.xlsx (backward compat copy, identical content)
```

**Section 7 (Write XLSX Output), lines 403-420:**
Replace with grain-prefixed sheet names and dual save.
Use "Enc: Sub-Category Detail" (26 chars) and "Enc: Treatment Detail" (22 chars) — both within 31-char limit.

New Section 7:
```r
# SECTION 7: WRITE XLSX OUTPUT (Phase 89: grain-labeled) ----

message()
message("--- Writing multi-sheet XLSX output (encounter-level grain) ---")

wb <- wb_workbook()

# Sheet 1: Encounter-Level Sub-Category Detail (abbreviated for 31-char Excel limit)
wb$add_worksheet("Enc: Sub-Category Detail")
wb$add_data("Enc: Sub-Category Detail", table1, start_row = 1, col_names = TRUE)

# Sheet 2: Encounter-Level Treatment Detail (abbreviated for 31-char Excel limit)
wb$add_worksheet("Enc: Treatment Detail")
wb$add_data("Enc: Treatment Detail", table2, start_row = 1, col_names = TRUE)

# Save to new self-documenting filename (per D-02)
wb$save(NEW_OUTPUT_XLSX)
message(glue("Saved (new): {NEW_OUTPUT_XLSX}"))

# Save to old filename for backward compatibility (per D-03)
wb$save(OLD_OUTPUT_XLSX)
message(glue("Saved (backward compat): {OLD_OUTPUT_XLSX}"))
```

**Section 8 (Console Summary):**
- Line 431: "Table 1 (Sub-Category Encounter Detail):" -> "Table 1 (Enc: Sub-Category Detail):"
- Line 437: "Table 2 (Encounter Treatment Code Detail):" -> "Table 2 (Enc: Treatment Detail):"
- Lines 443-448: Update the "Verify drug_grouping_tables.xlsx was NOT modified" block — keep the check but update the variable reference. Change `old_file <- file.path(CONFIG$output_dir, "drug_grouping_tables.xlsx")` to use the literal string (not OUTPUT_XLSX), and update messages to mention both old and new filenames exist.
- Add at end before "Done.":
```r
message(glue("  Output files:"))
message(glue("    {NEW_OUTPUT_XLSX} (primary)"))
message(glue("    {OLD_OUTPUT_XLSX} (backward compatibility)"))
```

**All remaining references to bare OUTPUT_XLSX:** Replace with NEW_OUTPUT_XLSX or OLD_OUTPUT_XLSX as appropriate.

---

**B) Modify R/58_code_reference_tables.R:**

R/58 reads from R/56's output file using the old filename and old sheet name. Since R/56 still produces `drug_grouping_tables.xlsx` (backward compat), R/58 could keep using the old filename. However, the sheet names WITHIN that file have changed. Update:

**Line 40:** Change the DRUG_XLSX path to use the new primary filename:
```r
DRUG_XLSX <- file.path(CONFIG$output_dir, "episode_level_drug_grouping_tables.xlsx")
```

**Line 13 (header comment):** Update input reference:
```r
#   - output/episode_level_drug_grouping_tables.xlsx ("Ep: Sub-Category Summary" sheet)
```

**Line 358:** Update sheet name reference:
```r
drug_raw <- wb_to_df(drug_wb, sheet = "Ep: Sub-Category Summary")
```

---

**C) Modify R/88_smoke_test_comprehensive.R:**

Update smoke test checks to verify the NEW filenames and sheet names exist in R/56 and R/57 source code, while ALSO verifying the old filenames still appear (backward compat).

**Lines 1005-1010 (R/56 checks):**
Replace with:
```r
  check("R/56 outputs episode_level_drug_grouping_tables.xlsx (new) and drug_grouping_tables.xlsx (compat)",
        any(grepl("episode_level_drug_grouping_tables\\.xlsx", r56_lines)) &&
        any(grepl("drug_grouping_tables\\.xlsx", r56_lines)))

  check("R/56 has grain-prefixed sheets (Ep: Sub-Category Summary + Ep: Encounter Treatment)",
        any(grepl("Ep: Sub-Category Summary", r56_lines)) &&
        any(grepl("Ep: Encounter Treatment", r56_lines)))
```

**Lines 1437-1442 (R/57 checks):**
Replace with:
```r
  check("R/57 outputs encounter_level_drug_grouping_instances.xlsx (new) and drug_grouping_instances.xlsx (compat)",
        any(grepl("encounter_level_drug_grouping_instances\\.xlsx", r57_lines)) &&
        any(grepl("drug_grouping_instances\\.xlsx", r57_lines)))

  check("R/57 has grain-prefixed sheets (Enc: Sub-Category Detail + Enc: Treatment Detail)",
        any(grepl("Enc: Sub-Category Detail", r57_lines)) &&
        any(grepl("Enc: Treatment Detail", r57_lines)))
```

**Line 1533 (Phase 88 summary message):**
Update to reflect Phase 89 changes:
```r
message("  * P88-D07/D08: New encounter_level_drug_grouping_instances.xlsx with 2 sheets, per-encounter grain")
```
  </action>
  <verify>
    <automated>powershell -Command "
      $r57 = Get-Content 'R/57_drug_grouping_instances.R' -Raw;
      $r58 = Get-Content 'R/58_code_reference_tables.R' -Raw;
      $r88 = Get-Content 'R/88_smoke_test_comprehensive.R' -Raw;
      $checks = @(
        ($r57 -match 'encounter_level_drug_grouping_instances\.xlsx'),
        ($r57 -match 'Enc: Sub-Category Detail'),
        ($r57 -match 'Enc: Treatment Detail'),
        ($r57 -match 'NEW_OUTPUT_XLSX'),
        ($r57 -match 'OLD_OUTPUT_XLSX'),
        ($r58 -match 'episode_level_drug_grouping_tables\.xlsx'),
        ($r58 -match 'Ep: Sub-Category Summary'),
        ($r88 -match 'episode_level_drug_grouping_tables'),
        ($r88 -match 'encounter_level_drug_grouping_instances'),
        ($r88 -match 'Ep: Sub-Category Summary'),
        ($r88 -match 'Enc: Sub-Category Detail')
      );
      $passed = ($checks | Where-Object {$_}).Count;
      Write-Output \"PASS: $passed/11 checks\";
      if ($passed -lt 11) { exit 1 }
    "
    </automated>
  </verify>
  <acceptance_criteria>
    - R/57 contains `NEW_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "encounter_level_drug_grouping_instances.xlsx")`
    - R/57 contains `OLD_OUTPUT_XLSX <- file.path(CONFIG$output_dir, "drug_grouping_instances.xlsx")`
    - R/57 contains `wb$add_worksheet("Enc: Sub-Category Detail")`
    - R/57 contains `wb$add_worksheet("Enc: Treatment Detail")`
    - R/57 contains two `wb$save()` calls: one for NEW_OUTPUT_XLSX, one for OLD_OUTPUT_XLSX
    - R/57 does NOT contain bare `OUTPUT_XLSX` (only NEW_OUTPUT_XLSX, OLD_OUTPUT_XLSX, REFERENCE_XLSX)
    - R/58 line 40 references `episode_level_drug_grouping_tables.xlsx` (not old filename)
    - R/58 line 358 references sheet `"Ep: Sub-Category Summary"` (not old sheet name)
    - R/88 checks for `episode_level_drug_grouping_tables.xlsx` in R/56 source
    - R/88 checks for `encounter_level_drug_grouping_instances.xlsx` in R/57 source
    - R/88 checks for `Ep: Sub-Category Summary` and `Ep: Encounter Treatment` in R/56 source
    - R/88 checks for `Enc: Sub-Category Detail` and `Enc: Treatment Detail` in R/57 source
  </acceptance_criteria>
  <done>R/57 saves to both encounter_level_drug_grouping_instances.xlsx (primary) and drug_grouping_instances.xlsx (backward compat) with "Enc:" prefixed sheet names; R/58 reads from new R/56 filename and sheet name; R/88 smoke test validates new filenames and sheet names for both scripts</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Grep R/56 for "Ep: Sub-Category Summary" and "Ep: Encounter Treatment" — both present
2. Grep R/56 for dual save pattern: two wb$save() calls with NEW_OUTPUT_XLSX and OLD_OUTPUT_XLSX
3. Grep R/57 for "Enc: Sub-Category Detail" and "Enc: Treatment Detail" — both present
4. Grep R/57 for dual save pattern: two wb$save() calls with NEW_OUTPUT_XLSX and OLD_OUTPUT_XLSX
5. Grep R/58 for "episode_level_drug_grouping_tables.xlsx" and "Ep: Sub-Category Summary" — both present
6. Grep R/88 for all 4 new sheet names and both new filenames — all present
7. No bare `OUTPUT_XLSX` variable in R/56 or R/57 (only NEW_OUTPUT_XLSX, OLD_OUTPUT_XLSX, REFERENCE_XLSX)
8. All sheet names are <= 31 characters (Excel limit)
</verification>

<success_criteria>
- R/56 produces two files: episode_level_drug_grouping_tables.xlsx (primary) and drug_grouping_tables.xlsx (backward compat) with identical content
- R/57 produces two files: encounter_level_drug_grouping_instances.xlsx (primary) and drug_grouping_instances.xlsx (backward compat) with identical content
- R/56 sheets use "Ep:" prefix: "Ep: Sub-Category Summary" and "Ep: Encounter Treatment"
- R/57 sheets use "Enc:" prefix: "Enc: Sub-Category Detail" and "Enc: Treatment Detail"
- R/58 reads from the new R/56 filename and new sheet name without breaking
- R/88 smoke test validates new filenames and grain-prefixed sheet names
- All sheet names are within Excel's 31-character limit
- No workbook modification occurs between the two wb$save() calls (prevents file divergence)
</success_criteria>

<output>
After completion, create `.planning/phases/89-clear-up-episode-vs-encounter-distinction-for-r-56-export-both-versions-as-labeled-excel-files/89-01-SUMMARY.md`
</output>
