---
phase: 67-cancer-payer-qa-reorganization
plan: 02
type: execute
wave: 1
depends_on: ["67-01"]
files_modified:
  - R/SCRIPT_INDEX.md
  - R/87_smoke_test_full_pipeline.R
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REORG-01
  - REORG-02
gap_closure: true

must_haves:
  truths:
    - "SCRIPT_INDEX.md payer decade section lists all 10 scripts at positions 60-69 with correct filenames"
    - "R/87_smoke_test_full_pipeline.R payer_expected array contains all 10 correct script names (60-69)"
    - "R/87_smoke_test_full_pipeline.R payer count check expects 10 scripts, not 9"
    - "ROADMAP.md Phase 67 success criteria #2 says 10 scripts (60-69)"
  artifacts:
    - path: "R/SCRIPT_INDEX.md"
      provides: "Accurate payer decade listing"
      contains: "66_all_site_duplicate_dates.R"
    - path: "R/87_smoke_test_full_pipeline.R"
      provides: "Correct payer test array"
      contains: "66_all_site_duplicate_dates.R"
    - path: ".planning/ROADMAP.md"
      provides: "Correct success criteria count"
      contains: "10 scripts (60-69)"
  key_links:
    - from: "R/87_smoke_test_full_pipeline.R"
      to: "R/ filesystem"
      via: "payer_expected array matches all 10 payer scripts on disk"
      pattern: "66_all_site_duplicate_dates.*67_multi_source.*68_overlap_classification.*69_per_patient"
    - from: "R/SCRIPT_INDEX.md"
      to: "R/ filesystem"
      via: "Payer & QA section lists scripts 60-69 matching actual files"
      pattern: "66_all_site_duplicate_dates"
---

<objective>
Fix 3 documentation/reference gaps identified by 67-01-VERIFICATION.md: (1) SCRIPT_INDEX.md has wrong payer script numbers and is missing 68_overlap_classification.R, (2) smoke test payer_expected array lists 9 scripts with wrong names instead of 10 correct ones, (3) ROADMAP.md says 9 payer scripts but 10 exist.

Purpose: Verification found that plan 67-01 incorrectly assumed the payer decade had 9 scripts (60-65, 67-69) after moving the smoke test. In reality, position 66 was immediately filled by 66_all_site_duplicate_dates.R during Phase 66 renumbering, so the decade has always had 10 scripts (60-69). All three gaps are mechanical text fixes aligning documentation and test arrays to filesystem truth.

Output: Corrected R/SCRIPT_INDEX.md, corrected R/87_smoke_test_full_pipeline.R payer_expected array, corrected .planning/ROADMAP.md success criteria.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-cancer-payer-qa-reorganization/67-CONTEXT.md
@.planning/phases/67-cancer-payer-qa-reorganization/67-01-VERIFICATION.md
@.planning/phases/67-cancer-payer-qa-reorganization/67-01-SUMMARY.md

<interfaces>
<!-- Authoritative filesystem state for payer decade (60-69): 10 scripts -->
<!-- These are the CORRECT filenames that all references must match -->

Payer/QA decade (60-69) -- 10 scripts on filesystem:
```
60_tiered_same_day_payer.R
61_tiered_encounter_level.R
62_tiered_date_level.R
63_value_audit.R
64_all_source_missingness.R
65_uf_insurance_missingness.R
66_all_site_duplicate_dates.R
67_multi_source_overlap_detection.R
68_overlap_classification.R
69_per_patient_source_detection.R
```

From R/SCRIPT_INDEX.md -- current WRONG payer entries (lines 81-83):
```markdown
| 67_all_site_duplicate_dates.R | All-site duplicate date investigation... | 00_config |
| 68_multi_source_overlap_detection.R | Detect same-date and same-week... | 00_config |
| 69_per_patient_source_detection.R | Per-patient source detection... | 00_config |
```
Missing: 66_all_site_duplicate_dates.R and 68_overlap_classification.R entries.

From R/87_smoke_test_full_pipeline.R -- current WRONG payer_expected (lines 108-112):
```r
payer_expected <- c("60_tiered_same_day_payer.R", "61_tiered_encounter_level.R",
                    "62_tiered_date_level.R", "63_value_audit.R",
                    "64_all_source_missingness.R", "65_uf_insurance_missingness.R",
                    "67_all_site_duplicate_dates.R",
                    "68_multi_source_overlap_detection.R", "69_per_patient_source_detection.R")
```
Wrong: Lists 9 scripts, skipping position 66 and missing 68_overlap_classification.R.

From R/87_smoke_test_full_pipeline.R -- current WRONG count check (lines 117-118):
```r
check(glue("Payer/QA decade scripts: {payer_found}/9 present"),
      payer_found >= 7)
```
Wrong: Says /9, should say /10; threshold >= 7 should be >= 8.

From .planning/ROADMAP.md -- current WRONG success criteria (line 112):
```
2. Payer/QA decade has 9 scripts (60-65, 67-69) with no collision
```
Wrong: Should say "10 scripts (60-69)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SCRIPT_INDEX.md payer decade and smoke test payer_expected array</name>
  <files>R/SCRIPT_INDEX.md, R/87_smoke_test_full_pipeline.R</files>
  <read_first>
    - R/SCRIPT_INDEX.md (full file -- find exact lines for payer section and script count section)
    - R/87_smoke_test_full_pipeline.R (full file -- find payer_expected array and count check)
    - R/66_all_site_duplicate_dates.R (first 20 lines -- extract header for SCRIPT_INDEX description)
    - R/68_overlap_classification.R (first 20 lines -- extract header for SCRIPT_INDEX description)
  </read_first>
  <action>
Per D-07 and D-08: Fix the payer decade entries in SCRIPT_INDEX.md and the payer_expected array in the smoke test.

**Part A -- Fix R/SCRIPT_INDEX.md payer decade section:**

The current Payer and QA (60-69) table (around lines 73-83) lists only 9 entries with wrong numbers for the last 3. Replace lines 81-83 (the entries for scripts 67, 68, 69) with 4 correct entries for scripts 66-69:

Replace these 3 WRONG lines:
```
| 67_all_site_duplicate_dates.R | All-site duplicate date investigation: extends Phase 20 FLM analysis to all sites | 00_config |
| 68_multi_source_overlap_detection.R | Detect same-date and same-week encounter pairs from different ENCOUNTER.SOURCE values (all encounter types) | 00_config |
| 69_per_patient_source_detection.R | Per-patient source detection by date: which SOURCE values present on each patient-date | 00_config |
```

With these 4 CORRECT lines:
```
| 66_all_site_duplicate_dates.R | All-site duplicate date investigation: extends Phase 20 FLM analysis to all sites | 00_config |
| 67_multi_source_overlap_detection.R | Detect same-date and same-week encounter pairs from different ENCOUNTER.SOURCE values (all encounter types) | 00_config |
| 68_overlap_classification.R | Classify multi-source encounter groups (same-date/same-week) as Identical, Partial, or Distinct with per-site recommendations | 00_config |
| 69_per_patient_source_detection.R | Per-patient source detection by date: which SOURCE values present on each patient-date | 00_config |
```

Also update the Script Count section -- change the Payer/QA line from:
```
  - Payer/QA (60-69): 9
```
to:
```
  - Payer/QA (60-69): 10
```

And update the total numbered count from 66 to 67, and the total from 82 to 83:
```
  - **Total numbered:** 67
```
and:
```
- **Total:** 83
```

Also update the 87_smoke_test_full_pipeline.R description in the Testing section to reflect the corrected payer script count. Change:
```
| 87_smoke_test_full_pipeline.R | Validates Phase 66 complete reorganization (outputs, tests, ad-hoc decades) and Phase 67 cleanup (87 in test decade, 9 payer scripts, archive created) | (standalone) |
```
to:
```
| 87_smoke_test_full_pipeline.R | Validates Phase 66 complete reorganization (outputs, tests, ad-hoc decades) and Phase 67 cleanup (87 in test decade, 10 payer scripts, archive created) | (standalone) |
```

**Part B -- Fix R/87_smoke_test_full_pipeline.R payer_expected array:**

Replace the payer_expected vector (lines 108-112) with all 10 correct script names:
```r
payer_expected <- c("60_tiered_same_day_payer.R", "61_tiered_encounter_level.R",
                    "62_tiered_date_level.R", "63_value_audit.R",
                    "64_all_source_missingness.R", "65_uf_insurance_missingness.R",
                    "66_all_site_duplicate_dates.R",
                    "67_multi_source_overlap_detection.R",
                    "68_overlap_classification.R", "69_per_patient_source_detection.R")
```

Replace the count check (lines 117-118) from 9 to 10, threshold from 7 to 8:
```r
check(glue("Payer/QA decade scripts: {payer_found}/10 present"),
      payer_found >= 8)
```

Also update the test section header comment (line 104) from:
```
# Test 5: Payer/QA decade (60-69) -- 10 scripts
```
Confirm it already says 10 scripts. If it says 9 or another number, fix to 10.
  </action>
  <verify>
    <automated>grep "66_all_site_duplicate_dates" R/SCRIPT_INDEX.md && grep "68_overlap_classification" R/SCRIPT_INDEX.md && grep "66_all_site_duplicate_dates" R/87_smoke_test_full_pipeline.R && grep "68_overlap_classification" R/87_smoke_test_full_pipeline.R && grep "payer_found}/10" R/87_smoke_test_full_pipeline.R</automated>
  </verify>
  <acceptance_criteria>
    - `grep "66_all_site_duplicate_dates" R/SCRIPT_INDEX.md` returns a match (was missing before)
    - `grep "68_overlap_classification" R/SCRIPT_INDEX.md` returns a match (was missing before)
    - `grep "67_all_site_duplicate_dates" R/SCRIPT_INDEX.md` returns zero matches (wrong number removed)
    - `grep "68_multi_source_overlap" R/SCRIPT_INDEX.md` returns zero matches (wrong number removed)
    - `grep -c "^|" R/SCRIPT_INDEX.md` in the Payer section shows 10 data rows (plus header and separator)
    - `grep "Payer/QA (60-69): 10" R/SCRIPT_INDEX.md` returns a match (count updated from 9)
    - `grep "Total numbered.*67" R/SCRIPT_INDEX.md` returns a match (updated from 66)
    - `grep "66_all_site_duplicate_dates" R/87_smoke_test_full_pipeline.R` returns a match (was missing)
    - `grep "68_overlap_classification" R/87_smoke_test_full_pipeline.R` returns a match (was missing)
    - `grep "payer_found}/10" R/87_smoke_test_full_pipeline.R` returns a match (count updated from 9)
    - `grep "payer_found >= 8" R/87_smoke_test_full_pipeline.R` returns a match (threshold updated from 7)
  </acceptance_criteria>
  <done>SCRIPT_INDEX.md payer section lists all 10 scripts (60-69) with correct filenames and descriptions. Script count updated (Payer/QA=10, Total numbered=67, Total=83). Smoke test payer_expected array lists all 10 correct script names. Count check expects 10 with threshold 8. Gap 1 (SCRIPT_INDEX wrong numbers) and Gap 2 (smoke test stale array) both closed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ROADMAP.md success criteria count</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (find Phase 67 success criteria section, specifically criterion #2)
  </read_first>
  <action>
Per D-09: Update ROADMAP.md Phase 67 success criteria #2 to reflect the correct payer decade count.

Find the Phase 67 success criteria section (around line 112). Change criterion #2 from:
```
  2. Payer/QA decade has 9 scripts (60-65, 67-69) with no collision
```
to:
```
  2. Payer/QA decade has 10 scripts (60-69) with no collision
```

Also find the progress table entry for Phase 67 (around line 210) and update the plan count from "1/1" to "2/2" since this gap-closure plan is being added.

Also update the Plans section under Phase 67 details (around lines 116-118):
Change:
```
**Plans:** 1/1 plans complete
Plans:
- [x] 67-01-PLAN.md -- Move smoke test to 87, archive unnumbered scripts, regenerate SCRIPT_INDEX
```
to:
```
**Plans:** 1/2 plans complete
Plans:
- [x] 67-01-PLAN.md -- Move smoke test to 87, archive unnumbered scripts, regenerate SCRIPT_INDEX
- [ ] 67-02-PLAN.md -- Fix payer decade counts in SCRIPT_INDEX, smoke test, and ROADMAP (gap closure)
```

Also update the "Last updated" footer at the bottom of the file to reflect the new plan.
  </action>
  <verify>
    <automated>grep "10 scripts (60-69)" .planning/ROADMAP.md && grep "67-02-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "9 scripts (60-65, 67-69)" .planning/ROADMAP.md` returns zero matches (old text removed)
    - `grep "10 scripts (60-69)" .planning/ROADMAP.md` returns a match (corrected count)
    - `grep "67-02-PLAN" .planning/ROADMAP.md` returns a match (new plan listed)
    - `grep "Plans.*1/2\|Plans.*2/2" .planning/ROADMAP.md` returns a match in Phase 67 section (plan count updated)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 67 success criteria #2 corrected from "9 scripts (60-65, 67-69)" to "10 scripts (60-69)". Plan list updated to include 67-02 gap closure plan. Gap 3 (ROADMAP wrong count) closed.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **SCRIPT_INDEX accuracy:** `grep "66_all_site_duplicate_dates\|67_multi_source\|68_overlap_classification\|69_per_patient" R/SCRIPT_INDEX.md` returns 4 matches with correct script numbers
2. **Smoke test accuracy:** `grep "66_all_site_duplicate_dates\|68_overlap_classification" R/87_smoke_test_full_pipeline.R` returns 2 matches (both previously missing)
3. **Smoke test count:** `grep "payer_found}/10" R/87_smoke_test_full_pipeline.R` returns a match
4. **ROADMAP accuracy:** `grep "10 scripts (60-69)" .planning/ROADMAP.md` returns a match
5. **No stale references:** `grep "67_all_site_duplicate_dates\|68_multi_source_overlap_detection" R/SCRIPT_INDEX.md` returns zero matches (wrong numbers gone)
6. **Smoke test runnable:** `Rscript R/87_smoke_test_full_pipeline.R` completes without payer decade failures (optional -- requires R environment)
</verification>

<success_criteria>
- All 3 verification gaps from 67-01-VERIFICATION.md resolved
- SCRIPT_INDEX.md payer section matches filesystem exactly: 10 scripts at positions 60-69
- Smoke test payer_expected array matches filesystem exactly: 10 scripts with correct names
- ROADMAP.md success criteria reflects actual count: 10 scripts (60-69)
- Zero inconsistencies between filesystem, SCRIPT_INDEX, smoke test, and ROADMAP for payer decade
</success_criteria>

<output>
After completion, create `.planning/phases/67-cancer-payer-qa-reorganization/67-02-SUMMARY.md`
</output>
