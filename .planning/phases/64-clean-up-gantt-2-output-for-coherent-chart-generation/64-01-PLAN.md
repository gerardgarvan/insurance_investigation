---
phase: 64-clean-up-gantt-2-output-for-coherent-chart-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/63_gantt_v2_export.R
  - output/gantt_episodes_v2.csv
  - output/gantt_detail_v2.csv
autonomous: true
requirements:
  - GANTT-CLEAN-01
  - GANTT-CLEAN-02
  - GANTT-CLEAN-03
  - GANTT-CLEAN-04
  - GANTT-CLEAN-05
  - GANTT-CLEAN-06
  - GANTT-CLEAN-07

must_haves:
  truths:
    - "Gantt v2 CSV files import directly into Tableau without manual preprocessing"
    - "Multi-value fields use semicolons as separators, not commas"
    - "Drug names show only generic names (doxorubicin, not '25 ML doxorubicin hydrochloride 2 MG/ML Injection')"
    - "No literal 'NA' text appears in any cell — empty cells are truly empty"
    - "Death and HL Diagnosis rows have meaningful triggering_code_descriptions"
    - "Episodes CSV has 14 columns, detail CSV has 13 columns (trimmed from 17/16)"
    - "Empty cancer_category values show 'Unlinked' instead of blank"
  artifacts:
    - path: "R/63_gantt_v2_export.R"
      provides: "Cleanup logic in Section 4D between pseudo-treatment rows and CSV write"
      contains: "Section 4D"
    - path: "output/gantt_episodes_v2.csv"
      provides: "Cleaned episodes CSV for Tableau"
    - path: "output/gantt_detail_v2.csv"
      provides: "Cleaned detail CSV for Tableau"
  key_links:
    - from: "R/63_gantt_v2_export.R Section 4D"
      to: "output/gantt_episodes_v2.csv"
      via: "write.csv in Section 5"
      pattern: "clean_multi_value|simplify_drug_name"
    - from: "R/63_gantt_v2_export.R Section 4D"
      to: "output/gantt_detail_v2.csv"
      via: "write.csv in Section 5"
      pattern: "clean_multi_value|simplify_drug_name"
---

<objective>
Add data quality cleanup logic to R/63_gantt_v2_export.R so the Gantt v2 CSV outputs (gantt_episodes_v2.csv, gantt_detail_v2.csv) are directly importable into Tableau without manual preprocessing.

Purpose: Current v2 CSVs have comma-delimited multi-value fields that cause CSV parsing conflicts, verbose RxNorm drug names, literal "NA" strings, blank descriptions on pseudo-treatment rows, and empty cancer_category values. These issues force manual data cleaning before Tableau import. This plan adds a Section 4D cleanup step that fixes all issues in-place.

Output: Modified R/63_gantt_v2_export.R with Section 4D cleanup logic; overwritten gantt_episodes_v2.csv (14 cols) and gantt_detail_v2.csv (13 cols).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-clean-up-gantt-2-output-for-coherent-chart-generation/64-CONTEXT.md
@.planning/phases/64-clean-up-gantt-2-output-for-coherent-chart-generation/64-RESEARCH.md
@.planning/milestones/v1.8-phases/63-enhanced-gantt-export/63-01-SUMMARY.md

<interfaces>
<!-- Key structure of R/63_gantt_v2_export.R that executor needs to understand -->

R/63_gantt_v2_export.R structure (525 lines):
  Section 1 (lines 76-98): Setup, library loads, input/output path constants
  Section 2 (lines 101-141): Load episodes and detail RDS, guard clauses for missing columns
  Section 3 (lines 144-169): Code description lookup helpers (lookup_description, map_codes_to_descriptions)
  Section 4 (lines 172-239): Select and order columns — builds episodes_export (17 cols) and detail_export (16 cols)
  Section 4B (lines 242-351): Death pseudo-treatment rows — bind_rows to episodes_export and detail_export
  Section 4C (lines 354-464): HL Diagnosis pseudo-treatment rows — bind_rows to episodes_export and detail_export
  <<<< INSERT SECTION 4D HERE (after line 464, before Section 5) >>>>
  Section 5 (lines 467-477): write.csv for both outputs
  Section 6 (lines 480-524): Final summary messages

Current episodes_export columns (17): patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, encounter_ids, drug_names, triggering_code_descriptions, cancer_category, is_hodgkin, cancer_link_method, regimen_label, is_first_line

Current detail_export columns (16): patient_id, treatment_type, treatment_date, triggering_code, ENCOUNTERID, drug_name, episode_number, episode_start, episode_stop, historical_flag, triggering_code_description, cancer_category, is_hodgkin, cancer_link_method, regimen_label, is_first_line

After cleanup, episodes_export columns (14): patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, drug_names, triggering_code_descriptions, cancer_category, regimen_label, is_first_line

After cleanup, detail_export columns (13): patient_id, treatment_type, treatment_date, triggering_code, drug_name, episode_number, episode_start, episode_stop, historical_flag, triggering_code_description, cancer_category, regimen_label, is_first_line

Columns DROPPED from episodes: encounter_ids, is_hodgkin, cancer_link_method
Columns DROPPED from detail: ENCOUNTERID, is_hodgkin, cancer_link_method
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 4D cleanup logic and column trimming to R/63_gantt_v2_export.R</name>
  <files>R/63_gantt_v2_export.R</files>
  <read_first>
    - R/63_gantt_v2_export.R (current script — understand Section 4/4B/4C/5 structure and variable names)
    - .planning/phases/64-clean-up-gantt-2-output-for-coherent-chart-generation/64-CONTEXT.md (locked decisions D-01 through D-13)
    - .planning/phases/64-clean-up-gantt-2-output-for-coherent-chart-generation/64-RESEARCH.md (implementation patterns and code examples)
  </read_first>
  <action>
Insert a new Section 4D between the end of Section 4C (after the HL Diagnosis pseudo-treatment rows, currently ending around line 464) and Section 5 (CSV write, currently starting around line 467). Section 4D performs all data quality cleanup. Also update Section 5 column count references and Section 6 summary stats to reflect the trimmed column counts.

**Step 1: Add two helper functions at the top of Section 4D.**

Helper 1 — `clean_multi_value(field_str, sep_in = ",", sep_out = ";")`:
```r
clean_multi_value <- function(field_str, sep_in = ",", sep_out = ";") {
  if (is.na(field_str) || field_str == "" || field_str == "NA") return("")
  values <- str_split(field_str, sep_in)[[1]]
  values <- str_trim(values)
  values <- values[values != "" & !is.na(values)]
  values <- unique(values)
  if (length(values) == 0) return("")
  paste(values, collapse = sep_out)
}
```

Helper 2 — `simplify_drug_name(drug_str)`:
```r
simplify_drug_name <- function(drug_str) {
  if (is.na(drug_str) || drug_str == "" || drug_str == "NA") return("")
  drugs <- str_split(drug_str, ";")[[1]]
  drugs <- str_trim(drugs)
  simplified <- sapply(drugs, function(d) {
    match <- str_extract(tolower(d), "[a-z]{2,}")
    if (is.na(match)) return(d)
    match
  }, USE.NAMES = FALSE)
  simplified <- unique(simplified)
  paste(simplified, collapse = ";")
}
```

**Step 2: Apply multi-value field cleanup (per D-06, D-07, D-08).**

On `episodes_export`, apply `clean_multi_value` via `sapply` to columns: `triggering_codes`, `drug_names`, `triggering_code_descriptions`.

On `detail_export`, apply `clean_multi_value` via `sapply` to column: `triggering_code_description`.

Also apply `clean_multi_value` to `detail_export$triggering_code` (single-value, but dedup+blank-drop still useful for consistency).

**Step 3: Simplify drug names (per D-09).**

On `episodes_export`, apply `simplify_drug_name` via `sapply` to column: `drug_names`.
On `detail_export`, apply `simplify_drug_name` via `sapply` to column: `drug_name`.

Note: `simplify_drug_name` runs AFTER `clean_multi_value` because it expects semicolon-separated input.

**Step 4: Fill pseudo-treatment descriptions (per D-11).**

On `episodes_export`:
```r
episodes_export <- episodes_export %>%
  mutate(
    triggering_code_descriptions = case_when(
      treatment_type %in% c("Death", "HL Diagnosis") &
        (triggering_code_descriptions == "" | is.na(triggering_code_descriptions)) ~ treatment_type,
      TRUE ~ triggering_code_descriptions
    )
  )
```

On `detail_export`:
```r
detail_export <- detail_export %>%
  mutate(
    triggering_code_description = case_when(
      treatment_type %in% c("Death", "HL Diagnosis") &
        (triggering_code_description == "" | is.na(triggering_code_description)) ~ treatment_type,
      TRUE ~ triggering_code_description
    )
  )
```

**Step 5: Convert NA to empty strings for all character columns (per D-10).**

```r
episodes_export <- episodes_export %>%
  mutate(across(where(is.character), ~ ifelse(is.na(.) | . == "NA", "", .)))

detail_export <- detail_export %>%
  mutate(across(where(is.character), ~ ifelse(is.na(.) | . == "NA", "", .)))
```

**Step 6: Fill blank cancer_category with "Unlinked" (per D-12).**

Must happen AFTER Step 5 (NA conversion) so that NA cancer_categories are already empty strings.

```r
episodes_export <- episodes_export %>%
  mutate(cancer_category = ifelse(cancer_category == "", "Unlinked", cancer_category))

detail_export <- detail_export %>%
  mutate(cancer_category = ifelse(cancer_category == "", "Unlinked", cancer_category))
```

**Step 7: Column trimming (per D-02, D-04).**

Trim `episodes_export` to 14 columns:
```r
episodes_export <- episodes_export %>%
  select(
    patient_id, treatment_type, episode_number,
    episode_start, episode_stop, episode_length_days,
    distinct_dates_in_episode, historical_flag,
    triggering_codes, drug_names, triggering_code_descriptions,
    cancer_category, regimen_label, is_first_line
  )
```

Trim `detail_export` to 13 columns:
```r
detail_export <- detail_export %>%
  select(
    patient_id, treatment_type, treatment_date,
    triggering_code, drug_name, episode_number,
    episode_start, episode_stop, historical_flag,
    triggering_code_description, cancer_category,
    regimen_label, is_first_line
  )
```

**Step 8: Add column count verification (after select, before write).**

```r
expected_ep_cols <- 14
expected_detail_cols <- 13

if (ncol(episodes_export) != expected_ep_cols) {
  stop(glue("ERROR: episodes_export has {ncol(episodes_export)} columns, expected {expected_ep_cols}"))
}
if (ncol(detail_export) != expected_detail_cols) {
  stop(glue("ERROR: detail_export has {ncol(detail_export)} columns, expected {expected_detail_cols}"))
}
```

Add a `message()` call after each step for console logging, following the project pattern (e.g., `message("  Multi-value fields cleaned (separator: semicolon, deduped, blanks dropped)")`).

**Step 9: Update Section 6 summary stats.**

Remove or update the lines in Section 6 that reference columns being dropped:
- Remove `episodes_with_cancer_link` stat (references `cancer_link_method` which is dropped)
- Update the v1 vs v2 column comparison message at the end to reflect the new trimmed column counts (14 episodes, 13 detail)
- Keep `episodes_with_regimen`, `episodes_first_line`, `death_rows`, `hl_dx_rows` stats (these still apply)

**Step 10: Update the v2 schema documentation in the script header comment (lines 12-48).**

Update the schema documentation at the top of the file to reflect the post-cleanup column set:
- Episodes: 14 columns (not 17) — list the 14 kept columns
- Detail: 13 columns (not 15/16) — list the 13 kept columns
- Add a note that columns encounter_ids, is_hodgkin, cancer_link_method, ENCOUNTERID are dropped in Section 4D cleanup
- Add PURPOSE line about Phase 64 cleanup additions

Also update the OUTPUTS comment to reflect 14 and 13 columns.

**Step 11: Pass `na = ""` to `write.csv()` calls in Section 5.**

Change both `write.csv()` calls to include `na = ""` so that R writes true empty cells for NA values rather than literal "NA":
```r
write.csv(episodes_export, OUTPUT_EPISODES_V2, row.names = FALSE, na = "")
write.csv(detail_export, OUTPUT_DETAIL_V2, row.names = FALSE, na = "")
```

This is the final safety net for D-10 — even if some non-character NA values survive Step 5, `write.csv(na = "")` catches them.
  </action>
  <verify>
    <automated>cd "C:/Users/Owner/Documents/insurance_investigation" && grep -c "Section 4D" R/63_gantt_v2_export.R && grep -c "clean_multi_value" R/63_gantt_v2_export.R && grep -c "simplify_drug_name" R/63_gantt_v2_export.R && grep -c "Unlinked" R/63_gantt_v2_export.R && grep "na = " R/63_gantt_v2_export.R</automated>
  </verify>
  <acceptance_criteria>
    - R/63_gantt_v2_export.R contains `--- Section 4D: Data Quality Cleanup ---`
    - R/63_gantt_v2_export.R contains function definition `clean_multi_value <- function(field_str`
    - R/63_gantt_v2_export.R contains function definition `simplify_drug_name <- function(drug_str`
    - R/63_gantt_v2_export.R contains `collapse = ";"` (semicolon separator per D-06)
    - R/63_gantt_v2_export.R contains `"Unlinked"` (cancer category fill per D-12)
    - R/63_gantt_v2_export.R contains `na = ""` in both write.csv() calls (per D-10)
    - R/63_gantt_v2_export.R contains `case_when(` with `treatment_type %in% c("Death", "HL Diagnosis")` for pseudo-treatment description fill (per D-11)
    - R/63_gantt_v2_export.R episodes_export select() lists exactly 14 column names: patient_id, treatment_type, episode_number, episode_start, episode_stop, episode_length_days, distinct_dates_in_episode, historical_flag, triggering_codes, drug_names, triggering_code_descriptions, cancer_category, regimen_label, is_first_line (per D-02)
    - R/63_gantt_v2_export.R detail_export select() lists exactly 13 column names: patient_id, treatment_type, treatment_date, triggering_code, drug_name, episode_number, episode_start, episode_stop, historical_flag, triggering_code_description, cancer_category, regimen_label, is_first_line (per D-04)
    - R/63_gantt_v2_export.R does NOT contain `encounter_ids` in the final Section 4D select() call (dropped per D-02)
    - R/63_gantt_v2_export.R does NOT contain `ENCOUNTERID` in the final Section 4D select() call (dropped per D-04)
    - R/63_gantt_v2_export.R does NOT contain `is_hodgkin` in the final Section 4D select() call (dropped per D-02, D-04)
    - R/63_gantt_v2_export.R does NOT contain `cancer_link_method` in the final Section 4D select() call (dropped per D-02, D-04)
    - R/63_gantt_v2_export.R contains column count verification: `expected_ep_cols <- 14` and `expected_detail_cols <- 13`
    - R/63_gantt_v2_export.R header comment updated to show 14 episode columns and 13 detail columns
    - R/63_gantt_v2_export.R contains `across(where(is.character)` for NA-to-empty conversion (per D-10)
    - Section 4D appears AFTER Section 4C (HL Diagnosis rows) and BEFORE Section 5 (CSV write)
  </acceptance_criteria>
  <done>R/63_gantt_v2_export.R contains a complete Section 4D with all 7 cleanup operations (multi-value dedup with semicolons, drug name simplification, pseudo-treatment description fill, NA-to-empty, cancer category "Unlinked" fill, column trimming with verification), and the script header and Section 6 summary are updated to match the new column counts.</done>
</task>

<task type="auto">
  <name>Task 2: Run cleanup script and verify CSV output quality</name>
  <files>output/gantt_episodes_v2.csv, output/gantt_detail_v2.csv</files>
  <read_first>
    - R/63_gantt_v2_export.R (the modified script from Task 1)
    - .planning/phases/64-clean-up-gantt-2-output-for-coherent-chart-generation/64-CONTEXT.md (decisions to verify against)
  </read_first>
  <action>
**Step 1: Run the modified export script.**

Execute `Rscript R/63_gantt_v2_export.R` from the project root. Capture and review console output. The script should complete without errors and print Section 4D cleanup messages.

If the script fails due to missing RDS artifacts (running on dev machine without HiPerGator data), skip execution and proceed with structural verification only (grep-based checks on the R file).

**Step 2: If script ran successfully, verify output CSV quality.**

Check gantt_episodes_v2.csv:
- Count columns: should be exactly 14 (use `head -1 output/gantt_episodes_v2.csv | tr ',' '\n' | wc -l`)
- Verify no literal "NA" text in any cell: `grep -c '"NA"' output/gantt_episodes_v2.csv` should return 0
- Verify semicolons are used as multi-value separators: `grep -c ';' output/gantt_episodes_v2.csv` should be > 0
- Verify "Unlinked" appears for blank cancer categories: `grep -c 'Unlinked' output/gantt_episodes_v2.csv` should be > 0
- Verify no `encounter_ids` column: first line should NOT contain "encounter_ids"
- Verify no `is_hodgkin` column: first line should NOT contain "is_hodgkin"
- Verify no `cancer_link_method` column: first line should NOT contain "cancer_link_method"
- Verify Death rows have descriptions: grep for Death rows and check triggering_code_descriptions is "Death" not empty
- Spot-check drug names: grep a known drug (doxorubicin) — should appear as plain "doxorubicin", not "25 ML doxorubicin hydrochloride 2 MG/ML Injection"

Check gantt_detail_v2.csv:
- Count columns: should be exactly 13
- Verify no `ENCOUNTERID` column: first line should NOT contain "ENCOUNTERID"
- Same NA, semicolon, Unlinked, and description checks as above

**Step 3: If script could not run (no data), verify structural correctness of R code only.**

Run these grep verifications:
- Confirm `clean_multi_value` function defined and applied to correct columns
- Confirm `simplify_drug_name` function defined and applied
- Confirm `na = ""` in write.csv calls
- Confirm Section 4D ordering (after 4C, before 5)
- Confirm no syntax issues (check for balanced parentheses in key mutate blocks)
  </action>
  <verify>
    <automated>cd "C:/Users/Owner/Documents/insurance_investigation" && grep -n "Section 4D" R/63_gantt_v2_export.R && grep -n "Section 5" R/63_gantt_v2_export.R && grep -c "clean_multi_value" R/63_gantt_v2_export.R && grep 'na = ""' R/63_gantt_v2_export.R</automated>
  </verify>
  <acceptance_criteria>
    - Script runs without R errors (if data available), OR structural verification passes (if no data)
    - If CSV exists: gantt_episodes_v2.csv header line contains exactly 14 comma-separated column names
    - If CSV exists: gantt_detail_v2.csv header line contains exactly 13 comma-separated column names
    - If CSV exists: `grep -c '"NA"' output/gantt_episodes_v2.csv` returns 0
    - If CSV exists: `grep -c '"NA"' output/gantt_detail_v2.csv` returns 0
    - If CSV exists: gantt_episodes_v2.csv header does NOT contain `encounter_ids`, `is_hodgkin`, or `cancer_link_method`
    - If CSV exists: gantt_detail_v2.csv header does NOT contain `ENCOUNTERID`, `is_hodgkin`, or `cancer_link_method`
    - Section 4D line number is greater than Section 4C line number and less than Section 5 line number in R/63_gantt_v2_export.R
  </acceptance_criteria>
  <done>Gantt v2 CSV files are regenerated (or structurally verified) with all 7 cleanup operations applied: semicolon separators, deduped descriptions, simplified drug names, no literal NA, filled pseudo-treatment descriptions, "Unlinked" cancer category, and trimmed columns (14 episodes, 13 detail).</done>
</task>

</tasks>

<verification>
1. R/63_gantt_v2_export.R contains Section 4D with all cleanup operations
2. gantt_episodes_v2.csv has 14 columns (not 17) with clean data
3. gantt_detail_v2.csv has 13 columns (not 15/16) with clean data
4. No literal "NA" text in output CSVs
5. Multi-value fields use semicolons, not commas
6. Drug names are simplified to generics
7. Death/HL Diagnosis rows have meaningful descriptions
8. Empty cancer_category values show "Unlinked"
</verification>

<success_criteria>
- R/63_gantt_v2_export.R has a Section 4D containing: clean_multi_value(), simplify_drug_name(), pseudo-treatment description fill, NA-to-empty conversion, cancer_category "Unlinked" fill, and column trimming to 14/13 columns
- Output CSVs (if regenerated) pass all quality checks: no literal NA, semicolon separators, simplified drugs, Unlinked fills, correct column counts
- Script header documentation updated to reflect post-cleanup schema
- write.csv() calls include na = ""
</success_criteria>

<output>
After completion, create `.planning/phases/64-clean-up-gantt-2-output-for-coherent-chart-generation/64-01-SUMMARY.md`
</output>
