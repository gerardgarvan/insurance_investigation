---
phase: 120-normalize-supportive-care-meaning
plan: 02
type: execute
wave: 2
depends_on: ["120-01"]
files_modified:
  - R/39_run_all_investigations.R
  - R/88_smoke_test_comprehensive.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements: [SMOKE-120-01]

must_haves:
  truths:
    - "R/105 runs as part of the R/39 investigation-scripts stage"
    - "R/88 has a Phase 120 Section 15r that structurally validates R/105 and passes locally"
    - "R/SCRIPT_INDEX.md documents R/105 with the correct counts (100+ count 5->6, Total 91->92)"
  artifacts:
    - path: "R/39_run_all_investigations.R"
      provides: "R/105 registered in investigation_scripts vector"
      contains: "R/105_normalize_supportive_care_meaning.R"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Section 15r Phase 120 structural checks + SMOKE-120-01 summary line"
      contains: "SECTION 15r"
    - path: "R/SCRIPT_INDEX.md"
      provides: "R/105 row in Post-Renumber Investigations (100+) table + bumped counts"
      contains: "R/105_normalize_supportive_care_meaning.R"
  key_links:
    - from: "R/39_run_all_investigations.R"
      to: "R/105_normalize_supportive_care_meaning.R"
      via: "investigation_scripts vector entry"
      pattern: "R/105_normalize_supportive_care_meaning\\.R"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/105_normalize_supportive_care_meaning.R"
      via: "Section 15r structural check() calls"
      pattern: "SECTION 15r|105"
---

<objective>
Register the new `R/105_normalize_supportive_care_meaning.R` script (created in Plan 01)
into the pipeline's three registration surfaces: the R/39 runner, the R/88 smoke test
(new Section 15r), and R/SCRIPT_INDEX.md. This mirrors the exact pattern every 100+ script
followed (R/100-R/104).

Purpose: keep the script discoverable/runnable via the standard pipeline runner and
structurally validated by the smoke test (SMOKE-120-01).

Output: three modified registration files; R/88 gains a Phase 120 section that passes locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-in-all-codes-resolved-next-tables-v2-1-tab-supportive-care-normalize-meaning-into-a-new-column-to-disambiguate-dosage-spelling-variants-and-generic-names/120-RESEARCH.md

<interfaces>
<!-- Exact insertion points, verified against current repo state. -->

R/39_run_all_investigations.R (L176-195) — investigation_scripts vector. The LAST entry
is comma-less; R/104 is currently the final comma-less line:
```r
  "R/102_death_cause_nhl_flag.R",             # ...Phase 118; fixed Phase 119...
  "R/104_gantt_entire_history.R"              # ...quick-260710-i1e...
)
```
To add R/105 as the new final entry: add a trailing comma to the R/104 line and append
`"R/105_normalize_supportive_care_meaning.R"` (comma-less) as the new last line, so the
vector still parses.

R/88_smoke_test_comprehensive.R — Section 15q (R/104) is the current LAST 15-series section;
it ENDS at line ~2409 with:
```r
} else {
  for (i in 2:14) check(paste0("R/104 dependent check #", i, " -- SKIPPED (script missing)"), FALSE)
}
```
immediately BEFORE `# SECTION 15g: PROTON THERAPY...` at line ~2411. Insert the NEW
Section 15r block between those two (after the R/104 else-block, before Section 15g).

R/88 summary block (L3975-3983) — the SMOKE summary messages. The last one is:
```r
message("  * SMOKE-i1e-01: R/88 validates R/104 gantt entire-history structural integrity (Section 15q, 14 checks)")
```
immediately before `if (failed > 0) { quit(status = 1) }`. Add the SMOKE-120-01 line after it.

R/SCRIPT_INDEX.md:
- Post-Renumber Investigations (100+) table ends at L150 (the R/104 row). Add an R/105 row after it.
- L203: `- **Post-renumber investigations (100+):** 5 (...)` — bump to 6 and add R/105 to the parenthetical.
- L206: `- **Total:** 91` — bump to 92.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register R/105 in R/39 and R/SCRIPT_INDEX.md</name>
  <read_first>
    - R/39_run_all_investigations.R L176-195 (investigation_scripts vector; note comma-less last entry)
    - R/SCRIPT_INDEX.md L140-151 (Post-Renumber Investigations 100+ table) and L191-206 (Script Count block)
    - R/105_normalize_supportive_care_meaning.R (confirm exact filename + one-line purpose for the index row)
  </read_first>
  <files>R/39_run_all_investigations.R, R/SCRIPT_INDEX.md</files>
  <action>
    (1) R/39: In the `investigation_scripts` vector, add a trailing comma to the current final
    comma-less entry (the `"R/104_gantt_entire_history.R"` line) and append a NEW final
    comma-less line:
      `  "R/105_normalize_supportive_care_meaning.R"  # Supportive Care Normalized Meaning column (Phase 120); mutates data/reference/all_codes_resolved_next_tables_v2.1.xlsx in place`
    Ensure the vector still parses (exactly one comma-less entry, which is now R/105).

    (2) R/SCRIPT_INDEX.md:
      - Add a row to the Post-Renumber Investigations (100+) table (after the R/104 row, L150):
        `| \`R/105_normalize_supportive_care_meaning.R\` | Resolves each Supportive Care RXNORM code to its RxNorm IN generic ingredient via RxNav (\`related.json?tty=IN\`, historystatus fallback for retired codes, rule-based canonicalize_drug_name fallback for misses), caches to \`data/reference/rxnorm_ingredient_cache.csv\`, and appends a \`Normalized Meaning\` column (col G) to the Supportive Care tab of \`all_codes_resolved_next_tables_v2.1.xlsx\` in place (combos kept as sorted "/"-joined labels). | 120 |`
      - L203: change `**Post-renumber investigations (100+):** 5 (...)` to `6` and append
        `, R/105 Supportive Care Normalized Meaning` inside the parenthetical.
      - L206: change `**Total:** 91` to `**Total:** 92`.
  </action>
  <verify>
    <automated>grep -q 'R/105_normalize_supportive_care_meaning.R' R/39_run_all_investigations.R && grep -q 'R/105_normalize_supportive_care_meaning.R' R/SCRIPT_INDEX.md && grep -q 'Total:\*\* 92' R/SCRIPT_INDEX.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'R/105_normalize_supportive_care_meaning.R' R/39_run_all_investigations.R` >= 1.
    - `grep -c 'R/104_gantt_entire_history.R"' R/39_run_all_investigations.R` still 1 AND it now ends with a comma (`grep -n 'R/104_gantt_entire_history.R",' R/39_run_all_investigations.R` returns a match).
    - `Rscript -e 'x <- parse("R/39_run_all_investigations.R"); cat("PARSE_OK")'` prints PARSE_OK (vector still parses — no syntax break).
    - `grep -c 'R/105_normalize_supportive_care_meaning.R' R/SCRIPT_INDEX.md` >= 1.
    - `grep -c 'Post-renumber investigations (100+):\*\* 6' R/SCRIPT_INDEX.md` == 1.
    - `grep -c 'Total:\*\* 92' R/SCRIPT_INDEX.md` == 1.
    - `grep -c 'Total:\*\* 91' R/SCRIPT_INDEX.md` == 0 (old total replaced).
  </acceptance_criteria>
  <done>R/105 appears in the R/39 investigation_scripts vector (still parses) and in the SCRIPT_INDEX 100+ table; 100+ count is 6 and Total is 92.</done>
</task>

<task type="auto">
  <name>Task 2: Add R/88 Section 15r (Phase 120 structural validation) + SMOKE-120-01 summary line</name>
  <read_first>
    - R/88_smoke_test_comprehensive.R L2326-2409 (Section 15q block — copy its shape: message header, script-exists check, readLines/paste, numbered check()s, else-branch SKIPPED loop)
    - R/88_smoke_test_comprehensive.R L2411 (Section 15g header — the boundary; insert 15r before it)
    - R/88_smoke_test_comprehensive.R L3975-3987 (summary message block + quit guard)
    - R/105_normalize_supportive_care_meaning.R (the script being validated — confirm the exact strings the checks grep for)
  </read_first>
  <files>R/88_smoke_test_comprehensive.R</files>
  <action>
    Insert a new `# SECTION 15r: SUPPORTIVE CARE NORMALIZED MEANING (Phase 120) ----` block
    between the end of the Section 15q R/104 else-branch (~L2409) and the
    `# SECTION 15g: PROTON THERAPY...` header (~L2411). Follow the Section 15q shape exactly
    (message header; Check 1 = script exists; if exists, readLines + paste to a text var, then
    numbered checks; else-branch registers the dependent checks FALSE with a SKIPPED message
    so the total stays honest). Use these 14 structural checks (all against the R/105 file text,
    plus one bundled-artifact check). These are STRUCTURAL greps — no HiPerGator data needed
    (nyquist_validation is OFF); they pass LOCALLY:

      1.  `file.exists("R/105_normalize_supportive_care_meaning.R")`
      2.  R/105 has 150+ lines (`length(r105_lines) >= 150`)
      3.  sources R/00_config.R (`grepl("source.*00_config", t)`)
      4.  reads the Supportive Care sheet (`grepl('Supportive Care', t)` AND `grepl('start_row = 2|start_row=2', t)`)
      5.  calls the RxNav IN endpoint (`grepl('related\\\\.json\\\\?tty=IN', t)`)
      6.  does NOT use properties.json (`!grepl('properties\\\\.json', t)`)  — negative check
      7.  has the historystatus retired-code fallback (`grepl('historystatus', t)`)
      8.  reuses the httr2 retry wrapper (`grepl('req_retry', t)`)
      9.  keeps combos as sorted "/"-joined label (`grepl('sort\\\\(unique', t)` AND `grepl('rxnav_IN_combo', t)`)
      10. reuses canonicalize_drug_name for the rule-based fallback (`grepl('canonicalize_drug_name', t)`)
      11. writes the cache CSV (`grepl('rxnorm_ingredient_cache\\\\.csv', t)`)
      12. appends the Normalized Meaning column at col G (`grepl('Normalized Meaning', t)` AND `grepl('G2', t)` AND `grepl('G3', t)`)
      13. round-trip verify present (`grepl('== 171', t)` AND `grepl('== 7', t)` AND second `wb_load` — e.g. `length(gregexpr("wb_load", t)[[1]]) >= 2`)
      14. no ggplot/ggsave (`!grepl('ggplot|ggsave|geom_', t)`)  — data-mutation only

    Then in the summary block (after the SMOKE-i1e-01 message at ~L3983, before the
    `if (failed > 0)` guard) add:
      `message("  * SMOKE-120-01: R/88 validates Phase 120 Supportive Care Normalized Meaning structural integrity (Section 15r, 14 checks)")`

    Match the existing check() helper signature `check(label, condition)` used throughout R/88.
  </action>
  <verify>
    <automated>grep -q 'SECTION 15r' R/88_smoke_test_comprehensive.R && grep -q 'SMOKE-120-01' R/88_smoke_test_comprehensive.R && Rscript -e 'parse("R/88_smoke_test_comprehensive.R"); cat("PARSE_OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SECTION 15r' R/88_smoke_test_comprehensive.R` == 1.
    - `grep -c 'SMOKE-120-01' R/88_smoke_test_comprehensive.R` >= 1 (summary line present).
    - `grep -c 'R/105_normalize_supportive_care_meaning.R' R/88_smoke_test_comprehensive.R` >= 1.
    - `grep -c 'related\\.json?tty=IN' R/88_smoke_test_comprehensive.R` >= 1 (Check 5 present).
    - `grep -c 'rxnav_IN_combo' R/88_smoke_test_comprehensive.R` >= 1 (Check 9 present).
    - The 15r block is positioned before Section 15g: `grep -n 'SECTION 15r' ...` line number < `grep -n 'SECTION 15g:' ...` line number.
    - `Rscript -e 'parse("R/88_smoke_test_comprehensive.R"); cat("PARSE_OK")'` prints PARSE_OK (no syntax break introduced).
    - RUNTIME (local, script from Plan 01 already run): `Rscript R/88_smoke_test_comprehensive.R` — the Section 15r checks report as PASS in the output (all 14 TRUE), given R/105 exists and matches. (If R/88's full run stops earlier on unrelated HiPerGator-only sections, isolate the 15r checks per the Phase 116 precedent and confirm all 14 pass in isolation — note in SUMMARY.)
  </acceptance_criteria>
  <done>R/88 has a Section 15r with 14 Phase 120 structural checks (positioned before Section 15g), the SMOKE-120-01 summary line is present, the file still parses, and the 15r checks pass locally.</done>
</task>

</tasks>

<verification>
- R/105 is registered in the R/39 investigation_scripts vector and the vector still parses.
- R/88 Section 15r (14 checks) validates R/105 structurally and passes locally; SMOKE-120-01 summary line added.
- R/SCRIPT_INDEX.md documents R/105; 100+ count 5->6, Total 91->92.
- Both R/39 and R/88 still parse (no syntax breaks).
</verification>

<success_criteria>
- Running R/39 includes R/105 in the investigation stage.
- Running R/88 shows the 15 Phase 120 checks passing (in isolation if needed).
- SCRIPT_INDEX counts reflect the new script.
</success_criteria>

<output>
After completion, create `.planning/phases/120-in-all-codes-resolved-next-tables-v2-1-tab-supportive-care-normalize-meaning-into-a-new-column-to-disambiguate-dosage-spelling-variants-and-generic-names/120-02-SUMMARY.md`
</output>
