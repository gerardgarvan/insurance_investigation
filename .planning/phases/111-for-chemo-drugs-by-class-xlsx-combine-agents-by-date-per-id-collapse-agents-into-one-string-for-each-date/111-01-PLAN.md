---
phase: 111-for-chemo-drugs-by-class-xlsx-combine-agents-by-date-per-id-collapse-agents-into-one-string-for-each-date
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/36_tableau_ready_tables.R
  - R/88_smoke_test_comprehensive.R
autonomous: true
requirements: [T2COLLAPSE-01, T2COLLAPSE-02]

must_haves:
  truths:
    - "TABLE-2 xlsx contains one row per unique patient+date combination (not per encounter+medication)"
    - "Each row's agents column contains alphabetically sorted, comma-separated, deduplicated medication names for that patient+date"
    - "cancer_codes and cancer_category_names are merged and deduplicated across all encounters sharing the same patient+date"
    - "TABLE-2 has exactly 5 columns: PATID, treatment_date, agents, cancer_codes, cancer_category_names"
    - "R/88 smoke test validates the new TABLE-2 column structure (agents column, no ENCOUNTERID/drug_class/treatment_type)"
  artifacts:
    - path: "R/36_tableau_ready_tables.R"
      provides: "Date-grain TABLE-2 build logic in Section 5"
      contains: "group_by.*treatment_date"
    - path: "R/88_smoke_test_comprehensive.R"
      provides: "Updated Phase 106 validation for new TABLE-2 column structure"
      contains: "agents"
  key_links:
    - from: "R/36_tableau_ready_tables.R"
      to: "output/tableau_table2_chemo_drugs_by_class.xlsx"
      via: "openxlsx2 wb_workbook write"
      pattern: "wb2.*add_data.*table2"
    - from: "R/88_smoke_test_comprehensive.R"
      to: "R/36_tableau_ready_tables.R"
      via: "readLines structural grep checks"
      pattern: "grepl.*agents"
---

<objective>
Collapse TABLE-2 output from per-encounter+medication grain to per-patient+date grain, combining all chemo agent names on each date into a single comma-separated string.

Purpose: Makes the Tableau-ready chemo drugs table more useful for Amy's date-level analysis -- dates are clinically meaningful, encounters are billing artifacts (Phase 109 precedent).
Output: Modified R/36 (Section 5 date-grain collapse, Section 7 updated sanity check) and updated R/88 smoke test.
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-for-chemo-drugs-by-class-xlsx-combine-agents-by-date-per-id-collapse-agents-into-one-string-for-each-date/111-CONTEXT.md
@.planning/phases/111-for-chemo-drugs-by-class-xlsx-combine-agents-by-date-per-id-collapse-agents-into-one-string-for-each-date/111-RESEARCH.md

<interfaces>
<!-- Key patterns from R/36 that executor needs to understand -->

From R/36_tableau_ready_tables.R Section 3 (lines 153-158) -- existing cancer code aggregation pattern:
```r
encounter_dx <- dx_cancer %>%
  group_by(ENCOUNTERID) %>%
  summarise(
    cancer_codes = paste(sort(unique(DX)), collapse = ","),
    .groups = "drop"
  )
```

From R/36_tableau_ready_tables.R Section 5 (lines 300-305) -- CURRENT TABLE-2 build to be replaced:
```r
table2 <- chemo_detail %>%
  select(patient_id, ENCOUNTERID, treatment_date, treatment_type,
         medication_name, drug_class, cancer_codes, cancer_category_names) %>%
  distinct() %>%
  rename(PATID = patient_id) %>%
  arrange(PATID, treatment_date, medication_name)
```

From R/36_tableau_ready_tables.R Section 7 (lines 361-372) -- CURRENT sanity check to be updated:
```r
t2_encounters <- unique(table2$ENCOUNTERID)
t1_encounters <- unique(table1$ENCOUNTERID)
t2_not_in_t1 <- setdiff(t2_encounters, t1_encounters)
if (length(t2_not_in_t1) > 0) {
  warning(glue("[R/36 WARNING] {length(t2_not_in_t1)} TABLE-2 encounters not found in TABLE-1"))
} else {
  message(glue("  Sanity check PASSED: all {length(t2_encounters)} TABLE-2 encounters found in TABLE-1"))
}
```

From R/88_smoke_test_comprehensive.R lines 2688-2699 -- TABLE-2 specific checks to be updated:
```r
  # TABLE-2 specific
  check("R/36 filters to Chemotherapy for TABLE-2",
        any(grepl('treatment_type.*==.*"Chemotherapy"', r36_text)))
  check("R/36 resolves medication_name",
        any(grepl("medication_name", r36_text)))
  check("R/36 loads reference xlsx for drug mappings",
        any(grepl("all_codes_resolved_next_tables", r36_text)))
  check("R/36 outputs tableau_table2_chemo_drugs_by_class.xlsx",
        any(grepl("tableau_table2_chemo_drugs_by_class\\.xlsx", r36_text)))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse R/36 TABLE-2 to per-patient+date grain</name>
  <files>R/36_tableau_ready_tables.R</files>

  <read_first>
    - R/36_tableau_ready_tables.R (full file -- understand current Section 5 build logic, Section 6 xlsx write, Section 7 sanity check)
    - .planning/phases/111-for-chemo-drugs-by-class-xlsx-combine-agents-by-date-per-id-collapse-agents-into-one-string-for-each-date/111-CONTEXT.md (locked decisions D-01 through D-10)
  </read_first>

  <action>
Modify R/36_tableau_ready_tables.R in three areas:

**A) Update file header comment (lines 6-10)** -- change TABLE-2 description:
- Old: "TABLE-2: Chemotherapy drugs by class/category with associated cancer codes per encounter, for Tableau drug classification analysis."
- New: "TABLE-2: Chemotherapy agents collapsed by date per patient, with merged cancer codes per patient-date, for Tableau date-level drug analysis."
- Update D-06 comment (line 34-35) from "TABLE-2 columns: PATID, ENCOUNTERID, treatment_date, treatment_type, medication_name, drug_class, cancer_codes, cancer_category_names" to "TABLE-2 columns: PATID, treatment_date, agents, cancer_codes, cancer_category_names (per Phase 111 D-05)"

**B) Replace Section 5 build logic (lines 259-319)** -- change from per-encounter+medication to per-patient+date grain:

Replace the Section 5 message header:
```r
message("--- Building TABLE-2: Chemo Agents by Date (Phase 111: D-01 through D-07) ---")
```

Keep lines 264-291 UNCHANGED (reference xlsx loading, chemo_detail filtering, medication_name resolution via 3-tier cascade). These stay because medication names still need resolving before collapse.

REMOVE lines 293-295 (the `drug_class = "Chemotherapy"` mutate) -- per D-02 this column is always "Chemotherapy" and carries no information.

Replace lines 297-305 (the TABLE-2 build block) with date-grain collapse:
```r
# Build TABLE-2: one row per patient+date, agents collapsed (Phase 111 D-01 through D-07)
# Drop ENCOUNTERID (D-01: meaningless at date grain), drug_class (D-02: always "Chemotherapy"),
# treatment_type (D-02: always "Chemotherapy")
# Collapse medication_name -> agents: alphabetically sorted, deduplicated, comma-separated (D-06, D-07)
# Merge cancer_codes across encounters sharing same patient+date: split, union, dedup (D-03)
# Merge cancer_category_names across encounters sharing same patient+date (D-04)
table2 <- chemo_detail %>%
  group_by(patient_id, treatment_date) %>%
  summarise(
    agents = paste(sort(unique(na.omit(medication_name))), collapse = ","),
    cancer_codes = {
      all_codes <- unique(na.omit(unlist(strsplit(cancer_codes, ","))))
      if (length(all_codes) == 0) NA_character_ else paste(sort(all_codes), collapse = ",")
    },
    cancer_category_names = {
      all_cats <- unique(na.omit(unlist(strsplit(cancer_category_names, ","))))
      if (length(all_cats) == 0) NA_character_ else paste(sort(all_cats), collapse = ",")
    },
    .groups = "drop"
  ) %>%
  rename(PATID = patient_id) %>%
  arrange(PATID, treatment_date)
```

Replace lines 307-319 (the TABLE-2 logging block) with date-grain logging:
```r
message(glue("  TABLE-2 rows (patient-date grain): {nrow(table2)}"))
message(glue("  TABLE-2 unique patients: {n_distinct(table2$PATID)}"))
message(glue("  TABLE-2 date range: {min(table2$treatment_date)} to {max(table2$treatment_date)}"))
n_unique_agents <- n_distinct(unlist(strsplit(table2$agents, ",")))
message(glue("  TABLE-2 unique agents across all rows: {n_unique_agents}"))

# Show agent combination frequency
combo_freq <- table2 %>%
  count(agents, sort = TRUE) %>%
  head(10)
message("  Top 10 agent combinations:")
for (i in seq_len(nrow(combo_freq))) {
  message(glue("    {combo_freq$agents[i]}: {combo_freq$n[i]} patient-dates"))
}
```

**C) Update Section 6 xlsx write (line 337)** -- change worksheet name:
- Old: `wb2$add_worksheet("Chemo Drugs by Class")`
- New: `wb2$add_worksheet("Chemo Agents by Date")`
- Old: `wb2$add_data("Chemo Drugs by Class", table2, ...)`
- New: `wb2$add_data("Chemo Agents by Date", table2, ...)`

**D) Update Section 7 summary and sanity check (lines 344-372)**:

Replace the TABLE-2 summary block (lines 354-358):
```r
message(glue("  TABLE-2 (Chemo Agents by Date, Phase 111):"))
message(glue("    Rows (patient-date grain): {nrow(table2)}"))
message(glue("    Unique patients:           {n_distinct(table2$PATID)}"))
message(glue("    Unique agents:             {n_distinct(unlist(strsplit(table2$agents, ',')))}"))
```

Replace the encounter-based sanity check (lines 361-372) with a patient-based sanity check:
```r
# Sanity check: TABLE-2 patients should be a subset of TABLE-1 patients
# (all chemo patients should have treatment encounters in TABLE-1)
t2_patients <- unique(table2$PATID)
t1_patients <- unique(table1$PATID)
t2_not_in_t1 <- setdiff(t2_patients, t1_patients)
if (length(t2_not_in_t1) > 0) {
  warning(glue("[R/36 WARNING] {length(t2_not_in_t1)} TABLE-2 patients not found in TABLE-1 -- data consistency issue"))
} else {
  message(glue("  Sanity check PASSED: all {length(t2_patients)} TABLE-2 patients found in TABLE-1 ({length(t1_patients)} total patients)"))
}
```
  </action>

  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && grep -n "group_by.*treatment_date" R/36_tableau_ready_tables.R && grep -n "agents" R/36_tableau_ready_tables.R | head -10 && grep -c "ENCOUNTERID" R/36_tableau_ready_tables.R && grep -c "drug_class" R/36_tableau_ready_tables.R && grep -n "patient-date grain" R/36_tableau_ready_tables.R</automated>
  </verify>

  <acceptance_criteria>
    - R/36 Section 5 contains `group_by(patient_id, treatment_date)` (date-grain grouping)
    - R/36 Section 5 contains `agents = paste(sort(unique(na.omit(medication_name))), collapse = ",")` (collapsed agent string)
    - R/36 Section 5 contains `unlist(strsplit(cancer_codes, ","))` (split-union for cancer code merge)
    - R/36 Section 5 contains `.groups = "drop"` (proper ungrouping)
    - R/36 Section 5 select/build does NOT reference ENCOUNTERID in the table2 build block (D-01: dropped)
    - R/36 Section 5 does NOT contain `drug_class = "Chemotherapy"` mutate line (D-02: dropped)
    - R/36 Section 7 sanity check uses `t2_patients` and `t1_patients` (not `t2_encounters` and `t1_encounters`)
    - R/36 Section 6 worksheet name is "Chemo Agents by Date" (not "Chemo Drugs by Class")
    - R/36 header comment for D-06 lists columns as "PATID, treatment_date, agents, cancer_codes, cancer_category_names"
    - R/36 contains the string "patient-date grain" in at least one message/comment
  </acceptance_criteria>

  <done>R/36 TABLE-2 builds one row per patient+date with collapsed agents string, merged cancer codes, no ENCOUNTERID/drug_class/treatment_type columns, and patient-based sanity check replacing encounter-based check</done>
</task>

<task type="auto">
  <name>Task 2: Update R/88 smoke test for new TABLE-2 column structure</name>
  <files>R/88_smoke_test_comprehensive.R</files>

  <read_first>
    - R/88_smoke_test_comprehensive.R (lines 2648-2717 -- SECTION 31H Phase 106 R/36 validation)
    - R/36_tableau_ready_tables.R (after Task 1 modifications -- to verify what patterns the smoke test should grep for)
  </read_first>

  <action>
Modify R/88_smoke_test_comprehensive.R SECTION 31H (lines 2648-2717) to validate the new TABLE-2 column structure from Phase 111.

**A) Update section comment (line 2648)**:
- Old: `# SECTION 31H: PHASE 106 R/36 -- TABLEAU-READY TABLES (TABLE-01, TABLE-02) ----`
- New: `# SECTION 31H: PHASE 106/111 R/36 -- TABLEAU-READY TABLES (TABLE-01, TABLE-02) ----`

**B) Update section description comment (line 2650)**:
- Old: `# Validates R/36 Tableau-ready table generation script structural integrity.`
- New: `# Validates R/36 Tableau-ready table generation script structural integrity.`
  `# Phase 111 update: TABLE-2 changed from per-encounter+medication to per-patient+date grain.`

**C) Add Phase 111 TABLE-2 date-grain checks** after the existing TABLE-2 checks (after line 2699). Insert these new checks:

```r
  # Phase 111: TABLE-2 date-grain collapse
  check("R/36 TABLE-2 groups by patient+date (Phase 111 D-08)",
        any(grepl("group_by.*treatment_date", r36_text)))

  check("R/36 TABLE-2 collapses agents string (Phase 111 D-06)",
        any(grepl("agents.*=.*paste.*sort.*unique.*medication_name", r36_text)))

  check("R/36 TABLE-2 merges cancer_codes via strsplit (Phase 111 D-03)",
        any(grepl("strsplit.*cancer_codes", r36_text)))

  check("R/36 TABLE-2 uses .groups = 'drop' in summarise",
        any(grepl('\\.groups.*=.*"drop"', r36_text)))
```

**D) Update the SECTION marker count check** (line 2711-2712):
- The `check("R/36 has 7+ SECTION markers"` assertion should remain as-is (R/36 still has 7 sections, unchanged).

**E) Update the requirements summary messages** at the bottom of R/88 (around lines 3134-3135). Find the lines:
```r
message("  * TABLE-01: Encounter cancer codes Tableau table with comma separators (R/36 Phase 106)")
message("  * TABLE-02: Chemo drugs by class Tableau table with medication names (R/36 Phase 106)")
```
Replace the TABLE-02 line:
```r
message("  * TABLE-02: Chemo agents by date (patient-date grain, collapsed agents) (R/36 Phase 106+111)")
```
  </action>

  <verify>
    <automated>cd C:\Users\Owner\Documents\insurance_investigation && grep -n "Phase 111" R/88_smoke_test_comprehensive.R && grep -n "agents.*paste.*sort" R/88_smoke_test_comprehensive.R && grep -n "group_by.*treatment_date" R/88_smoke_test_comprehensive.R && grep -c "Phase 111" R/88_smoke_test_comprehensive.R</automated>
  </verify>

  <acceptance_criteria>
    - R/88 SECTION 31H header contains "PHASE 106/111" (updated from "PHASE 106")
    - R/88 contains check for `group_by.*treatment_date` pattern in R/36
    - R/88 contains check for `agents.*=.*paste.*sort.*unique.*medication_name` pattern in R/36
    - R/88 contains check for `strsplit.*cancer_codes` pattern in R/36
    - R/88 contains check for `.groups.*=.*"drop"` pattern in R/36
    - R/88 contains at least 4 occurrences of "Phase 111" string
    - R/88 requirements summary line for TABLE-02 contains "patient-date grain" and "Phase 106+111"
  </acceptance_criteria>

  <done>R/88 smoke test validates the new TABLE-2 date-grain column structure including agents collapse, cancer_codes split-union merge, and group_by patient+date pattern</done>
</task>

</tasks>

<verification>
1. `grep -n "group_by.*treatment_date" R/36_tableau_ready_tables.R` returns at least 1 match
2. `grep -c "ENCOUNTERID" R/36_tableau_ready_tables.R` returns a count -- ENCOUNTERID should still appear in Section 3 (cancer code extraction per encounter) and Section 4 (TABLE-1) but NOT in the table2 build block in Section 5
3. `grep -n "agents" R/36_tableau_ready_tables.R` shows agents column in Section 5 build and Section 7 summary
4. `grep -n "Phase 111" R/88_smoke_test_comprehensive.R` returns at least 4 matches
5. R/36 still has 7 SECTION markers (verify: `grep -c "SECTION.*----" R/36_tableau_ready_tables.R` returns >= 7)
</verification>

<success_criteria>
- R/36 TABLE-2 output is per-patient+date grain with 5 columns: PATID, treatment_date, agents, cancer_codes, cancer_category_names
- No ENCOUNTERID, drug_class, or treatment_type columns in TABLE-2 output
- Agents are alphabetically sorted, deduplicated, comma-separated per patient+date
- Cancer codes are split, unioned, deduplicated, and re-collapsed across encounters sharing same patient+date
- R/88 smoke test validates the new TABLE-2 structure with 4 new Phase 111 checks
- Both files pass grep-based structural verification
</success_criteria>

<output>
After completion, create `.planning/phases/111-for-chemo-drugs-by-class-xlsx-combine-agents-by-date-per-id-collapse-agents-into-one-string-for-each-date/111-01-SUMMARY.md`
</output>
