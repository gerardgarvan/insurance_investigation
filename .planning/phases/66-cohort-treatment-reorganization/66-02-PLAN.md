---
phase: 66-cohort-treatment-reorganization
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  # Cancer decade renames (Task 1):
  - R/40_cancer_site_frequency.R
  - R/41_extract_all_codes.R
  - R/42_build_code_descriptions.R
  - R/43_cancer_site_confirmation.R
  - R/44_cancer_site_confirmation_7day.R
  - R/45_cancer_summary.R
  - R/46_cancer_summary_table.R
  - R/47_cancer_summary_refined.R
  - R/48_cancer_summary_post_hl.R
  - R/49_cancer_summary_pre_post.R
  - R/50_all_codes_resolved.R
  - R/51_gantt_data_export.R
  - R/52_gantt_v2_export.R
  - R/53_death_date_validation.R
  # Payer/QA decade renames (Task 2):
  - R/60_tiered_same_day_payer.R
  - R/61_tiered_encounter_level.R
  - R/62_tiered_date_level.R
  - R/63_value_audit.R
  - R/64_all_source_missingness.R
  - R/65_uf_insurance_missingness.R
  - R/66_all_site_duplicate_dates.R
  - R/67_multi_source_overlap_detection.R
  - R/68_overlap_classification.R
  - R/69_per_patient_source_detection.R
autonomous: true
requirements:
  - REORG-01
  - REORG-02

must_haves:
  truths:
    - "Cancer analysis scripts exist at 40-53 with gantt exports in cancer decade (per D-08)"
    - "Payer/QA scripts exist at 60-69 (core payer analysis fills all 10 slots)"
    - "No a/b suffixes remain on any cancer or payer scripts (per D-07)"
    - "All source() references to old cancer/payer numbers updated to new numbers"
    - "No files exist at old numbers (47-58, 17-24, 33-36, 45a-46a)"
  artifacts:
    - path: "R/40_cancer_site_frequency.R"
      provides: "Cancer site classification (renamed from 47)"
    - path: "R/51_gantt_data_export.R"
      provides: "Gantt data export v1 (renamed from 49, per D-08 stays in cancer)"
    - path: "R/52_gantt_v2_export.R"
      provides: "Gantt data export v2 (renamed from 63, per D-08 stays in cancer)"
    - path: "R/60_tiered_same_day_payer.R"
      provides: "Core payer tiering (renamed from 36)"
    - path: "R/61_tiered_encounter_level.R"
      provides: "Encounter-level payer (renamed from 45a, no suffix per D-07)"
    - path: "R/62_tiered_date_level.R"
      provides: "Date-level payer (renamed from 46a, no suffix per D-07)"
  key_links:
    - from: "R/64_all_source_missingness.R"
      to: "R/14_build_cohort.R"
      via: "conditional source() call"
      pattern: 'source.*R/14_build_cohort'
    - from: "R/65_uf_insurance_missingness.R"
      to: "R/02_harmonize_payer.R"
      via: "source() call"
      pattern: 'source.*R/02_harmonize_payer'
---

<objective>
Renumber 14 cancer analysis scripts to the 40-59 decade and 10 payer/QA scripts to the 60-69 decade, eliminating all a/b suffixes, and update every source() cross-reference.

Purpose: Complete the cancer (40-53) and payer/QA (60-69) decades of the v2.0 numbering scheme (REORG-01). Gantt export scripts stay with cancer per D-08. Payer overflow scripts (AV+TH overlap, radiation audit, treatment cross-reference) move to ad-hoc (90-99) in Plan 03.

Output: 24 scripts renamed to final decade positions, all source() cross-references updated, zero stale references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-cohort-treatment-reorganization/66-CONTEXT.md
@.planning/phases/66-cohort-treatment-reorganization/66-RESEARCH.md
@.planning/phases/66-cohort-treatment-reorganization/66-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renumber cancer analysis scripts to 40-53 and update all source() references</name>
  <files>
    R/40_cancer_site_frequency.R, R/41_extract_all_codes.R,
    R/42_build_code_descriptions.R, R/43_cancer_site_confirmation.R,
    R/44_cancer_site_confirmation_7day.R, R/45_cancer_summary.R,
    R/46_cancer_summary_table.R, R/47_cancer_summary_refined.R,
    R/48_cancer_summary_post_hl.R, R/49_cancer_summary_pre_post.R,
    R/50_all_codes_resolved.R, R/51_gantt_data_export.R,
    R/52_gantt_v2_export.R, R/53_death_date_validation.R
  </files>
  <read_first>
    R/47_cancer_site_frequency.R (lines 1-35 for header and source() calls)
    R/49_gantt_data_export.R (lines 1-65 for header and source() calls -- sources 00_config, utils_duckdb, utils_dates)
    R/63_gantt_v2_export.R (lines 1-95 for header and source() calls)
    R/55_cancer_summary_refined.R (lines 1-50 for header and source() calls)
    R/59_death_date_validation.R (lines 1-25 for header and source() calls)
  </read_first>
  <action>
    **Cancer decade assignment (Claude's discretion, ordered by analysis flow):**

    Cancer site analysis flow: frequency -> extract codes -> build descriptions -> confirmation -> 7-day confirmation -> summary -> summary table -> refined summary -> post-HL summary -> pre/post summary -> all codes resolved -> gantt v1 -> gantt v2 -> death validation.

    **Step 1: Rename files using git mv (14 renames)**

    COLLISION WARNING: Some source numbers (47-54) overlap with target range (40-53). To avoid collisions, rename in safe order: first rename scripts whose source numbers do NOT collide with any target, then rename the rest.

    Safe rename order (no collisions possible):
    ```bash
    # First: scripts from outside 40-53 range (no collision)
    git mv R/55_cancer_summary_refined.R R/47_cancer_summary_refined.R
    git mv R/56_cancer_summary_post_hl.R R/48_cancer_summary_post_hl.R
    git mv R/58_cancer_summary_pre_post.R R/49_cancer_summary_pre_post.R
    git mv R/59_death_date_validation.R R/53_death_date_validation.R
    git mv R/63_gantt_v2_export.R R/52_gantt_v2_export.R

    # Then: scripts that vacate slots needed by others, in dependency order
    # 49 needs to vacate before 49 is used as target; 47 needs to vacate before 47 is target
    # Rename OUT first (scripts currently at 47-54 that need to move):
    git mv R/54_cancer_summary_table.R R/46_cancer_summary_table.R
    git mv R/53_cancer_summary.R R/45_cancer_summary.R
    git mv R/52_all_codes_resolved.R R/50_all_codes_resolved.R
    git mv R/51_cancer_site_confirmation_7day.R R/44_cancer_site_confirmation_7day.R
    git mv R/50_cancer_site_confirmation.R R/43_cancer_site_confirmation.R
    git mv R/49_gantt_data_export.R R/51_gantt_data_export.R
    git mv R/48b_build_code_descriptions.R R/42_build_code_descriptions.R
    git mv R/48a_extract_all_codes.R R/41_extract_all_codes.R
    git mv R/47_cancer_site_frequency.R R/40_cancer_site_frequency.R
    ```

    IMPORTANT: The executor MUST verify the order above avoids collisions. If git mv fails because a target already exists, adjust the order. The key insight: rename scripts that vacate higher slots first, then rename scripts that occupy lower slots. If collisions are unavoidable, use a temporary name (e.g., `git mv R/49_gantt_data_export.R R/tmp_gantt_data_export.R` then `git mv R/tmp_gantt_data_export.R R/51_gantt_data_export.R`).

    **Step 2: Update header/self-reference comments in each renamed file**

    Full mapping for headers (update both header line and Usage: source() comment):
    - `47_cancer_site_frequency` -> `40_cancer_site_frequency`
    - `48a_extract_all_codes` -> `41_extract_all_codes` (drop `a` suffix per D-07)
    - `48b_build_code_descriptions` -> `42_build_code_descriptions` (drop `b` suffix per D-07)
    - `50_cancer_site_confirmation` -> `43_cancer_site_confirmation`
    - `51_cancer_site_confirmation_7day` -> `44_cancer_site_confirmation_7day`
    - `53_cancer_summary` -> `45_cancer_summary`
    - `54_cancer_summary_table` -> `46_cancer_summary_table`
    - `55_cancer_summary_refined` -> `47_cancer_summary_refined`
    - `56_cancer_summary_post_hl` -> `48_cancer_summary_post_hl`
    - `58_cancer_summary_pre_post` -> `49_cancer_summary_pre_post`
    - `52_all_codes_resolved` -> `50_all_codes_resolved`
    - `49_gantt_data_export` -> `51_gantt_data_export` (per D-08: gantt stays in cancer)
    - `63_gantt_v2_export` -> `52_gantt_v2_export` (per D-08: gantt stays in cancer)
    - `59_death_date_validation` -> `53_death_date_validation`

    **Step 3: Update source() calls in renamed cancer scripts**

    Most cancer scripts only source `R/00_config.R` and `R/01_load_pcornet.R` -- these are UNCHANGED (foundation decade stayed at 00-03).

    Scripts with utils source() calls that are already correct (updated in Phase 65):
    - R/51_gantt_data_export.R (formerly 49): sources `R/utils/utils_duckdb.R` and `R/utils/utils_dates.R` -- CORRECT, no change
    - R/52_gantt_v2_export.R (formerly 63): sources `R/utils/utils_duckdb.R` and `R/utils/utils_dates.R` -- CORRECT, no change
    - R/53_death_date_validation.R (formerly 59): sources `R/utils/utils_duckdb.R` and `R/utils/utils_dates.R` -- CORRECT, no change

    No cancer scripts source other cancer scripts, so no cross-cancer source() updates needed.

    **Step 4: Verify with grep**
    ```bash
    grep -rn 'source("R/47_cancer' R/*.R
    grep -rn 'source("R/48[ab]_' R/*.R
    grep -rn 'source("R/49_gantt' R/*.R
    grep -rn 'source("R/50_cancer' R/*.R
    grep -rn 'source("R/51_cancer' R/*.R
    grep -rn 'source("R/52_all_codes' R/*.R
    grep -rn 'source("R/53_cancer' R/*.R
    grep -rn 'source("R/54_' R/*.R
    grep -rn 'source("R/55_' R/*.R
    grep -rn 'source("R/56_' R/*.R
    grep -rn 'source("R/58_' R/*.R
    grep -rn 'source("R/59_' R/*.R
    grep -rn 'source("R/63_' R/*.R
    ```
    All must return zero matches.

    NOTE: 55_search_C8190.R is an ad-hoc script -- it moves to 90-99 in Plan 03, not here.
  </action>
  <verify>
    <automated>bash -c "test -f R/40_cancer_site_frequency.R && test -f R/45_cancer_summary.R && test -f R/51_gantt_data_export.R && test -f R/52_gantt_v2_export.R && test -f R/53_death_date_validation.R && test ! -f R/47_cancer_site_frequency.R && test ! -f R/49_gantt_data_export.R && test ! -f R/63_gantt_v2_export.R && echo PASS || echo FAIL"</automated>

    Verify cancer decade file count:
    ```bash
    ls R/4[0-9]_*.R R/5[0-3]_*.R 2>/dev/null | wc -l
    ```
    Expect: 14

    Verify no stale references:
    ```bash
    grep -rn 'source("R/47_cancer\|source("R/49_gantt\|source("R/63_gantt' R/*.R | wc -l
    ```
    Expect: 0
  </verify>
  <acceptance_criteria>
    - R/40_cancer_site_frequency.R exists and contains `40_cancer_site_frequency` in header
    - R/41_extract_all_codes.R exists and contains `41_extract_all_codes` (no `a` suffix)
    - R/42_build_code_descriptions.R exists and contains `42_build_code_descriptions` (no `b` suffix)
    - R/43_cancer_site_confirmation.R exists and contains `43_cancer_site_confirmation`
    - R/44_cancer_site_confirmation_7day.R exists and contains `44_cancer_site_confirmation_7day`
    - R/45_cancer_summary.R exists and contains `45_cancer_summary`
    - R/46_cancer_summary_table.R exists and contains `46_cancer_summary_table`
    - R/47_cancer_summary_refined.R exists and contains `47_cancer_summary_refined`
    - R/48_cancer_summary_post_hl.R exists and contains `48_cancer_summary_post_hl`
    - R/49_cancer_summary_pre_post.R exists and contains `49_cancer_summary_pre_post`
    - R/50_all_codes_resolved.R exists and contains `50_all_codes_resolved`
    - R/51_gantt_data_export.R exists and contains `51_gantt_data_export` (per D-08)
    - R/52_gantt_v2_export.R exists and contains `52_gantt_v2_export` (per D-08)
    - R/53_death_date_validation.R exists and contains `53_death_date_validation`
    - R/47_cancer_site_frequency.R (old) does NOT exist (now at 40)
    - R/48a_extract_all_codes.R does NOT exist
    - R/48b_build_code_descriptions.R does NOT exist
    - R/49_gantt_data_export.R (old) does NOT exist (now at 51)
    - R/55_cancer_summary_refined.R does NOT exist
    - R/56_cancer_summary_post_hl.R does NOT exist
    - R/58_cancer_summary_pre_post.R does NOT exist
    - R/59_death_date_validation.R does NOT exist
    - R/63_gantt_v2_export.R (old) does NOT exist (now at 52)
    - `grep -rn 'source("R/47_cancer' R/*.R` returns zero matches
    - `grep -rn 'source("R/49_gantt' R/*.R` returns zero matches
    - `grep -rn 'source("R/63_' R/*.R` returns zero matches
  </acceptance_criteria>
  <done>14 cancer scripts renamed to 40-53, gantt exports in cancer decade per D-08, a/b suffixes eliminated per D-07, zero stale references remain</done>
</task>

<task type="auto">
  <name>Task 2: Renumber payer/QA scripts to 60-69 and update all source() references</name>
  <files>
    R/60_tiered_same_day_payer.R, R/61_tiered_encounter_level.R,
    R/62_tiered_date_level.R, R/63_value_audit.R,
    R/64_all_source_missingness.R, R/65_uf_insurance_missingness.R,
    R/66_all_site_duplicate_dates.R, R/67_multi_source_overlap_detection.R,
    R/68_overlap_classification.R, R/69_per_patient_source_detection.R
  </files>
  <read_first>
    R/36_tiered_same_day_payer.R (lines 1-60 for header, usage, and source() calls)
    R/45a_tiered_encounter_level.R (lines 1-35 for header and source() calls)
    R/46a_tiered_date_level.R (lines 1-45 for header and source() calls)
    R/17_value_audit.R (lines 1-30 for header and source() calls)
    R/20_all_source_missingness.R (lines 1-60 for header, source() calls -- references build_cohort)
    R/22a_multi_source_overlap_detection.R (lines 1-50 for header and source() calls)
    R/22b_generate_phase19_20_pptx.R (lines 150-160 for source() calls to 18_ and 19_ scripts)
  </read_first>
  <action>
    **Payer/QA decade assignment (Claude's discretion, ordered by analysis importance):**

    Core payer tiering first (60-62), then quality audits (63-66), then overlap detection (67-69).

    **Step 1: Rename files using git mv (10 renames)**

    COLLISION WARNING: After Plan 01 Task 2 renamed treatment scripts, old 20/21/22a/23/24 no longer exist. But 17, 18, 19, 33, 34, 35 are still at their old numbers. And after Task 1 of this plan, 45a/46a/36 are still at old numbers. So source ranges do not collide with 60-69 targets. Safe to proceed.

    NOTE: 65_smoke_test_foundation.R currently occupies slot 65. This will be renamed to 86 in Plan 03 (test decade). So we must rename 65_smoke_test_foundation FIRST or use the payer script's final name. Since Plan 03 handles tests, and we need slot 65 now, the executor should rename R/65_smoke_test_foundation.R to a temporary name OR renumber it to its final position (86) here, before placing the payer script at 65.

    IMPORTANT: Rename 65_smoke_test_foundation.R to R/86_smoke_test_foundation.R FIRST to free slot 65:
    ```bash
    git mv R/65_smoke_test_foundation.R R/86_smoke_test_foundation.R
    ```
    Then update header in R/86_smoke_test_foundation.R: `65_smoke_test_foundation` -> `86_smoke_test_foundation`.
    Then update self-reference comment: `Rscript R/65_smoke_test_foundation.R` -> `Rscript R/86_smoke_test_foundation.R`.

    Now rename payer/QA scripts:
    ```bash
    git mv R/36_tiered_same_day_payer.R R/60_tiered_same_day_payer.R
    git mv R/45a_tiered_encounter_level.R R/61_tiered_encounter_level.R
    git mv R/46a_tiered_date_level.R R/62_tiered_date_level.R
    git mv R/17_value_audit.R R/63_value_audit.R
    git mv R/20_all_source_missingness.R R/64_all_source_missingness.R
    git mv R/18_uf_insurance_missingness.R R/65_uf_insurance_missingness.R
    git mv R/21_all_site_duplicate_dates.R R/66_all_site_duplicate_dates.R
    git mv R/22a_multi_source_overlap_detection.R R/67_multi_source_overlap_detection.R
    git mv R/23_overlap_classification.R R/68_overlap_classification.R
    git mv R/24_per_patient_source_detection.R R/69_per_patient_source_detection.R
    ```

    **Step 2: Update header/self-reference comments in each renamed file**

    Full mapping (update header line and Usage: source() comment):
    - `36_tiered_same_day_payer` -> `60_tiered_same_day_payer`
    - `45a_tiered_encounter_level` -> `61_tiered_encounter_level` (drop `a` suffix per D-07)
    - `46a_tiered_date_level` -> `62_tiered_date_level` (drop `a` suffix per D-07)
    - `17_value_audit` -> `63_value_audit`
    - `20_all_source_missingness` -> `64_all_source_missingness`
    - `18_uf_insurance_missingness` -> `65_uf_insurance_missingness`
    - `21_all_site_duplicate_dates` -> `66_all_site_duplicate_dates`
    - `22a_multi_source_overlap_detection` -> `67_multi_source_overlap_detection` (drop `a` suffix)
    - `23_overlap_classification` -> `68_overlap_classification`
    - `24_per_patient_source_detection` -> `69_per_patient_source_detection`

    **Step 3: Update source() calls WITHIN renamed payer scripts**

    These payer scripts reference `R/14_build_cohort.R` (already renumbered in Plan 01):
    - R/64_all_source_missingness.R (formerly 20): line 60 should already say `source("R/14_build_cohort.R")` from Plan 01. Verify.
    - R/64_all_source_missingness.R (formerly 20): line 39 sources `R/02_harmonize_payer.R` -- UNCHANGED (foundation)

    R/65_uf_insurance_missingness.R (formerly 18): line 35 sources `R/02_harmonize_payer.R` -- UNCHANGED

    All payer scripts sourcing `R/00_config.R` or `R/01_load_pcornet.R` -- UNCHANGED (foundation).

    **Step 4: Update source() calls in OTHER scripts that reference payer/QA files**

    R/22b_generate_phase19_20_pptx.R (will be renumbered to 73 in Plan 03, but update source() calls NOW):
    - Line 157: `source("R/18_uf_insurance_missingness.R")` -> `source("R/65_uf_insurance_missingness.R")`
    - Line 158: `source("R/19_flm_duplicate_dates.R")` -> keep as-is (19_flm is renumbered to ad-hoc in Plan 03 -- but wait, checking source list: 19_flm_duplicate_dates is NOT in the payer/QA mapping. It's a standalone diagnostic. Let me check: it sources 00_config and 01_load_pcornet. It investigates FLM duplicate dates. This should go to ad-hoc (90-99). So leave this reference for Plan 03 to update when 19_flm is renumbered.)

    Actually, 22b references 18_ (becoming 65) and 19_ (becoming ad-hoc in Plan 03). Update the 18_ reference now, leave 19_ for Plan 03.
    - R/22b_generate_phase19_20_pptx.R line 157: `source("R/18_uf_insurance_missingness.R")` -> `source("R/65_uf_insurance_missingness.R")`

    R/11_generate_pptx.R line 1281: `source("R/16_encounter_analysis.R")` -- script 16 is renumbered to outputs decade (75) in Plan 03. Leave for Plan 03.

    **Step 5: Verify with grep**
    ```bash
    grep -rn 'source("R/36_' R/*.R
    grep -rn 'source("R/45a_' R/*.R
    grep -rn 'source("R/46a_' R/*.R
    grep -rn 'source("R/17_' R/*.R
    grep -rn 'source("R/20_all_source' R/*.R
    grep -rn 'source("R/18_' R/*.R
    grep -rn 'source("R/21_' R/*.R
    grep -rn 'source("R/22a_' R/*.R
    grep -rn 'source("R/23_overlap' R/*.R
    grep -rn 'source("R/24_' R/*.R
    grep -rn 'source("R/65_smoke' R/*.R
    ```
    All must return zero matches. (Note: 22b's reference to 19_ is expected to still show the old number -- Plan 03 handles that.)
  </action>
  <verify>
    <automated>bash -c "test -f R/60_tiered_same_day_payer.R && test -f R/61_tiered_encounter_level.R && test -f R/62_tiered_date_level.R && test -f R/63_value_audit.R && test -f R/69_per_patient_source_detection.R && test -f R/86_smoke_test_foundation.R && test ! -f R/36_tiered_same_day_payer.R && test ! -f R/45a_tiered_encounter_level.R && test ! -f R/17_value_audit.R && test ! -f R/65_smoke_test_foundation.R && echo PASS || echo FAIL"</automated>

    Verify payer decade file count:
    ```bash
    ls R/6[0-9]_*.R 2>/dev/null | wc -l
    ```
    Expect: 10

    Verify no stale references:
    ```bash
    grep -rn 'source("R/36_\|source("R/45a_\|source("R/17_value\|source("R/18_uf\|source("R/22a_' R/*.R | wc -l
    ```
    Expect: 0
  </verify>
  <acceptance_criteria>
    - R/60_tiered_same_day_payer.R exists and contains `60_tiered_same_day_payer`
    - R/61_tiered_encounter_level.R exists and contains `61_tiered_encounter_level` (no `a` suffix)
    - R/62_tiered_date_level.R exists and contains `62_tiered_date_level` (no `a` suffix)
    - R/63_value_audit.R exists and contains `63_value_audit`
    - R/64_all_source_missingness.R exists and contains `64_all_source_missingness`
    - R/65_uf_insurance_missingness.R exists and contains `65_uf_insurance_missingness`
    - R/66_all_site_duplicate_dates.R exists and contains `66_all_site_duplicate_dates`
    - R/67_multi_source_overlap_detection.R exists and contains `67_multi_source_overlap_detection` (no `a` suffix)
    - R/68_overlap_classification.R exists and contains `68_overlap_classification`
    - R/69_per_patient_source_detection.R exists and contains `69_per_patient_source_detection`
    - R/86_smoke_test_foundation.R exists (freed slot 65 for payer script)
    - R/36_tiered_same_day_payer.R does NOT exist
    - R/45a_tiered_encounter_level.R does NOT exist
    - R/46a_tiered_date_level.R does NOT exist
    - R/17_value_audit.R does NOT exist
    - R/18_uf_insurance_missingness.R does NOT exist
    - R/22a_multi_source_overlap_detection.R does NOT exist
    - R/65_smoke_test_foundation.R does NOT exist (now at 86)
    - `grep -rn 'source("R/36_' R/*.R` returns zero matches
    - `grep -rn 'source("R/45a_' R/*.R` returns zero matches
    - `grep -rn 'source("R/17_value' R/*.R` returns zero matches
    - `grep -rn 'source("R/18_uf' R/*.R` returns zero matches
    - `grep -rn 'source("R/22a_' R/*.R` returns zero matches
    - R/22b_generate_phase19_20_pptx.R contains `source("R/65_uf_insurance_missingness.R")` (updated ref)
  </acceptance_criteria>
  <done>10 payer/QA scripts renamed to 60-69, 65_smoke_test moved to 86 to free slot, a/b suffixes eliminated per D-07, all cross-references updated</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Cancer decade (40-53) complete:
   ```bash
   ls R/4[0-9]_*.R R/5[0-3]_*.R 2>/dev/null | wc -l
   ```
   Expect: 14

2. Payer/QA decade (60-69) complete:
   ```bash
   ls R/6[0-9]_*.R 2>/dev/null | wc -l
   ```
   Expect: 10

3. No old cancer files remain:
   ```bash
   test ! -f R/47_cancer_site_frequency.R && test ! -f R/49_gantt_data_export.R && test ! -f R/55_cancer_summary_refined.R && echo "PASS" || echo "FAIL"
   ```

4. No old payer files remain:
   ```bash
   test ! -f R/36_tiered_same_day_payer.R && test ! -f R/17_value_audit.R && test ! -f R/45a_tiered_encounter_level.R && echo "PASS" || echo "FAIL"
   ```

5. No a/b suffixes remain:
   ```bash
   ls R/*[ab]_*.R 2>/dev/null | wc -l
   ```
   Expect: remaining a/b files are only: 43b, 44b, 45b, 46b, 22b (these move to test/ad-hoc/outputs in Plan 03)
</verification>

<success_criteria>
- 14 cancer scripts at 40-53 (gantt exports in cancer per D-08)
- 10 payer/QA scripts at 60-69 (core analysis, no overflow needed)
- All a/b suffixes eliminated on cancer and payer scripts (per D-07)
- 65_smoke_test_foundation moved to 86 to free payer slot
- Zero grep matches for old script numbers in source() calls
- No old-numbered files exist in R/ directory for these decades
</success_criteria>

<output>
After completion, create `.planning/phases/66-cohort-treatment-reorganization/66-02-SUMMARY.md`
</output>
