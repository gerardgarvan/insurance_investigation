---
phase: quick
plan: 260819-k0k
type: execute
wave: 1
depends_on: []
files_modified:
  - R/118_build_zip5_adi_summary.R
  - R/SCRIPT_INDEX.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Script is named R/118_build_zip5_adi_summary.R with a matching header line 1 and run-line comment"
    - "The 0000 gate is gone; stopifnot block has 4 checks; message says 'All 4 validation gates passed.'"
    - "The three summarise lines are wrapped in if(any(!is.na(adi_num))) guards, with unname() on the quantiles"
    - "ADI_COVERAGE_FLOOR <- 0.50 constant exists; adi_coverage column computed in mutate after summarise; suppressed-count message printed; header comment lists adi_coverage"
    - "METHOD string states the beneficiary-based denominator basis"
    - "SCRIPT_INDEX.md Post-Renumber table row for R/118 names the new file and correct description"
  artifacts:
    - path: "R/118_build_zip5_adi_summary.R"
      provides: "Corrected ZIP5 ADI summary builder"
    - path: "R/SCRIPT_INDEX.md"
      provides: "Updated script index with R/118 new name and description"
  key_links:
    - from: "R/118_build_zip5_adi_summary.R constants block"
      to: "ADI_COVERAGE_FLOOR"
      via: "assigned as named constant before Step 4"
      pattern: "ADI_COVERAGE_FLOOR\\s*<-\\s*0\\.50"
    - from: "stopifnot block"
      to: "4 gates only"
      via: "0000 line removed"
      pattern: "All 4 validation gates passed"
---

<objective>
Apply all code-level steps from 118-FIX-PLAN.md to R/118_build_centroid_crosswalk.R:
rename it, remove the bad 0000 gate, guard empty-group quantile calls, add the ADI_COVERAGE_FLOOR
constant and adi_coverage column with suppression message, update METHOD to state the denominator,
and update SCRIPT_INDEX.md.

Purpose: The current script has a misleading filename, a gate that rejects valid ZIP5s ending in
0000, an abort-on-real-data quantile bug, and a METHOD string that conceals the beneficiary-based
denominator. Steps 6/7 of the fix plan (Florida coverage check and re-run) are HiPerGator-only
user actions performed after this commit.

Output: R/118_build_zip5_adi_summary.R (corrected), R/SCRIPT_INDEX.md (updated row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@118-FIX-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename script and apply all code fixes (Steps 1-5 of fix plan)</name>
  <files>R/118_build_zip5_adi_summary.R</files>
  <action>
**Step 1 — Rename via git mv, then fix header + run-line.**

Run:
```
git mv R/118_build_centroid_crosswalk.R R/118_build_zip5_adi_summary.R
```

Replace line 1 of the header:
```r
# R/118_build_zip5_adi_summary.R
```
Replace the run-line comment (currently `# Run on HiPerGator: Rscript R/118_build_centroid_crosswalk.R`):
```r
# Run on HiPerGator: Rscript R/118_build_zip5_adi_summary.R
```

**Step 2 — Delete the 0000 gate and update the message.**

In the stopifnot() block (Step 5 of the script), remove this line entirely:
```r
  "no synthetic 0000 entries"         = !any(grepl("0000$", out$ZIP5)),
```
Change the message immediately after stopifnot from:
```r
message("All 5 validation gates passed.")
```
to:
```r
message("All 4 validation gates passed.")
```
Do NOT touch the 0000 guard inside approximate_zip9() in utils_address.R — that is a different
schema and must remain.

**Step 3 — Guard empty-group quantile/median calls.**

In the summarise() block (Step 4 of the script), replace these three lines:
```r
    adi_natrank_median = median(adi_num, na.rm = TRUE),
    adi_natrank_p25    = quantile(adi_num, 0.25, na.rm = TRUE),
    adi_natrank_p75    = quantile(adi_num, 0.75, na.rm = TRUE),
```
with:
```r
    # quantile() ERRORS on an all-NA vector even with na.rm = TRUE; median() only warns.
    # A ZIP5 whose every ZIP+4 is suppressed (GQ / GQ-PH / PH / QDI) has no usable values
    # and must emit NA, not abort the build.
    adi_natrank_median = if (any(!is.na(adi_num))) median(adi_num, na.rm = TRUE) else NA_real_,
    adi_natrank_p25    = if (any(!is.na(adi_num))) unname(quantile(adi_num, 0.25, na.rm = TRUE)) else NA_real_,
    adi_natrank_p75    = if (any(!is.na(adi_num))) unname(quantile(adi_num, 0.75, na.rm = TRUE)) else NA_real_,
```

**Step 4 — Add ADI_COVERAGE_FLOOR constant, adi_coverage column, suppression message, and
update the header column list.**

In the Constants block (after the existing constants), add:
```r
# Below this share of ZIP+4s carrying a usable ADI, the ZIP5 median is not reported.
# A median over 3 of 400 segments is not the same quantity as one over 400, and the
# reader cannot tell them apart without this.
ADI_COVERAGE_FLOOR <- 0.50
```

The current mutate() after summarise() is:
```r
  dplyr::mutate(
    vintage = VINTAGE,
    method  = METHOD,
    source  = SOURCE
  )
```
Replace it with:
```r
  dplyr::mutate(
    adi_coverage = n_zip9_with_adi / n_zip9_in_zip5,
    adi_natrank_median = dplyr::if_else(adi_coverage < ADI_COVERAGE_FLOOR, NA_real_, adi_natrank_median),
    adi_natrank_p25    = dplyr::if_else(adi_coverage < ADI_COVERAGE_FLOOR, NA_real_, adi_natrank_p25),
    adi_natrank_p75    = dplyr::if_else(adi_coverage < ADI_COVERAGE_FLOOR, NA_real_, adi_natrank_p75),
    vintage = VINTAGE,
    method  = METHOD,
    source  = SOURCE
  )
```

After the existing summary message (the one printing ZIP5s and non-NA median count), add:
```r
n_below <- sum(out$adi_coverage < ADI_COVERAGE_FLOOR, na.rm = TRUE)
message(glue::glue(
  "Coverage floor {ADI_COVERAGE_FLOOR}: {format(n_below, big.mark=',')} ZIP5s suppressed to NA ",
  "({round(100 * n_below / nrow(out), 1)}%)"
))
```

In the header comment's Output column list, add `adi_coverage` after `n_zip9_with_adi`:
```
#   adi_coverage          numeric, share of ZIP+4s with a non-NA ADI (n_zip9_with_adi / n_zip9_in_zip5)
```

**Step 5 — Update METHOD string to state the denominator basis.**

Replace:
```r
METHOD              <- "zip5_adi_summary_route_b"
```
with:
```r
METHOD              <- "zip5_adi_summary_route_b; median/IQR of ADI_NATRANK over Neighborhood Atlas ZIP+4 records within each ZIP5 (denominator = ZIP+4 segments present in the Atlas file, which is beneficiary-based, NOT all USPS delivery segments)"
```
  </action>
  <verify>
    <automated>
grep -c "118_build_zip5_adi_summary" R/118_build_zip5_adi_summary.R
grep -c "0000" R/118_build_zip5_adi_summary.R
grep -c "All 4 validation gates passed" R/118_build_zip5_adi_summary.R
grep -c "any(!is.na(adi_num))" R/118_build_zip5_adi_summary.R
grep -c "ADI_COVERAGE_FLOOR" R/118_build_zip5_adi_summary.R
grep -c "adi_coverage" R/118_build_zip5_adi_summary.R
grep -c "beneficiary-based" R/118_build_zip5_adi_summary.R
</automated>
  </verify>
  <done>
    - R/118_build_zip5_adi_summary.R exists (renamed via git mv)
    - Header line 1 and run-line comment both say 118_build_zip5_adi_summary.R
    - grep -c '0000' R/118_build_zip5_adi_summary.R returns 0
    - stopifnot has 4 named checks; message says "All 4 validation gates passed."
    - All three summary lines use if(any(!is.na(adi_num))) guard; quantile calls wrapped in unname()
    - ADI_COVERAGE_FLOOR <- 0.50 is a named constant in the Constants block
    - adi_coverage column computed in mutate after summarise; suppression count message present
    - Header comment column list includes adi_coverage
    - METHOD string contains "beneficiary-based, NOT all USPS delivery segments"
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SCRIPT_INDEX.md Post-Renumber table for R/118</name>
  <files>R/SCRIPT_INDEX.md</files>
  <action>
In the Post-Renumber Investigations (100+) table in R/SCRIPT_INDEX.md, add a new row for R/118
after the R/116 row (the current last row). Do NOT add a second row if a row for R/118 already
appears under the old name — correct it in place instead.

The new row must read:
```
| `R/118_build_zip5_adi_summary.R` | ZIP5-level ADI summary (Route B, D-02). Groups Neighborhood Atlas ZIP+4 ADI scores by ZIP5 prefix, computing median + IQR with an `ADI_COVERAGE_FLOOR` = 0.50 suppression floor. Writes `data/reference/zip5_adi_summary.csv`. Beneficiary-based denominator (Atlas file is BENE_ZIP_CD-based, not all USPS delivery segments). | 148 |
```

Also update the script count in the "Post-renumber investigations (100+)" count line and the
parenthetical list at the bottom of Script Count section to include R/118. The current count is
17; it should become 18. Append `R/118 ZIP5 ADI summary builder (Phase 148)` to the end of the
parenthetical list.
  </action>
  <verify>
    <automated>grep -c "118_build_zip5_adi_summary" R/SCRIPT_INDEX.md</automated>
  </verify>
  <done>
    - SCRIPT_INDEX.md contains a row for R/118_build_zip5_adi_summary.R in the Post-Renumber table
    - No stale row exists for R/118_build_centroid_crosswalk.R
    - Script count updated to 18 post-renumber investigations
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `git status` shows R/118_build_centroid_crosswalk.R renamed to R/118_build_zip5_adi_summary.R
- `grep -c '0000' R/118_build_zip5_adi_summary.R` returns 0
- `grep -c '0000' R/utils/utils_address.R` is unchanged (the crosswalk guard is untouched)
- `grep 'All.*validation gates' R/118_build_zip5_adi_summary.R` shows "All 4"
- `grep 'ADI_COVERAGE_FLOOR' R/118_build_zip5_adi_summary.R` has 4+ hits (constant def + 3 uses)
- `grep '118_build_zip5_adi_summary' R/SCRIPT_INDEX.md` returns 1 hit
</verification>

<success_criteria>
All 5 code-level steps from 118-FIX-PLAN.md applied and verifiable via grep:
1. File renamed; header line 1 + run-line updated
2. 0000 gate removed; message updated to "All 4"
3. quantile/median guarded with if(any(!is.na(adi_num))); unname() on quantiles
4. ADI_COVERAGE_FLOOR constant, adi_coverage column, suppression message, header column list updated
5. METHOD string states beneficiary-based denominator
6. SCRIPT_INDEX.md row correct, no stale old-name row
Steps 6/7 (Florida coverage check, re-run) are HiPerGator user actions — NOT in this plan.
</success_criteria>

<output>
After completion, commit with:
  git commit -m "fix(148): rename R/118 to zip5_adi_summary, remove bad 0000 gate, guard quantile, add coverage floor"

No SUMMARY.md needed for quick tasks.
</output>
