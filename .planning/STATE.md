---
gsd_state_version: 1.0
milestone: v3.4
milestone_name: below)
status: executing
stopped_at: Phase 141 complete — 1/1 plans executed
last_updated: "2026-08-14T17:56:38.960Z"
last_activity: 2026-08-14
progress:
  total_phases: 134
  completed_phases: 120
  total_plans: 247
  completed_plans: 237
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-07-23 after starting v3.4)

**Core value:** A working cohort filter chain that reads like a clinical protocol — with logged attrition at every step and clear payer-stratified visualizations showing how patients flow from enrollment through diagnosis to treatment.

**Current focus:** Phase 143 — 180-day-file-enrichment-parity-and-review-follow-ups

## Current Position

Phase: 143 (180-day-file-enrichment-parity-and-review-follow-ups) — EXECUTING
Plan: 2 of 3 (143-02 Task 1 COMPLETE, Task 2 pending HiPerGator verification)

  - 140-01: COMPLETE (3/3 tasks). Task 1: compute_c02() exposes n_present_no_usable_zip5, console/QC breakdown (commit 6862a64). Task 2 (D-1 checkpoint): resolved by the user this session as a distinct comparison-basis correction -- NOT either of the plan's anticipated option-a (re-derive C02_EXPECTED) or option-b (leave everything unchanged) branches. C02_EXPECTED (26L) and C02_TOLERANCE (5L) stay unchanged; the reconciliation comparison itself is corrected to use n_present_no_usable_zip5 (9, the only population a genuine coalescing defect could produce) instead of the conflated n_patients_no_zip5_ever (665, which mixes in 656 coverage-gap patients absent from addr_coal). Task 3 implemented this resolution (commit b6f1e74) -- SECTION 12 console messages, SECTION 13 qc_tbl labels, SECTION 14 KEY-sheet fields, and the QC-sheet subtitle all updated. NOTE: this comparison basis was itself superseded by 140-09 (Wave 2a) -- see below. See 140-01-SUMMARY.md.
  - 140-02: COMPLETE (3/3 tasks). Task 1: crosswalk staging contract documented in data/reference/README.md; R/115 SECTION 8/13 match-rate coverage gate wired (commits 5d4309b, 6862a64). Task 2 (human-action, crosswalk acquisition): resolved "not available -- defer" -- block-group tier stays gracefully degraded (not a regression from Phase 139); P-03b/P-03c deferred as follow-up. Task 3 (D-2 decision, ZIP5-vs-ZIP9): resolved option-a -- ZIP5 accepted as the primary analysis unit now, unblocking Plans 140-04 through 140-07 (140-04's S1-fold-in design, per 140-08-PATCH FIX-03, confirmed applicable). See 140-02-SUMMARY.md.
  - 140-09 (Wave 2a, inserted by 140-09-PATCH): COMPLETE (3/3 tasks) AND COMMITTED (`9123628`, 2026-08-08). Task 1: compute_c02_baseline() (pre-coalesce baseline). Task 2: c02_reconciled rewired onto c02a_monotone && c02b_partition, retiring the unverifiable C02_EXPECTED=26L comparison from the gate (still retained/reported as a superseded historical footnote). Task 3 (D-5 checkpoint): resolved option-c ("no floor -- report only") -- pct_cohort_with_any_address is reported, not gated. See 140-09-SUMMARY.md.
  - 140-03 (Wave 2b): COMPLETE (3/3 tasks). Task 1 (commit d76a485): classify_unparseable_dates_vec()/classify_unparseable_dates() added to SECTION 1B. Task 2 (commit fef75e8): SECTION 3 captures/classifies unparseable ADDRESS_PERIOD_START strings before the drop-filter (unparseable_date_breakdown, unparseable_date_examples); SECTION 12/13 add n_patients_lost_to_filters_c02 (P-02c, 280-patient filter-loss). Task 3 (checkpoint:human-action, Wave 2.5 blocking gate per 140-08-PATCH FIX-14): **RESOLVED 2026-08-08.** Real HiPerGator run against actual LDS_ADDRESS_HISTORY/PCORnet data confirmed: C-02a (monotonicity, pre-coalesce 26 -> post-coalesce 9) PASS; C-02b (partition identity) PASS; c02_reconciled PASS (first PASS this phase); coverage 92.9% (reported only); unparseable-date breakdown 100% blank_or_null (0 in the other 3 categories) -- **P-02b resolved: residual is genuinely unparseable, no parser gap, no parse_pcornet_date() extension needed**; P-02c confirmed exactly 280; testthat::test_dir('tests/testthat') passed 35/35 (first-ever real execution of every fixture from 140-01/140-09/140-03). All figures matched 140-CONTEXT.md's assumptions with zero material divergence. Notable non-blocking finding: n_present_no_usable_zip5_precoalesce computed to exactly 26, matching the retired historical C02_EXPECTED -- supports the hypothesis that the original "26" was a pre-coalesce figure; flagged to Erin/Amy, not gate-relevant. See 140-03-SUMMARY.md.
  - 140-04 (Wave 3, depends on 140-01/140-02/140-03): **COMPLETE (3/3 tasks) AND COMMITTED.** Task 1 (`45aef76`): `assign_scenarios(encounter_zip_flagged, backward_only)` added to SECTION 1B (D-2 = option-a confirmed applicable, per 140-02) -- S1 folded into S3 universally (S3 test is `has_direct_zip5_only` in both modes); S2 test varies by mode (`s2_either` vs `s2_backward`); SECTION 11's old 5-level inline `case_when` replaced with two independent `assign_scenarios()` calls via direct `---

gsd_state_version: 1.0
milestone: v3.4
milestone_name: below)
status: Executing Phase 141
stopped_at: Phase 141 context gathered
last_updated: "2026-08-13T17:28:50.087Z"
last_activity: 2026-08-10
progress:
  total_phases: 132
  completed_phases: 118
  total_plans: 241
  completed_plans: 234
  percent: 21
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-07-23 after starting v3.4)

**Core value:** A working cohort filter chain that reads like a clinical protocol — with logged attrition at every step and clear payer-stratified visualizations showing how patients flow from enrollment through diagnosis to treatment.

**Current focus:** Phase 141 — context-zip9-imputation

 assignment (140-08-PATCH FIX-09), producing `scenario_assigned` (forward-inclusive, now 4 levels) and `scenario_assigned_backward_only` (new); `tests/testthat/test-115-scenarios.R` created (140-08-PATCH FIX-16 new file) with a 4-case fixture proving the S1-forward-only case resolves to S3 in both modes and a forward-only-S2 case differs between modes. Task 2 (`8bf3a87`): `scenario_priority` collapsed to 4 levels; `waterfall_encounter`/`waterfall_patient` recomputed against the collapsed factor; an INDEPENDENTLY-computed backward-only waterfall (`waterfall_encounter_backward`/`waterfall_patient_backward`, own `count()`/`arrange()`/`cumsum()` call, never derived by subtraction) joined on as parallel `n_backward_only`/`cumulative_n_backward_only`/`cumulative_pct_backward_only` columns; console headline comparison block with explicit never-sum-B_direction_split caveat; coverage-neutrality `stopifnot()` (140-08-PATCH FIX-04); KEY sheet's "Ordered vs unordered S3" field, `B_scenario_counts` subtitle, and `C_completeness` title/subtitle all updated to state the discrepancy has INVERTED (ordered S3 now EXCEEDS unordered S3-eligible), not eliminated. Task 3 (`8e980e8`, D-4 `checkpoint:decision`): **RESOLVED 2026-08-08 as option-a** -- backward-only stays PRIMARY, forward-inclusive stays SENSITIVITY-only, matching 140-CONTEXT.md Section 7's own recommendation. No waterfall-logic code changes required (Tasks 1-2 already implemented this framing); one documentation gap found and fixed -- `C_completeness`'s subtitle referenced "the KEY sheet for the D-4 primary-spec decision" before that field existed, so a new KEY sheet row ("Primary vs sensitivity spec (D-4, resolved 2026-08-08)") was added recording the resolution and explicitly stating option-b was rejected and P-06d is NOT triggered/NOT built. `R/utils/utils_address.R` remains untouched. Structural brace/paren/bracket balance check passed on the file after every edit (1425/1425 parens, 164/164 braces, 42/42 brackets; Rscript unavailable in this Windows environment; true R-parse deferred to a future HiPerGator run). See `140-04-SUMMARY.md`.

Status: Executing Phase 141

  - 140-05 (Wave 4, depends on 140-04): **COMPLETE (3/3 tasks) AND COMMITTED.** Task 1 (`98cca02`): `compute_gap_days_at_assignment()` added to SECTION 1B -- two independently-computed signed per-encounter gap covariates (`gap_days_at_assignment` forward-inclusive, `gap_days_at_assignment_backward_only` never-negative, stopifnot-enforced), mirroring `assign_scenarios()`'s dual-mode design (140-08-PATCH FIX-01/FIX-02/FIX-08 -- backward-wins-ties tiebreaking on the either-direction reductions). `classify_encounter_zip()` now retains `direct_zip_period_start`. Factored as a SECTION 1B pure function (deviation from the plan's literal inline-in-SECTION-11 wording) specifically so `tests/testthat/test-115-scenarios.R`'s `sys.source()`-based test harness -- which stops at SECTION 2's probe gate before SECTION 11 ever runs -- can actually exercise this logic; a 6-case fixture (a)-(f) covers already_has_zip9, S3 backward/forward-only, unresolvable, the FIX-01 forward-only-S2 case, and the FIX-08 exact-tie case. Task 2 (`c765eb9`): `median_gap_within_bin` added to `validation_curve` (P-04b non-monotonicity diagnostic) plus a console NOTE and a new KEY sheet field. Task 3 (`51e2064`, D-3 `checkpoint:decision`, gate="blocking"): **RESOLVED 2026-08-10 as option-a** -- ZIP5 carried forward WITHOUT a hard time-window cap, using the two gap covariates as analytic/sensitivity variables instead of a discard rule. Task 1 already implemented this design exactly, so no code-logic changes were needed; a new KEY sheet field ("Uncapped carry-forward design (D-3, resolved 2026-08-10)") was added recording the resolution and explicitly stating option-b (a cap at a team-specified N days) was NOT selected/NOT built. Structural brace/paren/bracket balance check passed after every edit (1542/1542 parens, 169/169 braces, 43/43 brackets across all three commits; Rscript unavailable in this Windows environment). See `140-05-SUMMARY.md`.
  - 140-06 (Wave 5, depends on 140-05): **COMPLETE (2/2 tasks) AND COMMITTED.** Task 1 (`176cf84`): `build_encounter_anchored_validation_cases()` added to SECTION 1B -- filters to `has_direct_zip9 == TRUE` encounters (ground truth), predicts from the `zip9_seq` spell immediately prior to the covering record's own `direct_zip_period_start` (excluding the covering record itself), `gap_days` measured as `ADMIT_DATE - period_start_dt` of the *predicting* spell (140-08-PATCH FIX-05 -- encounter-to-record staleness, not the record-to-record quantity A-06 already plots). Returns the same shape as `build_validation_cases()` so `aggregate_validation_curve()` (unchanged, from 139-02) applies directly. Two testthat fixtures added to `tests/testthat/test-115-validation-curve.R`: a 3-spell prediction/gap-value case (proving `gap_days` = 220, not 200 -- the FIX-05 distinction) and a single-spell no-prior-spell exclusion case. Task 2 (`87e980a`): SECTION 11 (after `encounter_zip` fully assembled, post-140-05) wires in `validation_curve_encounter_anchored` with a console summary; new `A_validation_curve_encounter_anchored` sheet (workbook's 9th sheet, SECTION 15) states its own 74.4%-subpopulation limitation (FIX-06) and the ZIP9-derived-not-`zip5_coalesced` ZIP5-accuracy caveat; `A_validation_curve`'s subtitle and the KEY sheet's "A-06 sampling frame" field now explicitly label the record-anchored curve a LOWER BOUND (P-05b); new KEY-sheet field "A-06 vs P-05a" explains how/why the two curves can diverge. One auto-fixed deviation (Rule 1 - stale accuracy text): SECTION 15's "8-sheet"/"8 sheets" header/console text corrected to "9-sheet"/"9 sheets" to match the new sheet count. No checkpoint in this plan (`autonomous: true`); none encountered. Structural brace/paren/bracket balance check passed after every edit (1615/1615 parens, 174/174 braces, 43/43 brackets; Rscript unavailable in this Windows environment). See `140-06-SUMMARY.md`.
  - 140-07 (Wave 6, final plan, depends on 140-06): **COMPLETE (2/2 tasks) AND COMMITTED.** Task 1 (`9460498`): SECTION 12 gains a live `n_distinct()`-based universe cross-check (`n_cohort_in_addr`/`n_cohort_not_in_addr`, `stopifnot` against `c02_result$n_cohort_absent_from_addr`) immediately after `compute_c02()` runs -- not `intersect()` + `length()`, which dedupes one side but not the other (140-08-PATCH FIX-07); confirmed structurally positioned between the SECTION 12 and SECTION 14 markers. SECTION 14 gains two new KEY sheet fields ("Part A vs Part B/C universe overlap (P-07a)", "Sentinel ZIP nulling (Pitfall 2) -- status", reviewed-and-closed per P-07b). SECTION 15 appends a universe note to all 4 Part A sheet subtitles. Task 2 (`814358f`, `tdd="true"`): `get_zip9_at_date(ids, dates, addr_full = NULL)` gains an additive test-injection seam (P-07c, 140-08-PATCH FIX-10) -- default-NULL path byte-for-byte unchanged (still touches `CONFIG$data_dir`, D-06 no-caching intact); when supplied, `addr_full` is character-coerced to match the CSV load path's column types, with the character/Date column contract documented in the roxygen block (a POSIXct column would not survive coercion into a `parse_pcornet_date()`-parseable format). `normalize_zip9()`/`normalize_zip5()`/`normalize_zip5_raw()`/`is_sentinel_zip5()` confirmed byte-for-byte unchanged via `git diff`. New `tests/testthat/test-utils-address.R` (140-08-PATCH FIX-16, dedicated file, sourced via `R/00_config.R`'s standard auto-load rather than the `.test_env`/`sys.source()` pattern R/115's own tests use) covers all three `match_type` outcomes via `addr_full` with no file I/O. One auto-fixed deviation (Rule 1 - avoided a factual contradiction): `A_validation_curve_encounter_anchored`'s universe note was corrected to state its actual cohort-restricted-encounter population instead of the plan's literal addr_coal wording, since that sheet's own pre-existing 140-06 subtitle already states its population is cohort ENCOUNTER `ADMIT_DATE`s, not `addr_coal`. No checkpoint in this plan (`autonomous: true`); none encountered. Structural brace/paren/bracket balance check passed on the diff introduced by every edit (Rscript unavailable in this Windows environment; `R/utils/utils_address.R`'s pre-existing file-wide 1-paren/1-bracket imbalance predates this plan, confirmed via `git show HEAD` before editing). **This was the final plan (8/8) in Phase 140 -- all 24 of 140-CONTEXT.md's task/decision IDs (P-01a..P-07c, D-1..D-4, plus D-5) are now addressed.** See `140-07-SUMMARY.md`.
  - **140-09-PATCH applied** (2026-08-06/07, uncommitted as of this session): FIX-17 rewires C-02 onto internally-checkable invariants; FIX-18 supersedes 140-CONTEXT.md's P-01a/b/c (withdrawn, not completed) with P-01d/P-01e and adds decision D-5; FIX-19 retro-corrects 140-01-SUMMARY.md/140-02-SUMMARY.md frontmatter (`requirements-completed` split into `requirements-met`/`requirements-deferred`/`requirements-withdrawn`, since both files claimed completion their own prose contradicts); FIX-20 retroactively documents that 140-02's Task 1 R/115 edit (SECTION 8 match-rate gate) landed in Wave 1 alongside 140-01's own R/115 edit (commit `6862a64`, verified via `git show` to contain exactly the intended hunks from both plans) -- a violation of the "no two plans share a wave on R/115" rule now stated explicitly going forward; FIX-21 adds a `test_dir()` run to 140-03 Task 3's HiPerGator checkpoint (every fixture this phase has added has never actually executed -- Rscript is unavailable in this Windows environment); FIX-22 corrects `data/reference/README.md`'s crosswalk vintage section (a single vintage cannot span LDS_ADDRESS_HISTORY's 13-year study period; offers single-vintage-with-caveat vs two-vintage-compare-spread as explicit options); FIX-23 records that D-2 (ZIP5 as analysis unit) was decided without the block-group evidence, not against it, in both 140-02-SUMMARY.md and the R/115 KEY sheet.
  - **140-09 (Wave 2a) COMPLETE, 3/3 tasks.** Task 1: `compute_c02_baseline()` added to SECTION 1B (pre-coalesce baseline, raw ZIP5 only), with a recovery-demonstrating fixture and a synthetic monotonicity-violation fixture in `tests/testthat/test-115-c02.R`. Task 2: SECTION 12's `c02_reconciled` rewired onto `c02a_monotone` (post-coalesce <= pre-coalesce) and `c02b_partition` (independent recount via `filter()`+`nrow()`, not the subtraction `compute_c02()` already performs); `C02_EXPECTED <- 26L`/`C02_TOLERANCE <- 5L` retained but removed from the gate; `n_zip5_recovered_by_coalesce` reported; SECTION 13 `qc_tbl` and SECTION 14 KEY sheet updated (superseded-constant footnote + D-2 sequencing note); the `"c02_reconciled"` `qc_tbl$Metric` string is byte-for-byte unchanged (SECTION 15 `match()` lookup intact). **Task 3 (D-5, checkpoint:decision) resolved 2026-08-07 as option-c: "no floor -- report coverage without gating on it"** -- NOT the plan's recommended option-a (0.90 floor). `c02c_coverage`/`C02_COVERAGE_FLOOR` were removed from the script entirely (clean removal, not dead code, confirmed via `grep`); `pct_cohort_with_any_address` (92.9% observed) remains computed and reported (qc_tbl row, KEY-sheet field, QC-sheet subtitle) but does not gate `c02_reconciled`. `c02_reconciled = c02a_monotone && c02b_partition` is the final gate expression. Structural brace/paren/bracket balance check passed on both modified files after every edit pass (Rscript unavailable in this Windows environment; true R-parse + `testthat::test_dir()` execution deferred to the Wave 2.5 HiPerGator run per FIX-21). See `140-09-SUMMARY.md`. **None of this session's edits (140-09-PATCH's doc fixes, the new 140-09-PLAN.md/140-09-SUMMARY.md, or the R/115 code changes) are committed to git yet -- awaiting explicit instruction to commit.**

Last activity: 2026-08-14

**2026-08-08 executor re-entry into 140-03 (first pass):** Re-confirmed (not re-executed) that 140-03's Task 1 (`d76a485`) and Task 2 (`fef75e8`) are committed and present in `R/115_zip_stability_counts.R`/`tests/testthat/test-115-c02.R` (`grep` confirms `classify_unparseable_dates_vec`, `unparseable_date_examples`, `n_patients_lost_to_filters_c02` all present). Also confirmed via `git diff --stat` that the working tree's `R/115_zip_stability_counts.R` carried 140-09's still-uncommitted rewiring (`c02_reconciled <- c02a_monotone && c02b_partition`, line ~1304) layered on top of 140-03's committed Tasks 1-2. `Rscript` confirmed still unavailable in this Windows environment. Task 3 returned to the user as a blocking `checkpoint:human-action`, unresolved at that point.

**2026-08-08 close-out (second pass, this session):** A separate agent session ran the real HiPerGator job and reported results (see 140-03-SUMMARY.md for full figures). Also discovered 140-09's C-02 gate rewiring is now committed as `9123628` (`R/115_zip_stability_counts.R`/`tests/testthat/test-115-c02.R` both clean per `git status`). Verified via `git log --oneline --all | grep` that `d76a485`, `fef75e8`, and `9123628` all exist. Created `140-03-SUMMARY.md` (self-check PASSED), marked 140-03 COMPLETE (3/3) in this file, updated `.planning/ROADMAP.md` (checkbox list: 140-03 -> `[x]`, added missing 140-09 checkbox row, `**Plans:**` line -> `4/8 plans executed`; also ran `gsd-tools roadmap update-plan-progress 140` which independently bumped plan_count to 8/summary_count to 3 before the manual checkbox edits). No code changes made -- this was a documentation/close-out pass only. Docs commit follows this update.

**2026-08-08 140-04 execution (Wave 3, Tasks 1-2):** Executed `assign_scenarios()` (P-06b, S1 folded into S3 universally since D-2 = option-a, confirmed by 140-02) and the independent backward-only waterfall (P-06a, 140-08-PATCH FIX-04's inverted-discrepancy KEY-sheet language). Both tasks committed (`45aef76`, `8bf3a87`). Task 3 (D-4, `checkpoint:decision`) is a blocking gate per `140-04-PLAN.md`'s `autonomous: false` frontmatter and this project's non-auto-advance config -- stopped and returned to the user rather than guessing the answer.

**2026-08-08 140-04 close-out (Task 3, D-4 resolved):** The team answered D-4 as **option-a** -- keep backward-only PRIMARY, forward-inclusive SENSITIVITY-only (recommended, matches 140-CONTEXT.md Section 7). Verified commits `45aef76`/`8bf3a87`/`4a8b5b8` present via `git log` before proceeding (not redone). Found and fixed one documentation gap: `C_completeness`'s subtitle (written in Task 2) pointed to a KEY sheet D-4 field that didn't exist yet; added it, recording the resolution and explicitly noting P-06d (forward-lookup extension to `R/utils/utils_address.R`, shared production utility) is NOT triggered and NOT built by this plan. Committed (`8e980e8`). Created `140-04-SUMMARY.md` (self-check PASSED). 140-04 is now COMPLETE (3/3 tasks).

**2026-08-08 140-05 execution (Wave 4, Tasks 1-2):** Executed `compute_gap_days_at_assignment()` (P-04a -- two independently-computed signed gap covariates, 140-08-PATCH FIX-01/FIX-02/FIX-08) and `median_gap_within_bin` (P-04b non-monotonicity diagnostic). Both tasks committed (`98cca02`, `c765eb9`). Deviation from the plan's literal wording: the gap-covariate computation was factored into a new SECTION 1B pure function (`compute_gap_days_at_assignment()`) rather than left as inline SECTION 11 code, because `tests/testthat/test-115-scenarios.R`'s `sys.source()`-based harness stops at SECTION 2's probe gate before SECTION 11 ever executes -- inline placement would have made the plan's required 6-case test fixture untestable against the real implementation. `s2_nearest_backward`'s existing `select()` was left unchanged (the plan's FIX-02 `period_start_dt` addition became unnecessary once the gap logic was self-contained in its own function, computing its own internal backward reduction instead). Task 3 (D-3, `checkpoint:decision`, `gate="blocking"`) is a blocking gate per `140-05-PLAN.md`'s `autonomous: false` frontmatter and this project's non-auto-advance config (`workflow.auto_advance: false`) -- stopped and returned to the user rather than guessing the answer.

**2026-08-10 140-05 close-out (Task 3, D-3 resolved):** The team answered D-3 as **option-a** -- ZIP5 carried forward WITHOUT a hard time-window cap, using `gap_days_at_assignment`/`gap_days_at_assignment_backward_only` (Task 1) as analytic covariates/sensitivity variables instead of a discard rule; option-b (a cap at a team-specified N days) was NOT selected. Verified commits `98cca02`/`c765eb9`/`a6df1e1` present via `git log` before proceeding (not redone). No code-logic changes were required -- Task 1 already implements the uncapped design exactly as the decision specifies. Added one new KEY sheet field ("Uncapped carry-forward design (D-3, resolved 2026-08-10)") recording the resolution, the gap-distribution rationale, and the explicit non-selection of option-b. Committed (`51e2064`). Structural brace/paren/bracket balance check passed after the edit (1542/1542 parens, 169/169 braces, 43/43 brackets; Rscript unavailable in this Windows environment). Created `140-05-SUMMARY.md` (self-check PASSED). 140-05 is now COMPLETE (3/3 tasks).

**2026-08-10 140-06 execution (Wave 5, Tasks 1-2, autonomous):** Executed `build_encounter_anchored_validation_cases()` (P-05a, 140-08-PATCH FIX-05) and the encounter-anchored sheet + lower-bound relabeling wiring (P-05b, 140-08-PATCH FIX-06). Both tasks committed (`176cf84`, `87e980a`). Task 1: predicts from the `zip9_seq` spell just prior to the covering (`has_direct_zip9`) record's own `direct_zip_period_start`, `gap_days = ADMIT_DATE - predicting_period_start_dt` (the FIX-05 encounter-to-record staleness quantity, distinct from A-06's record-to-record gap); two testthat fixtures added confirming the gap-value distinction (220 vs. the wrong 200) and the no-prior-spell exclusion. Task 2: wired into SECTION 11 (after `encounter_zip` fully assembled) producing `validation_curve_encounter_anchored`; new 9th workbook sheet `A_validation_curve_encounter_anchored` states its own 74.4%-subpopulation limitation and the ZIP9-derived-not-`zip5_coalesced` ZIP5-accuracy caveat; `A_validation_curve`'s subtitle and KEY sheet's "A-06 sampling frame" field now explicitly say LOWER BOUND; new KEY field "A-06 vs P-05a" added. One auto-fixed deviation (Rule 1): SECTION 15's stale "8-sheet"/"8 sheets" text corrected to "9-sheet"/"9 sheets". No checkpoint task existed in this plan (`autonomous: true` per frontmatter) and none was encountered -- both tasks executed straight through to completion. `Rscript` confirmed still unavailable in this Windows environment (re-checked via `which Rscript`); structural brace/paren/bracket balance check passed after every edit (1615/1615 parens, 174/174 braces, 43/43 brackets). Created `140-06-SUMMARY.md` (self-check PASSED). 140-06 is now COMPLETE (2/2 tasks).

**2026-08-10 140-07 execution (Wave 6, Tasks 1-2, autonomous, final plan of Phase 140):** Executed the universe cross-check relocation + Part A/KEY documentation (P-07a, P-07b, 140-08-PATCH FIX-07) and the `get_zip9_at_date()` `addr_full` test-injection seam (P-07c, 140-08-PATCH FIX-10). Both tasks committed (`9460498`, `814358f`). Task 1: `n_cohort_in_addr`/`n_cohort_not_in_addr` computed and asserted in SECTION 12 immediately after `compute_c02()`, using `n_distinct()` on both sides; KEY sheet and all 4 Part A sheet subtitles carry an explicit universe note. Task 2: additive `addr_full = NULL` parameter, default path unchanged, `normalize_*()`/`is_sentinel_zip5()` confirmed untouched via `git diff`; new `tests/testthat/test-utils-address.R` (4th Phase-140 test file, per 140-08-PATCH FIX-16). One auto-fixed Rule-1 deviation: corrected `A_validation_curve_encounter_anchored`'s universe note to state its true cohort-restricted-encounter population rather than the plan's literal (factually incorrect for this one sheet) addr_coal wording -- documented in `140-07-SUMMARY.md`. No checkpoint existed in this plan (`autonomous: true`) and none was encountered. `Rscript` confirmed still unavailable in this Windows environment; structural brace/paren/bracket balance check passed on the diff introduced by every edit. Created `140-07-SUMMARY.md` (self-check PASSED). **140-07 is now COMPLETE (2/2 tasks) -- Phase 140 is 8/8 plans executed.** Per this session's explicit scope instruction, phase-level goal verification, marking Phase 140 complete, and advancing to a new phase/milestone were NOT attempted -- that remains the orchestrator's next step.

Phase 139 (zip-stability-imputation-occurrence-counts) remains complete (4/4 plans), ready for goal verification (unchanged by this session).
Phase 138 (resolve-log2-txt-problems) remains complete, ready for verification (unchanged by this session).

Progress: [██░░░░░░░░] 21% (1/5 v3.4 phases complete); Phase 139: 4/4 plans complete (phase complete); Phase 140: 8/8 plans complete and VERIFIED (140-01, 140-02, 140-09, 140-03, 140-04, 140-05, 140-06, 140-07 all done) -- goal verification human_needed, user-approved 2026-08-10; real HiPerGator confirmation of Waves 3-6 + xlsx re-issue tracked as follow-up

## v3.3 Status (open in parallel, not abandoned)

v3.3 (Rituximab/Methotrexate-Associated Diagnoses of Interest) is fully executed (12/12 plans, Phases 127-131) but NOT yet shipped — its remaining TODOs are tracked below under "v3.3 Active TODOs" and stay open alongside v3.4 per explicit user decision (2026-07-23). Run `/gsd:complete-milestone` for v3.3 once its TODOs are cleared.

## Performance Metrics

**Milestone velocity:**

- v3.2: 23 phases (104-126) completed (2026-06-15 to 2026-07-15)
- v3.1: 4 phases (100-103) completed in 1 day (2026-06-12)
- v3.0: 5 phases (95-99) completed in 3 days

**Planning efficiency:**

- Average plans per phase: 1.0 (recent milestones)
- Average tasks per plan: 3.0

**Recent plan executions:**

- Phase 139 Plan 01 (ZIP stability foundation): 25 min, 3 tasks, 2 files
- Phase 139 Plan 02 (A-06 carry-forward validation curve): 15 min, 3 tasks (2 auto + 1 checkpoint), 2 files
- Phase 139 Plan 03 (Part B: encounter ZIP classification + S1-S4 scenario counts): 20 min, 2 tasks, 2 files
- Phase 139 Plan 04 (Part C waterfall + C-02 reconciliation + full 8-sheet xlsx assembly + registration): 35 min, 2 tasks, 5 files

## Accumulated Context

### v3.3 Roadmap Decisions

- Phase numbering continues from Phase 126 (v3.2 last phase) — v3.3 starts at Phase 127
- Granularity: coarse (4 phases for 19 requirements, following research's recommended 4-phase decomposition)
- Phase 127 (Code-Set & Infrastructure) is a strict prerequisite for all others: DOI_CODE_MAP + utils_doi.R must exist before any classification code is written
- Phase 128 (Classification) is a strict prerequisite for Phase 129 (Attribution): doi_encounters.rds must exist before attribution joins
- Phase 130 (Registration/Smoke Test) can begin once Phase 129's R/111 structure is stable; its HiPerGator runtime gate is the v3.3 definition-of-done
- DOI_ATTRIBUTION_WINDOW_DAYS = 90L (one clinical quarter) — wider than cancer cascade's ±30 days because RA/psoriasis/IBD indication timelines span months, not weeks
- Three-state likely_non_lymphoma_directed flag: NA must not be collapsed to FALSE (would undercount the clinically interesting ambiguous HL+DoI co-occurrence cases)
- DuckDB-native prefix filter mandatory: never load full DIAGNOSIS table into R (OOM risk on HiPerGator)
- I77.82 excluded (seed error — "Dissection of artery"), D47.Z2 excluded (CANCER_SITE_MAP conflict)
- utils_doi.R as new file (not extension of utils_cancer.R): classify_codes() has 10+ consumers expecting cancer-site output; merging would silently corrupt them

### Quick Task Log

- [260715]: R/106 Section 9 (Sheet 3 "Time Between Changes") gap-day computation now bounds ADDRESS_PERIOD_START to the LDS_ADDRESS_HISTORY study period (ZIP_STUDY_PERIOD_MIN 2012-01-01 / ZIP_STUDY_PERIOD_MAX 2025-03-31) before computing gaps, with a logged out-of-range drop count; R/88 Section 15s extended to 16 checks (2 new) to verify. See [260715-for-change-in-zip-make-sure-addresses-ar](./quick/260715-for-change-in-zip-make-sure-addresses-ar/)
- [260716]: Added `ICD_CODES$nhl_histology` (34-code, unverified/needs-clinical-review) to R/00_config.R and new `R/113_confirmed_hl_nhl_tumor_registry_counts.R` — a console-only diagnostic printing confirmed-HL, confirmed-NHL, and confirmed-BOTH (overlap) distinct-patient counts from TUMOR_REGISTRY histology codes only (no DIAGNOSIS table query). Also fixed a pre-existing gap (since Phase 119): created the missing `tests/fixtures/DEATH_CAUSE_Mailhot_V1.csv` fixture, unblocking any local `R/01_load_pcornet.R` run. See [260716-add-script-to-count-confirmed-hl-and-nhl](./quick/260716-add-script-to-count-confirmed-hl-and-nhl/)
- [260725]: Created `run_investigations.sh` — SLURM batch script to run R/39_run_all_investigations.R with dual timestamped log (SLURM %j log + named output/logs/investigations_YYYYMMDD_HHMMSS.log)
- [260814-gwb]: Applied 142-PATCH.md corrections to CONTEXT.md, 142-01-PLAN.md, 142-02-PLAN.md, and created 142-DISCOVERY.md — all P0 blockers, P1 should-fix items, and P2 minor items addressed. See [260814-gwb-apply-142-patch-md-corrections-to-phase-](./quick/260814-gwb-apply-142-patch-md-corrections-to-phase-/)

### Roadmap Evolution

- Phase 131 added: Update all_codes_resolved.xlsx to include MED_ADMIN NDC-resolved codes and a normalized drug-name column
- Phase 138 added: resolve log2.txt problems
- Phase 139 added: ZIP Stability & Imputation Occurrence Counts (source doc: 140-CONTEXT.md, self-titled "Phase 140")
- Phase 140 added: Resolve C-02 reconciliation gate and finalize ZIP assignment design (ZIP5 unit, uncapped carry-forward, backward-only primary spec) for zip_stability_counts workbook (source doc: planzip.md — follow-up plan after Phase 139's C-02 gate failed)
- Phase 140 plans amended: 140-08-PATCH.md (16 fixes, FIX-01..FIX-16) applied to 140-01/03 (test-file split) and 140-02/04/05/06/07 (dual gap-day columns, D-2-conditional S1 fold, inverted KEY-sheet caveat, encounter-anchored gap formula, n_distinct() cross-check relocation, crosswalk vintage/match-rate gate, unparseable-date example table, HiPerGator-run checkpoint inserted between Wave 2 and Wave 3, D-1-option-b contingency notes, test-file split across 4 files) before any plan was executed — re-verified via gsd-plan-checker, all 24 CONTEXT.md IDs still covered
- Phase 141 added: CONTEXT-zip9-imputation
- Phase 142 added: 180-Day Treatment Episodes + Drug-Name Deduplication
- Phase 143 added: 180-Day File: Enrichment Parity and Review Follow-ups

### Known Blockers (new)

- Phase 139's CONTEXT.md (`140-CONTEXT.md`) was authored assuming a prior "Phase 139: zip9-approximation" already shipped `approximate_zip9()`, `is_sentinel_zip5()`, and an "AMEND-01" ZIP5-coalesce fix, and references `.planning/phases/139-zip9-approximation/` as a canonical ref. None of that exists — no roadmap entry, directory, commit, or function by that name; only Phase 137 (`get_zip9_at_date()`) is real. RESOLVED by 139-01: `is_sentinel_zip5()` was built directly in `R/utils/utils_address.R` and the "AMEND-01" ZIP5-coalescing fix was implemented locally in `R/115_zip_stability_counts.R` via a single `coalesce_zip5()` function (139-05-PATCH's requirement); `approximate_zip9()` was determined to be unneeded since Part B/C only counts imputation-scenario occurrences, it does not write imputed values.

### Phase 140 Decisions

- [Phase 140-02]: D-2 resolved as **option-a** -- ZIP5 accepted as the primary analysis unit now, not deferred pending the block-group crosswalk re-run. Based on 140-CONTEXT.md's real accuracy-by-gap-bin evidence (ZIP5 exact-match 61.8% overall vs. ZIP9 exact-match 37.1%; 39.3% of ZIP9 transitions are +4-only). The block-group crosswalk re-run stays confirmatory, not gating, per 140-CONTEXT.md's own design. This decision unblocks Plans 140-04 through 140-07 and specifically confirms 140-04's S1-fold-in design (140-08-PATCH FIX-03) is applicable.
- [Phase 140-02]: P-03a (Neighborhood Atlas crosswalk acquisition) explicitly deferred -- "not available." Block-group tier remains gracefully degraded (`block_group_tier_status = "not available (crosswalk file not found)"`), unchanged from Phase 139's shipped behavior, not a regression. P-03b (A-06 re-run with block-group tier populated) and full P-03c confirmation are follow-up work outside Phase 140's plan scope. The staging contract (path/columns/vintage/90%-match-rate threshold) is fully documented in `data/reference/README.md` so staging the file later requires zero further code changes to R/115.

### Phase 139 Decisions

- [Phase 139-01]: `is_sentinel_zip5()` added as a new sibling function in `R/utils/utils_address.R` (00000/99999/any single-repeated-digit ZIP5 flagged as sentinel, NA passed through as NA); `get_zip9_at_date()` left byte-for-byte unchanged (Out of Scope for this phase)
- [Phase 139-01]: ZIP5 coalescing implemented as exactly one function, `coalesce_zip5()`, defined in `R/115_zip_stability_counts.R`'s new SECTION 1B prelude (before the probe gate, so a future testthat file can `sys.source()` pure functions even when the source CSV is absent) — not duplicated inline, not added to `utils_address.R`
- [Phase 139-01]: `R/115` stops loudly (prints available columns, quits) if raw `ADDRESS_ZIP5` is absent, rather than silently degrading to derived-from-ZIP9-only (139-05-PATCH FIX-04a)
- [Phase 139-01]: `period_end_dt` keeps NA for open-ended address records; a separate `period_end_eff` column (sentineled to 9999-12-31) is used only for interval-matching by downstream consumers, never for exposure-span math (139-05-PATCH FIX-02)
- [Phase 139-01]: Per-patient exposure-denominator math (A-04) uses `period_end_eff` capped at `DATA_THROUGH` (alias for `ZIP_STUDY_PERIOD_MAX`), not the far-future sentinel directly and not `period_start_dt` alone, with single-record patients getting `NA_real_` (not `Inf`) for the per-patient-year rate (139-05-PATCH FIX-04b)
- [Phase 139-01]: `gap_days_summary` (A-05) includes deciles (`quantile(gaps_days, seq(0.1,0.9,0.1))`) in addition to median/p25/p75/min/max/histogram, satisfying A-05's explicit "median, IQR, deciles" requirement
- [Phase 139-02]: A-06's hold-out validation is record-anchored (F1, per 139-05-PATCH FIX-01), not spell-anchored: `build_validation_cases()` operates on `addr_coal` directly, holding out one address RECORD and predicting it from the most recent PRIOR record with a non-NA ZIP9 -- the pre-patch design operated on `zip9_seq` spells, which removed every unchanged-address case by construction and produced a silent 0.0% exact-match reading in every bin
- [Phase 139-02]: `tests/testthat/test-115-validation-curve.R`'s stable-patient fixture asserts `pct_exact_zip9_match == 100` as a computed-value check (139-05-PATCH FIX-05) -- this is the specific assertion that would have caught the pre-patch defect; confirmed non-structural (not a grep for a column name) during the Task 3 checkpoint close-out
- [Phase 139-02]: gap-bin scheme separates "0 (same-day)" from "0-30" so same-day version-correction record pairs don't inflate the shortest elapsed-time bin's accuracy; `n_excluded_no_prior` (single-record patients) is counted and logged, never silently dropped
- [Phase 139-02]: Block-group accuracy tier is attempted only if `data/reference/neighborhood_atlas_block_group_crosswalk.csv` is found (confirmed absent in this repo); degrades to a documented `NA_real_` column with an explicit console message, not an error and not silent omission
- [Phase 139-02]: Task 3 human-verify checkpoint approved with the automated-verification portion satisfied (0 failures / 13 expectations, stable-patient assertion confirmed real) and the plan's own step 3 (reviewing the real `validation_curve` table's Overall row on an actual HiPerGator run) explicitly deferred to a future session, per the plan text's own scoping ("out of scope for this planning session")
- [Phase 139-03]: `classify_encounter_zip()` interval-matches against `period_end_eff` (sentineled), never `period_end_dt` (NA-preserving) -- an encounter covered by an open-ended (current) address record now classifies `has_direct_zip9`, not `has_neither` (139-05-PATCH FIX-02); replicates `get_zip9_at_date()`'s own tie-break priority locally rather than calling it, since Part B needs the coalesced ZIP5 visible alongside ZIP9
- [Phase 139-03]: `has_neither` split into `has_no_record` (no covering record exists) and `has_record_but_empty` (a covering record exists with both ZIP9 and ZIP5 NA), both reported, `has_neither` kept as their union (139-05-PATCH FIX-04e); `semi_join` pre-filter against the cohort-restricted encounters applied unconditionally, not gated on profiling (139-05-PATCH FIX-04c)
- [Phase 139-03]: Part B/C's population is the study cohort via `get_hl_patient_ids()` (139-05-PATCH FIX-03a, reusing the R/106/R/107/R/109/R/111 pattern verbatim) -- both the ENCOUNTER pull and `classify_encounter_zip()`'s address view are cohort-restricted, with cohort N logged, making Plan 04's C-02 26-patient reconciliation comparable
- [Phase 139-03]: S1/S2 eligibility (backward/forward/either) computed via new local join logic against `zip9_seq`/`zip5_seq`; S2 uses `zip5_seq` (not `zip9_seq`) since it covers both the take-ZIP9 and centroid-only resolution paths, with a `resolution_path` column for reporting transparency only (not gating eligibility); S3 stays backward-only (complement of S1-eligible-backward within the `has_direct_zip5_only` set)
- [Phase 139-03]: `scenario_assigned` applies the ordered S1-then-S2-then-S3 `case_when()` rule; unordered per-scenario eligibility flags are ALSO computed and reported so overlap hidden by the ordered rule stays visible -- an encounter eligible only via S1-forward is assigned S1 (ordered) while remaining S3-eligible (unordered), explicitly called out via a console NOTE (139-05-PATCH FIX-04d); S3 headline flagged "S3 resolution still pending as of 08/04 notes," never presented as decided (B-01); every count reported at both encounter and patient level with denominators named (B-02)
- [Phase 139-04]: `compute_c02(cohort_ids, addr_coal)` (SECTION 1B) is computed over `COHORT_IDS` (the study cohort), NOT `addr_coal`'s own patient set -- a cohort patient entirely absent from `addr_coal` (dropped by Plan 01's filters, or never in `LDS_ADDRESS_HISTORY` at all) is `coalesce()`d to `has_any_zip5 = FALSE` and counts toward `n_patients_no_zip5_ever`, fixing the pre-patch defect where such patients were invisible to `group_by()` (139-05-PATCH FIX-03b)
- [Phase 139-04]: A pre-filter comparison (`n_patients_no_zip5_ever_prefilter`, via `coalesce_zip5(addr_raw)` -- no second coalescing implementation) is computed and reported alongside the post-filter C-02 figure so study-period/unparseable-date filter-driven denominator drift is visible (139-05-PATCH FIX-03c); the C-02 tolerance (+/-5) is documented in the KEY sheet as covering cohort-definition drift only, not denominator or methodological uncertainty (FIX-03d)
- [Phase 139-04]: `waterfall_encounter`/`waterfall_patient` (C-01) are cumulative stepwise tables built directly from Part B's ordered `scenario_assigned` column; patient-level bucket is the furthest-resolved scenario per patient (one mutually-exclusive bucket, `group_by(ID)` + `slice_min` on priority rank), distinct from Part B's own patient-presence reporting
- [Phase 139-04]: A C-02 reconciliation failure produces both a loud console message AND a UF_ORANGE-filled QC-sheet cell in the xlsx itself -- the deliverable that ships to Erin/Amy carries the warning, not just build-time console output
- [Phase 139-04]: Full 8-sheet `zip_stability_counts_YYYYMMDD.xlsx` assembled via `wb_workbook()` (KEY first, then A/B/C sheets, then QC) with locally-copied `add_styled_sheet()`/UF_BLUE (`#0021A5`)/UF_ORANGE (`#FA4616`) constants (distinct from `utils_pptx.R`'s own UF_BLUE); `A_stability_summary` carries Plan 01's `gap_days_summary` through in full, including `gap_days_deciles`, not just the fixed-bucket histogram
- [Phase 139-04]: `R/115` registered as the final `investigation_scripts` entry in `R/39` (not `expected_xlsx`, per R/106's own dated-filename precedent); `R/88` Section 15ad (14 structural checks, `SMOKE-139-01` footer) validates single-implementation checks for `is_sentinel_zip5()`/`coalesce_zip5()` plus Part B/C/C-02 presence; `R/SCRIPT_INDEX.md` updated (16 post-renumber investigations, 102 total scripts)
- [Phase 139-04]: Two pre-existing `R/88` failures (`source_coverage_analysis.csv` existence, Phase 122 `get_chemo_hits('MED_ADMIN')` fixture) are unrelated to Phase 139 (confirmed via an additive-only diff to `R/88` this session) and were logged to `deferred-items.md` rather than fixed, per the scope-boundary rule; Section 15ad itself (this plan's own checks) shows 14 PASS / 0 FAIL
- [Phase 139 COMPLETE]: All 4 plans (01-04) executed; Phase 139 is ready for goal verification. Runtime verification against real `LDS_ADDRESS_HISTORY`/`ENCOUNTER` data on HiPerGator (producing the actual `output/zip_stability_counts_YYYYMMDD.xlsx` and reviewing the real C-02 reconciliation result) remains an explicitly-deferred open item requiring HiPerGator access -- this is the phase's own stated definition-of-done gate, not a blocker to this phase's planning/execution completion

### Phase 140 Decisions

- [Phase 140-01]: `compute_c02()` (SECTION 1B) gains `n_present_no_usable_zip5 = n_patients_no_zip5_ever - n_cohort_absent_from_addr`, computed arithmetically from its two existing return values (no second `group_by()`/`summarise()`), reproducing the 656-vs-9 breakdown from the script itself rather than only asserting it in planning docs
- [Phase 140-01]: D-1 (the 26-patient C-02 control total's denominator) was resolved by the user in-session on 2026-08-06 as a THIRD, distinct resolution type -- neither the plan's anticipated option-a (team confirms a narrower historical denominator; re-derive `C02_EXPECTED` with a cited source) nor option-b (cannot confirm; leave `C02_EXPECTED`/`C02_TOLERANCE` unchanged with an unresolved-status note only). Instead: `C02_EXPECTED` (26L) and `C02_TOLERANCE` (5L) stay exactly unchanged, AND the reconciliation comparison itself is corrected -- `c02_reconciled` now compares `n_present_no_usable_zip5` (9, the only population a genuine ZIP5-coalescing defect could actually produce) against `C02_EXPECTED`, instead of the conflated `n_patients_no_zip5_ever` (665, which mixes in 656 cohort patients who are entirely absent from `addr_coal` -- a coverage question, not a coalescing defect). This is a comparison-BASIS correction, not a re-derivation of the historical control total; the original 26's exact provenance/denominator remains UNCONFIRMED and an OPEN ITEM. The gate is EXPECTED to continue reading FAIL (9 vs. 26±5 = [21,31]) for this legitimate, non-circular reason -- the workbook's shippability stays explicitly BLOCKED on C-02 pending further team (Erin/Amy) follow-up on the original 26's provenance. Documented in SECTION 12 console messages, SECTION 13 `qc_tbl`, two new SECTION 14 KEY-sheet fields, and the QC-sheet subtitle. See `140-01-SUMMARY.md` for full rationale.
- [Phase 140-01]: Per 140-08-PATCH FIX-16, C-02 fixtures live in `tests/testthat/test-115-c02.R` (new sibling file to `test-115-validation-curve.R`), not the shared validation-curve test file -- carries the moved FIX-03 `compute_c02()` fixture plus the new 4-patient present-vs-absent breakdown fixture
- [Phase 140-02]: D-2 (ZIP5-vs-ZIP9 primary analysis unit) resolved option-a -- ZIP5 accepted as the primary analysis unit now, not deferred pending the block-group crosswalk re-run; unblocks Plans 140-04 through 140-07, confirms 140-04's S1-fold-in design (140-08-PATCH FIX-03) is applicable
- [Phase 140-02]: P-03a (Neighborhood Atlas block-group crosswalk acquisition) explicitly deferred -- "not available." Block-group tier remains gracefully degraded (same behavior Phase 139 already shipped, not a regression); P-03b/P-03c remain open follow-up work outside Phase 140's scope
- [Phase 140 Wave 1 COMPLETE]: Both 140-01 and 140-02 fully executed (3/3 tasks each). Wave 2 (140-03, depends on 140-01) is next.
- [Phase 140-09, Wave 2a, inserted by 140-09-PATCH]: D-1's comparison-basis fix (140-01) was itself superseded -- `c02_reconciled` now gates on two internally-checkable invariants (`c02a_monotone`: post-coalesce <= pre-coalesce via new `compute_c02_baseline()`; `c02b_partition`: independent recount matches `compute_c02()`) instead of comparing against the unverifiable 26-patient `C02_EXPECTED` control total, which is retained/reported only as a superseded historical footnote. D-5 (whether to also gate on `pct_cohort_with_any_address`) resolved **option-c, "no floor -- report only"** -- NOT the plan's recommended 0.90-floor option-a; coverage is a named, non-gating QC/KEY-sheet figure.
- [Phase 140-03]: P-02b (whether any unparseable-date failure-mode category is recoverable via extending `parse_pcornet_date()`) resolved 2026-08-08 from a real HiPerGator run: **"genuinely unparseable"** -- 100% of the 4,912 unparseable `period_start_dt` records are `blank_or_null` (zero in `numeric_looking_but_invalid`/`text_looking_but_invalid`/`other_unrecognized`). No parser extension is warranted.
- [Phase 140-03 / Wave 2.5 gate, 140-08-PATCH FIX-14]: Resolved 2026-08-08. Real HiPerGator run confirms `c02a_monotone`/`c02b_partition`/`c02_reconciled` all PASS, `testthat::test_dir()` passes 35/35, and every assumed figure (656 vs 9; 92.9%; 4,912; 280; 385/665) matches with zero material divergence. Non-blocking notable finding: the pre-coalesce baseline (`n_present_no_usable_zip5_precoalesce`) computed to exactly 26, matching the retired historical `C02_EXPECTED` -- supports the hypothesis that the original "26" was a pre-coalesce figure; flagged to Erin/Amy for provenance follow-up, not gate-relevant. Wave 3 (140-04) is unblocked.
- [Phase 140-04]: D-4 (whether the forward-inclusive waterfall becomes the PRIMARY specification reported to Erin/Amy) resolved 2026-08-08 as **option-a** -- backward-only stays PRIMARY (already_has_zip9 + S2-backward + S3), forward-inclusive stays SENSITIVITY-only. Matches 140-CONTEXT.md Section 7's own recommendation. Option-b (forward-inclusive as primary) was rejected; it would have triggered P-06d (extending `R/utils/utils_address.R`, a shared production utility, with a forward-lookup variant) as a distinct out-of-scope follow-up plan. P-06d is explicitly NOT built by 140-04 -- `utils_address.R` remains untouched.
- [Phase 140-05]: D-3 (whether ZIP5 is carried forward WITHOUT a hard time-window cap, using `gap_days_at_assignment`/`gap_days_at_assignment_backward_only` as analytic covariates/sensitivity variables instead of a discard rule) resolved 2026-08-10 as **option-a** -- accept uncapped carry-forward with gap-days as covariate. Matches 140-CONTEXT.md Section 5's own recommendation (median gap 547 days; ZIP5 accuracy flattens 52-57% past 90 days with no decay cliff justifying a cutoff). Task 1 of 140-05 already implemented this design, so the decision required zero code-logic changes -- only a new KEY sheet field recording the resolution. Option-b (a cap at a team-specified N days) was rejected/not built; it would have required a follow-up plan specifying N, which is moot since it was not selected.
- [Phase 140-06]: No new decision -- P-05a/P-05b were fully specified by 140-CONTEXT.md Section 6 and 140-08-PATCH FIX-05/FIX-06 (exact gap-day formula, 74.4%-subpopulation limitation, ZIP9-derived-not-zip5_coalesced ZIP5 accuracy) before this plan executed; both tasks were literal implementations with no checkpoint task in the plan (`autonomous: true`) and none encountered during execution.
- [Phase 140-07]: No new decision requiring a checkpoint -- P-07a/P-07b/P-07c were fully specified by 140-CONTEXT.md Section 8 and 140-08-PATCH FIX-07/FIX-10/FIX-16 before this plan executed. One auto-fixed (Rule 1) documentation deviation: `A_validation_curve_encounter_anchored`'s universe note states its actual cohort-restricted-encounter population rather than the plan's literal addr_coal wording, which would have contradicted that sheet's own pre-existing 140-06 subtitle text. See `140-07-SUMMARY.md`.
- [Phase 140 ALL WAVES COMPLETE]: 140-01, 140-02, 140-09, 140-03, 140-04, 140-05, 140-06, and 140-07 all fully executed and committed (8/8 plans). All 24 of 140-CONTEXT.md's task/decision IDs (P-01a..P-07c, D-1..D-4, plus D-5 added by 140-09-PATCH) are addressed. Phase-level goal verification (a real HiPerGator run of the full amended `R/115_zip_stability_counts.R`, confirming `c02_reconciled` plus the backward-only/forward-inclusive waterfall split, the encounter-anchored validation curve, the `gap_days_at_assignment` covariate, and this plan's new universe-overlap figures against real data) remains the outstanding item before the workbook ships to Erin/Amy -- not attempted this session, per explicit scope instruction that phase-level verification/completion is the orchestrator's responsibility.

### Phase 131 Decisions

- [Phase 131-01]: Reused Phase 120's R/105 col-G Supportive Care output in MEDICATION_LOOKUP (per-sheet `med_col` selector: col 7 when present, col 3 fallback) instead of re-deriving Supportive Care names via the new fallback normalizer
- [Phase 131-01]: Copied (not imported) R/105's `rule_based_ingredient()` strip logic and word lists into `fallback_normalize_medication()`, since R/105 is a one-time reference-Excel enrichment script, not a shared utility module
- [Phase 131-01]: `fallback_normalize_medication()`'s HCPCS and RxNorm-STR branches both route through `canonicalize_drug_name()` so fallback output stays consistent with MEDICATION_LOOKUP's brand->generic collapsing
- [Phase 131-02]: `get_chemo_hits()` gained an additive `return_source` param (default FALSE) tagging PRESCRIBING / MED_ADMIN (RX) / MED_ADMIN (NDC) / DISPENSING (NDC) rows; all 6 existing callers unaffected since none pass it
- [Phase 131-02]: R/50's RXNORM loop now queries PRESCRIBING + MED_ADMIN (RX+ND) + DISPENSING generically for all 4 RXNORM vectors (`filter(code_type == "RXNORM")`), with Records/Patients de-duplicated on `(ID, treatment_date, code)` — existing Records counts for multi-row-per-day codes will drop vs. prior `all_codes_resolved.xlsx` runs (intended Pitfall-2 fix, not a regression; flag to collaborators on next regeneration)
- [Phase 131-03]: Added `all_codes_df$medication` column (Section 4) gated by category/code_type, and a shared `resolved_xlsx_layout(category)` helper consumed by both `write_resolved_xlsx()` and the combined-workbook per-category loop so the 5 per-type xlsx files and `all_codes_resolved.xlsx` can never diverge on Medication column layout/values; Radiation sheets keep their original unchanged 6-column shape (no Medication column at all)
- [Phase 131-04]: Added R/88 Section 15x (12 structural checks) + SMOKE-131-01 summary line validating every 131-01/02/03 artifact; all illustrative grep patterns from the plan matched the actual source text verbatim, no pattern adjustments needed

### v3.3 Active TODOs (open in parallel with v3.4 — see "v3.3 Status" above)

- [ ] Plan Phase 127 (Code-Set and Infrastructure Centralization) — NOTE: PROJECT.md's Validated list already shows Phase 127 shipped (DOI_CODE_MAP + utils_doi.R); this line may be stale, confirm before acting on it
- [ ] Verify Phase 131 end-to-end on HiPerGator (regenerate all_codes_resolved.xlsx, run R/88 with real R packages, confirm Section 15x checks pass and Medication column populates as expected)
- [ ] Add a Phase 131 section to `.planning/REQUIREMENTS.md` so MEDXLSX-01..07/SMOKE-131-01 can be checked off via `gsd-tools requirements mark-complete`

### v3.4 Roadmap Decisions

- Phase numbering continues from Phase 131 (v3.3's last used phase number, even though v3.3 is not yet archived to MILESTONES.md) — v3.4 starts at Phase 132
- Scope locked to: 8 critical/high findings + 8 cross-cutting patterns (A-H) + 2 loose-end confirmations from `R_pipeline_code_review.md`; ~80 per-script Low/Med findings explicitly deferred to backlog (not this milestone)
- Research explicitly skipped (config.json `workflow.research: false`) — this is remediation of known, already-diagnosed defects from the code review, not new-capability discovery; `.planning/research/SUMMARY.md` (if present) is stale content from the unrelated v3.3 DoI milestone and was not consulted
- 5 phases (132-136), one per stage of the review's own "Suggested fix order" section — used as the primary phase-sequencing signal per REQUIREMENTS.md's locked scope, rather than inventing a different grouping:
  - Phase 132 Crash Fixes = stage 1 (CRASH-01, CRASH-02)
  - Phase 133 Critical Correctness Fixes = stage 2 (DATA-01..07 + DOCS-01 — DOCS-01/finding#7 and DATA-07/finding#8 folded in here since the review's critical/high findings table lists them adjacent to the other "wrong output" findings without their own stage, and coarse granularity disfavors single-requirement phases)
  - Phase 134 Ingest Integrity and Honest Tests = stage 3 (INGEST-01, PATTERN-E)
  - Phase 135 Shared-Helper Standardization = stage 4 (PATTERN-A/B/C/D/F/G/H — PATTERN-A's R/46 instance stays with DATA-02 in Phase 133 per the requirement's own note; PATTERN-F/G/H aren't literally named in the review's stage-4 prose but are the remaining in-scope cross-cutting patterns and share the "standardize once at the shared-helper layer" character)
  - Phase 136 Confirm Loose Ends = stage 5 (CONFIRM-01, CONFIRM-02)
- All 21 v3.4 requirements mapped 1:1 to exactly one phase — no orphans, no duplicates (see REQUIREMENTS.md Traceability table)
- Phase dependencies are sequential (132→133→134→135→136), mirroring the review's recommended fix order rather than a hard technical prerequisite chain — the underlying scripts touched by each phase mostly don't overlap, so phases could technically be reordered, but the review's stated rationale (crashers first, honest tests before relying on them, shared-helper fixes last so downstream per-script fixes aren't re-touched by the standardization pass) is preserved as the default execution order

### Known Blockers

- Phase 131 requirement IDs (`MEDXLSX-01..05`) are referenced in 131-01/131-02 PLAN frontmatter but are not defined in `.planning/REQUIREMENTS.md` (no Phase 131 section exists there yet). `gsd-tools requirements mark-complete` cannot find/check them off. Not blocking execution, but the traceability table needs a Phase 131 section added before this can be closed out cleanly.
- `node gsd-tools.cjs state update-progress` and `state advance-plan` corrupt this project's non-standard STATE.md format (confirmed during 139-01 execution and again during 139-02 close-out) — apply STATE.md position/progress/decision updates manually via direct file edits instead of via that CLI command. `state record-metric`/`add-decision`/`record-session` were not invoked either, for the same reason; all STATE.md changes for Phase 139 plans are hand-authored.
- A-06's `validation_curve` table has only been verified against synthetic testthat fixtures (0 failures / 13 expectations) in this Windows planning environment; verifying its actual computed output (the real `pct_exact_zip9_match` Overall-row value) against real `LDS_ADDRESS_HISTORY` data requires a HiPerGator run, deferred per the plan's own scoping — not a blocker to Plans 03/04, which do not depend on A-06's real-data output.

## Session Continuity

**Last command:** execute 143-02 Task 1 -- COMPLETE. R/28 parameterised with getOption()-based EPISODES_RDS_PATH/DETAIL_RDS_PATH/OUT_SUFFIX; overwrite guard added; R/39 p143 option reset + withr::with_options 180-day wiring block added. Commit: e8896cf. Task 2 (HiPerGator: 90-day all.equal() + 180-day fill-rate check) is pending and requires HiPerGator access.
**Stopped at:**
143-02 Task 1 complete — awaiting Task 2 HiPerGator verification

- 140-02-PLAN.md COMPLETE (3/3 tasks). D-2 resolved option-a (ZIP5 as primary analysis unit). See `140-02-SUMMARY.md`.
- 140-09-PLAN.md (Wave 2a, inserted by 140-09-PATCH) COMPLETE (3/3 tasks) AND COMMITTED (`9123628`, 2026-08-08). `c02_reconciled` rewired onto `c02a_monotone && c02b_partition` (retiring the unverifiable 26-patient control total from the gate); D-5 resolved option-c ("no floor -- report only"). See `140-09-SUMMARY.md`.
- 140-03-PLAN.md COMPLETE (3/3 tasks). Task 1 (`d76a485`): `classify_unparseable_dates_vec()`/`classify_unparseable_dates()`. Task 2 (`fef75e8`): SECTION 3 wiring + `unparseable_date_examples` + `n_patients_lost_to_filters_c02` (P-02c). **Task 3 (Wave 2.5 blocking checkpoint, 140-08-PATCH FIX-14) RESOLVED 2026-08-08** via a real HiPerGator run: `c02a_monotone` PASS (26 -> 9), `c02b_partition` PASS, `c02_reconciled` PASS (first PASS this phase); coverage 92.9% (reported only); unparseable-date breakdown 100% `blank_or_null` (P-02b resolved: no parser gap, `parse_pcornet_date()` extension NOT needed); P-02c confirmed exactly 280; `testthat::test_dir()` 35/35 pass (first real execution of every 140-01/140-09/140-03 fixture). All figures matched 140-CONTEXT.md's assumptions, zero material divergence. Notable finding (non-blocking): pre-coalesce baseline == 26, matching the retired historical C02_EXPECTED -- flagged to Erin/Amy as a provenance clue. See `140-03-SUMMARY.md`.
- 140-04-PLAN.md (Wave 3) **COMPLETE (3/3 tasks) AND COMMITTED.** Task 1 (`45aef76`): `assign_scenarios()` (SECTION 1B, dual-mode, S1 folded into S3 universally per P-06b) + `tests/testthat/test-115-scenarios.R` (new file, 140-08-PATCH FIX-16). Task 2 (`8bf3a87`): independently-computed backward-only waterfall joined as parallel columns on `waterfall_encounter`/`waterfall_patient`, coverage-neutrality assertion, KEY/B_scenario_counts/C_completeness text updated for the INVERTED ordered-vs-unordered S3 discrepancy (140-08-PATCH FIX-04). Task 3 (`8e980e8`, D-4 `checkpoint:decision`): **RESOLVED 2026-08-08 as option-a** -- backward-only stays PRIMARY, forward-inclusive stays sensitivity-only; new KEY sheet field added recording the decision; P-06d explicitly not triggered/not built. See `140-04-SUMMARY.md`.
- 140-05-PLAN.md (Wave 4, depends on 140-04) **COMPLETE (3/3 tasks) AND COMMITTED.** Task 1 (`98cca02`): `compute_gap_days_at_assignment()` added to SECTION 1B -- `gap_days_at_assignment` (forward-inclusive, signed) and `gap_days_at_assignment_backward_only` (never-negative, stopifnot-enforced), each independently computed via its own pair of nearest-match reductions with FIX-08 backward-wins-ties tiebreaking; `classify_encounter_zip()` retains `direct_zip_period_start`; `tests/testthat/test-115-scenarios.R` extended with a 6-case fixture (a)-(f). Task 2 (`c765eb9`): `median_gap_within_bin` added to `validation_curve` (P-04b), console NOTE, new KEY sheet field. Task 3 (`51e2064`, D-3 `checkpoint:decision`): **RESOLVED 2026-08-10 as option-a** -- uncapped carry-forward accepted, gap covariates serve as analytic/sensitivity variables; no code-logic changes needed (Task 1 already implemented this design); new KEY sheet field added recording the resolution and the non-selection of option-b. See `140-05-SUMMARY.md`.
- 140-06-PLAN.md (Wave 5, depends on 140-05) **COMPLETE (2/2 tasks) AND COMMITTED.** Task 1 (`176cf84`): `build_encounter_anchored_validation_cases()` added to SECTION 1B (P-05a) -- predicts from the `zip9_seq` spell immediately prior to a `has_direct_zip9` encounter's covering record, `gap_days = ADMIT_DATE - predicting_period_start_dt` (140-08-PATCH FIX-05, encounter-to-record staleness, not A-06's record-to-record gap); two new `tests/testthat/test-115-validation-curve.R` fixtures (gap-value distinction + no-prior-spell exclusion). Task 2 (`87e980a`): wired into SECTION 11 producing `validation_curve_encounter_anchored`; new `A_validation_curve_encounter_anchored` sheet (9th workbook sheet) states its own 74.4%-subpopulation and ZIP9-derived-not-`zip5_coalesced` limitations (P-05b, 140-08-PATCH FIX-06); `A_validation_curve`'s subtitle and the KEY sheet's "A-06 sampling frame" field now explicitly say LOWER BOUND; new KEY field "A-06 vs P-05a" added. One auto-fixed Rule-1 deviation (stale "8-sheet" text corrected to "9-sheet"). No checkpoint in this plan (`autonomous: true`); none encountered. See `140-06-SUMMARY.md`.
- 140-07-PLAN.md (Wave 6, final plan, depends on 140-06) **COMPLETE (2/2 tasks) AND COMMITTED.** Task 1 (`9460498`): universe cross-check (`n_cohort_in_addr`/`n_cohort_not_in_addr`) relocated to SECTION 12 immediately after `compute_c02()`, `n_distinct()` on both sides, `stopifnot` against `c02_result$n_cohort_absent_from_addr` (140-08-PATCH FIX-07); KEY sheet gains "Part A vs Part B/C universe overlap (P-07a)" and "Sentinel ZIP nulling -- status (P-07b, reviewed-and-closed)" fields; universe note appended to all 4 Part A sheet subtitles. Task 2 (`814358f`): `get_zip9_at_date(ids, dates, addr_full = NULL)` additive test-injection seam (P-07c, 140-08-PATCH FIX-10), default path unchanged, character/Date column contract documented, `normalize_*()`/`is_sentinel_zip5()` confirmed untouched; new `tests/testthat/test-utils-address.R` (4th Phase-140 test file, 140-08-PATCH FIX-16). One auto-fixed Rule-1 deviation: `A_validation_curve_encounter_anchored`'s universe note corrected to state its actual cohort-restricted-encounter population, not the plan's literal addr_coal wording (would have contradicted that sheet's own 140-06 subtitle). No checkpoint in this plan (`autonomous: true`); none encountered. See `140-07-SUMMARY.md`.

**What's next:** Phase 140 is now 8/8 plans executed -- all 24 of 140-CONTEXT.md's task/decision IDs addressed. Phase-level goal verification (a real HiPerGator run of the full amended R/115_zip_stability_counts.R, confirming c02_reconciled plus reviewing the backward-only/forward-inclusive waterfall split, the encounter-anchored validation curve, the gap_days_at_assignment covariate, and this plan's new universe-overlap figures against real data) is the outstanding item before the workbook ships to Erin/Amy -- explicitly out of scope for this 140-07 execution (orchestrator's responsibility). Phase 139 remains complete (4/4 plans), ready for goal verification, real HiPerGator runtime verification still deferred. v3.3 remains open in parallel — see "v3.3 Status" and "v3.3 Active TODOs" above. v3.4 Phase 132 (Crash Fixes) planning is still pending separately. Note: several 140-09-PATCH-related doc files (140-01-SUMMARY.md, 140-02-PLAN.md, 140-02-SUMMARY.md, 140-03-PLAN.md, 140-CONTEXT.md, data/reference/README.md, plus 140-09-PLAN.md, 140-09-SUMMARY.md) remain uncommitted in the working tree from a prior session -- still out of scope for this 140-07 execution to commit.
