# PCORnet Payer Variable Investigation -- Pipeline Reference Manual

> **Auto-generated** by `R/89_generate_reference_manual.R`

---

## Note: Regeneration Required

This file is a placeholder. To generate the complete reference manual with dependency matrix, run-order guide, and onboarding documentation:

```bash
# On HiPerGator (or any system with R installed)
Rscript R/89_generate_reference_manual.R
```

The generator script will:
1. Parse 5-field headers from all 69 numbered R scripts
2. Parse headers from all 10 utils modules
3. Detect config constant usage per script
4. Build dependency matrix tables grouped by decade
5. Document key functions in each utils module
6. Write onboarding guide with HiPerGator and Windows setup instructions
7. Create run-order guide showing canonical execution sequence
8. Document all 11 config constants (CONFIG, ICD_CODES, PAYER_MAPPING, CANCER_SITE_MAP, TIER_MAPPING, etc.)

**Expected output:** ~400+ lines of markdown with 6 major sections.

---

*Placeholder created by GSD Phase 74 Plan 02. Regenerate on HiPerGator to populate full content.*
